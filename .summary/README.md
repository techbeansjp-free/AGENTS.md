# `.summary/` — GENERATED / NOT SOURCE OF TRUTH

このディレクトリに置かれる **`*.txt`・`output_*.py` 等は、ツール出力・作業用メモ・過去スナップショット**です。

- **設計・ディレクトリ・契約の正本**は **`docs/`**、ルート `README.md`、`AGENTS.md` を参照すること。
- 検索・レビュー・AI コンテキストでは **本ディレクトリを正本とみなさない**。

ルート **`.gitignore`** で本ディレクトリの生成物は原則除外する（**本 README のみ追跡する場合は `!.summary/README.md` で例外**）。
