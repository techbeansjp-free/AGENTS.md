# DESIGN: ローカル実行用 `.agent-skill-chain/` 配下シェルスクリプトがCLI未検出時に自動導入フォールバックを持たない

- Issue: `ISSUE-677`
- 対応する SPEC: `SPEC.md`

## 設計の要約

対象スクリプト集合54本が各自コピーしている3経路解決ブロックを削除し、単一の共有実装 `.agent-skill-chain/scripts/cli-resolve.sh`（bash ライブラリ、`source` して使う）へ集約する。共有実装は「3経路解決（第3経路は実体検査と `--help` による起動可能性検証を常時伴う）→ 失敗時の自動導入フォールバック → 導入後の再解決」までを一手に担い、解決結果を配列変数 `ASC_CLI` として呼び出し元へ返す。

54本のラッパーは呼び出し形態により**2つの契約**に分かれる（設計判断 D3）。直接実行される52本を**契約W**、他スクリプトから `source` されて使われるアダプタ2本（`.agent-skill-chain/adapters/claude.sh`・`同 human.sh`）を**契約A**とする。両契約は、自身の位置から共有実装を読み込む前文（マーカーコメントで囲まれた固定テキスト）を共有し、前文のテキストは終了形を表す語（契約Wは `exit`、契約Aは `return`）を除いて54本で文字単位に同一である。前文の責務は共有実装の読み込みまでに限り、解決関数 `asc_resolve_cli` の呼び出しは前文に含めない——契約Wでは前文の直後（トップレベル、起動時）に呼び、契約Aでは `_asc_cli` 関数の本体で呼ぶ。これにより、アダプタが `source` された時点で自動導入（グローバル領域への `npm install -g`）が発火する経路も、同一プロセスで解決手続きが二重に走る経路も持たない。

委譲の記述形は3種あり、いずれも解決結果 `ASC_CLI` を使う点で共通する（設計判断 D15）。契約Wのうち49本は末尾の `exec "${ASC_CLI[@]}" <サブコマンド> "$@"` 1行で委譲する薄いラッパーであり、残る3本（`worker-launch.sh`・`worker-launch-verify.sh`・`gate-launch-reviewer.sh`）は既存の `_cli` 関数を保持したままCLIを複数回呼ぶスクリプトで、関数本体が `"${ASC_CLI[@]}" "$@"` を実行する形になる。契約Aの2本は既存の `_asc_cli` 関数の本体で解決と委譲を行う。

CIワークフローテンプレートの「Ensure agent-skill-chain CLI」ステップも同じ共有実装を読み込む形へ統一し、リポジトリ内で3経路解決の記述箇所を共有実装1つに限定する。

## 要件 → 設計要素の対応表

設計要素の識別子は本ファイル内で次のとおり定義する（詳細は「責務・境界」）。

- `E1` 共有実装ファイル `.agent-skill-chain/scripts/cli-resolve.sh`
- `E1a` 公開関数 `asc_resolve_cli`（共有実装の唯一の入口）
- `E1b` 内部関数 `_asc_try_resolve`（3経路解決＋採用候補の健全性検査）
- `E1c` 内部関数 `_asc_auto_install`（オプトアウト判定→対話確認→`npm install -g`→`E1b` の再実行による再解決）
- `E2` ラッパー定型前文（対象スクリプト集合54本に埋め込む共通テキスト。共有実装の読み込みまでを担い、解決呼び出しは含まない）
- `E2w` 契約W の解決・委譲部（直接実行される52本の末尾2行）
- `E2a` 契約A の `_asc_cli` 関数本体（`source` されるアダプタ2本）
- `E3` CIワークフローテンプレートの「Ensure agent-skill-chain CLI」ステップ（共有実装への統一）
- `E4` 重複検出テスト（`.agent-skill-chain/` 配下 `*.sh` の走査）
- `E5` 全数実行テストハーネス（54本を1本ずつ実行する共通フィクスチャ）
- `E6` 経路順序テスト／`E7` オプトアウトテスト／`E8` 並行実行テスト
- `E9` 利用者向け記述（共有実装ヘッダコメントと README の自動導入・オプトアウト説明）

| 要件 / AC-ID | 対応する設計要素 | 備考 |
|---|---|---|
| 要件（単一の共有実装からCLI解決ロジックを得る） | `E1`, `E1a`, `E2`, `E2w`, `E2a` | 各ラッパーは `E2` で `E1` を読み込み、自ファイル内に判定ロジックを持たない。解決の呼び出しは契約Wでは `E2w`、契約Aでは `E2a` が行う |
| 要件（既存の3経路解決順序を維持する） | `E1b` | 判定順を `bin/agents-md.js` → `node_modules/.bin/agent-skill-chain` → PATH で固定 |
| 要件（共有実装は配布物に含まれ、consumer 展開後も自身の位置基準で解決できる） | `E1`, `E2` | 配置先を `.agent-skill-chain/scripts/` とすることで既存の配布経路にそのまま乗る（設計判断 D1） |
| 要件（共有実装の解決失敗時は日本語エラー＋探索パス＋非ゼロ終了、インライン再実装禁止） | `E2` | 定型前文が読み込み前の可読性検査・`source` 失敗検査・関数定義有無検査の3段で検出する。終了形は契約Wが `exit`、契約Aが `return`（いずれも非ゼロ。設計判断 D3・D13） |
| 要件（3経路失敗時にCI同等の自動導入を試行、非対話では無確認） | `E1c` | `npm install -g agent-skill-chain@latest` |
| 要件（対話環境の確認と拒否時の挙動、「確認も導入もせず即失敗」の禁止） | `E1c` | 対話判定は `[[ -t 0 ]]` かつ `[[ -t 2 ]]`。拒否時は理由付き日本語エラーで非ゼロ終了 |
| 要件（`AGENT_SKILL_CHAIN_AUTO_INSTALL` によるオプトアウト、既定は試行） | `E1c`, `E9` | 値 `0` のみオプトアウト（設計判断 D6） |
| 要件（並行自動導入時の2挙動への収束・部分配置の不採用） | `E1b`, `E1c` | 第3経路の採用条件に「ファイル単位検査」と「`--help` による起動可能性検証」を常時含め、不採用時は未解決として `E1c` の終了形（日本語エラー＋非ゼロ）へ収束させる（設計判断 D5） |
| 要件（導入成功後の再解決、再解決不能時は理由付き非ゼロ終了） | `E1c` | `npm prefix -g` の bin ディレクトリを含め、`E1b` を再実行して再解決する（起動可能性検証は `E1b` に内包される） |
| 要件（自動導入失敗時は原因を含む日本語エラーで非ゼロ終了） | `E1c` | `npm` 不在・コマンド失敗の双方を同一分岐で扱う |
| 要件（既存の呼び出しインターフェースの後方互換） | `E2`, `E2w`, `E2a` | 契約Wは `exec "${ASC_CLI[@]}" <サブコマンド> "$@"` の形と引数透過が不変。契約Aは `_asc_cli` の関数名・引数透過・戻り値の伝播（非ゼロを `return`）が不変 |
| 要件（CIワークフロー側との重複を解消、しない場合は理由をコメント明記） | `E3` | 統一する（設計判断 D7） |
| 要件（`package.json` への devDependency 自動追加は対象外） | — | 本設計では扱わない（SPEC のスコープ外に一致） |
| `AC-1`（重複排除） | `E1`, `E2`, `E4` | 検出条件と合格条件を機械判定可能な形で確定（設計判断 D8）。前文テキストの同一性表明（検査D）も同じテスト群が担う |
| `AC-2`（3経路失敗時に自動導入が試行される） | `E1c`, `E5` | `npm` スタブによる隔離環境で検証 |
| `AC-3`（自動導入が完遂しない場合の日本語エラー） | `E1c` | (a) コマンド失敗 (b) 対話拒否 の2経路を同一の終了形へ収束 |
| `AC-4`（正常系呼び出しインターフェースの後方互換） | `E2`, `E2w`, `E2a`, `E5` | 対象集合を設計判断 D10 の識別基準で列挙し、54本全数をスタブCLIで実行して引数列とexit codeを比較。契約A の2本は driver 経由で `_asc_cli` を呼ぶ手順で評価する（設計判断 D13） |
| `AC-5`（CIワークフロー側との重複の扱い） | `E3` | 統一により重複を解消 |
| `AC-6`（consumer 展開状態での起動） | `E1`, `E2`, `E2w`, `E2a`, `E5` | 展開物の複製に対し、設計判断 D10 の識別基準で列挙した54本全数を実行（契約A の2本は D13 の driver 経由） |
| `AC-7`（導入成功後の再解決） | `E1c` | 再解決は `E1b` の再実行として行い、再解決不能時は理由付き非ゼロ終了 |
| `AC-8`（共有実装を解決できない場合の挙動） | `E2`, `E5` | 欠落・部分展開・権限不足・構文エラーの4状態を、設計判断 D10 の識別基準で列挙した54本全数に対し検証（契約A の2本は D13 の driver 経由。driver の exit code が非ゼロであることをもって「非ゼロ終了」と判定する） |
| `AC-9`（3経路の判定順序） | `E1b`, `E6` | 3経路それぞれに識別可能なスタブを置いて順序を実測 |
| `AC-10`（オプトアウト） | `E1c`, `E7` | 未設定時に自動導入が試行されることも併せて確認 |
| `AC-11`（並行実行時の収束） | `E1b`, `E1c`, `E8` | 部分配置状態の不採用は `E1b` の採用条件（ファイル単位検査＋起動可能性検証、常時適用）が単独で担保し、不採用となったプロセスの終了形（並行導入の後着失敗・オプトアウト時の停止を含む日本語エラー＋非ゼロ終了）は `E1c` が担う（設計判断 D5） |

## 責務・境界

### コンポーネント構成

- `E1` `.agent-skill-chain/scripts/cli-resolve.sh`: CLI実体の解決と自動導入フォールバックに関する判断を一手に引き受ける唯一の場所。自身の位置（`BASH_SOURCE[0]`）から2階層上をリポジトリルートとして算出し、呼び出し元のカレントディレクトリに依存しない。副作用は「`npm install -g` の実行」「`ASC_CLI` 配列変数への代入」「標準エラー出力へのメッセージ出力」に限る。標準出力へは何も書かない（ラッパーの正常系標準出力を汚さないため。自動導入コマンド自身の標準出力も標準エラー出力へ迂回させる）。委譲先CLIサブコマンドの決定・実行は行わない（それはラッパーの責務）。
  - `E1a` `asc_resolve_cli`: 共有実装が外部へ公開する唯一の関数。`_asc_try_resolve` を試み、成功なら 0 を返す。失敗した場合のみ `_asc_auto_install` へ進み、その結果をそのまま返す。戻り値 0 のとき、かつそのときに限り `ASC_CLI` が有効な argv 前置配列として設定されている。
  - `E1b` `_asc_try_resolve`: 3経路を固定順で判定し、最初に採用条件を満たした経路の argv を `ASC_CLI` へ設定する。第3経路（PATH上の実体）の採用条件は、経路の成立（`command -v` で発見できること）に加えて「実体が存在し、実行可能で、サイズが0でない」というファイル単位検査と、「`--help` を1回起動して exit code 0 で終了する」という起動可能性検証の双方を満たすことであり、この2検査は自動導入分岐の有無にかかわらず常時適用する（設計判断 D5）。第3経路が成立しても採用条件を満たさない場合は、当該経路を採用せず「いずれの経路でも未解決」として扱う。いずれの経路も採用に至らなければ非ゼロを返す（メッセージは出さない。文言の決定は `E1a`／`E1c` 側の責務）。
  - `E1c` `_asc_auto_install`: オプトアウト判定 → 対話確認 → `npm` 可用性判定 → `npm install -g agent-skill-chain@latest` 実行 → 再解決、の順に進む単方向の手続き。再解決は `_asc_try_resolve` の再実行として行い、導入直後の起動可能性検証は当該関数の第3経路採用条件に内包される（本関数は独立の検証段を持たない。同一の検証を2箇所に書くと、本Issueが解消しようとしている重複を共有実装の内部に再導入することになるため）。どの段で止まっても、必ず「原因を含む日本語メッセージを標準エラー出力へ出し、非ゼロを返す」形で終わる。無言終了と 0 での終了は構造上起こらない。
- `E2` ラッパー定型前文（対象スクリプト集合54本の冒頭部、マーカーコメントで囲まれた固定テキスト）: 自身の位置（`BASH_SOURCE[0]`、設計判断 D12）から共有実装のパスを算出し、読み込み可能性を検査し、`source` し、公開関数の定義有無を検査する。**ここまでが前文の責務であり、`asc_resolve_cli` の呼び出しは前文に含めない**（設計判断 D3）。前文で生じうる失敗はすべて「共有実装を解決できない」ことに帰着するため、この前文だけが当該失敗のメッセージを持つ。前文は3経路解決に関する判定を一切含まない（インライン再実装の禁止）。前文の実行は共有実装ファイルの読み込み（bash 関数定義）のみを副作用とし、`npm install -g` を含むいかなる外部への書き込みも起こさない。前文のテキストは54本すべてで、終了形を表す語（契約Wは `exit`、契約Aは `return`）を除いて文字単位に同一である（設計判断 D3・D13）。
  - `E2w` 契約W（直接実行される52本）の解決・委譲部: 前文の直後（トップレベル）で `asc_resolve_cli` を呼び、非ゼロならその値で `exit` する。解決を起動時に済ませることで、引数・分岐によらず必ず解決（と必要なら自動導入）を通る。委譲の記述形は2種で、49本は末尾の `exec "${ASC_CLI[@]}" <サブコマンド> "$@"` 1行（ラッパーごとに異なるのはサブコマンド名のみ）、3本（`worker-launch.sh`・`worker-launch-verify.sh`・`gate-launch-reviewer.sh`）は既存の `_cli` 関数を保持し、その本体を `"${ASC_CLI[@]}" "$@"` に置き換える（当該3本は同一プロセス内でCLIを複数回呼ぶため。詳細は設計判断 D15）。
  - `E2a` 契約A（他スクリプトから `source` されるアダプタ2本 `.agent-skill-chain/adapters/claude.sh`・`同 human.sh`）の `_asc_cli` 関数本体: 既存の関数名・引数透過・戻り値伝播の境界を保ったまま、本体を「`asc_resolve_cli` を呼び、非ゼロならその値を `return`、成功なら `"${ASC_CLI[@]}" "$@"` を実行する」形へ置き換える。`exit` は用いない（呼び出し元シェルを巻き添えに終了させないため）。解決（および自動導入）は `_asc_cli` が呼ばれた時点で初めて行われ、アダプタが `source` されただけでは行われない。
- `E3` CIワークフローテンプレートの「Ensure agent-skill-chain CLI」ステップ: 3経路のインライン判定を廃し、共有実装を `source` して `asc_resolve_cli` を呼ぶ形へ置き換える。ステップの目的（後続ステップが依存するCLIを事前に用意する）と観測可能な結果（CLIが3経路のいずれかで解決可能な状態でステップが成功する／解決も導入もできなければ非ゼロで失敗する）は変わらない。配布元テンプレートと本リポジトリの `.github/` の双方を同一内容で更新する。もう一方のワークフローテンプレート（root-cleanup 用）にはCLI解決・自動導入の記述が存在しないため、変更対象に含めない。
- `E4`〜`E8` テスト群: 受入条件の機械的検証。実装セグメントの成果物として `test/` 配下に追加する。
- `E9` 利用者向け記述: 共有実装のヘッダコメント（自動導入の既定挙動・オプトアウト・対話確認の有無）と README の導入手順への追記。新しい観測可能な副作用（グローバル領域への `npm install -g`）を利用者が事前に知り、無効化できる状態にする。

### 共有実装の公開契約

共有実装は次の契約のみを外部へ公開する。契約外の内部関数・内部変数は下線始まりの名前とし、呼び出し元から使わない。

- 入力: 環境変数 `AGENT_SKILL_CHAIN_AUTO_INSTALL`（値 `0` のときのみ自動導入を行わない）、標準入力・標準エラー出力が端末に接続されているか、`npm` の可用性、3経路それぞれの実体の有無、第3経路の実体の起動可能性（`--help` の exit code）。
- 出力: 戻り値 0（解決成功。`ASC_CLI` 配列が有効）／非ゼロ（解決失敗。標準エラー出力へ原因を含む日本語メッセージ済み）。
- 変数 `ASC_CLI`: CLI実体を起動する argv の前置部分。第1経路では2要素（`node` と `bin/agents-md.js` の絶対パス）、第2・第3経路では1要素。呼び出し元は `exec "${ASC_CLI[@]}" <サブコマンド> "$@"` の形でのみ使う。
- 冪等性: 同一シェル内で複数回呼んでよい。2回目以降も同じ判定を行う（結果のキャッシュは持たない。キャッシュを持つと同一プロセス内で環境が変わった場合に古い結果を返すため）。

### ラッパー定型前文の契約

前文が検査するのは次の3点であり、いずれか1つでも成立しなければ「共有実装を解決できない」として、探索したパスを含む日本語メッセージを標準エラー出力へ出し、非ゼロで停止する。

1. 算出した共有実装パスが読み取り可能であること（欠落・部分展開の一部・読み取り権限不足を検出する）。
2. `source` が成功すること（構文エラーによる読み込み失敗を検出する。`set -e` に依存せず、`source` の戻り値を明示的に分岐する）。
3. 読み込み後に公開関数 `asc_resolve_cli` が定義されていること（ファイルが途中で切り詰められている等、`source` が成功しても内容が不完全な場合を検出する）。

「非ゼロで停止する」の実現手段は契約ごとに一意に定まる。契約W（直接実行される52本）は `exit` で当該プロセスを非ゼロ終了させる。契約A（`source` されるアダプタ2本）は `return` で当該ファイルの読み込みを非ゼロで打ち切る。契約Aで前文が非ゼロ `return` した場合、以降のファイル内容（`_asc_cli` を含む関数定義）は実行されないため関数は未定義のまま残り、呼び出し元から見た `source` の戻り値が非ゼロになる。**契約Aの呼び出し元は、`source` の戻り値を明示的に検査して停止する責務を負う**。`set -e` の有無に依存してはならない——本リポジトリ内で唯一アダプタを `source` する `.agent-skill-chain/scripts/worker-launch-verify.sh` は `set -uo pipefail`（`-e` を含まない）で動作しており、戻り値を検査しなければ、共有実装が解決できない状態でも処理が継続して後続の `_asc_cli` 呼び出しが「コマンドが見つからない」というシェル既定のエラーで終わる（日本語メッセージも探索パスも伴わない）。したがって本Issueの変更には、当該呼び出し元へ `source` の戻り値検査を追加する作業を含める。契約Aで `exit` を用いないのは、呼び出し元シェルを巻き添えにして終了させると、アダプタを読み込んだだけの呼び出し元が自らのエラー処理を行えなくなるためである。この2種の終了形は互いに排他であり、同一ファイルが両方を持つことはない。

### 依存関係

```text
ラッパー（対象スクリプト集合54本）
  → 共有実装 cli-resolve.sh
      → 3経路の実体（bin/agents-md.js ／ node_modules/.bin/agent-skill-chain ／ PATH上の agent-skill-chain）
      → npm（自動導入フォールバック時のみ）
  → 解決されたCLI実体（exec による委譲）

CIワークフローの Ensure ステップ
  → 共有実装 cli-resolve.sh（ラッパーと同一の依存先）
```

共有実装はラッパーを一切参照しない（逆依存が無く、循環依存は生じない）。共有実装が新たに獲得する外部依存は `npm` のみで、これは自動導入フォールバック分岐でのみ使用し、不在時は想定内の失敗として扱う。

### 状態遷移

```mermaid
stateDiagram-v2
    [*] --> 共有実装の解決
    共有実装の解決 --> 起動失敗_共有実装不明: 読取不可 / source失敗 / 関数未定義
    共有実装の解決 --> 三経路解決: 読み込み成功
    三経路解決 --> 委譲実行: いずれかの経路が採用条件を満たす
    三経路解決 --> オプトアウト判定: いずれの経路でも未解決（第3経路が成立しても実体検査・起動可能性検証に不合格の場合を含む）
    オプトアウト判定 --> 停止_オプトアウト: AGENT_SKILL_CHAIN_AUTO_INSTALL が 0
    オプトアウト判定 --> 対話確認: それ以外（未設定を含む）
    対話確認 --> 停止_利用者拒否: 対話環境で拒否
    対話確認 --> 自動導入実行: 非対話 / 対話環境で承諾
    自動導入実行 --> 停止_導入失敗: npm不在 / コマンド失敗
    自動導入実行 --> 導入後の再解決: コマンド成功
    導入後の再解決 --> 停止_再解決不能: 実体を発見できない / 実体検査・起動可能性検証に不合格
    導入後の再解決 --> 委譲実行: 採用条件を満たす実体を発見
    委譲実行 --> [*]: 委譲先CLIのexit code
    起動失敗_共有実装不明 --> [*]: 日本語エラー + 非ゼロ
    停止_オプトアウト --> [*]: 日本語エラー + 非ゼロ
    停止_利用者拒否 --> [*]: 日本語エラー + 非ゼロ
    停止_導入失敗 --> [*]: 日本語エラー + 非ゼロ
    停止_再解決不能 --> [*]: 日本語エラー + 非ゼロ
```

終端は「委譲実行」と5種の「停止（日本語エラー＋非ゼロ）」のみであり、無言終了・メッセージ無しの exit code 0 での終了に到達する経路は存在しない。

本図は契約W（直接実行される52本）の1プロセス分の遷移として読む。契約A（`source` されるアダプタ2本）でも遷移の順序・分岐条件・終端の種類は同一だが、発生時点と停止の実現手段が次のように異なる——「共有実装の解決」はアダプタが `source` された時点で起こり、「三経路解決」以降（オプトアウト判定・対話確認・自動導入・再解決・委譲）は `_asc_cli` が呼ばれた時点で初めて起こる。停止はプロセスの `exit` ではなく非ゼロの `return`（前文では `source` の打ち切り、`_asc_cli` では関数戻り値）として現れ、呼び出し元がその戻り値で停止する。委譲は `exec` ではなく通常の実行であり、`_asc_cli` は委譲先CLIの exit code をそのまま返す（既存の関数境界を保つため）。

### 図示要否の判断

- 判断: `要`
- 根拠: 依存関係が3つ以上ある（ラッパー→共有実装、共有実装→3経路の実体、共有実装→`npm`、CIワークフロー→共有実装）。状態遷移も2つ以上ある（共有実装の解決 → 3経路解決 → オプトアウト判定 → 対話確認 → 自動導入 → 導入後の再解決 → 委譲実行、および5種の停止状態）。責務境界も3つ以上ある（ラッパー定型前文、共有実装の公開関数、共有実装の内部2関数、CIワークフローステップ）。上記いずれの基準にも該当するため、状態遷移図を必須とし記載した。

## 設計判断（SPEC 未決事項の確定）

### D1: 共有実装の配置先を `.agent-skill-chain/scripts/cli-resolve.sh` とする

`.agent-skill-chain/` 直下に新しいディレクトリ（`lib/` 等）を設ける案と比較して採用する。理由は配布経路の変更を一切要さないこと。`scripts/` は既に「配布・展開対象の名前空間」および「npm パッケージ同梱対象」の双方に登録済みで、ディレクトリ単位で複製される。新規ディレクトリを設けると、パッケージ同梱設定・名前空間一覧・導入時の所有権記録・アンインストール対象・アップグレード時の削除候補判定という複数の機構を同時に更新することになり、いずれか1つの漏れが「consumer 展開物に共有実装だけが含まれず54本すべてが起動不能」という本Issue最大の退行を直接引き起こす。`scripts/` 配下には既に、CLIサブコマンドへのラッパーではない補助スクリプトが複数存在するため、ライブラリを置くことは既存の配置慣行から逸脱しない。AC-1 の走査対象（`.agent-skill-chain/` 配下の `*.sh`）に含まれるため、走査結果が0件になる懸念も生じない。

### D2: 読み込み方式は `source`（bash 関数ライブラリ）とし、実行可能スクリプトへの委譲は採らない

解決結果は「`node <path>` のような複数要素の argv 前置」であり、これを子プロセスの標準出力で文字列として返すと、引用・空白を含むパスの復元が呼び出し元の再解析に依存して壊れうる。`source` なら bash 配列としてそのまま受け渡せる。加えて、ラッパー1回の起動につきプロセス生成が1つ増えることを避けられる。アダプタ2本が持つ既存の関数境界（`_asc_cli`）も、関数ライブラリであればそのまま維持できる。

### D3: ラッパーを契約W（直接実行52本）と契約A（`source` される2本）の2契約に明示的に分け、前文は「終了形の語を除いて54本で文字単位に同一」とする

**本設計における唯一の結論**: 対象54本は単一の契約では扱わない。呼び出し形態が異なる以上、失敗時の停止手段（`exit` と `return`）は両立しないためである。両者を1つのテキストへ押し込む案（`return … || exit …` のような、直接実行時と `source` 時で自動的に振る舞いが変わる記述）は採らない——当該記述の挙動が bash の版・実行形態に依存し、依存が破れた場合に「エラーを出したまま処理が継続する」という最悪の失敗形（AC-8 が禁じる委譲到達）へ倒れるためである。代わりに次の2契約を定義し、可変部分を最小化する。

| 契約 | 対象 | 呼び出し形態 | 前文の終了形 | 解決呼び出しの位置 | 委譲の形 |
|---|---|---|---|---|---|
| 契約W | `scripts/` 直下40本・`ci/` 配下12本の計52本 | 直接実行 | `exit` 非ゼロ | 前文の直後（トップレベル、`E2w`） | 末尾の `exec "${ASC_CLI[@]}" <サブコマンド> "$@"`（49本）／`_cli` 関数本体の `"${ASC_CLI[@]}" "$@"`（3本。設計判断 D15） |
| 契約A | `adapters/claude.sh`・`同 human.sh` の2本 | 他スクリプトから `source` | `return` 非ゼロ | `_asc_cli` 関数本体（`E2a`） | `"${ASC_CLI[@]}" "$@"`（`exec` を用いない） |

前文（`E2`）のテキストは、この終了形を表す語（`exit`／`return`）と、それに続く終了値の受け渡しを除いて、54本で文字単位に同一である。対象54本はすべて `.agent-skill-chain/<ディレクトリ>/<名前>.sh` という同一の階層深さにあるため、自身の位置から `.agent-skill-chain/` を求める式（1階層上）と、そこから共有実装を求める式（`scripts/cli-resolve.sh`）はすべてのラッパーで同一になる。したがって「`scripts/` 用に算出した相対パスを他ディレクトリへ機械的に複製すると解決に失敗する」という失敗モードは、前文の共通部分を文字単位で同一にすることで構造的に排除できる。前文で `.agent-skill-chain/` の位置を経由するのは、リポジトリルートを各ラッパーが独自に算出しないため（ルート算出は共有実装の責務に一本化する）。単一テキストが直接実行される52本と `source` される2本の双方で成立するためには、自己位置の算出手段が `BASH_SOURCE[0]` であることが必須である（設計判断 D12）。

**解決呼び出しを前文から外す理由**: 前文を `asc_resolve_cli` の呼び出しまで含む形にすると、契約Aのアダプタは `source` された時点で解決・自動導入分岐へ到達し、`_asc_cli` を一度も呼ばない呼び出し元に対しても `npm install -g agent-skill-chain@latest` という不可逆なグローバル副作用を発生させうる。さらに `_asc_cli` 本体でも同じ関数を呼ぶため、解決手続き（第3経路採用時は `--help` 起動を伴う）が二重に走る。前文の責務を「共有実装の読み込みまで」に限定すれば、副作用の発火点は契約Wでは委譲直前、契約Aでは `_asc_cli` 呼び出し時の各1回だけになり、利用者の同意を求める設計意図（対話確認・オプトアウト）と発火点が一致する。契約Wにおいても、この変更で外部から観測できる挙動は変わらない（前文の直後に解決呼び出しが続くため、実行順序は同一である）。

**前文の同一性を機械的に表明する**: 前文はマーカーコメント（開始・終了の固定行）で囲み、テストが各ファイルから前文ブロックを抽出できるようにする。抽出した54個のブロックを、行頭空白の除去と `return`→`exit` の正規化を施したうえで比較し、全件が一致することを表明する（設計判断 D8 の検査D）。併せて、正規化前のブロックが `exit` 形であるファイルがちょうど52件・`return` 形がちょうど2件であり、後者が `adapters/claude.sh`・`同 human.sh` に一致することも表明する。これにより「前文が同一である」という本設計の不変条件は、AC-4・AC-6・AC-8 の全数実行とは独立に、テキストとして直接検証される。

### D4: 共有実装の解決失敗の検知は「読み取り可能性・`source` の成否・公開関数の定義有無」の3段で行う

存在判定のみでは、部分展開・切り詰めによって `source` は成功するのに関数が定義されない状態を見逃す。逆に読み込み結果の完全な検証（チェックサム照合等）は、配布物に digest を同梱する新機構を要し、本Issueの範囲を超える。3段検査は追加の機構を要さず、SPEC が列挙する4状態（欠落・部分展開・読み取り権限不足・構文エラー）をすべて被覆する。メッセージには算出した共有実装の絶対パスを「探索パス」として必ず含める。

### D5: 第3経路の採用条件を「ファイル単位検査」＋「`--help` による起動可能性検証」とし、双方を常時適用する。相互排他は導入しない

**本設計における唯一の結論**: 第3経路（PATH上の `agent-skill-chain`）は、経路が成立しただけでは採用しない。採用条件は次の2検査の双方を満たすことであり、自動導入を行ったか否か・その呼び出しが何回目かにかかわらず、`_asc_try_resolve` を通るすべての解決で常時適用する。

1. ファイル単位検査: `command -v` で得たパスを辿った先の実体が存在し、実行可能で、サイズが0でないこと（追加のプロセス生成を伴わない）。
2. 起動可能性検証: 当該実体を `--help` で1回起動し、exit code 0 で終了すること（標準出力・標準エラー出力は破棄し、exit code のみを判定に使う）。この判定は「健全なCLI実体は `--help` に対し exit code 0 を返す」という前提に依存する。当該前提は設計セグメントで実CLIの入口実装を確認して成立を確かめ、破綻を検出する回帰表明を設ける（設計判断 D14）。

採用条件を満たさない第3経路は「成立したが採用しない」ではなく「いずれの経路でも未解決」として扱い、以降のオプトアウト判定・自動導入分岐へ進む。したがって、採用判定時点で部分配置状態にあるCLI実体が委譲先として採用される経路は存在しない。この帰結について、本設計内の他の記述および ADR の記述は、いかなる例外・未検出の残余も主張しない（唯一の例外は、後述する「採用判定後・委譲前の時間差」であり、これは採用時点の状態に関する上記の結論を弱めない）。

**2検査を常時適用する理由**: グローバル導入の途中結果には、(a) bin エントリのリンクが未作成またはリンク先が未配置、(b) 実体は完全に配置済みだが依存パッケージが未配置、の少なくとも2種がある。ファイル単位検査は (a) しか判別できず、(b) は実際に起動して初めて判別できる。起動可能性検証を自動導入直後の1回に限定する設計を採ると、他プロセスが導入中の実体を第3経路で拾った呼び出し（自らは自動導入を行わないため検証段に到達しない）が (b) を検出できず、委譲先CLI自身のエラーによる非ゼロ終了——SPEC が定める2挙動のいずれでもない終了形——へ落ちる。SPEC の要件「部分配置状態を検知した場合は日本語エラー＋非ゼロ終了を採る」と AC-11 の Then を満たすには、検証を「自動導入を行ったプロセス」ではなく「第3経路を採用しようとするすべてのプロセス」へ課す必要がある。

**受け入れるコスト**: 第3経路を採用する呼び出しでは、ラッパー1回の起動につき `--help` のプロセス生成が1つ増える。このコストが生じるのは第1経路（`bin/agents-md.js`）・第2経路（`node_modules/.bin/agent-skill-chain`）のいずれの実体も存在しない構成に限られる。本リポジトリ自身のCIは第1経路が常に成立するため増加は無く、consumer のCIワークフローとローカル実行のうちグローバル導入のみで運用する構成でのみ増加する。実行時間は本SPECのいずれのACにも拘束されない一方、AC-11 の充足は受入条件であるため、正しさを優先する。第1・第2経路に同じ検証を課さないのは、それらが本設計の自動導入（`npm install -g`）の書き込み先ではなく、AC-11 の Given が対象とする並行導入の途中結果がそこに現れないためである（利用者自身が並行して実行するローカル `npm install` の途中結果は AC-11 の対象外であり、本設計はこれを扱わない）。

**相互排他（ロックファイル等）は導入しない**: SPEC が要求するのは2挙動への収束であって直列化ではなく、SPEC はロック機構の新設をスコープ外としている。ロックはロックファイルの後始末・異常終了時の残留・並行プロセス数の上限といった新たな失敗モードを持ち込む。同一パッケージの `npm install -g` が並行実行された場合、後着側が失敗すれば「導入失敗として日本語エラーで非ゼロ終了」へ、成功すれば再解決（＝採用条件を満たす第3経路の再判定）を経て委譲へ収束するため、いずれも定義済みの2挙動の範囲に収まる。

**残る限界の範囲（明示）**: 上記2検査は「`--help` の起動が成功する状態」を採用条件とするため、`--help` 起動後・委譲先サブコマンド実行前の極小時間に他プロセスが実体を破壊した場合まではカバーしない。これは並行導入固有の問題ではなく、あらゆる `exec` 委譲に共通する時間差であり、SPEC が禁じる「部分配置状態のCLI実体を**採用して**委譲する」（採用時点の状態に対する要求）には該当しない。本設計はこの時間差を除く全ての部分配置状態について、採用を構造的に排除する。

### D6: `AGENT_SKILL_CHAIN_AUTO_INSTALL` は値が厳密に `0` のときのみオプトアウトとする

未設定・空文字列・`false`・`no`・その他の任意の値はすべて「既定（自動導入を試行する）」として扱い、警告もエラーも出さない。`false`・`no` 等を追加でオプトアウトと解釈する案は、利用者が意図していない綴りを黙って有効化する危険（および将来の解釈拡張が挙動を静かに変える危険）があり、未知値をエラーにする案は、無関係な理由で値が入っている環境のラッパー起動を巻き添えで失敗させる。SPEC が必須としているのは `0` の場合の挙動のみであるため、契約を `0` に限定し、それ以外は既定へ倒す。この判断は共有実装のヘッダコメントと README に明記する。

### D7: CIワークフローテンプレートの「Ensure agent-skill-chain CLI」ステップを共有実装へ統一する

本Issueが解消しようとしている構造的原因は「同一ロジックが複数箇所にコピーされているため、片方の修正が他方へ届かない」ことそのものである。ワークフロー側のインライン記述を残すと、共有実装への今後の変更が再びワークフローへ届かない同型の乖離を残すことになる。ワークフローはリポジトリを checkout した状態でこのステップを実行するため、配布物に含まれる共有実装は当該時点で必ず利用可能であり、参照先が存在しない懸念は生じない。ステップは非対話環境で実行されるため、確認プロンプトを経ずに自動導入が試行される既存挙動と一致する。統一により、リポジトリ全体で3経路解決を記述する箇所は共有実装1つだけになる。

### D8: AC-1 の走査手段を3つの検査で確定し、窓幅に依存する検査の合格条件を「部分集合」とする。併せて前文同一性の検査Dを同じテスト群で表明する

判定に用いる literal は `bin/agents-md.js`、`node_modules/.bin/agent-skill-chain`、`command -v agent-skill-chain` の3つとする。`.agent-skill-chain/` 配下の `*.sh` 全件を対象に、次の3検査すべての成立を AC-1 の合格条件とする。

- 検査A（窓検査・重複の再導入検出）: 3つの literal が連続する12行の窓に同居するファイルを列挙し、**その集合が `{共有実装}` の部分集合であること**（0件も1件も合格。2件以上、または共有実装以外が含まれれば不合格）。
- 検査B（単独 literal 検査・共有実装の一意性）: literal `node_modules/.bin/agent-skill-chain` を含むファイルがちょうど1件で、それが共有実装であること。
- 検査C（窓に依存しない存在確認）: 共有実装が3つの literal をすべて含むこと（同居する行間距離は問わない）。

さらに、AC-1 の合格条件そのものではないが同じテスト群（`E4`）で表明する不変条件として、次を加える。

- 検査D（前文同一性・契約割当）: 対象54本からマーカーコメント間の前文ブロックを抽出し、行頭空白の除去と `return`→`exit` の正規化の後に全件が文字単位で一致すること。加えて、正規化前に `exit` 形である件数がちょうど52、`return` 形がちょうど2であり、後者が `adapters/claude.sh`・`同 human.sh` に一致すること（設計判断 D3 の契約割当）。検査Dを設けるのは、D3 が置く「前文は終了形の語を除いて同一」という不変条件を、AC-4・AC-6・AC-8 の全数実行（挙動の検証）とは独立にテキストとして直接検証するためである。全数実行だけでは、前文が個別に書き換えられていても偶然すべて動作する限り検出できない。

検査Aの合格条件を「ちょうど1件」ではなく部分集合とするのは、`_asc_try_resolve` の記述スタイルと窓幅12という定数を結合させないためである。共有実装の第3経路にはファイル単位検査と起動可能性検証（設計判断 D5）が加わり、コメント・`local` 宣言・エラー分岐によって3 literal 間の距離が12行を超えうる。「ちょうど1件」を要求すると、重複が一切存在しない正しい実装に対して検査Aが0件を返し、AC-1 が誤って不合格になる。部分集合であれば、共有実装が窓に収まるか否かに関係なく正しい実装は常に合格し、他ファイルへの重複再導入だけが不合格になる。共有実装が3経路解決を保持し続けていること自体は、窓幅に依存しない検査B・検査Cが担保する（この役割分担により、検査Aの唯一の責務は「共有実装以外への重複再導入の検出」に限定される）。

コメント行を除外する前処理は行わない（前処理自体が「どこからがコメントか」の解釈を持ち込み、判定を再び実装依存にするため）。多くのラッパーはヘッダコメントで `bin/agents-md.js` に言及するが、3つすべてが12行以内に同居するのは条件分岐として記述されている場合に限られるため、検査Aでコメントを巻き込む誤検出は起こらない。literal `node_modules/.bin/agent-skill-chain` は現状ヘッダコメントに現れないため、検査Bは窓幅の設定に依存しない安全網として機能する。条件分岐を関数へ分割して重複を再導入した場合、分割後の断片が窓に収まらなければ検査Aは検出しないが、第2経路を判定する以上その断片は必ず literal `node_modules/.bin/agent-skill-chain` を含むため検査Bが検出する。

### D9: 全数実行テストの環境初期化を「シナリオごとに1つのフィクスチャ複製、実行ごとに可変状態のみ初期化」とする

AC-4・AC-6・AC-8 は54本の全数実行を要求する。ラッパーはフィクスチャ内のファイルを書き換えないため、シナリオ（後方互換／consumer 展開／共有実装解決不能）ごとにフィクスチャを1回だけ用意し、その中で54本を順に実行してよい。実行ごとに初期化するのは可変状態、すなわちスタブの呼び出し記録ファイルと、各実行へ渡す環境変数（`PATH`・`HOME`・`AGENT_SKILL_CHAIN_AUTO_INSTALL`）だけとする。スタブCLIは argv を記録して exit code 0 を返し、`--help` に対しても exit code 0 を返す（第3経路の採用条件に起動可能性検証が常時含まれるため、PATH 上へ置くスタブは `--help` に exit code 0 を返さなければ採用されない。第1経路として置くスタブにはこの要求は掛からないが、経路を入れ替えた派生シナリオで同じスタブを再利用できるよう統一する）。各実行の標準入力は端末に接続しない（非対話環境として評価する）。

### D10: 対象本数の判定基準と、変更後の対象集合の識別基準

SPEC は AC-4・AC-6・AC-8 の判定基準に本数 54 を埋め込んでいる。設計セグメントで実数を確認した結果、変更着手前の版で3経路解決のコード片を保持する `.agent-skill-chain/` 配下の `*.sh` は54本（`scripts/` 直下40本、`ci/` 配下12本、`adapters/` 配下2本）であり、SPEC の記載と一致した。

変更後のラッパーは3経路解決の literal を一切含まない（`E2` の定義）ため、変更着手前の識別基準（3経路解決のコード片を保持すること）は変更後には使えない。そこで**変更後の対象集合の識別基準を「`.agent-skill-chain/` 配下（サブディレクトリを含む）の `*.sh` のうち、ラッパー定型前文の開始マーカーコメント（固定の1行）を含むファイル」と確定する**。当該マーカーは前文の一部であり、契約W・契約Aのいずれでも文字単位に同一である（設計判断 D3。両契約で異なるのは終了形の語のみで、マーカー行はその対象外）。したがってこの基準は判定者の解釈を要さず一意に定まり、契約の別によって列挙結果が変わることもない。共有実装自身は前文を持たないため対象集合に入らない。

全数実行テスト（`E5`）は次の4点を表明する。

1. 上記基準による動的列挙の件数が 54 であること。
2. ディレクトリ別の内訳が `scripts/` 直下 40・`ci/` 配下 12・`adapters/` 配下 2 であること（階層深さが異なる3ディレクトリの代表が必ず含まれることを保証する）。
3. 補集合（`.agent-skill-chain/` 配下の `*.sh` のうち前文を含まないファイル）の一覧が、既知の非対象集合（共有実装、`hooks/` 配下、`adapters/codex.sh`）と完全に一致すること。
4. 列挙した54本すべてを1本ずつ実行すること（抜取りを行わない）。実行手段は契約ごとに定め、契約Wの52本は直接実行、契約Aの2本は driver 経由の `_asc_cli` 呼び出しとする（設計判断 D13）。

1と2は「本数が変わる変更が入った際にテストが黙って対象を減らして通過すること」を防ぎ、3は「前文への置換を1本忘れたまま、動的列挙が53本を返して整合してしまう」ことを防ぐ（忘れられた1本は補集合へ現れ、既知の非対象集合と一致しなくなる）。対象本数を意図的に増減させる将来の変更では、テスト内の定数と非対象集合の一覧を同じ変更で更新することを要求する。

### D11: 文書量の上限に関する確認

文書量の上限検査は `AGENTS.md`（150行）と `.agent-skill-chain/templates/` 配下の各テンプレート（各100行）のみを対象としており、Issue 単位の成果物実体（`SPEC.md`・`DESIGN.md`・`PLAN.md`・`VALIDATION.md`）は検査対象に含まれない。したがって本Issueの成果物本文の行数は上限違反にならない。SPEC で下流セグメントの確認事項として残されていた本項は、ここで解消済みとする。

### D12: ラッパー定型前文の自己位置算出は `BASH_SOURCE[0]` を用いる（`$0` を用いない）

定型前文は共有実装のパスを「自身の位置」から算出するが、その「自身の位置」を得る手段を `${BASH_SOURCE[0]}` に固定する。`$0` を用いてはならない。理由は、対象54本のうち `.agent-skill-chain/adapters/claude.sh`・`同 human.sh` の2本が他スクリプトから `source` される形で使われ、その文脈で `$0` は呼び出し元スクリプトのパスを指すため、`$0` 基準では当該2本が誤ったディレクトリを基準に共有実装を探し、解決に失敗するからである。

設計判断 D3 は前文を「終了形の語を除いて54本で文字単位に同一」とすることを不変条件としており、直接実行される52本（契約W）と `source` される2本（契約A）の双方で同一テキストが正しく動作するには、算出手段が呼び出し形態に依存しない `BASH_SOURCE[0]` であることが必須である。すなわち D3 の同一性と D12 の「`BASH_SOURCE[0]` 基準」は独立した2つの制約ではなく、前者が後者を必要条件として含む。`${BASH_SOURCE[0]}` は関数内で参照した場合も当該関数を定義したファイルのパスを指すため、契約Aで前文がファイル冒頭にある場合も、将来同じ算出式を関数内へ移した場合も、基準ディレクトリは変わらない。前文が算出するのは `.agent-skill-chain/` の位置（前文を持つファイルから1階層上）であり、そこから共有実装 `scripts/cli-resolve.sh` を求める。リポジトリルートの算出は前文では行わない（共有実装の責務に一本化する）。

### D13: 契約A（アダプタ2本）に対する AC-4・AC-6・AC-8 の実施手順と合格条件

AC-4・AC-6・AC-8 は対象54本の全数実行を要求するが、契約Aの2本は直接実行を想定した実体ではなく、固定の委譲先サブコマンドも持たない（`_asc_cli` は呼び出し元から受け取った引数列をそのままCLIへ渡す）。したがって「実行する」「委譲先サブコマンド名が一致する」「非ゼロ終了する」の意味を契約Aについて確定させなければ、当該2本の受入判定が実施不能になる。本設計は次のとおり確定する。

**共通の実施手段（driver）**: テストは、契約Aの各ファイルに対して1本の呼び出し元スクリプト（driver）を用意する。driver は `set -euo pipefail` の下で対象アダプタを `source` し、`_asc_cli` を所定の引数列で1回だけ呼び、その戻り値を自身の exit code として終了する。driver はテスト側の一時ディレクトリに置き、`.agent-skill-chain/` 配下へは置かない（対象集合の動的列挙へ混入させないため）。driver 自身はCLI解決・自動導入に関する記述を一切持たない。「契約Aのラッパーを1本実行する」とは、本 driver を1回実行することを指す。

**AC-4（後方互換）の合格条件**: 変更前の版の複製と変更後の複製の双方に対し、同一の driver・同一の引数列（各アダプタが自身の実装内で実際に用いる引数列の代表として、`gate reviewer-context <issue_id>` を用いる）で実行し、(a) スタブCLIが記録した引数列が完全に一致すること、(b) driver の exit code が一致すること、(c) 変更後の実行で共有実装が解決されて `_asc_cli` の呼び出しが成立すること。契約Aには固定の委譲先サブコマンド名が無いため、AC-4 が要求する「委譲先CLIサブコマンド名の一致」は、記録された引数列の先頭要素の一致として評価する（引数列全体の一致がこれを含意する）。

**AC-6（consumer 展開状態）の合格条件**: 展開物の複製上で driver を実行し、「共有実装ファイルが見つからない」に類する失敗で終わらず、`npm` スタブへの呼び出しが記録される（AC-2 と同じ自動導入分岐へ到達する）こと。

**AC-8（共有実装を解決できない場合）の合格条件**: 共有実装を4状態（欠落・切り詰め・読み取り権限剥奪・構文エラー）にした複製上で driver を実行し、(a) 共有実装を解決できない旨と探索パスを含む日本語メッセージが標準エラー出力へ出力され、(b) driver の exit code が非ゼロであり、(c) スタブCLIが一度も呼ばれないこと。契約Aでは前文が `return` 非ゼロで `source` を打ち切るため、`_asc_cli` は未定義のまま残り、driver は `source` の非ゼロ戻り値で停止する。この停止が driver の非ゼロ exit code として観測されることをもって、AC-8 の「非ゼロのexit codeで停止する」を満たしたと判定する。

**契約A固有の追加表明**: 上記に加え、アダプタを `source` するだけで `_asc_cli` を呼ばない場合に、`npm` スタブへの呼び出しが一度も記録されないことを表明する（`source` 時点で自動導入の副作用が発生しないという設計判断 D3 の帰結を、テストとして固定する）。

### D14: 第3経路の起動可能性検証が前提とする「実CLIの `--help` は exit code 0 で終了する」を確認済みの事実として記録し、回帰表明を設ける

設計判断 D5 の起動可能性検証は「CLI実体を `--help` で起動すると exit code 0 で終了する」ことを前提とする。この前提が偽であれば、グローバル導入のみで運用する構成で第3経路が恒久的に採用されず、自動導入成功後も毎回「再解決不能」で非ゼロ終了する（AC-2・AC-7 の実環境挙動が破れる）。

設計セグメントで実CLIの入口実装を確認した結果、引数が空・`-h`・`--help` のいずれかである場合は使用方法を標準出力へ書いて 0 を返す実装であり、前提は成立している。しかしこの前提は実装の変更で静かに破れうるうえ、テスト用スタブは `--help` に必ず exit code 0 を返すため、スタブのみを用いる自動テストでは前提が偽になっても全件が通過する。

そこで、**実CLI実体（第1経路の `bin/agents-md.js`）に対して `--help` を実行し exit code が 0 であることを表明する回帰テストを追加する**。本リポジトリのCIは第1経路が常に成立する（CLIをビルドする）ため、この表明は毎回実行される。前提が破れた場合はスタブ由来のテストではなくこの表明が失敗し、原因が「起動可能性検証の前提の破綻」であることが直接示される。前提が将来意図的に変更される場合は、起動可能性検証の判定条件（`--help` 以外の手段、または exit code 以外の観測）を同じ変更で設計し直すことを要求する。

### D15: 契約W内に存在する2つの委譲形（末尾 `exec` 49本・`_cli` 関数保持3本）の扱い

設計セグメントで対象54本の記述形を確認した結果、内訳は次のとおりであった。

| 記述形 | 件数 | 対象 | 現状の委譲 |
|---|---|---|---|
| 末尾 `exec` 形（契約W） | 49 | `scripts/` 直下37本・`ci/` 配下12本 | 起動時に3経路解決して末尾で `exec "${CLI[@]}" <サブコマンド> "$@"` |
| `_cli` 関数保持形（契約W） | 3 | `scripts/worker-launch.sh`・`同 worker-launch-verify.sh`・`同 gate-launch-reviewer.sh` | 3経路解決を `_cli` 関数内に持ち、同一プロセス内でCLIを複数回呼ぶ |
| `_asc_cli` 関数保持形（契約A） | 2 | `adapters/claude.sh`・`同 human.sh` | 3経路解決を `_asc_cli` 関数内に持ち、`source` された呼び出し元から複数回呼ばれる |

**`_cli` 関数保持形3本の扱い**: 契約Wに属する（直接実行され、前文の終了形は `exit`）。`asc_resolve_cli` の呼び出しは、関数内ではなく前文の直後（トップレベル、起動時）に置く。`_cli` 関数は残し、その本体を `"${ASC_CLI[@]}" "$@"` の1行へ置き換える（関数名・引数透過・戻り値の伝播という既存の内部境界を保つため。当該3本は `_cli` の戻り値で分岐する記述を複数持つ）。解決を関数内に置かない理由は2つある。(1) 契約W全体で「起動時に必ず解決を通る」ことが保証され、引数や分岐に依存して自動導入分岐へ到達しない実行経路が生じない——これは AC-6（展開状態で全数が自動導入分岐へ到達する）と AC-8（全数が共有実装解決不能で日本語エラー＋非ゼロ終了する）を、当該3本についても引数の選び方に依存せず成立させる。(2) 同一プロセス内でCLIを複数回呼ぶ際、解決手続き（第3経路採用時は `--help` 起動を伴う）が呼び出し回数だけ繰り返されることを避けられる。

**受け入れる挙動変化**: 当該3本では、CLIが解決できない場合の失敗が「最初の `_cli` 呼び出し時点」から「起動時点」へ早まる。いずれも日本語エラー＋非ゼロ終了であり、正常系（CLIが解決できる場合）の引数透過・標準出力・exit code は変わらないため AC-4 の後方互換に抵触しない。CLI解決前に行われる引数検証のエラーメッセージより先に自動導入が試行されうる点は、契約Wの49本が変更前から持つ挙動と同じであり、契約W内で挙動が統一される方向の変化である。

**全数判定での扱い**: AC-4 の比較は、当該3本についても他の契約W同様に「変更前の版の複製と変更後の複製へ同一の引数・環境・フィクスチャを与え、スタブCLIが記録した引数列と exit code が完全に一致すること」で行う。当該3本は最初の `_cli` 呼び出しがCLIサブコマンド（`worker context` ／ `gate reviewer-context`）であり、フィクスチャで到達可能であるため、記録された引数列が1件以上であることを併せて表明する。テストは外部プロセス（エージェント起動・`gh`・`git` の書込み系）をフィクスチャ内のスタブへ差し替え、それらが一度も呼ばれないことを表明する（テストが実環境へ副作用を出さないことを機械的に保証するため）。

## 関連ADR

```yaml
related_adrs: []
```

本Issueの決定は `docs/adr/ADR-0064-cli-resolution-shared-implementation-and-auto-install-fallback.md`（`status: proposed`、本Issueで新規作成）として記録する。構造化フィールドである `related_adrs:` は `accepted` の ADR のみを参照可能とする規約のため、`proposed` の当該 ADR はここに列挙せず、由来としてのみ本文に示す。本設計が前提とする既存の決定のうち、`accepted` 状態のものは存在しないため列挙は空とする。

## 障害・ロールバック考慮

- 想定される失敗モード:
  - **共有実装の欠陥が54本すべてへ同時に波及する**: 本設計は単一障害点を意図的に作る（それが重複排除の目的そのものである）。緩和は3層で行う。(1) AC-4・AC-6・AC-8 の全数実行テストが54本すべての起動成立を毎回検証する。(2) 本リポジトリ自身のCIが対象54本のうち12本（`ci/` 配下）を実際に実行するため、共有実装の破壊はPR段階で必ず顕在化する。(3) ラッパー定型前文が3段検査を行うため、共有実装が壊れた場合でも「無言終了」ではなく探索パス付きの日本語エラーで停止する。
  - **consumer 展開物に共有実装が含まれない**: 配置先を既存の配布対象ディレクトリ内に限定する（設計判断 D1）ことで、配布設定の変更漏れという原因自体を排除している。加えて AC-6 が展開物の複製に対する全数実行で検証する。
  - **自動導入がグローバル環境を利用者の意図に反して変更する**: 対話環境では確認を挟み、非対話環境ではオプトアウト環境変数で事前に無効化できる。既定が「試行する」である点は、CIとローカルの挙動一致という本Issueの要求に由来する意図的な選択であり、README と共有実装ヘッダコメントで明示する。
  - **並行導入の途中結果を採用する**: 第3経路の採用条件に、ファイル単位検査（実体の存在・実行可能・非空）と `--help` による起動可能性検証の双方を常時課すことで防ぐ（設計判断 D5）。「実体は完全に配置済みだが依存パッケージが未配置」というファイル単位では判別できない中間状態も、起動可能性検証が第3経路の採用条件に常時含まれるため、自動導入を自ら行わないプロセスが他プロセスの導入中の実体を拾った場合を含めて検出される。検出時は当該経路を採用せず未解決として扱うため、部分配置状態のCLI実体が委譲へ進む経路は残らない。代償はコスト（第3経路を採用する構成でのラッパー1回あたり `--help` 起動1回分）であり、未検出の残余ではない。
  - **契約Aのアダプタが `source` されただけで不可逆な副作用を起こす**: 解決呼び出しを前文から外し、契約Aでは `_asc_cli` が呼ばれた時点で初めて解決・自動導入へ進む（設計判断 D3）。アダプタを読み込むだけの呼び出し元は `npm install -g` を誘発しない。この不在は D13 の追加表明でテストとして固定する。前文が行うのは共有実装ファイルの読み込みのみであり、外部への書き込みを伴わない。
  - **契約Aで前文が `exit` を用い、呼び出し元シェルを巻き添えに終了させる**: 契約Aの終了形を `return` に固定し（設計判断 D3）、`exit` 形と `return` 形の件数と割当先を検査D（設計判断 D8）が機械的に表明する。逆に契約Wが `return` 形を持つと直接実行時に処理が継続してしまうため、同じ検査が52件の `exit` 形も表明する。
  - **契約Aの呼び出し元が `source` の戻り値を検査せず処理を継続する**: 検査が無いと、共有実装を解決できない状態でも呼び出し元が進み、後続の `_asc_cli` 呼び出しがシェル既定の「コマンドが見つからない」エラー（日本語メッセージも探索パスも伴わない）で終わる。本Issueの変更に、本リポジトリ内で唯一アダプタを `source` するスクリプトへの戻り値検査の追加を含めることで塞ぐ。将来アダプタを `source` する箇所を追加する場合も同じ検査を要求する。
  - **`_asc_cli` の複数回呼び出しで解決手続きが繰り返される**: 契約Aのアダプタは1プロセス内で `_asc_cli` を複数回呼ぶ。共有実装の公開契約は結果をキャッシュしないため、呼び出しのたびに3経路判定が走る（第3経路採用時は `--help` 起動1回を伴う）。これは同一プロセス内で環境が変化した場合に古い結果を返さないための意図的な選択であり、キャッシュ導入は行わない。CLIが解決できない状態では自動導入の試行も呼び出しごとに繰り返されるが、いずれの試行も同一の日本語エラー＋非ゼロ `return` で終わるため、収束先の挙動は変わらない。
  - **起動可能性検証の前提（`--help` が exit 0）が将来破れる**: スタブは常に exit code 0 を返すため、スタブのみのテストでは前提の破綻を検出できない。実CLI実体に対する `--help` の exit code 0 を表明する回帰テストで検出する（設計判断 D14）。破綻時の影響は「グローバル導入のみで運用する構成で第3経路が採用されない」であり、無言の誤動作ではなく日本語エラー＋非ゼロ終了として現れる。
  - **CIワークフローの Ensure ステップが共有実装に依存することによる連鎖**: 共有実装が壊れると当該ステップも失敗する。ただし後続の各検査スクリプトも同じ共有実装に依存するため、依存を増やしたことによる新たな失敗点ではなく、失敗の顕在化がより早い段階へ移るだけである。
- ロールバック手順: 変更は (1) 共有実装ファイルの新規追加、(2) 対象54本の前文置換、(3) CIワークフローテンプレートと `.github/` の同一変更、(4) テスト追加、(5) README 追記、に限られ、いずれも同一PR内に閉じている。既存のCLIサブコマンド・スキーマ・設定項目・ディレクトリ構成規約は変更しないため、問題が判明した場合は当該PRの revert のみで、本Issue適用前の「各ラッパーがインラインで3経路解決し、未解決なら即座に失敗する」挙動へ完全に戻せる。部分的な切り戻し（例: CIワークフローだけを元に戻す）も、他の変更単位と独立に実施できる。
- 影響を受ける既存機能: 対象54本すべての起動経路（ただし正常系の引数透過・標準出力・exit code は不変。契約Aのアダプタについても `_asc_cli` という関数名・引数透過・委譲先CLIの exit code をそのまま返す契約は不変であり、変わるのは関数本体が解決を共有実装へ委ねる点だけである）、CIワークフローの「Ensure agent-skill-chain CLI」ステップ、および当該ステップの bash 実体をフィクスチャ上で実行している既存の単体テスト（ステップ本文の変更に追随する修正が必要）。CLIサブコマンドの実装（TypeScript 側）には一切変更を加えない。
