# DESIGN: 判定プロンプトへ gate_id 別の固定入力集合を展開し、成果物の事実主張を検証可能にする

- Issue: `ISSUE-751`
- 対応する SPEC: `SPEC.md`

## 目的・対象範囲

ゲートレビュアへの唯一の入力である判定プロンプトに、当該 gate_id で検証すべき対象と、それを照合する基準を必ず展開する。展開する入力の集合は SPEC の固定表で一意に定まり、実行時の裁量で増減しない。展開しきれない入力は、無言で切り捨てず、レビュアが判別できる形で明示するか、生成を明示的に失敗させる。

対象は判定プロンプト生成関数 `buildReviewerPrompt` と、その入力集合を導出する新規モジュールである。レビュアへのツール付与、生成物の再生成・照合機構、成果物側への引用義務は対象範囲に含めない。

## 前提

- レビュアにはツール呼び出しが一切許可されていない。プロンプト本文へ文字列として展開された内容だけが検証可能である。
- 判定プロンプトの受け渡しは argv を経由しない。制約はレビュアモデルの文脈長であり、超過は「入力の末尾側が読まれない」という無言の形で現れうる（SPEC の前提「実測 E」）。
- 判定プロンプトの生成経路は `gate reviewer-prompt` とレビュー証跡照合の 2 箇所から呼ばれるが、実体は `buildReviewerPrompt` 1 つである。両者が同一バイト列を得ることが現行の照合の前提である。

## 用語（本 DESIGN 内の定義）

- 展開: target SHA の blob 内容の全バイトを、パスを明示した見出しとともにプロンプト本文へ含めること。部分展開は行わない。
- 展開対象列: 固定表の「判定対象成果物」「上流の承認済み成果物」「憲法文書」の 3 列。各セルはパスを直接列挙する。
- 抽出元列: 固定表の「根拠ファイルの抽出元」列。当該セルは展開すべきパスを列挙せず、根拠ファイルを導出するための入力（成果物）を指定する。
- 根拠ファイル: 抽出元列が指定する成果物の本文が名指しし、target SHA に実在し、かつ展開対象列で既に展開されないファイル。
- 必須入力: 判定対象の差分と展開対象列。省略も部分展開もしない。差分を含めるのは、差分が要件 10 の保全対象である既存区間であり、省略も部分展開もできないためである。予算は要件 5 のとおり根拠ファイルにのみ適用する。
- 上限: 完成した判定プロンプト全体の UTF-8 バイト長に課す単一の値。固定の指示区間・見出し・フェンス・両一覧・展開された全内容を残らず算入し、対象外の区間を設けない。
- 予算: 根拠ファイルの展開に使える残余バイト数。上限から、必須区間のレンダー実測長と両一覧の予約長を差し引いた値。

## 固定表の各列の意味（展開対象か抽出元か）

SPEC の固定表は 2 種類の列を持つ。DESIGN はこれを次のとおり確定する。SPEC の表は変更しない。

| 列 | 種別 | 意味 |
|---|---|---|
| 判定対象成果物 | 展開対象列 | 当該セルが列挙するパスをそのまま展開する。現行の挙動であり本 Issue で変更しない |
| 上流の承認済み成果物 | 展開対象列 | 当該セルが列挙するパスをそのまま展開する |
| 憲法文書 | 展開対象列 | 当該セルが列挙するパス（`AGENTS.md`）をそのまま展開する |
| 根拠ファイルの抽出元 | 抽出元列 | 当該セルが列挙するのは導出の入力であり、展開されるのはそこから導出された根拠ファイルである |

したがって AC-1 の「当該行に挙げた入力」とは、展開対象列 3 列が直接列挙するパスと、抽出元列から導出された根拠ファイルの和集合である。この和集合に属さないファイルは本文に現れない。抽出元列のセル自体は、いずれも同じ行の展開対象列に含まれるため、追加の展開を生まない。

## 展開順序と一意性

展開は次の固定順序で 1 本の順序付きリストとして構築し、**同一パスは最初に現れた位置で 1 回だけ展開する**。

```text
判定対象成果物 → 上流の承認済み成果物 → 憲法文書 → 根拠ファイル
```

この 1 つの規則が、展開対象列どうしの重なり（例: 上流成果物の解決結果が判定対象成果物と重なる呼び出し）と、根拠ファイルの二重展開の双方を同時に排する。根拠ファイルの集合は先行 3 列を除いた後の集合として定義されるため、「抽出されたが既に展開済み」という状態は定義上生じない。よって抽出された根拠ファイルは、展開済み一覧か省略一覧のいずれか一方にちょうど 1 回現れる。

## 根拠ファイルの導出規則

1. `git ls-tree -r --name-only <target_sha>` で target SHA のツリーの全パスを取得する。同一の target SHA に対して git のツリー順は常に同一であり、これを走査順とする。
2. 抽出元列が指定する成果物の本文を連結した文字列に対し、各パスが部分文字列として現れるかを判定する。現れるものを名指しされたとみなす。正規表現による構文的な抽出は行わない（引用符・句読点・囲み記法の差異で結果が揺れるため）。
3. 先行 3 列で展開済みのパスを除く。残りが根拠ファイルの順序付き集合である。
4. 導出は 1 段に限る。根拠ファイル本文・`AGENTS.md` 本文・差分本文からの再抽出は行わない。

入力は抽出元本文と target SHA のツリーのみであり、実行時刻・作業ツリーの未コミット変更・実行環境・実行回数に依存しない。ファイル読み出しは全て `git show <target_sha>:<path>` 系で行い、作業ツリーを読まない。

## 上限と予算

単一の設定値 `review.prompt_max_input_bytes` を上限の正本とする。上限は完成した判定プロンプト全体の UTF-8 バイト長に課し、固定の指示区間・見出し・フェンス・両一覧・展開された全内容を残らず算入する。対象外の区間を設けないのは、内容バイトだけを数えて一覧を対象外に置くと、多数の小さな根拠ファイルを名指しする target SHA で一覧が件数に比例して増大し、内容バイトが上限内のままプロンプト全体が文脈長を超えるためである。それは本設計が排除しようとしている無言の劣化そのものである。

上限値は target SHA の `.agent-skill-chain/config/agent-skill-chain.yaml` blob から解決する。作業ツリーの同ファイルも、導入済みパッケージ側の設定も読まない。blob が無い場合とキーが無い場合は既定値を適用し、値が存在して不正な場合は既存の設定値検査と同じ規則で非 0 終了させる。これにより上限・予算・展開される根拠ファイル・本文へ表示する適用値のいずれも、未コミットの作業ツリー変更と実行環境に依存しない。設定項目として持つ理由は、上限超過が生成失敗になる以上その値がレビュアの文脈長に依存し、実行系アダプタとモデルが既にプロジェクト単位で設定可能だからである。追加の形は既存の任意設定項目と同じとし、スキーマ版は据え置き、移行処理は不要とする。

既定値は 1500000 バイトとする。実測（2026-08-19、本リポジトリ）では ISSUE-733 implementation セグメント（target `06d14f7e`、base `f72eadd6`）の現行生成物が 839397 バイトあり、本 Issue が追加する必須入力は `DESIGN.md` 105195・`PLAN.md` 41410・ADR 2 件 32519・`AGENTS.md` 23930 の合計 203054 バイトである。完全レンダーは 1042451 バイトとなるため、既定値はこれを上回り、一覧と根拠ファイルの余地を残す値とする。

1. 固定の指示区間と必須入力（判定対象の差分と展開対象列 3 列）をレンダーして UTF-8 バイト長 M を実測する。両一覧に載る行は、展開対象列で解決された全パスと根拠ファイルの全候補であり分類前に確定するため、各行について展開済み一覧の行形式と省略一覧の行形式の長い方を採った予約長 L も同時に算出する。
2. **M + L が上限を超える場合、判定プロンプトの生成を失敗させる。** 終了コードは非 0 とし、M・L・候補件数・上限、および対処（対象 Issue の分割、または設定値の引き上げ）を日本語で提示する。必須入力を切り捨てず、省略もしない。
3. 予算 B を `上限 − M − L` として求める。根拠ファイルを走査順に 1 件ずつ処理し、見出し・フェンス・改行を含むレンダー増分が B の残りに収まるものを全文展開する。
4. 完成した文字列全体の `Buffer.byteLength` を再検査し、上限を超えていれば非 0 終了させる。設計上ここへは到達しないが、レンダー増分の計上漏れを無言で通さないための最終防壁である。

必須区間と両一覧は予算の対象外であり、予算を理由に省略されることはない。両者だけで上限を超える事象は、省略ではなく生成失敗として扱う。必須入力を全文展開したまま超過事実を本文へ明示して生成を成功させる案は、通知が見えてもモデルが末尾まで読める保証にならず、不完全な材料で approved が導出される経路を残すため採用しない。

## 省略の 2 系統と通知

根拠ファイルの処理は走査順の 1 パスで行い、各ファイルは次のいずれか 1 つに分類される。

| 分類 | 条件 | 予算消費 | 記録先 |
|---|---|---|---|
| 展開 | テキストであり、レンダー増分が予算残に収まる | する（レンダー増分） | 展開済み一覧 |
| 省略（非テキスト） | blob の内容に NUL バイトを含む、または UTF-8 として厳密に復号できない | しない | 省略一覧（理由: 非テキスト） |
| 省略（予算超過） | テキストだがレンダー増分が予算残を超える | しない | 省略一覧（理由: 予算超過） |

非テキスト判定は blob の内容だけから決まるため決定的である。予算残を超えたファイルは省略一覧へ入れて走査を継続する（先着適合）。走査を打ち切らないため、後続の小さい根拠ファイルが 1 件の巨大ファイルによって一律に失われることはない。省略が予算を消費しないのは分類の段だけの話であり、省略された行も含めて一覧の全行は上限の内側で予約済みである。したがって候補件数が増えれば予約長 L が増え、超過は上限検査で明示的な失敗として現れる。

展開済み一覧は、展開対象列で解決されて全文展開した全パスと、全文展開した根拠ファイルを各 1 回記録する。省略一覧は非テキストまたは予算超過の根拠ファイルを各 1 回記録し、各行はパス・バイト長・内容 digest・省略理由を持つ。digest は既存の成果物 digest と同じ形式・同じ算出関数を用いる。ファイルの一部だけを展開することは、いずれの分類でも行わない。

## 判定プロンプトの区間構成

既存区間は削除・改変しない。変更は次の 3 点に限る。

1. 「上流の承認済み成果物（整合検査用）」区間を gate_id 別の解決結果で構築する。当該 gate_id に上流が無い場合はその旨を、target SHA に存在しない場合は不在を、base SHA 未指定で当該 Issue の ADR 集合を導出できない場合は導出不能を、それぞれ本文へ明示する。いずれも生成は成功させる。
2. 新規区間を、上流成果物の区間の後に「憲法文書」「展開済み・省略の一覧」「根拠ファイル」の順で追加する。一覧は根拠ファイル本文より前に置き、量が多い側の影響を受けにくくする。一覧には適用した上限・必須区間のレンダー長 M・一覧の予約長 L・予算の各値を含める。
3. ハルシネーション防止の区間を書き換え、展開済み一覧に列挙されたファイルは内容が与えられていること、省略一覧に列挙されたファイルは内容不明であり推測してはならないことを、両一覧を判別手段として明示する。展開済みファイルまで検証不能として扱うよう促す記述は置かない。

## 要件 → 設計要素の対応表

| 要件 / AC-ID | 対応する設計要素 | 備考 |
|---|---|---|
| 要件1 / `AC-1` | `resolveUpstreamArtifacts` | gate_id 別の上流解決 |
| 要件2 / `AC-1` | `reviewerInputTable` | 全 gate_id で憲法文書を展開 |
| 要件3 / `AC-2` | `collectEvidenceFiles` | 1 段導出・先行 3 列の除外 |
| 要件4 / `AC-3` | `collectEvidenceFiles`・`resolvePromptInputLimit` | 根拠ファイルの集合も上限値も target SHA の git object だけから決まる |
| 要件5 / `AC-5` | `resolvePromptInputLimit`・`reserveRenderBudget` | 上限は完成プロンプト全体、超過は省略ではなく生成失敗 |
| 要件6 / `AC-4` | `classifyEvidenceFiles` | 非テキスト・予算超過の 2 系統と通知 |
| 要件7 / `AC-6` | `renderEvidenceSections` | 両一覧と適用値の出力 |
| 要件8 / `AC-7` | `buildReviewerPrompt`（ハルシネーション防止区間） | 両一覧との整合 |
| 要件9 / `AC-8` | `resolveUpstreamArtifacts` | 不在・導出不能の明示 |
| 要件10 / `AC-9` | `buildReviewerPrompt`（既存区間） | 追加のみ、既存区間は不変 |
| 要件11 / `AC-9` | `buildReviewerPrompt` | 生成経路を 1 つに保つ |

## 責務・境界

### コンポーネント構成

- `src/lib/reviewer-prompt-inputs.ts`（新規）: 固定表の実体、上流成果物の解決、根拠ファイルの導出・分類、上限値の target SHA blob からの解決、予約長 L の算出と上限検査。純粋な導出とバイト計数だけを持ち、プロンプト文字列の組み立てを持たない。
- `src/lib/exec.ts`（既存に追加）: blob をバイト列のまま読み出す薄い実行ラッパー。非テキスト判定とバイト長計数の基礎。
- `src/commands/gate.ts` の `buildReviewerPrompt`（既存を変更）: 上記の導出結果を受け取り、区間を組み立て、完成後に全体の `Buffer.byteLength` を再検査する。導出規則を内部に持たない。
- `src/lib/config.ts` ほか設定層（既存に追加）: `review.prompt_max_input_bytes` のスキーマ受理・既定値・値の検査規則を持つ。作業ツリーからの読み出しは判定プロンプトの生成では用いない。

### 依存関係

```text
gate.ts(buildReviewerPrompt) → reviewer-prompt-inputs.ts → exec.ts / digest.ts / config.ts
```

`reviewer-prompt-inputs.ts` は `gate.ts` を参照せず、循環依存は生じない。判定プロンプトの生成経路は `buildReviewerPrompt` 1 つのままであり、`gate reviewer-prompt` の標準出力とレビュー証跡照合の期待値は同一の実体から得られる。

### 図示要否の判断

- 判断: `不要`
- 根拠: 依存関係は上記の一方向 1 本（4 モジュール、分岐なし）であり、状態遷移は無い。責務境界は 3 つで基準の 3 つ以上に該当するが、依存が一直線で分岐・循環を持たないため、テキスト矢印表記で構造を一意に表現できる。

## 関連ADR

```yaml
related_adrs: []
```

本設計の判断根拠は `docs/adr/ADR-0075-reviewer-prompt-fixed-input-set-and-overflow-failure.md`（status: proposed）に記録する。`related_adrs:` は accepted の ADR のみを対象とするため、本 Issue で新規作成する proposed ADR は当該フィールドへ記載しない。

## 障害・ロールバック考慮

- 想定される失敗モード 1: 必須区間と一覧の予約だけで 1500000 バイトを超える大規模 Issue の判定プロンプトは生成できない。観測されている最大の生成物である ISSUE-733 implementation の完全レンダー 1042451 バイトは既定値内であり、同規模の既存ゲートを失敗側へ変えない。それを超える場合の対処は対象 Issue の分割、またはより大きな文脈長を確認したプロジェクトでの設定値の引き上げである。全文と超過通知を出して成功させても全入力の可読性を保証できないため、明示的な失敗を安全側の挙動とする。
- 想定される失敗モード 2: 部分文字列照合による根拠ファイルの過剰抽出。短いパスが散文中に偶然現れると根拠ファイルへ入る。過剰分は予算により展開量が抑えられ、省略一覧へ記録されるため、無言の劣化にはならない。
- ロールバック手順: 設定値の引き上げは 1 行の変更で済むが、上限値を target SHA から解決する以上、commit して新しい target SHA でゲートを再実行するまで効かない。作業ツリーの編集だけでは変わらないことを超過時の失敗メッセージへ明記する。設計全体の切り戻しは当該 PR の revert で行い、判定プロンプトは変更前の区間構成へ戻る。
- 影響を受ける既存機能: 判定プロンプトの内容が変わるため、プロンプト digest が変わる。既存の golden fixture は再生成が必要になる。過去に記録済みのレビュー証跡の再検証は SPEC の対象外である。

## 未決事項

- 上限の既定値の妥当性は、本設計の適用後に実運用の生成サイズを観測して見直す余地がある。見直しは設定値の変更で完結する。

## 対象外

- 判定プロンプトの入力スナップショットからの再生成と照合機構。
- レビュアへのツール呼び出し許可、成果物内引用の機械的検証、成果物側への原文引用義務。
- 「判定対象成果物」列の変更、反証ルーブリックの内容、レビュアモデルの文脈長そのものの拡大とモデル選定。
- 名指しされたパスが target SHA に実在しない場合の検出と通知。
