# DESIGN: bugfix: gate workflowが未レビュー状態をジョブ失敗として扱い全PRのCIを恒常的に赤くしている

- Issue: `ISSUE-349`
- 対応する SPEC: `SPEC.md`

## 要件 → 設計要素の対応表

| 要件 / AC-ID | 対応する設計要素 | 備考 |
|---|---|---|
| `AC-1`（未確定ゲートでSUCCESS化+確定・未確定の区別表現） | D1: `verify.ts gateReport()` の exit code分離、D2: `agent-skill-chain-gate.yml` の `Verify gate report schema` ステップ改修＋`Publish Check Run`ステップのリテラルpending時skip | D1は`final=human_required`（実運用で到達する唯一の未確定値、確定値として扱う）を非pending違反が無ければexit 0で通過させ、D2の`Publish Check Run`ステップ（`gate publish`）がblockers・final実値・gate-report本文を含む詳細なaction_required Check Runを発行してジョブをSUCCESSで完了させる。リテラル`final=pending`（白紙スキャフォールド、実運用では到達しない）の場合のみD1がexit 2を返し、D2が`Verify gate report schema`ステップで汎用action_required Check Runを発行したうえで、後続`Publish Check Run`ステップ（`gate publish`が`final=pending`を拒否し非0終了する）を同一ジョブ内のstep output経由でskipし、ジョブ全体をSUCCESSで完了させる |
| `AC-2`（rejectedは引き続き失敗表現） | D1、D6: `src/commands/gate.ts`の`checkRunConclusionForFinal()`新設（`publish()`と`reconcile()`のunchanged分岐が共通のfinal→conclusion導出を用いる是正） | `gate.final`が確定値（approved/rejected/human_required）であれば（`conformance`/`falsification`が個別にpendingのまま提出されていても）従来通りexit 0で後続の`gate publish`へ進み、D6により`rejected`は`failure`のCheck Runとして発行される（`publish()`の拒否条件・分岐構造自体は変更せず、既存インライン三項演算子をD6の関数呼び出しへ置き換えたのみ） |
| `AC-3`（pending以外の不合格は引き続きジョブ失敗） | D1 | schema違反・digest不一致・target_sha不正は「pending以外の理由」として無条件にexit 1を維持する |
| `AC-4`（dependabotでdetect-segments skip） | D3: `detect-segments`ジョブへのdependabotスキップ分岐追加 | `agent-skill-chain-ci.yml`と同型のPR作成者判定を移植する |
| `AC-5`（抽出不能ブランチは引き続き拒否） | D3 | 既存のelse節（exit 1）をそのまま維持する |
| `AC-6`（配布テンプレート同期） | D4: `.agent-skill-chain/templates/github/.github/workflows/agent-skill-chain-gate.yml`へのD2・D3同期 | `verify-template-sync`検査の対象 |
| `AC-7`（実機確認） | D5: 実機検証手順（VALIDATION.mdでmanual実行） | 設計要素ではなく、D1〜D4マージ後の運用確認事項 |

## 責務・境界

### コンポーネント構成

- `src/commands/verify.ts` の `gateReport()`（D1）: gate-reportの検証結果を「pending以外の理由による違反（otherErrors）」と「真に未レビューであること自体（pendingErrors）」に分離して集計する責務のみを持つ。pendingErrorsへの計上基準は`gate.final === 'pending'`（リテラル）であることのみとする（`gate.final`が単一の権威あるフィールドである点は`.agent-skill-chain/schemas/gate-report.schema.yaml`の`final`定義、および`src/commands/gate.ts`の`deriveFinal()`/`aggregateVerdicts()`が判定確定を`final`のみで表現している既存実装と整合する）。`human_required`はpendingErrorsに計上しない。`human_required`は`gate-report.schema.yaml`の`final`定義が記すとおり「レビューは完了したが判定不能（inconclusive・origin衝突等）で人間判断が必要」な確定値であり（`verifyGithubReviewEvidence()`のfail()経路——現在のtarget SHA用review evidenceが無い、actorを解決できない等——も安全側停止としてこの確定値を表明する）、`gate publish`（`src/commands/gate.ts`の`publish()`）は`human_required`を拒否せずexit 0で、title `<gate> gate: human_required`・summary（blockersの実値）・output.text（gate-report全体のcanonical JSON）を含む詳細なaction_required Check Runを発行できる。`human_required`をpendingErrors（exit 2の救済分岐）へ計上すると、この詳細な発行が`Verify gate report schema`ステップの汎用Check Run（title `gate review pending`）に置き換わり、進行役がCheck Runのみから「レビュー未了でまだ待つべき状態」と「レビュー完了・人間判断が必要な状態」を区別できなくなる（design-gate指摘: human-required-collapsed-into-pending-check-runへの是正。AGENTS.md I6・I8）。リテラル`pending`は`gate review`が生成する白紙スキャフォールドにのみ存在する値であり、実運用でgate-reportを生成する唯一の経路である`gate verify-evidence`（`verifyGithubReviewEvidence()`）は`approved`/`rejected`/`human_required`の3値しか返さないため、pendingErrors分岐は実運用では到達しない理論上の安全側フォールバックとして残す。`final === 'pending'`の場合は`gate.conformance`/`gate.falsification`/`gate.final`各フィールドの案内メッセージを`pendingErrors`へ積む（白紙スキャフォールドは3フィールドとも同時にpending）。`gate.final`が確定値（approved/rejected/human_required）の場合は、`conformance`/`falsification`が個別に`pending`のまま提出されていても`gateReport()`はその2フィールドを一切チェックしない（otherErrorsにもpendingErrorsにも計上しない）。これは、standard profile（レビュア1体）で`falsification='fail'`（blocking finding付き）を提出しつつ`conformance`の網羅チェックを`pending`のまま提出する組み合わせが現実的に起こり得るため、`report.gate.final='rejected'`かつ`report.gate.conformance='pending'`という組み合わせがスキーマ的に正当（`isEvidenceVerdict()`にも`gate-report.schema.yaml`にもconformance/falsification/finalの整合を強制するクロスフィールド制約が無い）だからである。判定基準を`conformance`/`falsification`のいずれかのpendingにまで広げると、この組み合わせを誤って「未確定」（exit 2）と扱ってしまい、既に確定した`rejected`判定とblocking findingsがD2でpublishされずに失われる（design-gate指摘: pending-fast-path-swallows-rejected）。`otherErrors`が1件でもあればexit 1（従来通り、`final`の値を問わない）。`otherErrors`が0件で`pendingErrors`が1件以上ならexit 2（リテラル`final=pending`専用）。両方0件ならexit 0（approved/rejected/human_requiredのいずれも通常どおり後続の`gate publish`へ進む）。スキーマ検証・target_sha検証・approved_artifacts検証のロジック自体は変更しない。
- `.agent-skill-chain/ci/verify-gate-report.sh`: `verify gate-report`への薄いラッパーのまま実行ロジックは一切変更しない（`exec`によりexit code 2もそのまま伝播する）。ヘッダコメントのみ、旧記述「gate.conformanceとgate.falsificationの両方（立証・反証の2観点）が記録されているかを検査する」を、`final`（単一の権威あるフィールド）が確定値のときはconformance/falsificationの個別pendingを検査しない実際の挙動に即して是正する（終了コードの意味の正本はD1の`GATE_REPORT_USAGE`）。
- `.github/workflows/agent-skill-chain-gate.yml` の `verify-and-publish`ジョブ内`Verify gate report schema`ステップ（`id: schema`、D2）: `set +e`でexit codeを捕捉し、code=2（リテラル`final=pending`。実運用のgate-reportでは到達しない理論上のケース）の場合のみ既存の`Verify local-review evidence`ステップと同じ形式（`node -e` + `gh api check-runs`）で`agent-skill-chain/<gate>-gate`という名前のaction_required Check Runを`target_sha`へ発行したうえで`echo "pending=true" >> "$GITHUB_OUTPUT"`してからexit 0する。code=0（approved/rejected/human_required。`final=human_required`の未確定gate-reportもここに含まれる）の場合は`echo "pending=false" >> "$GITHUB_OUTPUT"`してから現状どおり素通りする。code=1はそのまま`exit "$CODE"`でジョブが停止する（pending出力は書かれないが、ジョブ自体が停止するため後続ステップは実行されず無関係）。
- `.github/workflows/agent-skill-chain-gate.yml` の `verify-and-publish`ジョブ内`Publish Check Run`ステップ（D2）: 現状の無条件実行（if条件なし）を`if: steps.schema.outputs.pending != 'true'`へ変更する。リテラルpendingの場合は直前の`Verify gate report schema`ステップが既にaction_required Check Runを発行済みであり、かつ`gate publish`（`src/commands/gate.ts`の`publish()`関数）は`report.gate.final === 'pending'`を「真に未レビュー」として明示的に拒否し非0で終了する実装になっているため、このステップ自体をskipして二重発行・後続ジョブ失敗を防ぐ。`final=human_required`の場合はskipせず本ステップが実行され、`publish()`が`human_required`を拒否せずexit 0で、conclusion=action_required・title `<gate> gate: human_required`・summary（blockersの実値）・output.text（gate-report全体のcanonical JSON）のCheck Runを発行する（救済分岐はCheck Runを発行していないため二重発行は起きない）。`publish()`関数のpending拒否ガード・approved矛盾拒否ガード・分岐構造自体は変更しないが、conclusion導出のみD6（後述の`checkRunConclusionForFinal()`）の関数呼び出しへ置き換える（既存のインライン三項演算子と同一の対応関係であり、発行されるconclusionの値は変わらない）。
- `src/commands/gate.ts` の `checkRunConclusionForFinal()`（D6、新設）: `final`が`approved`なら`success`、`rejected`なら`failure`、それ以外（`human_required`・`pending`）なら`action_required`を返す共通ヘルパー。`publish()`の既存インライン三項演算子をこの関数呼び出しへ置き換え、`reconcile()`のunchanged分岐（`approved_artifacts`のdigestが新SHAでも不変な場合）が無条件に発行していた`'success'`もこの関数から導出する値へ置き換える（blocking finding: human-required-publish-clobbers-reconciled-gateへの是正）。両経路が同一関数からconclusionを導出することで、`human_required`・`rejected`のconclusionが`publish()`経路と`reconcile()`経路とで乖離しないことを機構的に担保する。
- `.github/workflows/agent-skill-chain-gate.yml` の `detect-segments`ジョブ（D3）: `Resolve immutable context`ステップに`ACTOR`環境変数（`github.event.pull_request.user.login`）を追加し、Issue ID抽出失敗時に`ACTOR == 'dependabot[bot]' && BRANCH == dependabot/*`ならスキップ（`skip_checks=true`、`issue_id`空）とする分岐を、既存の抽出失敗時exit 1のelse節の**前**に挿入する。`Detect started segments`ステップに`if: steps.context.outputs.skip_checks != 'true'`を追加し、ジョブ出力`matrix`の参照式を`${{ steps.segments.outputs.matrix || '[]' }}`へ変更する（ステップ未実行時に空文字列ではなく`'[]'`が伝播するようにするため）。
- `.agent-skill-chain/templates/github/.github/workflows/agent-skill-chain-gate.yml`（D4）: D2・D3と同一内容を反映するだけの配布物であり、独立した設計判断を持たない。

### 依存関係

```text
gateReport()（D1） → verify-gate-report.sh（無変更） → agent-skill-chain-gate.yml「Verify gate report schema」ステップ（id: schema、D2）
「Verify gate report schema」ステップのpending出力（steps.schema.outputs.pending） → 同ジョブ「Publish Check Run」ステップのif条件（D2、新設）
detect-segmentsジョブのcontext/segmentsステップ（D3） → 同ジョブのmatrix出力 → verify-and-publishジョブのif条件（無変更）
D2・D3 → templates/github/.github/workflows/agent-skill-chain-gate.yml（D4） ※単純ミラー、逆方向の依存はない
```

D1はD2からのみ呼び出され、D3はD1と独立（detect-segmentsジョブとverify-and-publishジョブは別ジョブであり、循環はない）。D2は既存の`Verify local-review evidence`ステップが持つCheck Run発行ロジック（`node -e`＋`gh api`パターン）を再利用するのみで新規外部依存を導入しない。「Publish Check Run」ステップのif条件は同一ジョブ内のstep output参照のみである。`src/commands/gate.ts`の`publish()`関数への依存は既存のまま（リテラル`final=pending`以外——approved/rejected/human_required——の場合にこのステップから呼び出される）だが、`publish()`自体もD6（`checkRunConclusionForFinal()`）を利用するよう変更されている——変更内容は既存インライン三項演算子の共通関数呼び出しへの置き換えのみで、拒否条件・分岐構造・発行されるconclusionの値は変わらない。D6は`publish()`と`reconcile()`（unchanged分岐）の両方から呼ばれる。

## 関連ADR

```yaml
related_adrs: []
```

本Issue以前から存在する`accepted`のADRの中に本設計と直接関連するものは無い。本Issueで新設した`docs/adr/ADR-0018-gate-report-pending-as-action-required.md`は、設計ゲート承認時に`proposed`から`accepted`へ遷移した後、strictレビュアの指摘（ADR-0018のDecision文言が`human_required`と真のpendingの扱いの区別・Publish Check Runのskip条件・`gate reconcile()`のconclusion導出是正に言及していない件）を受けて同一PR内で`docs/adr/ADR-0019-gate-report-human-required-driven-pending-handling.md`（`supersedes: [ADR-0018]`）に置き換えられ、現在は`status: superseded`・`superseded-by: ADR-0019`である（accepted後の本文は`.agent-skill-chain/templates/adr/ADR.md`の不変項目規約により書き換えず、supersedeで置換した）。したがって本設計に対応する現在有効な決定はADR-0019であり、DESIGN.md読者はADR-0018単独ではなくADR-0019を参照する必要がある。ADR-0018・ADR-0019はいずれも本Issueの設計セグメントの主成果物（この設計を確定させるADR）であるため`related_adrs:`には計上しない。

## 障害・ロールバック考慮

- 想定される失敗モード: D1のpending/otherErrors分離ロジックに誤りがあると、本来exit 1にすべきgate-report（スキーマ違反・digest不一致・target_sha不正）を誤ってexit 2（pending扱い）に倒し、マージ阻害が緩む「fail-open」方向のバグとなり得る。これはI8（安全側ラチェット）に抵触するため、設計上「otherErrorsが1件でも存在すれば無条件にexit 1」を優先させ、pending判定はotherErrorsが完全に0件の場合にのみ働く排他的分岐とする（曖昧な優先度付けをしない）。
- 想定される失敗モード（pendingErrorsの判定基準を`conformance`/`falsification`の個別pendingにまで広げた場合）: standard profileのレビュア1体が`falsification='fail'`（blocking finding付き）を提出しつつ`conformance`の網羅チェックを完了させず`pending`のまま提出することは現実的に起こり得る。このとき`report.gate.final='rejected'`かつ`report.gate.conformance='pending'`という組み合わせが生成され得るが、これはスキーマ的に正当な形（`.agent-skill-chain/schemas/gate-report.schema.yaml`にconformance/falsification/finalの整合を強制するクロスフィールド制約が無い）である。もしpendingErrorsの判定基準を`conformance`/`falsification`のいずれかのpendingにまで広げると、この組み合わせを誤ってexit 2（pending扱い）に倒し、D2が`Publish Check Run`ステップを無条件にskipするため、既に確定した`rejected`判定とblocking findingsがCheck Run summaryにも`reviews/<gate>.yaml`にも一切publishされずに失われる（SPEC.md AC-2違反）。これもマージ阻害を緩める「fail-open」方向のバグでありI8に抵触するため、D1の判定基準を`gate.final`という単一の権威あるフィールドのリテラル`pending`のみに限定する（design-gate指摘: pending-fast-path-swallows-rejectedへの是正）。
- 想定される失敗モード（pendingErrorsの判定基準に`human_required`を含めた場合）: `human_required`は実運用（`gate verify-evidence`経由）で到達する唯一の「未確定」値であるため、これをexit 2の救済分岐へ倒すと、実運用の全未確定ケースで`Publish Check Run`ステップがskipされ、`gate publish`が発行するはずだった詳細情報（title `<gate> gate: human_required`・blockersの実値・gate-report全体のcanonical JSON）が、`Verify gate report schema`ステップの汎用Check Run（title `gate review pending`・固定summary）に置き換わって失われる。進行役はCheck Runのみを読んで次を判断する（AGENTS.md I6正準モデル）ため、「レビュー未了でまだ待つべき状態」と「レビュー完了・人間判断が必要な状態」（I8の人間判断への昇格対象）を区別できなくなる。`publish()`は`human_required`を既に正しく処理できる（拒否ガードはリテラル`pending`のみ、`human_required`はexit 0でaction_required発行）ため、D1は`human_required`をpendingErrorsに計上せずexit 0で通過させ、通常の`gate publish`経路に乗せる（design-gate指摘: human-required-collapsed-into-pending-check-runへの是正）。
- 想定される失敗モード（`Publish Check Run`ステップのif条件）: `steps.schema.outputs.pending`の書き出し漏れ・判定誤りにより、リテラルpendingであるにもかかわらず`if`条件がfalseと評価されず同ステップが実行された場合、`gate publish`（`publish()`関数の既存ガード）が`final=pending`を拒否し非0で終了するため、ジョブはFAILUREへ戻る。これはAC-1の効果が当該実行に限り得られない退行だが、マージ阻害を緩める方向（fail-open）ではなく従来の安全側（fail-closed）へ戻るだけであり、I8には抵触しない。逆にリテラルpendingでないにもかかわらず誤って`if`条件がskipと評価された場合は、`gate publish`が呼ばれずCheck Run発行（approved/rejected/human_requiredの確定結果）が欠落するため、この方向の誤りをテストで優先的に検出する。
- 想定される失敗モード（`gate reconcile`との相互作用、blocking finding: human-required-publish-clobbers-reconciled-gate）: `verify-and-publish`ジョブは`final=human_required`（レビュー未確定）の場合も`Publish Check Run`ステップ（`gate publish`）経由でaction_required Check Runを発行する（AC-1として意図通り。`Verify gate report schema`ステップが直接発行するのはリテラル`final=pending`の理論上のフォールバックのみ）。なお`gate publish`は`reviewFilePath()`経由で`reviews/<gate>.yaml`への書き込みを行うが、CI実行時（`verify-and-publish`ジョブ）にはこの書き込みは使い捨てcheckout上で行われコミットされずジョブ終了とともに破棄されるため、CI経路での実質的な効果はCheck Run発行のみである（`reviews/<gate>.yaml`のGit管理下での永続化は、CIジョブではなくローカル実行のtrusted recorder経路が担う）。一方`gate reconcile`（`.github/workflows/agent-skill-chain-reconcile.yml`、`on: push`で同一SHAに対し`agent-skill-chain-gate.yml`と並行起動し得る）は、直前に永続化済みの`reviews/<gate>.yaml`（＝このpush以前の、古いSHA時点の確定済み判定）を読み、その`approved_artifacts`のdigestが新SHAでも不変なら「無変更」として当該gateのCheck Runを再発行する。修正前の`reconcile()`はこの再発行時のconclusionを`report.gate.final`を見ずに無条件で`'success'`へ固定していたため、直前の確定判定が`human_required`（未確定）や`rejected`であっても、対象ファイルが変化していないだけで`success`へ昇格させてしまい、I6（正準モデル）が求める「単一の正準な調整状態」を毀損しうる。本Issueでは`reconcile()`のunchanged分岐のconclusionを、`publish()`と共通の`checkRunConclusionForFinal()`（D6、`final`が`approved`なら`success`、`rejected`なら`failure`、それ以外（`human_required`・`pending`）なら`action_required`）から導出するよう修正し、この「無変更＝無条件success」というfail-openな昇格を解消した（`src/commands/gate.ts`）。
- 意図した挙動変化（`reconcile()`自身が生成するリテラル`pending`がunchanged分岐へ流入するケース）: `reconcile()`のchanged分岐は`report.gate.conformance`/`falsification`/`final`をリテラル`'pending'`へ書き換えて`reviews/<gate>.yaml`へ永続化するが、`approved_artifacts`のdigest自体は更新しない。したがって一度invalidateされたgateは、後続pushで対象成果物が承認時のdigestと一致する内容へ戻った場合、unchanged分岐に入るものの`checkRunConclusionForFinal('pending')`が`action_required`を返すため、修正前のように`success`を再発行しない。これはAGENTS.md「ゲートの継承・無効化」の「変化なしなら最新SHAへ成功を再発行」という文言を字義通りに読んだ場合の継承挙動からの変更である（invalidate後に成果物が承認時点の内容へ復元されたケースに限る）が、一度invalidateされたgateを再レビュー無しに`success`へ自動復帰させることは、I8安全側ラチェット（降格は自動、昇格は人間の明示行為のみ）が禁じる自動昇格に該当する。「変化なしなら成功を再発行」の継承規定は、承認状態が維持されているgateのCheck RunをSHA間で引き継ぐためのものであり、invalidate（降格）を跨いだ`success`復活まで保証するものではないと解し、本設計はこの挙動変化を意図した是正として採用する（AGENTS.md当該文言との字面上の緊張は残るため本段落に明記し追跡可能とする）。invalidate後の`success`復帰経路は再レビュー（`gate verify-evidence`→`gate publish`）のみである。
- 未解消の残留リスク（別Issueとして切り出し）: 上記修正後も、`agent-skill-chain-gate.yml`の`verify-and-publish`ジョブ（`pull_request_target: synchronize`等）と`agent-skill-chain-reconcile.yml`（`push`）は同一push eventから並行起動し得り、両者の完了順序に関する調整（ロック・順序保証）が存在しない。`gate reconcile`は自身では新SHAに対する`gate verify-evidence`の再実行を行わず、直前に永続化された`reviews/<gate>.yaml`（＝古いSHA時点の判定）を基準に「無変更なら前回の判定を維持して再発行」するだけであるため、`gate.yml`側が新SHAに対して最新のreview evidenceに基づく異なる結論（例: 新しいreviewが提出されhuman_required→approvedへ確定）を同一SHAへCheck Run発行しようとしている最中に、`reconcile`が古い判定に基づくCheck Runを後から発行すると、実行順序次第で最新の判定が古い判定に上書きされ得る。これは単一スクリプトの1判定ロジック修正では解消できない、2つの独立workflow間の調整（発行順序・排他制御、またはreconcile側でも同一の`gate verify-evidence`相当の再検証を経由する設計変更）を要する構造的課題であり、本Issueのスコープ（AC-1〜AC-6の範囲）を超える。具体的な由来（Issue #349のimplementation-gateレビューで発見されたblocking finding: human-required-publish-clobbers-reconciled-gateの残留部分）を明記した別Issueとして起票し、対応方針（reconcile.yml側での再検証導入、または2 workflow間の排他制御）を改めて設計する。
- ロールバック手順: D1〜D4はいずれも本Issueの単一PR内の変更であり、当該PRをrevertすれば即座に旧動作（全pending=FAILURE、dependabotブランチでdetect-segments失敗）へ戻る。Check Run自体はcommit SHA単位で発行されるため、reconcile.shが後続pushで再評価し状態を追従させる（reconcile.shのunchanged分岐は本Issueの修正により`report.gate.final`に整合したconclusionを発行する）。
- 影響を受ける既存機能: `verify gate-report`の呼び出し元は`agent-skill-chain-gate.yml`の1箇所のみ（他script・workflowからの呼び出しなし）。既存の統合テスト（`test/integration/verify.test.ts`）では、白紙スキャフォールド（`final=pending`）を検証するケースがexit status 2を、`final=human_required`のケースが（非pending違反が無ければ）exit status 0を期待する（PLAN.mdの変更単位1で申し送る）。`gate publish`の`human_required`受理・action_required発行は`test/integration/gate-judgment.test.ts`の既存テストが検証済みであり、`publish()`側の追加変更は不要。
