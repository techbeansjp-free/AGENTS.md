# DESIGN: worker-selection関連ファイルの禁止参照（セクション番号参照）を是正しmainのCIを復旧する

- Issue: `ISSUE-325`
- 対応する SPEC: `SPEC.md`

## 要件 → 設計要素の対応表

| 要件 / AC-ID | 対応する設計要素 | 備考 |
|---|---|---|
| `AC-1` | 是正方針A（`worker-launch.sh`）／是正方針B（`worker-selection.ts`） | 両方針とも「参照の除去」を採用（下記「禁止参照の解消方針」参照） |
| `AC-2` | 是正方針A／是正方針B（設計判断の温存確認） | 除去後も設計判断がコメント本文に残ることを設計要素として明示する |
| `AC-3` | 変更範囲の限定（コメントのみ、ロジック不変） | ソースの実行内容を変更しないことを設計上の制約とする |
| `AC-4` | 是正方針B（`worker-selection.ts`）のスコープ拡張 | 是正方針Bは `DESIGN.md §選択解決の設計` 部分の削除に加え、`SPEC.md AC-1・AC-2・AC-3・AC-9` 部分（陳腐化する受入条件ID列挙）の削除も含む。両者は同一行・同一是正方針内の作業として扱う |

## 責務・境界

### `lint-references.sh` の判定ロジック（前提の確認）

是正方針を決めるにあたり、`.agent-skill-chain/scripts/lint-references.sh`（薄いラッパー）が委譲する agent-skill-chain CLI の `lint references` サブコマンド（実装本体）の判定ロジックを確認した。

- `§` の直後から、空白・句読点・各種括弧のいずれかが現れるまでの文字列をセクション参照として捕捉する（例:「`DESIGN.md §選択解決の設計`」からは「選択解決の設計」を捕捉する）。
- 捕捉した文字列は、検査対象として走査した `.md` ファイル群から抽出した見出しテキスト一覧と照合する。見出しが捕捉文字列で始まる／捕捉文字列が見出しで始まる、または末尾の英数字・ローマ数字風コード（例:「不変条件 I1〜I8」に対する「I7」）を除いた芯部分が一致すれば「見出しテキストで解決できる参照」として許可する。一致する見出しが1つも無ければ禁止参照として報告する。
- 見出し抽出・照合の対象となる `.md` ファイル群は、検査対象として走査されたファイルのうち拡張子が `.md` のものに限られる。デフォルトの走査対象は AGENTS.md・`docs/GLOSSARY.md`・`.agent-skill-chain/{standards,templates,config,schemas,scripts,ci}/`・`src/`・`.github/workflows/` であり、Issue 単位で作成される `SPEC.md`／`DESIGN.md`／`PLAN.md`（リポジトリルート直下、Issue のブランチ上にのみ存在し完了後は破棄される一時成果物）はこの走査対象に含まれない。

この確認から導かれる結論: 該当2箇所の禁止参照「`DESIGN.md §選択解決の設計`」は、そもそも参照先の見出し自体が（a）走査対象外のファイルにあり、かつ（b）Issue 完了後に破棄される一時成果物であるため、恒久的に存在し続ける見出しではない。したがって「見出しテキストで解決できる参照へ書き換える」方針（例えば AGENTS.md 側や `src/` 側の恒久的な見出しを指す形に変更する）は採用しない。仮に一時的に解決可能な見出しへ付け替えても、参照先が Issue 完了後に消える構造は変わらず、次に別の Issue の `DESIGN.md` が作られ同じ見出しが再利用されない限り再び陳腐化するため、根本解決にならない。

採用する方針は、AGENTS.md「成果物の自己完結性」節が定める「外部参照は由来情報としてのみ用い、参照元にはその文脈で必要な契約の要旨を本文へ記載する」原則に従い、**参照そのものを撤去する**方針である。撤去にあたり、参照先が説明しようとしていた契約の要旨が撤去後のコメントから読み取れるかを1箇所ずつ確認する。

### 是正方針A: `.agent-skill-chain/scripts/worker-launch.sh`

該当コメントは「worker context 自体の失敗（設定不正・ティア解決失敗）は、まだ何も起動していない段階のエラーとして扱う（DESIGN.md §選択解決の設計）。」という一文であり、括弧内の参照は同じ文中で既に述べた設計判断（`worker context` 解決失敗を「まだ何も起動していないエラー」として扱う）への末尾の裏付け引用にすぎない。設計判断の内容自体は参照を除いても文中に完全に残っているため、**参照句「（DESIGN.md §選択解決の設計）」を削除するのみ**とし、新たな文言の追記は不要と判断する。

### 是正方針B: `src/lib/worker-selection.ts`

該当コメントはファイル冒頭の「正本:」宣言行であり、`AGENTS.md §設定` / `SPEC.md AC-1・AC-2・AC-3・AC-9` / `DESIGN.md §選択解決の設計` の3つを列挙している（このうち `AGENTS.md §設定` は AGENTS.md に現存する見出し「設定」で解決できるため `lint-references.sh` の禁止参照には該当しない）。`DESIGN.md §選択解決の設計` が指していた「選択解決の設計」の内容（アダプタ・ティア・reasoning_effort の解決順序、責務分担）は、同ファイル内の `resolveWorkerSelection` 直前の docstring（解決順序：セグメント別上書き→`worker.adapter`→既定 human、ティア対応表からの具体モデル解決は別関数が担う旨）に既に完全な形で記載されている。

是正方針Bのスコープは、当初 `DESIGN.md §選択解決の設計` 部分の削除のみを想定していたが、手動ゲートレビューでの指摘（AC-4追加）を受け、`SPEC.md AC-1・AC-2・AC-3・AC-9` 部分の削除も含むよう拡張する。この部分は `lint-references.sh` の `§` パターン検出では捕捉されないが、AGENTS.md「参照・コメントの陳腐化防止」節が定める「ソースコードコメントの追跡識別子として認めるのは Issue ID のみ」という規約に抵触する陳腐化参照である（Issue 単位の `SPEC.md` は Issue 完了後に破棄される一時成果物であり、そこに定義された受入条件IDへの参照は当該 Issue の作業期間を超えて存続できない）。この参照が説明しようとしていた内容（当該モジュールが SPEC.md のどの受入条件に対応するかという由来情報）は、モジュール自体の責務理解に必須ではなく、1行目に続くモジュール概要のコメント本文（config とセグメント名だけから adapter・model_tier・reasoning_effort・モデル文字列を決める、という記述）で責務は既に自己完結して説明されているため、代替の書き下しは不要と判断する。

したがって是正方針Bは、`// 正本: AGENTS.md §設定 / SPEC.md AC-1・AC-2・AC-3・AC-9 / DESIGN.md §選択解決の設計` の1行から、**`DESIGN.md §選択解決の設計` 部分と `SPEC.md AC-1・AC-2・AC-3・AC-9` 部分の両方を削除する**方針とする。削除後の1行目は `// 正本: AGENTS.md §設定` のみとなる。

### 変更範囲の限定

いずれの方針も、コメント文字列の一部を削除するのみでコードの実行内容（関数シグネチャ・分岐・戻り値）を変更しない。これにより既存の単体テスト（`test/unit`）・結合テスト（`test/integration`）の結果に影響を与えない設計とする。

## 関連ADR

本 Issue はソースコードコメントの表現をAGENTS.mdの既存規約に適合させる是正であり、アーキテクチャ判断を新たに伴わないため、関連 ADR は無い。

## 障害・ロールバック考慮

- 想定される失敗モード: 参照句を削除した結果、当該コメントが説明しようとしていた設計判断が読み取れなくなる（AC-2 未達）。是正方針A・Bのいずれも、削除対象が「既に本文中に記載済みの内容への末尾引用」であることを確認済みであり、この失敗モードは生じない設計とした。
- 想定される失敗モード（AC-4 固有）: `SPEC.md AC-1・AC-2・AC-3・AC-9` 部分の削除範囲を誤り、直前の `AGENTS.md §設定` 部分まで巻き込んで削除してしまう、あるいは区切り文字（` / `）の削除漏れにより `// 正本: AGENTS.md §設定 / ` のような不正な行末が残る。この失敗モードは AC-4 の検証コマンド `! grep -q 'SPEC\.md AC-' src/lib/worker-selection.ts` では「`SPEC.md AC-` が残っていないこと」しか検査できず、`AGENTS.md §設定` の誤削除や区切り文字の残存は別途検出する必要がある。対策として PLAN.md の検証手順に、修正後の1行目全体が `// 正本: AGENTS.md §設定` と完全一致することの目視確認を含める。
- ロールバック手順: 本変更はコメント2箇所の削除のみであり、当該コミットを `git revert` すれば即座に元の状態（禁止参照が残る状態）へ戻せる。ロールバック自体に副作用は無い。
- 影響を受ける既存機能: 無し。`worker-launch.sh` の起動フロー・`worker-selection.ts` のエクスポート関数（`resolveWorkerSelection` 等）の入出力・分岐はいずれも変更しない。
