<!--
正本: AGENTS.md §4セグメント・4ゲート
このファイルは Issue 毎に複製して使う雛形である（セグメント: design、成果物: DESIGN.md（PLAN.md は別ファイル）、ゲート: design-gate）。
-->

# DESIGN: agent-skill-chain — lint vocab/references の src/ 対象拡大・CLIサブコマンド文脈判定の構造的抜け穴修正

- Issue: `ISSUE-187`
- 対応する SPEC: `SPEC.md`

## 目的・対象範囲

lint 機能（`vocab`/`references`）について、(1) 既定検査対象へ TypeScript 実装本体（`src/`）を加え、(2) 識別子文脈判定を「散文中の検出漏れを塞ぎつつ実コード・実設定の正当な識別子は誤検出しない」方式へ改め、(3) 対象拡大で顕在化する既存違反を是正する。既存の軽量な自前実装（正規表現ベースのスキャナ＋薄いシェルラッパー）というアーキテクチャは踏襲し、出力メッセージ書式・終了コード契約・引数インターフェースは一切変更しない。対象は `src/lib/scan.ts`（検査対象根の定義）と `src/commands/lint.ts`（禁止語出現の文脈判定・禁止参照検査）の 2 モジュールに閉じる。

## 要件 → 設計要素の対応表

| 要件 / AC-ID | 対応する設計要素 | 備考 |
|---|---|---|
| 要件1（対象拡大） / `AC-1` | `defaultLiveFileRoots` へ `src/` を追加（`defaultVocabFileRoots` が継承）。`bin/` は追加しない | `existsSync` フィルタ維持で非存在環境を無害化 |
| 要件2（既存違反の是正） / `AC-2` | 分類是正（コメント散文の正規用語化・自己言及 mention のバッククォート化・禁止参照コメントのインライン化・正当コード識別子のバッククォート付き添字化） | 契約要旨（意味）を変えない |
| 要件3（ファイル種別ディスパッチ） / `AC-3`, `AC-4` | `isIdentifierContext` へ拡張子 `ext` を伝播し、YAML 文脈判定を `.yaml/.yml` 限定・CLI サブコマンド bare 文脈判定を非 `.md` 限定で適用 | コード識別子・外部語彙許可リスト・上流除外は全 ext 共通で不変 |
| 要件4（正当利用の非後退） / `AC-4` | 上流 3 除外・複合コード識別子・外部語彙許可リストの評価順序と挙動を不変に保つ | `ext` は後段判定へ伝播するのみ |
| 要件5（CI での実効） / `AC-6`, `AC-5` | CI ワークフロー・ラッパーは無改修（引数なし呼び出しで新既定を継承）。テスト追加で回帰保証 | ワークフロー定義は変更しない |

## 責務・境界

### コンポーネント構成

- **検査対象根の定義（`src/lib/scan.ts`）**: 「どのファイルを検査するか」を決める責務。`defaultLiveFileRoots` の根集合へ `src/` を加える。`defaultVocabFileRoots` はこれを継承し `docs/GLOSSARY.md`（自己言及回避のため恒久除外）のみ除く。走査時の対象拡張子集合（`.md`/`.yaml`/`.yml`/`.sh`/`.json`/`.ts`）は変更せず、生成物 `.js` は従来どおり非対象。
- **文脈判定（`src/commands/lint.ts` の識別子文脈判定群）**: 「各禁止語出現が散文誤用か正当な技術的参照か」を決める責務。判定チェーン（`hasProseViolation` → `isCodeLikeReference` → `isIdentifierContext` → 各文脈判定）へ当該ファイルの拡張子を引き渡し、`isIdentifierContext` 内でファイル種別ディスパッチを局所化する。
  - 上流除外（バッククォートスパン・プレースホルダ・スラッシュ入りパストークン、および禁止語自体がパス文字列である場合の除外抑止）は全 ext 共通・評価順序不変。
  - コード識別子文脈（`snake_case`/`camelCase`/`SCREAMING_SNAKE_CASE` の複合識別子の一部としての禁止語）と外部語彙の明示許可リスト（改名・バッククォート付与のいずれも取れない既知の完全一致トークン）は全 ext 共通。
  - YAML 識別子文脈（キー構文・flow-sequence 要素）は `.yaml`/`.yml` のときのみ評価。
  - CLI サブコマンド bare 文脈（独立シェルトークンとして既知 CLI 動詞と隣接）は散文（`.md`）以外のときのみ評価。
  - 想定外拡張子は fail-safe に散文扱い（YAML/CLI 文脈を適用しない厳格側）とする。実運用では上流の対象拡張子フィルタにより判定器へ到達する拡張子が既知集合に限られるため、この経路は防御的措置である。
- **禁止参照検査（`src/commands/lint.ts` の `references`）**: 節番号記号参照・ファイルパス＋行番号参照を検出する責務。ロジックは不変。対象根拡大により `src/` のコメント中の禁止参照が新たに検査対象化する。
- **既存 `src/` 違反の是正（コンテンツ）**: 対象拡大で顕在化する違反を分類是正する責務。

### 是正戦略の内訳

対象拡大の実測で `src/` に禁止語違反 49 件・禁止参照違反 29 件が実在した（起票時実測と一致）。これらを次の分類で是正する。

- **コメント散文（禁止語違反の大半、約 40 件）**: 小文字 `issue` が GitHub Issue 概念を指す箇所は許可語 `Issue` または `成果物` へ、同様に `バックエンド`→`Coordination Backend`、`ロック`/`排他制御`→`writer lease` のコメントを正規用語へ書き換える。純粋な内容修正で意味は保持する。
- **ルール自己言及の禁止語 mention**: 禁止語そのものを説明のため引用しているコメント（用語定義注釈の例示・スキャナ説明中の旧パス名など）はバッククォートで囲み、全 ext 共通のバッククォートスパン除外で正当化する。
- **正当なコード識別子の残余（約 4〜5 件）**: `issue` が CLI 名前空間・ドメイン識別子として正当に現れる箇所（route キー文字列・設定メンバ参照など）は、実行時契約を変えないバッククォート付き添字アクセス等でコード上正当化する。いかなる残余是正も散文の検出漏れを再び開かないことを不変条件とし、回帰テストで担保する。
- **禁止参照 29 件（すべて `src/*.ts` のコメント中）**: コメントを「必要な契約要旨のインライン記述」へ書き換え、節番号記号参照・ファイルパス＋行番号参照を除去する。なお `references` 自身の違反メッセージ組み立てに現れる節番号記号は、生の記号を直接埋め込むとスキャナが自己言及として誤検出するため、Unicode エスケープ経由の定数で参照する（実行時の出力文字列は不変。機械処理用エラー出力での使用は許可）。

### 依存関係

```text
CI ワークフロー → lint-vocab.sh / lint-references.sh → bin/agents-md.js（src/*.ts のビルド成果）
src/commands/lint.ts → src/lib/scan.ts（既定対象根・走査）
src/commands/lint.ts → src/lib/cli-routes.ts（CLI 動詞集合を遅延評価で導出。既存の相互 import は遅延評価で初期化順序問題を回避済み）
```

新規の循環参照は導入しない。文脈判定への変更は `ext` 引数の追加伝播のみであり、モジュール境界を跨ぐ新しい依存を生まない。

## 設計判断の根拠

### ファイル種別ディスパッチを採用した理由

検討した代替案は次の 2 つ。(a) 動詞ホワイトリストを狭める案は、動詞集合が日常英単語（create/start/review 等）であるため恣意的で根治せず、狭めるほど実 CLI 参照の除外漏れが増える。(b) 同一行に CLI プログラム名がある場合のみ CLI 文脈とみなす行内アンカー案は、散文が偶然プログラム名を含む場合の漏れが残り、かつ「散文 `.md` 中の YAML キー風行を YAML キーと誤判定する」もう一方の抜け穴を放置する。両案とも根本原因「ファイル種別（実際に YAML か、実コードか）を見ていない」に対処しない。

ファイル種別ディスパッチはこの根本原因を直接是正する。散文中で CLI/コード参照を正当に示す正規形はバッククォート囲み（全 ext 共通除外）であり、真に構造化 YAML を検査対象化したい場合は `.yaml`/`.yml` ファイルへ置く運用と整合する。実測で、4 行の散文のみのファイルに対し旧実装は検出 0 件（検出漏れ再現）だったものが、ディスパッチ後は `.md` でこれらが検出に転じることを確認した。

**受容するトレードオフ**: 本方式は `.md` で YAML 識別子文脈を一律に無効化するため、`.md` 内の正当な構造化 YAML（YAML frontmatter・fenced yaml コードブロック）に含まれる bare な禁止語キーも除外を失い検出へ転じる。既定検査対象の `.md` を実測した結果、bare な行頭 YAML キー形・fenced yaml 内の bare 禁止語キーは不在であり（唯一の該当は複合コード識別子として全 ext 共通で除外され影響しない）、実害は生じない。将来 `.md` を新たに既定対象へ加える場合、または配布先 consumer が `.md` frontmatter に禁止語を bare キーとして記入した場合は、バッククォート化または `.yaml` 移設で解消する。

### `bin/` を対象へ加えない理由

要求文言は「`src/`・`bin/`」だが、`bin/` は `.gitignore` 対象のビルド生成物（`git ls-files bin/` は空）であり、走査対象拡張子集合が生成物 `.js` を含まないため、`bin/` を根へ加えても走査対象ファイルは 0 件（no-op）。実測で禁止参照違反 29 件はすべて `src/*.ts` 由来、`bin/` の寄与は 0 だった。実装本体の実体は完全に `src/` に存在し `bin/` はその生成物であるため、`src/` の対象化で「実装本体を検査する」という要求の意図は完全に満たされる。生成コードの lint は重複報告を招くアンチパターンでもある。以上より `bin/` は対象外とする。

## 関連ADR

```yaml
related_adrs: []
```

**新規ADR作成の要否判断**: 新規の正式 ADR（`docs/adr/` 配下）は作成しない。理由は次の 3 点。

1. 既存の正式 ADR（システム仕様書構築方針・writer lease 方式・ワーカー権限モデル・worktree パス解決）はいずれも Issue をまたいで再利用・被継承される、システム全体にわたる横断的アーキテクチャ決定である。本 Issue の設計判断（ファイル種別ディスパッチ・`src/` 追加・既存違反の是正戦略）は lint 経路に閉じた Issue ローカルな実装形状の選択であり、他 Issue から参照・継承される関係も、既存 ADR を置換する関係も持たない。
2. 本 Issue が改修する識別子文脈判定は、その基盤（識別子認識スキャナ）を確立した先行 Issue においても正式 ADR を作らず設計文書（DESIGN.md）内に自己完結して記録された。本 Issue はまさにその同じ仕組みの拡張であり、一貫性の観点からも同じ扱いが妥当である。
3. 設計判断の背景・検討した代替案・採否根拠・受容したトレードオフは本 DESIGN.md 内に自己完結して記載しており、追跡可能性（要求→設計→実装→検証）は本成果物と Git 履歴で担保される。lint に関連する既存の正式 ADR も存在せず、参照で足りる ADR も無い。

（注: 分析過程で用いた「ADR-1/ADR-2/ADR-3」という呼称は作業用の暫定ラベルであり、`docs/adr/` の正式 ADR 文書ではない。本 DESIGN.md では上記「設計判断の根拠」節がその内容を自己完結して収録する。ソースコード中のコメントで用いている「Issue #187 ADR-1」という追跡識別子は、実装当時の設計ラベルを Issue ID 付きで残したものであり、正式 ADR への参照ではない。）

## 障害・ロールバック考慮

- **想定される失敗モード**:
  - 識別子文脈判定の過剰除外（本来検出すべき散文誤用を誤って除外）: 全 ext 共通の複合コード識別子・外部語彙許可リストと、上流 3 除外の評価順序・挙動を不変に保つことで防ぐ。回帰テスト（散文 `.md` 中の偶然共起の検出、正当利用の非検出）が担保する。
  - 既存違反是正でコメントの意味（契約要旨）が変わる: 是正は正規用語への言い換え・バッククォート化・インライン記述への書き換えに限り、記述内容の意味を保持する。既存テスト全件通過で担保する。
  - `.md` 内の正当な構造化 YAML の偽陽性: 前述のトレードオフのとおり既定対象では不在を実測確認済み。将来の顕在化はバッククォート化・`.yaml` 移設で解消する。
- **ロールバック手順**: 本 Issue の全変更は単一の実装 commit にまとまっており、revert すれば既定対象根への `src/` 追加・ファイル種別ディスパッチ・既存違反是正がすべて同時に巻き戻る。
- **影響を受ける既存機能**:
  - `lint vocab`・`lint references` の既定対象範囲が `src/` の分だけ拡大する（AC-1・AC-2）。
  - `.md` ファイルにおける YAML キー風・CLI 動詞共起の bare 禁止語の扱いが「除外」から「検出」へ変わる（意図的なバグ是正。AC-3）。既存の識別子認識テストのうち `.md` に YAML/CLI ケースを置いていたものは、新仕様に合わせ `.yaml`/`.sh` フィクスチャへ移設し、非検出の期待を維持する。
  - CI 実行時間は `src/` の走査分わずかに増えるが、純関数の同期検査であり既存の CI ステップに比して軽微。
  - CI ワークフロー・ラッパー・branch protection / ruleset 設定は変更しない（引数なし呼び出しで新既定を継承）。
