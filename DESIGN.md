# DESIGN: agent-skill-chainをClaude Codeスキルとして提供する実現可能性調査

- Issue: `ISSUE-428`
- 対応する SPEC: `SPEC.md`（本Issueはスコープが「調査のみ・実装はしない」と明確なため、
  Issue本文の「求める対応」節（調査項目1〜5・完了条件）を要件相当として下表に対応させ、
  SPEC.mdは作成しない。実装計画を持たない調査Issueである点はADR-0023 Context節にも明記済み）

## 要件 → 設計要素の対応表

本Issueの成果物は `docs/adr/ADR-0023-agent-skill-chain-as-skill-feasibility.md`（調査結果を記録するADR、`status: proposed`）1点のみであり、コード変更を伴わない。「設計要素」はADR内のセクション構成として表現する。

| 要件 / AC-ID | 対応する設計要素（ADR内セクション） | 備考 |
|---|---|---|
| 調査1: スキル機構の技術的制約の整理（一次情報に基づく） | ADR「調査1: Claude Codeスキル機構の技術的性質」 | Claude Code公式文書・Agent Skills標準・第三者OSS事例など9件の一次情報URLを明記 |
| 調査2: 現行構成要素（AGENTS.md本体・hook・GitHub ruleset・CLI・scripts）がスキルに収まるか | ADR「調査2: 現行構成要素がスキルに収まるか」（対応表） | 構成要素ごとに「スキルとして表現可能」「代替不可能」を判定 |
| 調査3: 常時統制とオンデマンド呼び出しのトレードオフ整理 | ADR「調査3: 常時統制とオンデマンド呼び出しのトレードオフ」 | 強制層・規範層・手順層の3層分解 |
| 調査4: 想定利用者の分類 | ADR「調査4: 想定利用者の分類」（対応表） | 常時開発型・間欠開発型・非開発型の3類型 |
| 調査5: 既存の類似事例調査 | ADR「調査1(g)(j)」（プラグイン配布経路・feature-dev/hookify/Superpowers事例） | 調査1の一部として統合（別セクションに分離すると調査1の一次情報根拠と重複するため） |
| 暫定的な推奨案（(a)〜(d)のいずれか、根拠付き） | ADR「調査5: 検討した代替案」「Decision」 | (b)+(c)の合成を推奨、却下した(a)(d)の理由も明記 |
| 完了条件: 一次情報を実際に参照した根拠が示されていること | ADR「調査1」冒頭の参照URLリスト（取得日付2026-08-04明記） | 想像記述の排除 |
| 完了条件: 推奨案が自己完結的に記載されていること | ADR「Decision」節（推奨内容5項目、未確定事項4項目を明示区分） | 「引き続き調査・議論が必要な事項」として未決着点を隠さず列挙 |

## 責務・境界

### コンポーネント構成

本Issueはコードコンポーネントを持たない。ADR自体の構成要素（節）を責務単位として記述する。

- `Context`: 現行構成（`init`/`setup github`/`enforce on`/CLI）の記述と、問題提起の引用。調査対象の境界を確定する。
- `調査1`: 一次情報の収集・引用のみを担う。以降の判定・推奨の根拠データはすべてここに集約する。
- `調査2`〜`調査4`: 調査1で得た事実を、現行構成要素・トレードオフ構造・利用者類型という3つの異なる切り口へ適用し分析する。
- `調査5`・`Decision`: 調査2〜4の分析結果を代替案として比較し、推奨を確定する。
- `Consequences`: 推奨採用時の利点・欠点・後続の必要作業を列挙する。

### 依存関係

```text
一次情報（Claude Code公式文書・Agent Skills標準・第三者OSS） → 調査1（事実収集）
  → 調査2（現行構成要素への適用）─┐
  → 調査3（トレードオフの構造化）─┼→ 調査5（代替案比較） → Decision → Consequences
  → 調査4（利用者類型への適用）───┘
```

依存方向は一次情報からDecisionへの一方向であり、Issue・PR・commit・会話履歴を規範的根拠にしていない（AGENTS.md §docs/system-spec/ の参照可能情報源の原則と同じ制約を、ADR単体でも自己完結的に満たす）。

### 図示要否の判断

- 判断: `不要`
- 根拠: 本Issueはコンポーネント間のデータフローを持つ実装ではなく、単一ADR内の調査セクション間の論理的な参照関係（上記テキスト矢印表記）で全体像を十分に追える。責務境界も5ブロック程度で3つ以上の該当はあるが、状態遷移・外部システム依存が無い単純な逐次構造のため図示を必須としない。

## 関連ADR

```yaml
related_adrs: []
```

`docs/adr/ADR-0023-agent-skill-chain-as-skill-feasibility.md`は本Issueで新設する成果物であり（`status: proposed`、Decision節に明記のとおり本ADR自体は実装を承認しない調査記録であるため、design-gate通過後も`accepted`への遷移が唯一の帰結とは限らない——後続の実装Issueが起票された時点で改めて判断する）、同一設計セグメントの主成果物であるため`related_adrs:`には計上しない。`accepted`のADRの中に本調査と直接関連するものは無い。

## 障害・ロールバック考慮

- 想定される失敗モード: 一次情報の参照先URL・記述内容が将来陳腐化する（Claude Code公式文書の仕様変更）。本ADRはIssue完了条件に「一次情報を実際に参照した根拠」を求めているため、取得日付（2026-08-04）を明記することで陳腐化を検知可能にしている。
- ロールバック手順: 本Issueはコード変更を伴わないため、ロールバックは`docs/adr/ADR-0023-*.md`のrevertのみで完結する。他のADR・成果物からの`related_adrs:`参照が無いため、revertによる波及も無い。
- 影響を受ける既存機能: 無し（調査記録の追加のみ）。「引き続き調査・議論が必要な事項」節に列挙した未決着点は、実装Issueが起票された際にその設計セグメントで扱う。
