# DESIGN: security: reconcile workflowがpushトリガーのYAML自己参照によりGate Check Runを偽造可能

- Issue: `ISSUE-342`
- 対応する SPEC: `SPEC.md`

## 背景の要約（本設計の前提）

GitHub Actionsの`push`トリガーは、実行されるワークフローYAML自体を「pushされたコミット」から読み込む。`agent-skill-chain / reconcile`（`.github/workflows/agent-skill-chain-reconcile.yml`）は`on: push`かつ`permissions: checks: write`を持つため、writer権限保有者がワークフローYAML自体・agent-skill-chain CLIソース（`src/agents-md.ts`）・`gate reconcile`実装のいずれかを自分のissueブランチ上で改変してpushするだけで、その改変済みロジックがそのまま実行され`agent-skill-chain/{spec,design,implementation,validation}-gate`のCheck Runを偽造できる。

design-gateのstrictレビューで、この問題を`workflow_run`によるtrigger/trusted 2ファイル分離（`agent-skill-chain-reconcile-trigger.yml`を`on: push`・無権限で新設し、`agent-skill-chain-reconcile.yml`を`workflow_run`化する）で解消しようとした前ラウンドの設計に根本的な欠陥が指摘された（`reconcile-trigger-workflow-remains-self-referential-push-yaml`）。`agent-skill-chain-reconcile-trigger.yml`自体が`on: push`トリガーのままである以上、そのワークフローYAML自体（`permissions:`ブロックを含む）は攻撃者が完全に制御するpushされたコミットから読み込まれる。したがって攻撃者は`agent-skill-chain-reconcile-trigger.yml`自身の`permissions: {}`を`checks: write`へ書き換え、Checks APIを呼ぶstepを追加するだけで、trusted workflow（`agent-skill-chain-reconcile.yml`）を一切経由せずCheck Runを偽造できてしまう。`workflow_run`による分離は、GITHUB_TOKEN権限がプラットフォームにより自動的に読み取り専用へ制限されるfork由来のpull_request系イベントに対してのみ有効な緩和策であり、同一リポジトリ内のpush（fork不要）には同種の自動制限が働かない。

同リポジトリの`.github/workflows/agent-skill-chain-gate.yml`は`pull_request_target`トリガーにより、この種の自己参照問題を構造的に持たない安全なパターンを既に実装している。`pull_request_target`で起動するワークフローは、ワークフローYAML自体・チェックアウトされ`npm ci && npm run build`されるCLIソースを、常にPRのbaseブランチ（trusted。マージ後は常に最新のdefaultブランチ）から読み込む。PR head（攻撃者が完全に制御する内容）は`actions/checkout`の対象にはせず、`git fetch`によるread-onlyなgit objectとしてのみ取得し実行しない。本設計は`agent-skill-chain-reconcile.yml`をこの`pull_request_target`パターンへ全面的に作り直し、新規`agent-skill-chain-reconcile-trigger.yml`は作成しない。

判定ロジック自体の自己参照とは独立に、`gate reconcile`（`src/commands/gate.ts`の`reconcile()`）が「承認済み基準」（`approved_artifacts`）を、issueブランチへコミット可能な`issues/<id>/.agent-skill-chain/reviews/<gate>.yaml`（`reviewFilePath`）から読み込んでいる問題（承認済み基準データ自体の改ざん耐性、AC-5）、および判定対象識別子（target_sha・issue_id）自体の出所の限定（AC-6）は、前ラウンドで確定済みの設計（`resolveApprovedBaseline`・source identity検証）をそのまま維持する。本ラウンドはトリガー方式の変更にのみ集中する。

## 要件 → 設計要素の対応表

| 要件 / AC-ID | 対応する設計要素 | 備考 |
|---|---|---|
| AC-1（改変内容だけではCheck Run偽造不可） | `agent-skill-chain-reconcile.yml`の`on: pull_request_target: types: [synchronize]`化（既存ファイルの変更のみ、新規ファイルは作成しない） | `pull_request_target`はワークフローYAML自体・チェックアウトされるCLIソースを常にPRのbaseブランチ（trusted）から読む（GitHub Actionsの仕様）ため、PR head側でどう改変されても判定ロジックには反映されない。`workflow_run`+trigger.yml分離が抱えていたtrigger.yml自体の自己参照問題は、trigger.ymlという別ファイルを作らないため最初から発生しない。**ただし本設計が解消するのはreconcile自身の判定ロジックの自己参照に限られる。SPEC.md要求冒頭の「いずれのCheck Runも」という包括的文言全体（reconcile/gate以外の新規ワークフローファイルによる直接偽造を含む）は本設計では満たされない。この境界と根拠は本ファイル末尾「本設計のスコープ境界」節で明示する** |
| AC-2（既存の正規フロー継続） | `gate-reconcile.sh`・`artifactDigestAtSha`・Check Run再発行/無効化の判定分岐（`reconcile()`）は無変更 | SPEC.mdのAC-2 Givenは「正規のセグメントワーカーが、issueブランチに紐づくPRのheadブランチへ…push」であり、これは`synchronize`イベントそのものである。issue-start直後・Draft PR作成前の最初のpushはPRが未存在のため`pull_request_target`イベント自体が発火しないが、その時点では承認済みCheck Runがまだ1件も存在せず照合対象が無いため機能上のギャップにならない（後述「障害・ロールバック考慮」参照） |
| AC-3（本体・配布テンプレート同期） | `.github/workflows/agent-skill-chain-reconcile.yml`と`.agent-skill-chain/templates/github/.github/workflows/agent-skill-chain-reconcile.yml`の内容同期（既存の仕組みのまま） | 新規ファイルを追加しないため、同期対象はIssue着手前と同じ単一ファイルに戻る |
| AC-4（dependabot許可判定の意図維持） | `Derive issue_id`ステップの3分岐ロジックは維持しつつ、判定に用いる実PR作成者を`github.event.pull_request.user.login`から直接取得する（新規`env.PR_AUTHOR`） | `pull_request_target`イベントは常にPRコンテキストを伴うため、`push`イベント時代に必要だった`gh api repos/{owner}/{repo}/pulls?head=...`によるPR特定・作成者照会が不要になる。3分岐の意図（真正dependabotのみ許可、それ以外は`ISSUE-<n>`抽出または拒否）自体は変更しない |
| AC-5（承認済み基準データ自体の改ざん耐性） | `resolveApprovedBaseline`（`src/commands/gate.ts`の`reconcile()`内部、前ラウンドで確定済み）＋`reconcile()`側の`found`分岐（本ラウンドで新規追加）＋`changed=false`分岐でのpublishCheckRun `output.text`構築のtrusted baseline置換（本ラウンドで新規追加） | `pull_request_target`化により`pr_number`が`github.event.pull_request.number`としてワークフロー実行時に直接判明するため、`gate-reconcile.sh`の新設第3引数として渡す。これにより`resolveApprovedBaseline`は`GET commits/{target_sha}/pulls`によるPR特定ステップを（主経路では）省略できる。source identity検証・JSONパース失敗時のフォールバックは前ラウンドの設計をそのまま維持する。**加えて、`resolveApprovedBaseline`が「候補なし」（`found: false`）を返した場合を`approved_artifacts: []`と区別せず`changed`判定へ流し込むと、`.some()`が恒偽になり未レビューSHAへもsuccessを再発行してしまう欠陥（design-gate指摘`no-baseline-treated-as-vacuously-unchanged-reissues-success`）が本ラウンドで新たに指摘されたため、`found: false`の場合は照合そのものをスキップする第三分岐を新設した（詳細は下記「コンポーネント構成」）。さらに、`found: true`かつ`changed: false`の場合にpublishCheckRunへ渡す`output.text`（`canonicalJson(report)`）が、判定入力には使われなかったローカル`report`（`reviewFilePath`から読み込んだ、writerが改変可能なオブジェクト）の`approved_artifacts`をそのまま素通しして埋め込んでいたため、2回のpushにまたがる攻撃（push1で`reviews/<gate>.yaml`の`approved_artifacts`のみを未来のdigestへ書き換えてtrusted success Check Runへ汚染値を埋め込ませ、push2で実成果物をそのdigestへ一致させて汚染済みbaselineと照合させ`changed=false`を騙し取る、design-gate指摘`reconcile-republish-embeds-untrusted-local-approved-artifacts-enabling-two-push-baseline-poisoning`）が新たに指摘されたため、`changed: false`分岐でpublishCheckRunへ渡す直前に`report.gate.approved_artifacts`を`resolveApprovedBaseline`の戻り値（trusted baseline自体）で上書きしてから`canonicalJson(report)`を構築する（詳細は下記「コンポーネント構成」）** |
| AC-6（判定対象識別子の出所限定） | `Derive issue_id`ステップの`env.BRANCH`を`github.event.pull_request.head.ref`、`Reconcile gates against pushed SHA`ステップの対象SHA引数を`github.event.pull_request.head.sha`、pr_number引数を`github.event.pull_request.number`から取得 | いずれもGitHub Actionsランタイムが`pull_request_target`イベントの一部として提供する構造化フィールドであり、pushされたブランチのコミットメッセージ・ファイル内容・環境変数からは導出されない。SPEC.mdのAC-6原文は判定対象識別子の出所を「採用するトリガー種別に応じて`workflow_run`イベントの`head_sha`・`head_branch`、または`pull_request_target`イベントの`pull_request.head.sha`・`pull_request.head.ref`・`pull_request.number`等」とトリガー方式非依存の形で明記しており、本設計が参照する各フィールドはこのAC-6原文が列挙する`pull_request_target`側の構造化フィールドと字句上一致する（字句上の差分は存在しない） |

## 責務・境界

### コンポーネント構成

- `agent-skill-chain-reconcile.yml`（既存ファイルの`on:`・参照フィールドを変更、trusted。新規ファイルは作成しない）: `on: pull_request_target: types: [synchronize]`で起動する。ジョブに`if: github.event.pull_request.base.ref == github.event.repository.default_branch`を追加し（`gate.yml`と同型）、defaultブランチ以外を対象とするPRでは起動しない。既存の`permissions`（`contents: read`, `checks: write`, `pull-requests: read`）に`actions: read`を新規追加する（design-gate指摘`resolve-approved-baseline-missing-actions-read-permission`対応：下記`resolveApprovedBaseline`手順3が呼び出す`GET actions/runs?check_suite_id=...`は、GitHub Actionsの`GITHUB_TOKEN`権限モデル上、`actions`スコープの読み取り権限を独立に要求するエンドポイントであり、`checks: write`では代替できない。既存3スコープはそのまま維持したうえでの追加のみであり、削除・変更は行わない）。job名`reconcile`・既存3分岐（`ISSUE-<n>`抽出／dependabot許可／それ以外exit 1）判定ロジックの意図は維持したうえで、下記`Derive issue_id`ステップにdependabot分岐直後の`chore/root-cleanup-*`分岐を追加する。
  - checkout: `actions/checkout@v7`に`ref: ${{ github.event.pull_request.base.sha }}`・`fetch-depth: 0`・`persist-credentials: false`を指定し、baseブランチ（trust root）を明示的にcheckoutする（`gate.yml`と同一パターン）。
  - PR headの取得: `git fetch --no-tags origin "pull/${PR_NUMBER}/head:refs/agent-skill-chain/targets/${HEAD_SHA}"`でread-onlyなgit objectとしてのみ取得し、checkoutやビルド対象には含めない（`gate.yml`と同一パターン）。
  - `Detect unmigrated reconcile workflow`ステップ（design-gate指摘`migration-step-has-no-automated-enforcement-or-detection`対応。当初`agent-skill-chain-gate.yml`側の`detect-segments`ジョブへ追加しようとしたが、SPEC.mdスコープ外節が`agent-skill-chain-gate.yml`自体の変更を対象外とするため、design-gate指摘`gate-yml-change-violates-spec-scope-exclusion`対応でこちら（`agent-skill-chain-reconcile.yml`自身の既存ジョブ）へ移設した）: 直前の「PR headの取得」ステップが取得済みのread-onlyなgit object参照に対し、`git show refs/agent-skill-chain/targets/${{ github.event.pull_request.head.sha }}:.github/workflows/agent-skill-chain-reconcile.yml`をread-only参照する（新規checkout・ビルド不要、実行しない）。内容が`on: push`を含む場合（本Issue以前の自己参照構成が当該PR headに残存していることを意味する）、`::warning::`アノテーションを出力する（design-gate指摘`unmigrated-detection-step-lacks-write-permission`対応：PRへのコメント投稿は行わない。理由は、コメント投稿（`issues/{pr_number}/comments`へのPOST/PATCH）には`issues: write`または`pull-requests: write`権限が必須だが、既存`permissions`（`contents: read`, `checks: write`, `pull-requests: read`、上記`actions: read`追加後を含む）にはこれが含まれておらず、本Issueが最小権限・信頼境界の縮小を目的とする設計である以上、既に`checks: write`を保有するこの信頼済みワークフローへさらに書込み権限を追加することは目的に反するため。`::warning::`アノテーションはGitHub Actions実行ログおよび当該PRの「Checks」タブ上に表示され、追加権限なしで可視性を確保できる）。このステップの成否は後続の`Reconcile gates against pushed SHA`ステップ（Check Run発行）へは一切影響させない。既存のいずれのrequired status check（`agent-skill-chain/{spec,design,implementation,validation}-gate`・`verify`）の成否にも影響させず、`.agent-skill-chain/templates/github/provisioning/rulesets/main.json`の変更は伴わない。`agent-skill-chain-gate.yml`（PR gate本体）は本Issueの変更対象に含めない。これは`git worktree list`等に依存する人手中心の確認手順（PLAN.md変更単位#7）を代替するものではなく、開いている全PRについてsynchronizeのたびに自動的に未移行状態を検知・可視化する手段を追加するものである。
  - `npm ci && npm run build`: baseブランチ（trust root）由来のCLIソースに対して行う。
  - `Derive issue_id`ステップ（id: `ctx`）: `env.BRANCH`を`${{ github.event.pull_request.head.ref }}`から取得する。dependabot分岐は新規`env.PR_AUTHOR: ${{ github.event.pull_request.user.login }}`を直接参照し、`gh api repos/{owner}/{repo}/pulls?head=...`によるAPI照会ステップを削除する（`pull_request_target`イベントは常にPRコンテキストを伴うため、`push`イベント時代に必要だった代替手段が構造的に不要になる）。既存の`ISSUE-<n>`抽出・非対応ブランチ名でのexit 1・3分岐の字句順序はそのまま維持したうえで、dependabot分岐の直後に`chore/root-cleanup-*`（`agent-skill-chain-root-cleanup.yml`が作成する短命PRのhead branch prefix）に対する新規分岐を追加し`skip_checks=true`とする（design-gate指摘`root-cleanup-branch-exclusion-dropped`対応）。旧`on: push`版の`branches-ignore: [main, 'chore/root-cleanup-*']`は**pushされたブランチ自身**を対象に除外していたのに対し、`pull_request_target`の`branches`/`branches-ignore`フィルタは常にbaseブランチ側へ適用されるため、この除外は字句をそのまま移植しても機能しない。root-cleanup PRのheadブランチ名（`chore/root-cleanup-<UTC timestamp>`）は`^[^/]+/([0-9]+)-.*`に一致せず、新規分岐を追加しない場合は最終elseでexit 1となりジョブが失敗する（Check Run発行には影響しないが、スコープ検査で`human_required`停止した際の追加pushでsynchronizeイベントが発火した場合に不要な失敗を生む）。この分岐は真正性検証を伴わないブランチ名prefixの一致のみで判定する（dependabot分岐と異なりPR作成者APIでの追加検証は行わない）。理由：`agent-skill-chain-gate.yml`の`Resolve immutable context`ステップも同一の`^[^/]+/([0-9]+)-.*`抽出を要求するため、`chore/root-cleanup-*`という命名のブランチにはそもそも`agent-skill-chain/{spec,design,implementation,validation}-gate`のCheck Runが発行され得ず、`skip_checks=true`は「reconcileによる照合対象が元々存在しない」ことを反映するfail-safeなno-opであり、旧`on: push`版のbranches-ignoreと同一の判定粒度（ブランチ名prefix一致のみ）を維持するにとどまる。`env.GH_TOKEN`（`github.token`）・`env.REPO`（`github.repository`）・`env.OWNER`（`github.repository_owner`）は変更しない。
  - `Reconcile gates against pushed SHA`ステップ: 対象SHA引数を`${{ github.event.pull_request.head.sha }}`から、新設のpr_number引数を`${{ github.event.pull_request.number }}`から取得し、`gate-reconcile.sh <issue_id> <target_sha> <pr_number>`として呼び出す。
- `gate-reconcile.sh` / `gate reconcile`サブコマンド（既存、シグネチャのみ第3引数`pr_number`を追加）: 既存の`<issue_id> <target_sha>`に加え、`pr_number`（GitHub呼び出し経路では必須、ローカルモード・省略時は空文字列として渡す）を受け取る。`artifactDigestAtSha`が`git show <sha>:<path>`によるread-only参照のみで実装されている既存部分は無変更。
- `resolveApprovedBaseline`（`src/commands/gate.ts`の`reconcile()`内部、前ラウンドで確定済みの設計を維持）: `config.coordination.backend === 'github'`の場合のみ、承認済み基準（`approved_artifacts`）を`reviewFilePath`（issueブランチ上でwriterが改変可能なコミット済みファイル）からではなく、GitHub Check Run発行履歴から復元する。
  1. `pr_number`がワークフローから渡されている場合、`GET pulls/{pr_number}/commits`でPRのコミット列（GitHub側の正本）を直接取得する（`pull_request_target`イベントが常にPR番号を判明させるため、`commits/{target_sha}/pulls`によるPR特定を経由しない）。`pr_number`が空（将来の他呼び出し経路・ローカル検証用途）の場合のみ、従来通り`GET commits/{target_sha}/pulls`でPRを特定するフォールバックを行う。いずれの経路でも対応するPRが0件の場合は「承認済み基準なし」として当該ゲートの照合をスキップする（I8の安全側）。
  2. target_shaより前のコミットを新しい順に並べ、`GET commits/{sha}/check-runs?check_name=<config.checks[gateId]>`で照会し、`conclusion=success`な候補を新しい順に走査する。
  3. （source identity検証）各候補について`GET actions/runs?check_suite_id=<候補の`check_suite.id`>&head_sha=<候補コミットのSHA>`を照会し、応答`workflow_runs[].path`（発行元workflow定義ファイルのリポジトリ内パス、GitHub管理の権威あるメタデータで偽装不能）が既知のtrusted workflowファイルパス（`.github/workflows/agent-skill-chain-gate.yml`・`.github/workflows/agent-skill-chain-reconcile.yml`）のいずれかと一致すること、かつ同応答`workflow_runs[].event`（GitHub Actionsが記録する、当該workflow runを実際に起動したイベント種別。同じくGitHub管理の権威あるメタデータで偽装不能）が`pull_request_target`と一致することの両方を検証する。いずれか一方でも一致しない候補は棄却し次候補を試す。path不一致の候補は`checks: write`権限を持つ攻撃者の新規untrustedワークフローファイルが同名で偽装発行したCheck Runであり、path一致だがevent不一致（典型的には`push`）の候補は、本Issueのマージ前から存在しmainを未取込のissueブランチにファイル名として同一のまま残存する旧`on: push`版`agent-skill-chain-reconcile.yml`（自己参照構成のまま）が発行した偽装Check Runである（詳細は下記「障害・ロールバック考慮」の移行ギャップ節を参照）。（design-gate指摘`check-suite-id-filter-unverified-may-return-multiple-workflow-runs`対応）`check_suite_id`が`GET actions/runs`の有効な絞り込みクエリであることを本リポジトリの実在check_suite_id（`83423026122`）に対し`gh api repos/{owner}/{repo}/actions/runs?check_suite_id=83423026122`を実行して実測確認済み：`total_count=1`・当該check_suite_idに対応する単一のworkflow run（`path: .github/workflows/agent-skill-chain-reconcile.yml`, `event: push`）のみが返却された。同一head_shaに対し`check_suite_id`を指定しない`GET actions/runs?head_sha=<sha>`は6件のworkflow run（`agent-skill-chain-gate.yml`・`agent-skill-chain-ci.yml`・`agent-skill-chain-self-test.yml`・`agent-skill-chain-reconcile.yml`が異なる`event`で複数回）を返すため、`check_suite_id`による絞り込みが候補の一意特定に必須かつ有効に機能することを確認した。
  4. source identity検証を通過した候補の`output.text`を`canonicalJson(GateReport)`としてパースし`approved_artifacts`を復元する。GitHub Check Run APIの`output.text`は65,535文字が上限であり、超過時は切り詰められ不正なJSONになりうる。パース失敗時（切り詰めによるものを含む）も当該候補を棄却し次候補を試す。候補が尽きれば「承認済み基準なし」としてスキップする（I8）。
  5. （1）〜（4）いずれかのGitHub API呼び出し自体がエラー応答した場合（0件応答ではなくAPI呼び出し失敗）は、reconcileコマンド全体を失敗させる（`CliError`）。判定不能を「変化なし」や「スキップ」に倒さない。
  - 戻り値の型（design-gate指摘`no-baseline-treated-as-vacuously-unchanged-reissues-success`対応）: `resolveApprovedBaseline`は「承認済み基準あり」と「承認済み基準なし」を、呼び出し側が誤って同一視できない形で区別して返す。具体的には`{ found: true; approvedArtifacts: { path: string; digest: string }[] }`（`approvedArtifacts`が空配列である場合も含みうる正当な値）と`{ found: false }`（上記手順1でPRが0件、または手順2〜4で候補が尽きた場合）の判別可能な構造体（discriminated union）とし、`approvedArtifacts: []`という値と`found: false`を型レベルで衝突させない。呼び出し側（下記`reconcile()`の3分岐、AC-5）が`found`を先に分岐せず`approvedArtifacts`をいきなり`.some()`等へ渡すことを構造的に防ぐ。
  - `reconcile()`側の3分岐（GitHubモード限定、design-gate指摘`no-baseline-treated-as-vacuously-unchanged-reissues-success`対応）: 旧`report.gate.approved_artifacts.some(...)`への単純置換ではなく、`resolveApprovedBaseline`の戻り値を先に分岐する。
    1. `found: false`（承認済み基準が一度も存在しない。当該ゲートについて過去に一度もtrusted workflow由来のsuccess Check Runが発行されていない、またはPRが特定できない）の場合、**当該ゲートの照合を丸ごとスキップする**：`report`（`reviews/<gate>.yaml`）への書込みを行わず、`publishCheckRun`も一切呼び出さない（既存Check Run状態・`refreshed`/`invalidated`集計のいずれも変更しない）。`downstreamInvalidated`は現在値を変更せず次ゲートへそのまま引き継ぐ（このゲート自身の判定不能を理由に、既に確定している上流ゲートの無効化伝播を打ち消したり、下流ゲートへ誤って伝播させたりしない）。これにより、レビュー証跡が一度も存在しないSHAに対して`changed=false`（＝success再発行）が導出されることを構造的に防ぐ（AC-5）。
    2. `found: true`の場合のみ、従来通り`changed = downstreamInvalidated || approvedArtifacts.some((artifact) => artifactDigestAtSha(...) !== artifact.digest)`を評価し、`changed`の真偽に応じた既存の無効化／再発行分岐（`report.gate.conformance`等の更新、`publishCheckRun`呼び出し）をそのまま適用する（AC-2、既存の正規フロー継続）。**`changed: false`側（success再発行）に限り、`publishCheckRun`へ渡す直前に`report.gate.approved_artifacts`を、ローカル`report`（`reviewFilePath`から読み込んだ、writerが改変可能なオブジェクト）の値ではなく、本分岐の`approvedArtifacts`（`resolveApprovedBaseline`の戻り値、trusted baseline自体）で上書きしてから`canonicalJson(report)`を構築する（design-gate指摘`reconcile-republish-embeds-untrusted-local-approved-artifacts-enabling-two-push-baseline-poisoning`対応。理由・攻撃シナリオは下記「障害・ロールバック考慮」失敗モード8参照）。`changed: true`側（`action_required`発行）はこの上書きを行わない：発行される`conclusion`が`action_required`であり`resolveApprovedBaseline`の候補条件（`conclusion=success`）を満たさないため、後続のいかなる`resolveApprovedBaseline`呼び出しからもこのCheck Runがtrusted baseline候補として採用されることはなく、ローカル`report`由来の値がembedされていても信頼汚染は生じない。**
  - ローカルモード（`config.coordination.backend === 'local'`）の`reconcile()`は無変更。`reviewFilePath`が指す`reviews/<gate>.yaml`（Git管理下）から読み取った`report.gate.approved_artifacts`を引き続きそのまま`changed`判定の入力として用いる（AGENTS.md I6。ローカルモードでは`reviews/<gate>.yaml`自体が正本であり「候補が尽きる」という状態が存在しないため、上記3分岐は適用しない）。

### 依存関係

```text
PRのheadブランチへのpush（synchronize） → pull_request_target トリガー
                                            │
                                            ▼
agent-skill-chain-reconcile.yml（trusted, PRのbaseブランチ=defaultブランチのYAMLとして解決される）
  → baseブランチをcheckout（trust root, ref = pull_request.base.sha, persist-credentials: false）
  → PR head SHAをgit fetchでread-onlyなgit objectとして取得（checkoutしない）
  → baseブランチ由来のCLIをbuild（npm ci / npm run build）
  → gate-reconcile.sh <issue_id> <pull_request.head.sha> <pull_request.number>
      → resolveApprovedBaseline（GitHubモードのみ）:
          pulls/{pr_number}/commits → commits/{sha}/check-runs
            → actions/runs?check_suite_id=<candidate>（発行元workflowファイルパスのsource identity検証。trusted pathと不一致なら次候補へ）
            → 承認済み基準の再構築
      → artifactDigestAtSha: git show <pull_request.head.sha>:<path> でのみ内容参照
  → GitHub Check Runs API（gh api）
```

循環依存は無い。`resolveApprovedBaseline`が照会するCheck Run履歴・PRメタデータ・workflow run発行元パスもGitHub API（正本）からのみ取得し、対象SHAのブランチ内容物（ファイル・コミットメッセージ）を承認済み基準としては参照しない。候補Check Runがtrusted workflowファイルパス以外から発行されていた場合は、同一`checks: write`権限を持つ任意のワークフローファイル（攻撃者が新規追加したものを含む）を承認済み基準の情報源として信頼しない（AGENTS.md「ゲートの継承・無効化」節が禁じる、App IDのみをsource trustの証明とする構成を回避する）。

## 関連ADR

```yaml
related_adrs:
  - id: ADR-0016
    relation: adopts
```

## 障害・ロールバック考慮

- 想定される失敗モード（1）: PRのbaseブランチがdefaultブランチ以外へ変更（retarget）された場合、ジョブ条件`if: github.event.pull_request.base.ref == github.event.repository.default_branch`によりreconcileは起動しない。影響は当該PRのreconcileが実行されないことに限られ（fail-safe、Check Run偽造の方向には倒れない）、baseがdefaultブランチへ戻れば次のsynchronizeで復旧する。
- 想定される失敗モード（2）: 攻撃者が自分のPR head側で`agent-skill-chain-reconcile.yml`自体を改変・削除しても、`pull_request_target`はワークフローYAML自体を常にPRのbaseブランチ（trusted）から読むため、この改変は一切実行に反映されない。`workflow_run`+trigger.yml分離案が抱えていた「trigger.yml自体が攻撃者の完全な制御下にあるpushから読まれる」という自己参照問題は、trigger.ymlという別ファイルを作らない本設計では構造的に発生しない。
- 想定される失敗モード（3）: `resolveApprovedBaseline`のGitHub API照会（`pulls/{pr_number}/commits`・`commits/{sha}/check-runs`・source identity検証用の`actions/runs?check_suite_id=`、`pr_number`未提供時は`commits/{sha}/pulls`も含む）自体がエラー応答（レート制限・一時的な障害等）した場合、reconcileコマンド全体を失敗させる（判定不能を「変化なし」や無効化スキップへ倒さない、I8）。0件応答（対応PR無し・過去のsuccess Check Run無し・trusted workflowパスと一致する候補が0件）は既存同様に当該ゲートの照合をスキップする安全側動作とし、API呼び出し失敗とは区別する。
- 想定される失敗モード（4）: 攻撃者が自分のPR head側へ`checks: write`権限を持つ全く新規のuntrustedワークフローファイル（例: `.github/workflows/evil.yml`）を追加し、`agent-skill-chain/{spec,design,implementation,validation}-gate`と同名の偽Check Run（`conclusion=success`、改ざんした`approved_artifacts`を`output.text`へ埋め込み）を発行した場合、`resolveApprovedBaseline`のsource identity検証は当該Check Runの発行元workflow run `path`を`evil.yml`と特定し、trusted workflowファイルパスのいずれとも不一致のため**reconcile自身が承認済み基準として採用することは**棄却する。**ただし、この防御は`reconcile()`内部の判断にのみ及ぶ。同じ偽Check Runは、GitHub native rulesetのrequired status check（`context`名一致のみで判定、後述「本設計のスコープ境界」節参照）自体は満たしてしまうため、マージ可否判定そのものを偽造から守るものではない**。
- 想定される失敗モード（5）: GitHub Check Run APIの`output.text`フィールドの65,535文字上限により、承認済み成果物点数が多いゲート（実装・検証ゲート、`docs/system-spec/`のような多数ファイルパッケージを含むゲート）では`canonicalJson(GateReport)`が切り詰められ不正なJSONになりうる。`resolveApprovedBaseline`は当該候補のJSONパース失敗時点で「承認済み基準として使用不可」と判定し次候補を試す。API呼び出し自体のエラー応答（失敗モード3、reconcileコマンド全体を失敗させる）とは区別し、こちらは次候補フォールバックとして扱う（I8の安全側）。
- 想定される失敗モード（6）: 本修正のマージ前から存在しmainを未取込のissueブランチへ、writer権限を持つ者が旧`on: push`版`agent-skill-chain-reconcile.yml`（当該ブランチ内にファイル名として同一のまま残存、自己参照構成）を改変してpushし、`agent-skill-chain/{spec,design,implementation,validation}-gate`のいずれかの偽Check Run（`conclusion=success`）を発行させた場合、`resolveApprovedBaseline`のsource identity検証はpathは一致するがevent（`workflow_runs[].event`）は`push`であり`pull_request_target`と不一致であることを検出し、**reconcile自身が承認済み基準として採用することは**棄却する（下記「移行ギャップについて」参照）。**ただし失敗モード（4）と同様、この防御はreconcile内部判断にのみ及び、当該偽Check Run自体がGitHub native rulesetのrequired status checkを満たしマージ可能状態にしてしまうこと自体は防がない（後述「本設計のスコープ境界」節参照）。**
- 想定される失敗モード（7、design-gate指摘`root-cleanup-branch-exclusion-dropped`対応）: 旧`on: push`版の`branches-ignore: [main, 'chore/root-cleanup-*']`はpushされたブランチ自身を対象に除外していたのに対し、`pull_request_target`の`branches`/`branches-ignore`フィルタは常にbaseブランチ側へ適用されるため字句をそのまま移植しても機能しない。上記「コンポーネント構成」`Derive issue_id`ステップに追加した`chore/root-cleanup-*` prefix分岐（`skip_checks=true`）により、`agent-skill-chain-root-cleanup.yml`が作成する短命PRのsynchronizeイベント（スコープ検査で`human_required`停止した際の追加pushで発生しうる）でも`ISSUE-<n>`抽出失敗によるジョブ失敗（exit 1）を起こさない。この分岐が存在しない場合でもCheck Run偽造の方向には倒れない（fail-safeなジョブ失敗に留まる）ため、本失敗モードはI2以外の運用上のノイズに関するものであり、Gate偽造の脅威モデルには影響しない。
- 想定される失敗モード（8、二段階pushによる承認済み基準汚染への対応、design-gate指摘`reconcile-republish-embeds-untrusted-local-approved-artifacts-enabling-two-push-baseline-poisoning`対応）: `resolveApprovedBaseline`によるtrusted baseline照合（AC-5）は`changed`判定の入力としては機能していても、成功再発行時に`publishCheckRun`へ渡す`output.text`自体がローカル`report`（`reviewFilePath`、writerが改変可能）の`approved_artifacts`をそのまま埋め込んでいた場合、次の2回のpushにまたがる攻撃が成立しうる：push1で、実成果物のcontentは不変のまま（digest D1）`reviews/<gate>.yaml`の`approved_artifacts`のみを攻撃者が選ぶ将来のdigest D2へ書き換える。`changed`判定はtrusted baseline（実content一致、digest D1）に対し行われるため`changed=false`となりsuccess Check Runが再発行されるが、その`output.text`にローカル`report`由来の汚染済みD2がそのまま埋め込まれてしまう。push2で、実成果物を実際にdigest D2となる未レビュー内容へ書き換える。`resolveApprovedBaseline`はpush1で発行された直近のsuccess Check Run（source identity検証も通過する正規発行）をbaselineとして採用し、その`output.text`に埋め込まれたD2を「承認済み」とみなす。実content（D2）と一致するため`changed=false`となり、未レビューの内容にsuccessが再発行されてしまう。上記「コンポーネント構成」`reconcile()`側の3分岐で追加した是正（`changed: false`分岐に限り、`publishCheckRun`へ渡す直前に`report.gate.approved_artifacts`を`resolveApprovedBaseline`の戻り値（trusted baseline自体）で上書きする）により、push1で発行されるCheck Runの`output.text`には常にD1（trustedな値）のみが埋め込まれ、ローカル`reviews/<gate>.yaml`への改ざん（D2）は反映されない。したがってpush2の時点で`resolveApprovedBaseline`が採用するbaselineはD1のままであり、実content（D2）との比較で`changed=true`となり当該ゲートは無効化される（AC-5 Thenが要求する「実際にはレビュー未了である当該変更を『変化なし』とは判定せず、成功を再発行しない」を満たす）。
- ロールバック手順: `agent-skill-chain-reconcile.yml`の`on:`を`pull_request_target`から`push`へ戻せば旧構成（本Issueが解消しようとする脆弱性を含む）に復帰できる。単一commitのrevertで復元可能。`resolveApprovedBaseline`導入分（`output.text`のtrusted baseline置換を含む）は独立にrevert可能で、revertすると`reconcile()`の承認済み基準取得・republish内容はいずれも`reviewFilePath`読み込みへ戻る（AC-5が解消する脆弱性が再発する点に留意）。
- 移行ギャップについて（`workflow_run`分離案からの改善点、design-gate指摘`migration-gap-claim-false-for-existing-push-triggered-branches`対応）: `pull_request_target`版`agent-skill-chain-reconcile.yml`自体は、マージ後は既存の開いているPRを含め次のsynchronizeイベントから正しくtrustedな構成で起動する。しかしGitHub Actionsの`on: push`トリガーは常にpushされたブランチ自身に存在するワークフロー定義を読む仕様であるため、本修正のマージ前から存在しmainを未取込のissueブランチは、そのブランチ内に旧`on: push`版`agent-skill-chain-reconcile.yml`（`permissions: checks: write`を含む自己参照構成）をファイル名として同一のまま保持し続ける。そのブランチへの通常push（synchronize）は、mainに新設された`pull_request_target`版ワークフローの起動とは独立に、ブランチ内に残る旧`on: push`版ワークフローも同時に発火させ、旧版は攻撃者が制御するブランチ内容から読み込まれるため引き続き改変・実行され得る。したがって「既存issueブランチに新しいmain内容を取り込ませる運用上の追従手順は不要」という前ラウンドの想定は誤りであり（前ラウンドは`pull_request_target`版の起動条件のみを検証し、ブランチ内に残存する旧`on: push`版ファイル自体が独立に発火し続けることを見落としていた）、本設計はこれを次の二段構えで是正する。
  1. （技術的な防御、上記「コンポーネント構成」resolveApprovedBaseline手順3参照）source identity検証をpath一致に加えてevent一致（`pull_request_target`）も要求する形へ拡張する。旧`on: push`版ワークフローが発行するCheck Runの`workflow_runs[].event`は`push`であり、path一致のみでは見逃されていたこの偽装候補も、event不一致により確実に棄却される。これにより、旧版ワークフローが引き続き起動・改変され得るとしても、その発行するCheck Runが後続reconcileの「承認済み基準」として採用され信頼が汚染されることは技術的に防がれる（候補が尽きれば「承認済み基準なし」としてスキップする、I8の安全側）。
  2. （運用上の必須手順）技術的防御(1)は承認済み基準の信頼汚染を防ぐが、旧版ワークフロー自体がissueブランチ内に残存し攻撃者の制御下で実行され得る状態自体は排除しない。したがって本Issueの完了条件として、進行役は本修正のマージ後、当該時点で開いている全issueブランチについて`git merge origin/main`（またはrebase）によるmain取込みを実施し、ブランチ内容から旧`on: push`版ファイルを除去することを必須の展開手順とする（対象ブランチの洗い出し・確認方法は`PLAN.md`変更単位#7、確認結果の記録方法は`VALIDATION.md`で確定する）。取込みが完了するまでの間、当該ブランチへの新規pushについては(1)の技術的防御により信頼汚染は防がれるものの、旧版ワークフロー自体が実行され得る状態が残るため、進行役はそのブランチのreconcile結果を完全に信頼できるものとしては扱わない。
- 影響を受ける既存機能: `agent-skill-chain/{spec,design,implementation,validation}-gate`のreconcileによる再発行・無効化が主。`agent-skill-chain-gate.yml`（PR gate本体）は本Issueの変更対象に含まれない（SPEC.mdスコープ外節）。未移行reconcile検知ステップ（上記「コンポーネント構成」参照）は`agent-skill-chain-reconcile.yml`自身の既存ジョブへ追加する。既存のsegment検出・issue_id解決・レビューworker起動ロジック（`agent-skill-chain-gate.yml`側）自体は無変更。`agent-skill-chain-ci.yml`（CI検査）は本設計の変更対象に含まれず影響を受けない。`issues/<n>/.agent-skill-chain/reviews/<gate>.yaml`自体の生成・スキーマは無変更（ローカルモードの正本、GitHubモードでもワーカー向けスキャフォールドとして引き続き使用）。

## 本設計のスコープ境界（新規ワークフローファイルによる直接偽造経路、design-gate指摘 `new-arbitrary-workflow-file-bypasses-fix` / `source-identity-event-check-does-not-prevent-direct-forgery-on-unmigrated-branches` 対応）

design-gateのstrictレビュー2名がいずれも、上記の失敗モード（4）・（6）が実際には解消していない残存経路を指摘した：攻撃者が自分のissueブランチへ、`agent-skill-chain-reconcile.yml`・`agent-skill-chain-gate.yml`のいずれとも無関係な**全く新規のワークフローファイル**（例: `.github/workflows/evil.yml`、`on: push`、`permissions: checks: write`を自身のYAML内で明示宣言）を追加してpushした場合、そのワークフローは攻撃者が完全に制御する内容のままそのpushをトリガーに実行され、GitHub Checks APIを直接呼び出して`agent-skill-chain/{spec,design,implementation,validation}-gate`と同名のCheck Run（`conclusion=success`）を任意のコミットSHAへ発行できる。この経路は`agent-skill-chain-reconcile.yml`にも`resolveApprovedBaseline`にも一切触れないため、本設計のいずれの防御（`pull_request_target`化・source identity検証）によっても阻止されない。

この経路が実際に成立することを本リポジトリで実測確認した：現在適用済みのGitHub native ruleset（`gh api repos/{owner}/{repo}/rulesets/19276510`で確認）の`required_status_checks`は`context`（Check Run名）のみを指定し、発行元Appを限定する`integration_id`を一切含まない。

```json
"required_status_checks": [
  {"context": "agent-skill-chain/spec-gate"}, {"context": "agent-skill-chain/design-gate"},
  {"context": "agent-skill-chain/implementation-gate"}, {"context": "agent-skill-chain/validation-gate"},
  {"context": "verify"}
]
```

したがって、標準`GITHUB_TOKEN`（全workflowが共有する単一App ID、`app.slug: github-actions`）で発行されたCheck Runであれば、それが`agent-skill-chain-gate.yml`由来か攻撃者の`evil.yml`由来かをGitHub側のマージ可否判定（required status check）は区別しない。`resolveApprovedBaseline`のsource identity検証（`workflow_runs[].path`・`.event`の照合）は、**reconcile自身が「承認済み基準として何を信頼するか」という内部判断のみを保護する**ものであり、GitHub nativeのrequired status check自体がどのCheck Runを「合格」とみなすかには一切関与しない。両者は独立した経路であり、後者を守らない限り前者の防御だけでは実際のマージ可否判定を偽装から守れない。

この経路の技術的な完全解消は、Check Run発行主体をrulesetの`integration_id`で専用GitHub Appへ限定する（標準`GITHUB_TOKEN`では当該Appの identityを騙れなくする）ことを要し、これは`docs/adr/ADR-0013-trusted-gate-check-materialization.md`（`status: proposed`）が提案する`dedicated_app`/`required_workflow`enforcement backendの役割そのものである。ADR-0013に対応する実装（`src/lib/trust-backend.ts`の`resolveDedicatedAppBackend`、`src/commands/setup.ts`の`renderRulesetWithDedicatedApp`、`.github/workflows/agent-skill-chain-trusted-gate.yml`）は既にコードベースに存在するが、これを有効化するには（a）専用GitHub Appの作成・installation（ADR-0013 Consequences節が明記する一度限りの運用手続き）、（b）`agent-skill-chain setup ruleset`の再実行による本リポジトリrulesetへの`integration_id`反映、の2つが必要であり、いずれも本Issueが変更するコード・設定ファイルの範囲外である。

SPEC.mdのスコープ外節は「`docs/adr/ADR-0013`...が提案する専用GitHub App／Required Workflowによる強制enforcement backend全体の導入」を明示的に本Issueの対象外としている。新規ワークフローファイルによる直接偽造という経路の解消は、この既に除外されたADR-0013の役割に完全に含まれる（ADR-0013が未承認・未活性化である限り、reconcile側の設計をどれだけ強化してもこの経路は残り続ける、という意味で本質的に独立である）。したがって本設計・本ADR-0016が実際に解消するのは、SPEC.md要求冒頭の包括的な文言（「いずれのCheck Runも…成功として発行できないようにしたい」）全体ではなく、要件節が個別に列挙する各項目のうち(a)reconcile自身の判定ロジックの自己参照（AC-1のGiven/Whenが具体的に記述する範囲）と(b)reconcileが参照する承認済み基準データの改ざん耐性（AC-5）に限定される。この境界は本Issueが新たに生む欠陥ではなく、`agent-skill-chain-gate.yml`（既に安全なパターンの参照実装とされる既存ファイル）自身も同一の経路（新規untrustedワークフローファイルによる直接偽造）に対して本Issue以前から同じ限界を持っており、本Issueのスコープでは変更しない。

この残存ギャップ（新規untrustedワークフローファイルによる直接Check Run偽造、`new-arbitrary-workflow-file-forgery-gap`）は、`check-run-output-patch-poisoning-gap`・AC-6のref/branch値自体の真正性検証ギャップ（`ac6-ref-name-spoofing-gap`）と同様、SPEC.mdの「未決事項」節に既知の限界として記録する（design-gate指摘`new-arbitrary-workflow-file-forgery-not-recorded-in-spec`対応、SPEC.md参照）。

## Check Run発行履歴を信頼源とすることの限界（多層防御としての位置づけ、design-gate指摘 `check-run-output-mutable-via-same-app-patch-poisons-approved-baseline` 対応）

design-gateのstrictレビューで、`resolveApprovedBaseline`のsource identity検証（発行元workflow runの`path`・`event`照合）が防ぐのは「新規のCheck Run発行によるsource偽装」のみであり、「既に発行され承認済みとなっているCheck Runの`output.text`を、発行後に同一App identity（標準`GITHUB_TOKEN`が認証する単一App、`app.slug: github-actions`）を持つ別の（攻撃者が新規追加した）ワークフローファイルから、Checks APIのUpdate a check run（`PATCH /repos/{owner}/{repo}/check-runs/{id}`、`checks: write`権限）で直接書き換える」経路を防がない、という指摘を受けた。

この指摘は正しい。PATCHによる`output.text`の書き換えは、対象Check Runとcheck_suite・その発行元workflow run（`workflow_runs[].path`・`.event`）との関連付けを変更しない。したがって上記「コンポーネント構成」`resolveApprovedBaseline`手順3のsource identity検証（path一致・event一致）は書き換え後もそのまま通過してしまい、手順4は改ざん済みの`approved_artifacts`をそのまま「承認済み基準」として復元する。攻撃者は自分のissueブランチへ`checks: write`権限を持つ新規ワークフローファイル（例: `evil.yml`、`on: push`）を追加してpushし、`GET commits/{sha}/check-runs`で列挙した過去の正規Check Run（例: design-gateの`conclusion=success`）のIDを特定したうえで、そのCheck Runの`output.text`だけを、改ざん後に自分がpushする成果物のdigestへ一致するよう書き換えることができる。

この経路をコード側の追加検証（例: `output.text`自体への署名・タイムスタンプの埋め込み等）で完全に閉じることは、本Issueのスコープでは行わない。GitHub Check Run APIそのものが「同一App identityを持つ任意のワークフローが既存Check Runを更新できる」という仕様である以上、GitHub Actions内で完結する対策には原理的な限界があり、これを技術的に完全に閉じるには（新規ワークフローファイルによる直接偽造の経路と同様に）Check Run発行・更新主体をrulesetの`integration_id`で専用GitHub Appへ限定する強制enforcement backend（`docs/adr/ADR-0013-trusted-gate-check-materialization.md`、既に本Issueのスコープ外、上記「本設計のスコープ境界」節参照）を要するためである。

したがって、本設計における`resolveApprovedBaseline`のGitHub Check Run発行履歴照合は、ゲート合否の完全な偽造耐性を単独で保証する仕組みではなく、多層防御の一つ（pushトリガーのYAML自己参照という当初の脆弱性を閉じる、AC-1〜AC-4の目的）として位置づける。本Issueが実際に運用されている実態に即して言えば、ゲート合否判断の実効的な信頼源は、GitHub Actions内でのCheck Run発行そのものではなく、進行役がwriter権限とは分離された別のcredential（trusted recorder）を用いてGitHub PR Review APIへ投稿するreview evidence（本Issue自身の設計ゲート審査も、この経路——`gh pr view --json reviews`で参照可能なPR review——によって行われている）である。reconcileのCheck Run照合は、この実効的な信頼源を代替するものではなく、GitHub Actions内で完結する自動化層としての防御を補完するものと位置づける。

この残存リスク（Check Run発行後の`output.text`改ざんによる承認済み基準の汚染、`check-run-output-patch-poisoning-gap`）は、AC-6のref/branch値自体の真正性検証ギャップ（`ac6-ref-name-spoofing-gap`）・移行ギャップ（migration-gap）と同様、SPEC.mdの「未決事項」節に既知の限界として記録する（SPEC.md参照）。
