# DESIGN: worker-selection関連ファイルの禁止参照（セクション番号参照）を是正しmainのCIを復旧する

- Issue: `ISSUE-325`
- 対応する SPEC: `SPEC.md`

## 要件 → 設計要素の対応表

| 要件 / AC-ID | 対応する設計要素 | 備考 |
|---|---|---|
| `AC-1` | 是正方針A（`worker-launch.sh`）／是正方針B（`worker-selection.ts`） | 両方針とも「参照の除去」を採用（下記「禁止参照の解消方針」参照） |
| `AC-2` | 是正方針A／是正方針B（設計判断の温存確認） | 除去後も設計判断がコメント本文に残ることを設計要素として明示する |
| `AC-3` | 変更範囲の限定（コメントのみ、ロジック不変） | ソースの実行内容を変更しないことを設計上の制約とする |
| `AC-4` | 是正方針B（`worker-selection.ts`）の全面適用（1行目・26行目付近・52行目付近・70行目付近の計4箇所）＋回帰防止テストの拡張 | 是正方針Bは1行目の `DESIGN.md §選択解決の設計`／`SPEC.md AC-1・AC-2・AC-3・AC-9` 部分の削除に加え、26行目付近の「本 Issue」文言・`SPEC.md スコープ外` 参照の書き換え、52・70行目付近の受入条件ID単独括弧書き（`SPEC.md ` 接頭辞なし）の削除を含む。4箇所とも「Issue完了後に破棄される `SPEC.md`／`DESIGN.md` への陳腐化参照」という同一根拠に基づく同一是正方針内の作業として扱う。回帰防止テスト（`test/unit/worker-selection-reference.test.ts`）の恒久検査への拡張も本ACに対応する設計要素とする |

## 責務・境界

### `lint-references.sh` の判定ロジック（前提の確認）

是正方針を決めるにあたり、`.agent-skill-chain/scripts/lint-references.sh`（薄いラッパー）が委譲する agent-skill-chain CLI の `lint references` サブコマンド（実装本体）の判定ロジックを確認した。

- `§` の直後から、空白・句読点・各種括弧のいずれかが現れるまでの文字列をセクション参照として捕捉する（例:「`DESIGN.md §選択解決の設計`」からは「選択解決の設計」を捕捉する）。
- 捕捉した文字列は、検査対象として走査した `.md` ファイル群から抽出した見出しテキスト一覧と照合する。見出しが捕捉文字列で始まる／捕捉文字列が見出しで始まる、または末尾の英数字・ローマ数字風コード（例:「不変条件 I1〜I8」に対する「I7」）を除いた芯部分が一致すれば「見出しテキストで解決できる参照」として許可する。一致する見出しが1つも無ければ禁止参照として報告する。
- 見出し抽出・照合の対象となる `.md` ファイル群は、検査対象として走査されたファイルのうち拡張子が `.md` のものに限られる。デフォルトの走査対象は AGENTS.md・`docs/GLOSSARY.md`・`.agent-skill-chain/{standards,templates,config,schemas,scripts,ci}/`・`src/`・`.github/workflows/` であり、Issue 単位で作成される `SPEC.md`／`DESIGN.md`／`PLAN.md`（リポジトリルート直下、Issue のブランチ上にのみ存在し完了後は破棄される一時成果物）はこの走査対象に含まれない。

この確認から導かれる結論: 該当2箇所の禁止参照「`DESIGN.md §選択解決の設計`」は、そもそも参照先の見出し自体が（a）走査対象外のファイルにあり、かつ（b）Issue 完了後に破棄される一時成果物であるため、恒久的に存在し続ける見出しではない。したがって「見出しテキストで解決できる参照へ書き換える」方針（例えば AGENTS.md 側や `src/` 側の恒久的な見出しを指す形に変更する）は採用しない。仮に一時的に解決可能な見出しへ付け替えても、参照先が Issue 完了後に消える構造は変わらず、次に別の Issue の `DESIGN.md` が作られ同じ見出しが再利用されない限り再び陳腐化するため、根本解決にならない。

採用する方針は、AGENTS.md「成果物の自己完結性」節が定める「外部参照は由来情報としてのみ用い、参照元にはその文脈で必要な契約の要旨を本文へ記載する」原則に従い、**参照そのものを撤去する**方針である。撤去にあたり、参照先が説明しようとしていた契約の要旨が撤去後のコメントから読み取れるかを1箇所ずつ確認する。

### 是正方針A: `.agent-skill-chain/scripts/worker-launch.sh`

該当コメントは「worker context 自体の失敗（設定不正・ティア解決失敗）は、まだ何も起動していない段階のエラーとして扱う（DESIGN.md §選択解決の設計）。」という一文であり、括弧内の参照は同じ文中で既に述べた設計判断（`worker context` 解決失敗を「まだ何も起動していないエラー」として扱う）への末尾の裏付け引用にすぎない。設計判断の内容自体は参照を除いても文中に完全に残っているため、**参照句「（DESIGN.md §選択解決の設計）」を削除するのみ**とし、新たな文言の追記は不要と判断する。

### 是正方針B: `src/lib/worker-selection.ts`（計4箇所）

`src/lib/worker-selection.ts` には、Issue #307 で作成され既に破棄された `SPEC.md`／`DESIGN.md` への陳腐化参照が、ファイル全体を通じて計4箇所存在する。前回改訂の是正方針Bは1行目のみを対象としていたが、strict design-gateレビュー（2/2レビュア）の指摘「同一の根拠（Issue完了後に破棄される Issue 単位の `SPEC.md` の受入条件ID参照は陳腐化する）が26/52/70行目にも等しく当てはまるのに対応が記載されていない」を受け、対象を4箇所全体へ拡張する。4箇所とも「AGENTS.md『参照・コメントの陳腐化防止』節が定める、ソースコードコメントの追跡識別子として認めるのは Issue ID のみという規約に抵触する陳腐化参照」という同一根拠に基づき、同一是正方針内の作業として扱う。

#### 1行目: `正本:` 宣言行

該当コメントはファイル冒頭の「正本:」宣言行であり、`AGENTS.md §設定` / `SPEC.md AC-1・AC-2・AC-3・AC-9` / `DESIGN.md §選択解決の設計` の3つを列挙している（このうち `AGENTS.md §設定` は AGENTS.md に現存する見出し「設定」で解決できるため `lint-references.sh` の禁止参照には該当しない）。`DESIGN.md §選択解決の設計` が指していた「選択解決の設計」の内容（アダプタ・ティア・reasoning_effort の解決順序、責務分担）は、同ファイル内の `resolveWorkerSelection` 直前の docstring（解決順序：セグメント別上書き→`worker.adapter`→既定 human、ティア対応表からの具体モデル解決は別関数が担う旨）に既に完全な形で記載されている。`SPEC.md AC-1・AC-2・AC-3・AC-9` 部分が説明しようとしていた内容（当該モジュールが `SPEC.md` のどの受入条件に対応するかという由来情報）は、モジュール自体の責務理解に必須ではなく、1行目に続くモジュール概要のコメント本文（config とセグメント名だけから adapter・model_tier・reasoning_effort・モデル文字列を決める、という記述）で責務は既に自己完結して説明されているため、代替の書き下しは不要と判断する。

したがって、`// 正本: AGENTS.md §設定 / SPEC.md AC-1・AC-2・AC-3・AC-9 / DESIGN.md §選択解決の設計` の1行から、**`DESIGN.md §選択解決の設計` 部分と `SPEC.md AC-1・AC-2・AC-3・AC-9` 部分の両方を削除する**。削除後の1行目は `// 正本: AGENTS.md §設定` のみとなる。

#### 26行目付近: `ModelTierTable` 直前のdocstring

該当コメントは「ティア対応表（`worker.model_tiers`）。ティア名をキーとし、値は実行系アダプタ名をキーとする具体的なモデル文字列の集合。本 Issue で許容するアダプタキーは `codex` のみ（SPEC.md スコープ外: claude/human 用モデルの追加）。」という記述である。ここには2種類の陳腐化参照が混在する。

- `SPEC.md スコープ外: claude/human 用モデルの追加` — 破棄された `SPEC.md` の対象外節へのスコープ参照。
- `本 Issue で許容する` — 「本Issue」という文言が Issue #307 を指して書かれており、Issue #307 完了後に別の Issue（本 Issue #325 を含む、以降このコメントを読む任意の Issue）がこのファイルを扱う際、あたかも「今読んでいる Issue」を指しているかのように誤読される自己参照的陳腐化である。

この記述が伝えていた実質的な意味は「`resolveModelForTier` が扱うアダプタキーは現時点では `codex` のみであり、`claude`／`human` 用のティア対応表は現時点で未定義である」というコード自体の現状の対応範囲に関する事実であり、Issue 番号にも `SPEC.md` にも依存せず恒久的に成立する。したがって、`SPEC.md` へのスコープ参照と「本 Issue」という自己参照的文言を削除し、Issue番号に依存しない恒久的な表現へ書き換える。書き換え後の文例：「具体的なモデル文字列の集合。現時点で対応するアダプタキーは `codex` のみであり、claude/human 用のモデル対応表は未定義である。」対応するアダプタキーが `codex` のみであること・claude/human 用が未対応であることという実質情報は書き換え後も維持する（「障害・ロールバック考慮」節で確認手順を定める）。

#### 52行目付近: `resolveWorkerSelection` 直前のdocstring末尾

該当コメント「adapter は『セグメント別上書き → worker.adapter（スカラー、4セグメント共通の既定値）→ human（最終フォールバック）』の順で解決する（AC-1, AC-3）。」のうち、末尾の括弧書き「（AC-1, AC-3）」は破棄された `SPEC.md` の受入条件番号への単独参照であり、`SPEC.md ` という接頭辞を伴わない記法のため `lint-references.sh` の `§` パターン検出では捕捉されない。直前の説明文（adapter の解決順序：セグメント別上書き→`worker.adapter`→既定 human）はこの括弧書きに一切依存せずそれ自体で完結しているため、**括弧書き「（AC-1, AC-3）」を削除するのみ**とし、新たな文言の追記は不要と判断する。

#### 70行目付近: `resolveModelForTier` 直前のdocstring末尾

該当コメント「ティア対応表（`worker.model_tiers`）を引き、ティア名とアダプタ名の組から具体的なモデル文字列を得る（AC-2, AC-9）。対応表そのものが無い・当該ティアのエントリが無い・当該アダプタ用のモデルが無い、のいずれの場合も値を推測せず解決失敗として返す」のうち、末尾の括弧書き「（AC-2, AC-9）」も同様に `SPEC.md ` 接頭辞を伴わない受入条件番号の単独参照である。直前の説明文（ティア対応表からモデル文字列を得る処理、および異常系の扱い）はこの括弧書きに依存せず完結しているため、**括弧書き「（AC-2, AC-9）」を削除するのみ**とし、新たな文言の追記は不要と判断する。

#### 機械検証手段

AC-4 の機械検証コマンドを次のとおり定義する。

```
! grep -nE 'AC-[0-9]+|本 ?Issue' src/lib/worker-selection.ts
```

このコマンドが4箇所すべてを捕捉できる理由は次のとおりである。

- `AC-[0-9]+` は「`AC-` に続く数字」という受入条件ID形式そのものにマッチするパターンであり、1行目（`AC-1・AC-2・AC-3・AC-9`）・52行目（`AC-1, AC-3`）・70行目（`AC-2, AC-9`）のいずれも、`SPEC.md ` という接頭辞の有無に関わらず捕捉する。旧検証コマンド（`! grep -q 'SPEC\.md AC-' ...`）は `SPEC.md ` 接頭辞を前提としていたため52・70行目の接頭辞なし単独括弧書きを素通りしていたが、新パターンは接頭辞への依存を持たないためこの穴を塞ぐ。
- `本 ?Issue`（`本Issue` または `本 Issue`）は26行目の自己参照的文言にマッチする。AGENTS.md が正当と定める追跡識別子としての Issue 参照（例：`// Issue #123: ...`）は「本」を伴わないため誤検知しない。
- `-n`（行番号表示）により、マッチした場合に該当行を特定できる。
- 全体を `!` で反転することで、「ファイル全体を通じて2種類の陳腐化パターンが1件も残存しない」ことを終了コード0の成功として一意に検証する。

#### 回帰防止テストの拡張設計

`test/unit/worker-selection-reference.test.ts` は現状 `assert.doesNotMatch(contents, /DESIGN\.md §/, ...)` のみを検査しており、上記4箇所のうち `DESIGN.md §` パターンに一致する記述（1行目の `DESIGN.md §選択解決の設計` 部分）しか恒久検知できない。実装セグメントでは、`src/lib/worker-selection.ts` を読み込む既存の `for` ループ内（`.agent-skill-chain/scripts/worker-launch.sh` と共通の走査ブロック）に、`worker-selection.ts` 固有の追加アサーションを次の2パターンで追加する。

- `assert.doesNotMatch(contents, /AC-[0-9]+/, ...)` — 1・52・70行目の受入条件ID形式の参照が再発しないことを恒久検証する。
- `assert.doesNotMatch(contents, /本 ?Issue/, ...)` — 26行目の自己参照的「本Issue」文言が再発しないことを恒久検証する。

このアサーションは `worker-selection.ts` のみに適用する（`worker-launch.sh` は AC-1 の対象であり `AC-[0-9]+`／`本 ?Issue` パターンの検査対象ではない）。テストケース名は既存の `ISSUE-325 AC-1: ...` から `ISSUE-325 AC-1/AC-4: ...` 等、AC-4 の恒久検証であることが分かる形へ更新する。

### 変更範囲の限定

いずれの方針も、コメント文字列の一部を削除または書き換えるのみでコードの実行内容（関数シグネチャ・分岐・戻り値）を変更しない。これにより既存の単体テスト（`test/unit`）・結合テスト（`test/integration`）の結果に影響を与えない設計とする。

## 関連ADR

本 Issue はソースコードコメントの表現をAGENTS.mdの既存規約に適合させる是正であり、アーキテクチャ判断を新たに伴わないため、関連 ADR は無い。

## 障害・ロールバック考慮

- 想定される失敗モード: 参照句を削除した結果、当該コメントが説明しようとしていた設計判断が読み取れなくなる（AC-2 未達）。是正方針A・Bのいずれも、削除対象が「既に本文中に記載済みの内容への末尾引用」であることを確認済みであり、この失敗モードは生じない設計とした。
- 想定される失敗モード（1行目固有）: `SPEC.md AC-1・AC-2・AC-3・AC-9` 部分の削除範囲を誤り、直前の `AGENTS.md §設定` 部分まで巻き込んで削除してしまう、あるいは区切り文字（` / `）の削除漏れにより `// 正本: AGENTS.md §設定 / ` のような不正な行末が残る。この失敗モードは AC-4 の検証コマンド `! grep -nE 'AC-[0-9]+|本 ?Issue' src/lib/worker-selection.ts` では「`AC-` に続く数字・『本Issue』文言のいずれも残っていないこと」しか検査できず、`AGENTS.md §設定` の誤削除や区切り文字の残存は別途検出する必要がある。対策として PLAN.md の検証手順に、修正後の1行目全体が `// 正本: AGENTS.md §設定` と完全一致することの目視確認を含める。
- 想定される失敗モード（26行目固有）: 「本 Issue で許容するアダプタキーは `codex` のみ（SPEC.md スコープ外: claude/human 用モデルの追加）」を書き換える際、Issue番号依存の文言・`SPEC.md` 参照を削除することのみに気を取られ、「対応するアダプタキーは `codex` のみであり claude/human 用は未対応である」という実質的な事実情報自体を落としてしまう（例：単に括弧書き全体を削除し「具体的なモデル文字列の集合。」で終わらせてしまうなど）。この失敗モードは AC-4 の grep 検証では検出できない（陳腐化パターンが消えたことしか確認できず、情報欠落は検出対象外）ため、PLAN.md の検証手順に、書き換え後の26行目付近のコメントを目視レビューし「対応アダプタキーが `codex` のみであること」「claude/human 用モデル対応表が未定義であること」の2点が引き続き読み取れることを確認する手順を含める（AC-2 の一部として扱う）。
- ロールバック手順: 本変更はコメント4箇所の削除・書き換えのみであり、当該コミットを `git revert` すれば即座に元の状態（禁止参照が残る状態）へ戻せる。ロールバック自体に副作用は無い。
- 影響を受ける既存機能: 無し。`worker-launch.sh` の起動フロー・`worker-selection.ts` のエクスポート関数（`resolveWorkerSelection`・`resolveModelForTier` 等）の入出力・分岐はいずれも変更しない。
