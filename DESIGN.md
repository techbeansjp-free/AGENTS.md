# DESIGN: worker-selection関連ファイルの禁止参照（セクション番号参照）を是正しmainのCIを復旧する

- Issue: `ISSUE-325`
- 対応する SPEC: `SPEC.md`

## 要件 → 設計要素の対応表

| 要件 / AC-ID | 対応する設計要素 | 備考 |
|---|---|---|
| `AC-1` | 是正方針A（`worker-launch.sh`）／是正方針B（`worker-selection.ts`） | 両方針とも、参照先の見出しが Issue 完了後に破棄される一時成果物にあり恒久的に解決できないため、見出しの付け替えではなく参照句自体を削除する「参照の除去」を採用する |
| `AC-2` | 是正方針A／是正方針B（設計判断の温存確認） | 除去後も設計判断がコメント本文に残ることを設計要素として明示する |
| `AC-3` | 変更範囲の限定（コメントのみ、ロジック不変） | ソースの実行内容を変更しないことを設計上の制約とする |
| `AC-4` | 是正方針B（`worker-selection.ts`）の全面適用（ファイル冒頭の`正本:`宣言行／`ModelTierTable`型のJSDocコメント／`resolveWorkerSelection`直前docstring末尾／`resolveModelForTier`直前docstring末尾の計4箇所）＋回帰防止テストの拡張 | 是正方針Bはファイル冒頭の`正本:`宣言行の `DESIGN.md §選択解決の設計`／SPEC.mdの受入条件番号（1番・2番・3番・9番）の列挙 部分の削除に加え、`ModelTierTable`型のJSDocコメントの「本 Issue」文言・`SPEC.md スコープ外` 参照の書き換え、`resolveWorkerSelection`直前・`resolveModelForTier`直前それぞれのdocstring末尾の受入条件番号単独括弧書き（`SPEC.md ` 接頭辞なし）の削除を含む。4箇所とも「Issue完了後に破棄される `SPEC.md`／`DESIGN.md` への陳腐化参照」という同一根拠に基づく同一是正方針内の作業として扱う。回帰防止テスト（`test/unit/worker-selection-reference.test.ts`）の恒久検査への拡張、および機械検証コマンドの空振り成功対策（下記「機械検証手段」参照）も本ACに対応する設計要素とする |
| `AC-5` | 回帰防止テスト自体の自己検証（複数独立fixtureに対する自己検証テストケース＋定義箇所数の`grep -c`検証） | `test/unit/worker-selection-reference.test.ts` へ、実ファイルを書き換えずin-memory文字列のみを用いて既存2正規表現（`/AC-[0-9]+/`・`/本 ?Issue/`）の検知能力を検証する新規テストケースを追加する。単一実例による空振り成功（vacuous pass）を防ぐため桁数違い・空白有無違いの複数独立fixtureへ拡張し、定数定義箇所数はAC-5 Then(b)の`grep -c`コマンド2件で検証する（下記「AC-5 Then(b) の機械検証コマンド」「回帰防止テストの拡張設計」参照） |

## 責務・境界

### `lint-references.sh` の判定ロジック（前提の確認）

是正方針を決めるにあたり、`.agent-skill-chain/scripts/lint-references.sh`（薄いラッパー）が委譲する agent-skill-chain CLI の `lint references` サブコマンド（実装本体）の判定ロジックを確認した。

- `§` の直後から、空白・句読点・各種括弧のいずれかが現れるまでの文字列をセクション参照として捕捉する（例:「`DESIGN.md §選択解決の設計`」からは「選択解決の設計」を捕捉する）。
- 捕捉した文字列は、検査対象として走査した `.md` ファイル群から抽出した見出しテキスト一覧と照合する。見出しが捕捉文字列で始まる／捕捉文字列が見出しで始まる、または末尾の英数字・ローマ数字風コード（例:「不変条件 I1〜I8」に対する「I7」）を除いた芯部分が一致すれば「見出しテキストで解決できる参照」として許可する。一致する見出しが1つも無ければ禁止参照として報告する。
- 見出し抽出・照合の対象となる `.md` ファイル群は、検査対象として走査されたファイルのうち拡張子が `.md` のものに限られる。デフォルトの走査対象は AGENTS.md・`docs/GLOSSARY.md`・`.agent-skill-chain/{standards,templates,config,schemas,scripts,ci}/`・`src/`・`.github/workflows/` であり、Issue 単位で作成される `SPEC.md`／`DESIGN.md`／`PLAN.md`（リポジトリルート直下、Issue のブランチ上にのみ存在し完了後は破棄される一時成果物）はこの走査対象に含まれない。

この確認から導かれる結論: 該当2箇所の禁止参照「`DESIGN.md §選択解決の設計`」は、そもそも参照先の見出し自体が（a）走査対象外のファイルにあり、かつ（b）Issue 完了後に破棄される一時成果物であるため、恒久的に存在し続ける見出しではない。したがって「見出しテキストで解決できる参照へ書き換える」方針（例えば AGENTS.md 側や `src/` 側の恒久的な見出しを指す形に変更する）は採用しない。仮に一時的に解決可能な見出しへ付け替えても、参照先が Issue 完了後に消える構造は変わらず、次に別の Issue の `DESIGN.md` が作られ同じ見出しが再利用されない限り再び陳腐化するため、根本解決にならない。

採用する方針は、AGENTS.md「成果物の自己完結性」節が定める「外部参照は由来情報としてのみ用い、参照元にはその文脈で必要な契約の要旨を本文へ記載する」原則に従い、**参照そのものを撤去する**方針である。撤去にあたり、参照先が説明しようとしていた契約の要旨が撤去後のコメントから読み取れるかを1箇所ずつ確認する。

### 是正方針A: `.agent-skill-chain/scripts/worker-launch.sh`

該当コメントは「worker context 自体の失敗（設定不正・ティア解決失敗）は、まだ何も起動していない段階のエラーとして扱う（DESIGN.md §選択解決の設計）。」という一文であり、括弧内の参照は同じ文中で既に述べた設計判断（`worker context` 解決失敗を「まだ何も起動していないエラー」として扱う）への末尾の裏付け引用にすぎない。設計判断の内容自体は参照を除いても文中に完全に残っているため、**参照句「（DESIGN.md §選択解決の設計）」を削除するのみ**とし、新たな文言の追記は不要と判断する。

### 是正方針B: `src/lib/worker-selection.ts`（計4箇所）

`src/lib/worker-selection.ts` には、Issue #307 で作成され既に破棄された `SPEC.md`／`DESIGN.md` への陳腐化参照が、ファイル全体を通じて計4箇所存在する。前回改訂の是正方針Bはファイル冒頭の`正本:`宣言行のみを対象としていたが、strict design-gateレビュー（2/2レビュア）の指摘「同一の根拠（Issue完了後に破棄される Issue 単位の `SPEC.md` の受入条件ID参照は陳腐化する）が `ModelTierTable`型のJSDocコメント・`resolveWorkerSelection`直前docstring末尾・`resolveModelForTier`直前docstring末尾にも等しく当てはまるのに対応が記載されていない」を受け、対象を4箇所全体へ拡張する。4箇所とも「AGENTS.md『参照・コメントの陳腐化防止』節が定める、ソースコードコメントの追跡識別子として認めるのは Issue ID のみという規約に抵触する陳腐化参照」という同一根拠に基づき、同一是正方針内の作業として扱う。

#### ファイル冒頭の`正本:`宣言行

該当コメントはファイル冒頭の「正本:」宣言行であり、`AGENTS.md §設定` / SPEC.mdの受入条件番号（1番・2番・3番・9番）の列挙 / `DESIGN.md §選択解決の設計` の3つを列挙している（このうち `AGENTS.md §設定` は AGENTS.md に現存する見出し「設定」で解決できるため `lint-references.sh` の禁止参照には該当しない）。`DESIGN.md §選択解決の設計` が指していた「選択解決の設計」の内容（アダプタ・ティア・reasoning_effort の解決順序、責務分担）は、同ファイル内の `resolveWorkerSelection` 直前の docstring（解決順序：セグメント別上書き→`worker.adapter`→既定 human、ティア対応表からの具体モデル解決は別関数が担う旨）に既に完全な形で記載されている。SPEC.mdの受入条件番号（1番・2番・3番・9番）の列挙 部分が説明しようとしていた内容（当該モジュールが `SPEC.md` のどの受入条件に対応するかという由来情報）は、モジュール自体の責務理解に必須ではなく、この宣言行に続くモジュール概要のコメント本文（config とセグメント名だけから adapter・model_tier・reasoning_effort・モデル文字列を決める、という記述）で責務は既に自己完結して説明されているため、代替の書き下しは不要と判断する。

したがって、`AGENTS.md §設定` ・ SPEC.mdの受入条件番号（1番・2番・3番・9番）の列挙 ・ `DESIGN.md §選択解決の設計` の3つをスラッシュ区切りで並べる修正前の`正本:`宣言行から、**`DESIGN.md §選択解決の設計` 部分と受入条件番号（1番・2番・3番・9番）の列挙部分の両方を削除する**。削除後のこの宣言行は `// 正本: AGENTS.md §設定` のみとなる。

#### `ModelTierTable`型のJSDocコメント

該当コメントは「ティア対応表（`worker.model_tiers`）。ティア名をキーとし、値は実行系アダプタ名をキーとする具体的なモデル文字列の集合。本 Issue で許容するアダプタキーは `codex` のみ（SPEC.md スコープ外: claude/human 用モデルの追加）。」という記述である。ここには2種類の陳腐化参照が混在する。

- `SPEC.md スコープ外: claude/human 用モデルの追加` — 破棄された `SPEC.md` の対象外節へのスコープ参照。
- `本 Issue で許容する` — 「本Issue」という文言が Issue #307 を指して書かれており、Issue #307 完了後に別の Issue（本 Issue #325 を含む、以降このコメントを読む任意の Issue）がこのファイルを扱う際、あたかも「今読んでいる Issue」を指しているかのように誤読される自己参照的陳腐化である。

この記述が伝えていた実質的な意味は「`resolveModelForTier` が扱うアダプタキーは現時点では `codex` のみであり、`claude`／`human` 用のティア対応表は現時点で未定義である」というコード自体の現状の対応範囲に関する事実であり、Issue 番号にも `SPEC.md` にも依存せず恒久的に成立する。したがって、`SPEC.md` へのスコープ参照と「本 Issue」という自己参照的文言を削除し、Issue番号に依存しない恒久的な表現へ書き換える。書き換え後の文例：「具体的なモデル文字列の集合。現時点で対応するアダプタキーは `codex` のみであり、claude/human 用のモデル対応表は未定義である。」対応するアダプタキーが `codex` のみであること・claude/human 用が未対応であることという実質情報は書き換え後も維持する（「障害・ロールバック考慮」節で確認手順を定める）。

#### `resolveWorkerSelection`直前のdocstring末尾の括弧書き

該当コメント「adapter は『セグメント別上書き → worker.adapter（スカラー、4セグメント共通の既定値）→ human（最終フォールバック）』の順で解決する（1番, 3番）。」のうち、末尾の括弧書き「（1番, 3番）」は破棄された `SPEC.md` の受入条件番号への単独参照であり、`SPEC.md ` という接頭辞を伴わない記法のため `lint-references.sh` の `§` パターン検出では捕捉されない。直前の説明文（adapter の解決順序：セグメント別上書き→`worker.adapter`→既定 human）はこの括弧書きに一切依存せずそれ自体で完結しているため、**括弧書き「（1番, 3番）」を削除するのみ**とし、新たな文言の追記は不要と判断する。

#### `resolveModelForTier`直前のdocstring末尾の括弧書き

該当コメント「ティア対応表（`worker.model_tiers`）を引き、ティア名とアダプタ名の組から具体的なモデル文字列を得る（2番, 9番）。対応表そのものが無い・当該ティアのエントリが無い・当該アダプタ用のモデルが無い、のいずれの場合も値を推測せず解決失敗として返す」のうち、末尾の括弧書き「（2番, 9番）」も同様に `SPEC.md ` 接頭辞を伴わない受入条件番号の単独参照である。直前の説明文（ティア対応表からモデル文字列を得る処理、および異常系の扱い）はこの括弧書きに依存せず完結しているため、**括弧書き「（2番, 9番）」を削除するのみ**とし、新たな文言の追記は不要と判断する。

#### 機械検証手段

AC-4 の機械検証コマンドを次のとおり定義する。

```
! grep -nE 'AC-[0-9]+|本 ?Issue' src/lib/worker-selection.ts
```

このコマンドが4箇所すべてを捕捉できる理由は次のとおりである。

- `AC-[0-9]+` は「`AC-` に続く数字」という受入条件ID形式そのものにマッチするパターンであり、ファイル冒頭の`正本:`宣言行（受入条件番号1番・2番・3番・9番の列挙）・`resolveWorkerSelection`直前docstring末尾（受入条件番号1番、3番の単独括弧書き）・`resolveModelForTier`直前docstring末尾（受入条件番号2番、9番の単独括弧書き）のいずれも、`SPEC.md ` という接頭辞の有無に関わらず捕捉する。旧検証コマンド（`! grep -q 'SPEC\.md AC-' ...`）は `SPEC.md ` 接頭辞を前提としていたため後2者の接頭辞なし単独括弧書きを素通りしていたが、新パターンは接頭辞への依存を持たないためこの穴を塞ぐ。
- `本 ?Issue`（`本Issue` または `本 Issue`）は `ModelTierTable`型のJSDocコメント内の自己参照的文言にマッチする。AGENTS.md が正当と定める追跡識別子としての Issue 参照（例：`// Issue #123: ...`）は「本」を伴わないため誤検知しない。
- `-n`（行番号表示）により、マッチした場合に該当行を特定できる。
- 全体を `!` で反転することで、「ファイル全体を通じて2種類の陳腐化パターンが1件も残存しない」ことを終了コード0の成功として一意に検証する。

**失敗モード（空振り成功／vacuous pass）**: 上記コマンドは `grep -nE 'AC-[0-9]+|本 ?Issue' src/lib/worker-selection.ts` を `!` で反転する構造のため、対象ファイル `src/lib/worker-selection.ts` が何らかの理由（パス誤記・ファイル移動等）で存在しない場合、`grep` はマッチ0件の終了コード1ではなく「ファイルが開けない」ことを示す終了コード2を返す。これを `!` で反転すると終了コード0（成功）となり、「陳腐化参照が0件だった」のではなく「そもそも検査対象を読めなかった」だけであるにもかかわらず、コマンドはあたかも検証に成功したかのように振る舞う（空振り成功）。実測: 対象ファイルが存在しないディレクトリで `! grep -nE 'AC-[0-9]+|本 ?Issue' src/lib/worker-selection.ts` を実行すると終了コード0を返すことを確認済み。

このコマンド自体はSPEC.mdのAC-4が検証コマンドとして一意に固定しているため、コマンド文字列自体（`test -f` 等によるファイル存在確認の前置）は変更しない。かわりに、negative control（本コマンドが実際に陳腐化パターンを検知できる状態にあることの事前確認）を、現在の作業ツリーの状態に依存しない形で実行する。具体的には、是正前のbase commit（`origin/main`、コミット `4618b590fb35c739b49182672d5446b1cf57ba42`）の内容を直接参照する次のコマンドを用いる。

```
git show 4618b590fb35c739b49182672d5446b1cf57ba42:src/lib/worker-selection.ts | grep -nE 'AC-[0-9]+|本 ?Issue'
```

このコマンドは作業ツリーの状態（変更単位#1・#2が適用済みか否か）に関係なく常に実行可能であり、是正前の内容に対して実際に4箇所がマッチ（陽性）することを確認できる。ただしこのコマンドは `git show` の出力を標準入力として `grep` へ渡す構成であるため、AC-4本来のコマンド `! grep -nE 'AC-[0-9]+|本 ?Issue' src/lib/worker-selection.ts` が持つ**パス引数**（`src/lib/worker-selection.ts` というファイルが実在すること）自体の妥当性は検証しない。負のコントロールが確かめられるのは「パターンを実際に検知できること」であり、「AC-4本来のコマンドの引数が正しいファイルを指し続けていること」ではないため、この限界は `git show` ベースの構成をどれだけ強化しても解消できない。

そのため、AC-4本来のコマンドが持つパス引数の妥当性（対象ファイルが実在することの検証）については、一時的なshellコマンドでの検証を諦め、恒久的な安全網の役割を `test/unit/worker-selection-reference.test.ts` に委ねるという設計判断を採る。同テストは `fs.readFileSync(path.join(repositoryRoot, 'src/lib/worker-selection.ts'), 'utf8')` によって対象ファイルを読み込むため、対象ファイルが不在であれば `readFileSync` が例外を投げてテスト自体が red になる。これにより、パス誤記・ファイル移動によって検査対象が読めなくなる事態は、AC-4の一時的な検証コマンドではなく、恒久的に実行され続ける自動テストの例外送出によって代替的に検知される。

#### AC-5 Then(b) の機械検証コマンド

strict design-gateレビューにて「SPEC.mdのAC-5 Then(b)が定義する2つの `grep -c` コマンドが、DESIGN.md・PLAN.mdのどこにも転記・配線されておらず、`automated` と定めた検証方法が実質 `manual` へ格下げされている」という指摘を受け、本節を新設する。

SPEC.mdのAC-5 Then(b)は、共有正規表現定数 `ACCEPTANCE_CRITERIA_ID_PATTERN`・`SELF_REFERENTIAL_ISSUE_PATTERN` それぞれの定義箇所（`<定数名> = /.../` という代入式）が `test/unit/worker-selection-reference.test.ts` 内に1箇所のみ存在することを、次の2つの `grep -c` コマンドで検証すると一意に定めている。

```
grep -c 'ACCEPTANCE_CRITERIA_ID_PATTERN = /' test/unit/worker-selection-reference.test.ts
grep -c 'SELF_REFERENTIAL_ISSUE_PATTERN = /' test/unit/worker-selection-reference.test.ts
```

いずれのコマンドも出力が `1` であることをもって成功とする。`grep -c` はパターンに一致した行数を返すため、この2コマンドは各定数の代入式がファイル内で複数箇所に分裂して定義されていないこと（リテラルの二重管理が無いこと）を検証する。出力が `2` 以上であれば定義が複数箇所に分裂しており、「回帰防止テストの拡張設計」で述べる vacuous pass のリスク（一方の定義だけが将来書き換えられて検知力を失っても他方が誤ってマッチし続け、AC-4の恒久検査とAC-5の自己検証テストケースが同一の正規表現定数を確実に共有しているとは保証できなくなる）が生じうる状態を示す。出力が `0` であれば定数が未定義であり、この場合はAC-5の自己検証テストケース自体が定数を参照できず実行時エラーとなるため、他の自動テスト（`npm test`）の失敗として別途検知される。この2コマンドはSPEC.mdが検証コマンドとして一意に固定しているため、コマンド文字列自体は変更しない。

#### 回帰防止テストの拡張設計

**テストコードコメントにおける陳腐化参照の禁止（設計制約）**: `test/unit/worker-selection-reference.test.ts` へ付与するコメントは、AGENTS.md「参照・コメントの陳腐化防止」節に従い、Issue単位の一時成果物（本Issueの `DESIGN.md`・`SPEC.md`・`PLAN.md` を含む。いずれも Issue のブランチ上にのみ存在し、Issue 完了後は破棄され恒久的な見出しではない）の**見出し・節タイトルへの参照**（例：「DESIGN.mdの『◯◯』節を参照」のような、文書の構造上の位置を指す表現）をしてはならない。当該参照が説明しようとしていた設計判断・検証根拠の要旨は、外部（本 DESIGN.md の見出し）へ意味を委譲する参照としてではなく、コメント本文へ直接書き下すこと。この制約は本Issue自身が是正対象とする問題（陳腐化する見出し参照をソースコメントへ焼き付けること）をテストコード自身に再発させないためのものである。

この制約が禁止するのは、あくまで陳腐化しうる文書の見出し・節タイトルという構造的な位置情報への参照であり、本Issue自身が定義する AC-ID（例：`AC-1`〜`AC-5`）を、当該 Issue の作業期間中に検証証跡を追跡するための識別子として言及すること自体は禁止しない。実際、本DESIGN.md「回帰防止テストの拡張設計」は、テストケース名を `ISSUE-325 AC-1: ...` から `ISSUE-325 AC-1/AC-4: ...` のようにAC-IDを含む形へ更新すること、および後述の各fixtureに「AC-5自己検証専用のテストデータである」旨のAC-ID言及コメントを付すことを別途明示的に指示しており、これらはAC-IDを検証証跡の追跡識別子として用いる正当な用法であって、上記制約の対象外である。SPEC.mdの「スコープ外」節は `test/` 配下に既に存在する `SPEC.md AC-...` 形式の陳腐化参照コメントの一掃をIssue #332へ委譲しているが、これは本Issue以前から存在していた既存コメントの是正対象範囲の話であり、本Issueが新規作成する `test/unit/worker-selection-reference.test.ts` 自身に含まれるAC-ID言及（テストケース名・fixtureコメント）は、本Issueが自ら生成する正当な成果物の一部であって、Issue #332が扱う一掃対象には含まれない。

この制約に照らすと、commit `2c7f1bfd1`（下記「対応済み」参照）で実装された複数fixture化コメントには当初「（DESIGN.md「回帰防止テストの拡張設計」参照）。」という一文が含まれており、これは見出しへの参照であり本節が禁止する陳腐化参照に該当していた（AC-ID言及ではないため上記の許容範囲には含まれない）。この一文の是正内容と是正済みであることの確認方法は本節末尾「対応済み: テストコメント内の陳腐化参照の是正」に記載する。

`test/unit/worker-selection-reference.test.ts` は現状 `assert.doesNotMatch(contents, /DESIGN\.md §/, ...)` のみを検査しており、上記4箇所のうち `DESIGN.md §` パターンに一致する記述（ファイル冒頭の`正本:`宣言行の `DESIGN.md §選択解決の設計` 部分）しか恒久検知できない。実装セグメントでは、`src/lib/worker-selection.ts` と `worker-launch.sh` の双方を読み込み `DESIGN.md §` パターンを検査する既存の共通 `for` ループとは別に、`worker-selection.ts` の内容のみを対象とする独立したテストケースとして、次の2パターンをそれぞれ個別の `test()` ブロックで追加する（`worker-launch.sh` は `AC-[0-9]+`／`本 ?Issue` パターンの検査対象ではないため、共通走査ループに混在させない設計とする）。

- `assert.doesNotMatch(contents, /AC-[0-9]+/, ...)` — ファイル冒頭の`正本:`宣言行・`resolveWorkerSelection`直前docstring末尾・`resolveModelForTier`直前docstring末尾の受入条件ID形式の参照が再発しないことを恒久検証する。
- `assert.doesNotMatch(contents, /本 ?Issue/, ...)` — `ModelTierTable`型のJSDocコメント内の自己参照的「本Issue」文言が再発しないことを恒久検証する。

このアサーションは `worker-selection.ts` のみに適用する（`worker-launch.sh` は AC-1 の対象であり `AC-[0-9]+`／`本 ?Issue` パターンの検査対象ではない）。テストケース名は既存の `ISSUE-325 AC-1: ...` から `ISSUE-325 AC-1/AC-4: ...` 等、AC-4 の恒久検証であることが分かる形へ更新する。

**AC-5対応: 回帰防止テスト自体の自己検証**。上記2アサーションは、実ファイル `src/lib/worker-selection.ts` に陳腐化パターンが存在しないことしか検証せず、正規表現 `/AC-[0-9]+/`・`/本 ?Issue/` 自体が陳腐化パターンを正しく検知できる能力を持つことは検証しない（正規表現の書き方に不備があれば、対象パターンが実在してもマッチせず恒久的にgreenのまま気付かれない可能性がある）。この空振りを防ぐため、同テストファイルへ自己検証専用のテストケースを追加する。

当初案は「実ファイルの内容から受入条件ID形式の文字列を抽出・連結してin-memory文字列を作る」構成だったが、この構成はAC-4の恒久検査（是正後の実ファイルにこれら2パターンが1件も残らないことを要求する）と自己矛盾する。是正が完了した実ファイルからは受入条件ID形式・「本Issue」文言のいずれも抽出できず、常に空文字列となるため「実ファイルの内容を使う」という構成自体が意味を成さない。この矛盾を避けるため、汚染データの構成方法を全面的に書き換え、実ファイルの内容には一切依存しない方式を採用する。

具体的には、ファイル冒頭で2つの正規表現定数 `ACCEPTANCE_CRITERIA_ID_PATTERN = /AC-[0-9]+/`・`SELF_REFERENTIAL_ISSUE_PATTERN = /本 ?Issue/` を定義し、AC-4の恒久検査2件（上記2アサーション）とAC-5の自己検証テストケースの双方がこの同一定数を参照する（正規表現リテラルを複製しない）。AC-5の自己検証テストケースは、実ファイルを一切読み取らず、テスト内で明示的に構成した合成汚染文字列リテラル（受入条件ID形式の文字列例〔`AC-` という接頭辞に数字を続けた形式であり具体的な数字は問わない〕と「本Issue」という文言例〔例：`本 Issue のスコープ外`〕の両方を含む、意図的に汚染させたin-memory文字列）を用意し、この合成文字列に対して上記2つの共有正規表現定数が `assert.match` でマッチすることを検証する。実ファイルへの参照・書き戻しは一切行わない（CI上で安全に自動実行できるようにするための設計上の制約）。

この「正規表現定数を共有し、独立した複製を持たない」設計により、将来 `ACCEPTANCE_CRITERIA_ID_PATTERN`・`SELF_REFERENTIAL_ISSUE_PATTERN` の書き方が誤って壊れ検知力を失った場合、AC-4側の恒久検査とAC-5側の自己検証テストケースが同時に、かつ整合して失敗する。仮にAC-5側が独立した複製の正規表現を保持していたら、その複製だけが誤ってマッチし続け自己検証テストケースがgreenのまま維持されてしまい、AC-4側の劣化に気付けなくなる（vacuous pass）。定数を共有することでこの事態を防ぐ。

**AC-5対応（追加）: 単一実例に起因する正規表現の縮退が空振り成功（vacuous pass）する問題**。strict design-gateレビューにて、実装済みの自己検証テストケースが合成汚染文字列として単一の実例（受入条件ID形式については数字1桁の `AC-9` という1種のみ、自己参照Issue文言については空白ありの「本 Issue」という1種のみ）しか用いていないことへの指摘を受け、本項を新設する。

単一実例のみでは次の失敗経路が生じる。将来 `ACCEPTANCE_CRITERIA_ID_PATTERN` が誤って `/AC-9/` のような特定の数字専用パターンへ縮退した場合、AC-4の恒久検査（是正済みの実ファイル `src/lib/worker-selection.ts` には元々 `AC-9` という文字列が存在しないため `assert.doesNotMatch` は縮退の前後で変わらずgreenのまま）・AC-5の自己検証テストケース（単一fixtureが `AC-9` そのものであるため、縮退後のパターンに対しても引き続きマッチしgreenのまま）のいずれもこの検知力喪失に気付かず通過し続ける。同様に `SELF_REFERENTIAL_ISSUE_PATTERN` が `/本 Issue/`（空白必須）へ縮退した場合、「本Issue」（空白無し）という表記に対する検知力喪失を、空白ありの単一fixtureだけでは検出できない。

この失敗経路への対策として、自己検証テストケースの合成汚染文字列を単一の実例ではなく複数の独立したfixtureへ拡張する設計を採る。この設計は commit `2c7f1bfd1`（ISSUE-325: AC-5自己検証テストを複数fixture化しIssue#332除外コメントを付与）として実装済みである。

- 受入条件ID形式（`ACCEPTANCE_CRITERIA_ID_PATTERN` 用）: 桁数の異なる複数の具体値（例：1桁の `AC-9`、2桁の `AC-42`、3桁の `AC-777` など、実在の受入条件番号〔AC-1〜AC-5〕と紛れない任意の数字を持つ複数のfixture文字列）を用意し、それぞれ独立に `assert.match` でマッチすることを検証する。
- 自己参照Issue文言（`SELF_REFERENTIAL_ISSUE_PATTERN` 用）: 空白ありパターン（「本 Issue」）・空白無しパターン（「本Issue」）の両方を独立したfixture文字列として用意し、それぞれ独立に `assert.match` でマッチすることを検証する。

各fixtureに対する `assert.match` は、それぞれ独立したアサーションとして実行する（複数のfixtureを結合した単一の文字列に対する単一アサーションへまとめない）。1つの結合文字列にまとめた場合、一部の実例だけにマッチする縮退した正規表現でも結合文字列全体には引き続きマッチしてしまい、他の実例に対する検知力喪失を見逃すため、fixtureごとの独立性を設計上の制約とする。

この複数fixture方式への拡張は、`test/unit/worker-selection-reference.test.ts` のAC-5自己検証テストケースの実装内容を、単一fixtureの合成文字列に対する2アサーションのみの構成から、上記の複数独立fixture・複数独立アサーション構成へ拡張する変更であり、commit `2c7f1bfd1` として実装済みである。

**SPEC AC-5 Whenとの整合性**: SPEC.mdのAC-5 Whenは、受入条件ID形式の文字列例と「本Issue」という文言例の両方を含む単一の合成文字列リテラルを汚染データとする最小構成を想定しているのに対し、本設計（およびcommit `2c7f1bfd1` による実装）は桁数・空白パターンの異なる5つの独立したfixtureへ分割している。単一の合成文字列でSPEC AC-5 Whenが要求するマッチが成立する状態は、複数fixtureのうち該当する1つに対するマッチとして引き続き成立するため、本設計はSPEC AC-5のWhenが示す最小構成を包含・強化する変更であり、Then(a)（各定数が合成汚染文字列に対して実際にマッチすること）・Then(b)（各定数の定義箇所がテストファイル内に1箇所のみ存在することの `grep -c` 検証）の充足には影響しない。SPEC.md自体の当該記述をこの実装済みの構成に合わせて改訂するか否かは、本DESIGN.mdの変更範囲を超えるため別途進行役が判断する。

**AC-5対応（追加）: 自己検証fixtureと別Issue #332の一掃対象との偶然の一致**。自己検証テストケースが用いる合成汚染文字列の実例（`'// 正本: AGENTS.md §設定 / SPEC.md AC-9'`）は、SPEC.mdのスコープ外節が挙げる別Issue #332（`test/` 配下に残存する `SPEC.md AC-...` 形式の陳腐化参照コメントの一掃を扱う）の一掃対象と、偶然同じ表記形式をしている。Issue #332側の機械的な一掃処理が本テストファイルの走査対象範囲まで及んだ場合、当該fixtureが意図的な合成テストデータであることを認識されず誤って書き換え・削除されると、AC-5の自己検証テストケースが検知対象を失い意味を成さなくなるリスクがある。

対策として、合成汚染文字列を定義する箇所（複数fixture化後は各fixtureそれぞれ）の直前に、これが意図的な合成テストデータであり実際の陳腐化参照ではないこと、Issue #332の一掃対象から除外されるべきことを明示するコードコメントを付す設計とする（例：`// 意図的な合成fixture。実際の陳腐化参照ではなくAC-5自己検証専用のテストデータであり、Issue #332の一掃対象から除外する。`）。この設計要素は commit `2c7f1bfd1` として実装済みである。

**対応済み: テストコメント内の陳腐化参照の是正**。commit `2c7f1bfd1` で実装された、単一実例に起因する正規表現の縮退（vacuous pass）対策を説明するコメントには、当初「// （DESIGN.md「回帰防止テストの拡張設計」参照）。」という一文が含まれていた。これは、本節冒頭「テストコードコメントにおける陳腐化参照の禁止（設計制約）」が禁止する、Issue単位の一時成果物（本Issueの `DESIGN.md`）の見出しへの参照そのものであり、本Issueが是正対象とする問題をテストコード自身に再発させてしまっていた。

この参照句は commit `7160b5b`（ISSUE-325: 回帰テストコメントのDESIGN.md見出し参照を除去し設計判断を直接書き下し）で既に削除され、参照が指していた設計判断の要旨——「単一実例のみでは正規表現の縮退（vacuous pass）に気付けないため、桁数・空白パターンの異なる複数独立fixtureで検証する」という趣旨——が、参照を用いず直接書き下した文言（`test/unit/worker-selection-reference.test.ts` 内、桁数違い・空白有無違いの各fixture定義直前のコメント）へ置き換え済みである。書き換え後も、当該コメントが伝えていた実質的な情報（縮退のリスクと複数fixtureによる対策という設計判断）は変わらず読み取れる。`grep -n 'DESIGN\.md' test/unit/worker-selection-reference.test.ts` の結果が0件であることで機械的に確認できる。

### 変更範囲の限定

いずれの方針も、コメント文字列の一部を削除または書き換えるのみでコードの実行内容（関数シグネチャ・分岐・戻り値）を変更しない。これにより既存の単体テスト（`test/unit`）・結合テスト（`test/integration`）の結果に影響を与えない設計とする。

## 関連ADR

本 Issue はソースコードコメントの表現をAGENTS.mdの既存規約に適合させる是正であり、アーキテクチャ判断を新たに伴わないため、関連 ADR は無い。

## 障害・ロールバック考慮

- 想定される失敗モード: 参照句を削除した結果、当該コメントが説明しようとしていた設計判断が読み取れなくなる（AC-2 未達）。是正方針A・Bのいずれも、削除対象が「既に本文中に記載済みの内容への末尾引用」であることを確認済みであり、この失敗モードは生じない設計とした。
- 想定される失敗モード（ファイル冒頭の`正本:`宣言行固有）: SPEC.mdの受入条件番号（1番・2番・3番・9番）の列挙 部分の削除範囲を誤り、直前の `AGENTS.md §設定` 部分まで巻き込んで削除してしまう、あるいは区切り文字（` / `）の削除漏れにより `// 正本: AGENTS.md §設定 / ` のような不正な行末が残る。この失敗モードは AC-4 の検証コマンド `! grep -nE 'AC-[0-9]+|本 ?Issue' src/lib/worker-selection.ts` では「`AC-` に続く数字・『本Issue』文言のいずれも残っていないこと」しか検査できず、`AGENTS.md §設定` の誤削除や区切り文字の残存は別途検出する必要がある。対策として PLAN.md の検証手順に、修正後の宣言行全体が `// 正本: AGENTS.md §設定` と完全一致することの目視確認を含める。
- 想定される失敗モード（`ModelTierTable`型のJSDocコメント固有）: 「本 Issue で許容するアダプタキーは `codex` のみ（SPEC.md スコープ外: claude/human 用モデルの追加）」を書き換える際、Issue番号依存の文言・`SPEC.md` 参照を削除することのみに気を取られ、「対応するアダプタキーは `codex` のみであり claude/human 用は未対応である」という実質的な事実情報自体を落としてしまう（例：単に括弧書き全体を削除し「具体的なモデル文字列の集合。」で終わらせてしまうなど）。この失敗モードは AC-4 の grep 検証では検出できない（陳腐化パターンが消えたことしか確認できず、情報欠落は検出対象外）ため、PLAN.md の検証手順に、書き換え後の `ModelTierTable`型のJSDocコメントを目視レビューし「対応アダプタキーが `codex` のみであること」「claude/human 用モデル対応表が未定義であること」の2点が引き続き読み取れることを確認する手順を含める（AC-2 の一部として扱う）。
- 想定される失敗モード（AC-4検証コマンドの空振り成功／vacuous pass）: AC-4 の機械検証コマンド `! grep -nE 'AC-[0-9]+|本 ?Issue' src/lib/worker-selection.ts` は、対象ファイル `src/lib/worker-selection.ts` が存在しない場合（パス誤記・ファイル移動等）でも「成功」と誤判定されうる。`grep` は対象ファイルを開けない場合に終了コード2を返すが、これは「マッチ0件」を意味する終了コード1と区別されないまま `!` で反転され、終了コード0（成功）として扱われてしまう。実測でも、対象ファイル不在の状態で本コマンドを実行すると終了コード0を返すことを確認済みである（「機械検証手段」節参照）。この失敗モードはコマンドの構造上の欠陥であり、修正内容の正しさとは無関係に発生しうる。対策として、「機械検証手段」節が採用した `git show` ベースのnegative control（是正前のbase commit `4618b590fb35c739b49182672d5446b1cf57ba42` の内容を直接参照し、4箇所がマッチ〔陽性〕することを確認するコマンド）をPLAN.md「検証手順」の手順0として必須化する。作業ツリー依存の別方式（是正前の作業ツリーに対して本コマンドを実行する方式）は、変更単位#1が既にcommit `f56cde1` で適用済みで「是正前の作業ツリー」自体がもはや存在せず実行不能であるため採用しない。`git show` ベースの方式は作業ツリーの状態に関係なく常に実行可能であり、対象ファイルが正しく読めており実際にマッチを検知できる状態にあることを、修正適用の前後を問わず担保する。
- ロールバック手順: 本変更は `src/lib/worker-selection.ts` 4箇所・`.agent-skill-chain/scripts/worker-launch.sh` 1箇所の計5箇所のコメント削除・書き換えに加え、`test/unit/worker-selection-reference.test.ts` の新規作成・拡張を含み、複数コミット（`f56cde1`・`1758e21`・`2c7f1bfd1`・`7160b5b` 等）に分散して実装されている。単一コミットの `git revert` では元の状態へ戻らないため、ロールバックする場合は本Issueの全変更コミット範囲（`f56cde1` から `7160b5b` 等、本Issueのブランチ上の全コミット）をまとめて `git revert` する、またはPRをクローズしマージしないことで対応する。ロールバック自体に副作用は無い。
- 影響を受ける既存機能: 無し。`worker-launch.sh` の起動フロー・`worker-selection.ts` のエクスポート関数（`resolveWorkerSelection`・`resolveModelForTier` 等）の入出力・分岐はいずれも変更しない。
