# DESIGN: セグメント別 worker アダプタ・モデルティア選択の恒久設定

- Issue: `ISSUE-307`
- 対応する SPEC: `SPEC.md`
- 対象ブランチ: `feature/307-segment-worker-adapter-config`

## 目的・対象範囲

4セグメント（spec・design・implementation・validation）の作業ワーカーを起動する際に選ばれる実行系アダプタ（`claude` / `codex` / `human`）とモデル選択を、セグメント単位で設定の正本へ恒久記録できるようにする。あわせて、具体的なモデル文字列を設定ファイル・設定スキーマから排除し、抽象名（モデルティア）から具体名への解決を、本リポジトリに既に存在する宣言を唯一の値の出所として行う。

対象は「ワーカー起動時のアダプタ・モデル選択の解決経路」だけである。すなわち設定スキーマ、設定読み込みと選択解決、`worker` コマンド群の解決結果出力、起動ラッパーからアダプタへの伝達、codex アダプタにおけるティア解決の5点である。ゲートレビュア側の選択、コア独立レビューの契約・トリガー・レビュア選定・attestation 検証、4セグメント構成、writer lease・ゲート・Check Run の状態遷移は本設計の対象外であり、一切変更しない。

## 前提

- 設定の正本は `.agent-skill-chain/config/agent-skill-chain.yaml` であり、その構造検査は `.agent-skill-chain/schemas/config.schema.yaml` が行う。設定読み込みはスキーマ違反時に日本語の理由付きで失敗する。
- ワーカー起動の経路は「進行役 → `.agent-skill-chain/scripts/worker-launch.sh` → `agent-skill-chain worker context` で解決 → 選ばれたアダプタの `launch_worker`」である。
- 現在の `worker context` は Issue ID のみを入力とし、`adapter` / `backend` / `issue_number` を `KEY=VALUE` 形式で返す。human アダプタもこの出力（`backend` / `issue_number`）を利用している。
- codex アダプタは現在、実装セグメントには `gpt-5.6-terra` / `medium`、それ以外には `gpt-5.6` / `high` をフォールバック既定値として持ち、環境変数でのみ上書きできる。これらの既存フォールバック値は本 Issue では削除しない（ティア未指定時の従来動作を保つため）。
- codex アダプタは、起動コマンド全体を差し替える環境変数（`CODEX_WORKER_CMD`、後方互換の `WORKER_CMD`）をテスト用の完全上書き経路として解釈する。
- codex アダプタは既に agent-skill-chain CLI を子プロセスとして呼ぶ経路（`_asc_cli`）を持つ。
- codex CLI が存在しない・認証が成立しない場合は、writer lease 取得後に blocked 報告・lease 解放・0 でも 3 でもない終了コードで返すフェイルセーフが既に存在する。
- 本リポジトリには、コア独立レビュー用のモデル宣言が登録済みプロジェクトポリシー（`.agent-skill-chain/project/manifest.yaml` の `model_selection.core_review`）として既に存在し、`src/lib/model-selection.ts` の読み取り関数がプロジェクトポリシーのスキーマ検証を伴って読み出している。宣言には codex 用の具体モデル文字列と reasoning effort が含まれる。
- `.agent-skill-chain/schemas/project-policy.schema.yaml` は、そのモデル文字列と reasoning effort を `const` として固定している。これは重複ではなく、コア監査レビュアのモデルが無断で差し替えられていないことを検証するための固定値であり、attestation 用の環境変数と対になったセキュリティ機構である。本 Issue はこの機構に一切手を加えない。
- プロジェクトポリシー資産（`.agent-skill-chain/project/`）はパッケージへ同梱されず、`upgrade` の更新対象にも含まれない（consumer 固有・更新非対象）。
- 進行役は調整状態（Issue・ラベル・PR review 証跡・マージ・worktree ライフサイクル）のみを読み書きし、成果物の著述・内容の取り込みを行わない（不変条件 I5）。
- 本 Issue が変更するパスは設定・スキーマ・アダプタ・スクリプト・CLI 実装であり、登録済みプロジェクトポリシーが定めるコア変更の対象に該当する。したがって独立レビューは Strict（レビュア2体）で実施される。

## 用語（本設計で用いる語）

- セグメント別上書き: セグメント名をキーに、そのセグメントにのみ適用する値の集合。設定キーは `worker.segment_overrides.<segment>`。
- モデルティア: 具体的なモデル文字列を含まない抽象名。設定キーは `model_tier`、本 Issue で扱う値は `highest_capability` のみ。
- ティア解決: ティア名から具体的なモデル文字列を得ること。本設計では、値の出所を登録済みプロジェクトポリシーの宣言に限定する。
- worker 起動コンテキスト: `worker context` が返す `KEY=VALUE` 形式の解決結果。
- 選択解決: 設定とセグメント名から `adapter` / `model_tier` / `reasoning_effort` を決める純粋な計算。
- コア独立レビュー契約: 登録済みプロジェクトポリシーが宣言する、コア変更・コア監査時のレビュア能力要件と adapter ごとのモデル・reasoning effort・attestation 手段の集合。

## 要件 → 設計要素の対応表

| 要件 / AC-ID | 対応する設計要素 | 備考 |
|---|---|---|
| AC-1 セグメント別のアダプタ解決 | 選択解決モジュール（`src/lib/worker-selection.ts`、新設）、`worker context`（`src/commands/worker.ts`）の segment 引数 | 上書き→スカラー→`human` の3段フォールバック |
| AC-2 ティア・reasoning effort の解決と codex 実行への反映 | 選択解決モジュール、`worker context` の出力キー拡張、`worker-launch.sh` の環境変数 export、codex アダプタの単一ティア解決定義、ティア解決コマンド `worker model-for-tier` | 完全上書き環境変数が最優先である順序を維持 |
| AC-3 既存設定の後方互換 | 選択解決モジュールのフォールバック、`worker context` の segment 省略時挙動、codex アダプタの既存フォールバック値の保持 | 既存設定・既存呼び出しの結果を変えない |
| AC-4 スキーマによる新旧両形式の検証 | `.agent-skill-chain/schemas/config.schema.yaml` の `worker` 定義拡張 | 任意項目追加＋`additionalProperties: false`＋enum＋組合せ制約 |
| AC-5 解決ロジックの単体テスト | 選択解決モジュール・ティア解決関数・スキーマに対する単体テスト（`test/unit/`） | 解決・フォールバック・受理・拒否の4観点 |
| AC-6 本リポジトリの実装セグメントの恒久切替 | `.agent-skill-chain/config/agent-skill-chain.yaml` の `worker.segment_overrides.implementation` | 設定値はティア名のみ。具体モデル名は書かない |
| AC-7 設定更新手順の文書化 | 設定ファイル `worker` セクションのコメント（正本）、`worker context` のヘルプ出力 | 操作・実行主体・タイミングと I5 との関係を明記 |
| AC-8 設定項目追加手続きの帰結の明記 | 本文書「設定項目追加手続きの帰結」、ADR-0015 | 5点（理由・可変性・スキーマ版数・既定値・migration） |
| AC-9 アダプタ内ティア解決の存在と更新容易性 | codex アダプタ内の単一のティア解決定義 `_codex_model_for_tier`（値は `worker model-for-tier` へ委譲）、プロジェクトポリシーの既存宣言 | 追従はプロジェクトポリシーの宣言値の更新のみ。SPEC 記述との差異は「SPEC との差異」節に明記 |

## 責務・境界

### コンポーネント構成

- 設定スキーマ（`.agent-skill-chain/schemas/config.schema.yaml`）: 設定ファイルの構造・許容値・許容される組合せだけを定義する。具体的なモデル文字列は持たない。
- 設定読み込み（`src/lib/config.ts`）: 設定ファイルの読込・スキーマ検証・型付けのみを担う。選択の判断は持たない。
- 選択解決（`src/lib/worker-selection.ts`、新設）: 設定値とセグメント名から `adapter` / `model_tier` / `reasoning_effort` を決める純粋関数と、ティア名とコア独立レビュー契約からモデル文字列を決める純粋関数を持つ。ファイル入出力・プロセス起動・環境変数の読み書きを行わない。単体テストの主対象。
- プロジェクトポリシー読み取り（`src/lib/model-selection.ts`、既存・変更しない）: 登録済みプロジェクトポリシーを読み、スキーマ検証を経てコア独立レビュー契約を返す既存関数を、読み取り専用でそのまま再利用する。コア独立レビューの判定・トリガー・attestation 検証には手を加えない。
- worker コマンド（`src/commands/worker.ts`）: 引数解釈、設定読み込み・プロジェクトポリシー読み取り・純粋関数の合成、`KEY=VALUE` 形式の出力、恒久設定の変更手順を示すヘルプ出力を担う。読み取り専用のティア解決サブコマンド `worker model-for-tier <model_tier>` を提供する。
- 起動ラッパー（`.agent-skill-chain/scripts/worker-launch.sh`）: 解決結果からアダプタファイルを選び、ティア名と reasoning effort をベンダー中立な環境変数として export し、`launch_worker` を呼ぶ。ティア名の意味を解釈せず、具体モデル文字列を扱わない。
- codex アダプタ（`.agent-skill-chain/adapters/codex.sh`）: ティア名から具体モデル文字列を得る単一の解決定義 `_codex_model_for_tier` を持つ。この定義は値を自前で保持せず、`worker model-for-tier` の出力を用いる。優先順位を適用して codex CLI 起動コマンドを組み立てる。
- claude / human アダプタ: 本 Issue では変更しない。ワーカー用のモデル選択環境変数を解釈しない。

責務集中を避けるため、「設定の読込」「選択の解決」「出力整形」「実行系への変換」を別モジュールへ分離する。純粋関数を独立させることで、CLI・スクリプト・アダプタのいずれを経由せずに単体検証できる。

### 依存関係

```text
worker-launch.sh → worker context（CLI） → 選択解決 → 設定読み込み → 設定スキーマ
worker-launch.sh → codex.sh（launch_worker）
codex.sh（_codex_model_for_tier） → worker model-for-tier（CLI） → 選択解決（純粋関数）
                                                              → プロジェクトポリシー読み取り（既存）
                                                                → project/manifest.yaml（値の正本）
codex.sh → codex CLI
```

依存は一方向であり循環しない。設定スキーマは実装に依存せず、選択解決は型と引数にのみ依存し、アダプタは CLI の出力にのみ依存する。本 Issue で新たに生じる依存は「ワーカー用のティア解決 → 登録済みプロジェクトポリシーの既存宣言」であり、方向は「新設側が既存資産を読む」の一方向に限る。既存のコア独立レビュー経路が新設側へ依存することはない。

プロジェクトポリシーはパッケージ既定値より上位の優先順位を持つ層であり、そこへ「このプロジェクトにおける具体モデル」を置き、パッケージ側の設定には「どのティアを使うか」だけを置く分担は、優先順位の規約（不変条件 ＞ プロジェクトポリシー ＞ 標準規約・既定値 ＞ adapter 既定動作）と整合する。

## 設定スキーマの設計

`worker` に任意項目 `segment_overrides` を追加する。キーは4セグメント名に限定し、値は次の任意項目だけを持つオブジェクトとする。

- `adapter`: `claude` / `codex` / `human` のいずれか。
- `model_tier`: `highest_capability` のみ。
- `reasoning_effort`: `medium` / `high` / `xhigh` のいずれか。

すべての階層で `additionalProperties: false` を維持するため、未知のセグメント名・未知のキーは拒否される。enum により未知のアダプタ名・未知のティア名も拒否される。加えて、`model_tier` または `reasoning_effort` を持つ上書きには `adapter: codex` を必須とする条件制約を置く。これは、これらの値を解釈しないアダプタ（claude・human）と組み合わせた設定が黙って無視されることを防ぐためであり、将来 claude 側がティアを解釈するようになった時点で制約を緩める（緩和は後方互換）。

許容値の集合は「本リポジトリで現に使われている値」に限定する。値の追加はスキーマ1箇所の変更で済み、その変更自体がレビュー対象になる。

`worker.adapter`（スカラー）は従来どおり任意項目のまま残し、意味は「セグメント別上書きが無いセグメントの既定アダプタ」とする。ティアと reasoning effort はセグメント別上書きの中にのみ置き、全セグメント共通の既定値は設けない（必要性が実証されていないため設けない）。

### 設定項目追加手続きの帰結

1. ハードコードで対応できない理由: どのセグメントをどの実行系で走らせるかは、利用可能な実行系の認証状況・費用・品質要求によって決まる運用上の選択であり、パッケージ側で固定すると当該利用者が変更する手段を失う。
2. プロジェクト単位で変わる必要性: codex CLI を導入していないプロジェクトは従来どおり claude だけで運用する必要があり、逆に本リポジトリのように実装だけ別実行系へ寄せたいプロジェクトもある。両者は同一の既定値では満たせない。
3. スキーマ版数の扱い: `agent-skill-chain/config/v1` のまま据え置く。追加はすべて任意項目であり、旧形式の設定は無変更で妥当かつ同一挙動を保つ（後方互換な追加）。版数は設定ファイル側の値と一致必須であるため、版を上げると既存のすべての設定ファイルが即座に不正となり、実利のない一斉書き換えを強制する。版を上げるのは必須項目の追加・許容値の削除・既存項目の意味変更といった破壊的変更に限る。
4. 既定値: `worker.segment_overrides` 未設定が既定である。この場合の解決結果は `worker.adapter`（未設定時は `human`）であり、ティアと reasoning effort は未解決（アダプタの従来フォールバック値を使用）となる。
5. migration: 不要（no-op）。既存の設定ファイルは書き換えずに妥当のまま動作する。移行スクリプト・変換処理は追加しない。
6. ADR: 作成する（ADR-0015）。ティア抽象化の採用、値の正本を既存宣言へ集約する判断、スキーマ版数据え置きの判断基準、恒久設定を更新する操作と実行主体の確定は、後から理由を再構成できない判断であるため記録する。

## 選択解決の設計

`worker context` は `<issue_id> [segment]` を受け取る。segment を伴う場合の解決規則は次のとおり。

- `adapter` = `worker.segment_overrides.<segment>.adapter` → `worker.adapter` → `human` の順で最初に見つかった値。
- `model_tier` = `worker.segment_overrides.<segment>.model_tier`（無ければ未解決）。
- `reasoning_effort` = `worker.segment_overrides.<segment>.reasoning_effort`（無ければ未解決）。

出力は `KEY=VALUE` の行集合とし、未解決のキーは行そのものを出力しない（空値の行を出さない）。segment を省略した呼び出しは従来どおり `adapter`（スカラー由来）・`backend`・`issue_number` の3行のみを返す。これにより、segment を渡していない既存の呼び出し（human アダプタが `backend` と `issue_number` を得るための呼び出し）は無変更で従来どおり動作する。

不正なセグメント名は解決せず、日本語の理由付きで失敗させる（推測して既定値へ倒さない）。

`worker context` は具体モデル文字列を出力しない。ティア名のままアダプタへ渡し、具体名への解決はアダプタの解決定義が要求した時点で行う。これにより、起動ラッパーと `worker context` の出力はベンダー中立に保たれ、モデル文字列を扱う箇所が codex アダプタの1経路に限定される。

## 起動ラッパーからアダプタへの伝達

起動ラッパーは `worker context <issue_id> <segment>` を1回呼び、`adapter` / `model_tier` / `reasoning_effort` を取り出す。アダプタ選択は従来どおり `adapter` で行い、残り2つを次のベンダー中立な環境変数として export したうえで `launch_worker` を呼ぶ。

- `ASC_WORKER_MODEL_TIER`: 解決されたティア名（例: `highest_capability`）。
- `ASC_WORKER_REASONING_EFFORT`: 解決された reasoning effort（例: `high`）。

未解決の場合は export しない（空文字を設定しない）。名前空間 `ASC_` は、既にワーカー・レビュア起動で使われている系列（`ASC_ISSUE_ID` / `ASC_SEGMENT` / `ASC_ROLE` / `ASC_REVIEW_MODEL`）と揃えたものであり、特定ベンダーの名前を含まないため claude・human アダプタが将来解釈することも妨げない。設定由来の値であることを示すため、アダプタ固有の上書き変数（`CODEX_` 系）とは名前空間を分ける。

codex アダプタにおけるモデルおよび reasoning effort の決定順序は次のとおりとし、既存のテスト用完全上書きが最優先である規則を維持する。

1. `CODEX_WORKER_CMD` または `WORKER_CMD`: 起動コマンド全体を差し替える。モデル・effort の解決自体を行わない。
2. アダプタ固有の個別上書き（`CODEX_IMPLEMENTATION_MODEL` / `CODEX_HIGH_CAPABILITY_MODEL` / `CODEX_IMPLEMENTATION_REASONING_EFFORT` / `CODEX_HIGH_CAPABILITY_REASONING_EFFORT`）: 指定されていればその値を使う。
3. 設定由来の値（`ASC_WORKER_MODEL_TIER` / `ASC_WORKER_REASONING_EFFORT`）: ティア名はアダプタの解決定義を経て具体モデル文字列へ解決する。
4. 従来のフォールバック（実装セグメント: `gpt-5.6-terra` / `medium`、それ以外: `gpt-5.6` / `high`）。

既存の `_codex_worker_model` / `_codex_worker_effort` は関数名・引数（セグメント名）を維持したまま、上記2〜4の順序を明示的に評価する実装へ置き換える。現在の実装は個別上書き環境変数と既定値を単一の既定値展開で兼ねているため、そのままでは3の層を挿入できないからである。ティア指定が無い場合の結果は現在と同一になる（AC-3）。

## codex アダプタのティア解決

ティア名から具体モデル文字列を得る定義は、codex アダプタ内の関数 `_codex_model_for_tier` 1つだけとする。この関数は値を自前で保持せず、agent-skill-chain CLI の読み取り専用サブコマンド `worker model-for-tier <model_tier>` の出力を用いる。同サブコマンドは、登録済みプロジェクトポリシーのコア独立レビュー契約に宣言された codex 用モデル文字列を、ティア `highest_capability` の解決先として返す。

この形を採る理由は、具体モデル文字列を本リポジトリ内で新たに複製しないことにある。当該文字列は既にプロジェクトポリシーの宣言として存在し、既存の読み取り関数がスキーマ検証付きで読み出している。ワーカー用に bash 側へ同じ値を書き写せば、モデル世代の更新時に更新箇所が1つ増え、片方だけ古い値が残る不整合が起こり得る。読み取り側として既存宣言を参照すれば、ワーカー用途とコア独立レビュー用途が同一の宣言から解決され、値の分岐が構造的に生じない。

解決できない場合は推測しない。次のいずれも、既定モデルへ暗黙に倒さず、起動コマンドを既存の失敗値へ倒して writer lease 取得後の blocked 報告・lease 解放・0 でも 3 でもない終了コードという既存のフェイルセーフ経路に乗せる。あわせて日本語の理由を標準エラー出力へ出す。

- 未知のティア名が渡された場合（設定スキーマの enum により正規経路では到達しないが、環境変数を直接設定した場合の防御として層を残す）。
- 登録済みプロジェクトポリシーが存在しない、または codex 用モデルを宣言していない場合。ティアの参照先が当該プロジェクトで宣言されていない状態であり、パッケージ側が代替値を推測してはならない。
- CLI 呼び出し自体が失敗した場合。

モデル世代が変わった場合の追従は、プロジェクトポリシーの宣言値の更新で完結する。設定ファイル・設定スキーマ・起動ラッパー・アダプタのいずれも変更を要さない。

## 具体モデル文字列の正本と既存資産との関係

本 Issue 適用後も、具体モデル文字列 `gpt-5.6-sol` の出現箇所は増えない。現況の出現箇所とその役割は次のとおりであり、いずれも本 Issue では変更しない。

- 登録済みプロジェクトポリシー（`.agent-skill-chain/project/manifest.yaml`）: 宣言データ本体。値の正本。ワーカー用のティア解決はここを読む。
- プロジェクトポリシーのスキーマ（`.agent-skill-chain/schemas/project-policy.schema.yaml`）: `const` による固定。コア監査レビュアのモデルが無断で差し替えられていないことを検証するセキュリティ機構であり、attestation 用環境変数と対になる。意図的な固定であって重複ではない。
- コア独立レビュー契約の型（`src/lib/model-selection.ts`）とコア監査証跡の検証（`src/lib/bootstrap-ledger.ts`）: 同じ機構の型レベル・実行時レベルの固定。
- 登録済み文書（`.agent-skill-chain/project/MODEL_TIER_TABLE.md`）: 上記契約を自然文で述べた規範文書。

したがって「ティア→具体モデル名の対応が1箇所に集約される」という主張は、ワーカー用ティア解決経路に限った主張として述べる。リポジトリ全体では、コア独立レビューの attestation 機構が意図的に同じ値を複数箇所へ固定している。この固定は「無断の差し替えを検出する」ことが目的であり、集約してしまうと機構が成立しない。モデル世代を更新する作業は、値の正本（プロジェクトポリシーの宣言）と、この attestation 機構の固定値を同一変更で更新することになる。ワーカー用途はその作業に新たな更新箇所を追加しない。

codex アダプタには既存のフォールバックモデル文字列（実装セグメント向けと汎用の2つ）が残る。これらはティア未指定時の従来動作を保つためのものであり、AC-3 が無変更を要求している。本 Issue はこれらを増やさず、また `gpt-5.6-sol` を bash 側へ書かない。

### ティア語彙の並立について

本リポジトリには既にティア語彙が存在する。コア独立レビュー契約の `frontier_coding`（モデル能力の要求水準）と `maximum_reasoning`（推論強度の要求水準）である。これらは「レビュアに要求する水準の宣言」であり、スキーマで `const` に固定され、attestation で照合される期待値として使われる。値そのものは選択の指示ではない。

本 Issue の `highest_capability` は「ワーカー起動時に選ぶモデルの区分」であり、解決される選択値である。reasoning effort は別フィールドで持つため、ティアはモデルの1軸のみを表す。

既存語彙 `frontier_coding` を再利用しない理由は次の2点である。第一に、`frontier_coding` はプロジェクトポリシーのスキーマで `const` として固定されたセキュリティ機構の語彙であり、同じ名前を設定側の「選択の指示」へ流用すると、照合される期待値と解決される選択値が同名になり、検証側と選択側の区別が名前から付かなくなる。第二に、SPEC が語彙として `highest_capability` を確定させ、他のティア名の新規定義をスコープ外としている。

語彙は2つ並立するが、両者が指す具体モデルは同一の宣言から解決されるため、値が分岐することはない。ティア語彙自体を1つへ統合するかどうかは、コア独立レビューの契約表現に影響するため本 Issue の範囲外とし、別 Issue の論点として残す。

## SPEC との差異

SPEC.md の AC-9 は「具体モデル文字列がアダプタ内の単一のマッピング定義から解決される」、制約は「具体的なモデル文字列は設定ファイル・スキーマに書かず、アダプタ内部のティアマッピングにのみ保持する」と述べている。本設計は次の点で文言と異なる。

- ティア解決の定義はアダプタ内の単一の関数として存在する（アダプタが解決の責務を持つ点は満たす）。
- ただし解決に用いる値はアダプタ内に保持せず、既存のプロジェクトポリシー宣言を読み取る。

この差異は、「モデル世代の更新時に直す箇所を分散させない」という上位の要求を満たすための実現手段の選択である。SPEC の受入条件が検証しようとしている性質——設定にはティア名だけを書ける、モデル世代の更新が設定ファイル・設定スキーマ・CLI・起動ラッパーの変更を要さない、アダプタが単一のティア解決定義を持つ、未知のティアを推測しない——はいずれも満たされ、AC-9 は機械的に検証可能なままである。SPEC 本文の文言を差異に合わせて更新するかどうかは、承認済み仕様の変更に当たるため設計セグメントでは行わず、進行役の判断に委ねる。

## 恒久設定を更新する操作・実行主体・タイミング

恒久設定の変更は、設定ファイル `.agent-skill-chain/config/agent-skill-chain.yaml` の直接編集で行う。設定を書き換える CLI サブコマンドは新設しない（本 Issue で追加するのは読み取り専用の解決サブコマンドのみである）。理由は次のとおり。

- 設定ファイルは Git 管理下の正本アセットであり、その変更は成果物ブランチへの commit として PR・ゲート・レビューという既存の統制を通る。コマンド一発で証跡なく書き換わる経路を作ると、恒久的な方針変更が検証されないまま入る。
- 書き換えコマンドは、コメント保持・整形・スキーマ検証・実行主体の制限という4つの新しい問題を持ち込む。1行の追加で足りる変更にこれらを抱えるのは、疑わしい機能を追加しない方針に反する。
- 書き換えコマンドを設けると、CLI を実行できる進行役がそれを実行できてしまい、I5 の境界を運用でしか守れなくなる。編集をワーカーによるファイル編集に限定すれば、writer lease と branch protection という機械的な統制がそのまま効く。

実行主体は writer lease を保持するセグメント作業ワーカーであり、進行役は編集しない。設定ファイルは調整状態（Issue・ラベル・PR review 証跡・マージ・worktree ライフサイクル）ではなく、Git 管理下の正本アセット＝成果物側の資産である。調整状態の正本は Coordination Backend のプリミティブに限るという規範（I6）と、進行役は成果物ブランチへ commit せず成果物を著述しないという規範（I5）から、進行役が設定ファイルを直接編集することは許されない。「設定資産だから調整状態寄りとして進行役が触ってよい」とはしない。

タイミングは、当該変更を扱う Issue の実装セグメントである。進行役の関与は、対話で受けた指示内容（どのセグメントをどの実行系・ティアで実行するか）を Issue へ記録し、通常どおりワーカーを起動することに限られる。この分担であれば、進行役は調整状態のみを書き、内容の著述はワーカーが行うため I5 を侵さない。

この手順は、設定ファイルの `worker` セクション直上のコメント（正本）と `worker context` のヘルプ出力に記載する。ヘルプ出力には、変更操作・実行主体・タイミングの要旨と、現在の解決結果を確認する手段（`worker context <issue_id> <segment>`）を含める。

## 関連ADR

```yaml
related_adrs: []
```

本 Issue の判断は、同じ設計セグメントで `status: proposed` として作成する `ADR-0015` に記録する。未承認 ADR は構造化参照へ入れず、設計に必要な判断——ティア抽象化の採用、値の正本を既存宣言へ集約すること、コア独立レビュー機構を変更しないこと、スキーマ版数の据え置き、恒久設定を更新する操作と実行主体、伝達経路と優先順位、SPEC との差異——はすべて本文へ自己完結して記載する。

## 障害・ロールバック考慮

想定される失敗モード:

- 設定に codex を指定した環境で codex CLI が不在・未認証: 既存のフェイルセーフにより writer lease 取得後に blocked 報告・lease 解放・0 でも 3 でもない終了コードとなり、人間確認へ倒れる。誤った成果物が作られることはない。
- ティアの参照先が宣言されていない（登録済みプロジェクトポリシーが無い、または codex 用モデルを宣言していない）: 同じフェイルセーフ経路へ倒す。パッケージ側で代替モデルを推測しない。
- 未知のティア名が環境変数経由で渡る: 同じフェイルセーフ経路へ倒す。
- スキーマ拡張の誤りで既存設定が不正判定される: 設定読み込み時点で日本語の理由付きで失敗するため、ワーカー起動より前に検出される。単体テストで旧形式の受理を明示的に固定する。
- 起動ラッパーが解決結果を取り違える: ティアと reasoning effort を未解決のまま export しない設計のため、取り違えた場合の症状は「従来のフォールバック値で動く」であり、暴走ではなく退行として現れる。結合テストで環境変数の値と起動コマンドを直接確認する。

### 配布既定値への波及と、オプトアウト手段が存在しないこと

設定ファイルはパッケージへ同梱され、導入時にそのまま複製される。さらに更新コマンドは、`.agent-skill-chain/` 配下の正本アセット（設定を含む）を衝突検知なしの無条件上書きで同期する（不可侵はプロジェクトポリシーと展開済みの GitHub 資産のみ）。したがって次が成り立つ。

- 本リポジトリの実装セグメント上書きは、新規導入プロジェクトの初期値になる。
- 既存導入プロジェクトが当該上書きをローカルで削除しても、次回の更新コマンド実行で必ず復活する。設定ファイルに対する恒久的なオプトアウト手段は現時点で存在しない。
- codex CLI を導入していない、またはティアの参照先を宣言していない導入先では、更新のたびに実装セグメントが blocked（人間確認が必要な停止）へ倒れ続ける。

これは意図された挙動である。実装セグメントを最高能力の実行系で走らせることを全体既定として強制する、という所有者の明示的判断に基づく。AGENTS.md が掲げる「既定は常に安全側」という原則との緊張関係は認識している——導入先の環境で成立しない既定値を配ることは、導入先にとって安全な既定とは言えない。それでもこの判断を採る根拠は、(a) 失敗の現れ方が誤った成果物ではなく停止であり、危険側へ黙って倒れないこと、(b) 更新コマンドは人間が明示的に実行する操作であり、実行により正本アセットが同期されることは既に規範文書が述べていること、(c) 導入先には、更新の影響を受けないプロジェクトポリシーの層で自プロジェクトのティア参照先を宣言するという恒久的な手段があること、の3点である。

配布既定値と本リポジトリ自身の運用設定を分離すること、および更新コマンドが設定ファイルのローカル変更を保護する方式（マージ・不可侵化・オプトアウト表現の導入）は、いずれも本 Issue の範囲を超える構造上の論点であり、別 Issue として扱う（起票の判断は進行役に委ねる）。

### ロールバック手順

- 本リポジトリの設定だけを戻す場合: `worker.segment_overrides` の当該セグメントの上書きを削除する。他の変更（スキーマ・CLI・アダプタ）は任意項目の追加と優先順位の明示化のみであるため、設定を戻せば挙動は本 Issue 適用前と一致する。導入先においては、この削除は次回の更新コマンドまでしか持続しない（前節）。
- 全体を戻す場合: 本 Issue の commit を revert する。永続状態・migration を持たないため、データ移行や後始末は不要である。成果物 digest の変化により下流ゲートは自動的に無効化され、再通過が要求される。

影響を受ける既存機能: ワーカー起動経路（`worker context`・起動ラッパー・codex アダプタ）。ゲートレビュア起動、コア独立レビューの契約・トリガー・attestation 検証、writer lease、Check Run、4セグメント構成には変更を加えない。プロジェクトポリシーは読み取りのみで、書き換えない。human アダプタの `worker context` 呼び出しは segment を渡さない従来形のまま維持する。

## 検証方法

- 単体: 選択解決（セグメント別解決・フォールバック・スカラーのみ・未設定）、ティア解決の純粋関数（宣言ありでモデル文字列を返す・宣言なしで解決不能を返す・未知ティアで解決不能を返す）、スキーマ（新形式受理・旧形式受理・未知アダプタ／未知ティア／未知セグメント／未知キー／不正な組合せの拒否）。
- 結合: 起動ラッパーからアダプタまでの伝達（環境変数の値と codex 起動コマンドのモデル・reasoning effort 指定）、完全上書き環境変数の優先、ティア未指定時の従来値、ティア解決不能時の blocked 経路、本リポジトリ設定での実装セグメント解決結果、ティア解決結果が登録済みプロジェクトポリシーの宣言値と一致すること（bash 側に複製が無いことの機械的な担保）。
- 常時必須: lint / format、型検査、単体テスト、変更範囲の結合テスト、静的アプリケーションセキュリティテスト、依存関係・secret スキャン。

## 完了条件

SPEC.md の AC-1〜AC-9 に対応する設計要素が本文書に定義され、実装単位と順序が `PLAN.md` に定義され、判断の根拠が ADR-0015 に記録されていること。

## 未決事項

なし。SPEC.md が設計セグメントへ委ねた4点（更新操作の形、スキーマ版数、伝達経路の具体形、ティア解決の実装形と未知ティアの扱い）は本文書で確定した。SPEC 本文の文言を本設計との差異に合わせて更新するかどうかは、承認済み仕様の変更に当たるため設計セグメントの判断事項ではなく、進行役へ引き継ぐ。

## 対象外

ゲートレビュア側のセグメント別選択、コア独立レビューの契約・トリガー・レビュア選定・attestation 検証の変更、codex 以外の新規アダプタ、claude / human アダプタのモデル選択方式の変更、`highest_capability` 以外のティア定義、ティア語彙（`frontier_coding` 系と `highest_capability`）の統合、4セグメント構成の変更、起動の都度切り替える実行時オプション、配布既定値と本リポジトリ運用設定の分離、更新コマンドが設定ファイルのローカル変更を保護する方式の導入。
