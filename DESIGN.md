# DESIGN: セグメント別 worker アダプタ・モデルティア選択の恒久設定

- Issue: `ISSUE-307`
- 対応する SPEC: `SPEC.md`
- 対象ブランチ: `feature/307-segment-worker-adapter-config`

## 目的・対象範囲

4セグメント（spec・design・implementation・validation）の作業ワーカーを起動する際に選ばれる実行系アダプタ（`claude` / `codex` / `human`）とモデル選択を、セグメント単位で設定の正本へ恒久記録できるようにする。あわせて、具体的なモデル文字列の保持場所を設定ファイルの `worker.model_tiers` ただ1箇所に定め、ティア名から具体名への解決を CLI 側で完結させる。

対象は「ワーカー起動時のアダプタ・モデル選択の解決経路」だけである。すなわち設定スキーマ、設定ファイル、設定読み込みと選択解決、`worker context` の出力、起動ラッパーからアダプタへの伝達、codex アダプタにおける優先順位の6点である。ゲートレビュア側の選択、コア独立レビュー用のモデル宣言と attestation 機構、4セグメント構成、writer lease・ゲート・Check Run の状態遷移は本設計の対象外であり、参照も変更もしない。

## 前提

- 設定の正本は `.agent-skill-chain/config/agent-skill-chain.yaml` であり、その構造検査は `.agent-skill-chain/schemas/config.schema.yaml` が行う。設定読み込みはスキーマ違反時に日本語の理由付きで失敗する。
- ワーカー起動の経路は「進行役 → `.agent-skill-chain/scripts/worker-launch.sh` → `agent-skill-chain worker context` で解決 → 選ばれたアダプタの `launch_worker`」である。
- 現在の `worker context` は Issue ID のみを入力とし、`adapter` / `backend` / `issue_number` を `KEY=VALUE` 形式で返す。human アダプタもこの出力（`backend` / `issue_number`）を利用している。
- codex アダプタは現在、実装セグメントには `gpt-5.6-terra` / `medium`、それ以外には `gpt-5.6` / `high` をフォールバック既定値として持ち、環境変数でのみ上書きできる。これらの既存フォールバック値は本 Issue では削除しない（ティア未指定時の従来動作を保つため）。
- codex アダプタは、起動コマンド全体を差し替える環境変数（`CODEX_WORKER_CMD`、後方互換の `WORKER_CMD`）をテスト用の完全上書き経路として解釈する。
- codex CLI が存在しない・認証が成立しない場合は、writer lease 取得後に blocked 報告・lease 解放・0 でも 3 でもない終了コードで返すフェイルセーフが既に存在する。
- 起動ラッパーは、アダプタを解決できない場合など「まだ何も起動していない」段階の失敗を、lease 取得前の error（0 でも 3 でもない終了コード）として返す既存のフェイルセーフを持つ。
- 本リポジトリには、コア独立レビュー用のモデル宣言と attestation 機構が既に存在する（登録済みプロジェクトポリシー・そのスキーマの固定値・関連する型と実行時検証）。これは本 Issue と目的も経路も異なる別の仕組みであり、本設計はこれを参照も変更もしない。
- 進行役は調整状態（Issue・ラベル・PR review 証跡・マージ・worktree ライフサイクル）のみを読み書きし、成果物の著述・内容の取り込みを行わない（不変条件 I5）。
- 本 Issue が変更するパスは設定・スキーマ・アダプタ・スクリプト・CLI 実装であり、登録済みプロジェクトポリシーが定めるコア変更の対象に該当する。したがって独立レビューは Strict（レビュア2体）で実施される。

## 用語（本設計で用いる語）

- セグメント別上書き: セグメント名をキーに、そのセグメントにのみ適用する値の集合。設定キーは `worker.segment_overrides.<segment>`。値は `adapter`・`model_tier`・`reasoning_effort` のみで、具体的なモデル文字列を含まない。
- モデルティア: 具体的なモデル文字列を含まない抽象名。本 Issue で扱う値は `highest_capability` のみ。
- ティア対応表: ティア名と実行系アダプタの組から具体的なモデル文字列を与える構造化データ。設定キーは `worker.model_tiers`。具体的なモデル文字列を保持する唯一の場所。
- ティア解決: ティア対応表を引いて具体的なモデル文字列を得ること。CLI 側で完結する。
- worker 起動コンテキスト: `worker context` が返す `KEY=VALUE` 形式の解決結果。
- 選択解決: 設定とセグメント名から `adapter` / `model_tier` / `model` / `reasoning_effort` を決める純粋な計算。

## 要件 → 設計要素の対応表

| 要件 / AC-ID | 対応する設計要素 | 備考 |
|---|---|---|
| AC-1 セグメント別のアダプタ解決 | 選択解決モジュール（`src/lib/worker-selection.ts`、新設）、`worker context`（`src/commands/worker.ts`）の segment 引数 | 上書き→スカラー→`human` の3段フォールバック |
| AC-2 ティア・reasoning effort の解決と codex 実行への反映 | 選択解決モジュールのティア解決、`worker context` の出力キー拡張、`worker-launch.sh` の環境変数 export、codex アダプタの優先順位層 | 完全上書き環境変数が最優先である順序を維持 |
| AC-3 既存設定の後方互換 | 選択解決モジュールのフォールバック、`worker context` の segment 省略時挙動、codex アダプタの既存フォールバック値の保持 | 既存設定・既存呼び出しの結果を変えない |
| AC-4 スキーマによる新旧両形式の検証 | `.agent-skill-chain/schemas/config.schema.yaml` の `worker` 定義拡張（`segment_overrides` と `model_tiers`） | 任意項目追加・`additionalProperties: false`・enum・組合せ制約。モデル値は固定値にしない |
| AC-5 解決ロジックの単体テスト | 選択解決モジュール・ティア解決・スキーマに対する単体テスト（`test/unit/`） | 解決・フォールバック・解決失敗・受理・拒否の5観点 |
| AC-6 本リポジトリの実装セグメントの恒久切替 | `.agent-skill-chain/config/agent-skill-chain.yaml` の `worker.segment_overrides.implementation` と `worker.model_tiers` | 上書き側はティア名のみ、具体名は対応表のみ |
| AC-7 設定更新手順の文書化 | 設定ファイル `worker` セクションのコメント（正本）、`worker context` のヘルプ出力 | 操作・実行主体・タイミングと I5 との関係を明記 |
| AC-8 設定項目追加手続きの帰結の明記 | 本文書「設定項目追加手続きの帰結」、ADR-0015 | 5点（理由・可変性・スキーマ版数・既定値・migration） |
| AC-9 ティア対応表の存在と更新容易性 | `worker.model_tiers` の単一エントリ、スキーマ上のモデル値の型（固定値にしない） | 追従は当該エントリの値の変更のみ。コード・スキーマの変更を要さない |

## 責務・境界

### コンポーネント構成

- 設定スキーマ（`.agent-skill-chain/schemas/config.schema.yaml`）: 設定ファイルの構造・許容値・許容される組合せだけを定義する。具体的なモデル文字列を固定値として持たない。
- 設定ファイル（`.agent-skill-chain/config/agent-skill-chain.yaml`）: セグメント別上書きとティア対応表を保持する。具体的なモデル文字列はティア対応表にのみ現れる。
- 設定読み込み（`src/lib/config.ts`）: 設定ファイルの読込・スキーマ検証・型付けのみを担う。選択の判断は持たない。
- 選択解決（`src/lib/worker-selection.ts`、新設）: 設定値とセグメント名から `adapter` / `model_tier` / `reasoning_effort` を決める純粋関数と、ティア対応表とティア名・アダプタ名から具体的なモデル文字列を決める純粋関数（解決失敗を明示的に区別して返す）を持つ。ファイル入出力・プロセス起動・環境変数の読み書きを行わない。単体テストの主対象。
- worker context コマンド（`src/commands/worker.ts`）: 引数解釈、設定読み込みと純粋関数の合成、`KEY=VALUE` 形式の出力、恒久設定の変更手順を示すヘルプ出力を担う。
- 起動ラッパー（`.agent-skill-chain/scripts/worker-launch.sh`）: 解決結果からアダプタファイルを選び、解決済みのモデル文字列と reasoning effort をベンダー中立な環境変数として export し、`launch_worker` を呼ぶ。ティア対応表を読まず、ティア名から具体名を導く処理も持たない。
- codex アダプタ（`.agent-skill-chain/adapters/codex.sh`）: 受け取った値をそのまま codex CLI 起動コマンドへ反映する。ティア名から具体名を導く処理・具体的なモデル文字列の新規追加をいずれも持たない。
- claude / human アダプタ: 本 Issue では変更しない。ワーカー用のモデル選択環境変数を解釈しない。

責務集中を避けるため、「設定の読込」「選択の解決」「出力整形」「実行系への変換」を別モジュールへ分離する。ティア解決を純粋関数として独立させることで、CLI・スクリプト・アダプタのいずれを経由せずに単体検証できる。

### 依存関係

```text
worker-launch.sh → worker context（CLI） → 選択解決（純粋関数） → 設定読み込み → 設定スキーマ
                                                              → 設定ファイル（ティア対応表）
worker-launch.sh → codex.sh（launch_worker） → codex CLI
```

依存は一方向であり循環しない。設定スキーマは実装に依存せず、選択解決は型と引数にのみ依存し、アダプタは環境変数として渡された値にのみ依存する。アダプタが設定ファイルを読むことはなく、CLI を呼び出してモデルを解決することもない。コア独立レビュー用のモデル宣言・その読み取り関数・attestation 機構への依存は持たない。

## 設定スキーマの設計

`worker` に次の2つの任意項目を追加する。

`segment_overrides`: キーは4セグメント名に限定し、値は次の任意項目だけを持つオブジェクトとする。

- `adapter`: `claude` / `codex` / `human` のいずれか。
- `model_tier`: `highest_capability` のみ。
- `reasoning_effort`: `medium` / `high` / `xhigh` のいずれか。

`model_tiers`: キーはティア名に限定し（現時点では `highest_capability` のみを許容）、値は実行系アダプタ名をキーとするオブジェクトとする。現時点で許容するアダプタ名は `codex` のみとし、必須とする。モデル文字列は最小長1の通常の文字列として検証し、固定値にしない。これは、モデル世代の更新が設定値の変更だけで完結し、スキーマ変更を伴わないようにするためである（AC-9）。

すべての階層で `additionalProperties: false` を維持するため、未知のセグメント名・未知のティア名・未知のアダプタ名・未知のキーは拒否される。加えて、`model_tier` または `reasoning_effort` を持つセグメント別上書きには `adapter: codex` を必須とする条件制約を置く。これは、これらの値を解釈しないアダプタ（claude・human）と組み合わせた設定が黙って無視されることを防ぐためであり、将来 claude 側がティアを解釈するようになった時点で制約を緩める（緩和は後方互換）。

ティア名を開いた文字列パターンではなく閉じた列挙とするのは、セグメント別上書きの `model_tier` 側も同じ列挙で検証しており、両者を同じ集合に保つためである。開いた集合にすると、対応表に定義したティアを `model_tier` から参照できない（あるいはその逆の）不整合が設定ファイル内で成立してしまう。ティアを増やす変更はスキーマ2箇所の列挙追加を伴うが、これはモデル世代の更新とは別種の、レビューされるべき語彙の追加である。

`worker.adapter`（スカラー）は従来どおり任意項目のまま残し、意味は「セグメント別上書きが無いセグメントの既定アダプタ」とする。

### 設定項目追加手続きの帰結

1. ハードコードで対応できない理由: どのセグメントをどの実行系で走らせるかは、利用可能な実行系の認証状況・費用・品質要求によって決まる運用上の選択であり、パッケージ側のコードで固定すると当該利用者が変更する手段を失う。ティアに対応する具体的なモデル文字列も、実行系のモデル世代が更新されるたびに変わる値であり、コードへ埋め込むとコード変更とリリースを要する。
2. プロジェクト単位で変わる必要性: codex CLI を導入していないプロジェクトは従来どおり claude だけで運用する必要があり、逆に本リポジトリのように実装だけ別実行系へ寄せたいプロジェクトもある。利用可能なモデルも契約や提供状況によってプロジェクトごとに異なりうる。両者は同一の既定値では満たせない。
3. スキーマ版数の扱い: `agent-skill-chain/config/v1` のまま据え置く。追加はすべて任意項目であり、旧形式の設定は無変更で妥当かつ同一挙動を保つ（後方互換な追加）。版数は設定ファイル側の値と一致必須であるため、版を上げると既存のすべての設定ファイルが即座に不正となり、実利のない一斉書き換えを強制する。版を上げるのは必須項目の追加・許容値の削除・既存項目の意味変更といった破壊的変更に限る。
4. 既定値: `worker.segment_overrides` 未設定・`worker.model_tiers` 未設定が既定である。この場合の解決結果は `worker.adapter`（未設定時は `human`）であり、モデルと reasoning effort は未解決（アダプタの従来フォールバック値を使用）となる。
5. migration: 不要（no-op）。既存の設定ファイルは書き換えずに妥当のまま動作する。移行スクリプト・変換処理は追加しない。
6. ADR: 作成する（ADR-0015）。ティア抽象化の採用、具体的なモデル文字列の保持場所の確定、スキーマ版数据え置きの判断基準、恒久設定を更新する操作と実行主体の確定は、後から理由を再構成できない判断であるため記録する。

## 選択解決の設計

`worker context` は `<issue_id> [segment]` を受け取る。segment を伴う場合の解決規則は次のとおり。

- `adapter` = `worker.segment_overrides.<segment>.adapter` → `worker.adapter` → `human` の順で最初に見つかった値。
- `model_tier` = `worker.segment_overrides.<segment>.model_tier`（無ければ未解決）。
- `model` = `model_tier` が解決されている場合に、`worker.model_tiers.<model_tier>.<adapter>` を引いた値。
- `reasoning_effort` = `worker.segment_overrides.<segment>.reasoning_effort`（無ければ未解決）。

出力は `KEY=VALUE` の行集合とし、未解決のキーは行そのものを出力しない（空値の行を出さない）。segment を省略した呼び出しは従来どおり `adapter`（スカラー由来）・`backend`・`issue_number` の3行のみを返す。これにより、segment を渡していない既存の呼び出し（human アダプタが `backend` と `issue_number` を得るための呼び出し）は無変更で従来どおり動作する。

不正なセグメント名は解決せず、日本語の理由付きで失敗させる（推測して既定値へ倒さない）。

`model_tier` が解決されているのに対応表を引けない場合（対応表が無い、当該ティアのエントリが無い、当該アダプタ用のモデルが無い）は、値を推測せず、日本語の理由付きで失敗させる。この失敗はワーカー起動前・writer lease 取得前に起こるため、起動ラッパーはこれを既存の「まだ何も起動していない段階の error（0 でも 3 でもない終了コード）」として返す。lease を取得していない段階で blocked 報告を書くことはしない（報告のためだけに lease を取得するのは writer lease の意味を壊すため採らない）。

未知のティア名は設定スキーマの列挙により設定ファイルの段階で拒否されるため、正規経路ではこの失敗に至らない。

## 起動ラッパーからアダプタへの伝達

起動ラッパーは `worker context <issue_id> <segment>` を1回呼び、`adapter` / `model_tier` / `model` / `reasoning_effort` を取り出す。アダプタ選択は従来どおり `adapter` で行い、次のベンダー中立な環境変数を export したうえで `launch_worker` を呼ぶ。

- `ASC_WORKER_MODEL`: ティア対応表から解決済みの具体的なモデル文字列。
- `ASC_WORKER_REASONING_EFFORT`: 解決された reasoning effort。
- `ASC_WORKER_MODEL_TIER`: 解決されたティア名。アダプタ側の防御的検査にのみ用いる。

未解決の場合は export しない（空文字を設定しない）。名前空間 `ASC_` は、既にワーカー・レビュア起動で使われている系列（`ASC_ISSUE_ID` / `ASC_SEGMENT` / `ASC_ROLE` / `ASC_REVIEW_MODEL`）と揃えたものであり、特定ベンダーの名前を含まないため claude・human アダプタが将来解釈することも妨げない。

既存の `CODEX_IMPLEMENTATION_MODEL` 等のアダプタ固有変数を起動ラッパーが直接設定する案は採らない。起動ラッパーがセグメントごとの変数名の対応を知る必要が生じてベンダー中立性が失われること、および利用者・テストによる明示的な上書きと設定由来の値が同じ変数に混ざり、優先順位を機械的に区別できなくなることが理由である。

codex アダプタにおけるモデルおよび reasoning effort の決定順序は次のとおりとし、既存のテスト用完全上書きが最優先である規則を維持する。

1. `CODEX_WORKER_CMD` または `WORKER_CMD`: 起動コマンド全体を差し替える。モデル・effort の解決自体を行わない。
2. アダプタ固有の個別上書き（`CODEX_IMPLEMENTATION_MODEL` / `CODEX_HIGH_CAPABILITY_MODEL` / `CODEX_IMPLEMENTATION_REASONING_EFFORT` / `CODEX_HIGH_CAPABILITY_REASONING_EFFORT`）: 指定されていればその値を使う。
3. 設定由来の解決済み値（`ASC_WORKER_MODEL` / `ASC_WORKER_REASONING_EFFORT`）。
4. 従来のフォールバック（実装セグメント: `gpt-5.6-terra` / `medium`、それ以外: `gpt-5.6` / `high`）。

既存の `_codex_worker_model` / `_codex_worker_effort` は関数名・引数（セグメント名）を維持したまま、上記2〜4の順序を明示的に評価する実装へ置き換える。現在の実装は個別上書き環境変数と既定値を単一の既定値展開で兼ねているため、そのままでは3の層を挿入できないからである。ティア指定が無い場合の結果は現在と同一になる（AC-3）。アダプタにティア名から具体名を導く関数は置かない。

防御的検査として、`ASC_WORKER_MODEL_TIER` が設定されているのに `ASC_WORKER_MODEL` が空である場合は、従来フォールバックへ黙って落ちず、起動コマンドを既存の失敗値へ倒して writer lease 取得後の blocked 報告・lease 解放・0 でも 3 でもない終了コードという既存のフェイルセーフ経路に乗せる。あわせて日本語の理由を標準エラー出力へ出す。正規経路ではこの状態に至らない（起動ラッパーの手前で失敗する）が、アダプタが単独で呼ばれた場合に「ティアを指定したのに別のモデルで走る」ことを防ぐ。

## 具体モデル文字列の正本と既存資産との関係

本 Issue における具体的なモデル文字列の正本は、設定ファイルの `worker.model_tiers` ただ1箇所である。設定スキーマはモデル値を固定値として持たず、起動ラッパーとアダプタも保持しない。したがってモデル世代の更新は、この対応表のエントリの値を書き換えるだけで完結する。

本リポジトリには、これとは別に `gpt-5.6-sol` という文字列が既に複数箇所に存在する。登録済みプロジェクトポリシーの宣言、プロジェクトポリシーのスキーマにおける固定値、コア独立レビュー契約の型、コア監査証跡の実行時検証、登録済みのモデル選択方針文書、および関連するテストである。これらはコア独立レビュー（ゲートレビュア）用の attestation 機構であり、コア監査レビュアのモデルが無断で差し替えられていないことを検証するために意図的に複数箇所へ固定されている。集約すると機構が成立しないため集約対象ではない。

本 Issue はこの機構に一切触れない。読み取りも行わない。結果として、同一のモデル文字列がリポジトリ内に2系統（ワーカー用のティア対応表と、コア独立レビュー用の attestation 機構）で独立して存在する。この重複は意図的なものであり、次の理由による。

- 目的が異なる。ワーカー用は「起動時に選ぶモデルの指示」であり、attestation 用は「レビュアのモデルが差し替えられていないことの照合値」である。前者は利用者が変更してよく、後者は変更されたら検出されなければならない。
- 変更の自由度が異なる。attestation 用の値はスキーマで固定されており、導入先が自分の都合で変更する余地が無い。ワーカー用の値を同じ宣言に相乗りさせると、モデル選択を変えるためにセキュリティ機構の固定値を触ることになる。
- 結合させると副作用が生じる。ワーカーのモデル解決がコア監査の分類・判定経路と結び付き、片方の変更が他方の挙動へ波及しうる。

モデル世代が変わる場合、両系統をそれぞれ更新することになる。ワーカー用は設定値1箇所、attestation 用は当該機構が定める複数箇所（固定値の性質上まとめられない）である。本 Issue はワーカー用の更新箇所を1箇所に保ち、attestation 用の箇所を増やしも減らしもしない。

codex アダプタには既存のフォールバックモデル文字列（実装セグメント向けと汎用の2つ）が残る。これらはティア未指定時の従来動作を保つためのものであり、AC-3 が無変更を要求している。本 Issue はこれらを増やさず、アダプタのソースへ新たな具体モデル文字列を追加しない。

### ティア語彙の並立について

本リポジトリには既にティア語彙が存在する。コア独立レビュー契約の `frontier_coding`（モデル能力の要求水準）と `maximum_reasoning`（推論強度の要求水準）である。これらは「レビュアに要求する水準の宣言」であり、スキーマで固定され、attestation で照合される期待値として使われる。値そのものは選択の指示ではない。なお、claude アダプタがティアから具体モデル名を導く対応表を持っているわけではない。claude アダプタは渡されたティア名が期待値と一致するかを検証し、具体モデル名は別の環境変数から受け取るだけである。

本 Issue の `highest_capability` は「ワーカー起動時に選ぶモデルの区分」であり、対応表を引いて解決される選択値である。reasoning effort は別フィールドで持つため、ティアはモデルの1軸のみを表す。

既存語彙 `frontier_coding` を再利用しない理由は2点である。第一に、`frontier_coding` はプロジェクトポリシーのスキーマで固定された attestation 機構の語彙であり、同じ名前を設定側の「選択の指示」へ流用すると、照合される期待値と解決される選択値が同名になり、検証側と選択側の区別が名前から付かなくなる。第二に、SPEC が語彙として `highest_capability` を確定させ、他のティア名の新規定義をスコープ外としている。

語彙は2つ並立し、現時点では同じモデルを指す。値は独立に保持されるため、片方の更新が他方へ自動的には波及しない。この点は前節に記したとおり意図的な選択である。ティア語彙自体を1つへ統合するかどうかは、コア独立レビューの契約表現に影響するため本 Issue の範囲外とし、別 Issue の論点として残す。

## SPEC との整合

差異なし。SPEC.md は本設計と同一の方式（具体的なモデル文字列の保持場所は設定ファイルの `worker.model_tiers`、ティア解決は CLI 側で完結、アダプタは解決済みの値を受け取る）で記述されており、目的・背景・用語・要件・受入条件・制約・未決事項のいずれにも本設計と矛盾する記述は無い。

## 恒久設定を更新する操作・実行主体・タイミング

恒久設定の変更は、設定ファイル `.agent-skill-chain/config/agent-skill-chain.yaml` の直接編集で行う。設定を書き換える CLI サブコマンドは新設しない。理由は次のとおり。

- 設定ファイルは Git 管理下の正本アセットであり、その変更は成果物ブランチへの commit として PR・ゲート・レビューという既存の統制を通る。コマンド一発で証跡なく書き換わる経路を作ると、恒久的な方針変更が検証されないまま入る。
- 書き換えコマンドは、コメント保持・整形・スキーマ検証・実行主体の制限という4つの新しい問題を持ち込む。数行の追加で足りる変更にこれらを抱えるのは、疑わしい機能を追加しない方針に反する。
- 書き換えコマンドを設けると、CLI を実行できる進行役がそれを実行できてしまい、I5 の境界を運用でしか守れなくなる。編集をワーカーによるファイル編集に限定すれば、writer lease と branch protection という機械的な統制がそのまま効く。

実行主体は writer lease を保持するセグメント作業ワーカーであり、進行役は編集しない。設定ファイルは調整状態（Issue・ラベル・PR review 証跡・マージ・worktree ライフサイクル）ではなく、Git 管理下の正本アセット＝成果物側の資産である。調整状態の正本は Coordination Backend のプリミティブに限るという規範（I6）と、進行役は成果物ブランチへ commit せず成果物を著述しないという規範（I5）から、進行役が設定ファイルを直接編集することは許されない。「設定資産だから調整状態寄りとして進行役が触ってよい」とはしない。

タイミングは、当該変更を扱う Issue の実装セグメントである。進行役の関与は、対話で受けた指示内容（どのセグメントをどの実行系・ティアで実行するか）を Issue へ記録し、通常どおりワーカーを起動することに限られる。この分担であれば、進行役は調整状態のみを書き、内容の著述はワーカーが行うため I5 を侵さない。

この手順は、設定ファイルの `worker` セクション直上のコメント（正本）と `worker context` のヘルプ出力に記載する。ヘルプ出力には、変更操作・実行主体・タイミングの要旨と、現在の解決結果を確認する手段（`worker context <issue_id> <segment>`）を含める。

## 関連ADR

```yaml
related_adrs: []
```

本 Issue の判断は、同じ設計セグメントで `status: proposed` として作成する `ADR-0015` に記録する。未承認 ADR は構造化参照へ入れず、設計に必要な判断——ティア抽象化の採用、具体的なモデル文字列の保持場所の確定、コア独立レビュー機構と結合させないこと、スキーマ版数の据え置き、恒久設定を更新する操作と実行主体、伝達経路と優先順位——はすべて本文へ自己完結して記載する。

## 障害・ロールバック考慮

想定される失敗モード:

- 設定に codex を指定した環境で codex CLI が不在・未認証: 既存のフェイルセーフにより writer lease 取得後に blocked 報告・lease 解放・0 でも 3 でもない終了コードとなり、人間確認へ倒れる。誤った成果物が作られることはない。
- ティア対応表に該当エントリが無い: `worker context` が日本語の理由付きで失敗し、起動ラッパーは lease 取得前の error として返す。誤ったモデルで起動しない。
- ティアを指定したのに解決済みモデルがアダプタへ届かない: アダプタ側の防御的検査により、従来フォールバックへ黙って落ちず blocked へ倒れる。
- スキーマ拡張の誤りで既存設定が不正判定される: 設定読み込み時点で日本語の理由付きで失敗するため、ワーカー起動より前に検出される。単体テストで旧形式の受理を明示的に固定する。
- 起動ラッパーが解決結果を取り違える: 未解決の値を export しない設計と上記の防御的検査により、症状は「起動しない」であって「別のモデルで走る」ではない。結合テストで環境変数の値と起動コマンドを直接確認する。

### 配布既定値への波及と、オプトアウト手段が存在しないこと

設定ファイルはパッケージへ同梱され、導入時にそのまま複製される。さらに更新コマンドは、`.agent-skill-chain/` 配下の正本アセット（設定を含む）を衝突検知なしの無条件上書きで同期する（不可侵はプロジェクトポリシーと展開済みの GitHub 資産のみ）。したがって次が成り立つ。

- 本リポジトリの実装セグメント上書きとティア対応表は、新規導入プロジェクトの初期値になる。
- 既存導入プロジェクトが当該設定をローカルで削除・変更しても、次回の更新コマンド実行で必ず配布時の内容へ戻る。設定ファイルに対する恒久的なオプトアウト手段は現時点で存在しない。
- codex CLI を導入していない導入先では、更新のたびに実装セグメントが blocked（人間確認が必要な停止）へ倒れ続ける。

これは意図された挙動である。実装セグメントを最高能力の実行系で走らせることを全体既定として強制する、という所有者の明示的判断に基づく。AGENTS.md が掲げる「既定は常に安全側」という原則との緊張関係は認識している——導入先の環境で成立しない既定値を配ることは、導入先にとって安全な既定とは言えない。それでもこの判断を採る根拠は、(a) 失敗の現れ方が誤った成果物ではなく停止であり、危険側へ黙って倒れないこと、(b) 更新コマンドは人間が明示的に実行する操作であり、実行により正本アセットが同期されることは既に規範文書が述べていること、の2点である。導入先が恒久的にオプトアウトする手段は無い。

配布既定値と本リポジトリ自身の運用設定を分離すること、および更新コマンドが設定ファイルのローカル変更を保護する方式（マージ・不可侵化・オプトアウト表現の導入）は、いずれも本 Issue の範囲を超える構造上の論点であり、別 Issue として扱う（起票の判断は進行役に委ねる）。

### ロールバック手順

- 本リポジトリの設定だけを戻す場合: `worker.segment_overrides` の当該セグメントの上書きと、不要になったティア対応表のエントリを削除する。他の変更（スキーマ・CLI・アダプタ）は任意項目の追加と優先順位の明示化のみであるため、設定を戻せば挙動は本 Issue 適用前と一致する。導入先においては、この削除は次回の更新コマンドまでしか持続しない（前節）。
- 全体を戻す場合: 本 Issue の commit を revert する。永続状態・migration を持たないため、データ移行や後始末は不要である。成果物 digest の変化により下流ゲートは自動的に無効化され、再通過が要求される。

影響を受ける既存機能: ワーカー起動経路（`worker context`・起動ラッパー・codex アダプタ）。ゲートレビュア起動、コア独立レビューの契約・attestation 検証、writer lease、Check Run、4セグメント構成には変更を加えない。プロジェクトポリシーは読み書きしない。human アダプタの `worker context` 呼び出しは segment を渡さない従来形のまま維持する。

## 検証方法

- 単体: 選択解決（セグメント別解決・フォールバック・スカラーのみ・未設定）、ティア解決（対応表から解決・エントリ不在で解決失敗・アダプタ用モデル不在で解決失敗）、スキーマ（新形式受理・旧形式受理・モデル値変更の受理・未知アダプタ／未知ティア／未知セグメント／未知キーの拒否）。
- 結合: 起動ラッパーからアダプタまでの伝達（環境変数の値と codex 起動コマンドのモデル・reasoning effort 指定）、完全上書き環境変数の優先、ティア未指定時の従来値、ティア解決失敗時に起動しないこと、アダプタ側防御的検査の blocked 経路、本リポジトリ設定での実装セグメント解決結果、アダプタのソースに具体的なモデル文字列が新たに追加されていないこと。
- 常時必須: lint / format、型検査、単体テスト、変更範囲の結合テスト、静的アプリケーションセキュリティテスト、依存関係・secret スキャン。

## 完了条件

SPEC.md の AC-1〜AC-9 に対応する設計要素が本文書に定義され、実装単位と順序が `PLAN.md` に定義され、判断の根拠が ADR-0015 に記録されていること。

## 未決事項

なし。SPEC.md が設計セグメントへ委ねた4点（更新操作の形、スキーマ版数、伝達経路の具体形、ティア解決失敗時に倒れる先）は本文書で確定した。

## 対象外

ゲートレビュア側のセグメント別選択、コア独立レビューの契約・スキーマ固定値・attestation 検証の変更、codex 以外の新規アダプタ、claude / human アダプタのモデル選択方式の変更、ティア対応表への claude 用・human 用モデルの追加、`highest_capability` 以外のティア定義、ティア語彙の統合、4セグメント構成の変更、起動の都度切り替える実行時オプション、配布既定値と本リポジトリ運用設定の分離、更新コマンドが設定ファイルのローカル変更を保護する方式の導入。
