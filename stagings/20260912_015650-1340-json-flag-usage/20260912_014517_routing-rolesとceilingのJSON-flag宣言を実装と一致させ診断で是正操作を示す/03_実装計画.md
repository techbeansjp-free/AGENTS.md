# 03 実装計画

> 設計を、riskに比例したEvidenceで完了を判定できる小さな実装単位へ分解する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とする。実装中の発見は前工程を自動再起動せず、事実と対処を本書へ追記する。目的・scope・受け入れ条件・security境界・不可逆操作の契約が変わる場合だけ影響する上流成果物を再確定する。

## 0. 管理情報と実装条件

| 項目 | 内容 |
|---|---|
| 件名・正本 | routing rolesとceilingのJSON flag宣言を実装と一致させ診断で是正操作を示す、https://github.com/techbeansjp-free/AGENTS.md/issues/1340 |
| 設計の版 | 02_設計.md（Step 5記録） |
| 基点 | origin、main、`88ec4cd704464357549083944b569c48c2f35c99`（#1349のmerge後に追随する） |
| 専用ブランチ・worktree | `bugfix/1340-json-flag-usage`、`.worktrees/<timestamp>-1340-json-flag-usage`（Step 9開始時に確定） |
| 作成・更新日 | 2026-09-12T02:05:00+09:00 |
| 停止点 | PR作成後 |

### 0.1 routing入力契約

| task | role欄（担当role） | 許可path・操作 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄 |
|---|---|---|---|---|---|---|---|---|
| T01〜T03 | implementer | `src/`、`test/`、`docs/specs/`、`.agent-skill-chain/`、README生成区画。implement_product、run_test | failing_test、test_result | standard | Claude Opus以下 / Codex high以下 | trusted tierMapping | 解決不能なら開始せず保持 | reviewerと別session/context |
| T04（review） | reviewer | `.agent-skill-chain/role-log/`、`.agent-skill-chain/tmp/` | affirmative_review、adversarial_review、finding_classification、independence | standard | 同上 | 同上 | Critical/High未解決なら停止 | implementerと別context（artifact §9） |

## 1. 実装方針と順序

### 1.0 開発考慮事項の適用判定（必須）

開発考慮事項の適用判定は00_要求定義.md §6.1と同じ

- 最小実装の方針: usage正本2項の型・説明・実行例、入力helper2関数の先頭文字判定、生成区画の再生成、unit feature 1 file、仕様4 file。報告の対応策2（宣言を実装に合わせる）と3（診断の是正操作）に限る
- 既存変更を保持する方法: 専用worktree
- 旧資産を復元・実行しない範囲: `.workflow`、`memo/`、アーカイブ済み`.agents`
- 実装順の根拠: 再現test → usage正本 → helper診断 → 生成区画 → 仕様。生成区画は正本の後でしか作れない
- dependency graphのtopological order: USAGE-SOURCE(1) → INPUT-HELPER / GENERATED-GUIDE(2)（02 §2.3）
- cycle/self-loop/unknown nodeを拒否するSCNと観測結果: SCN-UNIT-JSONFLAG-001（生成区画が正本と一致。Step 9で§10へ記録）
- head変更で再生成するtest/CI/review/PR証拠: `npm test`、`cli:check`、review round、PR evidence

## 2. 受け入れ条件・シナリオ・実装の追跡

| AC ID | SCN ID | テスト層 | 必要Evidence | 実装箇所 | 最終確認 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-JSONFLAG-001 | unit | 実行可能 | `src/cli-usage.ts` | `npm run test:unit`、`cli:check` |
| AC-02 | SCN-UNIT-JSONFLAG-002 | unit | 実行可能 | `src/cli.ts` | `npm run test:unit` |
| AC-03 | SCN-UNIT-JSONFLAG-003 | unit | 実行可能 | `src/cli.ts` | 同上 |
| AC-04 | SCN-UNIT-JSONFLAG-004 | unit | 実行可能 | `src/cli.ts` | 同上 |

## 3. 検証戦略

| Requirement ID | Acceptance Criteria ID | 変更種別 | risk | 影響境界 | Impact Analysis | 選択する検証 | 選択理由 | 証拠保存先 |
|---|---|---|---|---|---|---|---|---|
| FR-01〜03 | AC-01〜03 | bug-fix | low | CLI adapter | security無、data loss無、不可逆無、外部契約変更あり（`--help`表示と診断文言）、並行無 | bug-reproduction、regression-test、integration-test、contract-test、mutation-test | `workflow verification-set`の出力 | staging §10、review artifact §7 |

| 選ばれた検証 | 充足手段 | 実測結果 | 充足しない場合の理由 |
|---|---|---|---|
| bug-reproduction | SCN-UNIT-JSONFLAG-001・002を修正前に実行して失敗を確認 | 修正前: 001（型が`path`）と002（`offset 0`診断）が失敗、003は合格。修正後: 3件合格 | - |
| regression-test | SCN-UNIT-JSONFLAG-003、既存role-tier scenario、`npm test` | `npm test`: 1855 scenarios（1839 passed、16 skipped、0 failed） | - |
| integration-test | 既存integration scenario（CLI経路）の回帰実行。新規integrationは追加しない | `npm test`に含まれるintegration層全件合格。新規scenarioは別processで`dist/bin`を起動する実CLI経路 | 変更はusage表示と診断文言であり、CLI経路の入出力契約はunitで`main`を直接呼んで担保する |
| contract-test | `cli:check`（usage正本と生成区画・実装flagの一致）、`docs:format` | cli:check合格（生成区画は型情報を含まないため再生成差分なし）、docs:format・trace:check・source:check・conformance:check・package:check合格 | - |
| mutation-test | 変異A: usage型を`path`へ戻す、B: 先頭文字判定を消す（診断が既存のoffset 0へ戻る）、C: 判定を`[`だけにする（objectのoverrideを誤拒否）、D: 診断文から「path」の語を落とす。各変異で1件以上落ちることを確認し、複写で戻す | 初版（先頭文字gate）: A〜E各1 failed。REV-02/03の是正後: A（型をpathへ）1 failed、B（是正操作の併記除去）2 failed、C（overrideを併記経路へ通さない）1 failed、D（pathの語除去）1 failed、E（ceiling実行例からoverride除去）1 failed。復旧後4件合格 | - |

Bug Fixのため`再現 → failing regression test → 修正 → regression PASS`を使う。

## 4. 実装タスク

### T01 再現testとusage正本

- 目的: FR-01、AC-01
- 変更対象: `test/features/unit/routing-json-flag.feature`、`test/steps/routing-json-flag.steps.ts`（新規）、`src/cli-usage.ts`
- 入力・出力: `--help` → usage JSON
- 依存タスク: なし
- 対応要件・シナリオ: SCN-UNIT-JSONFLAG-001
- 選択するVerification Setと根拠: bug-reproduction、contract-test
- 最小実装: 2項の型を`JSON`、説明を「inline JSON（pathではない）」、実行例をinline JSONへ
- 整理可能な範囲: なし
- 仕様更新: T03
- 完了条件: 001が修正前に失敗し修正後に合格、`cli:check`合格（生成区画再生成後）
- ロールバック・再開地点: commit revert

### T02 入力helperの診断

- 目的: FR-02、FR-03、AC-02、AC-03
- 変更対象: `src/cli.ts`の`assignmentInput`・`humanOverrideInput`
- 入力・出力: flag値 → 構造 / Error
- 依存タスク: T01
- 対応要件・シナリオ: SCN-UNIT-JSONFLAG-002、003
- 選択するVerification Setと根拠: bug-reproduction、regression-test、mutation-test
- 最小実装: `value.trimStart()`の先頭が`[`（assignments）/`{`（override）でなければ`--<flag>にはinline JSONを渡します（pathではありません）`のErrorを投げる
- 整理可能な範囲: なし
- 仕様更新: T03
- 完了条件: 002・003合格、既存role-tier・workflow-step-enforcement scenario合格
- ロールバック・再開地点: commit revert

### T03 生成区画と仕様

- 目的: 仕様整合性
- 変更対象: `.agent-skill-chain/00_利用案内.md`・`README.md`の生成区画（`generate_cli_usage_guide`）、`docs/specs/02_要件/01_ワークフロー要件.md`（REQ-WF-009）、`02_要件/00_要件一覧.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md`
- 入力・出力: Markdown
- 依存タスク: T01、T02
- 対応要件・シナリオ: FR-01〜03
- 選択するVerification Setと根拠: contract-test
- 最小実装: 生成と各1〜2行の追記
- 整理可能な範囲: なし
- 仕様更新: 本task
- 完了条件: `cli:check`、`trace:check`、`docs:format`合格
- ロールバック・再開地点: commit revert

### T04 独立reviewとPR

- 目的: Step 10〜11
- 変更対象: `docs/reviews/<連番>_課題1340JSON-flag宣言一致レビュー.md`
- 入力・出力: H_impl → review artifact
- 依存タスク: T01〜T03
- 対応要件・シナリオ: 全AC
- 選択するVerification Setと根拠: 独立review（context-isolated）
- 最小実装: review round、artifact commit、Step 10記録、`pr create`
- 整理可能な範囲: なし
- 仕様更新: なし
- 完了条件: `outcome=pull-request`
- ロールバック・再開地点: staging journalとreview session

## 4.1 実装中発見の前向き記録

| 発見ID | workflow mode | 目的変更 | scope変更 | AC変更 | security境界拡大 | 不可逆操作 | モード失格条件 `{ id, evidence }[]` | CLI disposition | 影響成果物 |
|---|---|---|---|---|---|---|---|---|---|
| DISC-1340-01 | full | false | false | true | false | false | [] | rebaseline-affected-contracts | 01_要件定義.md、02_設計.md、03_実装計画.md |

- discoveryId: DISC-1340-01
- 事実: round 1の独立reviewで、先頭文字gateがscalarの正当なJSON（`null`）をpath扱いにし（REV-02）、`[a].json`のようなpathは是正操作を示さない（REV-03）ことが判明。ceilingの実行例にoverrideが無くAC-01を満たさない（REV-01）
- 影響: FR-02の観測方法とAC-04の追加（受け入れ条件の追加）。目的・scope・security境界は不変
- 判断: gateを捨て、解析errorへ是正操作を併記する方式へ変更。AC-04とSCN-UNIT-JSONFLAG-004を追加
- 対処: `inlineJsonInput`、ceiling実行例、feature・steps、仕様3 file、01〜03の影響部分を再確定
- 検証: SCN-UNIT-JSONFLAG-001〜004合格、変異A〜E全kill
- 仕様更新: `02_要件/01_ワークフロー要件.md`、`15_要件追跡/`

## 5. 実行可能な受け入れ例とtest計画

### 5.1 project選択test layer

| SCN ID | project選択test layer | 対象責務 | 正常・異常・境界・悪用・回帰 | 模擬境界 |
|---|---|---|---|---|
| SCN-UNIT-JSONFLAG-001 | unit | usage正本の型・実行例 | 正常 | なし |
| SCN-UNIT-JSONFLAG-002 | unit | path風の値の診断 | 異常・悪用（値本文非転記） | なし |
| SCN-UNIT-JSONFLAG-003 | unit | inline受理の回帰 | 境界（先頭空白）・回帰 | なし |
| SCN-UNIT-JSONFLAG-004 | unit | scalar JSONと`[`始まりpathの診断 | 境界・異常 | なし |

Gherkinの受け入れ例は01 §9に記載した。

### 5.2 安全性の必須観点

- [ ] パストラバーサルとシンボリックリンク脱出: 対象外（fileを読まない）
- [ ] Unicode制御文字と正規化: 対象外（先頭1文字のASCII判定のみ）
- [ ] 同時作成と原子性: 対象外
- [ ] リポジトリ・リモート誤認: 対象外
- [x] 秘密情報の混入と伏字化: 値本文を診断へ転記しない（SCN-UNIT-JSONFLAG-002）
- [ ] TOCTOU: 対象外
- [x] 実ワークスペース、実リモート、他worktreeを変更しない: file I/Oなし

## 6. 検証コマンド

| 検証 | コマンド | 合格条件 |
|---|---|---|
| project選択の文書・受け入れ形式 | `npm run docs:format`、`npm run test:format` | 違反0件 |
| project選択の静的検査 | `npm run lint`、`npm run cli:check` | エラー0件 |
| source formatter非破壊検査 | `npm run format:check` | 差分0件 |
| strict型検査と実行時入力検証 | `npm run typecheck` | 型error0件 |
| project選択の全test layer | `npm test` | 失敗0件 |
| 既存テスト一式 | `npm run conformance:check`、`npm run trace:check`、`npm run audit:check` | 失敗0件 |
| 配布物 | `npm run package:check` | 開発専用資産0件 |

## 7. 仕様更新

| 変更 | `docs/specs/`更新先 | 要件・SCN・テストの追跡 |
|---|---|---|
| usage型宣言と診断の是正操作 | `02_要件/01_ワークフロー要件.md`、`02_要件/00_要件一覧.md` | REQ-WF-009、AC-WF-009、SCN-UNIT-JSONFLAG-001〜003 |
| 追跡と履歴 | `15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | 同上 |

- `no-spec-impact`の場合の限定的根拠: 該当しない
- ドメイン用語台帳の追加・変更・廃止taskと仕様変更履歴: 用語変更なし。変更履歴1行
- 用語IDとAPI・CLI、データ、UI、ログ・診断、test表現の整合確認: 「inline JSON」「path」をusage説明と診断で同じ語にする
- UI有無と条件付きトークンの判断: UIなし
- 新規プロジェクト / 既存導入の判断: 既存導入

## 8. レビュー計画

| ラウンド | 対象 | 肯定観点 | 敵対観点 | 完了条件 |
|---:|---|---|---|---|
| 1 | 全差分・全評価基準 | 正しさ、価値、実現可能性、整合性、保守性 | 反例、失敗、境界、悪用、安全性、損失、復旧、範囲 | 指摘確定 |
| 2 | 未解決Critical/High、修正差分、隣接範囲 | 修正の有効性 | 回避・回帰の有無 | 停止指摘解消 |
| 3 | 収束状態 | 最終整合性 | 残存リスク | 全指摘分類とAI裁定 |

## 9. 提出、ロールバック、再開

- コミット単位: 実装commit（T01〜T03）→ review artifact commit
- push先: origin `bugfix/1340-json-flag-usage`
- PR本文のIssue参照: `Closes #1340`を1回
- マージ、Issue終了、ブランチ削除、release、公開、cleanupの権限分離: PR作成で停止。mergeは進行役がowner許可のもと別操作で行う
- ロールバック手順: PRのrevert
- 失敗時に保持するworktree・ブランチ・証拠: worktree、branch、staging、review session
- 次回の再開地点: staging journalの最終Step

## 10. 進捗

| タスク | 状態 | コミット | テスト結果 | 仕様 | 備考 |
|---|---|---|---|---|---|
| T01 | 完了 | 実装commit（H_impl） | SCN-UNIT-JSONFLAG-001合格、cli:check合格 | - | usage型・説明・実行例 |
| T02 | 完了 | 実装commit（H_impl） | SCN-UNIT-JSONFLAG-002・003合格 | - | `requireInlineJson` |
| T03 | 完了 | 実装commit（H_impl） | trace:check・docs:format合格 | 4 file | 生成区画に差分なし |
| T04 | 進行中 | - | - | - | Step 10へ |

## 11. 実装中の発見と対処

発見記録は4.1節を正本とする。現時点で発見なし。

## 12. risk比例検証hook

差分・risk・成功証拠でtargeted検証を選び、最終full gateと独立reviewを維持する。budgetは`.agent-skill-chain/project-policy.json`の`budgets`を参照する。
