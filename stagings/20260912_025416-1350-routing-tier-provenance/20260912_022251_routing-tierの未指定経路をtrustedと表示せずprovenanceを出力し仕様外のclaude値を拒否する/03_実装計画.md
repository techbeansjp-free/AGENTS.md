# 03 実装計画

> 設計を、riskに比例したEvidenceで完了を判定できる小さな実装単位へ分解する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とする。実装中の発見は前工程を自動再起動せず、事実と対処を本書へ追記する。目的・scope・受け入れ条件・security境界・不可逆操作の契約が変わる場合だけ影響する上流成果物を再確定する。

## 0. 管理情報と実装条件

| 項目 | 内容 |
|---|---|
| 件名・正本 | routing tierの未指定経路をtrustedと表示せずprovenanceを出力し仕様外のclaude値を拒否する、https://github.com/techbeansjp-free/AGENTS.md/issues/1350 |
| 設計の版 | 02_設計.md（Step 5記録） |
| 基点 | origin、main、`ec7a98274b19bcebb1120fa4305eb3cc4514b785` |
| 専用ブランチ・worktree | `bugfix/1350-routing-tier-provenance`、`.worktrees/<timestamp>-1350-routing-tier-provenance`（Step 9開始時に確定） |
| 作成・更新日 | 2026-09-12T04:00:00+09:00 |
| 停止点 | PR作成後 |

### 0.1 routing入力契約

| task | role欄（担当role） | 許可path・操作 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄 |
|---|---|---|---|---|---|---|---|---|
| T01〜T03 | implementer | `src/`、`test/`、`docs/specs/`、`.agent-skill-chain/`。implement_product、run_test | failing_test、test_result | advanced | Claude Opus以下 / Codex high以下 | trusted tierMapping | 解決不能なら開始せず保持 | reviewerと別session/context |
| T04（review） | reviewer | `.agent-skill-chain/role-log/`、`.agent-skill-chain/tmp/` | affirmative_review、adversarial_review、finding_classification、independence | advanced | 同上 | 同上 | Critical/High未解決なら停止 | implementerと別context（artifact §9） |

## 1. 実装方針と順序

### 1.0 開発考慮事項の適用判定（必須）

開発考慮事項の適用判定は00_要求定義.md §6.1と同じ

- 最小実装の方針: `cli.ts`の`routing tier` handler（受理値検証、出力への`provenance`・`usage`追加、診断文言）、`cli-usage.ts`の1項、unit feature 1 file、仕様6 file。判定関数・loaderは変えない
- 既存変更を保持する方法: 専用worktree
- 旧資産を復元・実行しない範囲: `.workflow`、`memo/`、アーカイブ済み`.agents`
- 実装順の根拠: 再現test → 受理値検証 → 未指定経路の出力・診断 → codex経路の出力 → usage → 仕様
- dependency graphのtopological order: POLICY-LOADER(1) → TIER-HANDLER(2)（02 §2.3）
- cycle/self-loop/unknown nodeを拒否するSCNと観測結果: SCN-UNIT-TIERPROV-002（loaderの観測値が出力に写る。Step 9で§10へ記録）
- head変更で再生成するtest/CI/review/PR証拠: `npm test`、`cli:check`、review round、PR evidence

## 2. 受け入れ条件・シナリオ・実装の追跡

| AC ID | SCN ID | テスト層 | 必要Evidence | 実装箇所 | 最終確認 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-TIERPROV-001 | unit | 実行可能 | `src/cli.ts` | `npm run test:unit` |
| AC-02 | SCN-UNIT-TIERPROV-002 | unit | 実行可能 | `src/cli.ts` | 同上 |
| AC-03 | SCN-UNIT-TIERPROV-003 | unit | 実行可能 | `src/cli.ts` | 同上 |
| AC-04 | SCN-E2E-AM-001（既存） | unit | 実行可能 | `src/cli.ts` | 同上、既存SCN-E2E-AM-001 |

## 3. 検証戦略

| Requirement ID | Acceptance Criteria ID | 変更種別 | risk | 影響境界 | Impact Analysis | 選択する検証 | 選択理由 | 証拠保存先 |
|---|---|---|---|---|---|---|---|---|
| FR-01〜03 | AC-01〜04 | bug-fix | medium | Agent Routing | security関連（信頼境界の表示）、data loss無、不可逆無、外部契約変更あり、並行無 | bug-reproduction、regression-test、integration-test、negative-test、security-analysis、contract-test、mutation-test | `workflow verification-set`の出力 | staging §10、review artifact §7 |

| 選ばれた検証 | 充足手段 | 実測結果 | 充足しない場合の理由 |
|---|---|---|---|
| bug-reproduction | SCN-UNIT-TIERPROV-001〜003を修正前に実行して失敗を確認 | 修正前は3件とも失敗（claude受理、provenance/usage欠落、trustedの診断）。修正後3件合格 | - |
| regression-test | 既存SCN-E2E-AM-001・SCN-UNIT-TIER・SCN-UNIT-ROLE、`npm test` | 12 scenario合格。`npm test`はStep 9で記録 | - |
| integration-test | 既存integration/e2e（SCN-E2E-AM-001等）の回帰実行。新規integrationは追加しない | Step 9で記録 | 新規scenarioはCLI `main`を隔離repositoryで直接呼ぶunitで担保する |
| negative-test | `--provider=claude`、`CODEX`、空値、`codex-preview`の拒否（SCN-UNIT-TIERPROV-001） | 4値とも終了値1で案内つき拒否 | - |
| security-analysis | 諮問2件（codex・fable、2026-09-12）の結論を00 §12へ記録済み。出力の信頼源がloader観測値だけであることを変異Cで確認 | 変異C（`source`を`git`へ固定＝信頼源の捏造）で2件失敗。捏造を検出する | - |
| contract-test | `cli:check`、`docs:format`、`trace:check`、`conformance:check` | 4件合格（trace:checkは要件本文へSCN到達行を追加して合格） | - |
| mutation-test | 変異A: `claude`受理を戻す、B: 未指定経路の`usage`を落とす、C: `provenance.source`を固定文字列`"git"`にする（信頼源の捏造）、D: 失敗診断の`next`へtrustedの語を戻す、E: codex経路の`provenance`を落とす。各変異で1件以上落ちることを確認し、複写で戻す | A（claude受理を戻す）1 failed、B（usage除去）2 failed、C（source捏造）2 failed、D（nextへtrustedを戻す）1 failed、E（失敗出力のprovenance除去）1 failed、F（codex経路のprovenance除去）初回生存→既存SCN-E2E-AM-001へprovenance観測を足して1 failed。復旧後合格 | - |

Bug Fixのため`再現 → failing regression test → 修正 → regression PASS`を使う。

## 4. 実装タスク

### T01 再現testと受理値検証

- 目的: FR-01、AC-01
- 変更対象: `test/features/unit/routing-tier-provenance.feature`、`test/steps/routing-tier-provenance.steps.ts`（新規）、`src/cli.ts`
- 入力・出力: argv → 診断
- 依存タスク: なし
- 対応要件・シナリオ: SCN-UNIT-TIERPROV-001
- 選択するVerification Setと根拠: bug-reproduction、negative-test
- 最小実装: `flags.provider !== undefined && flags.provider !== "codex"`でError（文言はFR-01）
- 整理可能な範囲: なし
- 仕様更新: T03
- 完了条件: 001が修正前に失敗し修正後に合格
- ロールバック・再開地点: commit revert

### T02 出力と診断

- 目的: FR-02、FR-03、AC-02〜04
- 変更対象: `src/cli.ts`、`src/cli-usage.ts`、生成区画
- 入力・出力: loaderの`provenance` → 出力field
- 依存タスク: T01
- 対応要件・シナリオ: SCN-UNIT-TIERPROV-002・003、既存SCN-UNIT-AM-001
- 選択するVerification Setと根拠: regression-test、mutation-test、contract-test
- 最小実装: 未指定経路の`output`と`roleTierFailure`の結果へ`provenance`・`usage`を添付し、`next`・`requiredAuthority`を変更。codex経路の`print`へ`provenance`・`usage`を追加。usage説明を更新
- 整理可能な範囲: なし
- 仕様更新: T03
- 完了条件: 002・003合格、既存codex-launch scenario（SCN-UNIT-AM-001）合格、`cli:check`合格
- ロールバック・再開地点: commit revert

### T03 仕様

- 目的: 仕様整合性
- 変更対象: `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`（`routing tier`行）、`10_セキュリティ/01_信頼境界.md`、`02_要件/01_ワークフロー要件.md`（REQ-WF-007）、`02_要件/00_要件一覧.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md`
- 入力・出力: Markdown
- 依存タスク: T01、T02
- 対応要件・シナリオ: FR-01〜03
- 選択するVerification Setと根拠: contract-test
- 最小実装: 各1〜3行
- 整理可能な範囲: なし
- 仕様更新: 本task
- 完了条件: `trace:check`、`docs:format`合格
- ロールバック・再開地点: commit revert

### T04 独立reviewとPR

- 目的: Step 10〜11
- 変更対象: `docs/reviews/<連番>_課題1350routing-tier信頼源表示レビュー.md`
- 入力・出力: H_impl → review artifact
- 依存タスク: T01〜T03
- 対応要件・シナリオ: 全AC
- 選択するVerification Setと根拠: 独立review（context-isolated）
- 最小実装: review round、artifact commit、Step 10記録、`pr create`
- 整理可能な範囲: なし
- 仕様更新: なし
- 完了条件: `outcome=pull-request`
- ロールバック・再開地点: staging journalとreview session

## 4.1 実装中発見の前向き記録

| 発見ID | workflow mode | 目的変更 | scope変更 | AC変更 | security境界拡大 | 不可逆操作 | モード失格条件 `{ id, evidence }[]` | CLI disposition | 影響成果物 |
|---|---|---|---|---|---|---|---|---|---|
| DISC-1350-01 | full | false | false | true | false | false | [] | rebaseline-affected-contracts | 01_要件定義.md、02_設計.md、03_実装計画.md |

- discoveryId: DISC-1350-01
- 事実: `codex`経路の回帰は既存のSCN-UNIT-AM-001（公式観測stubつきの実CLI経路でtierを2通り実行する）が同じfixtureで担保しており、新規SCN-UNIT-TIERPROV-004は同型のfixtureを二重に持つことになる
- 影響: AC-04の例IDを既存SCNへ差し替え（ACの内容は不変）。目的・scope・security境界は不変
- 判断: 新規scenarioを足さず既存SCNを参照する。支援層を成果物へ比例させる（運用ポリシー「既存手段の縮小で目的を満たせないかを先に評価する」）
- 対処: 01〜03のAC-04行と層対応を既存SCN-E2E-AM-001へ差し替え
- 検証: SCN-E2E-AM-001合格（codex経路の終了値0/1が変わらないこと）
- 仕様更新: `15_要件追跡/`は既存SCN行に実装pathを追加する

| 発見ID | workflow mode | 目的変更 | scope変更 | AC変更 | security境界拡大 | 不可逆操作 | モード失格条件 `{ id, evidence }[]` | CLI disposition | 影響成果物 |
|---|---|---|---|---|---|---|---|---|---|
| DISC-1350-02 | full | false | false | false | false | false | [] | continue | なし |

- discoveryId: DISC-1350-02
- 事実: 変異F（`codex`経路の`provenance`・`usage`除去）が生存した。既存SCN-E2E-AM-001は`routing tier --provider=codex`の終了値だけを見ており、出力fieldを観測していなかった
- 影響: 契約は変わらない。AC-04の担保が空虚だった
- 判断: 新規scenarioを足さず、既存SCN-E2E-AM-001のstepへ`provenance.source`が`git`・`ref`が40桁SHA・`usage`が`codex-adoption`である観測を追加する（既存手段の拡張）
- 対処: `test/steps/codex-launch.steps.ts`のtier実行部へassertionを追加
- 検証: 変異Fを再適用してSCN-E2E-AM-001が失敗することを確認。復旧後合格
- 仕様更新: なし

| 発見ID | workflow mode | 目的変更 | scope変更 | AC変更 | security境界拡大 | 不可逆操作 | モード失格条件 `{ id, evidence }[]` | CLI disposition | 影響成果物 |
|---|---|---|---|---|---|---|---|---|---|
| DISC-1350-03 | full | false | false | false | false | false | [] | rebaseline-affected-contracts | 01_要件定義.md、02_設計.md |

- discoveryId: DISC-1350-03
- 事実: round 1の独立reviewでREV-01（Medium）。`--provider=claude`の拒否はpolicyを読む前にthrowするため、終了値1の出力に`provenance`・`usage`が無く、INV-01の字面（「出力は必ず含む」）への反例になる
- 影響: 不変条件の字面の是正。実装・AC・security境界は不変
- 判断: 拒否側へ信頼源を足さない。policyを読んでいない時点で信頼源を書くのは、変異Cで検出対象にした「信頼源の捏造」と同じ形になる。INV-01を「tier判定の結果を返すとき」へ限定し、入力検証の拒否は`ASC-CLI-VALIDATION-001`の構造化診断だけを返すと明記する
- 影響した契約ID: INV-01
- 対処: 01 §2.3のINV-01と02 §1.1を再確定。実装差分なし
- 検証: `routing tier --provider=claude`が`ASC-CLI-VALIDATION-001`で終了値1（SCN-UNIT-TIERPROV-001、変更なし）
- 仕様更新: なし（仕様06・10は判定の結果を返す出力を対象にしており字面の変更を要しない）

## 5. 実行可能な受け入れ例とtest計画

### 5.1 project選択test layer

| SCN ID | project選択test layer | 対象責務 | 正常・異常・境界・悪用・回帰 | 模擬境界 |
|---|---|---|---|---|
| SCN-UNIT-TIERPROV-001 | unit | provider受理値 | 異常・境界（空・大文字） | 隔離repository |
| SCN-UNIT-TIERPROV-002 | unit | 未指定経路の成功出力 | 悪用（working treeのkey追加） | 隔離repository |
| SCN-UNIT-TIERPROV-003 | unit | 未指定経路の失敗診断 | 異常 | 隔離repository |
| SCN-E2E-AM-001（既存） | unit | codex経路の出力 | 回帰 | 公式観測stub |

Gherkinの受け入れ例は01 §9に記載した。

### 5.2 安全性の必須観点

- [ ] パストラバーサルとシンボリックリンク脱出: 対象外（既存loaderの検証に従う）
- [ ] Unicode制御文字と正規化: 対象外
- [ ] 同時作成と原子性: 対象外（read-only）
- [x] リポジトリ・リモート誤認: 未指定経路がremoteを読まないことを`provenance.source`で明示（SCN-UNIT-TIERPROV-002）
- [x] 秘密情報の混入と伏字化: 出力へ書くのはpathとSHAだけ
- [ ] TOCTOU: 対象外
- [x] 実ワークスペース、実リモート、他worktreeを変更しない: 隔離repositoryのみ

## 6. 検証コマンド

| 検証 | コマンド | 合格条件 |
|---|---|---|
| project選択の文書・受け入れ形式 | `npm run docs:format`、`npm run test:format` | 違反0件 |
| project選択の静的検査 | `npm run lint`、`npm run cli:check` | エラー0件 |
| source formatter非破壊検査 | `npm run format:check` | 差分0件 |
| strict型検査と実行時入力検証 | `npm run typecheck` | 型error0件 |
| project選択の全test layer | `npm test` | 失敗0件 |
| 既存テスト一式 | `npm run conformance:check`、`npm run trace:check`、`npm run audit:check` | 失敗0件 |
| 配布物 | `npm run package:check` | 開発専用資産0件 |

## 7. 仕様更新

| 変更 | `docs/specs/`更新先 | 要件・SCN・テストの追跡 |
|---|---|---|
| 受理値・出力・診断 | `06_外部インターフェース/01_コマンド・GitHub契約.md`、`10_セキュリティ/01_信頼境界.md`、`02_要件/01_ワークフロー要件.md`、`02_要件/00_要件一覧.md` | REQ-WF-007、AC-WF-007、SCN-UNIT-TIERPROV-001〜004 |
| 追跡と履歴 | `15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | 同上 |

- `no-spec-impact`の場合の限定的根拠: 該当しない
- ドメイン用語台帳の追加・変更・廃止taskと仕様変更履歴: 用語変更なし。変更履歴1行
- 用語IDとAPI・CLI、データ、UI、ログ・診断、test表現の整合確認: `provenance.source`はloaderの語彙をそのまま使う
- UI有無と条件付きトークンの判断: UIなし
- 新規プロジェクト / 既存導入の判断: 既存導入

## 8. レビュー計画

| ラウンド | 対象 | 肯定観点 | 敵対観点 | 完了条件 |
|---:|---|---|---|---|
| 1 | 全差分・全評価基準 | 正しさ、価値、実現可能性、整合性、保守性 | 反例、失敗、境界、悪用、安全性、損失、復旧、範囲 | 指摘確定 |
| 2 | 未解決Critical/High、修正差分、隣接範囲 | 修正の有効性 | 回避・回帰の有無 | 停止指摘解消 |
| 3 | 収束状態 | 最終整合性 | 残存リスク | 全指摘分類とAI裁定 |

## 9. 提出、ロールバック、再開

- コミット単位: 実装commit（T01〜T03）→ review artifact commit
- push先: origin `bugfix/1350-routing-tier-provenance`
- PR本文のIssue参照: `Closes #1350`を1回
- マージ、Issue終了、ブランチ削除、release、公開、cleanupの権限分離: PR作成で停止。mergeは進行役がowner許可のもと別操作で行う
- ロールバック手順: PRのrevert
- 失敗時に保持するworktree・ブランチ・証拠: worktree、branch、staging、review session
- 次回の再開地点: staging journalの最終Step

## 10. 進捗

| タスク | 状態 | コミット | テスト結果 | 仕様 | 備考 |
|---|---|---|---|---|---|
| T01 | 完了 | 実装commit（H_impl） | SCN-UNIT-TIERPROV-001合格 | - | provider受理値 |
| T02 | 完了 | 実装commit（H_impl） | SCN-UNIT-TIERPROV-002・003、SCN-E2E-AM-001合格 | - | provenance・usage・診断 |
| T03 | 完了 | 実装commit（H_impl） | trace:check・docs:format合格 | 6 file | 要件本文へSCN到達行 |
| T04 | 進行中 | - | - | - | Step 10へ |

## 11. 実装中の発見と対処

発見記録は4.1節を正本とする。現時点で発見なし。

## 12. risk比例検証hook

差分・risk・成功証拠でtargeted検証を選び、最終full gateと独立reviewを維持する。budgetは`.agent-skill-chain/project-policy.json`の`budgets`を参照する。
