# サイレントモードガイド - 省トークン運用ガイド

> このファイルは、会話ログ肥大化を防ぐための**省トークン運用ガイド**です。  
> Agent の発話量を最小限に抑えながら、品質を維持するための実践的なガイドです。

---

## 概要

**設計思想は維持し、発話量だけを削減**するための運用ルールです。

- ✅ フェーズ強制、状態の外部化、ドキュメント同期は**一切変更なし**
- ✅ Agent の「やること」は**完璧なまま**
- ✅ **SILENT MODE がデフォルト**（通常運転）
- ✅ 通常モードは例外条件の時のみ（デバッグ／説明モード）

**効果**: token 使用量を **1/5〜1/10** に削減可能

**重要**: 状態は `.workflow/` に完全外部化されており、md は成果物そのもの（会話はUIにすぎない）であるため、**SILENT MODE をデフォルトにしないと設計が破綻する**。

---

## 省トークン運用の基本原則

### キーコンセプト

> **Agent の仕事量 ≠ Agent の発話量**

Agent は `.workflow/` 配下に**実体を書く**ので、会話に全文を貼る必要はありません。

### 基本ルール

1. **会話は「指令」だけ**: 「TASK-20260209-001 を実行して」のような短い指令のみ
2. **成果物はファイルへ**: Agent は変更を Git 差分で、文章を `.workflow/` や `docs/run/` に出力
3. **会話への報告は最小限**: 完了/未完了、追加ファイル一覧、必要な判断（最大3つ）のみ
4. **詳細はファイル参照**: 会話には要点だけを書き、詳細はファイルへのリンクを提供

---

## デフォルト動作モード（必須）

**重要**: エージェントは **常に SILENT MODE で起動する**。

### デフォルト動作モードの原則

- **SILENT MODE = 通常運転**（デフォルト）
- **通常モード = デバッグ／説明モード**（例外）

状態は `.workflow/` に完全外部化されており、md は成果物そのもの（会話はUIにすぎない）であるため、**SILENT MODE をデフォルトにしないと設計が破綻する**。

### 通常モードへ切り替える条件（例外）

以下の **いずれかを満たした場合のみ**、通常モード（全文・詳細説明）に切り替える：

1. **初回フェーズ（`00_要求定義`）の新規作成時**（全文ドラフトが必要）
2. **ユーザーが明示的に要求した場合**：
   - 「全文を見せて」
   - 「詳細を説明して」
   - 「理由を教えて」
3. **致命的なエラーや設計不整合の説明が必要な場合**：
   - エラーや不整合が発生した場合（原因説明が必要）
   - 設計判断が複数あり、比較説明が必要な場合
4. **レビューで比較・判断理由の説明が必須な場合**：
   - 致命的な不整合・エラーが発生した場合
   - 複数の設計選択肢があり、判断理由が必要な場合

👉 **それ以外は一切しゃべらない**（SILENT MODE を維持）

### SILENT MODE の出力フォーマット

**重要**: SILENT MODE 時は、出力の先頭に `🧠 Mode: SILENT MODE` を**必ず**付与する。

```
🧠 Mode: SILENT MODE
✅ 完了フェーズ: {フェーズ名}
📄 更新対象:
  - {ファイルパス1}
  - {ファイルパス2}
🔜 次フェーズ: {次のフェーズ名}
⚠️ 判断が必要: {なし or 最大3点}
```

**効果**: 人間側が「詳細が出ない理由」を即座に理解でき、「全文見せて」→ token 再消費を防げる

### SILENT MODE の効果

- 会話は **常に短い**（最大15行）
- Agent は **勝手に仕事を進める**（自律的連続作業）
- `.workflow/` が **唯一の真実**（状態の完全外部化）
- token は **安定して減る**（指数関数的増加を防止）
- 長期タスクでも **context が死なない**

**結果**: Agent が「優秀な無口な同僚」になる

---

## 会話が3行で済む運用テンプレート

### パターンA：連続フェーズ進行

**ユーザー**:
```
01_要件定義完了後、02_設計に進んで
```

**Agent（SILENT MODE）**:
```
🧠 Mode: SILENT MODE
✅ 完了フェーズ: 01_要件定義
📄 更新対象:
  - .workflow/20260209_111457_実装コード問題点の洗い出しと対応/01_要件定義.md
🔜 次フェーズ: 02_設計
⚠️ 判断が必要: なし
```

### パターンB：タスク実行

**ユーザー**:
```
TASK-20260209-001 を実行して
```

**Agent**:
```
✅ タスク完了: TASK-20260209-001
📄 変更ファイル:
  - frontend/src/components/react/ui/Button/Button.tsx
  - frontend/src/components/react/ui/Button/Button.test.tsx
⚠️ 判断が必要: なし
```

### パターンC：レビュー

**ユーザー**:
```
レビューして
```

**Agent**:
```
✅ レビュー完了
📊 指摘件数: 致命的0 / 重要2 / 軽微5
⚠️ 致命的な指摘: なし
📄 詳細: .workflow/{issue}/04_review.md
```

---

## 省トークンAgent初期プロンプト（Cursor / Claude Code用）

### Cursor用

```
あなたは省トークンAgentです。以下のルールを厳守してください：

1. デフォルト動作モード: 常に SILENT MODE で起動する
2. 会話出力は最大15行まで
3. 詳細は必ずリポジトリ内のファイルに書く（.workflow/、docs/run/、memo/）
4. 途中経過は報告しない（完了時のみ報告）
5. 思考過程・調査ログは会話に書かない

詳細は 実行ルール.md の「デフォルト動作モード」セクションを参照してください。
```

### Claude Code用

```
あなたは省トークンAgentです。以下のルールを厳守してください：

1. デフォルト動作モード: 常に SILENT MODE で起動する
2. 会話出力は最大15行まで
3. 詳細は必ずリポジトリ内のファイルに書く（.workflow/、docs/run/、memo/）
4. 途中経過は報告しない（完了時のみ報告）
5. 思考過程・調査ログは会話に書かない

詳細は 実行ルール.md の「デフォルト動作モード」セクションを参照してください。
```

---

## 運用チェックリスト

### 会話開始時

- [ ] ユーザーの指示が「次へ」「続けて」などのSILENT MODE指示か確認
- [ ] 連続フェーズ進行か確認
- [ ] 詳細要求がないか確認

### 作業中

- [ ] 思考過程を会話に書いていないか確認
- [ ] 途中経過を報告していないか確認
- [ ] 詳細ログが必要なら `memo/` に書く

### 完了報告時

- [ ] 会話出力が15行以内か確認
- [ ] 報告フォーマットが正しいか確認
- [ ] 詳細はファイルに書いたか確認

---

**Q&A**: 切り替え条件・品質・初回/レビュー/エラー時の扱いは [`.agents/実行ルール.md`](./実行ルール.md) の「デフォルト動作モード」を参照。

---

## 参考資料

- [`実行ルール.md`](./実行ルール.md) - SILENT MODE の詳細ルール
- [`AGENTS.md`](../AGENTS.md) - ワークフロー全体の規約
- [`レビュールール.md`](./レビュールール.md) - レビュー時の出力ルール

---

**最終更新**: 2026 年 02 月 09 日（会話出力上限を15行に統一、SILENT MODEをデフォルト動作モードに変更、モード自動宣言追加）
