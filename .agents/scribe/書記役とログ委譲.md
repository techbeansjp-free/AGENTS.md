# 書記役とログ委譲（必須ルール）

> **AI 向け**: 実行ログの記録は**書記役（ログ記録担当）に一任**する。メインエージェントはログを書かず、**書記サブにだけログ記録タスクを委譲**する。他サブはログを書かず、結果だけメインに返す。

---

## 1. 原則（1 文）

**ログの記録は書記役を 1 人置き、その役に一任する。書くのは書記のみ。他は書記にログ項目を渡すだけ。**（P1・P2 共通。参照: 本 issue 調査・結論「3.1 書記役の一任」）

---

## 2. メインエージェントの責務

- 各サブの実行が終わったら、**ログ項目**（issue_id, agent_id, アクション種別, 対象成果物, 要約 等）をまとめる。
- **ログを書くタスクは書記サブにだけ委譲する。** メイン自身はログ用ファイルや DB に直接書かない。
- 委譲時は「この内容で 1 件の実行ログを記録して」と明示し、書記の説明（description）に合う形でタスクを渡す。

---

## 3. 他サブ（実装・レビュー・探索等）の責務

- ログ用のファイルや DB に**直接書かない**。実行結果・要約だけをメインに返す。
- メインが書記にログを渡す前提なので、サブは「自分が何をしたか」を返す内容に含めればよい。

---

## 4. 書記サブの責務

- メインから渡された**ログ項目**を受け取り、決まったフォーマットで記録する。1 件分のスキーマは [CONTRACT.md](./CONTRACT.md) に固定する。
- **P1（ファイル）**: `.workflow/{issue}/logs/` に 1 実行 1 ファイル（例: `YYYYMMDD_HHMMSS_agent名_アクション.md`）。frontmatter に issue_id を含める。
- **P2（SQLite）**: 指定された SQLite に 1 件 INSERT。ログ記録はキュー＋単一書者で直列化する設計とする。スキーマ・必須項目は [ワークフローログ_SQLiteスキーマ](../ledger/ワークフローログ_SQLiteスキーマ.md) を参照。
- 書記サブは「ログ記録専用」であること。コードや一般ドキュメントの編集は行わない。

---

## 5. テンプレート・配置

- 書記用サブエージェントの定義例は次のテンプレートを参照する。
  - Claude Code: [.workflow/templates/agents/scribe_claude.md](../../.workflow/templates/agents/scribe_claude.md)
  - Cursor: [.workflow/templates/agents/scribe_cursor.md](../../.workflow/templates/agents/scribe_cursor.md)
- プロジェクトで使う場合は、**プロジェクトルート直下**の `.claude/agents/` または `.cursor/agents/` に配置する（プロジェクト内完結。リポジトリ外へコピーしない）。必要に応じてパス・フォーマットを調整する。

---

## 6. 参照

- **AGENTS-spec 本流**: メインの入口・委譲ルールは [boot/CORE.md](../boot/CORE.md) と [boot/LOAD_POLICY.md](../boot/LOAD_POLICY.md)。ログ保存先・スキーマは [ワークフローログ_SQLiteスキーマ](../ledger/ワークフローログ_SQLiteスキーマ.md) と [ledger/README.md](../ledger/README.md)。
- ログの保存先・形式・P1/P2: 本 issue の「決めなければいけないこと_調査検討と結論」セクション 1〜3。
- 実現可能性（Claude / Cursor）: 本 issue の「サブエージェント仕様実現可能性調査_Claude_Cursor」。
