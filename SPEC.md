# SPEC: ゲート差し戻しのラウンド予算と非追記型の是正方針

- Issue: `ISSUE-786`
- 作成者: `run-489d29fa`
- 対象ブランチ: `process/786-gate-round-budget-remediation-policy`

## 目的・背景

停止条件のないゲート差し戻しと、finding を追記で塞ぐ是正による成果物の膨張を止め、Issue が品質を維持したまま有限の判断で次のセグメントへ進めるようにする。配布先では、進行役がラウンド上限と上限到達時の分類手続きを毎回考案せずに適用でき、セグメント作業ワーカーが矛盾の攻撃面を増やさない是正を選べなければならない。

由来となる全量集計では、21 Issue・188ラウンドのうち spec は平均4.3ラウンド、design は3.4ラウンド、implementation は4.1ラウンドであり、同一 finding が6ラウンド連続で再提出された。15ラウンドの design-gate では SPEC が151行から493行へ膨張した。一方、矛盾の発生源を上流で最小改訂した是正は1ラウンドで収束した。これらは要求の由来であり、本仕様の意味は外部資料へ委譲しない。

## 用語・前提

- ラウンド: 同一 Issue・同一ゲートに対するレビュー反復。初回を `round 0` とする。
- 最終ラウンド: 既定ラウンド予算で通常の差し戻しを終えるラウンド。既定値は `round 4` であり、round 0からround 4までの最大5回を許容する。
- evidence 原文: レビュアが finding の根拠として提出した文字列。severity の変更時も要約・整形・置換しない。
- 上流最小改訂: finding の原因を含む最も上流の成果物について、目的達成に不要な要求または矛盾だけを書き換えるか削除する変更。
- 本仕様は GitHub モードとローカルモード、Standard・Light・Strict のいずれでも、ゲートレビューを実施する場合の配布契約を対象とする。明示設定が既定値を上書きする場合、進行役には解決済みの最終ラウンド値が届かなければならない。

## 要求 → 要件 → 受入条件

### 要求

1. `ROUND_BUDGET_DISTRIBUTION_MISSING`: 各ゲートの既定ラウンド上限と上限到達時の扱いを配布先の進行役へ届け、事後裁量による無限差し戻しをなくす。
2. `REMEDIATION_APPEND_LOOP`: finding の是正を追記に依存させず、既存記述・既存実装の書換えまたは削除と、必要な場合の上流最小改訂をセグメント作業ワーカーの既定行動にする。
3. 検査・レビュア・ゲートを減らさず、warning へ分類した finding の追跡可能性と evidence を失わない。

### 要件

#### R1: 進行役へ届くラウンド予算

- 配布される進行役の契約は、次の既定最終ラウンドをゲートごとに明示する。

| ゲート | 既定の最終ラウンド |
|---|---:|
| spec-gate | round 4 |
| design-gate | round 4 |
| implementation-gate | round 4 |
| validation-gate | round 4 |

- 進行役は最終ラウンドを開始させる差し戻しの時点までに、「次回が最終ラウンドであること」「最終ラウンド後に blocking を限定する4類型」「その他を warning として follow-up Issue へ移すこと」を調整状態へ記録する。最終結果を見てから打ち切りを決めてはならない。
- 最終ラウンド後も blocking とする finding は、次の限定列挙のいずれかに該当するものだけとする。
  1. 以前のラウンドで提示された blocking が未是正である。
  2. Issue の明記された目的達成を直接妨げる。
  3. テスト失敗、ビルド失敗、または既存挙動の回帰を生じさせる。
  4. データ喪失またはセキュリティ低下を生じさせる。
- データ喪失またはセキュリティ低下は、ラウンド、risk、review profile にかかわらず blocking のままとする。最終ラウンド前の既存の blocking 判定は変更しない。
- 4類型のいずれにも該当しない finding は、検査結果から削除せず warning に分類する。進行役は元の severity、降格理由、4類型に該当しない根拠、evidence 原文、follow-up Issue 番号を同じ追跡記録に残し、follow-up Issue の永続化を確認してからゲート後の進行を確定する。
- ラウンド値を取得できない場合や follow-up Issue を永続化できない場合は、上限到達を推測して承認せず、人間判断を要求する。finding を便宜的に blocking へ戻すことで記録失敗を隠してはならない。

#### R2: セグメント作業ワーカーへ届く是正方針

- 配布される全セグメント作業ワーカーの契約は、blocking finding への既定の是正を、当該の既存記述または既存実装の書換え・削除とする。finding を閉じることだけを目的に、新しい条項、例外、分岐、フラグを追加してはならない。
- ワーカーは是正前に、不要な要求または挙動を減らして矛盾の発生源を除けるかを評価する。原因が上流成果物にある場合は、下流成果物を膨らませず上流最小改訂を選び、必要な再ゲートを受ける。
- Issue の明記された目的に本来必要な追加まで禁止するものではない。ただし追加を finding の局所的な穴埋めとして用いてはならず、書換え・削除・上流最小改訂では目的を達成できない理由が検証可能な作業報告に残る場合に限る。

#### R3: 品質と安全側ラチェットの維持

- ラウンド予算は severity の分類と進行判断だけを制御する。ゲート、conformance または falsification の観点、検査項目、review profile が要求するレビュア数を省略または削減しない。
- `risk != normal` または `autonomy == full` による Strict 固定を維持し、warning 降格を autonomy の昇格として扱わない。
- quick の成果物免除範囲、反証ルーブリック自体の停止条件、一般的な成果物分量上限、一般的なテスト適用性は変更しない。

### 入力・出力

- 入力: Issue の目的・risk・autonomy、対象ゲート、現在ラウンド、過去 finding、当該ラウンドの全レビュー結果、明示されたラウンド設定。
- 出力: 進行役には解決済みラウンド予算と分類・事前宣言・follow-up 手続き、セグメント作業ワーカーには非追記型の是正方針が、それぞれ配布される役割契約として届く。
- 完了状態: 4類型の blocking が残れば差し戻しまたは人間判断となる。それ以外は原文 evidence と follow-up Issue の追跡を維持した warning として、ゲート後の進行を妨げない。

### 受入条件（Acceptance Criteria）

#### AC-1: 各ゲートの既定最終ラウンドが進行役へ届く

- Given: ラウンド上限を明示上書きしていない配布先で進行役の契約を生成する
- When: 4種類のゲートについて契約内容を確認する
- Then: spec・design・implementation・validation の全ゲートで、初回round 0、既定の最終round 4、最大5回という同じ予算が明示される
- 検証方法見込み: `automated`

#### AC-2: 最終ラウンドを事前宣言する

- Given: 次のレビューが解決済み予算の最終ラウンドとなる
- When: 進行役が差し戻しを記録する
- Then: 次回が最終であること、4類型、その他のwarning降格とfollow-up起票がレビュー開始前の調整状態に記録され、事後宣言では通過できない
- 検証方法見込み: `automated`

#### AC-3: 最終ラウンド後のblockingを4類型に限定する

- Given: 最終ラウンドのレビューに複数種類のfindingがある
- When: 進行役が最終ラウンド後のseverityを分類する
- Then: 既出未是正、目的直接阻害、テスト・ビルド失敗または回帰、データ喪失またはセキュリティ低下だけがblockingとして残る
- 検証方法見込み: `automated`

#### AC-4: データ喪失とセキュリティ低下を常にblockingにする

- Given: 任意のラウンド、risk、review profileでデータ喪失またはセキュリティ低下のfindingがある
- When: severityを分類する
- Then: 当該findingは例外なくblockingとなり、自動承認またはwarning降格されない
- 検証方法見込み: `automated`

#### AC-5: warning降格の追跡と原文を保持する

- Given: 最終ラウンド後に4類型外のfindingがある
- When: 進行役がwarningへ分類する
- Then: evidence原文を改変せず、元severity・降格理由・根拠・永続化済みfollow-up Issue番号を保持し、finding自体も削除されない
- 検証方法見込み: `automated`

#### AC-6: 取得・永続化不能時に安全側へ止まる

- Given: 現在ラウンドを取得できない、またはwarningのfollow-up Issueを永続化できない
- When: 進行役がゲート後の進行を判定する
- Then: 上限到達や追跡完了を推測せず人間判断を要求し、未追跡のまま進行しない
- 検証方法見込み: `automated`

#### AC-7: 非追記型の是正方針が全セグメント作業ワーカーへ届く

- Given: 任意セグメントの作業ワーカーがblocking findingを受け取る
- When: 配布された役割契約を確認する
- Then: 既存記述・既存実装の書換えまたは削除、不要要求の削減、原因が上流なら上流最小改訂を先に選び、局所的な条項・例外・分岐・フラグの追加で塞がない規則が明示される
- 検証方法見込み: `automated`

#### AC-8: ゲート品質と既存の安全側ラチェットを維持する

- Given: ラウンド予算を適用する任意のreview profileとquick状態がある
- When: ゲートの実行内容とprofile解決結果を比較する
- Then: ゲート・2観点・検査・必要レビュア数・quick免除範囲は減らず、riskまたはautonomyによるStrict固定も緩和されない
- 検証方法見込み: `automated`

## 制約・対象外

- 新規 ADR は作成しない。既定値とラウンドの数え方は accepted な ADR-0068 の判断を由来とするが、必要な契約は本仕様内に記載した。
- ゲート省略、レビュア削減、quick免除拡大、反証ルーブリック停止は対象外とする。
- Issue #777 の一般的な成果物分量上限・テスト適用性、Issue #784 のStrict並列起動、Issue #785 の別スループット施策、Issue #744 の診断改善は取り込まない。
- finding台帳全般の新設や、ラウンド予算と無関係なCoordination Backend変更は対象外とする。

## 完了条件・未決事項

- AC-1からAC-8が検証証跡へ一意に対応し、配布先の進行役と全セグメント作業ワーカーの観測可能な契約として成立すること。
- 未決事項はない。
