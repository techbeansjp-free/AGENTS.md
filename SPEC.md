# SPEC: npm testの間欠的失敗の原因調査・再発防止（テスト実行ログ保全の整備）

- Issue: `ISSUE-236`
- 作成者: `Codex segment worker`
- 対象ブランチ: `bugfix/236-preserve-test-execution-logs`

## 目的・背景

`npm test` で一度だけ発生した間欠的失敗は、当時の完全な出力が保存されておらず、失敗したテストとエラーを特定できなかった。本変更は原因未特定のテストを推測で変更せず、次の失敗時に調査可能な完全ログを残す。

## 対象範囲・前提

### 対象範囲

- `.github/workflows/agent-skill-chain-ci.yml` の `npm test` 実行と、その実行出力を保存する GitHub Actions artifact。
- `.agent-skill-chain/standards/` 配下にある独立検証の手動ゲートレビュー手順。
- 次回失敗時の Issue #236 への記録と、原因調査用フォローアップ Issue の起票手順。

### 前提・用語

- CI は GitHub Actions 上で実行され、artifact upload には GitHub 公式の upload-artifact action を用いる。
- 「完全ログ」は `npm test` の標準出力と標準エラー出力を同一ファイルに保存したものを指す。
- 「独立検証」は、実装者と別の検証担当者が実行する手動の受入・回帰確認を指す。

### 入力・出力・制約

- 入力: `npm test` の標準出力・標準エラー出力、手動検証で実行したコマンドと保存ログ。
- 出力: CI artifact 内のテスト実行ログ、および `VALIDATION.md` に記録されたログの位置または失敗箇所の抜粋。
- 制約: テストの実行順序・並列度・対象テストを変更せず、テスト結果の終了コードをログ保存処理によって隠蔽しない。機密情報をログへ意図的に追加しない。

## 要求 → 要件 → 受入条件

### 要求

CI と独立検証のどちらでも、テストが失敗したときに出力を後から確認できるようにする。

### 要件

- CI は `npm test` の標準出力・標準エラー出力を単一ログファイルに記録する。
- テストの成否にかかわらず、CI はそのログをダウンロード可能なアーティファクトとして保存する。
- 手動・独立検証ではテスト出力をファイルへ保存し、`.agent-skill-chain/standards/` 配下の手動ゲートレビュー手順と検証成果物にログ位置または失敗箇所の抜粋を残す。

### 受入条件（Acceptance Criteria）

#### AC-1: CI が完全なテスト出力を常時保存する

- Given: pull request の CI が `npm test` を実行する
- When: テストが成功または失敗する
- Then: 標準出力と標準エラー出力の全文がログファイルへ記録され、`if: always()` のアーティファクト upload step により保存される
- 検証方法見込み: `automated`

#### AC-2: 手動検証でテスト出力を保存して証跡化する

- Given: 独立検証担当者が手元で `npm test` を実行する
- When: テスト出力を確認する
- Then: `.agent-skill-chain/standards/` 配下の手動ゲートレビュー手順が、出力をファイルへリダイレクトして保存し、`VALIDATION.md` に保存先または失敗箇所の抜粋を記録することを明示している
- 検証方法見込み: `manual`

#### AC-3: 次回の間欠的失敗をフォローアップ可能にする

- Given: 保存済みログを伴う間欠的失敗が発生する
- When: 検証担当者が失敗を報告する
- Then: ログからテストファイル名、テスト名、エラー、スタックトレースを Issue #236 に追記し、タイミング依存・順序依存・リソース競合・非同期処理の race condition を評価対象に含む原因調査と再発防止のフォローアップ Issue を起票する手順がある
- 検証方法見込み: `manual`

## スコープ外

- 原因未特定のテストまたはプロダクトコードの変更。
- `--test-concurrency=1` など、テスト実行方式を恒久的に変更すること。
- 保存されていない過去の失敗ログから原因を遡及特定すること。

## 完了条件・未解決事項

### 完了条件

- AC-1 の CI workflow 構造を自動検査し、失敗時にもログ artifact を upload することを確認する。
- AC-2 と AC-3 の手動手順をレビューし、保存ログの証跡と次回失敗時の記録・フォローアップ手順を確認する。
- CI、型検査、対象変更の検査が成功し、各検証結果を `VALIDATION.md` に記録する。

### 未解決事項

- 間欠的失敗を発生させるテスト、再現率、根本原因は未特定である。
- 次回失敗時に保存されるログの保持期間と artifact 容量が GitHub の既定を超える場合の運用は、実際のログ量を観測してから別途判断する。
