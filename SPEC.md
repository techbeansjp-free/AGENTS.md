# SPEC: verify spec-bdd / verify design-diagram が本文中の正当な `<...>` パス変数表記を未置換プレースホルダと誤判定する不具合を修正する

- Issue: `ISSUE-461`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/461-verify-spec-bdd-placeholder-false-positive`

## 目的・背景

`.agent-skill-chain/scripts/verify-spec-bdd.sh`（`src/commands/verify.ts` の `specBdd()` 関数）は、SPEC.md の各 AC-ID（`#### AC-N: ...`）について、要約・Given・When・Then・検証方法見込みの各フィールドが `.agent-skill-chain/templates/issue/SPEC.md` のテンプレートプレースホルダ（`<...>` 形式）のまま未置換で残っていないかを検査する。判定には共有正規表現 `UNFILLED_PLACEHOLDER_RE = /<[^<>\n]*>/` を使い、フィールド値のどこかに `<...>` 部分文字列が1つでも含まれれば未置換プレースホルダとみなす。

この判定は、フィールド値が実内容で埋まっていても、その本文中に AGENTS.md 自身や本テンプレートが一般的に使う「パス変数表記」（例: `reviews/<gate>.yaml` の `<gate>`、`.worktrees/<YYYYMMDD_HHMMSS>-...` の `<YYYYMMDD_HHMMSS>`）が1箇所でも含まれるだけで誤って一致してしまう。実際に、Issue #449（`review:light` 軽量レビュープロファイル導入）の SPEC.md AC-10 の Then は「ゲート証跡（ローカルモード: `reviews/<gate>.yaml`、GitHubモード: ...）に...」という実内容の記述であり、テンプレートプレースホルダは一切残っていなかったが、`<gate>.yaml` という正当なパス変数表記が含まれていたことにより `verify spec-bdd` が「AC-10 の Then が未記入またはプレースホルダのままです」と誤検出した。この誤検出により、PR #460（Issue #449）は SPEC.md 初回commit以降、全commitで一貫して `verify` ジョブが恒久的にfailし続け、`gh pr merge --admin` によるブランチ保護の回避でしかマージできなかった（2026-08-05〜06、`gh run list --branch feature/449-review-light-profile` で確認済み）。

SPEC.md は承認後の編集が原則不可であり（AGENTS.md I2、承認後のAC変更はゲート再通過を強制する）、一度この誤検出に該当すると当該Issueのライフサイクル終了までCIをgreenにできない。同一の共有正規表現 `UNFILLED_PLACEHOLDER_RE` は `verify design-diagram`（`designDiagram()` 関数、DESIGN.md の「図示要否の判断」セクションの判断・根拠フィールドを検査する）でも使われており、同種の誤検出リスクを構造的に共有している。本Issueは、この誤検出を解消しつつ、実際に未置換のまま残っているテンプレートプレースホルダの検出能力（真陽性）を一切損なわないことを要求する。

## 要求 → 要件 → 受入条件

### 要求

`verify spec-bdd` および同一の未置換プレースホルダ検出ロジックを使う `verify design-diagram` は、検査対象フィールドの実内容の記述中に、テンプレート由来ではない正当な `<...>` 形式のパス変数表記が含まれるだけの理由で、そのフィールドを未置換プレースホルダと誤判定して失敗してはならない。同時に、実際にテンプレートのプレースホルダが未置換のまま残っている場合の検出は、修正後も現状と同一の確実性で機能し続けなければならない。

### 要件

- 要件1: `.agent-skill-chain/templates/issue/SPEC.md` が実際に使用する未置換プレースホルダの記法（フィールド値全体が単一の `<...>` のみで構成される場合、および検証方法見込みフィールドの `<automated | manual | hybrid>` のようなテンプレート固有の記法）は、修正後も引き続き未置換プレースホルダとして確実に検出されなければならない。
- 要件2: フィールド値の実内容の記述中に、テンプレート由来ではない正当な `<...>` 形式のパス変数表記（例: `reviews/<gate>.yaml` の `<gate>`）が埋め込まれている場合、そのパス変数表記の存在のみを理由に当該フィールドを未置換プレースホルダとして検出してはならない。
- 要件3: フィールド値が部分的に実内容化されていても、テンプレート由来の説明的なプレースホルダ表現（例: `<期待される結果>` のような、日本語の解説文言や複数語からなる `<...>` 表現）が本文中に残っている場合は、未置換プレースホルダとして検出しなければならない（要件2の緩和が検出漏れを生んではならない）。
- 要件4: 判定ロジックの修正は、`verify spec-bdd` が検査する SPEC.md の要約・Given・When・Then・検証方法見込みフィールドと、`verify design-diagram` が検査する DESIGN.md の図示要否の判断・根拠フィールドの双方に一貫した意味論で適用されなければならない。一方だけが誤検出緩和の対象となり、共有ロジックの意味論が検査対象ごとに分岐してはならない。
- 要件5: 修正後も、`verify spec-bdd` の既存の出力仕様（AC-ID ごとの要約・Given・When・Then・検証方法見込みの過不足をそれぞれ独立したエラーメッセージとして報告し、全て充足していれば終了コード0、いずれか不足があれば終了コード1で返す）を変更してはならない。`verify design-diagram` の既存の出力仕様（判断・根拠の未記入検出、判断が `要` の場合の mermaid コードフェンス存在検査、終了コード規約）についても同様に変更してはならない。

### 受入条件（Acceptance Criteria）

#### AC-1: 正当なパス変数表記を含むThenフィールドはプレースホルダとして誤検出されない

- Given: SPEC.md の AC セクションの Then フィールドが「ゲート証跡（ローカルモード: `reviews/<gate>.yaml`、GitHubモード: Check Run またはPRレビュー相当の記録）に...」のように、実内容の記述中に正当なパス変数表記 `<gate>` を含む（他の全フィールドは実内容で充足している）。
- When: `agent-skill-chain verify spec-bdd <spec.md のパス>` を実行する。
- Then: 当該フィールドは未置換プレースホルダとして検出されず、他の検査項目も充足していれば終了コード0で成功する。
- 検証方法見込み: `automated`

#### AC-2: テンプレートをそのまま複製したSPEC.mdは従来通り全項目がプレースホルダとして検出される

- Given: `.agent-skill-chain/templates/issue/SPEC.md` をそのまま複製した、Given/When/Then/検証方法見込み/要約が全てテンプレートのプレースホルダのまま残っている SPEC.md。
- When: `verify spec-bdd` を実行する。
- Then: 修正前と同一のエラーメッセージ（各AC-IDの要約・Given・When・Then・検証方法見込みそれぞれについてプレースホルダ残存または空欄を指摘するメッセージ）が全て出力され、終了コード1で失敗する。
- 検証方法見込み: `automated`

#### AC-3: 部分的に実内容化されていても説明的プレースホルダが残っていれば検出される

- Given: SPEC.md の AC セクションの Then フィールドが「ログイン後、`<期待される結果>` へ遷移する」のように、実内容の記述の一部にテンプレート由来の説明的なプレースホルダ表現が未置換のまま残っている。
- When: `verify spec-bdd` を実行する。
- Then: 当該フィールドは未置換プレースホルダが残っているものとして検出され、対応するエラーメッセージとともに終了コード1で失敗する。
- 検証方法見込み: `automated`

#### AC-4: DESIGN.mdの図示要否の根拠に正当なパス変数表記が含まれてもプレースホルダとして誤検出されない

- Given: DESIGN.md の「### 図示要否の判断」セクションの根拠フィールドが、実内容の記述中に正当な `<...>` 形式のパス変数表記を含む（判断フィールドは `要` または `不要` のいずれかで充足している）。
- When: `agent-skill-chain verify design-diagram <design.md のパス>` を実行する。
- Then: 当該根拠フィールドは未置換プレースホルダとして検出されず、他の検査項目（判断が `要` の場合の mermaid コードフェンス存在等）も充足していれば終了コード0で成功する。
- 検証方法見込み: `automated`

#### AC-5: DESIGN.mdの図示要否判断・根拠が未置換のまま残っている場合は従来通り検出される

- Given: DESIGN.md の「### 図示要否の判断」セクションの判断または根拠がテンプレートのプレースホルダのまま未置換で残っている。
- When: `verify design-diagram` を実行する。
- Then: 修正前と同一の意味論で未置換プレースホルダ残存として検出され、終了コード1で失敗する。
- 検証方法見込み: `automated`

## 未決事項

### 1. 誤判定緩和の実現アプローチ

Issue #461 本文は、実現アプローチの候補として (a) テンプレート側のプレースホルダ表記を `<...>` と衝突しない別記法（例: `{{要約}}`）へ変更する、(b) 判定ロジック側で既知の非プレースホルダパターン（パス文脈で使われる短い `<...>` 表記）を除外する、(c) Given/When/Then 内の `<...>` は原則プレースホルダとみなさず要約行のみを検査対象にする、の3案を挙げている。いずれを採るか、および要件1〜3を同時に満たせるかどうかの技術的検証は design segment の責務であり、本SPECはどの実現アプローチを採るかを規定しない。

### 2. 既存の未マージPRへの影響範囲

修正前の `UNFILLED_PLACEHOLDER_RE` の誤検出により、他にも同種の誤判定でCIが恒久failしているオープンなPR・Issueが存在する可能性がある。本Issueのスコープは判定ロジックの修正のみとし、影響を受けた既存PR・Issueの洗い出しと再検証（CI再実行等の個別対応）が必要かどうかの判断は、本Issueのマージ後に進行役が別途行う。

## スコープ外

- 判定ロジックの具体的な実装（正規表現の変更内容、テンプレート記法の変更要否等）の決定。これは `DESIGN.md` の責務であり、本SPECは「何を満たすべきか」のみを定める。
- `verify spec-bdd` / `verify design-diagram` 以外の検査コマンド（`verify ac-coverage` 等）における同種の誤検出リスクの洗い出しと対応。現時点で共有正規表現 `UNFILLED_PLACEHOLDER_RE` を使用しているのは `specBdd()` と `designDiagram()` の2箇所のみであり、本Issueはこの2箇所を対象とする。
- `bdd.profile: strict`（構造化マーカー強制モード）自体の仕様変更。
- 既に本不具合の影響で `--admin` マージにより回避済みの過去のPR（例: PR #460）に対する事後対応（再検証・CI再実行等）。未決事項2の通り、対応要否の判断は本Issueのマージ後に進行役が別途行う。
