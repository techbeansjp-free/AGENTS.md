# SPEC: 判定プロンプトへ gate_id 別の固定入力集合を展開し、成果物の事実主張を検証可能にする

- Issue: `ISSUE-751`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/751-reviewer-prompt-input-closure`

## 目的・背景

ゲートレビュアはツール呼び出しを一切許可されておらず、判定プロンプトへ文字列として展開された内容だけを検証できる。ところが現行の判定プロンプトは、(a) gate_id によらず上流成果物として `SPEC.md` だけを展開し、(b) 憲法文書 `AGENTS.md` を一切展開せず、(c) 成果物が事実主張の根拠として名指しするリポジトリ内ファイルを展開しない。

このため 2 種の害が同時に出ている。第一に、レビュアが「実物を確認できないため断定できない」として `inconclusive` を返し、I8 により `approved` が導出されず `human_required` へ倒れる。これは成果物にも実装にも欠陥が無い場合に発生する。第二に、比較対象が展開されないことで反例探索そのものが実行不能となり、実在した設計と実装の乖離が blocking 0・両観点 pass のまま素通りする。前者は「反証を開始できない」害、後者は「反証が空振りする」害であり、いずれも入力側の欠落が原因である。

本 Issue の目的は、gate_id ごとに展開する入力の集合を固定の表として定めてそのとおりに展開すること、および展開できなかった入力をレビュアが判別できる形で明示することである。本 Issue は #766・#767・#771 を統合しており、4 件はいずれも判定プロンプトが何を展開するかという同一の変更点に帰着する。

## 用語（本 SPEC 内での定義）

- 判定プロンプト: `gate reviewer-prompt` が標準出力へ生成する、ゲートレビュアへの唯一の入力文字列。
- 展開: あるファイルの全文（target SHA の blob と同一の内容）を、パスを明示した見出しとともに判定プロンプト本文へ文字列として含めること。
- 散文成果物: `SPEC.md`・`DESIGN.md`・`PLAN.md`・`VALIDATION.md`・当該 Issue の ADR。
- 当該 Issue の ADR: base SHA から target SHA までの差分で追加または変更された `docs/adr/` 配下のファイル。
- 根拠ファイル: 散文成果物の本文がリポジトリルート相対のパス文字列として名指しし、かつ target SHA のツリーに実在するファイル。
- 無言の切り捨て: 入力の一部だけを展開する、または展開を省略しながら、その事実をレビュアが判別できる形で提示しないこと。

## 前提（2026-08-18 の実測。本リポジトリ、CLI 0.2.129）

- 実測 A: ISSUE-729 の implementation セグメント（target `d957ca7`、base `28631aff`）に対し `gate reviewer-prompt` を実行した。生成物は 639,496 バイトで、`# DESIGN: ` で始まる行 0 件、`# PLAN: ` で始まる行 0 件、`AGENTS.md` の冒頭行 0 件、`# SPEC: ` で始まる行 1 件だった。当該 target SHA には `DESIGN.md`（43,234 B）・`PLAN.md`（16,506 B）・ADR 1 件（13,195 B）が実在する。
- 実測 B: 同 Issue の design セグメント（target `edd5238`）の生成物は 108,554 バイトで、`AGENTS.md` の冒頭行は 0 件だった。`AGENTS.md` は 23,930 B である。
- 実測 C: 上流成果物区間の構築は gate_id に依存しない。`src/commands/gate.ts` の当該箇所の原文は次のとおり。

````ts
    if (gateId !== 'spec') {
      sections.push('## 上流の承認済み成果物（整合検査用）');
      sections.push('### SPEC.md');
      sections.push(specText ? '```\n' + specText.trimEnd() + '\n```' : '(未検出)');
      sections.push('');
    }
````

- 実測 D: target `d957ca7` のツリーにある 988 個のパス文字列のうち、各成果物の本文へ部分文字列として現れるものを数えると `SPEC.md` 0 件、`PLAN.md` 0 件、`DESIGN.md` 7 件（合計 229,366 B）、`VALIDATION.md` 30 件（合計 333,316 B）だった。最大は `src/commands/gate.ts` の 105,330 B である。
- 実測 E: 判定プロンプトのレビュアへの受け渡しは argv を経由しない。claude アダプタは一時ファイルへ書き出して標準入力へリダイレクトし、codex アダプタは末尾 `-` により標準入力から読む。現に実測 A の 639,496 バイトが失敗せず生成・使用されている。したがって制約となるのは argv 長上限ではなくレビュアモデルの文脈長であり、超過は「入力の末尾側が読まれない」という無言の形で現れうる。

## 要求

gate_id ごとに、レビュアが検証すべき対象と、それを照合する基準が判定プロンプトへ必ず展開されること。展開する入力の集合は固定の表で一意に定まり、実行時の裁量で増減しないこと。量の上限により展開しきれない入力が生じる場合は、その事実と対象をレビュアが判別できる形で明示し、無言の切り捨てを行わないこと。

## 方針の選択

判定プロンプトの入力範囲を拡張する方針を採る。「成果物側へ実物の原文引用を義務づける」「事実主張の検証を後段セグメントへ委譲する」の 2 方針は採らない。理由は次のとおり。

- 引用義務は運用として既に適用済みだが、引用の忠実性・網羅性そのものが検証不能であるため `inconclusive` を解消しない。ISSUE-741 の design-gate では原文引用を追加した後も、strict 2 slot の一方が blocking 0 のまま falsification を pending とした。
- 検証の後段委譲は、比較対象が無いまま approved を導出することを追認する。実測として、accepted な ADR と実装の乖離が implementation-gate を blocking 0 で通過した事例がある。
- 入力範囲の拡張はレビュアの read-only 性を変えない。展開は target SHA の git object から trusted な生成側が行い、候補コードの実行を伴わない。

## 展開入力の固定表（本表を唯一の正本とする）

| gate_id | 判定対象成果物（現行維持） | 上流の承認済み成果物 | 憲法文書 | 根拠ファイルの抽出元 |
|---|---|---|---|---|
| spec | `SPEC.md` | 無し | `AGENTS.md` | `SPEC.md` |
| design | `DESIGN.md`・`PLAN.md`・当該 Issue の ADR | `SPEC.md` | `AGENTS.md` | 左の判定対象成果物と `SPEC.md` |
| implementation | 差分に含まれる成果物以外の全ファイル | `SPEC.md`・`DESIGN.md`・`PLAN.md`・当該 Issue の ADR | `AGENTS.md` | 左の上流成果物 |
| validation | `VALIDATION.md`・`test-execution.log` | `SPEC.md`・`DESIGN.md`・`PLAN.md`・当該 Issue の ADR | `AGENTS.md` | `VALIDATION.md` と左の上流成果物 |

「判定対象成果物」列は現行の挙動であり本 Issue で変更しない。残る 3 列が本 Issue で追加する展開である。表に無い入力を展開せず、表にある入力を実行時の判断で省略しない。唯一の例外は要件 5 の予算超過であり、その場合も要件 6 の通知を出す。

## 要件

- 要件 1: 固定表の「上流の承認済み成果物」列を gate_id ごとに切り替えて展開する。
- 要件 2: 固定表の「憲法文書」列に従い、全 gate_id で `AGENTS.md` を展開する。
- 要件 3: 固定表の「根拠ファイルの抽出元」列の散文成果物から根拠ファイルを抽出して展開する。抽出は 1 段に限り、根拠ファイル・`AGENTS.md`・差分本文からの再抽出を行わない。既に展開済みのファイルを二重に展開しない。
- 要件 4: 根拠ファイルの集合は、抽出元本文と target SHA のツリーのみから決まる決定的な集合とする。実行時刻・作業ツリーの未コミット変更・実行環境・実行回数に依存しない。
- 要件 5: 根拠ファイルの展開量に総バイト予算を設ける。予算を超える分は展開せず省略として扱う。「判定対象成果物」「上流の承認済み成果物」「憲法文書」の 3 列は予算の対象外とし、予算を理由に省略しない。
- 要件 6: 展開しない根拠ファイル（予算超過・非テキスト）について、パス・バイト長・内容 digest・省略理由を列挙した通知を出す。ファイルの一部だけを展開しない。
- 要件 7: 展開済みファイルの一覧、省略ファイルの一覧、適用した予算値を判定プロンプト本文へ明示する。
- 要件 8: ハルシネーション防止の指示を、展開済み一覧・省略一覧と整合する形へ更新する。展開済みとして列挙されたファイルは内容が与えられていること、省略として列挙されたファイルは内容不明であり推測してはならないことを、両一覧を判別手段として明示する。
- 要件 9: 上流成果物が target SHA に存在しない場合、および base SHA 未指定で当該 Issue の ADR 集合を導出できない場合も、生成は失敗せず当該入力の不在・導出不能を明示する。
- 要件 10: 既存区間（conformance・falsification ルーブリック、出力 JSON 契約、判定対象の差分、判定対象の成果物、過去ラウンドの判定記録、Light プロファイル追加ルーブリック）を削除・改変しない。
- 要件 11: 判定プロンプトの生成経路を 1 つに保ち、`gate reviewer-prompt` の標準出力と、レビュー証跡の照合が期待値として生成するプロンプトを同一にする。

## 受入条件（Acceptance Criteria）

#### AC-1: 固定表のとおりに展開される

- Given: 固定表の各列に対応する入力が target SHA に実在するリポジトリと、4 つの gate_id
- When: 各 gate_id で判定プロンプトを生成する
- Then: 固定表の当該行に挙げた全入力が、パスを明示した見出しとともに全文で本文に現れる。当該行に挙げていない入力は本文に現れない
- 検証方法見込み: `automated`

#### AC-2: 根拠ファイルの抽出が閉じている

- Given: 散文成果物が実在パス・非実在パスの双方を本文で名指しし、`AGENTS.md` と実装ファイルがさらに別のパスを名指ししている target SHA
- When: 判定プロンプトを生成する
- Then: 展開される根拠ファイルは、固定表が定める抽出元の本文が名指しし、かつ target SHA に実在するものに限られる。`AGENTS.md`・根拠ファイル・差分本文だけが名指しするファイルは展開されない。同一ファイルは 2 回以上展開されない
- 検証方法見込み: `automated`

#### AC-3: 生成が決定的である

- Given: 同一の issue_id・gate_id・target SHA・base SHA・ラウンド文脈
- When: 判定プロンプトを 2 回生成し、その間に作業ツリーへ未コミットの変更を加える
- Then: 2 回の生成物はバイト列として一致する
- 検証方法見込み: `automated`

#### AC-4: 予算超過を無言で切り捨てない

- Given: 根拠ファイルの合計バイト長が総バイト予算を超える target SHA
- When: 判定プロンプトを生成する
- Then: 展開されなかった根拠ファイルはパス・バイト長・内容 digest・省略理由を伴う通知として列挙される。いずれの根拠ファイルも本文の一部だけが展開されることはなく、全文展開か省略のいずれかである
- 検証方法見込み: `automated`

#### AC-5: 必須の入力は予算を理由に省略されない

- Given: AC-4 と同じ、予算を超過する target SHA
- When: 判定プロンプトを生成する
- Then: 固定表の「判定対象成果物」「上流の承認済み成果物」「憲法文書」の各入力は全て全文で展開されており、省略一覧に現れない
- 検証方法見込み: `automated`

#### AC-6: 展開済みと省略をレビュアが判別できる

- Given: 展開された根拠ファイルと省略された根拠ファイルが同時に存在する判定プロンプト
- When: 生成された本文を読む
- Then: 展開済みファイルの一覧、省略ファイルの一覧、適用した予算値が本文に現れる。抽出された根拠ファイルは全て、いずれか一方の一覧に必ず 1 回現れる
- 検証方法見込み: `automated`

#### AC-7: ハルシネーション防止の指示が両一覧と整合する

- Given: 展開済み一覧と省略一覧を持つ判定プロンプト
- When: ハルシネーション防止の区間を読む
- Then: 展開済みとして列挙されたファイルは内容が与えられている旨と、省略として列挙されたファイルは内容不明であり推測してはならない旨の双方が明示され、両一覧が判別手段として参照されている。展開済みファイルまで検証不能として扱うよう促す記述は存在しない
- 検証方法見込み: `automated`

#### AC-8: 入力が欠けても生成が失敗しない

- Given: `DESIGN.md`・`PLAN.md`・ADR のいずれかが target SHA に存在しない状態、および base SHA を与えない呼び出し
- When: 判定プロンプトを生成する
- Then: 生成は終了コード 0 で成功し、当該入力について不在または導出不能である旨が本文に明示される。空文字列で沈黙して埋められることはない
- 検証方法見込み: `automated`

#### AC-9: 既存区間と生成経路の単一性が保たれる

- Given: 本 Issue の変更を適用したリポジトリ
- When: 判定プロンプトを生成し、レビュー証跡の照合が期待値として生成するプロンプトと突き合わせる
- Then: 要件 10 が列挙する既存区間は削除も改変もされておらず、`gate reviewer-prompt` の標準出力と照合側の期待値はバイト列として一致する
- 検証方法見込み: `automated`

## スコープ外

- 判定プロンプトの入力スナップショットからの再生成と照合機構（Issue #750 の射程）。本 Issue は要件 4・要件 11 の性質を満たす生成を求めるにとどまり、生成物を後から再生成して突き合わせる機構は追加しない。
- レビュアへのツール呼び出し許可。read-only ファイル読み取りの付与を含む。隔離条件と証跡の再設計を伴うため扱わない。
- 成果物内の引用が引用元と一致するかの機械的検証。
- 成果物側への原文引用義務の規範化（方針の選択で不採用とした案）。
- 名指しされたパスが target SHA に実在しない場合の検出と通知。根拠ファイルの抽出は実在するパスとの照合に基づくため、実在しないパスは検出対象にしない。
- 「判定対象成果物」列の変更。validation ゲートへの実装差分の追加を含む。
- 反証ルーブリックの内容・ラウンド予算の閾値。ISSUE-729 で確定済みであり本 Issue は変更しない。
- レビュアモデルの文脈長そのものの拡大、およびモデル選定。
- ADR finalization が要求する design gate-report の digest 必須保証の変更。現行の保証を維持する。
- consumer リポジトリの導入版更新手順、および過去に記録済みのレビュー証跡の再検証。

## 未決事項

- 「リポジトリルート相対のパス文字列として名指しする」ことの具体的な判定規則。要件 4 の決定性を満たす範囲で設計セグメントが確定する。
- 総バイト予算の値と、その正本の所在（実装内の定数か設定項目か）。予算内で展開する根拠ファイルの選択順序も併せて確定する。
- 省略通知の digest 形式を既存の成果物 digest 形式と揃えるかどうか。
