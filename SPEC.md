# SPEC: codexアダプタのサンドボックスがworktreeのgitメタデータを書込み可能領域へ含めず、セグメント作業ワーカーがcommitできない

- Issue: `ISSUE-682`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/682-codex-sandbox-worktree-gitdir`

## 目的・背景

agent-skill-chain は 1 Issue = 1 ブランチ = 1 worktree = 1 PR を不変条件とし、セグメント作業ワーカーは常に当該 Issue 用の worktree を実行位置として起動される。ワーカーの実行系が codex アダプタに設定されている場合、アダプタは codex CLI をサンドボックス指定 `workspace-write` で起動する。このサンドボックスは実行位置配下を書込み可能にし、それ以外の領域は起動オプション `sandbox_workspace_write.writable_roots` に明示列挙した範囲だけを書込み可能にする。

git の linked worktree では、worktree 直下の `.git` は実体ディレクトリではなく、共有リポジトリ側の git ディレクトリ配下にある当該 worktree 専用のメタデータディレクトリを指し示す 1 行のファイルである。`git commit` は排他制御用の `index.lock` をこの worktree 専用メタデータディレクトリに作成し、その後 `HEAD`・`index`・reflog を同ディレクトリへ、オブジェクトと共有 ref を共有 git ディレクトリへ書き込む。

現状の codex アダプタは書込み可能領域として共有 git ディレクトリだけを列挙する。codex のサンドボックスは列挙された領域の子孫ディレクトリを自動的に書込み可能にはしないため、worktree 専用メタデータディレクトリへの書込みが「Read-only file system」で拒否され、`git commit` が実行できない。結果としてワーカーは実装・検証を完了しても成果物を commit・push できず、完了報告にも到達しない。書込み可能領域へ worktree 専用メタデータディレクトリを追加した場合に同じ書込みが成功することは、同一の codex CLI・同一モデルで列挙要素のみを変えた比較により実測確認されている。

本 Issue の目的は、codex アダプタで起動したセグメント作業ワーカーが worktree 上で commit・push を完遂できるようにし、同時にサンドボックスの書込み可能領域を必要最小限に保つことである。

## 前提・用語

- 共有 git ディレクトリ: リポジトリの全 worktree が共有する git メタデータの実体ディレクトリ（オブジェクト、共有 ref 等を保持する）。
- worktree 専用メタデータディレクトリ: linked worktree ごとに存在し、当該 worktree の `HEAD`・`index`・`index.lock`・reflog を保持するディレクトリ。共有 git ディレクトリの配下に置かれるが、書込み許可は共有 git ディレクトリの列挙によって自動的には及ばない。
- 通常の作業ツリー: linked worktree ではない作業ツリー。`.git` が実体ディレクトリであり、共有 git ディレクトリと worktree 専用メタデータディレクトリが同一パスになる。
- 書込み可能領域: codex 起動オプション `sandbox_workspace_write.writable_roots` に列挙する絶対パスの集合。
- 本仕様の対象は、codex アダプタがセグメント作業ワーカー用に組み立てる codex 起動コマンドの書込み可能領域の決定である。

## 要求 → 要件 → 受入条件

要求から要件、そして機械検証可能な受入条件（AC-ID）まで一意に追跡できる形で記述する。AC-ID は正規表現 `^AC-[0-9]+$` に従い、`#### ` 見出しとしてのみ宣言する。

### 要求

codex アダプタで起動されたセグメント作業ワーカーが、Issue 用 worktree 上で `git commit` と `git push` を実行し、成果物を remote へ残して完了報告できること。ただしサンドボックス自体は無効化せず、書込み可能領域を目的達成に必要な範囲より広げないこと。

### 要件

- 要件1: codex ワーカー起動コマンドの書込み可能領域に、起動対象作業ツリーが commit 時に書き込む git メタデータの実体（共有 git ディレクトリ、および linked worktree の場合は worktree 専用メタデータディレクトリ）の絶対パスを含める。
- 要件2: これらのパスは固定文字列で組み立てず、起動対象作業ツリーを実行位置とした git への問い合わせ結果から解決する。シンボリックリンクを解決した絶対パスを用いる。
- 要件3: 通常の作業ツリーでは 2 つのパスが同一になるため、重複要素を作らず、上位ディレクトリへの一般化も行わない。
- 要件4: いずれかのパスを解決できない場合は、書込み不能のまま起動して commit 段階で停止させず、codex を起動する前に日本語の理由付きエラーで停止する。
- 要件5: サンドボックス指定は `workspace-write` のまま維持し、全権限付与・サンドボックス迂回へ切り替えない。書込み可能領域には上記 git メタデータディレクトリ以外を追加しない。
- 要件6: linked worktree と通常の作業ツリーの双方について、上記の挙動を自動テストで検証する。

### 受入条件（Acceptance Criteria）

各 AC には、散文形式の Given/When/Then による受け入れシナリオを添える。検証方法見込みは `automated`・`manual`・`hybrid` のいずれか 1 語で記す。

#### AC-1: linked worktree では worktree 専用メタデータディレクトリと共有 git ディレクトリの両方が書込み可能領域に含まれる

- Given: `.git` が 1 行のファイルである linked worktree があり、ワーカーの実行系として codex アダプタが選択されている
- When: 当該 worktree を実行位置としてセグメント作業ワーカーの codex 起動コマンドを組み立てる
- Then: 組み立てられたコマンドの書込み可能領域に、当該 worktree の専用メタデータディレクトリの絶対パスと共有 git ディレクトリの絶対パスの両方が要素として含まれる
- 検証方法見込み: `automated`

#### AC-2: 通常の作業ツリーでは書込み可能領域が git ディレクトリ 1 要素のみになる

- Given: `.git` が実体ディレクトリである通常の作業ツリーがあり、ワーカーの実行系として codex アダプタが選択されている
- When: 当該作業ツリーを実行位置としてセグメント作業ワーカーの codex 起動コマンドを組み立てる
- Then: 書込み可能領域は当該作業ツリーの git ディレクトリの絶対パス 1 要素のみで構成され、同一パスの重複要素も上位ディレクトリの要素も含まない
- 検証方法見込み: `automated`

#### AC-3: linked worktree 上で実際の commit が書込み可能領域内で完遂する

- Given: linked worktree 上に未 commit の変更があり、当該 worktree 向けに組み立てられた書込み可能領域が得られている
- When: 当該 worktree を実行位置として実際に `git commit` を実行し、commit によって作成・更新されたファイルのパスを収集する
- Then: commit が成功し、収集した全パスが「当該 worktree 配下」または「書込み可能領域のいずれかの要素の配下」に含まれる
- 検証方法見込み: `automated`

#### AC-4: 書込み可能範囲を必要以上に広げない

- Given: linked worktree と通常の作業ツリーのそれぞれについて codex 起動コマンドが組み立てられている
- When: 組み立てられたコマンド全体を検査する
- Then: サンドボックス指定は `workspace-write` のままであり、全権限付与・承認とサンドボックスの迂回を指示するオプションを含まず、書込み可能領域の要素は git メタデータディレクトリのみで、作業ツリー root・ホームディレクトリ・一時ディレクトリ・ファイルシステムルートを含まない
- 検証方法見込み: `automated`

#### AC-5: パス解決に失敗した場合は起動前に日本語エラーで停止する

- Given: 起動対象作業ツリーで git メタデータディレクトリの絶対パスを解決できない状態にある
- When: codex アダプタでセグメント作業ワーカーの起動を試みる
- Then: codex プロセスを起動せず、非 0 の終了コードで停止し、標準エラー出力へ解決失敗の理由を示す日本語メッセージを出力し、取得済みの writer lease を保持したまま放置しない
- 検証方法見込み: `automated`

#### AC-6: 既存の codex ワーカー起動挙動を後退させない

- Given: 本変更の前後で同一の設定・環境変数を与えた codex ワーカー起動コマンドの組み立てを比較できる
- When: 書込み可能領域以外の要素（ネットワーク許可指定、モデル指定、reasoning effort 指定、契約文の標準入力渡しと位置引数渡しの切替）を検査する
- Then: これらの要素は本変更の前と同一であり、既存の自動テストがいずれも成功する
- 検証方法見込み: `automated`

## 制約

- 書込み可能領域の拡大は、commit・push の完遂に必要な git メタデータディレクトリの追加に限る。サンドボックスの目的（作業ワーカーによる想定外領域への書込みの防止）を損なう緩和を行わない。
- パスの決定は環境非依存でなければならない。リポジトリ配置・worktree 名・空白や記号を含むパスに依存した固定文字列を用いない。
- エラー時の停止は、まだ何も起動していない段階で行う安全側の停止として扱う。

## 完了条件

- 上記 6 件の AC がすべて満たされ、それぞれに対応する検証証跡が残っている。
- linked worktree と通常の作業ツリーの双方を対象とする自動テストが追加され、片方の構成でしか成立しない実装が全 AC を通過しない状態になっている。
- 既存のリポジトリ検査（build・型検査・shell 構文検査・lint・テンプレート同期検査・テスト）が成功する。

## 未決事項

- 現時点で未解決の曖昧さは無い。

## スコープ外

- claude アダプタによるワーカー起動経路。当該経路には同種のサンドボックス制約が無く、本欠陥は発生しない。
- codex のサンドボックス設定のうち、ネットワーク許可指定など本欠陥に関係しない項目の変更。
- サンドボックス指定を全権限付与へ切り替える方針変更。
- codex のコマンド実行ポリシーによる拒否（特定形式のコマンドが拒否される事象）。本欠陥とは別事象である。
- ゲートレビュア起動時の読取専用サンドボックス設定。
- codex CLI 自体の実装変更。

## 参照（由来・追跡のための補助情報。要求・制約は本文内に自己完結して記載済み）

- Issue #682（本仕様の起票元）
- Issue #677（本欠陥により実装セグメントが commit 段階で停止した事例）
