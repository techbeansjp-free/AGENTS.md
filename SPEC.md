# SPEC: 反証観点をゲートの合否条件から外し、finding のトリアージへ置き換える

- Issue: `ISSUE-808`
- 作成者: `run-7e2269c6`
- 対象ブランチ: `process/808-falsification-not-binary-gate`

## 目的・背景

セグメントゲートは立証（conformance）と反証（falsification）の双方に pass を要求している。立証は `SPEC.md` が確定させた有限の AC 集合に対する検査であり、全件を検査すれば終わる。反証は反例の全空間に対する探索であり、判定プロンプトは反例の探索を指示する一方で探索の停止条件を持たない。「反例が見つからなかった」は「反例が存在しない」を意味しないため、反証の pass は成果物の性質ではなく当該レビュアの当該実行における探索結果にすぎない。これを二値の合否条件に用いる限り、同一入力に対する判定は原理的に非決定的になる。

2026-08-19 の1日の運用実測では、判定プロンプトの digest が両レビュアで完全一致していたにもかかわらず判定が4回割れた。内訳は、spec で反証の pass/fail が割れた事例、implementation の round 2 で2体が別々の blocking を出した事例、validation で同一論点の severity が warning と blocking に割れた事例、validation で立証の pass/fail が割れた事例である。さらにラウンドごとに前ラウンドに無かった blocking が現れ、3ラウンド目でようやく承認された。`profile: strict` は独立2体のいずれか一方が blocking を出せば reject するため、停止条件を持たない探索の論理和になり、レビュア数を増やすほど通過確率が下がる。2026-08-20 時点の open Issue 41件のうち 23件が `risk:high` であり、現行の不変条件 I8 は過半の Issue を自動的に strict へ倒す。

反証の探索そのものは、立証観点では検出されない実欠陥を検出している。ゲートを恒久的に判定不能へ固定する機構の欠陥、終了コード0が成果物の残存を隠蔽する経路、存在しないテスト題名を pass として引用した検証結果の3件がその実例である。したがって本 Issue は探索を維持したまま、反証を合否条件として用いることだけをやめる。収束は「反証が決定的になること」では達成しない。「gate-report だけを読む第三者が同じ分類を再現できる根拠を伴う finding しかゲートを止めないこと」によって達成する。以上の実測・判断・根拠は本成果物内で完結しており、意味を外部資料へ委譲しない。

## 用語・前提

- 観点: finding を産出した判定の立場。`conformance`（立証）または `falsification`（反証）の2値をとる。
- 昇格類型: 反証観点の finding を blocking にできる類型。閉じた列挙で定義し、列挙外の値を受け付けない。
- 必須根拠: 昇格類型ごとに定めた根拠の形式。判定対象成果物からの引用と、対応する AC-ID・成果物パス・コマンド出力のいずれかを含み、gate-report だけを読む第三者が同じ分類を再現できることを要件とする。
- 反証探索記録: 当該 attempt で反証探索を実施した事実と、そこで産出した finding 群の記録。合否値を持たない。
- 明示オプトイン: 人間だけが行える設定変更またはラベル付与であり、付与主体が人間であることを機械的に確認できたものを指す。エージェントは自動で行えない。値は `full`（全セグメントへ `strict` を要求する。現行の `autonomy: full` 明示設定がこれに該当する）と `light` の2種類に限り、列挙外の値を受け付けない。
- コア対象: 次のいずれかが成立すること。(1) 変更差分が `AGENTS.md`・`.agent-skill-chain/schemas/` 配下・`.agent-skill-chain/config/segments.yaml`・`docs/adr/` 配下のいずれかを含む、(2) 変更差分がプロジェクトポリシーへ登録済みのコアレビュー対象パス列挙に該当する、(3) コア監査が明示指定されている。(1) は本仕様が固定し、(2) は機械的に読める形で閉じて登録された列挙だけを指し、未登録のパスは該当しない。いずれの経路も `risk` の値・レビュアの主観を入力に含めない。
- 本仕様は GitHub モードとローカルモードの双方、および spec・design・implementation・validation の4ゲートすべてに適用する。

## 要求 → 要件 → 受入条件

### 要求

1. `FALSIFICATION_AS_BINARY_GATE`: 反証観点の合否値をゲート最終判定の入力から外す。反証は finding を産出する装置として扱う。
2. `SUBJECTIVE_BLOCKING`: blocking への昇格を、閉じた列挙の昇格類型と必須根拠を満たす finding だけに限定する。レビュアの主観的な重大性判断のみでゲートを止めない。昇格しなかった finding は削除せず証跡に残す。
3. `STRICT_FORCED_BY_RISK`: `risk` の値を review profile 決定の入力から外す。profile 決定の入力を閉じた列挙で定め、既定を `standard` とし、`strict` は `full` の明示オプトイン、コア対象の成立、変更差分を解決できない場合の安全側固定でのみ発生させる。
4. 反証の探索そのものと、立証観点の厳格さを弱めない。

### 要件

#### R1: finding の観点宣言と severity 決定

- 全 finding は観点（`conformance` / `falsification`）を必須項目として宣言する。宣言の無い finding、列挙外の観点値・昇格類型 id・severity 値を含む verdict は受理しない。
- 立証観点の AC-ID 未達・未証跡は、昇格類型と必須根拠の要件を適用せず blocking のままとする。この要件は不変条件 I7 の維持であり、緩和しない。
- 反証観点の finding は、昇格類型に該当し、かつ当該類型の必須根拠を伴う場合だけ blocking とする。
- 昇格類型の集合は設計セグメントで確定する。出発点は「当該 Issue の目的の直接阻害」「既存挙動の回帰」「データ喪失またはセキュリティ低下」「CI・ビルドの失敗」の4類型とし、各類型に必須根拠の形式を1つずつ定める。現行の判定プロンプトが用いている目的阻害性・到達可能性・責務内是正可能性の3条件は、この確定作業へ引き継ぐ。
- 昇格しなかった finding は warning または info として記録し、severity・観点・origin・code・evidence 原文をそのまま保持する。要約・整形・置換・削除をしない。既存の降格記録および follow-up 起票の経路を妨げない。

#### R2: ゲート最終判定の導出

- 最終判定の入力は、立証観点の合否値、blocking と分類された finding の有無、反証探索記録の有無、判定不能表明の4つに限る。反証観点の合否値は入力に含めない。
- 導出は次の順序で評価し、最初に成立した規則で確定する。順序は仕様であり、実装が別順序で評価してはならない。
  1. 立証が fail、または blocking と分類された finding が1件以上ある → `rejected`
  2. 立証が pending、反証探索記録が無いか不完全、またはレビュアが判定不能を表明した → `human_required`
  3. 上記のいずれにも該当しない → `approved`
- 反証探索記録が無い attempt を `approved` にしてはならない。これが反証探索の維持を機械的に担保する。
- ゲート結果を保持するスキーマは、反証観点について pass/fail/pending の合否値ではなく、探索の実施有無と finding 群を保持する形へ変更する。

#### R3: レビュープロファイルの決定

- profile 決定の入力は次の4つに限る。`risk` の値はいずれの入力にも含めず、`high`・`unclassified` を含むいかなる `risk` 値も profile を変えない。(a) `full` の明示オプトインの有無、(b) `light` の明示オプトインの有無、(c) 変更差分を機械的に解決できたか、および解決した差分にコア対象が成立するか、(d) 当該 Issue の当該ゲートが過去のラウンドで `strict` へ確定済みか。
- 明示オプトインの付与主体を人間と機械的に確認できない場合、当該オプトインは無かったものとして扱う。`full`・`light` のいずれについても同様に扱い、確認不能を理由に profile を昇格させない。
- 決定は次の順序で評価し、最初に成立した規則で確定する。順序は仕様であり、実装が別順序で評価してはならない。規則4に条件が無いため、上記入力の全組合せに対して帰結が一意に定まる。
  1. `full` の明示オプトインがある → 全セグメントへ `strict`（2体）
  2. 変更差分を機械的に解決できない、解決した差分にコア対象が成立する、または当該ゲートが過去のラウンドで `strict` へ確定済みである → `strict`（2体）とし、以後のラウンドも `strict` を維持する
  3. `light` の明示オプトインがある → `light`（1体）
  4. 上記のいずれにも該当しない → `standard`（1体）
- 規則2は規則3に優先し、`light` の明示オプトインがあってもコア対象では `strict` となる。これは変更内容だけを入力とする安全側固定であり、この経路にも `risk` の値は入らない。
- エージェントは明示オプトインを自ら設定できない。適用中の `strict` を降格させる操作も人間の明示行為に限る。不変条件 I8 のうち autonomy の既定 `gated` と、その昇格を人間の明示行為に限る規則は変更しない。

#### R4: 破壊的変更の履行

- ゲート結果と Issue 状態のスキーマは `schema_version` を新版へ更新する。旧版で記録済みのゲート結果を無効化せず、旧版レコードの解釈規則を移行定義として明文化する。未定義動作と暗黙の読み替えを残さない。
- `AGENTS.md` の不変条件 I2・I8 の本文、レビュープロファイルの記述、および `docs/GLOSSARY.md` のゲートの定義を、本仕様の判定規則と一致する記述へ改める。`risk` の値を profile 決定の入力とする記述を残さない。

### 入力・出力

- 入力: 判定対象の成果物と差分、`SPEC.md` 由来の AC-ID 集合、レビュアが提出した verdict、R3 が列挙した4つの profile 決定入力。`risk` の値はいずれの出力にも影響しない。
- 出力: ゲートの最終判定（`approved` / `rejected` / `human_required`）、観点と severity を宣言した finding 群、適用された review profile。
- 完了条件: 上記の判定規則が実装・スキーマ・判定プロンプト・規範文書のすべてで一致し、`schema_version` の更新と移行定義を伴って配布されること。

### 受入条件（Acceptance Criteria）

判定は AC-1 から AC-4 までを finding 単位でこの順に、AC-5 から AC-7 までを attempt 単位でこの順に評価する。先に成立した AC が当該対象の帰結を確定させる。AC-8・AC-9 は profile 決定ごとに、AC-10・AC-11 は配布物ごとに評価し、finding 単位・attempt 単位の順序評価には含めない。

#### AC-1: 不正な verdict を受理しない

- Given: レビュアの verdict に、観点を宣言しない finding、または列挙外の観点値・昇格類型 id・severity 値を含む finding がある
- When: 記録側が verdict を受理しようとする
- Then: 当該 verdict を受理せず、最終判定を `human_required` とする。値を既定へ補完して受理することも、当該 finding だけを黙って捨てることもしない
- 検証方法見込み: `automated`

#### AC-2: 立証観点の AC-ID 未達は無条件に blocking とする

- Given: 観点が `conformance` の finding が、`SPEC.md` の AC-ID の未達または未証跡を指摘している
- When: severity を決定する
- Then: 昇格類型への該当と必須根拠の有無を問わず blocking とし、当該ゲートの最終判定は `rejected` になる
- 検証方法見込み: `automated`

#### AC-3: 反証観点は類型と必須根拠を満たす場合だけ blocking とする

- Given: 観点が `falsification` の finding が、閉じた列挙の昇格類型のいずれかに該当し、かつ当該類型の必須根拠を伴う
- When: severity を決定する
- Then: blocking とし、該当した昇格類型 id と必須根拠を gate-report へ記録する。第三者が gate-report だけを読んで同じ分類を再現できる
- 検証方法見込み: `automated`

#### AC-4: 昇格しない finding は warning 以下として保持しゲートを止めない

- Given: AC-1 から AC-3 までのいずれにも該当しない finding がある。反証観点で昇格類型に該当しない場合、昇格類型に該当するが必須根拠を欠く場合、立証観点だが AC-ID の未達・未証跡ではない場合を含む
- When: severity を決定する
- Then: warning または info として記録し、観点・origin・code・evidence 原文をそのまま保持する。当該 finding は最終判定を `rejected` にしない
- 検証方法見込み: `automated`

#### AC-5: 立証の fail と blocking は差し戻す

- Given: 立証観点が fail である、または blocking と分類された finding が1件以上ある
- When: 最終判定を導出する
- Then: `rejected` となる。反証観点に記録された内容は判定へ影響しない
- 検証方法見込み: `automated`

#### AC-6: 判定材料が揃わない attempt は人間判断へ倒す

- Given: AC-5 に該当せず、立証観点が pending である、反証探索記録が無いか不完全である、またはレビュアが判定不能を表明している
- When: 最終判定を導出する
- Then: `human_required` となる。反証探索記録の無い attempt が `approved` になることはない
- 検証方法見込み: `automated`

#### AC-7: 反証の反例が記録されていても承認できる

- Given: AC-5 と AC-6 のいずれにも該当しない。すなわち立証観点が pass、blocking と分類された finding が0件、反証探索記録が存在する
- When: 最終判定を導出する
- Then: warning と info の finding が何件記録されていても `approved` となる。反証観点で反例が記録されている事実だけを理由に承認を妨げない
- 検証方法見込み: `automated`

#### AC-8: 明示オプトインが無ければ risk によらず standard とする

- Given: `full` と `light` のいずれの明示オプトインも無く、変更差分を機械的に解決でき、その差分にコア対象が成立せず、過去のラウンドで `strict` へ確定していない Issue で、`risk` が `high`・`unclassified`・`normal` のいずれかである
- When: review profile を決定する
- Then: `risk` の値によらず `standard`（1体）となる。`risk` を入力とする strict 昇格経路が実装・設定・判定プロンプトのいずれにも存在しない
- 検証方法見込み: `automated`

#### AC-9: profile は閉じた入力と評価順序で一意に定まる

- Given: R3 が列挙した4つの profile 決定入力の任意の組合せ。`full` と `light` の明示オプトインが同時に成立する場合、`light` の明示オプトインとコア対象が同時に成立する場合、変更差分を解決できない場合、過去のラウンドで `strict` へ確定済みの場合、付与主体を人間と確認できない場合を含む
- When: review profile を決定する
- Then: R3 の評価順序どおり最初に成立した規則で確定し、同一入力に対して常に同じ profile となる。`full` の明示オプトインは他のすべてに優先し、コア対象・差分解決不能・過去ラウンドの `strict` 確定は `light` の明示オプトインに優先する。エージェントはこれらの入力を自ら設定できず、適用中の `strict` の降格も人間の明示行為に限られる
- 検証方法見込み: `automated`

#### AC-10: スキーマ更新と移行定義を伴う

- Given: ゲート結果と Issue 状態のスキーマを本仕様の判定規則へ変更する
- When: 変更後のスキーマと移行定義を確認する
- Then: `schema_version` が新版へ更新され、旧版で記録済みの承認が無効化されず、旧版レコードの解釈規則が移行定義として明文化されている
- 検証方法見込み: `automated`

#### AC-11: 規範文書が新しい判定規則と一致する

- Given: 本仕様を適用した後のリポジトリ
- When: `AGENTS.md` の不変条件 I2・I8 とレビュープロファイルの記述、および `docs/GLOSSARY.md` のゲートの定義を確認する
- Then: 反証が合否条件ではないこと、blocking が昇格類型と必須根拠に限定されること、`strict` の成立条件が R3 の評価順序と一致することが記述され、`risk` の値を profile 決定の入力とする記述が残っていない
- 検証方法見込み: `automated`

## 制約

- 反証の探索そのものを削らない。判定プロンプトから反例探索の指示を除去する変更、および目的・背景に挙げた3件の実欠陥が検出されなくなる変更は本仕様に反する。
- 不変条件 I1・I3・I5・I7 を変更しない。昇格しなかった finding を証跡から削除しない。
- 本変更は破壊的変更であり、ADR の作成、`AGENTS.md` の改定、`schema_version` の更新、移行定義を設計以降のセグメントで伴う。

## スコープ外

- 判定プロンプトの再現性（`prompt_digest` が実行時のゲート結果に依存する問題）。ISSUE-802 で扱う。
- ラウンド予算機構の撤去。ISSUE-786 で導入した機構は維持する。
- strict における独立2体の集約規則（論理和）そのものの変更、および2体の逐次起動。後者は ISSUE-784 で扱う。
- テスト実行時間の短縮。ISSUE-785 で扱う。
- レビュアの実行系アダプタの選択・権限・認証。
- 昇格類型の具体的な集合と、各類型の必須根拠の具体的な形式の確定。設計セグメントで確定する。
- 明示オプトイン（`full`・`light`）を人間が与える具体的な手段（ラベル名・設定キー）の確定。R3 の profile 決定規則自体は本仕様で確定済みであり、設計へ委譲しない。
