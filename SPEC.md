<!--
正本: AGENTS.md §4セグメント・4ゲート
このファイルは Issue 毎に複製して使う雛形である（セグメント: spec、成果物: SPEC.md、ゲート: spec-gate）。
-->

# SPEC: agent-skill-chain — lint vocab/references の src/ 対象拡大・CLIサブコマンド文脈判定の構造的抜け穴修正

- Issue: `ISSUE-187`
- 作成者: `claude`
- 対象ブランチ: `feature/187-lint-scope-expansion`

## 目的・背景

`lint vocab`（禁止語検査）・`lint references`（陳腐化しうる禁止参照＝節番号記号参照・ファイルパス＋行番号参照の検査）は、プロジェクトの語彙・参照規約を機械強制する要である。しかし両者の既定検査対象は `AGENTS.md`・`docs/GLOSSARY.md`・`.agent-skill-chain/{standards,templates,config,schemas,scripts,ci}` に限られ、TypeScript 実装本体（`src/`）が含まれていない。CI も引数なしで lint を呼ぶためこの状態のままである。実装本体が検査対象外では、実装コード自身が語彙・参照規約に反していても検知されず、規約の一貫性が担保されない。実測（本 Issue 起票前の統合ブランチ HEAD 時点）で `src/` には禁止語違反 49 件・禁止参照違反 29 件が実在した。

あわせて、識別子文脈判定（禁止語と字面が一致しても正当な技術的参照であれば違反としない仕組み）に構造的な検出漏れが存在する。CLI サブコマンド文脈判定は「禁止語の前後トークンが既知の CLI 動詞（create/start/review 等の日常英単語を含む）に一致すれば正当な識別子文脈とみなす」方式であり、ファイル種別を見ずに全ファイルへ一律適用されるため、散文（Markdown）中で CLI 動詞や YAML 風構文と偶然共起しただけの禁止語混入までも誤って除外し、検出漏れを起こす。例として「新しい issue create の手順を説明する。」という散文は、本来は散文中の禁止語混入として検出されるべきだが、`create` が CLI 動詞集合に含まれるため識別子文脈と誤判定され検出されない。この抜け穴は Issue #178 の独立検証で発見・記録された既知事項に由来する。

本 Issue は、(a) 既定検査対象へ `src/` を加えて実装本体も語彙・参照規約で検査されるようにし、(b) 識別子文脈判定をファイル種別で出し分ける方式へ改めて上記の検出漏れを塞ぎ、(c) 対象拡大で顕在化する `src/` の既存違反を是正する。

## 用語

- **識別子文脈**: コード識別子（変数・関数名）・YAML キー名・CLI サブコマンド名など、禁止語と字面が一致しても自然文の誤用ではなく構文上の正当な名前として語が出現する文脈。lint はこれを違反判定から除外する。
- **散文（Markdown）中の禁止語混入**: 識別子文脈ではなく、自然文の文章として禁止語（`docs/GLOSSARY.md` の禁止同義語列に列挙された語）が使われている状態。lint が検出すべき対象。
- **ファイル種別ディスパッチ**: 検査対象ファイルの拡張子に応じて、どの識別子文脈判定を適用するかを出し分けること。本 Issue の中心概念。
- **構造的抜け穴**: 動詞ホワイトリストや YAML 風構文への偶然共起により、散文を識別子文脈と誤判定して禁止語混入を検出漏れする、判定方式そのものに起因する欠陥。
- **禁止参照**: 節番号記号による参照・ファイルパス＋行番号参照など、章構成やファイル分割で陳腐化しうる参照。`lint references` が検出する。機械処理用エラー出力・テスト証跡での使用は許可される。

## 要求 → 要件 → 受入条件

### 要求

- 実装本体（`src/`）も `lint vocab`・`lint references` の検査対象に含め、実装コードが語彙・参照規約で機械検査される状態にしたい。対象拡大で顕在化する既存違反は是正するか、正当な識別子文脈であれば除外して、引数なし lint が正常終了する状態を保ちたい。
- 散文中で CLI サブコマンド名・YAML キー構文と偶然共起する禁止語混入が、識別子文脈と誤判定されず正しく違反として検出される状態にしたい。
- 上記変更後も、正当な識別子利用（実コード・実設定中の名前、およびバッククォートで囲まれた参照）は誤検出されないままであり、既存テストスイート全件が引き続き通る状態を維持したい。
- CI が対象拡大後の既定 lint（`src/` を含む）を実行し、ゲートとして継続的に効く状態にしたい。

### 要件

- **要件1（対象拡大）**: `lint vocab`・`lint references` の既定検査対象へ `src/`（TypeScript 実装本体）を追加する。`bin/`（ビルド生成物・`.gitignore` 対象・追跡対象外）は追加しない。実装本体の実体はすべて `src/` に存在し、`bin/` は `src/` の生成物であるため、`src/` の対象化で実装本体検査の意図は完全に満たされる。
- **要件2（既存違反の是正）**: `src/` に実在する禁止語違反・禁止参照違反を「実違反＝内容書き換え」「正当な識別子文脈＝除外」の二分類で対応し、恣意的な一括抑制は行わない。是正は各コメント・コードの契約要旨（意味）を変えない範囲で行う。
- **要件3（文脈判定のファイル種別ディスパッチ）**: 識別子文脈判定を拡張子で出し分ける。YAML キー・flow-sequence 構文の識別子文脈判定は真の YAML ファイル（`.yaml`/`.yml`）のみに、CLI サブコマンド bare 文脈判定は散文（`.md`）以外のファイル（コード・設定・スクリプト）のみに限定適用する。コード識別子文脈（複合識別子）判定・外部語彙の明示許可リスト・既存の上流除外（バッククォートスパン・プレースホルダ・スラッシュ入りパストークン）は全ファイル種別で共通のまま維持する。散文中で CLI/YAML 参照を正当に示す正規形はバッククォート囲みとする。
- **要件4（正当利用の非後退）**: 上記ディスパッチにより、実コード・実設定中の bare な識別子・CLI サブコマンド・YAML キー、およびバッククォート囲みの参照が誤検出されないことを維持する。既存の除外規則を後退させない。
- **要件5（CI での実効）**: CI ワークフローが引数なしで lint を呼ぶことにより、対象拡大後の既定対象（`src/` を含む）を無改修で継承して実行する。既存テストスイート全件が引き続き通る。

### 受入条件（Acceptance Criteria）

#### AC-1: 既定検査対象根に `src/` が含まれ `bin/` が含まれない

- Given: `src/` が存在するリポジトリルート
- When: 既定対象根を返す関数（`lint references` 用の `defaultLiveFileRoots`、および `lint vocab` 用にそれを継承する `defaultVocabFileRoots`）を呼び出す
- Then: 返り値に `<repoRoot>/src` が含まれ、`<repoRoot>/bin` は含まれない。`src/` 非存在の環境では存在チェックにより自然に除外され追加が無害化される
- 検証方法見込み: `automated`

#### AC-2: 是正後、引数なしの `lint vocab`・`lint references` が `src/` を含めて正常終了する

- Given: `src/` を既定対象に含め、`src/` の既存違反を是正・正当除外した状態
- When: `agent-skill-chain lint vocab`・`lint references`（引数なし＝既定対象、`src/` を走査）を実行する
- Then: いずれも終了コード0（違反なし）で完走する
- 検証方法見込み: `hybrid`（作業 worktree の実行時ルート解決の既知挙動により、実チェックアウト相当の環境で CLI を実行して実測確認する必要があるため。手順・実行者は `VALIDATION.md` で確定する）

#### AC-3: 散文（`.md`）中で CLI サブコマンド名・YAML キー構文と偶然共起する禁止語混入が違反として検出される

- Given: `.md` ファイルに、(a) YAML キー構文に見えるが真の YAML ではない散文行（例:「issue: これは会議の議題そのものを指す散文である。」）、(b) CLI 動詞と偶然共起する散文行（例:「新しい issue create の手順を説明する。」）を含める
- When: `agent-skill-chain lint vocab` をこのファイルに対して実行する
- Then: 修正後は両行とも識別子文脈と誤判定されず違反として検出され（終了コード1以上）、修正前は検出漏れ（終了コード0）だったことを実測で対比確認する
- 検証方法見込み: `automated`

#### AC-4: 正当な識別子利用は誤検出されない（過剰検出なし）

- Given: (a) `.yaml` 中の真の YAML キー・flow-sequence 要素、(b) `.sh` 等の非 `.md` 中の実 CLI サブコマンド、(c) 全ファイル種別共通の複合コード識別子（`issue_id`・`issueId`）・外部語彙許可リスト（`blank_issues_enabled`）・バッククォート囲み参照
- When: `agent-skill-chain lint vocab` をこれらに対して実行する
- Then: いずれも違反として検出されない（終了コード0）。同一テキストを `.md` 散文に置いた場合は（a）（b）が検出へ転じることをあわせて確認する
- 検証方法見込み: `automated`

#### AC-5: 既存テストスイートが全件通る（回帰なし）

- Given: 本 Issue の全変更（対象拡大・ファイル種別ディスパッチ・既存違反是正・テスト追加）を反映した状態
- When: リポジトリのテストスイート全体を実行する
- Then: 既存テストと新規追加テストがすべて通り、回帰が無いことを実測確認する
- 検証方法見込み: `automated`

#### AC-6: CI が `src/` を含めた lint を実行する

- Given: CI ワークフローが引数なしで lint ラッパー（`lint-vocab.sh`・`lint-references.sh`）を呼ぶ構成
- When: 本 Issue の変更後の既定対象で CI 相当の引数なし lint を実行する
- Then: CI ワークフロー・ラッパーを改修することなく新しい既定対象（`src/` を含む）が継承され、引数なし lint が終了コード0で完走することを確認する
- 検証方法見込み: `hybrid`（ワークフロー・ラッパー定義の内容検査と、CI 相当の引数なしローカル実行の両方で確認するため）

## スコープ外

- secret scan（認証情報混入検査）の導入・変更。別 Issue で対応済み。
- `bin/`（ビルド生成物）自体の lint 対象化。要件1のとおり生成物であり実体は `src/` に存在するため対象外とする。
- `.js` ファイルの lint 対象化。既存の対象拡張子集合が生成物として `.js` を含まないことは本 Issue で変更しない。
- ADR-0002（writer lease 方式）の finalize 手順の逸脱是正。別 Issue で対応。
- `docs/system-spec/`（システム仕様書）の実体構築。
- 本 Issue で導入する 3 種の識別子文脈（コード識別子・YAML キー・CLI サブコマンド）以外の未知の識別子文脈への対応。将来発見された場合は別 Issue で追加する。
- 日本語の部分文字列衝突（例:「ブロック」中に禁止語「ロック」が一致する等）の一般的な単語境界判定。本 Issue の 3 種の文脈判定では扱わず、必要性が再確認された時点で別 Issue とする。
