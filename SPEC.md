<!--
正本: AGENTS.md §4セグメント・4ゲート
このファイルは Issue 毎に複製して使う雛形である（セグメント: spec、成果物: SPEC.md、ゲート: spec-gate）。
-->

# SPEC: upgrade が配布元で廃止されたファイルを導入先から削除しない（過去の負債が残留する）

- Issue: `ISSUE-492`
- 作成者: `spec_worker`
- 対象ブランチ: `feature/492-upgrade-stale-file-ownership`

## 目的・背景

`agent-skill-chain upgrade` は、配布元パッケージの正本アセット（root直下の追跡対象ファイル、`.agent-skill-chain/` 配下の名前空間ディレクトリ群、`.claude/agents` 等の展開済みテンプレート）を導入先へミラーコピーするが、コピー先に存在し配布元には存在しないファイルを検知・削除する処理を持たない。配布元でファイルをリネーム・統合・廃止しても、過去にそのファイルを導入したリポジトリには古い内容がそのまま残り続ける。

残留した廃止ファイルは、`grep`・IDE補完・AI エージェントの参照先探索で古い内容がヒットし、どちらが正本か混乱を招く。廃止ファイルが実行可能スクリプトである場合は、古い挙動のスクリプトが呼び出され続けるリスクもある。

本 Issue のスコープは、配布元で廃止されたファイルを `upgrade` が安全に検知・削除できるようにすることである。「安全に」とは、(a) 過去に本パッケージ自身が書き込んだファイルであることが記録により確認でき、(b) 導入先でその内容が変更されていないことが確認できる場合に限り削除対象とし、それ以外は削除せず警告に留めることを意味する。単純な「ファイル名が既知パターンに一致する」だけでの削除は、過去に別 Issue で発生した「パッケージ所有と判定した名前を中身の由来を検証せず削除し、ユーザーが同名で配置していた無関係な自作ファイルを誤って消去した」事故と同種のリスクを再発させるため、本 Issue では採用しない。

## 要求 → 要件 → 受入条件

要求から要件、そして機械検証可能な受入条件（AC-ID）まで一意に追跡できる形で記述する。AC-ID は `AC-1` のように `^AC-[0-9]+$` の形式に従う。

### 要求

`upgrade` を実行した導入先リポジトリの正本アセットツリーが、配布元パッケージの現行状態と一致すること（配布元で廃止されたファイルが導入先に残留しないこと）を、人間の手作業による個別削除に頼らず実現したい。ただし、導入先の人間が意図的に変更した内容を `upgrade` が無断で消してしまう事故は避けたい。

### 要件

- 要件1: `upgrade`（または導入先の書き込みを担う仕組み）は、実行のたびに「自身が導入先へ書き込んだファイルの一覧」をバージョンごとに永続化する所有権記録を保持する。
- 要件2: `upgrade` は、所有権記録に基づき「(a) 過去に本パッケージが書き込み、(b) 現行配布元には存在せず、(c) 導入先で内容が変更されていない（内容が最後に本パッケージが書いた内容と一致する）」ファイルのみを削除候補として判定する。「内容が一致する」とはファイルのバイト内容の一致のみを指し、ファイル権限（実行ビット等）の差異は本 Issue のスコープでは「変更」とみなさない。
- 要件3: 削除要否判定の対象となるのは、(a) と (b) を満たすファイル（所有権記録があり、かつ現行配布元には存在しないファイル）に限る。このうち (c) を満たさない（導入先で内容が変更されている）ファイルは自動削除せず、「配布元で廃止されたが導入先で変更が検出されたため削除しなかった」ことが分かる理由を利用者へ提示する。(a) を満たさないファイル（所有権記録が無いファイル）は削除要否判定の対象外であり、通常ファイルと区別されず、削除も理由提示も一切発生しない。(b) を満たさないファイル（現行配布元に現存するファイル、すなわち現バージョンでも本パッケージが管理し続けるファイル）も削除要否判定の対象外であり、既存の通常の上書き更新処理に従うのみで、削除に関する理由提示は発生しない。
- 要件4: `.agent-skill-chain/project/` 配下は、所有権記録の対象・削除判定の対象のいずれにも含めない（consumer project 固有ポリシーの不可侵境界を維持する）。
- 要件5: `upgrade --dry-run` は、実ファイルを削除せず、(i) 削除候補として判定され削除される予定のファイルの一覧、および (ii) 削除候補判定ロジックの対象（(a)(b) を満たす）だが要件3が定める内容変更の検出により削除されない予定のファイルについての警告、の両方を、非 dry-run 実行時と同一の内容で他の更新内容と同様に標準出力へ提示する。
- 要件6: 削除実行時のエラー（権限不足・I/O エラー等）は個々のファイルの削除失敗として扱い、失敗したファイルを明示した上で `upgrade` 全体を異常終了させる。他ファイルの削除・更新結果を隠さない。この要件が対象とするのは削除操作自体のエラーであり、要件8が定める削除候補判定のための読み取りエラーは含まない。
- 要件7: 削除候補（(a)(b) を満たすファイル）が、導入先のファイルシステム上に物理的に既に存在しない場合（利用者による事前削除・移動等）は、削除操作を行わず、要件6のエラーにも該当させない。この場合は目的の状態（当該ファイルが導入先に存在しない）が既に達成されているとみなし、`upgrade` を異常終了させない。
- 要件8: 要件2の (c)（内容が一致するか）を判定するためのファイル読み取り（read/stat）自体が権限不足・I/O エラー等で失敗した場合、当該ファイルは (c) を満たすと確認できないため削除候補と判定せず、読み取りに失敗した旨が分かる警告を利用者へ提示する。この読み取り失敗は要件6が定める「削除実行時のエラー」とは区別し、`upgrade` 全体を異常終了させる理由にはしない（他ファイルの処理は継続する）。

### 受入条件（Acceptance Criteria）

各 AC には、散文形式の Given/When/Then による受け入れシナリオを添える。検証方法見込みは `automated`・`manual`・`hybrid` のいずれか1語で記す。

#### AC-1: 所有権記録が過去の書き込みファイル一覧を保持する

- Given: 導入先リポジトリで `init`（または `upgrade`）を実行し、正本アセットが書き込まれた状態である。
- When: 書き込み完了後の導入先を調べる。
- Then: 「そのバージョンで本パッケージが書き込んだファイルの一覧」が、導入先の永続化された記録から復元できる。
- 検証方法見込み: `automated`

#### AC-2: 配布元で廃止され、導入先で未改変のファイルは削除候補として提示・削除される

- Given: 導入先の所有権記録に含まれるファイルが、現行の配布元パッケージには存在しない。かつ、当該ファイルは導入先のファイルシステム上に物理的に存在し、その内容が所有権記録上の「最後に本パッケージが書き込んだ内容」と一致する（内容の変更が無い）。
- When: `upgrade`（非 dry-run）を実行する。
- Then: 当該ファイルは削除され、削除したことが更新結果の一覧に含まれる。
- 検証方法見込み: `automated`

#### AC-3: 導入先で内容が変更されたファイルは自動削除されない（dry-run の有無を問わず同一の警告を提示する）

- Given: 導入先の所有権記録に含まれるファイルが、現行の配布元パッケージには存在しない。かつ、当該ファイルは導入先のファイルシステム上に物理的に存在し、その内容が所有権記録上の「最後に本パッケージが書き込んだ内容」と一致しない（内容が変更済みである）。
- When: `upgrade`（非 dry-run）、および `upgrade --dry-run` の双方をそれぞれ実行する。
- Then: いずれの実行でも当該ファイルは削除されず、「配布元で廃止されたが導入先で変更が検出されたため削除しなかった」ことが分かる警告が更新結果に含まれる。当該警告の内容は非 dry-run 実行時と `--dry-run` 実行時とで同一である。
- 検証方法見込み: `automated`

#### AC-4: 所有権記録が無いファイルは削除候補にならない

- Given: 導入先に存在するファイルが、所有権記録（AC-1）に一切登場しない（記録導入前から存在した、または本パッケージ以外が作成した）。
- When: `upgrade`（非 dry-run）を実行する。
- Then: 当該ファイルは削除要否判定の対象外であり、削除も削除しない理由の提示も発生しない。
- 検証方法見込み: `automated`

#### AC-5: `.agent-skill-chain/project/` は削除判定の対象に含まれない

- Given: `.agent-skill-chain/project/` 配下にファイルが存在し、それが配布元の現行アセットには存在しない状態である（記録の有無を問わない）。
- When: `upgrade`（非 dry-run）を実行する。
- Then: `.agent-skill-chain/project/` 配下のいかなるファイルも削除候補として判定されず、削除されない。
- 検証方法見込み: `automated`

#### AC-6: `--dry-run` は削除候補を実削除せず一覧提示する

- Given: 導入先に AC-2 の条件（配布元で廃止・未改変）を満たすファイルが存在する。
- When: `upgrade --dry-run` を実行する。
- Then: 当該ファイルは実際には削除されず、削除予定である旨が標準出力の一覧に含まれる。
- 検証方法見込み: `automated`

#### AC-7: 削除に失敗した場合は異常終了し失敗ファイルを明示する

- Given: AC-2 の条件を満たす削除候補ファイルが存在し、そのファイルの削除操作がエラー（権限不足等）で失敗する状況である。
- When: `upgrade`（非 dry-run）を実行する。
- Then: `upgrade` は 0 以外の終了コードで終了し、削除に失敗したファイルのパスが分かるメッセージが出力される。
- 検証方法見込み: `automated`

#### AC-8: 削除候補ファイルが導入先に既に物理的に存在しない場合はエラーにも警告にもならない

- Given: 導入先の所有権記録に含まれるファイルが、現行の配布元パッケージには存在しない。かつ、当該ファイルが導入先のファイルシステム上に既に（利用者による事前削除・移動等の理由で）物理的に存在しない。
- When: `upgrade`（非 dry-run）を実行する。
- Then: 当該ファイルについて削除操作は行われず、削除失敗としても扱われない。`upgrade` はこの状態を理由に異常終了せず、他ファイルの削除・更新結果にも影響しない。
- 検証方法見込み: `automated`

#### AC-9: 配布元に依然として存在するファイルは削除候補にならず通常の上書き更新に従う

- Given: 導入先の所有権記録に含まれるファイルが、現行の配布元パッケージにも依然として存在する（現バージョンでも本パッケージが管理し続けるファイルである）。
- When: `upgrade`（非 dry-run）を実行する。
- Then: 当該ファイルは削除要否判定の対象外であり、削除も「削除しない理由」の提示も発生せず、既存の通常の上書き更新処理のみが適用される。
- 検証方法見込み: `automated`

#### AC-10: 削除候補判定のための読み取り自体が失敗した場合は安全側に倒れ、削除せず警告する

- Given: 削除候補判定の対象となりうるファイル（所有権記録に含まれ、現行の配布元パッケージには存在しないファイル）について、要件2の (c)（内容が一致するか）を判定するための読み取りが権限不足・I/O エラー等で失敗する。
- When: `upgrade` を実行する。
- Then: 当該ファイルは削除されず、読み取りに失敗した旨が分かる警告が提示される。`upgrade` 全体は異常終了せず、他ファイルの削除・更新処理は継続する。
- 検証方法見込み: `automated`

<!-- 本セクションは、今後 AC を追加する場合の一般的な記法ガイドである。追加時は、既存の最後の AC の番号の次から連番で付与する。このコメント自体は、現時点で追加の AC 番号が要求されている・未定義であることを意味しない。 -->

## 未決事項

- 所有権記録ファイル自体のフォーマット・保存場所の具体的な設計（データ構造・ファイル名・スキーマ定義）。設計セグメントで確定する。
- 削除候補への該当判定における「内容の一致」の具体的な比較方式（バイト比較・ハッシュ比較等）の選択。設計セグメントで確定する。
- 削除候補ファイルの種別が、所有権記録時点の通常ファイルからディレクトリ・シンボリックリンク（特に参照先が消失した broken symlink）等へ変化していた場合の扱い。本 SPEC では規定せず、設計セグメントで確定する。
- 所有権記録自体が改ざん・破損（不正な形式、部分的欠損等）していた場合の扱い。本 SPEC では規定せず、設計セグメントで安全側（削除候補と判定しない等）の具体策を確定する。

## スコープ外

- `uninstall` コマンド自体の削除ロジックの変更。本 Issue の所有権記録の仕組みを `uninstall` にも転用できるかの検討は対象外とする。
- `detectLegacyAssets` が検知する、所有権記録の仕組み導入以前（2026-07-22 以前の旧世代アセット）に導入されたファイルの一括削除。所有権記録が導入バージョン以降のファイルにしか適用されない場合、この移行期の既存旧アセットへの遡及適用は本 Issue のスコープ外とする。
- GitHub workflow（`.github/`）配下の同期・削除。`upgrade` は現行仕様どおり GitHub workflow を自動更新しない（`setup github` の明示実行に委ねる）ままとし、本 Issue はこの挙動を変更しない。
- ファイル権限（実行ビット等）のメタデータ差異の検知・警告。要件2が定めるとおり、本 Issue における「内容の一致」判定はバイト内容のみを対象とし、権限のみが変更されたファイルは「変更あり」とは扱わない。
