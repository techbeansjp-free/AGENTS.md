# SPEC: size:quick の Issue でゲートが構造上通過不能な問題の是正

- Issue: `ISSUE-733`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/733-quick-gate-conformance-unpassable`

## 目的・背景

agent-skill-chain の憲法は、`size:quick` を指定した Issue について `SPEC.md`・`DESIGN.md`・`PLAN.md`・`VALIDATION.md` の作成義務を免除する。一方でゲート実装は Issue の size を一切参照しないため、免除規定に正しく従って成果物を作らなかった Issue が、成果物の品質と無関係に構造上ゲートを通過できない。この矛盾は次の 2 経路として現れる。

**経路A（判定プロンプトが立証を封じる）**: ゲートレビュア判定プロンプト（`gate reviewer-prompt` の出力）は、`SPEC.md` から AC-ID を抽出できないとき「conformance は inconclusive とし human_required へ倒すこと」とレビュアへ明示指示する。conformance ルーブリック自体も「適用対象の全 AC-ID / 要件が証跡付きに充足されているか」を唯一の判定軸として書かれている。final は「conformance と falsification の両 pass かつ blocking finding 無し→approved／いずれか fail もしくは blocking→rejected／inconclusive→human_required」と機械的に導出されるため、conformance が pass にならない限り approved は導出されない。

**経路B（レビュアが起動できない）**: ゲートレビュー起動（`gate local-review` 系）は、対象セグメントの必須成果物を target SHA から読めないとき `target SHAの必須成果物を読めません` に相当する中断を起こす。この中断はレビュア起動前に起きるため、gate-report は conformance・falsification とも `pending`、final は `human_required`、blockers は空のまま確定する。implementation ゲートは成果物不在を許容するが、spec・design・validation の各ゲートは中断する。

観測事実（2026-08-17、profile: strict、独立レビュア 2 体）：ISSUE-680 の implementation ゲートは 13 ラウンド全てで `conformance: pending`（レビュア証跡に「AC-ID を検出できず」「conformance を立証できません」と明記）、ISSUE-692 は 9 ラウンドで同様。ISSUE-680 の validation ゲートは経路B により exit 2 でレビュア 0 体。対照として `SPEC.md` を持つ ISSUE-721 の implementation ゲートは 2 体とも conformance/falsification 双方 pass、blockers 0 件で 1 ラウンド通過した。すなわち欠陥は成果物側ではなく判定機構側にある。

害は 3 点。(1) quick の Issue が自走でマージへ到達せず、進行役が毎回判定を上書きする運用が必要になる。(2) 成果物作成の負担を減らすための quick が、結果としてゲートの手動通過を強いるため意図を失う。(3) conformance が確定しない分レビュアの重心が反証側へ移り、ラウンド数の膨張に寄与する。

## 用語

- **size シグナル**: Issue の作業規模指定。取得元は、GitHub モードが Issue の `size:` プレフィックスラベル、ローカルモードが `state.yaml` の `size` キーである。
- **risk シグナル**: Issue のリスク分類。取得元は、GitHub モードが Issue の `risk:` プレフィックスラベル、ローカルモードが `state.yaml` の `risk` キーである。
- **シグナルの解決**: 取得元から値を読み取り、定義された値域のいずれか 1 つへ写像する操作。取得元にシグナルが存在しない状態（ラベル未付与・キー不在）は「解決不能」ではなく、既定値へ決定的に解決する。size の既定値は `standard`、risk の既定値は `unclassified` である。
- **シグナルの解決不能**: 次のいずれかに該当し、値を一意に確定できない状態。(a) 取得元の読み取りに失敗した（API 障害・ファイル読み取り失敗等）。(b) 読み取れた値が定義された値域に含まれない。(c) 相反する複数の値が同時に与えられている（同一プレフィックスのラベルが 2 つ以上付与されている等）。シグナルが存在しない状態はここに含めない。
- **ガードレール対象パス**: `docs/adr/`・`.agent-skill-chain/config/segments.yaml`・`AGENTS.md`・`.agent-skill-chain/schemas/`。
- **ガードレール判定**: base SHA から target SHA までの変更差分を取得し、ガードレール対象パスを含むかを判定する操作。結果は「含まない」「含む」「判定不能」のいずれかである。base SHA を解決できない、または差分の取得に失敗した場合を「判定不能」とする。
- **quick 免除**: size シグナルが `quick` に解決され、かつ risk シグナルが `normal` に解決され、かつガードレール判定が「含まない」であるときに限り成立する、4 成果物の作成義務免除。
- **免除不成立**: 上記 3 条件のいずれかを満たさない状態のすべて。シグナルの解決不能とガードレール判定不能を含む。免除不成立は「判定不能」ではなく、standard と同一の従来経路へ決定的に解決される状態である。
- **判定プロンプト**: レビュアへ渡される read-only の判定指示文字列。レビュアにはツール呼び出しが一切許可されないため、判定に使える情報はこの文字列に展開済みの内容のみである。判定プロンプトは互いに重ならない複数の区間からなり、本仕様は次の 2 区間を区別する。
- **判定対象区間**: 判定プロンプトのうち、レビュアが読むべき成果物の内容、当該成果物が存在しない事由、および代替判定基準への適合を立証するための変更差分証跡を提示する区間。
- **変更差分証跡**: base SHA から target SHA までの変更差分。変更されたファイルの一覧と、各ファイルの内容差分から成り、変更されたファイルが 1 件も無い（空である）状態を含む。ワーカーが著述する判定対象そのものであるため、判定対象区間への展開に限って用い、代替判定基準の実体としては用いない。
- **判定軸区間**: 判定プロンプトのうち、conformance を何によって判定するかをレビュアへ指示する区間。取りうる値は「AC-ID 全件網羅」「代替判定基準」「inconclusive 指示」の 3 つである。
- **必須成果物の読み取り**: target SHA 時点の対象ゲートの必須成果物について、存在の有無を確定し、存在する場合は内容を取得する操作。結果は成果物ごとに「存在し内容を取得できた」「存在しないことを確認できた」「読み取り不能」のいずれかである。**読み取り不能**とは、存在の有無自体を確定できない状態（取得元の障害等）、または存在するが内容の取得・復号・解析に失敗して内容を確定できない状態をいう。存在しないことを確認できた状態は読み取り不能に含めない。
- **AC-ID 抽出**: target SHA 時点の `SPEC.md` から受入条件の識別子を取り出す操作。結果は「1 件以上抽出できた」「抽出に成功し 0 件であった」「抽出不能」のいずれかである。`SPEC.md` が存在しないことを確認できた場合は「抽出に成功し 0 件であった」に含める。**抽出不能**とは、`SPEC.md` の読み取りが読み取り不能であるために AC-ID の件数を確定できない状態をいい、0 件であることを確認できた状態と区別する。
- **代替判定基準**: quick 免除下で AC-ID 全件網羅に代わって用いる conformance の判定軸。実体は当該 Issue 本文に記載された要求・期待する挙動・受入基準であり、対象ゲート（spec・design・implementation・validation）の別を問わず同一の実体を用いる。ゲートごとに異なる情報源へ切り替えることはしない。
- **代替判定基準の展開可否**: 判定プロンプト生成時点で Issue 本文を取得でき、かつ本文から要求・期待する挙動・受入基準に相当する記述を 1 件以上取り出して判定軸区間へ文字列として書き出せる状態を「展開できる」とする。Issue 本文を取得できない場合、または取り出せる記述が 0 件の場合を「展開できない」とする。
- **trusted base 側の起動経路**: リポジトリの default branch を保護された基点として実行される、ゲートレビュー起動・判定プロンプト生成の実行経路。Coordination Backend に由来する入力（size シグナル・risk シグナル・ガードレール判定・Issue 本文）の取得元を Coordination Backend の一次情報（GitHub モードは Issue 本文・ラベルの取得 API、ローカルモードは Issue 状態ファイル）に限り、target SHA 時点の成果物に由来する入力（必須成果物の読み取り結果・AC-ID 抽出結果）の取得元を target SHA に限る。
- **ワーカー供給経路**: セグメント作業ワーカーまたはゲートレビュアが内容を書き込める経路。branch 上の成果物ファイル、PR 本文、Issue コメント、ワーカーが作成した Issue 本文の写しを含む。trusted base 側の起動経路と異なり、判定を受ける側が内容を制御できる点で区別する。区別の基準は書き込み主体であって格納媒体ではない。
- **inconclusive 指示**: 「conformance は inconclusive とし human_required へ倒すこと」に相当する、レビュアへの明示指示文字列。
- **attempt**: 1 回のゲートレビュー起動単位。同一の attempt ID・target SHA・base SHA・プロファイルのもとで起動されたレビュア群と、その証跡の集合を指す。
- **conformance / falsification / final**: 立証観点・反証観点の判定値（`pass|fail|pending`）と、そこから機械導出される最終判定（`approved|rejected|human_required`）。

## 前提・制約

- 本 Issue は quick 免除の範囲そのものを変更しない。憲法が定める免除規定を所与とし、判定機構側を適合させる。
- 不変条件 I8（安全側ラチェット）を緩めない。判定不能な状態から `approved` を導出してはならず、silent pass を作ってはならない。`risk != normal`（`unclassified` 含む）は非 normal 側として扱う。
- レビュアはツールを持たないため、conformance の判定軸は判定プロンプト内に実体として展開されていなければならない。展開できない場合は判定不能として扱う。
- レビュアは判定プロンプトの明示指示に従う。指示に反する verdict（inconclusive 指示を受けながら conformance を pass として返す等）の検知・強制は本仕様の対象外であり、既存どおり人間の確認に委ねる。
- size シグナル・risk シグナルの書き込み主体は憲法（`AGENTS.md`）が定める役割・権限に従い、セグメント作業ワーカーの権限には含まれないため、本仕様は当該シグナルの取得元をワーカー供給経路として扱わず、その書き込み主体を本仕様で再定義しない。
- **判定軸そのものの改変可能性**: quick 免除が成立し AC-ID を抽出できない場合、conformance の判定軸は Issue 本文だけになる。Issue 本文は人間・進行役・セグメント作業ワーカーが編集できるため、判定軸を緩める方向の編集によって `approved` へ到達しうる。本仕様はこれに対し 2 つの制約を確定する。(1) Issue 本文の取得は trusted base 側の起動経路のみが行い、レビュア・セグメント作業ワーカーが供給した本文を判定軸の入力にしない（要件9 として要件化し、AC-17 が検証する）。(2) 代替判定基準の一次情報そのものが判定を受ける側によって変更された場合に、防止・検知・無効化のうち少なくとも 1 つの手段が働き、変更後の基準による `approved` へ到達しない（要件13 として要件化し、AC-26・AC-27 が検証する）。設計セグメントの裁量は (2) のどの手段を採るかに限られ、いずれの手段も採らないという選択肢は無い。
- 反証（falsification）側のルーブリックは Issue #729 が扱う。両者は同一の判定プロンプト生成箇所を変更するため、着手順序を調整し、先にマージした側の変更を後続が作り直さないこと。

## 入力・出力

- 入力: size シグナル、risk シグナル、base SHA から target SHA までの変更差分、target SHA 時点の成果物、Issue 本文（要求・期待する挙動・受入基準の記述）、対象ゲート ID。
- 入力の取得元の信頼境界: Coordination Backend に由来する入力（size シグナル・risk シグナル・ガードレール判定・Issue 本文）は trusted base 側の起動経路のみが一次情報から取得し、ワーカー供給経路の値を**代替判定基準**の入力にしない。target SHA 時点の成果物はワーカーが著述する判定対象そのものであるため、判定対象区間への展開と AC-ID 抽出に限って用い、**代替判定基準**の実体としては用いない。base SHA から target SHA までの変更差分も同じくワーカーが著述する判定対象であるため、判定対象区間への変更差分証跡の展開に限って用い、**代替判定基準**の実体としては用いない。
- 出力: quick 免除の成否、判定プロンプト文字列（判定対象区間・判定軸区間を含む）、ゲートレビュー起動の成否、verdict と gate-report。

## 状態空間の定義

判定に影響する入力を独立した次元へ分解し、状態空間をその直積として定義する。受入条件は状態表の行に対して割り当てる。証跡照合機構を対象外（後掲「スコープ外」）へ移したことに伴い、入力次元・観測対象・状態表・要件・仕様判断の識別子には欠番が生じるが、残る要素の識別子は安定に保ち振り直さない。受入条件（AC-ID）のみ連続番号へ振り直す。

### 入力次元

| 記号 | 次元 | 取りうる値（互いに排他かつ網羅） |
|---|---|---|
| D1 | size シグナルの解決結果 | `quick` / `standard` / 解決不能 |
| D2 | risk シグナルの解決結果 | `normal` / `normal` 以外 / 解決不能 |
| D3 | ガードレール判定の結果 | 含まない / 含む / 判定不能 |
| D4 | 対象ゲートの必須成果物の読み取り結果 | すべて存在し内容を取得できた / 読み取り不能なし・1 件以上が不在（読み取り不能な成果物が無く、1 件以上について存在しないことを確認できた） / 読み取り不能あり・不在なし（1 件以上が読み取り不能で、不在を確認できた成果物が無い） / 混在（1 件以上が読み取り不能で、かつ別の 1 件以上について不在を確認できた） |
| D5 | AC-ID 抽出結果 | 1 件以上 / 0 件（`SPEC.md` が存在しないことを確認できた場合を含む） / 抽出不能 |
| D6 | 代替判定基準の展開可否 | 展開できる / 展開できない |
| D10 | レビュアが返した verdict | 両観点とも `pass` かつ blocking finding 無し / いずれかの観点が `fail` であるか blocking finding がある（他方が `pending` であるか否かを問わない） / いずれの観点も `fail` ではなく blocking finding も無く、かつ いずれかの観点が `pending` / レビュアが起動されず verdict が存在しない |
| D11 | ワーカー供給経路に置かれた「要求・期待する挙動・受入基準」相当の記述 | 存在する / 存在しない |
| D13 | base SHA から target SHA までの変更差分の内容 | 変更されたファイルが 1 件以上ある / 変更されたファイルが 1 件も無い |
| D14 | 代替判定基準の一次情報に対する、判定を受ける側（セグメント作業ワーカーの実行主体）による変更 | 無い / 有る（当該ゲートの `approved` 導出より前に行われた場合と後に行われた場合の双方を含む） |

対象ゲート ID は次元に含めない。ゲート ID は D4 の判定に用いる必須成果物集合を決めるパラメータにすぎず（spec は `SPEC.md`、design は `DESIGN.md` と `PLAN.md`、implementation は必須成果物なし、validation は `VALIDATION.md`）、期待結果はゲート ID ではなく D4 の値だけで決まるためである。各受入条件の When で対象ゲートの範囲を明示する。

### 派生値

いずれも状態表の次元だけから一意に決まる全域関数であり、受入条件が期待結果を委譲する場合の委譲条件はこれらの値のみで表現する。

- **E（quick 免除の成否）**: D1・D2・D3 の関数。表O1 が 27 通りすべてに対する値を定める。
- **S（判定軸区間の値）**: E・D4・D5・D6 の関数。表O2 が 48 通りすべてに対する値を定める。

### 観測対象

同一入力に対して 2 つの期待結果が定まる状態（＝実装不能）が生じるのは、2 つの受入条件が同一の観測対象を、同一の入力について争う場合に限る。観測対象が異なれば、同一入力に対する 2 つの期待結果は同時に成立でき、対応する自動テストも同時に成功する。入力集合が互いに素であれば、値域を共有する 2 つの観測対象も同一入力を争わない。したがって排他性は観測対象ごとの状態表の内部でのみ要求する。観測対象は次の 9 個である。O1〜O11 は互いに素であり、O12 は O8 と値域（承認の帰結）を共有するが、両者の入力集合は D14 の値により互いに素である（後掲の分離の根拠を参照）。

| 記号 | 観測対象 | 状態表 |
|---|---|---|
| O1 | quick 免除の成否という判定結果そのもの | 表O1 |
| O2 | 判定プロンプトの判定軸区間が指示する判定軸の種別（S の値）と、その種別に応じた内容が書き出されているか否か | 表O2 |
| O3 | 判定プロンプトの判定対象区間のうち、対象ゲートの必須成果物に関する提示内容 | 表O3 |
| O4 | ゲートレビュー起動経路が中断するか、レビュアが起動されるか | 表O4 |
| O8 | 1 回の attempt における final の導出値 | 表O8 |
| O9 | 判定軸区間へ書き出された代替判定基準の実体の取得経路 | 表O9 |
| O10 | 判定対象区間と判定軸区間の境界と、両区間が占める位置の非重複 | 表O10 |
| O11 | 判定対象区間のうち、代替判定基準への適合を立証するために提示される変更差分証跡の内容 | 表O11 |
| O12 | 判定を受ける側による代替判定基準の一次情報の変更に対する、当該ゲートの承認の帰結（変更後の基準による `approved` への到達可否と、導出済み承認の有効性） | 表O12 |

O2 と O3 が互いに素であることは、判定プロンプトが両区間を互いに重ならない別々の区間として含むこと（要件3）によって保証する。O2 の受入条件は判定軸区間のみを、O3 の受入条件は判定対象区間のうち必須成果物に関する提示内容のみを観測する。この非重複性そのものは O10 の受入条件が直接観測するため、O2 と O3 の分離は未検証の前提に依存しない。S が D4 にも依存すること（判断11）は、この分離を損なわない。O2 と O3 は同一の入力次元 D4 を共有するが、期待結果を定める観測位置（判定軸区間と判定対象区間）が互いに素であるため、同一入力に対して両表の行が同時に成立しても相反しない（判断5）。表O3 と表O4 が E・D4 を共有する場合と同じ構造である。

O2 と O9 は互いに素である。O2 は判定軸区間が指示する判定軸の種別と、その種別に応じた内容が書き出されているか否かのみを観測し、書き出された実体がどの取得経路の値であるかは観測しない。O9 は S が「代替判定基準」である場合に限り、判定軸区間へ書き出された実体の取得経路のみを観測し、S の値そのものを期待結果としない。O10 は両区間の境界と位置の重なりのみを観測し、各区間の内容が何であるか（O2・O3・O11）は観測しない。

O8 は 1 回の attempt における final の導出値を観測する。レビュアが起動されない入力では verdict が存在しないため、D10 は第 4 の値「レビュアが起動されず verdict が存在しない」を採り、表O8 の行9 が当該入力の final を `human_required` と定める。すなわち、起動経路が中断する入力でも final は導出され、判定の欠落は無言のまま放置されない。O4 はレビュアが起動されるか否か（起動経路の中断有無）のみを観測し、final の値は観測しない。したがって「中断してレビュアを起動しない」（O4）と「final が `human_required` として導出される」（O8）は互いに素な観測対象であり、同一入力に対して同時に成立する。

O3 と O11 は互いに素である。いずれも判定対象区間を観測位置とするが、O3 は当該区間のうち対象ゲートの必須成果物に関する提示内容のみを、O11 は当該区間のうち変更差分証跡の提示内容のみを観測する。両者は同一区間内の別々の提示要素であり、同一入力に対して両表の行が同時に成立しても相反しない。

O8 と O12 は値域（承認の帰結）を共有するが、入力集合が互いに素である。O8 は D14 を観測できない入力（S が「代替判定基準」でない場合）または D14 が「無い」入力における attempt 内の final 導出を定め、O12 は S が「代替判定基準」であり D14 が「有る」入力における承認の帰結を定める。D14 は 2 値であり両者は同時に成立しないため、同一入力に対して 2 つの期待結果は定まらない。

### 次元間の依存と、発生しない組み合わせ

- spec ゲートに限り、D4 が「読み取り不能なし・1 件以上が不在」であることは `SPEC.md` の不在を意味するため D5 は必ず「0 件」になり、D4 が「読み取り不能あり・不在なし」であることは D5 が必ず「抽出不能」になることを意味する。表O2 は D4 と D5 の双方を用いるため、spec ゲートではこの依存により到達しない行が生じる（D4 が「読み取り不能なし・1 件以上が不在」かつ D5 が「1 件以上」である行）。到達しない行があること自体は表の網羅・排他を損なわない。表は 4 ゲートに共通の状態空間を定義しており、当該行は design・validation ゲート（`DESIGN.md`・`PLAN.md`・`VALIDATION.md` が不在でも `SPEC.md` から AC-ID を抽出できる）で到達するためである。spec 以外のゲートでは `SPEC.md` が必須成果物ではないため、D4 が「すべて存在し内容を取得できた」であっても D5 が「抽出不能」でありうる。
- 必須成果物を定義しないゲート（implementation）では、D4 は常に「すべて存在し内容を取得できた」となる（空集合のすべての要素について内容を取得できたと解する）。したがって当該ゲートでは D4 が「読み取り不能なし・1 件以上が不在」「読み取り不能あり・不在なし」「混在」である行に到達しない。
- D4 の「混在」は、必須成果物を 2 件以上持つゲートでのみ到達する。同一の成果物が同時に「不在を確認できた」と「読み取り不能」の双方になることはないためである。4 ゲートのうち必須成果物を 2 件以上持つのは design ゲート（`DESIGN.md` と `PLAN.md`）だけであり、spec ゲート（`SPEC.md`）・validation ゲート（`VALIDATION.md`）・implementation ゲート（必須成果物なし）では「混在」に到達しない。到達しない行があること自体は表の網羅・排他を損なわない。
- D10 の第 4 の値「レビュアが起動されず verdict が存在しない」は、表O4 が中断を定める入力でのみ観測される。判定プロンプトが生成されレビュアが起動された入力では、D10 は他の 3 値のいずれかを採る。
- D11 は、S が「代替判定基準」であり判定軸区間へ実体が書き出される場合にのみ、その取得経路として観測できる。表O9 で除外行とその理由を明示する。D11 は判定プロンプトの入力ではないため（要件9 により判定軸の入力から除かれる）、他の表の期待結果を変えない。
- D13 は、S が「代替判定基準」である場合にのみ、判定対象区間への展開対象として観測できる。表O11 で除外行とその理由を明示する。D13 に「取得不能」という値は生じない——S が「代替判定基準」となるには quick 免除の成立（E が成立）が必要であり、その成立にはガードレール判定が「含まない」へ解決されていること、すなわち base SHA から target SHA までの差分を取得できていることを要するためである。
- D14 は、S が「代替判定基準」である場合にのみ観測できる。表O12 で除外行とその理由を明示する。D14 が「有る」入力では表O12 が承認の帰結を定め、表O8 の行1・行2 による `approved` の導出は行われない。

## 仕様判断とその根拠

**判断1: シグナルが存在しない状態は既定値へ決定的に解決し、「解決不能」は取得の失敗・値域外・相反する複数値に限る。**

未付与を「解決不能」に含めると、size ラベルを運用していない既存の全 Issue が解決不能となり、適正な `SPEC.md` を持つ通常の Issue まで `approved` へ到達できなくなる（最大の回帰）。逆に取得失敗・値域外まで既定値へ落とすと、外形的事由で quick 免除が誤成立しうる。両者を分離し、未付与は決定的な既定値、取得の失敗は安全側とする。これにより「size シグナルの定義」と「解決不能の定義」が同一入力を二重に定義する状態を解消する。

**判断2: risk シグナルの未付与は `unclassified` として `normal` 以外に解決する。**

不変条件 I8 が `risk != normal`（`unclassified` を含む）を非 normal 側として扱うと定めているため。帰結として risk ラベルの無い Issue では quick 免除が成立しない。quick 免除は進行役の明示的オプトインでのみ成立し自動昇格しないという憲法の規定と整合する。

**判断3: ガードレール差分を取得できない場合は第三の値「判定不能」とし、免除不成立側へ写像する。**

差分を取得できない状態を「含まない」とみなすと、ガードレール対象パスを変更した Issue が免除を得る経路が生じる。「含む」とみなすのは安全側だが、事由の異なる状態を同一視して原因追跡ができなくなる。第三の値として明示したうえで、免除の成否としては不成立側へ倒す。

**判断5: 状態空間を「観測対象 × 入力次元」の直積として定義し、排他性は観測対象ごとに要求する。**

受入条件どうしの相反（＝実装不能）は、同一入力に対して同一の観測対象へ 2 つの異なる期待結果が定まる場合にのみ生じる。観測対象が異なる 2 つの受入条件は、同一入力で同時に成立してよい。入力条件だけで排他性を確保しようとすると、観測対象の異なる受入条件を単一の表へ混在させることになり、実際には矛盾していない対を矛盾として扱うか、逆に真の矛盾を見落とす。観測対象を明示的な次元として立てることで、網羅と排他を機械的に確認できる形にする。

**判断6: 判定プロンプトを判定対象区間と判定軸区間の 2 つの独立な区間へ分解する。**

「何を読んで判定するか」（判定対象区間）は必須成果物の読み取り結果で決まり、「何をもって conformance とするか」（判定軸区間）は主として AC-ID 抽出結果と代替判定基準の展開可否で決まる。両者は別々の観測対象である。単一の観測対象として扱うと、たとえば「quick 免除成立・`DESIGN.md` 欠落・`SPEC.md` から AC-ID 抽出可」という入力に対して、成果物不在を扱う受入条件と AC-ID 網羅を扱う受入条件が相反する。区間を分離し、各受入条件がどちらか一方の区間のみを観測することで、この相反を構造的に排除する。分離が成り立つこと自体は前提として置かず、独立した観測対象（O10）として受入条件で検証する。前提のままにすると、他の表の排他性の根拠が未検証の前提に依存する。

ただし区間の分離が排除するのは受入条件どうしの相反（実装不能）だけであり、上記の入力で「網羅を立証すべき成果物が判定対象区間に存在しないまま AC-ID 全件網羅が判定軸として指示される」という判定不能そのものは解消しない。この残る判定不能は判断11 が扱い、判定軸区間の値 S を必須成果物の読み取り結果にも依存させることで解決する。したがって S は判定対象区間の**内容**には依存せず、判定対象区間の内容を決める入力次元（D4）にのみ依存する。区間の内容どうしが相互に依存しないという本判断の分離は保たれる。

**判断7: 代替判定基準の取得経路を trusted base 側へ限定することを、前提の記述ではなく要件・受入条件として確定する。**

quick 免除が成立し AC-ID を抽出できない場合、conformance の判定軸は代替判定基準だけになる。この実体の取得経路を制約しないと、判定を受ける側であるセグメント作業ワーカーが自ら判定軸を供給でき、緩い受入基準相当の記述を置くだけで `approved` へ到達する自己承認経路が成立する。これは quick 経路に限って I8（安全側ラチェット・silent pass 禁止）を実質的に無効化する。取得経路の限定を前提・制約の記述にとどめると実装・テストが観測しないため、要件として明記し、実体の取得経路を観測する受入条件で検証する。判定対象区間へ展開される成果物、および `SPEC.md` から抽出した AC-ID 集合はワーカーが著述するものであり、これは判定対象そのものであるため本限定の対象外とする（限定するのは**代替判定基準**の入力のみである）。

**判断9: 必須成果物の読み取り不能と AC-ID 抽出不能を、不在・0 件と同一視せず第三の値として扱う。**

判断1 が「取得の失敗を既定値と同一視しない」、判断3 が「差分の取得失敗を第三の値として安全側へ倒す」と定めた原則を、必須成果物の読み取りと AC-ID 抽出にも同じ形で適用する。読み取り失敗を不在へ吸収すると、quick 免除下では実在する成果物が「免除により正当に不在」として提示され、レビュアは成果物を読まないまま判定する。同時に AC-ID 抽出結果が「0 件」へ写像されるため、conformance の判定軸が AC-ID 全件網羅から Issue 本文由来の代替判定基準へ無言で降格したまま `approved` に到達しうる。これは判定不能な状態から `approved` を導出しないという I8 の要求を quick 経路でのみ破る。倒す先は 2 つに分ける。必須成果物の読み取り不能は起動を中断する側へ倒す——判定対象そのものを読めない状態ではレビューが成立せず、免除の成否によらず継続する理由が無いためである。AC-ID 抽出不能は中断させず inconclusive 指示側へ倒す——spec 以外のゲートでは判定対象区間の内容を提示できるため、レビュアへ判定不能である旨を明示したうえで人間判断へ渡す方が、無言の中断より原因追跡が容易だからである。いずれの場合も代替判定基準へは切り替えない。

**判断10: verdict の値を排他にするにあたり、`fail`・blocking finding を `pending` より優先する。**

D10 の値が互いに排他でなければ、同一の verdict に対して表O8 が 2 つの期待結果を定め、実装が一意に決まらない（判断5 が排除の対象とした実装不能そのものである）。したがって `fail`・blocking finding と `pending` が同時に成立する verdict の扱いを一意に確定する必要がある。fail・blocking 側を優先する理由は 3 点である。(1) `rejected` と `human_required` はいずれも `approved` ではないため、どちらを優先しても「判定不能な状態から `approved` を導出しない」という I8 の要求は等しく満たされ、安全性に差が生じない。(2) 既存の final 導出は fail・blocking を優先して `rejected` を返しており、pending 優先へ改めると本 Issue の目的（quick 経路でゲートが構造上通過できない欠陥の是正）と無関係な既存挙動の変更を持ち込み、免除不成立の主経路に回帰を生む。(3) blocking finding という決定的な否定判定が既に得られている状態を `human_required` へ倒すと、差し戻し先が確定しているにもかかわらず人間の判断を要求することになり、是正が遅れる。

**判断11: quick 免除により対象ゲートの必須成果物が正当に不在である場合は、AC-ID を抽出できても判定軸を AC-ID 全件網羅から代替判定基準へ切り替える。**

判定軸区間の値 S を E・D5・D6 だけで決めると、quick 免除が成立し `SPEC.md` から AC-ID を抽出できる Issue において、対象ゲートの必須成果物が免除により不在である入力（design ゲートで `DESIGN.md`・`PLAN.md` が不在である場合、validation ゲートで `VALIDATION.md` が不在である場合）で、判定軸が「AC-ID 全件網羅」のまま網羅を立証すべき成果物が判定対象区間に存在しない。conformance は成果物の品質と無関係に構造上 fail となり final は `rejected` へ落ちるため、本仕様の要求（quick 免除に正しく従った Issue が進行役の上書き無しにゲートを通過できること）が達成されない。この入力は状態空間に明示的に存在するため、放置は要求の未達を意味する。

採りうる案は 3 つある。(a) 網羅の対象 AC-ID を、不在の成果物に依存しないものへ限定する。(b) 免除下では網羅要求を緩め、不在の成果物に係る欠落を不充足として数えないよう判定軸区間で指示する。(c) 判定軸を代替判定基準へ切り替える。**本仕様は (c) を採る。** 理由は 4 点である。(1) (a) は、どの AC-ID の立証がどの成果物に依存するかを判定機構が機械的に決められず、限定規則そのものが判定不能になる。(b) は判定軸区間へ「欠落を不充足として数えない」という緩和指示を新設することになり、S の値域を増やしたうえで、レビュアが緩和の範囲をどう解釈するかに結果が依存する。(c) は quick 免除下で AC-ID を抽出できない場合のためにすでに定義済みの機構（代替判定基準・S の 3 値・AC-5・AC-6）をそのまま再利用でき、判定軸区間に新たな値も緩和指示も導入しない。(2) 切替の発火条件を「E が成立し、かつ必須成果物が不在であることを**確認できた**」に限り、読み取り不能（D4 が「1 件以上が読み取り不能」）では切り替えない。判断9 が禁じた「取得の失敗による判定軸の無言の降格」は起こさない。免除不成立の場合も切り替えないため、既存の主経路に回帰を生まない。(3) E の成立には size が `quick`・risk が `normal`・ガードレール対象パス非該当のすべてが解決済みであることを要し、これらのシグナルの書き込み主体は憲法上セグメント作業ワーカーの権限に含まれないため（前提・制約に記載）、判定を受ける側が切替を発火させられない。(4) 切替先の代替判定基準を展開できない場合は inconclusive 指示へ倒れるため、判定軸が実体化しない状態から `approved` は導出されない。

帰結として S は E・D4・D5・D6 の関数となる。必須成果物を定義しない implementation ゲートでは D4 が「1 件以上が不在」になりえないため切替は起きず、当該ゲートの既存挙動は変わらない。

**判断12: 必須成果物の不在と読み取り不能が混在する状態を、D4 の第 4 の値として独立に扱う。**

不在（存在しないことを確認できた）と読み取り不能（存在の有無または内容を確定できない）は、判断9 のとおり別の値として扱う。必須成果物を 2 件以上持つゲート（design）では両者が同一ゲートで同時に成立しうる。この混在を「1 件以上が読み取り不能」へ吸収すると、同時に不在である成果物を判定対象区間へどう提示するかがどの受入条件にも定まらず、要件6 が禁じる無言の空欄を生む。逆に「1 件以上が不在」へ吸収すると、読み取り不能な成果物が免除による正当な不在として提示され、判断9 が禁じた取得失敗の吸収そのものになる。したがって混在を第 4 の値として立て、判定対象区間では成果物ごとに状態を区別して提示する（表O3 の行7・行8、AC-22・AC-23）。判定軸（S）については混在を「読み取り不能あり」と同じ扱いとし、代替判定基準へ切り替えない——読み取り不能な成果物を含む以上、切替は判断9 が禁じた判定軸の無言の降格になるためである（表O2 の行1）。起動は読み取り不能を含むことを理由に中断する（表O4 の行7・行8、AC-21）。

**判断13: 代替判定基準への適合を立証する判定対象側の証跡を、base SHA から target SHA までの変更差分とする。**

判定軸だけを判定プロンプトへ展開し判定対象を展開しない場合、レビュアは何に対する適合を判定するのかを確定できず、返した pass を判定プロンプト内の証跡から正当に立証できない。これは「quick 免除に従った Issue が構造上通過できる」という要求を、根拠の無い pass によって見かけ上満たすことになり、I8（silent pass 禁止）に反する。quick 免除が成立する入力では、免除された成果物が判定対象区間に存在しないため、免除の対象外である変更差分が判定対象として残る唯一の実体である。証跡を Issue 本文側から採らないのは、それでは判定軸と判定対象が同一の情報源になり適合が自明に成立するためであり、要件9 が定める信頼境界（代替判定基準の実体は trusted base 側が取得した Issue 本文のみ、判定対象はワーカーが著述したもの）とも一致する。差分が空である場合はその旨を明示する——空であることを示さずに判定対象区間を空欄にすると、レビュアは証跡の不在を認識できないまま判定することになる。差分が空であることから `approved` を強制せず、判定はレビュアに委ねる。

**判断14: 代替判定基準の改変対策を採ること自体を本仕様の受入条件として確定し、採否を設計セグメントへ委ねない。**

quick 免除が成立し AC-ID を抽出できない場合、conformance の判定軸は代替判定基準だけになる。判定を受ける側であるセグメント作業ワーカーがこの一次情報を緩める方向へ変更できると、ワーカーが自ら判定軸を書き換えて `approved` に到達する自己承認経路が成立し、quick 経路に限って I8（安全側ラチェット・silent pass 禁止）が無効化される。本 Issue の要求は「quick 免除に正しく従った Issue がゲートを通過できること」であり、その通過経路が自己承認に開かれているならば要求を達成したことにならない。対策の採否を未決事項として設計セグメントへ委ねると、対策を 1 つも採らない設計であっても本仕様の完了条件を満たしうる。したがって「防止・検知・無効化のうち少なくとも 1 つが採られていること」自体を受入条件（AC-26）として強制し、手段の選択のみを設計の裁量とする。同時に、一次情報が変更されていない場合には承認を無効化も降格もしないこと（AC-27）を併せて要求する——無条件の無効化は AC-26 を形式的に満たす一方、quick 免除に従った Issue が通過できるという本 Issue の要求そのものを破壊するためである。

## 状態表

各表は当該観測対象について、行が互いに排他であり、当該表が用いる次元の全組み合わせを網羅する。各行はちょうど 1 つの受入条件に対応する。AC-1 から AC-27 はいずれもいずれかの表の行に対応する。

### 表O1: quick 免除の成否（次元 D1 × D2 × D3、27 通り）

| 行 | D1 | D2 | D3 | 組合せ数 | E（期待結果） | 対応 AC |
|---|---|---|---|---|---|---|
| 1 | `quick` | `normal` | 含まない | 1 | 成立 | AC-1 |
| 2 | `quick` または `standard` | `normal` または `normal` 以外 | 含まない または 含む（行1 を除く） | 7 | 不成立 | AC-2 |
| 3 | D1・D2・D3 の少なくとも 1 つが解決不能または判定不能 | 同左 | 同左 | 19 | 不成立 | AC-3 |

網羅: 3 次元とも解決できた組み合わせは 2×2×2 = 8 通りで、うち 1 通りが行1、残り 7 通りが行2。少なくとも 1 つが解決不能・判定不能である組み合わせは 27 − 8 = 19 通りで行3。合計 1 + 7 + 19 = 27。排他: 行1 と行2 は D1・D2・D3 の値の組で区別され、行3 は「解決できたか」で行1・行2 と区別される。

### 表O2: 判定軸区間の値 S（次元 E × D4 × D5 × D6、48 通り）

| 行 | E | D4 | D5 | D6 | 組合せ数 | S（期待結果） | 対応 AC |
|---|---|---|---|---|---|---|---|
| 1 | 成立 | すべて存在し内容を取得できた、読み取り不能あり・不在なし、または 混在 | 1 件以上 | 問わない | 6 | AC-ID 全件網羅 | AC-4 |
| 2 | 成立 | 読み取り不能なし・1 件以上が不在 | 1 件以上 | 展開できる | 1 | 代替判定基準 | AC-5 |
| 3 | 成立 | 読み取り不能なし・1 件以上が不在 | 1 件以上 | 展開できない | 1 | inconclusive 指示 | AC-6 |
| 4 | 成立 | 問わない | 0 件 | 展開できる | 4 | 代替判定基準 | AC-5 |
| 5 | 成立 | 問わない | 0 件 | 展開できない | 4 | inconclusive 指示 | AC-6 |
| 6 | 不成立 | 問わない | 1 件以上 | 問わない | 8 | AC-ID 全件網羅 | AC-7 |
| 7 | 不成立 | 問わない | 0 件 | 問わない | 8 | inconclusive 指示 | AC-8 |
| 8 | 問わない | 問わない | 抽出不能 | 問わない | 16 | inconclusive 指示 | AC-19 |

網羅: 2×4×3×2 = 48 通りを、行ごとの組合せ数 6 + 1 + 1 + 4 + 4 + 8 + 8 + 16 = 48 で覆っている。排他: 行8 は D5 が「抽出不能」である点で行1〜行7 と区別される。行1〜行5（E=成立）と行6〜行7（E=不成立）は E で区別される。行1〜行3（D5=1 件以上）と行4〜行5（D5=0 件）、および行6（D5=1 件以上）と行7（D5=0 件）は D5 で区別される。行1（D4 が「読み取り不能なし・1 件以上が不在」以外の 3 値）と行2・行3（D4 が「読み取り不能なし・1 件以上が不在」）は D4 で区別され、行2 と行3、および行4 と行5 は D6 で区別される。したがってどの 2 行も同時に成立しない。

行2・行3（quick 免除が成立し、AC-ID を抽出できるが対象ゲートの必須成果物が免除により正当に不在である）は、判断11 のとおり AC-ID 全件網羅を判定軸とせず、行4・行5 と同じ切替先（代替判定基準、展開できない場合は inconclusive 指示）を採る。網羅を立証すべき成果物が存在しない状態で AC-ID 全件網羅を判定軸とすると conformance が構造上 fail となり、免除に正しく従った Issue が通過できないためである。行1 は同じ quick 免除成立下でも、必須成果物がそろっている場合、1 件以上が読み取り不能で不在を確認できた成果物が無い場合、および読み取り不能と不在が混在する場合を含み、いずれでも切替を起こさない——読み取り不能な成果物を含む状態での切替は判定軸の無言の降格になるため、判断9・判断12 のとおり行わない（当該入力ではそもそも表O4 の行5〜行8 により起動が中断する）。行6（免除不成立かつ AC-ID を 1 件以上抽出できる）は既存の主経路であり、最大の回帰対象として AC-7 が単独で定める。免除不成立の場合は必須成果物が欠落していても切替を起こさないため、本変更の前後で行6 の期待結果は変わらない。行8（抽出不能）は、判断9 のとおり「0 件」（行4・行5・行7）と別の値として扱い、quick 免除が成立し代替判定基準を展開できる場合であっても代替判定基準へ切り替えない。

### 表O3: 判定対象区間のうち必須成果物に関する提示内容（次元 E × D4、8 通り）

| 行 | E | D4 | 期待結果 | 対応 AC |
|---|---|---|---|---|
| 1 | 成立 | すべて存在し内容を取得できた | 必須成果物の内容が展開され、不在を示す記述を含まない | AC-9 |
| 2 | 不成立 | すべて存在し内容を取得できた | 同上 | AC-9 |
| 3 | 成立 | 読み取り不能なし・1 件以上が不在 | 欠落した各必須成果物について、quick 免除により不在である旨を明示する | AC-10 |
| 4 | 不成立 | 読み取り不能なし・1 件以上が不在 | 欠落した各必須成果物について、本来存在すべき成果物が欠落している旨を明示する | AC-11 |
| 5 | 成立 | 読み取り不能あり・不在なし | 読み取り不能な各必須成果物について、内容を取得できなかった旨を明示する。免除による正当な不在としても、欠落としても提示しない | AC-20 |
| 6 | 不成立 | 読み取り不能あり・不在なし | 同上 | AC-20 |
| 7 | 成立 | 混在 | 成果物ごとに状態を区別して明示する。読み取り不能な各必須成果物については内容を取得できなかった旨を（不在としても欠落としても提示せず）、不在を確認できた各必須成果物については quick 免除により不在である旨を明示する | AC-22 |
| 8 | 不成立 | 混在 | 成果物ごとに状態を区別して明示する。読み取り不能な各必須成果物については内容を取得できなかった旨を（不在としても欠落としても提示せず）、不在を確認できた各必須成果物については本来存在すべき成果物が欠落している旨を明示する | AC-23 |

網羅: 2×4 = 8 行をすべて列挙している。排他: 各行は E・D4 の値の組が相異なる。行5・行6 の期待結果が E によって分岐しないのは、読み取り不能が免除の成否と無関係な取得側の失敗だからである。行7・行8 が E によって分岐するのは、混在状態に含まれる「不在を確認できた成果物」の提示が行3・行4 と同じく免除の成否に依存するためであり、同じ行の「読み取り不能な成果物」の提示は行5・行6 と同じく E に依存しない。混在状態を行5・行6 または行3・行4 へ吸収せず独立の行として立てる根拠は判断12 に示す。

### 表O4: ゲートレビュー起動経路の中断有無（次元 E × D4、8 通り）

| 行 | E | D4 | 期待結果 | 対応 AC |
|---|---|---|---|---|
| 1 | 成立 | すべて存在し内容を取得できた | 中断せずレビュアが起動される | AC-12 |
| 2 | 不成立 | すべて存在し内容を取得できた | 同上 | AC-12 |
| 3 | 成立 | 読み取り不能なし・1 件以上が不在 | 同上（必須成果物の不在を理由に中断しない） | AC-12 |
| 4 | 不成立 | 読み取り不能なし・1 件以上が不在 | 必須成果物の読み取り時点で中断しレビュアを起動しない | AC-13 |
| 5 | 成立 | 読み取り不能あり・不在なし | 読み取り不能を検知した時点で中断しレビュアを起動しない | AC-21 |
| 6 | 不成立 | 読み取り不能あり・不在なし | 同上 | AC-21 |
| 7 | 成立 | 混在 | 同上（読み取り不能な成果物を含むことを理由に中断する） | AC-21 |
| 8 | 不成立 | 混在 | 同上 | AC-21 |

網羅: 2×4 = 8 行をすべて列挙している。排他: 各行は E・D4 の値の組が相異なる。表O3 と E・D4 を共有するが観測対象が異なるため、両表の行が同時に成立しても相反しない。行4〜行8 の入力では verdict が存在しないため D10 は「レビュアが起動されず verdict が存在しない」を採り、当該入力の final は表O8 の行9 が `human_required` と定める。本表は中断の有無のみを観測し、final の値は観測しない。

### 表O8: 1 回の attempt における final の導出（次元 S × D10、12 通り）

D10 の 4 値は互いに排他である（入力次元の定義により、`fail`・blocking finding は `pending` に優先する。根拠は判断10。レビュアが起動されない入力では verdict 自体が存在せず、他の 3 値のいずれにも該当しない）。したがって 1 つの attempt はちょうど 1 つの D10 値に該当し、S と併せて行がちょうど 1 つ定まる。

本表の各行は、D14 を観測できない入力（S が「代替判定基準」でない場合）、または D14 が「無い」入力に適用される。S が「代替判定基準」であり D14 が「有る」入力の帰結は表O12 が定め、当該入力では本表の行1・行2 による `approved` の導出を行わない。両表の入力集合は D14 の値により互いに素である。

| 行 | S | D10 | 期待結果 | 対応 AC |
|---|---|---|---|---|
| 1 | AC-ID 全件網羅 | 両観点とも `pass` かつ blocking finding 無し | `approved` | AC-14 |
| 2 | 代替判定基準 | 両観点とも `pass` かつ blocking finding 無し | `approved` | AC-14 |
| 3 | AC-ID 全件網羅 | いずれかが `fail` もしくは blocking finding あり | `rejected` | AC-15 |
| 4 | 代替判定基準 | いずれかが `fail` もしくは blocking finding あり | `rejected` | AC-15 |
| 5 | AC-ID 全件網羅 | `fail`・blocking finding が無く、いずれかが `pending` | `human_required` | AC-15 |
| 6 | 代替判定基準 | `fail`・blocking finding が無く、いずれかが `pending` | `human_required` | AC-15 |
| 7 | inconclusive 指示 | いずれかが `fail` もしくは blocking finding あり | `rejected`。`approved` を導出しない | AC-16 |
| 8 | inconclusive 指示 | `fail`・blocking finding が無く、いずれかが `pending` | `human_required`。`approved` を導出しない | AC-16 |
| 9 | 問わない（3 値すべて。判定プロンプトが生成されない入力でも S は表O2 により一意に定まる） | レビュアが起動されず verdict が存在しない | `human_required`。`approved` を導出せず、final を未導出のまま放置しない | AC-24 |
| — | inconclusive 指示 | 両観点とも `pass` かつ blocking finding 無し | 除外。判定プロンプトが conformance を inconclusive とするよう明示指示している以上、前提・制約（レビュアは明示指示に従う）の下では conformance が `pass` にはならないため発生しない | 対応なし |

網羅: 3×4 = 12 行のうち、行1〜行8 が 8 行を、行9 が 3 行（S の 3 値それぞれ）を覆い、残る 1 行を上記の理由で除外している。排他: 各行は S・D10 の値の組が相異なり、D10 の 4 値が互いに排他であることは入力次元の定義が保証する。行9 は D10 が「レビュアが起動されず verdict が存在しない」である点で行1〜行8 と区別される。行7 は、レビュアが指示に従い conformance を `pending` としたうえで falsification を `fail` とした場合、または blocking finding を返した場合に到達する正規の行であり、除外行ではない。

### 表O9: 代替判定基準の実体の取得経路（次元 S × D11、6 通り）

| 行 | S | D11 | 期待結果 | 対応 AC |
|---|---|---|---|---|
| 1 | 代替判定基準 | 存在する | 判定軸区間へ書き出される実体は、trusted base 側の起動経路が Coordination Backend の一次情報から取得した Issue 本文のみに由来する。ワーカー供給経路にだけ置かれた記述は判定軸区間に現れない | AC-17 |
| 2 | 代替判定基準 | 存在しない | 同上（由来は trusted base 側の起動経路が取得した Issue 本文のみである） | AC-17 |
| — | AC-ID 全件網羅 または inconclusive 指示 | 存在する / 存在しない | 除外。代替判定基準の実体が判定軸区間へ書き出されないため、その取得経路という観測対象が存在しない | 対応なし |

網羅: 3×2 = 6 行のうち 2 行を行1・行2 が覆い、残る 4 行を上記の理由で除外している。排他: 行1 と行2 は D11 の値で区別される。表O2 と S を共有するが、表O2 が S の値そのものを、表O9 が S が「代替判定基準」である場合の実体の取得経路を観測するため、両表の行が同時に成立しても相反しない。

### 表O10: 判定対象区間と判定軸区間の非重複（全次元の全組み合わせ、1 行）

| 行 | 条件 | 期待結果 | 対応 AC |
|---|---|---|---|
| 1 | 判定プロンプトを生成する任意の入力（全次元の任意の組み合わせ） | 両区間の境界が判定プロンプト内で一意に識別でき、両区間が占める位置は互いに素である。一方の区間の入力にだけ与えた標識が他方の区間に現れない | AC-18 |

網羅: 全組み合わせが行1 に含まれる。排他: 行が 1 つのため自明に成立する。

### 表O11: 判定対象区間へ展開される変更差分証跡（次元 S × D13、6 通り）

| 行 | S | D13 | 期待結果 | 対応 AC |
|---|---|---|---|---|
| 1 | 代替判定基準 | 変更されたファイルが 1 件以上ある | 判定対象区間に、変更されたファイルの一覧と各ファイルの内容差分が変更差分証跡として展開される | AC-25 |
| 2 | 代替判定基準 | 変更されたファイルが 1 件も無い | 判定対象区間に、変更差分が空である旨が明示される。無言の空欄にしない | AC-25 |
| — | AC-ID 全件網羅 または inconclusive 指示 | 問わない | 除外。代替判定基準への適合を立証するための証跡という観測対象が存在しない（S が「AC-ID 全件網羅」である場合は判定対象区間の必須成果物が適合の立証対象であり、S が「inconclusive 指示」である場合は conformance の判定自体を行わない） | 対応なし |

網羅: 3×2 = 6 行のうち 2 行を行1・行2 が覆い、残る 4 行を上記の理由で除外している。排他: 行1 と行2 は D13 の値で区別される。表O3 と判定対象区間を共有するが、表O3 が必須成果物に関する提示内容を、表O11 が変更差分証跡の提示内容を観測するため、両表の行が同時に成立しても相反しない。

### 表O12: 代替判定基準の一次情報の変更に対する承認の帰結（次元 S × D14、6 通り）

| 行 | S | D14 | 期待結果 | 対応 AC |
|---|---|---|---|---|
| 1 | 代替判定基準 | 有る | 変更後の一次情報を代替判定基準として `approved` が導出されず、変更前に導出済みの `approved` も変更後そのまま有効として維持されない。この結果は防止・検知・無効化のうち少なくとも 1 つの手段によって達成される | AC-26 |
| 2 | 代替判定基準 | 無い | 防止・検知・無効化のいずれの措置も発動せず、導出済みの `approved` が維持され、当該ゲートの再通過は要求されない | AC-27 |
| — | AC-ID 全件網羅 または inconclusive 指示 | 問わない | 除外。代替判定基準が判定軸ではないため、その一次情報の変更が当該ゲートの承認に影響する経路が存在しない | 対応なし |

網羅: 3×2 = 6 行のうち 2 行を行1・行2 が覆い、残る 4 行を上記の理由で除外している。排他: 行1 と行2 は D14 の値で区別される。表O8 と承認の帰結という値域を共有するが、表O8 が D14 を観測できない入力または D14 が「無い」入力を、表O12 が D14 が「有る」入力を扱うため、両表の入力集合は互いに素であり同一入力に 2 つの期待結果を定めない。

## 要求 → 要件 → 受入条件

### 要求

quick 免除に正しく従った Issue が、進行役による判定の上書き無しにゲートを通過できること。同時に、成果物が本来存在すべきなのに欠落している Issue では、従来どおり安全側（`human_required`）へ倒れること。

### 要件

- 要件1: 判定機構は size シグナル・risk シグナル・ガードレール判定をモード別の取得元から解決し、quick 免除の成否を決定的に導く。シグナルが存在しない状態は既定値（size は `standard`、risk は `unclassified`）へ決定的に解決する。
- 要件2: size シグナル・risk シグナルの解決不能、およびガードレール判定不能は、いずれも免除不成立として扱う。解決できない値を既定値とみなして免除を成立させない。
- 要件3: 判定プロンプトは判定対象区間と判定軸区間を互いに重ならない区間として含み、各区間の内容が独立に決まる。
- 要件4: quick 免除が成立し、かつ (i) AC-ID を 1 件も抽出できない場合、または (ii) 対象ゲートの必須成果物のうち 1 件以上が不在であることを確認できた場合は、AC-ID 全件網羅を判定軸とせず、代替判定基準を判定軸区間へ実体として展開し、conformance が成立しうるようにする。展開できない場合は判定プロンプト生成を中断せず、inconclusive 指示へ倒す。免除により正当に不在の成果物があることのみを理由に conformance が構造上 fail となる判定軸を出力しない。対象ゲートの必須成果物のうち 1 件以上が読み取り不能である場合は、同時に別の成果物の不在を確認できた（混在）場合を含めて (ii) に該当させず、切替を起こさない。
- 要件5: AC-ID を 1 件以上抽出できる場合は、要件4 の (ii) に該当する場合を除き、免除の成否によらず AC-ID 全件網羅を判定軸とする。quick 免除が成立しない場合は、必須成果物が欠落していても代替判定基準へ切り替えない。免除不成立かつ AC-ID 抽出が 0 件である場合は従来どおり inconclusive 指示とし、代替判定基準は展開しない。AC-ID 抽出が不能である場合は、免除の成否と代替判定基準の展開可否によらず inconclusive 指示とし、抽出不能を 0 件と同一視して代替判定基準へ切り替えない。final の導出規則は従来どおりとし、`fail`・blocking finding と `pending` が同時に成立する verdict では `fail`・blocking finding を優先して `rejected` を導出する。
- 要件6: quick 免除が成立する場合、必須成果物が target SHA に存在しないことを理由にゲートレビュー起動を中断しない。免除が成立しない場合の中断挙動は維持する。欠落した必須成果物は判定対象区間で不在事由とともに明示し、無言の空欄にしない。必須成果物が読み取り不能である場合は、免除の成否によらずゲートレビュー起動を中断して `approved` を導出せず、読み取り不能を不在と同一視しない。判定対象区間を単独で生成する場合は、読み取り不能である旨を、免除による正当な不在とも欠落とも区別して明示する。同一ゲートの必須成果物について、一部が不在であることを確認でき、かつ別の一部が読み取り不能である混在状態では、成果物ごとに状態を区別して明示する——読み取り不能なものは内容を取得できなかった旨として、不在を確認できたものは免除の成否に応じて正当な不在または欠落として明示する。起動を中断してレビュアを起動しない入力では、当該 attempt の final を `human_required` として導出し、判定を未導出のまま放置しない。
- 要件9: 代替判定基準の実体となる Issue 本文は、trusted base 側の起動経路が Coordination Backend の一次情報（GitHub モードは Issue 本文、ローカルモードは Issue 状態ファイル）から直接取得したものに限る。レビュアまたはセグメント作業ワーカーが供給した値（branch 上の成果物ファイル・PR 本文・Issue コメント・本文の写し）を**代替判定基準**の入力にしない。本要件が限定するのは代替判定基準の実体の取得経路のみである。判定対象区間へ展開する成果物、および要件5 に従って `SPEC.md` から抽出した AC-ID 集合を判定軸区間へ書き出すことは本要件の対象外であり、これらを**代替判定基準**の実体としては用いない。
- 要件10: 判定軸（AC-ID 集合または代替判定基準）を実体化できない場合、`approved` を導出しない。
- 要件13: 代替判定基準の一次情報（GitHub モードは Issue 本文、ローカルモードは Issue 状態ファイル）が、判定を受ける側（セグメント作業ワーカーの実行主体）によって変更された場合に、変更後の一次情報を代替判定基準として `approved` へ到達せず、変更前に導出済みの `approved` もそのまま有効として維持されない。この結果は次の 3 手段のうち少なくとも 1 つによって達成する。(a) 防止: セグメント作業ワーカーの実行経路から当該一次情報を変更する操作を拒否する。(b) 検知: 当該変更を検知し、`approved` を導出せず `human_required` を導出する。(c) 無効化: 当該変更を検知し、当該ゲートおよび全下流ゲートの承認を無効化して再通過を要求する。どの手段を採るか、複数を組み合わせるかは設計セグメントの裁量とするが、いずれの手段も採らない実装は本仕様の完了条件を満たさない。一次情報が変更されていない場合には、これらの措置を発動せず既存の判定を維持する。
- 要件14: quick 免除が成立し判定軸が代替判定基準である場合、当該基準への適合を判定するための判定対象側の証跡として、base SHA から target SHA までの変更差分証跡を判定対象区間へ展開する。変更されたファイルが 1 件も無い場合はその旨を明示し、判定対象区間を無言の空欄にしない。当該証跡は判定対象区間にのみ展開し、代替判定基準の実体としては用いない（実体を Issue 本文に限ることは要件9 が定め、AC-17 が検証する）。
- 要件12: 本仕様が定めるすべての要件と受入条件が、GitHub モードとローカルモードの双方で成立する。モードによって異なるのは Coordination Backend 由来の入力（size シグナル・risk シグナル・代替判定基準の一次情報）の取得元のみであり、期待結果は両モードで同一である。AC-1 から AC-27 のすべてについて、両モードを入力として期待結果の成立を検証し、一方のモードでのみ期待結果を満たす実装を完了とみなさない。

### 要件と受入条件の対応

| 要件 | 対応する受入条件 |
|---|---|
| 要件1 | AC-1・AC-2 |
| 要件2 | AC-3 |
| 要件3 | AC-4・AC-9・AC-10・AC-11・AC-18 |
| 要件4 | AC-5・AC-6 |
| 要件5 | AC-4・AC-7・AC-8・AC-14・AC-15・AC-19 |
| 要件6 | AC-10・AC-11・AC-12・AC-13・AC-20・AC-21・AC-22・AC-23・AC-24 |
| 要件9 | AC-17 |
| 要件10 | AC-6・AC-13・AC-16・AC-19・AC-21・AC-24・AC-26 |
| 要件13 | AC-26・AC-27 |
| 要件14 | AC-25 |
| 要件12 | AC-1 から AC-27 のすべて（本仕様の全受入条件を両モードで検証する。モード由来の入力を持たない受入条件についても、双方のモードで同一の期待結果が成立することをもって成立とする） |

### 受入条件（Acceptance Criteria）

以下のすべての受入条件は、要件12 により GitHub モードとローカルモードの双方を入力として検証する。Given にモード条件を明示していない受入条件についても、双方のモードで同一の期待結果が成立することを要求し、一方のモードでのみ期待結果を満たす実装を完了とみなさない。

#### AC-1: 3 条件がすべて満たされる場合にのみ quick 免除が成立する

- Given: size シグナルが `quick` に解決され、risk シグナルが `normal` に解決され、ガードレール判定が「含まない」である Issue（表O1 の行1）。
- When: quick 免除の成否を判定する。
- Then: quick 免除が成立すると判定される。GitHub モードのラベル由来のシグナルと、ローカルモードの `state.yaml` 由来のシグナルの双方で同じ結果になる。
- 検証方法見込み: `automated`

#### AC-2: 3 シグナルをすべて解決できるが条件を満たさない場合は免除不成立となる

- Given: size シグナル・risk シグナル・ガードレール判定のすべてを解決でき、かつ AC-1 の Given に一致しない Issue（表O1 の行2。size が `standard` に解決される場合、risk が `normal` 以外に解決される場合、ガードレール判定が「含む」である場合の 7 通りすべてを含む。size ラベル未付与により `standard` へ解決される場合、risk ラベル未付与により `unclassified` へ解決される場合を含む）。
- When: quick 免除の成否を判定する。
- Then: quick 免除は成立しないと判定される。シグナルが存在しない状態は解決不能として扱われず、既定値（size は `standard`、risk は `unclassified`）へ決定的に解決されたうえでこの結果になる。
- 検証方法見込み: `automated`

#### AC-3: いずれかを解決できない場合は既定値へ落とさず免除不成立とする

- Given: size シグナル・risk シグナル・ガードレール判定の少なくとも 1 つが解決不能または判定不能である Issue（表O1 の行3。取得元の読み取り失敗、値域外の値、同一プレフィックスのラベルが 2 つ以上付与された状態、base SHA を解決できない状態、差分取得の失敗を含む）。
- When: quick 免除の成否を判定する。
- Then: quick 免除は成立しないと判定される。解決できない size を `quick` とみなさず、解決できない risk を `normal` とみなさず、取得できない差分を「ガードレール対象パスを含まない」とみなさない。解決不能であること自体を理由に判定を中断せず、免除不成立として決定的に解決する。
- 検証方法見込み: `automated`

#### AC-4: quick 免除成立下でも必須成果物が欠けていなければ AC-ID 全件網羅を判定軸とする

- Given: quick 免除が成立し、target SHA から AC-ID を 1 件以上抽出でき、かつ対象ゲートの必須成果物の読み取り結果が「すべて存在し内容を取得できた」「読み取り不能あり・不在なし」「混在」のいずれかである Issue（表O2 の行1。必須成果物がすべて存在し内容を取得できた場合、1 件以上が読み取り不能で不在を確認できた成果物が無い場合、および読み取り不能と不在が混在する場合の 3 通りを含み、代替判定基準を展開できるか否かを問わない）。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。必須成果物が読み取り不能である入力では、レビュー起動経路の中断を AC-21 が別途定めるため、本受入条件では判定プロンプト生成を直接呼び出して観測する。
- Then: 判定軸区間に、抽出された AC-ID の全件網羅を conformance の判定軸とする指示が出力される。代替判定基準への切り替えは起きず、inconclusive 指示も含まれない。必須成果物が読み取り不能である場合も、読み取り不能と不在が混在する場合も、読み取りの失敗を理由に代替判定基準へ切り替えない。本受入条件は判定軸区間のみを観測し、判定対象区間の内容は観測しない。
- 検証方法見込み: `automated`

#### AC-5: quick 免除成立下で AC-ID 全件網羅を立証できない場合は代替判定基準を判定軸とする

- Given: quick 免除が成立し、かつ代替判定基準を展開でき、かつ次のいずれかに該当する Issue（表O2 の行2・行4）。(i) target SHA からの AC-ID 抽出に成功したうえで結果が 0 件である（`SPEC.md` が存在しないことを確認できた場合を含み、抽出不能である場合を含まない。対象ゲートの必須成果物の充足状態を問わない）。(ii) AC-ID を 1 件以上抽出できるが、読み取り不能な必須成果物が 1 件も無く、対象ゲートの必須成果物のうち 1 件以上が不在であることを確認できた（design ゲートで `SPEC.md` に AC-ID がありながら `DESIGN.md`・`PLAN.md` が免除により不在である場合、validation ゲートで `VALIDATION.md` が免除により不在である場合を含む。読み取り不能な必須成果物を含む混在状態は本項に該当しない）。
- When: 対象ゲートの判定プロンプトを生成する。(i) は spec・design・implementation・validation の 4 ゲートすべてを対象に含める。(ii) は design・validation の 2 ゲートを対象に含める（implementation ゲートは必須成果物を持たず、spec ゲートは `SPEC.md` の不在が (i) を意味するため、いずれも (ii) に該当する入力が存在しない）。(i) のうち必須成果物が読み取り不能である入力では、レビュー起動経路の中断を AC-21 が別途定めるため、判定プロンプト生成を直接呼び出して観測する。
- Then: 生成が成功し、判定軸区間に inconclusive 指示が含まれない。判定軸区間には、AC-ID 全件網羅を判定軸としない旨（(i) では AC-ID が quick 免除により正当に存在しない旨、(ii) では網羅を立証すべき必須成果物が quick 免除により正当に不在である旨）と、代替判定基準の実体（Issue 本文由来の要求・期待する挙動・受入基準の記述）が conformance の判定軸として出力される。(ii) では、抽出された AC-ID の全件網羅を conformance の判定軸とする指示が出力されず、免除により正当に不在の成果物に係る立証の欠落のみを理由に conformance が fail となる判定軸が出力されない。対象ゲートの別によらず同一の実体が展開される。本受入条件は判定軸区間のみを観測する。
- 検証方法見込み: `automated`

#### AC-6: quick 免除成立下で判定軸を実体化できない場合は中断せず inconclusive 指示を出す

- Given: quick 免除が成立し、代替判定基準を展開できず（Issue 本文を取得できない、または取得できても要求・期待する挙動・受入基準に相当する記述が 0 件である）、かつ AC-5 の Given の (i) または (ii) に該当する Issue（表O2 の行3・行5。いずれも AC-ID 抽出が不能である場合を含まない）。
- When: 対象ゲートの判定プロンプトを生成する。対象ゲートの範囲と、必須成果物が読み取り不能である入力で判定プロンプト生成を直接呼び出す点は AC-5 の When と同一とする。
- Then: 生成は例外で中断せず成功し、判定軸区間に inconclusive 指示が出力される。代替判定基準は展開されず、AC-ID 全件網羅も判定軸として出力されず、conformance が pass へ到達しうる判定軸は判定軸区間に含まれない。本受入条件は判定軸区間のみを観測する。
- 検証方法見込み: `automated`

#### AC-7: 免除不成立かつ AC-ID を抽出できる既存主経路の挙動を維持する

- Given: quick 免除が成立せず、target SHA から AC-ID を 1 件以上抽出できる Issue（表O2 の行6。size が `standard` に解決される場合、risk が `normal` 以外である場合、ガードレール対象パスを含む場合、いずれかのシグナルが解決不能な場合をすべて含み、対象ゲートの必須成果物の充足状態と代替判定基準の展開可否を問わない）。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。必須成果物が 1 件以上不在または読み取り不能である入力では、レビュー起動経路の中断を AC-13・AC-21 が別途定めるため、判定プロンプト生成を直接呼び出して観測する。
- Then: 判定軸区間に、抽出された AC-ID の全件網羅を conformance の判定軸とする指示が出力される。代替判定基準は展開されず、inconclusive 指示も含まれない。必須成果物が 1 件以上不在である場合も代替判定基準へ切り替えない（切替は quick 免除が成立する場合に限る）。本変更の適用前後で、この入力に対する判定軸区間の内容が変化しない。本受入条件は判定軸区間のみを観測する。
- 検証方法見込み: `automated`

#### AC-8: 免除不成立かつ AC-ID 0 件では従来どおり inconclusive 指示を出す

- Given: quick 免除が成立せず、target SHA からの AC-ID 抽出に成功したうえで結果が 0 件である Issue（抽出不能である場合を含まない。表O2 の行7。対象ゲートの必須成果物の充足状態と代替判定基準の展開可否を問わない）。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。必須成果物が 1 件以上不在または読み取り不能である入力では、レビュー起動経路の中断を AC-13・AC-21 が別途定めるため、判定プロンプト生成を直接呼び出して観測する。
- Then: 判定軸区間に従来どおり inconclusive 指示が出力される。代替判定基準は、展開できる場合であっても展開されない。本受入条件は判定軸区間のみを観測する。
- 検証方法見込み: `automated`

#### AC-9: 必須成果物がそろっている場合は従来どおり内容が判定対象区間へ展開される

- Given: target SHA に対象ゲートの必須成果物がすべて存在し、その内容を取得できた Issue（表O3 の行1・行2。quick 免除の成否を問わない）。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。
- Then: 判定対象区間に当該必須成果物の内容が展開され、不在を示す記述は含まれない。必須成果物が定義されていないゲートでは、不在を示す記述が含まれないことのみを要求する。本受入条件は判定対象区間のうち必須成果物に関する提示内容のみを観測し、判定軸区間の内容と変更差分証跡は観測しない。
- 検証方法見込み: `automated`

#### AC-10: quick 免除下の必須成果物欠落は免除による不在として明示される

- Given: quick 免除が成立し、target SHA に対象ゲートの必須成果物が 1 件以上存在しないことを確認でき、読み取り不能な必須成果物が 1 件も無い Issue（表O3 の行3。spec は `SPEC.md`、design は `DESIGN.md` と `PLAN.md`、validation は `VALIDATION.md`）。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・validation の 3 ゲートすべてを対象に含める（implementation ゲートは必須成果物を持たないため本 Given に該当する入力が存在しない）。
- Then: 判定対象区間に、欠落した各必須成果物の名称と、それが quick 免除により正当に不在である旨が明示される。判定対象区間が無言の空欄にならない。本受入条件は判定対象区間のうち必須成果物に関する提示内容のみを観測し、判定軸区間の内容と変更差分証跡は観測しない。
- 検証方法見込み: `automated`

#### AC-11: 免除不成立下の必須成果物欠落は異常として明示される

- Given: quick 免除が成立せず、target SHA に対象ゲートの必須成果物が 1 件以上存在しないことを確認でき、読み取り不能な必須成果物が 1 件も無い Issue（表O3 の行4）。
- When: 対象ゲートの判定プロンプトを単独で生成する（レビュー起動経路の中断は AC-13 が定めるため、本受入条件では判定プロンプト生成を直接呼び出す）。spec・design・validation の 3 ゲートすべてを対象に含める（implementation ゲートは必須成果物を持たないため本 Given に該当する入力が存在しない）。
- Then: 判定対象区間に、欠落した各必須成果物の名称と、それが本来存在すべきであるのに欠落している旨が明示される。quick 免除による正当な不在として提示されない。本受入条件は判定対象区間のうち必須成果物に関する提示内容のみを観測する。
- 検証方法見込み: `automated`

#### AC-12: 必須成果物がそろっているか quick 免除が成立する場合はレビュアが起動される

- Given: 読み取り不能な必須成果物が 1 件も無く、かつ target SHA に対象ゲートの必須成果物がすべて存在してその内容を取得できた Issue、または 読み取り不能な必須成果物が 1 件も無く、かつ quick 免除が成立する Issue（表O4 の行1〜行3）。
- When: 当該ゲートのレビューを起動する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。
- Then: 「target SHAの必須成果物を読めません」に相当する中断が発生せず、レビュアが起動され、conformance と falsification の判定値を伴う gate-report が生成される。本受入条件は起動経路の中断有無のみを観測し、判定プロンプトの内容は観測しない。
- 検証方法見込み: `automated`

#### AC-13: 免除不成立時の必須成果物欠落は従来どおり中断する

- Given: quick 免除が成立せず、target SHA に対象ゲートの必須成果物が 1 件以上存在しないことを確認でき、読み取り不能な必須成果物が 1 件も無い Issue（表O4 の行4）。
- When: 当該ゲートのレビューを起動する。spec・design・validation の 3 ゲートすべてを対象に含める（implementation ゲートは必須成果物を持たないため本 Given に該当する入力が存在しない）。
- Then: 必須成果物の読み取り時点で従来どおり中断してレビュアを起動しない。本受入条件は起動経路の中断有無のみを観測する。当該入力では verdict が存在しないため D10 は「レビュアが起動されず verdict が存在しない」を採り、表O8 の行9（AC-24 が検証する）により当該 attempt の final は `human_required` として導出される。すなわち成果物が本来存在すべきなのに欠落している入力は、従来どおり安全側の `human_required` へ倒れ、`approved` は導出されない。
- 検証方法見込み: `automated`

#### AC-14: 判定軸が実体化されている場合に approved へ到達できる

- Given: 判定軸区間の値が「AC-ID 全件網羅」または「代替判定基準」である attempt（表O8 の行1・行2）。判定軸が「代替判定基準」である場合は、当該基準の一次情報が判定を受ける側によって変更されていない入力（D14 が「無い」）に限る。変更されている入力（D14 が「有る」）の帰結は表O12 が定め、AC-26 が検証する。
- When: レビュアが conformance と falsification の双方を pass、blocking finding 無しと判定した verdict を返す。
- Then: final が `approved` として導出され、判定プロンプト由来の理由で conformance が pending に固定されることがない。代替判定基準を判定軸とする場合も同様に `approved` へ到達する。
- 検証方法見込み: `automated`

#### AC-15: 判定軸が実体化されていても verdict が pass でなければ従来どおり導出される

- Given: 判定軸区間の値が「AC-ID 全件網羅」または「代替判定基準」である attempt（表O8 の行3〜行6）。
- When: レビュアが次の 3 種の verdict を返す。(i) いずれかの観点が `fail` であるか blocking finding があり、かつ両観点とも `pending` ではない。(ii) いずれかの観点が `fail` であるか blocking finding があり、かつ他方の観点が `pending` である。(iii) いずれの観点も `fail` ではなく blocking finding も無く、いずれかの観点が `pending` である。
- Then: (i) と (ii) では final が `rejected`、(iii) では `human_required` として導出される。すなわち `fail`・blocking finding と `pending` が同時に成立する verdict では `fail`・blocking finding を優先し、`human_required` ではなく `rejected` が一意に導出される。final の導出規則は本変更の適用前後で変化しない。
- 検証方法見込み: `automated`

#### AC-16: 判定軸が inconclusive 指示である attempt から approved は導出されない

- Given: 判定軸区間の値が「inconclusive 指示」である attempt（表O2 の行3・行5・行7・行8 が生成する判定プロンプト。表O8 の行7・行8）。
- When: レビュアが判定プロンプトの指示に従い conformance を `pending` として返す。falsification が `fail` であるか blocking finding がある場合と、いずれの観点も `fail` ではなく blocking finding も無い場合の双方を含める。
- Then: 前者では final が `rejected`、後者では `human_required` として導出され、いずれの場合も `approved` は導出されない。代替判定基準の展開不能・AC-ID 抽出結果 0 件・AC-ID 抽出不能という判定不能状態から `approved` へ到達する経路が存在しない。
- 検証方法見込み: `automated`

#### AC-17: 代替判定基準の実体は trusted base 側が取得した Issue 本文のみに由来する

- Given: 判定軸区間の値 S が「代替判定基準」となる入力（表O9 の行1・行2）。ワーカー供給経路（branch 上の成果物ファイル・PR 本文・Issue コメント・ワーカーが作成した Issue 本文の写し）に、trusted base 側が取得する Issue 本文とは異なる「要求・期待する挙動・受入基準」相当の記述（受入基準を緩める記述を含む）が置かれている状態と、置かれていない状態の双方を含む。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。
- Then: 判定軸区間へ書き出される代替判定基準の実体は、trusted base 側の起動経路が Coordination Backend の一次情報（GitHub モードは Issue 本文、ローカルモードは Issue 状態ファイル）から取得した Issue 本文のみに由来する。ワーカー供給経路にだけ置いた記述は判定軸区間に現れず、ワーカー供給経路に当該記述が有る場合と無い場合とで判定軸区間へ書き出される実体が一致する。本受入条件は判定軸区間へ書き出された実体の取得経路のみを観測し、S の値そのものと判定対象区間の内容は観測しない。
- 検証方法見込み: `automated`

#### AC-18: 判定対象区間と判定軸区間が判定プロンプト内で重ならない

- Given: 判定プロンプトを生成する任意の入力（表O10 の行1。quick 免除の成否、必須成果物の充足、AC-ID 抽出結果、代替判定基準の展開可否のいずれの組み合わせも含む）。判定対象区間の入力となる成果物内容のうち AC-ID 以外の箇所（変更差分証跡を展開する入力では当該差分の内容を含む）と、代替判定基準の入力となる Issue 本文に、それぞれ相異なる識別可能な標識文字列を与える。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。
- Then: 生成された判定プロンプトから判定対象区間と判定軸区間の開始位置と終了位置が一意に識別でき、両区間が占める位置の集合は互いに素である（いずれの区間にも属さない位置が両者の間にあってよいが、両区間が重なる位置は存在しない）。成果物内容および変更差分へ与えた標識は判定軸区間に現れない。代替判定基準を展開する入力では、Issue 本文へ与えた標識が判定対象区間に現れない。本受入条件は両区間の境界と位置の非重複のみを観測し、各区間の内容が何であるかは観測しない。
- 検証方法見込み: `automated`

#### AC-19: AC-ID 抽出が不能な場合は代替判定基準へ切り替えず inconclusive 指示を出す

- Given: target SHA の `SPEC.md` が読み取り不能であるために AC-ID の件数を確定できない Issue（表O2 の行8。quick 免除の成否、対象ゲートの必須成果物の充足状態、代替判定基準の展開可否のいずれの組み合わせも含み、quick 免除が成立し代替判定基準を展開できる入力を必ず含める）。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。spec ゲートでは `SPEC.md` が必須成果物であり、本 Given の入力では AC-21 によりレビュー起動経路が中断するため、本受入条件では判定プロンプト生成を直接呼び出して観測する。
- Then: 判定軸区間に inconclusive 指示が出力される。抽出不能が「抽出に成功し 0 件」と同一視されず、quick 免除が成立し代替判定基準を展開できる入力であっても代替判定基準は展開されない。AC-ID 全件網羅も判定軸として出力されない。本受入条件は判定軸区間のみを観測する。
- 検証方法見込み: `automated`

#### AC-20: 読み取り不能な必須成果物は不在としても欠落としても提示されない

- Given: target SHA において対象ゲートの必須成果物が 1 件以上読み取り不能であり、かつ不在であることを確認できた必須成果物が 1 件も無い Issue（読み取り不能とは、存在の有無を確定できない、または存在するが内容の取得・復号・解析に失敗した状態をいう。表O3 の行5・行6。quick 免除の成否を問わない。読み取り不能と不在が混在する入力は AC-22・AC-23 が定めるため本 Given に含めない）。
- When: 対象ゲートの判定プロンプトを単独で生成する（レビュー起動経路の中断は AC-21 が定めるため、本受入条件では判定プロンプト生成を直接呼び出す）。spec・design・validation の 3 ゲートすべてを対象に含める（implementation ゲートは必須成果物を持たないため本 Given に該当する入力が存在しない）。
- Then: 判定対象区間に、読み取り不能な各必須成果物の名称と、その内容を取得できなかった旨が明示される。quick 免除により正当に不在であるとも、本来存在すべき成果物が欠落しているとも提示されない。quick 免除が成立する入力と成立しない入力とで、この提示内容が一致する。本受入条件は判定対象区間のうち必須成果物に関する提示内容のみを観測する。
- 検証方法見込み: `automated`

#### AC-21: 必須成果物が読み取り不能な場合はレビュアを起動しない

- Given: target SHA において対象ゲートの必須成果物が 1 件以上読み取り不能である Issue（表O4 の行5〜行8。不在であることを確認できた必須成果物の有無を問わず、読み取り不能のみの入力と、読み取り不能と不在が混在する入力の双方を含める。quick 免除の成否を問わず、quick 免除が成立する入力を必ず含める）。
- When: 当該ゲートのレビューを起動する。spec・design・validation の 3 ゲートすべてを対象に含める（implementation ゲートは必須成果物を持たないため本 Given に該当する入力が存在しない。混在する入力は必須成果物を 2 件以上持つ design ゲートでのみ成立するため、当該入力は design ゲートで観測する）。
- Then: 読み取り不能を検知した時点で中断してレビュアを起動しない。quick 免除が成立していても中断し、読み取り不能を不在として扱って起動を継続しない。読み取り不能と不在が混在する入力でも、読み取り不能な成果物を含むことを理由に中断する。本受入条件は起動経路の中断有無のみを観測する。当該入力では verdict が存在しないため、表O8 の行9（AC-24 が検証する）により当該 attempt の final は `human_required` として導出され、`approved` は導出されない。
- 検証方法見込み: `automated`

#### AC-22: quick 免除下で不在と読み取り不能が混在する場合は成果物ごとに区別して提示される

- Given: quick 免除が成立し、target SHA において対象ゲートの必須成果物のうち 1 件以上が読み取り不能であり、かつ別の 1 件以上について不在であることを確認できた Issue（表O3 の行7）。
- When: 対象ゲートの判定プロンプトを単独で生成する（レビュー起動経路の中断は AC-21 が定めるため、本受入条件では判定プロンプト生成を直接呼び出す）。本 Given は必須成果物を 2 件以上持つゲートでのみ成立するため、design ゲート（`DESIGN.md`・`PLAN.md`）を対象とする。
- Then: 判定対象区間に、必須成果物ごとに状態を区別した提示が現れる。読み取り不能な各必須成果物については、その名称と内容を取得できなかった旨が明示され、quick 免除による正当な不在としても本来存在すべき成果物の欠落としても提示されない。不在であることを確認できた各必須成果物については、その名称と quick 免除により正当に不在である旨が明示される。いずれの成果物についても無言の空欄にならない。本受入条件は判定対象区間のうち必須成果物に関する提示内容のみを観測する。
- 検証方法見込み: `automated`

#### AC-23: 免除不成立下で不在と読み取り不能が混在する場合は成果物ごとに区別して提示される

- Given: quick 免除が成立せず、target SHA において対象ゲートの必須成果物のうち 1 件以上が読み取り不能であり、かつ別の 1 件以上について不在であることを確認できた Issue（表O3 の行8）。
- When: 対象ゲートの判定プロンプトを単独で生成する（レビュー起動経路の中断は AC-21 が定めるため、本受入条件では判定プロンプト生成を直接呼び出す）。本 Given は必須成果物を 2 件以上持つゲートでのみ成立するため、design ゲート（`DESIGN.md`・`PLAN.md`）を対象とする。
- Then: 判定対象区間に、必須成果物ごとに状態を区別した提示が現れる。読み取り不能な各必須成果物については、その名称と内容を取得できなかった旨が明示され、不在としても欠落としても提示されない。不在であることを確認できた各必須成果物については、その名称と本来存在すべき成果物が欠落している旨が明示され、quick 免除による正当な不在として提示されない。いずれの成果物についても無言の空欄にならない。本受入条件は判定対象区間のうち必須成果物に関する提示内容のみを観測する。
- 検証方法見込み: `automated`

#### AC-24: レビュアが起動されない入力の final は human_required として導出される

- Given: 起動経路が中断してレビュアが起動されず、verdict が存在しない attempt（表O8 の行9。表O4 の行4〜行8 が定める中断のすべてを含む——免除不成立下の必須成果物欠落、免除の成否を問わない読み取り不能、読み取り不能と不在の混在）。判定軸区間の値 S は「AC-ID 全件網羅」「代替判定基準」「inconclusive 指示」のいずれであるかを問わない（S は表O2 が定める全域関数であり、判定プロンプトが実際には生成されない入力でも値は一意に定まる）。
- When: 当該ゲートの gate-report を確定させる。spec・design・validation の 3 ゲートすべてを対象に含める。
- Then: final が `human_required` として導出され、`approved` は導出されない。final が未導出のまま放置されず、判定の欠落が無言で残らない。本受入条件は final の導出値のみを観測し、起動経路の中断有無は観測しない（中断有無は AC-13・AC-21 が観測する）。
- 検証方法見込み: `automated`

#### AC-25: 代替判定基準を判定軸とする場合は適合立証のための変更差分証跡が展開される

- Given: 判定軸区間の値 S が「代替判定基準」となる入力（表O11 の行1・行2）。base SHA から target SHA までの変更されたファイルが 1 件以上ある場合と、1 件も無い場合の双方を含む。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。
- Then: 判定対象区間に変更差分証跡が展開される。変更されたファイルが 1 件以上ある入力では、変更されたファイルの一覧と各ファイルの内容差分が展開され、レビュアが代替判定基準への適合を当該証跡に対して判定できる。変更されたファイルが 1 件も無い入力では、変更差分が空である旨が明示され、判定対象区間が無言の空欄にならない。展開量の上限や要約の具体的規則は設計セグメントで確定してよいが、変更されたファイルの一覧と、変更差分が空であるか否かは必ず展開される。本受入条件は判定対象区間のうち変更差分証跡の提示内容のみを観測し、必須成果物に関する提示内容と判定軸区間の内容は観測しない。
- 検証方法見込み: `automated`

#### AC-26: 判定を受ける側による代替判定基準の改変から approved へ到達しない

- Given: 判定軸区間の値 S が「代替判定基準」となる Issue において、当該基準の一次情報（GitHub モードは Issue 本文、ローカルモードは Issue 状態ファイル）が、判定を受ける側（セグメント作業ワーカーの実行主体）によって受入基準を緩める方向へ変更された状態（表O12 の行1）。変更が当該ゲートの `approved` 導出より前に行われた場合と、後に行われた場合の双方を含める。
- When: 当該ゲートおよびその下流ゲートの判定を行う。
- Then: 変更後の一次情報を代替判定基準として `approved` が導出されず、かつ変更前に導出済みの `approved` が変更後もそのまま有効として維持されない。この結果は次の (a)(b)(c) のうち少なくとも 1 つの手段によって達成される。(a) 防止: セグメント作業ワーカーの実行経路から当該一次情報を変更する操作が拒否され、変更自体が成立しない。(b) 検知: 当該変更が検知され、当該ゲートの final が `approved` とならず `human_required` が導出される。(c) 無効化: 当該変更が検知され、当該ゲートおよび全下流ゲートの承認が無効化されて再通過が要求される。どの手段を採るか、複数を組み合わせるかは設計セグメントの裁量とし、採用した手段に対応する自動テストで本 Then を検証する。3 手段のいずれも実装しない状態は本受入条件を満たさない。本受入条件は当該ゲートの承認の帰結のみを観測し、変更が無い入力における attempt 内の final 導出（AC-14・AC-15・AC-16）は観測しない。
- 検証方法見込み: `automated`

#### AC-27: 代替判定基準の一次情報が変更されていない場合は承認が維持される

- Given: 判定軸区間の値 S が「代替判定基準」であり、当該基準の一次情報が判定を受ける側によって変更されていない Issue（表O12 の行2。`approved` を導出した attempt の時点の実体と現時点の実体が一致する場合を含む）。
- When: 当該ゲートおよびその下流ゲートの判定を行う。
- Then: AC-26 が定める防止・検知・無効化のいずれの措置も発動せず、導出済みの `approved` が維持され、当該ゲートおよび下流ゲートの再通過は要求されない。一次情報が変更されていないにもかかわらず `human_required` へ降格することがなく、無条件の無効化によって quick 免除に従った Issue の通過経路が塞がれない。本受入条件は当該ゲートの承認の帰結のみを観測する。
- 検証方法見込み: `automated`

## 完了条件

- AC-1 から AC-27 のすべてが満たされ、対応する自動テストが成功する。
- 要件1・要件2・要件3・要件4・要件5・要件6・要件9・要件10・要件12・要件13・要件14 のそれぞれが、前掲の対応表のとおり少なくとも 1 つの受入条件と対応している。
- 表O1・表O2・表O3・表O4・表O8・表O9・表O10・表O11・表O12 のそれぞれについて、各行がちょうど 1 つの受入条件に対応し、除外行には除外理由が記載されている。
- AC-1 から AC-27 のすべてについて、GitHub モードとローカルモードの双方を入力とする自動テストが成功する。一方のモードのみで検証された受入条件が残っていない。
- 代替判定基準を判定軸とする経路について、要件13 が列挙する防止 (a)・検知 (b)・無効化 (c) のうち少なくとも 1 つの手段が実装され、AC-26 の自動テストが当該手段に対応する形で成功する。手段を 1 つも実装していない状態は完了とみなさない。あわせて AC-27 の自動テストが成功し、一次情報が変更されていない場合に承認が維持されることが確認されている。
- quick 免除が成立し判定軸が代替判定基準である入力で、判定対象区間に変更差分証跡が展開される（変更されたファイルが 1 件も無い場合は、その旨が明示される）。
- 既存のゲート判定の自動テストが回帰しない。とくに表O2 の行6（免除不成立かつ AC-ID を 1 件以上抽出できる既存主経路）の挙動が本変更の前後で変化しない。
- quick 免除が成立し、`SPEC.md` から AC-ID を 1 件以上抽出でき、かつ対象ゲートの必須成果物が免除により不在である入力（表O2 の行2）で、AC-ID 全件網羅を判定軸とする指示が出力されず、`approved` へ到達しうる判定軸（代替判定基準）が判定軸区間に実体として展開される。
- 常時必須の検証（lint / format、型検査、単体テスト、変更範囲の結合テスト、SAST、依存関係・secret スキャン）が成功する。本変更は利用者操作画面・API 境界・認証認可・性能ホットパス・DB migration・デプロイ運用・外部連携のいずれにも該当しないため、それらに紐づく追加検証は適用しない。

## 未決事項

- 代替判定基準の一次情報の改変に対して、要件13 が列挙する防止 (a)・検知 (b)・無効化 (c) のうちどの手段を採るか、複数を組み合わせるかは設計セグメントで確定する。設計へ委ねるのは手段の選択のみであり、少なくとも 1 つの手段を採ること自体は要件13・AC-26 が本仕様で確定済みである（いずれの手段も採らない設計は完了条件を満たさない）。一次情報の取得元を trusted base 側の起動経路に限ること（要件9、AC-17 が検証する）、および変更が無い場合に承認を維持すること（AC-27）も本仕様で確定済みとする。
- 代替判定基準として Issue 本文以外の入力（実装差分・テスト実行結果・CI 結果）をどこまで判定軸区間へ展開するかは設計セグメントで確定する。本仕様では、代替判定基準の実体を Issue 本文由来の記述に限り、ゲートごとに情報源を切り替えないことを確定済みとする。設計セグメントで入力を追加する場合も、全ゲートで同一の入力集合とし、ゲート別の分岐を導入しないこと。
- Issue 本文から「要求・期待する挙動・受入基準に相当する記述」を取り出す具体的な抽出規則（見出し一致・全文採用等）は設計セグメントで確定する。抽出結果が 0 件のとき展開不能として扱うこと（AC-6）は本仕様で確定済みとする。
- 判定プロンプト内で判定対象区間と判定軸区間を区切る具体的な表現（見出し文字列・区切りマーカーの形式）は設計セグメントで確定する。区切りが一意に識別でき、両区間が互いに重ならないこと（要件3）は本仕様で確定済みであり、AC-18 が区切りの具体形に依存しない形で検証する。
- Issue #729（反証ルーブリック）との着手・マージ順序の具体的な調整方法は進行役の判断で確定する。

## スコープ外

- quick 免除の対象そのものの見直し（憲法である `AGENTS.md` の改定）。
- 証跡照合機構。具体的には、判定プロンプト生成の決定性の保証、判定プロンプトの再生成による証跡照合、prompt digest の一致検査、およびそれらのために判定プロンプト生成時点の入力解決値をスナップショットとして確定・保存する機構をいう。本 Issue では扱わず、Issue #750 で扱う。本 Issue が確定させる判定プロンプトの区間分離（判定対象区間・判定軸区間）は Issue #750 の前提となる。要件13 が求める代替判定基準の一次情報の改変対策は本項に含めない。本項がスコープ外とするのは、判定プロンプト全体を対象とする決定性保証・再生成による照合・prompt digest の一致検査と、判定プロンプト生成に用いる全入力のスナップショット化であり、要件13 が扱うのは代替判定基準の一次情報という単一の入力に対する防止・検知・無効化に限られる。手段として当該一次情報の実体またはその digest を記録・照合する場合も、対象が当該単一入力に限られる限り本 Issue の範囲である。
- レビュアが判定プロンプトの明示指示に反する verdict を返した場合の検知・強制（表O8 の除外行）。
- falsification 側のルーブリックの停止条件・severity 基準・過去ラウンド反映（Issue #729）。
- 変更規模に応じた反証強度の調節（Issue #698）。
- strict attempt の実行独立性とレビュー履歴 pagination（Issue #732）。
- GitHub モードにおける I2 の自動 CI 強制の再導入。
