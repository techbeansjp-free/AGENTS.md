# SPEC: root成果物の削除がLLMワーカー1ラウンドを消費し、claude adapterでは完走できない

- Issue: `ISSUE-798`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/798-root-artifact-cleanup-role`

## 目的・背景

### 用語（本SPEC内での定義）

- **root成果物**: repository root 直下の `SPEC.md`・`DESIGN.md`・`PLAN.md`・`VALIDATION.md` の4ファイル。Issueセグメント成果物であり、既定ブランチの root 直下へ恒久残存させない。
- **本コマンド**: 本Issueで新設する決定的コマンドと、`.agent-skill-chain/scripts/` 配下に置くその薄いラッパー。具体的なサブコマンド名・ラッパーのファイル名は設計セグメントで確定する。
- **クリーンアップロール**: 本Issueで新設する、root成果物の削除だけを scope とする専用ロール。
- **対象worktree**: 指定された Issue に対応する worktree（1 Issue = 1 ブランチ = 1 worktree）。

### 前提とする現行動作（本SPEC内で自己完結させる事実）

1. マージ準備完了状態（draft ではない）の PR に対し、CI は repository root 直下に root成果物が残存していないことを検査し、残存していれば失敗する。したがって Issue ブランチは、PR を Ready へ移す前に root成果物を削除しておく必要がある。
2. 別途、既定ブランチへの push を契機として、既定ブランチの root 直下へ混入した root成果物を短命ブランチと機械生成PR経由で削除する既存の自動化が存在する。これは**既定ブランチの事後清掃**であり、Issue ブランチ上でのマージ前削除とは契機・対象ブランチ・実行主体が異なる。本Issueはこの既存自動化を置き換えない。
3. 成果物の内容は Git 履歴に残り、GitHub モードで Issue/PR 本文への転記が有効な場合は Issue/PR 本文にも保持される。したがって root成果物の削除は**配置の問題**であり、成果物の内容を失わせるものではない。
4. 現在この削除は、セグメント作業ワーカー（LLM）を1ラウンド起動することで行われている。実測所要は20〜30分である。削除するか否かは上記1の要求から一意に決まり、対象は固定4ファイル、内容の変更を伴わないため、ラウンド内に判断の余地は存在しない。
5. claude adapter のセグメント作業ワーカーは、許可コマンドを列挙し列挙外を拒否する方式で権限が決まる。この列挙にファイル削除系のコマンドが1つも含まれないため、claude adapter のワーカーは上記4のラウンドを構造的に完走できない（実測: `rm`・`git rm`・`find -delete`・`git update-index --force-remove` の全経路が拒否された）。一方 codex adapter のワーカーは sandbox 境界で権限が決まるため worktree 内の削除が可能である。すなわち**同一の「セグメント作業ワーカー」ロールの実効権限が実行系によって非対称**である。
6. `.agent-skill-chain/config/roles.yaml` には、ADR の status 更新のために scope を限定した専用ロールが既に定義されている（`lease: writer`、scope をライフサイクル項目のみに限定、capabilities を lease 操作と自ブランチへの commit/push に限定、forbidden に内容編集を明示）。同ロールには `roles:` 配下のロール定義と `role_contracts:` 配下の入出力契約の両方が置かれ、対応する決定的コマンドと `.agent-skill-chain/scripts/` 配下の薄いラッパーが存在する。進行役が起動するが、commit の主体は進行役ではなく当該ロールである。LLMラウンドを消費せず数秒で完了する。
7. claude adapter の許可コマンド列挙は `.agent-skill-chain/scripts/` 配下のスクリプト実行を許可する一方、`.agent-skill-chain/ci/` 配下の実行を許可していない。`ci/` 配下は成果物を変更しない read-only の機械検査であり、ワーカーが push 前に自ら実行できることが望ましいが、現状その手段がない。

### 欠陥

- **欠陥A**: 判断の余地がない固定操作に、LLMワーカー1ラウンド（20〜30分）を恒常的に消費している。
- **欠陥B**: 同一ロールの実効権限が実行系によって非対称であり、claude adapter のワーカーはマージ直前のラウンドを完走できない。この非対称性は、一方の実行系が利用不能になったときに初めて顕在化した。
- **欠陥C**: ワーカーが push 前に read-only の機械検査を自ら実行できず、検査失敗を PR 作成後まで発見できない。

## 要求 → 要件 → 受入条件

### 要求

root成果物の削除を、LLMワーカーのラウンドではなく、**scope を限定した専用ロールと、進行役が起動する決定的コマンド**によって実行できるようにする。既存の ADR status 更新ロールと同型の枠組みに収め、新しい枠組みを作らない。これにより、削除に要するLLMラウンドを消滅させ（欠陥A）、削除がどの実行系のワーカーの責務でもなくなることで実効権限の非対称も解消する（欠陥B）。あわせて、ワーカーが push 前に `.agent-skill-chain/ci/` 配下の read-only 検査を自ら実行できるようにする（欠陥C）。

削除は復元可能性を損なってはならない。Git から復元できない内容（未追跡ファイル・未commitの変更）を本コマンドが失わせないこと、および対象4ファイル以外へ一切波及しないことを、成功条件ではなく**停止条件**として担保する。

### 要件

- クリーンアップロールを `.agent-skill-chain/config/roles.yaml` へ定義する。writer lease を取る書込みロールであり、scope は root成果物の削除のみ、capabilities は lease 操作と自ブランチへの commit/push に限定し、forbidden に「対象4ファイル以外への変更」と「ファイル内容の編集」を明示する。既存の ADR status 更新ロールと同じく、ロール定義と入出力契約の両方を置く。
- 本コマンドを新設する。進行役が対象 Issue を指定して起動し、LLMの推論を一切介さずに決定的に完了する。`.agent-skill-chain/` 配下の他コマンドと同様、CLI 本体のサブコマンドと、`.agent-skill-chain/scripts/` 配下の薄いラッパーの2層で提供する。
- 本コマンドは成果物の**内容**を生成・編集する経路を持たない。入力は対象 Issue の識別子のみとし、ファイル内容・commit メッセージ本文・任意テキストを外部から受け取る引数や標準入力経路を設けない。これにより、進行役が本コマンドを経由して成果物を著述することが構造的に不可能であること（AGENTS.md の不変条件 I5）を担保する。
- **対象ファイルの状態区分**。対象4ファイルそれぞれについて worktree・index・HEAD の3か所を見て次のいずれか1つに分類する。3区分は相互排他かつ網羅的である。
  - **削除対象**: HEAD に存在し、worktree にも index にも HEAD と異なる内容を持たないもの。worktree 上に存在する場合と、起動時点で既に削除されている場合（未ステージの削除・ステージ済みの削除の双方）を含む。削除（不在）は「HEAD と異なる内容」に当たらない——削除によって失われる内容は無く、HEAD から復元できるためである。
  - **内容喪失リスクあり**: (a) HEAD に存在せず worktree または index に存在するもの（未追跡・新規ステージ済み）、または (b) HEAD に存在し、worktree または index の内容が HEAD と異なるもの（内容の変更）。いずれも Git から復元できない内容を持つ。
  - **不在**: worktree・index・HEAD のいずれにも存在しないもの。
- **判定順序**。上から順に評価し、最初に成立した1つだけを実行する。停止条件は常に no-op 判定より優先する。
  1. 後述の停止条件のいずれかが成立する場合は、commit を作らず非ゼロ終了する。
  2. 削除対象が1件以上ある場合は、それらを削除し、**削除のみで構成された commit** を作成して対象ブランチへ push する。結果の commit SHA を標準出力へ出し、終了コード0で終わる。
  3. 対象4ファイルがすべて不在の場合は、commit を作らず終了コード0で終わる（no-op）。進行役が到達判定を持たずに無条件で起動できるようにするためである。
- **終了コード0の事後条件**。終了コード0は、上記2・3のどちらの経路であっても、**対象ブランチの先頭 commit に root成果物が1件も含まれない状態が成立していること**を意味する。本コマンドの目的は削除の決定的完遂であり、成功が残存を隠蔽してはならない。この事後条件を満たせない状態は、no-op として成功を返さず、必ず停止条件として非ゼロ終了させる。
- 停止条件（いずれも commit を作らず非ゼロ終了し、日本語で原因と利用者が取るべき操作を示す）:
  - 対象4ファイル以外の変更が commit に含まれることになる場合。本コマンドは削除以外の変更を持ち込まない。
  - 内容喪失リスクありが1件以上ある場合。Git から復元できない内容を失わせないため、削除ではなく停止する。
  - 対象worktree が既定ブランチをチェックアウトしている場合。既定ブランチ root 直下の清掃は既存の別自動化の担当であり、既定ブランチへの直接 commit は 1 Issue = 1 ブランチ = 1 PR（AGENTS.md の不変条件 I4）に反する。
  - 対象 Issue の writer lease を他主体（セグメント作業ワーカー、または未解放の stale lease）が保持しており、本コマンドが lease を取得できない場合。AGENTS.md は1 Issue に同時1つの writer lease のみを許可する。待機も強制解放も行わず、保持者と失効時刻を診断に示して停止する。他主体の書込みと競合したまま commit を作らないためである。
- 対象4ファイル以外のパスに未ステージの変更が存在するだけの場合は、それを commit へ巻き込まず、実行後もその変更を worktree に保持したまま正常終了できる。
- claude adapter のセグメント作業ワーカーの許可コマンド列挙へ、`.agent-skill-chain/ci/` 配下のスクリプト実行を、`.agent-skill-chain/scripts/` 配下と同じ表記形式で追加する。
- 上記の追加と引き換えに、ファイル削除系のコマンドは許可コマンド列挙へ追加しない。無制限自動承認を既定にしない。列挙外は拒否という現行の安全側の設計を維持する。削除がワーカーの責務ではなくなったことと、その帰結として削除系コマンドを意図的に列挙しないことを、当該列挙の近傍に理由付きで記述する。
- 既存の既定ブランチ事後清掃自動化と、root 直下の残存を検査する既存の検査コマンドについて、外部から観測できる振る舞い（対象ファイル集合・契機・終了コード・出力仕様）を変更しない。
- 進行役の権限を拡大しない。進行役のロール定義から、成果物ブランチへの commit 禁止と成果物著述禁止を取り除かない。

### 受入条件（Acceptance Criteria）

#### AC-1: クリーンアップロールがロール定義と入出力契約の両方として存在する

- Given: `.agent-skill-chain/config/roles.yaml` が読める
- When: 内容を読む
- Then: クリーンアップロールが `roles:` 配下と `role_contracts:` 配下の両方に定義されている。`roles:` 側は writer lease の書込みロールであり、scope が root成果物の削除に限定され、capabilities が lease 操作と自ブランチへの commit/push だけで構成され、forbidden に「対象4ファイル以外への変更」と「ファイル内容の編集」が含まれる。`role_contracts:` 側は入力・出力・規約・完了条件・禁止事項を持ち、既存の ADR status 更新ロールと同じ構造をとる
- 検証方法見込み: `automated`

#### AC-2: 本コマンドが内容入力経路を持たない決定的コマンドとして提供される

- Given: CLI とその薄いラッパーが `.agent-skill-chain/scripts/` 配下に存在する
- When: 本コマンドの引数仕様と実装経路を読む
- Then: 受け取る入力は対象 Issue の識別子のみであり、ファイル内容・commit メッセージ本文・任意テキストを外部から与える引数も標準入力経路も存在しない。実装はLLMまたは対話エージェントの起動を含まず、同一入力に対して同一の動作を行う
- 検証方法見込み: `automated`

#### AC-3: 正常系で削除のみのcommitを作り、pushしてSHAを出力する

- Given: 対象worktree が既定ブランチ以外をチェックアウトしており、本コマンドが当該 Issue の writer lease を取得でき、root成果物のうち1件以上が「削除対象」であり、「内容喪失リスクあり」が1件も無く、index に対象4ファイル以外のステージ済み変更が無い
- When: 進行役が対象 Issue を指定して本コマンドを起動する
- Then: 削除対象の root成果物が HEAD から削除され、その削除だけで構成された commit（追加行・他パスへの変更を一切含まない）が対象ブランチ上に作られて push され、当該 commit の SHA が標準出力へ出力され、終了コード0で終わる。削除対象が起動時点で既に worktree から削除されていた場合（未ステージの削除・ステージ済みの削除）も同じ結果になり、no-op として終了してはならない
- 検証方法見込み: `automated`

#### AC-4: 対象外のステージ済み変更があるときはcommitせず停止する

- Given: 対象worktree の index に、対象4ファイル以外のパスに対するステージ済み変更が存在する
- When: 本コマンドを起動する
- Then: commit も push も行われず、非ゼロで終了し、対象外のパスが commit へ含まれることを理由として示す日本語の診断が標準エラー出力へ出る。worktree と index の状態は起動前から変化しない
- 検証方法見込み: `automated`

#### AC-5: 対象ファイルがどこにも存在しない場合のみcommitを作らず成功する

- Given: 対象4ファイルがすべて「不在」である。すなわち worktree・index・HEAD のいずれにも1件も存在しない
- When: 本コマンドを起動する
- Then: 新たな commit は作られず、push も行われず、no-op であることを示す日本語の出力とともに終了コード0で終わる。ブランチの先頭 commit は起動前から変化しない
- Then（境界の明示）: root 直下（worktree 上）に対象ファイルが1件も無くとも HEAD に1件以上存在する状態は本ACの Given に該当しない。この状態は AC-3 の削除経路をとり、no-op として終了コード0を返してはならない。終了コード0が残存を隠蔽することを禁じる「終了コード0の事後条件」による
- 検証方法見込み: `automated`

#### AC-6: 内容喪失リスクありの対象ファイルがあるときは削除せず停止する

- Given: 対象4ファイルのうち少なくとも1件が「内容喪失リスクあり」である。すなわち HEAD に存在せず worktree または index に存在する（未追跡・新規ステージ済み）か、HEAD に存在するが worktree または index の内容が HEAD と異なる（内容の変更）
- When: 本コマンドを起動する
- Then: いずれの対象ファイルも削除されず、commit も push も行われず、非ゼロで終了する。診断は日本語で、Git から復元できない内容を失わせないために停止したことと、該当ファイル名を示す
- Then（境界の明示）: 追跡済みファイルの未ステージ削除・ステージ済み削除は本ACの Given に該当しない。削除によって失われる内容は無く HEAD から復元できるため、当該ファイルは「削除対象」として AC-3 の経路で処理する。本停止条件は AC-5 の no-op 判定に優先する
- 検証方法見込み: `automated`

#### AC-7: 対象外パスの未ステージ変更を巻き込まず、実行後も保持する

- Given: AC-3 の前提に加えて、対象4ファイル以外のパスに未ステージの変更または未追跡ファイルが存在する
- When: 本コマンドを起動する
- Then: 作られる commit は対象4ファイルの削除のみで構成され、対象外パスの未ステージ変更・未追跡ファイルは commit へ含まれず、実行後も worktree 上に起動前と同じ内容で残る
- 検証方法見込み: `automated`

#### AC-8: 実行文脈の前提が満たされない場合は実行を拒否する

- Given: 次のいずれかが成立する。(a) 対象worktree が既定ブランチをチェックアウトしている。(b) 対象 Issue の writer lease を他主体が保持しており、本コマンドが lease を取得できない
- When: 本コマンドを起動する
- Then: (a)(b) いずれの場合も削除・commit・push のいずれも行われず、非ゼロで終了する。(a) の診断は日本語で、既定ブランチ root 直下の清掃が既存の別自動化の担当であること、および既定ブランチへ直接 commit しないことを理由として示す。(b) の診断は日本語で、lease の現在の保持者と失効時刻を示し、待機も強制解放も行わずに停止したことを示す
- 検証方法見込み: `automated`

#### AC-9: 終了コード0は経路によらずroot残存検査を満たす

- Given: 本コマンドが終了コード0で終了した対象worktree。AC-3 の削除経路と AC-5 の no-op 経路の双方を含む
- When: root 直下の残存を検査する既存の検査コマンドを、当該worktree の先頭 commit に対して実行する
- Then: 経路によらず当該検査コマンドが終了コード0を返す。すなわち本コマンドの終了コード0は、マージ準備完了状態の PR に課される root 残存検査を満たす状態が成立していることを意味し、残存したまま成功を返す経路が存在しない
- 検証方法見込み: `automated`

#### AC-10: adapterの許可コマンド列挙にci/実行が加わり、削除系は加わらない

- Given: claude adapter の許可コマンド列挙の定義が読める
- When: 内容を読む
- Then: `.agent-skill-chain/ci/` 配下のスクリプト実行が、`.agent-skill-chain/scripts/` 配下と同じ表記形式で許可されている。ファイル削除系のコマンド（`rm`・`git rm`・`find` の削除実行・index からの強制削除を含む）はいずれも許可されていない。無制限自動承認を既定とする指定は含まれない。削除がワーカーの責務ではなくなったこと、およびその帰結として削除系を意図的に列挙しないことが、当該列挙の近傍に理由として記述されている
- 検証方法見込み: `automated`

#### AC-11: 既存の清掃自動化と残存検査の外部挙動が変わらない

- Given: 既定ブランチの事後清掃自動化と、root 直下の残存を検査する既存の検査コマンドが、本Issue着手前の振る舞いで動作していた
- When: 本Issueの変更を適用したうえで、両者を同じ入力で実行する
- Then: 対象ファイル集合・起動契機・終了コード・標準出力および標準エラー出力の仕様が、本Issue着手前と同一である。両者の既存の自動テストは、期待値を変更することなく成功する
- 検証方法見込み: `automated`

#### AC-12: 進行役の権限が拡大していない

- Given: `.agent-skill-chain/config/roles.yaml` の進行役のロール定義と、クリーンアップロールの定義が読める
- When: 両者を読む
- Then: 進行役の forbidden から成果物ブランチへの commit 禁止と成果物著述禁止のいずれも取り除かれていない。クリーンアップロールの capabilities に成果物の著述・内容編集に相当する能力が含まれない。本Issueの変更は、進行役に成果物ブランチへ commit する能力を与えていない
- 検証方法見込み: `automated`

## スコープ外

- 進行役自身へ成果物ブランチへの commit 権限を与えること。AGENTS.md の不変条件 I5 は進行役が調整状態のみを読み書きすると定め、役割表は進行役の成果物ブランチへの commit を禁じている。進行役が成果物を書けるようになると、生成と判定を同一主体が担い、ゲートの独立性の根拠が失われる。I5 はプロジェクトポリシーでも上書きできない。
- claude adapter の許可コマンド列挙へ削除系コマンドを追加すること。当初の第1候補であったが、対象ファイルを限定できず、実行系間の実効権限の非対称も解消しないため採らない。
- 無制限自動承認をワーカー起動の既定にすること。
- codex adapter の sandbox 境界そのものの変更。
- 既定ブランチの事後清掃自動化の置き換え・統合。本Issueが新設するのは Issue ブランチ上のマージ前削除であり、契機・対象ブランチ・実行主体が異なる別機構である。
- 4セグメント・4ゲートの構成変更、および root成果物のファイル名・配置そのものの変更。
- root成果物を root 直下ではない場所へ生成するようにワーカーの出力先を変更すること。本Issueは削除の実行主体と手段だけを変える。
