# SPEC: 隔離環境で起動されたゲートレビュアが不当に human_required へ倒れる

- Issue: `ISSUE-691`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/691-reviewer-claude-cli-auth`

## 目的・背景

ゲートレビュア（read-only の独立レビュア）は、進行役が起動するランチャから、呼び出し元の環境変数を継承しない最小環境の子プロセスとして起動される。この隔離は、レビュアへ GitHub 資格情報・呼び出し元の git 設定・呼び出し元 HOME 全体を渡さないための安全側の設計であり、本Issueはこの隔離自体を廃止しない。

一方で、この隔離レビュア起動の周辺に、レビュアが実質的な判定を行えないまま当該ゲートが人間判断へ倒れる（`human_required` になる）事象が2種類、それぞれ独立した原因で観測されている。

第一に、呼び出し元の通常環境ではレビュア実行系（model provider の CLI）が非対話で認証済みとして動作するにもかかわらず、隔離環境で起動した同じ実行系が未ログイン状態を報告して失敗し続ける実行環境が存在する。この場合、起動は既定の再試行回数だけ繰り返されたうえで失敗し、フェイルセーフが `human_required` を書く。出力される診断は終了コードと試行回数だけであり、原因が認証であることを示さない。子プロセスの標準エラー出力は破棄されるため、実行系が返した未ログインの旨も残らない。結果として、利用者は「再試行回数を増やす」などの無効な対処へ誘導される。

第二に、隔離レビュア起動が成功して verdict が得られる実行環境においても、判定入力に実際に含まれている入力（上流の承認済み成果物の全文、および適用対象の受入条件見出し群）について、レビュアが「一切展開されていない」と報告し、それを理由に立証観点を pending・判定不能として返す事象が複数回発生している。この事象は、判定プロンプトの生成物を実測して当該入力が含まれることを確認したうえでも再現しており、生成から配送までの経路で入力が切り詰められている事実は確認されていない。この場合もゲートは人間判断へ倒れるが、その原因は成果物の欠陥ではなく、判定入力の提示方法とレビュアの受け取り方の側にある。

いずれも「安全側へ倒す」挙動そのものは正しく機能している。本Issueが解消するのは、レビュアが実質的な判定を行える条件が整っているにもかかわらず倒れる、不当な `human_required` の2経路である。真に認証が無い場合・真に判定入力が欠けている場合に人間判断へ倒れる挙動は、本Issueでも維持する。

## 対象範囲

本SPECが仕様を定める対象は次の3つに限る。

- ゲートレビュアを隔離環境で起動する処理と、その前段の認証可否判定・失敗時の診断・再試行の挙動。
- 隔離レビュア起動時に子プロセスへ渡す環境の内容（provider 資格情報の解決に必要なものと、隔離のため渡さないもの）。
- ゲートレビュアへ渡す判定入力の提示方法、配送の同一性確認、および「埋め込み済み入力の欠落」を主張する verdict の扱い。

上記の変更が既存の安全側挙動（真の未認証・実行系不在・未登録アダプタ・能力証明不成立・verdict 空・結線失敗・timeout における `human_required`）を損なわないことも本SPECの対象に含む。対象外は本SPEC末尾の「スコープ外」に列挙する。

## 前提

本SPECの要件・受入条件は、次の前提が成立する環境で評価する。

- ゲートレビュアは書込みツールを一切持たない read-only として起動され、判定に必要な情報はすべて判定入力として渡される。この read-only 契約は本Issueで変更しない。
- 隔離レビュア起動の実装は、CLI を実行系とする複数のアダプタ（claude・codex）で共有されている。codex アダプタは同じ起動ライフサイクルを取り込み、認証判定のみを差し替えている。
- 現行の隔離レビュア起動は、呼び出し元の環境変数を継承せず、PATH・一時 HOME・一時設定ディレクトリ・空の GitHub CLI 設定ディレクトリ・無効化した git の global/system 設定・一時ディレクトリ・ロケール・隔離ルートのパスを渡す。加えて、存在する場合に限り呼び出し元利用者の provider 設定ディレクトリのパスと、呼び出し元に設定されている provider トークンの環境変数を渡す。
- 現行の認証可否判定は、隔離環境ではなく呼び出し元の環境で実行される。したがって呼び出し元でのみ成立する認証状態を根拠に起動へ進みうる。
- 現行のレビュア起動は、失敗時に既定で3回まで再試行し、再試行の間に既定30秒待機する。子プロセスの標準エラー出力は破棄される。
- provider 資格情報の保存形態には、少なくとも次の3種が存在する。(a) 呼び出し元プロセスの環境変数として与えられるトークン、(b) 呼び出し元利用者の provider 設定ディレクトリ配下のファイル、(c) OS が提供する資格情報ストア（ファイル・環境変数のいずれでもない保管先）。
- 判定入力には、ハルシネーション防止のため「判定入力に展開されていないファイルの内容を推測してはならず、判定に不可欠ならば当該観点を pending とし判定不能として返す」旨の指示が含まれる。
- 判定入力には、判定対象の差分・判定対象の成果物・適用対象の受入条件一覧が含まれ、要求・要件の上流ゲート以外では上流の承認済み成果物の全文も含まれる。
- provider 資格情報を CI 実行環境・リモートへ移送しないことは、本リポジトリに登録済みのプロジェクトポリシーが定める制約であり、本Issueはこれを緩めない。
- レビュア以外の実行主体（セグメント作業ワーカー）は隔離レビュア起動を用いず、呼び出し元環境で起動される。

## 用語

本SPEC内で用いる語を次の意味に固定する。

- **隔離レビュア起動**: ゲートレビュアを、呼び出し元の環境変数を継承しない最小環境（呼び出し元 HOME とは別の一時 HOME を含む）の子プロセスとして起動する処理。
- **呼び出し元環境**: 隔離レビュア起動を行うランチャ自身のプロセス環境。
- **provider 資格情報**: レビュア実行系が model provider へ認証するために用いる秘密情報。保存形態は「前提」に挙げた3種を想定する。
- **判定入力**: レビュアプロセスの標準入力へ渡される、判定プロンプトの文字列全体。
- **埋め込み済み入力**: 判定入力の中に文字列として実際に含まれている入力。
- **非埋め込み入力**: 成果物本文等から名指しで言及されているが、判定入力の中に文字列として含まれていない入力。
- **誤欠落報告**: 埋め込み済み入力について「展開されていない」「内容が不明で検証できない」ことを理由に、立証または反証の観点を pending とし判定不能を真として返す verdict。
- **正当な人間判断への降格**: 真に provider 資格情報が無い、レビュア実行系が不在、未登録アダプタ、コアレビューの能力証明が成立しない、verdict が空、verdict の記録に失敗した、timeout した、あるいは判定に不可欠な入力が真に非埋め込みである場合に `human_required` となること。
- **不当な人間判断への降格**: 呼び出し元環境ではレビュア実行系が非対話で認証済みとして動作するのに隔離レビュア起動だけが認証で失敗した場合、および誤欠落報告を根拠として `human_required` となること。
- **配送同一性**: レビュアプロセスが実際に受け取った判定入力が、生成された判定入力と1バイトも相違しない状態。

## 要求 → 要件 → 受入条件

### 要求

ゲートレビューを起動する利用者が、レビュア実行系へ既にローカルでログイン済みの環境において、隔離レビュア起動を経由しても実質的な判定結果（立証・反証の判定と指摘）を得られること。認証が成立しない環境では、原因が認証であると分かる診断を得たうえで安全側へ倒れること。また、判定入力に実際に含まれている入力を「存在しない」とみなした判定が、成果物の欠陥として記録されたりゲートの停止理由になったりしないこと。

### 要件

認証と隔離に関する要件:

- 呼び出し元環境でレビュア実行系が非対話・認証済みとして動作する場合、隔離レビュア起動においても認証が成立し、レビュアが判定を実行できること。provider 資格情報の保存形態が「前提」に挙げた3種のいずれであっても成立させること。ただし、隔離を維持したままでは解決できない保存形態が残る場合は、後述の「残存制約の明示」の要件を満たすことで代替する。
- 認証可否の事前判定は、実際にレビュアを起動する環境と同一の認証解決条件で行うこと。呼び出し元環境でのみ成立する判定を根拠に起動へ進み、起動して初めて認証不成立が判明する状態を残さないこと。
- 認証不成立が原因でレビュアが判定に到達できない場合、原因が provider 認証であることを名指しした日本語メッセージを標準エラー出力へ出力し、`human_required` へ倒すこと。終了コードと試行回数のみを示す汎用の起動失敗メッセージで終えないこと。診断には、資格情報の実値を含めないこと。
- 認証不成立のように再試行しても状態が変わらない失敗については、残りの再試行回数と再試行間の待機を消費せずに停止すること。一時的な失敗（実行系の一過性エラー・通信断等）に対する再試行は維持してよい。
- 隔離境界を維持すること。レビュアプロセスへ、GitHub 資格情報・GitHub CLI の呼び出し元設定・呼び出し元の git の global/system 設定・呼び出し元 HOME 全体・Issue worktree が生成した実行コードを渡さない。provider 資格情報は隔離されたレビュアプロセスの環境内に閉じ、標準出力・標準エラー出力・gate-report・PR review 本文・その他の永続記録へ実値を出さない。CI 実行環境・リモートへ移送しない。
- **残存制約の明示**: 隔離を維持したままでは provider 資格情報を解決できない保存形態・プラットフォーム構成が残る場合、その条件、利用者が自分の環境が該当するかを判定する方法、および利用者が取りうる回避手段を、配布物に含まれる利用者可読な記述として残すこと。
- 真に provider 資格情報が無い構成では、従来どおり `human_required` へ倒すこと。承認側へ倒さないこと。

判定入力に関する要件:

- レビュアプロセスが実際に受け取った判定入力について配送同一性を機械的に確認できること。相違を検出した場合は、その事実を診断として残し、当該 verdict を成果物起因の判定として記録しないこと。
- 判定入力は、どの入力が埋め込み済みかをレビュアが取り違えられない形で提示すること。具体的には、埋め込み済み入力の一覧と、レビュア自身が本文と照合できる件数情報（適用対象の受入条件の件数を含む）を判定入力内に含めること。ハルシネーション防止の指示が非埋め込み入力にのみ適用されることを、判定入力の記述から一意に読み取れるようにすること。
- 誤欠落報告を機械的に検出できること。検出時は、当該 verdict を成果物起因の判定として記録せず、承認側へ倒さず、原因が誤欠落報告であることを名指しした診断を残すこと。人間判断へ倒す場合も、成果物の欠陥を理由とする降格と区別できる形で記録すること。
- 非埋め込み入力を理由とする pending・判定不能は、従来どおり正当な判定として尊重し、誤欠落報告として扱わないこと。ハルシネーション防止の目的（未提示ファイルの内容を推測した証跡の創作を禁じること）を損なわないこと。
- 既存の安全側挙動を維持すること。レビュア実行系の不在・未登録アダプタ・コアレビューの能力証明不成立・verdict 空・verdict 記録の失敗・timeout は、いずれも承認側へ倒さないこと。
- 隔離レビュア起動を共有する codex アダプタ経由のレビュア起動が、本Issueの変更によって回帰しないこと。

### 受入条件（Acceptance Criteria）

#### AC-1: 環境変数トークンのみで認証される構成で隔離レビュアが判定を完了する

- Given: provider トークンが呼び出し元環境の環境変数としてのみ与えられ、provider 設定ディレクトリにも資格情報が無い環境。レビュア実行系は、受け取った環境から provider 資格情報を解決できたときだけ有効な verdict を返すスタブへ差し替えてよい
- When: 進行役がゲートレビュアの起動を実行する
- Then: レビュアプロセスが provider 資格情報を解決でき、verdict が得られてゲートの判定として記録される。認証を理由とする失敗もフェイルセーフによる `human_required` も発生しない
- 検証方法見込み: `automated`

#### AC-2: 設定ディレクトリ配下のログイン情報のみで認証される構成で隔離レビュアが判定を完了する

- Given: 呼び出し元環境に provider トークンの環境変数が無く、呼び出し元利用者の provider 設定ディレクトリ配下のファイルにのみログイン情報がある環境
- When: 進行役がゲートレビュアの起動を実行する
- Then: レビュアプロセスが当該設定を参照して provider 資格情報を解決でき、verdict が得られてゲートの判定として記録される。認証を理由とする失敗もフェイルセーフによる `human_required` も発生しない
- 検証方法見込み: `automated`

#### AC-3: ファイル・環境変数以外にのみ資格情報がある構成で判定が完了するか、残存制約が明示される

- Given: provider トークンの環境変数が無く、provider 設定ディレクトリ配下のファイルにも資格情報が無く、OS が提供する資格情報ストアにのみ資格情報が存在する実環境。同一端末の呼び出し元環境では、レビュア実行系が非対話で認証済みとして動作することを事前に確認済みとする
- When: 進行役がゲートレビュアの起動を実行する
- Then: 次のいずれかが成立する。(a) 隔離レビュア起動でも認証が成立し、verdict が得られてゲートの判定として記録される。(b) 認証が成立しない場合は、原因が provider 認証であることを名指しした日本語メッセージが標準エラー出力へ出力されたうえで `human_required` となり、かつ当該構成が解決できない条件・自環境が該当するかの判定方法・回避手段が、配布物に含まれる利用者可読な記述として存在する。(a)(b) のいずれでもない挙動（原因不明の汎用起動失敗のみで倒れる、承認側へ倒れる）は不合格とする
- 検証方法見込み: `hybrid`
- 自動化可能性の根拠: 資格情報ストアの状態を持つ実環境の再現には実機準備を要するため実行自体は手動確認とし、(b) が要求する記述の存在は配布物に対する機械検査で確認する

#### AC-4: 真に認証が無い構成では安全側へ倒れる

- Given: provider トークンの環境変数が無く、provider 設定ディレクトリにも資格情報が無く、資格情報ストアにもログイン情報が無い環境
- When: 進行役がゲートレビュアの起動を実行する
- Then: gate-report の最終状態が `human_required` となり、承認側の状態にはならない。レビュアの verdict を捏造して記録することもしない
- 検証方法見込み: `automated`

#### AC-5: 認証不成立の診断が原因を名指しし、資格情報の実値を含まない

- Given: 隔離レビュア起動が provider 認証の不成立によって失敗する構成
- When: 進行役がゲートレビュアの起動を実行し、標準出力・標準エラー出力・gate-report・投稿された証跡本文を収集する
- Then: 標準エラー出力に、原因が provider 認証であることを名指しした日本語メッセージが含まれる。終了コードと試行回数のみを示す汎用の起動失敗メッセージだけで終わらない。収集したいずれの出力にも、provider トークン・ログイン情報の実値が現れない
- 検証方法見込み: `automated`

#### AC-6: 再試行で解消しない認証不成立では再試行と待機を消費しない

- Given: 隔離レビュア起動が provider 認証の不成立によって決定論的に失敗する構成と、再試行回数・再試行間隔を明示的に設定した実行
- When: 進行役がゲートレビュアの起動を実行し、レビュア実行系の起動回数と所要時間を記録する
- Then: レビュア実行系の起動が繰り返されず、再試行間隔に相当する待機も発生しないまま、原因を名指しした診断とともに `human_required` へ倒れる。同一環境で一時的な失敗（実行系が非決定的に失敗する構成）を与えた場合には、従来どおり設定された回数の再試行が行われる
- 検証方法見込み: `automated`

#### AC-7: 隔離境界が維持されている

- Given: GitHub 資格情報・GitHub CLI 設定・git の global/system 設定・呼び出し元 HOME 配下の任意ファイルが存在する呼び出し元環境。レビュア実行系は、自プロセスが受け取った環境と参照可能な設定を記録するスタブへ差し替えてよい
- When: 進行役がゲートレビュアの起動を実行し、レビュアプロセスが受け取った環境を検査する
- Then: レビュアプロセスの環境に GitHub 資格情報・GitHub CLI の呼び出し元設定・呼び出し元の git の global/system 設定・呼び出し元 HOME そのもの・Issue worktree が生成した実行コードのいずれも含まれない。provider 資格情報はレビュアプロセスの環境内に限って解決可能であり、永続記録へ出力されない
- 検証方法見込み: `automated`

#### AC-8: 配送同一性を機械的に確認でき、相違を検出できる

- Given: 判定入力を生成できるゲートと、受け取った標準入力の全体を照合可能な形で報告するスタブのレビュア実行系
- When: 進行役がゲートレビュアの起動を実行し、生成された判定入力とレビュアプロセスが受け取った判定入力を比較する
- Then: 両者が一致することを機械的に確認できる。両者が相違する状況を人為的に作った場合には相違が検出され、その事実が診断として残り、当該実行の verdict が成果物起因の判定として記録されない
- 検証方法見込み: `automated`

#### AC-9: 判定入力が埋め込み済み入力を取り違えられない形で提示される

- Given: 上流の承認済み成果物の全文と、複数件の受入条件見出しを含む成果物を対象とするゲート
- When: 当該ゲートの判定入力を生成し、その文字列を検査する
- Then: 判定入力に、埋め込み済み入力の一覧と、適用対象の受入条件の件数を含む照合可能な件数情報が含まれる。ハルシネーション防止の指示が非埋め込み入力にのみ適用されることが判定入力の記述から一意に読み取れる。上流の承認済み成果物の全文と受入条件見出し群が判定入力へ実際に含まれている
- 検証方法見込み: `automated`

#### AC-10: 誤欠落報告が検出され、成果物起因の判定として記録されない

- Given: 上流の承認済み成果物の全文と受入条件見出し群を含む判定入力と、それらが「一切展開されていない」ことを理由に立証観点を pending・判定不能として返すスタブの verdict
- When: 進行役がゲートレビュアの起動を実行する
- Then: 当該 verdict が誤欠落報告として検出され、成果物起因の判定として記録されない。承認側の状態にもならない。原因が誤欠落報告であることを名指しした診断が残り、成果物の欠陥を理由とする人間判断への降格と区別できる
- 検証方法見込み: `automated`

#### AC-11: 非埋め込み入力を理由とする判定不能は従来どおり尊重される

- Given: 成果物本文が名指しするファイルが判定入力に含まれておらず、その内容が判定に不可欠である状況と、当該ファイルが未提示であるため検証不能として立証観点を pending・判定不能とするスタブの verdict
- When: 進行役がゲートレビュアの起動を実行する
- Then: 当該 verdict は誤欠落報告として扱われず、正当な判定不能として記録され、承認側へは倒れない。ハルシネーション防止の指示が判定入力から失われていない
- 検証方法見込み: `automated`

#### AC-12: 他アダプタ経由のレビュア起動が回帰しない

- Given: 隔離レビュア起動のライフサイクルを共有する codex アダプタを選択した構成
- When: 認証が成立する構成と成立しない構成のそれぞれで、進行役がゲートレビュアの起動を実行する
- Then: 認証が成立する構成では verdict が得られてゲートの判定として記録され、成立しない構成では原因を名指しした診断とともに `human_required` へ倒れる。いずれの構成でも承認側へは倒れず、変更前に成立していた codex 固有の認証判定・sandbox 指定・model 指定の挙動が変わらない
- 検証方法見込み: `automated`

本SPECが定める受入条件は、本節に見出しとして列挙したものが全てである。

## 未決事項

次は設計セグメント（`DESIGN.md`）で確定させる。いずれも受入条件を満たす複数の実装形態のうちどれを採るかの選択であり、受入条件の判定基準そのものを設計側で変更することは認めない。

- 隔離を維持したまま資格情報ストア由来の provider 資格情報を解決させる手段。制約は、隔離境界の維持（GitHub 資格情報・呼び出し元 git 設定・呼び出し元 HOME 全体を渡さない）、資格情報の実値を永続記録へ出さないこと、CI へ移送しないこと。解決不能と結論する場合は残存制約の明示で代替する。
- 認証可否の事前判定を、実際の起動と同一の環境条件で行う方法（隔離環境内での軽量な確認か、起動失敗の分類による判定か）。制約は、判定のためにモデル呼び出しの課金・長時間の待機を新たに発生させないこと、および資格情報の実値を出力しないこと。
- 再試行で解消しない失敗と一時的な失敗を切り分ける方法。制約は、切り分けに失敗した場合でも承認側へ倒さないこと。
- 隔離レビュア起動時に破棄されている子プロセスの診断出力を、資格情報を漏らさずに原因分類へ利用する方法。
- 配送同一性の確認手段（レビュア側からの報告に依存するか、ランチャ側の記録で完結させるか）と、その記録の保持範囲。
- 埋め込み済み入力の一覧・件数情報の提示形式と、誤欠落報告を検出する手段（verdict に自己申告の照合フィールドを設けるか、返却された指摘内容と判定入力の突き合わせで判定するか）。制約は、正当な非埋め込み入力起因の判定不能を誤検出しないこと。
- 誤欠落報告を検出した場合に、再実行を試みるか直ちに人間判断へ倒すか。制約は、承認側へ倒さないこと、成果物の欠陥を理由とする降格と区別可能な診断を残すこと、および無限の再実行を行わないこと。

## スコープ外

- provider 資格情報を CI 実行環境・GitHub Actions・リモートへ移送する仕組みの新設。登録済みのプロジェクトポリシーが provider 資格情報を CI へ移送しないことを定めており、本Issueはこの制約を緩めない。
- ゲートレビュアの read-only 契約の変更。判定入力の不足をレビュア自身のファイル読取り・ネットワーク取得で補わせる方式は、レビュアに書込み不能なツールを与える場合であっても本Issueでは扱わない。
- 判定入力へ埋め込む対象範囲そのものの拡大・縮小（どの成果物・どの差分・どの上流成果物を埋め込むか）。本Issueは、現に埋め込まれている入力が正しく認識されることだけを扱う。
- 適用対象の受入条件一覧を成果物本文から抽出する際に、見出し宣言に限定せず同形の文字列一致で抽出している点の是正。これは判定入力の生成側にある別の欠陥であり、原因も修正対象も本Issueと異なるため別Issueとして扱う。本SPECでは、当該欠陥を誘発しうる同形文字列を本文へ書かないことで回避する。
- レビュープロファイルの選択規則、ゲート判定ロジック、Check Run・PR review 証跡の発行経路、コアレビューの能力証明の要件そのものの変更。
- provider へのログイン手順・トークン発行方法・利用者の認証操作の変更。本Issueは既にログイン済みの環境を前提とし、認証そのものの方式には介入しない。
- セグメント作業ワーカーの起動における認証・隔離の扱い。ワーカーは隔離レビュア起動を用いず呼び出し元環境で起動されるため、本Issueの原因も対象コンポーネントも共有しない。
- 人間アダプタを選択した場合の非同期レビュー経路の挙動。当該経路は provider 認証も判定入力の自動配送も行わないため、本Issueの2経路のいずれにも該当しない。
