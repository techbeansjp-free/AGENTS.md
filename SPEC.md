# SPEC: codexゲートレビュアが隔離環境で起動できない（sanitized PATHがHOME配下を除外しshebangのnodeを解決できない）

- Issue: `ISSUE-727`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/727-codex-reviewer-interpreter-path`

## 目的・背景

ゲートレビュアは、呼び出し元の環境を持ち込まない隔離環境（空の環境変数集合・固定PATH・隔離HOME・隔離workspace）で起動される。この隔離は、呼び出し元HOME配下やIssue worktree配下の任意コードをレビュア実行系として実行しないという信頼境界を守るために導入された。

しかし固定PATHは、実行系CLIの実体ディレクトリが呼び出し元HOME配下またはリポジトリ配下にある場合、そのディレクトリをPATHへ加えない。実行系CLI自体は絶対パスで起動されるため除外されても起動できるが、その実行ファイルがインタプリタを要求するスクリプト（例: 先頭行が `#!/usr/bin/env node`）である場合、インタプリタはPATH経由でしか解決されず、インタプリタが同じ除外対象ディレクトリにあると起動そのものが失敗する。「実行ファイルの絶対パスは信頼するが、そのインタプリタはPATHから探す」という非対称が本欠陥である。

さらに、この起動失敗は認証probeの実行時に最初に顕在化するため、レビュアは「認証情報が見つからない／認証probeに失敗した」と診断してフェイルセーフで `human_required` へ倒れる。認証情報が実在していても同じ診断が出るため、利用者は再ログイン等の無関係な対処へ誘導される。

実測された害は次の3点である。

1. codexアダプタのゲートレビュアが一切起動できず、`human_required` で停止して完全自走が成立しない。本リポジトリ自身の変更はほぼ全てがコアレビュー対象であり、その既定レビュアがcodexであるため影響は恒常的である。
2. 診断が根本原因（インタプリタ解決失敗）ではなく認証失敗を指し、原因究明を妨げる。
3. 信頼境界ハードニング導入前は同一環境でcodexレビュアが繰り返し完走していたため、回帰である。

ネイティブ実行ファイル（ELF）として配布されるclaudeアダプタは同じ隔離環境で動作するため、本欠陥はcodex固有ではなく、インタプリタを必要とする実行ファイル全般に効く。

## 要求 → 要件 → 受入条件

### 要求

隔離環境の信頼境界（呼び出し元HOME配下・Issue worktree配下の任意コードをレビュア実行系として実行しないこと）を緩めずに、正当に導入された実行系CLIが、それがスクリプト形式であってもゲートレビュアとして起動でき、verdictを返せること。あわせて、起動できない場合の診断が実際の原因を指すこと。

### 要件

- 隔離環境で起動する実行系CLIがインタプリタを必要とするスクリプトである場合、そのインタプリタも実行ファイル本体と同じ信頼判断で解決可能とし、実行ファイル本体だけを信頼して起動する現在の非対称を解消する。
- インタプリタの解決可能化は、Issue worktree（作業中ブランチが変更しうる領域）配下の実行ファイル・インタプリタを起動可能にしてはならない。
- 実行ファイルまたはそのインタプリタを解決できずに起動が失敗した場合、認証失敗ではなく起動失敗として診断する。診断は日本語で、解決できなかった対象名と、隔離環境へ渡した固定PATHの内容と、利用者が取るべき操作を区別して示す。
- 認証情報が実際に不在または不正な場合の診断は従来どおり認証失敗として提示され、上記の起動失敗診断と区別できる。
- 診断・ログ・標準出力・標準エラーに認証情報の実値を含めない。
- 上記をスクリプト形式・ネイティブ形式の双方の実行ファイルについて自動テストで検証する。
- 現在正常に動作しているネイティブ実行ファイル形式のレビュア起動経路を退行させない。

### 受入条件（Acceptance Criteria）

#### AC-1: スクリプト形式CLIのゲートレビュアが隔離環境で起動しverdictを返す

- Given: ゲートレビュアの実行系CLIが `#!/usr/bin/env node` 形式のスクリプトであり、その `node` インタプリタが呼び出し元HOME配下（バージョンマネージャ配下等）にのみ存在し、有効な認証情報が用意されている
- When: 当該アダプタでゲートレビュアを起動する
- Then: 隔離環境内で実行系CLIが起動し、認証probeが成功し、レビュアがverdictを返す。認証情報不在を理由とする `human_required` へ倒れない
- 検証方法見込み: `automated`

#### AC-2: 起動に用いる実行ファイル・インタプリタがIssue worktree配下でない

- Given: AC-1 と同じ環境で、Issue worktree配下に実行系CLIまたはインタプリタと同名の実行ファイルが存在する
- When: ゲートレビュアを起動する
- Then: 隔離環境が用いる実行ファイル・インタプリタはいずれもIssue worktree配下のものではなく、Issue worktree配下の実行ファイルは起動されない
- 検証方法見込み: `automated`

#### AC-3: 解決不能時は起動失敗として診断される

- Given: 実行系CLIの実行ファイル、またはそのインタプリタを隔離環境で解決できない
- When: ゲートレビュアを起動する
- Then: 認証失敗ではなく起動失敗として診断され、診断文には解決できなかった対象名と隔離環境へ渡した固定PATHの内容と利用者が取るべき操作が日本語で区別して示される。認証情報の実値は診断へ含まれない
- 検証方法見込み: `automated`

#### AC-4: 認証情報不在は従来どおり認証失敗として診断される

- Given: 実行系CLIとそのインタプリタは隔離環境で解決できるが、認証情報が不在または不正で認証probeが成立しない
- When: ゲートレビュアを起動する
- Then: 認証失敗として診断され、AC-3 の起動失敗診断とは区別できる文言となる。認証情報の実値は診断へ含まれない
- 検証方法見込み: `automated`

#### AC-5: スクリプト形式・ネイティブ形式の双方を対象とする自動テストが存在する

- Given: 本Issueの変更を含むリポジトリ
- When: リポジトリのテストスイートを実行する
- Then: AC-1〜AC-4 を検証する自動テストが存在して成功し、その対象にスクリプト形式（shebangでインタプリタを要求する）実行ファイルとネイティブ形式（インタプリタ不要）実行ファイルの双方が含まれる
- 検証方法見込み: `automated`

## スコープ外

- Issue #715 が扱う信頼境界そのもの（verdictの無検査永続化、実行ファイル解決が呼び出し元環境へ依存すること）の是正。本Issueは信頼境界を緩めずに起動できるようにすることだけを対象とし、実行ファイルの許可位置の条件設計は #715 で扱う。
- codex CLIをシステム領域へ導入し直す等、環境側の運用による回避。
- `review.adapter` の既定値の見直し。
- ゲートレビュア以外の実行経路（セグメント作業ワーカーの起動等、隔離環境を用いない経路）の挙動変更。
- レビュアのモデル選定・reasoning effort・レビュー品質・プロンプト内容の変更。
- ゲート判定結果の記録経路（PR review evidence投稿・gate-report結線）の変更。
