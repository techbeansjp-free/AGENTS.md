# SPEC: ローカル実行用 `.agent-skill-chain/` 配下シェルスクリプトがCLI未検出時に自動導入フォールバックを持たない

- Issue: `ISSUE-677`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/677-cli-resolution-fallback-scripts`

## 目的・背景

`.agent-skill-chain/` 配下のシェルスクリプトのうち、agent-skill-chain CLI サブコマンドへの薄いラッパーとなっているものは、いずれも同一の3経路解決ブロック（`bin/agents-md.js` → `node_modules/.bin/agent-skill-chain` → PATH上の `agent-skill-chain`）を個別にコピーして持っており、共通libへの切り出しはされていない。本SPEC作成時点で当該コピーを保持するのは54本（`.agent-skill-chain/scripts/` 直下40本、`.agent-skill-chain/ci/` 配下12本、`.agent-skill-chain/adapters/` 配下2本）である。3経路のいずれでも見つからない場合、これら54本は日本語エラーメッセージを出して即 `exit 1` するのみで、CLIを自動導入しない。

一方、Issue #536で `.agent-skill-chain/templates/github/.github/workflows/agent-skill-chain-ci.yml` の「Ensure agent-skill-chain CLI」ステップには、同じ3経路のいずれでも解決できない場合に `npm install -g agent-skill-chain@latest` を実行して自動導入するフォールバックが既に実装されている。この修正はCIワークフローテンプレート1箇所にのみ適用され、ローカル実行用スクリプト側には反映されなかった（共通化されていないため、#536時点で横展開が漏れた）。

この結果、consumerプロジェクトがテンプレート一式のみを導入し、CLI本体をグローバルにもプロジェクトローカルにも未導入のまま `.agent-skill-chain/scripts/setup.sh` 等をローカルで直接実行すると、CIでは同条件下で自動導入・続行するのに対し、ローカルでは何も自動化されず即失敗する。CIとローカルでの挙動乖離自体が、#536のような修正が今後も54本すべてへ横展開されず漏れ続ける構造的原因になっている。本Issueはこの重複と挙動乖離を解消する。

## 対象範囲

本SPECが仕様を定める対象は次の3つに限る。

- 「用語」節が定義する対象スクリプト集合（本SPEC作成時点で54本。`.agent-skill-chain/scripts/` 直下だけでなく `.agent-skill-chain/ci/`・`.agent-skill-chain/adapters/` 配下の同型ラッパーを含む）のCLI解決処理とその失敗時挙動。本リポジトリ内での実行と、consumer プロジェクトへ展開された状態での実行の双方を含む。
- 上記スクリプト群が共有するCLI解決処理の実装形態（重複コピーか単一の共有実装か）、共有実装が配布物に含まれること、および共有実装自体を解決できない場合の挙動。
- 上記共有実装と、CIワークフローテンプレート（`.agent-skill-chain/templates/github/.github/workflows/agent-skill-chain-ci.yml`・同 `agent-skill-chain-root-cleanup.yml`）内の既存CLI解決・自動導入処理との重複の扱い。

対象外は本SPEC末尾の「スコープ外」に列挙する。

## 前提

本SPECの要件・受入条件は、次の前提が成立する環境で評価する。前提が崩れる環境の挙動は本Issueの対象外とする。

- 対象スクリプトは POSIX 互換シェル（bash）で実行される。
- 実行文脈は次の2種があり、本SPECは両者を区別して扱う。(a) 非対話環境（CIランナー・自動実行など、標準入力が端末に接続されておらず人間へ確認を求められない環境）、(b) 対話環境（ローカル開発端末で人間が直接実行し、確認を求めうる環境）。
- 実行場所も次の2種があり、本SPECは両者を区別して扱う。(a) 本リポジトリ自身のworktree、(b) 本リポジトリから配布された `.agent-skill-chain/` 一式を展開した consumer プロジェクト。
- CLI解決の3経路とは、`bin/agents-md.js`（リポジトリ内ビルド成果物）、`node_modules/.bin/agent-skill-chain`（プロジェクトローカル導入）、PATH上の `agent-skill-chain`（グローバル導入）を、この順で判定する処理を指す。この順序と各経路の判定条件は本Issueでは変更しない。
- 自動導入の実行には `npm` コマンドと外部ネットワーク到達性が必要であり、いずれも欠ける環境では自動導入は失敗する。その失敗は異常ではなく想定内の分岐として扱う。
- グローバル導入（`npm install -g`）に権限が必要な環境が存在し、権限不足による失敗も同様に想定内の分岐として扱う。
- グローバル導入が成功しても、導入先の bin ディレクトリが実行中プロセスの PATH に含まれない環境が存在する。この場合、導入直後に PATH 経由でCLI実体を再探索しても発見できない。
- 配布物の展開は必ずしも完全とは限らず、共有実装ファイルが欠落・部分展開・読み込み不能となる環境が起こりうる。
- 本SPEC作成時点で、CIワークフローテンプレート側には3経路解決と自動導入フォールバックが既に実装済みであり、ローカル用スクリプト側には自動導入フォールバックが存在しない。

## 用語

本SPEC内で用いる語を次の意味に固定する。

- **3経路解決**: `bin/agents-md.js` → `node_modules/.bin/agent-skill-chain` → PATH上の `agent-skill-chain` の順に探索し、最初に見つかったものをCLI実体として採用する処理。
- **3経路解決のコード片**: `bin/agents-md.js` の存在判定・`node_modules/.bin/agent-skill-chain` の実行可能判定・PATH上の `agent-skill-chain` の探索という3分岐が、ひとつながりの条件分岐として同一ファイル内に現れる箇所。
- **対象スクリプト集合**: 本SPECが仕様を定めるスクリプトの集合。判定者依存を排するため、次の機械判定可能な条件で定義する——`.agent-skill-chain/` 配下（サブディレクトリを含む）の `*.sh` のうち、本Issueの変更着手前の版において3経路解決のコード片を自ファイル内に保持しているもの。本SPEC作成時点でこの条件に一致するのは54本である。「業務ロジックを持たない」等の主観的性質は集合の同定条件に用いない。`bin/agents-md.js` という文字列をコメント中にのみ含み3経路解決のコード片を持たないスクリプトは、本集合に含まない（Issue起票時に概数として述べられた件数と本SPECの54本との差はこの同定条件による確定結果である）。
- **ラッパースクリプト**: 対象スクリプト集合に属するスクリプト。解決したCLI実体へ引数を透過して対応するサブコマンドを実行することを主たる責務とする。
- **自動導入フォールバック**: 3経路解決がいずれも失敗した場合に、`npm install -g agent-skill-chain@latest` 相当のコマンドでCLIを導入し、成功時は処理を続行する分岐。
- **共有実装**: ラッパースクリプト群が各自コピーを持たず、単一のファイルを読み込む（source する等）ことで得るCLI解決・自動導入フォールバックの実装。
- **共有実装の解決失敗**: ラッパースクリプトが共有実装ファイルを特定・読み込みできない状態。欠落・部分展開・読み取り権限不足・構文エラーによる読み込み失敗を含む。
- **配布物**: 本リポジトリから consumer プロジェクトへ導入・更新時に展開される `.agent-skill-chain/` 配下のファイル一式。
- **呼び出しインターフェース**: ラッパースクリプトの外部から観測できる契約、すなわち受け付ける引数、委譲先CLIサブコマンド、正常系の標準出力・終了コード。

## 要求 → 要件 → 受入条件

### 要求

consumerプロジェクトの利用者が、agent-skill-chain CLI本体が未導入の状態で `.agent-skill-chain/` 配下のラッパースクリプトのいずれかをローカルで直接実行しても、CIワークフロー実行時と一貫した挙動（3経路で解決できない場合の自動導入試行）を得られること。また、CLI解決ロジックが54本に重複しているために将来の挙動変更（フォールバック方式の変更等）が横展開漏れを起こす状態を解消すること。

### 要件

- 対象スクリプト集合に属する全スクリプトが、CLI解決ロジックを重複コピーではなく単一の共有実装から得ること。
- 上記の共有実装は、既存の3経路解決順序（`bin/agents-md.js` → `node_modules/.bin/agent-skill-chain` → PATH上の `agent-skill-chain`）による判定を維持すること。
- 共有実装は配布物に含まれ、consumer プロジェクトへ展開された状態でも各ラッパースクリプトが自身の位置を基準に解決できること。
- 共有実装の解決に失敗した場合は、共有実装を解決できない旨と探索したパスを含む日本語エラーメッセージを標準エラー出力へ出力し、非ゼロ終了コードで停止すること。この経路では、委譲先CLIサブコマンドの実行・無言終了・終了コード0での終了のいずれも行わない。またこの経路の救済として3経路解決・自動導入をラッパー側へインラインで再実装することは認めない（コピーの再導入は本SPECが求める重複排除と両立せず、その違反は対象スクリプト集合が当該コード片を保持しないことを走査で確認する受入条件で検出する）。
- 上記3経路のいずれでも解決できない場合、CIの「Ensure agent-skill-chain CLI」ステップと同等の自動導入（`npm install -g agent-skill-chain@latest` 相当）を試みること。非対話環境では人間への確認を求めずに試行すること。
- 対話環境では自動導入の試行前に人間へ確認を求めてよい。確認を求める設計を採り利用者が導入を拒否した場合は、自動導入コマンドを実行せず、CLIが未解決であることと利用者が導入を拒否したことを含む日本語メッセージを標準エラー出力へ出力し、非ゼロ終了コードで停止すること。確認を求めるか否かにかかわらず、「確認も自動導入も行わずに即座に失敗して終了する」挙動（本Issue変更前の挙動）を採ってはならない。
- 自動導入には、実行前に無効化できるオプトアウト手段を設けること。環境変数 `AGENT_SKILL_CHAIN_AUTO_INSTALL` の値が `0` である場合、対話・非対話を問わず自動導入コマンドを実行せず、CLIが未解決であることとオプトアウト指定により自動導入を行わなかったことを含む日本語メッセージを標準エラー出力へ出力し、非ゼロ終了コードで停止すること。この手段を必須とする理由は、「前提」が定義する非対話環境が使い捨てのCIランナーだけでなく、開発端末上でフック・親スクリプト・エージェントから配管越しに起動された場合も含み、後者では利用者の同意なくグローバルnpm環境を変更する副作用が可逆でないためである。オプトアウト指定が無い場合の既定は自動導入を試行することとし、既定を「試行しない」側へ変えてはならない（CIとローカルの挙動一致という本Issueの要求そのものを無効化するため）。
- 同一マシン上で対象スクリプト集合の複数のラッパーがCLI未解決状態から同時に起動され、自動導入が並行実行されうることを前提とすること。この場合、各プロセスは最終的に「CLI実体を解決して委譲先CLIサブコマンドを実行する」か「原因を含む日本語エラーメッセージを標準エラー出力へ出力し非ゼロ終了する」かのいずれかへ収束すること。並行導入の途中結果（部分的に配置され実行可能でないCLI実体）をCLI実体として採用して委譲先CLIサブコマンドを実行してはならず、その状態を検知した場合は後者の挙動を採ること。並行導入を相互排他（ロック）により直列化することは要件としない。
- 自動導入が成功した場合は、導入先を含む形でCLI実体を再解決してから処理を続行すること。導入は成功したがCLI実体を再解決できない場合は、その事実を含む日本語エラーメッセージを出力し非ゼロ終了コードで停止すること（無言で成功扱いにも無言の失敗にもしない）。
- 自動導入が失敗した場合は、原因を含む明確な日本語エラーメッセージを出力し、非ゼロ終了コードで停止すること（無言で処理を継続しない）。
- 変更後も、対象スクリプト集合の各ラッパースクリプトの既存の呼び出しインターフェース（引数の透過、対応するCLIサブコマンドへの委譲、正常系のexit code）が変更前と同一であること。
- CIワークフローテンプレート（`agent-skill-chain-ci.yml`・`agent-skill-chain-root-cleanup.yml`）側の既存のCLI解決・自動導入ロジックとの重複・乖離が解消できるなら解消すること。解消しない場合は、その理由をCIワークフローテンプレート側のコメントとして明記すること。
- `init`/`upgrade`/`setup` コマンドが consumer の `package.json` へ `agent-skill-chain` を devDependency として自動追加する機能は本Issueの対象外とする。

### 受入条件（Acceptance Criteria）

#### AC-1: CLI解決ロジックの重複が排除されている

- Given: 「用語」節が定義する対象スクリプト集合（変更前の版において3経路解決のコード片を自ファイル内に保持する `.agent-skill-chain/` 配下の `*.sh`。本SPEC作成時点で54本）と、変更後に導入された共有実装
- When: `.agent-skill-chain/` 配下（サブディレクトリを含む）の全 `*.sh` を走査し、3経路解決のコード片（`bin/agents-md.js` の存在判定・`node_modules/.bin/agent-skill-chain` の実行可能判定・PATH上の `agent-skill-chain` の探索がひとつながりの条件分岐として現れる箇所）を保持するファイルを列挙する
- Then: 列挙結果がちょうど1件であり、それが共有実装のファイルである（0件も2件以上も不合格とする）。対象スクリプト集合のいずれもコード片のコピーを保持していない
- 検証方法見込み: `automated`

#### AC-2: 3経路のいずれでも見つからない場合に自動導入が試行される

- Given: `bin/agents-md.js` が未ビルド、`node_modules/.bin/agent-skill-chain` が不在、かつ PATH 上にも `agent-skill-chain` が存在しない非対話環境
- When: 対象スクリプト集合のいずれか（例: `setup.sh`）をローカルで直接実行する
- Then: 「CLIが見つかりません」で即座に失敗するのではなく、CIの「Ensure agent-skill-chain CLI」ステップと同等の自動導入（`npm install -g agent-skill-chain@latest` 相当）が人間への確認を求めずに試行され、導入に成功すれば再解決したCLI実体で処理が続行する
- 検証方法見込み: `automated`
- 自動化可能性の根拠: 本ACの自動検証は、`npm` コマンドをテスト用の代替実装（導入成功を模擬するスタブ）へ差し替えた隔離環境で行うことを許容し、実ネットワーク経由の実グローバル導入は要求しない。失敗系（AC-3）を `hybrid` とするのは、ネットワーク不通・権限不足という環境条件の再現に環境側の準備を要するためであり、成功系はスタブで完結するため `automated` とする

#### AC-3: 自動導入が完遂しない場合は明確な日本語エラーで停止する

- Given: CLIが3経路のいずれでも未解決であり、かつ次のいずれかにより自動導入が完遂しない状況——(a) 自動導入コマンド自体がネットワーク不通・権限不足等で失敗する環境、(b) 対話環境で確認プロンプトを設ける設計を採った場合に、利用者が導入を拒否して自動導入コマンドが実行されない経路
- When: 対象のラッパースクリプトを実行する
- Then: (a) では自動導入の失敗理由を、(b) では利用者が導入を拒否したためCLIを解決できない旨を、それぞれ含む明確な日本語エラーメッセージを標準エラー出力へ出し、いずれの場合も非ゼロのexit codeでスクリプトが停止する。無言終了・終了コード0での終了・委譲先CLIサブコマンドの実行を行わない
- 検証方法見込み: `hybrid`
- 自動化可能性の根拠: (b) は標準入力へ拒否応答を与える隔離環境で自動再現できるが、(a) のネットワーク不通・権限不足は環境条件の準備を要するため、全体としては `hybrid` とする。確認プロンプトを設けない設計を採った場合、(b) は発生しえない経路であり判定対象から外れる

#### AC-4: 既存の正常系呼び出しインターフェースが後方互換である

- Given: CLIが第1経路で解決可能な環境——`bin/agents-md.js` の位置に、受け取った引数列をそのまま記録して終了するスタブCLIを配置した、本リポジトリ内容の複製
- When: 変更後の対象スクリプト集合の全数（本SPEC作成時点で54本。`.agent-skill-chain/scripts/` 直下40本・`.agent-skill-chain/ci/` 配下12本・`.agent-skill-chain/adapters/` 配下2本のすべてを含み、抜取りを認めない）を、変更前と同じ引数で1本ずつ実行する
- Then: 54本すべてについて、(a) 共有実装が解決されて起動が成立し、(b) スタブCLIが記録した委譲先CLIサブコマンド名と引数列が変更前の版で同一手順を実施した場合と一致し、(c) 正常系のexit codeが変更前と同一である。1本でも起動失敗・不一致があれば不合格とする
- 検証方法見込み: `automated`
- 自動化可能性の根拠: 委譲先CLIをスタブへ差し替えることで実サブコマンドの副作用を起こさずに全数の引数透過とexit codeを比較でき、比較対象の変更前挙動は同一手順を変更前の版の複製に対して実行して得る。抜取りを認めないのは、各ラッパーに残る固有部分（自身の位置を基準とした共有実装パスの算出と引数透過の記述）がディレクトリ階層の深さごとに異なり、1本の合格が他の53本の合格を含意しないためである

#### AC-5: CIワークフロー側との重複・乖離の扱いが明示されている

- Given: 共通lib化後のローカル用CLI解決・自動導入ロジックと、`agent-skill-chain-ci.yml`（および `agent-skill-chain-root-cleanup.yml`）内の既存の「Ensure agent-skill-chain CLI」相当ロジック
- When: 両者の実装を比較する
- Then: 重複が解消（例: CIワークフロー側が共通libを参照する形へ統一）されているか、統一しない場合はその判断理由がCIワークフローテンプレート内のコメントとして明記されている
- 検証方法見込み: `manual`

#### AC-6: consumer プロジェクトへ展開した状態でもラッパーが起動できる

- Given: 本リポジトリの配布・更新手順で `.agent-skill-chain/` 一式を展開しただけの consumer プロジェクト（`bin/agents-md.js` が存在せず、`node_modules/.bin/agent-skill-chain` も不在、PATH上にも `agent-skill-chain` が無い状態）
- When: 展開先の対象スクリプト集合の全数（54本すべて。展開物内の3ディレクトリ `.agent-skill-chain/scripts/`・`.agent-skill-chain/ci/`・`.agent-skill-chain/adapters/` に属する全対象を含み、抜取りを認めない）を、その consumer プロジェクト内で1本ずつ直接実行する
- Then: 54本すべてについて、共有実装が展開物内に存在して当該ラッパーから解決され、「共有実装ファイルが見つからない」に類する起動失敗（自動導入の試行に到達しないまま終了する失敗）が発生せず、AC-2 と同じ自動導入フォールバックの分岐へ到達する。1本でも当該分岐へ到達しなければ不合格とする
- 検証方法見込み: `automated`
- 自動化可能性の根拠: 配布物の展開は一時ディレクトリへの複製で再現でき、CLI未導入状態と `npm` スタブは AC-2 と同じ隔離環境の手段で構成できる。抜取りを認めないのは、共有実装パスの算出がラッパーの階層深さごとに異なり、`.agent-skill-chain/scripts/` 直下向けの相対パスを他の2ディレクトリへ機械的に複製した実装では当該2ディレクトリのラッパーだけが解決に失敗するためである

#### AC-7: 自動導入成功後にCLI実体を再解決する

- Given: 3経路解決が失敗し自動導入コマンド自体は成功したが、導入先の bin ディレクトリが実行中プロセスの PATH に含まれない環境
- When: 対象スクリプト集合のいずれかを実行する
- Then: 導入先を含む形でCLI実体が再解決されて処理が続行するか、再解決できない場合は「導入は成功したがCLI実体を解決できない」旨と原因を含む日本語エラーメッセージを標準エラー出力へ出して非ゼロのexit codeで停止する。いずれでもない挙動（無言での成功扱い、メッセージ無しの終了）を取らない
- 検証方法見込み: `hybrid`
- 自動化可能性の根拠: 導入先が PATH 外である状況は `npm` スタブと PATH 制御で自動再現できるが、実環境のグローバル導入先レイアウトが多様であるため、実環境1件での手動確認を併用する

#### AC-8: 共有実装を解決できない場合の挙動が定義通りである

- Given: 共有実装ファイルが解決できない状態（配布物からの欠落、部分展開、読み取り権限不足、読み込み時の構文エラーのいずれか）にした配布物の複製
- When: 対象スクリプト集合の全数（54本すべて。3ディレクトリに属する全対象を含み、抜取りを認めない）を1本ずつ実行する
- Then: 54本すべてについて、共有実装を解決できない旨と探索したパスを含む日本語エラーメッセージが標準エラー出力へ出力され、非ゼロのexit codeで停止する。委譲先CLIサブコマンドは実行されず、無言終了・終了コード0での終了・ラッパー内にインライン再実装された3経路解決へのフォールバックのいずれも発生しない。1本でも異なる挙動があれば不合格とする
- 検証方法見込み: `automated`
- 自動化可能性の根拠: 配布物を複製した一時ディレクトリから共有実装ファイルを削除する・読み取り権限を落とす・不正な内容へ差し替えることで再現でき、外部ネットワーク到達性も特権操作も要さない。抜取りを認めない理由は AC-4・AC-6 と同一であり、共有実装パスの算出が階層深さごとに異なるためである

#### AC-9: 3経路解決の判定順序が変更前と同一である

- Given: 共有実装を組み込んだ配布物の複製に対し、3経路それぞれの位置（`bin/agents-md.js`、`node_modules/.bin/agent-skill-chain`、PATH上の `agent-skill-chain`）へ互いに識別可能な別々のスタブCLIを配置した隔離環境と、そこから上位経路を順に取り除いた派生環境
- When: 3経路すべてが解決可能な環境、第1経路を取り除いた環境、第1・第2経路を取り除いた環境のそれぞれで、対象スクリプト集合のラッパーを実行し、実際に起動されたスタブCLIを記録する
- Then: 3経路すべてが解決可能な環境では `bin/agents-md.js` が、第1経路を取り除いた環境では `node_modules/.bin/agent-skill-chain` が、第1・第2経路を取り除いた環境ではPATH上の `agent-skill-chain` が起動される。すなわち判定順序が変更前と同一であり、下位経路が上位経路に優先することがない
- 検証方法見込み: `automated`
- 自動化可能性の根拠: 各経路のスタブ配置とPATH制御のみで再現でき、外部ネットワーク到達性も特権操作も要さない。本ACを設ける理由は、共有実装への抽出時に第2経路（プロジェクトローカル導入）と第3経路（グローバル導入）の判定順が入れ替わっても、順序を検証しない受入条件だけでは合格しうるためである

#### AC-10: 自動導入のオプトアウトが機能する

- Given: CLIが3経路のいずれでも未解決であり、かつ環境変数 `AGENT_SKILL_CHAIN_AUTO_INSTALL` に `0` が設定された環境（非対話・対話の両方で評価する）
- When: 対象スクリプト集合のラッパーを実行する
- Then: 自動導入コマンドが一度も実行されず（`npm` スタブへの呼び出しが記録されない）、CLIが未解決であることとオプトアウト指定により自動導入を行わなかったことを含む日本語メッセージが標準エラー出力へ出力され、非ゼロのexit codeで停止する。委譲先CLIサブコマンドは実行されない。また同一環境で当該環境変数を設定しない場合には自動導入が試行される（既定が「試行する」側であることを併せて確認する）
- 検証方法見込み: `automated`
- 自動化可能性の根拠: 環境変数の設定と `npm` スタブへの呼び出し記録のみで判定でき、実グローバル導入を要さない

#### AC-11: 自動導入の並行実行時にプロセスが定義済みの2挙動へ収束する

- Given: CLIが3経路のいずれでも未解決の隔離環境と、呼び出しから完了までに遅延を持ち導入先を段階的に配置する `npm` スタブ（並行実行中に部分配置状態が観測される状況を再現する）
- When: 対象スクリプト集合のラッパーを複数同時に起動し、全プロセスの終了を待つ
- Then: 各プロセスは「CLI実体を解決して委譲先CLIサブコマンドを実行し正常系のexit codeで終了する」か「原因を含む日本語エラーメッセージを標準エラー出力へ出力し非ゼロのexit codeで終了する」かのいずれかである。部分配置状態のCLI実体を採用した委譲、無言終了、メッセージ無しの終了コード0での終了はいずれも発生しない
- 検証方法見込み: `hybrid`
- 自動化可能性の根拠: 並行起動と部分配置の観測は遅延付き `npm` スタブで自動再現できるが、実 `npm` による並行グローバル導入の挙動は環境依存であるため、実環境1件での手動確認を併用する

本SPECが定める受入条件は AC-1 から AC-11 までの11件で全てである。これ以外のAC-IDは存在しない。

## 未決事項

次は設計セグメント（`DESIGN.md`）で確定させる。いずれも受入条件を満たす複数の実装形態のうちどれを採るかの選択であり、各項目には本SPECの受入条件が制約として掛かる（受入条件の判定基準そのものを設計側で変更することは認めない）。

- 共有実装の配置先ファイル名と設置ディレクトリ。制約は AC-1（`.agent-skill-chain/` 配下に置くこと。走査対象外へ置くと列挙結果が0件となり不合格となる）、AC-6（配布物に含まれ、consumer 展開後も各ラッパーから自身の位置を基準に解決できること）、AC-8（解決できない場合の挙動）。
- 共有実装を読み込む方式（source による関数読み込みか、実行可能スクリプトへの委譲か）。制約は AC-4（既存の呼び出しインターフェースを変えないこと）と、CLI実体のパスを呼び出し元へ返せること。
- 共有実装の解決失敗を検知する手段（存在判定のみか、読み込み結果の検証まで行うか）と、エラーメッセージに含める探索パスの表現。制約は AC-8。
- 自動導入コマンドの実行条件の詳細（対話環境での確認プロンプトの有無と文言、`npm` 不在時の事前判定、権限不足時の再試行有無）。制約は、非対話環境では確認を求めずに試行すること（AC-2 は非対話環境を Given として判定する）、対話環境でも「確認も自動導入も行わずに即失敗」しないこと、AC-3 が要求する「自動導入コマンド失敗時／利用者拒否時のいずれも、理由を含む日本語エラーで非ゼロ終了」を常に満たすこと、および AC-10 が定めるオプトアウト時の挙動を優先すること（オプトアウト判定は確認プロンプトより前に行う）。
- 自動導入成功後の再解決手段（PATH の再評価か、導入先パスの直接利用か）。制約は AC-7。
- 並行自動導入時に部分配置状態を検知する手段（導入先CLI実体の実行可能性検査、版問い合わせの成否判定等）。制約は AC-11 が定める2挙動への収束であり、相互排他による直列化を採るか否かは設計側の裁量とする。
- CIワークフローテンプレート側を共有実装へ統一するか、意図的な重複として残すか（AC-5はいずれの結論も許容し、後者の場合はテンプレート内コメントでの理由明記を要求する）。判断基準は、ワークフロー実行時点でリポジトリ内の共有実装が確実に利用可能かどうか。

## スコープ外

- 対象スクリプト集合の同定条件（変更着手前の版で3経路解決のコード片を自ファイル内に保持する `.agent-skill-chain/` 配下の `*.sh`）に一致しないファイルの変更。具体的には、当該コード片を持たない `.agent-skill-chain/hooks/` 配下のスクリプトおよび `.agent-skill-chain/adapters/` 配下の一部スクリプトと、`.agent-skill-chain/` 配下以外のファイル（リポジトリ直下の `bin/`、`src/` 配下のTypeScript実装等）。理由は、これらがそもそも本Issueが解消対象とする重複コピーを保持しておらず、共有実装への切り替え対象にならないため。
- `init`/`upgrade`/`setup` コマンドが consumer プロジェクトの `package.json` へ `agent-skill-chain` を devDependency として自動追加する機能の新設（別の設計判断を要する大きめの変更であり、必要なら別Issueとして扱う）。
- CLI解決の3経路（`bin/agents-md.js` → `node_modules/.bin/agent-skill-chain` → PATH上の `agent-skill-chain`）の判定順序・条件自体の変更（順序が変更されていないことは AC-9 が機械的に確認する）。
- `npm install -g agent-skill-chain@latest` 以外の自動導入手段（例: ローカルビルドの自動実行）の新設。
- 自動導入するCLIの版指定を `@latest` 以外（consumer が導入済みのテンプレート版に対応する固定版など）にすること、および導入されたCLI版と consumer 側テンプレート版が乖離した場合の期待挙動（警告・拒否・自動更新等）の定義。本Issueの目的はCIワークフローと同一の導入挙動をローカルへ揃えることであり、版整合ポリシーはCI側の既存挙動も同時に変える別論点で、独立した設計判断を要するため本SPECでは扱わない。乖離時の挙動は本Issueの変更前後で変化しない。
- 対話環境における自動導入の同意確認を「必須」とすること。本SPECは非対話環境での無確認試行を必須とし、対話環境では確認を挟む設計を許容するに留める（確認UIの要否・文言は設計判断であり、本SPECはどちらの結論も受け入れる。ただし確認を挟む場合の拒否経路の期待挙動は AC-3 が定める）。非対話環境で利用者の同意を得られないままグローバルnpm環境が変更される副作用に対しては、同意確認の必須化ではなく事前オプトアウト（AC-10）で対処する。同意確認を非対話環境に持ち込めない以上、確認の必須化は「CIとローカルで挙動を揃える」という本Issueの要求と両立しないためである。
- 自動導入の相互排他（ロックファイル等による直列化）機構の新設。並行実行時に利用者が被る実害は「部分配置状態のCLI実体で委譲先サブコマンドが実行されること」であり、これは AC-11 が定める2挙動への収束を要件で拘束すれば防げる。直列化の要否と手段はプラットフォーム依存の設計判断であり、本SPECはどちらの結論も受け入れる。
- 自動導入のオプトアウト以外の設定手段（設定ファイル項目・CLIオプション等）の新設。本SPECが必須とするのは環境変数 `AGENT_SKILL_CHAIN_AUTO_INSTALL` による無効化のみであり、これは3経路解決が失敗した時点でCLI設定を読める保証が無いため、CLI非依存の手段に限定する判断による。
- ゲートレビュー用プロンプト生成が、成果物本文からAC-IDを見出し限定ではなく単純文字列一致で抽出し、本文の解説文・コメント中に現れた同形の文字列まで実在するAC-IDとして扱う不具合の修正。これは `.agent-skill-chain/` 配下のCLI実装側の別欠陥であり、本Issueが扱うシェルスクリプトのCLI解決処理とは対象コンポーネントも原因も異なるため、別Issueとして扱う。本SPECでは、当該不具合を誘発しうる同形文字列を本文へ書かないことで回避する。
