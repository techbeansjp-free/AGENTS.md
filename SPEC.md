# SPEC: セグメント別 worker アダプタ・モデルティア選択の恒久設定

- Issue: `ISSUE-307`
- 作成者: `spec worker`
- 対象ブランチ: `feature/307-segment-worker-adapter-config`

## 目的・背景

セグメント作業ワーカー（spec・design・implementation・validation の4セグメントで成果物を書く実行主体）は、設定ファイル `.agent-skill-chain/config/agent-skill-chain.yaml` の `worker.adapter` で選ばれた実行系アダプタ（`claude` / `codex` / `human`）によって起動される。現在この `worker.adapter` は4セグメント共通の単一スカラー値しか取れず、「実装セグメントだけ `codex` を使い、他は `claude` のまま」という運用ができない。

さらに `codex` アダプタが codex CLI を起動する際のモデル名と reasoning effort は、アダプタ実装内のフォールバック値（実装セグメント向け `gpt-5.6-terra` / `medium`、それ以外 `gpt-5.6` / `high`）を環境変数で上書きする経路しか持たない。環境変数はセッション限りで消えるため、プロジェクトの恒久的な既定値として設定の正本に記録できない。

モデル選択には、具体的なモデル文字列を設定に書かないティア抽象化方式を採る。設定が保持するのはティア名（`model_tier`）と reasoning effort だけであり、ティア名から具体的なモデル文字列への対応は `.agent-skill-chain/adapters/codex.sh` 側が保守する。これは claude アダプタが既に採っている、モデルと推論強度をティア名（`frontier_coding` / `maximum_reasoning`）で表して具体名を実行系側に閉じ込める方式と同じ思想である。理由は、将来より高性能なモデルが登場した際にアダプタ内のマッピング1箇所を更新するだけで追従でき、設定ファイルとスキーマの変更を要さないようにするためである。

本 Issue の目的は、(1) セグメントごとに実行系アダプタとモデルティア・reasoning effort を設定の正本で上書きできるようにすること、(2) 具体的なモデル文字列を設定とスキーマから排除し、ティア名から具体名への解決をアダプタ内部に閉じ込めること、(3) 本リポジトリの実装セグメントを恒久的に `codex` / ティア `highest_capability` / reasoning effort `high` に切り替え、以後は都度の指示なしにこの組み合わせが自動選択される状態にすること、(4) 進行役が「実装は◯◯で」という指示を受けたときに、どの操作でこの設定を変更すればよいかを規範文書から一意に判断できるようにすること、の4点である。

## 用語

- セグメント作業ワーカー: 4セグメント（spec・design・implementation・validation）のいずれかを担当し、writer lease を保持して自ブランチへ commit・push する実行主体。
- 実行系アダプタ: ベンダー中立の役割契約を特定の実行系へ変換する層。実体は `.agent-skill-chain/adapters/{claude,codex,human}.sh` の3種。
- セグメント別上書き: セグメント名をキーとして、そのセグメントにのみ適用する設定値を与える仕組み。設定キーは `worker.segment_overrides.<segment>` とする。
- モデルティア: 具体的なモデル文字列ではなく、`highest_capability` のような抽象名で表現するモデル選択の単位。ティア名から具体モデル名への対応は実行系アダプタ側が保守する。設定キーは `model_tier`。
- 値の正本: 具体的なモデル文字列の実体を保持する場所。本 Issue では、本リポジトリに既存の登録済みプロジェクトポリシー宣言（`.agent-skill-chain/project/manifest.yaml` のコア独立レビュー用モデル宣言）を指す。
- ティアマッピング: ティア名から具体的なモデル文字列を得る対応。値の正本を読み取る単一の解決定義としてアダプタ内に置き、アダプタ自身は具体的なモデル文字列を保持しない。
- reasoning effort: codex CLI に渡す推論強度の指定値。
- worker 起動コンテキスト: 起動ラッパー `.agent-skill-chain/scripts/worker-launch.sh` がアダプタ選択のために CLI から取得する `KEY=VALUE` 形式の解決結果。

## 前提

- 4セグメントの構成そのもの（`.agent-skill-chain/config/segments.yaml`）は本 Issue で変更しない。変更対象はワーカー起動時のアダプタ・モデル選択の解決経路のみである。
- 現在の worker 起動コンテキストは Issue ID のみを入力として `adapter` / `backend` / `issue_number` を返し、セグメントを入力として受け取らない。セグメント別の解決には、セグメントを入力に含めた解決結果が必要になる。
- codex アダプタの実行には codex CLI の認証・実行環境が必要である。認証が成立しない環境では既存のフェイルセーフにより blocked 報告・writer lease 解放を経て人間確認へ倒れる。
- 進行役は調整状態（Issue・ラベル・review 証跡・マージ・worktree ライフサイクル）のみを読み書きし、成果物の著述・内容の取り込みを行わない（不変条件 I5）。本 Issue の設定変更の実行主体とタイミングは、この制約を侵さない形で設計セグメントが確定する。

## 要求 → 要件 → 受入条件

### 要求

利用者は、セグメントごとに異なる実行系アダプタとモデルティア・reasoning effort を、プロジェクトの設定の正本に恒久的に記録した状態で運用したい。特に、実装セグメントを codex・ティア `highest_capability`・reasoning effort `high` で実行し、他の3セグメントは現行既定の claude のままにしたい。具体的なモデル文字列は設定に持たせず、モデル世代の更新にアダプタ側1箇所の変更だけで追従できるようにしたい。指示があれば別の値へ切り替えられる余地も残したい。既存プロジェクトの設定は変更なしで従来どおり動作しなければならない。

### 要件

- 設定ファイルの `worker` セクションに、セグメント（spec・design・implementation・validation）ごとの上書き（`adapter`・`model_tier`・`reasoning_effort`）を記述できる。
- 既存の `worker.adapter: <スカラー>` は4セグメント共通の既定値として引き続き有効であり、セグメント別上書きが無いセグメントのフォールバックとして機能する。`worker.adapter` 自体が未設定の場合の最終フォールバックは従来どおり `human` とする。
- worker 起動コンテキストの解決は、対象セグメントを入力として受け取り、そのセグメントに適用される `adapter`・`model_tier`・`reasoning_effort` を返す。
- 解決された `model_tier`・`reasoning_effort` は、ワーカー起動経路を通じて codex アダプタへ渡り、codex CLI 起動コマンドのモデル指定・reasoning effort 指定に反映される。このとき `model_tier` は、codex アダプタ内のティア→具体モデル名マッピングを経由して具体的なモデル文字列へ解決される。
- 具体的なモデル文字列は設定ファイルとスキーマのいずれにも現れない。ティア名と具体名の対応はアダプタ内部にのみ保持する。
- codex アダプタが従来から解釈しているテスト用の上書き環境変数（起動コマンド全体の上書き、および具体モデル名・reasoning effort の個別上書き）は維持し、設定由来の解決結果より優先される。
- 設定スキーマ `.agent-skill-chain/schemas/config.schema.yaml` は新旧両形式を許容し、未知のアダプタ名・未知のティア名・未知のセグメント名・未知のキーは従来どおり不正として拒否する。
- 本リポジトリ自身の設定で、実装セグメントを `adapter: codex` / `model_tier: highest_capability` / `reasoning_effort: high` に切り替える。spec・design・validation は現行既定（claude）を維持する。
- 進行役が対話中に受けた「実装は◯◯で」という指示を恒久設定へ反映する手順が、規範文書に自己完結的に記載される。
- 設定項目追加の手続き（ハードコード不可の理由・プロジェクト単位で変わる必要性・スキーマ更新・既定値定義・migration 定義）の結果が、設計成果物に明記される。

### 受入条件（Acceptance Criteria）

#### AC-1: セグメント別のアダプタ解決

- Given: 設定で `worker.adapter` が `claude`、実装セグメントのセグメント別上書きに `adapter: codex` が指定されている。
- When: 実装セグメントを指定して worker 起動コンテキストを解決する（`agent-skill-chain worker context <issue_id> implementation` 相当）。
- Then: `adapter=codex` が解決される。同じ設定で spec・design・validation を指定した場合は、いずれも `adapter=claude` が解決される。
- 検証方法見込み: `automated`

#### AC-2: モデルティア・reasoning effort の解決と codex 実行への反映

- Given: 実装セグメントのセグメント別上書きに `adapter: codex` / `model_tier: highest_capability` / `reasoning_effort: high` が指定されている。
- When: 実装セグメントのワーカーを起動ラッパー経由で起動する。
- Then: 起動コンテキストの解決結果にティア名と reasoning effort が含まれ、それらが codex アダプタへ渡る。`model_tier` は codex アダプタ内のティア→具体モデル名マッピングで具体的なモデル文字列に解決されたうえで、codex CLI 起動コマンドのモデル指定に反映され、reasoning effort は推論強度指定に反映される。ただし、codex アダプタが従来から解釈するテスト用の上書き環境変数（起動コマンド全体の上書き、具体モデル名・reasoning effort の個別上書き）が与えられている場合は、そちらが設定由来の解決結果より優先される。
- 検証方法見込み: `automated`

#### AC-3: 既存設定の後方互換

- Given: セグメント別上書きを持たず `worker.adapter: <スカラー>` のみを持つ既存の設定ファイル。
- When: 4セグメントそれぞれについて worker 起動コンテキストを解決し、ワーカーを起動する。
- Then: すべてのセグメントで `worker.adapter` の値がアダプタとして解決され、本 Issue 適用前と同一の選択結果になる。`worker.adapter` も未設定の場合は `human` にフォールバックする。ティア指定が無い場合は codex アダプタの従来のモデル・reasoning effort 選択がそのまま用いられる。codex アダプタの認証未成立時のフェイルセーフ（blocked 報告・writer lease 解放・0 でも 3 でもない終了コード）も従来どおり維持される。
- 検証方法見込み: `automated`

#### AC-4: 設定スキーマによる新旧両形式の検証

- Given: 設定スキーマ `.agent-skill-chain/schemas/config.schema.yaml`。
- When: セグメント別上書きを含む新形式の設定と、スカラーのみの旧形式の設定をそれぞれ検証する。加えて、未知のアダプタ名・未知のティア名・未知のセグメント名・未知のキーを含む設定を検証する。
- Then: 新形式・旧形式はいずれも妥当と判定される。未知のアダプタ名・未知のティア名・未知のセグメント名・未知のキーを含む設定は不正として拒否され、設定読み込みが日本語の理由付きで失敗する。
- 検証方法見込み: `automated`

#### AC-5: 解決ロジックの単体テスト

- Given: 本 Issue で追加・変更された設定解決ロジック（セグメント別のアダプタ解決、モデルティア・reasoning effort の解決、上書き未設定時のフォールバック、スキーマによる新旧両形式の受理と不正値の拒否）。
- When: リポジトリの単体テストを実行する。
- Then: 上記4項目それぞれについて、期待される解決結果を検証するテストが存在し、成功する。
- 検証方法見込み: `automated`

#### AC-6: 本リポジトリの実装セグメントの恒久切替

- Given: 本リポジトリの `.agent-skill-chain/config/agent-skill-chain.yaml`。
- When: 追加の指示・環境変数なしで、実装セグメントを指定して worker 起動コンテキストを解決する。
- Then: `adapter=codex` / `model_tier=highest_capability` / `reasoning_effort=high` が解決される。同じ設定で spec・design・validation を指定した場合は claude が解決される。設定ファイルには具体的なモデル文字列が一切現れない。
- 検証方法見込み: `automated`

#### AC-7: 設定更新手順の文書化

- Given: 進行役が対話中に「実装は◯◯で」という指示を受けた状況。
- When: 規範文書（リポジトリの規約文書または CLI のヘルプ出力）を参照する。
- Then: どの操作でこの恒久設定を更新するか（設定ファイルの直接編集か専用 CLI サブコマンドか）、実行主体は誰か、どのタイミングで行うかが一意に読み取れ、その手順が進行役の純粋性（成果物の著述・内容の取り込みを行わない）を侵さないことが確認できる。
- 検証方法見込み: `hybrid`（手順の記述の存在は機械検査可能だが、進行役の純粋性を侵さないかの妥当性判断は人間または検証ワーカーの読解を要する）

#### AC-8: 設定項目追加手続きの帰結の明記

- Given: 本 Issue の設計成果物と更新後の設定スキーマ。
- When: これらを参照する。
- Then: 新設フィールドについて、ハードコードで対応できない理由、プロジェクト単位で変わる必要性、スキーマ版数の扱い（更新の有無とその理由）、既定値、既存設定に対する migration 方針の5点がすべて明記されている。
- 検証方法見込み: `hybrid`（記述の存在は機械検査可能だが、内容の十分性は読解を要する）

#### AC-9: アダプタ内ティアマッピングの存在と更新容易性

- Given: `.agent-skill-chain/adapters/codex.sh`。
- When: `highest_capability` ティアが指定された状態で具体的なモデル文字列の解決を行う。
- Then: 当該ティアに対応する具体的なモデル文字列が、本リポジトリに既存の値の正本（`.agent-skill-chain/project/manifest.yaml` のコア独立レビュー用モデル宣言）を読み取る、アダプタ内の単一の解決定義から得られる。アダプタ自身は具体的なモデル文字列を新たに保持しない。将来より高性能なモデルが登場した場合、値の正本（既存の登録済みプロジェクトポリシー宣言）を更新するだけで追従でき、設定ファイル・スキーマ・CLI・起動ラッパーの変更を要さない構造になっている。
- 検証方法見込み: `automated`

## 制約

- 具体的なモデル文字列は、本 Issue で新設する設定ファイル・設定スキーマには書かない。値の実体は本リポジトリの既存の値の正本（登録済みプロジェクトポリシーの宣言）に置き、アダプタは単一の解決定義を通じてそれを参照する。
- 進行役の純粋性（不変条件 I5）を侵さない。設定変更の実行主体とタイミングは設計セグメントで確定する。
- 設定項目の追加は、ハードコード不可の理由・プロジェクト単位で変わる必要性・スキーマ更新・既定値定義・migration 定義・必要なら ADR、という手順に従う。
- 4セグメント構成そのものは変更しない。
- codex 実行には codex CLI の認証・実行環境が必要であり、認証未設定環境では既存のフェイルセーフにより人間確認へ倒れる挙動を維持する。
- 既存プロジェクトの設定ファイルは無変更で従来どおり動作しなければならない（後方互換の回帰を作らない）。

## 完了条件

- 上記すべての受入条件が満たされ、各受入条件に対する検証方法と証跡が独立検証セグメントの検証記録に対応付けられている。
- 常時必須のテスト（lint / format、型検査、単体テスト、変更範囲の結合テスト、静的アプリケーションセキュリティテスト、依存関係・secret スキャン）が実行され成功している。
- 本変更は設定と実行系選択の変更であり、画面フロー・API 境界・認証認可・性能ホットパス・DB migration・デプロイ運用・外部連携のいずれの変更種別にも該当しないため、それらに紐づく追加検証は不要と判断する（判断自体を検証記録に残す）。

## 未決事項（設計セグメントで確定する）

- 恒久設定を更新する操作を、設定ファイルの直接編集とするか専用 CLI サブコマンドとするか。
- 設定スキーマの版数を更新するか、後方互換な追加として据え置くか。
- セグメント別上書きの解決結果を worker 起動ラッパーからアダプタへ渡す経路の具体形（環境変数名を含む）。
- アダプタ内ティアマッピングの実装形（対応表の保持方法と、未知のティア名が渡された場合の扱い）。

これらはいずれも実現手段の選択であり、本 SPEC が定める受入条件はどの選択肢を採っても検証可能である。

## スコープ外

- ゲートレビュア側のセグメント別アダプタ・モデル切替。レビュア側の設定は現行の単一値のままとする。
- codex 以外の新規アダプタの追加。
- claude アダプタ・human アダプタのモデル選択方式の変更。
- `highest_capability` 以外の新規ティア名の定義。
- 4セグメント構成（セグメントの追加・削除・順序変更）の変更。
- 実行時（起動の都度）にアダプタ・モデルを一時的に切り替えるコマンドラインオプションの追加。本 Issue が対象とするのは設定の正本に記録される恒久的な既定値である。
