# SPEC: gh の --slurp 依存を除去し、古い gh の環境でゲートの取得経路が黙って劣化しないようにする

- Issue: `ISSUE-774`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/774-gh-slurp-silent-degradation`

## 目的・背景

ゲートは GitHub の一覧 API（PR の commits・reviews、対象 SHA に対応する PR、Check Run、Actions run、Issue の events）を `gh api` で取得する。現行実装はその全ページ取得に `--slurp` を用いる。`--slurp` は gh 2.63.0 で追加されたオプションであり、それより古い gh では未知フラグとして拒否される。LTS ディストリビューションの標準パッケージが配る gh はこれより古い場合があり、配布先の gh バージョンは制御できない。

害は 2 つに分かれる。第一に、ラウンド履歴（同一 Issue・同一ゲートの過去ゲート反復の判定記録）の取得は失敗が「取得不能」として安全側へ倒れるため、ラウンド番号が導出されず、ラウンド予算による限定と打ち切りが一度も発火しない。しかもこの失敗は利用者に見えず、過去ラウンドが存在しない場合と外形上区別できないまま判定が正常終了する。第二に、残りの取得経路は失敗時に明示的な失敗として停止するため無言ではないが、古い gh では当該コマンドが常に失敗する。

本 Issue の目的は、`--slurp` を持たない gh の環境でこれらの取得を成立させること、および取得や解釈に失敗した場合にその事実が利用者に見えることである。

## 用語（本 SPEC 内での定義）

- ページ結合: gh の複数ページ応答から、全ページの要素を欠落・重複なく 1 つの一覧として取り出すこと。
- 配列応答: トップレベルが JSON 配列である一覧応答（PR の commits・reviews、対象 SHA に対応する PR、Issue の events）。
- オブジェクト応答: トップレベルが JSON オブジェクトで、要素配列を属性として持つ一覧応答（Check Run 一覧、Actions run 一覧）。
- ラウンド履歴: 同一 Issue・同一ゲートの過去ゲート反復の判定記録。GitHub モードでは PR へ投稿されたレビュー証跡から導出される。
- 無言の劣化: 取得または解釈に失敗したにもかかわらず、失敗が利用者に見える形で提示されず、取得結果が空である場合と外形上区別できないまま後続処理が正常終了すること。

## 前提（2026-08-18 の実測。本リポジトリの開発環境）

- gh 2.45.0 に対し `--slurp` を伴う `gh api` を実行すると、標準エラー出力へ `unknown flag: --slurp` を出し終了コード 1 で失敗する。
- 3 ページに分かれる配列応答（PR review 一覧、要素 26 件）へ `--paginate` のみを与えた場合、標準出力は gh 2.45.0 と gh 2.97.0 で同一バイト列であり、全ページを平坦化した単一の JSON 配列として解釈できる。
- 3 ページに分かれるオブジェクト応答（Actions workflow 一覧）へ `--paginate` のみを与えた場合、標準出力は連結された複数の JSON 文書であり、単一の文書としては解釈できない。同じ取得に `--slurp` を与えると各ページを要素とする配列になる。
- `--slurp` は `--paginate` との併用が前提であり、`--jq`・`--template` とは併用できない。
- `--slurp` の使用箇所は `src/commands/gate.ts` に 10、`src/lib/gate-round.ts` に 1、`src/lib/review-light.ts` に 1 の計 12 である。ラウンド履歴の構築経路は `src/lib/gate-round.ts` の PR review 一覧取得である。
- 動作保証の下限は gh 2.45.0 とする。これより古い gh は実測しておらず本 Issue の対象に含めない。

## 要求

`--slurp` を持たない gh がインストールされた環境（下限は gh 2.45.0）で、ゲートの GitHub API 取得経路が全ページを正しく取得して機能すること。そのうえで、取得や解釈に失敗した場合は失敗が利用者に見えること。とりわけラウンド履歴が空のまま正常に見える状態を作らないこと。`--slurp` を受け付ける gh の環境における現在の挙動を変えないこと。

## 方針の選択

`--slurp` を使わない取得へ置き換える方針を採る。最低 gh バージョンを検査して満たさなければ明示的に失敗させる方針は採らない。理由は次のとおり。

- 配布先の gh バージョンを制御できず、標準パッケージが 2.63.0 より古い環境が現に存在する。バージョン検査による停止は、それらの環境でゲートを一律に利用不能にする。本 Issue が解こうとしている問題を、無言の劣化から恒久的な利用不能へ置き換えるだけである。
- 実測により、配列応答は `--paginate` のみで全ページが得られ、オブジェクト応答も連結された文書として全ページが標準出力へ現れる。`--slurp` を外しても取得できる情報は失われない。
- バージョン検査は「取得できなかったことが見える」という要求を代替しない。バージョン以外の理由（権限・レート制限・通信障害）による取得失敗は依然として無言のまま残る。

## 要件

- 要件 1: `src/` 配下の gh 起動引数から `--slurp` を除去する。全ページを取得する意図は保ち、`--paginate` は除去しない。
- 要件 2: gh の標準出力が (i) 単一の JSON 文書、(ii) 空白区切りで連結された複数の JSON 文書、(iii) 各ページを要素とする配列（従来 `--slurp` が返した形）のいずれであっても、全ページの要素を欠落・重複なく取り出す。
- 要件 3: JSON として解釈できない出力を空の一覧として扱わない。取得または解釈の失敗は、従来明示的な失敗として停止していた経路では引き続き明示的な失敗とし、ラウンド履歴経路では理由付きの取得不能とする。
- 要件 4: ラウンド履歴を取得できなかった場合、その旨と理由を診断として標準エラー出力へ出力する。判定プロンプト本文（標準出力）と終了コードは変更しない。
- 要件 5: `--slurp` を未知フラグとして拒否する gh に対して、ラウンド履歴の構築とラウンド番号の導出が成立する。
- 要件 6: 自動検証はテスト専用の gh スタブ（またはテストが注入する実行系）だけを用い、実行環境にインストールされている gh の実体・バージョン・設定を変更しない。
- 要件 7: `--slurp` を受け付ける gh の環境でも、変更前と同じ要素集合と同じ判定プロンプトを得る。

## 受入条件（Acceptance Criteria）

#### AC-1: gh 起動引数から --slurp が全廃されている

- Given: 変更後のリポジトリの `src/` 配下にある、gh へ渡す引数列のすべて
- When: それらの引数列を列挙する
- Then: `--slurp` を含む引数列が 1 つも存在しない。かつ、変更前に `--paginate` を与えていた取得経路は変更後も `--paginate` を与えている
- 検証方法見込み: `automated`

#### AC-2: 複数ページの配列応答を欠落なく取り出す

- Given: `--slurp` を受け付けない gh が、3 ページに分かれる配列応答を `--paginate` のみの取得に対して単一の平坦な JSON 配列として標準出力へ返す
- When: ゲートが当該一覧を取得する
- Then: 3 ページ分の全要素が欠落・重複なく取得される。同じ取得経路へ、各ページを要素とする配列、または連結された複数の文書を与えた場合も、得られる要素集合は同一である
- 検証方法見込み: `automated`

#### AC-3: 複数ページのオブジェクト応答を欠落なく結合する

- Given: `--slurp` を受け付けない gh が、複数ページに分かれるオブジェクト応答（Check Run 一覧・Actions run 一覧）を `--paginate` のみの取得に対して連結された複数の JSON 文書として標準出力へ返す
- When: ゲートが当該一覧を取得する
- Then: 全ページの文書が解釈され、各ページの要素配列を連結した全要素が欠落・重複なく取得される。先頭ページのみを採用する実装、および解釈失敗により空集合となる実装はこの条件を充足しない
- 検証方法見込み: `automated`

#### AC-4: 解釈できない出力を空の一覧として扱わない

- Given: gh が終了コード 0 を返しつつ、JSON として解釈できない標準出力を返す
- When: ゲートが当該一覧を取得する
- Then: 空の一覧として後続処理を続行しない。従来明示的な失敗として停止していた取得経路は引き続き停止し、ラウンド履歴経路は理由付きの取得不能となる
- 検証方法見込み: `automated`

#### AC-5: --slurp を拒否する gh でもラウンド履歴が構築される

- Given: `--slurp` を含む引数を `unknown flag: --slurp` を標準エラー出力へ出して非 0 終了で拒否し、`--paginate` のみの取得には正常応答を返す gh を用いる。対象 PR には同一 Issue・同一ゲートの過去ラウンドのレビュー証跡が投稿済みである
- When: `gate reviewer-prompt` を PR 番号と反復識別子つきで実行する
- Then: 過去ラウンドの判定記録が判定プロンプトへ展開され、ラウンド番号が導出される。過去ラウンドの判定記録を取得できなかった旨、および過去ラウンドが無い旨は展開されない
- 検証方法見込み: `automated`

#### AC-6: ラウンド履歴を取得できない場合に理由が利用者に見える

- Given: ラウンド履歴の取得が失敗する状況にある（gh の非 0 終了、または JSON として解釈できない応答）
- When: `gate reviewer-prompt` を実行する
- Then: 取得できなかった旨と理由を含む診断行が標準エラー出力へ出力される。終了コードは 0 のままであり、当該診断行は標準出力の判定プロンプト本文へは含まれない
- 検証方法見込み: `automated`

#### AC-7: 判定プロンプト本文が本変更で変化しない

- Given: 判定プロンプトの固定出力（golden fixture）と生成結果を突き合わせる既存テストが存在する
- When: 本 Issue の変更を適用したブランチ差分を確認する
- Then: golden fixture の内容は変更されていない。AC-6 の診断は標準エラー出力にのみ追加され、判定プロンプト本文へは追加されない
- 検証方法見込み: `automated`

#### AC-8: --slurp を受け付ける gh でも結果が変わらない

- Given: `--slurp` を受け付ける gh を用い、AC-2・AC-3・AC-5 と同一の応答内容を返す
- When: AC-2・AC-3・AC-5 と同じ取得と実行を行う
- Then: 得られる要素集合と判定プロンプトは AC-2・AC-3・AC-5 の結果と同一である
- 検証方法見込み: `automated`

#### AC-9: 自動検証が実行環境の gh を変更しない

- Given: 本 Issue が追加・変更した自動テスト
- When: テストコードと、テストが gh を解決する経路を確認する
- Then: テストはテスト専用の一時 gh スタブ、またはテストが注入した実行系だけを呼ぶ。実行環境にインストールされた gh の実体・バージョン・設定ファイル・恒久的な PATH 設定を書き換えない
- 検証方法見込み: `hybrid`

## スコープ外

- gh を介さず GitHub REST API を直接呼ぶ実装への移行。認証の扱いが増えるため本 Issue では扱わない。
- 最低 gh バージョンの検査と、それを満たさない場合の起動時停止（方針の選択で不採用とした案）。
- `--slurp` 以外の gh 互換性問題。他のオプションのバージョン差、gh 未インストール、認証方式の差を含む。
- ラウンド予算の閾値そのもの、および反証ルーブリックの内容。これらは別 Issue で確定済みであり、本 Issue はそれらが発火する前提条件を回復するにとどまる。
- `review:light` の付与者検証が失敗した場合の診断出力の追加。当該経路は失敗時に strict 側へ倒れ、規律が弱まる方向の劣化を生まないため本 Issue では扱わない。ただし当該経路の `--slurp` 除去は要件 1 に含む。
- 取得失敗時の再試行、応答のキャッシュ、レート制限対策。
- `--slurp` を含まない gh 呼び出しの変更。`.agent-skill-chain/scripts/` および adapters 配下の gh 呼び出しを含む。
- gh 2.45.0 より古い gh に対する動作保証。
- 既に削除済みの trusted-gate verifier ワークフローの復活。

## 未決事項

- 無し。ページ結合処理の配置と実装形態（共有ヘルパーの有無、実行系の注入方法）は設計セグメントで確定する。
