# SPEC: quick免除が解除されたIssueでも成果物が1つも無いセグメントが必須成果物検査の対象にならない欠陥の是正

- Issue: `ISSUE-741`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/741-verify-artifacts-unstarted-segments`

## 目的・背景

必須成果物検査（`.agent-skill-chain/ci/verify-artifacts.sh`）は、CI から「開始済みセグメント」ごとに1回ずつ実行される。あるセグメントが開始済みと判定される唯一の根拠は、そのセグメントの成果物がブランチ差分に現れることである。したがって成果物が1つも作られていないセグメントは検出されず、その必須成果物の欠落が永久に検査されない。成果物が欠けている場合こそ検査が必要なのに、欠けていること自体が検査をスキップさせる。

実測（ISSUE-692 / PR #695）では、quick 免除が解除される条件（変更差分にスキーマ定義を含む）に該当し `DESIGN.md`・`PLAN.md` が必須であったにもかかわらず、両方とも存在しないために設計セグメントが開始済みと判定されず、必須成果物検査自体が走らないまま CI が緑になった。欠落を発見したのは独立検証セグメントのレビュアであり、レビュアは AGENTS.md の不変条件 I2 においてガイドライン扱いで自動強制を持たない。結果として、quick 免除の解除条件（影響が大きい変更に通常フローを強制するための条件）そのものが機械的強制を失っている。AGENTS.md が不変条件 I1 の検査手段として名指しする必須成果物検査を、当該条件下で実効化することが本 Issue の目的である。

## 用語（本 SPEC 内での定義）

- 必須成果物検査: 指定した Issue と1つのセグメントについて、`.agent-skill-chain/config/segments.yaml` の `outputs` が定める必須成果物の充足を判定し、充足なら終了コード0、欠落なら終了コード1を返す検査。
- 開始済みセグメント集合: base ブランチとの差分に現れたファイルから導出される、着手済みと推定されるセグメントの集合。以下 S と呼ぶ。
- セグメント順序: `.agent-skill-chain/config/segments.yaml` の連鎖が定める固定順（要求・要件 → 設計・実装計画 → 実装 → 独立検証）。「上流」「下流」はこの順序に基づく。
- 対象集合: 必須成果物検査を実際に適用するセグメントの集合。以下 R と呼ぶ。現状は R が S と等しいことが本欠陥の原因である。
- 閉包追加対象: S が空でないとき、S の最も下流の要素より上流にありながら S に含まれない、要求・要件セグメントまたは設計・実装計画セグメント。実装セグメントと独立検証セグメントは閉包追加対象に含めない。
- quick 免除: 軽量な変更向けに Issue スコープ成果物ファイルの作成義務を免除する規則（ADR-0022）。シグナルは GitHub モードでは Issue ラベル、ローカルモードでは Issue 状態ファイルにのみ置かれる。
- quick 免除の解除: quick が要求されているが、リスク区分が normal でない、または変更差分がガードレール対象（ADR 配下・セグメント定義・AGENTS.md・スキーマ定義）を含むために免除を適用しないこと。

## 前提

- 本 Issue は GitHub Coordination Backend 上で進行し、quick シグナルの正本は Issue ラベルである。ローカルモードでは Issue 状態ファイルが正本となる。
- CI の検査ジョブはステップを順に実行し、先行ステップが失敗した時点でジョブが停止する。
- マージ準備完了（Draft ではない）PR に対しては root 直下成果物の検査が設計上つねに失敗する（ADR-0046）。この検査は必須成果物検査より前のステップにあるため、Draft か否かで強制の有無を切り替える設計は、非 Draft 側では必須成果物検査へ到達せず目的を達成できない。よって本 Issue の強制は Draft 状態に依存しない。
- 現在の quick シグナル解決は、GitHub モードでは Issue ラベルの取得に成功することを前提とする。取得手段へ権限とトークンが与えられていない実行環境では、quick を要求した Issue であっても免除が適用されない。

## 要求

quick 免除が解除された Issue について、必須成果物の欠落が CI で必ず検出されること。設計・実装計画セグメントの成果物が1つも存在しない場合を含む。同時に、現在 CI が緑になっている通常の PR（要求・要件から独立検証まで順に checkpoint する PR、および quick 免除が有効な PR）が、この変更によって失敗しないこと。

## 要件

- 要件1: 対象集合 R は、quick 免除を適用しない場合、S に閉包追加対象を加えた集合とする。すなわち S が空でなければ、S の最も下流の要素より上流にある要求・要件セグメント・設計・実装計画セグメントを、開始済みでなくても R に含める。
- 要件2: 実装セグメントと独立検証セグメントは閉包追加対象に含めない。これらの必須成果物は差分そのもの（コード変更・テスト変更）であり、コード変更を伴わない Issue には産出物が存在しないため、強制すると正当な文書のみの Issue を失敗させる。
- 要件3: quick 免除が有効な場合、R は S と等しく、免除対象の成果物は要求しない（現行動作を維持する）。
- 要件4: quick 免除が解除された場合、R は要件1に従って導出し、解除理由を標準エラー出力へ提示する（解除理由の提示は現行動作を維持する）。
- 要件5: quick シグナルを解決できない場合、免除を適用せず要件1に従って R を導出し、シグナルを解決できなかった旨と、そのため免除を適用しないことを標準エラー出力へ提示する。
- 要件6: S を解決できない場合は判定不能として失敗し、理由を標準エラー出力へ提示する（現行動作を維持する）。
- 要件7: S が空の場合、R も空とし、成果物欠落を理由に失敗しない（現行動作を維持する）。
- 要件8: 欠落による失敗メッセージは、セグメント名と欠落した成果物名を含む。閉包追加対象として R に加わったセグメントについては、当該セグメントが開始されていないこと自体が失敗理由であると読み取れる情報を含める。
- 要件9: 対象集合 R の導出は、単体テストから直接実行できる実行可能アセット（`.agent-skill-chain/` 配下のスクリプトまたは CLI）側で行い、CI ワークフロー定義のシェル記述へ埋め込まない。
- 要件10: CI ワークフローは、必須成果物検査を実行するステップが quick シグナルを解決できるだけの権限とトークンを与える。配布テンプレート（`.agent-skill-chain/templates/github/.github/workflows/`）と展開結果（`.github/workflows/`）は同一に保つ。

## 制約

- `.agent-skill-chain/config/segments.yaml` のセグメント定義・`outputs` は変更しない（セグメント自体の変更は破壊的変更に該当するため）。
- quick 免除の対象成果物集合・ガードレール対象パス・シグナルの置き場所（ADR-0022 が定める規則）は変更しない。
- セグメント順序はセグメント定義の連鎖から導出し、実装側へ固定列を再定義しない。
- 不変条件 I2（GitHub モードではゲートはガイドライン）を変更しない。本 Issue は CI の機械的検査のみを扱う。

## 完了条件

- quick 免除が解除された Issue で `DESIGN.md`・`PLAN.md` が存在しない PR について、CI の必須検査が失敗する。
- quick 免除が有効な Issue で、免除対象の成果物が無いことを理由に CI が失敗しない。
- 通常の主経路（要求・要件から順に checkpoint する PR、およびコード変更を伴わない文書のみの Issue）が失敗しない。
- 上記を検証する自動テストが存在する。

## 判定入力の次元（網羅・排他の根拠）

評価対象を2つに分ける。評価対象Aは「1つのブランチ状態に対する必須成果物検査の実行結果」、評価対象Bは「リポジトリ内の CI ワークフロー定義ファイルの内容」である。両者は観測対象が異なり、1つの観測が同時に両方の評価対象になることはない。

評価対象Aの入力次元と状態は次のとおりで、判定は次元1から次元4の順に進み、先に確定した段階でその受入条件が定まる。

- 次元1（検査実行可否）: K1=検査対象外の PR（ブランチ名から Issue 番号を解決できず、Dependabot または root-cleanup の許可条件に一致する）／K2=検査対象の PR。
- 次元2（S の形状。K2 のときのみ評価）: S1=S を解決できない／S2=S が空／S3=S が空でなく閉包追加対象が無い（実装セグメントが S に無いまま独立検証セグメントが S にある文書のみの Issue を含む）／S4=S が空でなく閉包追加対象が有る。
- 次元3（quick シグナルの解決結果。S3・S4 のときのみ評価）: E=quick 要求かつ免除有効／N=quick 未要求／B=quick 要求かつ免除解除／U=シグナルを解決できない。
- 次元4（R の必須成果物の充足）: F=すべて存在／M=1つ以上欠落。

各状態の組み合わせと受入条件の対応は次のとおりで、すべての組み合わせがちょうど1つの受入条件に対応する（網羅）。かつ、どの2つの受入条件も前提が同時に成立しない（排他）。

| 組み合わせ | 対応 |
|---|---|
| K1 | AC-1 |
| K2・S1 | AC-2 |
| K2・S2 | AC-3 |
| K2・(S3 または S4)・E・F | AC-4 |
| K2・(S3 または S4)・E・M | AC-5 |
| K2・S3・N・F | AC-6 |
| K2・S3・N・M | AC-7 |
| K2・S4・N・F | AC-8 |
| K2・S4・N・M | AC-9 |
| K2・(S3 または S4)・B・F | AC-10 |
| K2・(S3 または S4)・B・M | AC-11 |
| K2・(S3 または S4)・U・F | AC-12 |
| K2・(S3 または S4)・U・M | AC-13 |

評価対象Bの入力次元は「必須成果物検査を実行するステップへの権限・トークン付与の有無」の1次元であり、付与ありが AC-14 に対応する。付与なしは本 Issue の完了条件を満たさない状態であり、AC-14 の不成立として検出される。

## 受入条件（Acceptance Criteria）

#### AC-1: 検査対象外の PR では必須成果物検査を実行しない

- Given: ブランチ名から Issue 番号を解決できず、Dependabot または root-cleanup の許可条件に一致する PR がある
- When: CI の検査ジョブを実行する
- Then: 必須成果物検査のステップは実行されず、成果物欠落を理由にジョブが失敗しない
- 検証方法見込み: `automated`

#### AC-2: 開始済みセグメント集合を解決できない場合は判定不能として失敗する

- Given: 検査対象の PR であり、base ブランチを解決できないため開始済みセグメント集合を求められない
- When: 必須成果物検査の対象集合を導出する
- Then: 対象集合を導出せずに失敗（終了コードは0以外）し、base ブランチを解決できない旨を標準エラー出力へ提示する
- 検証方法見込み: `automated`

#### AC-3: 開始済みセグメントが1つも無い場合は対象集合も空になる

- Given: 検査対象の PR であり、開始済みセグメント集合を解決できたがその集合が空である
- When: 必須成果物検査の対象集合を導出する
- Then: 対象集合は空となり、必須成果物検査は1回も実行されず、成果物欠落を理由に失敗しない
- 検証方法見込み: `automated`

#### AC-4: quick 免除が有効なときは閉包追加を行わず、非免除成果物が揃えば成功する

- Given: 検査対象の PR であり、開始済みセグメント集合が空でなく、quick が要求されかつ免除が有効であり、閉包追加対象の有無を問わず開始済みセグメントの非免除成果物がすべて存在する
- When: 必須成果物検査を実行する
- Then: 対象集合は開始済みセグメント集合と等しくなり、免除対象の成果物ファイルは要求されず、検査は成功する（終了コード0）
- 検証方法見込み: `automated`

#### AC-5: quick 免除が有効でも非免除成果物が欠ければ失敗する

- Given: 検査対象の PR であり、開始済みセグメント集合が空でなく、quick が要求されかつ免除が有効であり、開始済みセグメントの非免除成果物に1つ以上の欠落がある
- When: 必須成果物検査を実行する
- Then: 検査は失敗し、欠落したセグメント名と成果物名を標準エラー出力へ提示する
- 検証方法見込み: `automated`

#### AC-6: quick 未要求で閉包追加対象が無く成果物が揃う主経路は成功する

- Given: 検査対象の PR であり、quick が要求されておらず、閉包追加対象が無く（要求・要件から順に checkpoint した PR、および実装セグメントが開始済みでないまま独立検証セグメントが開始済みである文書のみの Issue を含む）、対象集合の必須成果物がすべて存在する
- When: 必須成果物検査を実行する
- Then: 実装セグメントは閉包追加対象に含まれないため対象集合へ加わらず、検査は成功する（終了コード0）
- 検証方法見込み: `automated`

#### AC-7: quick 未要求で閉包追加対象が無く成果物が欠ければ失敗する

- Given: 検査対象の PR であり、quick が要求されておらず、閉包追加対象が無く、開始済みセグメントの必須成果物に1つ以上の欠落がある
- When: 必須成果物検査を実行する
- Then: 検査は失敗し、欠落したセグメント名と成果物名を標準エラー出力へ提示する
- 検証方法見込み: `automated`

#### AC-8: quick 未要求で閉包追加が起きても追加分の成果物が存在すれば成功する

- Given: 検査対象の PR であり、quick が要求されておらず、閉包追加対象が有るが、追加されたセグメントの必須成果物が未追跡ファイル等として実際に存在する
- When: 必須成果物検査を実行する
- Then: 追加されたセグメントも対象集合に含めたうえで欠落が無いと判定し、検査は成功する（終了コード0）
- 検証方法見込み: `automated`

#### AC-9: quick 未要求で閉包追加分の成果物が欠ければ失敗する

- Given: 検査対象の PR であり、quick が要求されておらず、より下流のセグメントが開始済みである一方で設計・実装計画セグメントが開始されておらず、`DESIGN.md`・`PLAN.md` がファイルとしても履歴としても存在しない
- When: 必須成果物検査を実行する
- Then: 設計・実装計画セグメントが対象集合へ加えられ、検査は失敗し、当該セグメントが開始されていないこと自体が失敗理由であると読み取れる情報とともに欠落成果物名を標準エラー出力へ提示する
- 検証方法見込み: `automated`

#### AC-10: quick 免除が解除されても閉包後の必須成果物が揃えば成功する

- Given: 検査対象の PR であり、開始済みセグメント集合が空でなく、quick が要求されているがリスク区分またはガードレール抵触により免除が解除され、閉包追加対象の有無を問わず対象集合の必須成果物がすべて存在する
- When: 必須成果物検査を実行する
- Then: 免除を適用せずに判定し、解除理由を標準エラー出力へ提示したうえで検査は成功する（終了コード0）
- 検証方法見込み: `automated`

#### AC-11: quick 免除が解除され閉包後の必須成果物が欠ければ失敗する（ISSUE-692 の再現条件）

- Given: 検査対象の PR であり、quick が要求されているがガードレール抵触（変更差分がスキーマ定義を含む等）により免除が解除され、実装セグメント・独立検証セグメントが開始済みである一方で設計・実装計画セグメントが開始されておらず `DESIGN.md`・`PLAN.md` が存在しない
- When: CI の検査ジョブを実行する
- Then: 設計・実装計画セグメントが対象集合へ加えられ、必須検査が失敗し、解除理由と欠落成果物名の両方を標準エラー出力へ提示する
- 検証方法見込み: `automated`

#### AC-12: quick シグナルを解決できないときは免除を適用せず、成果物が揃えば成功する

- Given: 検査対象の PR であり、開始済みセグメント集合が空でなく、quick シグナルの取得（GitHub モードでは Issue ラベル、ローカルモードでは Issue 状態ファイル）に失敗して要求の有無を判定できず、対象集合の必須成果物がすべて存在する
- When: 必須成果物検査を実行する
- Then: 免除を適用しない扱いで閉包追加を行ったうえで判定し、シグナルを解決できないため免除を適用しない旨を標準エラー出力へ提示し、検査は成功する（終了コード0）
- 検証方法見込み: `automated`

#### AC-13: quick シグナルを解決できないときに成果物が欠ければ失敗する

- Given: 検査対象の PR であり、開始済みセグメント集合が空でなく、quick シグナルの取得に失敗して要求の有無を判定できず、対象集合の必須成果物に1つ以上の欠落がある
- When: 必須成果物検査を実行する
- Then: 検査は失敗し、シグナルを解決できないため免除を適用しない旨と欠落成果物名の両方を標準エラー出力へ提示する
- 検証方法見込み: `automated`

#### AC-14: CI ワークフローが quick シグナル解決に必要な権限とトークンを付与している

- Given: リポジトリ内の CI ワークフロー定義ファイル（配布テンプレートと展開結果の両方）がある
- When: 定義内容を検査する
- Then: 必須成果物検査を実行するステップが Issue ラベルを取得できる権限とトークンを与えられており、配布テンプレートと展開結果の内容が一致する
- 検証方法見込み: `automated`

## 未決事項

なし。quick シグナルを解決できない場合の扱い（免除を適用せず閉包を適用し、理由を提示する）、閉包追加対象の範囲（要求・要件セグメントと設計・実装計画セグメントのみ）、Draft 状態への非依存の3点は本 SPEC で確定済みとする。

## スコープ外

- 開始済みセグメント判定そのものの用途変更。成果物が存在する場合にのみ意味を持つ他の検査（SPEC の BDD 記載検査・DESIGN の図示要否検査・AC 対応検査）の分岐条件は本 Issue では変更しない。
- quick 免除の対象成果物集合・ガードレール対象パス・シグナル置き場所の変更（ADR-0022 の規則の変更）。
- セグメント定義（`.agent-skill-chain/config/segments.yaml`）の追加・変更。
- ゲートレビューの自動強制化。GitHub モードで不変条件 I2 がガイドラインである点は変更しない。
- マージ準備完了 PR で root 直下成果物検査が常に失敗する設計（ADR-0046）の変更。
- 実装セグメント・独立検証セグメントを閉包追加対象に含めること。必要になった場合は別 Issue で扱う。
- ISSUE-692 自体の是正（設計セグメントへの差し戻し）。進行役が別途行う。
