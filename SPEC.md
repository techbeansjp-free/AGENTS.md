# SPEC: lint-vocab: isInSingleLineCommentが文字列リテラル内のコメント記号を誤ってコメント開始と判定する

- Issue: `ISSUE-487`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/487-lint-vocab-comment-marker-in-string`

## 目的・背景

Issue #484（PR #485）で追加した `isInSingleLineComment(line, pos, ext)` は、`commentMarkerFor(ext)` が返すコメント記号（`.ts` の `//`、`.sh`/`.yaml`/`.yml` の `#`）を `line.indexOf(marker)` で単純検索し、その位置が `pos` より前かどうかだけでコメント内文脈を判定する。この検索は文字列リテラル（`'...'`・`"..."`）の内部かどうかを一切考慮しない。

このため、同一行に文字列リテラルとしてコメント記号相当の文字列（例: URLの `//`、URLフラグメントの `#`）を含み、かつその後ろに `isQuotedLiteralContext`（Issue #469、ADR-0036）が対象とするコード値リテラル（配列要素・関数呼び出し引数）が存在する場合、後者が「コメント内」と誤判定され、コード値リテラル文脈としての除外が働かず、禁止語の誤検知が再発する。ADR-0037はこの限界を既知の制約としてスコープ外に明示していたが、Codexによるstop-time reviewが実際に発生しうる誤検知として修正を求めた。

本Issueは、コメント記号検索を文字列リテラルの外側限定に是正し、この誤検知を解消することを目的とする。

## 要求 → 要件 → 受入条件

### 要求

ユーザー（開発者）は、`lint vocab` がコード中の文字列リテラル（URL等）に含まれる `//`・`#` をコメント開始と誤判定して、その後ろにある正当なコード値リテラル（例: 配列要素としての `'issue'`）を誤って禁止語として検出しないことを求める。同時に、文字列リテラルの外側にある実際のコメント中の禁止語は、Issue #484で解消済みの誤検知抑制のとおり、引き続き検出され続けることを求める。

### 要件

- 要件1: コメント記号検索（`isInSingleLineComment` またはその実体）は、単一引用符・二重引用符で囲まれた文字列リテラル内部に出現するコメント記号を無視し、文字列リテラルの外側で最初に出現するコメント記号の位置だけを「コメント開始」として扱う。
- 要件2: 文字列リテラルの外側に実在するコメント記号以降の禁止語は、既存どおり（Issue #484／PR #485の挙動）コメント内文脈として検出され続ける。
- 要件3: `commentMarkerFor(ext)` の拡張子対応表（`.ts` → `//`、`.sh`/`.yaml`/`.yml` → `#`）、および `isQuotedLiteralContext` の既存の境界文字集合判定自体は変更しない。
- 要件4: バックスラッシュエスケープ（`\'`・`\"`）の考慮は行ってよいが、必須要件としない。
- 要件5: テンプレートリテラル（バッククォート）内の式展開（`${...}`）等の高度な `.ts` 文字列構文の完全な解析、複数行にまたがる文字列リテラル・複数行コメント（`/* ... */`）の解析は対象としない。

### 受入条件（Acceptance Criteria）

#### AC-1: 文字列リテラル内のコメント記号相当文字列は後続のコード値リテラル除外判定を阻害しない

- Given: `.ts` ファイルの1行に、単一引用符で囲まれたURL文字列リテラル（`//` を含む）と、その後ろに配列要素として単一引用符で囲まれた禁止語と同一のコード値リテラル（例: `const cfg = { url: 'https://example.com', tags: ['issue', 'other'] };`）が存在する
- When: `lint vocab` を当該ファイルへ実行する
- Then: 当該行の禁止語一致は `isQuotedLiteralContext` によるコード値リテラル除外が正しく適用され、禁止語違反として報告されない
- 検証方法見込み: `automated`

#### AC-2: 文字列リテラル外の実コメント中の禁止語は引き続き検出される

- Given: `.ts` ファイルの1行に、文字列リテラルを含まない実際の行コメント（`//`）があり、その後ろに禁止語がコメント文として存在する
- When: `lint vocab` を当該ファイルへ実行する
- Then: 当該コメント内の禁止語は禁止語違反として報告される（Issue #484／PR #485で確立した挙動が非退行である）
- 検証方法見込み: `automated`

#### AC-3: 文字列リテラル内の禁止語コード値リテラルと、その後ろの実コメント中の禁止語が同一行に共存する場合、双方が個別に正しく判定される

- Given: `.ts` ファイルの1行に、配列要素として単一引用符で囲まれた禁止語と同一のコード値リテラルがあり、かつその後ろに文字列リテラル外の実際の行コメント（`//`）中に禁止語が存在する
- When: `lint vocab` を当該行へ実行する
- Then: コード値リテラルとしての禁止語一致は除外され報告されず、実コメント中の禁止語一致は報告される
- 検証方法見込み: `automated`

#### AC-4: `.sh`/`.yaml`/`.yml` の `#` コメント記号でも同様に文字列リテラル内は無視される

- Given: `.sh`（または `.yaml`/`.yml`）ファイルの1行に、`#` を含む引用符付き文字列リテラル（例: URLフラグメント）と、その後ろにコード値リテラル文脈上の禁止語一致が存在する
- When: `lint vocab` を当該ファイルへ実行する
- Then: 当該禁止語一致はコード値リテラル除外が正しく適用され、禁止語違反として報告されない
- 検証方法見込み: `automated`

#### AC-5: リポジトリ全体への実行で既存の禁止語違反件数が悪化しない

- Given: 本リポジトリの現在のソースツリー全体
- When: `lint vocab` をリポジトリ全体へ実行する
- Then: 修正適用前に検出されていなかった禁止語違反が新たに検出されない（exit code・違反件数が修正前と同じか改善する）
- 検証方法見込み: `hybrid`

## スコープ外

- テンプレートリテラル（バッククォート）内の式展開（`${...}`）等、`.ts` の高度な文字列構文の完全な解析。
- コメント記号検索以外の既存判定（`isQuotedLiteralContext` の境界文字集合判定自体、`commentMarkerFor` の拡張子対応表）の変更。
- 複数行にまたがる文字列リテラル・複数行コメント（`/* ... */`）の解析。
- バックスラッシュエスケープ（`\'`・`\"`）を厳密に扱うことの必須化（実装が採用してもよいが要求しない）。
