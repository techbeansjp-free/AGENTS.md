# SPEC: quick免除が解除されたIssueでも成果物が1つも無いセグメントが必須成果物検査の対象にならない欠陥の是正

- Issue: `ISSUE-741`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/741-verify-artifacts-unstarted-segments`

## 目的・背景

必須成果物検査（`.agent-skill-chain/ci/verify-artifacts.sh`）は、CI から「開始済みセグメント」ごとに1回ずつ実行される。あるセグメントが開始済みと判定される唯一の根拠は、そのセグメントの成果物がブランチ差分に現れることである。したがって成果物が1つも作られていないセグメントは検出されず、その必須成果物の欠落が永久に検査されない。成果物が欠けている場合こそ検査が必要なのに、欠けていること自体が検査をスキップさせる。

実測（ISSUE-692 / PR #695）では、quick 免除が解除される条件（変更差分にスキーマ定義を含む）に該当し `DESIGN.md`・`PLAN.md` が必須であったにもかかわらず、両方とも存在しないために設計セグメントが開始済みと判定されず、必須成果物検査自体が走らないまま CI が緑になった。欠落を発見したのは独立検証セグメントのレビュアであり、レビュアは AGENTS.md の不変条件 I2 においてガイドライン扱いで自動強制を持たない。結果として、quick 免除の解除条件（影響が大きい変更に通常フローを強制するための条件）そのものが機械的強制を失っている。AGENTS.md が不変条件 I1 の検査手段として名指しする必須成果物検査を、当該条件下で実効化することが本 Issue の目的である。

## 用語（本 SPEC 内での定義）

- 必須成果物検査: 指定した Issue と1つのセグメントについて、`.agent-skill-chain/config/segments.yaml` の `outputs` が定める必須成果物の充足を判定し、充足なら終了コード0、欠落なら終了コード1を返す検査。
- 成果物存在述語: 1つの必須成果物について「存在する」を判定する、本 SPEC が用いる唯一の観測規則。成果物の種別ごとに次の (i)〜(iii) のいずれか1つだけを用い、これ以外の観測（ブランチ差分への出現、base 側コミットの内容など）を「存在する」の根拠に加えない。
  - (i) ファイル型（`SPEC.md`・`DESIGN.md`・`PLAN.md`、および `VALIDATION.md` の存在で代替判定される受入テスト結果・回帰テスト結果）: 「検査時のワークツリー上に当該ファイルが存在する」または「base から現ブランチ HEAD までのコミット履歴に当該ファイルの追加または変更が1回以上現れる」のいずれかが成り立つとき存在する。履歴に削除だけが現れる場合は根拠にしない。
  - (ii) ディレクトリ型（ADR）: 検査時のワークツリーの `docs/adr/` 配下に `.md` ファイルが1つ以上あるとき存在する。
  - (iii) 差分型（コード・単体テスト結果）: base と現ブランチ HEAD の差分に、当該成果物に対応するパスの変更が1つ以上現れるとき存在する。
- 成果物削除ケースの帰結: 上記述語により、ワークツリー上に無いファイル型成果物の扱いは一意に定まる。当ブランチで追加した後に削除した成果物は「存在する」（成果物ファイルの削除自体を目的とする Issue を自己言及的に失敗させないため）。base 側にのみ存在し当ブランチで削除した成果物は、ワークツリーに無く履歴にも追加・変更が現れないため「存在しない」。いずれも下記の次元4の判定へ吸収され、独立した次元を成さない。
- 開始済みセグメント集合: base と現ブランチ HEAD の差分に現れたファイルパスから導出される、着手済みと推定されるセグメントの集合。差分には追加・変更・削除のすべてを含む。以下 S と呼ぶ。
- セグメント順序: `.agent-skill-chain/config/segments.yaml` の連鎖が定める固定順（要求・要件 → 設計・実装計画 → 実装 → 独立検証）。「上流」「下流」はこの順序に基づく。
- 対象集合: 必須成果物検査を実際に適用するセグメントの集合。以下 R と呼ぶ。現状は R が S と等しいことが本欠陥の原因である。
- 閉包追加対象: S が空でないとき、S の最も下流の要素より上流にありながら S に含まれない、要求・要件セグメントまたは設計・実装計画セグメント。実装セグメントと独立検証セグメントは閉包追加対象に含めない。
- quick 免除: 軽量な変更向けに Issue スコープ成果物ファイルの作成義務を免除する規則（ADR-0022）。シグナルは GitHub モードでは Issue ラベル、ローカルモードでは Issue 状態ファイルにのみ置かれる。
- quick 免除の解除: quick が要求されているが、リスク区分が normal でない、または変更差分がガードレール対象（ADR 配下・セグメント定義・AGENTS.md・スキーマ定義）を含むために免除を適用しないこと。
- quick シグナルの解決不能: quick 要求の有無を判定するための入力自体を取得できないこと。GitHub モードでは Issue ラベルを取得できない場合（権限・トークン未付与、API エラー、レート制限、当該 Issue 不在を含む）、ローカルモードでは Issue 状態ファイルを読めない場合を指す。

## 前提

- 本 Issue は GitHub Coordination Backend 上で進行し、quick シグナルの正本は Issue ラベルである。ローカルモードでは Issue 状態ファイルが正本となる。
- CI の検査ジョブはステップを順に実行し、先行ステップが失敗した時点でジョブが停止する。
- マージ準備完了（Draft ではない）PR に対しては root 直下成果物の検査が設計上つねに失敗する（ADR-0046）。この検査は必須成果物検査より前のステップにあるため、Draft か否かで強制の有無を切り替える設計は、非 Draft 側では必須成果物検査へ到達せず目的を達成できない。よって本 Issue の強制は Draft 状態に依存しない。
- quick シグナル解決は、GitHub モードでは Issue ラベルの取得に成功することを前提とする。実測では、現在の CI ワークフローは必須成果物検査を実行するステップへ Issue 読み取り権限とトークンを与えていないため、CI 上ではシグナルがつねに解決不能であり、quick 免除は CI では一度も適用されていない。すなわち現在 quick の PR が緑である理由は免除ではなく、R が S と等しく開始済みセグメントの成果物しか要求されないことである。

## 要求

quick シグナルを解決できる実行環境において、quick 免除が適用されない Issue（免除が解除された Issue、および quick が要求されていない Issue）については、必須成果物の欠落が CI で必ず検出されること。設計・実装計画セグメントの成果物が1つも存在しない場合を含む。

同時に、次の PR が本変更によって新たに失敗しないこと。

- 要求・要件から独立検証まで順に checkpoint する PR。
- quick 免除が有効な PR（quick が要求され、リスク区分・ガードレールのいずれにも抵触しない PR）。
- quick シグナルを解決できない実行環境で実行されるすべての PR（本変更を含む CI ワークフローが未展開の配布先、API の一時障害・レート制限・権限未付与により Issue ラベルを取得できない環境を含む）。

例外（意図した挙動変更）: quick シグナルを解決できる実行環境において、quick 免除が適用されず、かつ上流セグメント（要求・要件／設計・実装計画）の必須成果物が成果物存在述語を満たさないまま下流セグメントだけを開始した PR は、本変更によって失敗するようになる。これは本 Issue が塞ぐ対象の穴そのものであるため、上記「新たに失敗しないこと」の対象から除外する。該当する PR は、上流成果物を作成するか、quick 免除の条件（quick 要求かつリスク区分 normal かつガードレール非抵触）を満たすことで解消できる。

## シグナル解決不能時の方針と配布先への影響

シグナルを解決できない場合の扱いは、要求側に例外を明記する方針と、要件5 を劣化方針つきで改める方針を併用して確定する。理由は次のとおり。

- 強制が有効になる条件（シグナルを解決できること）は CI ワークフロー定義の内容として静的に検査でき、実行時の外部応答に依存しない。どの環境で強制が効くかを配布物の側で確定できる。
- シグナル解決不能時に強制を強めると、ラベル取得の一時障害だけで無関係な PR が赤くなる。本検査は成果物作成の規律に関するものでセキュリティ低下・データ喪失には関与しないため、一時障害で赤くする不利益（強制への信頼低下と迂回の誘発）が、穴を一時的に開けたままにする不利益を上回る。
- シグナル解決不能時に R を S と等しくすると、当該環境での判定は本変更の適用前と完全に同一になり、「本変更によって新たに赤くなる PR は存在しない」と環境単位で言い切れる。

配布先（consumer project）で起きること。

- 本変更を含む CI ワークフローが未展開の配布先、および Issue ラベルを取得できない配布先: シグナルを解決できないため R は S と等しく、挙動は本変更前と同一で、CI が新たに赤くなることはない。
- 本変更を含む CI ワークフローを展開済みで、Issue ラベルを取得できる配布先: 閉包追加が有効になる。ブランチ名から Issue 番号を解決でき、かつ上流セグメントの成果物が存在しないまま下流セグメントだけを開始している PR は赤くなる（上記の意図した挙動変更に該当する）。quick 要求かつリスク区分 normal かつガードレール非抵触の Issue は免除により従来どおり緑になる。

## 要件

- 要件1: 対象集合 R は、quick 免除を適用せず、かつ quick シグナルを解決できた場合、S に閉包追加対象を加えた集合とする。すなわち S が空でなければ、S の最も下流の要素より上流にある要求・要件セグメント・設計・実装計画セグメントを、開始済みでなくても R に含める。
- 要件2: 実装セグメントと独立検証セグメントは閉包追加対象に含めない。これらの必須成果物は差分そのもの（コード変更・テスト変更）であり、コード変更を伴わない Issue には産出物が存在しないため、強制すると正当な文書のみの Issue を失敗させる。
- 要件3: quick 免除が有効な場合、R は S と等しく、免除対象の成果物は要求しない（現行動作を維持する）。
- 要件4: quick 免除が解除された場合、R は要件1に従って導出し、解除理由を標準エラー出力へ提示する（解除理由の提示は現行動作を維持する）。
- 要件5: quick シグナルを解決できない場合、免除を適用せず、かつ閉包追加も行わない。すなわち R は S と等しく、本変更の適用前と同一の対象集合になる。シグナルを解決できなかった旨と、そのため免除も閉包追加も適用しないことを標準エラー出力へ提示する。一時的失敗（API エラー・レート制限）と恒久的な未付与（権限・トークン未設定）は区別しない——観測側から区別できないため、いずれも同一の劣化挙動をとる。
- 要件6: S を解決できない場合は判定不能として失敗し、理由を標準エラー出力へ提示する（現行動作を維持する）。
- 要件7: S が空の場合、R も空とし、成果物欠落を理由に失敗しない（現行動作を維持する）。
- 要件8: 欠落による失敗メッセージは、セグメント名と欠落した成果物名を含む。閉包追加対象として R に加わったセグメントについては、当該セグメントが開始されていないこと自体が失敗理由であると読み取れる情報を含める。
- 要件9: 対象集合 R の導出は、単体テストから直接実行できる実行可能アセット（`.agent-skill-chain/` 配下のスクリプトまたは CLI）側で行い、CI ワークフロー定義のシェル記述へ埋め込まない。
- 要件10: CI ワークフローは、必須成果物検査を実行するステップが quick シグナルを解決できるだけの権限とトークンを与える。配布テンプレート（`.agent-skill-chain/templates/github/.github/workflows/`）と展開結果（`.github/workflows/`）は同一に保つ。
- 要件11: 検査対象の判定（ブランチ名からの Issue 番号解決と、解決できない場合の許可条件の照合）と、その結果に応じた検査実行可否は現行動作を維持する。Issue 番号を解決できず許可条件のいずれにも一致しない PR は、必須成果物検査へ到達する前に判定不能として失敗させる。検査対象か否かを確定できないまま検査を省略すると、規約に適合しないブランチが強制を回避できるためである。

## 制約

- `.agent-skill-chain/config/segments.yaml` のセグメント定義・`outputs` は変更しない（セグメント自体の変更は破壊的変更に該当するため）。
- quick 免除の対象成果物集合・ガードレール対象パス・シグナルの置き場所（ADR-0022 が定める規則）は変更しない。
- 成果物存在述語を変更しない。本 Issue が変えるのは R の導出のみであり、個々の成果物の存在判定規則は現行のまま用いる。
- セグメント順序はセグメント定義の連鎖から導出し、実装側へ固定列を再定義しない。
- 不変条件 I2（GitHub モードではゲートはガイドライン）を変更しない。本 Issue は CI の機械的検査のみを扱う。

## 完了条件

- quick 免除が解除された Issue で `DESIGN.md`・`PLAN.md` が存在しない PR について、quick シグナルを解決できる環境で CI の必須検査が失敗する。
- quick 免除が有効な Issue で、免除対象の成果物が無いことを理由に CI が失敗しない。
- quick シグナルを解決できない環境で、対象集合が本変更の適用前と同一（R が S と等しい）になる。
- 通常の主経路（要求・要件から順に checkpoint する PR、およびコード変更を伴わない文書のみの Issue）が失敗しない。
- 対象集合 R の導出を、CI ワークフローを起動せずに単体テストから直接呼び出して検証できる。
- 上記を検証する自動テストが存在する。

## 判定入力の次元（網羅・排他の根拠）

評価対象を3つに分ける。評価対象Aは「1つのブランチ状態に対する必須成果物検査の実行結果」、評価対象Bは「リポジトリ内の CI ワークフロー定義ファイルの内容」、評価対象Cは「対象集合 R の導出ロジックの所在」である。3者は観測対象が異なり、1つの観測が同時に複数の評価対象になることはない。

評価対象Aの入力次元と状態は次のとおりで、判定は次元1から次元4の順に進み、先に確定した段階でその受入条件が定まる。

- 次元1（検査実行可否）: K1=ブランチ名から Issue 番号を解決できないが許可条件（Dependabot ブランチかつ作成者が Dependabot、または root-cleanup ブランチかつ作成者が admin 権限）に一致する／K2=ブランチ名から Issue 番号を解決できる／K3=ブランチ名から Issue 番号を解決できず、許可条件のいずれにも一致しない（root-cleanup 形式だが作成者が admin 権限を持たない場合・作成者権限を確認できない場合を含む）。K2 は Issue 番号を解決できる場合、K1 と K3 は解決できない場合であり、K1 と K3 は許可条件一致の有無で排反する。3状態で網羅する。
- 次元2（S の形状。K2 のときのみ評価）: S1=S を解決できない／S2=S が空／S3=S が空でなく閉包追加対象が無い（実装セグメントが S に無いまま独立検証セグメントが S にある文書のみの Issue を含む）／S4=S が空でなく閉包追加対象が有る。
- 次元3（quick シグナルの解決結果。S3・S4 のときのみ評価）: E=解決成功かつ quick 要求かつ免除有効／N=解決成功かつ quick 未要求／B=解決成功かつ quick 要求かつ免除解除／U=シグナルを解決できない。
- 次元4（R の必須成果物の充足。R が空でないときのみ評価）: F=R の各セグメントについて要求される必須成果物（quick 免除が有効な場合は免除対象を除いたもの）がすべて成果物存在述語を満たす／M=1つ以上が成果物存在述語を満たさない。

各状態の組み合わせと受入条件の対応は次のとおりで、すべての組み合わせがちょうど1つの受入条件に対応する（網羅）。かつ、どの2つの受入条件も前提が同時に成立しない（排他）。E と U では閉包追加を行わないため R が S と等しくなり、S3 と S4 の区別が結果に影響しないため、両者を1行にまとめる。

| 組み合わせ | 対応 |
|---|---|
| K1 | AC-1 |
| K3 | AC-16 |
| K2・S1 | AC-2 |
| K2・S2 | AC-3 |
| K2・(S3 または S4)・E・F | AC-4 |
| K2・(S3 または S4)・E・M | AC-5 |
| K2・S3・N・F | AC-6 |
| K2・S3・N・M | AC-7 |
| K2・S4・N・F | AC-8 |
| K2・S4・N・M | AC-9 |
| K2・(S3 または S4)・B・F | AC-10 |
| K2・(S3 または S4)・B・M | AC-11 |
| K2・(S3 または S4)・U・F | AC-12 |
| K2・(S3 または S4)・U・M | AC-13 |

評価対象Bの入力次元は「必須成果物検査を実行するステップへの権限・トークン付与の有無」の1次元であり、付与ありが AC-14 に対応する。付与なしは本 Issue の完了条件を満たさない状態であり、AC-14 の不成立として検出される。

評価対象Cの入力次元は「R の導出ロジックの所在」の1次元であり、実行可能アセット側（単体テストから直接呼び出せる）が AC-15 に対応する。CI ワークフロー定義のシェル記述側にある状態は AC-15 の不成立として検出される。

## 受入条件（Acceptance Criteria）

#### AC-1: 許可条件に一致する検査対象外の PR では必須成果物検査を実行しない

- Given: ブランチ名から Issue 番号を解決できないが、Dependabot ブランチ（作成者が Dependabot）または root-cleanup ブランチ（作成者が admin 権限を持つ）の許可条件に一致する PR がある
- When: CI の検査ジョブを実行する
- Then: 必須成果物検査のステップは実行されず、成果物欠落を理由にジョブが失敗しない
- 検証方法見込み: `automated`

#### AC-2: 開始済みセグメント集合を解決できない場合は判定不能として失敗する

- Given: 検査対象の PR であり、base ブランチを解決できないため開始済みセグメント集合を求められない
- When: 必須成果物検査の対象集合を導出する
- Then: 対象集合を導出せずに失敗（終了コードは0以外）し、base ブランチを解決できない旨を標準エラー出力へ提示する
- 検証方法見込み: `automated`

#### AC-3: 開始済みセグメントが1つも無い場合は対象集合も空になる

- Given: 検査対象の PR であり、開始済みセグメント集合を解決できたがその集合が空である
- When: 必須成果物検査の対象集合を導出する
- Then: 対象集合は空となり、必須成果物検査は1回も実行されず、成果物欠落を理由に失敗しない
- 検証方法見込み: `automated`

#### AC-4: quick 免除が有効なときは閉包追加を行わず、非免除成果物が揃えば成功する

- Given: 検査対象の PR であり、開始済みセグメント集合が空でなく、quick シグナルを解決できて quick が要求されかつ免除が有効であり、閉包追加対象の有無を問わず開始済みセグメントの非免除成果物がすべて成果物存在述語を満たす
- When: 必須成果物検査を実行する
- Then: 対象集合は開始済みセグメント集合と等しくなり、免除対象の成果物ファイルは要求されず、検査は成功する（終了コード0）
- 検証方法見込み: `automated`

#### AC-5: quick 免除が有効でも非免除成果物が欠ければ失敗する

- Given: 検査対象の PR であり、開始済みセグメント集合が空でなく、quick シグナルを解決できて quick が要求されかつ免除が有効であり、開始済みセグメントの非免除成果物のうち1つ以上が成果物存在述語を満たさない
- When: 必須成果物検査を実行する
- Then: 検査は失敗し、欠落したセグメント名と成果物名を標準エラー出力へ提示する
- 検証方法見込み: `automated`

#### AC-6: quick 未要求で閉包追加対象が無く成果物が揃う主経路は成功する

- Given: 検査対象の PR であり、quick シグナルを解決できて quick が要求されておらず、閉包追加対象が無く（要求・要件から順に checkpoint した PR、および実装セグメントが開始済みでないまま独立検証セグメントが開始済みである文書のみの Issue を含む）、対象集合の必須成果物がすべて成果物存在述語を満たす
- When: 必須成果物検査を実行する
- Then: 実装セグメントは閉包追加対象に含まれないため対象集合へ加わらず、検査は成功する（終了コード0）
- 検証方法見込み: `automated`

#### AC-7: quick 未要求で閉包追加対象が無く成果物が欠ければ失敗する

- Given: 検査対象の PR であり、quick シグナルを解決できて quick が要求されておらず、閉包追加対象が無く、開始済みセグメントの必須成果物のうち1つ以上が成果物存在述語を満たさない（base 側にのみ存在した成果物ファイルを当ブランチが削除した場合を含む）
- When: 必須成果物検査を実行する
- Then: 検査は失敗し、欠落したセグメント名と成果物名を標準エラー出力へ提示する
- 検証方法見込み: `automated`

#### AC-8: quick 未要求で閉包追加が起きても追加分の成果物が存在すれば成功する

- Given: 検査対象の PR であり、quick シグナルを解決できて quick が要求されておらず、閉包追加対象が有るが、追加されたセグメントの必須成果物がすべて成果物存在述語を満たす（ブランチ差分に現れないワークツリー上のファイル、当ブランチで追加後に削除した履歴を持つファイルを含む）
- When: 必須成果物検査を実行する
- Then: 追加されたセグメントも対象集合に含めたうえで欠落が無いと判定し、検査は成功する（終了コード0）
- 検証方法見込み: `automated`

#### AC-9: quick 未要求で閉包追加分の成果物が欠ければ失敗する

- Given: 検査対象の PR であり、quick シグナルを解決できて quick が要求されておらず、より下流のセグメントが開始済みである一方で設計・実装計画セグメントが開始されておらず、`DESIGN.md`・`PLAN.md` が成果物存在述語を満たさない
- When: 必須成果物検査を実行する
- Then: 設計・実装計画セグメントが対象集合へ加えられ、検査は失敗し、当該セグメントが開始されていないこと自体が失敗理由であると読み取れる情報とともに欠落成果物名を標準エラー出力へ提示する
- 検証方法見込み: `automated`

#### AC-10: quick 免除が解除されても閉包後の必須成果物が揃えば成功する

- Given: 検査対象の PR であり、開始済みセグメント集合が空でなく、quick シグナルを解決できて quick が要求されているがリスク区分またはガードレール抵触により免除が解除され、閉包追加対象の有無を問わず対象集合の必須成果物がすべて成果物存在述語を満たす
- When: 必須成果物検査を実行する
- Then: 免除を適用せずに判定し、解除理由を標準エラー出力へ提示したうえで検査は成功する（終了コード0）
- 検証方法見込み: `automated`

#### AC-11: quick 免除が解除され閉包後の必須成果物が欠ければ失敗する（ISSUE-692 の再現条件）

- Given: 検査対象の PR であり、quick シグナルを解決できて quick が要求されているがガードレール抵触（変更差分がスキーマ定義を含む等）により免除が解除され、実装セグメント・独立検証セグメントが開始済みである一方で設計・実装計画セグメントが開始されておらず `DESIGN.md`・`PLAN.md` が成果物存在述語を満たさない
- When: CI の検査ジョブを実行する
- Then: 設計・実装計画セグメントが対象集合へ加えられ、必須検査が失敗し、解除理由と欠落成果物名の両方を標準エラー出力へ提示する
- 検証方法見込み: `automated`

#### AC-12: quick シグナルを解決できないときは本変更前と同一の対象集合を用い、成果物が揃えば成功する

- Given: 検査対象の PR であり、開始済みセグメント集合が空でなく、quick シグナルの取得（GitHub モードでは Issue ラベル、ローカルモードでは Issue 状態ファイル）に失敗して要求の有無を判定できず、開始済みセグメントの必須成果物がすべて成果物存在述語を満たす
- When: 必須成果物検査を実行する
- Then: 免除も閉包追加も適用せず、対象集合は開始済みセグメント集合と等しくなり、シグナルを解決できないため免除も閉包追加も適用しない旨を標準エラー出力へ提示し、検査は成功する（終了コード0）
- 検証方法見込み: `automated`

#### AC-13: quick シグナルを解決できないときも閉包追加は行わず、開始済み分が欠ければ失敗する

- Given: 検査対象の PR であり、開始済みセグメント集合が空でなく、quick シグナルの取得に失敗して要求の有無を判定できず、開始済みセグメントの必須成果物のうち1つ以上が成果物存在述語を満たさない
- When: 必須成果物検査を実行する
- Then: 対象集合は開始済みセグメント集合と等しく（閉包追加は行わない）、検査は失敗し、シグナルを解決できないため免除も閉包追加も適用しない旨と欠落成果物名の両方を標準エラー出力へ提示する。閉包追加対象が有る場合でも、当該セグメントの成果物欠落を理由には失敗しない
- 検証方法見込み: `automated`

#### AC-14: CI ワークフローが quick シグナル解決に必要な権限とトークンを付与している

- Given: リポジトリ内の CI ワークフロー定義ファイル（配布テンプレートと展開結果の両方）がある
- When: 定義内容を検査する
- Then: 必須成果物検査を実行するステップが Issue ラベルを取得できる権限とトークンを与えられており、配布テンプレートと展開結果の内容が一致する
- 検証方法見込み: `automated`

#### AC-15: 対象集合の導出が単体テストから直接呼び出せる実行可能アセット側にある

- Given: CI ワークフローを起動しない単体テスト実行環境があり、Issue・base ブランチ・開始済みセグメント集合・quick シグナルの解決結果を入力として与えられる
- When: 対象集合 R の導出を実行可能アセット（`.agent-skill-chain/` 配下のスクリプトまたは CLI）として直接呼び出す
- Then: R が呼び出し結果として得られ、CI ワークフロー定義のシェル記述を実行せずに閉包追加の有無を検証できる。CI ワークフロー定義側には R の導出規則（閉包追加対象の判定・上流判定）が記述されていない
- 検証方法見込み: `automated`

#### AC-16: Issue 番号を解決できず許可条件にも一致しない PR は判定不能として失敗する

- Given: ブランチ名から Issue 番号を解決できず、Dependabot・root-cleanup のいずれの許可条件にも一致しない PR がある（root-cleanup 形式のブランチだが作成者が admin 権限を持たない場合、作成者権限を確認できない場合を含む）
- When: CI の検査ジョブを実行する
- Then: 検査対象か否かを確定できないため、必須成果物検査のステップへ到達する前にジョブが終了コード0以外で停止し、Issue 番号を抽出できない旨または権限を確認できない旨を標準エラー出力へ提示する
- 検証方法見込み: `automated`

## 要件と受入条件の対応

| 要件 | 対応する受入条件 |
|---|---|
| 要件1（閉包追加による対象集合の導出） | AC-8, AC-9, AC-10, AC-11 |
| 要件2（実装・独立検証を閉包追加対象に含めない） | AC-6 |
| 要件3（quick 免除有効時は対象集合＝開始済みセグメント集合） | AC-4, AC-5 |
| 要件4（免除解除時は閉包適用と解除理由の提示） | AC-10, AC-11 |
| 要件5（シグナル解決不能時は本変更前と同一の対象集合＋理由提示） | AC-12, AC-13 |
| 要件6（開始済みセグメント集合が解決不能なら判定不能として失敗） | AC-2 |
| 要件7（開始済みセグメント集合が空なら対象集合も空） | AC-3 |
| 要件8（失敗メッセージの内容） | AC-5, AC-7, AC-9, AC-11 |
| 要件9（対象集合の導出は実行可能アセット側） | AC-15 |
| 要件10（ワークフローの権限・トークン付与とテンプレート同期） | AC-14 |
| 要件11（検査対象判定の現行動作維持） | AC-1, AC-16 |

## 未決事項

なし。quick シグナルを解決できない場合の扱い（免除も閉包追加も適用せず、対象集合を本変更前と同一に保ち、理由を提示する）、閉包追加対象の範囲（要求・要件セグメントと設計・実装計画セグメントのみ）、成果物存在述語の内容と成果物削除ケースの帰結、検査対象を確定できない PR の扱い（判定不能として失敗）、Draft 状態への非依存の5点は本 SPEC で確定済みとする。

## スコープ外

- 開始済みセグメント判定そのものの用途変更。成果物が存在する場合にのみ意味を持つ他の検査（SPEC の BDD 記載検査・DESIGN の図示要否検査・AC 対応検査）の分岐条件は本 Issue では変更しない。
- 成果物存在述語の変更（ワークツリー存在と履歴実績の OR による判定規則そのものの見直し）。
- quick 免除の対象成果物集合・ガードレール対象パス・シグナル置き場所の変更（ADR-0022 の規則の変更）。
- quick シグナル解決の再試行機構や、一時障害と恒久的未付与を区別する仕組みの導入。区別できないことを前提に同一の劣化挙動をとる。
- セグメント定義（`.agent-skill-chain/config/segments.yaml`）の追加・変更。
- ゲートレビューの自動強制化。GitHub モードで不変条件 I2 がガイドラインである点は変更しない。
- マージ準備完了 PR で root 直下成果物検査が常に失敗する設計（ADR-0046）の変更。
- 実装セグメント・独立検証セグメントを閉包追加対象に含めること。必要になった場合は別 Issue で扱う。
- ISSUE-692 自体の是正（設計セグメントへの差し戻し）。進行役が別途行う。
