# SPEC: size:quick の Issue でゲートが構造上通過不能な問題の是正

- Issue: `ISSUE-733`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/733-quick-gate-conformance-unpassable`

## 目的・背景

agent-skill-chain の憲法は、`size:quick` を指定した Issue について `SPEC.md`・`DESIGN.md`・`PLAN.md`・`VALIDATION.md` の作成義務を免除する。一方でゲート実装は Issue の size を一切参照しないため、免除規定に正しく従って成果物を作らなかった Issue が、成果物の品質と無関係に構造上ゲートを通過できない。この矛盾は次の 2 経路として現れる。

**経路A（判定プロンプトが立証を封じる）**: ゲートレビュア判定プロンプト（`gate reviewer-prompt` の出力）は、`SPEC.md` から AC-ID を抽出できないとき「conformance は inconclusive とし human_required へ倒すこと」とレビュアへ明示指示する。conformance ルーブリック自体も「適用対象の全 AC-ID / 要件が証跡付きに充足されているか」を唯一の判定軸として書かれている。final は「conformance と falsification の両 pass かつ blocking finding 無し→approved／いずれか fail もしくは blocking→rejected／inconclusive→human_required」と機械的に導出されるため、conformance が pass にならない限り approved は導出されない。

**経路B（レビュアが起動できない）**: ゲートレビュー起動（`gate local-review` 系）は、対象セグメントの必須成果物を target SHA から読めないとき `target SHAの必須成果物を読めません: <path>` で中断する。この中断はレビュア起動前に起きるため、gate-report は conformance・falsification とも `pending`、final は `human_required`、blockers は空のまま確定する。implementation ゲートは成果物不在を許容するが、spec・design・validation の各ゲートは中断する。

観測事実（2026-08-17、profile: strict、独立レビュア 2 体）：ISSUE-680 の implementation ゲートは 13 ラウンド全てで `conformance: pending`（レビュア証跡に「AC-ID を検出できず」「conformance を立証できません」と明記）、ISSUE-692 は 9 ラウンドで同様。ISSUE-680 の validation ゲートは経路B により exit 2 でレビュア 0 体。対照として `SPEC.md` を持つ ISSUE-721 の implementation ゲートは 2 体とも conformance/falsification 双方 pass、blockers 0 件で 1 ラウンド通過した。すなわち欠陥は成果物側ではなく判定機構側にある。

害は 3 点。(1) quick の Issue が自走でマージへ到達せず、進行役が毎回判定を上書きする運用が必要になる。(2) 成果物作成の負担を減らすための quick が、結果としてゲートの手動通過を強いるため意図を失う。(3) conformance が確定しない分レビュアの重心が反証側へ移り、ラウンド数の膨張に寄与する。

## 用語

- **size シグナル**: Issue の作業規模指定。取得元は、GitHub モードは Issue のラベル `size:quick`（付与されていなければ standard）、ローカルモードは `state.yaml` の `size`（`quick|standard`、未設定は standard）。
- **risk シグナル**: Issue のリスク分類。取得元は、GitHub モードは Issue の `risk:` プレフィックスラベル（`risk:normal`・`risk:high`・`risk:unclassified`）、ローカルモードは `state.yaml` の `risk`（`unclassified|normal|high`）。
- **シグナルの解決不能**: 当該シグナルの値を一意に確定できない状態。未付与、複数の相反する値の同時付与、読み取り失敗のいずれかを指す。既定値へ黙って落とすことはしない。
- **ガードレール対象パス**: `docs/adr/`・`.agent-skill-chain/config/segments.yaml`・`AGENTS.md`・`.agent-skill-chain/schemas/`。base SHA から target SHA までの変更差分がこれらのいずれかを含む場合、quick 免除は成立しない。
- **quick 免除**: size シグナルが quick に解決され、かつ risk シグナルが `normal` に解決され、かつ変更差分がガードレール対象パスを含まないときに限り成立する、4 成果物の作成義務免除。既定は standard であり、自動昇格しない。
- **免除不成立**: 上記 3 条件のいずれかを満たさない状態。size シグナルまたは risk シグナルの解決不能を含む。免除不成立は「判定不能」ではなく、standard と同一の従来経路へ決定的に解決される状態である。
- **判定プロンプト**: レビュアへ渡される read-only の判定指示文字列。レビュアにはツール呼び出しが一切許可されないため、判定に使える情報はこの文字列に展開済みの内容のみである。
- **AC-ID 抽出**: target SHA 時点の `SPEC.md` から受入条件の識別子を取り出す操作。`SPEC.md` が存在しない場合も「1 件も抽出できない」に含める。
- **代替判定基準**: quick 免除下で AC-ID 網羅に代わって用いる conformance の判定軸。実体は当該 Issue 本文に記載された要求・期待する挙動・受入基準であり、対象ゲート（spec・design・implementation・validation）の別を問わず同一の実体を用いる。ゲートごとに異なる情報源へ切り替えることはしない。
- **代替判定基準の展開可否**: 判定プロンプト生成時点で Issue 本文を取得でき、かつ本文から要求・期待する挙動・受入基準に相当する記述を 1 件以上取り出して判定プロンプト内へ文字列として書き出せる状態を「展開できる」とする。Issue 本文を取得できない場合、または取り出せる記述が 0 件の場合を「展開できない」とする。
- **inconclusive 指示**: 「conformance は inconclusive とし human_required へ倒すこと」に相当する、レビュアへの明示指示文字列。
- **可変入力**: size シグナル・risk シグナル・Issue 本文。target SHA・base SHA に固定されず、同一 SHA のまま外部で変化しうる入力を指す。
- **入力スナップショット**: 判定プロンプト生成時点で解決した可変入力の値の組。attempt に 1 対 1 で対応付く。
- **attempt**: 1 回のゲートレビュー起動単位。同一の attempt ID・target SHA・base SHA・プロファイルのもとで起動されたレビュア群と、その証跡の集合を指す。
- **conformance / falsification / final**: 立証観点・反証観点の判定値（`pass|fail|pending`）と、そこから機械導出される最終判定（`approved|rejected|human_required`）。

## 前提・制約

- 本 Issue は quick 免除の範囲そのものを変更しない。憲法が定める免除規定を所与とし、判定機構側を適合させる。
- 不変条件 I8（安全側ラチェット）を緩めない。判定不能な状態から `approved` を導出してはならず、silent pass を作ってはならない。`risk != normal`（`unclassified` 含む）は非 normal 側として扱う。
- レビュアはツールを持たないため、conformance の代替判定基準は判定プロンプト内に実体として展開されていなければならない。展開できない場合は判定不能として扱う。
- 判定プロンプトは digest 照合の対象である。証跡照合は、記録済み証跡の prompt digest と、照合器が同一条件で再生成した判定プロンプトの digest を比較して成立する。再生成が照合の要であるのは、レビュアが供給した digest を無検証で信頼しないためであり、この性質は維持する。
- 上記の帰結として、可変入力を判定プロンプトへ導入する場合、再生成の入力を attempt へ固定しなければ照合が破綻し、成果物に欠陥が無くても誤った `human_required` を生む。
- 再生成の入力に使えるのは trusted base 側の起動経路が生成・保存した値のみとする。レビュアまたはワーカーが供給した値を照合入力にしない。
- 反証（falsification）側のルーブリックは Issue #729 が扱う。両者は同一の判定プロンプト生成箇所を変更するため、着手順序を調整し、先にマージした側の変更を後続が作り直さないこと。

## 入力・出力

- 入力: size シグナル、risk シグナル、base SHA から target SHA までの変更差分、target SHA 時点の成果物、Issue 本文（要求・期待する挙動・受入基準の記述）、対象ゲート ID、attempt に対応する入力スナップショット（証跡照合時）。
- 出力: 判定プロンプト文字列、入力スナップショットの耐久記録、およびゲートレビュー起動の成否（起動できた場合は verdict と gate-report）。

## 仕様判断とその根拠

**判断1: size シグナル・risk シグナルの解決不能は「判定不能」ではなく「免除不成立」とする。**

シグナルを解決できない状態は、免除を適用しない standard 経路へ決定的に解決する。これは silent pass ではない——standard 経路は AC-ID 網羅という従来どおりのより厳しい判定軸を課し、AC-ID を抽出できなければ従来どおり inconclusive 指示へ倒れるためである。逆に「シグナル解決不能を一律に判定不能とする」案は採らない。ラベル取得の一時的失敗のような外形的事由で、適正な `SPEC.md` を持つ通常の Issue まで `approved` へ到達できなくなり、既存の全 Issue に対する回帰となるためである。判定不能として扱うのは、判定基準そのもの（AC-ID 集合または代替判定基準、および照合用の入力スナップショット）を実体化できない場合に限る。

**判断2: 可変入力は attempt ごとにスナップショットとして固定し、証跡照合は保存済みスナップショットのみを入力として再生成する。**

可変入力を digest 対象から除外する案は、判定プロンプトの実内容と証跡が乖離するため採らない。照合時に可変入力を読み直す案は、Issue 本文の編集やラベル変更だけで既存の正当な証跡が不一致となるため採らない。スナップショットを固定することで、再生成による照合の信頼性（前提・制約）と、可変入力の導入（要件2）を同時に満たす。可変入力が変化した後に改めて判定が必要な場合は、既存 attempt を書き換えず新しい attempt を起こす。

**判断3: 判定プロンプト生成の入力空間を、互いに排他な 4 状態へ分割し、各状態の期待結果を 1 つに確定する。**

同一入力に対して 2 つの期待結果を許すと、対応する自動テストが同時には成功しえず実装不能となる。そこで判定プロンプト生成の入力空間を次の表のとおり網羅かつ排他に分割し、状態ごとに 1 つの AC だけが期待結果を定める形にする。受入条件の Given は他の状態を包含しない。

| 状態 | 条件（互いに排他） | 判定プロンプトの期待結果 | 定める AC |
|---|---|---|---|
| S1 | quick 免除成立 かつ AC-ID を 1 件以上抽出できる | 従来どおり AC-ID 全件網羅を conformance の判定軸とする | AC-5 |
| S2 | quick 免除成立 かつ AC-ID を 1 件も抽出できない かつ 代替判定基準を展開できる | inconclusive 指示を含めず、代替判定基準を判定軸として展開する | AC-1（final への波及は AC-2） |
| S3 | quick 免除成立 かつ AC-ID を 1 件も抽出できない かつ 代替判定基準を展開できない | 生成は中断せず成功させ、inconclusive 指示を含める（pass 到達経路を残さない） | AC-6 |
| S4 | 免除不成立（size が standard に解決される／size が quick かつ risk が解決できて `normal` 以外／ガードレール対象パスを含む／size もしくは risk が解決不能） | 従来挙動。AC-ID を抽出できれば全件網羅、抽出できなければ inconclusive 指示 | AC-3・AC-4・AC-11 |

S4 を 3 つの AC へ分けるのは、免除不成立の成立事由ごとに独立した回帰対象があるためである（AC-3 は standard の既存挙動、AC-4 は risk・ガードレールによる不成立、AC-11 は解決不能の安全側処理）。3 者の Given も互いに排他とする。

## 要求 → 要件 → 受入条件

### 要求

quick 免除に正しく従った Issue が、進行役による判定の上書き無しにゲートを通過できること。同時に、成果物が本来存在すべきなのに欠落している Issue では、従来どおり安全側（`human_required`）へ倒れること。

### 要件

- 要件1: 判定機構が size シグナル・risk シグナルをモード別の取得元から解決し、「quick 免除により成果物・AC-ID が正当に存在しない状態」と「本来存在すべき成果物・AC-ID を検出できない異常状態」を区別する。
- 要件2: quick 免除が成立し AC-ID を抽出できない場合、AC-ID 網羅に代わる代替判定基準を判定プロンプトへ実体として展開し、conformance が成立しうるようにする。代替判定基準を展開できない場合は、判定プロンプト生成を中断せず、AC-ID 網羅不能時と同一の inconclusive 指示へ倒す（pass を導出しうる状態にしない）。
- 要件3: 免除不成立の場合、および quick 免除が成立していても AC-ID を抽出できる場合は、従来の AC-ID 網羅を判定軸とする。免除不成立かつ AC-ID を抽出できない場合は従来どおり inconclusive 指示を維持する。
- 要件4: quick 免除が成立する場合、必須成果物が target SHA に存在しないことを理由にゲートレビュー起動を中断しない。免除が成立しない場合の中断挙動は維持する。
- 要件5: size シグナルまたは risk シグナルを解決できない場合は免除不成立として扱う（risk を `normal` とみなさず、size を quick とみなさない）。免除不成立から先の到達可能性は成果物の状態が決める。
- 要件6: 判定プロンプト生成に用いた可変入力の解決値を入力スナップショットとして確定し、attempt に対応付けてレビュア起動前に耐久記録へ保存する。保存先は、GitHub モードは trusted base 側のゲート状態記録、ローカルモードは `reviews/<gate>.yaml` とする。保存に失敗した場合はレビュアを起動せず `approved` を導出しない。
- 要件7: 証跡照合時の判定プロンプト再生成は、attempt に対応する入力スナップショットのみを入力とし、照合時点の可変入力を読み直さない。スナップショットは trusted base 側の起動経路が生成・保存したものに限る。
- 要件8: 判定基準（AC-ID 集合または代替判定基準）または照合用の入力スナップショットを実体化・解決できない場合、`approved` を導出しない。
- 要件9: 判定プロンプトは、同一の Issue・ゲート・target SHA・base SHA・入力スナップショットに対して決定的である。
- 要件10: GitHub モードとローカルモードの双方で上記が成立し、自動テストで検証される。

### 要件と受入条件の対応

| 要件 | 対応する受入条件 |
|---|---|
| 要件1 | AC-1・AC-3・AC-4・AC-5・AC-11 |
| 要件2 | AC-1・AC-2・AC-6 |
| 要件3 | AC-3・AC-4・AC-5・AC-11 |
| 要件4 | AC-7・AC-8 |
| 要件5 | AC-11 |
| 要件6 | AC-13 |
| 要件7 | AC-10 |
| 要件8 | AC-6・AC-8・AC-12・AC-13 |
| 要件9 | AC-9 |
| 要件10 | AC-14 |

### 受入条件（Acceptance Criteria）

#### AC-1: S2 では inconclusive 指示を出さず代替判定基準を展開する

- Given: quick 免除が成立する Issue で、target SHA から AC-ID を 1 件も抽出できず（`SPEC.md` が存在しない場合を含む）、かつ代替判定基準を展開できる（状態 S2）。
- When: 当該 Issue の任意のゲートについて判定プロンプトを生成する。
- Then: 生成が成功し、出力に inconclusive 指示が含まれない。出力には、AC-ID が quick 免除により正当に存在しない旨と、代替判定基準の実体（Issue 本文由来の要求・期待する挙動・受入基準）が conformance の判定軸として含まれる。
- 検証方法見込み: `automated`

#### AC-2: S2 では conformance が pass へ到達し approved が導出される

- Given: AC-1 の Given（状態 S2）を満たす入力で生成された判定プロンプトによりレビュアが起動され、当該 attempt の入力スナップショットが保存済みである。
- When: レビュアが conformance と falsification の双方を pass、blocking finding 無しと判定した verdict を返す。
- Then: final が `approved` として導出され、判定プロンプト由来の理由で conformance が pending に固定されることがない。
- 検証方法見込み: `automated`

#### AC-3: size が standard に解決される Issue の従来挙動を維持する

- Given: size シグナルが standard に解決される Issue（状態 S4 のうち standard 事由）で、target SHA から AC-ID を 1 件も抽出できない。
- When: 判定プロンプトを生成する。
- Then: 従来どおり inconclusive 指示が出力に含まれ、代替判定基準は展開されない。
- 検証方法見込み: `automated`

#### AC-4: size が quick でも risk・ガードレール事由で免除不成立なら従来挙動へ倒れる

- Given: size シグナルが quick に解決され risk シグナルも解決できている Issue で、risk が `normal` 以外である、または base SHA から target SHA までの変更差分がガードレール対象パスを含む（状態 S4 のうち risk・ガードレール事由）。
- When: 判定プロンプトを生成する。
- Then: 代替判定基準は適用されない。target SHA から AC-ID を 1 件以上抽出できる場合は、抽出された AC-ID の全件網羅を conformance の判定軸とする指示が出力され、conformance は pass へ到達しうる。AC-ID を 1 件も抽出できない場合は inconclusive 指示が出力される。
- 検証方法見込み: `automated`

#### AC-5: S1 では従来どおり AC-ID 網羅で判定する

- Given: quick 免除が成立する Issue で、target SHA から AC-ID を 1 件以上抽出できる（状態 S1）。
- When: 判定プロンプトを生成する。
- Then: 従来どおり抽出された AC-ID の全件網羅を conformance の判定軸とする指示が出力され、代替判定基準への切り替えは起きない。
- 検証方法見込み: `automated`

#### AC-6: S3 では生成を中断せず inconclusive 指示を出す

- Given: quick 免除が成立する Issue で、target SHA から AC-ID を 1 件も抽出できず、かつ代替判定基準を展開できない（Issue 本文を取得できない、または取得できても要求・期待する挙動・受入基準に相当する記述が 0 件である。状態 S3）。
- When: 判定プロンプトを生成する。
- Then: 生成は例外で中断せず成功し、その出力は inconclusive 指示を含む。代替判定基準は展開されず、conformance が pass へ到達しうる指示は出力に含まれない。結果として当該 attempt から `approved` は導出されない。
- 検証方法見込み: `automated`

#### AC-7: quick 免除下でゲートレビューが起動でき、不在事由が明示される

- Given: quick 免除が成立する Issue で、target SHA に当該ゲートの必須成果物（spec は `SPEC.md`、design は `DESIGN.md` と `PLAN.md`、validation は `VALIDATION.md`）が存在しない。
- When: 当該ゲートのレビューを起動する。spec・design・validation の 3 ゲートすべてを対象に含める。
- Then: 「target SHAの必須成果物を読めません」に相当する中断が発生せず、レビュアが起動され、conformance と falsification の判定値を伴う gate-report が生成される。判定プロンプトには、当該必須成果物が quick 免除により不在である旨が明示され、判定対象欄が無言の空欄にならない。本 AC の判定軸は起動可否と不在事由の明示に限る——conformance の判定軸として何が展開されるかは代替判定基準の展開可否が決め、展開できる場合は AC-1 の Then、展開できない場合は AC-6 の Then に従う。
- 検証方法見込み: `automated`

#### AC-8: 免除不成立時の必須成果物欠落は従来どおり中断する

- Given: quick 免除が成立しない Issue（状態 S4）で、target SHA に当該ゲートの必須成果物が存在しない。
- When: 当該ゲートのレビューを起動する。
- Then: 従来どおり中断してレビュアを起動せず、`approved` を導出しない。本 AC の判定軸はレビュー起動経路の中断のみであり、判定プロンプト生成を単独で呼び出した場合の出力内容は AC-3・AC-4・AC-11 が定める（両者は呼び出し経路が異なり、同一の観測対象を争わない）。
- 検証方法見込み: `automated`

#### AC-9: 同一の入力スナップショットに対する判定プロンプト生成が決定的である

- Given: 同一の Issue・ゲート・target SHA・base SHA・入力スナップショット。
- When: 判定プロンプトを複数回生成する。
- Then: 出力が完全に一致し、prompt digest が一致する。quick 経路の追加によって既存の決定性が損なわれない。
- 検証方法見込み: `automated`

#### AC-10: 可変入力が変化しても記録済み attempt の証跡照合が成立する

- Given: attempt の記録後に、Issue 本文・size シグナル・risk シグナルのいずれかが変化している（target SHA と base SHA は不変）。
- When: 当該 attempt の証跡を照合する。
- Then: 照合器は attempt に対応付いた入力スナップショットから判定プロンプトを再生成し、その digest が記録済み prompt digest と一致する。照合時点の可変入力の変化は照合結果を変えない。
- 検証方法見込み: `automated`

#### AC-11: シグナルを解決できない場合は免除不成立として決定的に扱う

- Given: size シグナルを解決できない Issue、または size シグナルが quick に解決され risk シグナルを解決できない Issue（未付与・複数の相反する値・読み取り失敗のいずれか。状態 S4 のうち解決不能事由）。
- When: quick 免除の成否を判定し、判定プロンプトを生成する。
- Then: 解決できない risk を `normal` とみなさず、解決できない size を quick とみなさず、免除不成立として決定的に扱う。代替判定基準は適用されない。出力される判定プロンプトは AC-4 の Then と同一の規則に従う（AC-ID を抽出できれば全件網羅、抽出できなければ inconclusive 指示）。シグナルの解決不能自体を理由に `approved` の到達可能性を変えない。
- 検証方法見込み: `automated`

#### AC-12: 照合側で入力スナップショットを解決できない場合に approved を導出しない

- Given: 記録済み attempt について、対応する入力スナップショットを解決できない（保存されていない、読み取れない、attempt との対応付けが一意でない）、または保存済みスナップショットから再生成した判定プロンプトの digest が記録済み prompt digest と一致しない。
- When: 当該 attempt の証跡を照合する。
- Then: `approved` を導出せず、照合を失敗させるか `human_required` へ倒れる。照合時点の可変入力を読み直して一致を取り繕う経路は存在しない。本 AC の判定軸は照合側のみであり、生成側の判定不能は AC-6、起動側は AC-8、保存側は AC-13 が定める。
- 検証方法見込み: `automated`

#### AC-13: 入力スナップショットが起動経路で確定・保存される

- Given: trusted base 側の起動経路が判定プロンプトを生成し、attempt を記録しようとしている（quick 免除の成否と状態 S1〜S4 の別を問わない。AC-8 により起動が中断する入力は attempt を作らないため対象外）。
- When: 当該 attempt のレビュアを起動する。
- Then: 判定プロンプト生成時点で解決した可変入力（size シグナル・risk シグナル・Issue 本文）の値の組が入力スナップショットとして確定し、attempt と 1 対 1 で対応付けて耐久記録へ保存される。保存先は、GitHub モードは trusted base 側のゲート状態記録、ローカルモードは `reviews/<gate>.yaml` である。保存はレビュア起動前に完了し、保存後に当該 attempt のスナップショットを更新・上書きしない。保存に失敗した場合はレビュアを起動せず、`approved` を導出せずに中断するか `human_required` へ倒れる。本 AC の判定軸は耐久記録への保存という副作用のみであり、判定プロンプトの内容は判定軸に含めない。
- 検証方法見込み: `automated`

#### AC-14: 全 AC に対応する自動テストが両モードを対象に存在する

- Given: 本 Issue の変更が適用されたリポジトリ。
- When: `npm test` を実行する。
- Then: AC-1 から AC-13 のそれぞれに対応する自動テストが実行されすべて成功する。このうち size シグナル・risk シグナルの解決を含む AC については、GitHub モード由来のシグナル（Issue ラベル）とローカルモード由来のシグナル（`state.yaml`）の双方を入力とするケースを含む。本 AC は他 AC のテスト集合の存在を要求するメタ受入条件であり、自己言及を避けるため列挙対象に自身を含めない。
- 検証方法見込み: `automated`

## 完了条件

- AC-1 から AC-14 のすべてが満たされ、対応する自動テストが成功する（AC-14 は AC-1 から AC-13 のテスト集合が存在し成功することをもって満たされる）。
- 要件1 から要件10 のそれぞれが、前掲の対応表のとおり少なくとも 1 つの受入条件と対応している。
- 既存のゲート判定・証跡照合の自動テストが回帰しない。
- 常時必須の検証（lint / format、型検査、単体テスト、変更範囲の結合テスト、SAST、依存関係・secret スキャン）が成功する。本変更は利用者操作画面・API 境界・認証認可・性能ホットパス・DB migration・デプロイ運用・外部連携のいずれにも該当しないため、それらに紐づく追加検証は適用しない。

## 未決事項

- 代替判定基準として Issue 本文以外の入力（実装差分・テスト実行結果・CI 結果）をどこまで判定プロンプトへ展開するかは設計セグメントで確定する。本仕様では、代替判定基準の実体を Issue 本文由来の記述に限り、ゲートごとに情報源を切り替えないことを確定済みとする。設計セグメントで入力を追加する場合も、全ゲートで同一の入力集合とし、ゲート別の分岐を導入しないこと。
- Issue 本文から「要求・期待する挙動・受入基準に相当する記述」を取り出す具体的な抽出規則（見出し一致・全文採用等）は設計セグメントで確定する。抽出結果が 0 件のとき展開不能として扱うこと（AC-6）は本仕様で確定済みとする。
- 入力スナップショットの具体的なフィールド構造・スキーマ版・attempt との対応付けの表現方法は設計セグメントで確定する。保存先（GitHub モードは trusted base 側のゲート状態記録、ローカルモードは `reviews/<gate>.yaml`）、保存タイミング（レビュア起動前）、保存失敗時の挙動、照合が保存済みスナップショットのみを入力とすることは本仕様で確定済みとする。
- 本変更の適用前に記録され入力スナップショットを持たない attempt について、移行期に一括で再判定を促す運用手順は進行役の判断で確定する。当該 attempt の照合結果自体は AC-12 により `human_required` へ倒れることが本仕様で確定済みである。
- Issue #729（反証ルーブリック）との着手・マージ順序の具体的な調整方法は進行役の判断で確定する。

## スコープ外

- quick 免除の対象そのものの見直し（憲法である `AGENTS.md` の改定）。
- falsification 側のルーブリックの停止条件・severity 基準・過去ラウンド反映（Issue #729）。
- 変更規模に応じた反証強度の調節（Issue #698）。
- strict attempt の実行独立性とレビュー履歴 pagination（Issue #732）。
- GitHub モードにおける I2 の自動 CI 強制の再導入。
