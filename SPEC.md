# SPEC: ゲートレビュー用プロンプトの AC-ID 抽出を受入条件見出し限定にする

- Issue: `ISSUE-679`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/679-gate-ac-id-extraction-heading-only`

## 目的・背景

agent-skill-chain のゲートレビューでは、レビュアへ渡す判定プロンプトの中に「適用対象の AC-ID（SPEC.md 由来。全件を conformance 判定で網羅すること）」という一覧欄があり、対象 SHA の `SPEC.md` から収集した AC-ID がそこへ列挙される。同じプロンプトは「AC-ID 未達の指摘は常に blocking とし、warning 以下へ格下げしない」とレビュアへ指示する。したがってこの一覧欄に実在しない識別子が 1 つでも混入すると、レビュアはそれを実在する受入条件とみなし、Given/When/Then が存在しないことを根拠に必ず blocking finding を返す。

本 SPEC 作成時点の実装では、この一覧欄へ載せる識別子を `SPEC.md` の本文全体に対する単純一致走査（語境界付きの `AC-` + 数字の並び）で収集している。走査対象は見出し行に限定されておらず、散文・HTML コメント・引用のいずれに現れた同形文字列も実在する受入条件として収集される。

実際に、`SPEC.md` が受入条件見出しとして 5 件しか宣言していない Issue において、追記手順を説明するコメント中の同形文字列 2 件が一覧欄へ混入し、その 2 件の Given/When/Then が存在しないことを理由に spec ゲートが blocking finding 付きで不通過となった。ゲートは正しい成果物を不当に不通過とし、ワーカーの再起動と再レビューが空転した。

一方、同一リポジトリ内の別経路である `SPEC.md` と `VALIDATION.md` の突合検査では、同種の誤検出が先行 Issue で既に修正済みであり、受入条件見出しのみを AC-ID 宣言として扱う判定へ改められている。結果として、同一リポジトリ内に AC-ID 宣言の判定基準が 2 つ並存し、片方の修正がもう片方へ横展開されない構造になっている。本 Issue はこの並存を解消し、ゲートレビュー用プロンプト側の誤検出を除去する。

## 対象範囲

本 SPEC が仕様を定める対象は次の 3 つに限る。

- ゲートレビュー用プロンプト生成が `SPEC.md` から AC-ID を収集する処理の判定基準と、その結果がプロンプトの一覧欄へどう反映されるか（0 件の場合を含む）。プロンプト生成は 4 ゲート（spec・design・implementation・validation）すべてで同一の収集処理を用いるため、4 ゲートすべてを対象とする。
- 同一リポジトリ内に存在する AC-ID 抽出処理の個数と、各処理が用いる判定基準の同一性。
- 上記判定基準を機械的に検証する自動テストの有無。

対象外は本 SPEC 末尾の「スコープ外」に列挙する。

## 前提

本 SPEC の要件・受入条件は、次の前提が成立する状態で評価する。

- 受入条件の宣言記法は、Markdown の第 4 レベル見出し（行頭の `#` 4 個と 1 個以上の空白）に続けて識別子（`AC-` と 1 桁以上の数字）を置き、その直後にコロンを置き、以降へ要約を書く形である。この記法自体は本 Issue では変更しない。
- 上記記法は本 SPEC 作成時点でテンプレート・既存成果物・`SPEC.md` の記載充足検査（各受入条件の Given/When/Then とプレースホルダ残存を検査する処理）が一致して用いている唯一の宣言形である。
- Markdown 見出し行とは、行頭が `#` 1〜6 個とそれに続く空白文字で始まる行を指す。見出し行以外の行は本文行である。
- ゲートレビュー用プロンプト生成は、対象 SHA の `SPEC.md` の内容を Git から読み取る。対象 SHA に `SPEC.md` が存在しない場合は空文字列として扱う。この読み取り方法は本 Issue では変更しない。
- ゲートレビュー用プロンプトは CLI サブコマンドとして単体で標準出力へ生成でき、生成結果は文字列として検査できる。
- レビュープロンプトの一覧欄には、収集結果が 1 件以上なら識別子をカンマ区切りで列挙し、0 件なら「AC-ID を検出できず、conformance は inconclusive とし human_required へ倒すこと」という趣旨の代替文を出力する分岐が既に存在する。この分岐の存在自体は本 Issue では変更しない。
- 本 SPEC 作成時点（本 Issue の変更前）のプロンプト生成側 AC-ID 収集は、次のとおり振る舞う。(1) `SPEC.md` の本文全体を対象に単純一致走査を行い、一致した字面をすべて収集対象とする。(2) 同一の字面は集合として畳み込まれ、収集結果に重複は生じない。(3) 収集結果の並びは識別子の数値部分の昇順であり、宣言・言及の出現順には依存しない。(4) 一覧欄では収集結果をカンマと空白 1 個で連結する。(5) 収集結果が 0 件のときのみ代替文を出力する。したがって本 Issue が変更するのは (1) の「どの位置に現れた字面を収集対象とみなすか」の 1 点のみであり、(2)〜(5) の重複排除・列挙順・区切り・0 件分岐は変更前と同一の規則を維持する。
- 変更前のゲートレビュー用プロンプトは、次の構成要素をこの順に含む。(a) プロンプト冒頭の見出しとレビュア役割・read-only 指示、(b) 対象 Issue・ゲート・対象 SHA の識別情報、(c) 埋め込まれていない参照ファイルの扱い（ハルシネーション防止）の節、(d) 適用対象の AC-ID の一覧欄、(e) conformance（立証）ルーブリック、(f) falsification（反証）ルーブリック、(g) Light プロファイルが適用されている場合のみ追加ルーブリック、(h) final の扱い、(i) 出力 JSON 契約、(j) 比較元 SHA が与えられた場合のみ判定対象の差分、(k) 判定対象の成果物の埋め込み、(l) spec 以外のゲートでのみ上流の承認済み成果物の埋め込み。本 Issue が変更するのは (d) の内容のみである。
- 本 SPEC 作成時点の突合検査は、`SPEC.md` の AC-ID 宣言（用語節の定義と同一の見出し限定判定）と `VALIDATION.md` の報告済み識別子を照合し、次の 3 種の違反のみを報告する。(i) 孤児 AC（`SPEC.md` が宣言する識別子が `VALIDATION.md` に対応しない）、(ii) 孤児テスト参照（`VALIDATION.md` が報告する識別子が `SPEC.md` に存在しない）、(iii) 報告に証跡が 1 件も無い。違反が 1 件も無いときは成功（終了コード 0）で終了する。この判定内容は本 Issue では変更しない。

## 用語

本 SPEC 内で用いる語を次の意味に固定する。

- **AC-ID**: 受入条件を一意に識別する文字列。`AC-` に 1 桁以上の数字が続く形式（正規表現 `^AC-[0-9]+$`）。
- **AC-ID 宣言**: `SPEC.md` において受入条件が実在することを表す唯一の正規表現形。本 SPEC では「行頭から第 4 レベル見出し記号（`#` ちょうど 4 個）で始まり、1 個以上の空白文字を挟んで AC-ID が続き、その直後にコロンが置かれた見出し行」を指す。第 3 レベル以下・第 5 レベル以上の見出し、コロンを伴わない見出し、本文行中の同形文字列はいずれも AC-ID 宣言ではない。
- **同形文字列**: AC-ID と同じ字面を持つが AC-ID 宣言の位置には現れない文字列。散文・HTML コメント・箇条書き・引用中の言及を含む。
- **単純一致走査**: `SPEC.md` の本文全体に対して、見出し行かどうかを区別せず AC-ID の字面に一致するすべての箇所を収集する処理。
- **AC-ID 抽出**: 与えられた `SPEC.md` の内容から、実在する受入条件の AC-ID 集合を決定する処理。
- **ゲートレビュー用プロンプト**: ゲートレビュアへ渡される判定指示の文字列全体。「適用対象の AC-ID」の一覧欄を含む。
- **突合検査**: `SPEC.md` が宣言する AC-ID 集合と `VALIDATION.md` が報告する AC-ID 集合を照合し、孤児 AC・孤児テスト参照を検出する既存の検査処理。

## 入力・出力

- 入力: 対象 SHA の `SPEC.md` の内容（テキスト）。
- 出力: ゲートレビュー用プロンプト文字列。うち本 SPEC が仕様を定めるのは「適用対象の AC-ID」の一覧欄の内容である。

## 要求 → 要件 → 受入条件

### 要求

ゲートレビュアが、`SPEC.md` の見出し以外の位置（散文・HTML コメント・箇条書き・引用）に現れた同形文字列を実在する受入条件と誤認し、その Given/When/Then が無いことを理由にゲートを不通過にすることがなくなること。すなわちレビュアへ渡る AC-ID 一覧が、見出し以外の位置に現れた同形文字列を一切含まないこと。

本要求が到達を主張する状態には、次の残存を含む。Markdown のコードフェンス内に AC-ID 宣言と同形の行（テンプレート例・記法説明の例示等）が現れた場合、それは制約節のとおり引き続き AC-ID として収集され一覧欄へ載る。本 Issue はこの残存を要求未達ではなく許容された既知の残存欠陥として扱う（フェンス内外の区別を片方の経路にだけ導入すると、判定基準の並存という本要求のもう一方の柱を再び壊すため）。

あわせて、AC-ID 宣言の判定基準がリポジトリ内で 1 つに定まり、片方の経路だけが修正されて他方に横展開されない状態を解消すること。

### 要件

- ゲートレビュー用プロンプトの一覧欄へ載せる AC-ID は、AC-ID 宣言（用語節の定義）からのみ収集すること。単純一致走査を用いないこと。
- 見出し行以外の位置に現れた同形文字列は、AC-ID として収集しないこと。この扱いは、同形文字列が HTML コメント内・箇条書き内・散文中のいずれにあるかを問わない。
- 第 4 レベル以外の見出し（第 3 レベル以下・第 5 レベル以上）に AC-ID の字面が現れても、AC-ID として収集しないこと。
- 見出し行に AC-ID の字面が現れても、その直後にコロンが無い場合は AC-ID として収集しないこと。
- 同一の AC-ID が複数箇所に現れる場合でも、一覧欄には重複なく 1 回だけ現れること。
- 一覧欄の列挙順は AC-ID の数値部分の昇順であること（宣言の出現順に依存しないこと）。
- AC-ID 宣言が 1 件も存在しない場合は、同形文字列が本文中にいくつ存在しても収集結果を 0 件として扱い、一覧欄には 0 件時の代替文（conformance を inconclusive とし human_required へ倒す旨）を出力すること。同形文字列を列挙してはならない。
- ゲートレビュー用プロンプト生成が用いる AC-ID 抽出と、突合検査が用いる AC-ID 抽出は、同一の入力に対して常に同一の AC-ID 集合を返すこと。判定基準が乖離しうる形（同一基準を 2 箇所へ独立に実装する形）を残さないこと。
- 変更後のリポジトリに、単純一致走査による AC-ID 抽出の実装が残らないこと。
- 突合検査の既存の判定結果（孤児 AC・孤児テスト参照の検出内容）は、本 Issue の変更前後で変わらないこと。
- 一覧欄以外のゲートレビュー用プロンプトの構成（見出し・ルーブリック・出力契約・成果物の埋め込み）は、本 Issue の変更前後で変わらないこと。
- 見出し以外の位置に同形文字列を含む `SPEC.md` を入力として、抽出結果に当該文字列が含まれないことを確認する自動テストを追加すること。

### 制約

- 判定は行単位のテキスト解析で行い、Markdown のコードフェンス内外を区別しない。フェンス内に AC-ID 宣言と同形の行が現れた場合はこれを AC-ID として収集する。この扱いは本 SPEC 作成時点の突合検査側の判定と同一であり、本 Issue では変更しない（両経路の判定基準を 1 つに揃えることが本 Issue の要求であり、片方だけを高度化すると乖離が再発するため）。
- 判定基準の変更は AC-ID 宣言の記法そのものには及ばない。既存の `SPEC.md` 群のうち規定の記法に従うものは、変更前後で同一の AC-ID 集合を与えなければならない。
- 規定の記法に従わない既存の `SPEC.md`（受入条件を第 4 レベル以外の見出しで宣言している、または識別子の直後にコロンを置いていないもの）では、非準拠の宣言は本変更後に AC-ID として収集されない。宣言がすべて非準拠である `SPEC.md` は抽出結果が 0 件となり、その結果、一覧欄は 0 件時の代替文となり、当該 Issue のゲートは conformance を inconclusive として human_required へ倒れる。本 SPEC はこれを許容する。理由は、(1) 実在しない識別子を根拠に blocking finding を返して正しい成果物を不通過にする現状の誤りより、判定を人間へ委ねる方が害が小さく、安全側への降格であること、(2) 非準拠記法は宣言記法を規定どおりに直すだけで解消でき、恒久的な機能低下にならないことである。非準拠記法の既存成果物を一括で書き換えることは本 Issue では行わない。
- 収集処理は対象 SHA の `SPEC.md` を read-only で読むのみであり、成果物・ゲート記録の書き換えを伴わない。

### 受入条件（Acceptance Criteria）

#### AC-1: 見出し宣言のみを収集し散文中の同形文字列を除外する

- Given: 第 4 レベル見出しの AC-ID 宣言を連番で 5 件持ち、かつ本文コメント中に宣言されていない番号 2 件分の同形文字列を含む `SPEC.md` が対象 SHA に存在する
- When: 当該 SHA・当該 Issue に対してゲートレビュー用プロンプトを生成する
- Then: 一覧欄には見出し宣言由来の 5 件のみが列挙され、コメント中の同形文字列 2 件はプロンプトの一覧欄に一切現れない
- 検証方法見込み: `automated`

#### AC-2: 第 4 レベル以外の見出しは宣言として扱わない

- Given: 第 3 レベル見出しの形で識別子とコロンを持つ行 1 件、第 5 レベル見出しの形で識別子とコロンを持つ行 1 件、および第 4 レベル見出しの正規宣言 1 件（いずれも互いに異なる番号）を含む `SPEC.md` が対象 SHA に存在する
- When: 当該 SHA・当該 Issue に対してゲートレビュー用プロンプトを生成する
- Then: 一覧欄には第 4 レベル見出し由来の 1 件のみが列挙され、第 3 レベル・第 5 レベル見出し由来の番号は現れない
- 検証方法見込み: `automated`

#### AC-3: コロンを伴わない見出しは宣言として扱わない

- Given: 第 4 レベル見出しであるが識別子の直後にコロンが無い行 1 件と、コロンを伴う正規宣言 1 件（互いに異なる番号）を含む `SPEC.md` が対象 SHA に存在する
- When: 当該 SHA・当該 Issue に対してゲートレビュー用プロンプトを生成する
- Then: 一覧欄にはコロンを伴う宣言由来の 1 件のみが列挙され、コロンを伴わない行由来の番号は現れない
- 検証方法見込み: `automated`

#### AC-4: 宣言が 0 件なら同形文字列があっても 0 件として扱う

- Given: 第 4 レベル見出しの AC-ID 宣言を 1 件も持たず、散文とコメントにのみ同形文字列を 3 件含む `SPEC.md` が対象 SHA に存在する
- When: 当該 SHA・当該 Issue に対してゲートレビュー用プロンプトを生成する
- Then: 一覧欄は 0 件時の代替文（conformance を inconclusive とし human_required へ倒す旨）となり、散文・コメント由来の同形文字列は 1 件も列挙されない
- 検証方法見込み: `automated`

#### AC-5: 重複排除と数値昇順での列挙

- Given: 第 4 レベル見出しの AC-ID 宣言を数値の昇順でない出現順で 3 件以上持ち、そのうち 1 件と同じ識別子が散文中にも現れる `SPEC.md` が対象 SHA に存在する
- When: 当該 SHA・当該 Issue に対してゲートレビュー用プロンプトを生成する
- Then: 一覧欄では各識別子がちょうど 1 回ずつ現れ、かつ数値部分の昇順で並ぶ（例: 10 番の識別子は 9 番の識別子より後に並ぶ）
- 検証方法見込み: `automated`

#### AC-6: プロンプト生成経路と突合検査経路の抽出結果が一致する

- Given: 第 4 レベル見出しの正規宣言・散文中の同形文字列・第 4 レベル以外の見出し・コロンを伴わない見出しをすべて含む同一の `SPEC.md` 内容
- When: ゲートレビュー用プロンプト生成が用いる AC-ID 抽出と、突合検査が用いる AC-ID 抽出へ、それぞれ同一の内容を与えて結果を取得する
- Then: 両者が返す AC-ID 集合が完全に一致し、いずれも正規宣言由来の識別子のみを含む
- 検証方法見込み: `automated`

#### AC-7: 単純一致走査の実装がリポジトリに残らない

- Given: 本 Issue の変更を適用した後のソースツリー
- When: AC-ID 抽出を行う実装箇所を走査する
- Then: `SPEC.md` の本文全体を見出し行と区別せず走査して AC-ID を収集する実装が 1 箇所も存在せず、プロンプト生成経路と突合検査経路が同一の抽出実装を呼び出している
- 検証方法見込み: `automated`

#### AC-8: 突合検査の既存判定が変わらない

- Given: 規定の記法に従う AC-ID 宣言のみを含み同形文字列を含まない `SPEC.md` と `VALIDATION.md` の 3 通りの組。(i) `SPEC.md` が宣言する識別子のうち 1 件が `VALIDATION.md` に報告されていない組、(ii) `VALIDATION.md` が `SPEC.md` の宣言に無い識別子を 1 件報告している組、(iii) 双方の識別子集合が一致し各報告に証跡が 1 件以上ある組
- When: 3 通りそれぞれについて突合検査を実行する
- Then: (i) では当該 1 件の識別子を対象とする孤児 AC の違反がちょうど 1 件報告され、(ii) では当該 1 件の識別子を対象とする孤児テスト参照の違反がちょうど 1 件報告され、(iii) では違反が 1 件も報告されず検査が成功（終了コード 0）で終了する。いずれの組でも、上記以外の種類の違反も、上記以外の識別子を対象とする違反も報告されない
- 検証方法見込み: `automated`

#### AC-9: 同形文字列を含まない入力での出力全体が期待どおりである

- Given: 第 4 レベル見出しの正規宣言を 3 件持ち、同形文字列を一切含まない `SPEC.md` が対象 SHA に存在する。3 件の宣言は数値の昇順ではなく、3 番・1 番・2 番の順に出現するものとする
- When: 当該 SHA・当該 Issue に対してゲートレビュー用プロンプトを生成する
- Then: 一覧欄には 3 件の識別子が数値昇順（1 番・2 番・3 番の順）でカンマと空白 1 個の区切りにより列挙され、それ以外の識別子は現れない。かつプロンプトは前提節に列挙した構成要素を前提節と同じ順序・同じ見出し文言で含み、一覧欄以外の各構成要素の本文が欠落・重複・並び替えなく出力される
- 検証方法見込み: `automated`

#### AC-10: 規定の記法に従わない既存 SPEC は human_required へ倒れる

- Given: 受入条件を第 4 レベル以外の見出しで宣言する行、および識別子の直後にコロンを置かない第 4 レベル見出しの行のみを持ち、正規宣言を 1 件も含まない `SPEC.md` が対象 SHA に存在する
- When: 当該 SHA・当該 Issue に対してゲートレビュー用プロンプトを生成する
- Then: 一覧欄は 0 件時の代替文（conformance を inconclusive とし human_required へ倒す旨）となり、非準拠の見出し行に現れた番号は 1 件も列挙されない
- 検証方法見込み: `automated`

## 完了条件

- 上記すべての受入条件が充足され、その証跡が自動テストとして残っている。
- 見出し以外の位置に同形文字列を含む `SPEC.md` を入力とする自動テストが追加され、抽出結果に当該文字列が含まれないことを確認している。
- リポジトリの既存の機械検査（doc-length・vocab・reference・テスト一式）が成功している。

## 検証方法

すべての受入条件を自動テストで検証する。ゲートレビュー用プロンプトの一覧欄については、対象 SHA へ `SPEC.md` を含む一時リポジトリを構成し、プロンプト生成結果の文字列を検査する。抽出結果の一致（プロンプト生成経路と突合検査経路）については、同一入力に対する両経路の結果を比較する。単純一致走査の不在についてはソースツリーの走査で確認する。

## 未決事項

- 無し（本 SPEC の要件・制約は本文内で確定している）。

## スコープ外

- ゲートレビュー用プロンプトの「AC-ID 未達の指摘は常に blocking とし、warning 以下へ格下げしない」という指示の変更。抽出が正しくなれば当該指示は妥当である。
- 受入条件見出しの記法（第 4 レベル見出し＋識別子＋コロン）そのものの変更。
- テンプレートに含まれる受入条件の追記手順コメントの文言変更。抽出が見出し限定になれば当該コメントは誤検出の原因にならない。
- Markdown のコードフェンス内に現れる宣言形の扱いの変更（制約節のとおり現状維持）。
- ゲートの不通過結果がセグメント作業ワーカーへ伝わらない別欠陥の是正。
- `VALIDATION.md` 側の AC 報告記法・スキーマの変更。
