# SPEC: size:quick の Issue でゲートが構造上通過不能な問題の是正

- Issue: `ISSUE-733`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/733-quick-gate-conformance-unpassable`

## 目的・背景

agent-skill-chain の憲法は、`size:quick` を指定した Issue について `SPEC.md`・`DESIGN.md`・`PLAN.md`・`VALIDATION.md` の作成義務を免除する。一方でゲート実装は Issue の size を一切参照しないため、免除規定に正しく従って成果物を作らなかった Issue が、成果物の品質と無関係に構造上ゲートを通過できない。この矛盾は次の 2 経路として現れる。

**経路A（判定プロンプトが立証を封じる）**: ゲートレビュア判定プロンプト（`gate reviewer-prompt` の出力）は、`SPEC.md` から AC-ID を抽出できないとき「conformance は inconclusive とし human_required へ倒すこと」とレビュアへ明示指示する。conformance ルーブリック自体も「適用対象の全 AC-ID / 要件が証跡付きに充足されているか」を唯一の判定軸として書かれている。final は「conformance と falsification の両 pass かつ blocking finding 無し→approved／いずれか fail もしくは blocking→rejected／inconclusive→human_required」と機械的に導出されるため、conformance が pass にならない限り approved は導出されない。

**経路B（レビュアが起動できない）**: ゲートレビュー起動（`gate local-review` 系）は、対象セグメントの必須成果物を target SHA から読めないとき `target SHAの必須成果物を読めません` に相当する中断を起こす。この中断はレビュア起動前に起きるため、gate-report は conformance・falsification とも `pending`、final は `human_required`、blockers は空のまま確定する。implementation ゲートは成果物不在を許容するが、spec・design・validation の各ゲートは中断する。

観測事実（2026-08-17、profile: strict、独立レビュア 2 体）：ISSUE-680 の implementation ゲートは 13 ラウンド全てで `conformance: pending`（レビュア証跡に「AC-ID を検出できず」「conformance を立証できません」と明記）、ISSUE-692 は 9 ラウンドで同様。ISSUE-680 の validation ゲートは経路B により exit 2 でレビュア 0 体。対照として `SPEC.md` を持つ ISSUE-721 の implementation ゲートは 2 体とも conformance/falsification 双方 pass、blockers 0 件で 1 ラウンド通過した。すなわち欠陥は成果物側ではなく判定機構側にある。

害は 3 点。(1) quick の Issue が自走でマージへ到達せず、進行役が毎回判定を上書きする運用が必要になる。(2) 成果物作成の負担を減らすための quick が、結果としてゲートの手動通過を強いるため意図を失う。(3) conformance が確定しない分レビュアの重心が反証側へ移り、ラウンド数の膨張に寄与する。

## 用語

- **size シグナル**: Issue の作業規模指定。取得元は、GitHub モードが Issue の `size:` プレフィックスラベル、ローカルモードが `state.yaml` の `size` キーである。
- **risk シグナル**: Issue のリスク分類。取得元は、GitHub モードが Issue の `risk:` プレフィックスラベル、ローカルモードが `state.yaml` の `risk` キーである。
- **シグナルの解決**: 取得元から値を読み取り、定義された値域のいずれか 1 つへ写像する操作。取得元にシグナルが存在しない状態（ラベル未付与・キー不在）は「解決不能」ではなく、既定値へ決定的に解決する。size の既定値は `standard`、risk の既定値は `unclassified` である。
- **シグナルの解決不能**: 次のいずれかに該当し、値を一意に確定できない状態。(a) 取得元の読み取りに失敗した（API 障害・ファイル読み取り失敗等）。(b) 読み取れた値が定義された値域に含まれない。(c) 相反する複数の値が同時に与えられている（同一プレフィックスのラベルが 2 つ以上付与されている等）。シグナルが存在しない状態はここに含めない。
- **ガードレール対象パス**: `docs/adr/`・`.agent-skill-chain/config/segments.yaml`・`AGENTS.md`・`.agent-skill-chain/schemas/`。
- **ガードレール判定**: base SHA から target SHA までの変更差分を取得し、ガードレール対象パスを含むかを判定する操作。結果は「含まない」「含む」「判定不能」のいずれかである。base SHA を解決できない、または差分の取得に失敗した場合を「判定不能」とする。
- **quick 免除**: size シグナルが `quick` に解決され、かつ risk シグナルが `normal` に解決され、かつガードレール判定が「含まない」であるときに限り成立する、4 成果物の作成義務免除。
- **免除不成立**: 上記 3 条件のいずれかを満たさない状態のすべて。シグナルの解決不能とガードレール判定不能を含む。免除不成立は「判定不能」ではなく、standard と同一の従来経路へ決定的に解決される状態である。
- **判定プロンプト**: レビュアへ渡される read-only の判定指示文字列。レビュアにはツール呼び出しが一切許可されないため、判定に使える情報はこの文字列に展開済みの内容のみである。判定プロンプトは互いに重ならない複数の区間からなり、本仕様は次の 2 区間を区別する。
- **判定対象区間**: 判定プロンプトのうち、レビュアが読むべき成果物の内容、または当該成果物が存在しない事由を提示する区間。
- **判定軸区間**: 判定プロンプトのうち、conformance を何によって判定するかをレビュアへ指示する区間。取りうる値は「AC-ID 全件網羅」「代替判定基準」「inconclusive 指示」の 3 つである。
- **必須成果物の読み取り**: target SHA 時点の対象ゲートの必須成果物について、存在の有無を確定し、存在する場合は内容を取得する操作。結果は成果物ごとに「存在し内容を取得できた」「存在しないことを確認できた」「読み取り不能」のいずれかである。**読み取り不能**とは、存在の有無自体を確定できない状態（取得元の障害等）、または存在するが内容の取得・復号・解析に失敗して内容を確定できない状態をいう。存在しないことを確認できた状態は読み取り不能に含めない。
- **AC-ID 抽出**: target SHA 時点の `SPEC.md` から受入条件の識別子を取り出す操作。結果は「1 件以上抽出できた」「抽出に成功し 0 件であった」「抽出不能」のいずれかである。`SPEC.md` が存在しないことを確認できた場合は「抽出に成功し 0 件であった」に含める。**抽出不能**とは、`SPEC.md` の読み取りが読み取り不能であるために AC-ID の件数を確定できない状態をいい、0 件であることを確認できた状態と区別する。
- **代替判定基準**: quick 免除下で AC-ID 全件網羅に代わって用いる conformance の判定軸。実体は当該 Issue 本文に記載された要求・期待する挙動・受入基準であり、対象ゲート（spec・design・implementation・validation）の別を問わず同一の実体を用いる。ゲートごとに異なる情報源へ切り替えることはしない。
- **代替判定基準の展開可否**: 判定プロンプト生成時点で Issue 本文を取得でき、かつ本文から要求・期待する挙動・受入基準に相当する記述を 1 件以上取り出して判定軸区間へ文字列として書き出せる状態を「展開できる」とする。Issue 本文を取得できない場合、または取り出せる記述が 0 件の場合を「展開できない」とする。
- **trusted base 側の起動経路**: リポジトリの default branch を保護された基点として実行される、ゲートレビュー起動・判定プロンプト生成・入力スナップショット保存の実行経路。Coordination Backend に由来する可変入力（size シグナル・risk シグナル・ガードレール判定・Issue 本文）の取得元を Coordination Backend の一次情報（GitHub モードは Issue 本文・ラベルの取得 API、ローカルモードは Issue 状態ファイル）に限り、target SHA 時点の成果物に由来する可変入力（必須成果物の読み取り結果・AC-ID 抽出結果）の取得元を target SHA に限る。
- **ワーカー供給経路**: セグメント作業ワーカーまたはゲートレビュアが内容を書き込める経路。branch 上の成果物ファイル、PR 本文、Issue コメント、ワーカーが作成した Issue 本文の写しを含む。trusted base 側の起動経路と異なり、判定を受ける側が内容を制御できる点で区別する。区別の基準は書き込み主体であって格納媒体ではないため、Git 管理下のパスであっても次の trusted 保存領域はワーカー供給経路に含めない。
- **trusted 保存領域**: 入力スナップショットの保存先として trusted base 側の起動経路のみが書き込む領域。GitHub モードは trusted base 側のゲート状態記録、ローカルモードは default branch 上の `reviews/<gate>.yaml` を指す。セグメント作業ワーカーの writer lease は当該領域への書き込みを含まない。作業ブランチ上に同名・同形式のファイルが存在しても、それは trusted 保存領域ではなくワーカー供給経路である。
- **inconclusive 指示**: 「conformance は inconclusive とし human_required へ倒すこと」に相当する、レビュアへの明示指示文字列。
- **可変入力**: 判定プロンプト生成に用いる入力のうち、target SHA・base SHA だけでは値が定まらず、同一 SHA のまま変化しうるもの。size シグナルの解決結果・risk シグナルの解決結果・ガードレール判定の結果・必須成果物の読み取り結果・AC-ID 抽出結果・Issue 本文（およびそこから定まる代替判定基準の展開可否）がこれに当たる。いずれも取得の成否が外部要因で変動するため、同一 SHA のまま解決結果が変わりうる。変更差分の内容と target SHA 時点の成果物本文そのものは SHA に固定されるため可変入力ではないが、それらを取得できるか否かは可変入力である。
- **入力スナップショット**: 判定プロンプト生成時点で解決した可変入力の値の組。attempt に 1 対 1 で対応付く。
- **attempt**: 1 回のゲートレビュー起動単位。同一の attempt ID・target SHA・base SHA・プロファイルのもとで起動されたレビュア群と、その証跡の集合を指す。
- **conformance / falsification / final**: 立証観点・反証観点の判定値（`pass|fail|pending`）と、そこから機械導出される最終判定（`approved|rejected|human_required`）。

## 前提・制約

- 本 Issue は quick 免除の範囲そのものを変更しない。憲法が定める免除規定を所与とし、判定機構側を適合させる。
- 不変条件 I8（安全側ラチェット）を緩めない。判定不能な状態から `approved` を導出してはならず、silent pass を作ってはならない。`risk != normal`（`unclassified` 含む）は非 normal 側として扱う。
- レビュアはツールを持たないため、conformance の判定軸は判定プロンプト内に実体として展開されていなければならない。展開できない場合は判定不能として扱う。
- レビュアは判定プロンプトの明示指示に従う。指示に反する verdict（inconclusive 指示を受けながら conformance を pass として返す等）の検知・強制は本仕様の対象外であり、既存どおり人間の確認に委ねる。
- 判定プロンプトは digest 照合の対象である。証跡照合は、記録済み証跡の prompt digest と、照合器が同一条件で再生成した判定プロンプトの digest を比較して成立する。再生成が照合の要であるのは、レビュアが供給した digest を無検証で信頼しないためであり、この性質は維持する。
- 上記の帰結として、可変入力を判定プロンプトへ導入する場合、再生成の入力を attempt へ固定しなければ照合が破綻し、成果物に欠陥が無くても誤った `human_required` を生む。
- 再生成の入力に使えるのは trusted base 側の起動経路が生成・保存した値のみとする。レビュアまたはワーカーが供給した値を照合入力にしない。この制約は要件8 として要件化し、AC-24 が照合入力として採用された値の取得経路を観測して検証する。
- size シグナル・risk シグナルの書き込み主体は憲法（`AGENTS.md`）が定める役割・権限に従い、セグメント作業ワーカーの権限には含まれないため、本仕様は当該シグナルの取得元をワーカー供給経路として扱わず、その書き込み主体を本仕様で再定義しない。
- **判定軸そのものの改変可能性**: quick 免除が成立し AC-ID を抽出できない場合、conformance の判定軸は Issue 本文だけになる。Issue 本文は人間・進行役・セグメント作業ワーカーが編集できるため、判定軸を緩める方向の編集によって `approved` へ到達しうる。入力スナップショットの固定はスナップショット確定「後」の改変しか防げず、確定「前」の改変は防げない。本仕様は次の 2 点のみを制約として確定し、改変の禁止・自動検知は設計セグメントへ送る（未決事項へ記載）。(i) Issue 本文の取得は trusted base 側の起動経路のみが行い、レビュア・セグメント作業ワーカーが供給した本文を判定軸の入力にしない。(ii) 入力スナップショットは代替判定基準の由来として Issue 本文の digest を含め、どの本文で `approved` に到達したかを事後に一意に特定できる状態にする。確定制約 (i) は要件9 として要件化し AC-22 が、(ii) は要件7 として要件化し AC-14 が、それぞれ検証する。
- 反証（falsification）側のルーブリックは Issue #729 が扱う。両者は同一の判定プロンプト生成箇所を変更するため、着手順序を調整し、先にマージした側の変更を後続が作り直さないこと。

## 入力・出力

- 入力: size シグナル、risk シグナル、base SHA から target SHA までの変更差分、target SHA 時点の成果物、Issue 本文（要求・期待する挙動・受入基準の記述）、対象ゲート ID、attempt に対応する入力スナップショット（証跡照合時）。
- 入力の取得元の信頼境界: 可変入力のうち Coordination Backend に由来するもの（size シグナル・risk シグナル・ガードレール判定・Issue 本文）は trusted base 側の起動経路のみが一次情報から取得し、ワーカー供給経路の値を**代替判定基準**の入力にしない。target SHA 時点の成果物はワーカーが著述する判定対象そのものであるため、判定対象区間への展開と AC-ID 抽出に限って用い、**代替判定基準**の実体としては用いない。証跡照合時の入力スナップショットは trusted 保存領域に保存されたもののみを用い、ワーカー供給経路に置かれた同形式の記録を照合入力にしない。
- 出力: quick 免除の成否、判定プロンプト文字列（判定対象区間・判定軸区間を含む）、入力スナップショットの耐久記録、ゲートレビュー起動の成否、verdict と gate-report、証跡照合の結果。

## 状態空間の定義

判定に影響する入力を独立した次元へ分解し、状態空間をその直積として定義する。受入条件は状態表の行に対して割り当てる。

### 入力次元

| 記号 | 次元 | 取りうる値（互いに排他かつ網羅） |
|---|---|---|
| D1 | size シグナルの解決結果 | `quick` / `standard` / 解決不能 |
| D2 | risk シグナルの解決結果 | `normal` / `normal` 以外 / 解決不能 |
| D3 | ガードレール判定の結果 | 含まない / 含む / 判定不能 |
| D4 | 対象ゲートの必須成果物の読み取り結果 | すべて存在し内容を取得できた / 読み取り不能な成果物が無く 1 件以上が不在（存在しないことを確認できた） / 1 件以上が読み取り不能 |
| D5 | AC-ID 抽出結果 | 1 件以上 / 0 件（`SPEC.md` が存在しないことを確認できた場合を含む） / 抽出不能 |
| D6 | 代替判定基準の展開可否 | 展開できる / 展開できない |
| D7 | 入力スナップショットの trusted 保存領域への保存可否 | 保存できる / 保存できない |
| D8 | 記録済み attempt に対応する trusted 保存領域の入力スナップショットの解決と、それを入力とする判定プロンプト再生成の可否 | 解決でき再生成できる / 解決できないか再生成できない |
| D9 | 保存済みスナップショットから再生成した判定プロンプトの digest と記録済み prompt digest の一致 | 一致 / 不一致 |
| D10 | レビュアが返した verdict | 両観点とも `pass` かつ blocking finding 無し / いずれかの観点が `fail` であるか blocking finding がある（他方が `pending` であるか否かを問わない） / いずれの観点も `fail` ではなく blocking finding も無く、かつ いずれかの観点が `pending` |
| D11 | ワーカー供給経路に置かれた「要求・期待する挙動・受入基準」相当の記述 | 存在する / 存在しない |
| D12 | ワーカー供給経路に置かれた入力スナップショット相当の記録 | 存在する / 存在しない |

対象ゲート ID は次元に含めない。ゲート ID は D4 の判定に用いる必須成果物集合を決めるパラメータにすぎず（spec は `SPEC.md`、design は `DESIGN.md` と `PLAN.md`、implementation は必須成果物なし、validation は `VALIDATION.md`）、期待結果はゲート ID ではなく D4 の値だけで決まるためである。各受入条件の When で対象ゲートの範囲を明示する。

### 派生値

いずれも状態表の次元だけから一意に決まる全域関数であり、受入条件が期待結果を委譲する場合の委譲条件はこれらの値のみで表現する。

- **E（quick 免除の成否）**: D1・D2・D3 の関数。表O1 が 27 通りすべてに対する値を定める。
- **S（判定軸区間の値）**: E・D4・D5・D6 の関数。表O2 が 36 通りすべてに対する値を定める。

### 観測対象

同一入力に対して 2 つの期待結果が定まる状態（＝実装不能）が生じるのは、2 つの受入条件が同一の観測対象を争う場合に限る。観測対象が異なれば、同一入力に対する 2 つの期待結果は同時に成立でき、対応する自動テストも同時に成功する。したがって排他性は観測対象ごとの状態表の内部でのみ要求する。観測対象は次の 11 個で、互いに素である。

| 記号 | 観測対象 | 状態表 |
|---|---|---|
| O1 | quick 免除の成否という判定結果そのもの | 表O1 |
| O2 | 判定プロンプトの判定軸区間が指示する判定軸の種別（S の値）と、その種別に応じた内容が書き出されているか否か | 表O2 |
| O3 | 判定プロンプトの判定対象区間の内容 | 表O3 |
| O4 | ゲートレビュー起動経路が中断するか、レビュアが起動されるか | 表O4 |
| O5 | 入力スナップショットの確定と trusted 保存領域への保存という副作用 | 表O5 |
| O6 | 同一入力に対する判定プロンプト出力の一致 | 表O6 |
| O7 | 証跡照合の結果 | 表O7 |
| O8 | final の導出値 | 表O8 |
| O9 | 判定軸区間へ書き出された代替判定基準の実体の取得経路 | 表O9 |
| O10 | 判定対象区間と判定軸区間の境界と、両区間が占める位置の非重複 | 表O10 |
| O11 | 証跡照合の判定プロンプト再生成で入力として採用されたスナップショットの取得経路 | 表O11 |

O2 と O3 が互いに素であることは、判定プロンプトが両区間を互いに重ならない別々の区間として含むこと（要件3）によって保証する。O2 の受入条件は判定軸区間のみを、O3 の受入条件は判定対象区間のみを観測する。この非重複性そのものは O10 の受入条件が直接観測するため、O2 と O3 の分離は未検証の前提に依存しない。S が D4 にも依存すること（判断11）は、この分離を損なわない。O2 と O3 は同一の入力次元 D4 を共有するが、期待結果を定める観測位置（判定軸区間と判定対象区間）が互いに素であるため、同一入力に対して両表の行が同時に成立しても相反しない（判断5）。表O3 と表O4 が E・D4 を共有する場合と同じ構造である。

O2 と O9 は互いに素である。O2 は判定軸区間が指示する判定軸の種別と、その種別に応じた内容が書き出されているか否かのみを観測し、書き出された実体がどの取得経路の値であるかは観測しない。O9 は S が「代替判定基準」である場合に限り、判定軸区間へ書き出された実体の取得経路のみを観測し、S の値そのものを期待結果としない。O10 は両区間の境界と位置の重なりのみを観測し、各区間の内容が何であるか（O2・O3）は観測しない。

O7 と O11 は互いに素である。O7 は照合が成立するか否かという結果のみを観測し、再生成の入力として何を採用したかは観測しない。O11 は再生成の入力として採用された値の取得経路のみを観測し、照合の成否そのものを期待結果としない。

O8 はレビュアの verdict が存在する場合にのみ観測できる。レビュアが起動されない入力では final がそもそも導出されず、その旨は O4・O5 の受入条件が定める。したがって「レビュアを起動しないため `approved` に到達しない」という記述と、表O8 が定める final の導出規則は同一の観測対象を争わない。

### 次元間の依存と、発生しない組み合わせ

- spec ゲートに限り、D4 が「1 件以上が不在」であることは `SPEC.md` の不在を意味するため D5 は必ず「0 件」になり、D4 が「1 件以上が読み取り不能」であることは D5 が必ず「抽出不能」になることを意味する。表O2 は D4 と D5 の双方を用いるため、spec ゲートではこの依存により到達しない行が生じる（D4 が「1 件以上が不在」かつ D5 が「1 件以上」である行）。到達しない行があること自体は表の網羅・排他を損なわない。表は 4 ゲートに共通の状態空間を定義しており、当該行は design・validation ゲート（`DESIGN.md`・`PLAN.md`・`VALIDATION.md` が不在でも `SPEC.md` から AC-ID を抽出できる）で到達するためである。spec 以外のゲートでは `SPEC.md` が必須成果物ではないため、D4 が「すべて存在し内容を取得できた」であっても D5 が「抽出不能」でありうる。
- 必須成果物を定義しないゲート（implementation）では、D4 は常に「すべて存在し内容を取得できた」となる（空集合のすべての要素について内容を取得できたと解する）。したがって当該ゲートでは D4 が「1 件以上が不在」「1 件以上が読み取り不能」である行に到達しない。
- D9 は D8 が「解決でき再生成できる」場合にのみ定義される。表O7 で該当行を明示する。
- D7 は、必須成果物の読み取りで中断しない入力でのみスナップショット保存の副作用として観測できる。読み取り不能による中断も同じ位置で起きるため、D4 が「1 件以上が読み取り不能」である入力では D7 を観測できない。起動可否（表O4）には D7 も寄与するため、D4 と D7 を同一の表に置く。表O5 では観測できない行を除外行として明示する。
- D10 は、判定プロンプトが生成されレビュアが起動された場合にのみ観測できる。表O8 で除外行とその理由を明示する。
- D11 は、S が「代替判定基準」であり判定軸区間へ実体が書き出される場合にのみ、その取得経路として観測できる。表O9 で除外行とその理由を明示する。D11 は判定プロンプトの入力ではないため（要件9 により判定軸の入力から除かれる）、他の表の期待結果を変えない。
- D12 は、記録済み attempt の証跡照合を行う場合にのみ、再生成入力の取得経路として観測できる。D12 は照合器の入力ではないため（要件8 により照合入力から除かれる）、他の表の期待結果を変えない。とくに表O7 の照合結果は D12 の値によって変わらない。

## 仕様判断とその根拠

**判断1: シグナルが存在しない状態は既定値へ決定的に解決し、「解決不能」は取得の失敗・値域外・相反する複数値に限る。**

未付与を「解決不能」に含めると、size ラベルを運用していない既存の全 Issue が解決不能となり、適正な `SPEC.md` を持つ通常の Issue まで `approved` へ到達できなくなる（最大の回帰）。逆に取得失敗・値域外まで既定値へ落とすと、外形的事由で quick 免除が誤成立しうる。両者を分離し、未付与は決定的な既定値、取得の失敗は安全側とする。これにより「size シグナルの定義」と「解決不能の定義」が同一入力を二重に定義する状態を解消する。

**判断2: risk シグナルの未付与は `unclassified` として `normal` 以外に解決する。**

不変条件 I8 が `risk != normal`（`unclassified` を含む）を非 normal 側として扱うと定めているため。帰結として risk ラベルの無い Issue では quick 免除が成立しない。quick 免除は進行役の明示的オプトインでのみ成立し自動昇格しないという憲法の規定と整合する。

**判断3: ガードレール差分を取得できない場合は第三の値「判定不能」とし、免除不成立側へ写像する。**

差分を取得できない状態を「含まない」とみなすと、ガードレール対象パスを変更した Issue が免除を得る経路が生じる。「含む」とみなすのは安全側だが、事由の異なる状態を同一視して原因追跡ができなくなる。第三の値として明示したうえで、免除の成否としては不成立側へ倒す。

**判断4: 可変入力は attempt ごとにスナップショットとして固定し、証跡照合は保存済みスナップショットの解決値と SHA に固定された不変の内容のみを入力として再生成する。**

可変入力を digest 対象から除外する案は、判定プロンプトの実内容と証跡が乖離するため採らない。照合時に可変入力を読み直す案は、Issue 本文の編集やラベル変更だけで既存の正当な証跡が不一致となるため採らない。ガードレール判定の結果をスナップショットへ含めるのは、差分の内容が SHA で固定される一方、取得の成否が外部要因で変動し、同一 SHA で E が変わりうるためである。必須成果物の読み取り結果と AC-ID 抽出結果にも同じ基準をそのまま適用し、可変入力としてスナップショットへ含める。判断9 によりこれらは取得の失敗を第三の値（読み取り不能・抽出不能）として持つため、成果物本文が SHA に固定されていても取得の成否が外部要因で変動し、同一 target SHA のまま判定軸区間の値 S と判定対象区間の内容が変わりうるからである。これを含めない場合、照合時の一過性の読み取り失敗・成功だけで再生成結果が原 attempt と食い違い、成果物にも証跡にも欠陥が無いのに誤った `human_required` を生む。可変入力が変化した後に改めて判定が必要な場合は、既存 attempt を書き換えず新しい attempt を起こす。

**判断5: 状態空間を「観測対象 × 入力次元」の直積として定義し、排他性は観測対象ごとに要求する。**

受入条件どうしの相反（＝実装不能）は、同一入力に対して同一の観測対象へ 2 つの異なる期待結果が定まる場合にのみ生じる。観測対象が異なる 2 つの受入条件は、同一入力で同時に成立してよい。入力条件だけで排他性を確保しようとすると、観測対象の異なる受入条件を単一の表へ混在させることになり、実際には矛盾していない対を矛盾として扱うか、逆に真の矛盾を見落とす。観測対象を明示的な次元として立てることで、網羅と排他を機械的に確認できる形にする。

**判断6: 判定プロンプトを判定対象区間と判定軸区間の 2 つの独立な区間へ分解する。**

「何を読んで判定するか」（判定対象区間）は必須成果物の読み取り結果で決まり、「何をもって conformance とするか」（判定軸区間）は主として AC-ID 抽出結果と代替判定基準の展開可否で決まる。両者は別々の観測対象である。単一の観測対象として扱うと、たとえば「quick 免除成立・`DESIGN.md` 欠落・`SPEC.md` から AC-ID 抽出可」という入力に対して、成果物不在を扱う受入条件と AC-ID 網羅を扱う受入条件が相反する。区間を分離し、各受入条件がどちらか一方の区間のみを観測することで、この相反を構造的に排除する。分離が成り立つこと自体は前提として置かず、独立した観測対象（O10）として受入条件で検証する。前提のままにすると、他の表の排他性の根拠が未検証の前提に依存する。

ただし区間の分離が排除するのは受入条件どうしの相反（実装不能）だけであり、上記の入力で「網羅を立証すべき成果物が判定対象区間に存在しないまま AC-ID 全件網羅が判定軸として指示される」という判定不能そのものは解消しない。この残る判定不能は判断11 が扱い、判定軸区間の値 S を必須成果物の読み取り結果にも依存させることで解決する。したがって S は判定対象区間の**内容**には依存せず、判定対象区間の内容を決める入力次元（D4）にのみ依存する。区間の内容どうしが相互に依存しないという本判断の分離は保たれる。

**判断7: 代替判定基準の取得経路を trusted base 側へ限定することを、前提の記述ではなく要件・受入条件として確定する。**

quick 免除が成立し AC-ID を抽出できない場合、conformance の判定軸は代替判定基準だけになる。この実体の取得経路を制約しないと、判定を受ける側であるセグメント作業ワーカーが自ら判定軸を供給でき、緩い受入基準相当の記述を置くだけで `approved` へ到達する自己承認経路が成立する。これは quick 経路に限って I8（安全側ラチェット・silent pass 禁止）を実質的に無効化する。取得経路の限定を前提・制約の記述にとどめると実装・テストが観測しないため、要件として明記し、実体の取得経路を観測する受入条件で検証する。判定対象区間へ展開される成果物、および `SPEC.md` から抽出した AC-ID 集合はワーカーが著述するものであり、これは判定対象そのものであるため本限定の対象外とする（限定するのは**代替判定基準**の入力のみである）。

**判断8: 入力スナップショットの出所も判断7 と同じ形で要件・受入条件として確定し、保存先を trusted 保存領域として定義する。**

判定軸（Issue 本文）の取得経路だけを閉じても、照合入力である入力スナップショットの出所が開いていれば自己承認経路は残る。判定を受ける側が、緩い受入基準を含む Issue 本文の記述を解決値として持つスナップショットと、それに整合する prompt digest を自ら供給できれば、照合は成立し、非 `approved` 経路にも該当しない。自己承認経路は判定軸側と照合入力側の双方を閉じて初めて塞がるため、判断7 が確立した原則（取得経路の限定を前提の記述にとどめず、要件として明記し、取得経路を観測する受入条件で検証する）を要件8 にも同じ形で適用する。あわせて、ローカルモードの保存先 `reviews/<gate>.yaml` が作業ブランチ上に置かれるとワーカーが書き込めるため、保存先を default branch 上の実体（trusted 保存領域）と定め、作業ブランチ上の同名ファイルを保存先にも照合入力にもしない。用語『ワーカー供給経路』の区別基準を格納媒体ではなく書き込み主体とすることで、保存先が Git 管理下にあることと信頼境界とが矛盾しない形へ揃える。

**判断9: 必須成果物の読み取り不能と AC-ID 抽出不能を、不在・0 件と同一視せず第三の値として扱う。**

判断1 が「取得の失敗を既定値と同一視しない」、判断3 が「差分の取得失敗を第三の値として安全側へ倒す」と定めた原則を、必須成果物の読み取りと AC-ID 抽出にも同じ形で適用する。読み取り失敗を不在へ吸収すると、quick 免除下では実在する成果物が「免除により正当に不在」として提示され、レビュアは成果物を読まないまま判定する。同時に AC-ID 抽出結果が「0 件」へ写像されるため、conformance の判定軸が AC-ID 全件網羅から Issue 本文由来の代替判定基準へ無言で降格したまま `approved` に到達しうる。これは判定不能な状態から `approved` を導出しないという I8 の要求を quick 経路でのみ破る。倒す先は 2 つに分ける。必須成果物の読み取り不能は起動を中断する側へ倒す——判定対象そのものを読めない状態ではレビューが成立せず、免除の成否によらず継続する理由が無いためである。AC-ID 抽出不能は中断させず inconclusive 指示側へ倒す——spec 以外のゲートでは判定対象区間の内容を提示できるため、レビュアへ判定不能である旨を明示したうえで人間判断へ渡す方が、無言の中断より原因追跡が容易だからである。いずれの場合も代替判定基準へは切り替えない。

**判断10: verdict の値を排他にするにあたり、`fail`・blocking finding を `pending` より優先する。**

D10 の値が互いに排他でなければ、同一の verdict に対して表O8 が 2 つの期待結果を定め、実装が一意に決まらない（判断5 が排除の対象とした実装不能そのものである）。したがって `fail`・blocking finding と `pending` が同時に成立する verdict の扱いを一意に確定する必要がある。fail・blocking 側を優先する理由は 3 点である。(1) `rejected` と `human_required` はいずれも `approved` ではないため、どちらを優先しても「判定不能な状態から `approved` を導出しない」という I8 の要求は等しく満たされ、安全性に差が生じない。(2) 既存の final 導出は fail・blocking を優先して `rejected` を返しており、pending 優先へ改めると本 Issue の目的（quick 経路でゲートが構造上通過できない欠陥の是正）と無関係な既存挙動の変更を持ち込み、免除不成立の主経路に回帰を生む。(3) blocking finding という決定的な否定判定が既に得られている状態を `human_required` へ倒すと、差し戻し先が確定しているにもかかわらず人間の判断を要求することになり、是正が遅れる。

**判断11: quick 免除により対象ゲートの必須成果物が正当に不在である場合は、AC-ID を抽出できても判定軸を AC-ID 全件網羅から代替判定基準へ切り替える。**

判定軸区間の値 S を E・D5・D6 だけで決めると、quick 免除が成立し `SPEC.md` から AC-ID を抽出できる Issue において、対象ゲートの必須成果物が免除により不在である入力（design ゲートで `DESIGN.md`・`PLAN.md` が不在である場合、validation ゲートで `VALIDATION.md` が不在である場合）で、判定軸が「AC-ID 全件網羅」のまま網羅を立証すべき成果物が判定対象区間に存在しない。conformance は成果物の品質と無関係に構造上 fail となり final は `rejected` へ落ちるため、本仕様の要求（quick 免除に正しく従った Issue が進行役の上書き無しにゲートを通過できること）が達成されない。この入力は状態空間に明示的に存在するため、放置は要求の未達を意味する。

採りうる案は 3 つある。(a) 網羅の対象 AC-ID を、不在の成果物に依存しないものへ限定する。(b) 免除下では網羅要求を緩め、不在の成果物に係る欠落を不充足として数えないよう判定軸区間で指示する。(c) 判定軸を代替判定基準へ切り替える。**本仕様は (c) を採る。** 理由は 4 点である。(1) (a) は、どの AC-ID の立証がどの成果物に依存するかを判定機構が機械的に決められず、限定規則そのものが判定不能になる。(b) は判定軸区間へ「欠落を不充足として数えない」という緩和指示を新設することになり、S の値域を増やしたうえで、レビュアが緩和の範囲をどう解釈するかに結果が依存する。(c) は quick 免除下で AC-ID を抽出できない場合のためにすでに定義済みの機構（代替判定基準・S の 3 値・AC-5・AC-6）をそのまま再利用でき、判定軸区間に新たな値も緩和指示も導入しない。(2) 切替の発火条件を「E が成立し、かつ必須成果物が不在であることを**確認できた**」に限り、読み取り不能（D4 が「1 件以上が読み取り不能」）では切り替えない。判断9 が禁じた「取得の失敗による判定軸の無言の降格」は起こさない。免除不成立の場合も切り替えないため、既存の主経路に回帰を生まない。(3) E の成立には size が `quick`・risk が `normal`・ガードレール対象パス非該当のすべてが解決済みであることを要し、これらのシグナルの書き込み主体は憲法上セグメント作業ワーカーの権限に含まれないため（前提・制約に記載）、判定を受ける側が切替を発火させられない。(4) 切替先の代替判定基準を展開できない場合は inconclusive 指示へ倒れるため、判定軸が実体化しない状態から `approved` は導出されない。

帰結として S は E・D4・D5・D6 の関数となる。必須成果物を定義しない implementation ゲートでは D4 が「1 件以上が不在」になりえないため切替は起きず、当該ゲートの既存挙動は変わらない。

## 状態表

各表は当該観測対象について、行が互いに排他であり、当該表が用いる次元の全組み合わせを網羅する。各行はちょうど 1 つの受入条件に対応する。AC-1 から AC-27 はいずれかの表の行に対応し、AC-28 だけは入力状態を持たないメタ受入条件（他の受入条件のテスト集合が存在することを要求する）であるため、どの表の行にも対応しない。

### 表O1: quick 免除の成否（次元 D1 × D2 × D3、27 通り）

| 行 | D1 | D2 | D3 | 組合せ数 | E（期待結果） | 対応 AC |
|---|---|---|---|---|---|---|
| 1 | `quick` | `normal` | 含まない | 1 | 成立 | AC-1 |
| 2 | `quick` または `standard` | `normal` または `normal` 以外 | 含まない または 含む（行1 を除く） | 7 | 不成立 | AC-2 |
| 3 | D1・D2・D3 の少なくとも 1 つが解決不能または判定不能 | 同左 | 同左 | 19 | 不成立 | AC-3 |

網羅: 3 次元とも解決できた組み合わせは 2×2×2 = 8 通りで、うち 1 通りが行1、残り 7 通りが行2。少なくとも 1 つが解決不能・判定不能である組み合わせは 27 − 8 = 19 通りで行3。合計 1 + 7 + 19 = 27。排他: 行1 と行2 は D1・D2・D3 の値の組で区別され、行3 は「解決できたか」で行1・行2 と区別される。

### 表O2: 判定軸区間の値 S（次元 E × D4 × D5 × D6、36 通り）

| 行 | E | D4 | D5 | D6 | 組合せ数 | S（期待結果） | 対応 AC |
|---|---|---|---|---|---|---|---|
| 1 | 成立 | すべて存在し内容を取得できた、または 1 件以上が読み取り不能 | 1 件以上 | 問わない | 4 | AC-ID 全件網羅 | AC-4 |
| 2 | 成立 | 1 件以上が不在 | 1 件以上 | 展開できる | 1 | 代替判定基準 | AC-5 |
| 3 | 成立 | 1 件以上が不在 | 1 件以上 | 展開できない | 1 | inconclusive 指示 | AC-6 |
| 4 | 成立 | 問わない | 0 件 | 展開できる | 3 | 代替判定基準 | AC-5 |
| 5 | 成立 | 問わない | 0 件 | 展開できない | 3 | inconclusive 指示 | AC-6 |
| 6 | 不成立 | 問わない | 1 件以上 | 問わない | 6 | AC-ID 全件網羅 | AC-7 |
| 7 | 不成立 | 問わない | 0 件 | 問わない | 6 | inconclusive 指示 | AC-8 |
| 8 | 問わない | 問わない | 抽出不能 | 問わない | 12 | inconclusive 指示 | AC-25 |

網羅: 2×3×3×2 = 36 通りを、行ごとの組合せ数 4 + 1 + 1 + 3 + 3 + 6 + 6 + 12 = 36 で覆っている。排他: 行8 は D5 が「抽出不能」である点で行1〜行7 と区別される。行1〜行5（E=成立）と行6〜行7（E=不成立）は E で区別される。行1〜行3（D5=1 件以上）と行4〜行5（D5=0 件）、および行6（D5=1 件以上）と行7（D5=0 件）は D5 で区別される。行1（D4 が「1 件以上が不在」ではない）と行2・行3（D4 が「1 件以上が不在」）は D4 で区別され、行2 と行3、および行4 と行5 は D6 で区別される。したがってどの 2 行も同時に成立しない。

行2・行3（quick 免除が成立し、AC-ID を抽出できるが対象ゲートの必須成果物が免除により正当に不在である）は、判断11 のとおり AC-ID 全件網羅を判定軸とせず、行4・行5 と同じ切替先（代替判定基準、展開できない場合は inconclusive 指示）を採る。網羅を立証すべき成果物が存在しない状態で AC-ID 全件網羅を判定軸とすると conformance が構造上 fail となり、免除に正しく従った Issue が通過できないためである。行1 は同じ quick 免除成立下でも、必須成果物がそろっている場合と読み取り不能である場合を含み、切替を起こさない——読み取り不能による切替は判定軸の無言の降格になるため、判断9 のとおり行わない（当該入力ではそもそも表O4 の行9〜行12 により起動が中断する）。行6（免除不成立かつ AC-ID を 1 件以上抽出できる）は既存の主経路であり、最大の回帰対象として AC-7 が単独で定める。免除不成立の場合は必須成果物が欠落していても切替を起こさないため、本変更の前後で行6 の期待結果は変わらない。行8（抽出不能）は、判断9 のとおり「0 件」（行4・行5・行7）と別の値として扱い、quick 免除が成立し代替判定基準を展開できる場合であっても代替判定基準へ切り替えない。

### 表O3: 判定対象区間の内容（次元 E × D4、6 通り）

| 行 | E | D4 | 期待結果 | 対応 AC |
|---|---|---|---|---|
| 1 | 成立 | すべて存在し内容を取得できた | 必須成果物の内容が展開され、不在を示す記述を含まない | AC-9 |
| 2 | 不成立 | すべて存在し内容を取得できた | 同上 | AC-9 |
| 3 | 成立 | 1 件以上が不在 | 欠落した各必須成果物について、quick 免除により不在である旨を明示する | AC-10 |
| 4 | 不成立 | 1 件以上が不在 | 欠落した各必須成果物について、本来存在すべき成果物が欠落している旨を明示する | AC-11 |
| 5 | 成立 | 1 件以上が読み取り不能 | 読み取り不能な各必須成果物について、内容を取得できなかった旨を明示する。免除による正当な不在としても、欠落としても提示しない | AC-26 |
| 6 | 不成立 | 1 件以上が読み取り不能 | 同上 | AC-26 |

網羅: 2×3 = 6 行をすべて列挙している。排他: 各行は E・D4 の値の組が相異なる。行5・行6 の期待結果が E によって分岐しないのは、読み取り不能が免除の成否と無関係な取得側の失敗だからである。

### 表O4: ゲートレビュー起動経路の中断有無（次元 E × D4 × D7、12 通り）

起動経路は「必須成果物の読み取り」→「入力スナップショットの保存」→「レビュア起動」の順に進むため、D4 と D7 の双方が起動可否を決める。両者を同一の表に置き、期待結果を一意にする。

| 行 | E | D4 | D7 | 期待結果 | 対応 AC |
|---|---|---|---|---|---|
| 1 | 成立 | すべて存在し内容を取得できた | 保存できる | 中断せずレビュアが起動される | AC-12 |
| 2 | 不成立 | すべて存在し内容を取得できた | 保存できる | 同上 | AC-12 |
| 3 | 成立 | 1 件以上が不在 | 保存できる | 同上（必須成果物の不在を理由に中断しない） | AC-12 |
| 4 | 不成立 | 1 件以上が不在 | 保存できる | 必須成果物の読み取り時点で中断しレビュアを起動せず、`approved` を導出しない | AC-13 |
| 5 | 不成立 | 1 件以上が不在 | 保存できない | 同上（中断が保存より前に起きるため D7 の値は結果を変えない） | AC-13 |
| 6 | 成立 | すべて存在し内容を取得できた | 保存できない | スナップショット保存の失敗により中断しレビュアを起動せず、`approved` を導出しない | AC-15 |
| 7 | 不成立 | すべて存在し内容を取得できた | 保存できない | 同上 | AC-15 |
| 8 | 成立 | 1 件以上が不在 | 保存できない | 同上 | AC-15 |
| 9 | 成立 | 1 件以上が読み取り不能 | 保存できる | 読み取り不能を検知した時点で中断しレビュアを起動せず、`approved` を導出しない | AC-27 |
| 10 | 成立 | 1 件以上が読み取り不能 | 保存できない | 同上（中断が保存より前に起きるため D7 の値は結果を変えない） | AC-27 |
| 11 | 不成立 | 1 件以上が読み取り不能 | 保存できる | 同上 | AC-27 |
| 12 | 不成立 | 1 件以上が読み取り不能 | 保存できない | 同上 | AC-27 |

網羅: 2×3×2 = 12 行をすべて列挙している。排他: 各行は E・D4・D7 の値の組が相異なる。表O3 と E・D4 を共有するが観測対象が異なるため、両表の行が同時に成立しても相反しない。

### 表O5: 入力スナップショットの確定・保存（次元 E × D4 × D7、12 通り）

| 行 | 条件 | 期待結果 | 対応 AC |
|---|---|---|---|
| 1 | 表O4 の行1〜3 | レビュア起動前にスナップショットが確定し、attempt と 1 対 1 で trusted 保存領域へ保存される | AC-14 |
| 2 | 表O4 の行6〜8 | スナップショットは trusted 保存領域に残らず、対応する attempt も成立しない | AC-15 |
| — | 表O4 の行4・行5 | 除外。必須成果物の読み取り時点で中断し、スナップショット確定に到達しないため当該観測対象が存在しない | 対応なし |
| — | 表O4 の行9〜行12 | 除外。読み取り不能を検知した時点で中断し、スナップショット確定に到達しないため当該観測対象が存在しない | 対応なし |

網羅: 12 行のうち 6 行を行1・行2 が覆い、残る 6 行を上記の理由で除外している。排他: 行1 と行2 は D7 の値で区別される。

### 表O6: 判定プロンプト生成の決定性（全次元の全組み合わせ、1 行）

| 行 | 条件 | 期待結果 | 対応 AC |
|---|---|---|---|
| 1 | 全次元の任意の組み合わせ | 同一の Issue・ゲート・target SHA・base SHA・入力スナップショットに対する出力が完全に一致する | AC-16 |

網羅: 全組み合わせが行1 に含まれる。排他: 行が 1 つのため自明に成立する。

### 表O7: 証跡照合の結果（次元 D8 × D9、3 通り）

| 行 | D8 | D9 | 期待結果 | 対応 AC |
|---|---|---|---|---|
| 1 | 解決でき再生成できる | 一致 | 照合が成立する。照合時点の可変入力の変化は結果を変えない | AC-17 |
| 2 | 解決でき再生成できる | 不一致 | `approved` を導出せず、照合を失敗させるか `human_required` へ倒れる | AC-18 |
| 3 | 解決できないか再生成できない | 定義されない | 同上 | AC-18 |

網羅: D9 は D8 が「解決でき再生成できる」場合にのみ定義されるため、状態は 2 + 1 = 3 通りであり、すべて列挙している。排他: 各行は D8・D9 の値の組が相異なる。

### 表O8: final の導出（次元 S × D10、9 通り）

D10 の 3 値は互いに排他である（入力次元の定義により、`fail`・blocking finding は `pending` に優先する。根拠は判断10）。したがって 1 つの verdict はちょうど 1 つの D10 値に該当し、S と併せて行がちょうど 1 つ定まる。

| 行 | S | D10 | 期待結果 | 対応 AC |
|---|---|---|---|---|
| 1 | AC-ID 全件網羅 | 両観点とも `pass` かつ blocking finding 無し | `approved` | AC-19 |
| 2 | 代替判定基準 | 両観点とも `pass` かつ blocking finding 無し | `approved` | AC-19 |
| 3 | AC-ID 全件網羅 | いずれかが `fail` もしくは blocking finding あり | `rejected` | AC-20 |
| 4 | 代替判定基準 | いずれかが `fail` もしくは blocking finding あり | `rejected` | AC-20 |
| 5 | AC-ID 全件網羅 | `fail`・blocking finding が無く、いずれかが `pending` | `human_required` | AC-20 |
| 6 | 代替判定基準 | `fail`・blocking finding が無く、いずれかが `pending` | `human_required` | AC-20 |
| 7 | inconclusive 指示 | いずれかが `fail` もしくは blocking finding あり | `rejected`。`approved` を導出しない | AC-21 |
| 8 | inconclusive 指示 | `fail`・blocking finding が無く、いずれかが `pending` | `human_required`。`approved` を導出しない | AC-21 |
| — | inconclusive 指示 | 両観点とも `pass` かつ blocking finding 無し | 除外。判定プロンプトが conformance を inconclusive とするよう明示指示している以上、前提・制約（レビュアは明示指示に従う）の下では conformance が `pass` にはならないため発生しない | 対応なし |

網羅: 3×3 = 9 行のうち 8 行を行1〜行8 が覆い、残る 1 行を上記の理由で除外している。排他: 各行は S・D10 の値の組が相異なり、D10 の 3 値が互いに排他であることは入力次元の定義が保証する。行7 は、レビュアが指示に従い conformance を `pending` としたうえで falsification を `fail` とした場合、または blocking finding を返した場合に到達する正規の行であり、除外行ではない。

### 表O9: 代替判定基準の実体の取得経路（次元 S × D11、6 通り）

| 行 | S | D11 | 期待結果 | 対応 AC |
|---|---|---|---|---|
| 1 | 代替判定基準 | 存在する | 判定軸区間へ書き出される実体は、trusted base 側の起動経路が Coordination Backend の一次情報から取得した Issue 本文のみに由来する。ワーカー供給経路にだけ置かれた記述は判定軸区間に現れない | AC-22 |
| 2 | 代替判定基準 | 存在しない | 同上（由来は trusted base 側の起動経路が取得した Issue 本文のみである） | AC-22 |
| — | AC-ID 全件網羅 または inconclusive 指示 | 存在する / 存在しない | 除外。代替判定基準の実体が判定軸区間へ書き出されないため、その取得経路という観測対象が存在しない | 対応なし |

網羅: 3×2 = 6 行のうち 2 行を行1・行2 が覆い、残る 4 行を上記の理由で除外している。排他: 行1 と行2 は D11 の値で区別される。表O2 と S を共有するが、表O2 が S の値そのものを、表O9 が S が「代替判定基準」である場合の実体の取得経路を観測するため、両表の行が同時に成立しても相反しない。

### 表O10: 判定対象区間と判定軸区間の非重複（全次元の全組み合わせ、1 行）

| 行 | 条件 | 期待結果 | 対応 AC |
|---|---|---|---|
| 1 | 判定プロンプトを生成する任意の入力（全次元の任意の組み合わせ） | 両区間の境界が判定プロンプト内で一意に識別でき、両区間が占める位置は互いに素である。一方の区間の入力にだけ与えた標識が他方の区間に現れない | AC-23 |

網羅: 全組み合わせが行1 に含まれる。排他: 行が 1 つのため自明に成立する。

### 表O11: 照合入力として採用したスナップショットの取得経路（次元 D8 × D12、4 通り）

| 行 | D8 | D12 | 期待結果 | 対応 AC |
|---|---|---|---|---|
| 1 | 解決でき再生成できる | 存在する | 再生成の入力として採用されるのは trusted 保存領域のスナップショットのみであり、ワーカー供給経路に置かれた記録は採用されない | AC-24 |
| 2 | 解決でき再生成できる | 存在しない | 同上（採用される入力は trusted 保存領域のスナップショットのみである） | AC-24 |
| 3 | 解決できないか再生成できない | 存在する | 同上（ワーカー供給経路の記録を代替の照合入力として採用しない。照合の成否そのものは表O7 が定める） | AC-24 |
| 4 | 解決できないか再生成できない | 存在しない | 同上（採用しうる入力が存在しない） | AC-24 |

網羅: 2×2 = 4 行をすべて列挙している。排他: 各行は D8・D12 の値の組が相異なる。表O7 と D8 を共有するが、表O7 が照合の成否を、本表が再生成入力の取得経路を観測するため、両表の行が同時に成立しても相反しない。

## 要求 → 要件 → 受入条件

### 要求

quick 免除に正しく従った Issue が、進行役による判定の上書き無しにゲートを通過できること。同時に、成果物が本来存在すべきなのに欠落している Issue では、従来どおり安全側（`human_required`）へ倒れること。

### 要件

- 要件1: 判定機構は size シグナル・risk シグナル・ガードレール判定をモード別の取得元から解決し、quick 免除の成否を決定的に導く。シグナルが存在しない状態は既定値（size は `standard`、risk は `unclassified`）へ決定的に解決する。
- 要件2: size シグナル・risk シグナルの解決不能、およびガードレール判定不能は、いずれも免除不成立として扱う。解決できない値を既定値とみなして免除を成立させない。
- 要件3: 判定プロンプトは判定対象区間と判定軸区間を互いに重ならない区間として含み、各区間の内容が独立に決まる。
- 要件4: quick 免除が成立し、かつ (i) AC-ID を 1 件も抽出できない場合、または (ii) 対象ゲートの必須成果物のうち 1 件以上が不在であることを確認できた場合は、AC-ID 全件網羅を判定軸とせず、代替判定基準を判定軸区間へ実体として展開し、conformance が成立しうるようにする。展開できない場合は判定プロンプト生成を中断せず、inconclusive 指示へ倒す。免除により正当に不在の成果物があることのみを理由に conformance が構造上 fail となる判定軸を出力しない。必須成果物が読み取り不能である場合は (ii) に該当させず、切替を起こさない。
- 要件5: AC-ID を 1 件以上抽出できる場合は、要件4 の (ii) に該当する場合を除き、免除の成否によらず AC-ID 全件網羅を判定軸とする。quick 免除が成立しない場合は、必須成果物が欠落していても代替判定基準へ切り替えない。免除不成立かつ AC-ID 抽出が 0 件である場合は従来どおり inconclusive 指示とし、代替判定基準は展開しない。AC-ID 抽出が不能である場合は、免除の成否と代替判定基準の展開可否によらず inconclusive 指示とし、抽出不能を 0 件と同一視して代替判定基準へ切り替えない。final の導出規則は従来どおりとし、`fail`・blocking finding と `pending` が同時に成立する verdict では `fail`・blocking finding を優先して `rejected` を導出する。
- 要件6: quick 免除が成立する場合、必須成果物が target SHA に存在しないことを理由にゲートレビュー起動を中断しない。免除が成立しない場合の中断挙動は維持する。欠落した必須成果物は判定対象区間で不在事由とともに明示し、無言の空欄にしない。必須成果物が読み取り不能である場合は、免除の成否によらずゲートレビュー起動を中断して `approved` を導出せず、読み取り不能を不在と同一視しない。判定対象区間を単独で生成する場合は、読み取り不能である旨を、免除による正当な不在とも欠落とも区別して明示する。
- 要件7: 判定プロンプト生成に用いた可変入力の解決値を入力スナップショットとして確定し、attempt に対応付けてレビュア起動前に耐久記録へ保存する。保存先は trusted 保存領域とし、GitHub モードは trusted base 側のゲート状態記録、ローカルモードは default branch 上の `reviews/<gate>.yaml` とする。いずれのモードでも保存先は trusted base 側の起動経路のみが書き込む領域であり、セグメント作業ワーカーは書き込めない。作業ブランチ上の同名・同形式のファイルを保存先にしない。保存できない場合はレビュアを起動せず `approved` を導出しない。
- 要件8: 証跡照合時の判定プロンプト再生成は、attempt に対応する入力スナップショットの解決値と、target SHA・base SHA に固定された不変の内容（成果物本文）のみを入力とし、照合時点の可変入力を読み直さない。すなわち size シグナル・risk シグナル・ガードレール判定・必須成果物の読み取り・AC-ID 抽出・代替判定基準の展開のいずれについても、照合時点の解決結果ではなくスナップショットに記録した解決値を用いる。成果物本文はスナップショットに記録した digest と照合したうえで用い、digest が一致する本文を取得できない場合は再生成できないものとして扱う。再生成の入力として採用するのは trusted 保存領域に保存されたスナップショットのみとし、ワーカー供給経路（作業ブランチ上のファイル・PR 本文・Issue コメント）に置かれた同形式の記録は、trusted 保存領域のスナップショットを解決できる場合もできない場合も採用しない。
- 要件9: 代替判定基準の実体となる Issue 本文は、trusted base 側の起動経路が Coordination Backend の一次情報（GitHub モードは Issue 本文、ローカルモードは Issue 状態ファイル）から直接取得したものに限る。レビュアまたはセグメント作業ワーカーが供給した値（branch 上の成果物ファイル・PR 本文・Issue コメント・本文の写し）を**代替判定基準**の入力にしない。本要件が限定するのは代替判定基準の実体の取得経路のみである。判定対象区間へ展開する成果物、および要件5 に従って `SPEC.md` から抽出した AC-ID 集合を判定軸区間へ書き出すことは本要件の対象外であり、これらを**代替判定基準**の実体としては用いない。
- 要件10: 判定軸（AC-ID 集合または代替判定基準）または照合用の入力スナップショットを実体化・解決できない場合、`approved` を導出しない。
- 要件11: 判定プロンプトは、同一の Issue・ゲート・target SHA・base SHA・入力スナップショットに対して決定的である。
- 要件12: 上記が GitHub モードとローカルモードの双方で成立し、自動テストで検証される。

### 要件と受入条件の対応

| 要件 | 対応する受入条件 |
|---|---|
| 要件1 | AC-1・AC-2 |
| 要件2 | AC-3 |
| 要件3 | AC-4・AC-9・AC-10・AC-11・AC-23 |
| 要件4 | AC-5・AC-6 |
| 要件5 | AC-4・AC-7・AC-8・AC-19・AC-20・AC-25 |
| 要件6 | AC-10・AC-11・AC-12・AC-13・AC-26・AC-27 |
| 要件7 | AC-14・AC-15・AC-24 |
| 要件8 | AC-17・AC-24 |
| 要件9 | AC-22 |
| 要件10 | AC-6・AC-13・AC-15・AC-18・AC-21・AC-25・AC-27 |
| 要件11 | AC-16 |
| 要件12 | AC-28 |

### 受入条件（Acceptance Criteria）

#### AC-1: 3 条件がすべて満たされる場合にのみ quick 免除が成立する

- Given: size シグナルが `quick` に解決され、risk シグナルが `normal` に解決され、ガードレール判定が「含まない」である Issue（表O1 の行1）。
- When: quick 免除の成否を判定する。
- Then: quick 免除が成立すると判定される。GitHub モードのラベル由来のシグナルと、ローカルモードの `state.yaml` 由来のシグナルの双方で同じ結果になる。
- 検証方法見込み: `automated`

#### AC-2: 3 シグナルをすべて解決できるが条件を満たさない場合は免除不成立となる

- Given: size シグナル・risk シグナル・ガードレール判定のすべてを解決でき、かつ AC-1 の Given に一致しない Issue（表O1 の行2。size が `standard` に解決される場合、risk が `normal` 以外に解決される場合、ガードレール判定が「含む」である場合の 7 通りすべてを含む。size ラベル未付与により `standard` へ解決される場合、risk ラベル未付与により `unclassified` へ解決される場合を含む）。
- When: quick 免除の成否を判定する。
- Then: quick 免除は成立しないと判定される。シグナルが存在しない状態は解決不能として扱われず、既定値（size は `standard`、risk は `unclassified`）へ決定的に解決されたうえでこの結果になる。
- 検証方法見込み: `automated`

#### AC-3: いずれかを解決できない場合は既定値へ落とさず免除不成立とする

- Given: size シグナル・risk シグナル・ガードレール判定の少なくとも 1 つが解決不能または判定不能である Issue（表O1 の行3。取得元の読み取り失敗、値域外の値、同一プレフィックスのラベルが 2 つ以上付与された状態、base SHA を解決できない状態、差分取得の失敗を含む）。
- When: quick 免除の成否を判定する。
- Then: quick 免除は成立しないと判定される。解決できない size を `quick` とみなさず、解決できない risk を `normal` とみなさず、取得できない差分を「ガードレール対象パスを含まない」とみなさない。解決不能であること自体を理由に判定を中断せず、免除不成立として決定的に解決する。
- 検証方法見込み: `automated`

#### AC-4: quick 免除成立下でも必須成果物が欠けていなければ AC-ID 全件網羅を判定軸とする

- Given: quick 免除が成立し、target SHA から AC-ID を 1 件以上抽出でき、かつ対象ゲートの必須成果物に不在であることを確認できたものが 1 件も無い Issue（表O2 の行1。必須成果物がすべて存在し内容を取得できた場合と、1 件以上が読み取り不能である場合の双方を含み、代替判定基準を展開できるか否かを問わない）。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。必須成果物が読み取り不能である入力では、レビュー起動経路の中断を AC-27 が別途定めるため、本受入条件では判定プロンプト生成を直接呼び出して観測する。
- Then: 判定軸区間に、抽出された AC-ID の全件網羅を conformance の判定軸とする指示が出力される。代替判定基準への切り替えは起きず、inconclusive 指示も含まれない。必須成果物が読み取り不能である場合も、読み取りの失敗を理由に代替判定基準へ切り替えない。本受入条件は判定軸区間のみを観測し、判定対象区間の内容は観測しない。
- 検証方法見込み: `automated`

#### AC-5: quick 免除成立下で AC-ID 全件網羅を立証できない場合は代替判定基準を判定軸とする

- Given: quick 免除が成立し、かつ代替判定基準を展開でき、かつ次のいずれかに該当する Issue（表O2 の行2・行4）。(i) target SHA からの AC-ID 抽出に成功したうえで結果が 0 件である（`SPEC.md` が存在しないことを確認できた場合を含み、抽出不能である場合を含まない。対象ゲートの必須成果物の充足状態を問わない）。(ii) AC-ID を 1 件以上抽出できるが、対象ゲートの必須成果物のうち 1 件以上が不在であることを確認できた（design ゲートで `SPEC.md` に AC-ID がありながら `DESIGN.md`・`PLAN.md` が免除により不在である場合、validation ゲートで `VALIDATION.md` が免除により不在である場合を含む）。
- When: 対象ゲートの判定プロンプトを生成する。(i) は spec・design・implementation・validation の 4 ゲートすべてを対象に含める。(ii) は design・validation の 2 ゲートを対象に含める（implementation ゲートは必須成果物を持たず、spec ゲートは `SPEC.md` の不在が (i) を意味するため、いずれも (ii) に該当する入力が存在しない）。(i) のうち必須成果物が読み取り不能である入力では、レビュー起動経路の中断を AC-27 が別途定めるため、判定プロンプト生成を直接呼び出して観測する。
- Then: 生成が成功し、判定軸区間に inconclusive 指示が含まれない。判定軸区間には、AC-ID 全件網羅を判定軸としない旨（(i) では AC-ID が quick 免除により正当に存在しない旨、(ii) では網羅を立証すべき必須成果物が quick 免除により正当に不在である旨）と、代替判定基準の実体（Issue 本文由来の要求・期待する挙動・受入基準の記述）が conformance の判定軸として出力される。(ii) では、抽出された AC-ID の全件網羅を conformance の判定軸とする指示が出力されず、免除により正当に不在の成果物に係る立証の欠落のみを理由に conformance が fail となる判定軸が出力されない。対象ゲートの別によらず同一の実体が展開される。本受入条件は判定軸区間のみを観測する。
- 検証方法見込み: `automated`

#### AC-6: quick 免除成立下で判定軸を実体化できない場合は中断せず inconclusive 指示を出す

- Given: quick 免除が成立し、代替判定基準を展開できず（Issue 本文を取得できない、または取得できても要求・期待する挙動・受入基準に相当する記述が 0 件である）、かつ AC-5 の Given の (i) または (ii) に該当する Issue（表O2 の行3・行5。いずれも AC-ID 抽出が不能である場合を含まない）。
- When: 対象ゲートの判定プロンプトを生成する。対象ゲートの範囲と、必須成果物が読み取り不能である入力で判定プロンプト生成を直接呼び出す点は AC-5 の When と同一とする。
- Then: 生成は例外で中断せず成功し、判定軸区間に inconclusive 指示が出力される。代替判定基準は展開されず、AC-ID 全件網羅も判定軸として出力されず、conformance が pass へ到達しうる判定軸は判定軸区間に含まれない。本受入条件は判定軸区間のみを観測する。
- 検証方法見込み: `automated`

#### AC-7: 免除不成立かつ AC-ID を抽出できる既存主経路の挙動を維持する

- Given: quick 免除が成立せず、target SHA から AC-ID を 1 件以上抽出できる Issue（表O2 の行6。size が `standard` に解決される場合、risk が `normal` 以外である場合、ガードレール対象パスを含む場合、いずれかのシグナルが解決不能な場合をすべて含み、対象ゲートの必須成果物の充足状態と代替判定基準の展開可否を問わない）。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。必須成果物が 1 件以上不在または読み取り不能である入力では、レビュー起動経路の中断を AC-13・AC-27 が別途定めるため、判定プロンプト生成を直接呼び出して観測する。
- Then: 判定軸区間に、抽出された AC-ID の全件網羅を conformance の判定軸とする指示が出力される。代替判定基準は展開されず、inconclusive 指示も含まれない。必須成果物が 1 件以上不在である場合も代替判定基準へ切り替えない（切替は quick 免除が成立する場合に限る）。本変更の適用前後で、この入力に対する判定軸区間の内容が変化しない。本受入条件は判定軸区間のみを観測する。
- 検証方法見込み: `automated`

#### AC-8: 免除不成立かつ AC-ID 0 件では従来どおり inconclusive 指示を出す

- Given: quick 免除が成立せず、target SHA からの AC-ID 抽出に成功したうえで結果が 0 件である Issue（抽出不能である場合を含まない。表O2 の行7。対象ゲートの必須成果物の充足状態と代替判定基準の展開可否を問わない）。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。必須成果物が 1 件以上不在または読み取り不能である入力では、レビュー起動経路の中断を AC-13・AC-27 が別途定めるため、判定プロンプト生成を直接呼び出して観測する。
- Then: 判定軸区間に従来どおり inconclusive 指示が出力される。代替判定基準は、展開できる場合であっても展開されない。本受入条件は判定軸区間のみを観測する。
- 検証方法見込み: `automated`

#### AC-9: 必須成果物がそろっている場合は従来どおり内容が判定対象区間へ展開される

- Given: target SHA に対象ゲートの必須成果物がすべて存在し、その内容を取得できた Issue（表O3 の行1・行2。quick 免除の成否を問わない）。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。
- Then: 判定対象区間に当該必須成果物の内容が展開され、不在を示す記述は含まれない。必須成果物が定義されていないゲートでは、不在を示す記述が含まれないことのみを要求する。本受入条件は判定対象区間のみを観測し、判定軸区間の内容は観測しない。
- 検証方法見込み: `automated`

#### AC-10: quick 免除下の必須成果物欠落は免除による不在として明示される

- Given: quick 免除が成立し、target SHA に対象ゲートの必須成果物が 1 件以上存在しないことを確認でき、読み取り不能な必須成果物が 1 件も無い Issue（表O3 の行3。spec は `SPEC.md`、design は `DESIGN.md` と `PLAN.md`、validation は `VALIDATION.md`）。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・validation の 3 ゲートすべてを対象に含める（implementation ゲートは必須成果物を持たないため本 Given に該当する入力が存在しない）。
- Then: 判定対象区間に、欠落した各必須成果物の名称と、それが quick 免除により正当に不在である旨が明示される。判定対象区間が無言の空欄にならない。本受入条件は判定対象区間のみを観測し、判定軸区間の内容は観測しない。
- 検証方法見込み: `automated`

#### AC-11: 免除不成立下の必須成果物欠落は異常として明示される

- Given: quick 免除が成立せず、target SHA に対象ゲートの必須成果物が 1 件以上存在しないことを確認でき、読み取り不能な必須成果物が 1 件も無い Issue（表O3 の行4）。
- When: 対象ゲートの判定プロンプトを単独で生成する（レビュー起動経路の中断は AC-13 が定めるため、本受入条件では判定プロンプト生成を直接呼び出す）。spec・design・validation の 3 ゲートすべてを対象に含める（implementation ゲートは必須成果物を持たないため本 Given に該当する入力が存在しない）。
- Then: 判定対象区間に、欠落した各必須成果物の名称と、それが本来存在すべきであるのに欠落している旨が明示される。quick 免除による正当な不在として提示されない。本受入条件は判定対象区間のみを観測する。
- 検証方法見込み: `automated`

#### AC-12: 必須成果物がそろっているか quick 免除が成立し、かつ保存できる場合はレビュアが起動される

- Given: 読み取り不能な必須成果物が 1 件も無く、入力スナップショットを trusted 保存領域へ保存でき、かつ target SHA に対象ゲートの必須成果物がすべて存在してその内容を取得できた Issue、または 読み取り不能な必須成果物が 1 件も無く、入力スナップショットを保存でき、かつ quick 免除が成立する Issue（表O4 の行1〜行3）。
- When: 当該ゲートのレビューを起動する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。
- Then: 「target SHAの必須成果物を読めません」に相当する中断が発生せず、レビュアが起動され、conformance と falsification の判定値を伴う gate-report が生成される。本受入条件は起動経路の中断有無のみを観測し、判定プロンプトの内容は観測しない。
- 検証方法見込み: `automated`

#### AC-13: 免除不成立時の必須成果物欠落は従来どおり中断する

- Given: quick 免除が成立せず、target SHA に対象ゲートの必須成果物が 1 件以上存在しないことを確認でき、読み取り不能な必須成果物が 1 件も無い Issue（表O4 の行4・行5。入力スナップショットの保存可否を問わない）。
- When: 当該ゲートのレビューを起動する。spec・design・validation の 3 ゲートすべてを対象に含める（implementation ゲートは必須成果物を持たないため本 Given に該当する入力が存在しない）。
- Then: 必須成果物の読み取り時点で従来どおり中断してレビュアを起動せず、`approved` を導出しない。中断はスナップショット保存より前に起きるため、保存可否は結果を変えない。本受入条件は起動経路の中断有無のみを観測する。
- 検証方法見込み: `automated`

#### AC-14: 入力スナップショットがレビュア起動前に確定・保存される

- Given: 表O4 の行1〜行3 に該当する入力（読み取り不能な必須成果物が 1 件も無く、必須成果物がすべて存在して内容を取得できたか quick 免除が成立し、かつ入力スナップショットを trusted 保存領域へ保存できる）。
- When: 当該 attempt のレビューを起動する。
- Then: 判定プロンプト生成時点で解決した可変入力の値の組が入力スナップショットとして確定し、attempt と 1 対 1 で対応付けて trusted 保存領域へ保存される。保存される値は次のすべてを含む。(a) size シグナルの解決結果。(b) risk シグナルの解決結果。(c) ガードレール判定の結果。(d) 対象ゲートの必須成果物ごとの読み取り結果（内容を取得できたか、不在であることを確認できたか、読み取り不能であったかの別。内容を取得できた成果物についてはその本文の digest）。(e) AC-ID 抽出結果（抽出できた場合は抽出された AC-ID の集合、そうでない場合は 0 件であることまたは抽出不能であることの別）。(f) 代替判定基準の展開に用いた Issue 本文の記述とその digest。展開できなかった場合は、展開できなかったという事実そのものを (f) の値として保存する。保存先は、GitHub モードは trusted base 側のゲート状態記録、ローカルモードは default branch 上の `reviews/<gate>.yaml` であり、作業ブランチ上の同名ファイルへは保存されない。保存はレビュア起動前に完了し、保存後に当該 attempt のスナップショットを更新・上書きしない。本受入条件は保存という副作用のみを観測し、起動可否と判定プロンプトの内容は観測しない。
- 検証方法見込み: `automated`

#### AC-15: 入力スナップショットを保存できない場合はレビュアを起動しない

- Given: 必須成果物の読み取りは中断せず（読み取り不能な必須成果物が 1 件も無く、必須成果物がすべて存在して内容を取得できたか、quick 免除が成立する）、かつ入力スナップショットを trusted 保存領域へ保存できない入力（表O4 の行6〜行8、表O5 の行2）。
- When: 当該 attempt のレビューを起動する。
- Then: レビュアを起動せず、`approved` を導出せずに中断するか `human_required` へ倒れる。スナップショットは trusted 保存領域に残らず、対応する attempt も成立しない。
- 検証方法見込み: `automated`

#### AC-16: 同一の入力スナップショットに対する判定プロンプト生成が決定的である

- Given: 同一の Issue・ゲート・target SHA・base SHA・入力スナップショット（表O6 の行1。quick 免除の成否と表O2 のいずれの行であるかを問わない）。
- When: 判定プロンプトを複数回生成する。
- Then: 出力が完全に一致し、prompt digest が一致する。入力スナップショットが必須成果物の読み取り結果と AC-ID 抽出結果の解決値を含むため、判定軸区間の値 S と判定対象区間の内容は Given の 5 項目から一意に定まり、生成の各回で必須成果物の読み取りや AC-ID 抽出が成功するか失敗するかによって出力が変化しない。quick 経路の追加によって既存の決定性が損なわれない。
- 検証方法見込み: `automated`

#### AC-17: 可変入力が変化しても記録済み attempt の証跡照合が成立する

- Given: 記録済み attempt に対応する入力スナップショットを解決でき、保存済みスナップショットから判定プロンプトを再生成でき、その digest が記録済み prompt digest と一致する（表O7 の行1）。加えて attempt の記録後に可変入力のいずれかが変化している（target SHA と base SHA は不変）。変化する可変入力には、Issue 本文・size シグナル・risk シグナル・ガードレール判定の結果に加え、必須成果物の読み取り結果と AC-ID 抽出結果を含める。とくに、原 attempt では AC-ID 抽出が不能であったが照合時点では 1 件以上抽出できる場合、原 attempt では AC-ID を 1 件以上抽出できたが照合時点では抽出が不能である場合、原 attempt では必須成果物の内容を取得できたが照合時点では読み取り不能である場合の 3 通りを含める。
- When: 当該 attempt の証跡を照合する。
- Then: 照合が成立する。照合器は attempt に対応付いた入力スナップショットの解決値と、target SHA・base SHA に固定された不変の内容（スナップショットに記録した digest と一致する成果物本文）のみを入力として判定プロンプトを再生成し、照合時点の可変入力を読み直さない。判定軸区間の値 S と判定対象区間の内容はスナップショットに記録した必須成果物の読み取り結果・AC-ID 抽出結果・代替判定基準の展開可否から決まるため、これらの照合時点での解決結果が原 attempt と異なっても再生成される判定プロンプトは変化しない。すなわち可変入力の変化は照合結果を変えない。
- 検証方法見込み: `automated`

#### AC-18: 照合側でスナップショットを解決できない、または digest が一致しない場合は approved を導出しない

- Given: 記録済み attempt について、対応する入力スナップショットを解決できないか、解決できても判定プロンプトを再生成できない（スナップショットが保存されていない、読み取れない、attempt との対応付けが一意でない、またはスナップショットに記録した digest と一致する成果物本文を target SHA から取得できない）、または解決でき再生成できたが判定プロンプトの digest が記録済み prompt digest と一致しない（表O7 の行2・行3）。
- When: 当該 attempt の証跡を照合する。
- Then: `approved` を導出せず、照合を失敗させるか `human_required` へ倒れる。照合時点の可変入力を読み直して一致を取り繕う経路は存在しない。本受入条件は照合結果のみを観測する。
- 検証方法見込み: `automated`

#### AC-19: 判定軸が実体化されている場合に approved へ到達できる

- Given: 判定軸区間の値が「AC-ID 全件網羅」または「代替判定基準」である attempt（表O8 の行1・行2。AC-14・AC-15 により、verdict が返る attempt は入力スナップショットが保存済みであることが含意されるため、これを追加の次元とはしない）。
- When: レビュアが conformance と falsification の双方を pass、blocking finding 無しと判定した verdict を返す。
- Then: final が `approved` として導出され、判定プロンプト由来の理由で conformance が pending に固定されることがない。代替判定基準を判定軸とする場合も同様に `approved` へ到達する。
- 検証方法見込み: `automated`

#### AC-20: 判定軸が実体化されていても verdict が pass でなければ従来どおり導出される

- Given: 判定軸区間の値が「AC-ID 全件網羅」または「代替判定基準」である attempt（表O8 の行3〜行6）。
- When: レビュアが次の 3 種の verdict を返す。(i) いずれかの観点が `fail` であるか blocking finding があり、かつ両観点とも `pending` ではない。(ii) いずれかの観点が `fail` であるか blocking finding があり、かつ他方の観点が `pending` である。(iii) いずれの観点も `fail` ではなく blocking finding も無く、いずれかの観点が `pending` である。
- Then: (i) と (ii) では final が `rejected`、(iii) では `human_required` として導出される。すなわち `fail`・blocking finding と `pending` が同時に成立する verdict では `fail`・blocking finding を優先し、`human_required` ではなく `rejected` が一意に導出される。final の導出規則は本変更の適用前後で変化しない。
- 検証方法見込み: `automated`

#### AC-21: 判定軸が inconclusive 指示である attempt から approved は導出されない

- Given: 判定軸区間の値が「inconclusive 指示」である attempt（表O2 の行3・行5・行7・行8 が生成する判定プロンプト。表O8 の行7・行8）。
- When: レビュアが判定プロンプトの指示に従い conformance を `pending` として返す。falsification が `fail` であるか blocking finding がある場合と、いずれの観点も `fail` ではなく blocking finding も無い場合の双方を含める。
- Then: 前者では final が `rejected`、後者では `human_required` として導出され、いずれの場合も `approved` は導出されない。代替判定基準の展開不能・AC-ID 抽出結果 0 件・AC-ID 抽出不能という判定不能状態から `approved` へ到達する経路が存在しない。
- 検証方法見込み: `automated`

#### AC-22: 代替判定基準の実体は trusted base 側が取得した Issue 本文のみに由来する

- Given: 判定軸区間の値 S が「代替判定基準」となる入力（表O9 の行1・行2）。ワーカー供給経路（branch 上の成果物ファイル・PR 本文・Issue コメント・ワーカーが作成した Issue 本文の写し）に、trusted base 側が取得する Issue 本文とは異なる「要求・期待する挙動・受入基準」相当の記述（受入基準を緩める記述を含む）が置かれている状態と、置かれていない状態の双方を含む。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。
- Then: 判定軸区間へ書き出される代替判定基準の実体は、trusted base 側の起動経路が Coordination Backend の一次情報（GitHub モードは Issue 本文、ローカルモードは Issue 状態ファイル）から取得した Issue 本文のみに由来する。ワーカー供給経路にだけ置いた記述は判定軸区間に現れず、ワーカー供給経路に当該記述が有る場合と無い場合とで判定軸区間へ書き出される実体が一致する。本受入条件は判定軸区間へ書き出された実体の取得経路のみを観測し、S の値そのものと判定対象区間の内容は観測しない。
- 検証方法見込み: `automated`

#### AC-23: 判定対象区間と判定軸区間が判定プロンプト内で重ならない

- Given: 判定プロンプトを生成する任意の入力（表O10 の行1。quick 免除の成否、必須成果物の充足、AC-ID 抽出結果、代替判定基準の展開可否のいずれの組み合わせも含む）。判定対象区間の入力となる成果物内容のうち AC-ID 以外の箇所と、代替判定基準の入力となる Issue 本文に、それぞれ相異なる識別可能な標識文字列を与える。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。
- Then: 生成された判定プロンプトから判定対象区間と判定軸区間の開始位置と終了位置が一意に識別でき、両区間が占める位置の集合は互いに素である（いずれの区間にも属さない位置が両者の間にあってよいが、両区間が重なる位置は存在しない）。成果物内容へ与えた標識は判定軸区間に現れない。代替判定基準を展開する入力では、Issue 本文へ与えた標識が判定対象区間に現れない。本受入条件は両区間の境界と位置の非重複のみを観測し、各区間の内容が何であるかは観測しない。
- 検証方法見込み: `automated`

#### AC-24: 照合入力として採用されるスナップショットは trusted 保存領域のみに由来する

- Given: 記録済み attempt の証跡を照合する入力（表O11 の行1〜行4）。trusted 保存領域のスナップショットを解決でき再生成できる状態と、解決できないか再生成できない状態の双方を含み、かつワーカー供給経路（作業ブランチ上のファイル・PR 本文・Issue コメント）に、trusted 保存領域の値とは異なる可変入力の解決値（受入基準を緩める記述を含む Issue 本文の記述を伴う組）と、それに整合する prompt digest を持つスナップショット相当の記録が置かれている状態と、置かれていない状態の双方を含める。
- When: 当該 attempt の証跡を照合する。GitHub モードのゲート状態記録由来のスナップショットと、ローカルモードの default branch 上の `reviews/<gate>.yaml` 由来のスナップショットの双方を対象に含める。
- Then: 照合器が判定プロンプト再生成の入力として採用するのは trusted 保存領域に保存されたスナップショットのみである。ワーカー供給経路に置いた記録は、trusted 保存領域のスナップショットを解決できる場合に採用されず、解決できない場合にも代替の入力として採用されない。ワーカー供給経路に当該記録が有る場合と無い場合とで、再生成に用いられた入力の値が一致する。本受入条件は再生成入力として採用された値の取得経路のみを観測し、照合の成否そのものと final の導出値は観測しない。
- 検証方法見込み: `automated`

#### AC-25: AC-ID 抽出が不能な場合は代替判定基準へ切り替えず inconclusive 指示を出す

- Given: target SHA の `SPEC.md` が読み取り不能であるために AC-ID の件数を確定できない Issue（表O2 の行8。quick 免除の成否、対象ゲートの必須成果物の充足状態、代替判定基準の展開可否のいずれの組み合わせも含み、quick 免除が成立し代替判定基準を展開できる入力を必ず含める）。
- When: 対象ゲートの判定プロンプトを生成する。spec・design・implementation・validation の 4 ゲートすべてを対象に含める。spec ゲートでは `SPEC.md` が必須成果物であり、本 Given の入力では AC-27 によりレビュー起動経路が中断するため、本受入条件では判定プロンプト生成を直接呼び出して観測する。
- Then: 判定軸区間に inconclusive 指示が出力される。抽出不能が「抽出に成功し 0 件」と同一視されず、quick 免除が成立し代替判定基準を展開できる入力であっても代替判定基準は展開されない。AC-ID 全件網羅も判定軸として出力されない。本受入条件は判定軸区間のみを観測する。
- 検証方法見込み: `automated`

#### AC-26: 読み取り不能な必須成果物は不在としても欠落としても提示されない

- Given: target SHA において対象ゲートの必須成果物が 1 件以上読み取り不能である Issue（存在の有無を確定できない、または存在するが内容の取得・復号・解析に失敗した。表O3 の行5・行6。quick 免除の成否を問わない）。
- When: 対象ゲートの判定プロンプトを単独で生成する（レビュー起動経路の中断は AC-27 が定めるため、本受入条件では判定プロンプト生成を直接呼び出す）。spec・design・validation の 3 ゲートすべてを対象に含める（implementation ゲートは必須成果物を持たないため本 Given に該当する入力が存在しない）。
- Then: 判定対象区間に、読み取り不能な各必須成果物の名称と、その内容を取得できなかった旨が明示される。quick 免除により正当に不在であるとも、本来存在すべき成果物が欠落しているとも提示されない。quick 免除が成立する入力と成立しない入力とで、この提示内容が一致する。本受入条件は判定対象区間のみを観測する。
- 検証方法見込み: `automated`

#### AC-27: 必須成果物が読み取り不能な場合はレビュアを起動しない

- Given: target SHA において対象ゲートの必須成果物が 1 件以上読み取り不能である Issue（表O4 の行9〜行12。quick 免除の成否と入力スナップショットの保存可否を問わず、quick 免除が成立する入力を必ず含める）。
- When: 当該ゲートのレビューを起動する。spec・design・validation の 3 ゲートすべてを対象に含める（implementation ゲートは必須成果物を持たないため本 Given に該当する入力が存在しない）。
- Then: 読み取り不能を検知した時点で中断してレビュアを起動せず、`approved` を導出しない。quick 免除が成立していても中断し、読み取り不能を不在として扱って起動を継続しない。中断はスナップショット保存より前に起きるため、保存可否は結果を変えない。本受入条件は起動経路の中断有無のみを観測する。
- 検証方法見込み: `automated`

#### AC-28: 全 AC に対応する自動テストが両モードを対象に存在する

- Given: 本 Issue の変更が適用されたリポジトリ。
- When: `npm test` を実行する。
- Then: AC-1 から AC-27 のそれぞれに対応する自動テストが実行されすべて成功する。このうち size シグナル・risk シグナルの解決を含む AC については、GitHub モード由来のシグナル（Issue ラベル）とローカルモード由来のシグナル（`state.yaml`）の双方を入力とするケースを含む。本 AC は他 AC のテスト集合の存在を要求するメタ受入条件であり、自己言及を避けるため列挙対象に自身を含めない。
- 検証方法見込み: `automated`

## 完了条件

- AC-1 から AC-28 のすべてが満たされ、対応する自動テストが成功する（AC-28 は AC-1 から AC-27 のテスト集合が存在し成功することをもって満たされる）。
- 要件1 から要件12 のそれぞれが、前掲の対応表のとおり少なくとも 1 つの受入条件と対応している。
- 表O1 から表O11 のそれぞれについて、各行がちょうど 1 つの受入条件に対応し、除外行には除外理由が記載されている。
- 既存のゲート判定・証跡照合の自動テストが回帰しない。とくに表O2 の行6（免除不成立かつ AC-ID を 1 件以上抽出できる既存主経路）の挙動が本変更の前後で変化しない。
- quick 免除が成立し、`SPEC.md` から AC-ID を 1 件以上抽出でき、かつ対象ゲートの必須成果物が免除により不在である入力（表O2 の行2）で、AC-ID 全件網羅を判定軸とする指示が出力されず、`approved` へ到達しうる判定軸（代替判定基準）が判定軸区間に実体として展開される。
- 常時必須の検証（lint / format、型検査、単体テスト、変更範囲の結合テスト、SAST、依存関係・secret スキャン）が成功する。本変更は利用者操作画面・API 境界・認証認可・性能ホットパス・DB migration・デプロイ運用・外部連携のいずれにも該当しないため、それらに紐づく追加検証は適用しない。

## 未決事項

- 判定軸そのものの改変（前提・制約に記載）をどう扱うかは設計セグメントで確定する。確定すべき事項は次の 3 点である。(a) `approved` へ到達した attempt の後に Issue 本文が変化した場合を、下流ゲートの無効化事由とするか。(b) quick 免除下でセグメント作業ワーカーが Issue 本文を編集することを禁止するか、編集を検知した場合に `human_required` へ倒すか。(c) 改変の検知を入力スナップショット内の digest 比較のみで行うか、Issue 本文の改訂履歴を併用するか。本仕様では、Issue 本文の取得元を trusted base 側の起動経路に限ること（要件9、AC-22 が検証する）、および入力スナップショットへ Issue 本文の digest を含めること（要件7、AC-14 が検証する）のみを確定済みとする。
- 代替判定基準として Issue 本文以外の入力（実装差分・テスト実行結果・CI 結果）をどこまで判定軸区間へ展開するかは設計セグメントで確定する。本仕様では、代替判定基準の実体を Issue 本文由来の記述に限り、ゲートごとに情報源を切り替えないことを確定済みとする。設計セグメントで入力を追加する場合も、全ゲートで同一の入力集合とし、ゲート別の分岐を導入しないこと。
- Issue 本文から「要求・期待する挙動・受入基準に相当する記述」を取り出す具体的な抽出規則（見出し一致・全文採用等）は設計セグメントで確定する。抽出結果が 0 件のとき展開不能として扱うこと（AC-6）は本仕様で確定済みとする。
- 判定プロンプト内で判定対象区間と判定軸区間を区切る具体的な表現（見出し文字列・区切りマーカーの形式）は設計セグメントで確定する。区切りが一意に識別でき、両区間が互いに重ならないこと（要件3）は本仕様で確定済みであり、AC-23 が区切りの具体形に依存しない形で検証する。
- 入力スナップショットの具体的なフィールド構造・スキーマ版・attempt との対応付けの表現方法、および trusted 保存領域への書き込みをワーカーに対して機構的に禁止する手段（ファイル権限・branch protection・書き込み経路の分離のいずれを用いるか）は設計セグメントで確定する。保存先が trusted 保存領域であること（GitHub モードは trusted base 側のゲート状態記録、ローカルモードは default branch 上の `reviews/<gate>.yaml`）、保存タイミング（レビュア起動前）、保存できない場合の挙動、照合が trusted 保存領域の保存済みスナップショットのみを入力とすること（要件8、AC-24 が検証する）は本仕様で確定済みとする。
- 本変更の適用前に記録され入力スナップショットを持たない attempt について、移行期に一括で再判定を促す運用手順は進行役の判断で確定する。当該 attempt の照合結果自体は AC-18 により `human_required` へ倒れることが本仕様で確定済みである。
- Issue #729（反証ルーブリック）との着手・マージ順序の具体的な調整方法は進行役の判断で確定する。

## スコープ外

- quick 免除の対象そのものの見直し（憲法である `AGENTS.md` の改定）。
- レビュアが判定プロンプトの明示指示に反する verdict を返した場合の検知・強制（表O8 の除外行）。
- falsification 側のルーブリックの停止条件・severity 基準・過去ラウンド反映（Issue #729）。
- 変更規模に応じた反証強度の調節（Issue #698）。
- strict attempt の実行独立性とレビュー履歴 pagination（Issue #732）。
- GitHub モードにおける I2 の自動 CI 強制の再導入。
