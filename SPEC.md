# SPEC: env トークン非設定の資格情報ストア限定環境でゲートレビュアが認証できない

- Issue: `ISSUE-758`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/758-keychain-auth-config-dir`

## 目的・背景

claude アダプタのゲートレビュアは、呼び出し元の資格情報を持ち込まない隔離サブプロセスとして起動する。
このとき Claude Code CLI が認証を成立させられる「認証情報の所在」は次の3分類に限られる。

- 分類A: 環境変数（`ANTHROPIC_API_KEY` / `CLAUDE_CODE_OAUTH_TOKEN`）
- 分類B: 設定ディレクトリ配下の通常ファイル（`.credentials.json`）
- 分類C: 外部資格情報ストア限定（macOS Keychain 等。設定ディレクトリ配下に通常ファイルとして存在しない）

分類Cの利用者（macOS で対話ログインし、環境変数トークンを一切設定していない構成）は、全ゲートのゲートレビューが常に
`human_required` へ倒れ、ゲートレビュー自動化が機能しない。

### 過去修正が担保した範囲と、担保しなかった範囲

| 修正 | 担保した分類 | 担保しなかった分類 |
|---|---|---|
| Issue #562（トークン引き継ぎ） | A | B・C |
| Issue #691（認証ファイルのみ複製・隔離環境での事前確認・watchdog） | A・B | C（原因を推測どまりで確定していない） |
| 本 Issue | A・B・C | 分類の追加（対象外を参照） |

同じ不具合を3度目に繰り返した原因は、修正のたびに「CI で表現できた構成」だけを回帰テストにし、分類Cを回帰テストの
対象集合へ入れなかったことである。本 SPEC は分類Cの成立自体に加え、3分類すべてを回帰テストの対象とすることを要求する。

### 実地確認した現行実装の事実

以下はいずれも `.agent-skill-chain/adapters/claude.sh` の原文引用である。隔離サブプロセスへ渡す環境の基底集合には
`USER` も `LOGNAME` も含まれない（同ファイル全体を `USER` および `LOGNAME` で検索して該当行なし）。

```bash
  local -a clean_env=(
    /usr/bin/env -i
    "PATH=$sanitized_path"
    "HOME=$isolated_root/home"
    "XDG_CONFIG_HOME=$isolated_root/xdg"
    "GH_CONFIG_DIR=$isolated_root/xdg/gh"
    "GIT_CONFIG_GLOBAL=/dev/null"
    "GIT_CONFIG_SYSTEM=/dev/null"
    "GIT_TERMINAL_PROMPT=0"
    "TMPDIR=/tmp"
    "LANG=${LANG:-C.UTF-8}"
    "LC_ALL=${LC_ALL:-}"
  )
```

claude 経路では `CLAUDE_CONFIG_DIR` を隔離領域内の staging 先へ無条件に向ける。認証ファイルを複製できたかは問わない。

```bash
    clean_env+=("CLAUDE_CONFIG_DIR=$staged_claude_config")
```

分類Aの環境変数トークンは非空の場合に限り隔離環境へ引き継がれる。分類Cではいずれも非空でないため追加されない。

```bash
    [[ -n "${ANTHROPIC_API_KEY:-}" ]] && clean_env+=("ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY")
    [[ -n "${CLAUDE_CODE_OAUTH_TOKEN:-}" ]] && clean_env+=("CLAUDE_CODE_OAUTH_TOKEN=$CLAUDE_CODE_OAUTH_TOKEN")
```

隔離領域と staging 先は 0700、複製した認証ファイルは 0600 で作られる。複製は通常ファイルが実在する場合に限られ、
分類Cでは複製が起きず staging 先は空のまま渡る。

```bash
  /bin/chmod 700 "$isolated_root" "$isolated_root/home" "$isolated_root/workspace" \
    "$isolated_root/xdg" "$isolated_root/auth" "$isolated_root/auth/claude" "$isolated_root/auth/codex"
```

```bash
  if [[ "$review_adapter" == "claude" && -n "$claude_config" && -f "$claude_config/.credentials.json" && ! -L "$claude_config/.credentials.json" ]]; then
    /bin/cp "$claude_config/.credentials.json" "$staged_claude_config/.credentials.json"
    /bin/chmod 600 "$staged_claude_config/.credentials.json"
  fi
```

隔離領域は正常終了・watchdog 起動失敗・timeout のいずれの復帰経路でも削除される。

```bash
  /bin/rm -rf -- "$isolated_root"
```

認証不成立時の診断は標準エラーへ出力され、`.agent-skill-chain/scripts/gate-launch-reviewer.sh` は
`launch_gate_reviewer` の標準エラーをリダイレクトせずに呼び出すため、診断は呼び出し元まで到達する。

```bash
  _fail_safe() {
    echo "launch_gate_reviewer: $1（フェイルセーフで human_required へ倒します）" >&2
```

```bash
launch_gate_reviewer "$ISSUE_ID" "$GATE_ID" "$PROFILE" "$REPORT_PATH" "$TARGET_SHA"
```

現行の診断は分類Cを「利用できない」と断定し、環境変数の設定を促す。

```bash
    printf '%s\n' "隔離環境へ持ち込める認証情報がありません。macOS Keychainなどcaller HOMEに紐づく資格情報ストアは利用できません。"
```

分類Bが隔離領域内の staging 先で成立することは既存の自動テストが固定しており、呼び出し元の `settings.json`
（hooks を含む）が staging 先へ持ち込まれないことも同時に固定されている（`test/integration/gate-adapters.test.ts` 原文引用）。

```javascript
    'grep -q staged-credential "${CLAUDE_CONFIG_DIR}/.credentials.json"',
    'test ! -e "${CLAUDE_CONFIG_DIR}/settings.json"',
```

隔離環境の認証確認はレビュア本体と同じ隔離サブプロセス内で実行され、その実行系はレビュア実行系の上書き
（`GATE_REVIEWER_CMD`）とは別個の環境変数 `CLAUDE_AUTH_PROBE_CMD` で上書きできる。同変数は実装上テストの
モック境界として位置づけられている（以下2件は `.agent-skill-chain/adapters/claude.sh` の原文引用）。

```bash
_claude_reviewer_auth_ok() {
  local probe="${CLAUDE_AUTH_PROBE_CMD:-}"
```

```bash
#     行い、終了コード0を authed とみなす。CLAUDE_AUTH_PROBE_CMD で完全上書き可能（テストのモック境界。
#     WORKER_CMD/GATE_REVIEWER_CMDと同型）。claude 不在かつ CLAUDE_AUTH_PROBE_CMD 未指定なら
#     真の認証欠如として1を返す。プローブの出力（`auth status --json`はアカウント情報を含みうる）は
#     stdout/stderrとも非ログ（呼び出し元で2>/dev/null等により捨てる）。
```

上書き値が隔離サブプロセスへ到達する手段は環境変数ではない。上書き値は呼び出し元プロセスで解決され、隔離サブプロセスで
実行するコマンド文字列として実行系へ渡される（以下2件は `.agent-skill-chain/adapters/claude.sh` の原文引用）。

```bash
  _run_reviewer_sanitized "" "$probe" "$t" >/dev/null 2>&1
```

実行系は上記の基底集合を `env -i` で与えたうえで、その文字列を子プロセスの引数として実行する。したがって上書きの有無は
子プロセスの環境変数名の集合を変えない。レビュア実行系の上書き（`GATE_REVIEWER_CMD`）も同じ形をとる。

```bash
      exec "${clean_env[@]}" /bin/bash -c "$reviewer_cmd" <"$prompt_file" >"$output_file" 2>/dev/null
```

分類Cの認証情報が呼び出し元で機械的に取得可能であることは Issue #758 本文の実測が示す（同 Issue 原文引用）。

```text
$ security find-generic-password -s "Claude Code-credentials" -w
{"claudeAiOauth":{"accessToken":"sk-ant-...
```

本 SPEC は、この取得結果が分類Bの認証ファイルと同一形式のテキスト（先頭が `{"claudeAiOauth":{"accessToken":`
である JSON）であり、変換なしに隔離設定ディレクトリの認証ファイルとして配置すれば認証が成立することを前提とする。
上記実測は先頭部分のみの記録であり、全体構造・末尾の余分な出力の有無は確認できていない（未決事項に記載）。

### 是正方針と、採らない方針

本 SPEC は「呼び出し元の `HOME` や `USER` をゲートレビュア環境へ渡す」方針を**採らない**。渡した場合、CLI の設定探索先が
呼び出し元の実設定ディレクトリとなり、呼び出し元由来の設定・hooks・MCP サーバ定義・権限設定がゲートレビュア子プロセスへ
適用され得る。これはレビュアの独立性と read-only 性を損ない、隔離領域の構成方式そのものの変更にあたる。

採る方針は「**認証に必要な資格情報だけを、隔離領域内の制御された設定ディレクトリへ配置する**」である。上記引用のとおり、
分類Bは同じ経路（隔離領域内 staging 先＋`CLAUDE_CONFIG_DIR`）で既に成立しており、呼び出し元設定の持ち込みが起きないことも
既存テストで固定済みである。分類Cは資格情報の取得元が資格情報ストアである点だけが分類Bと異なる。基底環境集合は変更しない。

Issue #758 は実測表から「`CLAUDE_CONFIG_DIR` の設定そのものが Keychain 参照を妨げる」と結論し、是正案として
「配下に認証情報ファイルが存在する場合にのみ設定する」を挙げている。本 SPEC はこの是正案を採らない。実測で
`CLAUDE_CONFIG_DIR` を与えた対象は、同 Issue が同時に記録したとおり認証ファイルを含まないディレクトリだからである
（同 Issue 原文引用）。

```text
$ ls ~/.claude/.credentials.json
No such file or directory
```

したがって当該実測は「設定ディレクトリを指定すると認証が壊れる」ではなく「指定先に認証情報が無ければ認証できない」と
読める。指定先へ資格情報を配置すれば成立することは、上記の分類B既存テストが同一経路で示している。加えて、認証情報を
用意できないときに `CLAUDE_CONFIG_DIR` を未設定にする案は、CLI の探索先を呼び出し元ホーム配下へフォールバックさせ、
呼び出し元由来の設定を再び露出させる。よって未設定にはせず、用意できない場合は起動せず診断へ倒す。

## 要求 → 要件 → 受入条件

### 要求

環境変数トークンを持たず外部資格情報ストアのみで認証している利用者が、`GATE_REVIEWER_CMD` による上書きなしに
ゲートレビュアを起動して verdict を得られること。認証が成立しない場合は、原因が診断から特定できること。
かつ、その実現によってゲートレビュアの隔離と read-only 性が弱まらないこと。

### 要件

- 要件1: ゲートレビュア子プロセスへ与える設定ディレクトリは常に隔離領域内の制御されたパスとする。呼び出し元の設定ディレクトリを解決先にしない。未設定にもしない（未設定は呼び出し元ホーム配下へのフォールバックを招くため禁止する）。
- 要件2: 分類A・分類Bで認証情報を用意できなかった場合に限り、呼び出し元の資格情報ストアから認証に必要な資格情報のみを取得し、隔離領域内の設定ディレクトリへ配置する。取得が時間上限内に完了しない場合は待ち続けず失敗として扱う。
- 要件3: 隔離領域内の設定ディレクトリへ持ち込むのは認証に必要な要素のみとする。呼び出し元の設定ファイル・hooks・MCP サーバ定義・権限設定を持ち込まない。
- 要件4: 呼び出し元の `HOME`・`USER`・`LOGNAME` その他の識別情報をゲートレビュア環境へ渡さない。本 Issue の対応で基底環境集合へ環境変数を追加しない。テストのための実行系上書き（隔離環境の認証確認・レビュア実行系）の値も、子プロセスの環境変数としては渡さず、隔離サブプロセスで実行するコマンド文字列として渡す。上書きの有無によって子プロセスの環境変数名の集合が変わってはならない。
- 要件5: 隔離領域へ配置した資格情報は呼び出し元利用者のみアクセス可能な権限で作成し、レビュア終了時に隔離領域ごと削除する。本 Issue の対応で新たな残存経路を作らない。資格情報の実値を環境変数・プロセス起動列・診断・ログへ出さない。
- 要件6: ゲートレビュア子プロセスによる書込みは隔離領域内に閉じ、呼び出し元のホームディレクトリおよび設定ディレクトリへ副作用を生じない。
- 要件7: 認証が成立しない場合、分類ごとの検出結果と設定ディレクトリの扱いを含む診断を、呼び出し元が観測できる標準エラーへ出力する。
- 要件8: 認証情報の所在3分類それぞれを対象とする自動回帰テストを持ち、分類Cについては実機確認の実施時期・実行者・記録先を定める。

要件と受入条件の対応（各要件は最低1つの AC で検証される）。

| 要件 | 検証する AC |
|---|---|
| 要件1 | AC-1 |
| 要件2 | AC-2 / AC-3 / AC-11 |
| 要件3 | AC-5 |
| 要件4 | AC-4 |
| 要件5 | AC-6 / AC-8 / AC-12 / AC-13 |
| 要件6 | AC-7 |
| 要件7 | AC-3 |
| 要件8 | AC-9 / AC-10 |

### 受入条件（Acceptance Criteria）

#### AC-1: 設定ディレクトリは常に隔離領域内を指す

- Given: 分類A（環境変数トークンのみ）・分類B（複製可能な通常ファイルあり）・分類C（いずれも無く資格情報ストアのみ）の3構成
- When: 各構成で claude アダプタのゲートレビュアを起動する
- Then: いずれの構成でも、ゲートレビュア子プロセスの `CLAUDE_CONFIG_DIR` が設定されており、その値が隔離領域配下のパスであって、呼び出し元の設定ディレクトリとも呼び出し元ホーム配下とも一致しない
- 検証方法見込み: `automated`

#### AC-2: 資格情報ストア限定構成でゲートレビュアが確定判定を返す

- Given: 分類C構成（環境変数トークン未設定・設定ディレクトリ配下に複製可能な認証ファイル無し・呼び出し元の資格情報ストアからは認証情報を取得できる）。本 AC では次の3つの実行系のみを代替へ差し替えてよい。(i) 資格情報ストアへ問い合わせるコマンド：実機の資格情報ストアを CI で再現できないため、既知の資格情報テキストを返す代替とする。(ii) 隔離環境の認証確認：`CLAUDE_AUTH_PROBE_CMD` による上書きを許可する。取得される資格情報が実機の有効値ではなく実経路の認証確認は必ず失敗するため、この許可なしに本 AC を automated で実施できないことによる。代替は隔離設定ディレクトリに配置された認証ファイルの内容を読み、(i) が返した資格情報と一致する場合に限り成功を返すものとする。当該代替は比較対象の期待値をファイルから読み取るものとし、資格情報の実値をコマンド文字列・コマンド引数・環境変数へ置かない（AC-8 と同一構成で両立させるため）。(iii) レビュア実行系：`GATE_REVIEWER_CMD` により判定文字列を返す代替とする。(ii)・(iii) の上書き値は、要件4 のとおり子プロセスの環境変数としてではなく隔離サブプロセスで実行するコマンド文字列として渡し、上書きの有無によって子プロセスの環境変数名の集合を変えない（AC-4 と同一構成で両立させるため）
- When: spec ゲートのゲートレビュアを起動する
- Then: 隔離領域内の設定ディレクトリに当該資格情報が配置された状態でゲートレビュアが起動し、(ii) の代替が (i) の資格情報との一致を確認して成功し、verdict を返し、gate report の final が `human_required` 以外の確定値になる
- 代替へ差し替えず実経路のまま検証する範囲: 分類A・分類Bの検出、資格情報ストアへ問い合わせるか否かの判定、取得結果を隔離設定ディレクトリの認証ファイルへ配置する処理、設定ディレクトリの決定と子プロセス環境の構成、復帰時の隔離領域削除。本 AC が模倣するのは「呼び出し元の資格情報ストアが資格情報を返すこと」と「その資格情報が実サービスで有効であること」の2点に限られ、資格情報を隔離領域内で扱う経路自体の安全性は実経路のまま AC-6・AC-8・AC-12・AC-13 が検証する。上書きを一切行わない状態での成立は AC-10 が担保する
- 検証方法見込み: `automated`

#### AC-3: 資格情報を用意できない場合は安全側で停止し診断が呼び出し元へ届く

- Given: 3分類のいずれからも認証情報を用意できない構成（資格情報ストアからの取得が時間上限内に完了しない構成を含む）
- When: ゲートレビュアを起動する
- Then: 呼び出し元の標準エラーに、分類ごとの検出結果と設定ディレクトリの扱いを含む診断が出力され（呼び出し経路で破棄されない）、gate report の final は `human_required` になる。呼び出し元の設定ディレクトリを解決先とする起動は行われない
- 検証方法見込み: `automated`

#### AC-4: 呼び出し元の識別情報がゲートレビュア環境へ渡らない

- Given: AC-2 と同じ構成。比較の基準（baseline）は、本 SPEC が原文引用した基底集合の10個（`PATH`・`HOME`・`XDG_CONFIG_HOME`・`GH_CONFIG_DIR`・`GIT_CONFIG_GLOBAL`・`GIT_CONFIG_SYSTEM`・`GIT_TERMINAL_PROMPT`・`TMPDIR`・`LANG`・`LC_ALL`）に、claude 経路が無条件に追加する `CLAUDE_CONFIG_DIR` を加えた11個とする（分類C構成では環境変数トークンがいずれも非空でないため引き継ぎ対象にならない）。AC-2 の (i)〜(iii) の差し替えを含む同一構成で実施するが、要件4 によりそれらの上書き値は子プロセスの環境変数として渡らないため、baseline は上書きの有無にかかわらずこの11個である
- When: ゲートレビュアを起動する
- Then: ゲートレビュア子プロセスの環境変数名の集合が上記11個と完全に一致する。すなわち `USER`・`LOGNAME` および上書き用の変数名（`CLAUDE_AUTH_PROBE_CMD`・`GATE_REVIEWER_CMD`）を含む追加の環境変数が存在せず、`HOME` は隔離領域配下であって呼び出し元の `HOME` と一致しない
- 検証方法見込み: `automated`

#### AC-5: 隔離設定ディレクトリの内容が認証要素のみに限定される

- Given: AC-2 と同じ分類C構成であり、かつ呼び出し元の設定ディレクトリに設定ファイル・hooks 定義・MCP サーバ定義・権限設定が存在する構成
- When: ゲートレビュアを起動する
- Then: 隔離領域内の設定ディレクトリに存在するのは認証に必要な要素のみであり、上記の呼び出し元由来ファイルはいずれも存在しない
- 検証方法見込み: `automated`

#### AC-6: 資格情報の複製が最小権限で作られ、正常終了後に残らない

- Given: AC-2 と同じ構成
- When: ゲートレビュアが verdict を返して正常終了する経路でゲートレビュア起動を終える
- Then: 実行中は隔離領域のディレクトリが 0700・隔離設定ディレクトリへ配置した資格情報ファイルが 0600 であり、正常終了後は隔離領域が削除されて資格情報の複製が残らない
- 検証方法見込み: `automated`

#### AC-7: 呼び出し元のホーム・設定ディレクトリへ副作用が生じない

- Given: AC-2 と同じ構成で、呼び出し元のホームディレクトリと設定ディレクトリの内容一覧・各エントリの更新時刻を起動前に記録する
- When: ゲートレビュアを起動して終了させる
- Then: 記録した内容一覧と更新時刻が起動前後で一致する（新規作成・更新・削除が無い）
- 検証方法見込み: `automated`

#### AC-8: 資格情報の実値が要件5 の禁止経路へ現れない

- Given: AC-2 と同じ構成
- When: ゲートレビュアを起動し、要件5 が禁じる露出先（環境変数・プロセス起動列・診断・ログ）に限って次を収集する。(a) ゲートレビュア子プロセスの環境変数とプロセス起動列、およびその標準出力・標準エラーのうち呼び出し元が記録・保全する内容（要件5 の「ログ」に該当する）、(b) 呼び出し元ホスト上で実行される資格情報の取得ステップ（資格情報ストアへ問い合わせるコマンド）の起動列と環境変数、(c) 取得した資格情報を隔離設定ディレクトリへ配置するステップが実行するコマンドの起動列と環境変数、(d) アダプタが呼び出し元の標準出力・標準エラーへ出す診断と、実行中に書き出すログ、(e) 資格情報の取得ステップの標準出力の接続先（隔離設定ディレクトリの認証ファイルへ接続されているか、アダプタの標準出力・標準エラーその他へも接続されているか）と、当該接続先のうち隔離設定ディレクトリの認証ファイル以外へ流れる内容
- Then: (a)〜(e) のいずれにも資格情報の実値が含まれない。すなわち取得した実値をコマンド引数として渡す実装、実値を保持する環境変数を作る実装、および取得ステップの標準出力を隔離設定ディレクトリの認証ファイル以外（アダプタの標準出力・標準エラーを含む）へも接続する実装は本 AC を満たさない
- 収集対象外（要件5 が禁じる露出先ではないため免責する）: (i) 資格情報ストアへ問い合わせるコマンドの標準出力のうち、その接続先が隔離設定ディレクトリの認証ファイルのみであることを (e) の収集結果により検証できたもの（接続先を検証できない標準出力、および当該認証ファイル以外〔アダプタの標準出力・標準エラーを含む〕へも接続された標準出力は免責せず、(a)〜(e) と同じく実値を含んではならない）、(ii) その標準出力が隔離設定ディレクトリの認証ファイルへ渡る際に経由するパイプ等のプロセス間データ流路、(iii) 隔離領域内へ配置した資格情報ファイルの内容自体。これらは資格情報が取得元から隔離先ファイルへ渡る正規の経路であり、本 SPEC が前提とする取得手段では実値が通ることを避けられない。免責は当該経路に閉じている場合に限られ、経路が呼び出し元へ届く標準出力・標準エラーへ分岐する場合は要件5 の「ログ」への露出として (e) が検出する。要件5 が禁じる露出先は環境変数・プロセス起動列・診断・ログであって、当該経路に閉じている限りこれらを含まないため、本 AC は要件5 を超えて制約しない
- 検証方法見込み: `automated`

#### AC-9: 3分類すべてに自動回帰テストが存在する

- Given: 認証情報の所在3分類（環境変数・設定ディレクトリ配下の通常ファイル・外部資格情報ストア限定）
- When: リポジトリの自動テストを実行する
- Then: claude アダプタのゲートレビュア起動経路を対象とする自動テストが各分類につき最低1件存在し、すべて成功する
- 検証方法見込み: `automated`

#### AC-10: macOS の対話ログイン実機で上書きなしに成立する

- Given: macOS で `claude` の対話ログイン済み、環境変数トークン未設定、設定ディレクトリ配下に認証ファイル無し、レビュア実行系・認証確認系の上書き（`GATE_REVIEWER_CMD` を含む）を一切設定していない
- When: macOS 実機を保有する人間の実行者（本リポジトリのメンテナ。AI ワーカーは代替しない）が、ゲートレビュア起動スクリプトを spec ゲートに対して実行する
- Then: ゲートレビュアが verdict を返し、gate report の final が `human_required` 以外の確定値になる。実行時期は次の2時点とする。(a) 本 Issue の独立検証セグメントで検証結果を確定させる前、(b) 以降の Issue が分類C経路（隔離設定ディレクトリの構成・資格情報の取得と配置）を変更した場合は当該 Issue の独立検証セグメントで再実行。実行者名・実行日時・結果は当該 Issue の `VALIDATION.md` へ記録する
- 検証方法見込み: `manual`

#### AC-11: 分類A・分類Bで用意できた場合は資格情報ストアへ問い合わせない

- Given: 2構成。構成(a) 分類A＝環境変数トークンが設定されている。構成(b) 分類B＝環境変数トークン未設定だが呼び出し元の設定ディレクトリに複製可能な認証ファイルが存在する。本 AC では次の3つの実行系のみを代替へ差し替えてよい。(i) 資格情報ストアへ問い合わせるコマンド：起動された事実を記録したうえで失敗する代替とする。(ii) 隔離環境の認証確認：`CLAUDE_AUTH_PROBE_CMD` による上書きを許可する。自動テストが用いる構成(a) の環境変数トークン値および構成(b) の認証ファイル内容は、AC-2 の分類C資格情報と同じく実機の有効値ではなく実経路の認証確認は必ず失敗するため、この許可なしに本 AC を automated で実施できないことによる。代替は期待値をファイルから読み取り、構成(a) では隔離サブプロセスへ引き継がれた環境変数トークンの値と、構成(b) では隔離設定ディレクトリの認証ファイルの内容と照合し、一致する場合に限り成功を返すものとする。当該代替は資格情報の実値をコマンド文字列・コマンド引数・環境変数へ置かない。(iii) レビュア実行系：`GATE_REVIEWER_CMD` により判定文字列を返す代替とする。(ii)・(iii) の上書き値は、要件4 のとおり子プロセスの環境変数としてではなく隔離サブプロセスで実行するコマンド文字列として渡し、上書きの有無によって子プロセスの環境変数名の集合を変えない
- When: 各構成で claude アダプタのゲートレビュアを起動する
- Then: いずれの構成でも資格情報ストアへの問い合わせ記録が1件も存在せず（取得を試みない）、ゲートレビュアは起動して verdict を返し、gate report の final が `human_required` 以外の確定値になる
- 代替へ差し替えず実経路のまま検証する範囲: 分類A・分類Bの検出、資格情報ストアへ問い合わせるか否かの判定、分類Aの環境変数トークン引き継ぎ、分類Bの認証ファイル複製、設定ディレクトリの決定と子プロセス環境の構成、復帰時の隔離領域削除。本 AC が模倣するのは「用意できた資格情報が実サービスで有効であること」の1点に限られる。上書きを一切行わない状態での成立は、分類Cについては AC-10 が担保し、分類A・分類Bについては本 Issue が変更しない既存経路（Issue #562・Issue #691 が担保済み）である
- 検証方法見込み: `automated`

#### AC-12: 認証が成立しない経路でも資格情報の複製が残らない

- Given: 分類C構成で、資格情報ストアからは資格情報を取得できるが、その資格情報では隔離環境の認証確認が成立しない構成
- When: ゲートレビュアの起動を試み、認証不成立として復帰する
- Then: 隔離領域が削除され、配置した資格情報の複製がファイルシステム上に残らない。gate report の final は `human_required` になり、呼び出し元の標準エラーへ認証不成立の診断が出力される
- 検証方法見込み: `automated`

#### AC-13: 取得の時間上限・timeout・watchdog 起動失敗の復帰経路でも資格情報の複製が残らない

- Given: AC-2 と同じ構成を基にした3構成。構成(a) レビュアが時間上限内に応答しない。構成(b) watchdog を起動できない。構成(c) 要件2 が定める取得の時間上限失敗＝AC-2 の (i) の代替を「時間上限を超えても応答しない代替」へ置き換え、資格情報ストアへの問い合わせが時間上限内に完了しない。構成(c) では取得ステップの標準出力が隔離設定ディレクトリの認証ファイルへ接続されている以上、打ち切り時点までに書き込まれた部分的な内容が隔離領域内に生じ得る
- When: 各構成でゲートレビュア起動を終える
- Then: いずれの経路でも隔離領域が削除され、配置した資格情報の複製が残らない。構成(c) では打ち切り時点までに書き込まれた部分的な内容についても同様に残らない。gate report の final は `human_required` になる
- 検証方法見込み: `automated`

## 未決事項

- 3分類の自動回帰テストはいずれも、隔離環境の認証確認を模した代替実行系（`CLAUDE_AUTH_PROBE_CMD`）に依存する。実サービスで有効な資格情報を CI で用意できないため実経路の認証確認は分類によらず必ず失敗し、AC-2（分類C）も AC-11（分類A・分類B）もこの上書きを前提とする。分類Cはこれに加えて、呼び出し元の資格情報ストアを模した代替実行系にも依存する。模倣の前提（取得コマンドの成否・出力形式・認証確認の成立条件）が実機と乖離した場合、自動テストは成功したまま実機で失敗し得る。この限界のうち分類Cについては AC-10 の実機確認でのみ埋まる。分類A・分類Bについては本 Issue が当該経路を変更しないため、既存の担保（Issue #562・Issue #691）を超える追加の実機確認は本 Issue では求めない。設計セグメントでは、模倣の前提を実機挙動と対応付けて記録する手段を定めること。
- 資格情報ストアが返す値の形式について、本 SPEC は分類Bの認証ファイルと同一形式でありそのまま配置すれば認証が成立することを前提とするが、根拠とした実測は先頭部分の記録のみである。全体構造・末尾に付随する出力の有無・改行の扱いが前提と異なる場合は取得結果の変換が必要となり、分類C経路の実装方式が変わる。設計セグメントで実機実測により確定すること。
- 資格情報ストアからの取得が対話的承認を要求するか否か（macOS の項目 ACL に依存）は設計セグメントで実測により確定する。承認要求が発生する環境では要件2の時間上限により診断へ倒れる。分類A・分類Bが成立している構成では取得自体を試みないことを AC-11 が固定するため、承認要求と時間上限待機は分類C構成に限って生じる。

## スコープ外

- codex アダプタのゲートレビュアが起動できず stderr 破棄で原因を特定できない件（Issue #744）。本 Issue は claude アダプタの認証経路のみを対象とする。
- ゲートレビュア子プロセス自身の標準エラーを呼び出し元へ保全すること。本 SPEC が扱う診断はアダプタが自ら出力するものに限る。
- ゲートレビュアの evidence 投稿が無言で失敗する件（Issue #762）。本 Issue は verdict を得るまでの認証成立を対象とする。
- セグメント作業ワーカー起動経路の認証判定。本 Issue はゲートレビュア起動経路に限る。
- macOS Keychain 以外の外部資格情報ストア（Linux Secret Service 等）への個別対応。
- 認証情報の取得・更新・失効の管理、および codex アダプタの環境構成の変更。
- 呼び出し元プロセスが強制終了（`SIGKILL`）された場合の隔離領域の残存。これは分類Bの複製を含む隔離領域全体に既存の性質であり、本 Issue で新たに生じる残存経路ではない。
- 隔離方針そのものの再設計。基底環境集合・隔離領域の構成方式・`HOME` の値はいずれも変更しない。
