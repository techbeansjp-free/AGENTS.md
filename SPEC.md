# SPEC: codexゲートレビュアが隔離環境で起動できない（インタプリタがPATH探索でしか解決されず起動に失敗する）

- Issue: `ISSUE-727`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/727-codex-reviewer-interpreter-path`

## 目的・背景

ゲートレビュアは、呼び出し元の環境を持ち込まない隔離環境（空の環境変数集合から組み立てた固定の環境変数・固定PATH・隔離HOME・隔離workspace）で起動される。この隔離は、呼び出し元HOME配下やIssue worktree配下の任意コードが「PATH探索という非決定的な解決経路」を通じてレビュア実行系として実行されることを防ぐために導入された。

### 前提とする現行動作（本SPEC内で自己完結させる事実）

以下は本Issue着手時点で既に成立している動作であり、本SPECの要求・受入条件はこれを前提に読む。

1. レビュア実行系CLIの実行ファイルは、PATH探索を隔離環境内で行うのではなく、**呼び出し元環境で決定的に解決した絶対パス**として起動コマンドへ埋め込まれる。すなわち「呼び出し元HOME配下の実行ファイルを、絶対パス指定に限って実行する」ことは本Issue以前から成立している既存の信頼水準である。
2. 隔離環境へ渡すPATHは `/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin` を基本とする固定値である。これは絶対パス起動を禁じるためではなく、PATH探索という非決定的な解決経路を隔離環境へ持ち込まないためのものである。
3. 認証情報は、呼び出し元の設定ディレクトリ全体を持ち込むのではなく、認証に必要な既知の通常ファイル（シンボリックリンクでないもの）だけを隔離領域へ複製し、隔離環境の設定ディレクトリ環境変数をその複製へ向けることで渡される。codexアダプタでは呼び出し元の `CODEX_HOME`（未設定時は呼び出し元HOME配下の `.codex`）にある `auth.json`、claudeアダプタでは呼び出し元の設定ディレクトリにある `.credentials.json` が対象で、claudeアダプタでは呼び出し元に設定されている場合に限り `ANTHROPIC_API_KEY`・`CLAUDE_CODE_OAUTH_TOKEN` も隔離環境へ引き継ぐ。
4. 隔離環境ではGitHub credential・gh/git設定・呼び出し元HOMEは渡さない。判定対象の情報はプロンプトへ埋め込み済みで、レビュアは隔離workspace以外を読む必要がない。

### 欠陥

実行ファイル本体は上記1により絶対パスで起動できるが、その実行ファイルがインタプリタを要求するスクリプト（例: 先頭行が `#!/usr/bin/env node`）である場合、インタプリタだけは隔離環境内のPATH探索でしか解決されない。インタプリタの実体が固定PATHに含まれないディレクトリ（バージョンマネージャ配下等）にあると、起動そのものが失敗する。**「実行ファイル本体は決定的に解決した絶対パスで起動するが、そのインタプリタはPATH探索へ委ねる」という解決規則の非対称が本欠陥である。**

さらに、この起動失敗は認証probeの実行時に最初に顕在化するため、レビュアは「認証情報が見つからない／認証probeに失敗した」と診断してフェイルセーフで `human_required` へ倒れる。認証情報が実在していても同じ診断が出るため、利用者は再ログイン等の無関係な対処へ誘導される。

実測された害は次の3点である。

1. codexアダプタのゲートレビュアが一切起動できず、`human_required` で停止して完全自走が成立しない。本リポジトリ自身の変更はほぼ全てがコアレビュー対象であり、その既定レビュアがcodexであるため影響は恒常的である。
2. 診断が根本原因（インタプリタ解決失敗）ではなく認証失敗を指し、原因究明を妨げる。
3. 信頼境界ハードニング導入前は同一環境でcodexレビュアが繰り返し完走していたため、回帰である。

ネイティブ実行ファイル（ELF）として配布されるclaudeアダプタは同じ隔離環境で動作するため、本欠陥はcodex固有ではなく、インタプリタを必要とする実行ファイル全般に効く。

## 要求 → 要件 → 受入条件

### 要求

インタプリタを、実行ファイル本体と**同一の解決規則・同一の信頼水準**で扱う。すなわち、呼び出し元環境で決定的に解決した絶対パスとしてのみ用い、隔離環境のPATHへディレクトリを追加しない。これにより、隔離環境の信頼境界（PATH探索で解決された任意コード・Issue worktree配下の任意コードをレビュア実行系として実行しないこと）を緩めずに、正当に導入された実行系CLIが、それがスクリプト形式であってもゲートレビュアとして起動でき、verdictを返せること。あわせて、起動できない場合の診断が実際の原因を指すこと。

守るべき性質は「呼び出し元HOME配下だから実行しない」ではなく「PATH探索で解決したものは実行しない」である。

### 要件

- 隔離環境で起動する実行系CLIがインタプリタを必要とするスクリプトである場合、そのインタプリタを実行ファイル本体と同一の解決規則で扱う。インタプリタは呼び出し元環境で決定的に解決し、その**絶対パス**でのみ用いる。
- 隔離環境のPATHへインタプリタの所在ディレクトリを追加しない。追加すると、同一ディレクトリ内の他の任意実行ファイルまでPATH探索の対象になるためである。隔離環境のPATHは `/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin` を基本とし、本Issueで新たなディレクトリを追加しない。
- 解決した実行ファイル本体またはインタプリタがIssue worktree（作業中ブランチが変更しうる領域）配下である場合は起動しない。シンボリックリンク経由でIssue worktree配下の実体を指す場合も、実体解決後に判定して起動しない。
- 実行ファイルまたはそのインタプリタを解決できずに起動が失敗した場合、認証失敗ではなく起動失敗として診断する。診断は日本語で、解決できなかった対象名と、隔離環境へ渡した固定PATHの内容と、利用者が取るべき操作を区別して示す。
- 起動失敗時のゲート判定は成功（pass）として扱わず、安全側に `human_required` へ帰着させる。ただし提示する診断は認証失敗ではなく起動失敗である。
- 認証情報が実際に不在または不正な場合の診断は従来どおり認証失敗として提示され、上記の起動失敗診断と区別できる。
- 認証情報の実値を、診断・ログ・標準出力・標準エラーのいずれにも含めない。起動成功経路・起動失敗経路・認証失敗経路のすべてに適用する。
- 上記をスクリプト形式・ネイティブ形式の双方の実行ファイルについて自動テストで検証する。
- 現在正常に動作しているネイティブ実行ファイル形式のレビュア起動経路を退行させない。

### 受入条件（Acceptance Criteria）

#### AC-1: スクリプト形式CLIのゲートレビュアが隔離環境で起動しverdictを返す

- Given: ゲートレビュアの実行系CLIが `#!/usr/bin/env node` 形式のスクリプトであり、その `node` インタプリタの実体が隔離環境の固定PATH（`/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin`）に含まれないディレクトリ（バージョンマネージャ配下等、呼び出し元HOME配下）にのみ存在する。認証情報は、呼び出し元の設定ディレクトリにある認証ファイル（codexアダプタでは `CODEX_HOME`（未設定時は呼び出し元HOME配下の `.codex`）の `auth.json`、claudeアダプタでは設定ディレクトリの `.credentials.json`）が通常ファイルかつシンボリックリンクでない状態で存在することにより成立し、起動時に隔離領域へ複製され、隔離環境の設定ディレクトリ環境変数がその複製を指す（claudeアダプタでは呼び出し元に設定されている場合に限り `ANTHROPIC_API_KEY`・`CLAUDE_CODE_OAUTH_TOKEN` も隔離環境へ引き継がれる）
- When: 当該アダプタでゲートレビュアを起動する
- Then: 実行ファイル本体とインタプリタの双方が呼び出し元環境で決定的に解決した絶対パスとして用いられ、隔離環境内で実行系CLIが起動し、認証probeが成功し、レビュアがverdictを返す。インタプリタの所在ディレクトリは隔離環境のPATHへ追加されず、隔離環境のPATHは固定値のまま本Issueによる新規ディレクトリ追加を含まない。認証情報不在を理由とする `human_required` へ倒れない
- 検証方法見込み: `automated`

#### AC-2: 実行ファイル本体・インタプリタのいずれもIssue worktree配下の実体を用いない

- Given: AC-1 と同じ環境で、Issue worktree配下に実行系CLIと同名の実行ファイル、およびインタプリタと同名の実行ファイル（実体、またはIssue worktree配下の実体を指すシンボリックリンク）が存在する
- When: ゲートレビュアを起動する
- Then: 起動に用いる実行ファイル本体・インタプリタはいずれもPATH探索で解決されず、シンボリックリンクの実体解決後の判定でもIssue worktree配下ではない。解決結果がIssue worktree配下となる場合は起動せず、起動失敗として診断される。Issue worktree配下の同名実行ファイル・同名インタプリタはいずれも実行されない
- 検証方法見込み: `automated`

#### AC-3: 解決不能時は起動失敗として診断され安全側へ帰着する

- Given: 実行系CLIの実行ファイル、またはそのインタプリタを呼び出し元環境で決定的に解決できない、もしくは解決結果が起動を許されない実体である
- When: ゲートレビュアを起動する
- Then: 認証失敗ではなく起動失敗として診断され、診断文には解決できなかった対象名と隔離環境へ渡した固定PATHの内容と利用者が取るべき操作が日本語で区別して示される。ゲート判定は成功（pass）として扱われず `human_required` へ帰着する
- 検証方法見込み: `automated`

#### AC-4: 認証情報不在は従来どおり認証失敗として診断される

- Given: 実行系CLIとそのインタプリタは決定的に解決でき隔離環境で起動できるが、認証情報が不在または不正で認証probeが成立しない
- When: ゲートレビュアを起動する
- Then: 認証失敗として診断され、AC-3 の起動失敗診断とは区別できる文言となる
- 検証方法見込み: `automated`

#### AC-5: いずれの経路でも認証情報の実値が外部へ露出しない

- Given: 起動成功経路（AC-1 の条件）・起動失敗経路（AC-3 の条件）・認証失敗経路（AC-4 の条件）のそれぞれ
- When: ゲートレビュアを起動し、診断・ログ・標準出力・標準エラーの全出力を収集する
- Then: 収集した全出力のいずれにも、隔離領域へ複製した認証ファイルの内容および引き継いだ認証用環境変数の実値が現れない
- 検証方法見込み: `automated`

#### AC-6: スクリプト形式・ネイティブ形式の双方を対象とする自動テストが存在する

- Given: 本Issueの変更を含むリポジトリ
- When: リポジトリのテストスイートを実行する
- Then: AC-1 から AC-5 までを検証する自動テストが存在して成功し、その対象にスクリプト形式（shebangでインタプリタを要求する）実行ファイルとネイティブ形式（インタプリタ不要）実行ファイルの双方が含まれる。ネイティブ形式のレビュア起動経路が退行していないことも同テストで確認される
- 検証方法見込み: `automated`

## スコープ外

- 実行ファイル・インタプリタの**許可位置**（どのディレクトリにある実体なら信頼するか）の条件設計。本Issueで確定するのは**解決規則**（PATH探索を用いず、呼び出し元環境で決定的に解決した絶対パスのみを用いる）であり、許可位置の条件設計は Issue #715 が扱う。両者の境界はこの一文が定める。
- Issue #715 が扱う信頼境界そのもの（verdictの無検査永続化、実行ファイル解決が呼び出し元環境へ依存すること）の是正。
- codex CLIをシステム領域へ導入し直す等、環境側の運用による回避。
- `review.adapter` の既定値の見直し。
- ゲートレビュア以外の実行経路（セグメント作業ワーカーの起動等、隔離環境を用いない経路）の挙動変更。
- レビュアのモデル選定・reasoning effort・レビュー品質・プロンプト内容の変更。
- ゲート判定結果の記録経路（PR review evidence投稿・gate-report結線）の変更。
