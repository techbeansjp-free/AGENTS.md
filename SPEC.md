# SPEC: 反証ルーブリックに合格到達条件・blocking 基準・過去ラウンド・ラウンド上限を与え、ゲートを収束させる

- Issue: `ISSUE-729`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/729-falsification-rubric-nonterminating`

## 目的・背景

ゲートレビュアの判定プロンプトが生成する反証ルーブリックには、合格に到達する条件が 1 文も書かれていない。実物の記述は次のとおりで、反例の能動的探索だけを命じ、探索を打ち切ってよい条件を与えない。

```
- 反例（未処理エッジ・矛盾・危険な既定・未テストの失敗経路・spec⇔実装乖離）を能動的に 1 件以上探索する。blocking な反例が 1 件でもあれば falsification=fail とし origin 付き blocking finding を付与する。
```

同じ節の立証側は「1 件でも欠落・未証跡なら fail」と書かれ、裏返せば「全 AC-ID が充足されていれば pass」という到達条件が一意に定まる。反証側にはこれに対応する到達条件が無い。その結果、同じ品質の成果物でもレビュアが反例を 1 件構成できれば fail、できなければ pass になる。

実測では ISSUE-733 の spec-gate が 9 ラウンド、ISSUE-692 の implementation-gate が 10 ラウンド、ISSUE-680 が 13 ラウンドを要した。ISSUE-692 では同一の finding code が 6 ラウンド連続で再提出されている。投入資源の大半がゲート通過そのものに費やされ、製品が前へ進まない。一方で ISSUE-721 の implementation-gate は両レビュアがともに pass・blocking 0 件を返しており、現行ルーブリックでも合格は到達可能である。欠陥は「合格が存在しないこと」ではなく、合格への到達が偶然に委ねられ成果物の品質と対応していないことである。

本 Issue は Issue #697（過去ラウンドの判定結果が判定プロンプトに含まれない）と Issue #698（反証の停止条件・severity 基準が無い）の統合先であり、両者の要求を含む。

## 前提

- ゲートレビュアはツール呼び出しを持たず、判定プロンプトへ文字列として展開された内容のみを入力とする。したがってレビュアの振る舞いを変える手段は、プロンプトの記述内容と、そこへ展開する情報の変更に限られる。
- severity を `code`・`evidence` のテキスト照合で機械的に書き換える機構は設けない。ADR-0031 が accepted 済みの判断として、誤検知と見落としの双方でテキスト照合が不適切であり、レビュアの判定を最終的な正とする設計を採ることを確定させている。本 Issue はこの判断を継承する。
- 既存の最終判定導出は、レビュアの verdict の `inconclusive` が真であるとき最終判定を `human_required` とする。進行役が本 Issue の起票時点の実装で実測し確認済みである。本 Issue はこの既存挙動へ接続するのみで、最終判定導出そのものは変更しない。
- GitHub モードでは、ゲートレビューの各ラウンドの証跡が PR のレビューとして投稿され、セッションの終了後も残る。1 回のゲート反復には 1 つの反復識別子が採番され、当該反復で実行されるレビュア全体（Standard は 1 体、Strict は 2 体）の証跡へ同一の値が記録される。各レビュア実行はこれとは別に自身の実行識別子と slot 番号を持つ。進行役が本 Issue の起票時点の実装で実測し確認済みである。したがって同一 Issue・同一ゲートの過去ラウンド数は、新規の永続ストアを設けずに既存の耐久記録から復元できる。
- 上記の帰結として、本 Issue の受入条件は「生成される判定プロンプトの内容」「ラウンド番号の導出結果」「verdict から最終判定を導出する処理の挙動」を検証対象とする。レビュアの自然言語判断そのものの当否は検証対象にできない。

## 用語

- ゲート反復: 1 回のゲートレビューの実行単位。レビュープロファイルが定めるレビュア実行の全体（Standard は 1 体、Strict は 2 体）をまとめて 1 回と数える。差し戻し・是正・再レビューを繰り返すループ全体ではなく、そのうちの 1 回分を指す。
- 反復識別子: 1 回のゲート反復に対して 1 つ採番され、当該反復の全レビュア実行の証跡へ共通に記録される識別子。レビュア実行ごとの実行識別子・slot 番号とは別の値であり、レビュア体数によらず 1 反復につき 1 つである。
- ラウンド: 同一 Issue・同一ゲートに対するゲート反復の通し番号。初回を 0 とする。1 回のゲート反復がちょうど 1 ラウンドに対応する。計数の単位はレビュア実行ではなくゲート反復であり、レビュープロファイルが定めるレビュア体数によらない。是正のたびに target_sha が変わっても同一 Issue・同一ゲートのゲート反復は同一の通し番号の系列に属し、target_sha の変化を理由に 0 へ戻らない。
- blocking 基準: 反例を blocking severity の finding として提出してよい条件。
- 到達可能性: 反例が前提とする入力が、本システムの他の機械的検査（ブランチ名規約・worktree パス規約等）を通過して実際に成立し得ること。
- 実証性: 反例が成立する経路を、当該ラウンドの判定対象成果物の記述を引用して具体的に示せること。抽象的な可能性の指摘にとどまり、成果物のどの記述からその経路が生じるかを示せないものは実証性を満たさない。
- 耐久記録: セッションの終了後も残り、同一 Issue・同一ゲートの過去ラウンド数を復元できる記録。GitHub モードでは PR へ投稿された各ラウンドのレビュー証跡がこれに該当し、各証跡には当該ゲート反復の反復識別子が記録されている。
- 限定閾値: これ以上のラウンドで blocking の適用範囲をさらに絞る、設定されたラウンド番号。打ち切り閾値より真に小さくなければならない。両者が等しいと、限定が初めて効くラウンドと打ち切りが発火するラウンドが一致し、限定が承認率を引き上げる機会を一度も持たない。
- 打ち切り閾値: これに達したラウンドで未解消の blocking が残る場合に人間判断へ移す、設定されたラウンド番号。

## 入力・出力

- 入力: 判定プロンプト生成の既存入力（Issue 番号・ゲート識別子・target_sha・判定対象の成果物と差分・上流の承認済み成果物・レビュープロファイルの解決結果）に加え、耐久記録から取得した同一 Issue・同一ゲートの過去ラウンドの判定記録とラウンド番号。
- 出力: レビュアへ渡す判定プロンプト文字列と、レビュアの verdict から導出される最終判定。

## 要求 → 要件 → 受入条件

### 要求

ゲートが有限ラウンドで承認または人間判断へ到達すること。ここで rejected は各ラウンドの中間帰結であり、差し戻し・是正・再レビューの反復を経て最終的に承認または人間判断へ到達することを求める。反復そのものを取り除くことは要求ではない。同時に、データ喪失・セキュリティ低下・既存機能の回帰・当該 Issue の目的の未達を見逃さないこと。単にゲートを緩めて通しやすくするだけでは要求を満たさない。同一の指摘が繰り返し再提出されないこと。

### 要件

要件は次の順序で効く。要件 1 と要件 2 が承認率そのものを引き上げ、要件 3 と要件 4 が同一論点の反復を抑える。要件 5 は高ラウンドで blocking の適用範囲を要件 2 が定める集合の部分集合へさらに絞り、要件 6 が停止性を保証する。要件 6 の保証はラウンド番号が正しい単位で求まることに依存するため、ラウンド番号の計数単位の確定と耐久記録からの導出も要件 6 に含める。ラウンド上限だけを導入した場合、承認率が変わらないまま打ち切りだけが働き、溢れた判断がすべて人間へ流れるため要求を満たさない。

- 要件 1: 反証ルーブリックに、立証側と対称な合格到達条件を置く。blocking 基準を満たす反例が 1 件も無いことを、正常かつ第一級の帰結として明記する。反例を必ず 1 件以上見つけることを求める記述を除去する。
- 要件 2: 反例を blocking として提出してよい基準を定義する。次の 3 条件をすべて満たす反例だけを blocking とし、いずれかを満たさないものは warning 以下として記録する。この 3 条件はラウンド番号によらず常に必要条件である。
  - 目的阻害性: 当該 Issue の目的の達成を直接妨げる、または既存機能の回帰・データ喪失・セキュリティ低下を伴う。
  - 到達可能性: 前提とする入力が、本システムの他の機械的検査によって到達可能である。
  - 責務内是正可能性: 当該セグメントの責務範囲内で是正できる。下流セグメントで確定する事項を先取りしない。
- 要件 3: 同一 Issue・同一ゲートの過去ラウンドの判定記録を判定プロンプトへ展開する。展開する内容は finding code・severity・立証と反証の判定、および各 finding の根拠要約である。根拠要約は、当該 finding が成果物のどの記述を対象とし、そこにどの欠陥があると述べたかを示すものとし、レビュアが当該ラウンドの成果物と突き合わせて是正済みか未修正かを判別できる水準を満たす。code は短い識別子にすぎず、code・severity・判定の 3 項目だけでは要件 4 が求める是正済みと未修正の判別が実行できないため、根拠要約を必須の展開項目とする。あわせて、過去ラウンドの判定記録が存在しない初回と、判定記録が存在しうるが耐久記録から取得できない場合とを区別する。前者では過去ラウンドが無い旨を展開し、後者では過去ラウンドが無い旨を展開せず、過去ラウンドの判定記録を取得できなかった旨を展開する。取得失敗を初回と同一に扱うと、実際には過去ラウンドが存在するのに「過去ラウンドが無い」と誤って展開され、要件 4 の蒸し返し抑止が黙って無効化されるためである。
- 要件 4: 過去ラウンドで記録済みの finding code のうち、当該ラウンドの成果物で是正済みと確認できる論点を、新たな根拠を示さずに再び blocking として提出しないことを判定プロンプトで指示する。禁止の対象は是正済みの論点の蒸し返しに限る。当該ラウンドの成果物で未修正のまま残っている blocking を同一 code で再提出することは、この禁止の対象外であり許容する。
- 要件 5: Standard・Strict のレビュープロファイルにもラウンド上限を導入する。ラウンド番号が限定閾値以上の場合、blocking として提出してよい反例を、要件 2 の 3 条件をすべて満たし、かつ実証性を満たすものだけに限定する。限定は要件 2 の 3 条件のいずれも取り除かない。したがって限定後の集合は限定前の集合に含まれ、実証性を満たさない反例の分だけ真に小さい。限定により blocking から外れた反例は warning 以下として記録する。閾値は設定で与え、既定値を定義する。限定閾値は打ち切り閾値より真に小さくなければならず、両者が等しい設定を含め、この順序関係を満たさない設定は設定エラーとして拒否する。等号を許すと、限定が初めて効くラウンドと要件 6 の打ち切りが発火するラウンドが一致し、限定が承認率を引き上げる機会を一度も持たないまま判断が人間へ流れるためである。進行役が毎回の宣言で範囲を限定する運用に依存しない。
- 要件 6: ラウンド番号を、ゲート反復を単位として耐久記録から導出する。GitHub モードでは、PR へ投稿された過去ラウンドのレビュー証跡に記録された反復識別子のうち相異なるものの個数から導出する。証跡の件数そのものからは導出しない。同一の反復識別子を持つ複数体の証跡は 1 ラウンドへ畳むため、ラウンド番号は Standard・Strict のいずれでもゲート反復の回数と一致し、レビュア体数に比例して整数倍にずれない。是正のたびに target_sha が変わっても同一 Issue・同一ゲートの過去のゲート反復を計数へ含め、target_sha の変化を理由に 0 へ戻さない。ラウンド番号のための新規の永続ストアは設けない。ラウンド番号が打ち切り閾値に達しても未解消の blocking が残る場合、判定を inconclusive とし、既存の最終判定導出を経由して人間判断へ移す。この inconclusive は、レビュアへの判定プロンプトの指示によってレビュアが自己申告するのではなく、verdict を受け取って記録する trusted な処理が、ラウンド番号・打ち切り閾値・未解消 blocking の有無から機械的に設定する。レビュアの自己申告に依存させると、レビュアの自然言語判断の当否を検証できないという前提の下で停止性が保証されないためである。承認を自動的に記録しない。ラウンド番号を耐久記録から導出できない場合は、既存の差し戻しの反復を維持する側へ倒す。すなわち未解消の blocking が残る場合は通常どおり rejected とし、差し戻し・是正・再レビューの反復を継続する。導出不能時に禁じるのは、要件 5 の限定を適用すること（安全側）と、打ち切りを理由に承認を記録することの 2 点だけであり、rejected とする経路そのものは禁じない。以上により各ラウンドの帰結は、ラウンド番号の導出可否によらず approved・rejected・human_required のいずれかとなる。blocking が解消されれば approved、blocking が残れば rejected、ラウンド番号を導出できかつ打ち切り閾値に達した状態で blocking が残れば human_required である。
- 要件 7: 要件 1 と要件 5 による緩和は、severity の区分を軸として行わない。すなわちラウンド番号やレビュープロファイルを理由に、データ喪失・セキュリティ低下・既存機能の回帰・当該 Issue の目的の未達という区分そのものを blocking の対象から除外しない。要件 5 の限定は反例の根拠提示に対する要件であり、これらの区分に該当し、要件 2 の 3 条件と実証性をすべて満たす反例は、ラウンド番号とプロファイルによらず blocking のままとする。

要件 2 の到達可能性と責務内是正可能性は、散文の仕様が反例を構成されやすくテスト付きの実装が構成されにくいという観測された偏りにも効く。仕様段階で提出される反例の多くは、下流で確定する事項の先取りか、他の機械的検査により到達不能な入力を前提とするものであり、この 2 条件がそれらを blocking から外す。要件 5 の実証性はこの偏りの残りに効く。高ラウンドまで残る反例は、成果物の具体的な記述ではなく抽象的な可能性を根拠とするものへ移っていくため、根拠提示の水準を上げることで発散が止まる。

### 受入条件（Acceptance Criteria）

#### AC-1: 反証ルーブリックが合格到達条件を明示する

- Given: 任意の Issue・ゲート・target_sha に対して、レビュア判定プロンプトを生成できる状態にある
- When: 判定プロンプトを生成し、その反証ルーブリックの記述を読む
- Then: blocking 基準を満たす反例が 1 件も無い場合に反証を pass とする旨が明記されており、反例を必ず 1 件以上見つけることを求める記述が含まれない
- 検証方法見込み: `automated`

#### AC-2: blocking 基準の 3 条件が全ラウンドの必要条件として明記される

- Given: 任意の Issue・ゲート・target_sha に対して、レビュア判定プロンプトを生成できる状態にある
- When: ラウンド番号が限定閾値未満の場合と限定閾値以上の場合の双方で判定プロンプトを生成し、その反証ルーブリックの記述を読む
- Then: いずれの場合も、目的阻害性・到達可能性・責務内是正可能性の 3 条件をすべて満たす反例だけを blocking とし、いずれかを満たさない反例は warning 以下として記録する旨が明記されている。3 条件のいずれかを不要とする記述はどちらの場合にも含まれない
- 検証方法見込み: `automated`

#### AC-3: 過去ラウンドの判定記録が判定プロンプトへ展開される

- Given: 同一 Issue・同一ゲートについて、(a) 過去ラウンドの判定記録が存在し取得できる場合、(b) 過去ラウンドが存在しない初回の場合、(c) 過去ラウンドの判定記録が存在しうるが耐久記録から取得できない場合の 3 通りがある
- When: それぞれの場合について、次のラウンドのレビュア判定プロンプトを生成する
- Then: (a) では過去ラウンドの finding code・severity・立証と反証の判定に加え、各 finding の根拠要約（当該 finding が対象とした成果物の記述と、そこに述べられた欠陥）が判定プロンプトへ展開される。(b) では過去ラウンドが無い旨が展開される。(c) は (b) と区別され、過去ラウンドが無い旨は展開されず、過去ラウンドの判定記録を取得できなかった旨が展開される。(c) で (b) と同一の文言が展開される実装はこの条件を充足しない
- 検証方法見込み: `automated`

#### AC-4: 是正済み論点の無根拠な蒸し返しのみを禁じる指示が含まれる

- Given: 同一 Issue・同一ゲートについて、過去ラウンドの判定記録が存在する
- When: 次のラウンドのレビュア判定プロンプトを生成する
- Then: 過去ラウンドで記録済みの finding code のうち当該ラウンドの成果物で是正済みの論点を、新たな根拠を示さずに再び blocking として提出しないことを求める指示が含まれる。あわせて、当該ラウンドの成果物で未修正のまま残っている blocking を同一 code で再提出することはこの禁止の対象外である旨が明記される
- 検証方法見込み: `automated`

#### AC-5: 限定閾値以上のラウンドで blocking の適用範囲が真に狭くなる

- Given: レビュープロファイルが Standard または Strict であり、同一 Issue・同一ゲートのラウンド番号が設定された限定閾値以上である
- When: 当該ラウンドのレビュア判定プロンプトを生成し、同条件でラウンド番号のみを限定閾値未満にしたプロンプトと比較する
- Then: 限定閾値以上のプロンプトでは、blocking として提出してよい反例が「要件 2 の 3 条件をすべて満たし、かつ実証性を満たすもの」に限定される旨が明記され、要件 2 の 3 条件はいずれも取り除かれず併記される。限定閾値未満のプロンプトには、実証性を blocking の追加要件として課す記述、すなわち実証性を満たさない反例を blocking から外す旨の記述が含まれない。一方、要件 7 が全ラウンドで求める「実証性を満たす反例は blocking のままである」旨の言明は、実証性を必要条件として追加するものではないため本条の禁止対象に含まれず、限定閾値未満のプロンプトに存在してよい。限定閾値は設定ファイルから与えられ、既定値が定義されている
- 検証方法見込み: `automated`

#### AC-6: 打ち切り閾値に達しても blocking が残る場合は人間判断へ移る

- Given: 同一 Issue・同一ゲートのラウンド番号が設定された打ち切り閾値に達しており、当該ラウンドの verdict に未解消の blocking finding が残っている。当該 verdict がレビュア自身の申告として持つ inconclusive は偽である
- When: 当該ラウンドの判定を記録し、最終判定を導出する
- Then: verdict を受け取って記録する trusted な処理が、ラウンド番号・打ち切り閾値・未解消 blocking の有無から判定を inconclusive として機械的に設定し、既存の最終判定導出により最終判定が `human_required` となる。承認は記録されない。レビュアが自己申告した inconclusive のみを入力とし、レビュアが偽を返した本条件で `human_required` へ到達しない実装はこの条件を充足しない
- 検証方法見込み: `automated`

#### AC-7: 重大性の区分を軸とした緩和が行われない

- Given: 同一ゲートのラウンド番号が限定閾値以上または打ち切り閾値以上である場合を含む、任意のラウンド
- When: 当該ラウンドのレビュア判定プロンプトを生成する
- Then: ラウンド番号やレビュープロファイルを理由に、データ喪失・セキュリティ低下・既存機能の回帰・当該 Issue の目的の未達という区分そのものを blocking の対象から除外する記述が含まれない。これらの区分に該当し要件 2 の 3 条件と実証性をすべて満たす反例が blocking のままである旨が明記される。当該言明は十分条件を除外しないことの表明であり、実証性を blocking の必要条件として追加するものではないため、限定閾値未満のラウンドでも成立し得る
- 検証方法見込み: `automated`

#### AC-8: 限定閾値が打ち切り閾値より真に小さいことが強制される

- Given: 限定閾値と打ち切り閾値が設定ファイルから与えられる
- When: 限定閾値が打ち切り閾値より大きい設定と、限定閾値が打ち切り閾値と等しい設定をそれぞれ読み込む
- Then: いずれも設定エラーとして拒否され、当該設定でゲートレビューが実行されない。両閾値の既定値は限定閾値が打ち切り閾値より真に小さい関係を満たす。等号の設定を受理する実装はこの条件を充足しない
- 検証方法見込み: `automated`

#### AC-9: ラウンド番号がゲート反復を単位として耐久記録から復元される

- Given: 同一 Issue・同一ゲートについて、レビュープロファイルが Strict のゲート反復が N 回実行され、各反復が 2 体分のレビュア証跡を耐久記録へ残している。同一反復の 2 体の証跡には同一の反復識別子が、異なる反復の証跡には相異なる反復識別子が記録されている。各反復の target_sha は是正のたびに変わっており、直前のラウンドで使われた一時的な作業領域は存在しない
- When: 次のラウンドの判定を実行し、ラウンド番号を求める
- Then: 初回を 0 とする通し番号として、ラウンド番号が耐久記録に現れる相異なる反復識別子の個数 N と一致し、証跡の件数 2N とは一致しない。同じ N 回の反復を Standard（1 体）で実行した場合も同じ N になる。一時的な作業領域の消失および target_sha の変化を理由に 0 へ戻らない。耐久記録からラウンド番号を導出できない場合は、要件 5 の限定を適用せず、打ち切りを理由とする承認も記録しない。この場合に未解消の blocking が残るときの最終判定は human_required ではなく通常どおり rejected となり、差し戻し・是正・再レビューの反復が維持される
- 検証方法見込み: `automated`

## 完了条件

AC-1 から AC-9 のすべてが充足され、対応する自動テストが存在し成功すること。既存のゲート関連の自動テストが回帰しないこと。

## 未決事項

- 限定閾値・打ち切り閾値の具体的な数値は設計セグメントで確定する。本 Issue の受入条件は、閾値が設定で与えられ既定値を持つこと、および限定閾値が打ち切り閾値より真に小さいことのみを要求し、特定の数値を要求しない。
- 過去ラウンドの判定記録（要件 3 が求める根拠要約を含む）を判定プロンプトへ展開する際の分量上限は設計セグメントで確定する。展開項目そのものは要件 3 が確定させており、設計へ委ねるのは分量上限だけである。
- 要件 6 のラウンド番号導出と、ADR-0031 が軽量なレビュー経路の再是正ラウンド計数について定めた格納先の判断との整合の取り方（新規 ADR の要否を含む）は設計セグメントで確定する。本 Issue は当該経路の計数に関する ADR-0031 の判断を変更しない。
- 既存の最終判定導出が `inconclusive` の真に対して `human_required` を返す経路の実在は進行役の実測で確認済みだが、設計セグメントで当該経路を改めて確認する。

## スコープ外

- 進行役の降格判断を耐久化し、ワーカーの動作契約へ反映する finding 台帳の永続化機構。Issue #745 の射程とする。本 Issue が要件 6 で求めるのはラウンド番号の復元だけであり、finding ごとの状態管理は含まない。本 Issue が扱う過去ラウンドの展開は、レビュア自身の判定記録に限る。
- 判定プロンプトへの実装ファイル・設定ファイルの展開。Issue #751 の射程とする。
- 網羅要求型の受入条件について充足を判定する方法。Issue #755 の射程とする。
- 変更規模（成果物のファイル数・差分行数）に応じた反証強度の調節。要件 2 の blocking 基準が重要度で絞るため規模軸の制御は重複であり、条項を増やすと基準間の矛盾が新たな反例の発生源になる。必要が生じた場合は別 Issue とする。
- risk トリアージの機械化、および Strict の 2 体を専門分化させること。
- 立証ルーブリックの見直し。立証側は明確な停止条件を既に持ち発散していない。
- ラウンド番号のための新規の永続ストアの追加。要件 6 は既存の耐久記録からの導出のみを求める。
- ラウンド番号を耐久記録から導出できない環境における有限性の保証。要件 6 の停止性はラウンド番号の導出が成立することに依存し、導出不能な環境では打ち切りが発火しないため有限ラウンドでの到達は保証されない。この場合も rejected による差し戻しの反復は維持され、各ラウンドは approved・rejected・human_required のいずれかへ到達する。導出不能な環境そのものを解消する手段は本 Issue の射程外とする。
- severity をテキスト照合で機械的に書き換える機構。ADR-0031 が採らない判断を確定させている。
- レビュアのモデルおよび reasoning tier の選択。
- ゲートレビュア起動経路の信頼境界（Issue #715）と、実行ファイル・インタプリタの解決（Issue #727）。
