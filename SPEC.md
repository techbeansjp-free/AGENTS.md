# SPEC: ゲートレビュー判定プロンプトの二重展開を解消し、判定材料を減らさずにバイト数を削減する

- Issue: `ISSUE-681`
- 作成者: `spec_worker`
- 対象ブランチ: `bugfix/681-gate-prompt-duplicate-expansion`

## 目的・背景

agent-skill-chain は、セグメントゲートの conformance（立証）／falsification（反証）判定を read-only のレビュアへ委譲する。レビュアへ渡す入力は、CLI サブコマンド `gate reviewer-prompt`（引数は issue_id・gate_id・target_sha、および任意の base_sha）が標準出力へ出す 1 本のテキスト（以下「判定プロンプト」）だけである。レビュアにはツール呼び出しが許可されておらず、判定プロンプト内に文字列として展開された内容以外は検証できない。したがって判定プロンプトの内容と構造は、そのままゲート判定の可否を決める。

現在の判定プロンプトは、base_sha が与えられたとき、対象成果物の内容を 2 回展開している。見出し「## 判定対象の差分」の区間は base_sha から target_sha までの差分を丸ごと載せ、見出し「## 判定対象の成果物」の区間は同じ対象成果物それぞれの target_sha 時点の全文を丸ごと載せる。対象成果物が当該ブランチで新規追加されたファイルである場合、差分は全行が追加行として現れるため、差分区間の内容は成果物区間の内容と（各行の先頭に付く `+` を除いて）完全に一致する。すなわちバイト数が概ね 2 倍になるが、レビュアが得る情報は 1 バイトも増えない。

本 SPEC 作成にあたり、実在の design セグメント（対象成果物 3 点がすべて新規追加）に対して判定プロンプトを生成し、構造を実測した。総量は 108,295 バイト・567 行であり、その内訳は差分区間 43,130 バイト、成果物区間 42,313 バイト、上流成果物区間 19,347 バイト、先頭の指示・ルーブリック・出力契約が 3,502 バイトであった。差分区間の追加行から先頭の `+` を除いた 185 行は、成果物区間へ展開された 3 成果物の全文 185 行と行単位で完全一致した。つまり総量の約 4 割が情報を増やさない重複である。implementation ゲートの実測では、同種の構造で総量が 448,531 バイトに達する例も確認した。

区間の並び順は「差分 → 成果物 → 上流の承認済み成果物」であり、重複によって肥大する差分区間が先頭側に置かれるため、整合検査に使う上流成果物は常に末尾へ押し出される。実運用では、同一 target_sha に対する 4 回のレビュー実行のうち 3 回で、レビュアが「上流の承認済み成果物が展開されていない」「差分が途中で切断されている」「成果物区間が存在しない」と報告し、conformance・falsification をいずれも pending として判定不能を返した。その結果ゲートが通過も棄却もできず、作業の進行が停止した。

配送経路での切り詰めが起きていないことは調査済みである。レビュア起動に使う標準入力経由の経路へ 300,195 バイト・1,186 行の検証用テキストを与えたところ、レビュアは最終行の識別子を正しく報告した。したがってレビュアの「切断」報告は入力の実態に基づかない。本 Issue は、レビュアの報告精度そのものを改善するのではなく、情報量を一切減らさずに判定プロンプトのバイト数を削減し、誤報告の発生確率を下げることを目的とする。

由来・根拠の補助情報: GitHub Issue #681（本 Issue）、GitHub Issue #677（本事象により design ゲートが判定不能となった事例）。

## 用語・前提

本 SPEC 内で用いる語を次のとおり定める。以降の要件・受入条件はすべてこの定義に従う。

- 判定プロンプト: `gate reviewer-prompt` が標準出力へ出力する 1 本のテキスト全体。
- 対象成果物: 当該ゲートの判定対象となるリポジトリ相対パスの集合。spec ゲートは `SPEC.md`、design ゲートは `DESIGN.md`・`PLAN.md` と差分に含まれる `docs/adr/` 配下のパス、validation ゲートは `VALIDATION.md` と差分に含まれる `test-execution.log`、implementation ゲートは差分に含まれるパスのうち上記いずれにも該当しないもの、という現行の決定規則を前提とする。本 Issue はこの決定規則を変更しない。
- 差分区間: 見出し「## 判定対象の差分」から次の見出しまでの区間。base_sha が与えられた場合のみ出力される。
- 成果物区間: 見出し「## 判定対象の成果物」から次の見出しまでの区間。対象成果物ごとに小見出しを付けて内容を載せる。
- 上流区間: 見出し「## 上流の承認済み成果物（整合検査用）」から末尾までの区間。spec ゲート以外で `SPEC.md` の target_sha 時点の全文を載せる。
- 全文再掲: ある対象成果物パスについて、差分区間内の当該パスに対応するハンクの追加行から先頭の `+` を除いた行の並びが、target_sha 時点の当該パスの全文の行の並びと一致する状態。新規追加ファイルは常にこの状態になる。
- 変更種別: base_sha から target_sha までの間に当該パスへ生じた変更の種類（追加・変更・削除・改名）。

前提として、判定プロンプトの生成は read-only であり、リポジトリの作業ツリー・git 参照・いかなるファイルも書き換えない。この性質は本 Issue でも維持する。

## 要求 → 要件 → 受入条件

要求から要件、そして機械検証可能な受入条件（AC-ID）まで一意に追跡できる形で記述する。AC-ID は `^AC-[0-9]+$` の形式に従い、`#### ` 見出しとしてのみ宣言する。

### 要求

判定プロンプトが、レビュアの判定に必要な材料を一切失わないまま、同一内容の二重展開を含まないこと。そして、内容の省略や再配置を行った場合に、レビュアが「本来あるべき情報が展開されていない」と誤認しないよう、何が展開済みで何がどこへ集約されたかを判定プロンプト自身が明示すること。

ここでいう「判定に必要な材料」とは次の全部を指す。(1) 対象成果物パスの集合。(2) 各対象成果物の target_sha 時点の全文（target_sha 時点で存在しないパスを除く）。(3) 各対象成果物の変更種別と、変更の場合はその変更箇所。(4) spec ゲート以外における上流 `SPEC.md` の全文。(5) 適用対象の AC-ID 一覧。(6) レビュア向けの指示・conformance／falsification ルーブリック・final の導出規則・出力 JSON 契約。

### 要件

- 判定プロンプトは、いずれの対象成果物についても、その target_sha 時点の全文と実質的に同一の内容を 2 箇所以上へ全文展開してはならない。判定は対象成果物パスごとに独立に行い、全文再掲に該当するパスとしないパスが同一の判定プロンプト内に混在してよい。
- 全文再掲に該当するパスについて差分区間での展開を省略する場合でも、当該パスが対象成果物であること、その変更種別、およびその全文がどの区間に展開されているかは判定プロンプトから読み取れなければならない。
- 全文再掲に該当しないパス（既存ファイルの変更など）については、差分区間の変更箇所の記載を省略・要約・切り詰めしてはならない。
- target_sha 時点で存在しないパス（削除されたパス）については、削除された事実と、削除された内容が判定できる差分表現を保持しなければならない。全文が存在しないことをもって、当該パスへの言及自体を落としてはならない。
- 上流 `SPEC.md` の全文は、spec ゲート以外のすべてのゲートで従来どおり全文展開しなければならない。spec ゲートでは上流区間を出力しない。
- 判定プロンプト内でレビュアへ「実際の内容を検証できるのは本プロンプト内に展開済みの区間のみである」と告げる注意書きが列挙する区間名の集合は、その判定プロンプトが実際に出力した区間の集合と齟齬してはならない。存在しない区間名を列挙してはならず、実際に出力した内容展開区間を列挙から落としてもならない。
- 省略・集約・並べ替えを行った場合、判定プロンプト本文に、対象パス名を伴う一定の書式で、どこを省略しどこに全文があるかを明記しなければならない。
- base_sha が与えられない場合の出力は従来どおりとし、差分区間を出力せず、対象成果物の全文を展開する。
- 判定プロンプトの生成は read-only を維持し、同一の引数に対して同一のバイト列を出力する決定性を維持する。
- 適用対象 AC-ID の抽出規則、conformance／falsification ルーブリック、Light プロファイル追加ルーブリック、final の導出規則、出力 JSON 契約の各文言は変更しない。
- 対象成果物が全て新規追加である構成において、変更後の判定プロンプトの総バイト数は変更前より小さくなければならない。

### 受入条件（Acceptance Criteria）

各 AC には、散文形式の Given/When/Then による受け入れシナリオを添える（構造化マーカーの強制は `bdd.profile: strict` の場合のみ。`.agent-skill-chain/config/agent-skill-chain.yaml` 参照）。検証方法見込みは `automated`・`manual`・`hybrid` のいずれか1語で記す。

#### AC-1: 対象成果物がすべて新規追加である差分で二重展開が解消される

- Given: base_sha から target_sha までの差分において、当該ゲートの対象成果物がすべて新規追加ファイルであるリポジトリ状態
- When: 当該 issue_id・gate_id・target_sha・base_sha で判定プロンプトを生成する
- Then: いずれの対象成果物についても、差分区間の追加行から先頭の `+` を除いた行の並びと、成果物区間へ展開された当該成果物の全文の行の並びが一致する状態が存在しない。かつ、各対象成果物の target_sha 時点の全文は判定プロンプト内にちょうど 1 箇所、途中省略なしで存在する。かつ、判定プロンプトの総バイト数は変更前の実装が同一入力に対して出力するバイト数より小さい
- 検証方法見込み: `automated`

#### AC-2: 既存ファイルの変更のみからなる差分では差分情報が保持される

- Given: base_sha から target_sha までの差分において、当該ゲートの対象成果物がいずれも base_sha 時点で既に存在し、内容が変更されただけであるリポジトリ状態
- When: 当該 issue_id・gate_id・target_sha・base_sha で判定プロンプトを生成する
- Then: 差分区間には各対象成果物の変更箇所が省略・要約・切り詰めなしで含まれ、その内容は当該範囲の差分そのものと一致する。かつ成果物区間には各対象成果物の target_sha 時点の全文が含まれる。すなわち二重展開が発生しない構成では、変更前の実装が出力していた判定材料が 1 つも失われない
- 検証方法見込み: `automated`

#### AC-3: 新規追加と既存変更が混在する差分ではパスごとに扱いが分かれる

- Given: base_sha から target_sha までの差分において、当該ゲートの対象成果物のうち少なくとも 1 つが新規追加、少なくとも 1 つが既存ファイルの変更であるリポジトリ状態
- When: 当該 issue_id・gate_id・target_sha・base_sha で判定プロンプトを生成する
- Then: 新規追加であるパスについては全文再掲が発生せず、既存変更であるパスについては差分区間の変更箇所が省略・要約・切り詰めなしで残る。かつ両種別のすべてのパスについて、target_sha 時点の全文が判定プロンプト内にちょうど 1 箇所存在する。すなわち省略はパス単位で判定され、一方の存在が他方の扱いを変えない
- 検証方法見込み: `automated`

#### AC-4: 新規追加された対象成果物の内容が空でも追加の事実が失われない

- Given: base_sha から target_sha までの差分において、当該ゲートの対象成果物のうち少なくとも 1 つが、内容が空のまま新規追加されたファイルであるリポジトリ状態
- When: 当該 issue_id・gate_id・target_sha・base_sha で判定プロンプトを生成する
- Then: 当該パスが対象成果物であること、および新規追加であって内容が空であることが判定プロンプトから読み取れる。当該パスへの言及自体が判定プロンプトから消えることはなく、内容が空であることと展開の省略とが判定プロンプト上で区別できる
- 検証方法見込み: `automated`

#### AC-5: 削除されたパスを含む差分で削除の事実と削除内容が失われない

- Given: base_sha から target_sha までの差分において、当該ゲートの対象成果物のうち少なくとも 1 つが削除され、target_sha 時点では存在しないリポジトリ状態
- When: 当該 issue_id・gate_id・target_sha・base_sha で判定プロンプトを生成する
- Then: 当該パスが削除されたこと、および削除された内容が判定プロンプトから読み取れる。target_sha 時点に全文が存在しないことを理由に当該パスへの言及や削除差分が落とされることはない
- 検証方法見込み: `automated`

#### AC-6: 差分が空である場合に従来の表示と全文展開が保たれる

- Given: base_sha と target_sha の間で、当該ゲートの対象成果物に一切の差分が無いリポジトリ状態
- When: 当該 issue_id・gate_id・target_sha・base_sha で判定プロンプトを生成する
- Then: 差分区間は差分が無いことを示す表示を保ち、成果物区間には各対象成果物の target_sha 時点の全文が展開される。差分が無いことを理由に成果物区間の全文展開が省略されることはない
- 検証方法見込み: `automated`

#### AC-7: base_sha を与えない場合の出力が従来どおりである

- Given: 任意の issue_id・gate_id・target_sha
- When: base_sha を与えずに判定プロンプトを生成する
- Then: 差分区間が出力されず、当該ゲートの既定の対象成果物の全文が成果物区間へ展開される。spec ゲート以外では上流区間に `SPEC.md` の全文が展開される。この出力は本 Issue の変更によって影響を受けない
- 検証方法見込み: `automated`

#### AC-8: 省略・集約の明示と区間名の列挙が実際の出力と整合する

- Given: 少なくとも 1 つの対象成果物について差分区間での展開を省略した判定プロンプト、および省略が一切発生しなかった判定プロンプトの両方
- When: それぞれの判定プロンプトの本文を検査する
- Then: 省略が発生した判定プロンプトには、省略された対象パス名と、その全文が展開されている区間名とを一定の書式で明記した記述が含まれる。省略が発生しなかった判定プロンプトにはその記述が現れない。かつ両方の判定プロンプトにおいて、レビュアへ検証可能な範囲を告げる注意書きが列挙する区間名の集合が、その判定プロンプトに実際に出現した内容展開区間の見出しの集合と一致し、存在しない区間名の列挙も、出力済み区間の列挙漏れも無い
- 検証方法見込み: `automated`

#### AC-9: 変更前後で判定材料の情報集合が一致する

- Given: 同一の issue_id・gate_id・target_sha・base_sha に対して、変更前の実装と変更後の実装がそれぞれ生成した判定プロンプト。対象は、全対象成果物が新規追加である構成、既存変更のみの構成、両者が混在する構成、削除を含む構成のすべて
- When: 双方の判定プロンプトから、対象成果物パスの集合、各パスの target_sha 時点の全文、各パスの変更種別と変更箇所、上流 `SPEC.md` の全文、適用対象 AC-ID の一覧、レビュア向け指示とルーブリックと final の導出規則と出力 JSON 契約の各文言、を機械的に抽出して比較する
- Then: 抽出したすべての項目が一致し、変更後にのみ欠落する項目が存在しない。かつ変更後の総バイト数は変更前以下であり、全対象成果物が新規追加である構成では変更前より小さい
- 検証方法見込み: `automated`

#### AC-10: 読み取り専用性・決定性と既存テスト全体の成功が保たれる

- Given: 変更後の実装を含む本リポジトリ
- When: 同一引数で判定プロンプトを 2 回続けて生成し、その前後でリポジトリの作業ツリーと git 参照の状態を比較する。あわせて本リポジトリの自動テストスイート全体を実行する
- Then: 2 回の出力がバイト列として同一であり、生成の前後で作業ツリーの変更・追加・削除およびいかなる git 参照の更新も発生しない。かつテストスイート全体が成功する。テストスイートには、対象成果物の全文が判定プロンプト内に 2 回以上展開された場合に失敗する回帰テストが含まれ、二重展開を再導入する変更を機械的に検知できる。対象テストを本件関連だけの部分集合へ絞り込むことは許さない
- 検証方法見込み: `automated`

## スコープ外

- レビュアが展開済みの内容を「未展開」「切断されている」と誤って報告する挙動そのものの是正。本 Issue は入力側のバイト数削減と明示のみを扱い、レビュアの報告精度は変更対象としない。
- ゲート判定のルーブリック、final の導出規則、出力 JSON 契約、レビュープロファイル（Standard／Light／Strict）の変更。
- 判定プロンプトのバイト数上限の新設、上限超過時の分割・要約・段階送信の導入。
- 対象成果物の決定規則そのものの変更。ゲートごとにどのパスを判定対象とするかは現行規則を維持する。
- レビュア起動アダプタの入力配送方式（標準入力経由のパイプ）、およびアダプタ側のサニタイズ処理の変更。
- 判定プロンプト内で成果物本文を囲むコードフェンスが、成果物本文自身に含まれるコードフェンスと入れ子になり、構造の見かけが崩れうる問題の是正。実測でこの入れ子は確認しているが、原因も対処も本 Issue の二重展開とは独立であり、混在させると変更の可逆性が下がるため本 Issue では扱わない。
- ゲート判定結果の記録経路（gate report・Check Run・trusted recorder）の変更。
