# 開発ワークフロー

## モード

既定は`full`で、`0 → 1 → … → 11`を実行する。`quick`はQ-01〜Q-08がすべて真かつ根拠付きの場合だけ許可し、`0 → 1 → 4 → 9 → 10 → 11`を実行する。偽、不明、未回答、根拠なし、プロジェクト閾値なしが1つでもあれば`full`とする。公開契約、データ移行、安全境界、依存関係・実行環境・基盤、不可逆操作、曖昧性、複数コンテキストへの影響を発見した`quick`は`full`へ単調昇格し、`full`から途中降格しない。

`quick`のステップ1は目的、対象内外、影響する境界づけられたコンテキストとドメイン不変条件、受け入れ条件、最小Gherkin、確認方法を00へ集約する。両モードでP-01 worktree、P-02 Markdown、P-03 UNIX、P-04 DDD、P-05 BDD、P-06 TDD、P-07 Zero Trustの証拠を残す。

## ステップ0〜11

各ステップの開始前に、次表の対応する`SKILL.md`の全文を読む。成果物名だけから手順やテンプレートを推測しない。

| ステップ | skill contract | 単一責務 | 成果物 |
|---:|---|---|---|
| 0 | [step-00-stage](../skills/step-00-stage/SKILL.md) | モード判定と一時ステージングの原子的開始 | `.agent-skill-chain/tmp/issues/<timestamp>_<title>/` |
| 1 | [step-01-request](../skills/step-01-request/SKILL.md) | 要求定義。`quick`は計画全体を集約 | `00_要求定義.md` |
| 2 | [step-02-requirements](../skills/step-02-requirements/SKILL.md) | ストーリー、受け入れ条件、ドメイン規則の定義 | `01_要件定義.md` |
| 3 | [step-03-requirements-review](../skills/step-03-requirements-review/SKILL.md) | 要求・要件の肯定・敵対レビュー | 修正文書と`04_レビュー.md` |
| 4 | [step-04-issue-sync](../skills/step-04-issue-sync/SKILL.md) | レビュー済み計画を1つの耐久トラッカーへ同期 | 書き込み後読み取り確認済みトラッカー |
| 5 | [step-05-design](../skills/step-05-design/SKILL.md) | 依存関係、安全性、失敗、ロールバックの設計 | `02_設計.md` |
| 6 | [step-06-plan](../skills/step-06-plan/SKILL.md) | 失敗テストと最小実装の計画 | `03_実装計画.md` |
| 7 | [step-07-design-review](../skills/step-07-design-review/SKILL.md) | 設計・計画の肯定・敵対レビュー | 修正文書と`04_レビュー.md` |
| 8 | [step-08-design-sync](../skills/step-08-design-sync/SKILL.md) | 同じトラッカーへ設計・計画を追記 | 書き込み後読み取り確認済みトラッカー |
| 9 | [step-09-implement](../skills/step-09-implement/SKILL.md) | 専用worktreeで実装 | コード、Gherkinテスト、`docs/specs/`更新 |
| 10 | [step-10-review](../skills/step-10-review/SKILL.md) | 実装レビュー、テスト、仕様整合性 | 有限レビューの承認証拠 |
| 11 | [step-11-pr](../skills/step-11-pr/SKILL.md) | PRを作成して停止 | `pr_opened / waiting_for_human_review` |

GitHubがなくても現在のセッションにおけるローカル計画・実装は妨げない。耐久トラッカーがなければ別セッションからの再開を保証しない。リポジトリ直下の`issues/`を新規作成先にしない。

## 成果物用語と責務境界

この節を開発工程で使う統制語彙の正本とする。案件固有の業務用語・略語・ユビキタス言語は各案件の00/01と生成後の`docs/specs/01_システム概要/02_用語・略語.md`が所有し、この節の用語を別の意味へ再定義しない。

| 用語 | 定義 | 含めないもの・境界 |
|---|---|---|
| 要求 | 利害関係者が解決したい問題、実現したい価値、期待する状態を表す上流の意図 | 実現手段を要求として固定しない。ただし法令、契約、期限等の真正な制約は分離して記録する |
| 要件 | 承認済み要求を満たすためにシステムまたは開発成果が充足しなければならない、一意IDを持つ観測・検証可能な条件 | 設計案、実装task、テスト結果を要件として扱わない |
| 制約 | 法令、契約、互換性、環境、authority等によって解空間を制限する変更不能または変更権限が別にある条件 | 期待価値や未確認の前提と混同しない |
| 前提 | 計画時点で真と仮定するが、未成立なら要求・要件・設計を再評価する条件 | 検証済み事実または恒久的制約として扱わない |
| 対象外 | 今回の変更が責任を負わない範囲と、その除外理由 | 未検討、記入漏れ、将来対応という語だけで除外しない |
| 受け入れ条件 | 個々の要件が満たされたかを利害関係者の観点で真偽判定する条件 | Gherkin例やテストコードそのものではない。実行可能な受け入れ例とテスト結果を判定証拠にする |
| 設計 | 要求・要件を満たす責務、境界、依存方向、インターフェース、データ、状態、失敗、復旧への写像 | 要求・要件・受け入れ条件を下流都合で再定義しない |
| 設計判断 | 選択肢、採否、根拠、帰結を伴う具体的な選択 | 長期かつ横断的な判断だけを必要に応じてADRへ分離し、未決事項を決定済みにしない |
| 実装計画 | 承認済み設計を実現する、依存順・失敗テスト・最小変更・検証・仕様更新・rollbackを持つ実行task列 | 新しい要求や設計判断を計画内で暗黙に追加しない |
| 実装 | 設計と計画を具体化するコード、設定、データ、migration等の変更 | 文書上の宣言や未実行の計画を実装済みとしない |
| システム仕様書 | 現在の対象版について、外部振る舞い、構造、データ、非機能、安全、運用等の成立中のシステム契約を保持する耐久的な正本 | 案件内だけの検討経緯、未来のtask、未採用案、実装レビュー記録を混入しない |
| 検証証拠 | 要件・受け入れ条件・仕様への適合を、対象版・SHA・コマンド・結果・観測元へ結び付けた再確認可能な記録 | 実行していない主張、別headの成功、自己申告だけを証拠にしない |
| 正本 | その情報について更新権限と競合時の優先権を持つ一意な情報源 | 同じ契約を複数文書へ複製して独立更新しない |

変更単位の成果物は`要求 → 要件・受け入れ条件 → 設計・設計判断 → 実装計画 → 実装・検証証拠 → レビュー`の順に詳細化する。システム仕様書はこの直列工程の一時成果物ではなく、承認された変更によって更新される現在状態の正本である。下流で上流の不足・矛盾・新事実を発見した場合は、その場で意味を上書きせず、所有する上流成果物へ戻って版と追跡を更新する。`quick`は1ファイルへ集約しても、この論理境界と依存方向を維持する。

## ドメイン用語台帳

業務・システム固有の用語はDDDのユビキタス言語として継続管理する。現在有効な定義の正本は生成後の`docs/specs/01_システム概要/02_用語・略語.md`とし、00は変更で発見した候補語、01は要求・business ruleと対応付けて確定した用語差分を所有する。未実装の候補を現在有効な仕様へ混入させない。

- 作業開始時に既存の用語台帳を読み、既存IDを再利用する。全用語をIssueへ複製せず、追加・意味変更・改名・廃止する差分と、変更で参照する既存IDだけを記録する。
- 用語ID、標準語、定義、種別、境界づけられたコンテキスト、成立例・反例、類義語・禁止表現、根拠となる要求・business rule・一次資料、owner、状態、適用版を追跡する。
- 同じ表記でもコンテキストごとに意味が違う場合は別IDにする。複数コンテキストをまたぐ共有語は各ownerの意味と変換境界を明示し、曖昧な全体共通定義を作らない。
- 状態は`candidate / active / deprecated`とする。`candidate`は要求・要件の検討用で、実装済みの正本ではない。`deprecated`は削除せず、置換先、互換期間、廃止理由を残す。
- 設計、API・CLI、データ名、UI文言、ログ・診断、テスト、運用手順は標準語と用語IDへ整合させる。新語や意味変更を下流で発見した場合は01へ戻し、影響する要件、business rule、仕様、実装、テストを再評価する。
- 実装後は用語差分を耐久用語台帳と仕様変更履歴へ反映し、レビューで未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、置換先なしの廃止、成果物間の表記揺れをfindingにする。

## テンプレート契約

内容成果物を新規作成する各Step skillは、自身の`SKILL.md`から使用する正確なテンプレートへ相対リンクし、作業開始前の全文読取、正規成果物、モードまたは条件による選択を明示する。同期など新しい内容成果物を作らないStepも、直接使用するテンプレートがない理由を明示する。AIによるファイル名の推測や独自構成への置換を契約として扱わず、全12 Stepの対応漏れ、誤対応、リンク切れ、templates境界外参照をpackage buildで拒否する。

## policy拡張と障害時の前進

本システムの主経路は`要求 → domain境界と不変条件 → 実行可能な受け入れ条件 → 最小実装 → risk比例検証 → 独立review → PR`である。policy enforcementはこの経路を止めない薄い支援層であり、UNIXの単一責務と合成可能性、DDD、BDD/TDD、品質担保、AI駆動開発を置き換えない。

成果物の依存方向は`要求 → 設計 → test → 実装 → review → PR`とし、設計時にtopological orderと循環反例を確認する。headが変われば、そのheadに依存するtest・CI・review・PR証拠を失効させる。修正loopは同一scope最大3 review round、単調revision、明示終了条件を持ち、以前の証拠へ逆向きに依存させない。

拡張はpreview、正確な対象、適用直前の再確認、適用、読取確認、rollbackを合成する。candidateはtrusted条件を自己緩和できない。外部障害時はlocalの安全な実装・test・記録を継続し、外部必須操作だけを保留する。詳細な構造とversion契約はschemaと`docs/specs/`を正本とし、この規範文書へ複製しない。

## 提出とライフサイクル

状態遷移は`ステップ10のレビュー → 仕様整合性 → PR → 既定停止または別途許可されたマージ → 完了処理 → 後片付け`とする。`pr create`はマージ・後片付けを連鎖しない。`pr merge`は独立コマンドで、既定ブランチ上の信頼済みポリシーとGitHubの最新状態を検証し、無期限の繰り返し確認をせずGitHub標準の自動マージ・マージキューを優先する。

完了処理はworktree・ブランチ・基点・パス、変更済み・未追跡・stash・一時・無視対象、コミット・push・リモート、PRマージ、仕様、テスト、レビュー、復旧方法を検査する。危険または状態変化時はworktreeを保持する。

新規プロジェクトだけがコードより先に`docs/specs/`を生成できる。既存プロジェクトは明示的な導入操作を要求する。デザイン・レイアウトトークン、Mermaid図、ドメインUUIDは条件付きである。ADRは`docs/adr/`と任意の`related_adrs`という将来拡張点だけとし、サブシステムを実装しない。
