# 要件追跡表

| 要件ID | 受け入れ条件 | SCN ID | テスト層 | Feature | 実装 | 結果・HEAD SHA |
|---|---|---|---|---|---|---|
| REQ-{domain}-001 | AC-{domain}-001 | SCN-... | {project選択test layer、層ごとに1行} | {パス} | {パス} | {結果・SHA} |

`projectChoices.testLayers`で選択した全層を、同じ要件・受け入れ条件・SCN IDに対して1層1行で記録する。固定の層名や固定件数へ読み替えない。

## 共通既定値

- すべての必須要件を、判定可能な受け入れ条件と一意のSCN IDへ対応付ける。
- 仕様更新、レビュー、テスト結果を同じHEAD SHAへ結び付ける。
- `no-spec-impact`は、確認範囲と影響しない根拠を記録した場合だけ使用する。
