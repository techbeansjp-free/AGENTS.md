# データライフサイクル

| データ | 生成 | 利用 | 共有 | 保存 | アーカイブ | 削除 | 法的根拠・同意 |
|---|---|---|---|---|---|---|---|
| {DATA ID} | {条件} | {目的} | {相手} | {期間} | {条件} | {手順} | {根拠} |

```mermaid
flowchart LR
  create_state["{project-selected label: create}"] --> validate_state["{project-selected label: validate}"]
  validate_state --> use_state["{project-selected label: use}"]
  use_state --> archive_state["{project-selected label: archive}"]
  archive_state --> delete_state["{project-selected label: delete}"]
```
