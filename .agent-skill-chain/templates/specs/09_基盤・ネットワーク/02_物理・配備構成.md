# 物理・配備構成

```mermaid
flowchart TB
  region_a["{project-selected label: region A}"] --> zone_a["{project-selected label: zone A}"]
  region_a --> zone_b["{project-selected label: zone B}"]
  zone_a --> service_a["{project-selected label: service A}"]
  zone_b --> service_b["{project-selected label: service B}"]
  service_a --> store_node["{project-selected label: store}"]
  service_b --> store_node
```

| ノードID | 配置 | 役割 | 台数・スケール | 冗長化 | 障害影響 | 復旧 |
|---|---|---|---|---|---|---|
| NODE-001 | {場所} | {役割} | {条件} | {方式} | {範囲} | {方式} |
