# データフロー

```mermaid
flowchart LR
  source_node["{project-selected label: source}"] -->|"DATA-001"| validation_node["{project-selected label: validation}"]
  validation_node --> processing_node["{project-selected label: processing}"]
  processing_node --> store_node["{project-selected label: store}"]
  processing_node -->|"{project-selected label: minimal output}"| destination_node["{project-selected label: destination}"]
```

| フローID | データ | 送信元 | 境界・検証 | 宛先 | 暗号化 | 保持 | 監査 |
|---|---|---|---|---|---|---|---|
| FLOW-001 | {DATA ID} | {元} | {検証} | {先} | 保存時 / 通信時 | {期間} | {証拠} |
