# 論理ネットワーク

```mermaid
flowchart LR
  external_zone["{project-selected label: external}"] --> edge_zone["{project-selected label: edge zone}"]
  edge_zone --> application_zone["{project-selected label: application zone}"]
  application_zone --> data_zone["{project-selected label: data zone}"]
  administration_zone["{project-selected label: administration zone}"] --> application_zone
```

| 通信ID | 送信元 | 宛先 | 方向 | プロトコル・ポート | 認証・暗号化 | 用途 | 既定拒否 |
|---|---|---|---|---|---|---|---|
| NET-001 | {区画} | {区画} | inbound / outbound | {契約} | {方式} | {用途} | はい |
