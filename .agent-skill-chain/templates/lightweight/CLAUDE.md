# CLAUDE.md

本プロジェクトは agent-skill-chain を軽量プロファイル（`profile: lightweight`）で導入している。`AGENTS.md` はroot直下に存置されているが、本ファイルからの常時importは行わない。`AGENTS.md` の規範・`.claude/skills/` 配下のスキル群はオンデマンド参照（AIエージェントが必要と判断した時、または利用者が明示的に呼び出した時）の対象である。

応答は日本語とする。
