# 04 レビュー

> 肯定と敵対の両観点を毎ラウンド確認する。指摘なしの承認は有効。Medium/Lowは記録するだけで、自動修正・追加ラウンド・ゲート停止を起こさない。用語と責務の境界は`成果物用語と責務境界`（`.agent-skill-chain/docs/01_開発ワークフロー.md`）を正本とする。
>
> 埋めた成果物は`docs/reviews/`へ置き、実装commitの後にその1 fileだけをcommitする。

| 読者 | 読む節 |
|---|---|
| 発注・評価する人 | 要約 → §5 指摘 → §11 総合判定 |
| 実装・レビューする人 | 全節 |
| 運用する人 | 要約 → §8 配布物影響 → §10 仕様整合性 |

## 要約

**この5行だけで、何を見たreviewかが分かるように書く。** 各行は1〜2文。詳細は対応する節が持つ。

| 項目 | 内容 |
|---|---|
| 何が問題だったか | （利用者・運用から見た不具合または不足。§1の要求・受け入れ条件へ対応） |
| 何を解決しようとしたか | （この変更が達成する状態。達成しない範囲があれば併記） |
| 何を行ったか | （主要な変更を2〜3件。§1.1の個別監査が全件を持つ） |
| 何を確認したか | （合否を決めた観測。実行したtestと反例の種類。§3・§4・§7が詳細を持つ） |
| 判定 | approved / rejected（§11と一致させる。未解決Critical/Highがあればrejected） |

## 1. 入力証拠

PR番号、Actions run ID、immutable review IDはPR作成後にしか存在しない。**この文書へ書かない。** `review evidence`とdelivery stateがappend-onlyで保持する。

reviewerの独立性は`merge.reviewIndependence`が決める。既定の`context-isolated`はimplementerと別session/context、exact HEAD固定、対象差分の非変更、肯定・敵対reviewとfinding記録を要求し、同一GitHub actorでも成立する。このときtracked artifactの`approved`と保存済みreview session・Step 10 bindingがformal approvalになる。`actor-independent`はPR authorおよび観測済み`H_impl` commit authorと別のstable actor IDによるprovider `APPROVED`を要求する。両modeともexact HEAD一致は必須とする。tracked文書へ自身のcommit SHAを書かず、**H_final後はartifactを更新しない。**

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | （URL・ID） | （要約） | 人間判断 / 実行観測 / 既存コード / 一次資料 / テスト出力 / 推論 |
| 差分 | （SHA・URL） | （要約） | 既存コード |
| テスト | （コマンド・CI） | （件数） | テスト出力 |
| 仕様 | （更新先） | updated / no-spec-impact | 既存文書 |
| commit前candidate | （path manifest） | （candidate tree SHA） | Git index |
| Phase A artifact | （evidence-only path） | （SHA-256 / blob OID / H_impl..H_final paths） | Git観測 |
| review session | （staging path） | （session ID / anchor / round） | Git観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない:
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである:
- reviewerの独立性が要求水準を満たす:
- 既定branch追随を行った場合、取り込みがartifact commitより前にあり、`比較基点`が取り込んだ既定branch tip、`H_impl`がartifact直前の最新commitを指し、個別監査表を`比較基点..H_impl`から再生成した:

### 1.1 変更ファイル個別監査

全ファイルを1ファイル1行で記録する。まとめ行、directory単位の一括承認、test成功だけによる代替を認めない。版管理下の生成物（`dist/`等）は`audit:check`が照合対象から外すため本表に載せず、生成元を監査して配布影響は§8で判定する。

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| （repository相対path） | A / M / D / R | （owner） | （target layer） | （根拠） | pass / finding（根拠） | （追跡先） | （境界・復旧） | pass / finding |

- 基準SHAとの差分path集合と表のpath集合が完全一致する:
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない:
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した:

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

発見IDは実装計画（fullは03、quick/pocは集約00）の`DISC-*`と同じ字面を使い、本文書内で別IDへ言い換えない。

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | （観測事実） | （境界・AC） | なし / 種別 | （対処） | （再現可能な証拠） | updated / no-spec-impact | pass / finding |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-... | （箇所） | （結果） | pass / finding / not-applicable | （参照） |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable / not-applicable | （範囲を限定した理由） | （脅威、個人情報・秘密、保持・削除、SCN） |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable / not-applicable | （範囲を限定した理由） | （伏字化、相関、権限、保持、rotation、監視、復旧結果） |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | applicable / not-applicable | （範囲を限定した理由。CLIでも「JSON出力のみ」を非適用の根拠にせず、画面契約とa11y検証の対象を持たないことを書く） | （利用task、状態・error、a11y検証または非UI証拠） |
| DC-TOKENS | Design System・Design/Layout Token | applicable / not-applicable | （範囲を限定した理由） | （token、component state、breakpoint、layout検証または非該当証拠） |

## 3. 肯定的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ（要件と観測結果の一致） | pass / finding / not-applicable | （根拠） |
| 価値（利用者・運用上の目的） | pass / finding / not-applicable | （根拠） |
| 実現可能性（環境・依存・権限） | pass / finding / not-applicable | （根拠） |
| 整合性（設計・コード・テスト・仕様） | pass / finding / not-applicable | （根拠） |
| 保守性（責務・命名・変更容易性） | pass / finding / not-applicable | （根拠） |

## 4. 敵対的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 反例（要件を破る入力・状態） | pass / finding / not-applicable | （根拠） |
| 失敗経路（外部失敗・部分失敗） | pass / finding / not-applicable | （根拠） |
| 境界値（空、最大、最小、重複、Unicode） | pass / finding / not-applicable | （根拠） |
| 悪用（注入、経路脱出、権限外） | pass / finding / not-applicable | （根拠） |
| 安全性（認証、承認、秘密情報、Zero Trust） | pass / finding / not-applicable | （根拠） |
| データ損失（上書き、削除、部分公開、履歴消失） | pass / finding / not-applicable | （根拠） |
| ロールバック（復旧参照、状態保持、再開可能性） | pass / finding / not-applicable | （根拠） |
| 範囲漏れ（呼び出し元、利用側、配布物、文書） | pass / finding / not-applicable | （根拠） |

## 5. 指摘

指摘なしの場合は「指摘なし」と明記する。

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| H-01 | Critical / High / Medium / Low | （内容） | （再現・参照） | （範囲） | （修正・縮小・対象外） | valid / resolved / duplicate / false-positive / out-of-scope | （内容） |

## 6. ラウンド固有の確認

実施したラウンドの項目だけを残す。

### ラウンド1

- 全評価基準を確認した:
- 指摘を確定した:
- 次ラウンド対象のCritical/High:

### ラウンド2

- 未解決Critical/High:
- 修正差分と、触れた隣接範囲:
- 既承認・未変更範囲を再走査していない:

### ラウンド3

- 全指摘の最終分類:
- 危険範囲を除外・既定無効・ロールバック可能へ縮小した結果:
- 同じ範囲の予算を自動更新していない:

## 7. テスト結果

手書きの件数表だけを承認証拠にしない。local実行の観測として次を書く。成功CI runはPR作成後にしか観測できないため`review evidence`が記録する。

- 実行したcommandの一覧:
- 全layerの合計（シナリオ数、成功、失敗、スキップ）と、失敗が0件であること:
- 失敗またはskipがある層だけを`projectChoices.testLayers`の順に1層1行で展開:
- runnerと`projectChoices.gherkinDialect`:

## 8. 配布物影響

packageとして配布する場合だけ記入する。配布境界はpackage manifestの配布file指定を正本とし、compileされて配布される`source`も含める。

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| {パス} | 入る / 入らない | {利用者に見える変化。なければ「なし」} |

判断: 配布物を更新した
判断: 配布物を更新しない

{上のどちらか一方の行だけを残す。`判断:`の行は1件だけにする}

根拠: {更新した場合は更新先。更新しない場合は、runtimeの利用条件が変わらない理由。`根拠:`の行も1件だけにする}

## 9. 独立reviewの成立

PR作成前に観測できるものだけを書く。immutable review IDやapproval件数は書かない。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated / actor-independent |
| その要求を満たすこと | はい / いいえ |
| reviewerとimplementerのidentity・context比較 | {実体の観測値。checkの結論を書かない} |
| reviewerが対象差分を変更していないこと | はい / いいえ（変更したpath集合の観測値） |

外部への不可逆な配布で独立reviewの外部証拠を要求され、かつそれが無い場合だけ次を記入する。承認元、承認者、承認日時、失効日時は正本を参照し、ここへ複製しない。

| 項目 | 内容 |
|---|---|
| 適用する例外の識別子 | {正本fileのexceptionId} |
| 観測値 | {未実行と判定した根拠の実測値} |

## 10. 仕様整合性

- 判定: updated / no-spec-impact / unknown
- 更新した仕様:
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる:
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない:
- 要件・変更・SCN・テストの追跡:
- `no-spec-impact`の場合の限定的根拠:
- UI・トークンの判断:

## 11. 総合判定と再開地点

- 未解決Critical/High:
- Medium/Lowの記録:
- 判定: approved / rejected
- 新しい権限が必要な事項:
- 残存リスク:
- 次に許可される操作:
- 次回の再開地点:

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 要求・要件 / 設計・計画 / 実装 |
| ラウンド | 1 / 2 / 3 |
| 対象SHA・文書ダイジェスト | （正確な値） |
| 比較基点 | （40桁SHA） |
| H_impl | （40桁SHA） |
| 対象差分 | （パス、コミット、URL） |
| 対象外 | （範囲と理由） |
| 残り予算 | （同一範囲で最大6 counted round。収束後のHEAD移動に対する取り直し2 counted roundは別枠。Git検証済みのfollow-only roundは記録するが数えない） |
| ラウンド数 | （実施したラウンド数。先頭を整数にする） |
| Step chain | （`経由: <staging path>` または `迂回: <理由>`） |
| 仕様の所有箇所 | （着手時に読んだ仕様の正本と引用。無い場合は`該当なし: #<Issue番号>`） |
| 成果物行数 | （製品の変更行数と支援層の行数。閾値判定はしない） |
| 縮小の先行評価 | （既存手段の流用・縮小で足りない理由。「評価していない」を残さない） |
| 実施者・日時 | （役割、ISO 8601形式） |

`比較基点`と`H_impl`の値は40桁の小文字hexをbacktickで囲んだものだけにする。注記・branch名・短縮SHAを同じcellへ書かない。由来は別行へ書く。

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | （肯定・敵対review、finding分類） | （risk・scopeからの決定値） | （provider上限） | （tier mappingの観測値） | （未解決時の停止・再開条件） | （reviewerとimplementerのidentity・context比較、対象差分path不変） |
