# 04 レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 要求・要件 / 設計・計画 / 実装 |
| ラウンド | 1 / 2 / 3 |
| 対象SHA・文書ダイジェスト | （正確な値） |
| 比較基点 | （SHA・版） |
| 対象差分 | （パス、コミット、URL） |
| 対象外 | （範囲と理由） |
| 残り予算 | （同一範囲で最大3ラウンド） |
| ラウンド数 | （実施したラウンド数。先頭を整数にする。注記は続けてよい） |
| Step chain | （`経由: <staging path>` または `迂回: <理由>`。記録であり内容は検証しない） |
| 仕様の所有箇所 | （着手時に読んだ仕様の正本と引用。無い場合は`該当なし: #<Issue番号>`で仕様側の欠落の起票先を示す） |
| 成果物行数 | （製品の変更行数と支援層の行数。閾値判定はせず記録だけを残す） |
| 縮小の先行評価 | （既存手段の流用・縮小で足りない理由。「評価していない」を残さない） |
| 実施者・日時 | （役割、ISO 8601形式） |

### 0.1 routing入力契約

providerとmodel設定はproject choiceとrouting evidenceの観測値を用い、固有のmodel slugだけからreview authorityを推測しない。

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | （肯定・敵対review、finding分類） | （risk・scopeからの決定値） | （provider上限） | （tier mappingの観測値） | （未解決時の停止・再開条件） | （reviewerとimplementerのidentity・context比較、対象差分path不変） |

## 1. 入力証拠

レビュー証拠は二段階で完成させる。製品・仕様・testを固定した`H_impl`の後、Phase Aではtracked review artifactだけを加えた`H_final`を作る。`H_impl`は`H_final`のancestor、commit間差分はproject policyが選ぶevidence-only pathだけとし、artifact path、SHA-256、Git blob OIDを観測する。trusted providerから`H_impl` commit SHA/author ID、repository、PR number/current head/author ID、Actions run ID/event/head/conclusion/関連PR番号、immutable review ID/commit/user ID/submittedAt/stateを実観測する。PR・成功CI・承認reviewは`H_final`へ一致させ、reviewerはPR authorとobserved implementation commit authorのどちらとも異なることを要求する。caller申告actor、任意JSON、別PRのrun、COMMENTED、未完了CI、自己reviewは承認証拠にしない。tracked文書へ自身のcommit SHAを書かず、H_final後はartifactを更新しない。完了はtrusted providerの外部attestationだけで記録し、head変更時は外部証拠を作り直す。

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | （URL・ID） | （要約） | 人間判断 / 実行観測 / 既存コード / 一次資料 / テスト出力 / 推論 |
| 差分 | （SHA・URL） | （要約） | 既存コード |
| テスト | （コマンド・CI） | （件数） | テスト出力 |
| 仕様 | （更新先） | updated / no-spec-impact | 既存文書 |
| commit前candidate | （path manifest） | （candidate tree SHA） | Git index |
| Phase A artifact | （evidence-only path） | （SHA-256 / blob OID / H_impl..H_final paths） | Git観測 |
| commit後external | （trusted providerのPR / CI run / immutable review ID） | （repository、H_final、event、run/review ID、actor ID、提出日時、verdict） | 外部のimmutable証拠 |

重要判断を推論だけで承認しない。新しい権限が必要な場合は、対象操作と決定権者を明示する。

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない:
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけであり、trusted providerが観測したPR/CI/reviewが`H_final`へ一致している:
- reviewer stable IDがPR author/provider観測済み`H_impl` author stable IDと異なる:
- 既定branch追随を行った場合、取り込みがreview artifact commitより前にあり、`比較基点`が取り込んだ既定branch tip、`H_impl`がartifact直前の最新commitを指し、個別監査表を`比較基点..H_impl`から再生成した:

### 1.1 変更ファイル個別監査

追加・変更・削除した全ファイルを省略せず1ファイル1行で記録する。まとめ行、directory単位の一括承認、test成功だけによる代替を認めない。

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| （repository相対path） | A / M / D / R | （owner） | package / project / spec / evidence | （根拠） | pass / finding（根拠） | （追跡先） | （境界・復旧） | pass / finding |

- 基準SHAとの差分path集合と表のpath集合が完全一致する:
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない:
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した:

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

モード別実装計画（fullは03、quickとpocは集約00）の発見記録を全件転記せずIDで参照し、最終状態とEvidenceを確認する。目的、scope、受け入れ条件、security境界、不可逆操作を変えなかった発見は上流再起動を要求しない。契約を変えた発見は、影響する成果物だけが再確定されていることを確認する。

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | （観測事実） | （境界・AC） | なし / 種別 | （対処） | （再現可能な証拠） | updated / no-spec-impact | pass / finding |

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-... | （箇所） | （結果） | pass / finding / not-applicable | （参照） |

### 2.1 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable / not-applicable | （範囲を限定した理由） | （脅威、個人情報・秘密、保持・削除、SCN） |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable / not-applicable | （範囲を限定した理由） | （伏字化、相関、権限、保持、rotation、監視、復旧結果） |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | applicable / not-applicable | （範囲を限定した理由） | （利用task、状態・error、a11y検証または非UI証拠） |
| DC-TOKENS | Design System・Design/Layout Token | applicable / not-applicable | （範囲を限定した理由） | （token、component state、breakpoint、layout検証または非該当証拠） |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass / finding / not-applicable | （根拠） |
| 価値 | 利用者・運用上の目的を満たすか | pass / finding / not-applicable | （根拠） |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass / finding / not-applicable | （根拠） |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass / finding / not-applicable | （根拠） |
| 保守性 | 責務、命名、変更容易性が妥当か | pass / finding / not-applicable | （根拠） |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass / finding / not-applicable | （根拠） |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass / finding / not-applicable | （根拠） |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass / finding / not-applicable | （根拠） |
| 悪用 | 注入、経路脱出、権限外操作等 | pass / finding / not-applicable | （根拠） |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass / finding / not-applicable | （根拠） |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass / finding / not-applicable | （根拠） |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass / finding / not-applicable | （根拠） |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass / finding / not-applicable | （根拠） |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| H-01 | Critical / High / Medium / Low | （内容） | （再現・参照） | （範囲） | （修正・縮小・対象外） | valid / resolved / duplicate / false-positive / out-of-scope | （内容） |

指摘なしの場合は「指摘なし」と明記する。

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した:
- 指摘を確定した:
- 次ラウンド対象のCritical/High:

### ラウンド2

- 未解決Critical/High:
- 修正差分:
- 修正で触れた隣接範囲:
- 既承認・未変更範囲を再走査していない:

### ラウンド3

- 全指摘の最終分類:
- 任意の危険範囲を除外・既定無効・ロールバック可能へ縮小した結果:
- 同じ範囲の予算を自動更新していない:
- AIによる最終裁定:

## 7. テスト結果

`projectChoices.testLayers`で選択された各層を、選択順に1層1行で記録する。runnerと`projectChoices.gherkinDialect`も記録し、固定の層名・件数・構造keywordへ読み替えない。

| 層・検査 | コマンド | シナリオ・件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---:|---:|---:|---:|---|
| 形式 | （コマンド） | （数） | （数） | （数） | （数） | pass / finding |
| {project policyの選択層} | （コマンド） | （数） | （数） | （数） | （数） | pass / finding |
| 型・既存一式・配布物 | （コマンド） | （数） | （数） | （数） | （数） | pass / finding |

## 8. 配布物影響

projectがpackageとして配布される場合だけ記入する。配布境界はpackage manifestの配布file指定を単一正本とし、compileされて配布される`source`も配布境界に含める。

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| {パス} | 入る / 入らない | {利用者に見える変化。なければ「なし」} |

判断: 配布物を更新した
判断: 配布物を更新しない

{上のどちらか一方の行だけを残し、他方は削除する。`判断:`の行は1件だけにする}

根拠: {更新した場合は更新先。更新しない場合は、runtimeの利用条件が変わらない理由。`根拠:`の行も1件だけにする}

**runtimeの挙動が変わるのに配布文書が追随していない状態を残さない。** 利用者は配布物だけを読むため、repository内部の仕様書へ書いても届かない。

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | あり / なし |
| reviewerがPR author・実装commit authorと異なる | はい / いいえ |
| 観測したreview commentとapprovalの件数 | {実体の観測値。checkの結論を書かない} |

外部証拠が無い場合だけ記入する。

| 項目 | 内容 |
|---|---|
| 適用する例外の識別子 | {正本fileのexceptionId} |
| 観測値 | {未実行と判定した根拠の実測値} |

**承認元、承認者、承認日時、失効日時は正本を参照し、ここへ複製しない。**

例外が正本に無い、または失効している場合は進めない。記録があれば実装・PR・mergeは進み、mergeを契機に自動で走るtag作成とReleaseも進む。止めても不可逆な行為を防げないためである。package registryへの公開など外部への不可逆な配布だけが、記録の有無にかかわらず独立reviewの外部証拠を要求する。削除、force push、履歴書き換えは独立reviewの対象外であり、既存のauthority経路で守る。

## 10. 仕様整合性

- 判定: updated / no-spec-impact / unknown
- 更新した仕様:
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる:
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない:
- 要件・変更・SCN・テストの追跡:
- `no-spec-impact`の場合の限定的根拠:
- UI・トークンの判断:

## 11. 総合判定と再開地点

- 未解決Critical/High:
- Medium/Lowの記録:
- 判定: approved / rejected
- 新しい権限が必要な事項:
- 残存リスク:
- 次に許可される操作:
- 次回の再開地点:
