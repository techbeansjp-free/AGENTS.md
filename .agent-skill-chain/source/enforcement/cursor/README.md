# enforcement/cursor — Cursor 用ルールの正本

**配置するもの**: 規約要約・「必ず CORE/LOAD_POLICY を読む」等のルール。Cursor が読み込む形（.mdc や .md 等、プロジェクトで定めた形式）。

**展開先**: setup 実行後は .cursor/rules/ 等へコピーされる。

**責務**: 入口での読了義務の明示。違反経路の遮断は claude/ と同様の方針で行う。
