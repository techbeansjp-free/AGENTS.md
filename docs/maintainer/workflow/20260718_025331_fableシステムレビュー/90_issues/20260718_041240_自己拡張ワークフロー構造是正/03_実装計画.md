---
document_id: "e6104693-9cf3-475a-9736-776343aad11d"
issue_id: "6c0620df-488e-40dd-bc30-f0ecdf9f587b"
---

# 実装計画書: 自己拡張ワークフロー.md の構造是正（G-1〜G-12）

**前のステップ**: [02_設計.md](./02_設計.md)
**実装先**: 別 worktree `chore/20260718_092843-自己拡張ワークフロー構造是正`（本ファイルを保存する worktree とは別）の `.agent-skill-chain/project/自己拡張ワークフロー.md`

---

## 1. 実装方針

02_設計.md §2（確定6件の具体改訂差分）・§3（要設計判断6件の推奨案）に従い、G-1〜G-12 全12件を対象ファイル1件のみに実装した。要設計判断6件（G-2, G-4, G-5, G-8, G-9, G-10）はいずれもユーザー承認済みの推奨案（**文書内の矛盾是正・索引/一覧表追加・申し送り明記に限定し、settings.json 変更・新規スクリプト・ファイル分割等の機構変更は行わない**）どおりに実装した。

## 2. 指摘ID別の実装内容

| ID | 実装内容 |
|---|---|
| G-1 | 行18（issue作成場所）・close移動記述・「理由（名前空間の役割分担）」表の該当行・footnote の計4箇所を、「00〜04の追跡可否は実効Issue追跡モード依存」という条件付き記述に全面改訂。加えて §memo の作成場所 内の「git 追跡する成果物は00〜04のみ」という無条件断言も同様に是正（自己レビューで検出・追加是正、下記4.参照）。 |
| G-2 | 「## ブランチ紐づけ・PR紐づけの機械強制」冒頭に「機械強制の実効性一覧（Tier×設計上の強制力×本リポ現在の実効性）」表を新設。ブロッキング化自体は見送り（§3.5 の申し送りへ誘導）。 |
| G-3 | bare `ADR-n` のうち衝突が実測確認された ADR-1・ADR-2・ADR-4・ADR-5 の一部を、トピック接尾辞で一意化（例: `ADR-5-配置境界` と `ADR-5-照合粒度`）。全 bare ADR 引用（衝突有無を問わず）を「詳細な検討経緯は `<ID>` を参照」という補足参照フレーズへ統一し、close/ 未読でも本文が自己完結するようにした。 |
| G-4 | §2.5-3（全件起票後の一括突合）に、`gh issue list` と frontmatter grep を使った実行可能な bash コマンド例を追加。専用スクリプトの新設はしない（要否は別 issue）。 |
| G-5 | R8 先行有効化は見送り（§0.3(e) は不変）。§0.1 の削除前目視確認を「①`git status --ignored` 実行 → ②重要成果物の不在確認 → ③DECISIONS.md転記済み確認 → ④削除実行」の番号付き必須チェックリストへ格上げ。 |
| G-6 | ブランチ名記載例3箇所（`feat/some-feature`・`feat/issue-branch-pr-linkage-enforcement`・`chore/close-move-<日付>`）を §0.1 準拠形式（`feature/20260716_143000-some-feature` 等）に統一。 |
| G-7 | 「本ファイル §268 の記録手順」を、実際の該当見出し（§非追跡化・スイッチ投入・DECISIONS 記録の正当性）への見出しアンカーリンクへ置換。 |
| G-8 | close/ deny 設定の推奨形態（Read許可・Grep/Glob/Edit/Write は deny 維持）の記述は維持。「既知リンク切れ約1,060件の追跡issue起票要否・settings.json変更要否はいずれも別issue／保守者判断」という申し送りを明記。 |
| G-9 | ファイル冒頭に「目次（トピック索引）」を新設。全 H2 見出し（9節）へのアンカーリンクと1行要約を掲載。分割は今回実施せず、別issue検討の申し送りを明記。 |
| G-10 | 分岐C（github_native close移動）の「経路（追跡状態でファイル単位に分ける）」を、追跡状態→経路→実行手段の決定表に整理。close-move-issue.sh の改修は行わず、経路自動選択の寄せ込みは別issue申し送りとして明記。 |
| G-11 | 表示テキストとhrefの不一致を機械的に一括是正（`../.agent-skill-chain/source/X` → `../source/X` 相当のパターン8箇所、DECISIONS.md表示3箇所、冒頭README.mdリンク1箇所、末尾参照2箇所）。 |
| G-12 | §2.5 末尾に「役割の明確化（github_native前提）」の注記ブロックを追加。frontmatter書き戻し＝ローカルゲート用の一時記録、恒久トレーサビリティ＝Issue本文＋PRのClosesが正本、と役割分離を明記。 |

## 3. 実装手順（実施順）

1. 対象ファイル全文を Read し、02_設計.md の変更前→変更後の文案・行番号を現行の見出し・文字列で再特定した（設計時点の行番号はドラフト編集でずれるため、02_設計.md 自身の指示どおり見出し／文字列ベースで照合）。
2. G-9（目次索引）を最初に追加し、以降の編集で増える見出しへのアンカーも索引に反映されるようにした。
3. G-1（4箇所）→ G-3（ADR一意化・全箇所）→ G-4/G-5/G-6/G-7/G-12 → G-2（一覧表新設）→ G-10（決定表）→ G-8（申し送り追記）→ G-11（機械的一括置換）の順で実装した。
4. 全編集後、ファイル全文を再度 Read し、既存記述（非追跡ドラフト前提の記述群）との整合をセルフレビューした（詳細は 04_review.md）。
5. 実装先 worktree で `git add` / `git commit` を実行（push・PR作成はしない）。

## 4. 設計からの差分（自己レビューで追加是正した箇所）

02_設計.md の自己レビュー（§5）が対象としていた既存記述箇所（168・190・192・247・261-263・275・279・314・348 等、設計時点の行番号）に加え、実装時のセルフレビューで以下を追加検出・是正した。

- `## memo の作成場所` 節末尾の「git 追跡する成果物は issue ドキュメント（`00`〜`04`）のみである」という一文が、G-1 適用後の「追跡可否はモード依存」という原則と矛盾していた（memo との対比のみを意図した断言だったが、文言上は 00〜04 の無条件追跡を主張していた）。「memo が git 追跡対象になることはないこと」と「00〜04 自体の追跡可否はモード依存であること」を分離する形に是正した。

この追加是正は G-1 のスコープ内（追跡ポリシーの正確な記述への統一）であり、対応方針・スコープの変更は伴わない。

## 5. 変更しなかったもの（スコープ境界の再確認）

- `.claude/settings.json`（close/ deny 設定）: 変更なし。
- 新規スクリプトファイル: 作成なし。
- `project/自己拡張ワークフロー.md` のファイル分割: 実施なし（単一ファイルのまま）。
- 03_実装計画.md・04_review.md 以外の親 issue 側ファイル: 変更なし。

---

**次のステップ**: [04_review.md](./04_review.md)
