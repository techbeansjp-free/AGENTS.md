---
document_id: "8e154871-9e8a-4c02-bcdd-07a85b36f8bf"
---

# 03_実装計画: 台帳・記録(ledger/scribe)の整合強化

**前のステップ**: [02_設計.md](./02_設計.md)
**対象**: 指摘 19 件（D-1〜D-7, D-9, D-10, D-13〜D-16, D-18, E-2, E-3, E-10, E-14, E-20）
**実装先**: `.worktree/bugfix/20260718_092843-台帳記録整合強化/`（main から新規ブランチ）

> 02 設計の推奨案（entry_hash の `hash_version` 列による新旧併存・DB パス宣言の文言修正のみ・dod_met の一律強化はしない）はユーザー承認済み。本計画はファイル重複を避けた実装順序と検証を確定する。

---

## 1. 実装順序（ファイル重複回避・依存順）

02 §7 の 5 系統を、ファイル競合を避けて次の順で実施した。

1. **entry_hash v2 系（D-1・E-2・E-3）＋スクリプト堅牢化系（E-14・E-20・D-18）**: スクリプト 3 本を先に確定。
   - `scripts/gen-entry-hash.sh`: v1 を不変のまま温存し、`gen_entry_hash_v2`（20 カラム・バイト長プレフィックス枠付け・LC_ALL=C）を新設。ヘッダに v1/v2・hash_version 分岐・利用側役割を明記。
   - `scripts/write-workflow-log.sh`:
     - 生成を `gen_entry_hash_v2` へ切替、INSERT に `hash_version=2` を明示付与（E-2/E-3）。
     - `ensure_column` に索引省略オプション（第 3 引数 `noindex`）を追加。tier_rationale/tier_exception を noindex 化、`hash_version` を noindex で追加（D-18・E-2）。
     - 再作成 heredoc（review-docs/create-pr-review-issue）の CREATE / INSERT...SELECT に `hash_version` を追随、tier 索引 2 本を削除（E-2・D-18）。
     - heredoc を `if ! sqlite3 ...; then エラー; exit 1; fi` 形式に変更しデッドコードを解消（E-14）。
     - `to_json_array` の値エスケープを `json_escape_str`（awk・LC_ALL=C・制御文字 \b\t\n\f\r＋残余 C0 の \u00XX）へ差し替え（E-20）。
     - ヘッダ 11 行・コメント 138 行の「DB パス固定」宣言を resolve-wf-db.sh の実挙動（ADR-132-1 の意図的 worktree 横断上書き）に正確化（E-10）。
   - `scripts/resolve-wf-db.sh`: 変更なし（E-10 の説明対象。override は維持）。
2. **スキーマ正本（schema.sql）**: `hash_version INTEGER NULL`（DEFAULT なし）を追加、tier_rationale/tier_exception の CREATE INDEX を削除（model_tier 索引は維持）、document_id に NULL 許容の意図コメント（E-2・D-18・D-2）。
3. **正本一元化系（D-2・D-3・D-4・D-7・D-13）＋二層記述（D-5・D-6・D-1）**: `ledger/schema.md` を再構成（逐語 CREATE 写し・旧スキーマ SQL・memo_ref SQL を削除し参照へ縮退。列説明・command 別必須表・移行手順は維持。actor_role/delegated_by_role を不変条件固定と再定義、dod_met 自己申告を明記、entry_hash v1/v2 と検証経路・劣化境界を記述、INSERT OR IGNORE 非推奨化、hash_version を列一覧へ追加）。
   - `scribe/CONTRACT.md`: document_id を「新規記録で必須／DB は NULL 許容」に正確化、memo_ref 言及を削除、created_at の `datetime('now')` 記述を是正、必須キー表を schema.md 正本参照へ縮退。
   - `scribe/README.md`: 独自必須キーリストを schema.md 正本参照へ。
   - `ledger/README.md`: 列名 `changed_files`→`changed_files_json`、schema.md の CREATE TABLE 参照を schema.sql 参照へ是正（D-13 整合）。
4. **ポリシー整合系（D-9・D-10・D-14・D-16）**: `REVIEW_RULE.md`（外部根拠の inference_only フォールバック・徹底調査の深度スケール）、`CODE_COMMENT_RULES.md`／`DOCS_NOISE_RULES.md`（ドッグフーディング固有パスを `.agent-skill-chain/project/` 上書き参照の抽象表現へ／実在しない R-5 を「手順 2」へ張替え）。
5. **二層表記系（D-15）**: `HEARTBEAT.md`（各チェック項目見出しへ `[機械強制]`/`[推奨・自己規律]` タグ付与＋末尾に二層区分節を新規追加。トリガー文言 3 行・使用タイミング 43-47 行は #143 領域のため不変）、`CLOSEOUT.md`（凡例節＋全 ## 見出しへタグ付与）。

## 2. 変更ファイル（12 件）

| ファイル | 対応指摘 |
| --- | --- |
| scripts/gen-entry-hash.sh | D-1, E-2, E-3 |
| scripts/write-workflow-log.sh | E-2, E-3, E-10, E-14, E-20, D-18 |
| ledger/schema.sql | E-2, D-18, D-2 |
| ledger/schema.md | D-1, D-2, D-4, D-5, D-6, D-7, D-13 |
| scribe/CONTRACT.md | D-2, D-3, D-7 |
| scribe/README.md | D-3 |
| ledger/README.md | D-3, D-13(整合) |
| REVIEW_RULE.md | D-9, D-10 |
| CODE_COMMENT_RULES.md | D-14 |
| DOCS_NOISE_RULES.md | D-14, D-16 |
| HEARTBEAT.md | D-15 |
| CLOSEOUT.md | D-15 |

**未編集の target 3 件**: `resolve-wf-db.sh`（E-10 の説明対象・override 維持）、`EVIDENCE_POLICY.md`（D-9 が参照する §節4 は既存）、`DOCS_RULES.md`（D-16 が参照する継続追随ゲート手順 2 は既存）。いずれも参照・説明対象で編集不要。

## 3. クロスパッケージ依存（本パッケージ非所有・follow-up）

02 §5 のとおり、entry_hash v2 の外部検証実効化には次の追随が別パッケージで必要:
1. `scripts/export-ndjson.sh` の全カラム（model_tier/tier_rationale/tier_exception/hash_version）出力（ハード依存）。
2. `enforcement/ci/audit.sh` のチェーン検証 or doctor/verify 新設（gen-entry-hash.sh を source し hash_version 別に v1/v2 再計算）。
3. D-14 の enforcement grep 側の固有パス検出の project/ 移設（ソフト依存）。

## 4. テスト観点（04_review で実測）

- SQL: schema.sql の CREATE／hash_version 付き INSERT／新旧スキーマ移行の実挙動。
- シェル: `bash -n` 構文・entry_hash v2 の決定性（locale 非依存）・往復検証（DB 再構成→再計算一致）・prev_hash チェーン・JSON 制御文字エスケープ。
- 整合: schema.sql↔schema.md↔write-workflow-log.sh の列・索引・必須キーの一致。
