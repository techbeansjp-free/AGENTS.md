---
document_id: "3c779b0e-e413-47a2-8760-ee672e63b06f"
---

# レビュー書: audit.sh 残骸・マーカ系チェックの散文誤検知（偽陽性）是正

**プロジェクト名**: audit.sh 残骸・マーカ系チェックの散文誤検知（偽陽性）是正
**作成日**: 2026 年 06 月 16 日
**最終更新**: 2026 年 06 月 16 日

> **重要**: 本レビューは verify-and-close（実装完了後レビュー）の成果物。深度は **standard**（[RULES.md §実行モード](../../../../.agents/RULES.md)）。二観点（[REVIEW_DUAL_LENS.md](../../../../.agents/REVIEW_DUAL_LENS.md)）＝敵対的観点＋must-preserve を必須記載する。
>
> **用語**: [.agents/CONCEPTS.md §用語規約](../../../../.agents/CONCEPTS.md#用語規約) を参照。

---

## 1. レビュー概要

### 1.1 レビュー目的（必須）

実装内容の確認・品質保証・クローズ前最終チェック。#7（重要パスに TODO/FIXME 残存）の三段正規化（フェンス除去→インライン除去→構文アンカー判定）への差し替えが、設計（02）・実装計画（03）どおりで、偽陽性除去（SC1/SC4）と偽陰性ゼロ（SC2）を両立し、既存ガード・他チェック・I/F を不変に保つことを検証する。

### 1.2 レビュー対象（必須）

- **実装範囲**: (1) `.agents/enforcement/ci/audit.sh` #7（296-334 行）の検知ロジックを三段判別（(a) awk フェンス除去・未閉フォールバック保険 → (b) sed インライン除去 → (c) `LC_ALL=C grep -qE '(TODO|FIXME)[[:space:]]*[:：(]'`）へ差し替え。(2) `test/test-audit.sh` に回帰ケース C-SC1／C-SC2a–e／C-SC4a／C-SC4b／C-guard／C-edge を追加。
- **レビュー期間**: 2026-06-16 ～ 2026-06-16
- **レビュー担当者**: 検証・クローズ worker（verify-and-close）

---

## 2. 実装内容の確認

### 2.1 実装完了タスク

| タスク名 | 実装内容 | 実装日 | 担当者 | ステータス |
| -------- | -------- | ------ | ------ | ---------- |
| T1 回帰テスト追加 | test-audit.sh に SC1-4・guard・edge の回帰ケースを追加 | 2026-06-16 | 実装サブ | 完了 |
| T2 #7 検知差し替え | audit.sh #7 を三段正規化＋アンカー判定へ差し替え | 2026-06-16 | 実装サブ | 完了 |
| T3 検証・アダプタ再生成 | 全テスト PASS・本リポ実走査クリーン・アダプタ再生成 | 2026-06-16 | verify-and-close | 完了 |

### 2.2 実装内容の詳細

#### タスク 1/2: #7 検知ロジックの三段差し替え

- **実装内容**: 旧 `grep -qE 'TODO|FIXME' "$f"`（語の部分一致）を、ファイル単位の三段パイプライン
  `awk(フェンス除去・未閉フォールバック) | sed -E 's/`[^`]*`//g'（インライン除去） | LC_ALL=C grep -qE '(TODO|FIXME)[[:space:]]*[:：(]'（アンカー判定）` に置換（audit.sh:311-321）。
- **変更ファイル**: `.agents/enforcement/ci/audit.sh`（#7 セクションのみ）、`test/test-audit.sh`（回帰ケース追加）。
- **実装方法**: 02 §2.3／§3.1.3／§5.1・03 §2.2.2 骨格に一致。awk は POSIX 構文のみ。未閉フェンス時は EOF で `infence` ならバッファ行を救済出力（偽陰性ゼロ保険）。意図コメントを #7 に付与（audit.sh:304-310）。
- **確認事項**: templates 除外（:301）・`/close/` 除外（:303）・find 収集（:328）・FAIL 出力様式（:323-326,330-332）は不変であることを差分・実走査で確認した。

---

## 3. テスト結果の確認

### 3.1 単体テスト

#### テスト実行結果（必須・数値）— **独立再実行（実装サブの自己申告を鵜呑みにせず検証担当が自分で実行）**

- **実行日**: 2026-06-16
- **テストファイル数**: 1（`test/test-audit.sh`）
- **テストケース数**: 18
- **成功**: 18
- **失敗**: 0
- **スキップ**: 0（sqlite3 在環境のため #17 系も実行）

実測ログ（`bash test/test-audit.sh`）:

```
== 結果: PASS=18 FAIL=0 ==
```

全 18 件 PASS（既存 T1〜T4b の 8 件＋本是正の C-SC1／C-SC2a–e／C-SC4a／C-SC4b／C-guard／C-edge の 10 件）。

#### tmp 隔離（mktemp -d）での SC 独立再現（本番 .agents/・workflow.db を破壊しない）

| 再現項目 | 期待 | 実測 |
| -------- | ---- | ---- |
| SC1: 言及のみ（「TODO/FIXME 残骸チェック」「残骸タグ（TODO 等）0件」） | 発火しない | **no-fire（rc=0）** ○ |
| SC2a: `// TODO: fix`（プロセ） | FAIL | **FIRE** ○ |
| SC2b: `<!-- FIXME: x -->` | FAIL | **FIRE** ○ |
| SC2c: 箇条書き `- TODO:` | FAIL | **FIRE** ○ |
| SC2d: `TODO(alice): do`（所有者付き） | FAIL | **FIRE** ○ |
| SC2e: 全角 `TODO：` | FAIL | **FIRE** ○ |
| SC4: 本 issue 00-03 自身を tmp 走査 | 発火しない | **no-fire（rc=0）** ○ |
| 未閉フェンス直後の実マーカ | FAIL（保険） | **FIRE** ○ |

偽陰性ゼロ（SC2）と偽陽性除去（SC1/SC4）が両立。未閉フェンスの偽陰性も保険で検知。

### 3.2 統合テスト

CLI スクリプトの自己テスト（外部 API なし）。該当なし。

### 3.3 E2E テスト

本リポ実走査 `bash .agents/enforcement/ci/audit.sh .` を実行し、**#7 FAIL（「重要パスに TODO/FIXME が残存」）が 1 件も出ないこと**を実測確認（SC4 の E2E 代替）。本 issue 00-03 および memo 起因の #7 発火なし。

---

## 4. コードレビュー

### 4.1 コード品質

- **リント結果**: shell スクリプト・bash 構文エラーなし（実行成功）。
- **フォーマット**: 問題なし（意図コメントを #7 に明示・既存スタイル踏襲）。
- **型チェック**: 該当なし（bash）。

#### コードレビュー観点

| 観点 | 確認内容 | 結果 | コメント |
| ---- | -------- | ---- | -------- |
| 可読性 | 三段（フェンス/インライン/アンカー）の意図をコメント明記、各段が単機能 | OK | 06_設計判断の優先順位（可読性優先）に合致 |
| 保守性 | #7 セクション内に局所化、他チェック未変更、新規ファイルなし | OK | 単一責務・最小変更 |
| パフォーマンス | find 収集・ループ不変、ファイル単位に awk\|sed\|grep を 1 回（行ごとプロセス起動なし） | OK | 走査量不変・体感影響なし |
| セキュリティ | 外部アクセスなし（ローカル/CI のファイル走査のみ） | OK | 追加要件なし |

### 4.2 指摘事項

#### 指摘 1（nice-to-have・継承 B-new-1）: フェンス区切り行に同居した実マーカは取りこぼす

- **重要度**: 低
- **指摘内容**: フェンス区切り行の行末にコメント形の実マーカを同居させると、フェンス行が awk の next で捨てられ検知されない。実務上ほぼ無い記法であり、02 §3.1.2／03 §5.1 が「コードブロック内の実マーカは理論的偽陰性として許容（例示が支配的）」と既に射程に含む範囲の延長。SC1-4・01 BDD を脅かさない。
- **対応状況**: nice-to-have（must-fix ではない）。実装サブが第2ラウンドで継承済み・収束済み。
- **対応方法**: 是正不要。将来 test-audit の境界ケースに含められれば望ましい程度。

> must-fix の新規指摘は無し（敵対的観点で偽陰性・退行を探索したが検知力低下・退行は検出されなかった）。

---

## 5. ドキュメントの確認

### 5.1 ドキュメント更新状況

| ドキュメント | 更新状況 | 確認者 | 確認日 |
| ------------ | -------- | ------ | ------ |
| [`00_要求定義.md`](./00_要求定義.md) | 更新済み | verify worker | 2026-06-16 |
| [`01_要件定義.md`](./01_要件定義.md) | 更新済み | verify worker | 2026-06-16 |
| [`02_設計.md`](./02_設計.md) | 更新済み | verify worker | 2026-06-16 |
| [`03_実装計画.md`](./03_実装計画.md) | 更新済み | verify worker | 2026-06-16 |

### 5.2 ドキュメントの整合性

- **実装と設計の整合性**: 整合している（実装は 02 §2.3 三段＋§5.1 未閉フォールバック・03 §2.2.2 骨格に 1:1 対応）。
- **要件と実装の整合性**: 整合している（SC1-4・01 BDD と実装/テストが対応。下記カバレッジ表参照）。
- **コメント**: in-progress memo 2 件（014214／015607）の散文インラインバッククォート不均衡による #7 偽発火を、設計 03 §2.3.2 方針（memo 執筆側で均衡化・是正ロジックは不変）に従い意味を変えずに均衡化して解消した。

---

## 受け入れ基準カバレッジ（map-coverage）

| 受け入れ基準 / BDD | 実装での担保 | 対応テスト | 結果 |
| ------------------ | ------------ | ---------- | ---- |
| **SC1**（言及のみ PASS） | (b)(c) インライン除去＋アンカー判定で語の言及を除外 | C-SC1／tmp SC1 | ○ PASS |
| **SC2**（実マーカ FAIL・偽陰性ゼロ） | (c) `(TODO\|FIXME)[[:space:]]*[:：(]` で各実形を検知 | C-SC2a–e／tmp SC2×5 | ○ 全 FIRE |
| **SC3**（既存テスト全 PASS＋回帰追加） | #7 のみ局所変更・他チェック不変 | test-audit.sh 18/18 | ○ PASS |
| **SC4**（本 issue 00/01/02/03 自身 PASS） | (a) フェンス除去で BDD/コード例示を除外 | C-SC4a/C-SC4b／tmp SC4／実走査 | ○ no-fire |
| **01 BDD シナリオ1**（言及 PASS） | SC1 と同一 | C-SC1 | ○ |
| **01 BDD シナリオ2**（実マーカ FAIL） | SC2 と同一 | C-SC2a–e | ○ |
| **01 UC2**（既存ガード/他チェック非干渉） | templates/close 除外不変・他 check 不変 | C-guard／T1〜T4b | ○ |
| **02 観点6**（未閉フェンス偽陰性ゼロ） | (a) EOF 未閉フォールバック保険 | C-edge／tmp 未閉 | ○ FIRE |

**欠落: ゼロ。** 全受け入れ基準・BDD に対応テストが 1:1 で存在し、独立再実行で実測 PASS。

---

## docs 更新

- 要否: 不要
- 対象: なし
- 理由: 本是正は enforcement スクリプト（audit.sh）の検知ロジック局所変更であり、システム仕様書（docs/）の記述に影響しないため。

---

## 9. 設計・境界の確認（review-architecture）

### 9.1 設計の確認

- **設計原則の準拠**: UNIX 哲学（grep/sed/awk/find の小さな組み合わせ）・単一責務（#7 セクションに局所化）・AI フレンドリー（意図コメント）を満たす（02 §1.2・spec/06）。
- **ディレクトリ構成**: 正本 `.agents/enforcement/ci/audit.sh` のみ編集。テストは保守者自己テスト `test/`（非配布）。`.agents-project/自己拡張ワークフロー.md` の名前空間分離に合致。
- **命名規則**: 既存 #7 セクション内のローカルロジックとして実装、新規ファイル/関数名の追加なし。

### 9.2 境界・依存の確認

- **責務の境界**: #7 の検知ロジック 1 箇所のみ差し替え。`resolve_workflow_dirs`・引数/環境変数 I/F・find 収集・FAIL 出力様式・templates/`/close/` 除外ガード・他チェック #1〜#6,#8〜#29 は不変（差分・実走査で確認）。
- **依存関係**: 循環参照なし。`set -e` 下でも各段 `2>/dev/null`＋`if … ; then` 評価で非致命停止を維持（pipefail を #7 局所で有効化せず既存挙動を保持）。
- **移植性**: #7 セクションに `grep -P`・`\b`・`\w` を含まないことを静的確認（ERE のみ）。全角コロンは `LC_ALL=C` 採用（#26 前例と一貫・ASCII コロン検知は LC_ALL 不依存で安全側）。
- **指摘・推奨**: なし（must-fix なし）。

### 9.3 重要判断の根拠（evidence_source）

| 判断内容 | evidence_source | 備考 |
| -------- | --------------- | ---- |
| 三段正規化が設計どおり実装されている | existing_code | audit.sh:311-321 の差分確認 |
| 偽陰性ゼロ（実マーカ全形 FAIL 維持） | test_output | test-audit 18/18・tmp SC2×5 FIRE 実測 |
| 偽陽性除去（SC1/SC4 PASS） | test_output / observed_runtime | tmp SC1/SC4・本リポ実走査 no-fire |
| 未閉フェンス保険の有効性 | test_output | C-edge／tmp 未閉で FIRE 実測 |
| 既存ガード・他チェック不変 | existing_code / test_output | 差分・T1〜T4b PASS |

---

## 二観点レビュー（REVIEW_DUAL_LENS）

### 敵対的観点リスト（反証・破壊を試みた観点と結論）

| # | 攻めた観点 | 結論 |
| - | ---------- | ---- |
| 1 | 実マーカの各形態（コメント/HTML/箇条書き/所有者付き/全角）を見逃さないか | 全 5 形で FIRE 実測（偽陰性なし） |
| 2 | 未閉フェンス以降の実マーカを抑制して見逃さないか | EOF フォールバック保険で FIRE（偽陰性なし） |
| 3 | フェンス除去でコードブロック外の散文実マーカまで誤って消さないか | バッファ破棄は閉フェンス時のみ・フェンス外行はそのまま print。tmp SC2 で散文実マーカ FIRE を確認 |
| 4 | ロケール差で全角コロンの検知が揺れないか | `LC_ALL=C` 採用で安定。ASCII 主形は LC_ALL 不依存（02 §5 実測根拠） |
| 5 | `set -e`／no-match で audit 全体が異常終了しないか | 各段 `2>/dev/null`＋`if` 評価で非致命。test 全 PASS（exit 0） |
| 6 | templates/`/close/` 除外や他チェックが退行していないか | C-guard・T1〜T4b PASS。#7 セクションのみ差分 |
| 7 | フェンス区切り行同居マーカ（B-new-1） | 取りこぼすが nice-to-have・射程内・SC を脅かさない |

**結論**: 不確実側に倒して攻めたが、検知力低下・退行・偽陰性は新規検出なし。must-fix ゼロ。

### must-preserve リスト（不変条件と保持確認・実装前レビュー観点B 8件をラウンド継承）

| # | 不変条件（must-preserve） | 保持確認 |
| - | -------------------------- | -------- |
| 1 | 是正対象は **#7 のみ**（他チェックに触れない） | ○ 差分は #7 セクションのみ |
| 2 | 既存ガード（templates 除外・`/close/` 除外）を撤去せず重畳維持 | ○ :301/:303 不変・C-guard PASS |
| 3 | 引数・環境変数 I/F（PROJECT_ROOT/GIT_RANGE/WORKFLOW_DIR(S)/WORKFLOW_SCAN_DIRS）不変 | ○ resolve_workflow_dirs・I/F 未変更 |
| 4 | FAIL 出力様式（メッセージ・対象パス列挙・ROLLBACK_MSG・EXIT_CODE=1）不変 | ○ :323-326,330-332 不変 |
| 5 | 正本（audit.sh）のみ編集＋アダプタは build/setup で再生成（手編集禁止） | ○ build-adapters.sh で再生成し一致確認 |
| 6 | 偽陰性ゼロ（実マーカ見逃しを新規に生まない） | ○ SC2 全形＋未閉フェンス FIRE |
| 7 | `set -e` 下で no-match による致命停止を起こさない | ○ 各段ガード＋if 評価維持 |
| 8 | 移植性（ERE のみ・BSD/GNU 両対応・POSIX awk） | ○ `-P`/`\b`/`\w` 不使用・全角は LC_ALL=C |

**全 8 件保持。退行なし。**

---

## 10. 課題と改善点

### 10.1 発見された課題

- なし（must-fix ゼロ）。

### 10.2 改善提案

- **改善 1（nice-to-have）**: フェンス区切り行同居マーカ（B-new-1）の境界ケースを将来 test-audit に追加できれば網羅性が上がる。
  - **効果**: 理論的偽陰性 1 形態の明示的退行検知。必須ではない。

---

## アダプタ再生成の判定と結果

- **判定**: 正本 `.agents/enforcement/ci/audit.sh` を変更したため `.adapters/claude`・`.adapters/cursor` の同梱 audit.sh が古くなっていた（再生成前は差分あり）。これらは gitignore 対象（git 影響なし）だが、ローカル生成物の整合のため正規フローで再生成した。
- **結果**: `bash .agents/scripts/build-adapters.sh` を実行（手編集せず）。再生成後 `.adapters/{claude,cursor}/.agents/enforcement/ci/audit.sh` が正本と **diff 一致**を確認。`.claude/` は hooks/skills のみで audit.sh を同梱しないため対象外。

---

## memo 2 件の #7 偽発火対処の結果

- 対象: `memo/20260616_014214_実装前ドキュメントレビュー証跡.md`（line 32・37）、`memo/20260616_015607_実装前ドキュメントレビュー第2ラウンド証跡.md`（line 45・67）。
- 原因: 散文中のインラインバッククォートが不均衡で、sed のインライン除去が均衡対のみ除去した結果、コメント形の実マーカ語が露出し `(TODO|FIXME)[[:space:]]*[:：(]` に MATCH していた。
- 対処: 設計 03 §2.3.2 方針（**memo 執筆側でバッククォート均衡化・是正ロジックは変更しない**）に従い、意味を変えずに該当行のバッククォート/コード例示の記法を均衡化。
- 結果: 全 memo を再走査し **no-fire（解消）**。is-progress の本リポ実走査でも #7 FAIL なしを確認。是正ロジック（audit.sh）は一切変更していない。

---

## 12. レビュー結果

### 12.1 総合評価

- **実装品質**: 良好（設計どおり・最小変更・意図明示）。
- **テスト品質**: 良好（SC1-4・BDD・未閉フェンス境界を網羅、独立再実行で 18/18 PASS）。
- **ドキュメント品質**: 良好（00-04 整合・memo 偽発火解消）。
- **総合評価**: **クローズ可（合格）**。must-fix ゼロ・差し戻し不要。

### 12.2 承認状況

- **レビュー承認者**: verify-and-close worker
- **承認日**: 2026-06-16
- **承認コメント**: 二観点（敵対的＋must-preserve 8 件）を満たし、偽陰性ゼロ・偽陽性除去を実測で両立確認。クローズ可。コミットは orchestrator が別途委譲する（本フェーズではコミットしない）。

---

## 13. 参考資料

### 13.1 プロジェクトドキュメント

- [`00_要求定義.md`](./00_要求定義.md) - 要求定義
- [`01_要件定義.md`](./01_要件定義.md) - 要件定義
- [`02_設計.md`](./02_設計.md) - 設計
- [`03_実装計画.md`](./03_実装計画.md) - 実装計画

### 13.2 その他の参考資料

- [`.agents/enforcement/ci/audit.sh`](../../../../.agents/enforcement/ci/audit.sh) - 是正対象（#7 は 296-334 行）
- [`test/test-audit.sh`](../../../../test/test-audit.sh) - 回帰テスト
- [`.agents/REVIEW_DUAL_LENS.md`](../../../../.agents/REVIEW_DUAL_LENS.md) - 二観点レビュー

---

## 14. 前のステップ

- **前**: [`03_実装計画.md`](./03_実装計画.md) - 実装計画フェーズ

---

## 15. 次のステップ

- 外部設定不要のため最終確認チェックリストは省略。クローズ（commit は orchestrator が委譲）へ。
