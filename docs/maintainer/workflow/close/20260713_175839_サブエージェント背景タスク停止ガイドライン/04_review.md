---
# document_id: 必須。作成時または major 更新時に UUID（8-4-4-4-12 形式）を付与すること。既存の場合は変更しない。
document_id: "289dc4b7-cb7d-43f4-a2ad-aedddfde4040"
# issue_id: 当該 issue の一意識別子。00/01/02/03 と同一値を維持する（04 では新規発行しない）。
issue_id: "ffe95495-88a3-47c0-a2d3-55ec3381d7e5"
---

# レビュー書: サブエージェント委譲時の長時間コマンド・バックグラウンド化による停滞再発防止ガイドライン

**プロジェクト名**: サブエージェント委譲時の長時間コマンド・バックグラウンド化による停滞再発防止ガイドライン
**作成日**: 2026 年 07 月 14 日
**最終更新**: 2026 年 07 月 14 日

> **重要**: **このドキュメントは常に更新**: レビューで発見した問題点や改善提案、対応内容などがあった場合は、即座にこのドキュメントを更新してください。
>
> **必須**: 本レビューは [`.agent-skill-chain/source/REVIEW_RULE.md`](../../../../../.agent-skill-chain/source/REVIEW_RULE.md) および [`REVIEW_DUAL_LENS.md`](../../../../../.agent-skill-chain/source/REVIEW_DUAL_LENS.md)（敵対的観点リスト・must-preserve リスト両立を必須化。両リスト欠落は未完了）に従う。ユーザー指示「review は実装前 review 含めてすべて肯定的・敵対的観点から review すること」に基づき、実装前 review-docs（memo）の二観点も継承・再確認する。
> **レビュー深度**: standard（変更 2 ファイル各 1 行追記のドキュメント差分・実行コードなし）。

---

## 1. レビュー概要

### 1.1 レビュー目的（必須）

実装内容の確認 / 品質保証 / クローズ前最終チェック。implement-feature で追記された停滞再発防止ガイドライン（正本＝run_command.md、想起補強＝AGENT_CONDUCT.md 凝縮版）が 00-03 の受け入れ基準・BDD を満たし、既存規約（5 ゲート・書記依頼強制・凝縮版 7 項目）を弱化・矛盾させていないことを、肯定的（must-preserve）・敵対的（反証）の二観点で検証する。

### 1.2 レビュー対象（必須）

- **実装範囲**:
  - `run_command.md` §委譲の形 → Constraints（:69）へ正本項目 1 件を追記（案 A）。
  - `AGENT_CONDUCT.md` 第 3 部凝縮版コードブロック（:90）へ要点 1 行を追記（案 B）。
  - `00_要求定義.md` frontmatter（github_issue: #37 / branch: docs/subagent-bg-guidance）＋ §2.2 機序註釈の review-docs 修正。
- **レビュー期間**: 2026-07-14（本 verify-and-close 実行時）
- **レビュー担当者**: verify-and-close サブエージェント（fresh・review-docs 継承）

---

## 2. 実装内容の確認

### 2.1 実装完了タスク

| タスク名 | 実装内容 | 実装日 | 担当者 | ステータス |
| ------------ | ----------------- | -------- | ---------- | ---- |
| タスク 1（案 A） | run_command.md Constraints へ正本項目 1 件追記 | 2026-07-14 | implement-feature | 完了 |
| タスク 2（案 B） | AGENT_CONDUCT.md 凝縮版へ要点 1 行追記 | 2026-07-14 | implement-feature | 完了 |
| タスク 3 | 受け入れ確認（grep 存在検査・差分非改変・lint） | 2026-07-14 | verify-and-close | 完了（lint は環境制約により代替検証） |

### 2.2 実装内容の詳細

#### タスク 1: run_command.md 正本追記（案 A）

- **実装内容**: `### Constraints` 内、「委譲時の行動規範凝縮版転記」項目（現行 :70）の直前（:69）へ 02 §3.1.3 の確定文言を独立項目として挿入。
- **変更ファイル**: `.agent-skill-chain/source/skills/agent/run_command.md`（+1 行）。
- **実装方法**: 既存 Constraints の箇条書き記法（`- **見出し**: …`・**強調**・相互参照）に揃えた 1 項目追加。既存項目の順序・文言は不変。
- **確認事項（実ファイル :69 で実測）**: 02 §3.1.4 の 5 要素が全て含有。
  1. フォアグラウンド原則（「原則フォアグラウンドで実行して完了を待つこと」）→ 存在。
  2. 閾値を置かない（「判断の分かれ目は実行時間の長短（秒数などの閾値）ではなく…」）→ 存在。
  3. 停滞メカニズム（「サブエージェントが応答を終了すると Agent 呼び出しが完了扱いとなり再開されないため、その通知を受け取れない」）→ 存在・機序正確（後述 §重点確認 4）。
  4. background 起動後の応答終了禁止（「`run_in_background: true` で長時間コマンドを起動して自身の応答を終了する行為は禁止」）→ 存在。
  5. フォールバック（「成立しないランタイムでは本項は自然に無害化される（フォールバック。ロックアウトしない）」）→ 存在。

#### タスク 2: AGENT_CONDUCT.md 凝縮版追記（案 B）

- **実装内容**: 第 3 部凝縮版 ```markdown フェンス（80-91 行）内、「境界」項目（:89）直後（:90）へ要点 1 行を追加。
- **変更ファイル**: `.agent-skill-chain/source/AGENT_CONDUCT.md`（+1 行）。
- **確認事項（実ファイル :90 で実測）**: 「背景タスク抑止」で始まり、self-contained な行為指示（run_in_background 不使用＋フォアグラウンド＋応答終了しない＋停滞理由）を含み、末尾に正本参照「正本は run_command.md §委譲の形」を含む（BR-2 適合）。閉フェンス（:91）は破壊されずフェンス内に収まる。

---

## 3. テスト結果の確認

本成果物は実行コードを持たないドキュメント追記のため、「テスト」＝**追記文言の存在検査・整合検査・非改変検査**と読み替える（02 §6・03 §2）。実行結果を以下に記載する。

### 3.1 単体（存在・整合検査）実行結果（必須: 数値で記載）

- **実行日**: 2026-07-14
- **検査ケース数**: 9（run_command.md 6・AGENT_CONDUCT.md 3）
- **成功**: 9
- **失敗**: 0
- **スキップ**: 0（ただし markdownlint は環境制約で未実行＝§3.3 で代替検証）

実測（grep 実行、対象＝実ファイル）:

| 検査 | 対象 | 実測 | 判定 |
| ---- | ---- | ---- | ---- |
| `run_in_background` 抑止項目の存在 | run_command.md | 1 件 | OK |
| 起動元（機序）語 | run_command.md | 1 件 | OK |
| フォアグラウンド | run_command.md | 1 件 | OK |
| フォールバック | run_command.md | 2 件（本文＋文末） | OK |
| 閾値（閾値なし方針） | run_command.md | 1 件 | OK |
| 応答を終了する行為は禁止 | run_command.md | 1 件 | OK |
| 既存ゲート enforcement 番号 | run_command.md | #32/#34/#35/#36 全存続 | OK |
| 「背景タスク抑止」1 行 | AGENT_CONDUCT.md | 1 件 | OK |
| 正本参照「正本は run_command.md」 | AGENT_CONDUCT.md | 1 件 | OK |
| 凝縮版 既存 7 項目見出し | AGENT_CONDUCT.md | 規約優先/結論先行/即行動/進捗の実証/スコープ規律/ターン終了規律/境界 全存続 | OK |

### 3.2 結合 / E2E

- 該当なし（実行フロー・エンドポイント・データを持たない。02 §6.1 で N/A）。

### 3.3 バリデーション（markdownlint）

- **markdownlint は環境制約により未実行**: 本 worktree はオフライン環境で `npx --no-install markdownlint-cli2` を実行したところ `npm error npx canceled due to missing packages`（markdownlint-cli2 未導入・ネットワーク不可）で起動できなかった。実装フェーズでも同制約で未実行だった旨は 03 の想定どおり。
- **代替検証（目視・構造照合）**: 追記 2 箇所が既存 Markdown リスト記法に構造的に準拠しているか実ファイルで目視確認した。
  - run_command.md :69 は既存の各 Constraints 項目と同一の `- **強調見出し**: 本文…` 形式で、リスト連続を分断していない。
  - AGENT_CONDUCT.md :90 は凝縮版既存 7 項目と同一の `- **強調見出し**: 本文` 形式で、開フェンス（:80 ```markdown）と閉フェンス（:91 ```）の内側に収まりフェンスを破壊していない。
  - いずれも新規の見出しレベル・テーブル・リンク切れを導入していない（追記は箇条書き 1 項目のみ）。
  - **結論**: markdownlint 新規エラーを生む構造的要因（フェンス破壊・リスト記法逸脱・見出し階層飛び）は認められない。ただし lint 実機実行による最終確認は環境復旧時に委ねる（残リスクは軽微・構造検証で代替済み）。

---

## 4. コードレビュー

### 4.1 コード品質

- **リント結果**: 実機未実行（§3.3）。構造目視で新規エラー要因なし。
- **フォーマット**: 問題なし（既存記法に一致）。
- **型チェック**: 該当なし（ドキュメント）。

#### コードレビュー観点

| 観点 | 確認内容 | 結果 | コメント |
| -------------- | ---------------------- | ---- | ---- |
| 可読性 | 追記が既存 Constraints/凝縮版のトピック隣接位置に置かれ一読で意図が通るか | OK | :69 は「委譲時の行動規範凝縮版転記」の直前でトピック隣接（spec/06 可読性）。 |
| 保守性 | 正本 1 箇所＋凝縮版は要点＋参照で二重定義がないか（BR-2） | OK | 機序詳細・5 要素は正本のみ。凝縮版は要点＋正本参照のみ。 |
| 互換性 | 既存 5 ゲート・書記依頼強制・凝縮版 7 項目が非改変か（BR-3） | OK | git diff は全体で +5/-4 行、うち本文実追記は 2 ファイル各 +1 行。既存行の削除・改変なし。 |
| セキュリティ | 認証情報・外部書き込みを伴うか | OK | 該当なし（ドキュメント追記のみ）。 |

### 4.2 指摘事項

本 verify-and-close で**新規に指摘化した欠陥は 0 件**。実装前 review-docs（memo 20260714_075416）で検出された A1（機序の自己矛盾・00 との不整合）・A2（01 内部矛盾）は既に修正済みで、本レビューで実装後ファイルへの反映を最終確認した（§重点確認 4）。

---

## 5. ドキュメントの確認

### 5.1 ドキュメント更新状況

| ドキュメント | 更新状況 | 確認者 | 確認日 |
| ------------------------------------ | ----------------- | -------- | ------ |
| [`00_要求定義.md`](./00_要求定義.md) | 更新済み（frontmatter＋§2.2 機序註釈） | verify-and-close | 2026-07-14 |
| [`01_要件定義.md`](./01_要件定義.md) | 更新済み（review-docs で機序統一） | verify-and-close | 2026-07-14 |
| [`02_設計.md`](./02_設計.md) | 更新済み（§3.1.3 確定文言が実装と一致） | verify-and-close | 2026-07-14 |
| [`03_実装計画.md`](./03_実装計画.md) | 更新済み（BDD 対応表が実装と一致） | verify-and-close | 2026-07-14 |

### 5.2 ドキュメントの整合性

- **実装と設計の整合性**: 整合。run_command.md :69 の実文言と 02 §3.1.3 の確定文言、AGENT_CONDUCT.md :90 と 02 §3.2.3 が逐語一致。
- **要件と実装の整合性**: 整合（§6 カバレッジ表で全 BDD/AC の対応を確認）。

---

## 6. 受け入れ基準の確認（generate-scenarios / map-coverage）

01 §2.2 の BDD・受け入れ基準と実装（実ファイル）の対応。全項目が実測で充足。

| 01 の BDD / 受け入れ基準 | 検証方法 | 実測・結果 |
| ---- | ---- | ---- |
| UC1 シナリオ 1: フォアグラウンド実行で完了を待つ | run_command.md にフォアグラウンド原則の記述が存在 | :69 に「原則フォアグラウンドで実行して完了を待つこと」→ **充足** |
| UC1 シナリオ 2: background 起動後の応答終了禁止＋理由（機序） | 正本に「応答終了禁止」＋機序理由が存在 | :69 に「…応答を終了する行為は禁止」＋「応答を終了すると…再開されないため、その通知を受け取れない」→ **充足** |
| UC2 シナリオ 1: 既存 Constraints と非重複・非矛盾・正本 1 箇所 | 独立項目追加・既存非改変・凝縮版は要点＋参照 | git diff で既存行非改変・独立項目追加、凝縮版は要点＋正本参照のみ → **充足** |
| UC2 シナリオ 2: 適用対象外環境でのフォールバック | 正本にフォールバック（無害化・ロックアウトしない）が存在 | :69 文末に該当記述 → **充足** |
| ストーリー 1 AC: 正本に 1 箇所記載・独立項目 | run_command.md に 1 件・既存と非重複 | grep 1 件・独立項目 → **充足** |
| ストーリー 2 AC: 判断基準を秒数でなく run_in_background 使用有無に置く | 正本に「閾値を置かない」旨が存在 | :69 に該当記述（ADR-2） → **充足** |
| ストーリー 3 AC: 正本 1 箇所・他所は参照 | 凝縮版に正本参照が含まれ二重定義でない | AGENT_CONDUCT.md :90 に「正本は run_command.md §委譲の形」 → **充足** |

**カバレッジ結論**: 01 の BDD 4 シナリオ＋ストーリー 3 件の受け入れ基準に**未達（欠落）なし**。03 §BDD 対応表とも一致。

---

## 7. 敵対的観点リスト（反証・破壊を試みた観点と結論）

REVIEW_DUAL_LENS §2.1 に従い、不確実な場合は要修正に倒す方針で反証を試みた。

- **AD-1【破れず】機序記述が実装後ファイルでも自己矛盾していないか（重点確認 4）**: run_command.md :69 実文言を精読。「その起動元＝コマンドを起動したサブエージェント自身の会話コンテキストに届く仕組みだが、サブエージェントが応答を終了すると Agent 呼び出しが完了扱いとなり再開されないため、その通知を受け取れない」。起動元＝サブ自身と正しく同定し、"orchestrator にしか届かない" という誤記（review-docs A1 で摘出）は残存しない。00 §1.2 正本・00 §2.2 修正後註釈・AGENT_CONDUCT.md :90（「応答終了後はサブエージェント自身が完了通知を受け取れず停滞する」）とも一貫。→ **指摘化しない（機序は正確）**。
- **AD-2【破れず】既存 5 ゲートの弱化・矛盾（重点確認 3）**: 追記は「委譲後にサブが従う実行作法」で、既存ゲート（review-docs 必須 #32／GitHub Issue 起票／branch 紐づけ／PR 紐づけ／書記依頼強制）は「実装着手前の手続き・記録の強制」でトピック非交差。文末に「本項は既存の各ゲート・書記依頼の強制とは非交差の独立項目であり、既存項目を置換・弱化しない」と自己言及で明示。enforcement 番号 #32/#34/#35/#36 は全存続、既存行に差分なし。→ **弱化・矛盾なし。指摘化しない**。
- **AD-3【破れず＝残余リスクは明記】読み飛ばしケースは防げるか（重点確認 6・有効性の反証）**: 「サブがガイドラインを読み飛ばせば停滞は再発しうる」。これは advisory（機械強制なし）である以上、原理的に完全排除はできない。ただし (i) 正本のみでなく凝縮版が委譲パケット Constraints 末尾へ**毎回転記**され手元に必ず現れる（ADR-1(c)）、(ii) 凝縮版 1 行が self-contained な行為指示（bg 不使用＋フォアグラウンド＋応答終了しない）で、読めば即座に既定挙動が foreground に定まる、(iii) 「原則フォアグラウンド」を安全側デフォルトに置き迷いを foreground に一意化。→ **想起性を構造的に最大化しており、issue スコープ（advisory ガイドライン）内では十分。ただし「完全な再発防止は保証しない」限界は §10 に正直に記載する**。
- **AD-4【破れず】高度サブの解釈余地（ADR-2 残余曖昧性）**: 機序が「bg＋応答終了」が真の失敗モードと読めるため、「応答終了しなければ bg 可」と解釈する余地が理論上ある。しかし確定文言は「**原則フォアグラウンド**」を安全側デフォルトとして先に明示し、精密条項（bg 起動後の応答終了禁止）を併記。迷った場合の既定が foreground に一意に定まり実害を生まない。→ **指摘化しない**。
- **AD-5【観測のみ】非改変 grep 検査の弱さ**: 03 §2.1.4 の `grep -q '#32...'` は 1 文字列でも残れば pass する弱い代理検査。ただし本レビューは grep に依らず `git diff` 本文で既存行の非改変を直接確認済み（belt-and-suspenders）。→ 設計として妥当、指摘化しない。
- **AD-6【破れず】スコープ逸脱（重点確認 1）**: `git diff main --stat` は run_command.md（+1）・AGENT_CONDUCT.md（+1）・00_要求定義.md（frontmatter＋§2.2 註釈）の 3 ファイルのみ。01/02/03 は本 issue の新規ファイル（main 未追跡）で追跡差分に現れないのは正常。実装スコープ（source 変更）は狙いどおり 2 ファイルに限定され、00 の変更は issue メタデータ＋review-docs 由来のドキュメント整合修正で逸脱ではない。→ **スコープ境界は保持**。

## 8. must-preserve リスト（壊してはならない不変条件・保持を確認）

REVIEW_DUAL_LENS §2.2/§6 に従い、review-docs memo §4 の 10 項目をラウンド継承のうえ実装後ファイルで退行の有無を再確認した。

1. **正本 1 箇所（BR-2）**: 正本＝run_command.md、凝縮版＝要点＋参照。→ 保持（機序詳細・5 要素は正本のみ）。
2. **既存 Constraints 5 ゲート＋書記依頼強制の非改変（BR-3）**: :69 直前の独立項目追加のみ。git diff で既存行非改変。→ 保持。
3. **AGENT_CONDUCT 凝縮版 既存 7 項目の非改変**: 「境界」直後・フェンス内 1 行追加のみ。7 見出し全存続。→ 保持。
4. **§0 読み替え規則（規約優先）**: 新規 1 行は advisory、矛盾時は規約優先の適用下。→ 保持。
5. **フォールバック／ロックアウトしない設計（ADR-3）**: :69 文末に前提不成立ランタイムでの無害化を明記。→ 保持。
6. **```markdown フェンスを破壊しない**: :90 追記はフェンス（:80-:91）内側。閉フェンス存続。→ 保持。
7. **確定文言の機序記述の正確性**: 実装後 :69 で機序正確（AD-1）。→ 保持（review-docs で改善済み）。
8. **enforcement 番号 #32/#34/#35/#36 への非影響**: 追加のみ・4 番号存続。→ 保持。
9. **frontmatter（document_id / issue_id）不変**: run_command.md/AGENT_CONDUCT.md は本文のみ変更、frontmatter 非改変。00 の frontmatter は github_issue/branch のメタデータ記録（document_id/issue_id は不変）。→ 保持。
10. **BDD カバレッジの完全性**: §6 で全 BDD/AC 充足を実測。→ 保持。

> **ラウンド継承（REVIEW_DUAL_LENS §6）**: review-docs（実装前）で同定した must-preserve 1-10 を本 verify-and-close（実装後）へ継承し、実装が 1-10 のいずれも退行させていないことを実ファイルで再確認した。退行 0 件。

---

## docs 更新

- **要否**: 不要
- **対象**: なし
- **理由**: 本変更は `.agent-skill-chain/source/` 配下のワークフロー規約（run_command.md・AGENT_CONDUCT.md）への advisory ガイドライン追記であり、システム仕様書（`docs/00_review/` が対象とする RULES.md/spec 等のプロダクト仕様）の構造・契約・データ・API に影響しない（DOCS_RULES.md §継続追随ゲートの軽量パス＝根拠付き更新不要判定）。ハーネス挙動・enforcement 機構は不変（ADR-3・§7 AD-2）。

---

## 9. 設計・境界の確認（review-architecture）

### 9.1 設計の確認

- **設計原則の準拠**: spec/01 の単一責務（正本 1 箇所＝BR-2）・明確な境界（既存ゲートと非交差の独立項目＝BR-3）・AI フレンドリー設計（既存記法に揃え想起しやすい位置）に準拠（02 §1.2）。
- **ディレクトリ構成**: 既存ファイルへの追記のみ。新規ファイル・移動なし。
- **命名規則**: 見出し「委譲タスク内の長時間コマンドはフォアグラウンド実行（`run_in_background` 抑止）」「背景タスク抑止」は既存 Constraints/凝縮版の命名トーンに一致。

### 9.2 境界・依存の確認

- **責務の境界**: 正本（run_command.md）＝唯一の詳細保持、凝縮版（AGENT_CONDUCT.md）＝要点＋正本参照。既存ゲート群＝変更なし。境界は 02 §2.1 の責務表どおり。
- **依存関係**: 凝縮版 → 正本の一方向参照のみ（逆参照なし＝循環なし）。既存の「委譲時の行動規範凝縮版転記」（:70）の参照構造に寄生し新規リンクを増やさない。→ 意図しない依存・循環なし。
- **指摘・推奨**: なし（新規指摘 0 件）。有効性の限界（advisory ゆえ読み飛ばしを機械強制で防げない点）は §10 に記載。

### 9.3 重要判断の根拠（evidence_source）

| 判断内容 | evidence_source | 備考 |
| -------------------- | ---------------- | ---- |
| 機序記述の正確性（実装後 :69） | existing_code（実ファイル :69 実読）＋ observed_runtime（停滞を 2 回観測した orchestrator_report） | run_command.md 実測・00 §1.2 |
| 既存 5 ゲート非改変 | existing_code（git diff・grep で #32/#34/#35/#36 存続） | §7 AD-2 |
| BDD 全充足 | existing_code（grep 実測）＋ test_output（存在検査 9/9 成功） | §6 |
| 有効性の限界（読み飛ばし残余リスク） | inference_only | §10・要人間確認（advisory の効果は定量検証不能） |

> **inference_only の扱い**: 「想起性向上による再発防止効果」の定量評価は本 issue スコープで検証不能（inference_only）。ただし承認可否を左右する中核判断（機序正確性・既存非改変・BDD 充足）はいずれも existing_code / test_output に裏付けられており、inference_only のみに依存する重要判断は存在しない。有効性の限界は §10 に正直に記載し人間確認に供する。

---

## 10. 課題と改善点

### 10.1 発見された課題

- **課題 1（有効性の限界・重点確認 6）**: 本ガイドラインは advisory（機械強制なし）であり、サブエージェントが委譲パケットを**読み飛ばした／解釈を誤った**場合の停滞再発を原理的に完全排除はできない。
  - **影響範囲**: 想起性は凝縮版の毎回転記（ADR-1(c)）で構造的に最大化されているが、hooks/CI による強制はスコープ外（00 §7.1・02 §2.1.2）。
  - **対応方法**: 現時点では advisory 最大化で妥当と判断。将来、再発が観測された場合は enforcement（PreToolUse で `run_in_background: true` の使用を検知・警告する等）を別 issue で検討する余地がある（本 issue スコープ外）。

### 10.2 改善提案

- **改善 1**: markdownlint 実機実行は環境復旧時に一度回す（§3.3）。構造検証で代替済みのため close をブロックしない軽微事項。

---

## 11. システム仕様書の更新

- 本 issue は `.agent-skill-chain/source/` 規約への advisory 追記であり、システム仕様書（docs/）の更新は不要（§docs 更新の根拠付き判定）。docs/00_review への記録も不要（不発動＝軽量パス）。

---

## 12. レビュー結果

### 12.1 総合評価

- **実装品質**: 良好（確定文言と実装が逐語一致・既存規約非改変）。
- **テスト品質**: 良好（存在・整合・非改変検査 9/9 成功。lint のみ環境制約で代替検証）。
- **ドキュメント品質**: 良好（00-03 と実装が整合。機序の自己矛盾は review-docs で解消済み）。
- **総合評価**: **合格（close 可）**。新規指摘 0 件、must-preserve 10 項目退行 0 件、BDD/AC 未達 0 件。残存は advisory の原理的限界（§10.1）と lint 実機実行（§10.2・軽微）のみで、いずれも close をブロックしない。

### 12.2 承認状況

- **レビュー承認者**: verify-and-close サブエージェント
- **承認日**: 2026-07-14
- **承認コメント**: 二観点（敵対的 6・must-preserve 10）両リスト記載済み。重点確認 1-6 全確認。中核判断は existing_code/test_output 裏付けあり。有効性の限界は §10.1 に明記し人間確認に供する。

---

## 13. 参考資料

### 13.1 プロジェクトドキュメント

- [`00_要求定義.md`](./00_要求定義.md) - 要求定義（実害事例・スコープの正本）
- [`01_要件定義.md`](./01_要件定義.md) - BDD・受け入れ基準・BR-1〜3
- [`02_設計.md`](./02_設計.md) - 確定文言（§3）・ADR-1〜3
- [`03_実装計画.md`](./03_実装計画.md) - タスク分解・BDD 対応表
- [`memo/20260714_075416_review-docs.md`](./memo/20260714_075416_review-docs.md) - 実装前ドキュメントレビュー（二観点・A1/A2 修正）

### 13.2 その他の参考資料

- [`.agent-skill-chain/source/REVIEW_DUAL_LENS.md`](../../../../../.agent-skill-chain/source/REVIEW_DUAL_LENS.md) - 二観点必須化の正本
- 本セッションで進行役が観測した停滞事例（同一サブで 2 回）。evidence_source: orchestrator_report。

---

## 14. 前のステップ

- **前**: [`03_実装計画.md`](./03_実装計画.md) - 実装計画フェーズ（実装は implement-feature で完了）

---

## 15. 次のステップ

- 外部設定は不要。本 issue はレビュー合格につき close 可。
- **次（本 command 範囲外）**: commit（1 論理コミット・feature ブランチ docs/subagent-bg-guidance・push はユーザー明示時のみ）→ close 移動は進行役が判断。
