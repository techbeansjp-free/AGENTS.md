---
# document_id: 必須。作成時または major 更新時に UUID（8-4-4-4-12 形式）を付与すること。既存の場合は変更しない。
document_id: "df47d09e-6d08-4f0a-9637-e510a58a88cc"
---

# レビュー書: enforcement 失敗条件記述の重複解消・未実装ラベル・欠落行の統合（バッチ5 代表）

**プロジェクト名**: enforcement 表セル長大・重複解消（163330／163403／163432 統合）
**作成日**: 2026 年 07 月 14 日
**最終更新**: 2026 年 07 月 14 日

> 本 04 はバッチ5・3 件統合（`163330` 重複解消・`163403` 未実装ラベル・`163432` 欠落行）の **主たる検証結果の正本**である。`163403`・`163432` の 04_review は本書を参照し、各 issue 固有の受け入れ基準確認のみを記載する。
>
> 本レビューは verify-and-close フェーズの独立検証で、実装サブの自己申告値ではなく、worktree 上で `test/run-all.sh`・`enforcement/ci/audit.sh` を**独立実測**した結果に基づく。

---

## 1. レビュー概要

### 1.1 レビュー目的

3 件（重複解消・未実装ラベル・欠落行）を、`enforcement/README.md` の失敗条件レジストリ 1 回の構造変更で同時に満たしたことを確認し、`audit.sh` の判定挙動（PASS/FAIL/SKIP）が変更前後で一致すること（回帰非発生）を独立実測で保証する。

### 1.2 レビュー対象（変更ファイル）

- `.agent-skill-chain/source/enforcement/README.md` — 失敗条件対応表を単一レジストリ化（列に `実装状態` 追加）、欠落行 #2/#4/#7/#21/#37 追加、未実装 7 項目へ統制語彙付与、ゲート共通前提ノート新設（§ゲート共通の前提）、散文の参照化。
- `.agent-skill-chain/source/enforcement/ci/audit.sh` — ヘッダの冗長「失敗とみなす条件」ブロックを 1 行/チェックの索引（README #N への矢印付き）へ短縮。**関数本体は無改造**。

---

## 2. 実装内容の確認

### 2.1 実装完了タスク

| タスク | 実装内容 | ステータス |
| ------ | -------- | ---------- |
| T1 レジストリ列拡張 | 表 A を `# / 対象｜概要｜実装の所在｜実装状態｜強制レベル｜差し戻し先` の単一レジストリへ | 完了 |
| T2 欠落行追加 | 表 A に #2・#4・#7・#21・#37、表 B・表 C に #37 を追加 | 完了 |
| T3 統制語彙 | `実装済み`／`推奨（未強制）`／`未実装（人手監査）`／`未実装（抽象仕様のみ・将来hook）` を定義し 7 未実装項目へ付与 | 完了 |
| T4 共通前提ノート | grandfather 発効日・env トグル・共通 SKIP を `### ゲート共通の前提` 1 か所へ集約、各行は参照 | 完了 |
| T5 audit.sh ヘッダ短縮 | 冗長ブロックを 1 行索引＋README 参照へ短縮（コメントのみ） | 完了 |
| T6 run_command/project 参照点検 | 判定ルール再掲の有無を点検（下記 §2.3 で対応不要と判定） | 完了 |

### 2.2 実装内容の詳細

- **単一レジストリ化**: `enforcement/README.md` §失敗条件と差し戻し の表 A が 6 列レジストリになり、各失敗条件の「概要・実装の所在・実装状態・強制レベル・差し戻し先」の正本が 1 か所に集約された。
- **共通前提ノート**: `### ゲート共通の前提（SKIP・grandfather・env トグル）` を新設。#32〜#36 等の各行は「共通前提ノート参照＋固有 env 名」の形へ短縮され、grandfather 発効日・トグル名の重複記述が減少した。
- **audit.sh ヘッダ**: 旧ヘッダの #1〜#37 詳細ブロック（判定ルール・SKIP 条件を README と重複記載）を、`(N) 概要 → README #M` の 1 行索引へ短縮。audit 内部の番号と README #N がずれる箇所のみ矢印で明示。

### 2.3 T6（run_command / project 自己拡張ワークフロー の参照化点検）— 追加対応不要と判定

02_設計 §3.4・§2.6 は、`skills/agent/run_command.md` Constraints と `.agent-skill-chain/project/自己拡張ワークフロー.md` について「**手続き指示は維持**」「判定ルールの再掲が**あれば**参照へ寄せる（大きな変更不要）」と条件付きで規定していた。verify フェーズで両ファイルの実内容を点検した結果:

- `run_command.md` Constraints の各ゲート項は、既に末尾で「enforcement #NN（`enforcement/README.md` §失敗条件と差し戻し）で FAIL する」と**参照形**になっており、失敗条件の判定ルール自体を再掲していない。残る記述は「いつ・どの frontmatter に何を記録してから implement するか」という**手続き指示**であり、02_設計 が明示的に維持対象とした内容である。
- `project/自己拡張ワークフロー.md` は grandfather 発効日・env トグル名・`gh` コマンド等の**本リポ固有の具体値／具体手順の正本**であり、抽象原則は既に source 側を参照している。これは重複ではなく配置境界（コア抽象／project 具体）どおりの正しい正本分担である。

したがって「判定ルールの再掲」は両ファイルに存在せず、参照化の追加短縮は不要と判定した（02_設計 の条件付きタスクの条件が不成立）。これは実装フェーズの変更ファイル一覧に両ファイルが含まれないことと整合する（対応漏れではない）。

---

## 3. テスト結果の確認（独立実測）

### 3.1 単体・一括テスト

- **実行**: `bash test/run-all.sh`
- **結果**: 合計=21 PASS=15 FAIL=0 SKIP=6（exit 0）。FAIL 0 件。

### 3.2 audit.sh 回帰確認（挙動不変の証左）

- **audit.sh の diff がコメントのみであること**: `git diff main -- .agent-skill-chain/source/enforcement/ci/audit.sh` の追加・削除行のうち `#` コメント・空行・diff ヘッダを除いた**実行可能行の差分は 0 件**。関数本体は無改造であり、PASS/FAIL/SKIP の判定ロジックは構造的に不変。
- **audit.sh 実行**: `bash .agent-skill-chain/source/enforcement/ci/audit.sh .`（worktree）を実行。本変更に起因する新規 FAIL は無い。残存 FAIL はすべて本 3 issue の変更ファイル外（他サブ issue 163129/163502/163531 の 03/04 未整備、`src/agents-md.ts` の #26、`docs/00_review/*` の #37）で、main baseline にも同一に存在する既知事項。

### 3.3 verify フェーズで検出・修正した新規 FAIL 1 件

- **事象**: 追加した `163330/02_設計.md` の失敗条件表に `#7 TODO/FIXME 残存` の行があり、audit #7（重要パス TODO/FIXME 走査。ランナーの ugrep バックエンド `grep` で発火）が当該 issue ドキュメントを新規 FAIL とした。
- **修正**: 当該行の裸の `TODO`/`FIXME` トークンをインラインコード（バッククォート）で囲み、audit のインラインコード除去 sed で走査前に落ちるようにした（意味・表示は保持）。修正後、audit #7 の当該ファイル FAIL は解消（再実測で no match）。README の同一表記が発火しないのは #7 の走査対象が workflow/runtime の issue ディレクトリに限られ source を含まないためで、issue ドキュメント側は明示的な回避が必要だった。

### 3.4 受け入れ基準 × 実測

| 受け入れ基準（163330） | 実測 | 結果 |
| ---------------------- | ---- | ---- |
| 失敗条件の {概要・実装所在・実装状態・強制レベル} の正本が README 1 か所に集約 | README §失敗条件と差し戻し 表 A が 6 列レジストリ化（実読） | PASS |
| audit.sh ヘッダ・README 散文が要点 1 行＋参照へ短縮 | audit.sh ヘッダが `(N)→README #M` 索引化・diff はコメントのみ | PASS |
| 共通前提の共有ノート化でセルが有意に短縮 | `### ゲート共通の前提` 新設・#32〜#36 が「共通前提ノート参照＋固有 env」へ短縮 | PASS |
| 核: 1 ゲート変更時の同期箇所が 6 箇所から削減 | 詳細正本が README レジストリ 1 か所に集約、audit.sh は参照へ | PASS |
| BDD: audit.sh 関数本体不変・diff はヘッダコメントのみ | 実行可能行の差分 0 件（§3.2） | PASS |
| BDD: PASS/FAIL/SKIP 出力が変更前後で一致 | コメントのみ差分＋run-all 全 PASS＋新規 FAIL 無し | PASS |

---

## 4. コードレビュー

- **可読性**: 統制語彙 4 種を表直前に定義し、各行の `実装状態` 列が語彙に一致。読者が実装/未実装を一目で判別可能。
- **保守性**: 詳細正本が 1 レジストリへ集約され、ゲート追加時の同期対象が減少。共通前提ノートで grandfather/トグルの重複が縮小。
- **回帰安全性**: audit.sh はコメントのみ変更で判定ロジック不変。表 B（判定ルール）・表 C（差し戻し）は残置し、DESIGN.md 等の既存参照・見出し `## 失敗条件と差し戻し` を保全。

### 4.1 指摘事項

- **指摘 1（軽微・対応済み）**: issue ドキュメント表内の裸 `TODO/FIXME` 表記が audit #7 を誤発火（§3.3）。バッククォート囲みで解消済み。
- **指摘 2（情報・対応不要）**: workflow.db は worktree で空（スキーマ未生成）かつ gitignore 配下のため PR/CI に載らず、DB 系 audit（#29 等）は worktree・CI とも SKIP。本 PR の合否に影響しない。

---

## docs 更新

- 要否: 不要
- 対象: なし
- 理由: 本変更は enforcement 基盤（`.agent-skill-chain/source/enforcement/` の README とスクリプトのヘッダコメント）の内部整理に閉じ、`docs/` 配下のシステム仕様書（機能・画面・データ・API）の内容には影響しないため。

---

## 5. 設計・境界の確認

- **設計原則の準拠**: 「重複禁止・正本 1 か所」原則の自己適用。失敗条件の詳細正本を README レジストリへ集約し、audit.sh ヘッダ・散文・他ファイルは参照に徹する。単一責務（`spec §単一責務`）に整合。
- **境界の維持**: コア抽象（source）と project 具体（grandfather 発効日・env 名・`gh` 手順）の配置境界を保持。run_command は手続き指示、project は具体値、README は判定正本、と責務分離を崩していない。
- **依存の一方向性**: 参照は audit.sh ヘッダ／散文／run_command／project → README レジストリの一方向。循環参照なし。

### 5.1 重要判断の根拠（evidence_source）

| 判断 | evidence_source | 備考 |
| ---- | --------------- | ---- |
| audit 判定挙動が不変 | test_output / existing_code | audit.sh 実行可能行 diff 0 件・run-all 全 PASS |
| 新規 FAIL は #7 の 1 件のみで修正済み | test_output | 修正後 audit #7 当該ファイル no match |
| run_command/project の追加短縮は不要 | existing_code | 両ファイルは参照形／具体値正本で判定ルール再掲なし |

---

## 6. REVIEW_DUAL_LENS（#27・両リスト必須）

### A. 敵対的観点（壊れ得る所を能動的に探した結果）

1. **audit の判定が実は変わっていないか** → `git diff main` の実行可能行差分 0 件（コメントのみ）を機械確認。run-all 全 PASS。**挙動不変**。
2. **表構造変更で既存参照（DESIGN.md 名指し・見出し）が壊れないか** → 見出し `## 失敗条件と差し戻し`・表 B/表 C を残置。**破損なし**。
3. **欠落行追加が新規 audit FAIL を誘発しないか** → #7 行の裸トークンが #7 を誤発火 → バッククォート囲みで解消（§3.3）。**残存誤発火なし**。
4. **未実装ラベルの機械抽出が破れないか** → 表 A の未実装 7 行が `未実装（...）` で始まる統制語彙を持ち grep 抽出可能。**破れなし**。
5. **run_command/project を「参照化漏れ」として放置していないか** → 両ファイルは既に参照形／具体値正本であり判定ルール再掲なしと点検（§2.3）。**放置ではなく正しい配置**。
6. **workflow.db 未記録が PR を止めないか** → DB は gitignore・CI で DB checks SKIP。**PR 合否に非影響**。

### B. must-preserve（壊してはならない既存挙動・保持を確認）

1. **audit.sh の PASS/FAIL/SKIP 判定ロジック** → 関数本体無改造で保持。
2. **見出し `## 失敗条件と差し戻し`（外部参照アンカー）** → 維持。
3. **表 B（判定ルール一覧）・表 C（差し戻し先）の既存行** → 残置し #37 を追加したのみ。
4. **grandfather 発効日・env トグルの既定値** → 共通前提ノートへ集約したが値は不変。
5. **コア抽象／project 具体の配置境界** → 維持。

---

## 7. 総合評価

- **総合評価**: **PASS**。3 件の受け入れ基準を README レジストリ 1 回の構造変更で同時達成。audit.sh はコメントのみ変更で判定不変。verify で検出した #7 誤発火 1 件を修正済み。本変更に起因する新規 FAIL は無い。

---

## 8. 前のステップ・次のステップ

- **前**: [`03_実装計画.md`](./03_実装計画.md)
- **次**: 本レビュー承認後、orchestrator が commit / push / PR を握る（本 issue は PR で main へ統合、PR マージは進行役・ユーザー判断）。
