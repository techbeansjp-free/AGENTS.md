---
document_id: "7f50dfa3-b4c9-48e2-8c58-35f19a2d08b0"
---

# 実装計画書: AGENTS.md 実在しない参照先の修正

**プロジェクト名**: AGENTS.md 実在しない参照先の修正
**作成日**: 2026 年 07 月 14 日
**最終更新**: 2026 年 07 月 14 日

> **用語**: [.agent-skill-chain/source/CONCEPTS.md §用語規約](../../../../../../../.agent-skill-chain/source/CONCEPTS.md#用語規約) を参照。

---

## 1. 実装概要

### 1.1 実装方針（必須）

`AGENTS.md:62` の「.agent-skill-chain/source/workflow 配下の issue 作成関連ドキュメントを参照」を、実在する `.agent-skill-chain/source/workflow/PHASES.md §一般的な issue 作成ステップ` への具体的な Markdown リンク参照に修正する。

### 1.2 実装順序（必須: 1 件以上）

1. `AGENTS.md:62` の該当箇所をリンク付き具体参照に修正する。
2. リンク先（PHASES.md の見出しアンカー）が実在することを確認する。

---

## 2. タスク分解

### 2.1 タスク 1: AGENTS.md:62 の参照修正

#### 2.1.1 概要（必須）

`AGENTS.md:62` の参照記載を `.agent-skill-chain/source/workflow/PHASES.md §一般的な issue 作成ステップ` への具体リンクに修正する。

#### 2.1.2 実装内容（必須: 1 件以上）

- `AGENTS.md:62` 末尾の「詳細は .agent-skill-chain/source/workflow 配下の issue 作成関連ドキュメントを参照。」を「詳細は [.agent-skill-chain/source/workflow/PHASES.md §一般的な issue 作成ステップ](.agent-skill-chain/source/workflow/PHASES.md#一般的な-issue-作成ステップ) を参照。」に置換する。

#### 2.1.3 テスト観点（必須）

##### 単体（本タスクの具体ケース・必須 1 件以上）

- `AGENTS.md:62` に「issue 作成関連ドキュメント」という実在しない参照名が残っていないこと（grep で非該当）。
- `AGENTS.md:62` のリンク先ファイル `.agent-skill-chain/source/workflow/PHASES.md` が実在すること。
- リンク先の見出し「一般的な issue 作成ステップ」が `PHASES.md` 内に実在すること。

##### 結合/API（本タスクの具体ケース・該当時は必須 1 件以上）

- 該当なし（単一ファイルの参照修正）。

##### E2E/受け入れ（本タスクの具体ケース・未達時は理由記載）

- 該当なし（実行系の振る舞いを変更しないドキュメント修正のため E2E は不要）。

##### バリデーション（該当する場合の具体ケース）

- 該当なし。

#### 2.1.4 単体テスト仕様（BDD）（必須）

```gherkin
Feature: AGENTS.md:62 の参照先修正
  Scenario: 実在する具体節への参照に修正されている
    Given AGENTS.md:62 を修正済みである
    When AGENTS.md:62 のリンク先ファイル・見出しを確認する
    Then .agent-skill-chain/source/workflow/PHASES.md が実在し「一般的な issue 作成ステップ」という見出しを含む
```

**テストコード例（シェルによる静的検証）**:

```bash
# Given: AGENTS.md:62 を修正済みである
# When: リンク先ファイルと見出しの実在を確認する
# Then: ファイルが存在し、見出しが一致する
test -f .agent-skill-chain/source/workflow/PHASES.md
grep -n "一般的な issue 作成ステップ" .agent-skill-chain/source/workflow/PHASES.md
grep -n "issue 作成関連ドキュメント" AGENTS.md || echo "OK: 実在しない参照名は残っていない"
```

#### 2.1.5 依存関係

- 依存なし（単独タスク）。

#### 2.1.6 見積もり

- **工数**: 5 分未満。
- **優先度**: 低。

---

## テスト観点

参照先ファイル・見出しの実在を grep / test コマンドで確認する静的検証。実行系コードの変更が無いため、単体/結合/E2E テストコードは不要。

---

## 単体テスト

`grep` / `test -f` による静的検証（2.1.4 参照）。

---

## BDD

2.1.4 のシナリオ 1 件（参照先の実在確認）。

---

## 3. 実装スケジュール

### 3.1 マイルストーン

| マイルストーン | 期日 | 成果物 |
| --- | --- | --- |
| 参照修正・検証完了 | 即日 | 修正済み `AGENTS.md`、04_review.md |

---

## 4. テスト計画

grep / test コマンドによる静的確認のみ。回帰対象は無し（影響範囲がドキュメント 1 行のため）。

---

## 5. リスク管理

### 5.1 実装リスク

- **リスク**: 将来 `PHASES.md` の見出しが変更・削除された場合、AGENTS.md の参照が再びリンク切れになる。
  - **対策**: `PHASES.md` の見出し変更時は AGENTS.md からの被参照有無を確認する運用を徹底する（保守性）。

---

## 6. 参考資料

### プロジェクトドキュメント

- [`02_設計.md`](./02_設計.md) - 設計

---

## 7. 前のステップ

- **前**: [`02_設計.md`](./02_設計.md) - 設計フェーズ

---

## 8. 次のステップ

- 実装フェーズ（implement-feature）に直接進む（単一 issue のためサブ issue 分割なし）。
