---
document_id: "e19d4aac-9821-44ed-92ce-a1ac24239586"
---

# 実装計画書: 配備済み hook と正本の乖離検知スクリプト

**プロジェクト名**: 配備済み PreToolUse.sh が正本より陳腐化しロックアウト既知バグ修正が未反映
**作成日**: 2026年07月14日
**最終更新**: 2026年07月14日

---

## 1. タスク一覧

### タスク1: check-hook-drift.sh の新規作成

- **対象**: `.agent-skill-chain/source/scripts/check-hook-drift.sh`（新規）
- **内容**: 02_設計 §2.2 の仕様どおり、正本 `enforcement/claude/` と `<project_root>/.claude/hooks/` の内容を sha256 で比較する read-only スクリプトを実装する。実行権限（`chmod +x`）を付与する。
- **完了条件**: `bash -n` で構文エラーが無い。

### タスク2: enforcement/README.md への運用ルール追記

- **対象**: `.agent-skill-chain/source/enforcement/README.md`
- **内容**: 「正本 `enforcement/claude/*.sh` を修正した場合は配備済み環境で `upgrade` を実行し `.claude/hooks/` を同期すべきこと」「乖離の有無は `check-hook-drift.sh` で read-only に確認できること」を追記する。既存節の記述と重複しない場所（配備関連の記述がある節、または新設の小節）に追記する。
- **完了条件**: 追記箇所が既存の見出し構成と整合し、他節の記述と矛盾・重複が無い。

### タスク3: tmp 隔離環境での動作確認（差分あり/なし両ケース）

- **対象**: `check-hook-drift.sh`（手動検証。自動テストファイルの新規追加は行わない＝小規模スクリプトのため、実行ログを 04_review に記録する形で検証する）
- **内容**: `mktemp -d` で隔離環境を作り、以下を確認する。
  1. 正本と同一内容の配備物を置いた場合 → 全件 `[OK]`・終了コード 0。
  2. 配備物の内容を 1 バイトでも変えた場合 → 該当ファイルが `[DRIFT]`・終了コード 1。
  3. 配備物のファイル自体を削除した場合 → 該当ファイルが `[DRIFT]`（未配備）・終了コード 1。
  4. `.claude/hooks/` 自体が存在しない場合 → `[INFO]`・終了コード 0。
- **完了条件**: 上記 4 ケースすべてで期待どおりの出力・終了コードが得られる。本リポジトリの実 `.claude/`（メイン作業ツリー・他 worktree）は一切変更しない。

### タスク4: 00_要求定義.md frontmatter の branch 記録

- **対象**: `00_要求定義.md`
- **内容**: frontmatter `branch:` を実際の作業ブランチ名に更新する。

## 2. テスト計画

- タスク3の 4 ケースを tmp 隔離環境で実行し、結果を 04_review.md の「テスト結果の確認」に記録する。
- 既存スクリプト（`sync-version.sh` 等）・既存テストへの回帰が無いことを確認する（本スクリプトは新規追加のみで既存ファイルを変更しないため、既存 `test/*.sh` への影響は無い想定。念のため `test/` 配下に本スクリプトを参照するテストが無いことを確認する）。

## 3. 完了条件（DoD）

- タスク1〜4が完了している。
- タスク3のテストが全件期待どおりの結果となっている。
- 04_review.md が作成され、受け入れ基準の確認が記録されている。
- 「実際の本番 `.claude/` への同期は orchestrator が別途ユーザー許可を得て実施すべき」という申し送りが 00・04 に明記されている。
