---
document_id: "4bfb80b0-5a7b-42f7-8324-4021a44ddeb3"
issue_id: "f7c09455-cb99-4f67-b4d9-7f6c7858836e"
---

# レビュー書: 配備済み hook と正本の乖離検知スクリプト

**プロジェクト名**: 配備済み PreToolUse.sh が正本より陳腐化しロックアウト既知バグ修正が未反映
**作成日**: 2026年07月14日
**最終更新**: 2026年07月14日

> **重要**: 本レビューは verify-and-close command（skill chain: generate-scenarios → map-coverage → review-code → review-architecture → write-workflow-log 相当）に従い、issue 難易度（小・独立した read-only 診断スクリプト 1 本の新規追加）に見合った quick レビューとして実施した。

> **申し送り（重要・スコープ確定事項）**: 本 issue は「乖離を検知する仕組み」の追加のみを対象とする。**実際に配備済み本番環境の `.claude/`（メイン作業ツリー `/home/adachi/projects/AGENTS.md/.claude/` 等）へ `upgrade`/`setup` を実行して同期させる操作は、本 issue のスコープ外である。** 今回 main にマージ済みの `PreToolUse.sh` 正本修正（他 issue: allowlist への `Skill` 追加等）を配備物へ反映する作業は、稼働中の他セッションの実行環境に影響しうるため、**別途ユーザーの許可を得たうえで orchestrator が手動実行すべき事項**とする。この判断は [01_要件定義.md](./01_要件定義.md) 冒頭の判断欄にも明記済み。

---

## 1. レビュー概要

### 1.1 レビュー目的（必須）

実装内容の確認・品質保証（`check-hook-drift.sh` の新規追加、`enforcement/README.md` への運用ルール追記が 02_設計・03_実装計画の内容に一致し、01 の受け入れ基準・00 の成功基準を満たすかの検証）。

### 1.2 レビュー対象（必須）

- **実装範囲**: 03_実装計画のタスク1（`check-hook-drift.sh` 新規作成）、タスク2（`enforcement/README.md` への運用ルール追記）、タスク3（tmp 隔離環境での動作確認）、タスク4（00 frontmatter の branch 記録）。
- **レビュー期間**: 2026-07-14 ～ 2026-07-14
- **レビュー担当者**: verify-and-close 委譲サブエージェント（実装・レビュー兼務）

---

## 2. 実装内容の確認

### 2.1 実装完了タスク

| タスク名 | 実装内容 | 実装日 | 担当者 | ステータス |
| ------------ | ----------------- | -------- | ---------- | -------------------------------------- |
| タスク1 | `check-hook-drift.sh` 新規作成 | 2026-07-14 | 実装担当サブ | 完了 |
| タスク2 | `enforcement/README.md` へ乖離検知・upgrade 運用ルールを追記 | 2026-07-14 | 実装担当サブ | 完了 |
| タスク3 | tmp 隔離環境で 4 ケースの動作確認 | 2026-07-14 | 実装担当サブ | 完了 |
| タスク4 | 00 frontmatter の `branch` を実ブランチ名に更新 | 2026-07-14 | 実装担当サブ | 完了 |

### 2.2 実装内容の詳細

#### タスク1: check-hook-drift.sh の新規作成

- **実装内容**: `.agent-skill-chain/source/scripts/check-hook-drift.sh` を新規作成。正本 `enforcement/claude/`（`<スクリプト位置>/../enforcement/claude/`）配下のトップレベル通常ファイル（`.gitkeep` 除く）と、`<project_root>/.claude/hooks/`（既定 `pwd`、第 1 引数で指定可）配下の同名ファイルを sha256（`sha256sum`／フォールバック `shasum -a 256`）で比較する read-only スクリプト。実行権限を付与済み（`chmod +x`）。
- **変更ファイル**: `.agent-skill-chain/source/scripts/check-hook-drift.sh`（新規）
- **確認事項**: 02_設計 §2.2 の仕様（引数・比較対象・出力・終了コード）どおりに実装。`.claude/` への書き込み処理は一切含まれないことをコードレビューで確認済み（`cp`/`>`/リダイレクトによる書き込みが無い。標準出力への `echo` のみ）。

#### タスク2: enforcement/README.md への運用ルール追記

- **実装内容**: `.agent-skill-chain/source/enforcement/README.md` §配置するファイル一覧 内、既存の「成果物パス（PROTECTED_PATHS）」段落の直後に新規段落「配備済み hook と正本の乖離検知（アップグレード運用ルール）」を追加。正本修正時は `upgrade` を実行して `.claude/hooks/` を同期すべきこと、乖離の有無は `check-hook-drift.sh` で read-only に確認できること、本スクリプトは検知のみで同期は行わないことを明記。
- **変更ファイル**: `.agent-skill-chain/source/enforcement/README.md`
- **確認事項**: 既存の他節（系統D の hooks overlay 抽象仕様等）と記述が重複・矛盾しないことを確認。CORE.md への追記は 02_設計 §3 の検討により見送り（CORE.md は実行契約の正本に責務を限定しており、配備物同期という運用手順の追記は責務境界を越えるため。CORE.md §境界「実行契約の正本は本ファイル 1 か所のみ」との整合を優先した）。

#### タスク3: tmp 隔離環境での動作確認

下記 4 ケースを `mktemp -d` 相当の隔離ディレクトリ（`/tmp/claude-1000/.../scratchpad/hook-drift-test/`。本リポジトリの実 `.claude/` は一切変更していない）で実行した。

**ケース1: 正本と配備物が一致**

```
$ bash check-hook-drift.sh <tmp>/case1
[OK]    PostToolUse.sh: 正本と一致
[OK]    PreToolUse.sh: 正本と一致
[OK]    README.md: 正本と一致
[OK]    orchestrator-allowlist.example.txt: 正本と一致

check-hook-drift: 乖離なし。
exit=0
```

**ケース2: 配備物の内容が正本と不一致（1 行追記して改変）**

```
$ bash check-hook-drift.sh <tmp>/case2
[OK]    PostToolUse.sh: 正本と一致
[DRIFT] PreToolUse.sh: 正本と配備済みファイルの内容が一致しません。
        正本   : .../enforcement/claude/PreToolUse.sh
        配備先 : <tmp>/case2/.claude/hooks/PreToolUse.sh
[OK]    README.md: 正本と一致
[OK]    orchestrator-allowlist.example.txt: 正本と一致

check-hook-drift: 乖離を検知しました（上記 [DRIFT] を参照）。
exit=1
```

**ケース3: 配備先にファイル自体が存在しない（PostToolUse.sh を削除）**

```
$ bash check-hook-drift.sh <tmp>/case3
[DRIFT] PostToolUse.sh: 配備先に存在しません（.../case3/.claude/hooks/PostToolUse.sh）。upgrade 未実行の可能性があります。
[OK]    PreToolUse.sh: 正本と一致
[OK]    README.md: 正本と一致
[OK]    orchestrator-allowlist.example.txt: 正本と一致

check-hook-drift: 乖離を検知しました（上記 [DRIFT] を参照）。
exit=1
```

**ケース4: .claude/hooks/ 自体が存在しない（enforcement 未 opt-in）**

```
$ bash check-hook-drift.sh <tmp>/case4
[INFO] <tmp>/case4/.claude/hooks が存在しません（enforcement 未配備、または opt-in 前の環境）。乖離チェック対象外として終了します。
exit=0
```

**追加ケース: 正本ディレクトリが見つからない前提エラー**（スクリプトのみを孤立したディレクトリへコピーして実行）

```
エラー: 正本ディレクトリが見つかりません: <tmp>/isolated-no-source/scripts/../enforcement/claude
exit=2
```

全 5 ケースで 01 §2.1 の受け入れ基準・02_設計 §4 のテスト観点どおりの出力・終了コードを得た。検証後、`<tmp>` 配下は削除済み（本リポジトリの `.claude/`・他 worktree には一切書き込みを行っていない。`git status --porcelain` で新規追加ファイル以外の差分が無いことも確認済み）。

#### タスク4: 00 frontmatter の branch 記録

- **実装内容**: `00_要求定義.md` frontmatter の `branch` を `worktree-agent-a8b6509e077823ec2`（文書作成セッションの名残）から実際の作業ブランチ `worktree-agent-a2c1d2c423f9c3510` へ更新。
- **変更ファイル**: `00_要求定義.md`

---

## 3. テスト結果の確認

### 3.1 単体テスト

本 issue は独立した read-only 診断スクリプト 1 本の新規追加であり、既存の自動テストスイート（`test/*.sh`）には対象が無い。01 §4 成功基準・03 §2 テスト計画のとおり、tmp 隔離環境での手動シミュレーション（上記§2.2 タスク3）を検証手段として採用した（新規の自動テストファイル追加は 01 §3 除外要件のとおりスコープ外とした。理由: 小規模な独立診断スクリプトであり、将来 `doctor` へ組み込む場合に併せて自動テスト化する方が二重管理を避けられるため）。

- **実行日**: 2026-07-14
- **検証ケース数**: 5（一致・内容不一致・未配備ファイル・hooks 未配備・前提エラー）
- **成功**: 5
- **失敗**: 0

### 3.2 統合テスト

該当なし（独立スクリプトであり他コンポーネントとの統合を伴わない。`setup.sh`・`bin/agents-md.js` は本 issue で変更していない）。

### 3.3 E2E テスト

`check-hook-drift.sh` を実際にシェルから実行し、正本ディレクトリの実ファイル（`enforcement/claude/PreToolUse.sh` 等）をコピーして作った隔離配備先に対して動作させており、実運用時の呼び出し経路と同一の経路を検証済み（モック無し）。

### 3.4 回帰確認

本 issue は新規ファイル追加（`check-hook-drift.sh`）と `enforcement/README.md` への追記のみであり、既存ファイル（`setup.sh`・`bin/agents-md.js`・`PreToolUse.sh` 等）を変更していない。`git status --porcelain` で意図した新規ファイルのみが変更対象であることを確認しており、既存スクリプト・既存テストへの回帰は無い。

---

## 4. コードレビュー

### 4.1 コード品質

| 観点 | 確認内容 | 結果 | コメント |
| -------------- | ---------------------- | ------------------------------ | -------------------------------- |
| 可読性 | 既存 `sync-version.sh` と同水準のヘッダーコメント（背景・使い方・比較対象・終了コード・対応する運用ルール）を備えているか | OK | ヘッダーコメントに背景・仕様・運用ルールへの参照を明記 |
| 保守性 | 比較対象ファイル一覧をハードコードせず正本ディレクトリの実体から動的導出しているか | OK | `for f in "$SOURCE_DIR"/*` で動的導出。`setup.sh` の `copy_owned_files` と同一規則（`.gitkeep` 除外） |
| 安全性 | `.claude/` への書き込み処理が存在しないか（read-only であること） | OK | コードレビューで `cp`/リダイレクトによる書き込みが無いことを確認。`set -euo pipefail` で未定義変数・エラー時の暴走も防止 |
| 互換性 | `sha256sum` 不在環境（macOS 等）へのフォールバックがあるか | OK | `command -v sha256sum` 判定で `shasum -a 256` にフォールバック |
| 対象外の越境がないか | `bin/agents-md.js`・CI（audit.sh）への組み込みを行っていないか | OK | 01 §3 除外要件どおり、対象ファイル一覧（本 issue の作業指示）の範囲内のみを変更 |

### 4.2 敵対的観点（アドバーサリアルレビュー）

- **懸念**: 本スクリプトを誤って稼働中の本番 `.claude/`（メイン作業ツリー）に対して実行してしまうリスクはないか。
  - **確認結果**: スクリプトは read-only（書き込み処理を含まない）であるため、誤って実行対象に本番ディレクトリを指定しても内容が破壊されることはない。最悪でも `[DRIFT]` の警告が出力されるのみ。
- **懸念**: `.claude/hooks/` 配下にユーザー独自ファイル（正本に存在しないファイル）がある場合に誤検知しないか。
  - **確認結果**: 比較のループは正本側 `enforcement/claude/*` を起点に回すため、配備先にのみ存在する独自ファイルは比較対象に含まれず誤検知しない（`setup.sh` の `copy_owned_files` が正本側ファイルのみを上書きコピーし、配備先の独自ファイルを保持する設計と対称的に安全側）。
- **懸念**: 大量ファイルや巨大ファイルでの性能劣化はないか。
  - **確認結果**: `enforcement/claude/` はトップレベル数ファイルのみで小規模（現状 4 ファイル）。sha256 計算はテキストファイル規模で無視できるコスト。
- **懸念**: シンボリックリンクや特殊ファイルが正本・配備先に混在した場合の挙動。
  - **確認結果**: `[[ -f "$f" ]]` で通常ファイルのみを対象とし、ディレクトリ・特殊ファイルはスキップする（`sync-version.sh`/`copy_owned_files` と同様の判定方式）。

### 4.3 must-preserve（保持すべき既存挙動）

- 既存の `setup.sh`（`copy_owned_files`・`upgrade`/`init` の配備ロジック）は無変更。
- 既存の `bin/agents-md.js`（`doctor`/`upgrade`/`enforce` 等の CLI サブコマンド）は無変更。
- 既存の `enforcement/claude/PreToolUse.sh`・`PostToolUse.sh` の内容自体は無変更（本 issue は「検知」のみを追加し、実際のフック挙動・配備物には一切手を入れていない）。
- 既存の `enforcement/README.md` の既存節（強制の4層、失敗条件対応表、系統A〜E 等）は既存文言を変更せず、新規段落の追加のみに留めた。

### 4.4 指摘事項

- 指摘なし（要修正 0 件）。

---

## 5. ドキュメントの確認

### 5.1 ドキュメント更新状況

| ドキュメント | 更新状況 | 確認者 | 確認日 |
| ------------------------------------ | ----------------- | -------- | ------ |
| [`00_要求定義.md`](./00_要求定義.md) | 更新済み（branch frontmatter） | レビュワー | 2026-07-14 |
| [`01_要件定義.md`](./01_要件定義.md) | 新規作成 | レビュワー | 2026-07-14 |
| [`02_設計.md`](./02_設計.md) | 新規作成 | レビュワー | 2026-07-14 |
| [`03_実装計画.md`](./03_実装計画.md) | 新規作成 | レビュワー | 2026-07-14 |
| `.agent-skill-chain/source/enforcement/README.md` | 更新済み（乖離検知・upgrade 運用ルール追記） | レビュワー | 2026-07-14 |

### 5.2 ドキュメントの整合性

- **実装と設計の整合性**: 整合している（02_設計 §2.2 のスクリプト仕様どおりに実装、§3 の CORE.md 見送り判断どおり CORE.md は無変更）。
- **要件と実装の整合性**: 整合している（01 §2.1 の受け入れ基準を §3.1 のテスト結果で充足）。
- **コメント**: なし。

---

## 6. パフォーマンス確認

該当なし（数ファイルの sha256 計算のみであり、性能特性への影響は無い）。

---

## 7. セキュリティ確認

### 7.1 セキュリティチェック

| 項目 | 確認内容 | 結果 | コメント |
| ---------- | ---------- | ----------- | ---------- |
| read-only 保証 | 本スクリプトが `.claude/` への書き込みを一切行わないか | OK | コードレビューで書き込み処理の不在を確認済み（§4.1・§4.2） |
| スコープ逸脱防止 | 本番 `.claude/`（メイン作業ツリー）への実同期を本 issue で行っていないか | OK | 実装・検証はすべて tmp 隔離環境のみで実施。本番 `.claude/` は未変更 |
| 対象ファイル遵守 | 00 の「対象ファイル」制約範囲内のみを変更しているか | OK | 変更ファイルは §5.1 の一覧のみ（`bin/agents-md.js`・CI 追加は行っていない） |

---

## 8. 受け入れ基準・成功基準のカバレッジ確認（map-coverage）

| 基準 | 出典 | 検証方法 | 結果 |
| ---- | ---- | -------- | ---- |
| 正本と配備物が一致する場合 `[OK]`・終了コード 0 | 01 §2.1 受け入れ基準1 | ケース1 | 充足（PASS） |
| 内容不一致・未配備の場合 `[DRIFT]`・終了コード 1 | 01 §2.1 受け入れ基準2 | ケース2・ケース3 | 充足（PASS） |
| `.claude/hooks/` 未配備環境では `[INFO]`・終了コード 0 | 01 §2.1 受け入れ基準3 | ケース4 | 充足（PASS） |
| 同期（書き込み）を一切行わない | 01 §2.1 受け入れ基準4 | コードレビュー（§4.1・§4.2）＋実行後の `git status` 確認 | 充足（PASS） |
| 差分あり/なし両ケースを tmp 隔離環境でシミュレート | 00 §6 成功基準／01 §4 成功基準 | ケース1〜4＋前提エラーケース | 充足（PASS） |
| upgrade 運用ルールが enforcement/README.md に明記される | 00 §6 成功基準／01 §4 成功基準 | §2.2 タスク2 | 充足（PASS） |
| 本番 `.claude/` への実同期はスコープ外である旨の申し送り明記 | 00 制約／01 §4 成功基準 | 本書冒頭「申し送り」・01 冒頭「判断」欄 | 充足（PASS） |

未達項目: なし。

---

## 9. 設計・境界の確認（review-architecture）

### 9.1 設計の確認

- **設計原則の準拠**: 単一責務・明確な境界・単一正本（02_設計 §1.2）を維持。比較対象ファイル一覧のハードコード一覧を持たず、正本ディレクトリの実体から動的導出する設計により、正本の構成変更に追従できる。
- **命名規則**: 既存 `.agent-skill-chain/source/scripts/` 配下のスクリプト命名規則（`kebab-case.sh`）に準拠。

### 9.2 境界・依存の確認

- **責務の境界**: 「検知」と「同期」の責務を明確に分離した。同期（`upgrade`/`setup.sh`）の既存責務には一切変更を加えていない。
- **依存関係**: 新規の外部依存なし（`bash`・`sha256sum`/`shasum` のみ。いずれも既存スクリプト群と同じ前提）。
- **越境の有無**: `bin/agents-md.js`（CLI 本体）・CI（audit.sh）への組み込みは行っておらず、01 §3 除外要件・00 の対象ファイル制約を逸脱していない。
- **指摘・推奨**: 将来的に `doctor` サブコマンドへ本チェックを組み込む場合は、`bin/agents-md.js` の変更を伴うため別 issue とすることを推奨する（01 §3・02_設計 §2.3 のとおり）。

### 9.3 重要判断の根拠（evidence_source）

| 判断内容 | evidence_source | 備考 |
| -------------------- | ------------------------------------- | ------------------------ |
| CORE.md は変更せず enforcement/README.md に運用ルールを追記する | existing_code | CORE.md §境界「実行契約の正本は本ファイル 1 か所のみ」の記述を確認したうえでの判断（02_設計 §3） |
| 比較対象ファイル一覧は `copy_owned_files` と同一規則（動的導出・`.gitkeep` 除外） | existing_code | `.agent-skill-chain/source/scripts/setup.sh` の `copy_owned_files` 関数の実装を確認済み |
| 本番 `.claude/` への実同期は本 issue のスコープ外 | user_instruction | 00 §制約・タスク委譲時の明示指示（本 issue 特有の制約） |
| 5 ケースの動作確認結果（PASS=5, FAIL=0） | test_output | 本書 §2.2 タスク3 に記載の実行ログ |

---

## docs 更新（DOCS_RULES §継続追随ゲート）

- 要否: 不要
- 対象: なし
- 理由: `docs/04_機能設計/スクリプト群/README.md` 等のシステム仕様書を確認したが、`.agent-skill-chain/source/scripts/` 配下の個別スクリプト一覧を網羅列挙する形にはなっておらず、本追加は既存の抽象的な記述（スクリプト群による配備・検証補助）の範囲内である。README.md（enforcement 正本）自体への追記は本 issue の作業そのものであり、別途 docs/ 追随は不要と判断した。

---

## 10. 課題と改善点

### 10.1 発見された課題

- なし。

### 10.2 改善提案

- 将来的に `check-hook-drift.sh` を `agents-md doctor` に組み込み、`doctor` 実行時に自動的に乖離を警告できるようにすると利便性が上がる（01 §3・02_設計 §2.3 のとおり本 issue のスコープ外。別 issue 候補）。
- 本番の `.claude/` への実際の同期（`upgrade` 実行）は、本 issue のスコープ外・別途ユーザー許可を得て orchestrator が手動実行する事項として申し送る（本書冒頭「申し送り」参照）。

---

## 11. システム仕様書の更新

該当なし（§docs 更新 のとおり不要と判定済み）。

---

## 12. レビュー結果

### 12.1 総合評価

- **実装品質**: 良好（既存 `sync-version.sh` と同水準の read-only 検証スクリプトを、既存の配備ロジック（`copy_owned_files`）と整合する形で追加）。
- **テスト品質**: 良好（一致・不一致・未配備ファイル・hooks 未配備・前提エラーの 5 ケースを隔離環境で検証、全 PASS）。
- **ドキュメント品質**: 良好（00〜04 が一貫し、運用ルールが enforcement/README.md に明記された）。
- **総合評価**: 完了（要修正なし）。

### 12.2 承認状況

- **レビュー承認者**: verify-and-close 委譲サブエージェント
- **承認日**: 2026-07-14
- **承認コメント**: 受け入れ基準・成功基準を全て充足し、回帰なし。本番 `.claude/` への実同期はスコープ外である旨を明記済み。要修正事項なし。

---

## 13. 参考資料

### 13.1 プロジェクトドキュメント

- [`00_要求定義.md`](./00_要求定義.md) - 要求定義
- [`01_要件定義.md`](./01_要件定義.md) - 要件定義
- [`02_設計.md`](./02_設計.md) - 設計
- [`03_実装計画.md`](./03_実装計画.md) - 実装計画

### 13.2 その他の参考資料

- `.agent-skill-chain/source/scripts/check-hook-drift.sh`
- `.agent-skill-chain/source/scripts/setup.sh`（`copy_owned_files`）
- `.agent-skill-chain/source/enforcement/README.md`

---

## 14. 前のステップ

このレビュー書は、以下のドキュメントを基に作成されています：

- **前**: [`03_実装計画.md`](./03_実装計画.md) - 実装計画フェーズ

---

## 15. 次のステップ

- 外部設定は不要のため、issue 完了。PR 作成に進む（マージは行わない）。
