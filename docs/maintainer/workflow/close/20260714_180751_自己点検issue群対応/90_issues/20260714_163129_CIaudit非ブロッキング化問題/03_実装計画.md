---
document_id: "9c4f7a2d-1e6b-4a8c-b3d5-7f2a9c6e4b81"
issue_id: "c0373076-361d-430f-b825-a2a14bc3e3d3"
---

# 実装計画書: CI audit の非ブロッキング化問題（最終保証層の形骸化）

**作成日**: 2026年07月14日

---

## 1. タスク分解

1. `.agent-skill-chain/source/enforcement/README.md`: 「ツール別強制力マトリクス」の CI 行・直後の解説（§101-104 付近）に、本リポジトリ（自己拡張・ドッグフーディング環境）の暫定例外への参照コメントを追加（詳細は自己拡張ワークフロー.md 参照、重複記載しない）。
2. `.agent-skill-chain/project/自己拡張ワークフロー.md`: 「### 3. 本リポ CI（self-enforce.yml）への PR_BODY 配線」節の直後に、audit step 全体の非ブロッキング運用の理由・解消条件・申し送りを記す新設節を追加。
3. `.github/workflows/self-enforce.yml`: step 7（Enforcement audit (non-blocking)）のコメントに、暫定運用である旨・解消条件（workflow.db の CI 追跡状況解消後に再判断）を一文追記。`continue-on-error: true` 等の機能行は変更しない。外部参照（ドキュメント名・章節番号・issue パス）は追加しない。
4. `00_要求定義.md`: frontmatter `branch` を実ブランチ名に更新（着手前に実施済み）。§9 に対応方針の確定内容を追記（実施済み）。
5. 検証: `check-comment-refs.sh .github/workflows` 実行、YAML 構文確認、`audit.sh` 無変更確認。
6. `04_review.md` 作成・完了報告。

## 2. テスト仕様（BDD、01 と対応）

| # | シナリオ（01 対応） | 検証方法 | 結果 |
|---|---|---|---|
| 1 | README.md に暫定例外への参照がある | `grep` で該当記載を目視確認 | 実装後に記載 |
| 2 | 自己拡張ワークフロー.md に理由・解消条件・申し送りがある | `grep` で該当記載を目視確認 | 実装後に記載 |
| 3 | self-enforce.yml の機能（continue-on-error・呼び出しコマンド）が不変 | `git diff` で当該 step の実行行に変更が無いことを確認 | 実装後に記載 |
| 4 | audit.sh 本体が無変更 | `git diff --stat` で対象外であることを確認 | 実装後に記載 |
| 5 | YAML 構文が壊れていない | Python `yaml.safe_load` 等で構文確認 | 実装後に記載 |
| 6 | コメント外部参照禁止 blocking step を壊さない | `bash check-comment-refs.sh .github/workflows` を実行し exit 0 | 実装後に記載 |

## 3. 対象外・非機能

- `audit.sh` のチェックロジック追加・変更は行わない（00 §5）。
- `self-enforce.yml` の `continue-on-error: true` は維持し、ブロッキング化はしない（ADR-1）。
