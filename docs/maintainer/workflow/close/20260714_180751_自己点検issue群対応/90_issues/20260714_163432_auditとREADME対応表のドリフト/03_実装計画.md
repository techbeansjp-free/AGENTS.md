---
document_id: "03fbb921-91f3-407b-8dc4-5a5d13eceaf9"
---

# 実装計画書: 対応表ドリフト解消（統合計画への参照＋差分）

**プロジェクト名**: audit と README 対応表のドリフト（バッチ5・統合の一部）
**作成日**: 2026 年 07 月 14 日
**最終更新**: 2026 年 07 月 14 日

> 統合実装計画の**正本**は `../20260714_163330_enforcement表セル長大重複/03_実装計画.md`。
> 本 issue の作業は同計画の**タスク T1（全項目突合）・T2（表 A 欠落補完）・T4（表 B/C への #37 追加）**に割り当てられる。本書は本 issue 固有のタスク差分のみ記す。

---

## 1. 実装概要

### 1.1 実装方針
統合 03 の T1/T2/T4 に本 issue の関心（audit.sh 実装済み全チェックの表 A/B/C への漏れなき記載）を織り込む。単独 PR は作らず 3 件同一 PR で実装。

### 1.2 実装順序
統合 03 の順序に従う（T1 突合 → T2 表 A 補完 → T4 表 B/C 補完）。

---

## 2. タスク分解

### 2.1 タスク A: audit.sh と表の網羅突合（統合 T1）

#### 2.1.1 概要（必須）
audit.sh の実装済み check 関数集合と表 A の掲載番号を突合し、欠落を実測確定する。

#### 2.1.2 実装内容（必須）
- `grep -nE 'check_[a-z0-9_]+\(\)' audit.sh` と `必須チェック:` (1)–(37) を集約。
- 表 A の `#N` 集合と差集合を取り、{#2,#4,#7,#21,#37} を確定（02 §2.1 の暫定表を実測で裏付け）。

#### 2.1.3 テスト観点（必須）
##### 単体（必須 1 件以上）
- 差集合が {#2,#4,#7,#21,#37} と一致する。
##### 結合/API
- 該当なし。
##### E2E/受け入れ
- 突合結果を memo に残す。
##### バリデーション
- 実装済みで表に無いチェックがゼロになる（補完後）。

#### 2.1.4 単体テスト仕様（BDD）
```gherkin
Feature: 網羅突合
  Scenario: 実装済みと表の差集合
    Given audit.sh の実装済みチェック集合
    When 表A の番号集合と突合する
    Then 差集合が #2 #4 #7 #21 #37 になる
```
```bash
# Given: audit.sh チェック集合と表A番号集合
# When: 差集合を計算
# Then: {#2,#4,#7,#21,#37}
```

### 2.2 タスク B: 表 A への欠落 5 行追加（統合 T2）

#### 2.2.1 概要（必須）
表 A に #2・#4・#7・#21・#37 の行を追加（実装の所在・実装状態・強制レベルを付与）。

#### 2.2.2 実装内容（必須）
- 統合 02 §3.2 の内容で 5 行を追加。#21 は `推奨（未強制）`／案内のみ、他 4 行は `実装済み`／CI FAIL。

#### 2.2.3 テスト観点（必須）
##### 単体（必須 1 件以上）
- 表 A に #2/#4/#7/#21/#37 行が存在（grep）。
##### 結合/API
- 該当なし。
##### E2E/受け入れ
- 該当なし。
##### バリデーション
- 追加行の実装状態が統制語彙に一致。

#### 2.2.4 単体テスト仕様（BDD）
```gherkin
Feature: 表A欠落補完
  Scenario: 5行追加
    Given 補完後の表A
    When 番号を検索する
    Then #2 #4 #7 #21 #37 の行がある
```
```bash
# Given: 補完後の enforcement/README.md
# When: 表A を grep
# Then: 5行が存在
```

### 2.3 タスク C: 表 B / 表 C への #37 追加（統合 T4）

#### 2.3.1 概要（必須）
表 B（判定ルール一覧）・表 C（差し戻し先の固定）に #37 行を追加。

#### 2.3.2 実装内容（必須）
- 表 B に #37 の失敗条件・説明・差し戻し先を追加。
- 表 C に #37 の修正対象・再実行手順を追加。
- 記述は audit.sh ヘッダ line 100–105 の #37 説明と整合。

#### 2.3.3 テスト観点（必須）
##### 単体（必須 1 件以上）
- 表 B・表 C に #37 行が存在（grep）。
##### 結合/API
- 該当なし。
##### E2E/受け入れ
- 3 表すべてに #37 が揃う。
##### バリデーション
- #37 の記述内容が DOCS_NOISE_RULES (iv-b)・close/ 対象外の既存仕様と矛盾しない。

#### 2.3.4 単体テスト仕様（BDD）
```gherkin
Feature: 表B/C の #37
  Scenario: 両表に #37
    Given 補完後の表B と 表C
    When #37 を検索する
    Then 両表に #37 行がある
```
```bash
# Given: enforcement/README.md
# When: 表B/表C を grep '#37'
# Then: 両表に #37 行
```

---

## テスト観点
audit.sh 実装済み全チェックが表 A に漏れなく載り、#37 が 3 表すべてに揃うこと。差集合ゼロで実装と正本のドリフトが解消されること。

## 単体テスト
各タスク §2.x.4 の grep/差集合チェック。

## BDD
上記 Gherkin を参照。

---

## 5. リスク管理
- **リスク**: 突合過程で #2/#4/#7/#21/#37 以外の未記載項目が新たに見つかる。
  - **対策**: T1 で全 check 関数と表を網羅突合し、見つかった項目も同 PR で追加する（00 §7.1 対策）。

## 6. 参考資料
- 統合正本: `../20260714_163330_enforcement表セル長大重複/02_設計.md`・`03_実装計画.md`
- `.agent-skill-chain/source/enforcement/ci/audit.sh` / `enforcement/README.md`

## 7. 前のステップ
- **前**: `02_設計.md`

## 8. 次のステップ
- 実装フェーズ（統合 PR で対応）。
