---
document_id: "a85fe0fe-8ca3-4044-bc16-b781bb0e5088"
---

# 実装計画書: 未実装項目の実装状態ラベル付与（統合計画への参照＋差分）

**プロジェクト名**: 未実装強制項目の正本堆積問題（バッチ5・統合の一部）
**作成日**: 2026 年 07 月 14 日
**最終更新**: 2026 年 07 月 14 日

> 統合実装計画の**正本**は `../20260714_163330_enforcement表セル長大重複/03_実装計画.md`。
> 本 issue の作業は同計画の**タスク T2（実装状態列付与）・T4（AGENT_CONDUCT 整合）**に割り当てられる。本書は本 issue 固有のタスク差分のみ記す。

---

## 1. 実装概要

### 1.1 実装方針
統合 03 のタスク T2・T4 に本 issue の関心（未実装 7 項目のラベル付与＋抽象仕様本文の保全）を織り込む。単独 PR は作らず、3 件同一 PR で実装。

### 1.2 実装順序
統合 03 の順序に従う（T2 → T4 の局面が本 issue の主担当）。

---

## 2. タスク分解

### 2.1 タスク A: 未実装 7 項目へのラベル付与（統合 T2 の一部）

#### 2.1.1 概要（必須）
失敗条件対応表の `実装状態` 列に、#22/#23/#24/#30＝`未実装（人手監査）`、系統A/C/E＝`未実装（抽象仕様のみ・将来hook）` を付与する。

#### 2.1.2 実装内容（必須）
- 7 項目の各行に統制語彙を付与（統合 02 §3.3 の表どおり）。

#### 2.1.3 テスト観点（必須）
##### 単体（必須 1 件以上）
- `grep -E '未実装（人手監査）'` で #22/#23/#24/#30 が抽出できる。
- `grep -E '未実装（抽象仕様のみ・将来hook）'` で 系統A/C/E が抽出できる。
##### 結合/API
- 該当なし。
##### E2E/受け入れ
- 未実装 7 項目が実装済み行と機械的に区別できる（区別可能化の完了条件）。
##### バリデーション
- `実装状態` 列に統制語彙以外が現れない。

#### 2.1.4 単体テスト仕様（BDD）
```gherkin
Feature: 未実装ラベル
  Scenario: 7項目の抽出
    Given 実装状態列を付与した対応表
    When grep で未実装項目を抽出する
    Then #22 #23 #24 #30 系統A 系統C 系統E が得られる
```
```bash
# Given: 実装状態列付与後の enforcement/README.md
# When: grep -E '未実装'
# Then: 7項目が抽出される
```

### 2.2 タスク B: 抽象仕様本文の保全確認と AGENT_CONDUCT 整合（統合 T4 の一部）

#### 2.2.1 概要（必須）
系統A/C/E・#30 の抽象仕様本文を削除しないことを確認し、AGENT_CONDUCT.md の #22–#24/#30 分類とラベル文言を整合させる。

#### 2.2.2 実装内容（必須）
- README §系統A・§系統C・§系統E・#30 本文の存置確認。
- AGENT_CONDUCT.md の「未実装・runtime/人手監査」表記と README ラベルの齟齬点検（必要なら文言統一）。

#### 2.2.3 テスト観点（必須）
##### 単体（必須 1 件以上）
- README に `### 系統A・系統C`・`### 系統E` 見出しが存置されている（grep）。
##### 結合/API
- 該当なし。
##### E2E/受け入れ
- 該当なし。
##### バリデーション
- README ラベルと AGENT_CONDUCT の分類が矛盾しない。

#### 2.2.4 単体テスト仕様（BDD）
```gherkin
Feature: 抽象仕様の保全
  Scenario: 本文を消さずにラベルだけ付ける
    Given ラベル付与後の README
    When 系統A/C/E の見出しを検索する
    Then 見出しと本文が存置されている
```
```bash
# Given: ラベル付与後の enforcement/README.md
# When: grep '系統A・系統C' / grep '系統E'
# Then: 抽象仕様の見出しが残っている
```

---

## テスト観点
未実装 7 項目のラベルが機械抽出可能で、抽象仕様本文が保全され、AGENT_CONDUCT と整合すること。

## 単体テスト
各タスク §2.x.4 の grep チェック。

## BDD
上記 Gherkin を参照。

---

## 5. リスク管理
- **リスク**: ラベル付与のついでに抽象仕様本文を「未実装だから」と削除してしまう。
  - **対策**: 本 issue はラベル付与のみ。本文削除は禁止（統合 02 §3 本 issue must-preserve）。T2/T4 のバリデーションで本文存置を確認。

## 6. 参考資料
- 統合正本: `../20260714_163330_enforcement表セル長大重複/02_設計.md`・`03_実装計画.md`

## 7. 前のステップ
- **前**: `02_設計.md`

## 8. 次のステップ
- 実装フェーズ（統合 PR で対応）。
