---
document_id: "60a36bba-7d50-4d08-8c06-c499a3643147"
---

# レビュー記録: 行番号相互参照の脆弱性是正

**プロジェクト名**: 行番号相互参照の脆弱性是正
**レビュー日**: 2026 年 07 月 15 日
**フェーズ**: verify-and-close（opus ティア）

> 本 issue は 163640（リンク切れ大量発生の是正）と**同一 PR・同一 worktree**（`worktree-agent-adf0b84c62a94c8b5`）で対応した。検証基盤・CI 導入・監査再現・設計境界の総括は [163640 の 04_review.md](../20260714_163640_リンク切れ大量発生の是正/04_review.md) に記載する。本書は 164050 固有の受け入れ基準確認に絞る。

---

## 受け入れ基準の確認（01 §3 BDD）

| シナリオ | 検証方法 | 結果 |
| --- | --- | --- |
| A: 他文書行番号がアンカーへ置換 | `grep 'run_command.md:57\|TEMPLATES.md](...):12-13'` → 0 件。`#constraints`・`#テンプレートの正本` が存在し、`--check-anchors` 実在判定 | PASS。両アンカーが `自己拡張ワークフロー.md:192` に置換済み。アンカー検査 ON で切れ 0 件 |
| B: 自己参照が名称参照へ | `grep -rE ':[0-9]{1,3} のとおり' CLAUDE.md PHASES.md` → 0 件 | PASS。`:18 のとおり`→「作成場所の上書き最優先」、`:33 のとおり`→「作成場所の上書き」へ変換 |
| C: 参照内容が現行と一致 | `#constraints` が `run_command.md` の Constraints 節（memo 作成規約）を指すことを確認 | PASS。行番号非依存で正しい節を指す |
| D: 運用ルール明文化 | `DOCS_RULES.md §ドキュメント間参照：行番号直リンク禁止` に「他文書=アンカー・同一文書=節名/規則名」「継続検査は `check-relative-links.sh --check-anchors`」を追記 | PASS。CI 非ブロッキング（163640 T4 で導入）で継続検査 |

## 実装内容の確認

- **他文書参照（#3・#4）**: `自己拡張ワークフロー.md` の `run_command.md:57`→`#constraints`、`TEMPLATES.md:12-13`→`#テンプレートの正本` へ変換。display text も節名表記へ整理。実行順は 163640 の相対パス是正（Grp A: `../.agent-skill-chain/source/`→`../source/`）適用後にアンカー化しており、パスとアンカーの双方が実在解決する。
- **自己参照（#1・#2）**: 見出しアンカーを持たないブレット参照のため、行番号でなく規則名で指す表現へ置換（アンカー化ではなく名称化という設計判断が正しく実装されている）。
- **運用ルール（FR-4）**: `DOCS_RULES.md` の既存「行番号直リンク禁止」節へ最小追記（新節を乱立させず既存規約へ集約）。

### 敵対的観点リスト

- **アンカー slug の不一致リスク**: 手書きした `#constraints`・`#テンプレートの正本` が実見出しの slug と一致するか → `check-relative-links.sh --check-anchors` で実在判定させ、切れ 0 件を確認（想像でなく機械検査で裏取り）。
- **置換の一意性リスク**: `:18`・`:33` が他所にも出現し誤置換しないか → 03 計画どおり `grep` で一意性を事前確認する手順があり、変換後 `:NN のとおり` が 0 件であることを再確認。
- **参照先ドリフト再発リスク**: アンカー化しても参照先見出しが将来変わればまた切れる → CI 非ブロッキング検査（`--check-anchors`）で継続検知される導線を確認。

### must-preserve リスト（不変条件）

- 参照先ファイル（`run_command.md`・`TEMPLATES.md`）の**見出し・本文は不変**。→ `git diff` で参照先本体に変更なしを確認。
- 自己参照の名称化で**指し先の意味（規則）が変わらない**こと。→ 「作成場所の上書き最優先」「作成場所の上書き」が元の `:18`/`:33` が指していた規則と同一であることを確認。

## docs 更新

- 要否: 必要。`DOCS_RULES.md` へ行番号参照禁止・アンカー継続検査の運用ルールを追記した（163640 と共同の単一追記点）。`CLAUDE.md`・`PHASES.md`・`自己拡張ワークフロー.md` の参照表現を安定参照へ是正。

## 次のステップ

- 総括・監査再現・PR 情報は 163640 の 04_review.md を参照。進行役へ返却（マージは行わない）。
