---
document_id: "d7ce9fdb-fcde-4500-a208-e59eb8d99209"
---

# 実装計画: .agent-skill-chain: 運用スクリプトのバグ修正

**前のステップ**: [02_設計.md](./02_設計.md)
**次のステップ**: `04_review.md`

**実装先**: `/home/adachi/projects/AGENTS.md/.worktree/bugfix/20260718_092843-運用スクリプトバグ修正/`（`main` から新規に切ったブランチ `bugfix/20260718_092843-運用スクリプトバグ修正`）

---

## 1. 実装順序・依存関係

02_設計.md §6 の方針どおり、ファイル単位でまとめて実装した。

1. **setup.sh**（E-1・E-9）: E-1 は own 判定との非対称是正（`pwd -P` 統一＋削除直前アサート）を `AGENTS.md/CLAUDE.md` 比較・`ASC_SOURCE_DEST` 削除・`WF_TEMPLATES` 削除の 3 箇所に適用。E-9 は `init_workflow_db` 冒頭に sqlite3 存在検査を追加。
2. **close-move-issue.sh**（E-4・E-5）: 同一 while ループを対象とするため同時に変更。E-5（クォート）を先に適用してから E-4（trap・symlink 列挙）を重ねた。
3. **create-pr-review-issue-dir.sh**（E-6・E-17）: E-6（hint 検証緩和）、E-17（mkdir 非 -p ＋連番）。E-12 の波及分（TZ フォールバック無条件化）もこのファイルに含めて一括対応。
4. **check-relative-links.sh**（E-7・E-19）: E-19（fence 判定の記号種・長さ追跡）を先に両 python ヘルパーへ適用し、E-7（一時ファイル＋終了コード検査）を extract_links 呼び出し側に適用。
5. **new-workflow-memo.sh**（E-11）＋ **memo-prefix.sh**（E-12）: 02_設計.md §1 E-12「波及」の指示どおり、new-workflow-memo.sh の TZ フォールバックも無条件 JST 化し、E-11（noclobber 原子化）と同時に対応。
6. **lib/deploy-skills.sh**（E-13）: cp 終了コード検査＋警告 stderr 出力。
7. **lib/package-manifest.sh**（E-15・E-18）: E-15 をバックアップ 5 箇所（source 退避・templates 退避・legacy source/project/runtime 退避）に適用、E-18（uninstall コマンド名）を同ファイルに適用。
8. **sync-version.sh**（E-16）: 読取クォートトリム。
9. **SETUP.md**（E-18）: テーブル名 `execution_logs` → `workflow_log`。

依存の観点で他パッケージとのファイル重複は無い（対象 10 ファイルは本 issue 専有。00_要求定義.md §7.1 相当の重複確認を実施済み）。

## 2. E-1 再発防止テスト（02_設計.md §2 の実施結果）

`test/` 配下への恒久テストファイル追加は本実装の対象外（実装計画確定時点で追加テストスクリプトの新設は別途要判断と整理し、まずは実装フェーズ内で手動シナリオ実行による検証を優先した）。02_設計.md §2 の T-E1-1・T-E1-2・T-E1-3 相当のシナリオを、`/tmp` スクラッチ上に一時的なパッケージコピー＋symlink 環境を構築して実行し、結果を 04_review.md に記録した（一時環境は検証後に削除済み）。

## 3. 実装後の確認

- 変更した 9 本のシェルスクリプトすべてに対し `bash -n` で構文チェックを実施し、全て成功。
- E-1・E-5・E-6・E-7・E-11・E-16・E-17・E-19 について、実際にスクリプトを実行して修正前後の挙動差を確認（詳細は 04_review.md）。
- E-4・E-9・E-12・E-13・E-15・E-18 はコードレビューによる確認（ロジックが単純で実行環境依存が薄いため、`bash -n` 構文チェック＋目視でのロジック確認を実施）。

## 4. 完了状態

対象 14 件（E-1, E-4, E-5, E-6, E-7, E-9, E-11, E-12, E-13, E-15, E-16, E-17, E-18, E-19）すべてを実装完了。実装先 worktree で `git add`・`git commit` 済み（push・PR 作成は本タスクの対象外）。
