---
document_id: "36aeaba9-4ba2-452b-a020-b61dba6ff76c"
---

# レビュー書: 外部サービス書き込み操作の実行主体明文化

**プロジェクト名**: 外部サービス書き込み操作の実行主体明文化
**作成日**: 2026 年 07 月 15 日
**最終更新**: 2026 年 07 月 15 日

> **重要**: **このドキュメントは常に更新**: レビューで発見した問題点や改善提案、対応内容などがあった場合は、即座にこのドキュメントを更新してください。
>
> **用語**: [.agent-skill-chain/source/CONCEPTS.md §用語規約](../../../../../.agent-skill-chain/source/CONCEPTS.md#用語規約) を参照。
>
> **必須**: レビュー深度は **standard**（`00_要求定義.md` frontmatter `mode: standard`）。ドキュメント成果物のため「テスト」＝ドキュメント審査（grep による記述検査）に相当する。

---

## 1. レビュー概要

### 1.1 レビュー目的（必須）

実装内容の確認・品質保証・クローズ前最終チェック。正本（`run_command.md`）への外部書き込み実行主体ルールの追記と、CORE / RULES / project への参照接続が、01 の BDD（ストーリー1〜5・UC1〜UC3）・02 の ADR-1〜ADR-8・03 のタスク別テスト観点をすべて充足しているかを、独立に再実行した grep 審査で裏取りする。

### 1.2 レビュー対象（必須）

- **実装範囲**: 03_実装計画.md タスク1〜5。(1) `skills/agent/run_command.md` §Constraints へ新規正本項目「外部書き込み操作の実行主体（進行役限定）」を追記、(2) `boot/CORE.md` §メインがやってはいけないこと へ carve-out 参照1行、(3) `RULES.md` §高リスク操作 へ実行主体の参照1行、(4) `project/自己拡張ワークフロー.md` の GitHub Issue 起票ゲート節と close 移動 PR フロー節へ「実行主体＝進行役」整合注記を各1行、(5) 一貫性検証（相互参照・重複無し・分類器非依存）。
- **レビュー期間**: 2026-07-15 ～ 2026-07-15
- **レビュー担当者**: verify-and-close サブエージェント（opus 固定・品質ゲート相当）

---

## 2. 実装内容の確認

### 2.1 実装完了タスク（または Issue）

| タスク名 | 実装内容 | 実装日 | 担当者 | ステータス |
| ------------ | ----------------- | -------- | ---------- | ------ |
| タスク1 run_command 正本追記 | §Constraints に実行主体項目（6+要素）を追記 | 2026-07-15 | implement-feature | 完了 |
| タスク2 CORE carve-out | §メインがやってはいけないこと へ carve-out 参照1行 | 2026-07-15 | implement-feature | 完了 |
| タスク3 RULES 参照接続 | §高リスク操作 へ実行主体の参照1行 | 2026-07-15 | implement-feature | 完了 |
| タスク4 project 整合注記 | 起票ゲート節・close 移動 PR フロー節へ各1行 | 2026-07-15 | implement-feature | 完了 |
| タスク5 一貫性検証 | 相互参照・重複無し・分類器非依存の横断検証 | 2026-07-15 | verify-and-close | 完了 |

### 2.2 実装内容の詳細

#### タスク1: run_command.md §Constraints 正本項目

- **変更ファイル**: `.agent-skill-chain/source/skills/agent/run_command.md`（+11 行、52〜64 行）
- **実装内容**: 新規箇条書き項目「外部書き込み操作の実行主体（進行役限定）」を追加。以下を1か所に収める。
  1. **原則**（承認の直接性を実行主体に持たせる／伝聞承認のみのエージェントは実行してはならない）
  2. **進行役の定義**（役割名ではなく「承認したユーザー発言を自会話に持つエージェント」・並行セッションで会話ごとに別個成立）
  3. **二段構え**（ローカル `git commit`＝サブ可 / `git push`・`gh pr create`・`gh issue create` 以降＝進行役）
  4. **対象/非対象の列挙**（対象＝gh issue/pr create・git push・repo visibility 変更等、抽象定義が第一基準／対象外＝参照系・ローカル commit／既承認済み但し書き）
  5. **例外の限定と準備分担**（副作用 Bash 実行のみが例外・content authorship はサブ準備・進行役は最終確認＋実行のみ・`--body-file` 参照実行推奨）
  6. **実行前の対象検証**（repo/branch/worktree/issue 番号・必須）
  7. **原則ベース記述**（分類器非依存・緩和策不採用・脚注 `[^ext-write-classifier]`）
  8. **実行専用フェーズは新設しない**（punctual な副作用として既存フローに組み込む）
  9. **既存規定との関係**（Execution Path Rule / 高リスク操作事前確認＝例外・整合関係、サブによる独断起票＝補完・直交関係）
- **確認事項**: 6要素すべて存在・分類器適合の規範文が本文に無い・脚注1対の対応。§3 で機械再検査済み。

#### タスク2: CORE.md carve-out 参照

- **変更ファイル**: `.agent-skill-chain/source/boot/CORE.md`（+2 行、76 行付近）
- **実装内容**: 「コマンド実行」禁止の限定例外として、外部書き込みコマンドの実行は進行役自身が行い、副作用アクション（Bash 実行）のみの限定例外で content authorship の委譲原則を弱めない旨を明記し、正本 run_command への参照1行を付す。原則本文（対象/非対象列挙）は転記していない。

#### タスク3: RULES.md 参照接続

- **変更ファイル**: `.agent-skill-chain/source/RULES.md`（§高リスク操作、1行編集）
- **実装内容**: 外部サービス・公開リモートへの書き込みは高リスク操作であり事前確認を要すると同時に実行主体は進行役である旨を追記し、正本 run_command への参照1行を付す。原則本文は重複させていない。

#### タスク4: project 自己拡張ワークフロー整合注記

- **変更ファイル**: `.agent-skill-chain/project/自己拡張ワークフロー.md`（+4 行）
- **実装内容**: GitHub Issue 起票ゲート節（23 行付近）と close 移動 PR フロー節（分岐 A・219 行付近）に「実行主体（進行役限定）」注記を各1行追加。原則本文は source を正本とし重複させない旨を明記。

---

## 3. テスト結果の確認（ドキュメント審査＝独立再実行）

本 issue はドキュメント成果物のため、単体テスト＝ grep による記述検査に相当する（03 §単体テスト・02 §6）。実装担当が実行した検査は全パス済みだが、**verify-and-close サブが worktree ルートで独立に再実行**して裏取りした。実行日: 2026-07-15。

### 3.1 単体（記述検査）— 03 §2.x.4 の bash 検査を再実行

| # | 検査観点（03 対応） | コマンド概要 | 期待 | 実測結果 | 判定 |
| - | ------------------- | ------------ | ---- | -------- | ---- |
| 1 | 6要素の存在（§2.1.4） | 実行主体項目本文を `sed` 抽出し要素を `grep -c` | 全要素 ≥1 | 承認したユーザー=2 / 進行役定義=1 / 二段構え=1 / git commit=2・git push=3 / 準備分担=1 / 対象検証=2 / 原則ベース記述=1 / 実行専用フェーズ新設しない=1 | OK |
| 2 | 分類器適合の規範文が本文に無い（§2.1.3 異常系） | 本文の分類器適合規範を検索 | 0 件 | 「分類器に適合するため実行する」=0。唯一のヒットは ADR-6 の否定文「への適合として**ではなく**」（規範化ではない） | OK |
| 3 | 列挙本文は run_command 1か所のみ（§2.5.4） | 4ファイルで `grep -rl "gh issue view"` | COUNT=1 | run_command のみ 1 件 | OK |
| 4 | CORE 重複無し（§2.2.3 異常系） | CORE に carve-out あり・対象外列挙本文なし | 参照≥1・列挙0 | 「外部書き込み」=1・「gh issue view」=0 | OK |
| 5 | RULES 参照あり・重複無し（§2.3.3） | RULES に「実行主体」あり・列挙本文なし | 参照≥1・列挙0 | 「実行主体」=1・「gh issue view」=0 | OK |
| 6 | project 整合注記あり・重複無し（§2.4.3） | project に「実行主体」あり・列挙本文なし | 注記≥1・列挙0 | 「実行主体」=2（起票ゲート＋close 移動）・「gh issue view」=0 | OK |

### 3.2 結合（相互参照リンク解決）— §2.5.2

| 参照元 | リンク相対パス | 解決先 | 判定 |
| ------ | -------------- | ------ | ---- |
| CORE.md（source/boot/） | `../skills/agent/run_command.md` | source/skills/agent/run_command.md 実在 | OK |
| RULES.md（source/） | `skills/agent/run_command.md` | 同上 実在 | OK |
| project/自己拡張ワークフロー.md（project/） | `../source/skills/agent/run_command.md` | 同上 実在 | OK |
| 正本セクション見出し | `- **外部書き込み操作の実行主体（進行役限定）**` | run_command.md 52 行に実在 | OK |
| 脚注参照↔定義 | `[^ext-write-classifier]` | 参照60行・定義64行の1対1対応 | OK |

### 3.3 バリデーション（緩和誘発語の不在）— §2.5.3

- 実行主体項目内の「バイパス」「緩和」出現箇所を全件目視。すべて**否定・禁止文脈**（「緩和（分類器バイパス・権限緩和）を採らず」「緩和策は一切採用しない」）または**意味的用法**（「本ルールが緩和するのは…リスク」）であり、分類器バイパス・Bash 権限緩和を**推奨する記述は皆無**。 → OK
- 「declined」等の別文脈語の混入なし。

### 3.4 テスト実行サマリ（数値）

- **検査項目数**: 単体6 + 結合5 + バリデーション1 = **12 項目**
- **成功**: 12
- **失敗**: 0
- **スキップ**: 0
- 実装担当の事前検査結果と独立再実行結果は**完全一致**（裏取り成立）。

---

## 4. コードレビュー（記述レビュー）

### 4.1 品質観点

| 観点 | 確認内容 | 結果 | コメント |
| ---- | -------- | ---- | -------- |
| 可読性 | 委譲を受けたサブが正本1か所を読めば対象/非対象を列挙で即自己判定できるか | OK | 判定 flowchart（02 §2.2.2）を箇条書きに規約化。二段構え・既承認済み但し書きが明快 |
| 保守性 | 正本一元化（BR-6）・重複記載禁止 | OK | 原則本文は run_command 1か所。CORE/RULES/project は参照1行のみ（§3 #3-#6 で機械確認） |
| 頑健性 | 分類器仕様非依存（ADR-6・BR-5） | OK | 本文は原則、分類器実例は脚注に隔離。緩和誘発語なし（§3.3） |
| セキュリティ | 伝聞承認による無自覚な外部公開の防止・権限緩和不採用 | OK | 承認の所在（進行役自会話）を実行前提に組込み。private→public 化を対象列挙に明示 |

### 4.2 指摘事項

- **指摘なし（0 件）。** 全 grep 検査パス、相互参照解決、緩和誘発語不在を確認。要修正項目は検出されなかった。

---

## 5. ドキュメントの確認

### 5.1 ドキュメント更新状況

| ドキュメント | 更新状況 | 確認者 | 確認日 |
| ------------ | -------- | ------ | ------ |
| [`00_要求定義.md`](./00_要求定義.md) | 更新済み | verify-and-close | 2026-07-15 |
| [`01_要件定義.md`](./01_要件定義.md) | 更新済み | verify-and-close | 2026-07-15 |
| [`02_設計.md`](./02_設計.md) | 更新済み | verify-and-close | 2026-07-15 |
| [`03_実装計画.md`](./03_実装計画.md) | 更新済み | verify-and-close | 2026-07-15 |

### 5.2 ドキュメントの整合性

- **実装と設計の整合性**: 整合している（ADR-1〜ADR-8 がすべて run_command 本文へ反映。下表参照）。
- **要件と実装の整合性**: 整合している（BR-1〜BR-6・成功基準5項目・ストーリー1〜5・UC1〜UC3 すべて充足）。

---

## 受け入れ基準の確認（BDD / ADR / タスク観点の対応）

### A. 01 BDD（UC）↔ 正本記述 の 1:1 対応

| BDD | シナリオ | 正本での充足箇所 | 判定 |
| --- | -------- | ---------------- | ---- |
| UC1-S1 | 伝聞承認のサブは実行しない | 原則文「伝聞承認…のみを持つエージェントは実行してはならない」（run_command 52行） | OK |
| UC1-S2 | 直接承認の進行役が実行する | 原則文「承認したユーザーの直接発言を自会話に持つ…が実行する」＋対象検証手順 | OK |
| UC2-S1 | 読取専用・既承認は対象外 | 対象外列挙（参照系）＋既承認済み操作の但し書き | OK |
| UC2-S2 | commit/push 二段構え | 二段構え項（commit=サブ可 / push 以降=進行役）＋理由 | OK |
| UC3-S1 | 実行専用フェーズ案の採否記録 | 「実行専用フェーズは新設しない」明記（ADR-5） | OK |

### B. 02 ADR ↔ 実装反映

| ADR | 決定 | 実装反映 | 判定 |
| --- | ---- | -------- | ---- |
| ADR-1 | 正本を run_command §Constraints に | 新規項目を §Constraints に追加・CORE/RULES/project は参照 | OK |
| ADR-2 | 二段構え採用 | 二段構え項に明記＋理由 | OK |
| ADR-3 | 例外は副作用 Bash 実行のみ | 「例外の限定と準備分担」項・content authorship はサブ準備 | OK |
| ADR-4 | 対象/非対象を列挙＋抽象定義第一基準 | 対象/対象外/既承認済み但し書き＋「列挙は代表例・抽象定義が第一基準」 | OK |
| ADR-5 | 実行専用フェーズ新設せず | 「実行専用フェーズは新設しない」項 | OK |
| ADR-6 | 分類器非依存の原則ベース記述 | 「原則ベース記述」項＋脚注隔離 | OK |
| ADR-7 | 実行前の対象検証必須 | 「実行前の対象検証（必須）」項 | OK |
| ADR-8 | enforcement 機械強制はスコープ外 | 脚注で機械強制の要否を将来申し送りと明記（audit.sh 新設せず） | OK |

### C. 03 タスク別テスト観点 ↔ 再実行結果

§3 の表 #1〜#6（各タスク §2.x.3/§2.x.4 由来）がすべて OK。00 成功基準5項目（原則ベース記載・境界列挙・進行役定義・例外境界・二段構え/実行専用フェーズ案の採否）はいずれも正本記述で充足。

### D. 重点確認 (a)〜(f)

| # | 確認項目 | 判定 | 根拠 |
| - | -------- | ---- | ---- |
| (a) | 「伝聞承認のサブは実行しない」が正本に読めるか | 合格 | run_command 52行 原則文で「実行してはならない」と規範的に明記 |
| (b) | 二段構え（commit=サブ可 / push 以降=進行役）の採否と理由 | 合格 | ADR-2 採用。二段構え項に「push しない限り外部状態を変えない」理由を併記 |
| (c) | 実行専用フェーズ不採用（ADR-5）の明記 | 合格 | 「実行専用フェーズは新設しない」項で punctual な副作用として既存フロー組込みと明記 |
| (d) | 例外が副作用アクションのみに限定され content authorship 委譲を弱めていないか（ADR-3） | 合格 | 「例外の限定と準備分担」項で Bash 実行のみに限定・body 等はサブ準備・`--body-file` 参照実行推奨・「滑り坂を正本上で否定」 |
| (e) | 分類器依存記述が本文に無く脚注のみか（ADR-6） | 合格 | 本文はすべて原則記述。分類器実例は脚注 `[^ext-write-classifier]` に隔離（§3 #2 で本文の規範化文=0 を機械確認） |
| (f) | 緩和誘発語（分類器バイパス・権限緩和の推奨）が無いか | 合格 | 出現はすべて否定・禁止文脈（§3.3）。推奨・許容する記述は皆無 |

---

## docs 更新（継続追随ゲート・軽量パス）

- **要否**: 不要（軽量パス・指摘 0 件）
- **対象**: なし
- **理由（evidence_source: existing_code）**: システム仕様書 `docs/` は本変更に関わる領域を**抽象度**（`docs/01_システム概要/05_規約/README.md` 15行は command/skill を「skill chain 定義・委譲 I/F」と記述し `run_command.md` を正本として参照、`docs/04_機能設計/CLI/README.md` は薄ラッパ委譲を記述）でのみ記述し、**run_command §Constraints の個別ルール本文（外部書き込み実行主体の対象/非対象列挙）を列挙していない**。`grep -rln "外部書き込み\|実行主体\|gh issue create\|git push" docs/01_システム概要 docs/04_機能設計 docs/05_エラー処理と外部通知` は 0 件で、当該個別ルールが仕様書に非存在であることを実測確認した。本変更は run_command（正本）への追記であり、仕様書が記述する抽象（委譲 I/F・薄ラッパ）はいずれも不変。正本 `run_command.md` と `docs/` は抽象度の異なる別レイヤであり二重管理は不要。直近同一系統の軽量パス判定（`docs/00_review/20260715_134942_review.md` 等）と整合する。

---

## 9. 設計・境界の確認（review-architecture）

### 9.1 設計の確認

- **設計原則の準拠**: 単一責務（正本1か所）・明確な境界（content authorship の委譲 vs 外部書き込み Bash 実行）・AI フレンドリー（列挙で即自己判定）・CQRS 的分離（Command/Query 第一軸）を 02 §1.2 どおり充足。
- **配置構造**: 既存の #34/#35/#36 ゲート群と同じ「原則＝source / 具体手順＝project」境界を踏襲（ADR-1・ADR-5）。新規 command/phase/テンプレート/enforcement は不新設で規模比例。

### 9.2 境界・依存の確認

- **責務の境界**: run_command が原則の終端正本、CORE/RULES/project は一方向にそれを参照（02 §2.1.3）。§3 #3-#6 で列挙本文=run_command 1か所を機械確認済み。
- **依存関係**: 循環なし。参照リンクは全解決（§3.2）。
- **指摘・推奨**: なし。

### 9.3 重要判断の根拠（evidence_source）

| 判断内容 | evidence_source | 備考 |
| -------- | --------------- | ---- |
| 6要素の存在・分類器規範文の不在 | test_output | §3.1 grep 再実行（worktree ルートで独立実行、実装担当結果と一致） |
| 列挙本文が run_command 1か所のみ・参照側重複0 | test_output + existing_code | §3.1 #3-#6・実ファイル diff 確認 |
| 相互参照リンクの全解決 | test_output | §3.2 `ls` によるパス解決確認 |
| ADR-1〜8・UC1〜3 の正本反映 | existing_code | run_command.md 52〜64 行本文と 01/02 の対照 |
| docs 更新不要（軽量パス） | existing_code | docs/ の抽象度確認・個別ルール非存在の grep 実測 |
| 緩和誘発語の不在 | test_output | §3.3 全件目視＋文脈判定 |

---

## 11. システム仕様書の更新

- 継続追随ゲートは**軽量パス（更新不要・指摘0件）**。判定根拠は本書「docs 更新」節に記載。`docs/README.md §更新履歴` への版追記は不要（仕様書本文の版更新を伴わないため）。

---

## 12. レビュー結果

### 12.1 総合評価

- **実装品質**: 良好（正本項目が9要素を過不足なく収め、参照側は重複0の参照1行に徹している）
- **テスト品質（ドキュメント審査）**: 良好（12項目全パス・独立再実行で裏取り成立）
- **ドキュメント品質**: 良好（BDD UC1〜3・ADR-1〜8・成功基準5項目・重点確認 (a)〜(f) すべて充足）
- **総合評価**: **合格（要修正 0 件）**

### 12.2 承認状況

- **レビュー承認者**: verify-and-close サブエージェント（opus・品質ゲート相当）
- **承認日**: 2026-07-15
- **承認コメント**: 03 §2.x.4 の grep 検査を worktree ルートで独立再実行し、実装担当の事前検査結果と完全一致することを確認。重点確認 (a)〜(f) すべて合格。指摘 0 件でクローズ可。push・PR・commit は進行役/後続が実施する（本レビューのスコープ外）。

---

## 13. 参考資料

- [`00_要求定義.md`](./00_要求定義.md) / [`01_要件定義.md`](./01_要件定義.md) / [`02_設計.md`](./02_設計.md) / [`03_実装計画.md`](./03_実装計画.md)
- [.agent-skill-chain/source/skills/agent/run_command.md](../../../../../.agent-skill-chain/source/skills/agent/run_command.md)（正本・改定対象）
- [.agent-skill-chain/source/boot/CORE.md](../../../../../.agent-skill-chain/source/boot/CORE.md) / [.agent-skill-chain/source/RULES.md](../../../../../.agent-skill-chain/source/RULES.md) / [.agent-skill-chain/project/自己拡張ワークフロー.md](../../../../../.agent-skill-chain/project/自己拡張ワークフロー.md)

---

## 14. 前のステップ

- **前**: [`03_実装計画.md`](./03_実装計画.md) - 実装計画フェーズ

---

## 15. 次のステップ

- 外部設定は不要。指摘 0 件のため issue クローズへ進む（close 移動・push・PR は進行役/後続が実施）。
