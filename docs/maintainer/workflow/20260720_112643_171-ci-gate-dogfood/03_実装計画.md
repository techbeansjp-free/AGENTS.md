---
branch: "process/171-ci-gate-dogfood"
github_issue: "#171"
---

# 03_実装計画: #171 CI/gate運用の本番導入

**作成日**: 2026年07月20日

---

## タスク一覧

| ID | タスク | 実施内容 | 完了条件 |
| --- | --- | --- | --- |
| T1 | 依存インストール | `npm ci` | 終了コード0、`bin/agents-md.js`生成 |
| T2 | init dry-run確認 | `node bin/agents-md.js init --dry-run` | `.github/`配下18ファイルが`planned created`、他は`planned unchanged` |
| T3 | init実行 | `node bin/agents-md.js init` | dry-runと同一のファイル一覧が`created`、終了コード0 |
| T4 | config変更 | `review.adapter: claude` → `human`（`.agent-skill-chain/config/agent-skill-chain.yaml`1行編集） | `grep`で変更確認 |
| T5 | branch-name検証 | `node bin/agents-md.js verify branch-name process/171-ci-gate-dogfood` | 終了コード0 |
| T6 | 既存テスト実行 | `npm test` | 実測pass/fail件数を記録。fail発生時は原因特定し04_reviewへ記載 |
| T7 | docs成果物作成 | 本ディレクトリ配下00〜04を作成 | 5ファイル存在 |
| T8 | コミット | `git add` + `git commit`（ローカルのみ、push・PR作成は進行役が別途実施） | commit hashを報告 |

## 実施順序・依存関係

T1 → T2 → T3 → T4 → T5 → T6 → T7 → T8（直列。各タスクの実測結果が次タスクの前提となるため並行実行はしない）。

## 対象外

- `src/`配下の実装変更（本Issueは既存CLI・既存schemaの利用のみ）。
- push・PR作成（進行役が別途実施）。

---

## 次のステップ

- 次: [`04_review.md`](./04_review.md)
