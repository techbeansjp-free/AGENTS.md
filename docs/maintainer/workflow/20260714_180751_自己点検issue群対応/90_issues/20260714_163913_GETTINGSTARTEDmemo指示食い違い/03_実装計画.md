---
# document_id: 必須。作成時または major 更新時に UUID（8-4-4-4-12 形式）を付与すること。既存の場合は変更しない。
document_id: "ab8c10a3-3055-45e5-a8ad-728b2c88e546"
---

# 実装計画書: GETTING_STARTED が非推奨の memo 証跡運用を指示している

**プロジェクト名**: GETTING_STARTED が非推奨の memo 証跡運用を指示している
**作成日**: 2026 年 07 月 14 日
**最終更新**: 2026 年 07 月 14 日

> **重要**: **このドキュメントは常に更新**: 実装計画の変更、進捗状況の更新、バグの記録などがあった場合は、即座にこのドキュメントを更新してください。ドキュメントは「生きているドキュメント」として扱い、実装内容と常に同期させます。
>
> **用語**: [.agent-skill-chain/source/CONCEPTS.md §用語規約](../../../../../../.agent-skill-chain/source/CONCEPTS.md#用語規約) を参照。

---

## 1. 実装概要

### 1.1 実装方針（必須）

`.agent-skill-chain/source/GETTING_STARTED.md` の「1 issue を回す流れ」ステップ 2・4 のみを編集し、write-workflow-log（workflow.db・本則）を主軸とする文言に更新する。02_設計 ADR-1 のとおり他ファイルは変更しない。

### 1.2 実装順序（必須: 1 件以上）

1. タスク 1: GETTING_STARTED.md「1 issue を回す流れ」ステップ 2・4 の文言更新（優先度: 高、単一タスクのため Gantt は省略）。

---

## 2. タスク分解

**必須**: 各タスクには「テスト観点」（2.x.3）と「単体テスト仕様（BDD）」（2.x.4）を必ず記載する。

### 2.1 タスク 1: GETTING_STARTED.md の文言更新

#### 2.1.1 概要（必須）

「1 issue を回す流れ」ステップ 2 の「証跡を memo に書く。」を write-workflow-log（workflow.db・本則）主軸の文言に置き換え、ステップ 4 に「各 phase 完了時に write-workflow-log で証跡を記録する（本則）」を追記する。

#### 2.1.2 実装内容（必須: 1 件以上）

- `.agent-skill-chain/source/GETTING_STARTED.md` の「1 issue を回す流れ」ステップ 2 を、「証跡は write-workflow-log（workflow.db・本則）に記録する。workflow.db を採用しないプロジェクトの過渡的・例外運用でのみ memo に記録する（scribe/CONTRACT.md §どこに保存するか）。」を含む文言に更新する。
- 同ステップ 4 に「各 phase 完了時に write-workflow-log で証跡を記録する（本則）。」を追記する。
- scribe/CONTRACT.md・ledger/schema.md は変更しない。

#### 2.1.3 テスト観点（必須）

**参照**: 観点の定義は [02_設計 §6](./02_設計.md) を参照。以下は本タスクの具体ケースのみ記載する。

##### 単体（本タスクの具体ケース・必須 1 件以上）

- GETTING_STARTED.md「1 issue を回す流れ」ステップ 2 の本文に「write-workflow-log」という語が含まれ、かつ「本則」または「workflow.db」という語が同ステップ内に含まれることを `grep` で確認する。
- 同ステップ 2 の本文が「証跡を memo に書く。」のみで終わっていない（memo が「過渡的・例外運用」の文脈でのみ言及される）ことを目視確認する。

##### 結合/API（本タスクの具体ケース・該当時は必須 1 件以上）

- 該当なし（ドキュメント単体の変更であり、他コンポーネントとの結合点を持たない）。

##### E2E/受け入れ（本タスクの具体ケース・未達時は理由記載）

- 未達（理由: 本 issue は初心者向け案内文の記述整合のみであり、実行可能なワークフローの E2E シナリオを新設しないため）。

##### バリデーション（該当する場合の具体ケース）

- scribe/CONTRACT.md・ledger/schema.md の該当箇所（§どこに保存するか・冒頭定義）を再読し、GETTING_STARTED.md の新文言が両ファイルの表現（「本則」「過渡的・例外」「非推奨」）と矛盾しないことを確認する。

#### 2.1.4 単体テスト仕様（BDD）（必須）

```gherkin
Feature: GETTING_STARTED の証跡運用案内
  Scenario: ステップ2の文言が write-workflow-log を主軸としている
    Given .agent-skill-chain/source/GETTING_STARTED.md の「1 issue を回す流れ」ステップ2を編集した
    When ステップ2の本文を読む
    Then "write-workflow-log" という語が含まれる
    And "本則" または "workflow.db" という語が含まれる
```

**テストコード例（シェルによる確認・具体ケース）**:

```bash
# Given: GETTING_STARTED.md の「1 issue を回す流れ」ステップ2を編集済みである
# When: ステップ2を含む節をテキスト抽出する
STEP2=$(sed -n '/^## 1 issue を回す流れ/,/^---/p' .agent-skill-chain/source/GETTING_STARTED.md)
# Then: write-workflow-log と 本則/workflow.db の両方を含むことを確認する
echo "$STEP2" | grep -q "write-workflow-log" && echo "$STEP2" | grep -qE "本則|workflow\.db"
```

#### 2.1.5 依存関係

- 依存なし（単一タスク）。

#### 2.1.6 見積もり

- **工数**: 0.5h
- **優先度**: 高

---

## テスト観点

本実装計画は 1 タスクのみであり、テスト観点は §2.1.3 に集約する。自動テストコードは追加しない（ドキュメントのみの変更のため）。

---

## 単体テスト

§2.1.4 の BDD シナリオを、verify-and-close 実施時に手動（grep・目視）で再確認する。

---

## BDD

§2.1.4 参照。

---

## 3. 実装スケジュール

### 3.1 マイルストーン

| マイルストーン                       | 期日       | 成果物                          |
| -------------------------------------- | ---------- | -------------------------------- |
| GETTING_STARTED.md 更新・PR 作成       | 2026-07-14 | 更新済み GETTING_STARTED.md・PR |

### 3.2 実装フェーズ

#### フェーズ 1: 文言更新

- **期間**: 即日
- **タスク**: 2.1 のみ

---

## 4. テスト計画

### 4.1 テストの優先順位

- 記述の整合性確認（write-workflow-log 明記・memo が非推奨文脈のみで言及）を最優先とする。

---

## 5. リスク管理

### 5.1 実装リスク

- **リスク**: GETTING_STARTED.md の他箇所（サブがやること等）まで誤って書き換え、02_設計 ADR-1 が定めたスコープを超える。
  - **対策**: 変更対象を「1 issue を回す流れ」ステップ 2・4 のみに限定し、diff で変更範囲を確認してからコミットする。

---

## 6. 参考資料

### プロジェクトドキュメント

- [`02_設計.md`](./02_設計.md) - 設計

### その他の参考資料

- `.agent-skill-chain/source/GETTING_STARTED.md`

---

## 7. 前のステップ

この実装計画書は、以下のドキュメントを基に作成されています：

- **前**: [`02_設計.md`](./02_設計.md) - 設計フェーズ

---

## 8. 次のステップ

この実装計画書の承認後、以下のステップに進みます：

- **プロジェクトが 1 つの issue（またはタスク）である場合**: 実装フェーズ（execution）に直接進む
