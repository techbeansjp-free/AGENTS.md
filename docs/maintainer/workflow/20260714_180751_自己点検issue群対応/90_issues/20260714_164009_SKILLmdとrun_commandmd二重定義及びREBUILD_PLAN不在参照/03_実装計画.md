---
# document_id: 必須。作成時または major 更新時に UUID（8-4-4-4-12 形式）を付与すること。既存の場合は変更しない。
document_id: "4276bceb-ffbc-43c5-a3bb-ef63c909bbaa"
---

# 実装計画書: SKILL.md と run_command.md 二重定義及び REBUILD_PLAN 不在参照

**プロジェクト名**: SKILL.md と run_command.md 二重定義及び REBUILD_PLAN 不在参照
**作成日**: 2026 年 07 月 14 日
**最終更新**: 2026 年 07 月 14 日

> **重要**: **このドキュメントは常に更新**: 実装計画の変更、進捗状況の更新、バグの記録などがあった場合は、即座にこのドキュメントを更新してください。ドキュメントは「生きているドキュメント」として扱い、実装内容と常に同期させます。
>
> **用語**: [.agent-skill-chain/source/CONCEPTS.md §用語規約](../../../../../../.agent-skill-chain/source/CONCEPTS.md#用語規約) を参照。

---

## 1. 実装概要

### 1.1 実装方針

02_設計の ADR-1（REBUILD_PLAN は削除）・ADR-2（委譲の形は run_command.md へ参照集約）に従い、対象ファイルをテキスト修正のみで是正する。コード実行時の挙動変更はない。

### 1.2 実装順序

1. SKILL.md（agent）の是正（重複定義削除・REBUILD_PLAN 参照削除・証跡記述修正）
2. 残り 4 ファイルの REBUILD_PLAN 参照除去
3. `grep -rn REBUILD_PLAN .agent-skill-chain/source/` による確認
4. 00_要求定義.md frontmatter の `branch` 記録
5. verify-and-close（04_review 作成・write-workflow-log 記録）

---

## 2. タスク分解

### 2.1 タスク 1: SKILL.md（agent）の是正

#### 2.1.1 概要

`.agent-skill-chain/source/skills/agent/SKILL.md` の「委譲の形（共通）」節を run_command.md への参照に置き換え、REBUILD_PLAN 参照（line 16）と旧証跡記述（「memo または 04_review に残す」）を除去・修正する。

#### 2.1.2 実装内容

- 「## 委譲の形（共通）」節の Task/Constraints/OutputSpec ブロックを削除し、「run_command.md の当該節に一本化する」旨の 1 段落に置換する。
- 「## 実行要領」節の手順 5（証跡）を「本則は workflow.db（write-workflow-log を使用する）。memo は過渡的・例外運用のみ」に修正する。
- 「## 参照」節に run_command.md への参照を追加する。

#### 2.1.3 テスト観点

##### 単体（本タスクの具体ケース）

- SKILL.md 内に `REBUILD_PLAN` の文字列が存在しない（`grep -c REBUILD_PLAN SKILL.md` が 0）。
- SKILL.md 内に `### Task` / `### Constraints` / `### OutputSpec` の重複ヘッダが存在しない。
- SKILL.md の証跡記述に「memo または 04_review に残す」という文言が存在しない。

##### 結合/API

- 該当なし（ドキュメントのみ。command 実行フロー自体は commands/{name}.md 側が担うため無変更）。

##### E2E/受け入れ

- verify-and-close を本 issue に対して実行し、04_review.md に本タスクの完了が記録される。

##### バリデーション

- 該当なし。

#### 2.1.4 単体テスト仕様（BDD）

```gherkin
Feature: SKILL.md（agent）の重複定義・REBUILD_PLAN 参照の是正
  Scenario: SKILL.md から重複定義と REBUILD_PLAN 参照が除去される
    Given SKILL.md に Task/Constraints/OutputSpec の重複ブロックと REBUILD_PLAN 参照がある
    When SKILL.md を run_command.md 参照方式に書き換える
    Then grep -c REBUILD_PLAN SKILL.md が 0 になる
    And SKILL.md に Task/Constraints/OutputSpec の重複ヘッダが存在しない
```

**検証コマンド（テストコードの代わりに静的検証コマンドで代替。BDD の Given/When/Then に対応するコマンド実行ログを memo/04_review に記録する）**:

```bash
# Given: 修正前の状態を grep で確認済み（issue 起票時の調査結果）
# When: SKILL.md を編集する
# Then: 以下がいずれも 0 件であることを確認する
grep -c "REBUILD_PLAN" .agent-skill-chain/source/skills/agent/SKILL.md
grep -c "^### Task$" .agent-skill-chain/source/skills/agent/SKILL.md
```

#### 2.1.5 依存関係

- なし（他タスクと独立に実施可能）。

#### 2.1.6 見積もり

- **工数**: 小（1 ファイルの部分書き換え）
- **優先度**: 高

### 2.2 タスク 2: 残り 4 ファイルの REBUILD_PLAN 参照除去

#### 2.2.1 概要

`skills/requirements/write-bdd/{SKILL.md,README.md}`、`skills/requirements/extract-goals/README.md`、`commands/requirement-discovery.md`（2 箇所）から「REBUILD_PLAN 等」の言及を削除する。

#### 2.2.2 実装内容

- 各ファイルの該当行から `、REBUILD_PLAN 等` または `REBUILD_PLAN 等` の部分文字列を削除し、「親 00、03 等」「親 00/01 等」のように一般化した表現に統一する。

#### 2.2.3 テスト観点

##### 単体（本タスクの具体ケース）

- `grep -rn REBUILD_PLAN .agent-skill-chain/source/` の結果が 0 件になる。
- 各修正行が元の文意（参照元の明記を促す指示）を維持している（目視確認）。

##### 結合/API

- 該当なし。

##### E2E/受け入れ

- verify-and-close で `grep -rn REBUILD_PLAN .agent-skill-chain/source/` を再実行し 0 件であることを 04_review.md に記録する。

##### バリデーション

- 該当なし。

#### 2.2.4 単体テスト仕様（BDD）

```gherkin
Feature: source 内 REBUILD_PLAN 参照の全件解消
  Scenario: 5 ファイルすべてから REBUILD_PLAN 参照が消える
    Given grep -rn REBUILD_PLAN .agent-skill-chain/source/ が 5 ファイル・6 行をヒットする
    When 各ファイルの該当箇所を編集する
    Then grep -rn REBUILD_PLAN .agent-skill-chain/source/ の結果が 0 件になる
```

```bash
# Given: 修正前に grep -rn REBUILD_PLAN .agent-skill-chain/source/ で 5 ファイルを確認済み
# When: 各ファイルを編集する
# Then: 以下の実行結果が空になること
grep -rn "REBUILD_PLAN" .agent-skill-chain/source/
```

#### 2.2.5 依存関係

- タスク 1 と独立。

#### 2.2.6 見積もり

- **工数**: 小
- **優先度**: 高

---

## テスト観点

本実装は静的ドキュメント修正のみであり、単体/結合/E2E の自動テストコードは対象外。検証は `grep` による文字列不在確認と目視レビューで代替する（各タスクの §2.x.3 を参照）。

---

## 単体テスト

自動テストコードなし。各タスクの §2.x.4 に記載の検証コマンドを verify-and-close 実行時に再実行し、結果を 04_review.md に記録する。

---

## BDD

各タスクの §2.x.4 を参照。

---

## 3. 実装スケジュール

### 3.1 マイルストーン

| マイルストーン         | 期日           | 成果物                                  |
| ---------------------- | -------------- | ---------------------------------------- |
| 是正・grep 確認完了     | 2026-07-14     | 修正済み 6 ファイル、grep 0 件の確認結果 |
| verify-and-close 完了  | 2026-07-14     | 04_review.md、workflow.db 記録            |

### 3.2 実装フェーズ

#### フェーズ 1: 是正実装

- **期間**: 即日
- **タスク**: タスク 1、タスク 2

---

## 4. テスト計画

各タスクの §2.x.3（テスト観点）・§2.x.4（BDD/検証コマンド）に従い、`grep -rn REBUILD_PLAN .agent-skill-chain/source/` の 0 件確認を最終ゲートとする。

### 4.1 テストの優先順位

- REBUILD_PLAN 参照の完全解消（grep 0 件）を最優先で確認する。
- SKILL.md の重複定義除去・証跡記述整合を次点で確認する。

---

## 5. リスク管理

### 5.1 実装リスク

- **リスク**: SKILL.md の記述を削除しすぎて、SKILL.md 自体が Claude Code の Skill エントリとして機能しなくなる。
  - **対策**: frontmatter（name/description）と「command を実行するときは必ず本ファイルを読むこと」等の入口としての説明文は維持し、重複していた委譲の形の詳細ブロックのみを削除する。

---

## 6. 参考資料

### プロジェクトドキュメント

- [`02_設計.md`](./02_設計.md) - 設計

### その他の参考資料

- `.agent-skill-chain/source/skills/agent/SKILL.md`
- `.agent-skill-chain/source/skills/agent/run_command.md`

---

## 7. 前のステップ

この実装計画書は、以下のドキュメントを基に作成されています：

- **前**: [`02_設計.md`](./02_設計.md) - 設計フェーズ

---

## 8. 次のステップ

この実装計画書の承認後、以下のステップに進みます：

- **プロジェクトが 1 つの issue（またはタスク）である場合**: 実装フェーズ（execution）に直接進む

**用語**: [.agent-skill-chain/source/CONCEPTS.md §用語規約](../../../../../../.agent-skill-chain/source/CONCEPTS.md#用語規約) を参照。
