---
document_id: "91afe5cb-1b49-4208-8778-a6db49f2ae76"
---

# 実装計画書: orchestrator 許可ツール allowlist に Skill を追加する

**プロジェクト名**: orchestrator 許可ツール allowlist に Skill が無く委譲経路と矛盾（自己ロックアウトリスク）
**作成日**: 2026年07月14日
**最終更新**: 2026年07月14日

---

## 1. タスク一覧

### タスク1: PreToolUse.sh R2 allowlist へ Skill を追加

- **対象**: `.agent-skill-chain/source/enforcement/claude/PreToolUse.sh`
- **内容**: 239行目付近の `case "$TOOL" in` の許可パターンへ `Skill` を追加し、追加理由のコメントを付す（既存の Agent 追加時コメントに倣う）。
- **完了条件**: `bash -n` で構文エラーが無い。

### タスク2: 回帰テストの追加・実行

- **対象**: `test/test-pretooluse-hook.sh`
- **内容**: UC1 に `uc1_orchestrator_skill_allowed` を追加し、`tool_name=Skill` が orchestrator ロールで exit 0 になることを検証する。
- **完了条件**: `bash test/test-pretooluse-hook.sh` が全件 PASS（新規ケース含む）。

### タスク3: 00_要求定義.md frontmatter の branch 記録

- **対象**: `00_要求定義.md`
- **内容**: frontmatter `branch:` を実際の作業ブランチ名に更新する。

## 2. テスト計画

- `bash test/test-pretooluse-hook.sh` を実行し、既存 UC1〜UC11 全件と新規ケースが PASS すること。
- 関連する他テスト（`test-audit.sh` 等、PreToolUse.sh に依存しないテスト）に回帰がないことを確認する。

## 3. 完了条件（DoD）

- タスク1〜3が完了している。
- テストが全件 PASS している。
- 04_review.md が作成され、受け入れ基準の確認が記録されている。
