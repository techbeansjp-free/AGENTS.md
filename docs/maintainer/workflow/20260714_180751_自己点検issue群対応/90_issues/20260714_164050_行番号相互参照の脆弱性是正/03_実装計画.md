---
document_id: "39afeb23-9b73-436e-b338-c5bdf3119dc8"
---

# 実装計画書: 行番号相互参照の脆弱性是正

**プロジェクト名**: 行番号相互参照の脆弱性是正
**作成日**: 2026 年 07 月 15 日
**最終更新**: 2026 年 07 月 15 日

> 本 issue は 163640 と**同一 PR・同一 worktree**（`worktree-agent-adf0b84c62a94c8b5`）で対応する。**実行順序の依存**: `自己拡張ワークフロー.md` 行 #3 は 163640 の Grp A（相対パス是正）→本 issue のアンカー化、の順で処理する。

---

## タスク分解（実行順・163640 の後）

### T1. `自己拡張ワークフロー.md` 他文書参照のアンカー化（#3・#4）
前提: 163640 T2 の Grp A（`../.agent-skill-chain/source/` → `../source/`）適用済み。

1. **#3**（`run_command.md`・`TEMPLATES.md` を行番号で参照する行）を手動 Edit で確定形へ:
   - 変換後（[02 §3.1](./02_設計.md)）:
     `[run_command.md §Constraints](../source/skills/agent/run_command.md#constraints)・[TEMPLATES.md §テンプレートの正本](../source/workflow/TEMPLATES.md#テンプレートの正本)`
   - リンク末尾の `:57`・`:12-13` を削除。display text の冗長パスも節名表記へ整理。
2. **#4**（`上記 run_command.md:57 に従う`）を `上記 run_command.md（§Constraints）に従う` へ Edit。

### T2. 自己参照の名称化（#1・#2）
1. **#1** `CLAUDE.md`: 文字列 `:18 のとおり` → `上記「作成場所の上書き最優先」のとおり`（`sed -i 's/:18 のとおり/上記「作成場所の上書き最優先」のとおり/' CLAUDE.md`。ただし他に `:18` が無いことを `grep -n ':18' CLAUDE.md` で事前確認）。
2. **#2** `PHASES.md`: `:33 のとおり` → `前掲「作成場所の上書き」のとおり`（同様に `:33 のとおり` の一意性を確認してから置換）。

### T3. 運用ルール明文化（FR-4）
- 「正本間の恒常参照に行番号を使わない／他文書はアンカー・同一文書内はセクション名で指す」を、既存のドキュメント規約（`.agent-skill-chain/source/DOCS_RULES.md` 等の最小追記点）に 1 文追記。追記先は実装フェーズで既存構成を確認して決定。

### T4. 検証
- `bash .agent-skill-chain/source/scripts/check-relative-links.sh .`（`--check-anchors` 既定 ON）→ #3 のアンカー（`#constraints`・`#テンプレートの正本`）が実在判定・切れ 0 件。
- `grep -rnE ':[0-9]{1,3} のとおり' CLAUDE.md .agent-skill-chain/source/workflow/PHASES.md` → 0 件。
- `grep -rn 'run_command.md:57\|TEMPLATES.md]\(.*\):12-13' .agent-skill-chain/project/自己拡張ワークフロー.md` → 0 件。
- 参照先ファイルの見出し・本文が不変であること（`git diff` で `run_command.md`・`TEMPLATES.md`・参照先本体に変更が無いこと）。

---

## テスト観点

| 受け入れ基準（01） | 検証 |
| --- | --- |
| Scenario A: 他文書行番号→アンカー | T1・T4（`--check-anchors` 実在判定） |
| Scenario B: 自己参照→名称参照 | T2・T4（`:NN のとおり` 0 件） |
| Scenario C: 参照内容一致 | T1（`#constraints` が memo 規約を指す） |
| Scenario D: 運用ルール明文化 | T3・CI 非ブロッキング（163640 T4 で導入済み） |

---

## 修正対象ファイル一覧

| ファイル | 種別 | 変換 |
| --- | --- | --- |
| `.agent-skill-chain/project/自己拡張ワークフロー.md` | 他文書参照×2（#3,#4） | `:57`→`#constraints`、`:12-13`→`#テンプレートの正本`、文中 `:57` 除去 |
| `CLAUDE.md` | 自己参照（#1） | `:18 のとおり`→規則名参照 |
| `.agent-skill-chain/source/workflow/PHASES.md` | 自己参照（#2） | `:33 のとおり`→規則名参照 |
| `.agent-skill-chain/source/DOCS_RULES.md`（追記先候補） | 運用ルール | 行番号参照禁止の 1 文追記 |

## 次のステップ

- **次**: レビューフェーズ（実装完了後）／本計画に基づく実装（implement-feature）
