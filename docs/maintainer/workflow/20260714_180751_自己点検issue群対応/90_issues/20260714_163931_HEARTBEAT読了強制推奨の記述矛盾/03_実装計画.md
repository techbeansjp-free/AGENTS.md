---
document_id: "67085b84-9237-479d-973f-dbb86274526d"
---

# 実装計画書: HEARTBEAT 読了強制推奨の記述矛盾

**プロジェクト名**: HEARTBEAT 読了強制推奨の記述矛盾
**作成日**: 2026 年 07 月 14 日
**最終更新**: 2026 年 07 月 14 日

> **用語**: [.agent-skill-chain/source/CONCEPTS.md §用語規約](../../../../../../.agent-skill-chain/source/CONCEPTS.md#用語規約) を参照。

---

## 1. 実装概要

### 1.1 実装方針（必須）

`AGENTS.md:10` の「HEARTBEAT 読了の強制」を「HEARTBEAT 読了の推奨」に修正し、CORE.md §Heartbeat（既存の「推奨」記述、変更不要）と一致させる。

### 1.2 実装順序（必須: 1 件以上）

1. `AGENTS.md:10` の文言修正（強制 → 推奨）。
2. grep による表現一致の確認。

---

## 2. タスク分解

### 2.1 タスク 1: AGENTS.md:10 の文言修正

#### 2.1.1 概要（必須）

`AGENTS.md:10` の「HEARTBEAT 読了の強制」を「HEARTBEAT 読了の推奨」に修正する。

#### 2.1.2 実装内容（必須: 1 件以上）

- `AGENTS.md:10` の該当行を「- **HEARTBEAT 読了**の推奨: [CORE](.agent-skill-chain/source/boot/CORE.md) §Heartbeat。」に修正する。

#### 2.1.3 テスト観点（必須）

##### 単体（本タスクの具体ケース・必須 1 件以上）

- `AGENTS.md:10` に「強制」の文字列が残っていないこと（grep で非該当）。
- `AGENTS.md:10` と CORE.md:111 の両方に HEARTBEAT 読了に関する「推奨」という語が含まれること。

##### 結合/API（本タスクの具体ケース・該当時は必須 1 件以上）

- 該当なし（単一ファイルの文言修正であり、他コンポーネントとの結合点が無いため）。

##### E2E/受け入れ（本タスクの具体ケース・未達時は理由記載）

- 該当なし（実行系の振る舞いを変更しないドキュメント修正のため、E2E は不要）。

##### バリデーション（該当する場合の具体ケース）

- 該当なし。

#### 2.1.4 単体テスト仕様（BDD）（必須）

```gherkin
Feature: AGENTS.md と CORE.md の HEARTBEAT 読了強度表現の一致
  Scenario: 表現が一致している
    Given AGENTS.md:10 を修正済みである
    When AGENTS.md:10 と CORE.md:111 の HEARTBEAT 読了に関する記述を比較する
    Then 両者はいずれも「推奨」と記載されている
```

**テストコード例（シェルによる静的検証）**:

```bash
# Given: AGENTS.md:10 を修正済みである
# When: 両ファイルの HEARTBEAT 読了に関する行を抽出する
# Then: いずれも「推奨」を含み「強制」を含まない
grep -n "HEARTBEAT 読了" AGENTS.md
grep -n "HEARTBEAT を読了することを" .agent-skill-chain/source/boot/CORE.md
```

#### 2.1.5 依存関係

- 依存なし（単独タスク）。

#### 2.1.6 見積もり

- **工数**: 5 分未満。
- **優先度**: 低。

---

## テスト観点

ドキュメントの文言一致を grep による静的検証で確認する（実行系コードの変更が無いため単体/結合/E2E テストコードは不要）。

---

## 単体テスト

`grep` コマンドによる文字列検証（2.1.4 参照）。

---

## BDD

2.1.4 のシナリオ 1 件（表現一致の確認）。

---

## 3. 実装スケジュール

### 3.1 マイルストーン

| マイルストーン | 期日 | 成果物 |
| --- | --- | --- |
| 文言修正・検証完了 | 即日 | 修正済み `AGENTS.md`、04_review.md |

---

## 4. テスト計画

grep による静的確認のみ。優先度・回帰対象は無し（影響範囲がドキュメント 1 行のため）。

---

## 5. リスク管理

### 5.1 実装リスク

- **リスク**: 将来 CORE.md 側の HEARTBEAT 読了ポリシーが「強制」に変更された場合、AGENTS.md との再乖離が起こりうる。
  - **対策**: 正本（CORE.md）を単一の情報源とする原則を維持し、CORE.md 変更時は AGENTS.md の参照文言も同時レビューする運用を徹底する。

---

## 6. 参考資料

### プロジェクトドキュメント

- [`02_設計.md`](./02_設計.md) - 設計

---

## 7. 前のステップ

- **前**: [`02_設計.md`](./02_設計.md) - 設計フェーズ

---

## 8. 次のステップ

- 実装フェーズ（implement-feature）に直接進む（単一 issue のためサブ issue 分割なし）。
