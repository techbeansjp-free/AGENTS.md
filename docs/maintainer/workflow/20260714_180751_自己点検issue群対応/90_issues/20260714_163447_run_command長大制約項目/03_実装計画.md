---
# document_id: 必須。作成時または major 更新時に UUID（8-4-4-4-12 形式）を付与すること。既存の場合は変更しない。
document_id: "c385ef5d-08b0-4cc9-aeec-5538269651f2"
---

# 実装計画書: run_command.md Constraints の単一項目が異常に長大

**プロジェクト名**: run_command.md Constraints の単一項目が異常に長大
**作成日**: 2026 年 07 月 14 日
**最終更新**: 2026 年 07 月 14 日

> **用語**: [.agent-skill-chain/source/CONCEPTS.md §用語規約](../../../../../../.agent-skill-chain/source/CONCEPTS.md#用語規約) を参照。

---

## 1. 実装概要

### 1.1 実装方針

02_設計 の ADR-1・ADR-2 に従い、run_command.md の該当 2 項目（:69, :70）を編集し、理由・観測事例の詳細を新規ファイル BACKGROUND_EXEC_RATIONALE.md へ切り出す。コード変更・テストコード追加は発生しない（ドキュメント編集のみ）。

### 1.2 実装順序

1. タスク 1: BACKGROUND_EXEC_RATIONALE.md の新規作成
2. タスク 2: run_command.md:69（バックグラウンド実行禁止項目）の短縮
3. タスク 3: run_command.md:70（AGENT_CONDUCT 転記義務項目）の見直し
4. タスク 4: 文字数比較・リンク整合性の検証

```mermaid
gantt
    title 実装スケジュール
    dateFormat YYYY-MM-DD
    section 実装
    タスク1_新規ファイル作成 :a1, 2026-07-14, 1d
    タスク2_69行短縮 :a2, after a1, 1d
    タスク3_70行見直し :a3, after a2, 1d
    タスク4_検証 :a4, after a3, 1d
```

---

## 2. タスク分解

### 2.1 タスク 1: BACKGROUND_EXEC_RATIONALE.md の新規作成

#### 2.1.1 概要

run_command.md:69 に含まれていた「理由」「観測事例」「判断基準の補足」「フォールバックの根拠」「既存項目との関係」を、新規ファイルへ欠落なく移設する。

#### 2.1.2 実装内容

- `.agent-skill-chain/source/skills/agent/BACKGROUND_EXEC_RATIONALE.md` を新規作成する。
- 冒頭に「本文（run_command.md）が正本、本ファイルは補足」の境界を明記する。

#### 2.1.3 テスト観点

##### 単体（本タスクの具体ケース）

- 移設元（run_command.md:69 旧本文）にあった 5 つの情報（理由・観測事例・判断基準・フォールバック根拠・既存項目との関係）のうち、いずれも新規ファイルから欠落していないこと。

##### 結合/API

- 該当なし。

##### E2E/受け入れ

- run_command.md:69 のリンク `[BACKGROUND_EXEC_RATIONALE.md](BACKGROUND_EXEC_RATIONALE.md)` が新規ファイルへ解決すること（同一ディレクトリ `skills/agent/` に配置）。

##### バリデーション

- 該当なし。

#### 2.1.4 単体テスト仕様（BDD）

```gherkin
Feature: 理由・観測事例の移設
  Scenario: 情報欠落がないことを確認する
    Given run_command.md:69 の旧本文（理由・観測事例・判断基準・フォールバック根拠・既存項目との関係を含む）
    When BACKGROUND_EXEC_RATIONALE.md を新規作成する
    Then 上記 5 要素がすべて新規ファイルに記載されている
```

**確認方法（ドキュメント編集のため実行コマンドで代替）**:

```bash
# Given/When: 新規ファイルの内容を確認
# Then: 5要素（理由/観測事例/判断基準/フォールバック/既存項目との関係）の見出しが揃っていることをgrepで確認
grep -c "^## " .agent-skill-chain/source/skills/agent/BACKGROUND_EXEC_RATIONALE.md
```

#### 2.1.5 依存関係

なし（最初のタスク）。

#### 2.1.6 見積もり

- **工数**: 小
- **優先度**: 高

### 2.2 タスク 2: run_command.md:69 の短縮

#### 2.2.1 概要

禁止事項・対象範囲・フォールバック条件は本文に残したまま、理由・観測事例の詳細をタスク 1 の新規ファイルへの参照リンクに置き換える。

#### 2.2.2 実装内容

- run_command.md の該当 Constraints 項目を編集し、997 字から明確に削減する。
- 禁止事項（`run_in_background: true` で長時間コマンドを起動して応答を終了する行為の禁止）、対象範囲（非対話型・有限時間コマンドに限る）、フォールバック（前提が成立しないランタイムでは無害化）は本文に残す。

#### 2.2.3 テスト観点

##### 単体（本タスクの具体ケース）

- 変更後の文字数が変更前（997 字）より少ないこと（実測して比較）。
- 本文だけを読んで「何が禁止か」「対象範囲」「フォールバック条件」が判読できること（目視確認）。

##### 結合/API

- 該当なし。

##### E2E/受け入れ

- 00 要求定義 §6 成功基準（「文字数が現状（約 1,500 字）より削減されていること」）を満たすこと。

##### バリデーション

- 該当なし。

#### 2.2.4 単体テスト仕様（BDD）

```gherkin
Feature: バックグラウンド実行禁止項目の短縮
  Scenario: 文字数が削減されている
    Given run_command.md:69 の旧本文（997 字）
    When 理由・観測事例を外部ファイルへ切り出し、本文を短縮する
    Then 新本文の文字数は旧本文より少ない
```

```bash
# Given: 変更前の行を退避済み（git diff で確認可能）
# When: 変更後の行の文字数を計測する
# Then: 変更前（997）より少ないことを確認する
python3 -c "
with open('.agent-skill-chain/source/skills/agent/run_command.md', encoding='utf-8') as f:
    lines = f.readlines()
print(len(lines[68]))  # 69行目
"
```

#### 2.2.5 依存関係

- タスク 1（参照先ファイルが先に存在すること）

#### 2.2.6 見積もり

- **工数**: 小
- **優先度**: 高

### 2.3 タスク 3: run_command.md:70 の見直し

#### 2.3.1 概要

AGENT_CONDUCT.md 第 3 部の全文転記義務を、「参照リンクのみで足りる（全文転記は必須ではない）」仕様に変更する。

#### 2.3.2 実装内容

- run_command.md:70 の文言を「そのまま転記する」から「参照リンクのみで足りる」へ変更する。
- リンク先を読めないランタイム向けのフォールバック（従来どおり全文転記）を明記する。
- AGENT_CONDUCT.md §0 読み替え規則との整合（矛盾時は規約側が優先）の記載は維持する。

#### 2.3.3 テスト観点

##### 単体（本タスクの具体ケース）

- 変更後の文言に「全文の逐語転記は不要」という趣旨の記載があること。
- フォールバック（リンク先を読めないランタイムでは従来どおり全文転記）の記載があること。

##### 結合/API

- リンクアンカー `#第-3-部-サブエージェント向け凝縮版委譲時に-constraints-末尾へ転記する` が AGENT_CONDUCT.md の実見出し（`## 第 3 部: サブエージェント向け凝縮版（委譲時に Constraints 末尾へ転記する）`）と一致すること。

##### E2E/受け入れ

- AGENT_CONDUCT.md 自体は変更しないこと（スコープ外）を確認する。

##### バリデーション

- 該当なし。

#### 2.3.4 単体テスト仕様（BDD）

```gherkin
Feature: AGENT_CONDUCT 転記義務の見直し
  Scenario: 全文転記が必須でなくなる
    Given run_command.md:70 の旧本文（全文転記を義務化）
    When 転記要件を「参照リンクで足りる」へ変更する
    Then 新本文に「全文の逐語転記は不要」という趣旨の記載がある
    And リンク先を読めないランタイム向けのフォールバックが明記されている
```

```bash
# Given/When: 変更後のリンクアンカーを確認
# Then: AGENT_CONDUCT.md の実見出しと一致することを確認
grep -n "^## 第 3 部" .agent-skill-chain/source/AGENT_CONDUCT.md
grep -n "第-3-部-サブエージェント向け凝縮版" .agent-skill-chain/source/skills/agent/run_command.md
```

#### 2.3.5 依存関係

- タスク 2 と独立（同一ファイル内の別項目のため順序制約は緩いが、diff の見やすさのためタスク 2 の後に行う）。

#### 2.3.6 見積もり

- **工数**: 小
- **優先度**: 中

### 2.4 タスク 4: 文字数比較・リンク整合性の検証

#### 2.4.1 概要

00 要求定義 §6 成功基準を満たしているか、実測で確認する。

#### 2.4.2 実装内容

- 変更前後の run_command.md:69, :70 の文字数を比較する。
- BACKGROUND_EXEC_RATIONALE.md へのリンク、AGENT_CONDUCT.md へのリンクアンカーが正しいことを確認する。
- 04_review.md に結果を記載する。

#### 2.4.3 テスト観点

##### 単体（本タスクの具体ケース）

- 変更前後の文字数差分が 04_review.md に数値で記載されていること。

##### 結合/API

- 該当なし。

##### E2E/受け入れ

- 00 要求定義 §6 の成功基準（文字数削減）が満たされていること。

##### バリデーション

- 該当なし。

#### 2.4.4 単体テスト仕様（BDD）

```gherkin
Feature: 検証
  Scenario: 成功基準を満たしていることを確認する
    Given run_command.md の変更後ファイル
    When 変更前後の文字数を比較する
    Then 変更後の文字数が変更前より少ない
```

#### 2.4.5 依存関係

- タスク 2, タスク 3 完了後。

#### 2.4.6 見積もり

- **工数**: 小
- **優先度**: 高

---

## テスト観点

本実装計画はドキュメント編集のみのため、コードの単体/結合/E2E テストは対象外。テスト観点は「情報欠落の有無」「文字数削減の実測」「リンク整合性」の 3 点に集約する（各タスク §2.x.3 に具体ケースを記載）。

---

## 単体テスト

コードの単体テストは対象外。§2.x.4 の BDD 仕様（grep・python での実測確認）を検証手段として用いる。

---

## BDD

各タスク §2.x.4 を参照。

---

## 3. 実装スケジュール

### 3.1 マイルストーン

| マイルストーン | 期日 | 成果物 |
| --- | --- | --- |
| 実装完了 | 2026-07-14 | run_command.md 更新、BACKGROUND_EXEC_RATIONALE.md 新規 |
| レビュー完了 | 2026-07-14 | 04_review.md |

### 3.2 実装フェーズ

#### フェーズ 1: 実装

- **期間**: 2026-07-14
- **タスク**: タスク 1〜4

---

## 4. テスト計画

### 4.1 テストの優先順位

- 禁止事項の情報欠落がないことを最優先で確認する（00 §7.1 リスク対策）。
- 文字数削減の実測を次点で確認する。

---

## 5. リスク管理

### 5.1 実装リスク

- **リスク**: 要約しすぎて禁止事項の強制力・明確さが弱まる（00 §7.1）。
  - **対策**: 禁止事項・対象範囲・フォールバック条件は必ず本文に残し、理由・観測事例のみを外部ファイルへ切り出す（02_設計 ADR-1）。

---

## 6. 参考資料

### プロジェクトドキュメント

- [`02_設計.md`](./02_設計.md) - 設計

### その他の参考資料

- `.agent-skill-chain/source/skills/agent/run_command.md`

---

## 7. 前のステップ

この実装計画書は、以下のドキュメントを基に作成されています：

- **前**: [`02_設計.md`](./02_設計.md) - 設計フェーズ

---

## 8. 次のステップ

この実装計画書の承認後、以下のステップに進みます：

- 実装フェーズに直接進む（本 issue は 1 issue で完結し、サブ issue への分割は行わない）。
