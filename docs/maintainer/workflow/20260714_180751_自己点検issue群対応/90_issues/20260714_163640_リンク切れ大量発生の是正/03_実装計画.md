---
document_id: "ef78414c-d171-4206-8678-6160ebeca52a"
---

# 実装計画書: リンク切れ大量発生の是正（統合実装計画）

**プロジェクト名**: リンク切れ大量発生の是正
**作成日**: 2026 年 07 月 15 日
**最終更新**: 2026 年 07 月 15 日

> 実装は sonnet ティアが本 worktree（`worktree-agent-adf0b84c62a94c8b5`）で継続する。**1 件ずつの手動編集ではなく、パターン別 sed で一括処理**する。全パスはリポジトリルート相対で記載する。

---

## タスク分解（実行順）

### T0. 事前計測（ベースライン）
1. 検査スクリプトはまだ無いため、設計時と同じ判定を再現する。以下いずれかで現状 29 件を確認する:
   - 簡易: `grep -rEon '\]\([^)]+\)' .agent-skill-chain/source .agent-skill-chain/project *.md docs/maintainer/*.md` で候補抽出し、`realpath -m` で存在判定（inline code 除去に注意）。
   - もしくは先に T1 のスクリプトを作ってから実行し 29 件を確認。
2. ベースライン件数（29）を記録。

### T1. 検査スクリプト新設: `.agent-skill-chain/source/scripts/check-relative-links.sh`
- [02 §2](./02_設計.md) の擬似コード・終了コード契約（0/1/2）・既定スコープ・inline code / fenced code 除外・`--check-anchors`（既定 ON、スラッグ化規則は [164050/02](../20260714_164050_行番号相互参照の脆弱性是正/02_設計.md#アンカースラッグ化規則)）を実装。
- ヘッダに用途・使い方・終了コードの日本語コメント（既存 `check-hook-drift.sh` に倣う）。`set -euo pipefail`。
- `bash -n` が通ること。

### T2. リンク切れ 28 件をパターン別 sed で修正
> 各 sed は「置換件数」を修正前後 `grep -c` で検証すること（過修正防止）。対象は追跡ファイルのみ。

**Grp A（12 件）** — `project/*.md` の `../.agent-skill-chain/source/` → `../source/`:
```bash
for f in .agent-skill-chain/project/自己拡張ワークフロー.md \
         .agent-skill-chain/project/README.md \
         .agent-skill-chain/project/COVERAGE_EXCEPTIONS.md; do
  sed -i 's#](\.\./\.agent-skill-chain/source/#](../source/#g' "$f"
done
```

**Grp B（4 件）** — `project/*.md` のルート系深度 −1 補正:
```bash
sed -i 's#](\.\./test/#](../../test/#g;  s#](\.\./\.github/#](../../.github/#g' \
  .agent-skill-chain/project/COVERAGE_EXCEPTIONS.md
sed -i 's#](\.\./docs/#](../../docs/#g' \
  .agent-skill-chain/project/自己拡張ワークフロー.md
```
（注: Grp A を先に適用済みのため `../.agent-skill-chain/...` は残っておらず、`../.github/`・`../docs/`・`../test/` のみが対象。競合しない。）

**Grp C（4 件）** — source→ルート文書（ファイル別に深度が異なる）:
```bash
sed -i 's#](\.\./CLAUDE\.md)#](../../CLAUDE.md)#g' .agent-skill-chain/source/CONTEXT_EFFICIENCY.md
sed -i 's#](\.\./AGENTS\.md)#](../../AGENTS.md)#g'  .agent-skill-chain/source/GETTING_STARTED.md
sed -i 's#](\.\./\.\./CLAUDE\.md)#](../../../CLAUDE.md)#g' .agent-skill-chain/source/workflow/PHASES.md
```

**Grp D（5 件）** — source→project の重複プレフィックス除去:
```bash
sed -i 's#](\.\./\.agent-skill-chain/project/#](../project/#g' .agent-skill-chain/source/RULES.md
sed -i 's#](\.\./\.\./\.agent-skill-chain/project/#](../../project/#g' \
  .agent-skill-chain/source/boot/CORE.md .agent-skill-chain/source/workflow/PHASES.md
sed -i 's#](\.\./\.\./\.agent-skill-chain/source/#](../../#g' \
  .agent-skill-chain/source/spec/ai/03_AIレビュー基準.md
```

**Grp E（1 件）** — IO_CONTRACT 深度不足:
```bash
sed -i 's#](\.\./\.\./IO_CONTRACT\.md)#](../../../IO_CONTRACT.md)#g' \
  .agent-skill-chain/source/skills/requirements/write-bdd/SKILL.md
```

**Grp F（1 件）** — close 移動済み対象（手動 Edit 推奨・close/ 配下は Read 不要）:
- `docs/maintainer/apm-package.md`: `](./workflow/20260713_033406_apm-npx-skill重複解消/02_設計.md)` → `](./workflow/close/20260713_033406_apm-npx-skill重複解消/02_設計.md)`

**Grp G（0 件・修正しない）** — `project/自己拡張ワークフロー.md` の inline code 内 `` `](...)` `` は本文説明であり修正対象外。T1 の inline code 除去で検査からも除外される。

> **注意**: `project/自己拡張ワークフロー.md` 行 192 は Grp A で**パス**が是正されるが、末尾 `:57`・`:12-13` の**行番号参照**は 164050 の担当（同一 PR で [164050/03](../20260714_164050_行番号相互参照の脆弱性是正/03_実装計画.md) の手順で対応）。**164050 の該当行編集は本 T2（Grp A）適用後に行う**こと（パス是正→アンカー化の順）。

### T3. 配備後階層変化の確認（FR-4）
- `source/skills/**/SKILL.md` の相対リンクが配備後 `.claude/skills/**`（フラット名前空間・深度変化）で解決可能かを確認。
- 解決不能なものがあれば相対リンクを撤去し「リポジトリルートからのパス」または同梱ドキュメント名の文言誘導へ変更。無ければ 04/実行記録に「該当なし」を明記。

### T4. CI 統合
- `.github/workflows/self-enforce.yml` に [02 §2.3](./02_設計.md) の**非ブロッキング** step を追加（`continue-on-error: true`）。既存コメント規約（`check-comment-refs.sh` の外部参照禁止）に抵触しない書き方にすること。

### T5. 検証
- `bash .agent-skill-chain/source/scripts/check-relative-links.sh .` → **切れ 0 件・exit 0**。
- `bash -n .agent-skill-chain/source/scripts/check-relative-links.sh`。
- 既存 CI ローカル検証（`bash test/e2e-install-uninstall.sh` 等は無関係だが、`verify-npm-pack.sh` が新スクリプト追加で赤くならないことを確認）。
- 修正差分が**リンク文字列のみ**であること（`git diff` で本文改変が無いこと）を確認。

---

## テスト仕様（BDD 対応）

| 受け入れ基準（01） | 検証 |
| --- | --- |
| Scenario A: 既知箇所が解決可能 | T5: check スクリプト 0 件 |
| Scenario B: inline code 誤検出なし | T1 実装＋`自己拡張ワークフロー.md` 行 254 が非計上 |
| Scenario C: CI 非ブロッキング | T4: `continue-on-error: true` の step |
| Scenario D: 配備後階層の記録 | T3: 該当有無を記録 |

---

## 修正対象ファイル一覧（実測 28 件の由来・行番号は参考値／ドリフト前提）

| ファイル | Grp | 参考行 |
| --- | --- | --- |
| `.agent-skill-chain/project/自己拡張ワークフロー.md` | A×7, B×1 | 192,204,215,233,243,301,315 |
| `.agent-skill-chain/project/README.md` | A×1 | 7 |
| `.agent-skill-chain/project/COVERAGE_EXCEPTIONS.md` | A×2, B×3 | 3,5,30,31,32 |
| `.agent-skill-chain/source/CONTEXT_EFFICIENCY.md` | C×2 | 7,64 |
| `.agent-skill-chain/source/GETTING_STARTED.md` | C×1 | 3 |
| `.agent-skill-chain/source/workflow/PHASES.md` | C×1, D×2 | 33,79,84 |
| `.agent-skill-chain/source/RULES.md` | D×1 | 48 |
| `.agent-skill-chain/source/boot/CORE.md` | D×1 | 140 |
| `.agent-skill-chain/source/spec/ai/03_AIレビュー基準.md` | D×1 | 9 |
| `.agent-skill-chain/source/skills/requirements/write-bdd/SKILL.md` | E×1 | 8 |
| `docs/maintainer/apm-package.md` | F×1 | 19 |

> 行番号はドリフト前提の**参考値**。実装は行番号ではなくリンク文字列パターンで特定すること。

## 次のステップ

- **次**: レビューフェーズ（実装完了後）／本計画に基づく実装（implement-feature）
