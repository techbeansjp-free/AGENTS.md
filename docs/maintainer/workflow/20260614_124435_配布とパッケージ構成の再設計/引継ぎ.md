# 引継ぎ — 配布とパッケージ構成の再設計

> 本書は別セッションへの**継続用コーディネーション文書**であり、ワークフロー成果物（00-04）ではない。新セッションが最初に読む前提で記載する。
> 作成日: 2026-06-14 / ブランチ: `feature/package-adapter`（既定は `main`）

---

## 1. 目的・現在地

- 本リポジトリは二役を担う:
  1. 「AI 実行契約・ワークフロー仕様パッケージ」の**正本**
  2. その正本を自分自身で進化させる**自己拡張ワークスペース**
- 本 issue「配布とパッケージ構成の再設計」は、agents の正規フロー（orchestrator → implement-feature → verify-and-close → 書記 scribe）で **dogfood しながら**進めている。
- 現ブランチ: `feature/package-adapter`（`main` が既定）。
- issue 成果物は `docs/maintainer/workflow/20260614_124435_配布とパッケージ構成の再設計/` 配下に 00〜04 まで存在。直近は `04_review.md` 更新まで完了。

## 2. 確定した決定事項

- **配布方式**: npm 主軸 + Claude marketplace 併設。
  - `.adapters/` は **100% 生成物**（git 管理外。`.gitignore` で `/.adapters/` を無視）。手書き土台は正本側へ分離済み:
    - `.agents/platforms/claude/plugin.json`
    - `docs/maintainer/adapters.md`
  - marketplace はリリース時に CI で build → 専用ブランチ/タグへ commit（**案A**）。`main` には生成物を一切置かない。
- **対象ツール**: claude code / cursor / gemini / copilot / codex。
- **仮置き（未確定）**:
  - npm スコープ `@techbeansjp-free/agents-md`
  - LICENSE: MIT
  - marketplace のブランチ戦略
- **自己拡張 issue の置き場所**: `.workflow/` ではなく **`docs/maintainer/workflow/`**（`.agents-project/自己拡張ワークフロー.md` が既定挙動を上書き）。
  - `.workflow/` は**消費者ランタイム**として gitignore（`/.workflow/*`、`workflow.db*`）。
  - memo は `**/memo/*` で gitignore（必要時のみ個別に追跡）。
- **enforcement は未配線（意図的に保留）**: 再設計中は最厳格モードを被せない。`.claude/settings.json` 配線はフェーズ5で検討。
- **作業方針（記憶済み）**: 外部アクセス無しで安全な作業は許可確認せず **auto 継続**。外部送信 / push / 破壊的操作 / 挙動変更時のみ確認を取る。

## 3. 完了済み（コミット）

| commit | 内容 |
|---|---|
| `213a61e` | 配布/構成の再設計の土台。正本/ランタイム分離・手書き土台の正本化・`.adapters` 100% 生成物化・setup の `.agents-project` リーク削除・npm 土台（`package.json`, `bin/agents-md.js`）。 |
| `7e32bbc` | 基盤是正（自己拡張）。`workflow.db` スキーマを `.agents/ledger/schema.sql` に**単一正本化**・自己強制 CI `.github/workflows/self-enforce.yml` を実体化・`04_review`。 |
| `b8c89d8` | フェーズ2。生成器を `.agents/scripts/build-adapters.sh` に一般化（claude 出力 diff ゼロ・cursor 追加・`build-plugin-claude.sh` は互換ラッパ）・CI トリガを `push:[main,develop] + pull_request` に是正。 |

- **自己ホスト済み**: `bash .agents/scripts/setup.sh` 実行により `.claude/` `.cursor/` `workflow.db` を生成（いずれも gitignore）。
  - 注: `workflow.db` はランタイム生成物のため、クリーンチェックアウト直後は存在しない。`setup.sh` で再生成する。
  - 過去の実行では台帳に entry 3 件・`prev_hash` チェーン OK（implement-feature → verify-and-close …）を確認済み。

## 4. 残ロードマップ

### A. 安全・auto 継続可（許可確認不要）

- **（課題4）命名の二重定義解消**: `{domain}__{capability}` 命名規則が
  - `build-adapters.sh` の `deploy_skills`
  - `setup.sh` の `sync_skills`

  に二重定義されている → 共有ライブラリ化で単一化（setup.sh 配備の一般化）。
- **（フェーズ1 仕上げ）**:
  - `npm pack --dry-run` の検証を固定。
  - README に `npx agents-md init` の導入手順を追記。

### B. 外部 / 判断が必要（要確認・要 Web アクセス）

- **（フェーズ3）multi-tool 対応**: gemini / copilot / codex の設定配置・skill 機構・フック有無は外部仕様調査が必要（Web アクセス）。
- **（リリース）**: marketplace ブランチ戦略・npm スコープ/レジストリ・LICENSE の確定。
- **（フェーズ5）enforcement 有効化**: セッション挙動が変わるため要確認。

## 5. 進め方（新セッションが守ること）

1. **着手前に必ず読む**:
   - `.agents/boot/CORE.md`
   - `.agents/boot/LOAD_POLICY.md`
   - `.agents/workflow/PHASES.md`
   - `.agents/skills/agent/run_command.md`
   - `.agents-project/自己拡張ワークフロー.md`
2. **orchestrator として進行**: phase 判定 → command 選択 → サブ委譲。
3. **実装後は必ず verify-and-close**: `04_review` を更新し、`write-workflow-log.sh` で `workflow.db` に記録する。
4. **配置ルール**:
   - `04_review` は issue 直下。
   - memo は issue の `memo/` 配下に `TZ=Asia/Tokyo date +%Y%m%d_%H%M%S` プレフィックスで作成。
   - 台帳記録は `write-workflow-log.sh` 経由のみ（`sqlite3` 直接操作は禁止）。

## 6. 主要ファイル

- **issue**: `docs/maintainer/workflow/20260614_124435_配布とパッケージ構成の再設計/`（`00_要求定義` / `01_要件定義` / `02_設計` / `03_実装計画` / `04_review`）
- **生成器**: `.agents/scripts/build-adapters.sh`（+ 互換ラッパ `.agents/scripts/build-plugin-claude.sh`）
- **配備**: `.agents/scripts/setup.sh`
- **スキーマ正本**: `.agents/ledger/schema.sql`
- **CI**: `.github/workflows/self-enforce.yml`
- **npm**: `package.json`, `bin/agents-md.js`
- **手書き土台（正本）**: `.agents/platforms/claude/plugin.json`, `docs/maintainer/adapters.md`
- **上書きルール**: `.agents-project/自己拡張ワークフロー.md`
