# 信頼境界

- パス、Unicode、シンボリックリンク、Git remote、repository、branch、HEAD SHA、認証、認可、外部観測状態を信頼しない。
- 読み取り結果だけに依存せず、書き込み・削除直前に対象と状態を再検証する。
- 候補PR内のpolicy変更は当該PRのmerge判断に使用しない。
- 候補PR内の`issueProject`追加・変更は、当該PRのProject item追加またはStatus更新authorityに使用しない。trusted default branchの設定だけを使い、未設定ならproviderを呼ばない。
- review、test、spec証拠は同じHEAD SHAへ結び付ける。
- context-isolatedのformal approvalは保存済みreview session・Step 10 binding・tracked H_final artifactの再照合からだけ導出し、candidate申告やartifact本文単体をauthorityにしない。actor-independentはprovider APPROVEDとstable actor分離を維持する。
- context-isolated admin mergeはformal approvalと操作authorityを分離し、GitHub `BLOCKED`自体を許可根拠にしない。repository ADMIN、許可rule閉集合、全check成功、未解決thread 0件、MERGEABLE・exact HEADをproviderから二重観測し、1つでも不明ならadmin dispatchを拒否する。candidate policyからこの許可集合を拡張できない。
- merge、Issue close、branch削除、release、cleanupを別操作として認可する。
- `.gitignore`を再生成可能性の根拠にせず、trusted project policyとpackage既定値から解決したfinalize ignore allowlistだけを削除可能な生成物境界として扱う。

本repositoryの`merge.mode=assisted`への変更も候補による自己適用を許さない。当該policy変更PRは変更前のtrusted policyに従い、repository ownerがGitHubの通常の保護・独立review・CIを満たして既定branchへ取り込む。admin bypassやcandidate側policyをauthorityにせず、既定branchで信頼済みとなった後の対象PRについてのみ、PRごとの明示指示をmerge authorityとして扱う。

## 隔離copy検査のtrust anchor

Gitに依存する隔離copy検査のtrust anchorは、candidateの外部で検査前にrepository・ref・commitを固定し、Git metadataをread-onlyで供給する。candidate内で生成した`.git`、origin、commitはauthorityに使わない。この外部anchorを建てられないときは、検査対象自身のGit状態へfallbackせずfail closedとする。これをTERM-ASC-110およびREQ-SQ-028とし、SCN-1344-001とSCN-1344-002で追跡する。

## deliveryのpackage floor入力

`createPullRequest`へ`trustedPolicy`を供給するcallerは、candidateの有無、preview/applyの別にかかわらず有効な`packageFloor`を型とruntimeで要求される。省略、`undefined`、`null`、不正構造、空の`rules`はprovider呼出し前に固定errorで拒否し、raw入力を診断へ含めない。復旧には`loadEffectiveTrustedPolicySet`が返す`packageFloor`をcandidateから独立して供給する。candidateやそのruleからfloorを導出しない。構造がvalidな偽floorの真正性をdomain単独で証明せず、真正なauthorityの供給はtrusted callerが負う。非trusted previewと共通`resolveEffectivePolicy`のextension合成契約は維持する。

CLIはtrusted loader出力を`commonInput.packageFloor`へ渡し、preview、事前検証、dispatchの3呼出しで同じ入力を使う。正規floorを供給しても、無提案・無sourceの削除は拒否し、trusted提案とsourceが一致する二段階廃止およびpackage floor保護を維持する。

## project choice差分の信頼境界

trusted側に`projectChoices`がある場合、candidateとの差分はfield単位で次の3分類へ分ける。object全体のfingerprint不一致だけを理由に一律拒否しない。

| 分類 | 対象 | 判定と処置 |
|---|---|---|
| authority | `release`、`ci` | 値の変更をASC-TRUST-001で拒否し、既定ブランチのproject policy ownerによるtrusted側の先行更新とauthority fieldの復元方法を案内する |
| 弱化 | test層、禁止test suffix、禁止型、strict型検査、capability、補助言語宣言、tier引き下げ、および未知field・型不正 | 検証の縮小、`applicable`の格下げ、宣言削除をASC-TRUST-001で拒否し、弱化の取消しまたは強化方向への変更を案内する |
| 許可 | authority境界外の既知field、説明・証拠、検証済み`modelMapping`の追加・強化、検証の拡大 | 変更field pathを記録して拒否しない |
| 弱化 | test層、禁止test suffix、禁止型、strict型検査、quality command、capability、補助言語宣言の単調性違反、および未知field・型不正 | 検証の縮小、具体値から`not-applicable`、`applicable`の格下げ、宣言削除をASC-TRUST-001で拒否し、弱化の取消しまたは強化方向への変更を案内する |
| 許可 | authority境界外の既知field、説明・証拠、`modelMapping`、検証の拡大 | 変更field pathを記録して拒否しない |

test層、禁止test suffix、禁止型の追加、`not-applicable`から`applicable`への変更、補助言語宣言の追加は検証強化として許可する。test層、禁止test suffix、禁止型の**縮小**は、既定branch側のproject policyの`projectChoiceShrinkProposals`へ登録した提案とchoices fragment fileのraw byteが完全に一致する場合だけ受理する。提案は既定branch側からのみ読み、候補branch側へ置いた同形の宣言を受理判断に使わない。提案が無い縮小、対象3 field以外の縮小、raw byte列を取得できない経路はfail-closedで拒否する。`modelMapping`をlegacy文字列から検証済み構造化値へ変更するなど、authority境界外の変更は、ほかのpolicy・品質条件を満たす限り正当な拡張経路として通過する。未知fieldおよびobjectでない入力は、型定義の拡張を黙って自己適用しないようfail-closedで弱化へ分類する。

| 脅威 | 予防 | 検知・復旧 |
|---|---|---|
| パス走査・Unicode偽装 | NFC正規化、基準内解決、制御文字拒否 | 副作用前に拒否 |
| 未検証のGraphQLite extensionへのすり替え | 公式v0.6.1 releaseのplatform別URL・asset名・size・SHA-256を固定し、symlink・非通常file・unsupported platformを配置前に拒否する。entry pointを明示しload後の追加extension loadingを無効化する | 不一致資産を上書きせず、固定assetの再install previewを案内する |
| Graph query injection・第二の編集正本化 | adapter内の固定parameterized queryだけを許可し、任意SQL・Cypher・汎用CRUDとGraphDB単独write APIを公開しない | injection入力とGraph単独事実を反例testで拒否し、正本だけから再構築する |
| stale・破損・他worktreeのGraphをexact Evidenceに利用 | source、schema、builder、extension、worktree、manifest、実Graph hash・件数をread-back照合する | drift理由をstable順で全件返しGateをfail closedにする。安全なwrite authorityがあれば新generationを完全再構築する |
| Graph探索の資源枯渇 | node、edge、depth、result、operationのhard budgetをstart・初期化を含めて前提にし、再帰ではないTarjan実装を使う | 上限時はcanonical部分結果とcounterを`budget-exceeded`で返し、completeまたはGate PASSと報告しない |
| repository誤認 | URL構文解析とowner/name完全一致 | 外部書込を行わず診断 |
| Project item・Statusの誤更新 | trusted設定からorganization、Project番号、field名、option名を解決し、Issue stagingのtrackerとrepository・Issue番号を完全一致させる。item、field、optionの一意性とpagination完了をmutation前に要求する | 重複・欠落・不完全観測はwrite 0件。mutation後read-back不一致は再送せず`reconciliation-required`で停止する |
| PR作成先・base・head・contentのすり替えと二重作成 | create intentより前にproviderのwrite authority、default branch/tip、remote head SHAを再観測し、指定repository・base、trusted policy commit、exact HEAD、canonical title/body digestのtupleへ拘束する。provider create直前にone-shot dispatch claimを耐久化する | claim後は成否不明でも再送しない。照合は全page・全状態を読み、same-repositoryかつcross-repositoryでないhead、固定contentを含む1件だけのOPEN以外をexact matchとして採用しない。base前進時は既存OPEN PRのread-only照合だけを許可し、固定intentによる新規再試行を禁止する。現在tipでのcreateは別workflowの新規intentとしてのみ認可する。CLOSED・MERGED一致、複数一致、曖昧状態は新規PRを作らず照合要求にする |
| merge authorityの古いbaseへの拘束 | merge直前にprovider default branch tip、PR base SHA、trusted policy commitを一致させ、classic protectionの404時はexact branchへ適用されるactive rulesetを取得 | protectionとrulesetの両方が不明ならfail-closed。`deletion`だけのrulesetはknownだがmerge保護なしとして拒否し、観測不一致はmerge要求前に拒否する |
| exact-head review Evidenceの取り違え | H_finalをH_implの単一親かつreview artifact 1件だけの差分へ限定し、対象repository・PR・H_finalへ直接結び付くsuccessful CI runとPR/H_impl authorから独立したapprovalを固定する。両authorのstable IDを必須とし、review全pageのactor別最新eventだけを採用する | author ID欠落、後続CHANGES_REQUESTED/DISMISSED、H_impl、artifact path/digest、CI run ID、review ID、正規evidence IDの不一致ではmergeを拒否する。AI推論や別branchのCIを代替Evidenceにしない |
| auto-merge・merge queueの誤観測と二重要求 | 固定merge intentとone-shot dispatch claimを先に耐久化し、auto-mergeのmethod・requestedAt・head/base OID、queueのentry ID・state・requestedAt・head/base OIDをproviderから再読取する | claim後は成否不明でも再送せず`reconciliation-required`とする。未dispatch、exact absence、固定認可tupleの不変が決定的に成立する場合だけ同一intentを1回再試行する |
| 偽のmerged終端 | local/providerの異なる時計を因果判定に使わず、消費済みdispatch claim、exact PR binding、merge commit SHA、固定methodに対応する親、固定base/headから決定した期待tree、provider既定branch tipへのancestor関係、固定review Evidenceをread-only再観測する。**CI runの再観測は固定`ciRunId`の直読みで行い、head_shaによる一覧検索を再実行しない。** `pull_requests`はPRが閉じると空になるため、再検索はmerge成功後に必ず失敗する。**fork由来を排除するため`head_repository`まで突合する**（Issue #1280） | native requestやqueue登録だけをStep 11にしない。request消失時も捏造せずresult Evidenceで判定し、親・tree・ancestor・固定tupleのいずれかが不一致なら終端化せず照合要求にする |
| worktree配置・命名の偽装 | repository相対の`.worktrees/`直下1階層、`YYYYMMDD_HHMMSS-<Issue番号>-<slug>`、`<許可type>/<Issue番号>-<slug>`へ拘束し、絶対path、親参照、nested path、制御文字、非NFC、symlink脱出を拒否する | 作成直前にcanonical rootと登録済みworktreeを再取得し、同一Issue・branch・pathおよびcase・Unicode正規化衝突を副作用前に拒否する。既存worktreeと作業元状態を保持して再入力する |
| worktree基点の取り違え | `origin/HEAD`が示すremote default branch、取得済みremote tracking refの40桁SHA、指定baseの解決SHAを完全一致させ、古いlocal branchだけを根拠にしない | 不一致または参照不能なら作成せず、remote default branchを取得・再検証してから再実行する |
| ignore対象を利用者所有資産と取り違える | package既定は`node_modules/`と`dist/`だけとし、project追加はtrusted manifestの安全なdirectory prefixへ限定する。候補policyによるprefix追加は削除可能範囲の拡大としてASC-TRUST-001で自己承認を拒否する | allowlist外pathまたはallowlist・pathの種別不明をpath付き保持理由としてsurveyとfinalizeの共通判定で検知し、対象worktreeを保持する |
| merge authorityのcleanupへの流用 | merge、root更新、worktree cleanupをoperation単位で分離し、cleanup applyごとの`--cleanup-authority`と最新64桁digestを要求する。merge承認、`--authorize=approved`、candidate policyからcleanup authorityを生成しない | cleanup authorityなしはroot更新済み・cleanup pendingとして対象を保持する。authorityまたはdigestを再確認して同じ完了フローを再実行する |
| 長命branch同士のsquashによる履歴分断 | baseRefとheadRefの双方が`merge.branches`へ列挙されている場合はsquashとrebaseを`pr merge`で拒否し、`merge`だけを許可する。base branchごとのmethodsは一致する全`branchMethods` entryの積集合とし、global許可の拡大と空集合を拒否する | `policy validate`は長命branchへmergeなしのsquash・rebaseだけが解決される設定をwarnとして検知する。拒否時はmerge-baseが進まず以後のmergeが全面衝突する根拠、mergeでの再実行、必要authority、非実行rollbackを返す |
| 部分書込 | 隣接一時領域と原子的rename | 未公開一時物を除去し既存状態を保持 |
| TOCTOU | 適用直前の二重read、最新状態hash、mergeのHEAD CAS、protection/ruleset、適用後read-back | 不一致時に成功扱いせず照合待ちとする。GitHubがHEAD以外のPR metadata CASを提供しない残余競合は検知対象とし、完全予防とは報告しない |
| 広域filesystem操作 | 明示repository root、owner・containment・symlink境界、preview/apply分離、承認済みreport hash、operation許可list、非再帰removeを要求し、Git領域とpackage manager所有directoryをoverride不能にする | apply直前の全候補再previewと削除前後のGit HEAD・refs・status・worktree listを照合し、不一致・部分失敗では削除済み・未処理pathと復旧方法を報告して停止する |
| 未同期stagingの喪失・tracker曖昧化 | quick・pocはStep 4、fullはStep 8のtracker書き込み後再読取一致を記録し、trackerをabsolute GitHub Issue URLへ限定する。同期証拠、mode別成果物、content digest、保持期限をすべて満たす場合だけ候補にし、年齢、名前、短縮Issue番号、空でないlegacyだけを根拠にしない | writer・parser・schemaで短縮trackerを拒否する。report hashとfilesystem fingerprintをapply直前に再planし、不一致、symlink、root外、未知fileでは削除0件で拒否する。部分失敗は成功にせず残存対象と再preview手順を返す |
| Step 11 crash recoveryによる改変追認 | generic journal APIからStep 11を分離し、delivery終端経路だけが固定PR/merge Evidenceから追記する。journal保存後の復旧判定より前にstaging artifact一覧とcontent digestを保存値へ照合する | digest、outcome、journal digest、evidence IDのいずれかが違えばstateを前進させず、provider副作用やjournalを再送しない |
| 旧PR終端の暗黙な再利用 | `outcome=pull-request`終端はtrusted policy変更だけでは再開せず、full/quickのowner明示値、旧Step 11 evidence、現在のtrusted policy commit、同一PR identity、exact HEADを別`redelivery`へ固定して通常のmerge認可を再実行する | flagなし、PoC、現在もdisabled、closed/merged PR、identity・HEAD・review・checks・protection不一致はdispatchを0回のまま拒否し、旧Step 11とjournalを変更しない |
| terminal redelivery authorityの自己申告 | 明示flagのsourceと`repository.assert-write`で観測したactor・repository・Evidence IDを耐久化し、固定repositoryへ一致させる | authority Evidence欠落と候補内の値だけによるowner判断生成をdispatch前に拒否する |
| 秘密混入 | 引数配列、伏字化、配布許可リスト | CIの配布物検査 |
| 候補policy自己緩和 | default branchのtrusted ruleと強度・activation・overrideを比較 | ASC-TRUST-001でnon-override拒否しcandidate差分をrollback |
| project choiceによるauthority変更・検証弱化 | trustedとcandidateの差分をauthority、弱化、許可へfield単位で分類 | authority fieldをtrusted値へ復元し、弱化は取消しまたは強化方向へ変更する。許可差分はfield pathを記録して継続 |
| applicability laundering | bindingまたはqualityの`not-applicable`に範囲を限定した理由と証拠を必須化し、bindingのsource・enforcement・反例SCNとの併記や空配列による形式的回避を禁止する | 理由・証拠の欠落、`applicable` objectによる具体値省略、禁止field併記をschemaとruntimeで拒否し、入力を変更せずproject ownerへ戻す |
| conformance宣言の弱化 | trustedとcandidateのscope、I1〜I12のbinding、status、enforcement point数、反例SCN数を比較する | `repository-bound`から`package-attested`、binding削除、`applicable`の格下げ、要素数減少をASC-TRUST-001で拒否しtrusted宣言を復元する |
| 品質validator自己変更 | base validatorでprotected file・package fieldのbefore/after SHA-256と品質契約versionを比較する。同一PRで追加したproposalはauthorityにせず、baseに既存の`staged` proposalとの完全一致だけを許可する | 未登録、部分一致、proposal削除・変更、version飛越しを拒否し、protected差分を適用しない |
| override逸脱 | target ruleId、expected ruleId、override record ruleIdの三者、およびIssue、scope、actor、理由、期限、SHAの完全一致 | cross-rule再利用を監査記録なしで拒否しowner authorityを診断 |
| trusted ref不明 | origin/HEADをcommit SHAへ固定できなければauthority operationを停止。PR CIはtrusted GitHub providerが観測したexact repo/PRのdefault base ref/OIDとhead OIDを明示入力へ拘束する | candidate/worktree/package install元floorへfallbackせずremote構成またはprovider入力を修復 |
| provider capability mappingの自己適用 | provider能力、公式選択元、project choice、conformance bindingはtrusted refのGit objectから読み、candidate worktreeの同名資産を読まない。具体的model slugとrankはmappingへ保存しない | candidate headをtrusted refとして使う評価と固定slug・手動rankの持込みをpendingにし、mapping ownerによるtrusted側更新を要求 |
| routing評価器の自己評価 | candidateが`src/domain/routing.ts`または`src/domain/routing-independence.ts`を変更した場合はevaluatorRefを記録し、candidate headの評価結果をauthorityにしない | evaluatorRefがtrusted refと一致する評価だけで再開する |
| routing evidenceへの秘密混入 | 入力を未知field拒否の保存許可listで検証し、credential、prompt、メールアドレス、stderr本文を保存しない | 不正なfield、識別子、容量超過を副作用前に拒否する |
| role設定による自己review | reviewerへimplementerと異なるidentityという独立性制約を必須化する。同一providerかつ同一論理tierだけを理由に一律拒否せず、identity・context・対象差分の非変更・review evidenceで独立性を判定する（Issue #1317） | BR-836-12の拒否診断を返し、実装とreviewを開始しない |
| ローカルLLM reviewerのendpoint偽装 | `modelMapping.roles.reviewer`がローカルLLM形状（`mode`field保有）の場合、`endpoint`をschema・実行時の両方でloopback（`127.0.0.1`/`localhost`）限定とし、それ以外はdispatch前に拒否する | `launchReview`が`rejected`を返し、レビュー対象コードが外部ネットワークへ送信されない |
| ローカルLLM reviewer `replace`モードの自己緩和 | reviewer役割の正式な差し替え（`replace`）はtrusted project policy（git管理下）の`modelMapping.roles.reviewer.mode`宣言だけで成立させ、`launchReview`はCLI引数での`mode`上書きを受理しない | candidateがgit管理外の個人設定だけで`replace`相当の効果を主張しても、正式なmerge gateの判定は変わらない |
| legacy fallbackのrole混同 | 既存routing形式がcoordinatorをfallback implementer候補へ解決しても、新しいrole契約はcoordinatorのproduct実装を許可しない | routing decisionを実行authorityにせず、独立implementerへ再割当してrole operationを再検証する |
| role兼務・自己承認 | coordinatorのproduct実装、同一scopeで同一identityまたはcontextのimplementer・reviewer兼務、reviewerによる対象差分変更を禁止する | role・操作・path・必要証拠が不明ならPR、merge、finalizeを拒否し、独立したidentity・contextへ再割当する |
| model tier降格 | risk、mode、scope、finding重大度から必要tierを単調に決め、model mapping未定義と選択tier不一致を拒否する | scope縮小、mapping ownerによるtrusted更新、または必要tier以上への再選択で復旧する。tierからauthorityを付与しない |
| provider上限回避 | Codexはhigh、ClaudeはOpusを自律選択上限とし、alias、自動routing、silent fallbackも実際の選択値で判定する | 上限内へ戻すか、provider・選択値・Issue・scope・指示者・日時・失効条件へ拘束された人間overrideを提示する。AI自己発行と再利用を拒否する |
| Step欠落の無条件迂回 | `pr create`でStep 10まで、特にStep 4・10と同期状態を外部操作前に検証し、無条件のbypass flagを設けない | 対象Issue、`workflow.pr.create` scope、指示者、理由、指示・失効日時が有効なHumanOverrideだけを欠落Stepのjournal行へ記録する。AI・role自己発行、別Issue、失効、未知field、欠落以外の不整合を拒否する |
| 回復不能な新規PR anchor | provider観測のdefault branch tipとexact head SHAのmerge-baseを`git merge-base --all`で一意に取得し、観測tipとの一致でancestor関係を判定する。PR後もdeliveryが続く経路では不成立を拒否し、Git比較失敗または一意なmerge-baseを得られない場合は全modeで拒否する。検査完了までHumanOverride journalを含む内部書込みとprovider作成を行わない | 既定branchをmergeで取り込んでHEAD依存のtest・review artifact・Step 10を再生成するか、内容等価なrebase後にPR作成前用の`review reanchor`で証拠を再固定して再実行する。merge disabledは比較が成立した非ancestorだけをPR正式終端の警告に限定し、PoCはprovider観測base..HEADのfixture限定を証明できなければ拒否し、既存anchor復旧や書換えauthorityは追加しない |
| PoC観測の自己申告・fixture逸脱 | 観測期間の主観報告やcaller作成Evidenceを使わず、repository内の隔離疑似project、runner ID・path・固定argv、use case・BDD scenario・observableをPoC宣言へ固定する。ASCは任意shellを受理せず、HEAD blobから復元したfixtureの固定Node runnerだけをLinuxのbubblewrap・prlimit下で実行する。namespace、read-only mount、timeout、出力上限を主境界とし、Node Permission Modelは補助防御とする | dirty/untracked、symlink・hardlink・特殊file、digest、process終了値、全observable、exact HEAD、baseline/provider baseからのfixture-only実Git差分を再検証する。EvidenceはHEAD別append-only、Step 9以降はdigest bindingとし、同一HEAD差替え、外部path書込、非正常終了、不一致、tool欠落を外部副作用前に拒否する |

`providerFiles`、`project/providers/`、構造化`modelMapping`はtrusted policy loaderの入力文法を拡張するため、同じcandidateで文法追加とproject設定activationを完結させない。第1段階はlegacy文字列と既存inventoryを保持したままschema・validator・runtimeだけを追加し、mainのtrusted validatorでcandidate全体を検証する。第2段階は第1段階のbranchまたはmerge済みcommitをtrusted baseとしてproject mappingを有効化する。同一PRで追加した入力文法をcandidate自身のauthorityとして使う構成は拒否する。

Non-override対象は秘密保護、Git内部領域、remote/worktree同一性、不可逆操作、成果物汚染、候補自身の承認防止である。環境変数や無期限flagはoverride authorityにならない。

Agent Routingの信頼方向は、trusted ref側のproject choiceとprovider capability mapping、およびprovider実行入口のread-only観測からrouting decisionへ、routing decisionからrouting evidenceと実装差分へ向かう一方向とする。candidateが変更したmapping、choice、resolver、独立性validator、conformance bindingを同じcandidateの選択・review・merge authorityへ使わず、completion recordやreview判定からrouting decisionへ戻る辺も認めない。

## project rule廃止の信頼境界

project rule廃止提案（TERM-ASC-095）はtrusted側の`projectRuleRetirementProposals`だけから読む。候補側の提案をauthorityへ使わず、effective合成でもtrusted時だけcarryする。固定trusted commitのmanifest、rules、rawEntriesの対応からsourceを導出し、rule IDとraw UTF-8のbefore SHA-256が完全一致する削除だけを承認済みrule廃止（TERM-ASC-096）として返す。reasonとownerは監査入力であり認証に使わず、比較診断にも転記しない。

| 境界 | 拒否条件 | 復旧 |
|---|---|---|
| JSONから提案 | 未知field、非配列、17件以上、exact duplicate、rule ID不正、大文字等のSHA不正、空値・制御文字 | 最大16件の有効な提案をownerが先行登録する |
| trusted sourceから比較 | 欠落、重複、manifest/rule/raw対応不明、ID・hash不一致 | 固定commitを再読取し、観測SHAで提案を更新する |
| trustedからcandidate | 候補自己承認、部分弱化、未提案ruleの便乗削除 | 削除を戻すか各ruleを個別に先行承認する |
| project extensionからpackage floor | 最後のproject ruleを除いてもpackage floorを削除しない。operationの対象rule不在は拒否する | fragmentとmanifest参照を復元する |

validate、file migration、deliveryは同じmatcherへtrusted sourceを配送する。sourceを持たないlegacy monolith経路は全削除拒否を維持する。提案の撤回は別PRとし、自動消費しない。詳細手順は`.agent-skill-chain/schemas/00_利用案内.md`を参照する。

## Git metadataの追加書込境界

追加権限は明示されたworkspace-writeに限定する。rootの通常`.git`directoryはGit出力2件と同一であることを要求する。linkedの`.git`fileはcommon directory直下の`worktrees/<name>`を指し、その`gitdir`はrootの`.git`を、`commondir`は同じcommon directoryを指すことを要求する。common directoryも通常の非bare primaryの`.git`であることをGitで確認し、separate-git-dirを許可しない。

各path componentを正規化前に検査してsymlinkを拒否し、通常file・directory、canonical path、metadataの有限サイズと単一行を確認する。Git管理path・configを上書きする`GIT_*`環境変数も拒否する。provider観測後にdevice/inode、種別、metadata内容とGit topologyを再検証し、変化時はdispatchしない。Git照会は固定argv、各10秒・64KiB上限でありraw出力を公開しない。

common directoryには他branchのrefs等も含まれる。追加許可をGit操作単位の認可とは扱わない。再検証とspawnの間の並行writerを原子的に排除するものではなく、Git/Codexの実行結果と既存のfailed/unknown契約を維持する。

## Codex自動起動の境界

`routing launch`は`loadOperationPolicy`がorigin/HEADの既定branchから固定したpolicyだけを採用判定に使う。candidate worktreeの新しいtierMapping keyは同candidateの認可根拠にしない。**`routing tier`のprovider未指定経路は候補側worktreeのproject choiceを読む互換検証であり、認可に使わない。** 出力の`provenance`と`usage`が判定の信頼源と用途を示し、診断はtrusted側の定義を要求しない。`--provider`は`codex`と`claude`だけを受理する。provider別`provider_recommended_default:high:default` selectorは既存の非空key→tier契約内の採用条件であり、schema fieldの追加や入力文法の自己拡張を必要としない。Claudeの公式initialize応答はrequest ID、成功envelope、model entry、逐語解決ID、effortを厳密に検証し、不明・重複・不一致を安全側で拒否する。keyの配送と当該実行への適用を分離し、起動直前にtrusted commitが変化していないことを再検証する。

公式catalogの観測ではconfig/readで`model_catalog_json`を検査し、選択関連設定だけを固定する。利用者の認証・安全設定全体を消さない。実行argvは固定し、任意model/provider/shell/危険flagを受け取らない。promptはroot内の通常fileをcontainment、symlink、size、open前後のfile identityで検証してstdinへ渡す。promptとraw stdout/stderrを結果証跡へ転記しない。selector採用tierはsandboxやGitHub操作のauthorityを与えず、hostの既存制御を迂回しない。

## ローカルLLM reviewer起動の境界

`routing review-launch`は`implementer`向け`routing launch`（`launchCodex`）とは独立したdispatch経路であり、`src/domain/routing.ts`・`src/adapters/codex-launch.ts`・`src/adapters/codex-execution.ts`へ依存しない（INV-05）。`launchReview`は`loadOperationPolicy`がorigin/HEADの既定branchから固定したtrusted policyだけを採用判定に使い、**dispatch前に再取得したcommit SHAが変化していないことを再検証し、dispatch後（ローカルLLM応答待ちの間）にも同じ再検証を行う。** dispatch前の検証だけでは、ローカルLLMの応答待ち（既定最大15分）の間に発生したtrusted policyの変更を見逃す。

Git管理外の補助レビュー設定を読む`resolveDelegatedReviewConfig`は、model入力の空値・制御文字・過大入力だけを拒否し、model名・tagの妥当性はOllamaへ委ねたうえで、`provider=ollama`とloopback HTTP endpointを必須とする。将来reviewer registryへ別providerが登録されても個人設定からは受理しない。model選択に下記の自律的reviewer役割用ceilingは適用しない。補助レビュー結果はformal approvalやmerge authorityを構成しない。

reviewer役割の解決は`resolveReviewRouting`が担う。`modelMapping.roles.reviewer`が`mode`fieldを持つ場合だけローカルLLM providerとして扱い、`provider`が`DISPATCHABLE_REVIEWER_PROVIDERS`（`src/domain/reviewer-provider.ts`）に登録済みで、`PROVIDER_AUTONOMOUS_CEILINGS`の対応entryに`model`が含まれ、`endpoint`がloopbackであることを要求する。いずれか1つでも満たさない場合は`rejected`とし、Codex/Claude向けの既存reviewer形状（`logicalTier`/`reasoningEffort`/`speed`を持つ）は従来どおり`validateRoleConfigurationIndependence`の対象になる。**`DISPATCHABLE_REVIEWER_PROVIDERS`は2026-09時点で`ollama`だけを含む。** 別のローカル実行runtime、または将来の外部SaaS reviewerを追加する場合は、この集合と`src/adapters/reviewer-executors.ts`のREVIEWER_EXECUTORS表へ登録するだけで済むよう、`resolveReviewRouting`・`launchReview`はprovider名の文字列比較を分岐条件に埋め込まない。`implementer`向け`resolveRouting`/`launchCodex`（`codex`/`claude`固定）は本抽象化の対象外であり、変更しない（Issue #1425の範囲外）。`resolveReviewRouting`はさらに、coordinatorとreviewerの同一identityも拒否する（`validateRoleAssignment`はimplementer/reviewerの独立性しか見ないため、ここで個別に補う）。

実行（`executeLocalLlm`）はOllama互換HTTP APIへの単一POSTに限定し、`child_process.spawn`によるCodexのような外部process起動を行わない。有限timeout・有限出力上限を既定15分・8MiBで実装する。Node組み込みfetchの独立したheader timeoutを使わず、標準HTTP requestをAbortSignalで総時間制限し、client既定値が先に失効して利用者設定を無効化しない。Ollamaには`stream: true`を指定し、改行区切りJSONを有限量で連結する。これにより生成完了までresponse headerを保留する非stream動作を避ける。上限到達・不正応答・接続不可・stream読取中のtimeoutはいずれも`succeeded`を騙称せず`unknown`または`failed`として安全側に倒し、設定上限到達は一般的な接続不可と区別して診断する。**`succeeded`時は応答本文を`output`として返す。** ローカルLLMが実際に何を指摘したかを呼出し元へ伝えるために必須であり、破棄すると本機能はreviewer役割の指摘内容を一切利用者へ届けられない。prompt本文は結果証跡へ転記しない。

`replace`モードの採用可否はtrusted policy（git管理下）の`modelMapping.roles.reviewer.mode`宣言だけで決まり、`routing review-launch`のCLI flagには`mode`を上書きする入力経路が無い。candidateがgit管理外の個人設定でreviewer役割の正式な充足条件を変更することはできない。

`replace`モードでdispatchが`succeeded`した場合、`launchReview`は`output`を`evaluateLocalLlmReview`（`src/domain/review-verdict.ts`）へ渡し、reviewer役割の正式な充足条件（FR-107）をこの判定結果で満たす。同関数はLLM出力から**主観的な評価部分**（`round`・`developmentConsiderations`・`affirmative`/`adversarial`評価・`findings`・`tests`・`specConsistency`）だけを取り出し、既存`evaluateReviewJudgment`（`src/domain/review.ts`。Claude/Codexレビューと同一のCritical/High findings blocking判定）へ渡す。**既存`evaluateReview`は使わない。** 同関数の`validateImmutableCandidateEvidence`は「GitHub上で実際にreviewがapprovedとして提出された」という外部事実の存在を要求しており、これから判定しようとしている対象には適用できない。ローカルLLMにこの外部事実（headSha・PR番号・CI run ID・reviewer actor ID等）を自己申告させると、この関数が守るべき「外部の実在するreviewだけが承認を与える」という前提を、まさにこの経路で崩す。`evaluateLocalLlmReview`はLLM出力からこれらのkeyを一切採用しない（含まれていても無視する）。**`findings[].riskAcceptance`も個別に剥奪する。** これは「人間の権限者がこのリスクを受容した」という承認の主張であり、主観的な評価とは性質が異なる。LLM応答にそのまま含めさせると、Critical/High findingを`status: "valid"`のまま自分自身の`riskAcceptance`で`acceptedRisks`側へ動かし`blocking`を回避できてしまう（自己承認）。`replace`モードのローカルLLM経路からはriskAcceptanceを一切成立させず、受容が必要な指摘は常に`blocking`として安全側に倒す。`supplement`モードではこの判定を行わず、`launchReview`の結果に`verdict`を含めない（FR-106のとおり既存reviewの充足条件を変えない）。

## 補助レビュー起動の境界

Step 03/07/10の`routing delegated-review-*`は進行役が設定済みローカルLLMへreviewを委譲する経路である。projectローカル設定をユーザー共通設定より優先し、`provider=ollama`・loopback HTTP・model入力の空値/制御文字/過大入力・有限timeoutをdispatch前に検証する。model名とtagの妥当性・存在はOllamaへ委ね、補助reviewにformal reviewer用model allowlistを適用しない。無設定は`disabled`、設定不正・収集失敗・実行失敗・不正応答は`degraded`とし、後者をreview完了として扱わない。LLMの主観評価だけを採用し、Critical/High findingがあるときはLLM自身の承認申告を無効にする。exact HEADと入力・出力digestを結果へ固定する。Git管理外設定とLLM応答からtrusted policy、risk acceptance、merge authorityを導かない。既存の`routing supplemental-review-*`の判定非参加契約は維持する。委譲・補助の双方で、収集済み入力本文を既定24KiBのUTF-8安全なchunkへ漏れなく分割し、設定されたtimeoutを全chunkで共有する。生成量の既定はchunkごとに2,048 tokenとする。両上限はGit管理外の`local/supplemental-review.json`で端末ごとに安全範囲内の値へ変更でき、進行役の委譲review、補助review、投稿前検証へ同じ値を適用する。chunkごとの構造化応答を統合し、同一findingは重複排除する。これにより27B級modelへ350KiB超を一括送信せず、1回のreview全体の待機上限も増幅させない。LLM出力の`finding.file`を今回の変更fileまたは対象staging文書の完全一致集合と照合し、差分外の指摘を除外して件数だけを返す。関連fileは既存のimport参照・汎用stem閾値で選別した文脈に限り、別taskの指摘を今回のblockingへ混入させない。対象stagingと`--root`は同じ登録済みGit worktreeに属することをdispatch前に検証する。設定は対象worktree、主worktree、ユーザー共通の順で探索し、先に見つかった明示無効化は後続を抑止する。

`routing supplemental-review-diff`/`-staging`（TERM-ASC-124、Issue #1428）は`routing review-launch`（`launchReview`）とは別の独立したdispatch経路であり、`modelMapping.roles.reviewer`・`resolveReviewRouting`・`launchReview`のいずれも参照・変更しない（INV-1428-04）。設定はエンジニア個別のGit管理外file（既定`.agent-skill-chain/local/supplemental-review.json`）から読み、`enabled`が`true`かつ`provider`が`DISPATCHABLE_REVIEWER_PROVIDERS`に登録済みの場合だけ有効化する。ファイル不在・形状不正・無効時はCLI全体が`disabled`を返し正常終了する（INV-1428-03）。

既定の補助review設定が対象worktreeに存在しない場合だけ、登録済み主worktreeのローカル設定を参照する。明示した設定pathと対象worktreeの明示無効化は優先する。`supplemental-review-diff`の関連file探索では、変更fileの拡張子なしdotfileをstem検索から外し、stemがHEAD内の11件以上のfileに現れる場合は汎用名として候補を追加しない。検索結果が1MiBを超えたstemも汎用名として除外する。関連file上限による切り捨てとは区別し、汎用名の除外だけで`truncated`にしない。Gitの変更pathと関連pathはNUL区切りで取得して引用表示を実pathへ混入させない。設定時の差分・関連file・staging収集は各1MiBで打ち切り、stagingは通常fileを収集中に検査する。

送信先の検証は`assertLoopbackEndpoint`（Issue #1425の既存実装）をそのまま再利用し、loopback（`127.0.0.1`/`localhost`）以外への送信を実行前に拒否する（INV-1428-01）。実行（`executeLocalLlm`）もIssue #1425と共通であり、有限timeout・有限出力上限、`succeeded`以外を安全側の`degraded`として扱う挙動を継承する。

補助レビューの指摘は構造化JSON（`findings`配列）としてCLI呼び出し元へ返すだけであり、`pr create`/`pr merge`/`review round`のいずれの判定にも用いない（INV-1428-02）。`evaluateLocalLlmReview`・`evaluateReviewJudgment`は呼び出さず、`riskAcceptance`のような承認相当のkeyを解釈する経路自体が存在しない。

`.agent-skill-chain/local/`は`install`/`update`の管理対象namespace（`docs`・`skills`・`templates`・`schemas`・`policy`・`hooks`）に含まれない（`src/domain/lifecycle.ts`の`NAMESPACE_ASSETS`）。そのため`managed-assets.json`にも登録されず、`update`実行時にこの設定fileが上書き・削除されることはない。エンジニアが一度有効化した補助レビュー設定は、package更新の影響を受けずに存続する。

Issue #1439のprofileとEffortはGit管理外の個人設定とreview応答の表示情報だけに作用する。設定schemaを閉じた選択値で検証し、送信先のloopback制限、非永続、no-telemetry、正式reviewのauthorityは変更しない。
Issue #1440の`suggestionPatch`は未信頼のLLM出力としてfinding配列・第二pass入力と分離して保持する。単一のASCII安全path、確定HEADの通常Git blob、unified diff文脈、適用後のTS/JS/JSON構文を、一時領域で検証した場合だけ表示対象findingの`committableSuggestion`へHEAD付きで添える。検証は最大8候補・合計256KiB・全体5秒、Git処理は個別最大2秒に制限する。path脱出・複数file・symlink・不一致・timeoutは提案だけを棄却し、findingを維持する。作業treeやformal review authorityを変更せず、一時領域は終了時に削除する。
Issue #1438の差分finding検証は、差分対象へscopeした候補を同じloopback executorへ再送し、確定HEADのGit blobに対する第二passの提案を得る。第二passの`reason`・障害行・失敗経路・遮断コードを`verificationAssessments`として候補indexとHEAD commit/fileに結び付けて返す。`quote_matched`は引用文字列がHEAD blobに存在することだけを示し、到達可能性や意味論の正しさは保証しない。採用判定に遮断コード、または却下判定に障害コード・失敗経路があれば、片方だけでも`conflicting`とし、引用欠落などは`unsubstantiated`として残す。どちらも機械的な採否に使用しない。初回候補を`needs_coordinator_review`として保持し、応答を構造化できない場合はassessmentと`verificationSuggestedFindings`をnullにする。補助差分レビューは比較基点と対象HEADに40桁の確定SHAだけを許可し、対象HEADと現在のHEADの一致をdispatch前後に確認する。可変refを出典として記録しない。HEADで削除済みのfileなど入力を固定できない場合はdegradedへ倒す。Step 10の委譲レビューもモデルだけで`approved`/`changes_requested`を確定しない。進行役は候補ID・主張・出典・成立条件・経路・支持根拠・反証・検証結果・分類理由を記録し、未観測の検証を成功と扱わない。この分類はformal independent approvalを代替しない。promptと候補は永続化・telemetry送信しない。

## ローカルJev provider設定の信頼境界

`loadJevProviderConfig`（`src/domain/jev-provider-config.ts`、TERM-ASC-1484、Issue #1484）は個人ローカルのJev（Decision Provider）向けconfig loaderであり、`routing supplemental-review-diff`/`-staging`（TERM-ASC-124）と同様に、`modelMapping.roles.reviewer`・trusted project policy（`project-policy.json`等）のいずれのpathも読み込まない（INV-1484-04）。この構造はimport文に該当pathが一切現れないという静的事実で保証され、実行時の分岐によるものではない。

設定は`enabled: true`かつ`apiKeyEnvVar`で指定した環境変数がprocess.envに非空文字列として設定されている場合だけ有効なconfigを返す（INV-1484-01）。設定file（`.agent-skill-chain/local/jev-provider.json`）が存在しない・JSON構文が不正・schema不正（`ALLOWED_FIELDS`方式で未知keyを拒否）・`enabled`が`true`以外・指定env var未設定のいずれの場合も、原因を区別せず単一のtry/catchで包んだ経路が例外を投げず`undefined`を返す（INV-1484-02、`supplemental-review-config.ts`の`loadSupplementalReviewConfig`と同型）。

生のAPIキー値は`process.env[apiKeyEnvVar]`から存在確認のためだけに読み、戻り値の`JevProviderConfig`へは一切含めない。設定fileへは環境変数の名前だけを書き、値そのものを書かない（INV-1484-03）。`configPath`引数はunit testがtmpdir fixtureを指すための開発者専用の注入点であり、機構によるpath traversal防止は持たない。実行時の呼び出し元が常に既定値`JEV_PROVIDER_CONFIG_PATH`のまま呼ぶという到達可能性の限定によって安全性を担保する（`configPath`へ動的・信頼できない入力を渡さない設計上の前提が崩れる場合は、その時点で別途path検証を設計する必要がある）。

本Issueの完了条件は、Jev providerのconfig読み込みだけを対象とし、実際のJev APIへのHTTPクライアント実装、`provider-capability-mapping`（v2）へのJev登録、APIキーの発行・失効・ローテーション手順は対象外とする。
