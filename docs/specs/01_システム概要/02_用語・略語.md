# 用語・略語

agent-skill-chainで現在有効なドメイン用語台帳である。開発工程の一般語は`.agent-skill-chain/docs/01_開発ワークフロー.md`を参照し、ここでは製品固有語だけを管理する。

| 用語ID | 標準語 | 定義 | 種別 | 境界づけられたコンテキスト | 成立例・反例 | 類義語・禁止表現 | 根拠ID・資料 | owner | 状態・適用版・置換先 |
|---|---|---|---|---|---|---|---|---|---|
| TERM-ASC-001 | project policy | 利用projectに適用するauthority coreとfragment inventoryを同一setとして識別する契約 | system | Policy | manifest、choices、rules、conformanceを同じtrusted commitへ拘束する。package default単体は該当しない | 設定、policy fileだけ | FR-834-09 | project owner | active、v0.3.1、なし |
| TERM-ASC-002 | project choice | 汎用packageが固定せず、利用projectが選択してschemaで検証する開発・表示・実行上の値 | system | Policy | test layerや表示言語の選択。安全floorの無効化は該当しない | rule、default | FR-834-09 | project owner | active、v0.3.1、なし |
| TERM-ASC-003 | rule | scope内のriskへ強制強度、証拠、修復、override、rollbackを対応付けるproject policyの最小統制単位 | system | Enforcement | 一意rule IDでoperation observationを評価する。説明文だけの推奨は該当しない | gate、checkだけ | FR-834-01 | rule owner | active、v0.3.1、なし |
| TERM-ASC-004 | trusted policy | candidate自身では変更できない信頼済みrefから読み、candidate評価とoperation認可に使用するpolicy set | system | Trust Boundary | PR base SHAのpolicyでcandidateを比較する。作業treeの自己申告値は該当しない | current policy、candidate policy | FR-834-02、FR-834-11 | repository owner | active、v0.3.1、なし |
| TERM-ASC-005 | candidate policy | 変更対象として評価される未信頼のpolicy set | system | Trust Boundary | PR headのpolicy変更。既定branchから観測したtrusted setは該当しない | trusted policy | FR-834-02 | change owner | active、v0.3.1、なし |
| TERM-ASC-006 | conformance binding | 不変条件をenforcement source、export、反例SCN、evidenceへ一意に結ぶ実行可能な対応 | system | Conformance | I1からI12を実在exportと成功SCNへ結ぶ。文書上の主張だけは該当しない | traceだけ | FR-834-10 | project owner | active、v0.3.1、なし |
| TERM-ASC-007 | 耐久トラッカー | セッションをまたいで要求・要件・設計・計画を再開でき、書き込み後読取確認された外部正本 | business | Workflow | GitHub Issueへ同期して再読取する。local tmpだけは該当しない | staging、作業メモ | FR-834-12 | change owner | active、v0.3.1、なし |
| TERM-ASC-008 | H_impl | 製品、仕様、testを固定し、Phase A review artifactをまだ含まない実装commit | acronym | Review Evidence | H_finalのancestorである。review artifact更新後のheadは該当しない | implementation branch | FR-834-11 | implementation author | active、v0.3.1、なし |
| TERM-ASC-009 | H_final | H_implへ許可されたreview artifactだけを加え、外部CI・review証拠が一致すべき最終candidate head | acronym | Review Evidence | H_implとの差分がevidence-only pathだけである。追加実装を含むheadは該当しない | latest headだけ | FR-834-11 | change owner | active、v0.3.1、なし |
| TERM-ASC-010 | trusted品質proposal | repository固有の品質契約変更について、既定branchが次versionとprotected対象のbefore/after SHA-256を事前登録する二段階migration authority | system | Trust Boundary | baseへstaged登録した次PRでexact targetを有効化する。同一PRで追加したproposalや部分一致は該当しない | candidate設定、byte一致だけ | FR-834-15、I4/I6 | repository maintainer | active、v0.3.1、なし |
| TERM-ASC-011 | coordinator | Issue分解、依存関係、担当割当、状態遷移、証拠集約へ専念し、成果物を直接実装しない進行役 | system | Agent Routing | 独立implementerへ委譲する場合は成立し、provider障害を理由にproduct実装を兼務する場合は成立しない | orchestrator、main agent、fallback implementer | Issue #830 | repository owner | active、v0.3.2、なし |
| TERM-ASC-012 | implementer | ACに対応するVerification Evidence、product code、test、system spec更新を担当し、自己review・merge裁定を行わない役割 | system | Agent Routing | implementerが選択済みVerification Setと最小実装を具体化する場合は成立し、同一identityが自分の差分を承認する場合は成立しない | worker、subagent | RQ-01、BR-836-06、Issue #836 | repository owner | active、v0.3.2、なし |
| TERM-ASC-013 | Codex available | coordinatorがCodex実行入口を実観測し、指定model、reasoning effort、service tierでtaskを開始できる状態 | system | Agent Routing | read-only実行が正常終了しmodel解決に成功する場合は成立し、実行ファイルが存在するだけの場合は成立しない | codexがある、installed | RQ-06、FR-836-02、Issue #836 | coordinator | active、v0.3.2、なし |
| TERM-ASC-014 | 最新Codexモデル | 各起動前のCodex公式model/list観測時点で、現在の利用環境に一意に推奨されreasoning effort highに対応するpicker-visible model | system | Agent Routing | isDefault=trueが一意でhigh対応なら成立する。固定slug、名称順、発売日順、全用途の最高性能という推測は成立しない | highest available coding tierは旧称。発売順・最高性能と同義にしない | REQ-WF-007、Issue #1257、公式model/list | provider operator | active、v0.3.2、同一IDの標準語を改名 |
| TERM-ASC-015 | standard speed | API利用時に`service_tier=default`へ解決される標準価格・標準性能の処理tier | system | Agent Routing | 解決結果がdefaultである場合は成立し、別のservice tierへ自動変更する場合は成立しない | fast、priority、flex、ultrafast | RQ-04、BR-836-03、Responses API一次資料 | project owner | active、v0.3.2、なし |
| TERM-ASC-016 | routing evidence | 実装開始前に1件だけ発行し、route mode、provider、model選択元、切替理由、mapping version、reasoning effort、service tier、role、identity、Issue、scope、base SHA、評価器ref、発行時刻、開始状態を保持する書換不能な機械可読証拠 | system | Evidence | Codex preferredとClaude fallbackを区別して発行後は本体を変更せず、状態と終了を別recordへ追記する場合は成立する | ログ、debug出力 | RQ-09、RQ-10、BR-836-07、FR-836-07 | change owner | active、v0.3.2、なし |
| TERM-ASC-017 | provider capability mapping | providerの能力と公式model選択元を版付きで宣言し、具体的model slugを持たず既定branchで追跡するtrustedな対応表 | system | Trust Boundary | trusted ref側がcoding能力と`provider_recommended_default`を宣言し、実modelはprovider観測から解決する場合は成立する | model slug台帳、手動rank | RQ-12、BR-836-05、Issue #836 | mapping owner | active、v0.3.2、なし |
| TERM-ASC-018 | completion record | routing evidenceの識別子を参照し、終了状態、実装head、記録時刻を持つ追記record | system | Evidence | routing evidenceを読み取り専用のまま別recordを追記する場合は成立し、本体を書き換える場合は成立しない | routing evidenceの更新 | FR-836-18、AC-836-22 | change owner | active、v0.3.2、なし |
| TERM-ASC-019 | release計画 | version、tag、対象ref・SHA、品質gate、dry-runから各release stageの実行可否を外部更新なしで決める判断結果 | system | Release | `dry-run`では検証だけを有効にする。workflow stepの成否だけは該当しない | release実行、version変更 | RQ-826-01、AC-826-01 | repository maintainer | active、v0.3.1以降、なし |
| TERM-ASC-020 | release操作結果 | Git tagとGitHub Releaseの各stageについて外部状態の読取確認から成功・失敗・未実行を記録した結果 | system | Release | tag成功後のRelease失敗をpartialとして記録する。job開始だけで成功とする場合は該当しない | release計画、workflow結果だけ | RQ-826-04、AC-826-04 | release operator | active、v0.3.1以降、なし |
| TERM-ASC-021 | workspace衛生 | 明示したGit repository root内の空directory、既知の未追跡一時生成物、不要な空worktree containerをpreviewと承認済みapplyに分けて安全に検査・除去する機能 | system | Workspace Hygiene | 候補と除外理由をpreviewし同じhashを再検証して非再帰削除する場合は成立する。広域shell式による再帰削除は該当しない | cleanup、空directory一括削除 | Issue #833 | workspace owner | active、v0.3.2、なし |
| TERM-ASC-022 | worktree配置policy | 通常の実装worktreeについてrepository内root、directory名、branch名、許可type、remote default branch基点、merge後cleanup時期を一体で拘束するproject policy | system | Worktree Lifecycle | `.worktrees/20260825_093000-831-worktree-placement`と`feature/831-worktree-placement`を取得済みremote default branch SHAから作る場合は成立する。repository外pathや古いlocal branchだけから作る場合は該当しない | worktree path設定、任意base | Issue #831 | project owner | active、v0.3.2、なし |
| TERM-ASC-023 | 自動release計画 | main mergeのref、head commit message、package version、既存tag、現在と前回releaseの配布digestから、release、bump後release、停止のいずれかを外部更新なしで決める判断結果 | system | Release | 未releaseの現在tag、または既存tagから配布物が変化した場合は成立し、`[skip ci]`付きcommitや配布物が同一なのにreleaseする場合は成立しない | release実行、手動release計画 | Issue #861、Issue #879 | repository maintainer | active、v0.3.1以降、Issue #879で変更path判定を廃止 |
| TERM-ASC-024 | 配布digest | `npm pack --dry-run`が列挙する配布fileのpathと、version fieldを正規化した各内容のSHA-256をUnicode codepoint順に正準化して得るSHA-256 | system | Release | READMEの内容変更または`package.json.files`への対象追加で値が変わる。配布対象外の`docs/specs/`だけの変更やpackage versionだけのbumpで値が変わる場合は成立しない | 変更path digest、Release本文のdigest | Issue #879、AC-879-01〜05 | repository maintainer | active、v0.3.1以降、なし |
| TERM-ASC-030 | rule coverage matrix | project rule IDごとに規範、schema、runtime、CIへの出現を真偽値で示すconformance対応表 | system | Conformance | 全rule fileにruntimeまたはCIのenforcement pointがある。文書だけの一覧は該当しない | rule一覧、test一覧 | Issue #832 | project owner | active、v0.3.2、なし |
| TERM-ASC-031 | orphan rule | policy、規範、schema、runtime、CIの対応が欠け、暗黙または未検証になったproject rule | system | Conformance | 規範にあるがpolicy未定義、またはrule fileにあるがruntime・CI未参照なら成立する。全対応済みruleは該当しない | 未使用rule、暗黙CIだけ | Issue #832 | repository maintainer | active、v0.3.2、なし |
| TERM-ASC-050 | model tier | providerやmodel名に依存せず、taskに必要な推論能力をroutine、standard、advanced、criticalの順で表す能力契約 | system | Role Policy | riskとscopeから必要tierを決める場合は成立し、tierだけでmerge authorityを与える場合は成立しない | model rank、操作権限 | Issue #830 | project owner | active、v0.3.2、なし |
| TERM-ASC-051 | 人間override | provider自律選択上限を超える選択について、対象provider・選択値・Issue・scope・指示者・日時・失効条件へ拘束された人間の明示指示証拠 | system | Role Policy | 同一scopeで有効期限内の指示は成立し、別Issueの指示やagent自己発行は成立しない | approval flag、AI判断 | Issue #830 | human authority owner | active、v0.3.2、なし |
| TERM-ASC-060 | host skill登録アダプター | Claude CodeまたはCodexのrepository skill探索pathから、agent-skill-chainの開発ワークフローとStep skill正本へ誘導するpackage所有の共通`SKILL.md` | system | Host Integration | `.claude/skills/asc-step/SKILL.md`と`.agents/skills/asc-step/SKILL.md`へ同じ正本を配置する場合は成立する。Step本文をhost別に複製する場合は該当しない | discovery skill、Step skill本体 | Issue #847、FR-01〜FR-07 | package owner | active、v0.3.2、なし |
| TERM-ASC-061 | 一時ステージング・ライフサイクル | 一時成果物の生成、同期待ち、同期確認、削除可能、削除または保持を状態と証拠で管理する契約 | system | Temporary Staging Lifecycle | Step 8再読取成功後に対象をcleanup可能とする。年齢だけで未同期を削除する場合は成立しない | tmp cleanup、期限切れ一括削除は禁止 | RQ-01〜04、Issue #860、管理データ仕様 | runtime・project owner | active、v0.3.2、なし |
| TERM-ASC-062 | 後片付け走査 | merge完了時にlocal processが存在しない経路の後で、登録済みworktreeを読み取り専用で観測し、後片付け可否と保持理由を報告する事後走査 | system | Worktree Lifecycle | `worktree survey`がcleanup-readyとretainを理由付きで分類する場合は成立する。走査自身がworktreeを削除する場合は該当しない | cleanup実行、workspace衛生 | Issue #883、AC-883-01〜05 | workspace owner | active、v0.3.2、なし |
| TERM-ASC-063 | step journal | Issue staging内でStep 0〜11の実施順、mode、成果物、証拠を1行1entryで追記する機械可読記録 | system | Workflow | 行順で0、1、4と進み、再実行を重複記録する場合は成立する。時刻順だけで実施を推測する場合は成立しない | workflow log、実施時刻一覧 | Issue #877、FR-877-02 | runtime・change owner | active、v0.3.2、なし |
| TERM-ASC-064 | モード判定成果物 | requested mode、判定mode、Q-01〜Q-08の回答・根拠、理由、判定時刻、必要なPoC宣言を保持するStep 0の正準JSON成果物 | system | Workflow | 全問と根拠を保存する場合は成立し、利用者の「quickでよい」という発話だけは該当しない | mode flag、要求文だけ | Issue #877、FR-877-03 | runtime・change owner | active、v0.3.2、なし |
| TERM-ASC-065 | finalize ignore allowlist | worktree finalize時にignore対象を再生成可能な生成物として削除可能と判定する、package既定値とtrusted project追加値からなる安全なUnicode directory prefixと限定された再帰directory・拡張子・basename patternの一覧 | system | Worktree Lifecycle | `dist/src/cli.js`、日本語task配下の`staging-record.json`、`review-session-02.json`が明示patternに一致する場合は成立する。`.gitignore`にあるだけの`.env`、`.agent-skill-chain/tmp/`、`issues/`、`memo/`、`.claude/`は該当しない | gitignore、consumer assets、任意glob | Issue #894、#1447、#1458、AC-LC-009 | package・project policy owner | active、v0.3.2、なし |
| TERM-ASC-070 | モード判定質問 | `quick`を選べる安全条件を1問1分類で問う8件の質問。すべて真かつ根拠付きの場合だけ`quick`を許可する | system | Workflow | 「公開契約を追加・変更・削除しないか」は成立する。判定基準を持たない問いは該当しない | Qとだけ書かない | REQ-WF-011、Issue #957 | package owner | active、v0.3.2、なし |
| TERM-ASC-071 | quick失格分類 | 変更が該当すると`quick`を選べない分類。モード判定質問と1対1で対応する | system | Workflow | `public-api`、`data-migration`は成立する。対応する質問を持たない分類は該当しない | 昇格条件とだけ書かない | REQ-WF-011、Issue #957 | package owner | active、v0.3.2、なし |
| TERM-ASC-072 | 配布準備工程 | git依存としてinstallされる際に、consumer側で配布物を生成するために実行される工程 | system | Distribution | `prepare`と`prepack`によるbuildは成立する。`.git`や開発用固定物を要求する検査は該当しない | prepackと混同しない | Issue #965 | package owner | active、v0.3.1、なし |
| TERM-ASC-073 | 配布前品質検証 | 配布物を公開する前にCIが実行する品質gateの集合。consumerの準備工程では実行しない | system | Distribution | `npm run verify:distribution`は成立する。`prepack`から呼ぶ形は該当しない | prepareと混同しない | Issue #965 | package owner | active、v0.3.1、なし |
| TERM-ASC-074 | consumer acceptance | 配布artifactを利用者と同じ入口から隔離環境へ導入し、公開入口が起動することを観測して合否を決める検査 | system | Distribution | packed tarballを隔離環境へinstallしてbinを起動する場合は成立する。source buildに対する検査は該当しない | consumer test、source build検査と同一視しない | REQ-SQ-027、Issue #1024 | package owner | active、v0.3.2、なし |
| TERM-ASC-086 | 復旧可能性 | worktreeを削除してもHEAD commitを後から取り戻せること。upstream refから取り戻せるか、既定branchの履歴から取り戻せるかのいずれかが成立する状態 | system | Lifecycle | HEADが既定branchの祖先なら成立する。upstream refがHEADと一致しても成立する。squash mergeでbranch commitがどこにも残らない場合は成立しない | 「到達可能性」を同義に使わない。到達可能性はGitのancestor関係だけを指す | REQ-LC-009、Issue #1097 | package owner | active、v0.3.2、なし |
| TERM-ASC-087 | ASC本体 | 配布されるagent-skill-chainそのもの。規範文書、schema、CLI、検査、template、project policy機構を含み、利用projectがASCを用いて作る成果物を含まない | system | 開発ワークフロー規範 | `01_開発ワークフロー.md`の記述、`workflow assess-discovery`の判定、review artifactの書式検査は本体である。利用projectのsource、test、`docs/specs/`、Issue staging成果物は本体でない。**目的にASC本体の保守を含むIssueは、本体を変更してもscope外の是正にあたらない** | 「ASC」単独で成果物側と混用しない | REQ-WF-012、Issue #1115 | package owner | active、v0.3.2、なし |
| TERM-ASC-088 | head allowlist | `merge.branches`が担う役割のうち、PRのhead branch名を`*`をワイルドカードとして照合し、一致しないmergeを拒否するもの | system | Policy | `feature/*`が`feature/x`のheadを許可する場合は成立する。baseの評価は該当しない | 長命branch集合と混同しない | Issue #1035 | package owner | active、v0.3.1、なし |
| TERM-ASC-089 | gate呼び出しstep | release workflowのstepのうち、配布前品質検証の入口commandをcommand位置で実行するもの | system | Distribution | `run: npm run verify:distribution`は成立する。`run: echo npm run verify:distribution`は該当しない | 検証stepと書かずjobと混同しない | REQ-SQ-020、Issue #980 | package owner | active、v0.3.2、なし |
| TERM-ASC-090 | 無条件のstep | `if:`を持たず、かつ`continue-on-error`が静的な`false`以外でないstep | system | Distribution | 属性を持たないstepは成立する。`if: ${{ github.event_name == 'push' }}`を持つstepは該当しない | 有効なstepと書かず`if: false`だけを見る判定と混同しない | REQ-SQ-020、Issue #980 | package owner | active、v0.3.2、なし |
| TERM-ASC-091 | 伝播するskip | あるjobのskipが後続jobの`needs.<job>.result`条件を満たさなくし、配布へ到達させないもの | system | Distribution | `validate`のskipで`tag`の`needs.validate.result == 'success'`が偽になる場合は成立する。stepのskipはjobを成功させるため該当しない | 安全なskipと書かず判定基準を省略しない | Issue #980 | package owner | active、v0.3.2、なし |
| TERM-ASC-092 | 束縛対象 | ある故障注入証跡の主張が成立するために内容が変わっていないことを要する製品file | system | Distribution | `scale-output`の証跡にとって`src/lib/process.ts`は成立する。注入対象である`package.json`は該当しない | 対象fileと書かず注入対象と混同しない | REQ-SQ-027、Issue #1221 | package owner | active、v0.3.2、なし |
| TERM-ASC-093 | 機構別束縛 | 束縛対象を機構ごとに宣言し、他機構の宣言から独立させること | system | Distribution | `git-dependency`が`scripts/check_package_contents.ts`を宣言しない状態は成立する。3機構が同一集合を宣言する状態は該当しない | 証跡別hashと書かず粒度の主語をfileにしない | REQ-SQ-027、Issue #1221 | package owner | active、v0.3.2、なし |
| TERM-ASC-075 | 隔離環境 | source repositoryの構築物と一時ステージング領域のいずれにも到達しない作業場所と環境変数の組 | system | Distribution | source外の一時directoryに作りPATHとcacheをsourceの構築物および一時ステージング外へ向けた場合は成立する。git依存のsource参照は成立例であり、`node_modules/.bin`をPATHに残したものは該当しない | 隔離workspace、sandboxとだけ書かない | REQ-SQ-027、Issue #1024 | package owner | active、v0.3.2、なし |
| TERM-ASC-078 | project choice縮小提案 | 利用側project ownerが既定branch側のproject policyへ事前登録する、対象field pathと縮小後のchoices fragment fileのsha256・理由・ownerを持つ宣言 | system | Policy | `projectChoices.forbiddenTestFileSuffixes`を空へする変更を、既定branchへ登録した提案として宣言する場合は成立する。候補branch側へ置いた同形の宣言は該当しない | 品質proposal、承認済み縮小と同一視しない | Issue #1044、REQ-SQ-003 | 利用側project owner | active、v0.3.2、なし |
| TERM-ASC-079 | 承認済み縮小 | 弱化と分類された差分のうち、登録済みの縮小提案とchoices fragment fileのraw byteが完全に一致し受理されたもの | system | Policy | 提案の`afterSha256`と候補fragmentのsha256が一致する縮小は成立する。提案が無い縮小、1 byteでも違う縮小は該当しない | 許可差分、縮小提案と同一視しない | Issue #1044、REQ-SQ-003 | 利用側project owner | active、v0.3.2、なし |
| TERM-ASC-084 | 証跡再固定 | 耐久記録した対象HEADを、被レビュー内容が変わっていないことを実証したうえで新しいcommitへ移す明示操作。記録はappend-onlyで積み既存の記録を書き換えない | system | Review Evidence | 既定branchが動いたためrebaseし同じ差分を新baseの上へ載せ直す場合、または`pr-bound`後に同じreview済み実装へTERM-ASC-101を満たす監査合格済みartifactだけを付け直す場合は成立する。TERM-ASC-1437を満たす同一path artifactの前進是正も限定的に成立する。衝突解決や実装内容の変更は該当しない | 再review、reset、anchor変更、一般的なsupersession、`pr-bound`の一般的なrebase許可と同一視しない。「正規名」「正規命名」「非正規名」をfile名規則の意味で使わない | REQ-WF-005、Issue #1093、Issue #1377、Issue #1433、Issue #1437 | package owner | active、v0.3.2、なし |
| TERM-ASC-085 | 内容等価性 | 旧baseから旧headまでの完全diff digestとpath集合が、新baseから新headまでのそれと一致すること | system | Review Evidence | `git diff --binary --full-index --no-renames`をbaseとheadへ適用したsha256とpath集合が両側で一致する場合は成立する。最終blobだけが一致してfile modeが違う場合は該当しない | tree一致、patch-id一致、blob一致と同一視しない | REQ-WF-005、Issue #1093 | package owner | active、v0.3.2、なし |
| TERM-ASC-080 | release bump基準SHA | そのreleaseのrunでbump treeを導出する基準として一度だけ確定する既定branchのcommit SHA | system | Release | run開始時の既定branchのtipを基準SHAとし以降のtree導出をすべてそこから行う場合は成立する。既存bump branchのheadを導出元にする、runの途中で取り直すものは該当しない | bump base、main tipとだけ書かない | REQ-GH-003、Issue #1051 | package owner | active、v0.3.2、なし |
| TERM-ASC-081 | 正規bump差分 | bump branchが基準SHAとの間に持つことを許される差分の閉じた集合。`package.json`の最上位`version`、`package-lock.json`の最上位`version`、`package-lock.json`の`packages[""].version`の3箇所に限る | system | Release | 3箇所だけが目標versionへ変わっている場合は成立する。`integrity`や依存の追加、`docs/`配下の変更を含むものは該当しない | version差分、bump差分とだけ書かない。配布digestの除外集合と同一視しない | REQ-GH-003、Issue #1051 | package owner | active、v0.3.2、なし |
| TERM-ASC-082 | trace endpoint候補 | 追跡表のFeature列・実装列のinline code区間のうち、repository pathとして解釈し実在検査と投影の対象にする区間 | system | Graph Projection | `src/full.ts`、`.gitignore`、`AGENTS.md`は成立し、`src/components/`、`src/**/*.css`、`ci:quality`、`.feature`、`z-index`は該当しない | repository path候補、inline codeとだけ書かない | REQ-GR-005、Issue #1067 | package owner | active、v0.3.2、なし |
| TERM-ASC-083 | 導出checkId | project ruleの`ruleId`から接頭辞`ASC-`を取り除き小文字化した値。conformance bindingの`check-ref`が参照する識別子 | system | Policy / Conformance | `ASC-PROJ-MAIN-PROTECT-001`に対する`proj-main-protect-001`は成立する。`asc-proj-main-protect-001`や任意命名の`main-branch-protection`は該当しない。連続ハイフンを含む`ruleId`、`ASC-`の直後が数字で始まる`ruleId`、導出後64文字を超える`ruleId`は導出checkIdを持たない | `ruleId`と同一視しない。`checkId`とだけ書かない | REQ-WF-008、Issue #1069、`.agent-skill-chain/docs/00_運用ポリシー.md` | package owner | active、v0.3.2、なし |
| TERM-ASC-068 | 既定branch追随merge | candidate branch上で既定branchの現在tipを取り込むために作るmerge commit | system | Review Evidence | 親が実装headと既定branch tipである場合は成立する。release bump commit、既定branch上のPR merge commitは該当しない | 追随merge、sync mergeは禁止 | REQ-SQ-018、Issue #962 | change owner | active、v0.3.2、なし |
| TERM-ASC-069 | 損失検知token | mergeでの内容消失を検知するために観測する、大文字英数の接頭segmentと2桁以上の連番をハイフンで連結したtoken | system | Review Evidence | `REQ-WF-010`、`TERM-ASC-068`、`AC-01`、`SHA-256`は成立する。`worktree`、`main`、`v0.3.1`は該当しない | 安定IDと同一視しない、識別子とだけ書くのは禁止 | REQ-SQ-018、Issue #962 | package owner | active、v0.3.2、なし |
| TERM-ASC-067 | 契約正本registry | 契約IDごとに正本pathと検出tokenを宣言し、正本以外の規範宣言locationでの記述を正本参照へ置換させるための対応表 | system | Policy | [開発ワークフロー](../../../.agent-skill-chain/docs/01_開発ワークフロー.md)が所有する`Closes #`を検出tokenとして登録し、正本以外の規範宣言locationのfileがそれを含むとき正本へのMarkdown linkを要求する場合は成立する。証跡や一時ステージングを違反とする場合、2箇所の記述内容を比較して一致を要求する場合、modal表現から規範文を自動判定する場合は成立しない | 内容一致検査、規範文検出、字句による規約記述判別、lint ruleは禁止 | REQ-SQ-016、Issue #952 | project owner | active、v0.3.2、なし |
| TERM-ASC-094 | selector採用tier | provider、公式選択元、推論、service tierの条件へprojectが付与する採用上の能力tier。CodexとClaudeはtrusted tierMappingの各`provider_recommended_default:high:default` selectorを参照する | system | Policy / Agent Routing | trusted採用条件を必要tierと比較する場合は成立する。公式defaultだけからcriticalを保証する、selector keyを実行modelへ渡す場合は成立しない | 公式性能tier、固定model IDではない | REQ-WF-007、Issue #1257、#1343 | project owner | active、v0.3.2、Issue #1343でClaudeへ拡張 |
| TERM-ASC-095 | project rule廃止提案 | 利用projectのpolicy ownerが既定branchへ事前登録する、廃止対象rule ID、trusted fragmentのbefore SHA-256、理由、ownerを持つ宣言 | system | Policy / Trust Boundary | trusted側の登録は成立し、候補側だけの宣言は受理根拠にならない | project choice縮小提案、削除許可と同一視しない | REQ-SQ-004、Issue #1211 | 利用project policy owner | active、v0.3.2、なし |
| TERM-ASC-096 | 承認済みrule廃止 | 完全削除として検知したruleのうち、trusted側のproject rule廃止提案とrule ID・raw before SHA-256が一致し受理されたもの | system | Policy / Trust Boundary | 指定ruleの完全削除は成立し、部分弱化、hash不一致、未提案削除は成立しない | 弱化許可、廃止提案そのものと同一視しない | REQ-SQ-004、Issue #1211 | 利用project policy owner | active、v0.3.2、なし |
| TERM-ASC-097 | managed asset record | packageが展開したhost資産のpathと導入時点のcontent digestを対応付け、以後の更新と削除の対象範囲を定める利用側runtimeの記録 | system | Lifecycle | `install --apply`が生成する`.agent-skill-chain/managed-assets.json`は成立する。project policyとhost設定fileは該当しない | managed-assets.jsonという実file名だけで代替しない。project policyと混同しない | REQ-LC-001、AC-LC-001、Issue #1305 | package owner | active、v0.3.1、なし |
| TERM-LC-1306-01 | managed record snapshot | 旧managed asset recordまたは直前snapshotのdigestで親を固定し、更新後のmanaged asset recordを保持する不変file | system | Lifecycle | 旧recordを親とする最初のsnapshotと、そのdigestを親とする次のsnapshotは成立する。既存JSON pathのrename置換は該当しない | 更新record、単一pathの上書きと同一視しない | REQ-LC-001、AC-LC-001、Issue #1306 | package owner | active、v0.3.1、なし |
| TERM-ASC-098 | 実CLI経路 | 配布されるCLI entrypointを別processとして起動し、その終了値・標準出力・fileへの副作用を観測する検査経路 | system | Workflow / Delivery | `dist/bin/agent-skill-chain.js`を別processで起動して終了値を観測する場合は成立する。判定関数をimportして直接呼ぶ場合、同一processで`main()`を呼ぶ場合は該当しない | 判定関数の直接呼び出しを実CLI経路と呼ばない。「E2E」とだけ書くのは禁止 | REQ-GH-001、Issue #1320、Issue #1317のD1317-08 | package owner | active、v0.3.2、なし |
| TERM-ASC-099 | 開発考慮事項の参照行 | fullの01〜03の開発考慮事項欄で、4行の表の代わりに置く固定書式の1行`開発考慮事項の適用判定は00_要求定義.md §6.1と同じ`。同じ成果物に書いた行だけを差分として個別に検証する | system | Issue成果物検証 | 01に参照行1行だけを置く場合、参照行とDC-PRIVACYの1行を置く場合は成立する。00に置く場合、「00と同じ」「同上」の自由記述は該当しない | 「同上」「00参照」を参照行と呼ばない | REQ-SQ-009、Issue #1326 | package owner | active、v0.3.2、なし |
| TERM-ASC-100 | review round雛形 | `review round --init`が保存済みsessionと実Gitから、round・previousRoundDigest・anchor・candidateHeadSha・focusを埋め、findingsを空で出力する入力JSON。staging外の新規fileにだけ書く | system | review session | `--out=./round.json`へ書いた出力は成立する。staging内や既存fileへは書かない。findingsを埋めたものは雛形でなく入力である | 「テンプレJSON」「skeleton」と呼ばない | REQ-WF-014、Issue #1323 | package owner | active、v0.3.2、なし |
| TERM-ASC-101 | evidence-only suffix | review sessionの実効HEADを唯一の親とする1 commitが、evidence-only allowlist（`docs/reviews/`・`.agent-skill-chain/reviews/`）配下のmode `100644`の通常file 1件を追加（元mode `000000`）または変更（元mode `100644`）するもの。実効HEADがancestorであることを必須とし、Step 10記録とPR作成・mergeはこれをcandidate HEADと同一視する | system | review session / delivery | `H_impl`→`H_final`が`docs/reviews/191_x.md`1件の追加なら成立する。`src/a.ts`を含む、2 path、空差分、削除、rename、type・mode変更、merge、複数commit、非ancestorは該当しない | 「artifact差分」の自由記述、「取り直し不要のHEAD」 | REQ-WF-005、Issue #1272 | package owner | active、v0.3.2、なし |
| TERM-ASC-102 | 同期本文 | 検証済みIssue stagingからmodeとcheckpointの規定順で選んだ成果物を連結し、耐久Issue本文の全面置換へ渡す決定論的text。project policyが`staging.issueBody=pointer`なら全文でなく配置・digest・目的・受け入れ条件だけのpointer形にする。fullのStep 8は00を先頭に置き、01〜03を`<details>`の折りたたみ区画へ入れ、code外の生の`<details>`と`</details>`を実体参照へ置き換えて構造を保つ。開始tagも置き換えるため意図した入れ子foldは字面表示になる | system | Issue同期 | full Step 8で00の後に折りたたんだ01〜03が続く本文は成立する。未検証fileの手動連結、review artifactの混入は該当しない | Issue本文一般、body file一般と同一視しない | REQ-WF-015、REQ-SQ-033、Issue #1333、#1406 | package owner | active、v0.3.2、定義変更（#1406） |
| TERM-ASC-103 | review artifact雛形 | 実装者が実Gitと保存済みstagingから機械導出できる比較基点、H_impl、変更path、staging digestだけを04へ事前充填し、reviewer判断とtest結果を未確定で保持した新規file | system | Review Evidence | `review artifact --init`の新規出力は成立する。合格判定を自動記入したfile、既存fileの上書きは該当しない | review round雛形、完成済みreview artifactと同一視しない | REQ-WF-015、Issue #1333 | package owner | active、v0.3.2、なし |
| TERM-ASC-104 | low-risk短縮行 | Verification Set riskがlowのときだけ、02 §4.2・§8・§9または03 §5.2の対象外理由を固定接頭辞付き1行で保持する記述 | system | Issue成果物検証 | `対象外: UI変更なし`は成立する。空理由、複数行、medium/high/criticalは成立しない | 省略、同上、空欄は禁止 | REQ-WF-018、Issue #1334 | package owner | active、v0.3.2、なし |
| TERM-ASC-105 | SCN ID | `SCN-`に大文字英数字とハイフン（`[A-Z0-9-]`）だけを1文字以上続けた、Scenarioを一意に識別する安定ID。Issue成果物検証とdelivery証跡検査が`src/domain/scenario-id.ts`の同一の述語で判定する | system | Issue成果物検証 / Delivery証跡検証 | `SCN-69-001`、`SCN-UNIT-TRACE-001`は成立する。`SCN-69-001a`（小文字枝番）、`SCN-69_001`（下線）、`SCN-`（本体なし）は該当しない | シナリオIDと表記しない。scenario name（IDに続く説明文）と混同しない。正規化・読み替えを行わない | REQ-WF-006、Issue #1349 | package owner | active、v0.3.1、なし |
| TERM-ASC-106 | workflow advance | 保存済みjournalから次のStepを導出し、検証・記録・同期または専用gateへの委譲を1操作で計画し、明示apply時だけ次の1 Stepを適用するCLI | system | Workflow | Step 1を1件記録する、Step 4本文を同期して記録する場合は成立する。reviewを自動承認する、PRを作成・mergeする場合は成立しない | 自動承認、自動merge、複数Step連続実行は禁止 | REQ-WF-020、Issue #1335 | package owner | active、v0.3.2、なし |
| TERM-ASC-107 | parallel progress evidence | H_impl固定後にimplementerが専用journalへappendし、review・test・PR・merge・releaseの成否判定には使用されない、閉じたfieldだけの進捗証跡 | system | Review Evidence | 宣言済みtask IDの状態追記は成立する。test成功、review承認、自由記述、既存entry改変は該当しない | review evidence、approval、delivery authorityと同一視しない | REQ-WF-021、Issue #1336 | package owner | active、v0.3.2、なし |
| TERM-ASC-108 | artifact unit | 所要時間ではなく結合度で境界を決め、1 Issue内で一体として完成・review・merge・rollbackできるADR、契約、機能、文書、移行の成果物単位。fullの00 §2.1直下へ任意markerで明示できる | system | Issue Validation | `- [成果物:feature] CLI warning`は成立する。独立した切り戻し、異なるreview担当・観点、先行merge、失敗影響の分離は別単位の候補となる | file数、対象項目数、keyword一致、所要時間の長さと同一視しない | REQ-WF-022、Issue #1337、#1369 | package owner | active、v0.3.2、なし |
| TERM-ASC-109 | 上流再確定entry | 後続Stepを記録した後に上流Step（1〜9）を再実施した事実を、順序判定から外して追記するstep journalのentry。`reconfirmation: true`を持ち、同じStepの通常entryが先行していることを要する | system | Workflow | Step 8記録後の`workflow record --step=3 --reconfirm`は成立する。先行entryの無いStep、Step 10・11、`--reconfirm`無しの後付けは該当しない | 再記録・後付けと混同しない。post-terminal intake（Step 10専用）とは別 | REQ-WF-004、Issue #1342 | package owner | active、v0.3.1、なし |
| TERM-ASC-110 | 外部trust anchor | 隔離copyのcandidateが生成・変更できない場所で検査前に固定したrepository・ref・commitの組 | system | Policy / Trust Boundary | 実repositoryのGit metadataをread-onlyでcandidate work treeに結び付ける場合は成立する。candidate内の`git init`、同内で作ったoriginやcommitは該当しない | fixture repository、origin、trusted provenanceと同一視しない | REQ-SQ-028、Issue #1344 | package owner | active、v0.3.2、なし |
| TERM-ASC-111 | Claude公式default観測 | Claude Codeの公式stream-json initialize応答にある`value=default`の一意なentryから、現在のhost環境が解決した逐語`resolvedModel`と対応effortを取得するread-only観測 | system | Agent Routing | `default`が`claude-opus-5[1m]`へ解決されhigh対応と申告される場合は成立する。名称・発売日から最上位を推測する場合、Models API一覧だけから推奨を決める場合は該当しない | Claude最高性能model、固定Opus slugと同一視しない | REQ-WF-007、Issue #1343、Claude Code model configuration / Agent SDK | provider operator | active、v0.3.2、なし |
| TERM-ASC-112 | context-isolated formal approval | trusted policyが`context-isolated`を適用するとき、保存済みreview session、Step 10 binding、tracked H_final review artifactのexact H_impl・path/digest・別context・非変更・肯定/敵対観点・最終approvedを照合したapproval authority | system | Review Evidence / Delivery | 一致するround digestをmerge intentのreview IDに固定する場合は成立する。同一GitHub actorでも別session/contextなら成立しうる | PR authorのprovider自己APPROVED、candidate申告、untracked artifactと同一視しない | REQ-WF-005、REQ-WF-013、Issue #1317、#1379 | package owner | active、v0.3.2、なし |
| TERM-ASC-113 | terminal redelivery | `outcome=pull-request`で完了したfull/quickのopen PRへ、後から成立したtrusted policyとrepository ownerの新しい明示判断を用い、旧Step 11を保存した別epochで同一PR・exact HEADを再認可してmergeするdelivery | system | Delivery | disabled終端後にtrusted assisted policyと`--reopen-terminal=approved`を使う場合は成立する。flagなし、PoC、closed PR、旧Step 11の書換えは該当しない | 終端取消し、Step 11再実行、bypassと同一視しない | REQ-WF-013、Issue #1378 | repository owner | active、v0.3.2、なし |
| TERM-ASC-114 | context-isolated admin merge | GitHubがPR authorの自己承認制約を含むreview条件だけで通常mergeを`BLOCKED`にしているとき、context-isolated formal approval、repository admin authority、許可rule閉集合、全check成功、未解決thread 0件、exact HEADを再観測して行う管理者merge | system | Delivery | rulesetだけで保護されたMERGEABLE PRへ`--admin --match-head-commit`を送る場合は成立する。未知rule、classic protection、失敗check、未解決thread、actor-independentには該当しない | `BLOCKED`の無条件迂回、自己承認、一般admin bypassと同一視しない | REQ-WF-005、REQ-WF-013、Issue #1379 | repository owner | active、v0.3.2、なし |
| TERM-ASC-115 | post-PR review intake | PR identity固定後かつStep 11前の`pr-bound`中に外部reviewer指摘を前進commitで取り込み、同sessionの次roundとStep 10をexact headへ再束縛する操作 | system | Review Evidence / Delivery | `workflow record --step=10 --post-pr-intake`が収束sessionのlatest roundと新`H_impl`へ一致する場合は成立する。Step 11後はpost-terminal intakeを使う | 通常のStep 10再記録、force push、再reviewなしのPR head更新と同一視しない | REQ-WF-004、REQ-WF-005、Issue #1389 | package owner | active、v0.3.2、なし |
| TERM-ASC-116 | Git metadata write roots | workspace-write起動時にrootのGit管理関係を相互検証してCodexへ追加配送する、実在git directoryとcommon directoryの重複除去済み集合 | system | Agent Routing | registered linked worktreeのgit directoryとcommon directoryは成立。prompt・CLI入力由来の任意path、reverse link不一致は反例 | 任意writable root、repository全体の許可は禁止 | RQ-1383-01、BR-1383-01、Issue #1383 | package owner | active、v0.3.2、なし |
| TERM-ASC-117 | 起票時点 | 計画上のタスクをcanonical Issueとして耐久トラッカーへ作成してよい、着手の意思決定を条件とする時点。Git commitの有無を条件にしない | system | ASC開発工程 | 着手を決めた計画単位の開始で起票することは成立。着手予定の無い将来計画の先行起票は反例 | 「Issue作成日時」という観測値の意味で使わない。禁止表現は「起票期限」 | RQ-01、BR-01、Issue #1398 | package owner | active、v0.3.2、なし |
| TERM-ASC-118 | 計画単位 | 複数タスクのMVP、完了条件、対象外、依存を束ねる期間または成果のまとまり。長さの既定は1週間で利用projectが変更できる | system | ASC開発工程 | 1週間または利用projectが宣言した期間で束ねることは成立。sprintの採用を前提条件にすることは反例 | sprintと同義に固定しない。禁止表現は「スプリント必須」 | RQ-02、BR-03、Issue #1398 | package owner | active、v0.3.2、なし |
| TERM-ASC-119 | 上流再確定 | Step 10記録後かつStep 11記録前に、順序判定から除外される再確定entryを上流Step（1〜9）へ追記し、staging digestを再固定する操作 | system | ASC開発工程 | Step 10後に`--reconfirm`付きでStep 1へ追記しdigestを再固定することは成立。Step 11記録後の同じ操作は反例で拒否される | 「やり直し」「再実行」と言い換えない。禁止表現は「Step 0からの再開」 | RQ-01、BR-01、Issue #1342、Issue #1312 | package owner | active、v0.3.2、なし |
| TERM-ASC-120 | Issue着手 | canonical Issueがtrusted project policyの指定Projectに一意に属し、指定Status fieldが着手状態であることをprovider read-backで確認した状態 | system | ASC開発工程 / Project管理 | IssueをProjectへ追加して`In progress`へ更新し再読取した場合は成立。Issue作成だけ、候補設定だけ、Status未確認は反例 | Issue作成、commit開始、自己申告の進捗と同一視しない | REQ-GH-006、Issue #1404 | repository owner | active、v0.3.2、なし |
| TERM-ASC-121 | 文書の読者 | documentが最初の画面で要点を届ける相手。発注・評価する人（要求と成否）、実装・レビューする人（判断と根拠）、運用する人（導入と更新）の3区分 | system | workflow contract | 00は発注・評価する人向け、02は実装・レビューする人向けは成立する。「全員向け」は該当しない | 想定読者。role（TERM-ASC-011等）と混同しない | REQ-SQ-033、Issue #1406 | package owner | active、v0.3.2、なし |
| TERM-ASC-122 | ローカルLLM provider | reviewer役割を`supplement`/`replace`で満たせる、trusted project policyが認識する新しいprovider種別。`modelMapping.roles.reviewer`が`mode`fieldを持つ場合に成立し、`PROVIDER_AUTONOMOUS_CEILINGS`へ登録済みの`ollama`だけを受理する | system | Agent Routing | `provider: "ollama"`かつceiling登録済みmodelは成立する。未登録provider、ceiling未定義modelは該当しない | 「ローカルレビュー」単独では役割割当の意味を持たないため区別する | REQ-WF-007、Issue #1425 | package owner | active、v0.3.2、なし |
| TERM-ASC-123 | reviewerモード | `modelMapping.roles.reviewer`（ローカルLLM形状）が持つ`supplement`（既存Codex/Claudeレビューへの追加）/`replace`（reviewer役割の差し替え）の2値。`replace`はgit管理下のtrusted project policy宣言でのみ成立し、候補側の個人設定では成立しない | system | Agent Routing | trusted policy commitの`mode: "replace"`は成立する。candidate限定の設定、CLI引数によるmode上書きは該当しない | 他機構（poc/quick/full等のworkflow mode）の「モード」と混同しない | REQ-WF-007、Issue #1425 | package owner | active、v0.3.2、なし |
| TERM-ASC-124 | 補助レビュー | `modelMapping.roles.reviewer`とは独立な、エンジニア個別のGit管理外設定からローカルLLMへdiff・staging文書を渡し、判定に使わない指摘を得る機構 | system | Agent Routing | `routing supplemental-review-diff`/`-staging`が指摘を返せば成立。merge判定に使う指摘は該当しない | TERM-ASC-122・123と混同しない。両者はgit管理下・project共有のreviewer役割宣言であり、本語は別物 | FR-1428-04、Issue #1428 | package owner | active、v0.3.2、なし |
| TERM-ASC-125 | progress inventory不成立 | 03実装計画にparallel progress markerが1組あるにもかかわらずinventoryを構築できず、当該review sessionでparallel progressを利用できない状態。分類はmode不一致、通常fileでない、markerが正確に1組でない、宣言済みtask IDが0件の閉じた集合とする | system | Review Evidence | mode 100664の03でroundが開きinventoryを持たない場合は成立。inventory成立後のmode変化による拒否、分類外のI/O失敗による拒否は該当しない | review失敗、review拒否、progress無効化と同一視しない | REQ-WF-021、Issue #1408 | package owner | active、v0.3.2、なし |
| TERM-ASC-126 | record layer | review済みH_implへformal artifact 1件とsealed journalからbyte一致を証明した宣言済みprogress投影だけを加えた単一親suffix | system | Review Evidence | artifactとtask README投影は成立。任意README・実装変更は反例 | documentation-onlyと同一視しない | REQ-WF-005、Issue #1418 | package owner | active、v0.3.2、なし |
| TERM-ASC-127 | reviewer input bundle | exact candidate、scope、AC・不変条件、直前blocker、変更隣接範囲、証拠digestを256 KiB以下の正準round JSONへ束ねたreview入力 | system | Agent Routing | `review round --init`出力は成立。full staging dumpは反例 | prompt全文と同一視しない | REQ-WF-005、Issue #1418 | package owner | active、v0.3.2、なし |
| TERM-ASC-128 | Codex Sol | 利用環境で選択可能なCodex Solを、実装担当と独立したcontextで起動するreviewer。特定のCLI model slugへの固定を意味しない | system | Agent Routing | Codex Solを別sessionで固定HEADへ当てる場合は成立。進行役自身の判定を読み替える場合は該当しない | Codex一般、merge authorityと同一視しない | REQ-WF-007、Issue #1449 | repository owner | active、v0.3.2、なし |
| TERM-ASC-129 | Opus | 利用環境で選択可能なClaude Opusを、実装担当と独立したcontextで起動するreviewer。特定のCLI model slugへの固定を意味しない | system | Agent Routing | Claude Opusを別sessionで固定HEADへ当てる場合は成立。Claude一般のreviewをOpus実施と記録する場合は該当しない | Claude一般、merge authorityと同一視しない | REQ-WF-007、Issue #1449 | repository owner | active、v0.3.2、なし |
| TERM-ASC-130 | CodeRabbit利用枠制限 | 対象PRの最新HEADについてGitHub check・review・通知にrate limitが明示され、進行役が出典URL・時刻・文言を観測した状態 | system | Review Evidence | 最新HEADのcheckに`Review rate limited`が出た場合は成立。check不在、過去HEADの通知、単なる待機は反例 | CodeRabbit未導入やreview待ちと同一視しない | REQ-WF-007、Issue #1449 | repository owner | active、v0.3.2、なし |
| TERM-ASC-131 | 計測イベント | role切替・model呼び出し・deterministic tool呼び出しの開始・終了を、`.agent-skill-chain/metrics/<staging名>/events.jsonl`（staging digestの対象外領域）へ追記専用で記録する最小単位 | system | Workflow | `workflow mark`を進行役が明示発行した場合は成立する。compiled codeが自動生成する場合は該当しない | ログ一般と同一視しない | REQ-WF-026、Issue #1482 | runtime・change owner | active、v0.4.0、なし |
| TERM-ASC-132 | 支援層所要時間（support_ms） | 計測window全体の経過時間から`artifact_build_ms`を差し引いた時間 | system | Workflow | `total_ms - artifact_build_ms`として算出する場合は成立する | オーバーヘッドと同一視しない | REQ-WF-027、Issue #1482 | runtime・change owner | active、v0.4.0、なし |
| TERM-ASC-133 | 成果物構築所要時間（artifact_build_ms） | `role=implementer`として記録された計測イベントのrole_ms合計 | system | Workflow | implementer roleのstart/end区間の合計として算出する場合は成立する。Step番号だけからの推定は該当しない | 全role_msの合計と同一視しない | REQ-WF-027、Issue #1482 | runtime・change owner | active、v0.4.0、なし |
| TERM-ASC-1432 | 委譲reviewer | 進行役が有効な個別または共通設定に従いStep 03/07/10のreviewを依頼するローカルLLM。評価と指摘を返すがmerge authorityを持たない | system | Agent Routing | 設定済みで対象差分の肯定・敵対評価を返す場合は成立。無設定で起動する、差分外taskをfindingに採用する場合は該当しない | TERM-ASC-124の補助指摘、trusted roleのTERM-ASC-122と区別 | REQ-WF-007、Issue #1432 | package owner | active、v0.3.2、なし |
| TERM-ASC-1437 | artifact-supersession | `pr-bound`後に同じpath・通常fileのreview artifactだけを単一の前進commitで訂正し、判断本文・配布物影響・review session束縛と経由済みStep chainを保ち、H_implからのartifact suffixを最大8 commitに収めて証跡再固定する限定method | system | Review Evidence | 誤記の書式だけを直し旧新artifactの判断本文と§8が一致する場合は成立。§8の判断変更、別pathや複数fileのcommit、実装の変更は該当しない | 一般的なsupersession、再review、artifact-replacementと同一視しない | REQ-WF-005、Issue #1437 | package owner | active、v0.3.2、なし |
| TERM-DC-001 | Decision Contract | 有限選択判断（deterministic codeでもGenerative Modelの深い推論でもない判断）の入出力を表す型契約。値・confidence・呼び出し先（軽量tier／Jev等）を切替可能な形にする | system | Decision Contract | `src/domain/decision-contract.ts`の`DecisionContract`型が、fail-open方向の値を持たない`callableTarget`を持つ場合は成立する。呼び出し先を実際に起動する実行ロジック（`decision invoke`、v0.4.2/Issue #1485が実装）はTERM-ASC-1485-01が所有し、本語自体は型契約だけを指す | decision provider、tier selectorと混同しない | RQ-01、Issue #1483 | package owner | active、v0.4.1、なし |
| TERM-ASC-1484 | ローカルJev provider設定 | `modelMapping.roles.reviewer`（git管理下・project全体共有）とは独立な、エンジニア個別のGit管理外設定（`.agent-skill-chain/local/jev-provider.json`）からJev（Decision Provider）向けconfigを読み込む機構。APIキー値はprocess環境変数からのみ得て、fileへは変数名しか書かない | system | ASC providerローカル設定 | `enabled:true`かつ指定env var設定済みで`loadJevProviderConfig`がconfigを返す場合は成立。file不在・不正形状・env var未設定で例外を投げる場合は該当しない | TERM-ASC-124（補助レビュー、reviewer役割専用）・TERM-ASC-1432（委譲reviewer）と混同しない。本語はreview委譲ではなくDecision Provider設定を指す | REQ-WF-028、Issue #1484 | package owner | active、v0.4.*、なし |
| TERM-ASC-1485-01 | Decision Type | `executor`（`{kind:"deterministic",resolverId}`または`{kind:"provider",target,model?}`）と`authorityMode`（`authoritative`/`advisory`/`one-way-escalation`/`constrained-choice`）を持つ、有限選択判断1種類の型定義。`agent-skill-chain decision invoke --type=<id>`が実行する | system | Decision | `src/domain/decision-types.ts`の`DECISION_TYPES`に登録され、`decision invoke`が解決できる場合は成立。TERM-DC-001（Decision Contract、値の型契約だけ）とは別語であり、実行ロジックを持つ点で区別する | TERM-DC-001（型契約のみ）と混同しない | REQ-WF-030、Issue #1485 | package owner | active、v0.4.2、なし |
| TERM-ASC-1485-02 | Decision Journal | Decision Skillの判断結果を`<primaryRoot>/.agent-skill-chain/runtime/decisions/<staging名>/events.jsonl`へ追記する`DecisionJournalRecord`の並び | system | Decision | `decision invoke --apply`が書き込み、`decisionRecordId`で一意に参照できる場合は成立。primaryRoot以外（active worktree）へ書かれる場合は該当しない（INV-03違反） | metrics journal（`.agent-skill-chain/metrics/`、別contextの類似機構）と混同しない | REQ-WF-030、Issue #1485 | package owner | active、v0.4.2、なし |
| TERM-ASC-1485-03 | LocalConfigResolution | ローカル設定（Jev provider config等）の解決結果を表す4状態（`absent`／`enabled`+`source`／`disabled`+`source`／`invalid`+`source`+`reason`）の型 | system | Workspace resolution | `resolveLocalConfigWithWorkspaceFallback`が返す値がこの4状態のいずれかであり、`absent`のときだけprimary worktreeへfallbackした場合は成立。`disabled`・`invalid`でfallbackした場合は該当しない | — | REQ-WF-032、Issue #1485 | package owner | active、v0.4.2、なし |
| TERM-ASC-WR-04 | 影響集合（Impact Set） | 2 commit間の実Git差分とhead commitのtreeから構築した意味Graphだけから一度導出する、変更path・直接import元とimport先の隣接範囲・security注意path・検証feature選択・理由の組とそのdigest。影響を証明できない場合は`mode: "full"`になる | system | Review Evidence | `impact --base --head`とreview round 2以降の`focus.adjacentScope`が同じ導出を使う場合は成立。呼び出し側が申告した隣接pathや任意64hexのGraph Evidenceは該当しない | 影響範囲の推測、`graph impact`の探索結果と同一視しない | REQ-WF-039、REQ-WF-040 | package owner | active、v0.4.2、なし |
## 更新規則

用語の追加・意味変更・改名・廃止はIssueの用語差分、対応要件、仕様変更履歴へ用語IDで追跡する。`deprecated`語は置換先と互換期間を残し、コード、CLI、diagnostic、test、運用文書の表記を同じ標準語へ揃える。
