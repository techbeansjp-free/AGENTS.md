# 品質要件

| 品質特性 | 合格基準 | 確認方法 |
|---|---|---|
| 安全性 | 不明なmode、権限、remote、証拠、最新状態を拒否する | 悪用・失敗Gherkin |
| 原子性 | 失敗時に既存内容と正本を変更せず、partial状態を公開しない | collision・途中失敗テスト |
| 可読性 | 人が読む文書と全テスト起票を日本語にする | 日本語文書・Gherkin形式検査 |
| 追跡性 | 要件、SCN ID、テスト結果、実装、HEAD SHAを対応付ける | 追跡検証とPR証拠 |
| 移植性 | 非Graph CLIはNode.js 20以上、Graph subcommandはNode.js 22.13.0以上でESMとして動作する | typecheck、build、Node 20配布bin smoke、CI |
| 配布品質 | 開発専用資産と`memo/`をnpm packageへ含めない | 実際の`npm pack`内容検査 |
| QLT-DIST-001 配布判定整合性 | 自動release要否は手書きの変更path集合ではなく、`npm pack`対象fileのpathと正規化内容から算出した64桁小文字hexの配布digestを現在の`v${package.json.version}` tagから再計算した値と比較する。package metadataのversionだけは除外し、他fieldと全配布fileの変化を保持する | SCN-UNIT-DIGEST-001〜010、SCN-UNIT-AUTOREL-D01〜D08、SCN-INT-DIGEST-001〜006 |
| 復旧性 | worktree完了前に到達可能な上流branchを保持する | finalize結合テスト |
| 開発速度 | local feedbackとPR gateのbudgetをproject policyで設定可能 | gateWaitMsとbudget超過診断 |
| policy品質 | 重複、誤block、override、rollback、検出漏れを観測可能 | metrics集計とdogfood E2E |
| 型安全性 | 製品実装はTypeScript strictで検査し、明示・暗黙の`any`を許可しない | `npm run lint`、`npm run typecheck`、source quality反例 |
| 形式品質 | ソース・JSON・設定を非破壊的な整形検査で固定し、書き込み整形は明示操作に分離する | `npm run format:check`、`npm run format:write` |
| Issue template整合性 | full、quick、pocの出荷要求templateがIssue検証器の全必須見出しを含む。templateだけの任意見出しは許可し、quickのkey term `最小Gherkin`は規範・検証器・templateの3箇所に存在する | 既存`npm run skills:check`内の`checkIssueTemplateHeadings`、SCN-UNIT-ISSUETPL-001〜005、SCN-INT-ISSUETPL-004 |
| 入力安全性 | 外部から取り込むJSON・policy・CLI入力は`unknown`から開始し、policy、manifest、state、evidence等の入力種別ごとに実行時検証してから型を確定する。未知field・未知version・型不正を黙殺しない | schema、入力種別別validator、SCN-UNIT-QUALITY-010 |
| 品質契約更新性 | candidateによる品質gateの自己緩和を拒否しながら、baseへ事前登録した版付きproposalとbyte/hashが完全一致する強化だけを次PRで有効化できる | `qualityContractVersion`、trusted proposal registry、SCN-UNIT-QUALITY-006、008〜009 |
| Privacy/Security | 秘密・個人情報・識別子の収集を最小化し、診断・ログ・例外への混入を防ぐ | redaction・悪用BDD、review |
| 運用可能性 | ログ・metrics・traceの目的、アクセス、改ざん防止、保持、rotation/容量上限、削除、監視、alertを境界ごとに設計する | 運用設計、失敗BDD、review |
| UI/UX契約 | UIを追加する場合は利用者中心設計、アクセシビリティ、Design/Layout Tokenを必須とする。現在のCLIは理由・証拠付きで非適用 | project choice、開発契約の適用判断 |
| QLT-ROUTE-001 安全性 | routing evidenceと診断に保存許可list外のfieldが0件であり、providerのstderr本文をログ、診断、例外、evidenceへ転記しない | SCN-INT-ROUTING-004、SCN-UNIT-ROUTING-012 |
| QLT-ROUTE-002 性能 | 実装開始前の外部観測をavailability観測と実行直前の再検証の合計2回以内にする | SCN-UNIT-ROUTING-009の観測回数検査 |
| QLT-ROUTE-003 可用性・復旧 | Codex停止または推奨model解決不能時は、Codexの値を推測せずproject choiceのClaude fallbackへ切り替え、route mode、選択元、理由、実装identityを証拠化する | SCN-E2E-ROUTING-001、SCN-UNIT-ROUTING-007・011 |
| QLT-ROUTE-004 互換性 | `modelMapping`がlegacy文字列または未設定の既存projectは読み込みに成功し、構造化設定を有効化するまでroutingを開始しない | SCN-INT-ROUTING-006の既存fixture回帰とmainのtrusted validatorによるcandidate検証 |
| QLT-ROUTE-005 保守性 | 汎用packageとproject mappingに個別model slugを保持せず、Codex公式recommended defaultへ実行時追従する | SCN-UNIT-ROUTING-002、SCN-INT-ROUTING-002・005、conformance検査 |
| QLT-ROUTE-006 利用性・アクセシビリティ | routing診断はrule ID、目的、リスク、根拠、次の操作、必要authority、rollbackの7項目を空値なく持つ。画面部品を持たないため支援技術と視覚要素の基準は対象外 | SCN-UNIT-ROUTING-010 |
| QLT-CLEAN-001 安全性 | unsafe、unknown、未承認でworktree削除0件、対象外worktree・branch・Git metadata変更0件とする | SCN-UNIT-WTCLEAN-001〜005、SCN-INT-WTCLEAN-002・004、SCN-E2E-WTCLEAN-003 |
| QLT-CLEAN-002 性能 | 対象PRの登録済みworktreeとrepository直下`.worktrees/`だけを判定し、cleanup専用hygieneではfilesystem全体を再帰走査しない | operation監査、SCN-INT-WTCLEAN-007 |
| QLT-CLEAN-003 可用性・復旧 | phase別結果を返し、root更新後のcleanup失敗を部分完了として保持し、同じ入力の再実行で収束できる | SCN-UNIT-WTCLEAN-004、SCN-E2E-WTCLEAN-006 |
| QLT-CLEAN-004 互換性 | 既存finalize、root更新、worktree配置、workspace hygieneの公開契約とscenarioを維持する | 既存SCN-INT-FINALIZE、SCN-ROOTUPD、SCN-HYGIENE回帰 |
| QLT-CLEAN-005 保守性 | 完了計画、operation authority、既存適用関数、事後要約を分離した純関数とCLI合成にする | architecture check、`planCompletion`、`summarizeCompletion` |
| QLT-CLEAN-006 利用性・アクセシビリティ | CLI JSONはphase、state、理由、必要authority、実在する次操作を日本語で返す。GUIを持たないため視覚・支援技術基準は対象外 | SCN-UNIT-WTCLEAN-002〜004、SCN-E2E-WTCLEAN-003・006 |
| QLT-STAGE-001 安全性 | 未同期、不明、未承認、状態変化、root外、symlinkではstaging削除0件とする | SCN-UNIT-STAGING-001、005〜006、SCN-E2E-STAGING-002 |
| QLT-STAGE-002 性能 | repository内`.agent-skill-chain/tmp/issues/`の既知inventoryだけを有限走査し、repository外を探索しない | operation監査、SCN-UNIT-STAGING-006 |
| QLT-STAGE-003 可用性・復旧 | path別の削除・保持理由を返し、途中失敗をpartially-completedとして新previewから再実行できる | SCN-INT-STAGING-001〜002 |
| QLT-STAGE-004 互換性 | lifecycle、workspace hygiene、Issue stagingの既存scenarioを維持し、一般hygieneはconsumer stagingを保持する | SCN-UNIT-STAGING-006、既存lifecycle・hygiene回帰 |
| QLT-STAGE-005 保守性 | 状態検査、cleanup計画、authority再検証、物理削除callbackを分離する | architecture check、`inspectStaging`、`planStagingCleanup`、`applyStagingCleanup` |
| QLT-STAGE-006 利用性・アクセシビリティ | JSONと日本語診断へ対象、理由、次操作、必要authority、rollbackを含める。GUIを持たないため視覚・支援技術基準は対象外 | SCN-E2E-STAGING-001〜002 |
| QLT-AUDIT-001 監査整合性 | `H_impl..current`はcommitごとに導入差分を検査し、subjectのversionが`isPackageVersion`を満たすrelease bumpで、変更pathが`package.json`と`package-lock.json`だけ、かつ`package.json`の`version`以外を`stableJson`で比較して一致する場合に限り、その変更を監査対象path集合から除外する。接尾辞は許可し、3条件の反例と同じpathへ通常commitも触れた場合は除外しない | SCN-UNIT-AUDITBUMP-001〜004、SCN-INT-AUDITBUMP-001〜004、`check_file_audit.ts` |
| QLT-AUDIT-002 merge互換性 | release PRのmerge commitはfirst-parentに対して導入した変更を検査し、別親側の固有commitがすべてrelease bump commitで、merge自身の導入差分もfile条件と`version`限定条件を満たす場合だけ除外する。別親側のrelease bump commitは、親1個で3条件を満たすcommitと、同じ判定を再帰的に満たすmerge commitの双方を指す。自動releaseは`release/bump-*` branchのPR mergeとして着地するため、既定branch追随でbumpだけを取り込んだ場合の別親側は必ずmerge commitを含む | 実main履歴のPR #872・PR #974、SCN-INT-AUDITBUMP-001〜004 |
| QLT-AUDIT-003 既存契約維持 | 比較基点と`H_impl`の完全SHA解決、baseから`H_impl`までのpath集合と個別監査表の完全一致、baseと`H_impl`および`H_impl`とcurrentのancestor検査、全個別判定`pass`を維持する | 既存SCN-UNIT-PACKAGE-011、`audit:check`、SCN-UNIT-AUDITBUMP-002〜004 |
| QLT-AUDSEL-001 監査成果物選択の決定性 | `audit:check`はfile名の番号・辞書順・directory内の最大値を選択へ使用せず、末尾の正規release bumpを除外したreview headと、その親として推定した`H_impl`の差分pathがちょうど1件かつ`docs/reviews/`配下である場合だけ、そのpathをreview artifactとする | SCN-UNIT-AUDITSEL-001〜006・008、SCN-INT-AUDITSEL-001〜004、`check_file_audit.ts` |
| QLT-MERGEINT-001 追随手順 | 既定branch追随はreview artifact commitより前で行い、追随後は`比較基点`を取り込んだ既定branch tip、`H_impl`をartifact直前の最新commitとし、個別監査表を`比較基点..H_impl`から再生成する。既存の判定ロジックを変更せずに成立させる | SCN-INT-MERGEINT-001、SCN-INT-MERGEINT-009、SCN-UNIT-MERGEINT-014、`.agent-skill-chain/docs/02_品質基準.md` |
| QLT-MERGEINT-002 損失検知 | 監査範囲`比較基点..H_impl`の各merge commitは、`両親のtoken集合の積集合` ∪ `第1親がmerge-baseから導入したtoken` ∪ `第2親がmerge-baseから導入したtoken`を、出現位置ごとに保持する。対象path集合はrename検出を無効にした4差分の和集合とし、移動元pathを取りこぼさない。merge結果に当該pathが無い場合は、そのpathを保持していた全親で移動先が解決し一意に定まるときだけ移動先での存在を認める | SCN-UNIT-MERGEINT-003〜008、SCN-INT-MERGEINT-002〜004、011、012、`evaluateMergeIntegrity` |
| QLT-MERGEINT-003 判定不能の拒否 | 親が2個でないmerge、merge-baseが一意でないmerge、内容を観測できないpath、rename移動先が一意でないpath、一部の親でだけ移動先を特定できたpath、複数の出現位置が同じ移動先へ潰れたpathを拒否する。保持必須集合が空の移動元は衝突として数えず、正当なrenameを誤検出しない。内容観測は三値とし、観測失敗を空集合として扱わない | SCN-UNIT-MERGEINT-009、010、019〜024、SCN-INT-MERGEINT-005、006、012、`observeTokens` |
| QLT-MERGEINT-004 例外の不在 | release bump除外を損失検知へ適用せず、無効化flagや環境変数を設けない。監査範囲内のmergeは例外なく観測対象とする | SCN-INT-MERGEINT-007、`collectMergeObservations` |
| QLT-MERGEINT-005 既存契約維持 | 監査範囲にmergeが無い場合、`checkFileAudit`の戻り値のkey集合と判定結果を変えない。検査件数は判定結果だけが持つ | SCN-INT-MERGEINT-008、SCN-INT-MERGEINT-010、SCN-UNIT-MERGEINT-011、013 |
| QLT-MODEQ-001 質問の対応 | モード判定質問は8件で、質問IDが`Q-01`〜`Q-08`と一致し、分類の集合が承認済み8 tokenと完全一致する。件数と一意性だけでは空文字や未承認tokenを含む8件が通過する | SCN-UNIT-MODEQ-001、004、005、006、014、021、022、`validateModeQuestions` |
| QLT-MODEQ-002 規範文書との相互一致 | 人が読む規範文書と機械可読な定義を`(質問ID, 分類, 質問文)`の順序付き8行として比較する。IDと質問文だけを比べると分類の付け替えを通す | SCN-INT-MODEQ-009、020、SCN-INT-MODEQ-002、011、`checkModeQuestionText` |
| QLT-MODEQ-003 複製の不在 | 質問文が規範文書と機械可読な定義以外の追跡fileに現れないことを要求する。追跡fileの列挙が失敗した場合は複製が無いとみなさず拒否する | SCN-INT-MODEQ-001、008 |
| QLT-LIFEIGNORE-001 分類の単一正本 | 一時ライフサイクル領域の一覧は`STAGING_LIFECYCLE_AREAS`だけが持ち、領域判定とpackage資産の禁止entry一覧はそこから導出する。利用箇所は一覧を複製しない | SCN-UNIT-LIFEIGNORE-001、002、`isStagingLifecyclePath` |
| QLT-LIFEIGNORE-002 無視設定の実効性 | 各領域について、浅い位置と深い位置の双方で一致patternが領域のdirectory patternであり、一致元がrepositoryで追跡されるroot `.gitignore`であることを要求する。追跡中fileに領域配下が無いことも要求する | SCN-INT-LIFEIGNORE-001〜004、009〜012、`checkLifecycleIgnore` |
| QLT-SCRIPTPIN-001 参照集合の包含 | 保護workflowが参照するscriptを全件無効値へ差し替えた候補treeで、既定branch側validatorが各scriptを名指しで拒否することを要求する。判定不能は拒否する | SCN-INT-SCRIPTPIN-001〜007、`checkTrustedScriptPinning` |
| QLT-PROTBOOT-001 保護追加の初期値拘束 | 候補が保護対象へ新しく加えたpathについて、既定branch側と候補側の内容が同一であることを要求する。候補の一覧はsourceから静的に取り出し、候補のcodeを実行しない。取り出せない場合と双方に実在しない場合は拒否する | SCN-UNIT-PROTBOOT-001〜003、`validateProtectionBootstrap` |
| QLT-EXPORTSCAN-001 enforcement走査の健全性 | commentやliteralの中の名前だけでenforcementを満たせないことを、正規表現literalを含むsourceでも保つ。判別できない`/`は正規表現として除去し、走査が脱線した場合は解析不能として拒否する。**誤って残すと実在しないexportを実在と誤認する** | SCN-UNIT-SAT-014、`validateRepositoryConformance` |
| QLT-ENTRY-001 実行entry判定の単一正本 | 判定は`isExecutionEntry`だけが持ち、realpath正規化した比較でsymlink経由の起動でも検査が走ることを要求する。等値比較の直接記述と、正本を参照しない手書きを静的に拒否する。正本module自身は対象外 | SCN-UNIT-ENTRY-001〜008、`isExecutionEntry`、`checkExecutionEntry` |
| QLT-SPEEDOBS-001 速度観測の記録 | review artifactへ仕様の所有箇所、成果物と支援層の行数、縮小の先行評価を必須にする。空欄は未記入として拒否し、所有箇所が該当なしの場合は仕様側の起票先を要求する。**比率に閾値を設けず自動停止させない** | SCN-INT-SPEEDOBS-001〜005、`checkFileAudit` |
| QLT-STEPCHAIN-001 規律の成果物への定着 | review artifactへラウンド数とStep chainの申告を必須にし、ラウンド上限超過と申告の欠落を拒否する。申告内容は検証しない。独立oracleを持たないためであり、`経由`のjournalは追跡対象外で既定branch側から観測できない | SCN-INT-STEPCHAIN-001〜009、`checkFileAudit` |
| QLT-PRBODY-001 PR本文の構造拘束 | PR本文が配布templateの必須見出しをすべて含み、未解決のplaceholderを残さないことを要求する。必須見出しはtemplateから導出し、条件付き見出しは必須にしない。Issue参照規約との同時充足を要求する | SCN-INT-PRBODY-001〜009、`validatePullRequestBody` |
| QLT-DISTSCRIPT-001 準備工程の対称性 | 準備工程を構築だけへ移す場合、`prepack`と`prepare`の双方が同じ構築commandであることを要求する。片方だけの変更を拒否する | SCN-UNIT-DISTSCRIPT-002、006、`validateDistributionScripts` |
| QLT-DISTSCRIPT-002 gate集合と順序の保持 | 配布前品質検証の入口は従来の準備工程のgate列と順序を含めて完全一致することを要求する。早期終了の注入、順序の入れ替え、任意commandの追加を拒否する | SCN-UNIT-DISTSCRIPT-003、004、005、009、`validateDistributionScripts` |
| QLT-DISTSCRIPT-003 移行期の両受理と自己緩和の拒否 | 従来の形と新しい形の双方を受理しつつ、いずれの形でも準備工程の自己緩和を拒否する | SCN-UNIT-DISTSCRIPT-001、007、008、`validateDistributionScripts` |
| QLT-DISTGATE-001 形と呼び出し先の一致 | 配布準備工程が構築だけの形なら release workflowは配布前品質検証の入口を呼び、全gateの形なら準備工程を呼ぶ。片方だけの変更を双方向で拒否する | SCN-INT-DISTGATE-001〜004、010、`checkDistributionGateReachability` |
| QLT-DISTGATE-003 呼び出しの構造判定 | 呼び出しの有無はYAMLのstep構造で判定する。comment、`echo`の引数、`if: false`で無効化されたstepを実行とみなさない。block scalar内のcommand位置の呼び出しは認める | SCN-INT-DISTGATE-007〜009、011、012、`releaseRunSteps` |
| QLT-DISTGATE-004 失敗が伝播する経路の要求 | 検証の呼び出しは失敗が伝播する区間になければならない。`true ||`で実行されない呼び出しと`|| true`で失敗を握り潰す呼び出しを数えない | SCN-INT-DISTGATE-017、018、`reliableSegments` |
| QLT-DISTGATE-005 公開との前後関係 | `npm publish`がある場合、配布前品質検証はそれより前の有効なstepで実行されなければならない | SCN-INT-DISTGATE-019、020、`checkDistributionGateReachability` |
| QLT-DISTGATE-002 判定不能の拒否 | package.json・release.ymlの欠落、scriptsの不正、準備工程の欠落、既知でない準備工程の形をすべて拒否し、合格へ倒さない。未知の形を全gate形として扱わない | SCN-INT-DISTGATE-005、006、015、016、`checkDistributionGateReachability` |
| QLT-LIFEIGNORE-003 判定不能の拒否 | 問い合わせの失敗、追跡fileの列挙の失敗、解決されたrepository rootの不一致を拒否する。版管理への問い合わせはrepositoryとindexの選択へ影響する環境変数を除去した環境で行う | SCN-INT-LIFEIGNORE-014〜017、`isolatedGit` |
| QLT-AUDSEL-002 SHA整合性 | commit構造から先に`H_impl`を推定して循環を避け、選択したreview artifact本文の完全SHAと一致することを二重確認する。`比較基点`も同じ二重確認の対象とし、境界commitが親をちょうど2個持つとき、その第1親と`H_impl`の一意な`merge-base`を導出値として完全一致を要求する。導出は3状態に分ける。親1個は対象外として申告値を使う。親が2個以上で導出できない場合（親3個以上、浅いcloneでfork点が取得範囲外、`merge-base`が複数）は**判定不能として拒否し、合格へ倒さない**。親が2個でないmergeを拒否する扱いはQLT-MERGEINT-003と揃える。一意なら完全一致を要求する。不一致では双方のSHA、判定不能では境界commitの第1親のSHAと親の個数、いずれも修正方法を日本語で示して拒否する | SCN-UNIT-AUDITSEL-007、SCN-UNIT-AUDITSEL-009〜012、SCN-INT-AUDITSEL-005〜006、`inferReviewBoundary`、`uniqueMergeBase` |
| QLT-AUDSEL-003 診断可能性 | 差分0件、複数件、`docs/reviews/`外を区別し、0件ではartifactだけの後続commitを要求し、複数件では一意なartifact候補を除く余分なpathを全件、候補を一意化できなければ差分pathを全件、配下外では該当pathを示す。NFR-873のrelease bump 3条件による除外は維持する | SCN-UNIT-AUDITSEL-003〜006、SCN-INT-AUDITSEL-003、SCN-UNIT-AUDITBUMP-001〜004、SCN-INT-AUDITBUMP-001〜004 |
| QLT-WTDOC-001 正本の単一性 | 配布文書のworktree作成契約は`src/domain/worktree.ts`の定数から生成する。人が同じ事実を二度書かない。検査は生成区画が正本の描画結果と一致することと、正本の書式をruntimeが受理することを見る | SCN-UNIT-WTDOC-001〜007、`check_worktree_contract.ts` |
| QLT-DISTIMPACT-001 配布境界の可視性 | 配布境界は`package.json`の`files`から算出し、`bin/`と`src/`のcompile先も含める。review artifactの配布物影響の節へ、配布境界へ入る変更pathの全件と判断と根拠を要求し、欠落を`audit:check`が拒否する | SCN-UNIT-DISTIMPACT-001〜014、`check_file_audit.ts`。節の判定は行頭一致で行い番号付き見出しも受理する。`判断:`と`根拠:`はそれぞれ1件だけを要求し、許可外の判断値とplaceholderの根拠を拒否する |
| QLT-PROPFIELD-001 正本訂正可能性 | 登録済みproposalの契約fieldは不変で削除も拒否する。`owner`・`rationale`・`rollback`は値の更新だけを許可し、削除と空文字列化と12文字未満はregistry schemaが拒否する。訂正できない正本を残さない | SCN-UNIT-PROPFIELD-001〜008、`check_project_quality.ts` |
| QLT-TESTDET-001 test決定性 | `test/steps/`はTypeScript ASTで走査し、実時計・乱数の直接使用と、`package.json`・`package-lock.json`・`.agent-skill-chain/project/`への相対path accessを拒否する。dogfoodingに必要なaccessは理由付きの宣言だけを許可し、未使用の宣言も拒否する。検査は`test:format`から実行しCIとprepackの連鎖へ入る | SCN-UNIT-TESTDET-001〜012、`check_test_determinism.ts` |
| QLT-LOCKPROT-001 保護境界の目的整合 | trusted品質契約の保護snapshotは`package-lock.json`最上位の`version`と`packages`の空keyのentryの`version`だけを除いて算出する。`lockfileVersion`、dependencyのversion、`resolved`、`integrity`、dependencyの追加・削除は除かない。自動release bumpだけを原因とする差分でcandidateを拒否しない | SCN-UNIT-LOCKPROT-001〜008、`check_project_quality.ts` |
| QLT-RVX-001 例外の可視性 | 独立reviewが成立しない場合の扱いをrisk比例で定める。門は、止めても復旧可能かつ不可逆な行為を防げる位置にだけ置く。実装・PR・mergeと自動発火するtag・Releaseは記録があれば進み、外部への不可逆な配布は外部証拠を要求するが例外で覆える。ただし不可逆な配布を含む宣言は期限付きの失効日時を必須とする。破壊的操作は既存authority経路で守る。例外は正本fileで管理し、失効、未知field、必須field欠落、許可外種別、識別子重複を拒否する | SCN-UNIT-RVX-001〜013、`src/domain/conformance.ts` |
| QLT-REQID-001 体系の自己一致 | 配布templateの要件ID・受け入れ条件IDを、製品自身が使う`REQ-{domain}-{3桁}`・`AC-{domain}-{3桁}`と同じ体系へ揃える。旧体系の`FR-`・`NFR-`とdomainのない`AC-<連番>`を拒否し、domainの決め方の記述欠落も拒否する | SCN-UNIT-REQID-001〜007、`check_requirement_id_scheme.ts` |
| QLT-CLIDOC-001 正本の単一性 | 配布文書のCLI案内は`src/cli-usage.ts`のusage正本から生成する。人が同じ事実を二度書かない。検査は生成区画が正本の描画結果と一致することだけを見る | SCN-UNIT-CLIDOC-001〜006、`check_cli_usage.ts`、`generate_cli_usage_guide.ts` |
| QLT-STEP-001 正本整合性 | 規範文書のStep表4列とmode別Step列がコード正本と完全一致し、片側だけの変更を許可しない | `npm run workflow:check`、build、prepack、SCN-INT-WFSTEP-006〜008 |
| QLT-STEP-002 決定性 | journal順序は行順だけで判定し、domain内で現在時刻・乱数を取得しない | SCN-UNIT-WFJRNL-012、typecheck、source review |
| QLT-STEP-003 監査可能性 | Stepごとに1件以上のartifactと空でないevidenceを追記し、重複再実行は最後の記録を採用する | workflow journal schema、SCN-UNIT-WFJRNL-006・013、SCN-INT-WFSTEP-001 |
| QLT-GR-001 決定性 | 同じ意味sourceからcanonical node・edge順とgraph content hashが一致し、BFS・Kahn・Tarjan・Dijkstraの同距離結果が挿入順に依存しない | REQ-GR-003、007〜009のproperty・differential test |
| QLT-GR-002 可用性・復旧 | Graph runtimeはworktreeごとに隔離し、missing・corrupt・drift時もGit・text・provider耐久stateを変えず完全rebuildで収束する | 隔離疑似projectのworktree A/B・破損・source変更復旧scenario |
| QLT-GR-003 性能・安全性 | 全探索はdepth、node、edge、result、operationのhard budgetを持ち、上限時はcanonical部分結果を`budget-exceeded`として返す。GraphQLiteは固定digest assetとparameterized queryだけを使う | 上限・deep cycle・negative weight・query injection・digest不一致反例 |
| QLT-GR-004 観測可能性 | Full・Quick・PoCの別scenarioと通常・境界・失敗・復旧を隔離疑似projectで1回のsuiteで観測し、実日数の経過を完了条件にしない | OBS-GR-001〜018のBDD、固定instant・provider response・extension fixture |

性能の絶対時間SLOは、対話型のローカルCLIで外部APIと利用project規模に左右されるためv0.3では定めない。Graphの資源制御は経過時間ではなくhard budgetの決定論的counterで検証し、CIは15分でtimeoutする。

Targeted検証は差分・risk・証拠fingerprintが同一の成功だけをdeduplicateする。最終gateは常にsecurity、受け入れ条件、独立review、full testを実行する。

## project rule coverageとorphan拒否

conformance検査は`.agent-skill-chain/project-policy.json`の`ruleFiles`とrules directoryを完全一致させ、各ruleの安定ID、目的、所有者、scope、enforcement、証拠を検証する。拡張メタデータを持つruleは`packageDefault`、`projectOverride`、`changeAuthority`の3fieldを同時に持つ。

rule coverage matrixはrule IDごとに規範文書、schema、runtime、CIへの出現を真偽値で保持する。規範は3正本、schemaは`.agent-skill-chain/schemas/`、runtimeは`src/`、CIは`.github/workflows/`を走査する。次をorphanとしてfail closedで拒否する。

- rule fileに定義済みだがruntimeにもCIにもenforcement pointがない。
- 規範文書にIDがあるがproject policyへ定義されていない。
- CIだけにIDがある暗黙ruleである。
- schemaにIDがあるがruntimeで検証されていない。

同じ検査で品質CIの`pull_request`単独triggerとread-only permission、npm以外のlockfile不在、`package-lock.json`と`npm ci`、package engineとCI Node.jsの互換性、必須品質command、trusted default branch policy、npm配布境界を検証する。candidate policyはPR base SHAのvalidatorで評価し、同じPRがmerge・review・release条件を緩和して自己承認することを許可しない。
