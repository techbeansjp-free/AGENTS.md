# 品質要件

| 品質特性 | 合格基準 | 確認方法 |
|---|---|---|
| 安全性 | 不明なmode、権限、remote、証拠、最新状態を拒否する | 悪用・失敗Gherkin |
| 原子性 | 失敗時に既存内容と正本を変更せず、partial状態を公開しない | collision・途中失敗テスト |
| 可読性 | 人が読む文書と全テスト起票を日本語にする | 日本語文書・Gherkin形式検査 |
| 追跡性 | 要件、SCN ID、テスト結果、実装、HEAD SHAを対応付ける | 追跡検証とPR証拠 |
| 移植性 | Node.js 20以上でESMとして動作する | typecheck、build、CI |
| 配布品質 | 開発専用資産と`memo/`をnpm packageへ含めない | 実際の`npm pack`内容検査 |
| NFR-879-01 配布判定整合性 | 自動release要否は手書きの変更path集合ではなく、`npm pack`対象fileのpathと正規化内容から算出した64桁小文字hexの配布digestを現在の`v${package.json.version}` tagから再計算した値と比較する。package metadataのversionだけは除外し、他fieldと全配布fileの変化を保持する | SCN-UNIT-DIGEST-001〜010、SCN-UNIT-AUTOREL-D01〜D08、SCN-INT-DIGEST-001〜006 |
| 復旧性 | worktree完了前に到達可能な上流branchを保持する | finalize結合テスト |
| 開発速度 | local feedbackとPR gateのbudgetをproject policyで設定可能 | gateWaitMsとbudget超過診断 |
| policy品質 | 重複、誤block、override、rollback、検出漏れを観測可能 | metrics集計とdogfood E2E |
| 型安全性 | 製品実装はTypeScript strictで検査し、明示・暗黙の`any`を許可しない | `npm run lint`、`npm run typecheck`、source quality反例 |
| 形式品質 | ソース・JSON・設定を非破壊的な整形検査で固定し、書き込み整形は明示操作に分離する | `npm run format:check`、`npm run format:write` |
| 入力安全性 | 外部から取り込むJSON・policy・CLI入力は`unknown`から開始し、policy、manifest、state、evidence等の入力種別ごとに実行時検証してから型を確定する。未知field・未知version・型不正を黙殺しない | schema、入力種別別validator、SCN-UNIT-QUALITY-010 |
| 品質契約更新性 | candidateによる品質gateの自己緩和を拒否しながら、baseへ事前登録した版付きproposalとbyte/hashが完全一致する強化だけを次PRで有効化できる | `qualityContractVersion`、trusted proposal registry、SCN-UNIT-QUALITY-006、008〜009 |
| Privacy/Security | 秘密・個人情報・識別子の収集を最小化し、診断・ログ・例外への混入を防ぐ | redaction・悪用BDD、review |
| 運用可能性 | ログ・metrics・traceの目的、アクセス、改ざん防止、保持、rotation/容量上限、削除、監視、alertを境界ごとに設計する | 運用設計、失敗BDD、review |
| UI/UX契約 | UIを追加する場合は利用者中心設計、アクセシビリティ、Design/Layout Tokenを必須とする。現在のCLIは理由・証拠付きで非適用 | project choice、開発契約の適用判断 |
| NFR-836-01 安全性 | routing evidenceと診断に保存許可list外のfieldが0件であり、providerのstderr本文をログ、診断、例外、evidenceへ転記しない | SCN-INT-ROUTING-004、SCN-UNIT-ROUTING-012 |
| NFR-836-02 性能 | 実装開始前の外部観測をavailability観測と実行直前の再検証の合計2回以内にする | SCN-UNIT-ROUTING-009の観測回数検査 |
| NFR-836-03 可用性・復旧 | Codex停止または推奨model解決不能時は、Codexの値を推測せずproject choiceのClaude fallbackへ切り替え、route mode、選択元、理由、実装identityを証拠化する | SCN-E2E-ROUTING-001、SCN-UNIT-ROUTING-007・011 |
| NFR-836-04 互換性 | `modelMapping`がlegacy文字列または未設定の既存projectは読み込みに成功し、構造化設定を有効化するまでroutingを開始しない | SCN-INT-ROUTING-006の既存fixture回帰とmainのtrusted validatorによるcandidate検証 |
| NFR-836-05 保守性 | 汎用packageとproject mappingに個別model slugを保持せず、Codex公式recommended defaultへ実行時追従する | SCN-UNIT-ROUTING-002、SCN-INT-ROUTING-002・005、conformance検査 |
| NFR-836-06 利用性・アクセシビリティ | routing診断はrule ID、目的、リスク、根拠、次の操作、必要authority、rollbackの7項目を空値なく持つ。画面部品を持たないため支援技術と視覚要素の基準は対象外 | SCN-UNIT-ROUTING-010 |
| NFR-856-01 安全性 | unsafe、unknown、未承認でworktree削除0件、対象外worktree・branch・Git metadata変更0件とする | SCN-UNIT-WTCLEAN-001〜005、SCN-INT-WTCLEAN-002・004、SCN-E2E-WTCLEAN-003 |
| NFR-856-02 性能 | 対象PRの登録済みworktreeとrepository直下`.worktrees/`だけを判定し、cleanup専用hygieneではfilesystem全体を再帰走査しない | operation監査、SCN-INT-WTCLEAN-007 |
| NFR-856-03 可用性・復旧 | phase別結果を返し、root更新後のcleanup失敗を部分完了として保持し、同じ入力の再実行で収束できる | SCN-UNIT-WTCLEAN-004、SCN-E2E-WTCLEAN-006 |
| NFR-856-04 互換性 | 既存finalize、root更新、worktree配置、workspace hygieneの公開契約とscenarioを維持する | 既存SCN-INT-FINALIZE、SCN-ROOTUPD、SCN-HYGIENE回帰 |
| NFR-856-05 保守性 | 完了計画、operation authority、既存適用関数、事後要約を分離した純関数とCLI合成にする | architecture check、`planCompletion`、`summarizeCompletion` |
| NFR-856-06 利用性・アクセシビリティ | CLI JSONはphase、state、理由、必要authority、実在する次操作を日本語で返す。GUIを持たないため視覚・支援技術基準は対象外 | SCN-UNIT-WTCLEAN-002〜004、SCN-E2E-WTCLEAN-003・006 |
| NFR-860-01 安全性 | 未同期、不明、未承認、状態変化、root外、symlinkではstaging削除0件とする | SCN-UNIT-STAGING-001、005〜006、SCN-E2E-STAGING-002 |
| NFR-860-02 性能 | repository内`.agent-skill-chain/tmp/issues/`の既知inventoryだけを有限走査し、repository外を探索しない | operation監査、SCN-UNIT-STAGING-006 |
| NFR-860-03 可用性・復旧 | path別の削除・保持理由を返し、途中失敗をpartially-completedとして新previewから再実行できる | SCN-INT-STAGING-001〜002 |
| NFR-860-04 互換性 | lifecycle、workspace hygiene、Issue stagingの既存scenarioを維持し、一般hygieneはconsumer stagingを保持する | SCN-UNIT-STAGING-006、既存lifecycle・hygiene回帰 |
| NFR-860-05 保守性 | 状態検査、cleanup計画、authority再検証、物理削除callbackを分離する | architecture check、`inspectStaging`、`planStagingCleanup`、`applyStagingCleanup` |
| NFR-860-06 利用性・アクセシビリティ | JSONと日本語診断へ対象、理由、次操作、必要authority、rollbackを含める。GUIを持たないため視覚・支援技術基準は対象外 | SCN-E2E-STAGING-001〜002 |
| NFR-873-01 監査整合性 | `H_impl..current`はcommitごとに導入差分を検査し、subjectのversionが`isPackageVersion`を満たすrelease bumpで、変更pathが`package.json`と`package-lock.json`だけ、かつ`package.json`の`version`以外を`stableJson`で比較して一致する場合に限り、その変更を監査対象path集合から除外する。接尾辞は許可し、3条件の反例と同じpathへ通常commitも触れた場合は除外しない | SCN-UNIT-AUDITBUMP-001〜004、SCN-INT-AUDITBUMP-001〜002、`check_file_audit.ts` |
| NFR-873-02 merge互換性 | release PRのmerge commitはfirst-parentに対して導入した変更を検査し、別親側の固有commitがすべて3条件を満たすrelease bump commitで、merge自身の導入差分もfile条件と`version`限定条件を満たす場合だけ除外する | 実main履歴のPR #872、SCN-INT-AUDITBUMP-001〜002 |
| NFR-873-03 既存契約維持 | 比較基点と`H_impl`の完全SHA解決、baseから`H_impl`までのpath集合と個別監査表の完全一致、baseと`H_impl`および`H_impl`とcurrentのancestor検査、全個別判定`pass`を維持する | 既存SCN-UNIT-PACKAGE-011、`audit:check`、SCN-UNIT-AUDITBUMP-002〜004 |

性能の数値SLOは、対話型のローカルCLIで外部API時間が支配的なためv0.3では定めない。CIは15分でtimeoutする。

Targeted検証は差分・risk・証拠fingerprintが同一の成功だけをdeduplicateする。最終gateは常にsecurity、受け入れ条件、独立review、full testを実行する。

## project rule coverageとorphan拒否

conformance検査は`.agent-skill-chain/project-policy.json`の`ruleFiles`とrules directoryを完全一致させ、各ruleの安定ID、目的、所有者、scope、enforcement、証拠を検証する。拡張メタデータを持つruleは`packageDefault`、`projectOverride`、`changeAuthority`の3fieldを同時に持つ。

rule coverage matrixはrule IDごとに規範文書、schema、runtime、CIへの出現を真偽値で保持する。規範は3正本、schemaは`.agent-skill-chain/schemas/`、runtimeは`src/`、CIは`.github/workflows/`を走査する。次をorphanとしてfail closedで拒否する。

- rule fileに定義済みだがruntimeにもCIにもenforcement pointがない。
- 規範文書にIDがあるがproject policyへ定義されていない。
- CIだけにIDがある暗黙ruleである。
- schemaにIDがあるがruntimeで検証されていない。

同じ検査で品質CIの`pull_request`単独triggerとread-only permission、npm以外のlockfile不在、`package-lock.json`と`npm ci`、package engineとCI Node.jsの互換性、必須品質command、trusted default branch policy、npm配布境界を検証する。candidate policyはPR base SHAのvalidatorで評価し、同じPRがmerge・review・release条件を緩和して自己承認することを許可しない。
