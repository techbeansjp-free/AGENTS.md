# Issue #824 要件

公開GitHub Issue #824を要求・要件・設計・実装計画の唯一の正本とする。T01〜T14および受け入れ条件を実装対象とし、旧local staging、旧skill、旧harnessを実行規約にしない。

## 主要な受け入れ条件

- Q-01〜Q-08をすべて根拠付きで満たす場合だけquickとし、それ以外はfullへ単調昇格する。
- fullはStep 0〜11、quickは`0 → 1 → 4 → 9 → 10 → 11`を通る。
- 仕様、単体・結合・E2Eの日本語Gherkin、実装、レビュー結果を追跡する。
- PR自身によるpolicy緩和と自己承認を拒否する。
- merge、Issue close、branch削除、release、cleanupを別権限へ分離する。
- 今回の実行はPR作成後に停止し、branchとworktreeを保持する。

## 制約

新規Issue作成、Issue #824のclose、PR merge、branch/worktree削除、release、package公開を行わない。`memo/`はGit管理・配布対象外とする。

## 実装中に確定した配置判断

Issue本文のT02はrootの`POLICY.md / WORKFLOW.md / QUALITY.md`を記載していたが、実装確認中の利用者指示により、rootは短い`AGENTS.md`だけとし、3つの規範文書を`.agent-skill-chain/docs/`へ配置する。この変更は規範を3文書以内に保ちながら、package名前空間と所有権を明確にする。
