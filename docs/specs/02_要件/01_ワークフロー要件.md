# ワークフロー要件

#### REQ-WF-011 モード判定Q-01〜Q-08の質問文を正本へ定義し、quick失格分類と1対1で対応させる

モード判定はワークフロー全体の分岐点であり、`full`と`quick`ではStep数が12対6で倍違う。その分岐はQ-01〜Q-08への回答で決まる。**しかし各質問が何を問うているかは、どこにも書かれていなかった。** 存在したのは表の行ラベルと見出しの文字列検証だけである。記入者は昇格条件の分類から質問文を推測して埋めており、推測が実施者ごとに異なれば同じ変更が別のモードになる。

昇格条件は7分類として列挙されていた。Qは8件である。**数が合わない原因は、文書の「依存関係・実行環境・基盤」が実装では`dependency`と`infrastructure`の2分類だったことである。** 分けると8対8で一致する。

質問文は、人が読む規範文書と機械可読な定義の2箇所に同一内容で置き、`(質問ID, 分類, 質問文)`の順序付き8行としての完全一致を強制する。**これは独立した複製ではなく、機械的に一致を強制される写しである。** 機械可読側だけを書き換えられると、人が読む規範文書に差分が現れないままowner承認を迂回できる。

質問の向きは「真＝`quick`を選べる」で統一する。**この性質そのものは機械が判定できない。** 全件が真を宣言するだけの構造検査は常に成立し、何も検出しない。代わりに、向きの変更が必ず規範文書の差分として現れるようにし、package ownerの承認とreviewの判断へ結び付ける。

1つの変更が複数の分類へ該当することは正常であり、その場合は該当するすべての質問を偽とする。どれか1つを選ばせない。

- 受け入れ条件: AC-WF-011
- 根拠: Issue #957
- 実装: `src/domain/mode.ts`、`scripts/check_conformance.ts`、`.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/templates/issue/00_要求定義_quick.md`、`.agent-skill-chain/templates/issue/00_要求定義_full.md`

## REQ-WF-001 モードを安全側に判定する

Q-01〜Q-08を全件根拠付きで満たす場合だけquickとする。不明、根拠なし、公開契約・data・security・依存・不可逆操作への影響があればfullへ単調昇格する。明示PoCは専用宣言とhigh risk不在を必要とする。

- 受け入れ条件: AC-WF-001
- 根拠: Issue #824、#827
- 実装: `src/domain/mode.ts`

### REQ-WF-002 Issue計画を原子的に同期する

Issue stagingを隣接一時領域で完成後に公開し、成果物inventoryとdigestを保持する。directory prefixの`YYYYMMDD_HHMMSS`は渡された作成instantを実行環境によらず日本標準時（Asia/Tokyo、UTC+09:00）で表す。quick・pocはStep 4、fullはStep 8の耐久トラッカー書込後再読取一致だけを`sync-verified`とする。

- 受け入れ条件: AC-WF-002
- 根拠: Issue #824、#860
- 実装: `src/domain/issue.ts`

### REQ-WF-003 PoCを正式開発から隔離する

PoCは目的、repository内で実環境・実data・外部副作用から隔離した疑似project fixture、初期化Evidence、runner ID・安全な相対path・固定argv、1件以上のuse case、各use caseに属するGiven・When・Thenを持つBDD scenario、各scenarioの`exit-code=0`と1件以上のbehavior observable、成功・中止条件、非対象、data・security制約、責任者、昇格・廃止条件を必須とする。ID重複、参照切れ、未知field、安全でない相対path、不完全な宣言はpocとして受理しない。要求時点では未実装fixture/runnerのdigestを要求せず、観測期間の経過を完了条件にしない。

ASCは検証対象HEADに追跡済みでdirty・untrackedでないfixtureをHEAD blobから一時copyへ復元し、fixture内の固定Node runnerを宣言argvで実行する。任意shell・任意command・caller作成Evidenceは受理しない。Linuxの固定Git、bubblewrap、prlimit、timeout、出力上限を主隔離境界とし、Node Permission Modelは補助防御に限る。EvidenceはPoC宣言SHA-256、ASC実測fixture/runner SHA-256、scenarioごとの正常終了、ISO 8601 UTC観測時刻、exact HEAD、宣言順の全observableについて型別exact比較した期待値・実値・合否、各result/execution digest、全体digestを必須とする。`workflow poc-observation --staging ... --apply`が`poc-observations/<headSha>.json`をHEAD別append-onlyで固定し、Step 9以降のjournal、verify、PR作成で同じheadSha/evidenceDigest bindingを再検証する。review修正後は新HEAD世代を追加して旧世代を残し、Step 11後の追加を拒否する。baseline..HEADとprovider観測base..HEADの実Git差分がfixture root内の通常fileだけでなければfail-closedとする。

PoCはrelease・自動merge・本番cleanupを拒否する。正式化では不足する01要件、02設計、03計画を補完し、fullからpocへ降格しない。

- 受け入れ条件: AC-WF-003
- 根拠: Issue #827
- 実装: `src/domain/mode.ts`、`src/domain/poc-observation.ts`、`src/domain/issue.ts`、`src/adapters/workflow-journal.ts`、`src/cli.ts`、`.agent-skill-chain/schemas/workflow-mode-decision.schema.json`、`.agent-skill-chain/schemas/poc-observation.schema.json`

### REQ-WF-004 Step順序とjournalを機械検証する

Step 0〜11、mode別列、staging成果物path、共通staging検査を`src/domain/workflow.ts`の単一正本で定義する。journalは行順、必須成果物、evidence、mode単調昇格を検証し、PR作成前にStep 4・10・同期状態を含むStep 10までを要求する。モード判定成果物は`changedFiles`を保存し、再分類でも同じhigh risk判定を再現する。欠落overrideはIssue・scope・主体・理由・期限へ拘束し、承認元をcandidate管理下から分離する。**journal追記経路もStep順序判定と同じ条件で通す。**Step 11記録後の追記とterminal delivery state後のStep 0〜10は、post-terminal intakeのStep 10だけを受理し、それ以外は拒否する。公開CLIは`--post-terminal-intake`を値を取らないflagとして受理する。

- 受け入れ条件: AC-WF-004
- 根拠: Issue #877、#1194
- 実装: `src/domain/workflow.ts`

### REQ-WF-005 reviewと証拠を独立させる

実装前のStep 3・7は開始不能な契約穴を一度確認するreadiness checkとし、exact-head品質reviewや最大3ラウンド契約を適用しない。実装後のStep 10だけが肯定・敵対の全観点を最大3ラウンドで評価する。収束後にHEADが変われば同sessionの次roundで実Git fixed diffを再reviewし、同じHEAD、空fixed diff、reset、anchor変更、budget終了後を拒否する。**予算3ラウンドとは別枠で、収束後のHEAD移動に対する取り直しをround 4として1回だけ受理する。** round 4は直前sessionが`converged`である場合に限り、それ以外は拒否する。未解決blockerを抱えて予算を使い切った`budget-exhausted`からは開かない。round 4で未解決が残れば`budget-exhausted`とし、round 5以降を拒否する。**`pr create`がStep 11を記録した後も、条件を満たせば外部reviewerの指摘を同じPRへ取り込む。** 守る性質は「独立reviewerが確認した内容とmergeされる内容が一致すること」であり、HEADが動いたときに同sessionの次roundで再reviewすれば保たれる。取り込みは、前進commitでの是正、既存の有限レビュー予算内での次round収束、review artifactの`H_impl`更新、`workflow record --step=10 --post-terminal-intake`によるStep 10再記録、reviewスレッドへの判定返信をすべて満たす場合に限る。**この記録はStep 11より後にだけ置け、Step 10にだけ許し、順序判定から外す。記録自体は残す。** 予算超過、受け入れ条件の不充足、安全境界・authority・不可逆操作へ及ぶ指摘は、判定と分離先をreviewスレッドへ返信したうえでfollow-up Issueの新しいstagingで完全な工程を通す。Step 10 journalはsession ID、latest round digest、exact HEADを必須bindingとし、HumanOverrideで代替しない。PR作成はprovider副作用前とdispatch直前にそのbindingを保存sessionと再照合する。製品・仕様・testを固定した`H_impl`とreview artifactだけを加えた`H_final`を分離し、実装者・PR authorから独立した同一HEADのapprovalと成功CIだけを承認証拠とする。既定branchが動いてrebaseしHEADの系譜が置き換わった場合は、被レビュー内容が変わっていないことを旧baseから旧headまでの完全diff digestとpath集合の一致で実証したうえで、append-onlyの再固定記録を積んで照合対象を新headへ移す。記録は既存のanchor、round履歴、PR bindingを書き換えない。実効HEADはchainの終端とし、chainが空なら固定済み記録headとする。連鎖条件を満たさない記録は導出に使わない。`pr create`前は`review reanchor`、`pr create`後は`pr reanchor`が受け持ち、境界の逆は拒否する。収束していないsessionの再固定も拒否する。

- 受け入れ条件: AC-WF-005
- 根拠: Issue #824、#834
- 実装: `src/domain/review.ts`

### REQ-WF-012 実装中の発見を前向きに処理する

発見入力は`DISC-`で始まる安定`discoveryId`を必須とし、判定結果の7記録fieldへ同じIDを引き継ぐ。`workflow promote-full`は無指定でpreviewだけを返し、`--apply`時にだけ永続成果物を更新する。delivery stateが存在するstagingは昇格を拒否する。同期trackerはabsolute GitHub Issue URLに限り、短縮Issue番号を永続化しない。

実装中に問題や新しい事実を発見しても、要求から全工程を自動的に再起動しない。目的、scope、受け入れ条件、security境界、不可逆操作の契約を変えない発見は、`発見ID / 事実 / 影響 / 判断 / 対処 / 検証 / 仕様更新`をfullでは03実装計画、quickとpocでは集約00へ記録して実装を継続する。

**開始時に承認したcanonical Issueの目的にASC本体の保守を含まない作業では、発見したASC本体の欠陥を当該scopeへ追加して是正しない。例外を設けない。** ASC本体とは配布される規範文書、schema、CLI、検査、template、project policy機構であり、利用projectがASCを用いて作る成果物を含まない。scope拡張、candidateによる自己許可、別roleへの付け替えを例外として認めない。**目的にASC本体の保守を含むIssueは対象外であり、例外ではない。** 是正は別Issueとして別scope・別履歴・owner決裁のもとで開始する。**作業の成果物をASCの契約へ合わせることは従来どおり必須であり、この禁止に含まれない。ただし検査を通すための変更が、検査が無ければ弁護できない弱化・観測量の置き換え・実質を持たない充足になる場合は成果物側の是正ではない。** 作業が進まない場合は停止、記録、別Issueへの分離、owner決裁の順に扱い、未許可操作や未許可のsource編集で回避しない。owner決裁が扱うのは分離Issueの承認・延期・却下、修正版の反映後の元作業再開、元作業の中止であり、元Issueのscope内でASC本体を直す許可を含まない。**作業を止めない軽微な矛盾は記録と別Issueの起票だけを行って継続する。軽微かどうかは判断ではなく3条件で決める。** (1)その欠陥を是正せずに当該作業の受け入れ条件をすべて充足でき、(2)充足のために成果物を歪めず、(3)安全条件・authority分離・fail-closed不変条件のいずれにも触れない。**1つでも偽または不明なら軽微としない。** 判定は当該作業を実施するroleが行い根拠を記録へ残す。 **ASCへの改善提案と別Issueの起票は禁止しない。** 強制はreviewが担い、非保守IssueのPR差分にASC本体のpathが現れた場合にreviewerがfindingとする。**機械による自動照合は存在しない。**

契約を変える発見だけ、モードに応じて影響する成果物を再確定する。`full`では目的またはscopeの変更時に00と影響する01〜03、AC、domain invariant、requirement、interface、data、design responsibility、non-functional、operations、security境界、不可逆操作の変更時に01〜03を対象とする。`quick`の通常の契約変更は集約成果物00だけを再確定し、security境界の拡大、不可逆操作、または根拠付きモード失格条件の発見時は同じIssue stagingを`full`へ単調昇格して00〜03を補完する。`poc`の通常の学習契約変更は集約成果物00だけを再確定し、同じ失格条件の発見時は停止するか明示操作で`full`へ単調昇格し、PoCのまま続行しない。モード失格条件と契約種別はcanonical IDに限定し、空値、重複、未知ID・未知fieldを拒否する。

この判定は、現在モード、契約変更フラグ、`changedContractKinds`、根拠付きモード失格条件を構造化入力とする`workflow assess-discovery --input=<JSON>`の決定論的出力によって確定する。判定はpureとし、quickの`promote-to-full`またはPoCの`stop-or-promote-full`から昇格を選んだ場合だけ`workflow promote-full`が、同じtracker・journalを維持して正規モード判定、昇格前00・モード判定、発見Evidence、full用00〜03を永続化する。正規判定は元の`requestedMode`を保持し、発見に対応するQ回答またはPoC high-risk項目をEvidence付きで更新してfullを再現しなければならない。昇格前に全成果物の通常file・包含・一覧・digestを検証し、staging単位の排他lockと永続transaction markerを使用する。全成果物とmarker除外済み最終recordをfile・directory単位で耐久化した後にだけmarkerを削除する。process停止後の再実行はmarkerが残る未commit状態をrollbackして再適用し、marker削除済みなら永続した発見・判定backup・成果物一覧・digestと再実行入力が一致する場合だけ同じ結果へ収束する。Step 4同期済みなら`promotion-active`としてStep 8再同期までPRを拒否する。昇格後のStep 4と8は、昇格前に記録したabsolute GitHub Issue URLとrepository・Issue番号が完全一致する元Issueにだけ許可し、別IssueはGitHub副作用前に拒否する。Step 4は検証専用でstaging記録を更新せず、Step 8だけを最終同期Evidenceとする。未同期なら`local-active`を維持する。昇格前のStep 0・1だけを継承し、fullのStep 2〜10を順に補完する。Issueの再起票、変更のない上流成果物、完了済み調査をやり直さない。Step 9で成立状態を`docs/specs/`へ反映し、Step 10は実装中発見記録、コード、test、システム仕様書が一致するexact-headをreviewする。

- 受け入れ条件: AC-WF-012
- 根拠: ASC一部刷新
- 実装: `src/domain/agile-verification.ts`、`src/domain/staging.ts`、`src/adapters/workflow-journal.ts`、`src/cli.ts`、`.agent-skill-chain/templates/issue/00_要求定義_quick.md`、`.agent-skill-chain/templates/issue/00_要求定義_poc.md`、`.agent-skill-chain/templates/issue/03_実装計画.md`、`.agent-skill-chain/templates/issue/04_レビュー.md`

### REQ-WF-013 automatic deliveryをPRからmergeまで継続する

Step 11はdelivery終端専用とし、`workflow record --step=11`を拒否する。createとmergeのprovider call直前に永続dispatch claimをfsyncし、claim消費後は自動再送しない。PR作成後binding前のbase前進は既存PR照合を妨げず、一致するclosed・merged PRがあれば重複作成を拒否する。

merge認可はreview artifact直前の`H_impl`、artifactだけを加えた`H_final`、artifact path/digest、PRに直接関連する成功CI run ID、`H_final`への独立approval IDを不変intentへ含める。rulesetはPR・status checks・署名・non-fast-forward・linear historyの保護型だけをprotection Evidenceにし、deletionだけのrulesetをprotectedと判定しない。

Step 11 journal後のcrash復旧は保存済みartifact inventoryとcontent digestを復旧直前に再照合し、不一致ならstateを書き変えない。

`delivery.stopAt=pull_request`は`pr create`がmergeを同じ外部操作へ暗黙連結しないコマンド境界とし、ASC全体の正常終了をPRへ固定しない。PoCと`merge.mode=disabled`はPRで停止する。`assisted`は対象PR authorityがなければ停止して再開条件を返す。full・quickの`automatic`では、適用された`pr create`はPR URLと`merge_pending`を返し、Step 11をまだ記録しない。

`pr create`はprovider副作用より前にwrite authority、providerが観測したdefault branchとtip、remote head SHAを取得し、指定repository・base、trusted policy commit、exact HEADと一致させる。その後、repository、canonical IssueとURL、head ref/SHA、base refと作成時base SHA、canonical title/body digest、closing契約digestを同じIssue stagingへ耐久化する。作成後read-backでrepository、PR番号・URL、canonical Issue、same-repositoryかつcross-repositoryでないhead repository、head ref/SHA、canonical title/body、base ref、closing Issue 1件を再照合し、以後のdeliveryが使うidentityとして固定する。project外、別Issue、symlink経由のstagingはprovider副作用前に拒否する。作成結果が曖昧な場合はcreateを再送せず、固定identityによるprovider read-backで既存PRを照合する。一度もdispatchしていない`create-prepared`に限り、該当PRが0件であることを決定的に確認した場合だけ同一immutable intentを1回再試行できる。曖昧または複数候補は`reconciliation-required`として停止する。

後続の独立した`pr merge`は`pr create`で検証したものと同じstagingを必須入力とし、Step 0〜10、Step 4・10、`sync-verified`、モード、既定branch上のtrusted policy、required checks、独立review、固定したcurrent HEAD、classic protectionまたはexact branchへ適用されるactive ruleset、mergeable状態を外部操作前と直前に再検証する。`poc`では`pr merge`を物理的に拒否する。base refはPR作成時から固定し、同じbase ref上の通常のbase SHA前進は許容するが、provider default branch tip、PR base SHA、trusted policy commitを一致させる。外部merge要求より前にmethod、認可HEAD、intent IDを耐久化する。merge結果が曖昧な場合はmergeを再送せず、auto-mergeとmerge queueを含むprovider read-backで照合する。一度もdispatchしていない`merge-prepared`に限り、該当auto-mergeとqueue entryがいずれも存在しない決定的Evidenceを得た場合だけ同一immutable intentを1回再試行できる。曖昧または複数候補は`reconciliation-required`として停止する。

native auto-merge要求はmethodとrequestedAt、merge queue登録はentry ID・state・requestedAt・head/base OIDを`merge-observed`の進行状態として耐久化するが、Step 11成功にはしない。同じPRについてproviderからrepository、PR、Issue、HEAD、closing契約、`merged`状態、provider mergedAt、merge commit SHAを再観測してから`outcome=merged`とEvidence IDをStep 11へ記録する。PoCと`disabled`は固定PR bindingのdigestを`outcome=pull-request`とするStep 11終端にする。`assisted`のauthority待ちは終端ではなく`pr-bound`のまま再開条件を返し、Step 11を記録しない。PR停止またはmerged終端ではjournalを先にfsyncし、そのdigestをdelivery stateへ保存する。journal保存後・state保存前の停止は同じcommandの再実行でstateだけを前向きに復旧し、provider副作用とStep 11追記を再送しない。

automaticのPR URL取得だけを正常終了と報告しない。merge authorityはブランチ削除、Issue手動終了、release、公開、完了処理、後片付けへ伝播させない。GitHub側でmerge待機する場合は無期限pollingをせず、merge queue登録を進行状態として耐久化する。

- 受け入れ条件: AC-WF-013
- 根拠: ASC一部刷新
- 実装: `src/domain/agile-verification.ts`、`src/domain/delivery.ts`、`src/domain/delivery-state.ts`、`src/adapters/delivery-state.ts`、`src/cli.ts`、`src/adapters/github.ts`、`src/adapters/workflow-journal.ts`、`.agent-skill-chain/skills/step-11-pr/SKILL.md`

### REQ-WF-006 Gherkinと仕様追跡を検証する

project choiceのdialect・test layer・禁止suffixに従い、全Scenarioへ一意なSCN IDとGiven・When・Thenを要求する。要件からAC、SCN、Feature完全path、実装完全pathへの到達可能性を検証し、孤立と配置外定義を拒否する。

- 受け入れ条件: AC-WF-006
- 根拠: Issue #824、#881
- 実装: `scripts/check_trace.ts`

### REQ-WF-007 role・model・tierと独立性を解決する

6 roleの許可path・操作・禁止操作・必要証拠を検証し、implementerとreviewerのidentity・contextを分離する。risk・mode・scopeから必要tierを単調に決め、Codex high・Claude Opusの自律上限、trusted provider mapping、公式recommended default、実行直前再検証を用いる。解決不能時は値を推測せず安全なfallbackまたは停止を返す。

- 受け入れ条件: AC-WF-007
- 根拠: Issue #830、#836
- 実装: `src/domain/routing.ts`

### REQ-WF-008 conformance適用宣言を検証する

未指定を含む`repository-bound`はI1〜I12を各1件の実在source・enforcement・反例SCNへbindingする。`package-attested`はpackage証拠を再利用するがconsumer適合をclaimしない。理由・証拠のない適用外、空配列、scope格下げ、binding削除を拒否する。**enforcement exportの走査は「実在する」「実在しない」「sourceを解析できず判定できない」の3値で報告する。** 判定不能を「実在しない」と同じ診断へ潰さない。利用者が次に採る操作が異なるためである。実在しないならbindingを直すかexportを実装し、解析できないならsourceの記法を直す。**いずれの場合もerrorを積み、判定不能を合格へ倒さない。**

- 受け入れ条件: AC-WF-008
- 根拠: Issue #834、#837
- 実装: `src/domain/conformance.ts`

### REQ-WF-009 CLIの必須入力とusageを1回の実行で提示する

subcommandごとの要約、必須flag、条件付きflag、任意flagと既定値、実行例を`src/cli-usage.ts`の単一正本で定義する。`--help`は必須flag検証より先に評価してusageを返す。不足している必須flagは1件ずつではなく1回の実行で全件を`reasons`へ列挙する。値をとるflagの空白区切り指定は無言の未指定扱いにせず、`--flag=値`形式を求める専用診断で拒否する。trusted boundary評価は必須flag検証より先に行う。usage正本と`src/cli.ts`の実装が読むflagの一致は`scripts/check_cli_usage.ts`が双方向に検証する。

- 受け入れ条件: AC-WF-009
- 根拠: Issue #886
配布文書は`--help`の存在、値をとるflagが空白区切りを受理しないこと、不足flagを1回で全件返すことを述べる。**同じ事実を二度書かない。** `.agent-skill-chain/00_利用案内.md`とREADMEの該当区画は`scripts/generate_cli_usage_guide.ts`が`src/cli-usage.ts`のusage正本から生成し、`scripts/check_cli_usage.ts`は生成区画が正本の描画結果と一致することだけを検証する。一致しない場合は生成commandの実行を求める。

- 実装: `src/cli-usage.ts`、`src/cli.ts`、`scripts/check_cli_usage.ts`、`scripts/generate_cli_usage_guide.ts`、`.agent-skill-chain/00_利用案内.md`、`README.md`

### REQ-WF-010 正しさと開発速度の両立を命題として保持する

`.agent-skill-chain/docs/00_運用ポリシー.md`の「目的と成立条件」を命題の単一正本とする。正しい成果物をAIで速く作ることを目的とする。ここでいう正しさは、要求を満たし品質、セキュリティ、再利用性、保守性を担保していることであり、検査が緑であることは必要条件であって十分条件ではない。ここでいう速さは開発速度、すなわち要求から成果物を提出するまでに要する時間の短さであり、成果物自体の実行性能を指さない。正しさと開発速度の双方を成立条件として扱う。policy enforcement、gate、reviewは正しさを担保する手段であり、手段が開発速度を損なうときは手段の側を縮小する。

開発速度の観測は、支援層の所要時間が成果物構築の所要時間を上回らないこと、同一scopeのreviewラウンドが有限で予算内に収束すること、手段の追加より既存手段の縮小を先に評価することの3点とする。正しさの観測は`02_品質基準.md`が所有する。

この節は安全条件、authority分離、fail-closed不変条件を緩和せず、開発速度を理由に必要な検証を省略する根拠にしない。成果物の実行性能は非機能要件が所有する。`AGENTS.md`と`01_開発ワークフロー.md`は命題を複製せず参照だけを持つ。

- 受け入れ条件: AC-WF-010
- 根拠: Issue #949
- 実装: `.agent-skill-chain/docs/00_運用ポリシー.md`、`.agent-skill-chain/docs/01_開発ワークフロー.md`、`AGENTS.md`
