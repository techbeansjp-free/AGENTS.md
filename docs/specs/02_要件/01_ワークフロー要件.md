# ワークフロー要件

#### REQ-WF-011 モード判定Q-01〜Q-08の質問文を正本へ定義し、quick失格分類と1対1で対応させる

モード判定はワークフロー全体の分岐点であり、`full`と`quick`ではStep数が12対6で倍違う。その分岐はQ-01〜Q-08への回答で決まる。**しかし各質問が何を問うているかは、どこにも書かれていなかった。** 存在したのは表の行ラベルと見出しの文字列検証だけである。記入者は昇格条件の分類から質問文を推測して埋めており、推測が実施者ごとに異なれば同じ変更が別のモードになる。

昇格条件は7分類として列挙されていた。Qは8件である。**数が合わない原因は、文書の「依存関係・実行環境・基盤」が実装では`dependency`と`infrastructure`の2分類だったことである。** 分けると8対8で一致する。

質問文は、人が読む規範文書と機械可読な定義の2箇所に同一内容で置き、`(質問ID, 分類, 質問文)`の順序付き8行としての完全一致を強制する。**これは独立した複製ではなく、機械的に一致を強制される写しである。** 機械可読側だけを書き換えられると、人が読む規範文書に差分が現れないままowner承認を迂回できる。

質問の向きは「真＝`quick`を選べる」で統一する。**この性質そのものは機械が判定できない。** 全件が真を宣言するだけの構造検査は常に成立し、何も検出しない。代わりに、向きの変更が必ず規範文書の差分として現れるようにし、package ownerの承認とreviewの判断へ結び付ける。

1つの変更が複数の分類へ該当することは正常であり、その場合は該当するすべての質問を偽とする。どれか1つを選ばせない。

**Q-01の対象は外部へ提供するものに限る。** 列挙する「規範文書」に外部提供の限定がかからないと、利用側が自分の`docs/specs/`を規範文書と呼ぶ運用（本packageが推奨する運用そのもの）で、内部文書の更新がそのままQ-01の対象へ読まれる。**規範性は文書の権威を、外部公開性は提供先と契約境界を表しており、別の属性である。** そのため質問文は、列挙した文書等が外部へinterfaceとして提供するものを指すこと、内部の仕様・追跡記録の更新という事実だけでは該当しないこと、**変更fileの所在や配布の有無によらず生成物を含む当該interfaceまたはその外部観測可能な振る舞いを変える場合と、公開境界またはそれらへの影響を確認できない場合はfalseとすること**を述べる。「変更がある」と「変更の有無を確認できない」の双方をfalseへ結ぶため、生成物への影響が既知であることを抜け道にできない。

**この限定は判定の助けであって判定機構ではない。** `classifyMode`のquick経路は回答と根拠だけを読み、変更pathも文書の規範性も配布宣言も見ない。`detectQuickDisqualifiers`はpoc経路でしか呼ばれない。**是正前から内部仕様pathだけの変更で`quick`は到達可能であり、強制していたのは機構ではなく質問の文言だった。** 同じ節へ成立例・反例・不明例からなる判定例を置き、Step 0のskillからその節を参照させる。`SCN-UNIT-MODEQ-023`が質問文の5つの判断を、`SCN-UNIT-MODEQ-024`が判定例6行の変更の実態と判定値の**過不足ない一致**を、`SCN-UNIT-MODEQ-025`が確認対象4項目と条件付きの表記修正文を、`SCN-UNIT-PACKAGE-027`がskillからの参照と参照先の実在を、`SCN-UNIT-MODE-005`がQ-01の回答だけがmodeを決めることを検査する。**保証するのは確定した6例と判定値の対応が維持されることであり、個別案件の回答と根拠の妥当性は独立reviewが判断する。**

**Q-01の対象は公開Web画面を含む。** 利用側projectが公開Webサイトを運用する場合、画面は人や支援技術へ提供する外部interfaceを含む。**アクセス範囲が社内限定であることだけでは除外の根拠にならない。** 外部契約は、そのinterfaceが提供する情報の意味、操作、入力・出力、状態、利用可能性、および外部向けに定めた値・形式・互換性条件を指し、**明示的な互換性保証の存在を必須にしない。**

**外部契約の対象でない視覚的調整は、影響がないと確認できた場合にだけ対象外とする。** 色が状態を表す、配置が操作順や到達性を変える、寸法そのものを外部向けに保証している場合は対象である。**表記修正も、意味・操作識別・支援技術への伝達を変えず、外部契約および生成物を含む他の外部提供物への影響がないと確認できた場合に限りtrueとする。** 3条件だけでは、外部へ提供する固定文字列の変更、生成・同期先への影響、表記自体への外部向け保証の変更を除けない。

**trueの判定例は外部契約の追加・変更・削除または影響未確認を上書きしない。** 変更前後と生成・同期先を確認し、**宣言の不存在や保証の削除を対象外の根拠にしない。** 「約束したか」を唯一の判別子にすると、未宣言の提供物を取り逃し、内部の約束まで外部契約になり、生成・同期経路の提供先が漏れ、保証を削除して「契約ではなくなった」とする変更を対象外にできてしまう。**pathだけの一律allowlist・denylistも採らない。**

**Q-01の根拠欄には、公開境界と提供先、外部契約と保証範囲、画面への影響、生成・同期先への影響の4点を残す。** 新しい台帳とproject choice項目を追加しない。**この確定は判断基準の明示と維持までであり、利用者全員が同じ事実から同じ回答を作ることは保証しない。** `classifyMode`は根拠の非空しか確認しないため、誤った推論を根拠欄へ書いてもanswer=trueならquickになる。意味判断は独立reviewが担う。

- 受け入れ条件: AC-WF-011
- 根拠: Issue #957
- 実装: `src/domain/mode.ts`、`scripts/check_conformance.ts`、`.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/templates/issue/00_要求定義_quick.md`、`.agent-skill-chain/templates/issue/00_要求定義_full.md`

## REQ-WF-001 モードを安全側に判定する

Q-01〜Q-08を全件根拠付きで満たす場合だけquickとする。不明、根拠なし、公開契約・data・security・依存・不可逆操作への影響があればfullへ単調昇格する。明示PoCは専用宣言とhigh risk不在を必要とする。

- 受け入れ条件: AC-WF-001
- 根拠: Issue #824、#827
- 実装: `src/domain/mode.ts`

### REQ-WF-002 Issue計画を原子的に同期する

Issue stagingを隣接一時領域で完成後に公開し、成果物inventoryとdigestを保持する。directory prefixの`YYYYMMDD_HHMMSS`は渡された作成instantを実行環境によらず日本標準時（Asia/Tokyo、UTC+09:00）で表す。quick・pocはStep 4、fullはStep 8の耐久トラッカー書込後再読取一致だけを`sync-verified`とする。

- 受け入れ条件: AC-WF-002
- 根拠: Issue #824、#860
- 実装: `src/domain/issue.ts`

### REQ-WF-003 PoCを正式開発から隔離する

PoCは目的、repository内で実環境・実data・外部副作用から隔離した疑似project fixture、初期化Evidence、runner ID・安全な相対path・固定argv、1件以上のuse case、各use caseに属するGiven・When・Thenを持つBDD scenario、各scenarioの`exit-code=0`と1件以上のbehavior observable、成功・中止条件、非対象、data・security制約、責任者、昇格・廃止条件を必須とする。ID重複、参照切れ、未知field、安全でない相対path、不完全な宣言はpocとして受理しない。要求時点では未実装fixture/runnerのdigestを要求せず、観測期間の経過を完了条件にしない。

ASCは検証対象HEADに追跡済みでdirty・untrackedでないfixtureをHEAD blobから一時copyへ復元し、fixture内の固定Node runnerを宣言argvで実行する。任意shell・任意command・caller作成Evidenceは受理しない。Linuxの固定Git、bubblewrap、prlimit、timeout、出力上限を主隔離境界とし、Node Permission Modelは補助防御に限る。EvidenceはPoC宣言SHA-256、ASC実測fixture/runner SHA-256、scenarioごとの正常終了、ISO 8601 UTC観測時刻、exact HEAD、宣言順の全observableについて型別exact比較した期待値・実値・合否、各result/execution digest、全体digestを必須とする。`workflow poc-observation --staging ... --apply`が`poc-observations/<headSha>.json`をHEAD別append-onlyで固定し、Step 9以降のjournal、verify、PR作成で同じheadSha/evidenceDigest bindingを再検証する。review修正後は新HEAD世代を追加して旧世代を残し、Step 11後の追加を拒否する。baseline..HEADとprovider観測base..HEADの実Git差分がfixture root内の通常fileだけでなければfail-closedとする。

PoCはrelease・自動merge・本番cleanupを拒否する。正式化では不足する01要件、02設計、03計画を補完し、fullからpocへ降格しない。

- 受け入れ条件: AC-WF-003
- 根拠: Issue #827
- 実装: `src/domain/mode.ts`、`src/domain/poc-observation.ts`、`src/domain/issue.ts`、`src/adapters/workflow-journal.ts`、`src/cli.ts`、`.agent-skill-chain/schemas/workflow-mode-decision.schema.json`、`.agent-skill-chain/schemas/poc-observation.schema.json`

### REQ-WF-004 Step順序とjournalを機械検証する

Step 0〜11、mode別列、staging成果物path、共通staging検査を`src/domain/workflow.ts`の単一正本で定義する。journalは行順、必須成果物、evidence、mode単調昇格を検証し、PR作成前にStep 4・10・同期状態を含むStep 10までを要求する。モード判定成果物は`changedFiles`を保存し、再分類でも同じhigh risk判定を再現する。欠落overrideはIssue・scope・主体・理由・期限へ拘束し、承認元をcandidate管理下から分離する。**journal追記経路もStep順序判定と同じ条件で通す。**Step 11記録後の追記とterminal delivery state後のStep 0〜10は、post-terminal intakeのStep 10だけを受理し、それ以外は拒否する。公開CLIは`--post-terminal-intake`と`--post-pr-intake`を値を取らないflagとして受理する。**値を付けた形式は「値を付けずに指定してください」と名指しして拒否する。** 値付きを無言で真として扱うと、利用者が無効化したつもりの入力が有効として記録される。**上流再確定（`workflow record --reconfirm`）はREQ-WF-036で廃止した。** 公開CLIは`--reconfirm`を廃止済みと名指しして拒否し、journal追記経路も`reconfirmation`付きentryを書き込まない。**既存journalに残る上流再確定entry（`reconfirmation: true`）は読取り互換のためだけに受理する。** 読取り側はStep 1〜9に限り、同じStepの実施entry（`humanOverride`、他の再確定entry、post-terminal intakeを除く）が先行し、Step 11より前に置かれた場合だけ受理して順序判定から外す。

`pr-bound`中に外部reviewer指摘を取り込むStep 10再記録は、`workflow record --step=10 --post-pr-intake`で明示する。`postPrIntake: true`のentryは先行する通常Step 10を要し、Step 11より前かつdelivery stateが`pr-bound`の場合だけ順序判定から外す。post-terminal intakeとの併用、通常Step 10としての再記録、他Stepへの指定を拒否する。`workflow verify`はpost-PR intakeを含むjournalに対してdelivery stateの存在と許可状態をread-onlyで再照合し、pending delivery transactionを復旧・削除しない。Step 11前は保存済みstaging inventory/digest、`step11-recorded`後は固定journal digestを含む終端Evidenceが現在値へ一致しない場合にfail-closedとする（Issue #1389、SCN-UNIT-WFJRNL-032〜035、SCN-INT-DELTXN-007）。merge送信HEADは再観測した実効HEADと永続化済み認可HEADの一致を送信直前に要求する（SCN-1389-04、SCN-E2E-WFSTEP-060）。

- 受け入れ条件: AC-WF-004
- 根拠: Issue #877、#1194、#1342
- 実装: `src/domain/workflow.ts`

### REQ-WF-005 reviewと証拠を独立させる

`review progress`は既定の`03_実装計画.md`に加え、round 1の`--progress-target=<path,...>`で明示した最大16件のtask記録Markdownを同じsealed journalから決定的に投影する。旧単一target inventoryは1件へ正規化して読み続ける。targetはNFCの安全なstaging相対path、mode 100644、一意marker、辞書順・重複なしを要求し、marker外改変、rename、mode/type変更、未宣言path、journal不一致を拒否する。

review証跡 1件と、sealed journalから`H_impl`時点の各targetへ再投影したbyte列に一致する宣言済みtargetだけを同じ単一親commitへ含めた差分を`record layer`として受理する。effective candidateは`H_impl`のまま保持する。`recordLayerOnly=true` roundはGit実観測に一致しfindingがない場合だけ保存して予算へ数えない。実装変更、観測不能、findingありは非消費にしない。`review round --init`が出力するround JSONは256 KiB以下の正準reviewer input bundleとし、SHA-256 digestとbyte数を返す。

`review round`は、直前candidateを第1親、remote既定branch tipのancestorを第2親とする2親mergeで、両親の自動merge treeとcandidate treeが一致し、findingが無い場合だけ`followOnly=true`を受理する。follow-only roundはappend-onlyで記録するがreview予算に数えない。観測不能、親不一致、衝突解決、実装変更、findingありは予算外にしない。review予算はcounted round 6、収束後の取り直しを含む8、保存record容量は64とする（Issue #1287）。

実装前のStep 3・7は開始不能な契約穴を一度確認するreadiness checkとし、exact-head品質reviewや最大6ラウンド契約を適用しない。実装後のStep 10だけが肯定・敵対の全観点を最大6ラウンドで評価する。収束後にHEADが変われば同sessionの次roundで実Git fixed diffを再reviewし、同じHEAD、空fixed diff、reset、anchor変更、budget終了後を拒否する。**予算6ラウンドとは別枠で、収束後のHEAD移動に対する取り直しをcounted round 7と8として2回だけ受理する。** counted round 7と8は直前sessionが`converged`である場合に限り、それ以外は拒否する。未解決blockerを抱えて予算を使い切った`budget-exhausted`からは開かない。counted round 7または8で未解決が残れば`budget-exhausted`とし、counted round 9以降を拒否する。**`pr create`がStep 11を記録した後も、条件を満たせば外部reviewerの指摘を同じPRへ取り込む。** 守る性質は「独立reviewerが確認した内容とmergeされる内容が一致すること」であり、HEADが動いたときに同sessionの次roundで再reviewすれば保たれる。取り込みは、前進commitでの是正、既存の有限レビュー予算内での次round収束、review証跡の`H_impl`更新、`workflow record --step=10 --post-terminal-intake`によるStep 10再記録、reviewスレッドへの判定返信をすべて満たす場合に限る。**この記録はStep 11より後にだけ置け、Step 10にだけ許し、順序判定から外す。記録自体は残す。** 予算超過、受け入れ条件の不充足、安全境界・authority・不可逆操作へ及ぶ指摘は、判定と分離先をreviewスレッドへ返信したうえでfollow-up Issueの新しいstagingで完全な工程を通す。Step 10 journalはsession ID、latest round digest、exact HEADを必須bindingとし、HumanOverrideで代替しない。PR作成はprovider副作用前とdispatch直前にそのbindingを保存sessionと再照合する。製品・仕様・testを固定した`H_impl`とreview証跡だけを加えた`H_final`を分離し、実装者・PR authorから独立した同一HEADのapprovalと成功CIだけを承認証拠とする。既定branchが動いてrebaseしHEADの系譜が置き換わった場合は、被レビュー内容が変わっていないことを旧baseから旧headまでの完全diff digestとpath集合の一致で実証したうえで、append-onlyの再固定記録を積んで照合対象を新headへ移す。記録は既存のanchor、round履歴、PR bindingを書き換えない。実効HEADはchainの終端とし、chainが空なら固定済み記録headとする。連鎖条件を満たさない記録は導出に使わない。`pr create`前は`review reanchor`、`pr create`後は`pr reanchor`が受け持ち、境界の逆は拒否する。収束していないsessionの再固定も拒否する。

**rebase後の再固定は二層の等価性で判定する。** `audit:check`はreview証跡（REQ-WF-038）の`baseSha`と`implementationHeadSha`が実際のcommit構造と一致することを要求するため、rebaseしたら`review export --base`で証跡を再生成しなければならない。一方、完全diff digestは`比較基点..H_final`全体を見るため、**その再生成で必ず不一致になる。両者の要求が同時に満たせない。**

**`pr-bound`後にreview証跡のpathだけを是正した場合も、`pr reanchor`で再固定できる。** これは一般のrebase許可ではない。旧・新の`H_final`が同一SHAのreview済み`H_impl`からそれぞれ証跡 1 fileだけを加え、旧・新の証跡がbyte一致し、実装diffが完全に等価であることを要求する。さらに保存済みreview sessionが収束し最新Step 10 bindingと一致すること、証跡がそのsessionと一致すること、新証跡がevidence-only allowlist配下にあることを要求する。旧・新artifactのpathとdigestをappend-only recordへ保存し、既存のPR binding、delivery state、Step 11 journalを変更せず、provider呼び出しも行わない。通常rebase、allowlist外のartifact path、review判断本文の変更、監査不合格、`H_impl`差替え、artifact外差分、chain断裂は`pr-bound`で拒否する（Issue #1377）。この不変条件を強制するのは`SCN-1377-01`から`SCN-1377-03`と`SCN-UNIT-REANCHOR-034`である。

**review証跡の同定は、再固定の3経路すべてでevidence-only allowlistだけを使う。** allowlistは`docs/reviews/`と`.agent-skill-chain/reviews/`の2つである。**`pr create`・review session・record layer・再固定はこの述語を共有する。** `pr create`は`assertConvergedReviewSession`から`evidenceOnlySuffix`を経て到達する。**2 prefixを直書きで持つ箇所が`pr merge`側に残り**（`resolveImplementationCommitForMerge`とdelivery stateのMergeIntent解析）、そこは`..`・backslash・制御文字の拒否clauseを持たない。**受理集合は同じで、束縛済みpathの後段再読であるため合成経路から到達しない。** 合流は別Issueとする。**basenameの字面を受理条件にしない。** `review export`の既定出力`docs/reviews/<Issue番号>_review.json`も、`.agent-skill-chain/reviews/`配下の証跡も受理する。この述語は`..`、backslash、制御文字を含むpathを候補に数えないため、`docs/reviews/`配下であってもそれらを含むpathは拒否する。**これは受理範囲の狭まりであり、単純前方一致だった旧実装との差である。** 候補が1件でない差分は同定できないものとして従来どおり拒否する。**同定はpathだけを見るため、新`H_final`がevidence-only suffixの形を満たすことをmodeまで別に要求する。** `pr create`は`evidenceOnlySuffix`でこれを検査するが、**再固定で実効HEADへ入った新headは以後どこでも再検査されない。** `assertConvergedReviewSession`のsuffix検査は実効HEADとcurrent HEADが異なるときだけ走り、再固定後は両者が一致して素通りする。**再固定がこの形を確かめる唯一の地点である。** 旧`H_final`側へは適用せず、過去に受理した記録を遡って拒否へ変えない。mode `100755`の証跡は通常fileで本文が読めるため厳密解析を通過する。symlinkとgitlinkは本文が読めないため従来から解析で落ちる。同定規則を製品内の2箇所で別々に持つと片方だけが正本から外れる（Issue #1433）。**このmode検証は再固定の3経路（rebase・artifact-replacement・reviewed-forward）すべてに及ぶ。** round 2はartifact-replacementとreviewed-forwardの2経路にだけこの検査を足し、最も一般的な通常rebase経路（`isContentEquivalent`がmode変更で偽になった場合の等価性判定）は対象外のまま残っていた。round 4でこの3経路目にも同じ`evidenceOnlySuffix`検査を足した（外部review round 4、Codex指摘）。この不変条件を強制するのは`SCN-UNIT-REANCHOR-033`から`SCN-UNIT-REANCHOR-041`と`SCN-INT-REANCHOR-016`から`SCN-INT-REANCHOR-018`である。

**`pr-bound`後に外部reviewer指摘を取り込んで実装自体が前進した場合は、内容等価性ではなく新しいexact-head reviewで再固定する。** 旧delivery実効HEADが新`H_impl`のstrict ancestorであり、baseは不変、または既定branch追随のようにbase自体が変わる場合は旧baseが新baseのGit ancestorであり、同sessionの次roundが新`H_impl`で収束し、`workflow record --step=10 --post-pr-intake`の最新entryがsession ID・round digest・`H_impl`へ一致し、新`H_final`がsessionと一致する証跡のevidence-only suffixである場合に限り、`reviewed-forward`としてappend-only記録する。**base変更の正当性はGit objectのancestor関係だけで判定し、candidateの申告やreanchor記録の申告を根拠にしない。** 新baseが実際のGitHub既定branch tipであることは`pr reanchor`自身では検証せず、`pr merge`が別途provider観測で確認する。**`pr merge`側の確認は、terminal recordのmethodやbase変更有無で絞り込まない。** 有効な（`deriveEffectiveHead`と同じlink検証を通過した）reanchor chainが1件以上あるときは常に、その実効terminal record（chainの検証済み末尾）のnewBaseShaが検証済みtrusted policyの`defaultBranchTipOid`のGit ancestorであることをmerge直前にlocal Gitで再確認する。terminal recordのmethod・base変更有無だけで絞り込む設計は、1件目のreanchorで非ancestorなbaseを確立した後、2件目のreanchorで同じbaseを維持する（`oldBaseSha === newBaseSha`）ことで再確認を素通りできるため（Issue #966と同型のattack、Issue #1493のStep 10前独立review 2ラウンドで指摘・是正）、この再確認を欠かすかmethod・base変更有無で絞り込むとreviewed-forwardをはじめ全reanchor経路の安全性は成立しない。同一bindingの再記録は既存entryを返すno-opとする。reanchor公開後かつstaging digest更新前に停止した再実行は、terminal追記前のchainを用いた投影digestが保存済みdigestへ完全一致する場合だけdigestを前向き復旧し、無関係なstaging変更を追認しない。PR identity、create binding、delivery stateは書き換えない。force rewrite、非ancestorなbase変更、明示intake欠落、未収束・別session、監査不合格、artifact以後の製品差分は拒否する（Issue #1389、base変更を伴う前進の受理と`pr merge`側再確認はIssue #1493）。この不変条件を強制するのは`SCN-1389-01`から`SCN-1389-04`、`SCN-UNIT-REANCHOR-032`、`SCN-UNIT-REANCHOR-033`、`SCN-INT-REANCHOR-016`、`SCN-INT-REANCHOR-017`、`SCN-1493-01`から`SCN-1493-03`、`SCN-E2E-WFSTEP-067`から`SCN-E2E-WFSTEP-069`である。

reviewed-forwardの新artifactは`H_impl..H_final`の単一親suffixから選び、比較基点以後に保持された旧review証跡を新artifact候補へ混在させない。

**層を分ける。** `比較基点..H_impl`の完全diff digestは一致を要求し、強さを落とさない。terminal review証跡は、**`baseSha`・`implementationHeadSha`・`evidenceDigest`以外の全fieldの一致を要求する。**変更path集合とartifact pathは完全一致を要求する。

**緩めるのは2 fieldだけである。** session束縛・finding・独立性・検証記録はそのまま比較対象に残す。証跡は厳密解析を通るものだけを同定し、非正規書式は受理しない。**宣言した`H_impl`はGit objectで検証する。** `H_impl..H_final`が当該artifact 1件だけであることを要求し、caller申告を信用しない。**拒否は条件を名指しする。** 理由が1種類だと、条件を外す変更を検査が捕まえない。これを強制するのは`SCN-UNIT-REANCHOR-021`〜`030`である。

**review証跡 1 fileだけを加えたHEAD（evidence-only suffix）はcandidate HEADと同一視する。** `assertConvergedReviewSession`と`pr create`・`pr merge`のbinding検査は、sessionの実効HEADからcurrent HEAD・PR HEADまでの第一親suffixが最大8 commitで、各commitの差分（rename検出なし）がevidence-only allowlist（`docs/reviews/`・`.agent-skill-chain/reviews/`）配下の同じ通常file（mode 100644）の追加または変更ちょうど1件である場合に受理する。空差分・2 path以上・別artifact path・allowlist外・削除・rename・type変更・実行権限・merge commit・9 commit以上・非ancestorは拒否する。merge認可は`H_final`の直前親を無条件に`H_impl`とみなさず、tracked review証跡の`implementationHeadSha`、保存済みreview sessionのlatest candidate HEAD、各commitのevidence-only検査を一致させて元の`H_impl`を導出する。一致しない場合は直前親を検査し、artifactの宣言と一致しなければ拒否する。journalのStep 10 bindingはsessionのcandidate HEADのまま記録し、`audit:check`の`H_impl`と一致させる。artifactだけの前向き訂正には取り直しroundを要求しない（Issue #1437）。

**再固定が成立した後の前round headは、再固定chainから導出した実効HEADである。** 生の記録headを使うと、次の前進修正が「diff baseがcandidate HEADのancestorではありません」で拒否され、**正規経路が再び塞がる。**

**実効HEADの導出は、成立した再固定の記録時刻も返す。** 実効HEADが動いたなら、そのHEADに対する事象時刻も動く。元の固定時刻のまま経過を測ると、再固定直後で新しいHEADのCI runが未生成の状態を、古い時刻からの経過で不達と誤分類する（Issue #969）。**連鎖条件を満たさない記録の時刻は採用しない。** 導出に使わない記録の時刻を返すと、拒否した記録が判定へ影響する。**この不変条件を強制するのは`SCN-UNIT-REANCHOR-018`から`SCN-UNIT-REANCHOR-020`である。**

**再固定のpreviewはapplyと同じ受理条件を評価する。** `pr reanchor`と`review reanchor`は、入力、固定済みanchor、既存chain、冪等性、完全diff、二層の等価性を1つのread-only evaluatorで判定する。previewはdelivery stateの通常file、symlink・hardlink、size、TOCTOU、未完了transactionの整合性を既存の安全な読取境界で検査し、mutation lock、transaction復旧、再固定chain、staging digestを含む永続書込みを行わない。applyは入力検証後に既存mutation lockを取得し、delivery transactionを従来どおり復旧してから同じevaluatorを最新stateへ再実行し、成功時だけ追記とread-backを行う。preview成功後にstateが変わればapplyは最新状態で拒否できる。Git比較自体が失敗した診断は、実際の比較役割、旧・新のbase/head SHA、該当時は旧・新を区別した`H_impl`を含める。汎用のGit差分観測規則は変更しない。preview成功はapplyの認可でも成功保証でもない（Issue #1253）。**この不変条件を強制するのは`SCN-INT-REANCHOR-008`から`SCN-INT-REANCHOR-015`と`SCN-UNIT-REANCHOR-031`である。**

**Step 10は、差分が触れた成果物の種別に対応する検査を当てる。** 実装言語にはprojectのlintと型検査が当たるが、shell script、Makefile、CI workflowのようなrepository運用の足回りは、どの工程でも解析されないまま既定branchへ到達しうる。**これらは検査する側の仕組みであり、壊れると他のすべての検査が黙って素通りする。** 字面の照合は実行可能性を見ないため、種別に対応する解析の代替にならない。ツールを固定せず、当てられない場合はその事実と理由をレビュー成果物へ記録する。**この観点が配布skillから失われたことを回帰として検出するのは`SCN-UNIT-PACKAGE-020`である。**

**配布skillは規範文書と反対の方向を述べてはならない。** `pr create`より後に届いた外部reviewerの指摘の取り扱いについて、規範文書2件が「条件を満たす場合に同じPRへ取り込む」と定める一方、Step 10 skillは「取り込まない」と述べ、**その乖離は起票まで無言で残った。** skillが挙げた根拠「Step 10を再記録できない」は`--post-terminal-intake`の存在により実測で成立しない。**条件と手順の正本は`01_開発ワークフロー.md`であり、skillはこれを名指しして参照する。複写しない。** 複写すると二重管理になり再び乖離する。**検査は必須表現の存在と禁止表現の不在の両方を見る。** 必須だけでは禁止文を残したまま必須語を書き足せば通り、禁止だけでは段落ごと削れば通る。これを検出するのは`SCN-UNIT-PACKAGE-021`〜`023`である。 **本検査は信頼境界ではない。** 検査scriptは保護対象ではなく同一PRで緩和できるため、担保するのは継続的な強制ではなく削除の検出である。

`context-isolated`のmerge approvalは保存済みreview sessionのexact `H_impl`とStep 10 round digestを、tracked `H_final` artifactのpath/digest・別context・対象差分の非変更・肯定/敵対観点・未解決Critical/Highなし・`approved`と照合し、round digestをformal approval IDとする。`requiredReviews`が2以上ならformal approval 1件と同じHEADへのprovider approvalを合算する。`assisted`はformal quality approvalを対象PRの操作authorityへ読み替えず、`--authorize=approved`とrepository write authority照合を別に要求し、その後にだけ`merge-prepared`を耐久化する。`actor-independent`では従来どおり対象`H_final`のprovider `APPROVED`を観測し、PR authorと`H_impl` authorからstable actor IDを分離する。candidate申告、untracked artifact、session/Step 10 bindingが無いartifact本文はapproval authorityにしない（Issue #1379）。

GitHubの自己承認制約により`context-isolated`のformal approvalが成立しても通常mergeが`BLOCKED`となる場合は、`mergeable=MERGEABLE`、非draft、repository `ADMIN` authority、classic protection不在、active rulesが`deletion / non_fast_forward / pull_request / required_status_checks`の許可閉集合だけであること、その閉集合に適合する`pull_request` ruleが1件以上ありrequired approval 0・code owner reviewなし・last push approvalなし・required reviewerなし・選択method許可であること、rulesetが要求するcheckを含む全check成功、全review threadのpagination完了と未解決0件を正方向に観測したときだけcontext-isolated admin mergeを許可する。未知rule、未知parameter、classic protection、観測不能、失敗・pending check、未解決thread、admin権限不足、`actor-independent`はdispatch 0件で拒否する。通常の`CLEAN`経路はadminを使わない。この条件をSCN-1379-ADMIN-001、SCN-1379-ADMIN-002、SCN-E2E-WFSTEP-058、SCN-E2E-WFSTEP-059で検証する。

- 受け入れ条件: AC-WF-005
- 根拠: Issue #824、#834、#1083、#1253
- 実装: `src/domain/review.ts`、`src/adapters/evidence-reanchor.ts`、`src/adapters/delivery-state.ts`、`src/cli.ts`、`scripts/check_skill_templates.ts`、`.agent-skill-chain/skills/step-10-review/SKILL.md`

### REQ-WF-012 実装中の発見を前向きに処理する

発見入力は`DISC-`で始まる安定`discoveryId`を必須とし、判定結果の7記録fieldへ同じIDを引き継ぐ。`workflow promote-full`は無指定でpreviewだけを返し、`--apply`時にだけ永続成果物を更新する。delivery stateが存在するstagingは昇格を拒否する。同期trackerはabsolute GitHub Issue URLに限り、短縮Issue番号を永続化しない。

実装中に問題や新しい事実を発見しても、要求から全工程を自動的に再起動しない。目的、scope、受け入れ条件、security境界、不可逆操作の契約を変えない発見は、`発見ID / 事実 / 影響 / 判断 / 対処 / 検証 / 仕様更新`をfullでは03実装計画、quickとpocでは集約00へ記録して実装を継続する。**計画封印（REQ-WF-036）の後は00〜03へ追記しない。** 実装事実はcommit履歴が所有して記録を要さず、計画の変更は`05_計画変更.md`、現在のsystem契約は`docs/specs/`、scope外の事項はfollow-up Issueへ振り分ける。

**開始時に承認したcanonical Issueの目的にASC本体の保守を含まない作業では、発見したASC本体の欠陥を当該scopeへ追加して是正しない。例外を設けない。** ASC本体とは配布される規範文書、schema、CLI、検査、template、project policy機構であり、利用projectがASCを用いて作る成果物を含まない。scope拡張、candidateによる自己許可、別roleへの付け替えを例外として認めない。**目的にASC本体の保守を含むIssueは対象外であり、例外ではない。** 是正は別Issueとして別scope・別履歴・owner決裁のもとで開始する。**作業の成果物をASCの契約へ合わせることは従来どおり必須であり、この禁止に含まれない。ただし検査を通すための変更が、検査が無ければ弁護できない弱化・観測量の置き換え・実質を持たない充足になる場合は成果物側の是正ではない。** 作業が進まない場合は停止、記録、別Issueへの分離、owner決裁の順に扱い、未許可操作や未許可のsource編集で回避しない。owner決裁が扱うのは分離Issueの承認・延期・却下、修正版の反映後の元作業再開、元作業の中止であり、元Issueのscope内でASC本体を直す許可を含まない。**作業を止めない軽微な矛盾は記録と別Issueの起票だけを行って継続する。軽微かどうかは判断ではなく3条件で決める。** (1)その欠陥を是正せずに当該作業の受け入れ条件をすべて充足でき、(2)充足のために成果物を歪めず、(3)安全条件・authority分離・fail-closed不変条件のいずれにも触れない。**1つでも偽または不明なら軽微としない。** 判定は当該作業を実施するroleが行い根拠を記録へ残す。 **ASCへの改善提案と別Issueの起票は禁止しない。** 強制はreviewが担い、非保守IssueのPR差分にASC本体のpathが現れた場合にreviewerがfindingとする。**機械による自動照合は存在しない。**

契約を変える発見だけ、モードに応じて影響する成果物を再確定する。`full`では目的またはscopeの変更時に00と影響する01〜03、AC、domain invariant、requirement、interface、data、design responsibility、non-functional、operations、security境界、不可逆操作の変更時に01〜03を対象とする。`quick`の通常の契約変更は集約成果物00だけを再確定し、security境界の拡大、不可逆操作、または根拠付きモード失格条件の発見時は同じIssue stagingを`full`へ単調昇格して00〜03を補完する。`poc`の通常の学習契約変更は集約成果物00だけを再確定し、同じ失格条件の発見時は停止するか明示操作で`full`へ単調昇格し、PoCのまま続行しない。モード失格条件と契約種別はcanonical IDに限定し、空値、重複、未知ID・未知fieldを拒否する。

この判定は、現在モード、契約変更フラグ、`changedContractKinds`、根拠付きモード失格条件を構造化入力とする`workflow assess-discovery --input=<JSON>`の決定論的出力によって確定する。`--staging`を指定し、そのjournalに計画封印がある場合、影響成果物の再確定（`rebaseline-affected-contracts`）は`record-planning-amendment`へ置き換わり、影響成果物は`05_計画変更.md`、変更対象の封印済み文書は`amendmentTargets`となる。昇格・停止の判定は封印によって変わらない。判定はpureとし、quickの`promote-to-full`またはPoCの`stop-or-promote-full`から昇格を選んだ場合だけ`workflow promote-full`が、同じtracker・journalを維持して正規モード判定、昇格前00・モード判定、発見Evidence、full用00〜03を永続化する。正規判定は元の`requestedMode`を保持し、発見に対応するQ回答またはPoC high-risk項目をEvidence付きで更新してfullを再現しなければならない。昇格前に全成果物の通常file・包含・一覧・digestを検証し、staging単位の排他lockと永続transaction markerを使用する。全成果物とmarker除外済み最終recordをfile・directory単位で耐久化した後にだけmarkerを削除する。process停止後の再実行はmarkerが残る未commit状態をrollbackして再適用し、marker削除済みなら永続した発見・判定backup・成果物一覧・digestと再実行入力が一致する場合だけ同じ結果へ収束する。Step 4同期済みなら`promotion-active`としてStep 8再同期までPRを拒否する。昇格後のStep 4と8は、昇格前に記録したabsolute GitHub Issue URLとrepository・Issue番号が完全一致する元Issueにだけ許可し、別IssueはGitHub副作用前に拒否する。Step 4は検証専用でstaging記録を更新せず、Step 8だけを最終同期Evidenceとする。未同期なら`local-active`を維持する。昇格前のStep 0・1だけを継承し、fullのStep 2〜10を順に補完する。Issueの再起票、変更のない上流成果物、完了済み調査をやり直さない。Step 9で成立状態を`docs/specs/`へ反映し、Step 10は実装中発見記録、コード、test、システム仕様書が一致するexact-headをreviewする。

- 受け入れ条件: AC-WF-012
- 根拠: ASC一部刷新
- 実装: `src/domain/agile-verification.ts`、`src/domain/staging.ts`、`src/adapters/workflow-journal.ts`、`src/cli.ts`、`.agent-skill-chain/templates/issue/00_要求定義_quick.md`、`.agent-skill-chain/templates/issue/00_要求定義_poc.md`、`.agent-skill-chain/templates/issue/03_実装計画.md`

### REQ-WF-013 automatic deliveryをPRからmergeまで継続する

Step 11はdelivery終端専用とし、`workflow record --step=11`を拒否する。createとmergeのprovider call直前に永続dispatch claimをfsyncし、claim消費後は自動再送しない。PR作成後binding前のbase前進は既存PR照合を妨げず、一致するclosed・merged PRがあれば重複作成を拒否する。

**「provider call直前」は認証・権限・remote SHAの再検証より後を指す。** これらの検査で落ちた時点ではproviderへ変更要求を送っていないため、claimを消費せず`create-prepared`のまま残し、原因を解消した同じcommandで前進できるようにする。**claimの受け渡しがないPR作成をfail-closedで拒否する。** 受け渡しを任意にすると、claimを取らずに変更要求を送れる経路が公開され、並行実行で重複PRを作れる。**この不変条件を強制するのは`SCN-INT-GITHUB-019`から`SCN-INT-GITHUB-021`と`SCN-E2E-WFSTEP-043`である。**

merge認可はreview証跡直前の`H_impl`、review証跡だけを加えた`H_final`、証跡path/digest、PRに直接関連する成功CI run ID、`H_final`への独立approval IDを不変intentへ含める。rulesetはPR・status checks・署名・non-fast-forward・linear historyの保護型だけをprotection Evidenceにし、deletionだけのrulesetをprotectedと判定しない。

Step 11 journal後のcrash復旧は保存済みartifact inventoryとcontent digestを復旧直前に再照合し、不一致ならstateを書き変えない。

`delivery.stopAt=pull_request`は`pr create`がmergeを同じ外部操作へ暗黙連結しないコマンド境界とし、ASC全体の正常終了をPRへ固定しない。PoCと`merge.mode=disabled`はPRで停止する。`assisted`は対象PR authorityがなければ停止して再開条件を返す。full・quickの`automatic`では、適用された`pr create`はPR URLと`merge_pending`を返し、Step 11をまだ記録しない。

`pr create`はprovider副作用より前にwrite authority、providerが観測したdefault branchとtip、remote head SHAを取得し、指定repository・base、trusted policy commit、exact HEADと一致させる。その後、repository、canonical IssueとURL、head ref/SHA、base refと作成時base SHA、canonical title/body digest、closing契約digestを同じIssue stagingへ耐久化する。作成後read-backでrepository、PR番号・URL、canonical Issue、same-repositoryかつcross-repositoryでないhead repository、head ref/SHA、canonical title/body、base ref、closing Issue 1件を再照合し、以後のdeliveryが使うidentityとして固定する。project外、別Issue、symlink経由のstagingはprovider副作用前に拒否する。作成結果が曖昧な場合はcreateを再送せず、固定identityによるprovider read-backで既存PRを照合する。一度もdispatchしていない`create-prepared`に限り、該当PRが0件であることを決定的に確認した場合だけ同一immutable intentを1回再試行できる。曖昧または複数候補は`reconciliation-required`として停止する。

新規create intentでは、providerが観測したdefault branch tipとexact head SHAのmerge-baseを`git merge-base --all`で一意に取得し、そのSHAがprovider観測tipと一致することでancestor関係を検証する。PR後もdeliveryが続くautomaticとassistedは、不成立なら`create-prepared`永続化前に拒否する。`merge.mode=disabled`はPRが正式終端であるため、不成立だけを警告して続行する。PoCは既存のより厳しい「provider観測base..HEADのfixture root限定」を引き続き必須とし、baseがHEADのancestorでない場合はprovider書込み前に拒否する。Gitが比較自体を完了できない場合、または一意なmerge-baseを得られない場合は全modeでfail-closedとし、HumanOverride journalを含む内部書込みとprovider PR作成より前に拒否する。拒否時は、既定branchをmergeで取り込んでHEAD依存のtest・review証跡・Step 10を再生成するか、内容等価なrebase後にPR作成前用の`review reanchor`で証拠を再固定してから再実行する手順を返す。既存stateへ遡及適用せず、anchor書換えやoverride authorityを追加しない。この不変条件を`SCN-E2E-WFSTEP-061`から`SCN-E2E-WFSTEP-066`で検証する。

後続の独立した`pr merge`は`pr create`で検証したものと同じstagingを必須入力とし、Step 0〜10、Step 4・10、`sync-verified`、モード、既定branch上のtrusted policy、required checks、独立review、固定したcurrent HEAD、classic protectionまたはexact branchへ適用されるactive ruleset、mergeable状態を外部操作前と直前に再検証する。`poc`では`pr merge`を物理的に拒否する。base refはPR作成時から固定し、同じbase ref上の通常のbase SHA前進は許容するが、provider default branch tip、PR base SHA、trusted policy commitを一致させる。外部merge要求より前にmethod、認可HEAD、intent IDを耐久化する。merge結果が曖昧な場合はmergeを再送せず、auto-mergeとmerge queueを含むprovider read-backで照合する。一度もdispatchしていない`merge-prepared`に限り、該当auto-mergeとqueue entryがいずれも存在しない決定的Evidenceを得た場合だけ同一immutable intentを1回再試行できる。曖昧または複数候補は`reconciliation-required`として停止する。

native auto-merge要求はmethodとrequestedAt、merge queue登録はentry ID・state・requestedAt・head/base OIDを`merge-observed`の進行状態として耐久化するが、Step 11成功にはしない。同じPRについてproviderからrepository、PR、Issue、HEAD、closing契約、`merged`状態、provider mergedAt、merge commit SHAを再観測してから`outcome=merged`とEvidence IDをStep 11へ記録する。PoCと`disabled`は固定PR bindingのdigestを`outcome=pull-request`とするStep 11終端にする。`assisted`のauthority待ちは終端ではなく`pr-bound`のまま再開条件を返し、Step 11を記録しない。PR停止またはmerged終端ではjournalを先にfsyncし、そのdigestをdelivery stateへ保存する。journal保存後・state保存前の停止は同じcommandの再実行でstateだけを前向きに復旧し、provider副作用とStep 11追記を再送しない。

`disabled`で`outcome=pull-request`へ終端化したfull/quickのopen PRは、既定branch上のtrusted policy変更だけでは再開しない。repository ownerが同一PRへの新しいdelivery判断を明示した場合に限り、旧Step 11とjournalを不変に保った`redelivery`として、現在のtrusted policy、固定PR identity、exact HEAD、checks、独立review、protectionを再検証して既存merge経路へ進める。PoC、現在も`disabled`、closed/merged PR、identity・HEAD不一致、明示判断なしはprovider dispatchを0回のまま拒否する。完了は旧Step 11を`merged`へ書き換えず、固定した旧evidence IDとprovider merged observationを結ぶ`redelivery.outcome=merged`で表す。

automaticのPR URL取得だけを正常終了と報告しない。merge authorityはブランチ削除、Issue手動終了、release、公開、完了処理、後片付けへ伝播させない。GitHub側でmerge待機する場合は無期限pollingをせず、merge queue登録を進行状態として耐久化する。

**独立reviewの不足による拒否は、拒否そのものを変えずに診断を返す。** 診断には要求した独立approval数と対象HEADに対して観測した独立approval数、mode、対象HEAD SHA、数え方、次に採る操作、必要な最小authority、rollbackを含める。**宣言した`requiredReviews`が下限1へ引き上げられた場合は、宣言値と適用値の双方を根拠へ出す。** 沈黙した上書きは、policyを直しても拒否が変わらない理由を運用者から隠す。**根拠へreviewerのactor IDを載せない。** 判定に必要なのは件数であり、識別子は拒否の解消に要らない。**診断の生成は引数だけに依存する関数とし、永続状態も新しい門も持たない。** `docs/specs/02_要件/03_外部連携要件.md`のCI run観測と同じく、報告を増やしても判定は変えない。**この不変条件を強制するのは`SCN-INT-MERGE-013`から`SCN-INT-MERGE-016`である。**

`merge-prepared`の独立approval IDは、`context-isolated`ではformal review round digest、`actor-independent`ではprovider review IDとする。どちらも対象modeの検証済み証拠から導出し、candidateの自己申告を不変intentに固定しない。

- 受け入れ条件: AC-WF-013
- 根拠: ASC一部刷新
- 実装: `src/domain/agile-verification.ts`、`src/domain/delivery.ts`、`src/domain/delivery-state.ts`、`src/adapters/delivery-state.ts`、`src/cli.ts`、`src/adapters/github.ts`、`src/adapters/workflow-journal.ts`、`.agent-skill-chain/skills/step-11-pr/SKILL.md`

terminal redeliveryは、明示flagをauthority source、`repository.assert-write`で観測したactor、repository、write Evidence IDを`redelivery.authority`へ保存し、固定repositoryと一致させる。欠落したauthority Evidenceや候補による自己申告をowner判断として受理しない。

### REQ-WF-006 Gherkinと仕様追跡を検証する

project choiceのdialect・test layer・禁止suffixに従い、全Scenarioへ一意なSCN IDとGiven・When・Thenを要求する。要件からAC、SCN、Feature完全path、実装完全pathへの到達可能性を検証し、孤立と配置外定義を拒否する。**禁止suffixの空配列は「この方針を持たない」の正規表現とし、禁止対象0件として扱う。** `testLayers`と違い件数下限を持たない。既に宣言した禁止suffixの除去は単調性契約の二段階手順で行う。**test層の語彙はpackage側で定義せず、利用側projectが担保の水準を分類する軸として決める。** 宣言済みのtest層の除去も同じ二段階手順で行う。

**SCN IDの文法（`SCN-`に大文字英数字とハイフンだけを続ける）は`src/domain/scenario-id.ts`の単一正本とし、`issue validate`のscenario行検出と`pr create`のdelivery証跡検査は同じ述語で判定する。** 受理集合は工程の入口と終端で一致し、`issue validate`は行頭keywordに続く文法外のID（小文字の枝番、下線、全角、空本体）を当該IDと文法を名指しして拒否する。正規化や読み替えは行わない。

- 受け入れ条件: AC-WF-006
- 根拠: Issue #824、#881、#1349
- 実装: `scripts/check_trace.ts`、`src/domain/scenario-id.ts`、`src/domain/issue.ts`、`src/domain/delivery.ts`

### REQ-WF-007 role・model・tierと独立性を解決する

6 roleの許可path・操作・禁止操作・必要証拠を検証し、implementerとreviewerのidentity・contextを分離する。risk・mode・scopeから必要tierを単調に決め、Codex high・Claude Opusの自律上限を維持する。

Codex taskを起動するたびに公式model/listを観測し、現在の利用環境の一意のrecommended defaultでhigh対応のmodelを選択する。発売日・名称順・最高性能を推測しない。trusted tierMappingの論理key `codex:provider_recommended_default:high:default` が示すproject採用tierを必要tierと比較し、model slugの手書き追加を要求しない。公式のisDefaultはproject独自tierの性能保証ではなく、採用tierは操作authorityを付与しない。

Claudeは公式stream-json initialize応答のmodel一覧をread-onlyで観測し、`value=default`の一意なentryが申告する逐語`resolvedModel`とhigh対応を採用候補にする。trusted tierMappingの論理key `claude:provider_recommended_default:high:default`が示すproject採用tierを必要tierと比較し、`claude-opus-5[1m]`などhost解決IDの追加を要求しない。Claude Models APIの一覧、名称、発売日から推奨や最上位を推測しない。

`routing launch`はCodex選択を具体model・high・service tier defaultの起動へ接続する。trusted設定、role/context、task入力、公式観測が不成立なら起動しない。旧model・別providerへの自動代替と起動後の自動再送を禁止する。成功、失敗、結果不明を区別し、選択根拠とdispatchしたmodelを秘密本文なしで返す。dispatchの記録をproviderが実際に処理したmodelのattestationとは扱わない。既存`routing resolve`のfallback結果は互換照会として保持し、launchはpreferred Codex以外を拒否する。**`routing tier`は判定の信頼源と用途を出力へ必ず含め、`--provider`は`codex`と`claude`を受理する。** provider未指定はworking treeの既存台帳による互換検証であり、trusted側の定義を要求する診断を返さない。provider指定時の判定は固定したtrusted commitからpolicyを読み、candidate working treeのpolicyが欠落または不正でもcandidateを先読みしない。Claudeでは観測modelとselectorを分離し、公式defaultが0件・複数、解決ID不正、high非対応、要求model不一致を拒否する。**この契約を回帰として検出するのは`SCN-UNIT-TIERPROV-001`〜`SCN-UNIT-TIERPROV-005`、`SCN-UNIT-ROUTING-009`、`SCN-INT-TIERPROV-005`である。**

公式観測の`runJsonlSession`はstdin書込errorを固定された安全な理由付きの失敗へ反映し、後続の終了値0で成功へ戻さない。`allowFailure=true`は非0の結果、falseは例外を返し、入力本文や元errorの秘密を診断へ追加しない。

- 受け入れ条件: AC-WF-007。公式推奨Aから未登録Bへ変わった次回起動でBを使用する。候補自身の採用設定、推奨不定、high非対応、入力不正では起動0回。起動timeout・失敗・成功を区別し、promptやraw出力を結果に含めない。
- 根拠: Issue #830、#836、#1257、#1265、#1343、#1350。Issue #1265のREQ-PIO-001〜003、AC-PIO-001〜003は観測失敗と検証fixtureの詳細化として本要件へ統合する。Issue #1257のREQ-AM-001〜008、AC-AM-001〜006は本要件・受け入れ条件の詳細化として統合する。
- 実装: `src/domain/routing.ts`、`src/domain/role.ts`、`src/adapters/provider.ts`、`src/adapters/codex-launch.ts`、`src/adapters/codex-execution.ts`、`src/cli.ts`

`workspace-write`の起動はGit topologyをprovider観測前に検証する。通常primaryは追加許可0件、registered linked worktreeはGit metadata write rootsを各1回だけ配送する。root・git directory・common directoryとforward/reverse linkが一致しない場合、非Git、bare、separate-git-dir、symlink経由、不正出力、環境によるGit管理path上書きは`state=rejected`、`dispatched=false`で拒否する。read-onlyへ追加write rootを渡さず、拒否理由へprompt・raw Git stderr・private pathを含めない。

- 詳細追跡: Issue #1383のFR-1383-01〜03、AC-1383-01〜04をREQ-WF-007 / AC-WF-007へ統合する。SCN-INT-ROUTING-1383-001、SCN-UNIT-ROUTING-1383-002〜004で検証する。
- 追加実装: `src/adapters/git-workspace.ts`。
- 詳細追跡: Issue #1425のFR-101〜107、AC-101〜108（reviewer役割へのローカルLLM provider追加、`supplement`/`replace`モードの切替）をREQ-WF-007 / AC-WF-007へ統合する。implementer向けの既存`resolveRouting`/`launchCodex`/`executeCodex`とは独立した経路として追加し、既存契約は変更しない。provider dispatchは`DISPATCHABLE_REVIEWER_PROVIDERS`（`src/domain/reviewer-provider.ts`）へのレジストリ登録形式とし、reviewer側の別provider追加をprovider名のハードコードなしに行えるようにする（implementer側の`codex`/`claude`固定は対象外）。`replace`モードはローカルLLM出力の主観評価部分を既存`evaluateReviewJudgment`（Claude/Codexレビューと同一のCritical/High blocking判定。`findings[].riskAcceptance`はLLM出力から採用せず、Critical/High指摘の自己承認を許さない）へ渡すことでreviewer役割の正式な充足条件（FR-107）を満たす。unit 16・integration 15・e2e 3の計34 SCNで検証する。詳細なSCN IDと対応表は`15_要件追跡/00_追跡表.md`を正本とする。AC-109（本機能を要求した利用project自身のIssue上で実際に完走させる実地検証）はcucumberスイート対象外であり、この自動検証の追跡表には含めない。実施記録はAC-109を要求した利用project側のIssueで管理する。
- 追加実装: `src/domain/review-routing.ts`、`src/adapters/review-launch.ts`、`src/adapters/local-llm-execution.ts`。
- 詳細追跡: Issue #1428のFR-1428-01〜07、AC-1428-01〜09（`modelMapping.roles.reviewer`とは独立な「補助レビュー」機構の追加）をREQ-WF-007 / AC-WF-007へ統合する。`modelMapping.roles.reviewer`・`resolveReviewRouting`・`launchReview`（Issue #1425）はいずれも変更しない独立経路とし、エンジニア個別のGit管理外設定（既定`.agent-skill-chain/local/supplemental-review.json`）から有効化する。設定はpackageの`install`/`update`管理対象namespaceに含めず（`src/domain/lifecycle.ts`の`NAMESPACE_ASSETS`）、上書きされない。指摘は`pr create`/`pr merge`/`review round`のいずれの判定にも用いない。unit 4・integration 4の計8 SCNで検証する（AC-1428-09は文書の目視確認でありcucumberスイート対象外）。詳細なSCN IDと対応表は`15_要件追跡/00_追跡表.md`を正本とする。
- 詳細追跡: Issue #1439では、個人設定にchill/assertive profileを追加し、chillはCritical/Highのみ、assertiveは全severityを表示する。既定は従来互換のassertiveとする。隠した対象内候補は`suppressedFindings`へ残して進行役が確認できるようにし、差分reviewの第二passでは表示閾値に関係なく全候補を検証する。全候補の元の順序は`firstPassFindings`に残し、第二passの`findingIndex`と対応させる。findingに任意のEffortタグ（Quick win/Moderate/Heavy lift）を追加する。表示profileにかかわらず、根拠のないblocked判定を拒否する。SCN-SUPPL-019〜020、SCN-UNIT-DELEGREVIEW-015〜017で検証する。
- 詳細追跡: Issue #1438では、差分レビューで生成されたfindingを確定HEADのfile blobと照合する第二passを設ける。`valid`の自己申告を正式な採否に使わず、初回の差分内候補と第二passの提案を`needs_coordinator_review`として返す。第二passの引用、理由、失敗経路、遮断コードは候補indexとHEAD commit/file付きの`verificationAssessments`へ残す。引用一致は意味論の証明にしない。採用判定に遮断コードがある場合、または却下判定に障害コード・失敗経路がある場合は、片方の根拠だけでも`conflicting`とする。矛盾・引用欠落はその状態を残して提案を`null`とし、不正・取得不能な応答はassessmentも`null`にする。初回候補は常に残す。進行役がHEADのコード・仕様・test・失敗経路・反証に照らして候補を根拠付きで分類し、未観測の検証を成功と記録しない。補助差分レビューは比較基点と対象HEADに40桁の確定SHAだけを許可し、対象HEADを現在のHEADと照合してdispatch後の移動も拒否する。HEADで削除済みのfileなど入力を安全に固定できない場合はdegradedへ倒す。Step 10の委譲レビューも`approved`/`changes_requested`を自動確定しない。進行役の候補分類をformal independent approvalとして扱わない。SCN-UNIT-FINDVERIFY-001〜015、SCN-UNIT-DELEGREVIEW-005・009・014で検証する。
- Issue #1455（利用projectからのローカル委譲review報告）: Git管理外の補助設定における`model`は空値・制御文字・過大入力のみ拒否し、model名・tagの妥当性と存在はOllamaへ委ね、自律的reviewer役割の`PROVIDER_AUTONOMOUS_CEILINGS`に含まれるかどうかで起動可否を決めない。`qwen3.8:27b`等のpoint releaseやGemma・gpt-ossを補助reviewへ委譲できる。個人設定の`provider=ollama`とloopback HTTP endpointの検証は維持し、出力は進行役の判断材料とする。正式なreviewer選択とapproval/merge authorityには従来のtrusted policyとceilingを適用する。SCN-UNIT-DELEGREVIEW-018〜025で検証する。
- 追加実装: `src/domain/supplemental-review-config.ts`、`src/adapters/supplemental-review-collect.ts`、`src/adapters/supplemental-review-launch.ts`。
- Issue #1432のStep 03/07/10の進行役によるreviewer委譲: Git管理外のprojectローカル設定またはユーザー共通設定が有効な場合、設定されたローカルLLMへ対象staging（Step 10ではexact-head差分も）を渡し、肯定・敵対評価とfindingを受け取る。ローカル優先、明示無効化は共通設定を抑止する。未設定時はローカルLLMを起動せず、Issue #1449の独立reviewer経路を使う。設定済みで実行失敗・不正応答の場合は完了にしない。個人設定からtrusted policyやmerge権限を変更せず、従来の補助レビューCLIの契約を維持する。関連fileは文脈として渡すが、差分外の別taskや過去Issueをfindingとして採用しない。 対象stagingとrootは同じ登録済みGit worktreeへ固定し、対象worktree・主worktree・ユーザー共通の順に個人設定を選ぶ。関連fileのstem検索は拡張子なしdotfileと多件数一致の汎用名を除外する。 Gitの日本語pathを表示用引用文字列へ変えずに照合し、収集中に1MiBの入力上限を適用する。除外件数を結果に残し、対象差分のCritical/Highだけをblockingに用いる。実装は`src/domain/delegated-review-config.ts`、`src/domain/review-finding-scope.ts`、`src/adapters/delegated-review-launch.ts`。

- Issue #1449のreviewer併用方針: ローカルLLM設定時はその候補を優先し、別の独立reviewerも使う。未設定時はCodex Sol（TERM-ASC-128）を通常候補とし、利用不能ならOpus（TERM-ASC-129）を候補にする。Step 10の対象PRの最新HEADに対するGitHub check・review・通知からCodeRabbit利用枠制限（TERM-ASC-130）を観測した場合は、ローカルLLMの有無にかかわらずCodex SolとOpusの両方で独立レビューし、片方が利用不能なら未実施を記録して人間へ再開条件を確認する。制限の判定はPR・HEAD・観測URL・時刻・文言をreview成果物に記録し、check不在や過去HEADの通知を制限扱いしない。両者のfindingの採否は進行役が出典、成立条件、反証と再現結果で判断する。この方針は進行役の運用手順であり、製品のprovider registry、loopback限定、非永続、no-telemetry、正式なmerge authorityは変更しない。

- Issue #1440のStep 10差分findingには、LLMが単一fileのunified diffを任意で提案できる。候補patchはfinding本体や第二passの入力から分離し、確定HEADの通常Git blob、対象path、patch文脈、適用後のTS/JS/JSON構文を隔離一時領域で検証する。個別64KiB、最大8候補、合計256KiB、検証全体5秒・Git処理個別最大2秒を上限とする。成功時だけ`committableSuggestion: {headSha, patch}`を表示対象findingへ付ける。失敗時は提案のみ省略し、finding、第二pass、進行役の採否を維持する。作業treeにはpatchを適用せず、一時領域を終了時に削除する。Step 3/7の文書findingには適用しない。SCN-UNIT-SUGGESTION-001〜005、SCN-INT-SUGGESTION-001で検証する。

### REQ-WF-008 conformance適用宣言を検証する

未指定を含む`repository-bound`はI1〜I12を各1件の実在source・enforcement・反例SCNへbindingする。`package-attested`はpackage証拠を再利用するがconsumer適合をclaimしない。理由・証拠のない適用外、空配列、scope格下げ、binding削除を拒否する。**enforcement exportの走査は「実在する」「実在しない」「sourceを解析できず判定できない」の3値で報告する。** 判定不能を「実在しない」と同じ診断へ潰さない。利用者が次に採る操作が異なるためである。実在しないならbindingを直すかexportを実装し、解析できないならsourceの記法を直す。**いずれの場合もerrorを積み、判定不能を合格へ倒さない。**

- 受け入れ条件: AC-WF-008
- 根拠: Issue #834、#837
- 実装: `src/domain/conformance.ts`

### REQ-WF-009 CLIの必須入力とusageを1回の実行で提示する

subcommandごとの要約、必須flag、条件付きflag、任意flagと既定値、実行例を`src/cli-usage.ts`の単一正本で定義する。`--help`は必須flag検証より先に評価してusageを返す。**flagの先にある入力fileの契約は`inputContract`（構造の説明と例）としてusageへ載せ、`--help`のJSONへそのまま出す。** `review round`の`--file`と`pr create`の`--evidence`がこれを持ち、`review round`はdigestの算出式とenumを説明へ含める。**拒否診断は次の1手を含める。** staging digestの不一致（`review round`と`pr merge`の直前再検証）が案内する手順はREQ-WF-024が所有し、ここで重複して定義しない。`worktree finalize`のcleanup-apply拒否は`--approved-digest=<preview digest>`の指定を案内する。案内は判定を1つも変えない。不足している必須flagは1件ずつではなく1回の実行で全件を`reasons`へ列挙する。条件付きflagのうち与えられたflag集合から機械的に必須と判定できるもの（`requiredWhen`述語を持つもの）も同じ1回の診断へ含める。**session状態など保存済み記録に依存して必須が決まるflag（`review round --init`の`--base`・`--scope`・`--ac`）は一括診断の対象外で、handlerが個別に報告する。**値をとるflagの空白区切り指定は無言の未指定扱いにせず、`--flag=値`形式を求める専用診断で拒否する。trusted boundary評価は必須flag検証より先に行う。**flagの型宣言は実装が読む形式と一致させる。** `routing roles --assignments`と`routing ceiling --override`はinline JSONを読むため`<JSON>`型で宣言し、JSON本文として解析できない値（file path等）は、解析errorに「inline JSONを渡す（file pathではない）」の是正操作を併記して拒否する。正当なJSON本文は従来どおり解析し、配列・objectの型診断は解析後の既存検査が担う。fileは読まず、値本文を診断へ転記しない。usage正本と`src/cli.ts`の実装が読むflagの一致は`scripts/check_cli_usage.ts`が双方向に検証する。

- 受け入れ条件: AC-WF-009
- 根拠: Issue #886、#1340
  配布文書は`--help`の存在、値をとるflagが空白区切りを受理しないこと、不足flagを1回で全件返すことを述べる。**同じ事実を二度書かない。** `.agent-skill-chain/00_利用案内.md`とREADMEの該当区画は`scripts/generate_cli_usage_guide.ts`が`src/cli-usage.ts`のusage正本から生成し、`scripts/check_cli_usage.ts`は生成区画が正本の描画結果と一致することだけを検証する。一致しない場合は生成commandの実行を求める。

- 実装: `src/cli-usage.ts`、`src/cli.ts`、`scripts/check_cli_usage.ts`、`scripts/generate_cli_usage_guide.ts`、`.agent-skill-chain/00_利用案内.md`、`README.md`

### REQ-WF-014 review roundの入力雛形を実Gitとsessionから生成する

`review round --init --staging=<staging> --out=<path> --head=<sha>`は、次roundの入力JSONのうちfindings以外を確定した雛形を`--out`へ書く。review sessionが無ければround 1で、`--base`・`--scope`・`--ac`（任意で`--invariant`）を要求し、`anchor.initialDiffDigest`を実Gitから算出する。sessionがあれば次roundで、anchorをsessionから写し、`previousRoundDigest`を`latestRoundDigest`、`focus.previousBlocking`を前roundのblocking、`focus.fixedDiff`を再固定chainの実効HEADから`--head`までの実Git差分pathにする。sessionがあるのに`--base`・`--scope`・`--ac`・`--invariant`を指定した場合は、これらを無視してanchorをsessionから写したことを`notes`で通知する。findingsは常に空で、再評価が必要な前round blockerを`notes`で返す。**`--out`は親directoryのrealpathがstagingの外で、かつ存在しないpathに限る。** staging内に置くとstaging digestが変わり`review round`自身が拒否するためであり、symlink祖先経由の書き込みも拒否し、既存fileは上書きしない。**雛形の作成は固定した親directory descriptorから解決するpathだけを使う。descriptor path自身の`stat`値には依存せず、そのpathを再openしたdescriptorの`fstat`が固定済みdirectoryと同じdev・inoであることを検証する。** Darwinのfdescfsは`/dev/fd/N/leaf`の末尾pathを探索できないため、固定したdirectory descriptorを隔離した`/usr/bin/python3 -I -S`のfd 3へ継承し、POSIX `openat(dir_fd=3)`で末尾fileを排他的に作成する。名前付き親pathへのfallbackは行わず、helperを起動できない場合はfile作成前に固有errorでfail-closedにする。対応環境ではdescriptor相対pathから作成し、検査後に名前付き親pathを差し替えられてもstagingや別directoryを再解決しない（CWE-367）。作成後に失敗した場合、末尾entryのidentity確認とpathname削除は不可分でなく無関係fileを削除し得るため、pathname削除を行わない。まだ開いている作成descriptorを空にしてentryを保持し、その残存と利用者による確認・削除が必要なことをerrorで明示する。identity取得前の失敗も作成済み状態として同じ処理を行う。fileとdirectoryのfsyncおよびfile descriptorのcloseが完了した後のdirectory descriptor close失敗は、完成済み雛形を未確定へ戻さず成功時の`written`を返す。descriptor aliasもDarwin helperも提供しない環境はfile作成前にfail-closedで拒否する。成功時の`written`は従来どおり利用者指定pathを返す。sessionが`budget-exhausted`ならHEAD差分の有無より先に雛形を書かず固有errorにする。`--init`は`--file`・`--apply`と併用できず、stagingとreview-session.jsonのbyteを1つも変えない。**previewが拒否する雛形は書かない。** `--head`がcurrent HEADと異なる場合、sessionがあってHEADを進めていない場合はerrorで止め、anchorのID列は重複なし昇順へ正規化する。判定は従来どおり`review round --file=<out>`が行う。

Darwin helperは作成fileのdescriptorをdirectory fsync完了まで保持する。directory fsyncを含む作成後失敗では、まだ開いているdescriptorを空へsanitizeする。helperがsignal終了して結果を返せない場合はsignal名と未sanitizeを診断し、pathname削除は行わない。

雛形のnotesは検分対象の短縮SHAとcommit subject、およびreview結果を反映するcommitより先にroundを記録する必要を示す。空fixed diffではHEADを進めず、`review-session.json`の`candidateHeadSha`と実際のレビュー順の照合を案内する。非収束診断は`active`ならreview未完了またはHEAD対応誤り、`budget-exhausted`なら既知の未解決findingをownerが受容する状態として区別する。`SCN-UNIT-REVINIT-023`〜`025`と`SCN-UNIT-DIAGHINT-005`で検証する。

- 受け入れ条件: AC-WF-014
- 根拠: Issue #1323（利用側改善要求A-2）
- 実装: `src/adapters/review-session.ts`、`src/cli.ts`、`src/cli-usage.ts`

### REQ-WF-015 Issue工程の機械導出欄を安全に生成する

`issue create`は件名、作成日時、作成者、正本URLの未同期状態と、利用projectが宣言したrole・review・test・UI/UX・privacy・observability・security・tokenの選択肢を00〜03の対応欄へ事前充填する。fullの01〜03は00と同じ件名・正本表記を持ち、利用者が同じ管理情報を繰り返し転記しなくてよい状態で生成する。project policy manifestを持たないfixtureと導入前projectでは従来どおり既定templateから生成できる。

`issue sync --generate-body --staging-path=<path> --checkpoint=<4|8>`は保存済みartifact inventoryとdigest、mode、checkpoint、projectのGherkin方言による検証結果から同期本文を決定論的に生成する。quick・pocのStep 4は00、fullのStep 4は00・01を区切り線で連結し、fullのStep 8は00を先頭に置いて01〜03を`<details>`の折りたたみ区画へ規定順で連結する（REQ-SQ-033）。`--body-file`経路は互換維持し、生成経路との併用を副作用前に拒否する。通常のfull Step 4は生成にstagingを使っても最終同期記録へ進めず、`local-active`を保持する。writer lock内で生成digestを再照合してからproviderへ送る。

Step 10のreview成果物は雛形を充填する方式を持たない。review証跡は`review export`が収束済みreview sessionから生成する（REQ-WF-038）。

- 受け入れ条件: AC-WF-015。各mode/checkpointの同期対象が規定集合と一致する。
- 根拠: Issue #1333、#1325
- 実装: `src/domain/issue.ts`、`src/cli.ts`、`src/cli-usage.ts`、`.agent-skill-chain/skills/step-04-issue-sync/SKILL.md`、`.agent-skill-chain/skills/step-08-design-sync/SKILL.md`、`.agent-skill-chain/skills/step-10-review/SKILL.md`

### REQ-WF-016 暗黙CLI入力契約を実行前に露出する

`workflow record`はStep 4・8の同期証拠に64桁のhex digestと`sync`語が必要であることをusageと該当Step skillで案内する。`pr create --evidence`は`spec.impact="no-spec-impact"`時の`spec.rationale`に対象範囲を限定した12文字以上の根拠が必要であることをusage・Step 11 skill・拒否診断で同じfield名により示す。

`review round`は入力JSON fileをstaging外へ置くことと、blocking findingの`contractId`をanchorのACまたはINVに一致させることをhelpとStep 10 skillで示す。`worktree finalize --complete --apply`のcleanup例はpreview digestを`--report-hash`と`--approved-digest`の両方へ渡し、`--complete`を伴わない`--apply`は`--report-hash`だけを検証する。これらの説明追加はvalidator、authority、保存形式、成功・拒否判定を変更しない。

- 受け入れ条件: AC-WF-016
- 根拠: Issue #1331
- 実装: `src/cli-usage.ts`、`src/domain/delivery.ts`、`.agent-skill-chain/skills/step-04-issue-sync/SKILL.md`、`.agent-skill-chain/skills/step-08-design-sync/SKILL.md`、`.agent-skill-chain/skills/step-10-review/SKILL.md`、`.agent-skill-chain/skills/step-11-pr/SKILL.md`

### REQ-WF-017 review証跡を構造とsessionに照合して事前検証する

`review validate --artifact=<path> [--staging=<staging>] [--root=<path>]`は、repository root内のsymlinkでない通常fileをno-follow descriptorへ固定してread-onlyで読み、review証跡（REQ-WF-038）として厳密に解析する。未知field、型違い、不正なSHA、`evidenceDigest`不一致、正規直列化とのbyte不一致、空でない未解決Critical/High、reviewerとimplementerの同一、検証欄の欠落と`H_impl`・影響集合digestへの束縛違反、v1の証跡を拒否する。`--staging`を指定した場合は保存済みreview session・trusted policyの独立性モード・Git観測（比較基点と`H_impl`がreview済み実装を指すこと、diff digestと影響集合の再計算値）・stagingの観測記録へ照合する。Markdownのreview成果物は検証対象ではない。`review validate --file=<json>`の従来評価は変更せず、証跡の検証成功はreview approvalまたはdelivery authorityを生成しない。

- 受け入れ条件: AC-WF-017。正規の証跡はvalidとなり、改竄・再整形は理由付きで拒否され、JSON入力経路は従来結果を維持しroot外・symlink入力を拒否する。
- 根拠: Issue #1332、REQ-WF-038
- 実装: `src/domain/review-evidence.ts`、`src/adapters/review-evidence.ts`、`src/cli.ts`、`src/cli-usage.ts`

### REQ-WF-018 Verification Set riskがlowの02/03短縮形式を検証する

`issue validate`は、02 §4.2・§8・§9または03 §5.2に`対象外: <理由>`形式またはUnicode類似コロンを使う類似形式が現れた場合、staging直下の`verification-input.json`を既存のstrict Verification Set parserで読み、riskが`low`かつ節本文が非空理由を持つ字下げなしの1行だけであるときに受理する。medium・high・critical、未知risk、入力欠落・不正、空理由、複数行、類似コロン、字下げcodeはfail-closedで拒否し、artifact・節・期待書式を診断する。risk入力はsymlinkでない1 MiB以下の通常fileをno-follow descriptorで上限付き読取し、名前付きfileとdescriptorの同一性および読取前後のmetadataを確認する。既存の詳細記述とQ-01〜Q-08のmode判定は変更しない。

- 受け入れ条件: AC-WF-018。lowの可視文字を含む理由付き1行は成功し、non-low・unknown・空理由・HTML commentまたはUnicode format文字だけの理由は失敗し、既存詳細形式と`対象外理由`・`対象外ではない`で始まる通常の日本語本文は成功する。risk入力は読取後も名前付きpathとdescriptorのidentityが一致しなければ失敗する。
- 根拠: Issue #1334
- 実装: `src/domain/issue.ts`

### REQ-WF-019 配布02/03 templateがlow限定の短縮契約と強制主体を示す

配布する`02_設計.md`は§4.2・§8・§9、`03_実装計画.md`は§5.2について、Verification Set riskが`low`の場合だけ`対象外: <理由>`の理由付き1行を選べることを示す。medium・high・criticalでは従来の詳細内容を維持し、強制主体として`issue validate`と`verification-input.json`を名指しする。

- 受け入れ条件: AC-WF-019。両templateの注記とruntime判定が同じrisk・書式・強制主体を持つ。
- 根拠: Issue #1334
- 実装: `.agent-skill-chain/templates/issue/02_設計.md`、`.agent-skill-chain/templates/issue/03_実装計画.md`、`src/domain/issue.ts`

### REQ-WF-023 起票時点と計画単位を規範とStep入口と雛形で示す

`.agent-skill-chain/docs/01_開発ワークフロー.md`の「起票時点と計画単位」を規律の単一正本とする。canonical Issueは着手を決めた時点で作り、着手を決めていない内部計画をcanonical implementation Issueにしない。**ここでいう「着手を決めた」は意思決定であって、Git commitの有無を指さない。**この導出は同文書が既に定める「耐久トラッカーがなければ別セッションからの再開を保証しない」から行い、新しい原理を持ち込まない。**対象外は外部から届いたbug報告、security事象、法令・契約上の期限、他チームからの依存要求の4種に限り、これらの即時起票を妨げない。**

計画単位は複数タスクのMVP、完了条件、対象外、依存を束ねる期間または成果のまとまりとする。**長さの既定は1週間とし、利用projectが変更できる。** 既定を短い側へ置く理由は、期間を延ばすほどその期間ぶんの未着手Issueを抱えるためである。変更は慣習ではなく完了所要時間の90パーセンタイルから決める。sprintの採用を前提にしない。**計画単位をIssueの分割単位に使わない。** 分割はREQ-WF-022が定める成果物の結合度で決める。

`.agent-skill-chain/skills/step-00-stage/SKILL.md`は当該節への相対linkを持ち、規律の本文を複製しない。`.agent-skill-chain/templates/planning/01_計画単位.md`はMVP、完了条件、対象外、タスク表、依存の5欄を持ち、進捗・担当・優先度の欄を持たない。**本要件は強制点を追加しない。** 起票時点を検査するgate、CLI、schemaのいずれも設けず、本要件が追加したdirectory以外について、既存の検査の判定基準・CLI終了値・authorityを変更しない。新directoryの入口登録のため`scripts/check_directory_guides.ts`の`ENTRY_DOCUMENTS`へ1 entryを加えるが、既存directoryの判定結果は変わらない。

- 受け入れ条件: AC-WF-023。規範文書が4記述（起票条件と導出、対象外の列挙、既定1週間と変更可能性、分割単位に使わないことと結合度への参照）を持ち、Step 0 skillのlinkが解決し、計画単位templateが5欄を持ち、新directoryが導入先へ配置されて診断が入口不足を報告しない。**単一正本であることは複製の不在だけでは守れないため、下流の言い換えが起票条件をGit commitへ結び付けていないことも検査する。**
- 根拠: Issue #1398
- 実装: `.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/skills/step-00-stage/SKILL.md`、`.agent-skill-chain/templates/planning/00_利用案内.md`、`.agent-skill-chain/templates/planning/01_計画単位.md`、`scripts/check_directory_guides.ts`

### REQ-WF-024 staging digest不一致の診断が実在の復旧手順を返す

staging digest不一致の診断は、**何が変わったか**と、journalの記録状態で**実際に成立する**次の行動を返す。記録済み成果物一覧に対する追加・削除のpathと、計画封印（REQ-WF-036）のfile単位digestと比べて変化した計画文書を名指しする。記録は集合digestしか持たないため、それ以外の内容変化は「いずれかのfile」と述べ、推測で名指ししない。封印済み計画が変化していれば、封印時の内容へ戻し計画の変更を`05_計画変更.md`へ追記するよう案内する。

次の行動は記録状態で分岐する。journalにStep 11 entryがあるか、delivery stateが`merge-observed`または`step11-recorded`なら、digestを再固定できないため内容を戻す手順を返す。`recordStep11`は`merge-observed`からしか遷移しないため、merge観測とStep 11記録の間に必ず窓が開き、journalのStep集合だけを見ると見落とす。Step 10記録済みなら、収束済みsessionが前進commitの無い追加roundを拒否するため、前進commitを伴う新しいreview roundだけが再固定し、stagingだけの変更は戻すよう返す。Step 9記録済みなら`review round`が現在の成果物へdigestを再固定する。それより前は最新Stepの再記録を案内する。**上流再確定（`--reconfirm`）は案内しない。**

案内の生成は判定を持たない純関数が所有し、3箇所の診断（delivery直前検査、再配送直前検査、review session更新前検査）が同じ関数へ委譲する。**staging記録、journal、delivery stateは互いに独立に読み、1つの読み取り失敗で他の観測値を捨てない。** **判定結果、受理集合、CLI終了値を変更しない。** 案内文にはstagingの相対pathとcommand名以外を含めず、環境変数・token・絶対pathを出さない。

- 受け入れ条件: AC-WF-024。診断が追加・削除した成果物と封印後に変化した計画文書を名指しし、封印済み計画の変化では`05_計画変更.md`を案内し、Step 9記録後はreview roundによる再固定、Step 10記録後は前進commitを伴うreview roundか内容の復元、terminal状態（journalのStep 11またはdelivery state）では内容の復元を返し、`--reconfirm`を案内せず、delivery直前検査とreview session更新前検査の診断へ届き、delivery stateを読めなくてもjournalのStep 11で終端案内を返す。
- 根拠: Issue #1312、REQ-WF-036
- 実装: `src/domain/plan-seal.ts`、`src/adapters/workflow-journal.ts`、`src/adapters/review-session.ts`、`src/cli.ts`

### REQ-WF-025 stagingの配置をproject policyで版管理下へ置ける

project policyの`staging`節（`root`、`tracked`、`issueBody`）がIssue stagingの配置を決める。既定は版管理外の`.agent-skill-chain/tmp/issues`で、従来の全commandの振る舞いを変えない。`root`はrepository相対のdirectoryで、`*`は1 segmentに一致する（例: `docs/05_スプリント/*/tasks`）。最大16 segmentとし、`.`または`..`と等しいsegment、Cc/Cf制御文字、末尾`*`、絶対path、`.git`、`\`を含むrootと、rootを持たない`tracked=true`はschemaとruntimeの双方で拒否する。

`issue create`は`--staging-root=<親directory>`と`--name=<directory名>`を受け、`*`を含むrootでは両方を明示させ、patternに一致しない親を拒否する。`tracked=true`のstagingには生成時に機械記録（`journal/`、`staging-record.json`、`review-session*.json`、昇格transaction、`00_モード判定.json`、`verification-input.json`）だけを除外する`.gitignore`を置き、計画文書（00〜03と`05_計画変更.md`）のMarkdownは版管理する。tracked設定にかかわらずstaging内のfileはreview証跡ではなく、review証跡は`review export`が`docs/reviews/`へ生成してevidence-only suffixとして固定する（REQ-WF-038）。stagingを引数にとる全command（`workflow record`、`review round`、`review export`、`verify run`、`issue sync`、`pr create`、`pr merge`等）は、Step 0からPR・mergeまで同じpolicy解決済みstaging root直下を要求し、repository rootをstagingの祖先にある`project-policy.json`から導く（既定配置は従来どおり4階層上）。Step 9の候補worktree清浄検査は`:(exclude,glob)<root>/**`でstaging配下を除外する。

`issueBody=pointer`のprojectでは、`issue sync --generate-body`の本文を成果物全文の連結ではなく、00の表題、成果物のrepository相対pathとSHA-256の表、00の目的節と受け入れ条件節だけにする。digestの算出、read-back照合、`sync-verified`の判定は変えない。`issueBody=full`（既定）は従来の連結・折りたたみ形を維持する。`issueBody=pointer`は`tracked=true`（版管理下のroot）を宣言した場合だけ許す（REQ-WF-037）。

review roundの記録は、staging成果物の一覧またはdigestが保存記録と異なっても拒否せず、現在値をstaging記録へ再固定してから判定する。reviewが固定するのはcandidate HEADであり、Issue同期との一致は`pr create`が引き続きjournalの同期証拠で検証する。`review round --init`はsessionがあるとき、前round blockerのfindingをID・severity・relation・path・contractIdごと雛形へ写し、reviewerは`status`の更新と新規findingの追加だけを書く。

**同じ00〜03を一時staging・Issue本文・利用project側のtask記録の3箇所へ複製する運用を不要にする。** 利用projectの実測（2026-09-16、42 Issue）では、1 Issueあたりの出力の2/3がこの複製で、複製をdigestで拘束するcommitが全commitの2割を占めていた。

- 受け入れ条件: AC-WF-025。staging節が無ければ既定配置と全文同期で従来と同じ結果になり、不正な節は拒否され、`*`を含むrootでは`--staging-root`の明示と一致検査が働き、版管理下のstagingは文書だけをgitに見せ、版管理下のstagingからrepository rootを導けて配置検査が受理し、pointer本文は配置・digest・目的・受け入れ条件だけを含み、artifact雛形は種別から導ける列だけを埋めて判定列を未確定に保つ。
- 根拠: 利用project doepionの実測（2026-09-16）、Issue #1230（所要時間の短縮）、Issue #1416
- 実装: `src/domain/staging-layout.ts`、`src/domain/issue.ts`、`src/domain/policy.ts`、`src/adapters/workflow-journal.ts`、`src/adapters/review-session.ts`、`src/cli.ts`、`src/cli-usage.ts`、`.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/skills/step-00-stage/SKILL.md`、`.agent-skill-chain/skills/step-04-issue-sync/SKILL.md`、`.agent-skill-chain/skills/step-09-implement/SKILL.md`、`.agent-skill-chain/skills/step-10-review/SKILL.md`

### REQ-WF-010 正しさと開発速度の両立を命題として保持する

`.agent-skill-chain/docs/00_運用ポリシー.md`の「目的と成立条件」を命題の単一正本とする。正しい成果物をAIで速く作ることを目的とする。ここでいう正しさは、要求を満たし品質、セキュリティ、再利用性、保守性を担保していることであり、検査が緑であることは必要条件であって十分条件ではない。ここでいう速さは開発速度、すなわち要求から成果物を提出するまでに要する時間の短さであり、成果物自体の実行性能を指さない。正しさと開発速度の双方を成立条件として扱う。policy enforcement、gate、reviewは正しさを担保する手段であり、手段が開発速度を損なうときは手段の側を縮小する。

開発速度の観測は、支援層の所要時間が成果物構築の所要時間を上回らないこと、同一scopeのreviewラウンドが有限で予算内に収束すること、手段の追加より既存手段の縮小を先に評価することの3点とする。正しさの観測は`02_品質基準.md`が所有する。

この節は安全条件、authority分離、fail-closed不変条件を緩和せず、開発速度を理由に必要な検証を省略する根拠にしない。成果物の実行性能は非機能要件が所有する。`AGENTS.md`と`01_開発ワークフロー.md`は命題を複製せず参照だけを持つ。

- 受け入れ条件: AC-WF-010
- 根拠: Issue #949
- 実装: `.agent-skill-chain/docs/00_運用ポリシー.md`、`.agent-skill-chain/docs/01_開発ワークフロー.md`、`AGENTS.md`

### REQ-WF-020 次の1 Stepを検証・記録するworkflow advanceを提供する

`workflow advance --staging=<path>`は保存済みjournalから現在Stepと次Stepを導出し、既定では副作用のないpreviewを返す。同期previewは対象repository・Issue・checkpointと生成本文digestを表示する。`--apply`はStep 1〜3・5〜9について、その時点で成立する`issue validate`のstage検査と既存journal検査を行い、次Stepに対応する成果物だけを1件のjournalへ記録する。Step 1は00要求成果物だけ、Step 5は01要件定義と02設計、Step 6以降は01要件定義から03実装計画までを検査し、工程境界ごとの成立可否を独立に判定する。Step 9は候補worktree全体がcleanであることを要求し、成果物が現在HEADで追跡済みの通常pathであることと、検証前後・journal確定直前のHEAD一致を照合する。検証したHEADはjournalの構造化した`implementationHeadSha`へ固定し、PoC観測bindingとの合成後も保持する。最新Step 9にこの拘束がない旧journalはreviewへ委譲せず、current HEADでのStep 9再記録を案内する。初回`review round`のcandidate HEADが一致しなければ拒否して後続のexact HEAD reviewと同一内容へ拘束する。Step 4・8ではmode/checkpoint別の同期本文をmarkerで囲み、既存Issue本文の領域外にあるチェックリストや進捗記録を保全する。同期applyは直前のpreviewが表示した本文digestを`--expected-body-sha256`で再提示させ、直接applyまたはpreview後の本文driftを外部書込み前に拒否する。入力時刻を含む全local入力と、昇格中なら固定済みtrackerとの一致を先に検証してから、既存GitHub adapterの認可・書込後読み戻しを通した後だけ、実測digestを含む同期EvidenceとStep記録を生成する。利用者が前Step後に編集した成果物は保存済みcontent digestとの差だけを理由に拒否せず、初期previewで観測した成果物一覧とcontent digest、保存済みstaging record、journal、同期本文、remote本文digestを保持する。writer lock取得後の検証前後で成果物が同一の場合だけ、journal transaction内で保存digestを検証済み内容へ更新し、atomic publish直前まで同じdigestを再観測できなければ記録を拒否する。Issue本文の公開後にlocal記録が失敗した場合はtracker、本文digest、journal transaction状態を診断へ含める。再実行時にremote本文が同じなら外部同期を重複させず、local記録だけを復旧して`journal-recovered`を返す。未公開または公開状態を確認できない失敗は新しいpreviewから再開する。

Step 10・11はそれぞれ独立reviewとdeliveryの専用commandを案内して停止し、advanceがreview判定、PR作成、merge、release、cleanupを実行しない。未知state、不正path、成果物不備、認可欠落、provider読み戻し不一致は成功または完了記録へ倒さない。`--dry-run`と`--apply`の同時指定、および同期成果物内のworkflow advance予約markerは外部書込み前に拒否する。既存の`workflow record`、`issue sync`、review、delivery契約は維持する。

- 受け入れ条件: AC-WF-020。previewはjournalを変更せず、同期対象と本文digestを表示する。local applyは検証済みの次Stepを1件だけ記録し、未完成のStep 1要求成果物またはStep 5設計成果物は記録しない。Step 4同期は不正な入力時刻を外部書込み前に拒否し、成功時は書込後読み戻しdigestをEvidenceへ含める。Step 10は無変更でreviewへ委譲する。
- 根拠: Issue #1335
- 実装: `src/domain/workflow.ts`、`src/cli.ts`、`src/cli-usage.ts`

### REQ-WF-021 review入力を固定したまま進捗証跡だけを並行更新する

配布03の完全なHTML commentであるparallel progress markerは、Issueの未解決placeholderとして拒否しない。validationは元の03のbytesを変更せず、合格後も同じmarkerからparallel progress evidence（TERM-ASC-107）のinventoryを生成できる。placeholder走査のcomment境界はREQ-SQ-008が所有し、markerの字面・意味とprojectionの契約は維持する。

`review round --init`は、03実装計画にparallel progress markerが正確に1組ある場合だけ、対象path、baseline・prefix・suffixのSHA-256、mode 100644、宣言済みtask IDをoptional inventoryとしてreview session anchorへ固定する。`review progress`は同じsession IDとH_implへ結び付く閉じたtask/state/UTCだけを、最大256件・256 KiBの専用JSONLへdigest chainで追記する。専用journalだけを一般staging成果物digestから除外し、任意path・任意section・自由記述は除外しない。

projectionはmarker内だけをpure functionでread-only表示し、review入力treeへ書き込まない。review、test、PR、merge、releaseのgateは専用journalもprojectionも読まず、progressの失敗を拒否理由にしない。progress操作自身はbaseline、file identity・mode、session・H_impl、journal chainを再検証し、inventory欠落、HEAD移動、改変、競合、上限超過ではその操作だけを停止して既存の直列経路を維持する。

**inventoryを構築できないことは`review round`の拒否理由にならない。** markerが1組ある03に対してinventoryを構築できない場合、`review round --init`はinventoryを付けずにroundを開き、round番号、予算消費、収束判定、およびreviewを拘束するanchor（scope ID、Acceptance Criteria ID、domain invariant ID、diff基点、初回HEAD、初回diff digest）、candidate HEAD、focus、findingsのいずれも変えない。**anchorが成立時と違ってよいのはoptionalなprogress inventory keyの有無だけである。** round digestはanchorを含まないため不変であり、anchorから導くreview session IDだけがこのkeyの有無で変わる。 不成立として扱う分類はmode不一致、通常fileでない、markerが正確に1組でない、宣言済みtask IDが0件の閉じた集合とし、**それ以外の失敗は従来どおり拒否へ伝播させる。** 分類を例外捕捉で束ねると、分類外のI/O失敗やpath identity異常まで同じ枝で握り潰されるため、構築は成否を判別可能な値で返す。**宣言済みtargetが複数ある場合も同じ扱いとし、判定はall-or-nothingとする。** inventoryはprefix/suffix digestと宣言済みtask IDの集合を一体で固定する契約であるため、成立したtargetだけを取り込むと宣言したのに検証されないtargetが残る。いずれか1件でも不成立なら`review round --init`はinventoryを付けずにroundを開き、案内は不成立になったtargetのstaging相対nameを示す。target件数・重複・辞書順の違反は宣言の誤りであり、file状態の不成立とは区別して従来どおり拒否へ伝播させる。**この契約を回帰として検出するのは`SCN-INT-PROGRESS-040`である。**

不成立時は、分類、実測mode、期待mode `100644`、対象のstaging相対name、採る行動、必要authority、rollbackを返す。**必要authorityは分類ごとに分ける。** mode変更権限で全分類を代表させると、marker是正やtask ID追加や通常fileへの置換に必要な権限を誤って案内する。**案内を載せる`notes`の枠自体はREQ-WF-014が所有する。**

**対象が通常fileでない場合は`chmod`を案内しない。** symlinkの`lstat` modeはLinuxで`0o777`であり、mode不一致として扱うとlink先を書き換える案内になる。案内はstaging相対nameとcommand名だけを含み、絶対path、環境変数、tokenを含めない。

**stagingへ`03_実装計画.md`を生成する経路は、そのmodeを`100644`とし、実行環境のumaskとtemplateのon-disk modeに依存させない。** 対象は`issue create`の生成と`workflow promote-full`の昇格補完の両方であり、**片方だけを固定しない。** 当該03はどちらも配布templateの複製として作られ、複製は元fileのmodeをそのまま引き継ぐ。そのmodeはcheckoutまたは展開時の`0666 & ~umask`で決まるため、固定しなければ上のinventory契約と食い違う。**生成時にmodeを指定して書くだけでは足りない。** file作成時のmodeはumaskでmaskされるため、生成後にmodeを設定する操作でなければ独立しない。**この固定に失敗しても生成・昇格を拒否しない。** modeを保持しないfilesystem（chmodが`EPERM`・`ENOTSUP`を返すもの）で唯一の生成経路を塞がないためであり、その場合は上の非停止と案内が受け止める。

- 受け入れ条件: AC-WF-021。正規marker付きの記入済みfull Issueがvalidationに合格し、03のbytesを保持したまま宣言済みtask IDのinventoryを初回review roundのanchorへbindingでき、review previewが同じinventoryを受理する。**あわせて、inventoryを構築できない03を持つstagingでも`review round --init`がroundを開いてanchorにinventoryを持たず、round recordの差がinventory keyの有無だけであり、分類外の失敗は従来どおり拒否され、不成立の案内が分類・実測mode・期待mode・相対name・行動・authority・rollbackを持ち、通常fileでない対象へ`chmod`を案内せず、**複数の宣言済みtargetのうち1件が不成立でもroundが開いて案内が当該targetのnameを示し、**`issue create`と`workflow promote-full`が生成する03のmodeが`umask 0002`と`0077`およびtemplateのon-disk mode `0664`のいずれでも`100644`になる。**
- 根拠: Issue #1336、#1370、#1408
- 実装: `src/domain/review-progress.ts`、`src/adapters/review-progress.ts`、`src/adapters/review-session.ts`、`src/adapters/workflow-journal.ts`、`src/domain/issue.ts`、`src/cli.ts`

### REQ-WF-022 成果物の結合度と非停止warningで分割判断を支援する

1 Issueを分割するかは所要時間ではなく成果物の結合度で決め、独立に完成・review・rollbackできる成果物1単位を目安とする。片方だけを独立に切り戻す必要が生じうる、reviewの担当者または観点が異なる、片方を他方より先にmergeする必要がある、片方の失敗で他方の成果を巻き戻す場合は分割候補とする。45分という見積りは結合度を再確認する補助指標に限り、長いだけでは分割しない。分割しない判断も、成果物が密結合で一体としてreview・merge・rollbackすべき場合の正しい選択肢である。

分割にはIssueごとのStep 0〜11の記録、PRごとの外部reviewの往復、既定branch追随ごとのStep 9再記録とreview round、有限review予算（同一scope最大6回、収束後の取り直し2回、通算8回）の枠を各々消費する固定費がある。1 Issueを2つに分ければこれらの実行単位も2つになり、費用ゼロではない。利用projectの観測値ではStep 0から最終entryまでの中央値がquick約7分、full約30分であり、環境依存の参考値であって分割閾値ではない。modeごとに固定費は異なり、fullは00〜03を個別管理するため大きく、quick/pocは集約00により小さい。したがって全modeへ共通の時間閾値を適用せず、各modeの工程費用と分割による独立性の便益を比較する。

fullの00 §2.1直下に任意の`- [成果物:<kind>] 説明` markerを置ける。`kind`は`adr`、`contract`、`feature`、`documentation`、`migration`の閉じた集合とし、top-levelかつcode fence外の有効markerが2件以上なら`issue validate`は`ASC-ISSUE-ARTIFACT-UNIT-001`を1件返す。同じkindが2件でもcountは2とする。他section、入れ子、未知kind、説明なし、code fence内を数えず、自由文のkeywordから推測しない。説明にinline codeを含むmarkerは有効とする。

warningは`code`、`count`、出現順に依存しない閉集合順の`kinds`、日本語`message`だけを持ち、入力本文を複製しない。`warnings`は常に配列で返し、既存の`valid`、`errors`、`mode`、`blockedOperations`、CLI終了値、安全gate、authorityを変更しない。marker無しの既存成果物とquick/pocの集約形式は従来どおり有効であり、quick/pocはmarker warningの判定対象外とする。配布するfull/quick/poc templateとCLI helpは結合度による成果物1単位、45分は補助指標であること、modeごとの固定費、fullだけに適用するmarkerの閉集合、warningの非停止性を案内する。

- 受け入れ条件: AC-WF-022。fullの異種または同種の有効marker 2件以上はwarning 1件となり、0/1件、malformed、nested、他section、fence内はwarningなしとなる。warningの有無でvalid・errors・mode・blockedOperations・終了値は変化せず、workflow、3 template、helpがfull適用・quick/poc非適用を含む同じ契約を示す。
- 根拠: Issue #1337、Issue #1369
- 実装: `src/domain/issue.ts`、`src/cli.ts`、`src/cli-usage.ts`、`.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/templates/issue/00_要求定義_full.md`、`.agent-skill-chain/templates/issue/00_要求定義_quick.md`、`.agent-skill-chain/templates/issue/00_要求定義_poc.md`

### REQ-WF-029 有限選択判断のDecision Contractと候補一覧で呼び出し元を名指しする

「この判断は有限選択（deterministic codeでもGenerative Modelの深い推論でもない判断）として切り出せるか」を1つの型（`DecisionContract`）と1つのチェックリスト（`DecisionCandidateEntry`の採否・除外理由）で機械的に判定できる状態を提供する。`DecisionContract`は判断結果の値・確信度（confidence）・呼び出し先（`DecisionCallableTarget`）を表現するが、fail-open方向（quick化・tier降格・Gate PASS判定等）の呼び出し先は型として存在せず、実際にrouting・authorityへ組み込む実行ロジックは持たない。

候補は`DECISION_CANDIDATES`定数配列で管理し、disposition（採用・除外）別にTypeScriptのdiscriminated unionで必須fieldを分ける。採用（`disposition: "adopted"`）候補は判断箇所（`decisionSiteFile`/`decisionSiteLine`）と呼び出し元（`callerFile`/`callerLine`）をfile:line形式で必須とし、呼び出し元を名指しできない候補は採用できない。除外（`disposition: "excluded"`）候補は除外理由（`exclusionReason`）を必須とし、理由なしの除外を許さない。

判断結果と呼び出し先をjournalへ記録するための`DecisionJournalField`型（`decisionRecordId`・`value`・`confidence`・`callableTarget`・`decidedAt`）を設計し、既存journal schema（`workflow-step-journal.schema.json`・`routing-evidence.schema.json`・`review-progress-record.schema.json`・`delivery-state.schema.json`）のproperty名と衝突しないことを機械確認する。**`recordedAt`は使わない。** いずれの既存schemaも既に`recordedAt`を持ち、SCN-UNIT-DC-004の初回実行で実際に衝突を検出したため`decidedAt`へ変更した（実装中発見DISC-001）。実際のjournal書き込みロジックとschema fileの変更はこの要件のscopeに含まない。

- 受け入れ条件: AC-WF-029。`DecisionContract`がvalue・confidence・callableTargetの3fieldを持ち、callableTargetへfail-open方向の文字列リテラルを代入するとTypeScript型検査がcompile errorを報告する。`DECISION_CANDIDATES`の採用候補が5件以上あり、各候補の判断箇所・呼び出し元がfile:line形式で実在する。除外候補は全件exclusionReasonが空でない。`DecisionJournalField`のfield名（`decisionRecordId`・`value`・`confidence`・`callableTarget`・`decidedAt`）が既存journal schema 4fileのproperty名と1件も一致しない。
- 根拠: Issue #1483
- 実装: `src/domain/decision-contract.ts`

### REQ-WF-026 role・model・deterministic tool境界の計測イベントを記録できる

`journal/steps.jsonl`のStep完了記録だけでは、role切替・model呼び出し・deterministic tool呼び出しの境界を再構成できない（compiled codeがこれらの境界を自動観測する経路が無いため）。進行役が`workflow mark --staging --kind=<role｜model｜deterministic> --phase=<start｜end> --label`を明示発行すると、`.agent-skill-chain/metrics/<staging名>/events.jsonl`（staging digestの対象外領域。stagingのdigest inventoryがほぼ全fileを対象にするため、staging配下へ置くとworkflow markのたびに既存digest一致検査を壊す）へ1行追記される。各kindは独立にidle/openの2状態を持ち、同一kindの二重start、idle状態でのend、開区間とlabelが一致しないend、時間逆行するendはfail-closedで拒否する。既存`journal/steps.jsonl`・`review-session*.json`のschemaは変更しない。

- 受け入れ条件: AC-WF-026。`workflow mark`はkind・phase・labelの列挙値検証と状態遷移検証（同一kindの二重start拒否、idle状態でのend拒否）を行い、既存journalのschemaに影響しない。
- 根拠: Issue #1482
- 実装: `src/domain/metrics.ts`、`src/adapters/metrics-journal.ts`、`src/cli.ts`、`src/cli-usage.ts`

### REQ-WF-027 Step/role/model/deterministic tool/reviewの所要時間内訳を算出できる

`workflow metrics --staging`は、`journal/steps.jsonl`（既存`recordedAt`の行順差分。同じStep番号の再記録・reconfirmを縮約せず配列で返す）から`step_ms`を、`.agent-skill-chain/metrics/<staging名>/events.jsonl`（REQ-WF-026）から`role_ms`・`model_ms`・`deterministic_ms`（kind別合計）とlabel別内訳を、任意の`--review-session`から`review_rounds`（`countedRounds`と同じ`followOnly`・`recordLayerOnly`除外定義）を算出する。`role=implementer`のrole_ms合計を`artifact_build_ms`とし、計測window（`journal/steps.jsonl`とevent logを合わせた集合の最古〜最新）全体からそれを差し引いた値を`support_ms`とする。event logが無い、または不正な行を含む場合はrole_ms等をfail-closedでunavailable（null）とし、role区間が開いたままの場合はartifact_build_ms・support_msもunavailableとする。`--out`指定時は`.agent-skill-chain/metrics/<staging名>.json`（既存の予約済みgitignore対象runtime領域）へも書き込む。

- 受け入れ条件: AC-WF-027。step_ms・role_ms・model_ms・deterministic_ms・review_rounds・support_ms・artifact_build_msをJSONで返し、入力file欠落時は個別fieldをunavailableとして報告して他の算出を継続する。
- 根拠: Issue #1482
- 実装: `src/domain/metrics.ts`、`src/adapters/metrics-journal.ts`、`src/cli.ts`、`src/cli-usage.ts`

### REQ-WF-028 個人ローカルのJev provider設定を安全に読み込む

開発者が個人のGit管理外設定`.agent-skill-chain/local/jev-provider.json`とAPIキーを保持する環境変数を用意した場合だけ、Jev（Decision Provider）向けのconfigを読み込める。`enabled: true`かつ`apiKeyEnvVar`で指定した環境変数がprocess環境に設定されている場合だけ有効なconfigを返し、file不在・JSON不正・schema不正（未知key含む）・`enabled !== true`・指定env var未設定のいずれでも例外を投げず`undefined`を返す（TERM-ASC-1484、INV-1484-01〜04）。`apiKeyEnvVar`は`JEV_`で始まる英大文字・数字・`_`に限る（無関係な秘密、例えば`GITHUB_TOKEN`をBearer tokenとして送らせない）。`resolveJevProviderConfig`は、設定fileがそのGit repositoryで追跡されている場合は内容が有効でも`invalid`として読み込まない（clone元が内容を選べる設定を個人設定として扱わない）。

生のAPIキー値は`.agent-skill-chain/local/`配下のどのfileにも書かれず、戻り値のconfigオブジェクトにも含まれない。値は環境変数からの存在確認にのみ使う。この機構は`modelMapping.roles.reviewer`（Issue #1425、git管理下・project全体共有）やtrusted project policy（`project-policy.json`等）のいずれも読み込まず、それらの判断へ影響しない。既存の個人ローカルoptional provider設定の前例である`supplemental-review-config.ts`（Issue #1428、TERM-ASC-124、INV-1428-03・04）と同型の構造（`ALLOWED_FIELDS`方式、早期return、単一try/catch）で実装する。

本要件はJev providerのconfig読み込みだけを扱い、実際のJev APIへのHTTPクライアント実装、`provider-capability-mapping`（v2）へのJev登録、APIキーの発行・失効・ローテーション手順は対象外とする（Issue #1484）。

- 受け入れ条件: AC-WF-028。`.agent-skill-chain/local/jev-provider.json`が存在し`enabled: true`かつ指定env var設定済みの場合だけ有効なconfigを返し、file不在・JSON不正・schema不正・`enabled !== true`・env var未設定の5パターン全てで例外を投げず`undefined`を返す。`JEV_`で始まらない`apiKeyEnvVar`は`invalid`となり、Gitで追跡された設定fileは`resolveJevProviderConfig`が`invalid`として拒否する。生のAPIキー値がいかなるfileにも書かれず、`modelMapping`・trusted project policyを一切読まない。
- 根拠: Issue #1484
- 実装: `src/domain/jev-provider-config.ts`、`.agent-skill-chain/schemas/jev-provider-config.schema.json`

### REQ-WF-030 Decision Type Registryがdeterministic resolverを先に試し、無ければauthorityModeに応じてproviderへ安全に委譲する

BR-01（呼び出し元を名指しできない）を理由にDecision Contract（Issue #1483）の採用候補から除外されていたDCAND-006（finding分類記入）・DCAND-008（CodeRabbit利用枠制限）・DCAND-009（reviewer選定）・DCAND-010（軽微な矛盾3条件）を、`agent-skill-chain decision invoke --type=<id> --input=<file> --staging=<staging> [--apply]`というDecision Skill自身をBR-01が要求する呼び出し元として新設し採用する。DCAND-001〜003・005（`detectQuickDisqualifiers`・`inspectCiDelivery`・`requiresSpecUpdate`・`hasConcreteDecisionText`）はdeterministic resolverとして統合するが、既存呼び出し元（`issue.ts`・`cli.ts`・`policy.ts`）はresolverを経由せず既存関数を直接呼び続け、挙動を変えない。DCAND-004（`auditRowDraft`）はREQ-WF-038でMarkdown監査表ごと廃止した。

判断の提案（`proposedValue`）が有効値（`effectiveValue`）へどこまで自動反映されるかは`DecisionAuthorityMode`（`authoritative`・`advisory`・`one-way-escalation`・`constrained-choice`）が決める。`authoritative`は提案をそのまま反映する。`advisory`は`confirmedBy`（進行役の明示確認）が無ければ`effectiveValue`を`null`に留める。`one-way-escalation`は安全側（fail-closed）方向の提案だけを確認なしで反映し、緩和方向は`advisory`と同じ確認待ちにする。`constrained-choice`（DCAND-009）は`payload.candidateSet`の宣言を無条件に信頼せず、`decision-invoke.ts`がPolicy Allowed（`role.ts`の`PROVIDER_AUTONOMOUS_CEILINGS`。`codex`・`claude`）との積集合へ絞り込んだ上で、その集合外を`rejected`として拒否する。Configured/Dispatchable・Independence Eligibleの絞り込みは未実装であり、disclosed residual gapとして扱う（round 2の独立reviewで検出）。DCAND-006は`advisory`、DCAND-009は`constrained-choice`、DCAND-010は`one-way-escalation`（安全側`"not-minor"`）に固定する。DCAND-008は`resolveDcand008`（deterministic）が対象PR最新HEADに対する明示的なrate limit観測を確認できた場合だけ`confirmed-limited`/`no-limit-evidence`を`authoritative`に確定し、確認できない場合は`unknown`として`advisory`でproviderへ委譲する。この設計により、Providerの誤判定が`findingAdmission`（`review-convergence.ts`）のfail-open経路（Critical/High誤判定がrecord-onlyへ落ちる）を新たに開かない。

既定providerは`lightweight-tier`（自己申告。呼び出し側が計算した`proposedValue`をそのまま受け取る、追加の外部通信を持たない委譲先）であり、Jevは本要件のscopeでは実HTTP通信を持たずdispatchできない（Issue #1486以降）。`decision invoke`の出力は常に`workspace`（`activeRoot`/`primaryRoot`）と`jevProviderConfig`（`LocalConfigResolution`の4状態。値そのものは含まない）を含み、Jev config設定の有無・有効性・出所を消費側が独自スクリプトなしで確認できる。`--apply`時は`candidateHeadSha`が実行時の実HEADと一致することを検証したうえで（自己申告SHAを拒否する）、`DecisionJournalRecord`（`decisionRecordId`・`decisionTypeId`・`inputDigest`・`subjectRef`・`candidateHeadSha`・`executor`・`providerModel`・`providerVersion`・`proposedValue`・`effectiveValue`・`authorityMode`・`adjudicationReason`・`latencyMs`・`cost`・`decidedAt`）を`<primaryRoot>/.agent-skill-chain/runtime/decisions/<staging名>/events.jsonl`へ追記する（`primaryRoot`固定はREQ-WF-032が持つ`resolveGitWorkspace`を再利用する。Issue #1482のmetrics journalが`stagingRepositoryRoot`＝active worktreeへ書いていた設計の教訓を反映し、worktree削除でjournalが失われないようにする）。

- 受け入れ条件: AC-WF-030。DCAND-001〜003・005の既存呼び出し元のtestが全て変更前と同じ結果を返す。DCAND-006/008/009/010がそれぞれ設計正本の表が定める`executor`/`authorityMode`で応答する。`advisory`は`confirmedBy`無しで`effectiveValue=null`を返し、`one-way-escalation`は安全側方向だけ確認なしで反映し、`constrained-choice`は候補集合外を`rejected`にする。`--apply`は自己申告`candidateHeadSha`が実HEADと不一致だと拒否し、書き込んだ`DecisionJournalRecord`を同じ`primaryRoot`から読み返せる。
- 根拠: Issue #1485
- 実装: `src/domain/decision-types.ts`、`src/domain/decision-resolvers.ts`、`src/domain/decision-authority.ts`、`src/domain/decision-journal.ts`、`src/adapters/decision-invoke.ts`、`src/adapters/decision-journal-store.ts`、`src/cli.ts`、`src/cli-usage.ts`

### REQ-WF-031 Step 10 review roundが`decisionRef`を機械検証し、type・candidateHeadSha・inputDigest・provider versionの不一致を拒否する

DCAND-006（finding分類記入）をDecision Skill経由で行った場合、findingの`decisionRef`（`decisionRecordId`）欄へ記録するが、これが「SKILL.mdに書くだけ」の規約に留まらず「呼ばないと次のcompiled commandが通らない」機械強制になるよう、`review round`（`previewReviewRound`）が`decisionRef !== null`のfinding全件を検証する。検証は`decisionRef`が指すjournal記録が見つからない場合（欠落）、記録の`decisionTypeId`が`"DCAND-006"`と異なる場合（type不一致）、記録の`candidateHeadSha`がroundの`candidateHeadSha`と異なる場合（candidateHeadSha不一致）、finding自身の`{id, path, evidence}`から再計算した`inputDigest`が記録の`inputDigest`と異なる場合（inputDigest不一致、finding内容の事後書き換えを検出する）、または記録の`providerVersion`が現在のprovider設定から再計算した値と異なる場合（provider version期限切れ）のいずれかで例外を投げ、round全体を成立させない（fail-closed）。`decisionRef === null`（人・進行役が直接分類した場合）は検証しない。この検証は`findingAdmission`本体の既存fail-closed判定を緩めない。

- 受け入れ条件: AC-WF-031。有効な`decisionRef`を持つfindingはroundを通過する。存在しない`decisionRef`、別HEADで記録された`decisionRef`、finding内容書き換え後の`decisionRef`はそれぞれ対応する理由でroundを拒否する。
- 根拠: Issue #1485
- 実装: `src/domain/review-convergence.ts`、`src/adapters/review-session.ts`、`src/domain/decision-journal.ts`、`src/adapters/decision-journal-store.ts`

### REQ-WF-032 Git worktree境界を一般化して解決し、個人ローカル設定をactive worktreeから存在しない場合だけprimary worktreeへ安全にfallbackする

`resolveReviewRoot`（Issue #1428）が個別に持っていたGit worktree境界検証を`resolveGitWorkspace(root): {activeRoot, primaryRoot}`として一般化し、`resolveReviewRoot`はこれを呼ぶ薄いwrapperにする（既存呼び出し元の挙動は変えない）。個人ローカル設定（Jev provider config等）の継承は、`LocalConfigResolution<T>`という4状態（`absent`／`enabled`＋`source`／`disabled`＋`source`／`invalid`＋`source`・`reason`）で表現し、`resolveLocalConfigWithWorkspaceFallback`が`classify`関数（例: `classifyJevProviderConfig`。git・fsに一切触れない単一rootのpure classifier）をactive rootへ適用し、`absent`のときだけ`peekPrimaryReviewRoot`の安価な判定を経て`resolveGitWorkspace`でGit境界を確認したうえでprimary rootへ適用する。`disabled`・`invalid`はfallbackしない（個人が明示的に無効化・記入した設定を、worktreeを越えて無視しない）。`loadJevProviderConfig`（Issue #1484、`{root, configPath} → JevProviderConfig | undefined`）の既存signature・8scenarioの外部から見た挙動は変えず、`classifyJevProviderConfig`（`absent`/`enabled`/`disabled`/`invalid`を区別する内部分類器）の薄いwrapperとして再実装する。

- 受け入れ条件: AC-WF-032。`resolveGitWorkspace`は`resolveReviewRoot`と同じGit境界検証を行い`{activeRoot, primaryRoot}`を返す。active worktreeに設定が無ければprimary worktreeのenabled設定を`source=primary`で継承する。active worktreeの設定が`disabled`または`invalid`ならprimaryへfallbackせず`source=active`のまま返す。`loadJevProviderConfig`の既存8scenarioの結果は変わらない。
- 根拠: Issue #1485
- 実装: `src/adapters/review-workspace.ts`、`src/adapters/local-config-workspace.ts`、`src/domain/jev-provider-config.ts`、`src/domain/local-config-resolution.ts`

### REQ-WF-033 Jev HTTP adapterがdata egress policyに従って安全にrequestを構築・送信し、判断typeがproviderへ委譲された場合はcontinuous shadowとしてJevへも問い合わせ、authorityに影響しない形で記録する

`src/domain/jev-dispatch.ts`（pure）が、Jev API（`POST https://api.typesafe.ai/v1/systemone`。2026-09-26に安全な最小probeで実測確認したrequest/response形状）向けのchoice question requestを構築する。`options`が2件未満、`criteria`に未記載の`option`がある、`options`が重複する場合は例外を投げて拒否する。`state`へ入れる前のpayloadは`validateJevEgressPayload`が検査し、禁止key名（`token`・`password`・`secret`・`api[_-]?key`・`privateKey`等）を含む場合、または文字列が上限（8000文字）を超える場合は**redactして続行せず送信自体を拒否する**（fail-closed）。response（200/401/422/400/429/その他）は`parseJevChoiceResponse`がstatusコードだけで分類し、200でも`answers[key]`の形状が期待と不一致なら`schema-error`とし、未知の成功として扱わない（silent fallback禁止）。

`src/adapters/jev-http-client.ts`（I/O）は`process.env[config.apiKeyEnvVar]`から読んだ値をHTTP headerへ渡すだけで、それ以外のどのobject・log・戻り値にも含めない。値が印字可能ASCII（空白・改行・NUL等の制御文字を含まない）でなければtransportを呼ばず`auth-error`とする。transport例外のmessageは値やheaderを含み得るため複製せず、固定文言とerror class名・codeだけを`outcome.detail`へ残す。`transport`は常に注入可能で、`npm test`はfakeなtransportで決定的に検証し実networkへ依存しない。

`src/domain/jev-shadow.ts`・`src/adapters/jev-shadow-dispatch.ts`（continuous shadow、T-02）は、`decision invoke --apply`が実際にproviderへ委譲した呼び出し（`executor.kind === "provider"`。DCAND-008のunknown分岐・DCAND-009・DCAND-010）について、Jevが設定済み（`resolveJevProviderConfig`が`enabled`）であれば追加でJevへも問い合わせ、`JevShadowRecord`（`decisionRecordId`・`primaryProposedValue`・`jevProposedValue`・`matchesPrimaryProposedValue`・`outcomeKind`・`latencyMs`等）を`<primaryRoot>/.agent-skill-chain/runtime/decisions/<staging名>/jev-shadow.jsonl`へ記録する。**shadowはauthorityを一切持たない**——`effectiveValue`・`requiresConfirmation`・`rejected`は既存provider（lightweight-tier）の結果のまま変わらず、shadow dispatchが失敗しても例外を外へ投げず、`decision invoke`本体の結果へ一切影響しない。DCAND-006はshadow対象外とする（`decision invoke`自身が列挙可能な候補集合を知らないため。呼び出し元がseverity/relationの候補集合を先に決める設計変更を要する disclosed gap）。

- 受け入れ条件: AC-WF-033。egress policyに違反する`state`はtransportを呼ばずに拒否される。有効な設定でtransportがokを返せば、送信したbodyのmodel・question keyが期待通りであり、認証・schema・rate limit・network例外のいずれも明示的な`outcome.kind`として返り例外が外へ漏れない。`executor.kind !== "provider"`のdecision typeはshadow対象にならない。shadow dispatchの成否に関わらず`decision invoke`が返す`effectiveValue`・`requiresConfirmation`・`rejected`は変化しない。
- 根拠: Issue #1486
- 実装: `src/domain/jev-dispatch.ts`、`src/adapters/jev-http-client.ts`、`src/domain/jev-shadow.ts`、`src/adapters/jev-shadow-store.ts`、`src/adapters/jev-shadow-dispatch.ts`、`src/cli.ts`

### REQ-WF-034 Decision Recordと別に`EvaluationLabel`（referenceValue・labelSource・evidenceRefs）を記録し、実在するdecisionRecordId以外へのlabel記録を拒否する

`effectiveValue`（進行役が採用した値）はground truthではない。`src/domain/evaluation-label.ts`（pure）が`EvaluationLabel`（`decisionRecordId`・`referenceValue`・`labelSource`（`deterministic-oracle`／`evidence-adjudicated`／`independent-review`／`owner-adjudicated`のいずれか）・`evidenceRefs`・`labeledAt`）を検証しながら構築する。`owner-adjudicated`以外の3種は最低1件の`evidenceRefs`を要求し、`owner-adjudicated`だけは省略できる。`src/adapters/evaluation-label-store.ts`（I/O）は書き込み前に対象`decisionRecordId`がdecision journalへ実在することを確認し、存在しない場合は拒否する（fail-closed。架空のdecisionRecordIdを紛れ込ませない）。同じ`decisionRecordId`への重複labelは拒否する（後から確定した正解を後追いで書き換えない）。永続先はdecision journalと同じ`<primaryRoot>/.agent-skill-chain/runtime/decisions/<staging名>/evaluation-labels.jsonl`とし、既存journal schemaへは影響しない別fileに置く。

- 受け入れ条件: AC-WF-034。`owner-adjudicated`以外で`evidenceRefs`が空の入力は拒否される。未知の`labelSource`は拒否される。decision journalに実在しない`decisionRecordId`への追記は拒否される。実在する`decisionRecordId`への追記は成功し、同じIDで検索できる。同じ`decisionRecordId`への2回目の追記は拒否される。
- 根拠: Issue #1486
- 実装: `src/domain/evaluation-label.ts`、`src/adapters/evaluation-label-store.ts`、`src/cli.ts`

### REQ-WF-035 Jev provider設定のguided setup commandと、確認・値非表示を伴うshell起動fileへのexport追記を提供する

`agent-skill-chain decision configure --provider=jev --endpoint=<url> --model=<name> --api-key-env-var=<VAR> [--dry-run|--apply]`が`.agent-skill-chain/local/jev-provider.json`を正しいschemaで生成する。`endpoint`はhttps URLであることを検証し、不正なら（endpointがhttp・空・URL形式でない、modelが空、`apiKeyEnvVar`が`JEV_`で始まる英大文字・数字・`_`でないのいずれか）書き込まず`validationErrors`を返す。dry-runは書き込まず、書き込んだ場合に得られるはずの`LocalConfigResolution`を予測して返す。

`--shell-rc-append`を付けると、値をmode 0600の専用file（`~/.config/agent-skill-chain/jev.env`、親directoryは0700）へ`export <apiKeyEnvVar>='<値>'`（single quote、`'`は`'\''`へ置換）として書き、`$SHELL`から検出した起動file（bash→`~/.bashrc`、zsh→`~/.zshrc`。対応外shellはfail-closedで拒否）へは、その専用fileが存在すれば読み込む値を含まない1行だけを追記するモードへ切り替わる。rc fileは多くの場合world-readableでdotfile repositoryにも追跡されるため、値をrcへ直接書かない。改行・NUL等の制御文字を含む値は書き込まずに拒否し、symlinkの専用file・directoryも拒否する。**値そのものはこのcommandの引数として渡さない**——`process.env[apiKeyEnvVar]`（呼び出し時点でそのshellに既に`export`済みの値）を直接読むだけで、コマンド履歴・出力に値が現れない経路にする。追記には`--apply`と明示確認token`--confirm=APPEND`の両方を要求し（無言で書き換えない）、rc fileに既に同名変数の`export`行が存在する場合、または専用fileのexport行とrcの読み込み行が両方揃っている場合は常に無変更で返す（重複追記・既存記述の上書きを避ける）。戻り値・例外messageはどの状態でも値そのものを含まない。

この設計は、利用者（技術者でない場合を含む）が自分のAIエージェントへ自然言語でendpoint・model・env var名を伝え、AIエージェントがこのcommandを代行実行できることを前提にする。APIキーの値そのものについては、AIエージェントが値を直接受け取って処理するのではなく、人間が自分のshellで実行すべき正確な1コマンド（`export <apiKeyEnvVar>=<値>`）を提示する、という役割分担を可能にする——値を渡す入力経路自体を作らないことで実現する。

- 受け入れ条件: AC-WF-035。有効なendpoint・model・apiKeyEnvVarでの`--apply`は正しいschemaのfileを生成する。不正な入力は書き込まず理由を返す。`--shell-rc-append`は対応外shellを拒否し、bash/zshの既定rcを検出する。env var未設定では追記できない。dry-runは書き込まず戻り値に値を含めない。`--apply`かつ`--confirm=APPEND`が無い場合は例外になる。両方揃った場合だけ値が0600の専用fileへquoteして書かれ、rc fileには読み込み行だけが追記され、戻り値・例外メッセージのいずれにも値が含まれない。shell metacharacterを含む値は読み込んでも実行されず元の値に一致する。改行を含む値は書き込まずに拒否する。既に同名exportがあるか、2回目の実行では無変更で`alreadyPresent: true`を返す。`JEV_`で始まらないenv var名は拒否する。
- 根拠: Issue #1486
- 実装: `src/adapters/jev-guided-setup.ts`、`src/domain/jev-provider-config.ts`、`src/cli.ts`、`src/cli-usage.ts`

### REQ-WF-036 承認済み計画を封印し、封印後の計画変更を追記専用の計画変更記録へ分離する

承認済み計画は封印後に書き換えない。journal追記経路（`workflow record`と`workflow advance`の同期）がfullのStep 8、quick/pocのStep 4を記録するとき、CLIはmodeの計画文書（fullは`00_要求定義.md`〜`03_実装計画.md`、quick/pocは`00_要求定義.md`）のSHA-256を計算し、journal entryの`planSeal`へ固定する（計画封印、TERM-ASC-WR-01）。封印値は成果物fileからだけ計算し、呼出し側が渡した値は捨てる。計画文書が1件でも欠ければ封印せず記録を拒否する。読取り側はmodeの封印Step以外の`planSeal`、modeの計画文書集合と異なるkey集合、64桁SHA-256以外を拒否する。**封印は不変である。** 現在のmodeの封印が既にあれば、封印Stepの再記録はStep 9の前後・内容の同異を問わず、journalへ書き込む前（`workflow advance`では外部同期の前）に拒否し、承認済み計画は凍結されていることと計画の変更を`05_計画変更.md`へPlanning Amendmentとして追記することを返す。例外はquick/pocからfullへの昇格後のfull Step 8（modeが異なるため最初のfull封印になる）と、封印を作らないHumanOverride記録だけである。

封印後、`workflow record`のStep 9・10、`pr create`、`pr merge`（再配送を含む）は、封印済み計画文書の現在内容が封印と一致しない場合にfail-closedで拒否する。診断は変化した文書を名指しし、承認済み計画は凍結されていること、文書を封印時の内容へ戻し計画の変更を`05_計画変更.md`へ追記することを返す。この検査はdigest照合より先に行う。`planSeal`を持たない既存journal（本要件以前に記録したstaging）では検査しない。**この互換はjournal hash chainを持たないjournalにだけ成立する。** CLIはjournalへ追記する全行（`issue create`のStep 0行を含む）に、その行より前のjournal本文全体のSHA-256を`previousEntryDigest`（先頭行は`null`）として記録し、呼出し側の値は捨てる。読取り側は1行でもchainを持つjournalで、以降の全行のchainの存在と一致を要求し、封印Stepの非HumanOverride行の`planSeal`欠落と、封印後のStep 9・10の`planGeneration`欠落を旧journal互換として扱わず拒否する。計画凍結検査（Step 9・10記録、`pr create`、`pr merge`）はjournalの構造検査が失敗すれば成立させず、`pr create`・`pr merge`はchain付きjournalの最後のStep 10に`planGeneration`が無ければ世代検査を読み飛ばさず拒否する。`review round`はstaging digestを再固定する前にjournalを同じstrict parserで検査し、不正ならroundを拒否する。**chainは同じ端末の記録を改変できる者への防御ではない。** 全行からの`previousEntryDigest`除去、末尾行の書換え、末尾行の切詰めはjournal単独では検出できず、trust anchorはGit履歴と独立reviewである。

封印後の計画変更は、staging内の任意file`05_計画変更.md`（計画変更記録、TERM-ASC-WR-02）へ`## AMD-NNN`見出しのentryとして末尾へ追記する。各entryは`対象`・`変更`・`理由`の3項目だけを持つ。変更前の記述は封印済み計画から、影響範囲は影響集合（REQ-WF-039）から導出できるため書かせない。計画変更記録を書くのは、目的・受け入れ条件・不変条件・scope・security境界・公開interfaceや外部契約・data互換性・不可逆操作が変わる場合だけであり、それ以外の実装上の判断はGitだけに残す。封印後の上記4操作は、見出しが`AMD-001`から欠番・重複なく昇順であり、3項目がすべて空でなく`（…）`だけのplaceholderでもないことを要求する。**実効計画は「封印済みの基底計画＋追記専用のAMD列」であり、封印は1回だけ作る。** Step 9・10の記録時にCLIが各AMD entryの原文digestの列を計算し、封印digestから連鎖させてentryの`planGeneration`（`generation`・`previousDigest`・`amendments`・`digest`、`digest`は`sha256(stableJson({previousDigest, amendments}))`）へ記録する。これは記録済みAMD列を固定する機械記録であり、利用者やAIが操作する対象ではない。呼出し側の値は捨てる。記録済みAMDの編集・削除・並べ替えはStep 9・10記録と配送を拒否する。`pr create`・`pr merge`は現在のAMD列が最後のStep 10記録の世代と完全に一致することを要求し、Step 10記録後に追記した未reviewのAMDを配送しない（新しいreview roundとStep 10の再記録、pr-bound中は`--post-pr-intake`を案内する）。版管理下stagingでは検査するcommit上の`05_計画変更.md`もworktreeと一致させる。AMDのreviewはStep 10の差分reviewが担い、別の承認経路は設けない。`planGeneration`を持たないjournalでは世代を検査しない。`workflow assess-discovery --staging`は封印済みstagingで影響成果物の再確定を`record-planning-amendment`へ置き換える（REQ-WF-012）。上流再確定（`workflow record --reconfirm`）は廃止し、既存journalの再確定entryは読取り互換だけを残す（REQ-WF-004）。

- 受け入れ条件: AC-WF-036。quickのStep 4記録とfullのStep 8記録が計画文書から計算した`planSeal`を持ち、呼出し側の値を採用しない。封印後に計画文書を編集するとStep 9記録とdelivery直前検査が`05_計画変更.md`を名指しして拒否し、元の内容へ戻すと通る。封印の無い旧journal（hash chainを持たないjournal）では検査しない。chain付きjournalの封印Step行から`planSeal`を除去するとStep 10記録・配送直前検査・`review round`の再固定が拒否し、先行行の編集はchainの不一致として拒否される。chain付きjournalで封印後のStep 10から`planGeneration`を除くと配送直前検査が拒否する。新しいstagingのStep 0行は`previousEntryDigest: null`を持つ。`05_計画変更.md`の欠番・重複・項目欠落を拒否し、正しいentryを受理する。封印Step以外の`planSeal`と不正なkey集合をjournal構造検査が拒否する。封印済みの封印Stepの再記録は内容が同じでも拒否し、昇格後のfull Step 8封印は成立する。AMD-001を追記したStep 9は封印digestを`previousDigest`に持つ世代2を記録し、記録済みAMDの編集・削除はStep 10記録と配送直前検査が拒否し、Step 10記録後の未記録AMDは配送直前検査が拒否する。Step 9・10以外と不正な`planGeneration`をjournal構造検査が拒否する。`--reconfirm`を廃止済みと名指しして拒否し、既存journalの再確定entryは読める。封印済みstagingの`assess-discovery`が`record-planning-amendment`を返す。
- 根拠: owner承認の緊急再設計（計画凍結）
- 実装: `src/domain/plan-seal.ts`、`src/adapters/plan-seal.ts`、`src/adapters/workflow-journal.ts`、`src/adapters/review-session.ts`、`src/domain/issue.ts`、`src/domain/workflow.ts`、`src/domain/agile-verification.ts`、`src/cli.ts`、`src/cli-usage.ts`、`.agent-skill-chain/schemas/workflow-step-journal.schema.json`、`.agent-skill-chain/templates/issue/05_計画変更.md`

### REQ-WF-037 計画文書の永続的な正本を版管理下の1箇所に置き、Issue本文をpointerだけにする

**pointer本文は版管理下のstagingにだけ許す。** pointer本文は成果物の配置とdigestしか持たず、全文の正本はstagingである。版管理外のstaging（既定の`.agent-skill-chain/tmp/issues`）はPR mergeの後に消えるため、`issueBody=pointer`と組み合わせると計画文書00〜03の永続的な複製が1つも残らない。project policyの`staging`節は`issueBody=pointer`に`tracked=true`を要求し、`validateStagingPolicy`（`policy validate`と`readStagingLayout`の双方が通る）と2つのpolicy schemaがこれに反する節を拒否する。

版管理下のrootは`issue create`からStep 4/8の同期本文生成、`issue start`、`doctor`まで既定rootと同じく動く。`issue start`は既定rootの固定pathではなく配置契約でstagingの位置を検査する。`doctor`は宣言rootのうち機械記録（`staging-record.json`）を持つ作業中のstagingだけを検査し、merge済みで文書だけが版管理下に残ったstagingを不正と報告しない。宣言rootへ移行する前に既定rootへ作ったstagingは既定配置（版管理外・全文同期）として引き続き受理し、宣言が`pointer`でも全文を同期する。`pr create`のstaging探索も宣言rootと既定rootの双方を見る。worktree削除の安全判定は、版管理下stagingの機械記録（stagingの`.gitignore`が除外するもの）を従来どおりblockingのまま扱い、理由の末尾へ退避・削除の案内だけを足す。判定と資産分類は変えない。`issue staging`（一時stagingの衛生走査と削除）は既定rootだけを対象とし、版管理下のstagingは版管理が扱う。

`staging`節を持たない既存projectの既定配置（版管理外・全文同期）は変えない。`issue create`は版管理外かつ全文同期の配置のprojectで、版管理下・pointer配置の`staging`節を示す1行の通知をstderrへ出す。通知は判定・終了値・stdoutのJSONを変えない。`install`はproject policyを生成しないため、新規projectへの既定化は通知と案内で行う。本repositoryは`staging: {root: "docs/issues", tracked: true, issueBody: "pointer"}`を宣言する。

- 受け入れ条件: AC-WF-037。`tracked=true`でない`issueBody=pointer`はruntimeと2つのschemaで拒否され、`readStagingLayout`も拒否する。版管理下のroot（`docs/issues`）で`issue create`した文書00〜03と`.gitignore`は無視対象にならず、機械記録は無視対象になる。Step 8の`issue sync --generate-body --apply`は成果物のpathとdigestと目的・受け入れ条件だけのpointer本文をIssueへ書き、staging記録は`sync-verified`になる。`issue start`は版管理下のstagingの配置を受理する。`doctor`は作業中のstagingだけを検査する。移行前に既定rootへ作ったstagingは全文同期のまま受理される。機械記録の削除案内は安全判定と資産分類を変えない。既定配置の`issue create`は作成を妨げずに1行の通知をstderrへ出す。
- 根拠: owner承認の緊急再設計（2026-09-26、計画文書の3重複製の解消）、Issue #1412
- 実装: `src/domain/staging-layout.ts`、`src/domain/lifecycle.ts`、`src/domain/worktree-removal-safety.ts`、`src/adapters/workflow-journal.ts`、`src/cli.ts`、`.agent-skill-chain/schemas/project-policy.schema.json`、`.agent-skill-chain/schemas/project-policy-manifest.schema.json`、`.agent-skill-chain/project-policy.json`

### REQ-WF-039 実Git差分から影響集合を一度だけ導出し、review round 2以降の隣接範囲へ再導出照合付きで使う

`src/adapters/impact-set.ts`の`computeImpactSet`は、`observeReviewDiff`で観測した2 commit間の差分と、head commitのtreeから構築した意味Graph（`buildCommitSemanticGraph`。worktreeを読まない）だけから影響集合（TERM-ASC-WR-04）を導出する。純粋な判定は`src/domain/impact-set.ts`の`deriveImpactSet`が持つ。隣接範囲は変更source fileの直接import元と直接import先から変更pathを除いたpath昇順の集合で、各entryのGraph Evidenceは影響集合digest（`adjacent[].graphEvidence`を除いた本体の`sha256(stableJson(...))`）である。変更pathまたは隣接範囲が`src/lib/security.ts`・`src/lib/process.ts`・`src/lib/atomic.ts`・`src/lib/entrypoint.ts`・`.github/`配下、またはsrc配下でauth・trust・secret・permission・policy・merge・delivery・release・reanchor・evidence・review-convergence・jev-を名前に含むpathを含む場合は`securitySensitive`を立て、reviewerへ名指しする。**securityの扱いは縮小しない。** `securitySensitive`は単独では全体検証へ倒さない。

`review round --init`の雛形は、round 2以降で前round headから`--head`までの影響集合を導出し、`mode: "targeted"`のときだけ`focus.adjacentScope`へ隣接範囲を設定する。`mode: "full"`のときは空にし、全体reviewが適用されることを理由付きでnotesへ示す。round 1は従来どおり空である。`previewReviewRound`は記録前に同じ関数で影響集合を再導出し、提出された`adjacentScope`が一致しない場合はroundを拒否する。**呼び出し側が任意の64hexを添えても隣接範囲として受理されない。** admissionはround 2以降で、検証済み隣接範囲にあるfindingを修正差分と同じくcurrent scopeへ含める。前round blocker起因のCritical/High回帰（`fix-regression`）と、固定済みAcceptance Criteria・invariantへの違反はcurrent blockerになり、それ以外は従来どおり記録だけにする。補助reviewの差分文脈（`collectSupplementalReviewDiff`）は、影響集合がtargetedのとき関連fileを隣接範囲にし、fullまたは導出できないときは従来のstem探索へ戻る。

- 受け入れ条件: AC-WF-039。変更source fileの直接import元とimport先だけが隣接範囲になり、Graph Evidenceは影響集合digestと一致する。影響集合digestは同じ入力で一致し、入力が変われば変わる。意味Graphを構築できない場合は隣接範囲を持たずfullになる。security関連pathの変更は`securitySensitive`を立てるが単独ではfullにしない。round 2の雛形は影響集合の隣接範囲を設定し、任意のGraph Evidenceを持つpathを加えた記録は拒否される。隣接範囲の前round blocker起因Highと固定Acceptance Criteria違反はcurrent blockerになり、隣接範囲の改善提案と範囲外Highは記録だけになる。worktreeの未commit変更は影響集合を変えない。targetedの影響集合では補助reviewの関連fileが隣接範囲と一致する。
- 根拠: owner承認済み緊急再設計（review round 2以降と検証の再読込削減）
- 実装: `src/domain/impact-set.ts`、`src/adapters/impact-set.ts`、`src/adapters/repository-graph.ts`、`src/domain/review-convergence.ts`、`src/adapters/review-session.ts`、`src/adapters/supplemental-review-collect.ts`

### REQ-WF-040 影響集合から検証featureと検査を選び、影響を証明できない変更は全体検証へ倒す

影響集合は検証の選択を返す。変更source fileからimport元を推移的に辿り、path指定で起動される入口（import元を持たないfile、`src/`外のfile）と変更file自身についてはbasenameを字面で含む`src/`外のfile（test・script）も影響を受けるとみなす。影響を受けたmoduleを`satisfied-by`に持つ追跡表のscenarioから`verified-by`のfeatureを選び、影響を受けたstep定義fileを使うfeatureも選ぶ。featureとstep定義fileの対応は`src/domain/cucumber-binding.ts`がfeatureのstep本文をstep定義のpattern（Cucumber Expressionとregex）へ照合して導出し、どのpatternにも一致しないstepを持つfeatureは影響を受け得るとして含める。変更された`.feature`はそのまま選ぶ。Markdownだけの変更は形式検査（`docs:format`、spec文書では`trace:check`）だけを選び、字面で読むtest・scriptがあればそれを使うfeatureと検査も選ぶ。検査名は利用projectの`package.json`の`scripts`に存在するものだけを返す。

次のいずれかでは`mode: "full"`になり、`features`・`scenarios`・`checks`を空にして理由を`reasons`へ名指しする。意味Graphを構築できない、閉包が上限内で確定しない、全体に効く設定・基盤（`package.json`、lockfile、Cucumber設定、`tsconfig*.json`、eslint・prettier設定、`test/support/`、`test/steps/`、`scripts/compile.ts`、`scripts/build.ts`、`.github/`、`.agent-skill-chain/schemas/`、`bin/`）が変わった、変更fileがstep定義を登録する、影響を導出できない種別のfileが変わった、削除・改名されたsourceがある、build成果物だけが対応sourceなしに変わった、変更source fileから検証featureへ到達できない、影響を受けるstep定義fileが字面から取り出せない登録を含む、step定義を持たないhook・World登録fileが影響を受ける、隣接範囲が上限を超えた。step定義fileに同居するhookは、そのfileのstepが用意したWorld状態を後始末するものとして、そのfileを使うfeatureへ影響を限る。

`agent-skill-chain impact --base=<commit> --head=<commit> [--root] [--format=json|features]`は影響集合を出力する。`--format=features`は実行するfeature pathを1行1件で出力し、fullのときは終了値1と理由で全体検証を要求する（空の選択を「実行対象なし」と読み違えさせない）。**実行は利用projectのtest commandに委ね、ASCは選択だけを返す。**

**証跡に残す検証は観測する。** `agent-skill-chain verify run --staging=<staging> --scope=targeted|full [--base=<commit>] [--root] -- <argv...>`は、追跡fileと未追跡file（stagingの配置rootを除く）が現在HEADと完全一致するworktreeでだけ実行でき、HEADのexact SHAを記録する。比較基点は`--base`、無ければreview sessionの比較基点（どちらも無ければ拒否）であり、`比較基点..HEAD`の影響集合を`computeImpactSet`で導出する。**reviewの隣接範囲と同じ影響集合を検証へ束縛する。** 影響集合が`full`なら`--scope=targeted`を拒否する。**`--scope`に既定値は無く、scopeとargvの対応はtrusted policyで決まる。** 既定branch（`origin/HEAD`）のtrusted commitにあるproject policy manifestの`verification`節（`fullCommand`と`targetedRunner`、どちらもargv）を読み、`--scope=full`はargvが`fullCommand`と完全一致する場合だけ、`--scope=targeted`はargvが`targetedRunner`で始まり、後ろが`-`で始まらないpathだけで、影響集合の選んだfeatureを全部含む場合だけ実行する。candidateのworktreeや候補commitのmanifestは読まないため、候補が自分の`fullCommand`を`true`へ書き換えても受理集合は変わらない。trusted commitに`verification`節が無い、または`origin/HEAD`を解決できない場合は実行前に拒否する。**merge段階以降（delivery stateが`merge-prepared`・`merge-observed`・`reconciliation-required`、merge済みまたは再配送中の`step11-recorded`）では拒否する。** PR停止のStep 11記録後（post-terminal intakeの窓）は記録するが、staging digestを再固定しない（Step 11後にdigestを更新するのはpost-terminal intakeの`workflow record`だけである）。commandは**shellを通さず**argvのまま実行し、出力は標準エラーへ中継してdigestだけを残す。実行後にHEADまたはworktreeが変わっていれば記録しない。記録はstagingの`journal/verification-runs.jsonl`（版管理下stagingでは`.gitignore`の`journal/`が除外する）へ追記専用に1行ずつ、`schemaVersion`・比較基点・HEAD・argv・scope・影響集合digestとmode・終了値・signal・開始と終了時刻・標準出力と標準エラーのdigest・`recordDigest`を持つ。読取りは1行でも解析できなければ全体を拒否する。`verify run`の終了値はcommandの終了値であり、signalで止まった場合は1である。**観測するのはargv・終了値・HEAD・影響集合digestである。** trusted policyが宣言したcommandが実際に全件を実行するかは観測せず、宣言の妥当性は既定branchへのmergeを通すreviewが担う。**worktreeの一致は追跡fileと未追跡fileだけを見る。** `.gitignore`で無視されたfile（`.env`、`node_modules/`、localの設定file等）は一致検査の対象外であり、それらを書き換えれば同じHEAD・同じargvで結果を変えられる。この限界は検出しない。

**検証commandの信頼範囲を明記する。** trusted policyが固定するのはargvだけである。`["npm","test"]`は候補の`package.json`のscriptを、`targetedRunner`は候補の`cucumber.targeted.mjs`を実行するため、候補がscriptや設定を空洞化すれば同じargvのまま実質的な検証を省ける。この変更はGitの差分として独立reviewの対象になり、CIは既定branch側の設定で全件testを実行する。`verify run`の観測は「trusted argvを候補HEADで実行し、その終了値を得た」ことの証明であり、検証内容の十分性の証明ではない。

**導入手順（bootstrap）。** `verification`節はtrusted policy（既定branch）から読むため、既定branchに節が無いrepositoryでは`verify run`・`review export`・`review validate --staging`・`pr merge`がfail-closedで拒否する。節を追加するPRはこの検査を通れないので、`verification`節だけを追加するpolicy変更PRを、repository ownerの明示判断によるadmin mergeで先に取り込む。既存の利用projectは、この手順を終えるまで新しいreview証跡を生成できない。

- 受け入れ条件: AC-WF-040。`verify run`はshell記法を展開せずargvを実行し、終了値をそのまま返し、HEAD・影響集合digest・出力digestだけを記録して生の出力を保存しない。未commit変更、`--`の欠落、比較基点なし、fullの影響集合へのtargeted、実行中のworktree変更、起動できないcommandでは記録しない。`--scope`の省略、trusted policyの`fullCommand`と異なるargvの`--scope=full`（candidateのmanifestが同じargvを宣言していても）、trusted commitの`verification`節の欠落はcommandを実行せずに拒否する。merge段階以降のdelivery stateでは拒否し、PR停止のStep 11記録後は記録してもstaging digestを再固定しない。導出時点のtrusted policyの宣言外のcommand（`true`、宣言外のfull command、`targetedRunner`で始まらないtargeted、runnerの後ろのoption）を含む記録からは検証欄を導かない。記録の改竄・再整形・未知field・終了値とsignalの不整合・不正なargvを拒否する。src fileだけの変更はstep定義経由と追跡表経由のfeatureを選び、無関係featureを選ばない。全体に効く設定の変更とstep定義の変更はfullになり理由を名指しする。spec文書だけの変更はfeatureを選ばず`docs:format`と`trace:check`を選ぶ。step定義が字面で読む文書の変更はそのstep定義を使うfeatureを選ぶ。featureへ到達できない変更sourceはfullになる。build成果物は同時に変わったsourceへ委ね、単独の変化はfullになる。hook・World登録fileが影響を受けるとfullになる。feature照合はScenario Outlineの展開、alternation、optional、parameterを解釈し、未定義stepを含むfeatureを未照合として返す。`impact --format=features`はtargetedで終了値0とfeature pathを出力し、fullで終了値1と理由を出力する。
- 根拠: owner承認済み緊急再設計（review round 2以降と検証の再読込削減）
- 実装: `src/domain/impact-set.ts`、`src/domain/cucumber-binding.ts`、`src/adapters/impact-set.ts`、`src/domain/verification-run.ts`、`src/adapters/verification-run.ts`、`src/cli.ts`、`src/cli-usage.ts`

### REQ-WF-038 Step 10のreview証跡を収束済みreview sessionから`review export`で生成する構造化JSONとし、手書きの散文review成果物を要求しない

reviewは厳格に行うが、reviewの証明は散文ではなく構造化Evidenceとする。**Gitが示す事実（変更path・変更種別・配布物影響）をreview成果物へ書き直させない。** 判断の正本は`review round`が記録するreview session（finding・severity・relation・round・blocking）であり、Step 10の成果物は`review export --staging=<staging> --issue=<N> --reviewer=<id> --implementer=<id>`がそのsessionと`verify run`の観測記録（REQ-WF-040）から生成する`docs/reviews/<Issue番号>_review.json`（schema `agent-skill-chain/review-evidence/v2`、`.agent-skill-chain/schemas/review-evidence.schema.json`）である。

**証跡は観測（`observed`）と申告（`declared`）を分ける。hard gateは`observed`と、session・Gitから再導出した値だけを根拠にする。** `observed`は比較基点、`H_impl`、`比較基点..H_impl`の`observeReviewDiff` digest、session ID・最新round digest・count済みround数、`比較基点..H_impl`の影響集合digestとmode、検証欄（argv・scope・終了値0・`H_impl`・影響集合digest・終了時刻・`recordDigest`）を持つ。検証欄は`H_impl`と影響集合digestが一致する観測記録から導出し、同じargvの記録は最新の1件だけを見る（最新が不合格なら生成しない）。合格記録が1件も無いとき、影響集合が`full`で`scope=full`の合格記録が無いとき、`scope=targeted`の記録が影響集合の選んだfeatureを含まないとき、記録のcommandが導出時点のtrusted policyの`verification`宣言（REQ-WF-040）に一致しないときは生成しない。**`--verified=<command>`による検証の申告は廃止し、指定すると移行先を名指しして拒否する。** `declared`はreviewer・implementer identity、trusted policyの独立性モード、`reviewerModifiedCandidate: false`である。ほかにfinding IDごとの最終状態、空の未解決Critical/High、承認判定、`evidenceDigest`を持つ。v1の証跡（申告文字列の検証欄）は受理せず、`verify run`と`review export`での再生成を名指しして拒否する。

**reviewerの独立性は申告であり観測ではない。** reviewer identityを`H_impl`へ束縛する機械記録は存在しない。`review round`はreviewer identityを記録せず、`review launch`・`routing launch`・`routing delegated-review-diff`は呼び出し側が渡したidentityで起動するだけで、結果をstagingへ保存しない。したがって`context-isolated`の独立性（reviewerとimplementerが別のsession/context）は申告である。観測できる独立性は`actor-independent`の、GitHubのAPPROVED eventとcommit author・PR authorの照合だけである。`reviewerModifiedCandidate`も申告である。reviewer identityはGitのcommit author identityと対応付けられないため、Gitからreviewerによるcandidateの変更を判定できない。

`review export`は、sessionが未収束・budget-exhausted・blocker残存のとき、reviewerとimplementerが同一のとき、観測した合格検証が上記の条件を満たさないとき、current HEADがreview済みcandidate HEADでも内容等価なrebase後の`H_impl`でもないとき、出力先が`docs/reviews/`・`.agent-skill-chain/reviews/`配下の`<Issue番号>_review.json`でない・symlinkを経由する・staging内のときに生成しない。書込みはatomicで、書込み後に厳密に読み戻す。証跡の厳密解析は未知field・型違い・不正なSHA・digest不一致・正規直列化とのbyte不一致を拒否する。

消費側は証跡の値をauthorityにしない。`pr merge`と`review validate --artifact --staging`は、証跡を保存済みsession・trusted policyの独立性モード・Git観測へ照合し、さらに`observed`の全fieldを再導出して比較する。**`pr merge`はこの照合を独立性モードによらず行う。** 独立性モードが選ぶのは承認の出所（`context-isolated`は最新Step 10 bindingと一致するformal approval、`actor-independent`はproviderの独立review）だけである。diff digestと影響集合のdigest・modeは`比較基点..H_impl`から再計算する。**検証欄も再導出する。** stagingの全観測記録から、再計算した影響集合と照合時点のtrusted policyの`verification`宣言で検証欄を導出し（`review export`と同じ規則）、証跡の検証欄がその導出結果と完全一致することを要求する。記録に存在する合格entryだけを抜き出した手組みの証跡、同じargvの後続実行が不合格の証跡、再実行で最新の記録が変わった後の古い証跡は一致しない。記録を読めない、導出できない、trusted policyに宣言が無いときは拒否する。`pr merge`はtrusted policyをPR baseの既定branchから、`review validate`は`origin/HEAD`のtrusted commitから読む。`review reanchor`/`pr reanchor`のrebaseは比較基点・`H_impl`とそれに束縛される値（影響集合、検証欄の束縛値）以外の一致と、検証したargvとscopeの集合の一致を要求する。path是正は証跡のbyte一致、同一pathの前進修正は検証欄以外の全fieldの一致を要求し、前進修正・reviewed-forward・path是正の新証跡は`observed`の再導出（検証欄を含む）との一致も要求する。pr-bound後は、検証commandとscopeの集合が変わらない前進修正（同じcommandの再実行だけを差し替えた証跡）もrebaseとしては受理せず、同一pathの前進修正として判定する。`audit:check`は`H_impl..current`がreview証跡1 fileであること、証跡の`H_impl`・比較基点がcommit構造から導出した値と一致すること、ancestry、merge損失検知、round上限を検査し、配布物影響は変更pathとpackage filesから導出して報告する。Markdownのreview template、個別監査表、観測欄、Step chain欄、配布物影響の散文は要求しない。

**検証の限界を明記する。** `evidenceDigest`は鍵を持たないSHA-256であり、正規形は公開されているため、digestの一致は証跡fileの改ざん検知であって出所の証明ではない。観測記録（`journal/verification-runs.jsonl`）も利用者のlocal stagingにある機械記録であり、review sessionと同じ信頼水準である。照合できるのは「CLIが観測した形の記録が証跡の`H_impl`と影響集合に一致して存在すること」までで、記録の偽造を暗号的に防ぐものではない。review sessionとの照合（収束、round digest、未解決Critical/Highなし）は`pr merge`の`context-isolated` formal approvalと`review validate --staging`が行う。CIの`audit:check`は証跡とGitの整合（比較基点、`H_impl`、証跡1 fileだけのsuffix、merge保全）だけを検証し、sessionの収束を証明しない。ASCの`pr merge`を経由しないmerge（GitHub UI等）では、証跡は従来のMarkdown artifactと同じく自己申告を超える保証を持たない。

- 受け入れ条件: AC-WF-038。収束済みsessionから生成した証跡は正規直列化から同じ値へ読み戻せ、finding IDごとの最終状態と空の未解決Critical/Highを持つ。未知field・型違い・digest不一致・再整形・round上限超過を拒否する。v1の証跡を移行先つきで拒否する。検証欄は`H_impl`・影響集合digest・終了値0に束縛され、影響集合が`full`なら`scope=full`の記録を要求し、`recordDigest`の重複を拒否する。未収束session、同一identity、観測記録なし、最新の実行が不合格、`--verified`の申告、H_final上の実行、許可外の出力先から生成しない。`review validate --artifact --staging`は保存済みsession・Git・観測記録に照合し、改竄・安全でないpath・観測記録の欠落と改竄・同じargvの後続の不合格を拒否し、観測記録とtrusted policyから再導出した検証欄と一致しない証跡（記録に存在する合格entryだけを載せた手組みの証跡、再実行前の古い証跡）を拒否する。`actor-independent`の`pr merge`も同じ再導出を行い、一致しなければmergeを要求しない。内容等価なrebase後の`H_impl`では比較基点を指定し、その`H_impl`で観測した検証から再生成できる。`audit:check`は証跡とcommit構造の不一致を拒否する。
- 根拠: owner承認の緊急再設計（review成果物の散文依存の撤去、検証の自己申告の撤去）
- 実装: `src/domain/review-evidence.ts`、`src/adapters/review-evidence.ts`、`src/domain/verification-run.ts`、`src/adapters/verification-run.ts`、`src/adapters/evidence-reanchor.ts`、`src/domain/evidence-reanchor.ts`、`src/cli.ts`、`src/cli-usage.ts`、`scripts/check_file_audit.ts`、`scripts/check_skill_templates.ts`、`.agent-skill-chain/schemas/review-evidence.schema.json`
