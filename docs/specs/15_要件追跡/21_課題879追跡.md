# 課題879 要件追跡

| AC | 要件・不変条件 | 実装・enforcement point | Counterexample Gherkin | 証拠・rollback |
|---|---|---|---|---|
| AC-879-01 | 配布entryはpathのUnicode codepoint昇順と内容SHA-256で正準化し、入力順に依存しないdigestを返す。不正entry、重複path、0件ではdigestを返さない | `computeDistributionDigest` | SCN-UNIT-DIGEST-001〜005 | 64桁小文字hexと日本語error。rollbackでは自動releaseを停止し、不正digestを判定へ使わない |
| AC-879-02 | version bumpだけでdigestを変えず、それ以外の配布内容変更を保持する | `normalizeDistributionContent` | SCN-UNIT-DIGEST-006〜010 | package metadataの指定version fieldだけを除外する正規化結果。parse失敗時は元内容をhash対象にする |
| AC-879-03 | 比較対象はsortした最新tagでなく`v${package.json.version}`とし、未release tagでは比較せずreleaseする。tag存在時は同一digestだけを停止し、前回digest不明はfail-open、現在digest不正は停止する | `planAutoRelease`、`planAutoReleaseFromEnvironment` | SCN-UNIT-AUTOREL-D01〜D08、SCN-E2E-AUTORELEASE-001〜003 | 純関数計画と欠落・破損digest JSONの空文字fallback。rollbackでは外部更新を止めて対象tagを再確認する |
| AC-879-04 | workflowは`push.paths`で限定せず、build後の現在配布物と`v${currentVersion}` worktreeの配布物を算出し、summaryとRelease本文へ参考digestを記録する。自動npm公開、再帰guard、冪等性、権限を弱めない | `.github/workflows/release.yml`、`validateReleaseWorkflow` | SCN-UNIT-RELWF-P01〜P03、SCN-INT-AUTORELEASE-001〜003 | workflow validator、summary項目、Release本文の正本注意書き。rollbackでは自動triggerを停止して外部資産を再確認する |
| AC-879-05 | 配布file集合は`npm pack --dry-run --json --ignore-scripts`から取得し、`dist/`なしを拒否し、`--cwd`でtag worktreeを対象化できる | `scripts/compute_distribution_digest.ts` | SCN-INT-DIGEST-001〜003 | fixture packageのCLI JSONと日本語error。rollbackではdigest不明としてfail-openし、取りこぼさない |
| AC-879-06 | READMEだけの変更はrelease対象となり、配布対象外のdocsだけでは停止し、`package.json.files`への対象追加へ手書きpaths変更なしで追随する | npm pack fixtureと配布digest比較 | SCN-INT-DIGEST-004〜006 | 前後digestとentry件数。rollbackでは配布境界の変更を停止し、paths proxyを再導入しない |

## 仕様変更履歴

| 日付 | Issue | 用語差分 | 変更 |
|---|---|---|---|
| 2026-08-25 | #879 | TERM-ASC-023を変更、TERM-ASC-024「配布digest」を追加 | 自動release発火条件を変更path proxyからnpm配布物の内容digest比較へ移行 |
