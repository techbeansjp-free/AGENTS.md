# 課題860 要件追跡

| AC | 要件・不変条件 | 実装・enforcement point | Counterexample Gherkin | 証拠・rollback |
|---|---|---|---|---|
| AC-01 | FR-01、FR-02、INV-01 | `createIssueStaging`、`recordStagingSync`、`inspectStaging` | SCN-UNIT-STAGING-001〜003、SCN-INT-STAGING-003 | quick・pocはStep 4、fullはStep 8とbody再読取digest一致だけを`sync-verified`にする。異常時は記録更新前の`local-active`へrollbackする |
| AC-02 | FR-02、INV-01、INV-05 | `recordStagingSync`、`planStagingCleanup` | SCN-UNIT-STAGING-001、SCN-INT-STAGING-003 | tracker、checkpoint、時刻、期待・再読取digestの不足または不一致を保持理由にする。外部同期失敗時はstagingを保持する |
| AC-03 | FR-03、INV-02 | staging成果物inventory、mode別必須成果物、同期記録更新 | SCN-UNIT-STAGING-003、SCN-INT-STAGING-003 | 00を含む成果物一覧とcontent digestを同期時に原子的更新する。共有・不明入力は保持して再同期する |
| AC-04 | FR-04、INV-03 | `planStagingCleanup`、`stableJson`によるSHA-256 hash | SCN-UNIT-STAGING-002〜004、SCN-INT-STAGING-001、SCN-E2E-STAGING-001 | exact path、kind、owner、理由、fingerprint、同期証拠、除外、hashをread-only出力する。問題時はpreviewのみへ戻す |
| AC-05 | FR-05、INV-03、INV-04 | `applyStagingCleanup`、CLIの`removeStagingTarget` | SCN-UNIT-STAGING-005〜006、SCN-E2E-STAGING-002 | 64桁承認hash、plan再計算、再plan、issues直下、非symlink、realpath、通常file・directoryを副作用前に検証する。不一致は削除0件で新previewへ戻す |
| AC-06 | FR-06、BR-02 | `inspectDirectory`のlegacy分類 | SCN-UNIT-STAGING-004 | 記録なしで通常fileがあるlegacyは保持する。通常fileが0件の空または空directoryだけを限定候補にし、誤判定時はlegacy候補化を無効にする |
| AC-07 | FR-07、BR-03、BR-04 | `isStagingLifecyclePath`、`previewWorkspaceHygiene`、package ownership境界 | SCN-UNIT-STAGING-006、既存workspace hygiene・lifecycle回帰 | `.agent-skill-chain/tmp/`、`role-log/`、`metrics/`を一般hygieneから除外する。専用cleanupを無効化してもconsumer資産を保持する |
| AC-08 | FR-08、INV-05 | `applyStagingCleanup`の対象不存在確認・部分失敗result、CLIの安全な深さ優先削除 | SCN-INT-STAGING-001〜002 | path別removed・retained・recoveryを返し、例外後は停止する。fresh previewによる再実行へrollbackする |

実装前の反例は`src/domain/staging.ts`不在によるSCN-UNIT-STAGING群の失敗として確認する。実装後はunit、integration、e2eの各SCNと全品質gateを同じ差分で再実行し、実装者の結果を独立review承認として扱わない。

## 仕様変更履歴

| 日付 | Issue | 用語差分 | 変更 |
|---|---|---|---|
| 2026-08-25 | #860 | TERM-ASC-061をactiveで追加、TERM-ASC-021を参照 | staging記録、mode別同期checkpoint、preview/apply、legacy限定候補、hygiene隔離を追加 |
