# 課題873 要件追跡

| AC | 要件・不変条件 | 実装・enforcement point | Counterexample Gherkin | 証拠・rollback |
|---|---|---|---|---|
| AC-873-01 | subjectが`chore(release): bump version to <version>`で、`<version>`が`isPackageVersion`を満たし、任意の接尾辞だけが続くcommitをrelease bump候補にする | `releaseVersionFromSubject`、`isDirectReleaseBump` | SCN-UNIT-AUDITBUMP-001、SCN-UNIT-AUDITBUMP-004 | 正規subjectと非bump subjectの対。rollbackでは除外判定全体を戻し、通常監査へ安全側に復帰する |
| AC-873-02 | 候補commitの変更fileは`package.json`と`package-lock.json`の一方または両方だけでなければならず、別fileが1件でもあれば除外しない | `RELEASE_BUMP_PATHS`、`hasReleaseBumpChanges` | SCN-UNIT-AUDITBUMP-002 | 対象外fileを加えた反例の拒否。誤除外時はrelease path集合からの除外を停止する |
| AC-873-03 | `package.json`を変更する場合、commit前後をJSONとして読み、`version`以外のkeyを除外せず`stableJson`で比較し、追加・変更・削除が0件の場合だけ除外する | `packageJsonOnlyChangesVersion`、`parseJsonStrict`、`stableJson` | SCN-UNIT-AUDITBUMP-003 | `description`も変えた反例の拒否。JSON解決・解析不能時も通常監査へ戻す |
| AC-873-04 | `H_impl`からcurrentまで到達する親鎖をcommitごとに走査し、release bumpだけが触れた最終差分pathを除外する。同じpathへ通常commitも触れた場合は除外しない | `implementationPath`、`finalAuditPaths` | SCN-UNIT-AUDITBUMP-001〜004 | 直線履歴の正常・反例。rollbackではcommit走査を外して従来の全path監査へ戻す |
| AC-873-05 | release PRのmerge commitはfirst-parent相当の導入差分を検査し、非選択親側の固有commitを全件走査してすべてが3条件を満たす直接bumpで、merge導入差分も3条件のfile・JSON制約を満たす場合だけ除外する | `implementationPath`の親選択、`isReleaseBumpSide`、`isReleaseBumpTransition` | SCN-INT-AUDITBUMP-001、SCN-INT-AUDITBUMP-002 | mainの`13d703d2da9de4c8e016be860c22e7d4bcddccb2`はfirst-parent差分がpackage 2件、第二親側の固有commit `569e36bc3b0f7eb7efd4b5c5294cb4d52cfa4ab1`だけが正規bumpである。通常commit後に正規bumpを置く反例も拒否する。rollbackではmerge除外だけを停止する |
| AC-873-06 | bump以外の`package.json`変更は従来どおりreview artifact以外のpathとして拒否する | `regularPaths`を優先する`finalAuditPaths`、最終path集合検査 | SCN-UNIT-AUDITBUMP-004、SCN-INT-AUDITBUMP-002 | 非bumpの直線・merge反例。誤判定時は除外せず監査表への記載を要求する |
| AC-873-07 | baseと`H_impl`の完全SHA解決、baseから`H_impl`までのpath集合と監査表の完全一致、base・`H_impl`のancestor検査、個別判定`pass`要求を変更しない | `checkFileAudit`の既存SHA解決、`expectedKeys`・`actualKeys`照合、2つの`merge-base --is-ancestor`、個別判定loop | 既存SCN-UNIT-PACKAGE-011、SCN-UNIT-AUDITBUMP-002〜004 | `scripts/check_file_audit.ts`の既存検査を保持し、現在のmain履歴で`audit:check`が`valid: true`。rollbackでも既存検査は残す |

## 仕様変更履歴

| 日付 | Issue | 用語差分 | 変更 |
|---|---|---|---|
| 2026-08-25 | #873 | 追加なし | release bumpの限定除外、mergeのfirst-parent判定、反例時の通常監査を追加 |
