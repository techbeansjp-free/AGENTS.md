# 課題877 要件追跡

| AC | 要件・不変条件 | 実装・enforcement point | Counterexample Gherkin | 証拠・rollback |
|---|---|---|---|---|
| AC-877-01 | Step 0〜11、mode別列、省略不能Stepを1つのコード正本で定義する | `src/domain/workflow.ts` | SCN-UNIT-WFSTEP-001〜006 | full・quick・pocの列とStep 4・11の反例。rollbackでも規範表との一致gateは外さない |
| AC-877-02 | journalは必須Step、対象外Step、行順、modeの単調昇格、未知field、空成果物を検証する | `parseStepJournal`、`validateStepJournal`、journal schema | SCN-UNIT-WFJRNL-001〜013 | quickでStep 4欠落、順序違反、降格、壊れたJSONの反例。拒否時は行を追加しない |
| AC-877-03 | Step 0のモード判定を全Q回答と根拠を持つ正準JSONへ成果物化する | `renderModeDecision`、`parseModeDecision`、mode decision schema | SCN-UNIT-WFMODE-001〜004 | Q欠落、PoC宣言欠落、未知fieldを拒否。rollbackでは既存要求成果物を保持する |
| AC-877-04 | CLIでStep定義を機械読取し、journalを安全に追記・検証できる | `workflow steps / record / verify`、`src/adapters/workflow-journal.ts` | SCN-INT-WFSTEP-001〜003、SCN-E2E-WFSTEP-003 | 追記後digestと日本語診断。拒否時はjournalを変更しない |
| AC-877-05 | doctorは各stagingのモード判定成果物、journal、現在・次Stepを既存結果へ追加する | `doctor`の`workflow.stagings` | SCN-INT-WFSTEP-004〜005 | 欠落をstaging別に報告し、既存install healthの意味を維持する |
| AC-877-06 | PR作成前にStep 10まで、特にStep 4・10とsync-verifiedを要求する | `pr create`のworkflow前提検査 | SCN-E2E-WFSTEP-001〜002 | quickでStep 4を飛ばした逸脱をGitHub呼出前に非0で拒否する |
| AC-877-07 | 欠落受容は対象に拘束したHumanOverrideだけに限定し、journalへ承認者と理由を残す | `validateJournalHumanOverride`、`--workflow-override`、journal schema | SCN-E2E-WFSTEP-004 | overrideなし、AI自己発行、別Issue、失効は拒否し、無条件flagを設けない |
| AC-877-08 | 規範文書とコードのStep表・mode列が完全一致する | `scripts/check_workflow_steps.ts`、`workflow:check`、build、prepack | SCN-INT-WFSTEP-006〜008 | 表またはmode列の片側改変を非0にする |

## 仕様変更履歴

| 日付 | Issue | 用語差分 | 変更 |
|---|---|---|---|
| 2026-08-25 | #877 | TERM-ASC-062 step journal、TERM-ASC-063 モード判定成果物を追加 | Step正本、journal、モード判定成果物、workflow CLI、PR・doctor gate、規範一致検査を追加 |
