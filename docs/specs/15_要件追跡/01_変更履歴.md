# 仕様変更履歴

| 日付 | 変更 | 要件・SCN | 用語ID | 更新文書 | Issue・PR | 互換性 | 判断者 | HEAD SHA |
|---|---|---|---|---|---|---|---|---|
| 2026-09-12 | descriptor pathを再openしたdescriptorの`fstat`で固定済みdirectoryとのdev・ino一致を検証する。Darwinでは検証済みの名前付き親から排他的に作成し、内容書込み前にも親identityを再検証して、fdescfsの合成`st_dev`と末尾path非対応を安全に扱う | REQ-WF-014、AC-WF-014、SCN-UNIT-REVINIT-011〜012・015・017〜019 | なし | `02_要件/01_ワークフロー要件.md`、`15_要件追跡/01_変更履歴.md` | PRは本作業で作成 | descriptor aliasを再openできない環境は従来どおり作成前にfail-closed。固定済みdirectoryとのdev・ino一致条件は緩和せず、親差し替え時は内容を書かない | package ownerへの利用側報告 | 実装commitで確定 |
| 2026-09-12 | 成果物1単位と45分超見込み時の起票時分割を原則化し、fullの複数明示markerを非停止warningにする | REQ-WF-022、AC-WF-022、SCN-UNIT-ARTUNIT-001〜007・010〜011、SCN-INT-ARTUNIT-008〜009 | TERM-ASC-108 | workflow、3配布template、Issue validator、CLI契約、追跡 | Issue #1337 | marker無しの既存成果物とquick/poc集約形式は有効のまま。warningはvalidation成否・安全gate・authorityを変更しない | package owner | 作業tree、base `3b6dcb88` |
| 2026-09-12 | review入力を固定したまま判定非依存の進捗だけを専用digest chainへ追記し、read-only表示する安全な並行経路を追加 | REQ-WF-021、AC-WF-021、SCN-INT/UNIT/E2E-PROGRESS-001〜021 | TERM-ASC-107 | `01_システム概要/02_用語・略語.md`、`02_要件/`、`06_外部インターフェース/`、`07_データ/`、`11_非機能/`、`15_要件追跡/`、配布03 template | Issue #1336 | 専用journalだけをstaging digestから分離し、review入力treeへの投影applyとgateからjournalを読むedgeを禁止する。progress失敗は従来reviewを停止しない | repository ownerの指示とClaude Opus反例探索2回 | 作業tree、base `8e7405b9` |
| 2026-09-12 | SCN IDの文法を`src/domain/scenario-id.ts`の単一正本にし、`issue validate`のscenario行検出へ末尾境界と文法外IDの名指し診断を加える。delivery証跡検査は同じ述語を参照し、工程の入口と終端の受理集合を一致させる | REQ-WF-006、AC-WF-006、SCN-UNIT-SCNID-001〜004 | TERM-ASC-105 | `01_システム概要/`、`02_要件/`、`06_外部インターフェース/`、`15_要件追跡/` | Issue #1349 | `issue validate`は文法外のSCN ID（小文字枝番等）を新たに拒否する。正規IDの判定は不変 | package owner | 実装commitで確定 |
| 2026-09-12 | `workflow advance`で保存済みstateから次の1 Stepを導出し、既定preview、Step別成果物検証・journal記録、外部書込み直前のIssue本文競合検査、Step 4・8の生成本文同期と実測Evidence、公開済み同期の重複送信を避けるjournal復旧、Step 9成果物のexact HEAD拘束、Step 10・11の専用gate委譲を提供する | REQ-WF-020、AC-WF-020、SCN-UNIT-ADVANCE-001〜005、SCN-E2E-ADVANCE-001〜013 | TERM-ASC-106追加 | `01_システム概要/02_用語・略語.md`、`02_要件/01_ワークフロー要件.md`、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`15_要件追跡/`、`dist/` | Issue #1335 | 公開subcommand追加。既存個別command、journal形式、review・delivery authorityは不変 | package owner | 実装commitで確定 |
| 2026-09-11 | Verification Set risk=`low`のとき02 §4.2・§8・§9と03 §5.2を`対象外: <理由>`の理由付き1行へ縮め、`issue validate`がstrict risk入力と節を突合する | REQ-WF-018、REQ-WF-019、SCN-WF-1334-001、SCN-WF-1334-002、SCN-WF-1334-003、SCN-WF-1334-004、SCN-WF-1334-005、SCN-WF-1334-006、SCN-WF-1334-007、SCN-WF-1334-008、SCN-WF-1334-009、SCN-WF-1334-010、SCN-WF-1334-011、SCN-INT-WF-1334-001、SCN-INT-WF-1334-002 | TERM-ASC-104追加 | `01_システム概要/02_用語・略語.md`、`02_要件/01_ワークフロー要件.md`、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`15_要件追跡/`、配布02/03 template | Issue #1334 | lowへ後方互換な短縮形式を追加。medium/high/critical、既存詳細形式、Q-01〜Q-08は不変 | package owner | 実装commitで確定 |
| 2026-09-11 | `review validate --artifact`でMarkdown review artifactの§0識別行、ラウンド数、Step chain、必須見出し、配布物影響を行番号・期待書式付きで事前検証し、parserをaudit・reanchorと共有する | REQ-WF-017、AC-WF-017、SCN-UNIT-REVARTVAL-001〜003、SCN-INT-REVARTVAL-001 | TERM-ASC-103参照 | `02_要件/`、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`15_要件追跡/`、`dist/` | Issue #1332 | `--artifact`追加。`--file` JSON判定、approval、delivery authorityは不変 | package owner | 実装commitで確定 |
| 2026-09-11 | Step 4・8同期証拠の64hex digestとsync語、no-spec-impactの12文字以上spec.rationale、review round入力fileのstaging外配置とblocking contractId、finalizeの2 digest flagをusage・診断・Step skill・実行例へ露出 | REQ-WF-016、AC-WF-016、SCN-UNIT-CLICONTRACT-001〜004 | なし | `02_要件/`、`06_外部インターフェース/`、`15_要件追跡/` | Issue #1331 | 説明追加のみ。既存判定・authority・保存形式は不変 | package owner | 実装commitで確定 |
| 2026-09-11 | `issue create`で00〜03の件名・日時・正本状態とproject choicesを事前充填し、`issue sync --generate-body`でmode/checkpoint別の同期本文を生成する。`review artifact --init`でexact base・H_impl・全変更path・staging digestを04へ充填し、判断欄は未確定に保つ | REQ-WF-015、AC-WF-015、SCN-UNIT-ISSUEPREFILL-001、SCN-UNIT-ISSUESYNC-001、SCN-UNIT-REVIEWARTIFACT-001、SCN-UNIT-REVIEWARTIFACT-002 | TERM-ASC-102、TERM-ASC-103 | `02_要件/01_ワークフロー要件.md`、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`15_要件追跡/`、`01_システム概要/02_用語・略語.md`、Step 4・8・10 skill | Issue #1333、#1325、PRは本作業で作成 | `--body-file`を維持し生成経路を追加。policy manifest不在projectの`issue create`も従来どおり動作する。既存fileは上書きしない | package owner | 実装commitで確定 |
| 2026-09-11 | `review round --init`の雛形作成を固定した親directory descriptorから解決し、親pathの再差し替えでもstagingや無関係fileへ触れない。作成後の失敗はpathname削除を行わず、作成descriptorを空にしてentryの残存と手動削除を明示する。fileとdirectoryの耐久化後にdirectory descriptorのcloseだけが失敗しても、完成済み雛形を未確定と誤報しない。descriptor相対pathを提供しない環境は作成前に拒否する。`budget-exhausted`のsessionではHEAD差分より先に雛形を書かずerrorにし、`--invariant`だけの指定も無視を通知する。finalizeのcleanup-apply拒否の案内を`--approved-digest`だけにする。REQ-WF-009へsession依存flagの個別報告を明記。template 04へDC-UX根拠と発見IDの注記 | REQ-WF-009、REQ-WF-014、AC-WF-009、AC-WF-014、SCN-UNIT-REVINIT-011〜019、SCN-UNIT-DIAGHINT-004、SCN-UNIT-DOCROW-002 | なし | `02_要件/01_ワークフロー要件.md`、`15_要件追跡/00_追跡表.md`、template 04 | Issue #1329、PR #1339 | error 2種、作成後失敗時の安全な残存診断、耐久化後のclose失敗分類、descriptor相対path非対応環境のfail-closedを追加。対応環境の正常系出力は不変 | package owner | 実装commitで確定 |
| 2026-09-11 | review artifact 1 fileだけを加えたHEAD（evidence-only suffix）をreview sessionのcandidate HEADと同一視し、`workflow record --step=10`と`pr create`・`pr merge`のbinding検査がH_finalを受理する。artifact commitのための取り直しroundを要求しない。空差分・2 path・allowlist外・削除・rename・type変更・実行権限・merge commit・複数commit・非ancestorは従来どおり拒否。規範文書02のbinding記述を整合 | REQ-WF-005、AC-WF-005、SCN-UNIT-EVIDHEAD-001〜013 | TERM-ASC-101 | `02_要件/01_ワークフロー要件.md`、`15_要件追跡/00_追跡表.md`、`01_システム概要/02_用語・略語.md`、規範文書01、step-10 skill | Issue #1272 | 受理条件の拡大のみ。HEAD一致時と拒否文言は不変 | package owner | 0fb9c70fb6450b5cb0c406207ef3630c6c99a8b5 |
| 2026-09-11 | `review round --init`で次roundの入力雛形を実Gitとsessionから生成しstaging外へ書く。`review round`と`pr create`の`--help`へ`inputContract`を追加。staging digest不一致の診断へ`workflow record`再実行の案内、finalizeのcleanup-apply拒否へ`--approved-digest`の案内を追加。配布template 04 §0へ`H_impl`行と厳密書式の注記、規範文書01へQ-08の判定例表、step-09 skillへstagingをworktree内へ置く手順を追加 | REQ-WF-009、REQ-WF-014、AC-WF-009、AC-WF-014、SCN-UNIT-REVINIT-001〜010、SCN-UNIT-CLIHELP-010、SCN-UNIT-DIAGHINT-001〜003、SCN-UNIT-DOCROW-001 | TERM-ASC-100 | `02_要件/01_ワークフロー要件.md`、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`15_要件追跡/00_追跡表.md`、`01_システム概要/02_用語・略語.md` | Issue #1323、#1325、#1322 | 追加のみ。既存flag・field・拒否条件は不変 | package owner | 実装commitで確定 |
| 2026-09-11 | 開発考慮事項の理由・証拠の判定を、全角括弧の一律拒否から配布template由来の既知placeholder集合の部分一致へ変更し、自然文を受理する。fullの01〜03に参照行を導入し00を正本にする。Gherkin scenario IDの検出とGherkin除外をproject choiceの`gherkinDialect`のkeywordで行い、未知方言を拒否する。template 01〜03へ参照行の注記を追加し、03 §5.1の説明文に残っていたtemplate由来の丸括弧placeholderを言い換える | REQ-SQ-009、AC-SQ-009、SCN-UNIT-ISSUEDC-001〜011、SCN-UNIT-ISSUEGHK-001〜008 | TERM-ASC-099 | `02_要件/04_仕様・品質管理要件.md`、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`15_要件追跡/00_追跡表.md`、`01_システム概要/02_用語・略語.md` | Issue #1324、#1326 | 受理集合の拡大。4行表と`Scenario:`の既存成果物は同じ判定。未知方言のみ新たに拒否 | package owner | 実装commitで確定 |
| 2026-09-09 | managed asset recordの不在を失敗にせず、全資産が未記録である状態として評価する。`update`は正本一致資産を採用し相違資産を上書きせず保持する。`delete`のrecord不在拒否はpreviewが成功する場合に限り`--recover-record`の所属commandである`update`を名指しし、`install`の競合拒否はcommandを名指しせず競合pathだけを述べる。1資産の分類を純関数へ切り出し、previewとapplyが同じ判定を使う | REQ-LC-001、AC-LC-001、SCN-INT-LIFECYCLE-017、SCN-INT-LIFECYCLE-018、SCN-INT-LIFECYCLE-019、SCN-INT-LIFECYCLE-020、SCN-INT-LIFECYCLE-021、SCN-INT-LIFECYCLE-022、SCN-INT-LIFECYCLE-023、SCN-INT-LIFECYCLE-024、SCN-INT-LIFECYCLE-025、SCN-INT-LIFECYCLE-026、SCN-INT-LIFECYCLE-027、SCN-INT-LIFECYCLE-028、SCN-INT-LIFECYCLE-029、SCN-INT-LIFECYCLE-030、SCN-INT-LIFECYCLE-031、SCN-INT-LIFECYCLE-032、SCN-INT-LIFECYCLE-033、SCN-INT-LIFECYCLE-034、SCN-INT-LIFECYCLE-035、SCN-INT-LIFECYCLE-036、SCN-INT-LIFECYCLE-037、SCN-INT-LIFECYCLE-038、SCN-INT-LIFECYCLE-039、SCN-INT-LIFECYCLE-040、SCN-INT-LIFECYCLE-041、SCN-UNIT-LIFECYCLE-001 | TERM-ASC-097 | `02_要件/02_プロジェクトライフサイクル要件.md`、`01_システム概要/02_用語・略語.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md`、`.agent-skill-chain/00_利用案内.md` | Issue #1305、PRは本作業で未作成 | **利用側への影響がある。** recordを失った利用者は`update`で復旧できるようになる。**上書きの到達性は1経路も増えない。** record外かつ正本と相違する資産は従来どおり保持する。既存recordを持つ利用者の判定と出力は変えない。schemaも変えない。**record不在からの復旧は`--recover-record`の明示指定を要求し、指定が無ければ1 fileも書かない。** **dangling symlinkのrecordと壊れたrecordは不在扱いにせず拒否する。** **record不在からの復旧は`--recover-record`の明示指定を要求し、filesystemからの導入推測を撤去した（#1307 案A、owner決裁）。** 配布CLIへflagが1つ増える。 **拒否理由は成功しない手段を名指しせず、previewで判定する。** record公開の残存窓は #1306、祖先directory差し替えは #1309 が所有する。**棄却した代替（競合fileを退避して`install`を実行する運用）は、退避した資産がrecordへ正本digestで登録され終状態が一致しないため採らない。**配布される利用案内へ復旧手順を追記した | 進行役 | 作業tree、base `e3892d94d065c1f804680bece4ef9fdabcf052d6` |
| 2026-09-09 | merge要求時刻とprovider mergedAtの因果順序を要件本文へ明記する。あわせてgh stubがmerge要求時刻を実wall clockで書いていた違反を是正し、fixture clockから供給する | REQ-GH-001、AC-GH-001、SCN-E2E-WFSTEP-050 | 追加・変更なし | `02_要件/03_外部連携要件.md`、`15_要件追跡/`、`test/steps/workflow-step-enforcement.steps.ts`、`test/features/e2e/workflow-step-enforcement-cli.feature` | Issue #1300、PRは本作業で作成 | **製品codeは1行も変更していない。** 既存assertionの削除も緩和もない | **判定そのものは既に実装済みで、欠けていたのは要件本文への明記と意図的な反例だった。** 拒否へ到達していたのはstubの実wall clockによる偶発だけで、`grep -rn "mergedAtより後" test/`は是正前0件だった。決定化でその偶発が消えるため、SCN-E2E-WFSTEP-050で置き換えた。注入試験で基準時刻を現在の6分前にすると是正前は失敗し是正後は成功する | 変更なし | 作業tree、base `7385e94d23a7dc0ab68ccf206e28b1c6ebcab53a` |
| 2026-09-09 | 強制点hookの正本を`.agent-skill-chain/hooks/`へ置き、`install`と`update`が2つのhost pathへ展開する。登録は配らず`doctor`が報告する | REQ-LC-001、AC-LC-001、SCN-INT-LIFECYCLE-010、SCN-INT-LIFECYCLE-011、SCN-INT-LIFECYCLE-012、SCN-INT-LIFECYCLE-013、SCN-INT-LIFECYCLE-014、SCN-INT-LIFECYCLE-015、SCN-UNIT-PACKAGE-028、SCN-UNIT-PACKAGE-029、SCN-UNIT-DOCTORHOOK-001、SCN-UNIT-DOCTORHOOK-002、SCN-UNIT-DOCTORHOOK-003 | 追加・変更なし。**候補に挙げた語を台帳へ足さない。** 他の強制点との境界を決める必要があり本Issueの範囲を超える | `src/domain/lifecycle.ts`、`scripts/check_package_contents.ts`、`.agent-skill-chain/hooks/`、`package.json`、`test/features/integration/lifecycle-isolation.feature`、`test/features/unit/review-policy-package.feature`、`test/steps/lifecycle-isolation.steps.ts`、`test/steps/unit.steps.ts`、`02_要件/02_プロジェクトライフサイクル要件.md`、`15_要件追跡/`、`dist/` | Issue #1105、PRは本作業で未作成 | **「規約では守られなかったので機構にした」ものが機構ごと利用者へ届いていなかった。** `mappings()`にhookが1件も含まれず、実体6 fileはgit未追跡で`package.json`の`files`にも入っていない。**起票時の「両hookはdiffで完全一致」は現在は成立しない。** `.claude`側は2026-09-06にIssue #1233の是正（節の内部限定とカテゴリ付きIDの受理）を受けており、`.codex`側は2026-08-29の3条件独立判定のまま残っていた。**driftが実際に起きていた。** **shellではなくNodeへ移植した。** `check_source_quality.ts`が受理するsourceは`.ts`と`.mjs`だけで、`.claude`は除外directoryに含まれない。**`.sh`を配ると利用者側の`source:check`が必ず落ちる。** 移植の振る舞い同一性を7ケースで実測し全件一致した。**jq依存も消えた。** **登録は配らない。** hostの設定fileへの書き込みは`install`の権限を「packageの資産を置く」から「以後のtool callごとに自動実行されるcodeを登録する」へ広げる。独立諮問も同じ判断を返した。**`doctor`は報告するが`healthy`を変えない。** 本体の欠落・改変はmanaged assetの診断として`healthy`へ入るが、登録は利用者による有効化状態である。診断は「hookが無効です」と断定しない。project-localの設定だけを見ており、global・managed・plugin経由の有効化状態は見えない。**Codex対応を称さない。** 隔離した一時projectで相対pathのhookを置き`codex exec --dangerously-bypass-hook-trust`で実行させたが**発火しなかった**。設定探索path・event名・matcher対象tool名・標準入力schema・path変数のいずれも確認できていないため、Codex用の設定fileを配らない。**`isPackageOwnedPath`へ展開先を足す必要があった。** 足さないと展開はされるがmanaged record検証で拒否され`update`と`delete`が使えない。**同じ罠を#1219のコメントが警告していた。** **`HOST_SKILL_TARGETS`と共通のloopへまとめない。** まとめると展開先の一覧を消す変異が両方を同時に消し、片方だけを壊す変異を検出できない。**AC-04の当初の期待が2つの契約を混同していた。** 展開先を書き換えてupdateを流すのは利用者変更の保持の検査であり、正本の反映の検査ではない。消えた展開先の復元へ組み直し、保持の側は`delete`で測る。**SCN IDが7件すべて既存と衝突していた。** 実装前に実測して採番し直した。**変異試験14件で1件が生存した。** 登録診断をroot `healthy`へ混ぜる変異が、純関数の単体SCNだけでは検出できなかった。**判定関数でなく合成経路を検査する。** 未登録と登録済みの2状態で`doctor`を実行し`healthy`を突き合わせる`SCN-INT-LIFECYCLE-015`を足した。**この反例が実欠陥も捕まえた。** 設定fileが無い場合に`isRegularFile`の`lstatSync`がENOENTを投げて`doctor`が例外になっていた。未登録は正常な状態であり診断の対象であって失敗ではない。**full検証で2件が落ち、いずれも本変更の実影響だった。** (1) `.codex/`が`.gitignore`の対象外であり、`install`が展開先を作るとrepositoryがdirtyになって`SCN-INT-DOGFOOD-002`が落ちた。`.claude/`と`.agents/`は既に対象であり、**同じ扱いへ揃えた。** (2) `scripts/check_package_contents.ts`の変更で故障注入証跡のSHA-256が外れ`SCN-INT-CONSUMER-005`が落ちた。**同fileは証跡がfile全体を束縛しており1行変えると落ちる。** 記録hashを更新した。**外部reviewer（CodeRabbit）がMajor 1件を含む3件を指摘し、いずれも正しかった。** (1) `.claude`がroot外を指すsymlinkだと`resolveContained`が例外を投げ、**`doctor`全体が中断して他の診断を1件も返さなくなる**。登録状態の観測は任意であり、解決失敗は未登録として扱えば足りる。`SCN-INT-LIFECYCLE-016`を足し、是正前の実装が実際に落ちることを実測した。**`install`の前にsymlinkを置くと`mappings()`が先に落ちるため、install後に差し替える順序でなければ`doctor`の観測に到達しない。** (2) 案内が`bash`と`jq`を必要とすると書いていたが、Nodeへ移植済みで両方とも不要である。**移植の際に前提の記述を直していなかった。** (3) 追跡表の実行結果欄が新旧SCNを区別していなかった。001から009は基準commit、010から016は本作業treeの対象実行と分けた。**採らなかった手段を記録する。** 設定fileのmerge、Codex用設定fileの配布、hook本体をhost pathへ複製しない案、規範文書への追記だけ、段階導入flag | 変更なし | 作業tree、base `2eec452ff36705f4621734aa6b557d2dfea0f7c5` |
| 2026-09-08 | PR作成直後のread-backを、closing Issue索引が確定するまで有界で反復する。1回だけ読んで空を返していたため`pr create`の1回目が確定的に失敗していた | REQ-GH-001、AC-GH-001、SCN-INT-GITHUB-026、SCN-INT-GITHUB-027、SCN-INT-GITHUB-028、SCN-INT-GITHUB-029、SCN-INT-GITHUB-030、SCN-INT-GITHUB-031、SCN-INT-GITHUB-032、SCN-INT-GITHUB-033、SCN-E2E-WFSTEP-048、SCN-E2E-WFSTEP-049 | 追加・変更なし。**該当する耐久用語IDは台帳に存在しないため参照しない** | `src/adapters/github.ts`、`src/cli.ts`、`test/features/integration/delivery-finalize.feature`、`test/features/e2e/workflow-step-enforcement-cli.feature`、`test/steps/delivery-finalize.steps.ts`、`test/steps/workflow-step-enforcement.steps.ts`、`02_要件/03_外部連携要件.md`、`15_要件追跡/`、`dist/` | Issue #1271、PRは本作業で未作成 | **欠陥は確定的に毎回起きていた。** 2026-09-08のIssue #1276で自分で踏み、失敗が09:48:43Z、同一commandの再実行成功が09:48:59Zで、索引反映は約16秒だった。**空配列は「1件もcloseしない」という確定判定ではなく未確定である。** ASC経由のPRは作成前に本文の終端keywordがcanonical Issue 1件だけであることを検証され、baseは既定branchであるため、索引が空のまま確定する正当な理由が存在しない。**是正前の再現を先に固定した。** 製品codeだけをHEADへ戻して実測し、`SCN-INT-GITHUB-026`と`SCN-INT-GITHUB-027`が落ちることを確認した。**identityの照合は各観測に対して行う。** 当初案は反復の後に1回だけ照合する形だったが、独立reviewer（codex）が「1回目の不一致が2回目の正常な観測で洗い流される」経路を指摘した。**自分では見つけていなかった穴であり、INV-03として要件化して`SCN-INT-GITHUB-028`を置いた。** **上限は試行回数と経過時間の両方で閉じる。** 片方だけでは待機の実装を誤ったときに無期限へ倒れる。**上限の既定値は暫定である。** 根拠は1点観測だけで分布は測っていない。CLI flag・project choice・環境変数へ出さないのは、利用側が上限を伸ばせると未確定を待ち続ける経路が製品の外から作れるためである。**binding失敗時の案内が実際の回復経路と逆を向いていた。** 「pr createを再実行せず」と述べていたが、実際の回復は同一commandの再実行によるread-only照合である。文言を是正し、**headを動かす前に行うこと**を加えた。**既存fixtureが空のclosing索引を返していた。** `SCN-INT-GITHUB-005`のstubは`closingIssuesReferences`が空配列であり、settleを入れると読み戻し回数のassertionが壊れる。**実PRの契約に合わせてcanonical Issue 1件を返すよう是正した。** **採らなかった手段を記録する。** `sameCreate`からの`headSha`除去（`bindingFromCreatedPullRequest`と`assertImmutablePullRequestBinding`も同じheadへ拘束しており出口にならない）、新CLI `pr reconcile`の追加、delivery stateへのPR番号・URLの耐久化、回復経路`pr.find`へのsettle追加、上限の外出し、段階導入flag、`sleep` executableへの依存。**head移動後の復旧経路はauthority境界に触れるため別Issue #1294へ分離しowner決裁に回す。** 独立諮問2件が範囲で割れたため、広がらない側へ倒した。**変異試験は第1回15件で12 kill、生存3件だった。** 生存はいずれも実在の検査漏れである。(1) 待機を行わない変異は、testが待機0の上限を注入していたため恒等変換になっていた。待機の実在は戻り値へ現れないため、経過時間の下限だけを測る`SCN-INT-GITHUB-031`を足した。(2) 読み戻し自体の失敗をsettleの対象にする変異は、読み取り失敗の観測を1件も持っていなかったため生存した。`SCN-INT-GITHUB-032`を足した。(3) 経過時間の上限を落とす変異は、回数上限が先に効く条件でしか測っていなかったため生存した。**片方の上限が効いている条件では、もう片方を落とす変異を検出できない。** 回数上限を事実上無効にした`SCN-INT-GITHUB-033`を足した。**是正後の第2回で、待機を落とす変異が再び生き延びた。** 差分300msでは他scenarioの負荷に埋もれ、逆に1回目のcold startが大きいと正しい実装でも差分が縮んだ。**assertionが待機ではなくsubprocess起動コストを測っていた。** warm-upを1回捨ててから比べ、注入する待機を1000msにしてその6割を下限にした。baselineの合格と当該変異のkillをそれぞれ3回連続で実測した。**最終は19件で18 kill、生存1件である。** 生存した1件は変異script側の不備であり、置換後の式が振る舞いを変えていなかった。**空振りを生存と数えない。** **単調時計の選択は振る舞いtestでは強制できない。** `Date.now()`へ戻してもNTP補正が起きない環境では恒等であり、`path.sep`と同じ構造の限界である。検査の限界として記録する。 | 変更なし | 作業tree、base `f3bebed6bbe387adcc031d814b457d72b05871d9` |
| 2026-09-08 | 走査境界の観測層が正規化する対象を実行OS本来の区切りだけへ限定する。backslashを無条件に区切りとして解釈しない | REQ-SQ-032、AC-SQ-032、SCN-UNIT-SCANBND-006、SCN-UNIT-SCANBND-007、SCN-UNIT-SCANBND-008、SCN-UNIT-SCANBND-009 | 追加・変更なし。**走査境界に対応する耐久用語IDは台帳に存在しない。** 当初`TERM-ASC-006`と書いたが、同IDは`conformance binding`であり無関係である（独立reviewの指摘で是正） | `src/domain/scan-boundary.ts`、`test/features/unit/scan-boundary.feature`、`test/steps/scan-boundary.steps.ts`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/`、`dist/` | Issue #1276、PRは本作業で未作成 | **Issue本文の事実記述に誤りがあり訂正した。** 本文は`isStagingLifecycleScanPath`が偽を返すと書いていたが、POSIXでは`path.sep`が`/`であり`staging.ts`の`slash()`が恒等変換になるため、`..\draft.md`全体が1つの正常なsegmentになる。実測は3述語とも`false / true / true`であり、`lifecycle`と`scan`の間に差は無い。**隠れているのは`isIssueStagingPath`が偽である点であり、「3述語が1つも実行されない」という欠陥自体は成立する。** 独立reviewerの指摘を受けて自分で再実行して確認した。**正規化を単純に削除しない。** `path.sep`がbackslashとなる環境では`..\x`が本物の親参照である。実行OSの区切りへ限定すればPOSIXでは解釈せず、Windowsでは従来どおり拒否する。**新しい理由codeを足さない。** 解決不能は`incomplete`の`unresolvable-path`、非該当は`uncovered`が既に所有しており、増やすと二重表現になる（諮問で確認）。**受理集合が広がる範囲を10種類の表で列挙した。** 新たに入るのはbackslashの親参照様・現在参照様・先頭末尾・連続区切り様と合法POSIX名であり、真の親参照・現在参照・絶対path・drive修飾・空文字は拒否側に留まる。**当初の「変異試験13件、生存0」は成立しなかった。** 独立review（codex）が13件の外側に生存変異を構成し、実測で**6件中5件が全scenario緑のまま生存した。** drive修飾の字句検査を大文字だけへ弱める、親参照をindex 1以上でだけ拒否する、空segmentを先頭でだけ拒否する、述語へtrimした文字列を渡す、述語へ小文字化した文字列を渡す、の5件である。**原因は反例の作り方にある。** 拒否の各分類を1件ずつしか置かず、述語へ渡す文字列の同一性を1入力でしか測っていなかったため、位置・大小文字・空白で条件を狭める変異が素通しになっていた。**さらに`SCN-UNIT-SCANBND-008`は空振りしていた。** 合法な入力だけを与えていたため`incomplete`が空になり、走査が0回で終わり、testの中へ書き写した6件の配列は製品のunionと接続されていなかった。unionへcodeを1件足しても緑のままだった。**是正は3点である。** 拒否の各分類を先頭・中間・末尾と大小文字で15入力へ広げ、理由codeの列挙を`Record<ScanBoundaryIncompleteCode, true>`で型へ結び、述語引数の観測を小文字化・trim・Unicode正規化のいずれかで必ず変化する4入力へ広げた。**`SCN-UNIT-SCANBND-006`も1述語から3述語へ広げた。** 要求が説明する「3述語が1つも実行されない」を直接検査していなかったためである。**是正後の累計は21件、16 kill、等価3件、script不備2件、生存0である。** unionへcodeを足す変異は`npm run typecheck`が強制主体となる。**A2・A3・B5は等価変異である。** A2とA3はPOSIXで`path.sep`が`/`であるため恒等変換となり、正規化の有無と対象文字の違いが構造的に観測不能である。**この2件が示すのは、`path.sep`を使う選択がPOSIX環境の振る舞いtestでは強制できないという事実である。** B5は空文字検査であり、`""`は`split("/")`で`[""]`となり空segment検査が必ず拒否するため冗長である。**file冒頭のコメントが警告している「消しても検出できない等価な分岐」に当該箇所自身が該当していた。** D1とE1はscript不備であり、等価変異ではなく無効な変異として集計から外した。**空振りを生存と報告しない。****採らなかった手段を記録する。** 正規化の単純削除、新しいincomplete codeの追加、`slash`のexport、段階導入flag、述語側の入力契約の変更、`predicate-unavailable`の解消、空文字検査の削除（本Issueのscope外） | 変更なし | 作業tree、base `0f41b8321ebae692ca32ca559b0abcf32199d218` |
| 2026-09-08 | merge後のCI照合を、head SHAによるrun一覧の再検索から、merge前に固定した`ciRunId`の直読みへ戻す。merge前の選別は1文字も変えない。「run未生成」と「run有りだが対象PRへ未関連付け」を別の診断で報告する | REQ-GH-001、AC-GH-001、SCN-UNIT-CIDEL-008、SCN-UNIT-CIDEL-009、SCN-UNIT-CIDEL-010、SCN-INT-GITHUB-022、SCN-INT-GITHUB-023、SCN-INT-GITHUB-024、SCN-INT-GITHUB-025、SCN-E2E-WFSTEP-044、SCN-E2E-WFSTEP-045 | 追加・変更なし | `src/domain/ci-delivery.ts`、`src/adapters/github.ts`、`src/cli.ts`、`test/features/unit/ci-delivery.feature`、`test/features/integration/delivery-finalize.feature`、`test/features/e2e/workflow-step-enforcement-cli.feature`、`test/steps/ci-delivery.steps.ts`、`test/steps/delivery-finalize.steps.ts`、`test/steps/workflow-step-enforcement.steps.ts`、`02_要件/03_外部連携要件.md`、`10_セキュリティ/01_信頼境界.md`、`15_要件追跡/`、`dist/` | Issue #1280、PRは本作業で未作成 | **GitHub Actions runの`pull_requests`は「現在openで同一headを持つsame-repo PR」の一覧であり、PRが閉じた瞬間に空になる。** そのため`event=pull_request&head_sha=`による一覧再検索は、merge成功後には必ず対象PRを関連付けられず、`outcome=merged`のStep 11を構造的に記録できない。**mockを実仕様へ合わせると43 scenario中9件が落ちることを再現した。** **merge前の選別は1文字も変えていない。** `inspectCiDelivery`のfilter式はbyte単位で不変であり、空`pull_requests`を拒否し続ける。**merge dispatchが許される入力集合は1件も増えない。** 生成入力48件の差分試験で旧実装と新実装の判定が全件一致することを実測した。**共通flagで空許容を切り替える案は採らない。** 緩和がmerge前へ漏れるため、別関数`reconcileFixedMergeRun`と別型`FixedMergeRunObservation`にして、merge前の経路から型で到達できないようにした。**変異試験は累計25件、21 kill、最終的な生存0件である。** 内訳は第1回18件（15 kill）、是正後の第2回6件（5 kill）、第3回1件（1 kill）。**生存した4件はすべて私の検査側の欠陥であり、うち3件は製品の安全条件が無検証だった箇所である。** (1) **B2**「空文字を一致へ倒す」は、`expected === ""`の歯止めを**観測側**でしか測っていなかったため生存した。歯止めが守るのは**固定側**が空の場合であり、`state.merge.ciRunId`が空文字のまま保存されると観測側も空で照合が空虚に成立する。固定側を空にする4ケースを足してkillした。(2) **C2**「照合結果を無視する」は、cliの`if (!reconciled.reconciled) throw`を`if (false && ...)`にしても全緑だった。**E2Eが成立側しか通していなかった**ためである。照合関数の単体SCNは不一致を名指しできても、**cliがその戻り値を捨てる変異を1件も捕まえない。** 固定run観測を不一致側にする`SCN-E2E-WFSTEP-045`を足し、Step 11が記録されないことまで測ってkillした。(3) **C5**「run IDの字句検査を落とす」は等価変異ではない。`runId`は`repos/<repo>/actions/runs/<runId>`へそのまま連結されるため、字句検査はpath injectionの歯止めである。7変種を与える`SCN-INT-GITHUB-024`を足し、**拒否するだけでなくprovider要求を送っていないこと**まで測ってkillした。(4) **B4a**「headRepositoryへrepositoryを流用する」は、fixtureのhead側とbase側が同値だったため観測できなかった。**両者が同値のfixtureはforkを区別できない。** head側が`fork/x`の観測を置く`SCN-INT-GITHUB-025`を足してkillした。**B4（adapterの`head_repository`検証削除）は型検査で落ちるため、実行時の検出力の証拠にしない。** compile可能な弱化としてB4aを別に構成した。**AC-07が未実装のまま仕様がSCNを引用していた。** `SCN-UNIT-CIDEL-010`は存在せず、「run未生成」と「run有りだが未関連付け」は同じ診断へ潰れていた。#1280で確定した構造そのものがこの区別を要求する。**判定を1つも変えず、報告だけを分けた。** `headShaRunCount`を足し、`state`の導出には一切使わない。REQ-LC-011の「報告するがhealthyを変えない」と同じ形である。**`SCN-INT-GITHUB-022`を仕様が引用していたが実在しなかった。** 書いた場で実在を確かめておらず、報告でも「追加済み」と誤って述べた。identity取得の-022と拒否の-023へ分けて実装し、引用を是正した。**測定の汚染を2件記録する。** 変異scriptを背景実行したまま別作業を続けたため、注入中のsourceに対する`npm test`が23件失敗し、実装の欠陥に見えた。同じ状態で独立reviewerへ諮問したところ、注入中の変異体を実在欠陥として、変異scriptのbuild中の`dist/`を配布物削除として報告した。**変異試験・諮問・full検証を重ねない。** **採らなかった手段を記録する。** 共通flagによる空許容の切り替え、merge前の選別への空許容追加、`reconcileFixedMergeRun`の判定へのheadShaRunCountの流用、照合結果の固定merge intentへの書き戻し、reconciliationからの`pr.merge`再送 | 変更なし | 作業tree、base `98a4bc2b1c3d2d84af7b9563f821d150090e3436` |
| 2026-09-08 | worktree surveyとfinalizeの保持理由へ、その領域を所有するcommandの案内を加える。案内は判定を1つも変えない | REQ-LC-009、AC-LC-009、SCN-UNIT-FINALIGN-035、SCN-UNIT-FINALIGN-036、SCN-UNIT-FINALIGN-037 | 追加・変更なし。既存の`TERM-ASC-062`を参照する | `src/domain/worktree-removal-safety.ts`、`test/features/unit/finalize-ignored-artifacts.feature`、`test/steps/finalize-ignored-artifacts.steps.ts`、`02_要件/02_プロジェクトライフサイクル要件.md`、`15_要件追跡/`、`dist/` | Issue #1290、PRは本作業で未作成 | **診断の欠落が安全条件の緩和提案を誘発していた。** `worktree survey --root=.`が19 worktreeをretainとし同型の理由を379回返す一方、17 worktreeすべてで`issue staging --root=<worktree>`が`deletion-ready`を返す。採るべき行動が存在するのに拒否理由が示していなかった。**私自身この診断を機構の欠陥と誤読し、Issue #1248でfinalize allowlistへ一時staging領域を足す案を検討して、codexとfableの双方に棄却された。** **379件のうち187件は工程成果物ではなく作業scratchと派生投影である。** 内訳は`tmp/`直下のscratch 186件と`runtime/`1件で、案内が付くのは`tmp/issues/`配下の192件である。 どの緩和案を実装しても片付かない。**判定を1つも変えない形にした。** 案内はallowlist判定の後に生成し、`safe`にも2つのartifact配列にも影響しない。REQ-LC-011の「報告するがhealthyを変えない」と同じ形である。**`STAGING_LIFECYCLE_AREAS`を流用しない。** 同配列は「真なら保護する」分類の正本であり`tmp`全体を含む。案内が必要なのは`tmp/issues/`配下だけであり、流用すると将来の領域追加で誤った案内が出る。**exportもしない。** 分類の正本が削除許可の正本へ誤用される事故はIssue #1248で検討され棄却された。**変異試験8件、7 kill、等価1件。** 独立reviewerが構成した大小文字畳み込みの変異は当初生存し、`.AGENT-SKILL-CHAIN/TMP/ISSUES/x`の反例を足して是正した。**案内側で正規化を足す変異は等価と判定した。** 空segment、`.`、`..`、絶対path、`\`、制御文字、非NFCは`validArtifactPath`が案内より前で不正観測へ分離するため、案内側の正規化は観測不能である。 案内の連結削除、接頭辞の到達不能化、末尾slash除去による延長の巻き込み、案内文字列の空化、includes一致への緩和、案内が出たpathのallowed側への混入、案内判定のallowlist判定より前への移動。**うち3件は判定を変える方向の変異であり、案内と判定の分離を直接検査している。** **変異scriptの空振りを2件検出して直した。** 案内文字列の部分削除は「issue staging」を残すため反例にならず、前方一致の置換対象はprettier整形後の実体と食い違っていた。**空振りを生存と報告しない。** **「契約を一切変えない」とは書かない。** 変えないのは判定の契約であり、`reasons`の文字列は観測可能な診断契約として意図的に変えている。独立reviewerの指摘で書き直した。**案内文を実行可能な2段階commandへ直した。** 「previewと承認済みhashで」は説明であって実行できる形ではなかった。**採らなかった手段を記録する。** finalize allowlistの変更、`isStagingLifecyclePath`の流用、`owningCommandHint`のexport、理由文のstaging directory単位への集約、案内からの`issue staging`自動実行 | 変更なし | 作業tree、base `7f2aedac0225eccefd82c5a6ed02b8eb0cd09c71` |
| 2026-09-08 | `conformance:check`が起動するcucumberを、conformance bindingが名指しした反例SCNだけへ完全ID一致で限定する。全Gherkinの合格は`quality`段が担うことと受け入れる検出損失を要件へ明記する | REQ-SQ-005、AC-SQ-005、SCN-UNIT-CONFORMANCE-006、SCN-UNIT-CONFORMANCE-007、SCN-INT-CANON-007 | 追加・変更なし。既存の`TERM-ASC-006`を参照する | `src/domain/conformance.ts`、`scripts/check_conformance.ts`、`test/features/unit/risk-policy.feature`、`test/features/integration/canonical-single-source.feature`、`test/steps/risk-policy.steps.ts`、`test/steps/canonical-single-source.steps.ts`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/`、`dist/` | Issue #1281、PRは本作業で未作成 | **実測で決めた。** `npm test`全件1650 scenariosが9分37秒、`conformance:check`が同じsuiteを9分34秒で再実行していた。bindingの49 SCNへ限定すると87 scenarios（Scenario Outline 3件のExamples展開を含む）で26秒になった。**結果の再利用案は成立しない。** `cucumber.mjs`は`format: ["progress", "summary"]`でJSONを出さず、同fileは`PROTECTED_FILES`にある。再利用元が存在せず、hookへ書かせる形は自己評価禁止に触れる。**CIでは同一process tree再利用が一度も発火しない。** `ci.yml`は`quality`と`conformance:check`を別stepで呼ぶ。**判断を純関数へ閉じた。** 当初は呼び出し側にguardを置いたが、`counterexampleScenarioNamePattern(binding) ?? ""`で型を満たしたまま拒否を消す変異が全scenario緑のまま生存した。空patternは全scenarioに一致するため、bindingを空にするだけで検査が素通しになる。`conformanceTestArgv`へ判断を移して単体で反例を当て、是正後にkillすることを実測した。**完全ID一致で閉じた。** repository内の全SCN ID 1525件に対し前方一致の組は現時点0件だが、連番の桁が増えれば成立するため`\b`で両端を閉じた。**合成経路を検査した。** 既定値つき引数のrunner seamを足し、cucumberへ実際に渡るargvを`SCN-INT-CANON-007`で観測する。`--name`引数を落とす変異がkillすることを実測した。**採らなかった手段を記録する。** 結果の再利用・cache・digest条件付きskip、`cucumber.mjs`の`format`変更、`DISTRIBUTION_GATES`からの`quality`分解、純関数の`scripts/`配置、空bindingを限定なしへ倒すこと | 変更なし | 作業tree、base `2bb00af07f09935b4091cdb476c89ffd66c1a0dc` |
| 2026-09-08 | trusted deliveryでcandidate有無とpreview/applyにかかわらず有効なpackageFloorを要求し、省略・undefined・null・不正構造・空rulesを固定errorで副作用前に拒否する。trusted loaderの独立供給責務を明記 | REQ-SQ-004、AC-SQ-004、SCN-INT-LEDGER-010〜014 | 追加・変更なし（TERM-ASC-004/005/095/096を参照） | `02_要件/04_仕様・品質管理要件.md`、`10_セキュリティ/01_信頼境界.md`、`15_要件追跡/00_追跡表.md`、`.agent-skill-chain/schemas/00_利用案内.md` | Issue #1284 | trusted-only callerは正規floor供給へ移行。非trusted preview、共通extension、二段階廃止、package保護を維持。validな偽authorityの真正性はcaller責務 | Issue #1284のAC-1284-01〜05 | 実装 [759fbc413fa1303334cf9eb42dc58448b3ead6a8](https://github.com/techbeansjp-free/AGENTS.md/commit/759fbc413fa1303334cf9eb42dc58448b3ead6a8)。検証記録参照先: [課題1284delivery入力境界レビュー](../../reviews/177_課題1284delivery入力境界レビュー.md) |
| 2026-09-08 | trusted提案とrule ID・raw before SHA-256が一致する完全削除の二段階経路を追加。空project rule inventoryを許容しpackage floorを維持 | REQ-SQ-004、AC-SQ-004、SCN-UNIT-LEDGER-010〜017、SCN-INT-LEDGER-007〜009 | TERM-ASC-095、TERM-ASC-096追加 | `01_システム概要/02_用語・略語.md`、`02_要件/04_仕様・品質管理要件.md`、`10_セキュリティ/01_信頼境界.md`、`15_要件追跡/00_追跡表.md`、schema利用案内 | Issue #1211 | optional提案なし・sourceなしは全削除拒否。部分弱化は受理しない | project policy owner承認の案A | <a id="issue-1211-evidence"></a>実装 H_impl: [d076737306aeb033e2431030704b164a0116236f](https://github.com/techbeansjp-free/AGENTS.md/commit/d076737306aeb033e2431030704b164a0116236f)、[正式review](../../reviews/175_課題1211project-rule廃止経路レビュー.md)、[PR #1282 成功CI](https://github.com/techbeansjp-free/AGENTS.md/actions/runs/34166961391)（pull_request、CI HEAD: `2437b4dedd48153d7ca212e5e3d89208c95bf05e`） |
| 2026-09-08 | worktree surveyのHEAD SHA受理をGitのobject formatが定める40桁と64桁の2値へ整合させる。桁数判定を`isWorktreeHeadSha`へ単一化し、porcelain解析のCLI層と観測検証のdomain層の双方がそれを呼ぶ | REQ-LC-008、AC-LC-008、SCN-UNIT-WTSURVEY-027〜032、SCN-INT-WTSURVEY-016〜017 | 追加・変更なし。既存の`TERM-ASC-062`を参照する | `src/domain/worktree-survey.ts`、`src/cli.ts`、`test/support/world.ts`、`test/features/unit/worktree-survey.feature`、`test/features/integration/worktree-survey.feature`、`test/steps/worktree-survey.steps.ts`、`02_要件/02_プロジェクトライフサイクル要件.md`、`15_要件追跡/`、`dist/` | Issue #1255、PRは本作業で未作成 | **#1251で私が入れた回帰である。** #1251は「attachedなworktreeの分類結果は変更前後で一致する」を掲げたが、object formatを変数として扱っていなかった。**T00で単離した。** 40桁と64桁だけが異なる観測を`surveyWorktrees`へ与え、retain・in-progress・detached保持の3分類すべてが`entries`0件・`errors`1件へ落ちることを実測した。**述語を共有し、汎用名を避けた。** repository内には信頼源の異なるOID検証が50箇所以上あり、うち7箇所は既に40桁と64桁の両方を受理している。汎用名の述語を置くとGitHub provider経路やtrusted-root経路が無検証で採用しうるため、名前にworktree headの役割を含めた。**任意長hexと大文字正規化は採らない。** 前者は誤ったobject IDを分類へ通し、後者は観測経路の異常を隠す。**object formatと桁数の整合確認も採らない。** 追加観測を要し、新たな拒否条件の追加は受理範囲不足による沈黙を除くという本Issueの目的の逆方向である。**実Git fixtureで観測した。** `test/support/world.ts`の`initRepo()`へ任意引数のobject formatを足し、SHA-1側とSHA-256側で生成経路を分岐させなかった。remoteのbare repositoryもhash algorithmを揃えないとpushが拒否される。**独立reviewerが生存変異を2件構成し、どちらも私の見落としだった。** 第一は`registeredWorktreeHeads`へ`toLowerCase()`を挟む変異で、実gitは小文字しか出さないため結合testでは差が出ず、単体testはdomainを直接叩いていたため全scenario緑のまま通った。**解析を`parseWorktreeHeads`としてdomainへ切り出し**、異常porcelainを与える`SCN-UNIT-WTSURVEY-031`で塞いだ。第二は「64桁かつ未pushだけを落とす」変異で、64桁の検査が1状態に偏っていたため通った。全dispositionを走る`SCN-UNIT-WTSURVEY-032`のmetamorphic testで塞いだ。両変異とも是正後にkillすることを実測した。**`initRepo()`の省略時をSHA-1へ固定した。** 省略した`git init`の既定は`GIT_DEFAULT_HASH`と`init.defaultObjectFormat`で変わり、実測でも`GIT_DEFAULT_HASH=sha256`で64桁になった。固定しないと既存fixtureのobject formatが環境設定で無言に変わる。**主張を字句検証へ縮めた。** 「誤ったobject IDを混入させない」は強すぎる。null OIDは字句としては受理するため「誤長の文字列を混入させない」が正確である。**照合を採らない理由も書き直した。** 「目的の逆方向」は成立せず、正しくは追加観測の費用と、入力元が成功した実Gitのporcelainに限られるという信頼前提である。**採らなかった手段を記録する。** 新規file`src/domain/git-object-id.ts`、他約50箇所の一括是正、`src/cli.ts`のmerge commit OID検証の変更、`registeredWorktrees`の変更 | 変更なし | 作業tree、base `791c5da9cbdd3d91c3c980208b3ee45d2503889c` |
| 2026-09-08 | Q-01の対象へ公開Web画面を位置づけ、外部契約の対象でない視覚的調整は影響がないと確認できた場合にだけ対象外とする旨を質問文へ加える。判定例を3行から6行へ増やし、Q-01の根拠欄へ残す確認対象4項目と条件付きの表記修正文を正本へ置く | REQ-WF-011、AC-WF-011、SCN-UNIT-MODEQ-023、SCN-UNIT-MODEQ-024、SCN-UNIT-MODEQ-025 | 追加・変更なし | `src/domain/mode.ts`、`.agent-skill-chain/docs/01_開発ワークフロー.md`、`test/features/unit/mode-questions.feature`、`test/steps/mode-questions.steps.ts`、`02_要件/01_ワークフロー要件.md`、`15_要件追跡/` | Issue #1274、PRは本作業で未作成 | **質問の件数、ID、失格分類、極性、回答形式、`00_モード判定.json`のschemaを変更していない。** `classifyMode`と`detectQuickDisqualifiers`の判定logicも不変である。**#1268で入れた既存の判定例3行と「単独では判定根拠にならない」3つの言い方を削除していない。** 既に記入済みの成果物へ遡及的な要求をしない | **PR #1270が「画面の表示調整をQ-01でどう扱うかはpackage ownerの未決事項」として明記した残存リスクが、実利用のnexus-corporate-websiteで顕在化した。** 起票が示した「同じ事実からquickとfullの両方を説明できる」という整理は訂正した。**現行Q-01は「影響を確認できない場合はfalseとする」と定めており、確認していない状態はfalse側である。** 成立していたのは「表示調整を対象外と確定する基準が無いため解釈の相違が常にfullへ集約されること」である。**私が当初推した「約束したかを唯一の判別子にする」案は採らない。** 未宣言の提供物を取り逃し、内部の約束まで外部契約になり、生成・同期経路の提供先が漏れ、**保証を削除して契約外とする変更を対象外にできてしまう。** 要件レビューでは私のFR-07が同型の抜け穴になっており、「意味・操作識別・支援技術への伝達を変えない表記修正はtrue」の3条件では外部へ提供する固定文字列の変更を除けないと指摘された。条件付きへ是正した。設計レビューでは、正しい6行の直後へ**3 cellの誤った7行目**を足すと`tableBody`が終端として捨て、6行固定の完全一致を素通りすることを実測で再現した。helperを是正した | 作業tree、base `03cbfdfd3d0dfd7aebef5345145afb3d23231d33` |
| 2026-09-07 | SCN配置検査の除外範囲を一時ライフサイクル領域の正本へ揃える。入力契約helperを抽出し、SCN配置検査専用の厳格述語`isStagingLifecycleScanPath`を追加する。REQ-SQ-017の陳腐化した除外理由を改訂し、受け入れる検出損失を明記する | REQ-SQ-017、AC-SQ-017、REQ-SQ-032、SCN-UNIT-SCNSCOPE-010、SCN-UNIT-SCNSCOPE-011、SCN-UNIT-SCNSCOPE-012、SCN-UNIT-SCNSCOPE-013、SCN-UNIT-SCNSCOPE-014、SCN-UNIT-SCNSCOPE-015 | 追加・変更なし | `src/domain/staging.ts`、`scripts/check_trace.ts`、`scripts/report_scan_boundary.ts`、`src/domain/scan-boundary.ts`、`test/features/unit/scn-scope.feature`、`test/steps/scn-scope.steps.ts`、`02_要件/00_要件一覧.md`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/` | Issue #1273、PRは本作業で未作成 | **`isStagingLifecyclePath`の契約を変更していない。** 追跡混入検査とworkspace hygieneの判定は不変である。`isIssueStagingPath`のexportと判定結果も不変で、配布buildのexportは増えるだけである。診断文言と形式、走査範囲、他検査の観測対象も変えていない。**変わるのはSCN配置検査が報告するpath集合だけである** | **クリーンなmainで`trace:check`が18件のerrorで赤くなっていた。** 全件が`.agent-skill-chain/tmp/handoffs/`配下のgitignore済みfileを指し、**CIは緑で手元だけ赤という状態だった。** 原因は除外述語`isIssueStagingPath`の範囲が`tmp/issues`だけで、一時ライフサイクル領域の正本`STAGING_LIFECYCLE_AREAS`の4件より狭かったことである。**REQ-SQ-017が除外しない理由として書いていた「この2領域は`.gitignore`に無く追跡され得る」はREQ-SQ-019（Issue #959）が`.gitignore`へ3領域を追加した時点で陳腐化しており、REQ-SQ-019自身が「この非対称は無関係な検査の設計判断まで歪めた」と書いていた。歪められた側がREQ-SQ-017である。** 起票時に推した「走査を`git ls-files`で追跡fileへ限る」案は`REQ-SQ-017`の「判定にgit追跡状態を用いない」に反するため採らない。**`isStagingLifecyclePath`自身を厳格化する案も採らない。** `\`はPOSIXで通常文字であり`.agent-skill-chain/tmp/..\draft.md`は`tmp`配下に実在しうる合法なfile名であるため、正規化して判定不能へ倒すと`checkLifecycleIgnore`の追跡混入拒否が12件中12件すり抜ける（実測）。**「安全側」の向きは呼び出し元ごとに逆である。** 是正前後の実測は4領域の配置違反4件から0件である | 作業tree、base `1f0c546f299be80721d87a88f8a52e1928abe536` |
| 2026-09-07 | モード判定Q-01の対象を外部へ提供するinterfaceへ限定し、内部の仕様・追跡記録の更新だけでは該当しないことと、生成物を含む影響を確認できない場合はfalseとすることを質問文へ加える。正本へ成立例・反例・不明例の判定例を置き、Step 0のskillから当該節を参照させる | REQ-WF-011、AC-WF-011、SCN-UNIT-MODEQ-023、SCN-UNIT-MODEQ-024、SCN-UNIT-PACKAGE-027、SCN-UNIT-MODE-005 | 追加・変更なし | `src/domain/mode.ts`、`.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/skills/step-00-stage/SKILL.md`、`test/features/unit/mode-questions.feature`、`mode-security.feature`、`review-policy-package.feature`、`test/steps/mode-questions.steps.ts`、`test/steps/unit.steps.ts`、`02_要件/01_ワークフロー要件.md`、`15_要件追跡/` | Issue #1268、PRは本作業で未作成 | **質問の件数、ID、失格分類、極性、回答形式、`00_モード判定.json`のschemaを変更していない。** `classifyMode`と`detectQuickDisqualifiers`の判定logicも変更していない。**判定logicは変更せず、Q-01の質問文、正本の判定例、Step 0 skillの参照指示を変更した。** 配布されるStep 0 skillへ「回答前に正本を読む」という実行指示が1文増える。 既に記入済みの成果物へ遡及的な要求をしない | **起票時の前提「機構が`full`を強制している」は実測で成立しなかった。** `classifyMode`のquick経路は回答と根拠だけを読み、変更pathも文書の規範性も配布宣言も見ない。`detectQuickDisqualifiers`はpoc経路でしか呼ばれず、内部仕様pathへ空を返す。**実測で`docs/specs/`配下4件と`src/styles/_layout.scss`を変更pathとした全問true・根拠付きの回答は`quick`を返した。** 強制していたのは機構ではなく質問の文言であり、列挙の「規範文書」に外部提供の限定がかからないことだった。**起票者が推奨した案1（project-policyへの配布境界宣言）を採らない。** path宣言では生成物経由の外部契約変更を判定できず、宣言の信頼元・重複glob・変更手順の設計が新たに要り、単調性契約へ波及する。案2は配布物への収録と外部契約が一致しないため、案3は規範性を独立の失格分類にする必要性が未立証のため採らない。**正本の「手段の追加を提案する前に、既存手段の縮小で目的を満たせないかを先に評価すること」に従い、新しいgate、validator、CLI、schemaを追加していない。** **SCN-UNIT-MODE-005は是正前でも合格する回帰の固定であり、本件の効果の証拠ではない。** | 作業tree、base `6bf1cc888c74e19958c30b8f5752f99629477d10` |
| 2026-09-07 | 外部契約を変える変更のVerification Setへ`mutation-test`を加える。`02_品質基準.md`へ変異を契約本文の側から作る指示、`03_実装計画.md` templateへ選ばれた検証の充足を記録する欄を置く | REQ-SQ-029、AC-SQ-029、SCN-UNIT-AGILE-028、SCN-UNIT-AGILE-029、SCN-UNIT-AGILE-030、SCN-UNIT-PACKAGE-026 | 追加・変更なし | `src/domain/agile-verification.ts`、`.agent-skill-chain/docs/02_品質基準.md`、`.agent-skill-chain/templates/issue/03_実装計画.md`、`test/features/unit/agile-verification.feature`、`test/features/unit/review-policy-package.feature`、`test/steps/agile-verification.steps.ts`、`test/steps/unit.steps.ts`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/` | Issue #1259、PRは本作業で未作成 | **REQ-SQ-029は元から「critical logicは必要に応じてmutation等を追加する」と定めており、要求を変えていない。** 欠けていたのは追加する経路である。**既存SCN-UNIT-AGILE-005がcriticalだけでは選ばないことを固定しており、その判断を変更していない。** riskではなく`impactAnalysis.externalContractChanged`を条件にしたのはそのためである。`BASE_VERIFICATION`と他の増補規則5件を変更していない。**外部契約を変えない入力の選定結果は従来と同一である。** 一方で外部契約を変える入力の選定結果は`mutation-test`が1件増える。既存の`SCN-UNIT-AGILE-014`と`SCN-UNIT-AGILE-017`はこの条件に該当し、選定結果が増えるが、いずれも特定手段の存在確認であるため合格する。**「既存の選定結果を変えない」という無限定の記述は誤りであり、独立reviewerの指摘で訂正した。****gateを追加していない。選定は要求であって強制ではない** | **`mutation-test`はVerificationMethodとして宣言されていながら、どの入力でも選ばれなかった。** `BASE_VERIFICATION`の10種別のいずれにも含まれず、増補規則も足さなかった。実際に欠陥を捕まえている変異試験が規約でも機構でもなく個々の作業者の習慣として存在していた。**是正前の実測では、new-featureのhigh riskで外部契約変更ありの入力に対し`mutation-test`は選ばれなかった。** 是正後は同じ入力へ加わる。2026-09-07に外部契約を変えた3件すべてで、gateが緑のまま独立reviewerがassertionの空虚さを検出しており、その実測を根拠とした | 作業tree、base `b5346195b4554d9d05ba1856ee588ec3d166a8c5` |
| 2026-09-07 | stdin書込失敗を終了値0で成功に戻さず、安全な診断付きの失敗へ反映する。Codex模擬catalogを一回送信し、testのPATHを未設定・空・値ありへ復元する | REQ-WF-007、REQ-LC-008、AC-PIO-001〜003、SCN-UNIT-PROC-008、SCN-INT-PIO-002、SCN-INT-PIO-003 | 追加・変更なし | `02_要件/01_ワークフロー要件.md`、`14_開発・品質/02_テスト標準.md`、`15_要件追跡/` | [Issue #1265](https://github.com/techbeansjp-free/AGENTS.md/issues/1265)、旧PR #1263 | モデル選択・採用tier・authorityとallowFailure契約の形を維持。main #1262と旧 #1257双方の履歴を新branchへ保持し、旧review承認を流用しない | Issueの3件の独立再現と利用者の継続指示 | 統合基点 `b5118eb2`、現在の修正は作業tree。最終検証headはIssue #1265の提出・review証拠を参照 |
| 2026-09-07 | 影響範囲を扱うStep 5・9・10のskillから「Semantic Graphの利用」節を参照させる。利用節へ、追跡関係のedge種別と言及のedge種別の違い、投影の構築費用、未導入または構築失敗時の退避先を追記する | REQ-GR-011、AC-GR-011、SCN-UNIT-PACKAGE-024、SCN-UNIT-PACKAGE-025 | 追加・変更なし | `.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/skills/step-05-design/SKILL.md`、`.agent-skill-chain/skills/step-09-implement/SKILL.md`、`.agent-skill-chain/skills/step-10-review/SKILL.md`、`test/features/unit/review-policy-package.feature`、`test/steps/unit.steps.ts`、`02_要件/05_グラフ投影要件.md`、`15_要件追跡/` | Issue #1261、PRは本作業で未作成 | **参照の追加であってgateの追加ではない。** Graph Evidenceの`authority: "none"`と`mergeAuthorization: false`を変更していない。**Graphを必須の前提にしない。** 投影の未導入と構築失敗をStepの失敗にせず既存手段へ退避してよいと明記した。CIとnpm scriptからgraphを呼ぶ案は、構築費用と未導入projectでの失敗を理由に不採用とした。templateへのlink集合と既存の`skills:check`診断を変更していない | **実装済みで96 scenario検証済みの機構が、実行するAIへ届いていなかった。** 実測で`grep -rn graph .agent-skill-chain/skills/`が0件、配布templateが0件、CIとnpm scriptが0件だった。規範文書には3箇所あるが、実行契約は「各ステップの開始前に対応するSKILL.mdの全文を読む」であり、skillが参照しない節は実行経路へ届かない。**費用と利得はissuecomment-5563691358で実測済みで、追跡の問いに対しgrepは偽陽性39%、graphは偽陽性0%だった。** **実装者自身が`--edge-kinds=supported-by`だけで問うて誤答しかけたため、edge種別の選び分けを要件へ昇格させた。** `skills:check`のdocs link検査を再利用し、新しい検査器を追加していない。**ただし当該検査はfragmentへも小文字化と記号除去を適用するため、anchorの実在を一般に強制するとは言えない。** 現行3 linkに対しては、SCN-UNIT-PACKAGE-024の固定文字列照合が併せて担保する。変異12件中11件をkillし、生存1件はGraph Evidence正本への案内lineでAC範囲外のため記録のみとした | 作業tree、base `5f6a652a6b46bb892fa7b027e74d7a8ca7e816a3` |
| 2026-09-07 | 単一fileが`MAX_SOURCE_FILE_BYTES`を超えたとき、投影全体をfail-closedせずそのfileだけを意味Graphへ取り込まない。取り込まなかったpathを`graph rebuild`と`graph status`の出力へ件数と先頭5件で報告する。上限を注入できるseamを既定値つきで置き、実repository rootで構築する回帰を追加する | REQ-GR-003、AC-GR-003、SCN-UNIT-SEMGRAPH-034、SCN-UNIT-SEMGRAPH-035、SCN-UNIT-SEMGRAPH-036、SCN-UNIT-SEMGRAPH-037、SCN-UNIT-SEMGRAPH-038、SCN-UNIT-SEMGRAPH-039、SCN-INT-SEMGRAPH-033 | 追加・変更なし | `src/adapters/repository-graph.ts`、`src/cli.ts`、`test/features/unit/semantic-graph.feature`、`test/features/integration/semantic-graph-observation.feature`、`test/steps/semantic-graph.steps.ts`、`02_要件/05_グラフ投影要件.md`、`15_要件追跡/` | Issue #1262、PRは本作業で未作成 | **REQ-GR-003は元から「上限を超える単一file・source集合を意味Graphへ取り込まない」と定めており、要求を変えていない。** 実装が単一fileの超過に対して取り込まない代わりにthrowしていた。**集合byte上限とfile件数上限による拒否は変更していない。** `buildRepositorySemanticGraph`の署名を維持し、診断つきの別関数を追加したため既存呼び出しは壊れない。`graph rebuild`と`graph status`の出力へfieldを追加し、既存fieldを削除も改名もしていない | **是正前にこのrepositoryで`graph rebuild --root=.`が必ず失敗していた。** 診断は`graph source file上限を超えました: dist/vendor/typescript.cjs`で、当該fileは9,112,572 byteである。是正後は同じcommandが4.37秒で完了し、node 3,028・edge 14,304、除外1件を報告する。**既存96 scenarioがfixture rootだけを対象にしており、この欠陥を検出できなかった。** 変異11件中9件をkillした。生存2件は集合上限の二重防御で、片方を消してももう片方が拒否するため出力に差が出ない。**独立reviewerがI/O seamを前提とした反例を構成したため、「決定論的に観測できない」という当初の判断を「実fileのfixtureでは殺せない」へ訂正した。** I/O seamの導入は本件のscope外とし記録のみとする。**この2箇所について変えたのは閾値の参照先だけであり、比較演算・拒否の発生条件・既定の上限値は維持した。** | 作業tree、base `a6d150ac4748cd1388eb36c3135bab458d164121` |
| 2026-09-07 | 起動ごとの公式推奨Codex観測、既存tierMappingのselector採用tier、固定argv/stdinのrouting launchを接続し、Codex固定slugの保守を不要にする。tierのCodex指定も同じ公式観測とtrusted採用へ結ぶ | REQ-WF-007、AC-WF-007、SCN-UNIT-AM-002、SCN-UNIT-AM-004、SCN-INT-AM-003、SCN-INT-AM-005、SCN-INT-AM-006、SCN-E2E-AM-001 | TERM-ASC-014を改名、TERM-ASC-094を追加 | `01_システム概要/02_用語・略語.md`、`02_要件/`、`04_機能/01_ワークフローv0.3.md`、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`10_セキュリティ/01_信頼境界.md`、`12_運用保守/00_運用設計.md`、`15_要件追跡/` | Issue #1257 | 既存resolve fallback・他providerのtier mapping・過去Evidenceは維持。selector採用を既定branchへ反映後に自動起動を利用し、以降の公式推奨変更では設定不要。model証拠はdispatch引数でありprovider内部の実効model保証ではない | 利用者要求と独立review | 初回実装 `9570fd4025d4bd69224d4e41dd40923c5c70561b`。レビュー対象と比較基点は[レビュー168](../../reviews/168_課題1257Codex自動選択レビュー.md)、最新の提出headと外部検証は[PR #1263](https://github.com/techbeansjp-free/AGENTS.md/pull/1263)を参照 |
| 2026-09-07 | 業務ユースケースを単位として機能・画面・バッチと横断設計を突き合わせる設計契約を配布物へ置く。`01_開発ワークフロー.md`へ「ユースケース単位の設計と詳細化」の1節を追加し、Step 2・5・6はそこへ参照だけを持つ。業務・利用者フローtemplateへユースケース詳細の10欄（非同期の依存と完了観測を含む）、要件追跡表templateへユースケースと機能・画面・ジョブの対応表を追加する。個別機能・個別画面・個別ジョブへ関連ユースケース欄、アーキテクチャへ業務判断の所在と技術・基盤の選定根拠欄、API共通契約へschema検証の範囲、コーディング標準へ関数契約、非機能要件一覧へ全行の設計・監視復旧の参照列を追加する | REQ-SQ-008、AC-SQ-008、SCN-INT-SPEC-016 | 追加・変更なし | `.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/skills/step-02-requirements/SKILL.md`、`.agent-skill-chain/skills/step-05-design/SKILL.md`、`.agent-skill-chain/skills/step-06-plan/SKILL.md`、`.agent-skill-chain/templates/specs/`の9 file、`test/features/integration/issue-spec.feature`、`test/steps/issue-spec.steps.ts`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/` | Issue #1256、PRは本作業で未作成 | **既存の要件追跡表の7列と本文を1文字も変更していない。** 先頭7列が位置依存であるため、ユースケースの対応は同一file内の別表として追加した。既存15件のSCN-INT-SPECとstep定義も変更していない。**新しいCLI、schema、validator、deny gateを追加していない。** 既存consumerの仕様を再生成せず、templateの記入欄が増えるだけで既存の記入済み文書は通る | **機械検査が担保するのは記入欄の字面が生成物へ届くことだけである。** 業務意味の妥当性はSCN-INT-SPEC-016では判定できないため、具体例と反例に対する独立reviewのEvidenceを別に持つ。変異試験は記入欄の削除27件、欄名を残して値を空へ置き換える空洞化10件、走査回避5件の合計42件を実施し、40件をkillした。空洞化6件と走査回避3件は独立reviewerが各版で生存させた変異であり、assertionを1行丸ごとの字面、要件行のcell境界での分解、行数と列数と参照列位置の一致へ強化して解消した。生存2件は非機能要件の行の空白だけを変える変異で、cell分解後の入力が元と同一であり契約の充足が変わらないため等価変異と判定し、fixtureを足していない。**「抽出を回避できる」という同型の指摘が3ラウンド連続したため、そこで打ち切った。** Markdown表の構文解析を文字列操作で追いかける限り収束しないためであり、検査の目的を配布固定templateの回帰検出に限定して主張の範囲を要件本文へ明記した。ASC自身の`docs/specs/`へユースケース対応表を新設していない。ASCはCLIで画面とバッチを持たず、非適用行だけの表になるためである | 作業tree、base `58ed41c3756e8252cf4b35524e10faa0611789cb` |
| 2026-09-07 | detached HEADのworktreeをsurveyの分類から落とさず、`headState`と`branch: null`で分類して現行finalize非対応を理由へ加える。不正観測のerrorへpathを含める。text形式は`(detached)`を表示する | REQ-LC-008、AC-LC-008、SCN-UNIT-WTSURVEY-023、SCN-UNIT-WTSURVEY-024、SCN-UNIT-WTSURVEY-025、SCN-UNIT-WTSURVEY-026、SCN-INT-WTSURVEY-014、SCN-INT-WTSURVEY-015 | 追加・変更なし。既存の`TERM-ASC-062`を参照する | `src/domain/worktree-survey.ts`、`src/cli.ts`、`test/features/unit/worktree-survey.feature`、`test/features/integration/worktree-survey.feature`、`test/steps/worktree-survey.steps.ts`、`test/steps/finalize-ignored-artifacts.steps.ts`、`02_要件/02_プロジェクトライフサイクル要件.md`、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`15_要件追跡/`、`dist/` | Issue #1251、PRは本作業で未作成 | **起票時の主張2件を諮問と実測で訂正した。** 「`doctor`がunhealthyになる」は誤りで、`healthy`はinstall健全性だけから計算される。「detachedは一律cleanup-readyにしない」は過剰で、REQ-LC-009は既定branchからの到達を独立の免除根拠として認める。**ただしsurveyだけを変えてcleanup-readyを返すと現行finalizeが必ず拒否する状態を作る**ため、本変更は可視化（段階1）に限定し、finalizeの対象同一性拡張（段階2）を対象外とした。**明示markerだけを根拠にする。** branch行の欠落をdetachedと推測すると、producerの観測漏れと合法なdetachedを区別できない。両方ある・両方ないは不正観測としてpath付きで分離する。**分類はdetachedで自動的に変えず、理由で駆動する。** REQ-LC-010の「不一致は`reasons`へ報告するが分類を変えない」先例と同じ形にし、段階2で理由を外せば自然にcleanup-readyへ進める。**`registeredWorktrees`は変えていない。** 6呼び出し元があり、finalizeの認可条件へ波及するため、survey専用の観測関数を足した。**発生の実例がある。** 2026-09-06に35件中2件が脱落し、ownerの指示による整理では製品経路が31件中27件を拒否したため手動削除へ迂回した。**変異試験7件をすべてkillした。** detached理由の除去、headState不明の受理、矛盾の受理2件、errorからのpath除去、porcelainのdetachedの不正化、text表示の除去を1件ずつ落とした | 変更なし | 作業tree、base `4d83166d3e6167f9ebf6802f43fba0bccc7322a9` |
| 2026-09-07 | `pr/review reanchor --dry-run`を、applyの既存受理条件を共用するread-only evaluatorへ接続する。applyは既存mutation lock内で同じ評価を最新stateへ再実行し、成功時だけ追記・read-backする。Git比較失敗へ比較役割と旧・新のbase/head SHA、該当する`H_impl`を付ける | REQ-WF-005、AC-WF-005、SCN-INT-REANCHOR-008〜015、SCN-UNIT-REANCHOR-031 | 追加・変更なし | `src/adapters/evidence-reanchor.ts`、`src/adapters/delivery-state.ts`、`src/cli.ts`、`test/features/integration/evidence-reanchor.feature`、`test/features/unit/evidence-reanchor.feature`、`test/steps/evidence-reanchor.steps.ts`、`02_要件/01_ワークフロー要件.md`、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`15_要件追跡/`、`dist/` | Issue #1253、PRは本作業で未作成 | **既存の受理条件と順序を維持する。** 完全diff、二層等価、宣言済み`H_impl`検証、冪等性、旧側を先に評価する短絡順序は変えない。preview成功は認可でもapply成功保証でもない。元再現はdelivery stateの旧baseが旧headのancestorでなく、旧headから新headを比較する事象ではない。旧実装ではpreviewが0、applyが非0になる反例を先に確認した。**直接fileを読む初期案は採らなかった。** bounded read、symlink・hardlink・size・TOCTOU、transaction marker整合検査を迂回するためである。既存inspectorとsafe readerを合成したread-only observerを用い、previewでは復旧・lock・永続writeへ進まない。新しい例外class、rule、schema、汎用Git差分adapterは追加・変更しない | repository ownerが同期したIssue #1253 | 作業tree、base `58ed41c3756e8252cf4b35524e10faa0611789cb` |
| 2026-09-06 | 独立reviewの不足によるmerge認可の拒否へ診断を付与する。要求数・観測数・mode・対象HEAD・数え方・次の操作・必要authority・rollbackを返し、宣言した`requiredReviews`が下限1へ引き上げられた事実を根拠へ出す。判定は変えない | REQ-WF-013、AC-WF-013、SCN-INT-MERGE-013、SCN-INT-MERGE-014、SCN-INT-MERGE-015、SCN-INT-MERGE-016 | 追加・変更なし。既存の`TERM-ASC-088`を参照する | `src/domain/delivery.ts`、`test/features/integration/delivery-finalize.feature`、`test/steps/delivery-finalize.steps.ts`、`02_要件/01_ワークフロー要件.md`、`15_要件追跡/`、`dist/` | Issue #1245、PRは本作業で未作成 | **親Issue #1036の実装可能部分だけを切り出した。** #1036は「`pr merge`が承認済みreview例外宣言を参照しない」を欠陥とするが、例外宣言は候補側project policyに置かれ、schemaの`approvalSource`・`approver`・`scope`はいずれも自由文字列で`enum`も`pattern`も持たない。非空検査だけで受理すると**候補が同一PRで独立review要求を自己解除できる**。参照の実装はowner決裁が要るため対象外とした。**#1036の本文より欠陥は広い。** 本文は`assisted`の停止として書くが、`requiredIndependentReviews = Math.max(1, policy.requiredReviews ?? 0)`により有効な全modeが独立approval 0件で停止する。`requiredReviews: 0`を明示しても下限1へ上がる。**「永久に塞がる門を作らない」に対して破れているのは拒否ではなく回復経路の不記載である。** `authorizeMerge`の拒否17経路のうち`Diagnostic`を持つのは`ASC-MERGE-METHOD-001`の1経路だけだった。**`allowed`と`operations`は変えない。** `SCN-INT-MERGE-016`が独立approvalあり・なしの双方で従来どおりであることを検査する。**`policy.mode === "assisted"`の第2の拒否は到達不能である。** `Math.max(1, ...)`は常に1以上のため、`size < 1`のときは第1の条件が先に成立する。到達不能な経路へ診断を付けると検証できない実装が残るため、診断は第1の拒否へ与え`mode`を診断へ含めた。除去は対象外とし観測結果としてIssueへ報告した。**変異試験9件をすべてkillした。** 診断の引き渡し、`next`・`requiredAuthority`・`rollback`の空文字化、要求数・観測数・宣言値差の各記載の除去、`mode`のscopeからの除去、拒否そのものの無効化を1件ずつ落とし、対応シナリオが落ちることを確認した。**本変更は信頼境界を強くしない。** 追加したのは報告だけであり、拒否を許可へ倒す経路も、候補の宣言で認可を緩める経路も作っていない | 変更なし | 作業tree、base `29b2676d0d667f5645c706c67672c35cee8ff684` |
| 2026-09-06 | 配布gate到達性検査へjob-levelの`continue-on-error`を失格条件として加える。静的な`false`、gate呼び出しを持たない別job、`steps:`部分木の属性では失格させない | REQ-SQ-020、AC-SQ-020、SCN-INT-DISTGATE-032、SCN-INT-DISTGATE-033、SCN-INT-DISTGATE-034、SCN-INT-DISTGATE-035、SCN-INT-DISTGATE-036、SCN-INT-DISTGATE-037、SCN-INT-DISTGATE-038 | 追加・変更なし。既存の`TERM-ASC-023`を参照する | `scripts/check_conformance.ts`、`test/features/integration/distribution-gate-reachability.feature`、`test/steps/distribution-gate-reachability.steps.ts`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/` | Issue #1236、PRは本作業で未作成 | **Issue #980がjob-levelを一括で対象外にした根拠は、`continue-on-error`には及ばない。** #980の根拠は「job全体のskipは後続jobの条件へ伝播して配布を止める」であり、skipについては成立する。**握り潰しはskipではない。** gateは実行されて失敗し、そのうえで後続jobが読む`needs`のresultが`success`になる。**起票時は未確認だった。** Issue本文は「どちらであるかを私は実測していない。GitHub Actionsの仕様を根拠なしに断定しない」として2つの確認方法を挙げ、`failure`ならnot-a-bugとしてcloseすると定めていた。`actions/toolkit` Issue #1739の再現報告で`success`であることを確認してから着手した。**本repositoryでの実走行では確認していない。** この限定を要件本文と設計へ明記した。**`steps:`部分木を除外する必要がある。** 除外しないとstep-levelの`continue-on-error`を二重に数え、job全体を不当に失格させる。変異M5がこれを固定する。**job境界の識別も必要である。** gate呼び出しを持たない別jobの`continue-on-error`で失格させてはならない。変異M4がこれを固定する。**受理集合は狭まる方向にだけ動く。** 拒否していた形を受理する経路を追加していない。既存のSCN-INT-DISTGATE-001〜031は判定を変えない。**変異試験は初回6件、ラウンド2で追加2件の計8件をすべてkillした。** 初回は失格条件の除去、拒否理由の除去、静的`false`の誤扱い、job境界の無視、`steps:`部分木の非除外、属性の常時false化である。ラウンド2はindentless sequenceの部分木除外の解除と、sequence marker判定の常時真化である。いずれも1件ずつ落とし、対応シナリオが落ちることを確認した。**外部reviewerがindentless sequenceの誤検出を指摘した。** YAMLは`steps:`と同じ字下げの`-`をその値として認める。深さだけで部分木を切っていたため、2件目以降のstep-levelの`continue-on-error`をjob-levelとして拾い、**無関係なstepの設定だけで有効なworkflowを拒否していた。** 実測で再現してから是正した。**変異試験で追加の欠落も出た。** 部分木判定を常に真へ倒す変異が生存し、`steps:`より後ろへ置いたjob-level属性を固定するscenarioが無いことが分かった。SCN-INT-DISTGATE-038を足して両方killした。**診断文から山括弧を外した。** `needs.`に続く山括弧付きの語を書くと`issue validate`が未解決placeholderとして拒否するため、意味を変えずに表記を変えた | 変更なし | 作業tree、base `4d83166d3e6167f9ebf6802f43fba0bccc7322a9` |
| 2026-09-06 | rebase後の再固定を二層の等価性で判定する。`比較基点..H_impl`の完全diff digestは一致を要求し、terminal review artifactは機械導出される2欄だけを正規化してbyte一致を要求する。再固定後の前round headを実効HEADにする | REQ-WF-005、AC-WF-005、SCN-UNIT-REANCHOR-021〜030 | 追加・変更なし | `src/domain/evidence-reanchor.ts`、`src/adapters/evidence-reanchor.ts`、`src/adapters/review-diff.ts`、`src/adapters/review-session.ts`、`test/features/unit/evidence-reanchor.feature`、`test/steps/evidence-reanchor.steps.ts`、`02_要件/01_ワークフロー要件.md`、`15_要件追跡/`、`dist/` | Issue #1172、PRは本作業で未作成 | **当初案は外部アドバイザーに棄却された。** 「artifact本文の40桁hexを一律に伏せる」案は、別commitのcitation・検証Evidence・例示中のOIDまで変更可能にし、**`audit:check`が検証しないSHA改変を洗浄する。** 正しい範囲は「独立導出でき独立検証できる2欄」だけである。**節の一意性と欄の重複を検査する。** 全文regexでは同定できない。**宣言した`H_impl`はGit objectで検証する。** `H_impl..H_final`が当該artifact 1件だけであることを要求し、caller申告を信用しない。**拒否理由を条件別に名指しする形へ直した。** 理由を1種類にしていたとき、条件を外す変異が5件生存した。名指しへ変えて8件すべてkillした。**`review round`の前round headを実効HEADへ変えた。** これが無いと再固定成立後の前進修正が再び塞がり、正規経路が閉じたままになる。**採らなかった手段を記録する。** anchor・round履歴・Step 10 bindingのin-place更新（再固定ではなくsessionの再定義になる）、`docs/reviews/**`全体の除外、tree hash・patch-idへの弱化、reanchorなしの非ancestor round、staging再作成や`pr create`先行の正規手順化、`audit:check`の照合緩和 | 変更なし | 作業tree、base `f6666d7f11ce4b55656049508a288961d7dbb069` |
| 2026-09-06 | audit境界の`H_impl`を、`H_final`で終わるartifactだけの第1親suffixを遡って決める。`HEAD^`固定をやめる | REQ-GH-005、AC-GH-005、SCN-UNIT-AUDITSEL-015、SCN-UNIT-AUDITSEL-016、SCN-UNIT-AUDITSEL-017、SCN-UNIT-AUDITSEL-018 | 追加・変更なし | `scripts/check_file_audit.ts`、`test/features/unit/audit-artifact-selection.feature`、`test/steps/audit-artifact-selection.steps.ts`、`02_要件/03_外部連携要件.md`、`15_要件追跡/` | Issue #1074、PRは本作業で未作成 | **起票時の主張のうち1件は既に解消していた。** 「round 4は拒否される／予算超過後の正規経路が無い」は#1140の`REVIEW_RECOVERY_ROUND`で解消済みであり、2026-09-06に#980で実際にround 4を通した。Issueへ訂正コメントを残した。**残る中心の問題を実測で鋭くした。** #980では予算3のうち2ラウンドがreviewの実質ではなく帳簿合わせ（テスト件数の訂正と`H_impl`の追随）で消えた。原因は2機構の相互作用で、`pr create`が`latestCandidateHeadSha === PR head`を要求するため最後のroundより後のcommit 1つでroundを1消費し、`audit:check`の`H_impl`が`HEAD^`のため前進commitのたびにartifactの追随が要る。**本変更は後者だけを解く。** 前者（`pr create`のbinding要求）は`02_品質基準.md`のexact-head review契約に属し、変更していない。**外部アドバイザーが提案した「artifact専用のrebind」は採らなかった。** artifactは判定そのものを書く場所であり、機械生成欄と判断部分を区別するには新しい証跡schemaが要る。**起点を`HEAD^`のまま残した。** `HEAD`にすると`H_impl..current`が空になり既存の検出が消える。実装中に基底ケースを誤って既存scenario 2件を落とし、実測で気付いて直した | 変更なし | 作業tree、base `4ddeb7dbcb52566c2070739aa503fb2acf5a2667` |
| 2026-09-06 | 仕様変更履歴が名指しした用語IDが台帳に存在することを要求し、用語ID列の範囲記法を拒否する。既存2行の記法を個別列挙へ直す | REQ-SQ-007、AC-SQ-007、SCN-INT-SPEC-013、SCN-INT-SPEC-014、SCN-INT-SPEC-015 | 追加・変更なし | `src/domain/spec.ts`、`test/features/integration/issue-spec.feature`、`test/steps/issue-spec.steps.ts`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/` | Issue #1129、PRは本作業で未作成 | **#1129が挙げた5候補に加え、版管理どうしで突合する候補を2件測って棄却した。** 変更履歴を索引にする案は68行中8行しか用語ID列が埋まっておらず、review artifactを索引にする案は164件中31件しか言及していない。**「索引にする」ことを諦めて包含関係だけを見れば成立すると分かったのが設計上の転換点である。** 名指しされたIDが台帳に在ることは現在すでに全件成立している。**逆方向は要求しない。** 網羅を要求すると既存が即不合格になり永久に塞がる門になる。**範囲記法を許さない。** `TERM-ASC-089〜091`は走査で089と091しか拾えず中間の090が素通りする。既存2行を個別列挙へ直した。IDの集合は変えていない。**両方の更新忘れは検出できない。** これが#965の事象であり、本変更はその再発を検出しない。この限定を要件本文とreview artifactへ明記した。**実害の実例が出ていた。** 2026-09-06の#1221（PR #1238）で台帳へTERM-ASC-092・093を足しながら変更履歴の用語ID列を「追加・変更なし」のままにし、外部reviewerが指摘して初めて気付いた。**本検査は信頼境界ではない。** `src/domain/spec.ts`は保護対象ではなく同一PRで緩和できる | 変更なし | 作業tree、base `0f6913215646e14edff8fa70b4044b3a1b7b74c7` |
| 2026-09-06 | 配布skillが規範文書と反対の方向を述べないことを要求する。`step-10-review/SKILL.md`の該当段落を規範文書へ揃え、`check_skill_templates.ts`へ必須表現と禁止表現の検査を1組追加する | REQ-WF-005、AC-WF-005、SCN-UNIT-PACKAGE-021〜023 | 追加・変更なし | `.agent-skill-chain/skills/step-10-review/SKILL.md`、`scripts/check_skill_templates.ts`、`test/features/unit/review-policy-package.feature`、`test/steps/unit.steps.ts`、`02_要件/01_ワークフロー要件.md`、`15_要件追跡/` | Issue #1240、PRは本作業で未作成 | **同じ事項について3つの正本が2対1で割れていた。** `01_開発ワークフロー.md:119`と`02_品質基準.md:85`が「条件を満たす場合に同じPRへ取り込む」と定めるのに対し、`step-10-review/SKILL.md:22`は「同じPRへ取り込まない」と述べていた。**製品は規範文書の側を実装している。** 2026-09-06のIssue #980（PR #1237）で、`pr create`後に`review round --apply`（round 4、`REVIEW_RECOVERY_ROUND`）と`workflow record --step=10 --post-terminal-intake`がいずれも通りmergeまで到達した。**skillが根拠に挙げた「Step 11の記録でworkflow journalが封じられStep 10を再記録できない」はこの経路では成立しない。** skill側を規範文書へ揃えた。**条件5件はskillへ複写せず`01_開発ワークフロー.md`を名指しする。** 複写すると二重管理になり再び乖離するためである。**skillが持っていた正しい制約は落とさない。** 「取り直し1ラウンドは収束後にだけ開き`budget-exhausted`からは開かない」は実在する制約であり記述に残した。**検査は必須表現の存在と禁止表現の不在の両方を見る。** 必須だけでは禁止文を残したまま必須語を書き足せば通り、禁止だけでは段落ごと削れば通る。**本検査は信頼境界ではない。** `check_skill_templates.ts`は保護対象ではなく同一PRで緩和できるため、担保するのは偶発的劣化の回帰検出である。**字面検査であり意味を見ない。** 必須語を並べただけの無意味な段落は通る | 変更なし | 作業tree、base `b95ea9f38721e2d47accfc768fe770748088cac6` |
| 2026-09-06 | 故障注入証跡の束縛対象を機構別に宣言し、接続経路に存在しない機構をbridgeへ束縛しない。束縛の強さは変えない | REQ-SQ-027、AC-SQ-027、SCN-INT-CONSUMER-011〜015 | TERM-ASC-092、TERM-ASC-093を追加 | `test/steps/consumer-acceptance.steps.ts`、`test/features/integration/consumer-acceptance.feature`、`docs/evidence/1024-consumer-acceptance/`、`docs/specs/02_要件/04_仕様・品質管理要件.md`、`docs/specs/01_システム概要/02_用語・略語.md`、`docs/specs/15_要件追跡/` | Issue #1221、PRは本作業で未作成 | **起票時の主張のうち1件が実測と食い違った。** `artifact_sha256`と`distribution_digest`は64桁hexの書式検査だけで再算出も比較もせず、versionが進んでも証跡は落ちない。repository全体をgrepして再算出箇所が無いことを確認し、Issueへ訂正コメントを残してscopeから外した。**外部アドバイザーの中心提案（hash一致のhard gateを外し履歴証跡として扱う）は採らなかった。** 門の撤去に当たりowner決裁を要するためである。採ったのは「既存除外の拡大を正確に行う」で、**宣言から外したのは`git-dependency`の`scripts/check_package_contents.ts` 1件だけ**である。根拠は同fileが`checkConsumerAcceptance`を`mechanisms: ["packed-bin", "scale-output"]`で呼び、この機構が接続経路に存在しないという実測である。**判定を実装する`check_consumer_acceptance.ts`とprocess境界の`src/lib/process.ts`はどの機構からも外していない。** 後者は`git-dependency`のinstallでも`run`を使うためである。**残存する制約を2件記録する。** `scripts/check_package_contents.ts`の変更では依然2機構が落ちる。`required`集合と接続部が同一fileに同居しているためで、分離は関数refactorを要し1行の追加に対して支援層が重すぎる。また現行宣言は`src/lib/entrypoint.ts`を含まず依存閉包として既に不完全である。本Issueは閉包の完全性を扱わない。**既存assertionは弱めず強めた。** 診断へ機構名を足したためSCN-INT-CONSUMER-005のassertionを機構名込みへ更新した | 変更なし | 作業tree、base `51d0be6224c8c67c849a28c22a732f1b31737949` |
| 2026-09-06 | 配布前品質検証を呼ぶstepの無条件性を要求する。`if:`を持つstepと`continue-on-error`で失敗を許容するstepを実行とみなさない。前提として`releaseRunSteps`のstep block終端を是正する | REQ-SQ-020、QLT-DISTGATE-006、SCN-INT-DISTGATE-021〜031 | TERM-ASC-089、TERM-ASC-090、TERM-ASC-091を追加 | `scripts/check_conformance.ts`、`test/features/integration/distribution-gate-reachability.feature`、`test/steps/distribution-gate-reachability.steps.ts`、`docs/specs/02_要件/04_仕様・品質管理要件.md`、`docs/specs/11_非機能/01_品質要件.md`、`docs/specs/01_システム概要/02_用語・略語.md`、`docs/specs/15_要件追跡/` | Issue #980、PRは本作業で未作成 | **当初案の「静的に偽と決定できる条件だけを到達不能と扱う」分割軸は誤りだった。** 独立諮問が、危険度を決めるのはskipの伝播先であり、step-levelかjob-levelかであることを示した。job-levelのskipは`tag`の`needs.validate.result == 'success'`へ伝播して配布を止めるため安全側であり、実測でも`validate` jobは`[skip ci]`判定の`if:`を持つため悲観すると永久に塞がる門になる。**逆にstep-levelの実行時条件はjobを成功させたままgateだけを飛ばす真のfail-openであり、当初案はこれを楽観視して素通しにしていた。** 対象をstep-levelへ絞り、`if:`の真偽ではなく存在で失格させることで、GitHub式の評価器を書かずに閉じた。**`; exit 0`は静的検査が受理するだけで実行時の回避にならない。** `bash -e -c 'false; exit 0'`がexit 1を返すことを実測した。`needs:`は辿らない。**T01のstep block終端の是正を先に入れた。** 現行`release.yml`で次jobのheaderと`if:`を含むblockが2件あり、順序を逆にすると最終stepが他jobの条件で失格する。SCN-INT-DISTGATE-009のassertionは、拒否する事実を変えずに診断文言の変更へ追随させた。**外部reviewerがMajor 2件とMinor 2件を検出し、4件とも実測で再現した。** (1) `"if":`と`"continue-on-error":`はquoteしても妥当なYAMLのmapping keyであり、**quote1つで新しい失格条件を迂回できた。** step属性のkey照合をquote許容へ正した。(2) 順序判定が`invocationIndex`を使っていたため、**条件付きstepが公開より前に呼び無条件stepが公開より後に呼ぶworkflowが、存在検査と順序検査を別々のstepで通した。** 順序判定も`unconditionalIndex`へ揃えた。(3) `continue-on-error: ${{ false }}`はGitHubが`false`へ評価する定数式だが失敗許容と誤判定していた。`if:`の無効化判定が既に同じ形を受理しており、**正当なworkflowを拒否する偽陽性**だった。静的な`false`として受理する。(4) 本行の用語ID列が「追加・変更なし」のままTERM IDが要件・SCN列へ入っていた。SCN-INT-DISTGATE-028〜031を追加した | 変更なし | 作業tree、base `33242c62cee62cc131dc45279ffab0c682f3abc2` |
| 2026-09-06 | consumer acceptanceの機構1（実Git依存）を`validate` jobでtag作成より前に実行し、npm公開経路を削除する | REQ-SQ-027、AC-SQ-027、REQ-GH-002、REQ-GH-003、SCN-UNIT-AUTOREL-020〜023 | 追加・変更なし | `.github/workflows/release.yml`、`src/domain/release.ts`、`scripts/plan_release.ts`、`test/features/unit/auto-release.feature`、`test/features/unit/release-plan.feature`、`test/steps/auto-release.steps.ts`、`test/steps/release.steps.ts`、`02_要件/03_外部連携要件.md`、`02_要件/04_仕様・品質管理要件.md`、`12_運用保守/00_運用設計.md`、`15_要件追跡/` | Issue #1216、PRは本作業で未作成 | **「3機構がいずれも実行されていない」は過大だった。** `packed-bin`と`scale-output`は`check_package_contents.ts:163`から毎回実行されており、**未実行は`git-dependency`の1機構だけである。** 起票時の記述をアドバイザー諮問で訂正した。**所要時間を手元で3回実測した**（44・42・41秒、`npm pack`は13秒）。`validate`の`timeout-minutes: 20`に対し余裕がある。**現状のmainで`git-dependency`は合格する。** 本変更は「壊れているものを直す」ではなく「合格していることを一度も確かめていない状態を終わらせる」である。**新jobを作らず`validate`のstepにした。** checkout・Node・`npm ci`・bubblewrapの導入を再利用できる。**event種別で分岐しない。** `push`だけに置くと`workflow_dispatch`が迂回路になる。**tag作成より前で止める。** `v${version}` tag自体が`npx github:`の配布アドレスであり、tag作成後の削除では既にfetch・installされた利用者側状態を回収できない。**`package:check`のmechanisms一覧へは足さない。** `package:check`はCIでも走るため全PRへ42秒が乗る。**npm公開経路は条件付きで許すのではなく存在を許さない形にした。** 条件付きにすると、条件を満たす入力を与えるだけで方針違反の経路が開く。`npm_publish` job、`publish_npm`入力、`npm_enabled` output、release planのnpm stage、`publishNpm`入力fieldをすべて削除した。**独立reviewでCriticalを1件検出した。** `tag` jobの条件が `always() && needs.validate.outputs.state == 'ready'` または `'release'` であり、`needs.validate.result == 'success'` を要求していなかった。`plan_outputs`はacceptanceより前でstateを出力するため、**acceptanceが落ちてvalidateがfailureになってもtagが作られ、続いてGitHub Releaseも作られる。** job結果そのものを要求する形へ直し、`always()`の混入も拒否するようにした。**あわせてHigh 3件を是正した。** acceptance検査が文字列の存在しか見ておらず`continue-on-error`・`if`・`|| true`・commentへのmarker退避で迂回できた点、npm公開不在検査がquoted keyと行継続で迂回できた点、削除の取り残し（QLT-DISTGATE-005の契約、`scripts/inject_publish_version.ts`とそのSCN 2件、用語台帳・移行方針・運用設計のnpm記述）である。**外部reviewerがMajorをさらに2件検出した。** 検証器がworkflow全体の文字列検索だったため、`- name:`へmarkerを置いた非実行行が通り、`run:`の後に置いた`continue-on-error: true`も見落とし、job blockの先頭数行検索のため`name:`へ条件文字列を置く迂回も通っていた。**3件とも実測で再現した。** 検証器をjob rangeとstep rangeの構造単位へ書き直し、markerは`run:` blockの中にあることを要求し、step全体の属性を読み、job-levelの`if:`式だけを条件として読むようにした。変異12件（acceptance要求の除去、npm publish拒否の除去、publish_npm入力拒否の除去、stage一覧へのnpm復活、acceptance機構の差し替え、job結果要求の除去、always検査の除去、if検査の除去、continue-on-error検査の除去、握り潰し検査の除去、quote正規化の除去、行継続正規化の除去）をすべてkillした。**所要時間の読みも訂正した。** 44・42・41秒はacceptance単体で`npm pack`の13秒は別であり、workflowへの追加は約54〜57秒である。20分はacceptance専用ではなくsetup・apt・`verify:distribution`と共有する。**localのwarm cache 3回ではcold CIの上限を証明できない** | owner決裁（npm公開は不要、Git以外で公開しない） | 作業tree、base `6a2bdcbb791e6b677995d70dd5c60e150358add0` |
| 2026-09-06 | 要件本文が特定のSCNを強制主体または検出主体として断定したとき、同じ要件の追跡表行に登録されているかを`trace:check`が突合する。あわせて現存する不一致1件（REQ-WF-013）を是正する | REQ-SQ-007、AC-SQ-007、SCN-UNIT-NAMEDSCN-001〜007、REQ-WF-013 | 追加・変更なし | `scripts/check_trace.ts`、`test/features/unit/named-scenario-trace.feature`、`test/steps/named-scenario-trace.steps.ts`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/` | Issue #1229、PRは本作業で未作成 | **新しいcommandも新しい正本も作らない。** 既存の`collectRequirementDefinitions`と`collectTraceRows`の結果を使い、`collectNamedScenarioClaims`・`parseScenarioExpression`・`expandScenarioRange`の3関数と、`checkSpecNormalization`内の突合blockを足すだけである。**再現→failing check→修正→PASSの順で実測した。** 検査を入れた時点で`trace:check`がREQ-WF-013の4 SCNを不足として報告し、追跡表の是正後にvalidへ戻ることを観測した。**排他的所属を要求しない。** 追跡は多対多であり、名指しSCNが他要件へも結線されていること自体は欠陥ではない。欠陥は名指し元要件への辺が無いことである。**孤立SCNの不在では検出できない。** 名指しSCNが別要件から到達できていれば孤立にならない。**受理するのは閉じた語彙の帰属文に限る。** 動詞1語では通常の仕様記述へ誤って当たる。**偽陽性は全変更で既存gateを止めるため偽陰性より重く扱う。** `検出するのは`の形が既に実在したため、動詞1語を前提とした起票時の見立ては採らなかった。**errorの直接根拠を品質基準の「無記録での通過の禁止」に置かない。** その文言は独立review不在時の文脈にある。根拠はREQ-SQ-007である。**独立reviewでHigh 3件を検出した。** 安全整数を超える数値部で`value += 1`が値を変えず`trace:check`が停止しなくなること（隔離fixtureで`timeout-status=124`を実測）、`である`の後を拘束しないため否定・当為・過去の引用・未決の4文がすべて断定として一致すること、`から`の実位置を見ずに両端を決めるため`AとBからD`で中間IDが黙って落ちることである。**いずれも是正した。** 数値部へ桁数上限と安全整数検査を置き、文の終端を`である。`まで拘束し、列挙の各項が単独IDか`AからB`のいずれかであるとして解釈する形へ組み直した。あわせてsoft line breakをまたぐ帰属文の抽出、tilde fenceの認識、境界fixtureの追加を行った。**外部reviewerがMajorを1件検出した。** code fenceの開始delimiterを先頭1文字だけで保持していたため、4文字fenceの中の3文字fenceや情報付きの開始行を閉鎖と誤認していた。開始delimiterを全体で保持し、閉鎖を同じ文字・開始以上の長さ・後続が空白だけの行に限る形へ是正した。変異22件（突合loopの空化、診断からの4項目脱落、範囲の逆順・prefix・桁数・桁数上限・展開上限検査の除去、code fence除外の除去、tilde fence非認識、範囲の先頭1件への縮退、両端だけの列挙、始端・終端の脱落、結線の全要件横断解決、`である。`終端拘束の除去、連続範囲拒否の除去、`から`位置の先頭固定、実在検査の空化、本文位置の1行ずれ）をすべてkillした。**当初M7は範囲の中間IDを結線済みにしていたため生存した。** 中間IDを結線から外して塞いだ | 変更なし | 作業tree、base `7cf098d940f3ab52c856bf4c62d6e61bfc9e9c5d` |
| 2026-09-06 | 除外述語の被覆を観測し、どの述語にも掛からない生成物を差分として返す。`src/domain/scan-boundary.ts`へ純関数2件、`scripts/report_scan_boundary.ts`へ非権威的な報告scriptを足す | REQ-SQ-032、AC-SQ-032、SCN-UNIT-SCANBND-001〜005 | 追加・変更なし | `src/domain/scan-boundary.ts`、`scripts/report_scan_boundary.ts`、`package.json`、`test/features/unit/scan-boundary.feature`、`test/steps/scan-boundary.steps.ts`、`02_要件/00_要件一覧.md`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/`、`dist/` | Issue #960、PRは本作業で未作成 | **当初の「gateの判定差分を測る」設計は成立しなかった。** 独立reviewerが、除外述語はgate全体ではなくgate内の一部へ適用されることを実測で示した。`isIssueStagingPath`は`check_trace.ts:569`のSCN配置検査だけに適用され、同じMarkdownは`check_trace.ts:397`の要件本文検査へ届く。`check_directory_guides.ts:107`が再帰するのは`.agent-skill-chain`配下だけ、`check_package_contents.ts:129`の実対象は`npm pack`の出力である。**`.claude/hooks/generated.sh`を`directories:check`へ与えると、実gateは見ないのに差分が1になった。** **主張を証明できる範囲へ縮めた。** 観測の単位をgateから除外述語へ変え、測るのは「どの生成物が登録済みのどの述語にも掛からないか」とした。これは#953（`.agent-skill-chain/tmp/issues`は掛かる）と着手時点の`source:check`（`.claude/`はどの述語にも掛からない）の**両方を実際に説明する**。**全gate二重実行のharnessは作らない。** `conformance:check`単体4分22秒、全gate2回で10分超を実測した。**期待する述語の一覧を供給元から導出しない。** 導出すると供給元の削除で期待も同時に縮み欠落を検出できない。**不完全な観測どうしを比較しない。** 見ていない述語がある状態の差なしは根拠にならない。`check_source_quality.ts`の`EXCLUDED_DIRECTORIES`は非公開定数のため観測できず`predicate-unavailable`として不完全に数える。同fileは保護対象で本変更では触れない。**判定不能pathの検査を二重化しない。** 絶対path判定は先頭の空segmentで必ず拒否されるため等価であり、変異試験で生存したので削除した。drive修飾は別に拒否する。対応OSはPOSIXを前提とする。変異12件をすべてkillした。**当初M6は置換自体が失敗しており、M9は期待一覧外の述語をfixtureへ足すまで生存していた** | 変更なし | 作業tree、base `cef40bf3b714e7f9075d86cf7eb393131aefc05d` |
| 2026-09-05 | Step 10が差分の触れた成果物の種別に対応する検査を当てる観点を配布skillへ明記し、その削除を回帰検出する。`step-10-review/SKILL.md`へ2段落を追記し、`check_skill_templates.ts`へmarker検査を1組追加する | REQ-WF-005、AC-WF-005、SCN-UNIT-PACKAGE-020 | 追加・変更なし | `.agent-skill-chain/skills/step-10-review/SKILL.md`、`scripts/check_skill_templates.ts`、`test/features/unit/review-policy-package.feature`、`test/steps/unit.steps.ts`、`02_要件/01_ワークフロー要件.md`、`15_要件追跡/` | Issue #1083、PRは本作業で未作成 | **静的解析ツールを導入しない。** `shellcheck`・`actionlint`・`yamllint`はいずれも未導入であることを`command -v`で実測し、導入はQ-04の依存追加に当たるため対象外とした。CIでの強制も入れない。`.github/workflows/ci.yml`は`PROTECTED_FILES`であり、`package.json`のquality script構成はbase側validatorと候補側validatorが相反するため同一PRで変更できない（#1224）。**報告されたpath `.agents/skills/review/review-code`は`memo/test/アーカイブ/`配下のv0.2系資産であり現行の正本ではない。** 現行の`step-10-review/SKILL.md`は26行で`shellcheck`・`actionlint`・`静的解析`・`make -n`をいずれも0件しか含まず、指摘の本体は成立していた。**本repository自身にはshell 0件・Makefile 0件で、当たるのはworkflow YAML 3件である。** その3件に対する現行の検査は字面照合であり、checkout以前の`ls-remote`という順序の誤りを通してCriticalを1件見逃した実例がある。**本検査は信頼境界ではなく回帰検出である。** `check_skill_templates.ts`は保護対象ではなく同一PRで緩和できるため、「継続的担保」と書かず「削除の検出」と書いた。変異5件（marker定数から1件削除、検査経路ごと削除、errorがmarkerを名指ししない、判定を常に不成立へ、判定を反転）をすべてkillした。**当初M4は別blockの`if (!markdown.includes(marker))`を書き換えており生存に見えた。** 自分のblockを狙い直して落とした | 変更なし | 作業tree、base `ca305324342734514084dc19c7f3538261dd07c1` |
| 2026-09-05 | `doctor`が契約を満たしたまま未完で止まっているstagingを分けて報告する | REQ-LC-011、AC-LC-011、SCN-UNIT-WFPATH-003、SCN-UNIT-WFPATH-004、SCN-UNIT-WFPATH-005 | 追加・変更なし | `src/domain/workflow.ts`、`src/domain/lifecycle.ts`、`test/features/unit/workflow-step-enforcement.feature`、`test/steps/workflow-step-enforcement.steps.ts`、`02_要件/02_プロジェクトライフサイクル要件.md`、`15_要件追跡/` | Issue #954、PRは本作業で未作成 | **門を足さない。`healthy`の判定を1文字も変えない。** worktreeの後片付け報告と同じ扱いで、報告の分離だけを行う。**Issue本文の「列挙が無い」は成立しない。** `doctor`は既に全stagingを列挙し`currentStep`と`nextStep`を返していた（`lifecycle.ts:555`、`workflow.ts:1029`）。真の欠落は「契約を満たさない古い記録」と「単に途中で止まっている」が同じ`valid: false`へ潰れ、手を入れるべきものを選べないことだった。**実測で28件中9件が後者と判明した。** 当初「Step 11到達済みなのにnext 10を返すのは判定の誤り」と考えたが誤りで、同じ出力の`journal.errors`が`Step 10にreviewSession bindingが必要です`を返しており正しい挙動だった | 変更なし。報告の分離 | 作業tree、base `68c5bf1cfba06da6f4299a30f9c7717f8fda7620` |
| 2026-09-05 | CI runの観測を成否で絞り込まず、拒否診断へ配送状態と次に採る行動を含める | REQ-GH-001、AC-GH-001、SCN-UNIT-CIDEL-001〜007 | 追加・変更なし | `src/domain/ci-delivery.ts`（新規）、`src/adapters/github.ts`、`src/cli.ts`、`test/features/unit/ci-delivery.feature`（新規）、`test/steps/ci-delivery.steps.ts`（新規）、`02_要件/03_外部連携要件.md`、`15_要件追跡/` | Issue #969、PRは本作業で未作成 | **門を新設しない。** 拒否そのものは従来どおりで、診断の内容だけを厚くする。`actions/runs`の問い合わせから`status=success`を外し、絞り込みを純関数へ移した。**成功判定は従来どおり呼び出し側にあり、受理集合を1件も広げていない。** GitHub Actionsの遅延22分を不達と誤診し、organization管理者の対応が必要という誤った結論で人間を呼んだ実例がある。**観測値を並べるだけでは再発しうる**ため、`undelivered`を人間へ上げる唯一の条件として次の行動を文言で固定した。**猶予時間は正本文書へ宣言せず、判定コード内の命名済み定数とした。** 宣言しても判定はその定数を読むため、二重の正本を作らないためである。外部への諮問2者のうち1者が「無記録での通過の禁止は独立review節の文脈でありこれを根拠にできない」と指摘し、根拠を差し替えた | 変更なし。診断の是正 | 作業tree、base `f365944eae0bbfb42c4dcb1b092081ef9ceb406d` |
| 2026-09-05 | PR createのdispatch claimを、認証・権限・remote SHA再検証より後のprovider要求直前へ移す。claimの受け渡しがないPR作成をfail-closedで拒否する | REQ-WF-013、AC-WF-013、SCN-INT-GITHUB-019、SCN-INT-GITHUB-020、SCN-INT-GITHUB-021、SCN-E2E-WFSTEP-043 | 追加・変更なし | `src/adapters/github.ts`、`src/cli.ts`、`test/features/integration/delivery-finalize.feature`、`test/features/e2e/workflow-step-enforcement-cli.feature`、`test/steps/`2 file、`02_要件/01_ワークフロー要件.md`、`15_要件追跡/` | Issue #1157、PRは本作業で未作成 | **受理集合を広げない。** claim消費後の成否不明は従来どおり再送を禁止する。**変わるのは、変更要求を1度も送っていない失敗をその扱いから外すことである。** REQ-WF-013は既に「provider call直前に永続dispatch claimをfsyncし」と定めており、実装がこれに反していた。`gh auth status`は`pr.create`の第1文にある`verifyRepository`の第1文で呼ばれるため、認証の一時失敗でclaimだけが消費され、stagingが`reconciliation-required`から出られなくなっていた。**新しいstate fieldもerror型もsubcommandも足していない。** claimを消費したかどうかの真偽値だけで送信前の失敗と成否不明を分ける。**claimの受け渡しを型で必須にし、実行時にもfail-closedで確かめる。** 任意にすると、claimを取らずに変更要求を送れるprimitiveが公開され並行実行で重複PRを作れる（外部への諮問2者のうち1者が指摘） | 変更なし。既存要件への適合 | 作業tree、base `a226eaa11b5a0033616f060d75c1814d83edf40f` |
| 2026-09-05 | trusted-quality.ymlの読み取り失敗で、その値に依存しない`validateTrustedQualityMigration`まで飛ばしていた早期returnを`else`分岐へ置き換える。品質契約versionを11から12へ上げる | REQ-SQ-030、AC-SQ-030、SCN-UNIT-QUALITY-019 | 追加・変更なし | `scripts/check_project_quality.ts`、`package.json`、`test/features/unit/source-quality.feature`、`test/steps/unit.steps.ts`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/` | Issue #1125、PRは本作業で未作成 | **受理集合を狭めない。** 読み取り成功時の判定を1件も変えず、読み取り失敗が`valid: false`を返すことも変えない。**変わるのは1回の実行で報告されるerror集合だけである。** すぐ上の`ci.yml`の読み取りは既に`if/else`であり、同一file内の扱いの不一致も解消する。本変更は保護fileの内容変更であり、既定branchへ事前登録した`TQP-TRUSTED-READ-CONTINUE-002`（11→12）が承認境界を満たす。**是正後の内容はPR #1222の証跡`docs/reviews/148_…md`の2節のdiffから逐語で再現した**（sha256が`afterSha256`と一致）。両方向の変異試験2件（早期returnへ戻す／依存する検査もelseの外へ出す）をいずれもkillした | 変更なし。二段階proposalの2段目 | 作業tree、base `2f0d2288dea778f54769acfc5eb19f6a66e96ed2` |
| 2026-09-05 | Claude Code側の常時入口`CLAUDE.md`をrepository直下へ置き、配布物と`install`/`update`の展開対象へ加える | REQ-LC-001、AC-LC-001、SCN-E2E-LIFECYCLE-001 | 追加・変更なし | `CLAUDE.md`、`package.json`、`src/domain/lifecycle.ts`、`02_要件/02_プロジェクトライフサイクル要件.md`、`15_要件追跡/01_変更履歴.md` | Issue #1219、PRは本作業で未作成 | **hostごとの非対称を解消する。** Codexが読む`AGENTS.md`は存在し配布もされていたが、**Claude Codeが読む`CLAUDE.md`はrepositoryにも製品にも存在しなかった。** `install`/`update`が展開するhost資産は`HOST_SKILL_TARGETS`の2件だけで、**skillは呼び出されたときに読まれる登録口であり常時の入口ではない。** 本repositoryでの作業でも規範文書が自動では読み込まれておらず、2026-09-04に仕様の記述を実態と取り違えた誤りが複数発生した。**`CLAUDE.md`は規約を重複定義せず`AGENTS.md`を指す。** `ASC-CANON-SINGLE-SOURCE-001`のtoken検査はrepository rootを走査しないため機構では捉えられず、設計で避けた。あわせて`isPackageOwnedPath`がfile名を直接持っていた重複を`ROOT_ASSETS`参照へ直した（**展開はされるがrecord検証で拒否される形を実測した**） | 変更なし。owner指摘（2026-09-05）への対応 | 作業tree、base `de9dee38ea854f2021b6fc4e90fcaa9ebe3ddf20` |
| 2026-09-04 | `dist/`を版管理下へ置き、承認機構つき環境でも`npx github:`が即実行できるようにする。生成物を個別監査とモード判定質問の複製検査から外す | REQ-GH-005、AC-GH-005、REQ-SQ-005、SCN-UNIT-AUDITBUMP-008 | 追加・変更なし | `.gitignore`、`dist/`配下61 file、`scripts/check_file_audit.ts`、`scripts/check_conformance.ts`、`02_要件/03_外部連携要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1187、PRは本作業で未作成 | **受理集合を広げる。** 従来は`prepare`が`npm run build`を実行して`dist/`を作っていたが、**install scriptの承認機構を持つ環境（pnpm既定の`onlyBuiltDependencies`など）では実行されず、CLIが起動しなかった。** 利用側はpnpmである。`dist/`を版管理下へ置くと、Git remoteから取得したtreeがそのまま実行可能になる。**`prepare`と`prepack`は残す**（`check_project_quality.ts`が内容を完全一致で強制する保護fileであり、削除にはproposal経路が要る。#1187 Phase 2）。許可環境では再buildされるが内容は同一である。実行に不要な`.d.ts` 58件と`.map` 116件は追跡しない。**既存gateとの衝突2件を実測で検出し是正した**（個別監査表が生成file行で埋まる／モード判定質問が複製として拒否される） | 変更なし。外部への諮問（codex・fable）の結論に従う。**`build`の改名とCIの変更は不要と判定して採らなかった** | 作業tree、base `5512712a0b93da30eb9071d78c5c288fc0bd6a90` |
| 2026-09-04 | npm registryへ公開しないことをproject固有ruleとして宣言し、強制点の存在をconformanceで検査する | REQ-SQ-004、AC-SQ-004、SCN-UNIT-LEDGER-009 | 追加・変更なし | `package.json`、`.agent-skill-chain/project/rules/no-registry-publish.json`、`.agent-skill-chain/project-policy.json`、`scripts/check_conformance.ts`、`src/domain/conformance.ts`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1215、PRは本作業で未作成 | **強制点を1つ足す。** owner決裁（2026-09-04）「npm 公開は不要 かつ今後同じことが起きないようにプロジェクト固有ルールとして。ここは強制力があってもいい」に従う。**強制点はnpm自身であり、`package.json`の`private: true`が`EPRIVATE`でpublishを拒否する。** dummy認証つきの実publishで観測した（`--dry-run`と未認証はこの拒否に到達しない）。conformanceが見るのは**その宣言が存在し続けること**であり、npmの判定を重複させない。**release workflowの公開経路は対象外**とした。`npm_publish` jobがconsumer acceptance gate（#1024）の唯一の呼び出し元であり、除去にはgateの移設が要るためである（#1216） | 変更なし。owner決裁の範囲 | 作業tree、base `53604960e0076848b69f06a2277023185bf3b9e5` |
| 2026-09-04 | 公開入口の取得元をnpm registryからGit remoteへ是正する。README・中央利用案内・機能仕様へGit remote指定と版固定の形を記載し、短縮表記の意味を1箇所で定義する | REQ-SQ-005、AC-SQ-005、SCN-UNIT-README-001 | 追加・変更なし | `README.md`、`.agent-skill-chain/00_利用案内.md`、`docs/specs/04_機能/01_ワークフローv0.3.md`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/01_変更履歴.md` | Issue #1213、PRは本作業で未作成 | **文書を実態へ合わせる。** `npm view agent-skill-chain` はE404であり、案内どおりに実行すると404になっていた。**誤っていたのは実態ではなく記述である。** `npx`はregistryを経由せずGit remoteから動くことを実測した（`npx --yes github:techbeansjp-free/AGENTS.md --help` が21秒で成立。`#v0.3.1-beta.83` の版固定も成立）。owner決裁（2026-09-04）「Git以外で公開するつもりがない」「npxでのインストール及びアップデートしか対応しない」に従い、#984（npm publish）は対応しないでcloseした。**48件のusage例は書き換えず、短縮表記であることを1箇所で定義する形にした** | 変更なし。`cli:check` が要求する `npx agent-skill-chain <command>` の字面は保持している | 作業tree、base `34935668c086db9a819b2feda9553298fff80272` |
| 2026-09-04 | 追跡表と実装計画のtemplateが要求していたproject選択層×SCNの直積を、担保する層だけの記録へ縮小する | REQ-SQ-008、AC-SQ-008、SCN-UNIT-PACKAGE-008 | 追加・変更なし | `.agent-skill-chain/templates/specs/15_要件追跡/00_追跡表.md`、`.agent-skill-chain/templates/issue/03_実装計画.md`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/01_変更履歴.md` | Issue #995、PRは本作業で未作成 | **要求を縮小する。** 直積を要求していたが、実行される振る舞いを持たないSCN（設定fileの記述内容、文書の節の存在、文書間の機械突き合わせなど）では全層が非適用になり、追跡表の大半が非適用行で埋まる。利用側報告では21 SCNのうち14件が両層とも非適用だった。**担保漏れの検出という目的は、担保がある組み合わせと、担保が無いSCNの代替手段を記録することで維持される。** 本repository自身の追跡表は元から直積ではなく「1行は1つのFeature完全pathとtest layer」で運用しており、**templateだけが自分の実践と違うことを要求していた** | 変更なし。利用側報告 #995 への対応。`04_レビュー.md`は既に「失敗またはskipがある層だけ」へ縮小済みで、本変更は同じ向きの追随である | 作業tree、base `29ff66c6eae42a572ec5b9a73fff387e546bd0ea` |
| 2026-09-04 | trusted project ruleの削除・弱化について、拒否診断が必要authorityと候補側経路の不在まで返すようにする。あわせて廃止の正規手順と遷移モデルを利用案内へ明記する | REQ-SQ-004、AC-SQ-004、SCN-UNIT-LEDGER-008 | 追加・変更なし | `src/domain/enforcement.ts`、`.agent-skill-chain/schemas/00_利用案内.md`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #967、PRは本作業で未作成 | **判定を変えない。** 拒否する集合も理由も同一で、変わるのは`next`の文言だけである。**「既定ブランチへ正規migrationせよ」は、その手段が製品内に無いため利用者を循環させていた**（`policy migrate`も`compareTrustedPolicy`を使う）。実測で確認したうえで、authorityと経路の不在を返す形へ改めた。PR #1043 が`projectChoices`の縮小で採った形と同じである | 変更なし。利用側報告 #967 への対応。**廃止経路そのものの新設は配布契約変更でありowner決裁を要するため別Issueとする** | 作業tree、base `9a3303f28c1c1e1ac0fa095596cadd99bd682926` |
| 2026-09-04 | test層の語彙をpackage側で定義しないことと、宣言済みtest層の除去が二段階手順で可能であることをschemaへ明記する | REQ-WF-006、AC-WF-006、SCN-UNIT-TRACE-009 | 追加・変更なし | `.agent-skill-chain/schemas/project-choice.schema.json`、`.agent-skill-chain/schemas/project-policy.schema.json`、`02_要件/01_ワークフロー要件.md`、`15_要件追跡/01_変更履歴.md` | Issue #998、PRは本作業で未作成 | **受理集合を変えない。** `minItems: 1`も型も単調性判定も変えない。**変わるのは、語彙を定義しない設計判断と縮小経路が正本に書かれることだけである。** 起票時の「二度と直せません」は #1044 の縮小提案で解消済みであり、`SCN-UNIT-CHOICE-010`が`testLayers`の縮小を検査している。**残る要望（単調性の対象から外す案A）はowner決裁であり別Issueへ分離した** | 変更なし。利用側報告 #998 への対応 | 作業tree、base `5137183f96a5f104841ea8328844202d39e66cfa` |
| 2026-09-04 | 禁止テストsuffixの空配列を「この方針を持たない」の正規表現として明文化する。schema 2件へdescriptionを足し、運用ポリシーへ単調性3 fieldの縮小経路を明記し、空配列を受理することをscenarioで固定する | REQ-WF-006、AC-WF-006、SCN-UNIT-TRACE-010 | 追加・変更なし | `.agent-skill-chain/schemas/project-choice.schema.json`、`.agent-skill-chain/schemas/project-policy.schema.json`、`.agent-skill-chain/docs/00_運用ポリシー.md`、`02_要件/01_ワークフロー要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #982、PRは本作業で未作成 | **受理集合を変えない。** 空配列は変更前からschema上validであり、`check_trace.ts`も禁止0件として扱っていた。**変わるのは、その意味が正本に書かれることだけである。** 利用側が「設定ミスで空になった」と「意図的に方針を持たない」を区別できるようになる。schemaの`required`も型も変えないため、既存の利用側policyはすべてvalidのままである | 変更なし。利用側報告 #982 への対応であり、案(B)（`capabilities`形式への型変更）はowner決裁が要るため別Issueとする | 作業tree、base `7c9c91646d002d01836b8044eea0abcc278fbad0` |
| 2026-09-04 | 運用設計から廃止済みbump経路の記述を除き、release workflowのjob集合と運用設計の権限境界表の一致を機械検査へ加える | REQ-GH-003、AC-GH-003、SCN-UNIT-RELWF-P05〜P11 | 追加・変更なし | `02_要件/03_外部連携要件.md`、`12_運用保守/00_運用設計.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1191、PR #1197 | **配布物の記述を是正する。** `docs/`は配布境界に入るため、#1190 が残した「同一file内の矛盾」（64行が「bump branchは作らない」と書きながら権限境界表が`bump_version` jobを載せる）は運用者を存在しない経路へ誘導していた。**外部reviewerがPR #1190 でMajorとして検出した。** 8箇所を是正した。**検査は既存機構の流用を先に評価した。** `canonical-contracts.json`のASC-CANON-SINGLE-SOURCE-001はMarkdown間の複製検出であり、`.github/workflows/release.yml`をcanonicalに指定できないため使えなかった。判定は`src/domain/release.ts`が所有し、`scripts/check_conformance.ts`はfileを読んで渡すだけにした。**両方向を検査する。** 片方向だけでは文書を空にすれば通る。変異7件全kill。**PR #1195 の外部reviewでjob ID文法の狭さ（fail-open）と表の走査範囲の広さを指摘され、両方を是正した。** うちM3（job名のindent限定の除去）とM4（表の1列目限定の除去）は当初生存し、fixtureへ値を持たないnested keyと表外のjob名参照を足して塞いだ | PR #1190 への外部reviewer指摘 | 作業tree、base `0c485f66cf5a4d74c07c8f19b9fd4e75ec82a598` |
| 2026-09-04 | post-terminal intakeのjournal追記経路を実際に開く。`--post-terminal-intake`をboolean flagとして登録し、`appendWorkflowJournalEntryLocked`の2つの無条件封印をintakeのStep 10 1種類だけ通す条件へ変える | REQ-WF-004、AC-WF-004、SCN-UNIT-WFJRNL-028、SCN-E2E-WFSTEP-040〜042 | 追加・変更なし | `02_要件/01_ワークフロー要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1201、PRは本作業で未作成 | **受理集合を1種類だけ広げる。** #1194 は順序判定だけを直しており、追記経路は封じたままだった。**規範文書が許した取り込みがmain上で実行できない状態**であり、本変更はその差を埋める。他Step・通常のStep 10・intake指定の無いterminal後のStep 0〜10は引き続き拒否する。**D-2・D-3の封印にはtestが1件も無かった**ため、公開CLIと追記API経由の検査を4件追加し、3件の変異をすべてkillした | 変更なし。#1194 と同一のowner決裁の範囲 | 作業tree、base `38fe9ff2559447341b9a7b8c3632a4e7acaeea02` |
| 2026-09-04 | `pr create`後の外部reviewer指摘を、条件付きで同じPRへ取り込めるようにする。Step 10のjournal記録へ`postTerminalIntake`を設け、Step 11より後に置いたうえで順序判定から外す | REQ-WF-004、AC-WF-004、REQ-WF-005、AC-WF-005、SCN-UNIT-WFJRNL-022〜027 | 追加・変更なし | `.agent-skill-chain/docs/01_開発ワークフロー.md`、`02_要件/01_ワークフロー要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1194、PRは本作業で未作成 | **守る性質を変えない。** 「独立reviewerが確認した内容とmergeされる内容が一致すること」は、HEADが動いたときに同sessionの次roundで再reviewすれば保たれる。**封じていたのは記録側だけであり、性質そのものではなかった。** 2026-09-03から04にかけて、外部review指摘によるPRの作り直しが4回（#1177→#1179、#1189→#1190、#1192→再構築、#1195→再構築）発生し、1回あたり実測で概ね1時間を要した。実装の是正そのものは各30分未満である。運用ポリシーの「支援層の所要時間が成果物構築の所要時間を上回らないこと」に反していた。**`audit:check`は変更していない。** 実験で4 commit構造（実装→artifact→是正→artifact更新）を受理することを確認し、artifactの`H_impl`行を新しい実装commitへ直せば通ることを実測した。codexへ諮問し「削るべきは検証ではなく再演である」「同一PRで修正・exact-head再検証すれば得られる保証との差は示されていない」との判定を得た。変異6件のうち5件をkillし、M5（field許可一覧からの除去）は当初生存したためparseを通す往復の反例`SCN-UNIT-WFJRNL-027`を足して塞いだ | repository ownerの明示決裁（工程縮小の許可を含む） | 作業tree、base `0c485f66cf5a4d74c07c8f19b9fd4e75ec82a598` |
| 2026-09-03 | pass根拠語彙検査のcode fence誤検出と行内語彙の取りこぼしを是正する。fence追跡、登録語彙×cellの直積、判定値正規化の内部文字保持、循環引用の拒否の4件 | REQ-SQ-031、AC-SQ-031、SCN-UNIT-CLAIMVOCAB-009〜018 | 追加・変更なし | `02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1188、PRは本作業で未作成 | **受理集合が両方向に変わる。** fence追跡は受理を広げ（偽陽性の除去）、直積と循環引用拒否は受理を狭める（偽陰性の除去）。PR #1186 への外部reviewer指摘5件のうち**4件を実コードで確認しvalidと判定し、1件（証拠が別cellにある場合の反例が無い）は`SCN-UNIT-CLAIMVOCAB-006`が既に検査しているためvalidでないと判定した**。F-03（原文引用の判定がbacktick個数だけ）は、`docs/reviews/` 130 fileを走査して**`pass`行と登録語彙の組68件、うちSCN併記0件、引用あり16件、引用が登録語彙そのもの2件**を実測したうえで、実在検査（案A）を**採らず**循環引用の拒否（案B）を採った。実在検査は`grep -n "純関数"`・`[skip ci]`・`permissions`のような正当な引用を巻き込み、**F-01と逆向きの偽陽性を同じPRで作る**ためである。変異10件のうち9件をkillし、1件（判定列を対象から外す行の削除）は**等価と確認した**。生存した変異のたびにscenarioを足しており、M2（異なる記号でfenceを閉じる）・M6（fence記号の行頭限定の除去）・M9（閉鎖delimiterの長さ条件の除去）・M10（info string条件の除去）が該当する。`pass`へ正規化されるcellは登録語彙を含み得ない | PR #1186 への外部reviewer指摘 | 作業tree、base `c5650ffbc15d1964c3c53611880b6ba82e65cd76` |
| 2026-09-03 | releaseをtag駆動にし、既定branchへ書き込まない。`bump_version` jobとbump PRとadmin mergeを廃止し、versionの正本をGit tagへ移す。`package.json`はrelease追随をやめた sentinel `0.3.1-managed-by-tag` を持つ。npm公開直前にだけtagからversionを注入する。あわせて旧release bump除外をcutoff commitのancestorへ限定し、push起点のstale run停止と2-parent判定を追加する | REQ-GH-003、AC-GH-003、REQ-GH-005、AC-GH-005、SCN-INT-AUTORELEASE-003、SCN-INT-AUTORELEASE-011、SCN-INT-AUTORELEASE-012、SCN-INT-AUTORELEASE-013、SCN-UNIT-AUTORELEASE-007、SCN-UNIT-AUDITBUMP-005、SCN-UNIT-AUDITBUMP-006、SCN-UNIT-PACKAGE-012、SCN-E2E-AUTORELEASE-002 | 追加・変更なし | `02_要件/03_外部連携要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1184、PRは本作業で未作成 | **利用側への影響がある。** git依存consumerが読む`package.json.version`がsentinelになる。実利用者 `RUA-PROM/nexus-corporate-website` は `scripts/agent-skill-chain.sh` で**commit SHA固定**しており、かつ「固定参照の正本は`ASC_REF`であり`managed-assets.json`の version ではない」と自ら明記しているため、**この consumer にとっては既に成立している状態の明示化である。** codexとfableへ独立に諮問した。codexが案A（tag駆動）を条件付き承認し、私の起票内容の誤り2件（ASCが既に`queue: max`と後続更新スキップを持つという記述、配布digestがversion非依存である理由）を訂正した。fableがcodexの懸念（managed asset recordのversion依存）を実コードで**空振りと判定**し（`record.version`は書くだけで読んで比較する箇所が0件）、代わりに真の破壊点が`scripts/build.ts:99-103`の3箇所version一致検査であることを特定した。**案Aが消すのはprerelease番号のbumpだけであり、policy schemaのcore bump commitは既定branchへ残る。** `queue: max`はGitHub公式文書で実在を確認したうえで**追加しない**と判定した。tag駆動では取り消されたpending runを取り戻す必要がなく、運用ポリシーの「手段の追加より既存手段の縮小を先に評価する」に従う | repository ownerの「起票して最優先で行えば今後かなり早くなるのでは」という指示 | 作業tree、base `7a0fff678e99483baf0f25dd4132c67172a61f7e` |
| 2026-09-03 | base候補からGit revision syntaxを除外する。あわせて検査完了から`git worktree add`までの間へtest専用のseamを設け、TOCTOU回帰を観測可能にする | REQ-GH-004、AC-GH-004、SCN-UNIT-BASEBRANCH-006、SCN-UNIT-BASEBRANCH-007、SCN-INT-WORKTREE-016 | 追加・変更なし | `02_要件/03_外部連携要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1181、PRは本作業で未作成 | **受理集合を狭める方向の変更である。** `main~1`のようなrevision syntaxは`isMergeBaseCandidate`（`*`の有無だけを見る）を通っていたが、`refs/remotes/origin/main~1^{commit}`が親commitへ解決されるため**branch tipでないcommitをbaseにできた**。外部reviewer（CodeRabbit）がPR #1180 で検出し、`pr create`より後の指摘のため本Issueへ分離した。**#1179 のreview artifactが「変異M9は観測不能」と記録していたが、seamの追加でkillできるようになった。** 私の見立てが足りなかっただけである。変異2件（literal検査の除去でSCN 7件、seam経由のTOCTOU復活で1件）を全件kill | repository ownerの指示による外部review取り込み | 作業tree、base `9636ff1745e348c66915b0763b76049ef82242b4` |
| 2026-09-03 | review artifactの`pass`根拠に高リスク語彙が裸で置かれていないかを検査する。人が明示登録した語彙を検出し、同じcell内へSCN IDか原文引用の併記を要求する | REQ-SQ-031、AC-SQ-031、SCN-UNIT-CLAIMVOCAB-001〜008 | 追加・変更なし | `02_要件/00_要件一覧.md`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1169、PRは本作業で未作成 | **機械検査は真偽を判定しない。** 意味的真偽の完全自動判定は一般的方法を持たない。検査するのは「未検証の主張が黙って`pass`根拠として使われていないこと」だけである。codexとfableへ独立に諮問し、**適用単位はcurrent review artifact 1件（Q）、artifact内の検査範囲は`pass`判定行に限定（R）**という組み合わせを採った。**既存artifactを遡及検査しない。** 実測で`docs/reviews/` 130 fileに該当語が97箇所あり、該当88行のうち63件（71%）は同じ行にSCN IDまたはbacktickを持つ。**この71%は書式上の候補率であって検証済み率ではない。** 裸の25件のうち少なくとも18件が`pass`行である。遡及是正は本Issueの成果物を超え、過去のartifactへ事後証拠を足すと当時のreview事実と現在の文書が混ざる。既存記録の誤りが確定した場合は#1165型の追補・訂正記録で扱う。変異5件中4件kill、M2は等価変異 | repository ownerの指示による諮問結果 | 作業tree、base `9636ff1745e348c66915b0763b76049ef82242b4` |
| 2026-09-03 | worktreeのbase束縛を既定branch1本からtrusted policy宣言済みの長命branch N本へ広げる。**policy authorityとbase authorityを分離する。** trusted policyは常にprovider default branchのtipから読み、baseの選択に依存させない | REQ-GH-004、AC-GH-004、SCN-UNIT-BASEBRANCH-001〜005、SCN-INT-WORKTREE-010〜014 | 追加・変更なし | `02_要件/03_外部連携要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1139・#1179（#1139の再実施）、PRは本作業で未作成 | **既定branchをbaseにする従来の呼び出しは1文字も変わらない。** `--base-branch`を省略すると既定branchをbaseとして扱う。**受理集合は広がるが束縛の形は同一である。** 宣言（trusted policy）とprovider観測tipへの一致を各baseへ同じ強度で要求する。wildcardを含む要素はbaseにしない（既存の`isMergeBaseCandidate`を共有）。既定branchは宣言の有無によらず常に受理する。codexとfableへ独立に諮問し、両者が案A（宣言済み長命branchの受理）を条件付き採用、案B（既定branch固定の維持）を「永久に塞がる門」として不採用と判定した。**両者が独立に「policy authorityとbase authorityが1つの等式へ潰されている」ことを核心と指摘した。** 変異5件全kill。うちM5（基点commitとbase SHAの一致要求の除去）は当初生存し、SCN-INT-WORKTREE-014を追加して塞いだ。M3が「申告tipとprovider観測の不一致」、M5が「申告どおりのtipを持つbranchなのに実際の基点がそこでない」で**別の境界**である | repository ownerの指示による諮問結果 | 作業tree、base `28b7ebf6464a184fb17629c86d69a2e878eb79ae`（#1179で再実施） |
| 2026-09-03 | squashを基本NGとする。gh adapterのmerge方式解決から既定値のsquashを外して未知値を例外にし、配布するpolicy雛形の`methods`を`["merge"]`だけにする。規範文書の「policyが許可する場合にsquashを使用できる」とsquash専用projectのレシピを削除する | REQ-GH-004、AC-GH-004、SCN-UNIT-MERGEMETHOD-009、SCN-UNIT-MERGEMETHOD-010 | 追加・変更なし | `02_要件/03_外部連携要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1107、PRは本作業で未作成 | **利用側への影響がある。** squashで運用しているprojectは`pr merge`が拒否するようになる。**既にそうなっていた**（`methods`にsquashが無ければ`src/domain/delivery.ts`が拒否する）が、配布雛形と規範文書がsquashを勧めていたため利用側がsquash構成を選べていた。**製品側の強制だけでは不足である。** web UIと`gh pr merge`は`pr merge`を経由しないため、GitHubのrepository設定でsquash mergingを無効化する必要があることを利用案内へ明記した。実測では2026-09-02のmainでsquash 4件がすべてrelease failure、merge commit 3件がすべてsuccessだった（#1107の起票時実測）。変異3件（未知値をsquashへ倒す旧実装への復帰、雛形methodsへのsquash復帰、雛形branchMethodsへのsquash復帰）を全件kill。**着手時にadapterでsquashを無条件に例外にしたところ既存4件（SCN-E2E-WFSTEP-015・035・037、SCN-INT-GITHUB-011）が落ちた。** policyで許可した方式をadapterが上書きしていたためであり、**直すのは既定値だけ**に絞り直した | repository ownerの明示決裁「squashは基本NGとします」 | 作業tree、base `1db2a07ad268eb43f13dc7846be0f17f2394c8aa` |
| 2026-09-03 | `audit:check`のラウンド上限を判定の正本からimportする。`check_file_audit.ts`が`3`を直書きしていたため、PR #1150 が上限を4へ引き上げても追随せず、`review round`が受理するround 4を`audit:check`が拒否していた。`review-convergence.ts`の`REVIEW_RECOVERY_ROUND`をexportしてimportする形へ変え、同じ値を2箇所で持たない | REQ-SQ-023、AC-SQ-023、QLT-STEPCHAIN-001、SCN-INT-STEPCHAIN-001、SCN-INT-STEPCHAIN-010 | 追加・変更なし | `11_非機能/01_品質要件.md`、`15_要件追跡/` | Issue #1159、PRは本作業で未作成 | **上限そのものを変えない。** 4はowner決裁済み（2026-09-03）であり、本変更は追随漏れの是正である。**PR #1150 の実装漏れであり私の誤りである。** `check_file_audit.ts:626`のdoc commentは「上限は`02_品質基準.md`が所有する。ここは同じ値を強制するだけ」と明記していたにもかかわらず、所有元が変わったのに追随しなかった。反例`SCN-INT-STEPCHAIN-001`を4から5へ直し、4を受理する`SCN-INT-STEPCHAIN-010`を足した。**採番時に既存の`005`と衝突しており`010`へ直した。** 変異3件（上限を3へ直書き、上限を5へ、比較を`>=`へ）を全件kill。**M1が本欠陥そのものの再現であり回帰が固定された** | repository ownerのIssue指示 | 作業tree、base `dd063283` |
| 2026-09-03 | enforcement export走査の判定不能を「実在しない」と区別する。`hasExport`を3値の`findExport`へ変え、解析できないsourceには専用の診断を出す | REQ-WF-008、AC-WF-008、SCN-UNIT-SAT-014 | 追加・変更なし | `02_要件/01_ワークフロー要件.md`、`15_要件追跡/` | Issue #1134、PRは本作業で未作成 | **判定不能を合格へ倒さない点を変えない。** 3値のうち`unparsable`と`absent`はいずれもerrorを積む。迂回経路は増えない。**既存`SCN-UNIT-SAT-014`のverdictが2値だったため、解析不能の5 fixtureが`invalid`として通っていた。** verdictを3値へ広げ`unterminated-string`・`decimal-divide`・`string-divide`・`postfix-divide`・`regex-divide`を`unparsable`へ直した。変異4件（unparsableをabsentへ潰す、unparsableを診断しない、unparsableをpresentへ倒す、absentを診断しない）を全件kill。**M3は判定不能を合格へ倒す変異であり、fail-closedが保たれることを示す** | repository ownerのIssue指示 | 作業tree、base `7236c33603d5634d59ac5ec00746f75ca68b3441` |
| 2026-09-03 | 未解決placeholderのerrorが原因の字面を列挙する。先頭5件までを列挙し、6件目以降は残数だけを示す。あわせてquick・poc templateのラベル行2種の丸括弧を言い換えた | REQ-SQ-008、AC-SQ-008、SCN-UNIT-ISSUEPLC-005、SCN-UNIT-ISSUEPLC-006、SCN-UNIT-ISSUEPLC-007、SCN-UNIT-ISSUEPLC-008 | 追加・変更なし | `02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1147、PRは本作業で未作成 | **診断だけを変え、受理する文書の集合を変えない。** 起票時の主張のうちinline codeの取りこぼしは現在のコードに存在せず（`withoutCode`が行ごとに`withoutInlineCode`を呼ぶ）、全templateで`<...>`・`{...}`の一致は0件であることを実測した。丸括弧はquick 12件・full 19件・poc 21件・01 27件を検出するが、**埋めた文書に残るラベルは2件だけ**であり、検出器へ判別規則を足さずtemplate側の2行を言い換えて解消した。`TEMPLATE_PARENTHETICAL_PLACEHOLDERS`はtemplate自身から導出される集合であるため、この言い換えは受理集合をその2字面だけ広げる。判別規則を足す案は広がりが限定できないため採らない。変異5件（上限を10へ、上限を4へ、字面の非表示、丸括弧判定の除去、angle/brace判定の除去）を全件kill | repository ownerのIssue指示 | 作業tree、base `dd063283a0d8e02394e13ae163db2435612bbb6c` |
| 2026-09-03 | `runJsonlSession`の打ち切り理由を`stderr`へ残す。出力上限の超過、timeout、子processの起動失敗の3経路それぞれについて、どの上限を何バイト超えたか・何ミリ秒で打ち切ったか・起動できなかった原因を記録する | REQ-LC-008、AC-LC-008、SCN-UNIT-PROC-005、SCN-UNIT-PROC-006、SCN-UNIT-PROC-007 | 追加・変更なし | `15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md` | Issue #1027、PRは本作業で未作成 | **制御フローを変えない。** 3経路とも従来どおり`SIGKILL`して終了値1で終わる。変わるのは`stderr`の内容だけである。`allowFailure`の意味も変えない。**1MiBというbound自体を変えない。** #993の`run()`は既定64MiBへ引き上げたが、こちらはstreamであり同列に扱わない。変異5件（3経路の理由削除、stream名の固定、timeout値の固定）を全件kill | repository ownerのIssue指示 | 作業tree、base `dfa81e0efb6478f6a74b5dccf88bac623abf9990` |
| 2026-09-03 | 仕様変更履歴の1行が8列で見出しの9列と一致せず、`判断者`と`HEAD SHA`が列ずれしていたのを是正する。`Issue・PR`列を挿入した | 該当なし | 追加・変更なし | `15_要件追跡/01_変更履歴.md` | Issue #1155、PR未作成 | **表の内容を1文字も変えず、欠けていた列を挿入しただけである。** `docs/specs/`配下の全Markdown表を数え直し、**列数不一致は本件1件だけであることを確認した。** 最初の計数はエスケープ済みパイプ`\|`とinline code内のパイプを列区切りと誤認して8件と報告しており、訂正した。**列数検査は足さない。** 起票時に自ら定めた「1件のために検査を足すか、N件あるから足すかで判断が変わる」という条件に照らし、1件であるため検査を足す判断へ倒さない | repository ownerのIssue指示 | 作業tree、base `3d048a5c` |
| 2026-09-03 | 支援層の分量を判断の量へ比例させる原則を、運用ポリシーと配布template 4件へ明記する。機械検査が要求するのは節や欄の存在であって分量ではないこと、判断の無い箇所で語を増やさないこと、同じ理由で対象外になる項目は1行へまとめてよいことを述べる | REQ-SQ-024、AC-SQ-024 | 追加・変更なし | `.agent-skill-chain/docs/00_運用ポリシー.md`、`.agent-skill-chain/templates/issue/00_要求定義_full.md`、`01_要件定義.md`、`02_設計.md`、`03_実装計画.md`、`02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/` | Issue #1135、PR #1154 | **既存の受理集合を1件も変えない。** 検査codeへの変更0行であり、`issue validate`が要求する4点（fileの存在、開発考慮事項の4行表、未解決placeholderの不在、`Scenario: SCN-`の行）は不変である。**実測に基づく。** 本日merge した5 PRのstaging 00〜03は1件平均34,806文字で、うち機械検査が要求する最小は隔離実験で9,002文字だった。差の74%は要求されていない。**起票時の「29節のうち17節が機械検査に要求されていない」は誤りで、実測は14節・12強制・任意2である。** templateは1件3,400〜5,500文字であり削減対象として妥当でない。効くのは書き方の側である。`02_設計.md`の「該当しない節は削除せず、対象外理由を1文で記録する」が節ごとの反復を生んでいたため、無記録を防ぐ性質を保ったまま「同じ理由なら1行へまとめる」を足した | repository ownerの明示承認（2026-09-03） | 作業tree、base `239eb366d7aec4c64090fcc0b27983c048e10c69` |
| 2026-09-03 | Step 10のreviewラウンド予算3とは別枠で、収束後のHEAD移動に対する取り直しをround 4として1回だけ受理する。round 4で未解決が残れば`budget-exhausted`とし、round 5以降を拒否する。あわせて`pr create`より後に届いた外部reviewerの指摘を同じPRへ取り込まず、follow-up Issueの新しいstagingで完全な工程を通すことを規範文書へ明記する | REQ-WF-005、AC-WF-005、SCN-UNIT-REVIEWCONV-006、SCN-UNIT-REVIEWCONV-007 | 追加・変更なし | `02_要件/01_ワークフロー要件.md`、`.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/docs/02_品質基準.md`、`.agent-skill-chain/skills/step-10-review/SKILL.md`、`.agent-skill-chain/templates/issue/04_レビュー.md`、`15_要件追跡/` | Issue #1140、PR #1150 | **`budget-exhausted`からの取り直しを開かない。** 未解決blockerを抱えたまま予算を使い切った状態から新品の予算をもらえてはならず、`SCN-UNIT-REVIEWCONV-004`が固定する。**round 4を収束後だけに開く条件を別の分岐として書かない。** `REVIEW_ROUND_BUDGET`到達後の非収束状態は`budget-exhausted`しか取り得ないため到達せず、置くと外す変異が生存する死んだ分岐になる。status導出の`>=`がこの含意を保証し`SCN-UNIT-REVIEWCONV-007`が固定する。**取り直しが使えるのは`pr create`より前だけである。** Step 11の記録後はworkflow journalが封じられ、Step 10を再記録できない。PR #1145 でこの袋小路を実地で再現した。変異5件（上限を5へ、statusを`===`へ、上限を3へ、parse上限を3へ、budget-exhausted判定の削除）を全件kill | repository ownerの明示決裁（2026-09-03） | 作業tree、base `12444655` |
| 2026-09-03 | `audit:check`が境界commitの親をちょうど2個持つとき、`H_impl..current`の4診断へ両親それぞれの着地形file数と、追随merge上でHEADを取っている可能性と、未commitの可能性を注記として添える。**候補側の親の選択は変えない** | QLT-AUDSEL-001、QLT-AUDSEL-003、REQ-GH-005、AC-GH-005、SCN-UNIT-AUDITSEL-013、SCN-UNIT-AUDITSEL-014 | 追加・変更なし | `11_非機能/01_品質要件.md`、`15_要件追跡/` | Issue #1004、PRは本作業で未作成 | **判定の`valid`を1件も変えない。** `main`の直近30 mergeと非first-parent 25 mergeの計55 commitで是正前後の`valid`が全件一致し、緩和は0件である。うち20件で診断へ注記が付いた。**着地形で候補側を選び直す案を先に実装し、実測で棄却した。** `SCN-INT-AUDITBUMP-004`が従来の不合格を合格へ倒した。追随merge後のHEADでは、取り込んだ既定branchの変更を監査範囲から外したまま合格になる。祖先関係を先に見る案も`SCN-INT-AUDITBUMP-004`で同じ緩和を起こした。**したがって選択は`.at(-1)`のまま変えず、診断だけを直した。** Issue #1004 の「誤っているのは診断内容である」という記述と一致する。変異7件のうち6件をkill。生存1件は着地形の件数を`review head`でなく`parent`から数える変異で、release bump親を持つ構成でしか区別できない診断の数値差である | repository ownerのIssue指示 | 作業tree、base `89de9189` |
| 2026-09-03 | review成果物を版管理下へ置く手順を配布物へ明記。`docs/01_開発ワークフロー.md`・`skills/step-10-review/SKILL.md`・`templates/issue/04_レビュー.md`・`review evidence`のhelpへ、`docs/reviews/`または`.agent-skill-chain/reviews/`配下へ複写し実装commitの後にその1 fileだけをcommitして`H_final`にする手順を書く | REQ-WF-005、AC-WF-005、SCN-UNIT-PACKAGE-013 | 追加・変更なし | `.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/skills/step-10-review/SKILL.md`、`.agent-skill-chain/templates/issue/04_レビュー.md`、`src/cli-usage.ts`、`15_要件追跡/` | Issue #1141、PRは本作業で未作成 | **REQ-WF-005の本文を変えていない。検査codeへの変更は0行である。** `review evidence`・`audit:check`・`delivery-state.schema.json`の3箇所が既に版管理下を要求しており、**配布物に`docs/reviews`の記述が0件だった**ことが実利用者の詰まりの原因である。stagingの出力先と「一時情報を耐久化しない」性質は変えない。**report修正案のdigest照合切替は採らない。** staging digestはproviderからattestされず、`H_final`の独立承認境界を壊す | repository ownerの決裁 | 作業tree、base `3968a1c7` |
| 2026-09-03 | `checkRepositoryRuleLedger`の公開入口へ合成されている個別検査18件を、宣言と実際の呼び出しの双方向で突き合わせる。宣言にあって合成されていない検査と、合成されていて宣言に無い検査のいずれも拒否する | REQ-SQ-004、AC-SQ-004、SCN-INT-LEDGER-005、SCN-INT-LEDGER-006 | 追加・変更なし | `02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/` | Issue #988、PRは本作業で未作成 | **`scripts/check_conformance.ts`と`src/`を1行も変更していない。** 追加したのはtest層のScenario 2件とstep 5件だけである。既存の`SCN-INT-MODEQ-011`は`checkModeQuestionText`1件について同型の検査を持っており、本変更はそれを18件へ一般化する。**4件へ一律に与えるとは1件を4件へ複写することではない。** 複写すると5件目を足したとき同じ穴が残る。双方向にしたのはそのためであり、`validateReviewExceptions`の登録漏れを実際に検出した。個別検査ごとに違反treeを作って実行する案は、`replicate`がrepository全体の複写とproject policy一式を要求するため採らない。**配線の存在で回帰を検出するという既存の判断を踏襲する。** 変異5件（合成行の削除4件、未登録の検査追加1件）を全件kill | repository ownerのIssue指示 | 作業tree、base `2df6133b` |
| 2026-09-03 | 配布templateとStep skillのうち機械強制されていない支援層を縮小。`00_要求定義_full.md`の§5.2を削除して§6.1と01のNFRへ委譲し、`04_レビュー.md`の§7の手書き件数表を成功CI runの参照と合計と失敗層の展開へ置き換え、`step-10-review/SKILL.md`のtemplate全文読取をDC判定が`applicable`の領域と差分が触れた領域へ条件化する | REQ-SQ-008、AC-SQ-008、REQ-SQ-009、AC-SQ-009、SCN-UNIT-PACKAGE-013 | 追加・変更なし | `.agent-skill-chain/templates/issue/00_要求定義_full.md`、`.agent-skill-chain/templates/issue/04_レビュー.md`、`.agent-skill-chain/skills/step-10-review/SKILL.md`、`15_要件追跡/` | Issue #1137、PRは本作業で未作成 | **REQ-SQ-008とREQ-SQ-009の本文をいずれも変えていない。** Step 10のtemplate読取をDC判定へ条件化したため、判定の妥当性を所有するREQ-SQ-009も追跡へ含める。 機械強制されている節を1件も削っていない。**削減候補6件のうち3件が機械強制であることを実測で判明させ対象外にした。** `00`の§10と§11は`issueRequiredHeadings`の必須12見出しに含まれ、`04`の§0.1は`check_skill_templates.ts`の`ROUTING_INPUT_CONTRACT_MARKERS`が5 markerを強制する。`issue validate`・`skills:check`・`audit:check`の受理集合を変えない。既存の記入済み成果物は削除した節を持っていても通る。**根拠は`00_運用ポリシー.md`の成立条件「支援層の所要時間が成果物構築の所要時間を上回らないこと」であり、実測で違反していた** | repository ownerの決裁 | 作業tree、base `b6acf118` |
| 2026-09-02 | `ASC-MERGE-METHOD-001`のbase候補から、ワイルドカードを含む要素を除く。**`merge.branches`は3役を兼ねるが、head allowlistとしてのglobはbaseにならない。** 除外後の要素が2件以上のときだけ警告する。あわせて配布規範文書・schemaの`description`・policy利用案内へ3役とワイルドカードの意味を明記し、squash専用構成の記入例を足す | REQ-GH-004、AC-GH-004、SCN-INT-MERGEMETHOD-004〜006 | TERM-ASC-088を追加 | `02_要件/03_外部連携要件.md`、`01_システム概要/02_用語・略語.md`、`.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/schemas/project-policy.schema.json`、`.agent-skill-chain/policy/00_利用案内.md`、`15_要件追跡/` | Issue #1035、PRは本作業で未作成 | **`authorizeMerge`のhead allowlist判定と`resolveMergeMethod`の長命branchペア判定を1行も変更していない。** 変わるのは`warn`強度の助言の発火条件だけであり、`valid`と`errors`は不変である。**具体名を2件以上列挙した構成では従来どおり発火する。** 除外条件は「ワイルドカードを含む」という構文だけで、判定側が既定branch名を知らないため意味的な除外は行わない。**実利用者からの報告に基づく。** squash専用運用で誤警告が常時点灯していた | repository ownerの決裁 | 作業tree、base `f8ec54c1` |
| 2026-09-02 | `pr create`後のPR再観測が、固定済み`create.headSha`ではなく再固定chainの実効HEADと`observed.headRefOid`を照合するようにする。**chainが空なら実効HEADは固定済みheadと一致し、再固定記録を持たない既存stateの判定は変更前と完全に同一である。** 連鎖条件を満たさない記録は導出に使わない | REQ-WF-005、AC-WF-005、SCN-INT-REANCHOR-005〜007 | 追加・変更なし | `15_要件追跡/` | Issue #1101、PRは本作業で未作成 | **REQ-WF-005の本文を変えていない。** 同要件は既に「照合対象を新headへ移す」「`pr create`後は`pr reanchor`が受け持つ」と定めており、実装が要件へ達していなかった。**内容等価性の判定と`method`列挙は1行も変更していない。** これらは未reviewの内容を洗浄する経路を塞ぐ安全条件である。`appendEvidenceReanchor`がdelivery stateを書き換えない設計も維持した | repository ownerの決裁 | 作業tree、base `88aafd5c` |
| 2026-09-02 | Step skillから規範文書へ向けた相対linkの到達性検査を`skills:check`へ追加。link先fileの実在、anchorに対応する見出しの実在、docs境界外の解決先、symlinkによる境界脱出の6条件を拒否し、anchorを持たないlinkはfileの実在だけを見る。拒否するのは、link先fileの不在、anchorに対応する見出しの不在、解決先がdocs境界外、link先がsymlinkでdocs境界外、**docs root自体がsymlinkでnamespace境界外**、**anchorのpercent-encodingが復号できない**の6件である | REQ-SQ-008、AC-SQ-008、SCN-UNIT-PACKAGE-017〜019 | 追加・変更なし | `02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/` | Issue #1117、PRは本作業で未作成 | **既存の`templates/`向けlink検査の3診断を1件も変更していない。** 検査対象を`../../docs/`接頭辞のlinkに限定し、`docs/specs/`のlinkと見出し命名規則には触れない。**現行12 skillが変更前後で合格することを実測した。** 新規scriptを作らず既存`check_skill_templates.ts`を拡張した | repository ownerの決裁 | 作業tree、base `fbe1cef7` |
| 2026-09-02 | Issue #965が確定していたTERM-ASC-072「配布準備工程」とTERM-ASC-073「配布前品質検証」を耐久用語台帳へ`active`として反映。定義・成立例・反例・類義語の禁止は#965の一時stagingを正本とし、意味を変えずに台帳の列構成へ写像した | 追加・変更なし | TERM-ASC-072、TERM-ASC-073を追加 | `01_システム概要/02_用語・略語.md`、`15_要件追跡/` | Issue #1046、PRは本作業で未作成 | **既存51件の用語行を1件も変更していない。** 追加2行のみである。**反映漏れを検出する機構は追加していない。** 既存の`validateDomainGlossary`・`check_trace.ts`・`check_conformance.ts`はいずれも台帳に無い用語を検知する入力を持たず、用語IDの連番欠落検査は既存台帳に36箇所の欠番があるため成立せず、staging由来の突合は`.gitignore`が`.agent-skill-chain/tmp/`を除外するためCIから読めない | repository ownerの決裁 | 作業tree、base `f1d65061` |
| 2026-09-02 | 保護snapshotの外で値を取り出すために個別に読む5 fileの読み取り失敗を、snapshotと同じside・relative path・理由の構造化errorへ統合し、未捕捉例外をなくす。その値に依存する検査だけを飛ばして残りを続行する。**trusted baseを渡さない経路でも成立させる。** 候補側`PROTECTED_FILES`一覧の取り出しはtrusted比較専用のためこの経路では読まない。**読み取り成功後の内容不正は既存policyのまま維持し、読み取り失敗の診断へ倒さない** | REQ-SQ-030、AC-SQ-030、SCN-UNIT-QUALITY-015〜018 | 追加・変更なし | `02_要件/04_仕様・品質管理要件.md`、`15_要件追跡/` | Issue #1111、PRは本作業で未作成 | **REQ-SQ-030の既存3段落を変更していない。** snapshot経路の判定・文言・fail-closed条件は不変で、同じ書式をsnapshot外の5 pathへ広げる追記である。既存SCN-UNIT-QUALITY-011〜014のassertionを1件も変更していない | repository ownerの決裁 | 作業tree、base `fbe1cef7` |
| 2026-09-02 | 非ASC保守Issueの実行中に発見したASC本体の欠陥を、当該scopeへ追加して是正することを禁止する規則を配布規範文書へ追加。禁止対象を「ASC本体の是正」ではなく「非保守Issueへの偶発的なscope追加」として定義し、**目的にASC本体の保守を含むIssueを対象外（例外ではない）と明示する。** 「ASCに従う（必須）」と「ASCを直す（scope外）」の区別、検査を通すための歪曲を成果物側の是正から除く条項、出口（停止・記録・別Issueへの分離・owner決裁）と迂回の排除、owner決裁の結果の限定、軽微な矛盾での継続、改善提案と別Issue起票の非禁止、reviewによる強制で構成する | REQ-WF-012、AC-WF-012 | TERM-ASC-087を追加 | `.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/skills/step-09-implement/SKILL.md`、`02_要件/01_ワークフロー要件.md`、`01_システム概要/02_用語・略語.md`、`15_要件追跡/` | Issue #1115、PRは本作業で未作成 | **複数の利用者からの報告を受けたownerの指示による。** REQ-WF-012の発見処理へASC本体に関する分岐を追加している。**発見記録機構、mode判定、Step順序、gate、role契約は変更していない。****禁止対象の定義はcodexの指摘で改稿した。** 初版は「ASC本体の是正」を禁止対象としており、分離した保守Issue自身も禁止される読みを許し、ASCを保守する手段が失われる論理的な穴があった。**強制主体はreviewであり機械による自動照合は存在しない。** text presence検査を足さない理由は、費用に対して観測能力が低いことである。文言を残したまま意味を弱める変更、検査と規則を同じPRで削除する変更、実際のscope外是正のいずれも検出できない | repository ownerの決裁 | 作業tree、base `c6102964` |
| 2026-09-01 | `worktree finalize`のcli層が`reachableFromDefaultBranch`を`planWorktreeCleanup`へ渡していなかった配線漏れを是正。`--complete`の有無で安全判定の結果が変わる状態を解消する。**判定式も観測式も変えない** | REQ-LC-009、AC-LC-009、SCN-INT-FINALIGN-006〜013 | 追加・変更なし | `15_要件追跡/` | Issue #1099、PRは本作業で未作成 | **REQ-LC-009の本文は変えない。** #1097で確定した要件が`--complete`経路で成立していない実装欠陥の是正である。既存SCNを1件も変更していない | repository ownerの決裁 | 作業tree、base `01d08196` |
| 2026-09-01 | worktree削除の復旧可能性をupstream ref一致だけでなく既定branchからのHEAD到達でも成立させ、到達可能性を`reachableFromDefaultBranch`という**独立した観測field**として報告し、判定側はそれが**真であると観測できたときだけ**upstream ref由来の拒否理由を免除する。**`undefined`は免除しない。** `pushed`・`remoteBranch`・`recoveryRef`の意味は変えない。到達可能性を根拠にこれらを書き換えると観測の意味を上書きすることになり、判定側で他fieldから安全を推定するのと同じ誤りになるためである。既定branchは`refs/remotes/origin/HEAD`から解決し、解決不能とmerge-base判定不能はfail-closedで到達不成立とする。`worktree finalize`のusage正本へ`--approved-digest`と`--cleanup-authority`を宣言し`--merge-sha`の条件を実装へ合わせる | REQ-LC-009、AC-LC-009、SCN-UNIT-WTSURVEY-018〜022、SCN-UNIT-CLIUSAGE-015、SCN-INT-FINALIZE-006〜008、SCN-INT-WTSURVEY-013 | TERM-ASC-086を追加 | `01_システム概要/02_用語・略語.md`、`02_要件/02_プロジェクトライフサイクル要件.md`、`15_要件追跡/` | Issue #1097、PRは本作業で未作成 | **作業tree由来の拒否は維持する。** squash mergeで既定branchから到達できない場合の拒否も維持する。**既存SCNのassertionを1件も変更していない。** 判定側の免除は新設の`reachableFromDefaultBranch`が真であると観測できたときだけ働き、既存fixtureは同fieldを持たないため従来どおり拒否される。**squash mergeでlocalのremote-tracking refがprune前の場合、既存の`recoveryRef && pushed`側で`recoveryReachable`が真になり得る。これは本変更が導入したものではなく旧式と同値であり、残存リスクとして記録する** | repository ownerの決裁 | 作業tree、base `0f3efd15` |
| 2026-09-01 | `asc-step`共通登録アダプター正本の内容契約を明文化。Step skill一覧への相対linkを正本と配置先の双方で解決する`../../../`基点形に限り、`description`を各Stepの開始時の起動を促す単一行とし、Step skillの実名列挙を禁止する | REQ-LC-002、AC-LC-002、SCN-UNIT-HOST-SKILL-003、SCN-UNIT-HOST-SKILL-004 | 追加・変更なし | `02_要件/02_プロジェクトライフサイクル要件.md`、`04_機能/01_ワークフローv0.3.md`、`15_要件追跡/` | Issue #1084、PRは本作業で未作成 | 正本の配置・hash・managed登録の契約は変えない。既存の4 markerを保つ追記であり、利用projectの再installを要さない | repository ownerの決裁 | 作業tree、base `88358243` |
| 2026-08-31 | bump branchをrelease bump基準SHAから作り直す経路を`scripts/prepare_release_bump.ts`へ切り出し、正規bump差分の2段階判定、既に正規なbranchの非書き換え、create・rebuild共通の空expect対応`--force-with-lease`、fail-closed停止を追加。workflowは`npm ci`のあとscriptを1回呼ぶ専用stepへ接続し、旧`git switch --detach FETCH_HEAD`経路を削除 | REQ-GH-003、AC-GH-003、SCN-INT-AUTORELEASE-004〜008、SCN-INT-AUTORELEASE-011 | TERM-ASC-080、TERM-ASC-081を追加 | `02_要件/03_外部連携要件.md`、`12_運用保守/00_運用設計.md`、`01_システム概要/02_用語・略語.md`、`15_要件追跡/` | Issue #1051、PRは本作業で未作成 | bump branchの再利用条件を厳格化する。既に基準SHA基準で正規なbranchは従来どおり書き換えない。再帰防止の判定を「bump commitを`git commit`で作ること」から「既定branchへ着地するcommit messageへ`[skip ci]`を載せる指定があること」へ正した | repository ownerの決裁 | 作業tree、base `e2c4ed17` |
| 2026-08-30 | Git・text・provider耐久stateを正本とするworktree固有Semantic Graph派生投影、GraphQLite v0.6.1固定asset、Graph限定のNode.js 22.13.0境界、drift fail-closed、bounded BFS・Kahn・iterative Tarjan・Dijkstra、Graph Evidence、Full/Quick/PoC別の隔離疑似project観測を追加 | REQ-GR-001〜014、AC-GR-001〜014、OBS-GR-001〜018 | 追加・変更なし | `02_要件/05_グラフ投影要件.md`、`03_アーキテクチャ/`、`06_外部インターフェース/`、`07_データ/`、`09_基盤・ネットワーク/`〜`14_開発・品質/`、配布物の3規範文書 | ASC一部刷新、PRは本作業で未作成 | Graphは派生物であり既存正本とmode契約を変えない。packageのNode 20以上を維持し、Graph commandだけversion guard後にadapterをdynamic importする | repository ownerの指示 | 作業tree |
| 2026-08-30 | PR作成・mergeの外部副作用前intentとprovider read-backをdelivery stateへ耐久化し、曖昧失敗時の再送禁止、未dispatch・exact absenceの同一intent 1回再試行、provider authority三者一致、ruleset fallback、auto-merge/queue Evidence、PR/merged二種類のStep 11終端、crash-window前向復旧を追加 | REQ-WF-013、REQ-GH-001、SCN-UNIT-DELSTATE-001〜014、SCN-INT-GITHUB-011〜015、SCN-E2E-WFSTEP-006〜028 | 追加・変更なし | `02_要件/`、`04_機能/`、`06_外部インターフェース/`、`07_データ/`、`10_セキュリティ/`、`15_要件追跡/`、配布物のworkflow・品質基準・Step 11契約 | Issue #1061、PRは本作業で未作成 | PoCとdisabledのPR終端、assistedの再開可能待機、Quick/Fullのautomatic delivery、forward-only処理を維持。GitHubのHEAD以外のmetadata CAS不在は残余競合として検知対象化 | repository ownerの指示 | 作業tree、base `ec407833` |
| 2026-08-30 | 実装前reviewをreadiness checkへ縮小し、実装中発見のモード別前向き処理、Quick/PoCから同一Issueでの永続Full昇格、Evidence-driven Verification、automatic deliveryのPRからmergeまでの継続、Issue staging prefixの日本標準時固定を追加 | REQ-WF-002、REQ-WF-012、REQ-WF-013、REQ-SQ-029、SCN-INT-ISSUE-006、SCN-UNIT-AGILE-001〜027、SCN-UNIT-WFJRNL-014、SCN-INT-WFSTEP-010〜017、SCN-INT-STAGING-007〜008、SCN-INT-MERGE-010〜012、SCN-INT-GITHUB-011〜015、SCN-E2E-WFSTEP-004〜005 | 追加・変更なし | `02_要件/`、`04_機能/`、`06_外部インターフェース/`、`07_データ/`、`14_開発・品質/`、`15_要件追跡/`、配布物のworkflow・品質基準・Issue template・Step契約 | Issue #1061、PRは本作業で未作成 | quickとpocの分離、同一tracker、最終exact-head review、安全・authorityのfail-closed条件は維持。TDDは任意techniqueへ変更 | repository ownerの指示 | 作業tree、base `ec407833` |
| 2026-08-26 | 独立review不成立時の扱いをrisk比例で定め、承認済み例外を正本fileと配布schemaで管理 | REQ-SQ-015、SCN-UNIT-RVX-001〜013 | 追加・変更なし | `02_要件/`、`11_非機能/`、`15_要件追跡/`、配布物の品質基準・reviewtemplate・schema | Issue #921、PRは本作業で未作成 | 既定を停止から無記録通過の禁止へ変更する。不可逆操作は従来どおり外部証拠を要求する | repository ownerの指示 | 作業tree、base `359d2259` |
| 2026-08-26 | 配布templateの要件ID体系を製品と同じREQ-{domain}-{3桁}へ揃え、旧体系を拒否 | REQ-SQ-014、SCN-UNIT-REQID-001〜007 | 追加・変更なし | `02_要件/`、`11_非機能/`、`15_要件追跡/`、配布templateの要件・受け入れ条件・追跡・非機能・機能 | Issue #930、PRは本作業で未作成 | templateの識別子体系を変更する。既存の利用projectは自分の要件fileを移行する必要がある | repository ownerのIssue指示 | 作業tree、base `6faeeb68` |
| 2026-08-26 | worktree作成契約を配布文書へ届け、正本の定数から生成して二重管理を避ける | REQ-LC-010、SCN-UNIT-WTDOC-001〜007 | 追加・変更なし | `02_要件/`、`11_非機能/`、`15_要件追跡/`、配布物の開発ワークフロー | Issue #929、PRは本作業で未作成 | 配布文書への追記であり、runtimeの挙動は変えない | repository ownerのIssue指示 | 作業tree、base `b41e2786` |
| 2026-08-26 | CLIのusageと不足flag報告を配布文書へ届け、usage正本から生成して二重管理を避ける | REQ-WF-009、SCN-UNIT-CLIDOC-001〜006 | 追加・変更なし | `02_要件/`、`15_要件追跡/`、配布物の利用案内とREADME | Issue #931、PRは本作業で未作成 | 配布文書への追記であり、runtimeの挙動は変えない | repository ownerのIssue指示 | 作業tree、base `6367a7bc` |
| 2026-08-26 | 変更の配布境界への影響判断をreview証拠の必須要素にし、audit:checkで強制 | REQ-SQ-013、SCN-UNIT-DISTIMPACT-001〜014 | 追加・変更なし | `02_要件/`、`11_非機能/`、`15_要件追跡/` | Issue #928、PRは本作業で未作成 | 既存artifactは歴史的記録として遡及しない。新規artifactへ適用する | repository ownerの指示 | 作業tree、base `0abca9a7` |
| 2026-08-26 | 登録済み品質proposalの比較対象を契約fieldへ限定し、owner・rationale・rollbackの更新を許可 | REQ-SQ-012、SCN-UNIT-PROPFIELD-001〜008 | 追加・変更なし | `02_要件/`、`11_非機能/`、`15_要件追跡/` | Issue #920、PRは本作業で未作成 | 削除・契約field変更・空文字列化の拒否は不変。訂正できない正本を残さないための緩和である | repository ownerのIssue指示 | 作業tree、base `e96e16f3` |
| 2026-08-26 | 製品自身のlifecycle健全性をCIのinstall適用とdoctorで検証し、展開先とmanaged recordをgitignore対象にしてclean性を保つ | REQ-LC-011、SCN-INT-DOGFOOD-001〜006 | 追加・変更なし | `02_要件/`、`14_開発・品質/`、`15_要件追跡/` | Issue #882、PRは本作業で未作成 | 検出のみで削除は行わない。worktree報告は`healthy`を変えない | repository ownerのIssue指示 | 作業tree、base `315e36f8` |
| 2026-08-26 | test fixtureの実時計・実repository状態依存を機械的に拒否し、相対時刻helperと理由付き宣言台帳を追加 | REQ-SQ-011、SCN-UNIT-TESTDET-001〜012 | 追加・変更なし | `02_要件/`、`11_非機能/`、`14_開発・品質/`、`15_要件追跡/` | Issue #888、PRは本作業で未作成 | 既存fixtureの挙動は変えない。`Date.now()`の1箇所をhelperへ置換し、dogfooding accessは理由付きで宣言する | repository ownerのIssue指示 | 作業tree、base `315e36f8` |
| 2026-08-26 | trusted品質契約の保護snapshotからpackage自身のversionだけを正規化して除く | REQ-SQ-010、SCN-UNIT-LOCKPROT-001〜008 | 追加・変更なし | `02_要件/`、`11_非機能/`、`15_要件追跡/` | Issue #913、PRは本作業で未作成 | dependency構成・integrity・lockfileVersionの保護は不変。自動release bump後もrebase前のPRが通る | repository ownerのIssue指示 | 作業tree、base `003b802d` |
| 2026-08-26 | subcommand単位のusageと不足必須flagの全件報告、空白区切りflagの明示拒否、usage正本と実装の双方向検査を追加 | REQ-WF-009、SCN-UNIT-CLIUSAGE-001〜012 | 追加・変更なし | `02_要件/`、`06_外部インターフェース/`、`15_要件追跡/` | Issue #886、PRは本作業で未作成 | 既存診断構造`ASC-CLI-VALIDATION-001`を維持したまま`reasons`を複数件へ拡張。値をとるflagの空白区切り指定だけを新たに拒否する | repository ownerのIssue指示 | 作業tree、base `80257410` |
| 2026-08-26 | PR作成後journal失敗のURL保持・再開、workflow staging正本集約、changedFiles再分類再現、HumanOverride承認元分離、空Step拒否、test用一時repository path正規化を追加 | REQ-GH-001、REQ-WF-004、SCN-UNIT-PRJRNL-001〜004、SCN-UNIT-WFPATH-001〜002、SCN-UNIT-WFMODE-005、SCN-UNIT-WFSTEP-007 | 追加・変更なし | `02_要件/`、`06_外部インターフェース/`、`15_要件追跡/`、規範・Step 11契約 | Issue #906、PRは本作業で未作成 | PR成功出力を維持し、記録失敗だけ非0診断を追加。既存モード成果物はchangedFilesなしを空配列として互換読取 | repository ownerのIssue指示 | 作業tree、base `f4fe774e` |
| 2026-08-26 | worktree timestampの作成時刻検証、path自動構成、trusted boundaryの優先評価、作成後のIssue番号・slugずれ報告を追加 | REQ-LC-010、SCN-UNIT-WTTS-001〜010、SCN-UNIT-WTSURVEY-015〜017、SCN-INT-WTTS-001〜002、SCN-INT-WTSURVEY-003〜004 | 追加・変更なし | `02_要件/`、`06_外部インターフェース/`、`12_運用保守/`、`15_要件追跡/` | Issue #893、PRは本作業で未作成 | `--path`を任意化。明示pathはtrusted boundaryを先に評価して時刻検証を追加し、不正な既存入力だけを拒否 | repository ownerのIssue指示 | 作業tree、base `2e072ac9` |
| 2026-08-26 | cleanup計画の同一事実を表す複数観測の矛盾をfail-closedで拒否 | REQ-LC-009、SCN-UNIT-FINALIGN-030〜034 | 追加・変更なし | `15_要件追跡/` | Issue #894、PR #900のmerge後是正 | `trackedChanges`・`clean`と`untracked`・`consumerAssets`の矛盾時だけ拒否を強化し、片方だけまたは整合時の判定は維持 | repository ownerの是正指示 | 作業tree、base `d2dc0e93` |
| 2026-08-26 | cleanup計画の配列・boolean観測をfail-closedに統一 | REQ-LC-009、SCN-UNIT-FINALIGN-018〜029 | 追加・変更なし | `02_要件/`、`12_運用保守/00_運用設計.md`、`15_要件追跡/` | Issue #894、PR #900 | 不正・不明な安全性観測を安全と推定しない方向に強化 | repository ownerのIssue指示 | 作業tree、base `2e072ac9` |
| 2026-08-25 | finalize ignore allowlistとsurvey・finalize共通安全判定を追加 | REQ-LC-009、SCN-UNIT-FINALIGN-001〜017、SCN-INT-FINALIGN-001〜005 | TERM-ASC-065を追加 | `01_システム概要/02_用語・略語.md`、`02_要件/`、`04_機能/01_ワークフローv0.3.md`、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`10_セキュリティ/01_信頼境界.md`、`12_運用保守/00_運用設計.md`、`15_要件追跡/` | Issue #894、PR #900 | allowlist内の再生成可能な生成物があるworktreeの後片付けを可能にし、利用者資産の保持は維持 | repository ownerのIssue指示 | 作業tree、base `2e072ac9` |
| 2026-08-25 | Issue単位要件・課題別追跡をstable要件と単一追跡へ正規化 | REQ-SQ-007、SCN-UNIT/INT-SPECNORM | 追加・変更なし | `02_要件/`、必須仕様10件、`15_要件追跡/` | Issue #881、PRは本作業で未作成 | 要件本文の識別子を変更するが現行機能・CLI互換性は維持 | repository ownerのIssue指示 | 作業tree、base `01cfba6f` |

## 旧26件の移行対応

旧fileの要件本文・受け入れ条件は安定要件へ統合し、SCN・test・実装の対応は`00_追跡表.md`の完全path行へ移した。旧file自身はv0.3.1-beta.9の仕様正規化で廃止するが、機能・要件は廃止しない。導入PR番号をcommit metadataから確定できない行は、推測せず導入commitを記録する。

| 旧file | Issue | 回収した内容 | 安定要件 | 対応する追跡Feature | 導入版 | 廃止版 | 導入PR・commit |
|---|---|---|---|---|---|---|---|
| `00_課題824追跡.md` | #824 | T01〜T14、mode、Step、worktree、GitHub、policy、lifecycle、review、仕様gate | REQ-WF-001、002、004〜006、REQ-LC-003、REQ-GH-001、REQ-SQ-001、005、008 | `mode-security.feature`、`issue-spec.feature`、`workflow-step-enforcement.feature`、`lifecycle-worktree.feature`、`delivery-finalize.feature`、`review-policy-package.feature`、`trace.feature` | 0.3.0-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR #825、`320213fd` |
| `01_課題834追跡.md` | #834 | risk分類、trusted比較、migration、override、offline、証拠、conformance、template、型・用語契約 | REQ-WF-005、006、008、009、REQ-SQ-001〜006、008、009 | `risk-policy.feature`、`risk-policy-migration.feature`、`review-policy-package.feature`、`source-quality.feature`、`project-policy-satisfiability.feature` | 0.3.0-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`37e5d27e` |
| `02_機能拡張不変条件追跡.md` | #834 | I1〜I12、開発契約、型・source品質のbinding | REQ-SQ-002、004〜006、009 | `risk-policy.feature`、`project-rule-ledger.feature`、`source-quality.feature` | 0.3.0-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`37e5d27e` |
| `03_課題840追跡.md` | #840 | project choiceのauthority・弱化・許可分類 | REQ-SQ-003 | `project-choice-diff.feature` | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`02863d41` |
| `04_課題826追跡.md` | #826 | 手動release計画、dry-run、権限、段階結果、復旧 | REQ-GH-002 | `release-plan.feature`、`release-workflow.feature` | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`a8150bbe` |
| `05_課題833追跡.md` | #833 | workspace hygieneのpreview、Git保護、hash、非再帰削除 | REQ-LC-004 | `workspace-hygiene.feature`、`workspace-hygiene-cli.feature` | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`24fa5f04` |
| `06_課題828追跡.md` | #828 | lifecycle隔離、consumer保持、path/hash/TOCTOU、部分失敗 | REQ-LC-001 | `lifecycle-isolation.feature`、`lifecycle-isolation-cli.feature`、`cli.feature` | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`bf602388` |
| `07_課題827追跡.md` | #827 | PoC宣言、high risk判定、禁止操作、full昇格 | REQ-WF-003 | `poc-mode.feature`、`poc-mode-cli.feature` | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`4644e4a4` |
| `08_課題831追跡.md` | #831 | worktree path・branch・base・status・cleanup対象の同一性 | REQ-LC-003 | `worktree-placement.feature`、`worktree-placement-cli.feature`、`lifecycle-worktree.feature` | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`3e964f81` |
| `09_課題832追跡.md` | #832 | project rule inventory、coverage、CI・package境界 | REQ-SQ-004 | `project-rule-ledger.feature` | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`4d1c0f3a` |
| `10_課題829追跡.md` | #829 | root fast-forward、対象worktree、Issue終端参照、操作分離 | REQ-LC-005、REQ-GH-001 | `finalize-root-update.feature`、`finalize-root-update-cli.feature`、`delivery-finalize.feature` | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`b78f3170` |
| `11_課題830追跡.md` | #830 | 6 role、identity/context独立、tier、provider上限、authority分離 | REQ-WF-007 | `role-tier.feature`、`role-tier-cli.feature`、routing Feature群 | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`daeb7009` |
| `12_課題847追跡.md` | #847 | host skill正本、lifecycle、doctor、containment | REQ-LC-002 | `host-skill-adapter.feature`、`host-skill-adapter-cli.feature` | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`123cf11b` |
| `13_課題837追跡.md` | #837 | conformance scope、適用外、file entry、quality applicability | REQ-WF-008 | `project-policy-satisfiability.feature`、同CLI Feature | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`272918a7` |
| `14_課題856追跡.md` | #856 | merge完了phase、cleanup authority、TOCTOU、部分完了 | REQ-LC-006 | `merge-completion-cleanup.feature`、同CLI Feature | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`5b390400` |
| `15_課題861追跡.md` | #861 | 自動tag・Release、version bump PR、冪等性、npm非公開 | REQ-GH-003 | `auto-release.feature` | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`69a03119` |
| `16_課題858追跡.md` | #858 | README入口、CLI drift、正本link、package収録 | REQ-SQ-005 | `root-readme.feature` | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`1b846072` |
| `17_課題860追跡.md` | #860 | staging記録、同期checkpoint、preview/apply、legacy保持 | REQ-WF-002、REQ-LC-007 | `staging-lifecycle.feature`、`staging-lifecycle-cli.feature` | 0.3.1-beta.2 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`47561f77` |
| `18_課題869追跡.md` | #869 | bump経路でauditだけ除外し他gate維持 | REQ-GH-003、REQ-SQ-005 | `auto-release.feature` | 0.3.1-beta.1 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`303f7d5e` |
| `19_課題873追跡.md` | #873 | release bumpのsubject・path・version限定除外、merge履歴 | REQ-GH-005 | `audit-bump-exclusion.feature` | 0.3.1-beta.3 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`25aa7d54` |
| `20_課題875追跡.md` | #875 | branch methods積集合、長命branch間merge限定、警告 | REQ-GH-004 | `merge-method-policy.feature` | 0.3.1-beta.4 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`eacd3a70` |
| `21_課題879追跡.md` | #879 | pack内容digest、version正規化、tag比較、paths proxy廃止 | REQ-GH-003 | `auto-release.feature` | 0.3.1-beta.5 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`4036ce02` |
| `22_課題877追跡.md` | #877 | Step正本、journal、mode成果物、workflow CLI、PR gate | REQ-WF-004 | `workflow-step-enforcement.feature`、同CLI Feature | 0.3.1-beta.6 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`78817768` |
| `23_課題883追跡.md` | #883 | worktree分類、read-only CLI、doctor要約、削除責務分離 | REQ-LC-008 | `worktree-survey.feature` | 0.3.1-beta.6 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`ff33a3f7` |
| `24_課題887追跡.md` | #887 | Issue template見出し、stage別検証、placeholder文脈 | REQ-SQ-008 | `issue-template-contract.feature` | 0.3.1-beta.8 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`765513b6` |
| `25_課題892追跡.md` | #892 | review artifactをcommit構造と差分pathから選択 | REQ-GH-005 | `audit-artifact-selection.feature` | 0.3.1-beta.8 | 旧fileを0.3.1-beta.9で廃止、要件継続 | PR未確認、`799d3b17` |
| `26_課題894追跡.md` | #894 | finalize ignore allowlist、survey・finalize共通安全判定、既存blockerの維持、運用順序、不明観測のfail-closed | REQ-LC-009 | `finalize-ignored-artifacts.feature` | 0.3.2 | 旧fileを本仕様構造同期で廃止、要件継続 | PR #900、`14b803ce` |

各旧fileの完全な過去本文はGit履歴のbase `01cfba6f`で再現できる。現在有効な命題は上記安定要件、各category詳細仕様、`00_追跡表.md`へ移しており、Git履歴だけを現在仕様の代替にはしない。
