# 課題892 要件追跡

| AC | 要件・不変条件 | 実装・enforcement point | Counterexample Gherkin | 証拠・rollback |
|---|---|---|---|---|
| AC-892-01 | review artifactはdirectory内のfile名sortで選ばず、review headと推定`H_impl`の差分pathが1件だけの場合にそのpathを選ぶ | `scripts/check_file_audit.ts`の`inferReviewBoundary`、`finalAuditPaths`、`checkFileAudit` | SCN-UNIT-AUDITSEL-001〜002・008、SCN-INT-AUDITSEL-001・004 | 小さい番号、zero paddingなし、既存41件を含むfixture。rollbackでは監査を停止し、グローバル連番選択を再導入しない |
| AC-892-02 | 差分0件はreview artifactだけの後続commitを要求し、複数件は一意なartifact候補を除く余分なpathを全件、候補を一意化できなければ差分pathを全件、1件が`docs/reviews/`外ならそのpathと修正方法を日本語で示して拒否する | `checkFileAudit`の差分件数・directory境界診断 | SCN-UNIT-AUDITSEL-003〜005、SCN-INT-AUDITSEL-003 | 診断文と全path照合。rollbackでも不明なartifactを推測せずfail closedを維持する |
| AC-892-03 | 循環を避けるため、有効な末尾release bumpを除いたreview headを先に決め、通常PR mergeではreview側parentを使い、その親を`H_impl`と推定する。選択後にartifact本文の`H_impl`と完全一致を二重確認する | `withoutFinalReleaseBumps`、`inferReviewBoundary`、`parseFileAudit` | SCN-UNIT-AUDITSEL-007、SCN-INT-AUDITSEL-002 | 推定SHAと本文SHAの不一致診断。同じ番号を持つ並行branch 2本のmerge fixture |
| AC-892-04 | release bump除外はsubject、変更path、`package.json.version`限定の3条件をすべて維持し、通常変更を除外しない | 既存`isReleaseBumpTransition`、`hasReleaseBumpChanges`、`packageJsonOnlyChangesVersion`と新しい`withoutFinalReleaseBumps` | SCN-UNIT-AUDITSEL-006、SCN-UNIT-AUDITBUMP-001〜004、SCN-INT-AUDITBUMP-001〜002 | 正規bumpと3条件の反例。rollbackでは自動bumpを停止し、条件を緩和しない |
| AC-892-05 | review artifactの連番と既存fileは人向け整理として維持するが、検査結果は番号・桁数・merge順に依存しない | `AUDIT_NAME_PATTERN`の書式検査、`docs/reviews/`仕様、選択処理からのdirectory走査削除 | SCN-UNIT-AUDITSEL-002・008、SCN-INT-AUDITSEL-002・004 | 既存artifactをrenameしないGit差分。rollbackでも番号最大値による選択を復活させない |

## 仕様変更履歴

| 日付 | Issue | 用語差分 | 変更 |
|---|---|---|---|
| 2026-08-25 | #892 | TERM-ASC-008 `H_impl`とTERM-ASC-009 `H_final`を継続使用し、新語なし | `audit:check`のreview artifact選択をグローバル連番proxyからGit commit構造と差分pathの正本へ移行 |
