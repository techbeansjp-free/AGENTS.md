# 課題883 要件追跡

| AC | 要件・不変条件 | 実装・enforcement point | Counterexample Gherkin | 証拠・rollback |
|---|---|---|---|---|
| AC-883-01 | 観測配列をprimary、in-progress、cleanup-ready、retainへ順序付きで分類し、保持条件をすべて日本語理由へ列挙する | `src/domain/worktree-survey.ts`の`surveyWorktrees` | SCN-UNIT-WTSURVEY-001〜009 | 純関数の分類結果。rollbackでは全対象を保持し、削除へ進めない |
| AC-883-02 | 未知field、型、範囲、重複pathをentry単位で拒否し、正常entryの判定を継続する | `surveyWorktrees`の入力検査 | SCN-UNIT-WTSURVEY-010〜014 | 日本語`errors`と部分成功結果。rollbackでは不正entryだけを保持する |
| AC-883-03 | CLIは登録済みworktree、remote既定branchへのmerge、status、upstream差分、既存finalize意味の復旧到達性を読み取り専用で観測する | `src/cli.ts`の`collectWorktreeSurvey`、`worktree survey` | SCN-INT-WTSURVEY-001〜006 | JSON結果、directory内容の前後一致、`--apply`拒否。rollbackでは走査を停止しfileを変更しない |
| AC-883-04 | JSONと日本語text要約を提供し、候補の存在だけで非0にせず、retainには消せない理由を必ず含める | `worktree survey --format=json\|text` | SCN-INT-WTSURVEY-003、SCN-INT-WTSURVEY-006〜007 | 終了codeと理由付きentry。rollbackではJSONのみへ戻しても削除機能を追加しない |
| AC-883-05 | doctorへ走査要約を注入し、cleanup-ready pathと走査失敗をdiagnosticへ出すが`healthy`判定へ混在させない | `doctor(target, worktreeSurvey?)`とCLI観測注入 | SCN-INT-WTSURVEY-008〜009 | worktrees件数・diagnostics・既存healthy。rollbackではoptional入力を省略して従来診断を維持する |
| AC-883-06 | 自動マージ等では事後走査を正規経路とし、走査・削除・workspace衛生の責務を分離する | 規範、機能、CLI、運用仕様 | SCN-INT-WTSURVEY-004〜005 | `worktree survey`はread-only、削除は`worktree finalize`だけ。rollbackでも`hygiene`の除外を維持する |

## 仕様変更履歴

| 日付 | Issue | 用語差分 | 変更 |
|---|---|---|---|
| 2026-08-25 | #883 | TERM-ASC-062「後片付け走査」を追加 | merge後の登録済みworktreeを事後走査し、安全側の分類とdoctor要約を追加 |
