# 課題832 要件追跡

| AC | 要件・不変条件 | 実装・enforcement point | Counterexample Gherkin | 証拠・rollback |
|---|---|---|---|---|
| AC-832-01 | npm・Node.js・CI・日本語・連番・配布境界・dogfoodingをGit追跡project policyへ定義する | `.agent-skill-chain/project-policy.json`、`project/rules/*.json` | SCN-UNIT-LEDGER-001、SCN-INT-LEDGER-001 | 完全inventoryとrule構造。追加rule fileとmanifest entryを同時に戻す |
| AC-832-02 | 全ruleが安定ID、目的、所有者、scope、enforcement、test evidenceを持つ | `validateProjectRuleLedgerEntry` | SCN-UNIT-LEDGER-001 | conformance構造検査。拡張メタデータは3fieldを同時rollbackする |
| AC-832-03 | 規範・schema・runtime・CIのorphan ruleを拒否する | `buildRuleCoverage`、`checkRepositoryRuleLedger` | SCN-UNIT-LEDGER-002〜004、SCN-INT-LEDGER-001 | coverage matrixとorphan 0件。rule・規範・enforcement pointを同時に戻す |
| AC-832-04 | 固定Markdownは連番prefixと日本語名を持ち明示例外だけを許可する | `validateFixedMarkdownName`、`checkFixedMarkdownNames` | SCN-UNIT-LEDGER-005〜006 | directory検査成功。名称と全参照を同時に戻す |
| AC-832-05 | 品質CIは`pull_request`だけで発火しread-onlyを保つ | `checkQualityCiTriggers`、`checkQualityCiPermissions` | SCN-INT-LEDGER-002 | workflow静的検査。trustedな単一triggerと最小権限へ戻す |
| AC-832-06 | npm以外のpackage manager・lockfileを混在させず`npm ci`を使う | `checkPackageManagerBoundary` | SCN-INT-LEDGER-003 | root lockfileと全依存導入workflow検査。信頼済みlockfileへ戻す |
| AC-832-07 | candidateが同じPRのmerge・review・release条件を自己緩和できない | `checkTrustedPolicyBoundary`と既存trusted base validator | SCN-INT-LEDGER-001 | PR base SHA拘束。candidateを実行せずtrusted policyへ戻す |
| AC-832-08 | project policy、role log、metrics、test、CI、開発script、秘密をnpm配布物へ含めない | `checkPackageDistributionBoundary`と既存package内容検査 | SCN-INT-LEDGER-004 | `package.json.files`と`npm pack`の二重検査。公開せずallowlistへ戻す |
| AC-832-09 | unit・integration・E2Eの日本語Gherkinで正常系と反例を追跡する | `test/features/*/project-rule-ledger.feature`、`test/steps/project-rule-ledger.steps.ts` | SCN-UNIT-LEDGER-001〜006、SCN-INT-LEDGER-001〜004、SCN-E2E-LEDGER-001 | 全layer成功。失敗fixtureは一時directoryごと破棄する |

rule coverage matrixは`.agent-skill-chain/docs/`の3正本、全schema、`src/`、`.github/workflows/`を同じrule IDで照合する。project ruleの詳細値はmanifestが列挙するrule fileを正本とし、本追跡表には複製しない。

反例fixtureは一時directoryだけへ作成し、実repository、実workflow、lockfile、配布設定を変更しない。最終証拠はfull検査一式の同一HEAD成功へ結び、実装者の自己確認を独立reviewとして扱わない。
