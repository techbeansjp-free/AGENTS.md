# 課題887 要件追跡

| AC | 要件・不変条件 | 実装・enforcement point | Counterexample Gherkin | 証拠・rollback |
|---|---|---|---|---|
| AC-887-01 | full、quick、pocの出荷要求templateは検証器の全必須見出しを含み、任意見出しは許可する。fullの7章とquickの5章は検証器に揃え、見出し以外を変更しない | `.agent-skill-chain/templates/issue/00_要求定義_{full,quick}.md`、`issueRequiredHeadings` | SCN-UNIT-ISSUETPL-001〜005、SCN-INT-ISSUETPL-001〜002 | 3 modeの機械突合と埋めたtemplateのIssue検証。rollbackではtemplate見出しとskill記述を同時に戻す |
| AC-887-02 | 規範・検証器・出荷templateの不一致を既存skills gateで拒否し、散文規範はkey termだけを過不足なく照合する | `scripts/check_skill_templates.ts`の`checkIssueTemplateHeadings`、`npm run skills:check` | SCN-UNIT-ISSUETPL-004〜005、SCN-INT-ISSUETPL-004 | modeと不足見出しを示す日本語error。規範の完全な見出し列挙は要求せず、quickの`最小Gherkin`を3箇所で検査する |
| AC-887-03 | fullのIssue検証はrequirementsで00/01、designと未指定で00〜03を要求し、quickとpocはstageに依存しない | `validateIssue`の`IssueValidationStage`、`src/cli.ts`の`--stage`検証 | SCN-UNIT-ISSUESTG-001〜004、SCN-INT-ISSUETPL-003 | stage別の必要成果物errorとCLI終了code。rollbackではstage指定を外し従来の全件検証へ戻す |
| AC-887-04 | 全mode・全stageでGherkin scenario IDを要求し、`gherkinDialect`は必須の方言選択として維持する | `validateIssue`のGherkinシナリオID検査、既存project choice schema | SCN-UNIT-ISSUESTG-005 | requirementsでもID欠落を拒否する回帰証拠。rollbackでもGherkin要求とschemaを変更しない |
| AC-887-05 | code span、code block、Gherkin構文内の記法と一般説明の丸括弧をplaceholderにせず、template由来の既知placeholderは検出する | `withoutCode`、`withoutGherkin`、`TEMPLATE_PARENTHETICAL_PLACEHOLDERS`、`hasUnresolvedPlaceholder` | SCN-UNIT-ISSUEPLC-001〜005 | `<T>`、object literal、説明文、Scenario Outlineの正常系と既知placeholderの反例。rollbackでは文脈除外だけを戻しplaceholder検出自体は維持する |

報告された`gherkinDialect`未採用時のGherkin強制は、方言選択が必須で不採用値を持たない規範どおりの挙動であり、製品欠陥ではない。Gherkin scenario ID要求、project choice schema、project choice diffは本変更の対象外として維持する。

## 仕様変更履歴

| 日付 | Issue | 用語差分 | 変更 |
|---|---|---|---|
| 2026-08-25 | #887 | なし。既存のIssue検証とGherkinを使用 | template見出し整合検査、段階別Issue検証、Markdown・Gherkin文脈を考慮したplaceholder検出を追加 |
