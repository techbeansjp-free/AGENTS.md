# 課題894 要件追跡

| AC | 要件・不変条件 | 実装・enforcement point | Counterexample Gherkin | 証拠・rollback |
|---|---|---|---|---|
| AC-894-01 | finalize ignore allowlistはpackage既定の`node_modules/`と`dist/`を持ち、利用projectはtrusted manifestへ安全なdirectory prefixだけを追加できる | `src/domain/worktree-removal-safety.ts`、`policy.worktree.finalizeIgnoredPathAllowlist`、manifest・組立済みpolicy schema、`validateWorktreePlacementPolicy` | SCN-UNIT-FINALIGN-010〜012、014〜017 | schemaとruntimeの同時検証、ASC-TRUST-001による候補側の削除範囲拡大拒否。rollbackではproject追加fieldを無効化してpackage既定だけを維持する |
| AC-894-02 | allowlist内のignore対象は削除可能判定を妨げず、allowlist外・種別不明はpathと日本語理由を持つ保持条件にする | `assessWorktreeRemovalSafety`、`buildFinalizeReport`、`planWorktreeCleanup` | SCN-UNIT-FINALIGN-001〜004、SCN-INT-FINALIGN-001〜002 | reportのsafe、reasons、blocking path。rollbackではすべてのignore対象を保持し、件数だけの一律許可へ戻さない |
| AC-894-03 | surveyとfinalizeは追跡対象変更、未追跡file、stash、push、remote branch、merge、復旧、配置、ignore対象を1つの純粋関数で判定し、cleanup-readyをfinalizeが受理できる状態にする | `assessWorktreeRemovalSafety`を`surveyWorktrees`、`buildFinalizeReport`、`planWorktreeCleanup`から共有 | SCN-UNIT-FINALIGN-013、SCN-INT-FINALIGN-003〜004 | 同一fixtureのsurvey dispositionとfinalize終了code。rollbackでは不一致をcleanup-readyにせずretainへ倒す |
| AC-894-04 | 未commit追跡対象file、未追跡file、未push・到達不能commit、stash、PR未merge、allowlist外ignore対象、`.worktrees`直接の子でないpathを従来どおり拒否する | 共通安全判定、`planWorktreeCleanup`の登録・canonical path検査、CLIのapply直前再観測 | SCN-UNIT-FINALIGN-002・005〜009、SCN-INT-FINALIGN-002・004〜005 | blocker別理由とfixture path残存。rollbackでは対象worktreeを保持し、手動広域削除へ切り替えない |
| AC-894-05 | buildとgateで生成した`dist/`を保持理由から除外した後、survey、finalize preview、authority付きapplyの順で安全に後片付けする | CLI契約、信頼境界、運用設計、schema利用案内 | SCN-INT-FINALIGN-001・003 | 運用手順とdry-run report。rollbackではapplyへ進まず、対象と生成物を保持して新しいsurveyから再実行する |
| AC-894-06 | cleanup計画の配列・boolean観測が不正または不明なら、空・false・他の観測から安全状態を推定せず拒否する | `planWorktreeCleanup`、`assessWorktreeRemovalSafety` | SCN-UNIT-FINALIGN-018〜029 | `temporaryArtifacts`は「一時資産があるか状態が不明です」で拒否し、登録一覧・ignore対象・未追跡・追跡変更・allowlist・stash・remote branch・対象存在状態も不明状態を保持条件にする。代替観測があっても不正な`clean`と`consumerAssets`を無視しない。rollbackでは対象worktreeを保持する |

## 仕様変更履歴

| 日付 | Issue | 用語差分 | 変更 |
|---|---|---|---|
| 2026-08-25 | #894 | TERM-ASC-065 `finalize ignore allowlist`を追加 | `.gitignore`と再生成可能性を分離し、surveyとfinalizeのignore対象判定を共通化 |
| 2026-08-26 | #894 | なし | cleanup計画の配列・boolean観測をfail-closedに統一し、不正な一時資産状態を拒否 |
