---
document_id: "91dffacc-716b-47fd-bc6e-92458934f3b9"
---

このドキュメントは、システム仕様書（`docs/`）と実装の整合性を継続追随ゲート（DOCS_RULES §継続追随ゲート）に基づきレビューした結果を記録します。

# システム仕様書レビュー結果（継続追随ゲート・as-built 同期）

**レビュー実施日時**: 2026 年 07 月 14 日 22:12:15（JST）
**対応 issue**: 「enforce off が既定状態であり『絶対強制』の実態が宣言のみ（新規セットアップ既定 on 化）」（issue_id: `b78fc168-77b2-434d-8b98-cdfd68deafbf`）

## 1. 照合した実装変更

新規配備（`ASC_MODE=new`）時に enforcement を既定 on で自動配線する挙動変更（柱 A）と、各層/各ツールの強制力区分の記述整合（柱 B）。変更ファイル（コード・記述・テスト）:

- コード: `.agent-skill-chain/source/scripts/setup.sh`（`enforce_default_on_if_possible`・`ASC_MODE=new` 分岐）、`src/agents-md.ts`（init 経路の委譲・ヘルプ/コメント整合）
- 記述（source 正本）: `.agent-skill-chain/source/SETUP.md`、`.agent-skill-chain/source/enforcement/README.md`、`README.md`
- 記述（システム仕様書）: `docs/04_機能設計/CLI/README.md`（CLI コマンド一覧の `init`/`upgrade`/`enforce` 行）
- テスト: `test/test-enforce-default-on.sh`（新規）、`test/e2e-install-uninstall.sh`（R6 更新）、`test/run-all.sh`

## 2. レビュー対象のシステム仕様書セクション

- `docs/04_機能設計/CLI/README.md`（CLI サブコマンド仕様）— 主たる照合対象。本 issue で as-built 同期済み。
- `docs/04_機能設計/enforcement/README.md`（俯瞰・正本は source へ委譲）— enforcement 配線の on/off の粒度は下位詳細であり as-built を偽にしない。

## 3. 判定: 更新済み・整合確認（指摘 0）

**根拠**:

- `docs/04_機能設計/CLI/README.md` の CLI コマンド一覧は、`init`/`upgrade` の説明に「新規配備では enforcement を既定 on で自動配線する／既存配備・自己適用では touch しない」を、`enforce` 行に「サブコマンド自体は on/off/status 不変・新規配備は init/upgrade が既定で on を自動実行・off で opt-out」を反映済み。実装（setup.sh の `ASC_MODE=new` 分岐でのみ `enforce on` 相当を自動実行し、`match`/`own` では touch しない）と一致する（as-built 同期済み）。
- `docs/04_機能設計/enforcement/README.md` は俯瞰に留め、詳細は source 正本へ委譲している。今回の変更（既定 on 化）は「Layer2 PreToolUse が配線ありの環境で違反を exit 2 で block」「サブ委譲の絶対強制」という俯瞰記述を偽にしない（配線の自動/手動という下位詳細のみ変化）。詳細の正本（`.agent-skill-chain/source/enforcement/README.md`・`SETUP.md`）は本 issue で更新済み。
- 反復（指摘 N→0）は発生せず、as-built 記述の是正 1 件（CLI コマンド一覧）で整合し、残指摘 0。

**指摘件数**: 0。

## 4. evidence_source

| 判断 | evidence_source | 参照 |
| --- | --- | --- |
| CLI 仕様の as-built 記述を実装挙動に一致させた | existing_code | `docs/04_機能設計/CLI/README.md` の `init`/`upgrade`/`enforce` 行と `setup.sh` の `enforce_default_on_if_possible` を実読 |
| enforcement 俯瞰仕様は詳細を source 正本へ委譲・as-built 不変 | existing_code | `docs/04_機能設計/enforcement/README.md` を実読 |
| 挙動の回帰は実測でグリーン | test_output | `test/test-enforce-default-on.sh`（S1〜S5）・`test/run-all.sh`（合計=20 PASS=20 FAIL=0 SKIP=0） |

---

**最終更新**: 2026 年 07 月 14 日
