---
document_id: "727924eb-7c7e-41bb-a047-bf7aa01f0fb1"
---

このドキュメントは、システム仕様書（`docs/`）と実装の整合性を継続追随ゲート（DOCS_RULES §継続追随ゲート）に基づきレビューした結果を記録します。

# システム仕様書レビュー結果（継続追随ゲート・軽量パス）

**レビュー実施日時**: 2026 年 07 月 14 日 21:38:57（JST）
**対応 issue**: `docs/maintainer/workflow/20260714_185358_npmアップデートで無関係スキル消失/`（issue_id: `34d7b6bc-9d4a-4d80-84fd-9b7e7b34b790`、GitHub Issue #56）

## 1. 照合した実装変更

スキル配備・uninstall の由来判定にマーカー方式（`.agent-skill-chain-owned`）を導入し、同名衝突での誤削除を防止する恒久対策。変更ファイル（7 件＋テスト新規 1 件）:

- 実装: `.agent-skill-chain/source/scripts/lib/deploy-skills.sh`（`is_owned_skill_dir`・`list_owned_skill_entries`・マーカー書込み・`sync_skills_selective` 衝突ガード）、`src/agents-md.ts`（`isOwnedSkillDir`・`planSkillUninstall`・`runUninstall` 衝突ガード）、`.agent-skill-chain/source/scripts/setup.sh`（コメント契約更新）
- 文書: `.agent-skill-chain/source/SETUP.md`（所有区分表・保持契約・保証節）、`.agent-skill-chain/source/platforms/DESIGN_SYNC_SKILLS_NAMING.md`（別軸衝突の区別追記）
- テスト: `test/test-deploy-skills-owned-guard.sh`（新規）、`test/e2e-install-uninstall.sh`（C1/C2/C3）、`test/run-all.sh`（登録）

## 2. レビュー対象のシステム仕様書セクション

- `docs/04_機能設計/CLI/README.md`（`upgrade`/`uninstall` の as-built 記述）— 主たる照合対象
- `docs/04_機能設計/スクリプト群/README.md`（`setup.sh` の俯瞰）
- `docs/maintainer/apm-package.md`（npx 導線が個別スキル配備の唯一主体である旨）

## 3. 判定: 更新不要（軽量パス・DOCS_RULES §継続追随ゲート）

**根拠**:

- `docs/04_機能設計/CLI/README.md` は `uninstall` を「setup/init が配備した成果物のみ除去（ユーザー資産は既定で保持）」「非破壊」と俯瞰記述しており、削除判定の**機構**（名前一致か由来マーカーか）は保持していない。機構の正本は `.agent-skill-chain/source/`（SETUP.md）へ委譲されており、その SETUP.md は本 issue で更新済み。
- 今回の変更は spec の as-built 記述——「配備分のみ除去」「ユーザー資産保持」「非破壊」——の**いずれも偽にしない**。むしろ「同名の自作スキルも保持する」ようにし、俯瞰記述の主張を**より忠実に**実装へ一致させる方向の変更である（従来は同名 `agent` が保持されなかった隙間を閉じた）。
- したがってシステム仕様書の更新は不要。反復（指摘 N→0）は発生せず、根拠付き更新不要判定 1 件で通過。

**指摘件数**: 0（更新不要判定）。

## 4. evidence_source

| 判断 | evidence_source | 参照 |
| --- | --- | --- |
| spec は削除機構の詳細を source 正本へ委譲・as-built 不変 | existing_code | `docs/04_機能設計/CLI/README.md` を実読 |
| source 正本（SETUP.md）は本 issue で更新済み | existing_code | `git diff` に `SETUP.md` 含む |
| 変更は俯瞰記述を偽にせず保持契約をより忠実に実装 | test_output | `test/e2e-install-uninstall.sh` C1/C2 が同名自作スキル保持を再現（154 PASS） |

---

**最終更新**: 2026 年 07 月 14 日
