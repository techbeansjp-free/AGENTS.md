---
document_id: "d4604ab2-3d17-45f9-8c66-62891b0e1981"
---

このドキュメントは、システム仕様書（`docs/`）と実装の整合性を継続追随ゲート（DOCS_RULES §継続追随ゲート）に基づきレビューした結果を記録します。

# システム仕様書レビュー結果（継続追随ゲート・as-built 同期）

**レビュー実施日時**: 2026 年 07 月 15 日 00:09:10（JST）
**対応 issue**: 「モデルティア明記義務の機械強制欠如の是正」（Issue #73・issue_id: `2e2f3aca-208b-44ed-9ab7-c0408c347cc7`・`docs/maintainer/workflow/20260714_204451_モデルティア明記義務の機械強制欠如/`）

## 1. 照合した実装変更

`workflow_log` へモデルティア記録の 3 カラム（`model_tier`/`tier_rationale`/`tier_exception`）＋対応索引 3 件を追加し、`audit.sh` に記録有無を検査する新チェック #38（`check_model_tier_recorded`）を追加した。変更ファイル（コード・記述・テスト）:

- コード（スキーマ・記録層）: `.agent-skill-chain/source/ledger/schema.sql`（3 カラム＋3 索引）、`.agent-skill-chain/source/scripts/write-workflow-log.sh`（env `MODEL_TIER`/`TIER_RATIONALE`/`TIER_EXCEPTION` 受領・`ensure_column` 冪等追加・CHECK 再構築マイグレーションでの新カラム保全・`NULLIF` 挿入）
- コード（検証層）: `.agent-skill-chain/source/enforcement/ci/audit.sh`（#38・多層ガード・grandfather・fable 例外判定）
- 記述（source 正本）: `.agent-skill-chain/source/enforcement/README.md`（#38 追記）、`.agent-skill-chain/source/skills/agent/run_command.md`・`.agent-skill-chain/source/skills/logging/write-workflow-log/SKILL.md`（配線契約）、`.agent-skill-chain/source/ledger/schema.md`（意味正本・本レビューで as-built 同期）
- 記述（システム仕様書）: `docs/03_データ設計/README.md`（主要カラム俯瞰・索引件数）
- テスト: `test/test-model-tier-gate.sh`（新規・15 シナリオ 21 アサーション）、`test/run-all.sh`（登録）

## 2. レビュー対象のシステム仕様書セクション

- `docs/03_データ設計/README.md` §3.4 主要カラム（俯瞰）・§3.5 索引 — 主たる照合対象。`workflow_log` のカラム・索引の俯瞰を持つため as-built 影響あり。
- `docs/04_機能設計/enforcement/README.md` — 失敗条件の定義・実装所在を source 正本（`enforcement/README.md §失敗条件と差し戻し`）へ委譲し、例示は「#3・#25・#31・#32 等」と非網羅（「等」）で列挙。#38 追加は本俯瞰記述を偽にしないため更新不要。

## 3. 判定と反復（指摘 N→0）

**指摘 1（要修正・対応済み）**: `docs/03_データ設計/README.md` §3.5 が「索引が 7 件ある」と具体列挙していたが、実装は 3 索引（`model_tier`/`tier_rationale`/`tier_exception`）を追加し実体は 10 件。→ 「10 件」に是正し 3 索引名を追記（as-built 同期）。あわせて §3.4 主要カラム俯瞰に 3 カラムの行を追記。
**指摘 2（別正本・対応済み）**: システム仕様書ではないが同時にレビューした ledger 意味正本 `schema.md` の 2 箇所（解説写し CREATE TABLE・「期待スキーマのカラム一覧」）が 3 カラムを欠き実装矛盾（DOCS_NOISE_RULES）。→ 3 カラム・3 索引・カラム説明を追記して as-built 同期（詳細は 04_review §4.2 指摘 A）。

再レビュー: 上記是正後、`docs/03_データ設計/README.md` の俯瞰・索引記述と `schema.sql` 実体が一致。残指摘 0。

**指摘件数**: 2 → 0。

## 4. evidence_source

| 判断 | evidence_source | 参照 |
| --- | --- | --- |
| 索引件数 7→10 の as-built 是正 | existing_code | `schema.sql` の `CREATE INDEX` 実数（`grep -c` = 10）と `docs/03_データ設計/README.md §3.5` を実読 |
| 主要カラム俯瞰へ 3 カラム追記 | existing_code | `schema.sql` の `CREATE TABLE workflow_log` 追加 3 カラムを実読 |
| enforcement 俯瞰仕様は詳細を source 正本へ委譲・#38 追加で as-built 不変 | existing_code | `docs/04_機能設計/enforcement/README.md`（失敗条件は source 正本委譲・「等」列挙）を実読 |
| 実装挙動の回帰なし・#38 実働 | test_output | `test/test-model-tier-gate.sh`（PASS=21 FAIL=0）・`test/test-audit.sh`（PASS=123 FAIL=0） |

---

**最終更新**: 2026 年 07 月 15 日
