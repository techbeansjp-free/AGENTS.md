---
document_id: "912cb13a-423c-4d9b-a4cd-e026b0a1f7c0"
---

このドキュメントは、システム仕様書（`docs/`）と実装の整合性を継続追随ゲート（DOCS_RULES §継続追随ゲート）に基づきレビューした結果を記録します。

# システム仕様書レビュー結果（継続追随ゲート・軽量パス）

**レビュー実施日時**: 2026 年 07 月 17 日 09:16:11（JST）
**対応 issue**: S-2 本リポ github_native 採用（親 GitHub Issue #115 集約のサブ issue。作業フォルダの直接パスは可搬性のため記載せず、GitHub Issue #115 を安定参照とする＝DOCS_NOISE_RULES (iv-b) 準拠）

## 1. 照合した実装変更

本リポ自身を `ISSUE_TRACKING_MODE=github_native` 実効へ切り替える設定・運用変更。変更ファイル: `audit.sh`（#28 に github_native SKIP ガード 1 箇所）・`.gitignore`（新規ドラフト 00〜04 の非追跡化 5 パターン）・`docs/maintainer/decisions/DECISIONS.md`（新設）・`.github/ISSUE_TEMPLATE/issue-request.yml`（新設）・`.agent-skill-chain/project/自己拡張ワークフロー.md`（両モード手順追記）・`.github/workflows/self-enforce.yml`（audit step へ env 付与）。source 配布物（`.agent-skill-chain/source/`）は #28 ガードのみ変更、既定 `local_tracked` は不変。

## 2. レビュー対象のシステム仕様書セクション

- `docs/04_機能設計/enforcement/README.md` — enforcement/監査の系統仕様書。
- `docs/01_システム概要/05_規約/README.md`・`docs/01_システム概要/04_ディレクトリ構成/README.md` — 規約・ディレクトリ構成。
- `docs/03_データ設計/README.md` — workflow.db スキーマ・command enum。

## 3. 判定（軽量パス・更新不要）

**判定: 更新不要（軽量パス）。指摘 0 件。**

**根拠（evidence_source: existing_code）**: システム仕様書（`docs/01〜05・99`・`docs/README.md`・`docs/AI_CI_CD_VISION.md`）を grep 実測した結果、`ISSUE_TRACKING_MODE`／`github_native`／`check_issue_doc_in_gitignored`／`#28`／`#33`／`close 移動`／`二重モード` のいずれの記述も**存在しない**（ヒット 0。唯一のヒットは本 S-2 で新設した `docs/maintainer/decisions/DECISIONS.md` 自身）。docs system spec は enforcement を抽象度でのみ記述し、個別チェック番号・`ISSUE_TRACKING_MODE` 機構・close 移動運用を列挙しない。

- 二重モード機構（`resolve_issue_tracking_mode`・#33 SKIP ガード・close 移動の local_tracked 専用化）を導入・マージした S-1（PR #116）・S-3（PR #121）も docs system spec を更新していない（`git log -- docs/04_機能設計/enforcement/README.md` は初回新設コミットのみ）。これは docs が当該機構を記述対象としていないことの前例。
- S-2 は本リポ固有の設定投入（CI env・`.gitignore`・project 手順）＋既存チェック #28 へのモードガード追加であり、docs が記述する抽象（enforcement の存在・目的）はいずれも不変。

本判定は規模比例（DOCS_RULES §継続追随ゲートの軽量パス）に沿う。as-built 同期の加筆は不要。

## 4. evidence_source

| 判断 | evidence_source | 参照 |
| --- | --- | --- |
| docs system spec は ISSUE_TRACKING_MODE／#28／#33／close 移動／二重モードを記述しない | existing_code | `grep -rln` 実測（docs/01〜05・99・README・VISION でヒット 0、DECISIONS.md のみ） |
| S-1/S-3（機構導入）も docs を更新していない前例 | existing_code | `git log --oneline -- docs/04_機能設計/enforcement/README.md`（初回新設のみ） |
| source 差分は #28 ガードのみ・既定 local_tracked 不変 | observed_runtime | `git diff main...HEAD -- .agent-skill-chain/source/`（audit.sh のみ・github_native 既定化なし） |
