---
document_id: "2fafdd57-85cd-45f1-9465-bf5f3be80c43"
---

このドキュメントは、システム仕様書（`docs/`）と実装の整合性を継続追随ゲート（DOCS_RULES §継続追随ゲート）に基づきレビューした結果を記録します。

# システム仕様書レビュー結果（継続追随ゲート・軽量パス）

**レビュー実施日時**: 2026 年 07 月 17 日 11:00:41（JST）
**対応 issue**: worktree 記録 commit 漏れ検知（R9 / 失敗条件 #41）。親ワークフロー「worktree 運用規律」配下のサブ issue。作業フォルダの直接パスは可搬性のため記載せず、機構名（R9・#41・record-guard）を安定参照とする（DOCS_NOISE_RULES (iv-b) 準拠）。

## 1. 照合した実装変更

worktree 削除前・close 前の issue 記録（00〜04・90_issues.md）の commit/push 漏れ検知機構の新設。変更ファイル: `.agent-skill-chain/source/enforcement/lib/worktree_record_guard.sh`（新規・共有 lib）・`.agent-skill-chain/source/enforcement/claude/PreToolUse.sh`（R9 削除前ゲートを R8 直後・allow 直前に併置）・`.agent-skill-chain/source/enforcement/README.md`（失敗条件 #41 登録）・`.agent-skill-chain/source/commands/verify-and-close.md`（終了時契約アダプタ B 追記）・`test/test-worktree-record-guard.sh`（新規テスト）・`test/run-all.sh`（登録）。source 配布物のうち既存 R7（命名）・R8（物理退避）の関数本体は不変。

## 2. レビュー対象のシステム仕様書セクション

- `docs/04_機能設計/enforcement/README.md` — enforcement/監査の系統仕様書（強制の 4 層・主要強制事項の俯瞰・audit.sh）。
- `docs/AI_CI_CD_VISION.md`・`docs/05_エラー処理と外部通知/README.md`・`docs/maintainer/IMPLEMENTATION_REVIEW.md` — enforcement/PreToolUse への言及箇所。

## 3. 判定（軽量パス・更新不要）

**判定: 更新不要（軽量パス）。指摘 0 件。**

**根拠（evidence_source: existing_code）**: `docs/04_機能設計/enforcement/README.md` は F03.2「主要な強制事項（俯瞰）」で強制事項を**抽象度でのみ**列挙し（代表例 #3・#25・#31・#32 のみ）、F03.3 で明示的に「**失敗条件の定義と実装の所在は [enforcement/README.md §失敗条件と差し戻し] を正本とする**」と委譲している。すなわち docs system spec は**個別の失敗条件番号（#NN）・個別 R ルール（R7/R8/R9）を網羅列挙しない設計**であり、R9/#41 の追加は Layer2 hook（PreToolUse.sh が exit 2 で block）という既存の抽象記述の範囲内に収まる。正本反映は source 側 `.agent-skill-chain/source/enforcement/README.md`（本 PR で #41 を追加済み）で完結する。

- **前例整合**: 同じ親ワークフロー配下で PreToolUse へ R7（命名 Tier1）・R8（削除前退避）を導入した際も docs system spec に個別ルールを追記していない（docs は Layer2 の存在・目的を抽象記述するに留める）。R9 も同型。
- 実装変更は enforcement の**抽象仕様（存在・目的・4 層構造）**を変えず、個別具体の 1 ルール追加に閉じるため、as-built 同期の加筆は生じない。

本判定は規模比例（DOCS_RULES §継続追随ゲートの軽量パス）に沿う。

## 4. evidence_source

| 判断 | evidence_source | 参照 |
| --- | --- | --- |
| docs system spec は個別失敗条件番号・個別 R ルールを列挙しない（抽象＋source へ委譲） | existing_code | `docs/04_機能設計/enforcement/README.md` F03.2/F03.3（「正本とする」委譲文） |
| R9/#41 の正本反映は source enforcement/README.md で完結 | existing_code | `.agent-skill-chain/source/enforcement/README.md`（#41 行・本 PR 追加） |
| R7/R8 導入時も docs を個別更新していない前例 | existing_code | `docs/04_機能設計/enforcement/README.md`（R ルール個別列挙の不在） |
