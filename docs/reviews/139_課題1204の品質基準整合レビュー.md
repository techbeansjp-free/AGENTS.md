# 139 課題1204の品質基準整合レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1204 |
| ラウンド | Step 10 ラウンド1 |
| 比較基点 | `46a0e781b35f468ad19b913580a4a4b45fe7bd62` |
| H_impl | `27e14a78d6093245c4d4f356dd6bf7e74785b71c` |
| 比較基点の由来 | worktree作成時点の`origin/main`のtip |
| モード | full（Q-01がfalse） |
| 対象差分 | 2 file、+1 -1（製品）。commitは`27e14a78` |
| 対象外 | 取り込み条件そのものの変更。`01_開発ワークフロー.md`。`docs/reviews/`のrole authority不整合（#1047） |
| 残り予算 | **2**（同一範囲で最大3ラウンド、収束後にHEADが動いたときの取り直しを1回まで） |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260904_084159_品質基準が開発ワークフローと逆のことを書いている |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-005（**改訂しない**） |
| 成果物行数 | 製品 **+1 -1行**（`02_品質基準.md` 1段落の置換）。仕様 **0行**。支援層 **0行**。**支援層/成果物 = 0倍** |
| 縮小の先行評価 | **既存機構`ASC-CANON-SINGLE-SOURCE-001`の流用を先に評価し、棄却した。** 検出はtoken一致で行うため、`--post-terminal-intake`を登録すると`CANONICAL_SCAN_LOCATIONS`に含まれる`docs/specs/02_要件/01_ワークフロー要件.md`の**正当な要件記述が複製として拒否される**。既存契約の`reason`欄自身が「検出tokenを含まない言い換えは機構では捉えられない」と自認している。**偽陽性を作る強制点を足さない。** owner決裁「強制は基本使わない。どうしてもないと困るところだけ」に従う |
| 決裁 | **#1194 と同一のowner決裁の範囲。** 本件は #1194 の追随漏れである。外部への諮問（codex、2026-09-04）が検出した |
| 実施者・日時 | reviewer（claude）、2026-09-04（JST） |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節） | advanced | claude | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerとreviewerが**同一session**である。0.2節に逸脱として開示する。**本Issueの起点は外部への諮問である** |

### 0.2 開示する逸脱

1. **implementerとreviewerが同一sessionである。** 緩和は、**矛盾の検出が外部（codex）であること**、判定の根拠を2文書の原文読解に置いたこと、揃える方向をmerge済みで動作実証済みの側と定めたことである。
2. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 矛盾の実在 | `.agent-skill-chain/docs/02_品質基準.md` 85行 | 変更前は「同じPRへ取り込まない」。`01_開発ワークフロー.md` 119行は「条件を満たす場合に同じPRへ取り込む」 | 一次資料 |
| 根拠の失効 | 同上 | 「workflow journalは封じられ、Step 10を再記録できない」は #1194・#1201 のmergeで事実でなくなった | 一次資料 |
| 取り込みの成立実績 | PR #1198 | `--post-terminal-intake` で round 2 を記録し、同じPRで外部指摘を取り込んで merge した | 実行記録 |
| 流用可否 | `src/domain/conformance.ts` `CANONICAL_SCAN_LOCATIONS` | `docs/specs/` を含む。token登録は要件記述を巻き込む | 一次資料 |
| 事実でない根拠の除去 | `grep` | 「journalは封じられ」は0件 | 実行記録 |
| テスト | `npm run conformance:check` | `1493 scenarios (1477 passed, 16 skipped)` | テスト出力 |
| commit前candidate | 1 file | working tree clean | Git index |
| Phase A artifact | `docs/reviews/139_課題1204の品質基準整合レビュー.md` | `H_impl` = `27e14a78`。`H_impl..H_final`の差分pathは本file 1件 | Git観測 |
| commit後external | PR、CI run、review | Step 11で観測する | 外部のimmutable証拠 |

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/docs/02_品質基準.md` | M | project | project | 取り込み規定の1段落を置換した。**条件は要約し、正本が`01_開発ワークフロー.md`であることを明記する** | 適合。文書間の参照方向を正本1箇所へ向けた | REQ-WF-005 / AC-01〜04 | 記述だけで実行authorityを持たない。revertで戻る | pass |

## 2. 受け入れ条件の確認

### 2.1 受け入れ条件とシナリオ

| AC ID | 観測方法 | 実装 | 結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | 両文書の原文読解 | 置換後の段落 | 5条件の要約が`01_開発ワークフロー.md`の1〜5と対応する | pass | 前進commit・次round収束・`H_impl`更新・Step 10再記録・スレッド返信の5件 |
| AC-02 | `grep` | 同上 | 「journalは封じられ」0件 | pass | 実行記録 |
| AC-03 | 原文読解 | 同上 | 予算超過・受け入れ条件不充足・安全境界へ及ぶ場合のfollow-up Issue分離を残した | pass | 置換後の段落後半 |
| AC-04 | 両文書の原文読解 | 同上 | どちらを読んでも「条件付きで取り込む。満たさなければ分離する」に到達する | pass | 両文書の原文 |

### 2.2 不変条件

| INV ID | 内容 | 判定 | 証拠 |
|---|---|---|---|
| INV-01 | #1194 の5条件を動かさない | pass | `01_開発ワークフロー.md`を1 byteも変更していない |
| INV-02 | 条件の正本を1箇所にする | pass | `02_品質基準.md`が正本を名指しで参照する |
| INV-03 | 要件・受け入れ条件・シナリオを追加・変更・削除しない | pass | `docs/specs/`の差分は0 file。`trace:check`のorphanは0件 |

## 3. 肯定的評価

- **揃える方向を実績で決めた。** 「取り込む」側は #1194・#1201 でmergeされ、PR #1198 で実際に成立している。逆へ揃えると動作する機構を規範で禁じることになる。
- **条件を複製しなかった。** 要約して正本を参照する形にした。両方へ全文を置くと次の改訂で再び分岐する。**この矛盾はまさにその形で生まれた。**
- **事実でない根拠を消した。** 「journalが封じられる」は残すと読者を誤った前提へ導く。
- **強制点を足さなかった。** 既存機構の流用を実コードで評価し、偽陽性を作ることを確認して棄却した。
- **支援層が0行である。** 1段落の是正に対して支援層を積んでいない。

## 4. 敵対的評価

| 観点 | 攻撃 | 結果 |
|---|---|---|
| 方向の誤り | ワークフロー側を「取り込まない」へ戻す | **採らない。** merge済みで動作実証済みの機構を規範で禁じることになる |
| 複製の再発 | 両文書へ5条件を全文複製する | **採らない。** 次の改訂で再び分岐する。参照にした |
| 分離規定の消失 | 段落ごと削除する | **採らない。** 取り込まない場合の分離規定がこの段落にしかない |
| 検出の欠如 | 機械検査を足す | **採らない。** token登録が要件記述を巻き込む。5節ADV-01に記録する |
| 範囲の膨張 | 他の規範文書も同時に点検する | **採らない。** 本Issueの受け入れ条件外である。**必要なら別Issueとする** |

## 5. 指摘

| ID | severity | 事実 | 観測 | 由来 | 対処 | 判定 | 残余 |
|---|---|---|---|---|---|---|---|
| F-01 | **High** | **2つの配布規範文書が正反対のことを書いていた。** `01_開発ワークフロー.md`は「条件を満たす場合に取り込む」、`02_品質基準.md`は「取り込まない」 | 外部への諮問（codex） | 外部の判断 | 「取り込む」側へ揃え、条件の正本を1箇所にした | valid / resolved | ADV-01 |
| F-02 | Medium | **失効した根拠が残っていた。** 「workflow journalは封じられStep 10を再記録できない」は #1194・#1201 で事実でなくなった | 同上 | 外部の判断 | 除去した | valid / resolved | なし |
| DISC-601 | Low | **`CANONICAL_SCAN_LOCATIONS`に`docs/specs/`が入る。** token登録すると正当な要件記述が複製として拒否される | 実コード読解 | 実測 | 既存機構を流用しない判断を設計へ記録した | valid / resolved | なし |
| ADV-01 | Medium | **2文書の一致に強制点が無い。** 次の改訂で再び分岐しうる | 敵対評価 | 敵対評価 | **修正しない。** 検出機構は偽陽性を作る。owner決裁「強制は基本使わない。どうしてもないと困るところだけ」に従う。**失敗は無言でなく、読めば分かる形で残る** | valid / record-only | 分岐の再発を機械では止めない |

**未解決のCritical / Highは0件である。**

## 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `.agent-skill-chain/docs/02_品質基準.md` | 入る | 利用projectが読む規範から、`01_開発ワークフロー.md`と正反対の記述が消える |
| `docs/reviews/139_課題1204の品質基準整合レビュー.md` | 入らない | `package.json`の`files`に含まれない |

判断: 配布物を更新した
根拠: `.agent-skill-chain/docs/`は配布境界に入る。**矛盾した規範を配ったままにしない。**

## 6. ラウンド固有の確認

### ラウンド1

- 対象: 実装差分の1 file、1段落。
- 確認: 個別監査1行、AC-01〜04、INV-01〜03、肯定5観点、敵対5観点。
- 結果: blocking 0件。record-only 1件（ADV-01）。resolved 3件（F-01・F-02・DISC-601）。

## 7. テスト結果

**表を書く前に1本ずつ実行した。**

| 層・検査 | コマンド | 件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---|---:|---:|---:|---|
| unit・integration・e2e | `npm run conformance:check`（内部で`npm test`を実行する） | 1493 | 1477 | 0 | 16 | pass |
| 静的検査 | `lint`・`format:check`・`typecheck`・`source:check`・`trace:check`・`architecture:check`・`project:quality`・`cli:check`・`workflow:check`・`package:check`・`docs:format`・`test:format` | 12 | 12 | 0 | 0 | pass |
| 監査 | `npm run audit:check` | 1 | 1 | 0 | 0 | pass |

**変異試験は行わない。** 本変更は判定logicを持たない文書1段落であり、変異させる述語が存在しない。

## 8. 総合判定と再開地点

**approved。** 未解決のCritical / Highは0件である。再開地点は実装計画1節の順序表。
