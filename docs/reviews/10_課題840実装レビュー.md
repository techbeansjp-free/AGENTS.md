# 課題840 実装レビュー

> 状態: `ready-for-pr`。project choice差分のfield単位分類を、merge済みdefault branch headを比較基点として固定した内部監査証拠である。独立reviewの承認は自己申告せず、CIとPR reviewの外部証拠で確定する。

## 判定

| 項目 | 値 |
|---|---|
| 状態 | ready-for-pr |
| 比較基点 | `2bec106b06a518616da6c04c1c85c76d586744cb` |
| H_impl | `02863d41ec3d0fc9d58e8d9eb1a493932c4f98a7` |
| H_impl tree | `aea355dea00bdfc887950dcfa15d329953e94ffb` |
| Critical | 0 |
| High | 0 |
| Medium | 0 |
| Low | 0 |
| role分離 | implementerはCodex `gpt-5.6-sol`（reasoning high、service tier default）、coordinatorとreviewerはClaude Opus 5。同一scopeでimplementerとreviewerのidentityは異なる |

trusted側に`projectChoices`が存在して以降、project choiceを変更するPRがauthorityと無関係でも一律拒否されていた。差分をfield単位でauthority、弱化、許可へ分類し、拒否対象を安全・authority境界へ限定する。

## 開発考慮事項

| ID | 考慮事項 | 判定 | 理由 | 要求・確認証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | trusted境界の判定強度を変更するため、緩和方向の反例を先に固定する | SCN-UNIT-CHOICE-001〜005、SCN-INT-CHOICE-002 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 拒否理由と許可されたfield pathを運用判断へ使う | `projectChoiceChanges`、ASC-TRUST-001診断の理由・次の行動・必要authority・rollback |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | applicable | 停止時にCLI診断だけで次の行動を判断させる。画面部品は持たない | 日本語診断7項目、SCN-INT-CHOICE-002 |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | 視覚componentとlayoutを所有せずtoken適用対象が存在しない | projectKindがcli、UI source変更なし |

## 変更ファイル個別監査

| path | status | owner | target layer | 責務・配置 | 依存・循環 | 仕様・追跡 | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/project-choice-diff.ts` | A | package maintainer | domain | project choice差分をauthority・弱化・許可へ分類する単一責務の純関数 | `src/types.js`のtype importだけに依存し外部処理を持たない | AC-840-01、AC-840-05、SCN-UNIT-CHOICE-001〜008 | 型ガードで未知fieldとobject不正をfail-closedへ倒す。fileを削除すれば旧判定へ戻る | pass |
| `src/domain/enforcement.ts` | M | package maintainer | domain | fingerprint一括比較を分類器の呼び出しへ置換し、authorityと弱化を別診断へ分ける | enforcementからproject-choice-diffへの片方向依存。循環なし | AC-840-02、AC-840-04、SCN-UNIT-RISK-017 | 拒否時は理由・次の行動・必要authority・rollbackを日本語で返す。差分をrevertすれば旧挙動へ戻る | pass |
| `src/domain/migration.ts` | M | package maintainer | domain | `Compatibility`へ`projectChoiceChanges`を追加し戻り値契約を一致させる | 既存の`compareTrustedPolicy`参照だけ。循環なし | AC-840-04 | 型追加のみで挙動を変えない。field削除で戻せる | pass |
| `.agent-skill-chain/project/conformance/bindings.json` | M | repository maintainer | project conformance | I4のenforcement pointへ分類器を、反例へSCN-UNIT-CHOICE-003を追加する | project bindingから実在exportへの片方向参照 | AC-840-06 | 追加行だけを戻せば旧bindingへ復帰する | pass |
| `docs/specs/10_セキュリティ/00_信頼境界.md` | M | repository maintainer | 仕様 | project choice差分の3分類と脅威・予防・検知を成立中の契約として記録する | 仕様から実装への参照のみ | AC-840-01〜05 | 節を戻せば旧記述へ復帰する | pass |
| `docs/specs/00_仕様書構成/00_仕様書索引.md` | M | repository maintainer | 仕様 | 追跡文書の索引へ課題840追跡を追加する | 索引から追跡文書への片方向link | AC-840-01 | 行を戻せば旧索引へ復帰する | pass |
| `docs/specs/15_要件追跡/03_課題840追跡.md` | A | repository maintainer | 仕様 | AC、enforcement point、反例SCN、証拠、rollbackを対応付ける | 追跡表から実装とtestへの片方向参照 | AC-840-01〜06 | fileを削除すれば追跡が消えるため索引と同時に戻す | pass |
| `test/features/unit/project-choice-diff.feature` | A | package maintainer | test | 分類器のauthority、弱化、許可、fail-closedを反例で固定する | featureからstep定義への参照のみ | SCN-UNIT-CHOICE-001〜008 | 反例が消えると弱化検出を失うため実装と同時に戻す | pass |
| `test/features/integration/project-choice-diff.feature` | A | package maintainer | test | 実repositoryのproject choiceに対する許可と拒否を検証する | featureからstep定義への参照のみ | SCN-INT-CHOICE-001〜002 | 同上 | pass |
| `test/steps/project-choice-diff.steps.ts` | A | package maintainer | test | 上記featureのstep実装。実workspaceを変更しない | domainとpolicy loaderのexportだけへ依存 | SCN-UNIT-CHOICE-001〜008、SCN-INT-CHOICE-001〜002 | 実repositoryを読み取り専用で使い書き込みを行わない | pass |

Gitの`2bec106b06a518616da6c04c1c85c76d586744cb..02863d41ec3d0fc9d58e8d9eb1a493932c4f98a7`に含まれる10 pathと表の10行は重複なし・欠落なしで一致する。

## 肯定・敵対レビュー

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ | pass | `release`と`ci`の変更、test層・禁止型・capability・補助言語の弱化、未知field、object不正のすべてが反例で拒否される |
| 価値・実現可能性 | pass | Issue #836のactivationを含む正当なproject choice変更が通過し、開発の恒久deadlockが解消する |
| 整合性 | pass | `00_運用ポリシー.md`のrisk比例型ruleと、課題834の「正当な拡張経路を閉じない」原則へ一致する |
| 保守性 | pass | 分類は単一責務の純関数へ分離し、enforcementは診断整形だけを持つ |
| 反例・失敗経路 | pass | trusted側の値が型契約を満たさない場合もfail-closedで弱化へ倒す |
| 境界値・悪用 | pass | 未知fieldの黙認、`applicable`格下げ、宣言削除、legacy文字列への逆戻りを個別に拒否する |
| 安全性・データ損失 | pass | 判定は読み取り専用で外部writeを持たない |
| rollback | pass | 分類器fileの削除と`compareTrustedPolicy`のrevertで旧挙動へ戻せる |
| 範囲漏れ | pass | `compareTrustedPolicy`の呼び出し元4か所の戻り値契約をすべて確認した |

### 敵対観点の残論点

`quality.lintCommand`等のcommand文字列は許可分類へ入るため、無効なcommandへ差し替える緩和が理論上ありうる。ただし`scripts/check_project_quality.ts`がproject choiceとpackage scriptの完全一致を別gateで検証し、同fileはtrusted品質proposalで保護されているため、本変更では新しい迂回経路を作らない。out-of-scopeとして記録する。

## テスト結果

| 層・検査 | コマンド | 成功 | 失敗 | 判定 |
|---|---|---:|---:|---|
| 静的品質 | `lint`、`format:check`、`typecheck`、`source:check` | 4 | 0 | pass |
| 全BDD | `npm test` | 309 | 0 | pass |
| 配布・追跡・監査 | `build`、`docs:format`、`test:format`、`trace:check`、`architecture:check`、`conformance:check`、`package:check` | 7 | 0 | pass |

`audit:check`は本artifactを追加したH_finalで実行する。

## 停止点

- H_impl: `02863d41ec3d0fc9d58e8d9eb1a493932c4f98a7`。
- PR作成後のmergeはCIとreviewの外部証拠が揃った時点で別操作として行う。
- release、branch削除、worktree cleanupは本Issueの終了処理では実施しない。

H_finalはこのreview artifactだけを追加するcommitとし、以後このtracked artifactを変更しない。
