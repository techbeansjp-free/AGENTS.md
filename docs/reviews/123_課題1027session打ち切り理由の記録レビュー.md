# 123 課題1027 runJsonlSessionの打ち切り理由をstderrへ残す 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。**「量を判断へ比例させる」原則に従って書く。**

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1027 |
| 比較基点 | `9c6c94d631febe7e21f8ed901960b9845f81ba02` |
| H_impl | `ddf8e99a0bb0282b26edb408e046b5d9062c12af` |
| 対象差分 | 製品1件、test 2件、仕様2件、**#1024の故障注入証跡3件**の8 file |
| 対象外 | **次はいずれも同じ理由で対象外である。** 1MiBというbound自体（streamであり `run()` の64MiBと同列に扱わない）、制御フロー、`allowFailure` の意味、`SCN-UNIT-PROC` 群の要件再割当（§2.0のDISC-002） |
| 残り予算 | Step 10の上限3ラウンドのうち1を残す |
| ラウンド | Step 10 ラウンド2 |
| ラウンド数 | 2（Step 10のラウンド1〜2） |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260903_093325_runJsonlSessionの打ち切り理由をstderrへ残す |
| モード | quick（Q-01〜Q-08がすべて真） |
| 仕様の所有箇所 | `docs/specs/15_要件追跡/00_追跡表.md:9` の REQ-LC-008 行。**#993 が `SCN-UNIT-PROC` 群をこの行へ相乗りさせている（§2.0のDISC-002）** |
| 成果物行数 | 製品: `src/lib/process.ts` で30 insertions・8 deletions。test: 106 insertions。**支援層: staging 00が6,575文字（本日平均34,806文字の19%）** |
| 縮小の先行評価 | 実施済み。**boundを変えない。** #993 の `run()` は64MiBへ引き上げたが、streamでは出力量が対話長に比例するため上限そのものの妥当性は別判断である |
| authority | 変更しない。**制御フローと `allowFailure` の意味を変えない。3経路とも従来どおり終了値1で終わる** |
| 実施者・日時 | reviewer、2026-09-03（JST） |

### 0.1 routing入力契約

| role欄 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類、変異試験 | routine（riskがlow） | claude | Opus 5、effort high | 制御フローが1つでも変わったら停止する | implementerとreviewerはいずれも本sessionのAI agentであり、**Git commit authorも同一である** |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 3経路が理由を載せないこと | 是正前の `src/lib/process.ts` | 上限超過・timeout・起動失敗のいずれも `child.kill("SIGKILL")` して `finish(1)` するだけ | 実測 |
| **第3の経路の発見** | `child.on("error", () => finish(1))` | **起票時は上限超過とtimeoutの2経路だったが、起動失敗も同じ形だった** | 実測 |
| 消費者が1件であること | `grep runJsonlSession src/` | `src/adapters/provider.ts` のみ | 実測 |
| 保護対象外であること | `scripts/check_project_quality.ts:38-51` | `src/lib/process.ts` は `PROTECTED_FILES` に含まれない | 実測 |
| 制御フローの非変更 | `git diff` | 3経路とも `SIGKILL` と `finish(1)` を維持。`redactSecrets` の適用位置も不変 | 実測 |
| 変異の有効性 | 変異5件 | 3経路の理由削除、stream名の固定、timeout値の固定を**全件kill** | 実測 |
| 証跡束縛の追随 | `SCN-INT-CONSUMER-005` | `src/lib/process.ts` を変えたため #1024 の故障注入証跡3件が落ちた。束縛SHAを更新して合格 | 実行観測 |
| 全gate | `conformance:check` | 1422 scenario、0失敗（証跡SHA更新後） | 実行観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: pass。`failWithReason` → `finish` の一方向。`settled` guardで再入を防ぐ
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: pass
- reviewer stable IDがPR author/実装commit author stable IDと異なる: **異ならない。** §9に記録した
- 既定branch追随: **実施済み。** `main` が動いたため追随し、他Issueの履歴行への削除が0件であることを確認した

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/lib/process.ts` | M | package | package | `failWithReason` を切り出し3経路が呼ぶ | `failWithReason` → `finish` の一方向 | AC-01からAC-04。SCN-UNIT-PROC-005〜007 | revertで復旧 | pass |
| `test/features/unit/process-boundary.feature` | M | project | project | Scenario 3件を既存 `SCN-UNIT-PROC` 家族へ連番で足す | featureはstepsへ一方向 | 同上 | 同上 | pass |
| `test/steps/unit.steps.ts` | M | project | project | Given 2件、When 3件、Then 3件、World field 1件 | stepsは製品へ一方向 | 同上 | 同上 | pass |
| `docs/evidence/1024-consumer-acceptance/mechanism-1-git-dependency.md` | M | package | evidence | `src/lib/process.ts` の束縛SHAを現行内容へ更新する | 証跡は製品を参照する。循環なし | SCN-INT-CONSUMER-005 | 同上 | pass |
| `docs/evidence/1024-consumer-acceptance/mechanism-2-packed-bin.md` | M | package | evidence | 同上 | 同上 | 同上 | 同上 | pass |
| `docs/evidence/1024-consumer-acceptance/mechanism-3-scale-output.md` | M | package | evidence | 同上。**過去の実測記録（63行目）は書き換えず、#1024実施時点の値であることを明示した** | 同上 | 同上 | 同上 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package | spec | SCN 3件を既存行へ足す | 追跡のみ | 該当なし | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | 変更と非変更範囲を記録する | 履歴のみ | 該当なし | 1行の削除で復旧 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: pass（8件）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: pass

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | **起票時に挙がっていない第3の経路があった。** `child.on("error", () => finish(1))` も理由を載せていない | 対象が2経路から3経路へ増える | なし | 3経路すべてを `failWithReason` へ統合した | `SCN-UNIT-PROC-007` と変異M3 | no-spec-impact | pass |
| DISC-002 | `SCN-UNIT-PROC` 群が追跡表で **REQ-LC-008（worktreeを読み取り専用で走査する）** の行に相乗りしている。要件本文はprocess境界を述べていない | 追跡の正しさ | なし | **本Issueでは是正しない。** #993 が作った状態であり要件の再割当は別作業である | `git log -S` で `e844737e`（#993）が作ったことを確認 | no-spec-impact。**分離すべき事象として記録する** | pass |
| DISC-003 | **`src/lib/process.ts` を変えると #1024 の故障注入証跡3件が落ちる。** 証跡が同fileの内容SHAを束縛している | 同fileを触る全変更 | なし | 束縛SHAを現行内容へ更新した。**過去の実測記録（mechanism-3の63行目）は書き換えず、#1024実施時点の値であることを本文で明示した** | `SCN-INT-CONSUMER-005` の合格 | no-spec-impact | pass |

**DISC-003 は束縛の意図どおりの挙動である。** 証跡は「主張する実体が変わったら再確認させる」ために内容SHAを持つ。**過去の観測そのものを書き換えないことが、この束縛を意味あるものに保つ条件である。**

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-PROC-005 | 上限超過の理由 | 合格。**stream名と実バイト数まで要求する** | pass | §7 |
| AC-02 | SCN-UNIT-PROC-006 | timeoutの理由 | 合格。**実ミリ秒まで要求する** | pass | §7 |
| AC-03 | SCN-UNIT-PROC-007 | 起動失敗の理由 | 合格 | pass | §7 |
| AC-04 | SCN-UNIT-PROC-001〜004 | 制御フローと `allowFailure` の非変更 | 既存4件が合格 | pass | §7 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | `stderr` へ追記するため秘密情報が載る経路を増やさないことを確かめる必要がある | 追記はstream名、上限バイト数、timeoutミリ秒、`error.message` だけ。**`finish` が `redactSecrets` を通す経路を変えていない。追記は `finish` より前に行うため `redactSecrets` の対象に入る** |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | **本変更の目的そのものである** | 3経路それぞれの反例で理由が残ることを確認した |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 人が触れるUIを持たないlibrary関数である | `projectKind` が `cli` |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UI componentもthemeも持たず、色・間隔・typographyの決定を含まない | `capabilities.designTokens` が `not-applicable` |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 3経路すべてで理由が `stderr` に残る |
| 価値 | 利用者・運用上の目的を満たすか | pass | LLM sessionの失敗が理由不明でなくなる。**出力量は対話長に比例するため利用側で踏みうる** |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 1関数の切り出しで成立する |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 追跡表へSCN 3件を足した |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 3経路が同じ1関数を呼ぶ。理由の書式が1箇所に集まる |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| **悪用** | **理由に秘密情報が載る** | pass | `error.message` だけが外部由来である。**追記は `finish` より前に行うため `redactSecrets` の対象に入る。適用位置を変えていない** |
| 悪用 | 診断の追加を口実に制御フローを緩める | pass | 3経路とも `SIGKILL` と `finish(1)` を維持。既存4 Scenarioが回帰しない |
| 反例 | 要件を破る入力・状態がないか | pass | 変異5件を全件kill |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | `settled` guardで二重追記を防ぐ |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | `stderr` が空のときと改行終わりのときで改行の挿入を分岐する |
| 安全性・データ損失・ロールバック | 認可・上書き・復旧 | pass | 読み取り専用の診断追加。revertで戻る |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | 消費者は `src/adapters/provider.ts` の1件。**#1024の証跡3件への波及を検出して追随した** |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| M-01 | Medium | **`SCN-UNIT-PROC` 群がREQ-LC-008（worktree走査）の行に相乗りしている。** 要件本文はprocess境界を述べていない | 追跡表の実測 | 追跡の正しさ | **本Issueでは是正しない。** #993 が作った状態であり、要件の再割当は別作業である | valid（out-of-scope。record-only） | 残存する |
| L-01 | Low | **1MiBというboundの妥当性を評価していない。** streamでは出力量が対話長に比例する | 実測 | 将来 | 本Issueの対象外。**理由が残るようになったため、超過が起きたときに判断材料が得られる** | valid（record-only） | 残存する |

**Critical/High 0件。**

## 6. ラウンド固有の確認

### ラウンド1（Step 10、2026-09-03、candidate HEAD = `H_impl`）

- 全評価基準を確認した: はい。肯定5観点・敵対7観点をすべて評価した
- 指摘を確定した: はい。M-01とL-01はいずれも記録のみ
- 次ラウンド対象のCritical/High: **なし**
- blocking: 0件

### ラウンド2

- 本artifactのcommitでHEADが動くため再固定する

### ラウンド3

- 未実施。**予算1を外部reviewの是正のために残す**

## 7. テスト結果

**承認証拠は成功CI runである。**

実行したcommand: `docs:format`、`test:format`、`lint`、`format:check`、`typecheck`、`source:check`、`trace:check`、`architecture:check`、`skills:check`、`conformance:check`、`audit:check`、`issue validate`

全layerの合計: **1422 scenario、1405 成功、0 失敗、16 skip。** `conformance:check` は内部で `npm test` を実行するため単独で回した。

失敗またはskipがある層: skipした16 scenarioはsemantic graphのGraphQLite assetが未設定な環境に起因する。本差分はこの経路に触れない。

対応する成功CI run: PR作成後に取得し、merge前に現`H_final`へ束縛する。

**変異試験の全量。**

| 変異 | 結果 |
|---|---|
| 上限超過の理由を消す | kill |
| timeoutの理由を消す | kill |
| 起動失敗の理由を消す | kill |
| stream名を `stderr` へ固定する | kill |
| timeout値を「不明な時間」へ固定する | kill |

## 8. 配布物影響

| 変更path | 配布境界に入るか |
|---|---|
| `src/lib/process.ts` | **入る**（`dist/src/` として） |
| `test/` 2件 | 入らない |
| `docs/evidence/` 3件 | 入らない |
| `docs/specs/` 2件 | 入らない |

判断: 配布物を更新した

根拠: `package.json` の `files` は `dist/src/` を含む。**8 fileのうち1 fileが配布境界に入る。**

**releaseの起動と計画は分けて記録する。** `release.yml` は `main` への `push` で起動し、その後 `planAutoRelease` が配布digestの差分を見て計画を決める。**本変更は配布digestを変えるため、version bumpとtagが作られる見込みである。** merge後に実観測する。**npm公開は自動経路では発火しない。**

| 対象 | revertで戻るか | 実行者 | 手順 | 完了確認 |
|---|---|---|---|---|
| repository状態 | **戻る** | 実行役 | 打ち消しPRを通常mergeする | `git diff` が元の状態と一致する |
| 公開済みtagとGitHub Release | **戻らない** | repository owner | 削除せず次versionで前進する | `gh release list` に新tagが載る |
| npm registryの公開物 | **公開されない** | 該当しない | 本経路では公開しない | `npm view` に該当versionが無い |

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | **本artifact作成時点では無い。** exact-headは`H_final`。immutable GitHub reviewは0件 |
| reviewerがPR author・実装commit authorと異なる | **異ならない。** 同一sessionのAI agentであり、Git commit authorも同一である |
| 観測したreview commentとapprovalの件数 | 本session内のreview 2ラウンド（Critical/High 0、Medium 1、Low 1）。GitHub review 0件 |

**`02_品質基準.md` の「独立reviewが成立しない場合」に従う。** 同節は「既定は停止ではなく、無記録での通過の禁止である」と定める。

**ただし本件では、既存の機械的検査が独立した検出者として働いた。** `SCN-INT-CONSUMER-005` が #1024 の故障注入証跡の束縛破れを検出し、私が気付いていなかった波及を示した。

**PR作成後の外部reviewを待つ。** 予算1ラウンドを残してある。

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `15_要件追跡/00_追跡表.md` へSCN 3件、`01_変更履歴.md` へ1行
- ドメイン用語台帳: 新規用語を追加しない
- 要件・変更・SCN・テストの追跡: REQ-LC-008 → AC-LC-008 → SCN-UNIT-PROC-005〜007。**ただしM-01のとおり要件本文はprocess境界を述べていない**
- UI・トークンの判断: UIを所有しないため対象外。ADRを追加しない

## 11. 総合判定と再開地点

- 未解決Critical/High: **なし**
- Medium/Lowの記録: M-01（SCN群の要件相乗り）、L-01（boundの妥当性未評価）
- 判定: approved（Step 10 ラウンド2）
- 新しい権限が必要な事項: mergeは別authority
- 残存リスク:
  1. **`SCN-UNIT-PROC` 群の要件割当が正しくない。** #993 が作った状態であり別作業とした
  2. **1MiBというboundの妥当性を評価していない。** 理由が残るようになったため超過時に判断材料が得られる
  3. **`src/lib/process.ts` を触る全変更が #1024 の証跡3件を落とす。** 束縛の意図どおりだが、変更のたび追随が要る
- 次に許可される操作: push、PR作成、外部reviewの到着確認、必須check 2件の全緑確認、およびownerが承認したauthorityによる**通常merge commit方式**でのmerge。**squash mergeを使わない**
- 次回の再開地点: PR作成から
