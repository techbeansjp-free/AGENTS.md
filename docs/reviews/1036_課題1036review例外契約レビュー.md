# 04 レビュー

> 肯定と敵対の両観点を毎ラウンド確認する。指摘なしの承認は有効。Medium/Lowは記録するだけで、自動修正・追加ラウンド・ゲート停止を起こさない。用語と責務の境界は`成果物用語と責務境界`（`.agent-skill-chain/docs/01_開発ワークフロー.md`）を正本とする。
>
> 埋めた成果物は`docs/reviews/`または`.agent-skill-chain/reviews/`へ置き、実装commitの後にその1 fileだけをcommitする。

| 読者 | 読む節 |
|---|---|
| 発注・評価する人 | 要約 → §5 指摘 → §11 総合判定 |
| 実装・レビューする人 | 全節 |
| 運用する人 | 要約 → §8 配布物影響 → §10 仕様整合性 |

## 要約

**この5行だけで、何を見たreviewかが分かるように書く。** 各行は1〜2文。詳細は対応する節が持つ。

| 項目 | 内容 |
|---|---|
| 何が問題だったか | 例外宣言がmergeのreview不足を免除できると読める規範と、最低1件のexact-head reviewを要求する現行実装が食い違っていた。 |
| 何を解決しようとしたか | Issue #1036の選択肢に沿い、例外宣言とPR・merge認可を区別して現在の安全な契約を正本に記す。reviewer不在の無審査mergeは追加しない。 |
| 何を行ったか | 旧PR #1468の修正済み6文書を最新mainから再構成し、#1306・#1396の契約行を保持した。旧PRのCodeRabbit指摘2件を引き継いだ。 |
| 何を確認したか | Opusが新H_impl `aed8acf1`の6文書を別processで読取専用レビューし、レビュー・マージ契約と現行コードの一致を確認した。対象6 SCN・54 stepとconformance 87 SCN・816 stepは合格。 |
| 判定 | approved |

## 1. 入力証拠

PR番号、Actions run ID、immutable review IDはPR作成後にしか存在しないため**この文書へ書かない。** `review evidence`とdelivery stateがappend-onlyで保持する。reviewerの独立性は`merge.reviewIndependence`が決める。既定の`context-isolated`はimplementerと別session/context、exact HEAD固定、対象差分の非変更、肯定・敵対reviewとfinding記録を要求し、同一GitHub actorでも成立する。このときtracked artifactの`approved`と保存済みreview session・Step 10 bindingがformal approvalになる。`actor-independent`はPR authorおよび観測済み`H_impl` commit authorと別のstable actor IDによるprovider `APPROVED`を要求する。両modeともexact HEAD一致は必須とし、tracked文書へ自身のcommit SHAを書かず、**H_final後はartifactを更新しない。**

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | .agent-skill-chain/tmp/issues/1036_review_exception_merge | staging digest 0c9c9612c28119f627c971cd86d6f90400aff5a49fbcbe87a2a90ab0c30fcf8f | 既存コード |
| 差分 | `77d4ba021ede59ca290b8eb077d301f307c65467`..`aed8acf1342452578fd63642b6fb820881163496` | 実装6 path。review artifactは監査対象差分から除外 | Git差分 |
| テスト | 対象Cucumber 6 SCN、conformance 87 SCN・品質gate | 対象6 SCN・54 step、conformance 87 SCN・816 step合格。gate結果は§7 | テスト出力 |
| 仕様 | `docs/specs/02_要件/04_仕様・品質管理要件.md`ほか | updated | 既存文書 |
| commit前candidate | .agent-skill-chain/docs/02_品質基準.md、docs/specs/02_要件/04_仕様・品質管理要件.md、docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md、docs/specs/11_非機能/01_品質要件.md、docs/specs/15_要件追跡/00_追跡表.md、docs/specs/15_要件追跡/01_変更履歴.md | H_impl aed8acf1342452578fd63642b6fb820881163496 | Git index |
| Phase A artifact | `docs/reviews/1036_課題1036review例外契約レビュー.md` | H_impl後に本fileだけをcommitする。tracked artifactへ自身のSHAを書かない | Git観測 |
| 独立review | Claude Code CLI Opus、`--mode review` | 新H_implを対象に1回実施。旧PRのreview session digestは新HEADのformal bindingには使わない | CLI実測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **確認した。** Opusはread-onlyで差分に触れていない。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: artifact commit後にGitで確認する。
- reviewerの独立性が要求水準を満たす: implementerのCodex sessionと別のClaude Code CLI Opus processで新H_implへ1回実施した。
- 既定branch追随を行った場合、取り込みがartifact commitより前にあり、`比較基点`が取り込んだ既定branch tip、`H_impl`がartifact直前の最新commitを指し、個別監査表を`比較基点..H_impl`から再生成した: 追随なし。再構成の基点はmain `77d4ba02`。#1306・#1396の行を保持した。

### 1.1 変更ファイル個別監査

基準SHAとの差分にある全ファイルを、生成物（`dist/`等）も含めて1ファイル1行で記録する。まとめ行、directory単位の一括承認、test成功だけの代替を認めない。`audit:check`の他の検査対象外となる生成物でも、各行へ生成元との対応確認方法と配布影響の確認方法を記録し、差分path集合と表のpath集合を一致させる。

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/docs/02_品質基準.md` | M | ASC | docs | 独立reviewとmerge認可の規範 | docs → runtime、循環なし | AC-1036-01〜03 | 文書のみ。revert可能 | pass |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | M | ASC | docs/specs | REQ-SQ-015の適用契約 | spec → runtime、循環なし | AC-1036-01〜03、RVX-012/013 | 文書のみ。revert可能 | pass |
| `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | M | ASC | docs/specs | CLI認可契約 | spec → runtime、循環なし | AC-1036-01/02 | 文書のみ。revert可能 | pass |
| `docs/specs/11_非機能/01_品質要件.md` | M | ASC | docs/specs | QLT-RVX-001の旧記述を訂正 | spec → runtime、循環なし | AC-1036-01、MERGE-013 | 文書のみ。revert可能 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | ASC | docs/specs | 要件と既存SCNの追跡 | spec → test、循環なし | AC-1036-01〜03 | 文書のみ。revert可能 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | ASC | docs/specs | 変更理由と対象文書の記録 | spec → change、循環なし | Issue #1036 | 文書のみ。revert可能 | pass |

- 基準SHA..H_implの差分path集合と表のpath集合が完全一致する: **確認した。** 6 path。review artifactはH_impl後の独立した成果物として扱う。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **確認した。** runtime変更なし。
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: 新H_implで修正を要するfindingは無かった。旧PRの修正内容は現行runtimeと再照合した。

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

発見IDは実装計画（fullは03、quick/pocは集約00）の`DISC-*`と同じ字面を使い、本文書内で別IDへ言い換えない。

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| 該当なし | 実装中の契約変更を要する発見なし | なし | なし | 既存runtime契約に規範を合わせた | 対象SCNとOpus差分review | updated | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1036-01 | SCN-INT-MERGE-013、017 | 品質基準、QLT-RVX-001 | 2件を含む対象6 SCN・54 step合格 | pass | `delivery.ts:916`、focused Cucumber |
| AC-1036-02 | SCN-UNIT-RVX-001〜013、SCN-INT-MERGE-013 | REQ-SQ-015、CLI契約 | 宣言schemaとmerge経路を静的照合。対象integrationは合格 | pass | `conformance.ts:1702`、Opus review |
| AC-1036-03 | SCN-INT-MERGE-017〜021 | 品質基準、追跡表 | 5件を含む対象6 SCN・54 step合格 | pass | `delivery.ts:889-930`、focused Cucumber |

### 2.2 開発考慮事項の適用判定（必須）

00の判定・理由・証拠から差分が無ければ、表の代わりに`開発考慮事項の適用判定は00_要求定義.md §6.1と同じ`の1行を置ける（01〜03と同じ参照行）。差分がある行だけを表に残してよい。**`review validate`はこの§2.2の内容を検証しない**（01〜03の`issue validate`と異なり、review artifactのDC判定に対する機械検証は無い）。記述量を減らすための人・エージェント向けの案内であり、参照行を置いても4行の表を書いても合否は変わらない。

開発考慮事項の適用判定は00_要求定義.md §6.1と同じ。

## 3. 肯定的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ（要件と観測結果の一致） | pass | 最低1件のreview要求と例外宣言の非接続は`delivery.ts`・`conformance.ts`に一致。 |
| 価値（利用者・運用上の目的） | pass | 一人repositoryでも別context reviewで進める現行契約を明示した。 |
| 実現可能性（環境・依存・権限） | pass | 新しいmerge authorityを作らず既存Step10とtrusted policyで説明した。 |
| 整合性（設計・コード・テスト・仕様） | pass | 6 pathの規範を対象SCNへ追跡し、OpusのHigh2件を修正した。 |
| 保守性（責務・命名・変更容易性） | pass | runtime変更なし。現行契約を要件と品質基準へ限定して記録した。 |

## 4. 敵対的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 反例（要件を破る入力・状態） | pass | `context-isolated`はformal approvalが無ければprovider approvalがあっても拒否し、両者の合計が実効要求件数未満でも拒否する。`actor-independent`は別actorのprovider approvalが0件なら拒否する。MERGE-013合格。 |
| 失敗経路（外部失敗・部分失敗） | pass | reviewer不在では停止し、例外宣言で黙って通さない。 |
| 境界値（空、最大、最小、重複、Unicode） | pass | `requiredReviews=0`でも実効下限1件。Unicode本文は文書のみでruntime非変更。 |
| 悪用（注入、経路脱出、権限外） | pass | candidateの例外宣言でtrusted merge authorityを弱化できない。 |
| 安全性（認証、承認、秘密情報、Zero Trust） | pass | owner操作承認とreview証拠を別契約と記載。秘密情報を追加しない。 |
| データ損失（上書き、削除、部分公開、履歴消失） | pass | 文書差分のみ。Git revertで復旧可能。 |
| ロールバック（復旧参照、状態保持、再開可能性） | pass | review不足のPRは停止し、別context review後に再開可能。 |
| 範囲漏れ（呼び出し元、利用側、配布物、文書） | pass | Opusが見つけたQLT-RVX-001を追加修正し、6 pathの差分を監査した。 |

## 5. 指摘

指摘なしの場合は「指摘なし」と明記する。

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| REV-1036-01 | High | QLT-RVX-001がmergeを記録のみで許すと記載 | 旧PR Opus round 1、`delivery.ts:916` | 品質要件 | 新H_implへ最低1件のreview条件を移植 | resolved | なし |
| REV-1036-02 | High | REQ-SQ-015から不可逆配布例外の期限条件が欠落 | 旧PR Opus round 1、`conformance.ts:1702` | 仕様・品質管理要件 | 新H_implへ期限付き失効条件を移植 | resolved | なし |
| REV-1036-03 | Medium | 外部配布例外の文言がruntime認可と矛盾するとの疑い | Opus round 1、品質基準の別節 | 外部配布 | PR/merge認可と人の外部配布判断は別契約。後者は本Issueの実装対象外 | false-positive | 現在の外部配布契約は人が判断する |
| REV-1036-04 | Low | trace表の対象SCN実行済みの証拠不足 | Opus round 1時点のjournal | 追跡表 | 修正後の対象6 SCN・54 stepをホスト実行し合格 | resolved | なし |
| REV-1036-05 | Medium | stable actor IDの要件をactor-independent固有と読める | 旧PR Opus round 3、`delivery.ts:803-809` | CLI契約 | 新H_implへ両mode共通条件の訂正を移植 | resolved | なし |
| REV-1036-06 | Low | context-isolatedのprovider承認にactor別最新状態の折りたたみが見えない | 旧PR Opus round 3、`delivery.ts:810-872` | CLI契約 | 新H_implへ最新状態が`APPROVED`のactorだけを数える記述を移植 | resolved | なし |
| REV-1036-07 | Low | Phase A artifactに存在しないpathが記載されていた | CodeRabbit PR #1468 | review artifact | 本fileの正しいpathへ訂正 | resolved | なし |
| REV-1036-08 | Low | formalとprovider approvalの説明が未分離 | CodeRabbit PR #1468 | CLI契約 | formal必須・provider加算を分離して新H_implへ移植 | resolved | なし |
| REV-1036-09 | Low | 不可逆配布例外がREQ-SQ-015から欠落したとの疑い | 新H_impl Opus review | 要件文書 | 同じ段落で配布例外の対象明示と期限付き失効を要求している。コード上のpublish経路は本Issue対象外 | false-positive | なし |

## 6. ラウンド固有の確認

- 新main `77d4ba02`から再構成したH_impl `aed8acf1`を、Claude Code CLI Opusの別process・`--mode review`で読取専用確認した。
- Critical/High/Mediumの有効指摘は0件。Lowの配布例外指摘はREQ-SQ-015の同一段落を確認して不成立と判定した。旧PRで有効だったCodeRabbit 2件は移植後の現行文書とコードで解決を確認した。
- 旧PRでの4 roundとsession digestは履歴情報であり、新H_implのformal approvalには流用しない。Step 10 bindingは後続工程で新HEADに対して確立する。

## 7. テスト結果

- `npm run docs:format`、`trace:check`、`format:check`、`lint`、`typecheck`、`source:check`、`test:format`、`architecture:check`、`package:check`、`project:quality` は合格。
- `NPM_CONFIG_CACHE=/private/tmp/asc-1036-npm-cache npm run conformance:check`: 87 scenario・816 step全件合格。初回はsandboxのnpmログpathで1件失敗したため、cache/logを隔離して同一検査を再実行した。
- merge契約の対象 `SCN-INT-MERGE-013、017〜021`: 6 scenario・54 step全件合格。
- `audit:check`はreview artifactだけをH_impl後にcommitした後で実行する。`git diff --check` は合格。

## 8. 配布物影響

packageとして配布する場合だけ記入する。配布境界はpackage manifestの配布file指定を正本とし、compileされて配布される`source`も含める。

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| .agent-skill-chain/docs/02_品質基準.md | 入る | 利用者へ配るreview・merge運用規範を訂正 |
| docs/specs/02_要件/04_仕様・品質管理要件.md | 入らない | なし |
| docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md | 入らない | なし |
| docs/specs/11_非機能/01_品質要件.md | 入らない | なし |
| docs/specs/15_要件追跡/00_追跡表.md | 入らない | なし |
| docs/specs/15_要件追跡/01_変更履歴.md | 入らない | なし |

判断: 配布物を更新した

根拠: `package.json`のfiles境界に含まれる`.agent-skill-chain/docs/02_品質基準.md`を更新した。runtimeの実行条件は変更していない。

## 9. 独立reviewの成立

PR作成前に観測できるものだけを書く。immutable review IDやapproval件数は書かない。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated |
| その要求を満たすこと | はい |
| reviewerとimplementerのidentity・context比較 | implementerは本Codex session、独立reviewerは別processのClaude Code CLI Opus。新H_impl `aed8acf1`を読取専用で確認した。 |
| reviewerが対象差分を変更していないこと | はい（Opusは`--mode review`のRead/Glob/Grepのみ、review後のGit差分に想定外pathなし） |

外部への不可逆な配布で外部証拠を要求され、かつ無い場合だけ次を記入する。承認元・承認者・承認日時・失効日時は正本を参照し複製しない。

本PRは外部への不可逆な配布を行わないため、配布例外は適用しない。

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: REQ-SQ-015、CLI・GitHub契約、QLT-RVX-001、要件追跡表、変更履歴。
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: 既存のreview・merge用語だけを使い、新規定義なし。
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: Opusが規範と`delivery.ts`・`conformance.ts`を照合した。
- 要件・変更・SCN・テストの追跡: AC-1036-01〜03からMERGE-013/017〜021とRVX-001〜013へ追跡した。
- `no-spec-impact`の場合の限定的根拠: 非該当。仕様を更新した。
- UI・トークンの判断: 文書のみ。UI、layout、design tokenの変更なし。

## 11. 総合判定と再開地点

- 未解決Critical/High: なし
- Medium/Lowの記録: 旧PRの有効指摘2件を移植した。新H_implのLow指摘1件はREQ-SQ-015同段落の反証で不成立とした。
- 判定: approved
- 新しい権限が必要な事項: branch push・PR作成はownerの継続許可済み。mergeは行わない。
- 残存リスク: reviewer不在のno-review mergeは現行ASCで利用できない。これは選択した規範整合方針に沿う。
- 次に許可される操作: 本artifactだけをH_impl後にcommitし、audit確認後、後続工程で新H_implに対するStep 10 bindingを確立する。
- 次回の再開地点: 隔離branchの成果物監査後。push・PR作成・mergeは本作業では行わない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 4 |
| 対象SHA・文書ダイジェスト | aed8acf1342452578fd63642b6fb820881163496 |
| 比較基点 | `77d4ba021ede59ca290b8eb077d301f307c65467` |
| H_impl | `aed8acf1342452578fd63642b6fb820881163496` |
| 対象差分 | .agent-skill-chain/docs/02_品質基準.md、docs/specs/02_要件/04_仕様・品質管理要件.md、docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md、docs/specs/11_非機能/01_品質要件.md、docs/specs/15_要件追跡/00_追跡表.md、docs/specs/15_要件追跡/01_変更履歴.md |
| 対象外 | 比較基点に存在し変更されていないruntime。外部配布の新規認可機構はIssue #1036の選択した方針外 |
| 残り予算 | counted round 4で収束。残り2 |
| ラウンド数 | 1 |
| Step chain | 迂回: PR #1468を最新mainから再構成する委任作業。旧sessionのformal bindingは流用せず、後続で新HEADへ確立する |
| 仕様の所有箇所 | `.agent-skill-chain/docs/02_品質基準.md`、REQ-SQ-015、QLT-RVX-001。旧「mergeは記録があれば進む」がruntimeと矛盾した。 |
| 成果物行数 | 製品source 0行。文書7 path。PR後の訂正もCLI契約とreview artifactのみ。 |
| 縮小の先行評価 | #1317の別context reviewer経路が一人repositoryの停止を解消済み。新しいno-review merge認可を追加せず文書だけを訂正した。 |
| 実施者・日時 | implementer: Codex。本件独立reviewer: Claude Code CLI Opus。2026-09-23 |

`比較基点`と`H_impl`の値は40桁の小文字hexをbacktickで囲んだものだけにする。注記・branch名・短縮SHAを同じcellへ書かない。由来は別行へ書く。

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類、修正差分の取り直し | critical | Claude Code CLI | Opus、high effort、read-only | 未解決Highは停止 | Codexとは別provider process・context。`git status`は差分commit後clean |
