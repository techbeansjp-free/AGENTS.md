# 136 課題1191の運用設計整合レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1191 |
| ラウンド | Step 10 ラウンド1 |
| 比較基点 | `0c485f66cf5a4d74c07c8f19b9fd4e75ec82a598` |
| H_impl | `e5eab3a93c8eca3b1c761166c45589811763c210` |
| 比較基点の由来 | worktree作成時点の`origin/main`のtip |
| モード | full（Q-01とQ-05がfalse） |
| 対象差分 | 9 file、+197 -11。commitは`e5eab3a9` |
| 対象外 | `validateReleaseWorkflow`がcommand文字列の存在だけを見る問題（#1191のF-02）。変更履歴のPR列の方針（同F-03）。**いずれも本Issueの受け入れ条件に含めていない** |
| 残り予算 | **2**（同一範囲で最大3ラウンド、収束後にHEADが動いたときの取り直しを1回まで） |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260904_035239_運用設計の廃止済みbump経路を除き-release-job集合と権限境界表の一致を検査する |
| 仕様の所有箇所 | `docs/specs/02_要件/03_外部連携要件.md`のREQ-GH-003（**改訂**） |
| 成果物行数 | 製品 **+89行**（`release.ts` +62、`check_conformance.ts` +27）。仕様 **+13 -11行**。支援層 **+95行**（feature +15、steps +80）。**支援層/成果物 = 1.1倍** |
| 縮小の先行評価 | **既存機構の流用を先に評価した。** `canonical-contracts.json`の`ASC-CANON-SINGLE-SOURCE-001`は正本1箇所と複製の検出を行うが、`checkCanonicalDuplication`は`repositoryMarkdownPaths`が返すMarkdownだけを走査し、`canonical`もその集合内であることを検証する。**`.github/workflows/release.yml`をcanonicalに指定できないため使えない**（DISC-301）。判定を新設したが、**scriptは判定を持たずfileを読んで渡すだけにした** |
| 決裁 | PR #1190 への外部reviewer指摘。3件のうちF-01をMajorと判定した |
| 実施者・日時 | reviewer（claude）、2026-09-04 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節） | advanced | claude | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerとreviewerが**同一session**である。0.2節に逸脱として開示する。**本Issueの起点そのものが外部reviewerの指摘である** |

### 0.2 開示する逸脱

1. **implementerとreviewerが同一sessionである。** 緩和は、**本Issueの対象が外部reviewer（CodeRabbit）の検出であること**、判定の根拠を実コード読解とgrep実測に置いたこと、抽出精度そのものを変異対象にしたことである。
2. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 外部review | PR #1190 | Major「運用設計が廃止済みbump経路を現行手順として記載している」 | 外部の判断 |
| 矛盾の実測 | `docs/specs/12_運用保守/00_運用設計.md` | **64行が「bump branchは作らない」、83行の権限境界表が`bump_version` job。同一file内で矛盾** | 一次資料 |
| 影響範囲の実測 | 同上 | 25・60・70・74・83・92・93・157行の**8箇所**が旧経路のまま | 実行記録 |
| 配布境界 | `package.json`の`files` | `docs/`を列挙する。**運用設計は配布される** | 一次資料 |
| 流用可否 | `scripts/check_conformance.ts:84-116` | `checkCanonicalDuplication`は`repositoryMarkdownPaths`のMarkdownだけを走査し、`canonical`もその集合内で検証する | 一次資料 |
| 合成の登録漏れ | `SCN-INT-LEDGER-006` | **`LEDGER_COMPOSED_CHECKS`への登録漏れを機構が検出した** | テスト出力 |
| テスト | `npm run conformance:check` | `1469 scenarios (1453 passed, 16 skipped)` | テスト出力 |
| commit前candidate | 9 file | working tree clean | Git index |
| Phase A artifact | `docs/reviews/136_課題1191の運用設計整合レビュー.md` | `H_impl` = `e5eab3a9`。`H_impl..H_final`の差分pathは本file 1件 | Git観測 |
| commit後external | PR、CI run、review | Step 11で観測する | 外部のimmutable証拠 |

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/release.ts` | M | package | package | 純関数3つを足した。`validateReleaseWorkflow`と同じfileであり、release workflowの構造を読む責務がそこにある | pass。新しいimportを増やしていない。`architecture:check`合格 | REQ-GH-003 / AC-01〜03 / SCN-UNIT-RELWF-P05〜P07 | 診断を返すだけで外部へ到達しない。rollbackは3関数のrevert | pass |
| `scripts/check_conformance.ts` | M | project | project | fileを読んで`src/domain/release.ts`へ渡すだけにした。**判定を持たない** | pass。script → domainの一方向 | 同上 | 診断を`errors`へ積むだけである | pass |
| `docs/specs/12_運用保守/00_運用設計.md` | M | project | spec | 8箇所を是正した。権限境界表から`bump_version`行を除き、復旧手順をtag駆動の形へ替えた | pass | REQ-GH-003 | 記述だけで実行authorityを持たない | pass |
| `docs/specs/02_要件/03_外部連携要件.md` | M | project | spec | 権限境界表の一致要求をREQ-GH-003へ足した | pass | 同上 | 同上 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | 新規SCN 3件を結線した | pass | 同上 | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | project | spec | 変更履歴を1行足した | pass | 同上 | 同上 | pass |
| `test/features/unit/auto-release.feature` | M | package | package | scenario 3件を追加した。既存4件を変えていない | pass | AC-01〜03 | fixtureは文字列であり外部へ到達しない | pass |
| `test/steps/auto-release.steps.ts` | M | package | package | 対応するstep定義とfixtureを追加した | pass | 同上 | 同上 | pass |
| `test/steps/project-rule-ledger.steps.ts` | M | package | package | `LEDGER_COMPOSED_CHECKS`へ新しい検査名を登録した | pass | 同上 | **登録漏れは`SCN-INT-LEDGER-006`が検出した** | pass |

## 2. 受け入れ条件の確認

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-RELWF-P05 | `releaseJobDocumentationMismatch` | `7 scenarios (7 passed)` | pass | 廃止済みjobを載せた表が診断つきで拒否される。変異M1でkill |
| AC-02 | SCN-UNIT-RELWF-P06 | 同上 | 同上 | pass | jobを載せ落とした表が診断つきで拒否される。変異M2でkill |
| AC-03 | SCN-UNIT-RELWF-P07 | 同上 | 同上 | pass | 一致する表を受理する。**fixtureが抽出精度も検査する**（変異M3・M4でkill） |
| AC-04 | 該当なし | 文書の状態 | grep実測 | pass | `bump_version`・`bump PR`・`admin merge`・`bump-then-release` の一致が**0件** |

### 2.2 不変条件

| INV ID | 内容 | 判定 | 証拠 |
|---|---|---|---|
| INV-01 | 判定するのはjob名の集合一致だけである | pass | 権限列と開始条件列を読まない。`releaseJobDocumentationMismatch`は名前の集合差だけを返す |
| INV-02 | 判定は`src/domain/release.ts`が所有する | pass | `checkReleaseJobDocumentation`は`fs.readFileSync`と関数呼び出しだけである |
| INV-03 | 保護対象fileを変更しない | pass | 差分9 fileに`PROTECTED_FILES`は1件も含まれない |

## 3. 肯定的評価

- **既存機構の流用を先に評価し、使えない理由を実コードで確かめた。** 「似た機構があるから使える」と仮定していない。
- **判定とI/Oを分けた。** scriptは`fs.readFileSync`と関数呼び出しだけであり、判定はdomainにある。
- **両方向を検査する。** 片方向だけでは、権限境界表を空にすれば通る形が残る。
- **抽出精度そのものを変異対象にした。** DISC-302のとおりM3・M4が当初生存し、fixtureへ値を持たないnested keyと表外のjob名参照を足して塞いだ。
- **合成の登録漏れを機構が検出した。** `SCN-INT-LEDGER-006`が`LEDGER_COMPOSED_CHECKS`への未登録を拒否した。**私が忘れても機構が止めた。**

## 4. 敵対的評価

| 観点 | 攻撃 | 結果 |
|---|---|---|
| 検査の無効化 | 廃止job検出を外す | 変異M1でkill |
| 検査の無効化 | 未記載job検出を外す | 変異M2でkill |
| 抽出の緩和 | job名のindent限定を外す | 変異M3でkill（3 scenario失敗） |
| 抽出の緩和 | 表の1列目限定を外す | 変異M4でkill |
| 合成の切断 | `check_conformance.ts`の呼び出し1行を消す | `SCN-INT-LEDGER-006`が検出する |
| 逃げ道 | 権限境界表そのものを削除する | **検出する。** `defined \ documented`が全jobになる |
| 逃げ道 | 権限境界表の見出しを変える | **検出しない。** 表の形だけを見ており節見出しを見ない。5節ADV-01に記録する |

## 5. 指摘

| ID | severity | 事実 | 観測 | 由来 | 対処 | 判定 | 残余 |
|---|---|---|---|---|---|---|---|
| F-01 | Major | **運用設計が同一file内で矛盾していた。** 64行が「bump branchは作らない」と書きながら権限境界表が`bump_version` jobを載せていた | PR #1190 の外部review | 外部review | 8箇所を是正し、一致を機械検査へ加えた | valid / resolved | ADV-01 |
| DISC-301 | Low | **既存機構を流用できなかった。** `checkCanonicalDuplication`はMarkdown間の複製検出であり、YAMLをcanonicalに指定できない | 実コード読解 | 実測 | 新設した。**流用を先に評価した記録を要求定義へ残した** | valid / resolved | なし |
| DISC-302 | Medium | **変異M3・M4が当初生存した。** fixtureが抽出精度を検査していなかった | 変異試験 | 変異試験 | fixture YAMLへ値を持たないnested keyを、fixture Markdownへ表外のjob名参照を足した | valid / resolved | なし |
| DISC-303 | Low | **`LEDGER_COMPOSED_CHECKS`への登録を忘れた** | `SCN-INT-LEDGER-006`の失敗 | 機構 | 登録した。**機構が私の漏れを止めた** | valid / resolved | なし |
| ADV-01 | Medium | **権限境界表の見出しを変えると検出しない。** 表の形（1列目が`` `<job>` ``）だけを見ており、節見出しを見ていない | 敵対評価 | 敵対評価 | **修正しない。** 見出し文字列へ依存させると文書の言い換えで壊れる。表の形は権限境界表以外に現れておらず、現に他の表を誤検出していない | valid / record-only | 表を別の形へ書き替えると検査が空振りする |

**未解決のCritical / Highは0件である。**

## 6. ラウンド固有の確認

### ラウンド1

- 対象: 実装差分の9 file。
- 確認: 個別監査9行、AC-01〜04、INV-01〜03、肯定5観点、敵対7観点、変異4件。
- 結果: blocking 0件。record-only 1件（ADV-01）。resolved 4件（F-01、DISC-301〜303）。

## 7. テスト結果

**表を書く前に1本ずつ実行した。**

| 層・検査 | コマンド | 件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---|---:|---:|---:|---|
| unit・integration・e2e | `npm run conformance:check`（内部で`npm test`を実行する） | 1469 | 1453 | 0 | 16 | pass |
| 静的検査 | `lint`・`format:check`・`typecheck`・`source:check`・`architecture:check`・`skills:check`・`package:check`・`docs:format`・`test:format`・`trace:check` | 10 | 10 | 0 | 0 | pass |
| 監査 | `npm run audit:check` | 1 | 1 | 0 | 0 | pass |

### 7.1 変異試験

| ID | 変異 | 結果 |
|---|---|---|
| M1 | 廃止job検出を外す | kill |
| M2 | 未記載job検出を外す | kill |
| M3 | job名のindent限定を外す | kill（3 scenario失敗） |
| M4 | 表の1列目限定を外す | kill |

**4件すべてkill。** M3とM4は当初生存し、fixtureを強くして塞いだ。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/domain/release.ts` | **入る**（`files`が`dist/src/`を列挙する） | 判定関数3件が配布される |
| `scripts/check_conformance.ts` | **入らない**（`files`が`scripts/`を列挙しない） | 本repositoryのCIでのみ動く |
| `docs/specs/` 4 file | **入る**（`files`が`docs/`を列挙する） | **本Issueの主目的である。** 存在しない権限経路の記述が配布されなくなる |
| `test/` 3 file | 入らない | `files`が列挙しない |

判断: 配布物を更新した

根拠: `docs/`と`dist/src/`が配布境界に入る。**是正した記述そのものが配布物である。** `npm run package:check`はexit 0である。

## 9. 独立reviewの成立

**reviewerはimplementerと同一である**（`adachi-tatsuru`）。緩和は次の3点である。

1. **本Issueの対象は外部reviewer（CodeRabbit）がPR #1190 で検出したものである。** 私は実測して範囲を8箇所へ確定しただけである。
2. **変異試験が2件の不足を検出した**（DISC-302）。コード読解では見つけていない。
3. **`SCN-INT-LEDGER-006`が私の登録漏れを検出した**（DISC-303）。

## 10. 仕様整合性

- `REQ-GH-003`を改訂し、権限境界表の一致要求を明記した。
- 新規SCN 3件（`SCN-UNIT-RELWF-P05`〜`P07`）を追跡表へ結線した。`trace:check`のorphanは0件である。
- **既存SCNを1件も削除していない。**

## 11. 総合判定と再開地点

**判定: 合格。** 未解決のCritical / Highは0件である。

残余リスクはADV-01（権限境界表の見出しを変えると検査が空振りする）である。**表の形は権限境界表以外に現れておらず、現に他の表を誤検出していない。**

**#1191 のF-02（`validateReleaseWorkflow`がcommand文字列の存在だけを見る）とF-03（変更履歴のPR列の方針）は本PRの対象外である。** いずれも本Issueの受け入れ条件に含めておらず、#1191 へ残す。
