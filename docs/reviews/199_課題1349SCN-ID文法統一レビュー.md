# 04 レビュー

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | Issue #1349 の実装・test・仕様 |
| ラウンド | 1〜3（3は既定branch追随に対する取り直し） |
| 対象SHA・文書ダイジェスト | c8c10f5c0dd2c45d396deb064f0ce1cebf7950b8 |
| 比較基点 | `88ec4cd704464357549083944b569c48c2f35c99` |
| 比較基点の由来 | PR #1351（#1334）を取り込んだ`origin/main`のtip。追随mergeはreview artifact commitより前に置き、個別監査表を`比較基点..H_impl`から再生成した |
| H_impl | `c8c10f5c0dd2c45d396deb064f0ce1cebf7950b8` |
| 対象差分 | 14 path。生成済みdist 3 pathは配布物影響で監査する |
| 対象外 | 比較基点に存在し変更されていない範囲。`trace.ts`・`conformance.ts`の同型literal（DISC-1349-01でownerの指示により対象外） |
| 残り予算 | 0ラウンド（同一範囲3ラウンドのうち2を使用、取り直し1を使用） |
| ラウンド数 | 3 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260912_010159_SCN-IDの受理集合をissue-validateとpr-createで一致させる |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md` REQ-WF-006「全Scenarioへ一意なSCN IDとGiven・When・Thenを要求する」、`docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`の`issue validate`行、`.agent-skill-chain/schemas/project-conformance-binding.schema.json`の`^SCN-[A-Z0-9-]+$` |
| 成果物行数 | 製品 `src/` +63 / −5（新規module 23行を含む）、生成dist +53 / −5、支援層(test) +296、仕様 +13 / −5 |
| 縮小の先行評価 | 報告が名指しした`issue.ts`・`delivery.ts`の2 fileに限り、当初含めていた`trace.ts`・`conformance.ts`の置換とAC-05を外した（DISC-1349-01）。文法moduleは定数1つと述語1つだけで、新しいgate・validator・CLI・schema・台帳・project choice項目を追加していない |
| 実施者・日時 | reviewer: codex exec（read-only、implementerと別context）、進行役が転記、2026-09-12T03:10:00+09:00 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類 | standard | Codex high以下 | provider既定（`gpt-6-astra`/high）。trusted tierMappingの`codex:provider_recommended_default:high:default: critical` | Critical/High未解決なら停止 | H_impl固定後のread-only review、対象差分の変更なし（`git status` clean） |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | Issue #1349、staging `01_要件定義.md` | FR-01〜03、AC-01〜04、INV-01〜03 | 要件文書 |
| 差分 | `88ec4cd704464357549083944b569c48c2f35c99..c8c10f5c0dd2c45d396deb064f0ce1cebf7950b8` | 14 path、実装2 commit＋追随merge 1 commit | Git観測 |
| テスト | `npm test`ほか | 1856 scenarios（1840 passed、16 skipped、0 failed）、適合性87/87 | テスト出力 |
| 仕様 | `docs/specs/` | REQ・AC・SCN・CLI契約・用語・変更履歴を更新 | 既存文書 |
| commit前candidate | 上記14 path | H_impl `c8c10f5c0dd2c45d396deb064f0ce1cebf7950b8` | Git観測 |
| Phase A artifact | 本file | artifact-only commit後にGit blobとして観測する | Git観測 |
| review session | staging `review-session.json` | round 1 `01dbc412277325c8664565418c62d5eda6a6d453d3cc88f610f45631b92e2d6f`、round 2 `0b06cd8b77ec0435fc7aa6fd6d36e675c823dd08c45c6131a23f3d61a14f58b1`、round 3（取り直し）`ca42a2b1cd155d8f76d6ecf0f24a27c73c7544660b5628ce384cd2ae03c5e509`、status converged | Git観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: 文法module → issue.ts / delivery.ts の1段一方向。本fileは自身のSHAを書かない。pass
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: 本fileのcommit後にauditで検証する
- reviewerの独立性が`context-isolated`の要求水準を満たす: implementer（本session）と別processのcodex exec read-onlyがexact HEADで肯定・敵対評価とfindingを返し、対象差分を変更していない
- Phase BのPR・CI・review一致: PR #1353作成後にtrusted providerから観測する
- 既定branch追随を行った場合、取り込みがreview artifact commitより前にあり、`比較基点`が取り込んだ既定branch tip、`H_impl`がartifact直前の最新commitを指し、個別監査表を`比較基点..H_impl`から再生成した: はい。旧artifact（比較基点`b22f1ed5`）は外し、本fileを新たに作成した

### 1.1 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `docs/specs/01_システム概要/02_用語・略語.md` | M | spec | glossary | TERM-ASC-105を追加（#1334の104と衝突したため採番し直し） | 一方向追跡 | FR-03 | revert可能 | pass |
| `docs/specs/02_要件/00_要件一覧.md` | M | spec | requirements | REQ-WF-006の根拠Issue | 一方向追跡 | REQ-WF-006 | revert可能 | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | spec | requirements | 文法の単一正本と拒否条件 | 一方向追跡 | REQ-WF-006 | revert可能 | pass |
| `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | M | spec | interface | `issue validate`の拒否条件 | domainを参照 | FR-01 | revert可能 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | spec | trace | SCN-UNIT-SCNID行 | 一方向追跡 | AC-WF-006 | revert可能 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | spec | history | 変更記録と互換影響（TERM-ASC-105） | 追加依存なし | REQ-WF-006 | revert可能 | pass |
| `src/domain/delivery.ts` | M | package | domain | 述語参照とexport | scenario-idへ依存 | AC-03、INV-01 | 診断文不変、revert可能 | pass |
| `src/domain/issue.ts` | M | package | domain | 末尾境界と文法外ID診断（#1334の短縮形式検証と共存） | scenario-idへ依存 | AC-01、02、04、INV-03 | fileを読む範囲不変、revert可能 | pass |
| `src/domain/scenario-id.ts` | A | package | domain | 文法の単一正本（定数と述語） | 依存なし | FR-02、INV-01〜02 | SCN-UNIT-SCNID-003で述語の入出力を確認、revert可能 | pass |
| `test/features/unit/scenario-id-grammar.feature` | A | test | feature | 受入scenario4件 | productionへ非依存 | SCN-UNIT-SCNID-001〜004 | 一時directoryのみ | pass |
| `test/steps/scenario-id-grammar.steps.ts` | A | test | steps | scenario実装 | domainを観測 | AC-01〜04 | 一時directoryのみ | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: 生成済みdist 3 pathを除く11 pathが一致し、dist 3 pathは配布物影響で確認した。pass
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: pass
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: REV-01の修正は`issue.ts`とそのtestだけで、round 2で再監査した。round 3は追随mergeのnet deltaがround 2承認時と同一（reviewerが正規化payloadのsha256一致を実測）であることを確認した

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-1349-01 | ownerが「問題以上のことをしない」と指示。当初は`trace.ts`・`conformance.ts`まで置換しAC-05を持っていた | scope・ACの縮小 | requirement、design-responsibility | 2 fileを戻しAC-05を削除、00〜03を再確定しStep 8を再同期 | `workflow assess-discovery`=rebaseline-affected-contracts、issue validate valid | updated | pass |
| DISC-1349-02 | 再確定後のStep 7再記録が`outOfOrder=8`で拒否された（#1342該当） | 契約変更なし | なし | Step 8を再記録して再同期。新規起票しない | Step 8 entry 2件 | no-spec-impact | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-SCNID-001 | `src/domain/issue.ts` `malformedScenarioIds` | 合格（修正前は失敗を確認） | pass | `npm test`で合格 |
| AC-02 | SCN-UNIT-SCNID-002 | `src/domain/issue.ts` `scenarioIdPattern` | 合格 | pass | `npm test`で合格 |
| AC-03 | SCN-UNIT-SCNID-003 | `src/domain/scenario-id.ts`、`delivery.ts` | 合格（round 2で混在stagingを追加） | pass | `npm test`、reviewer実測 |
| AC-04 | SCN-UNIT-SCNID-004 | `src/domain/issue.ts` | 合格 | pass | `npm test`で合格 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | policy、credential、GitHub証拠を扱うCLIでありPrivacy/Security by Designを常に適用する | 診断へ転記するのはID字面だけ。fileを新たに読まない。SCN-UNIT-SCNID-001 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 診断、migration journal、metrics、CI/review証拠を運用判断へ使用する | 文法外IDの診断にIDと文法を含める。新規log・保持・rotationなし |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 現行製品はNode CLIで画面契約とa11y検証の対象を持たない | `package.json`のbinとCLI contract |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | 現行製品は画面レイアウトと視覚コンポーネントを所有しない | projectKind=cli、UI sourceなし |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | round 1で反例（小文字接頭辞の混在）をfinding、round 2で解消を実測、round 3でnet delta不変を実測 |
| 価値 | 利用者・運用上の目的を満たすか | pass | 入口と終端の文法を共有し、Step 1で文法違反を診断できる |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 新規依存なし、既存Node.js・TypeScript構成 |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 6仕様fileが文法・互換影響・追跡を一貫して記載。用語IDはTERM-ASC-105で台帳・変更履歴が一致 |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 文法本体を`scenario-id.ts`へ分離。`validateDeliveryEvidence`のexportは`private: true`のpackageで公開契約の拡大ではない。REV-02（Low）のsourceコメントの採番参照は記録に留める |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | round 1 REV-01（`SCN-GOOD`と`scn-69-001`の同居）をround 2で解消。混在集合5件を各1件名指し |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 診断は他検査を中断せずerrorsへ追加。delivery側は副作用前に例外 |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | 空本体、最短本体、下線、小文字枝番、全角、行末空白、CRLF、Outline、ja方言を確認 |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | 固定文字列の正規表現。散文中の言及は既存どおり数えない（SCN-UNIT-ISSUEGHK-008） |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | 認証・authority処理を変更しない |
| データ損失 | 上書き、削除、部分公開、履歴消失 | not-applicable | 読み取り専用の検証条件変更 |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | commit単位でrevert可能、data移行なし |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | source・dist・6仕様fileが同期。対象外literalに触れていない。追随mergeで#1334の変更と共存（typecheck・lint・source→dist照合をreviewerが実測） |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| REV-01 | High | 候補regexが大文字`SCN-`だけを捕捉するため、`scn-69-001`が正規ID行と同居すると入口だけ合格する（AC-03違反） | round 1、`src/domain/issue.ts` | `issue validate` | 捕捉を`[Ss][Cc][Nn]-`へ変更し、混在stagingの検査をSCN-UNIT-SCNID-003へ追加（commit `adf490c2`） | resolved | なし |
| REV-02 | Low | `src/domain/scenario-id.ts`のコメントが採番変更前のTERM-ASC-104を参照している | round 3、`src/domain/scenario-id.ts:2` | source comment | 記録に留める。取り直し予算を使い切ったためHEADを動かさない。runtime判定に影響しない | valid | コメントの参照先が台帳と1つずれる。次に同fileへ触る変更で直す |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: はい
- 指摘を確定した: REV-01（High）
- 次ラウンド対象のCritical/High: REV-01

### ラウンド2

- 未解決Critical/High: なし（REV-01 resolved）
- 修正差分: `src/domain/issue.ts`、`dist/src/domain/issue.js`、`test/steps/scenario-id-grammar.steps.ts`
- 修正で触れた隣接範囲: `scenarioIdPattern`と`isScenarioId`は不変であることを確認
- 既承認・未変更範囲を再走査していない: はい

### ラウンド3

- 全指摘の最終分類: REV-01 resolved、REV-02 valid（Low、記録のみ）
- 任意の危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: 追随mergeのnet deltaがround 2承認時と同一であることを正規化payloadのsha256一致で確認
- 同じ範囲の予算を自動更新していない: はい（取り直し1ラウンドを使用し、以後のHEAD移動を行わない）
- AIによる最終裁定: approved

## 7. テスト結果

- 実行したcommandの一覧: `npm test`（H_impl `c8c10f5c`で再実行）、`npm run conformance:check`、`npm run typecheck`、`npm run lint`、`npx prettier --check src test`、`npm run docs:format`、`npm run test:format`、`npm run trace:check`、`npm run cli:check`、`npm run workflow:check`、`npm run source:check`、`npm run package:check`、変異試験A〜F
- 全layerの合計: 1856 scenarios（1840 passed、16 skipped、0 failed）。skipは既存の環境依存scenarioで本変更と無関係
- 変異試験（H_impl `adf490c2`時点、追随mergeで対象codeは不変）: A（`$`アンカー除去）3 failed、B（文字集合拡張）3 failed、C（境界除去）1 failed、D（診断呼び出し除去）3 failed、E（述語空洞化）3 failed、F（接頭辞捕捉を大文字だけへ戻す）1 failed。全kill
- runner・Gherkin方言: cucumber-js、`gherkinDialect: en`

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `dist/src/domain/scenario-id.js` | 入る | SCN ID文法moduleを追加 |
| `dist/src/domain/issue.js` | 入る | `issue validate`が文法外SCN IDを名指しして拒否する |
| `dist/src/domain/delivery.js` | 入る | 判定不変（述語参照へ置換） |
| `src/domain/scenario-id.ts` | 入る | 配布buildの元source |
| `src/domain/issue.ts` | 入る | 同上 |
| `src/domain/delivery.ts` | 入る | 同上 |

判断: 配布物を更新した

根拠: domain sourceと対応する生成済みdistを同じ実装commitで更新した。利用者に見える変化は`issue validate`が小文字枝番などの文法外SCN IDを拒否することで、変更履歴へ互換影響として記録した。

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated（`merge.reviewIndependence`未宣言のため既定） |
| その要求を満たすこと | はい |
| reviewerとimplementerのidentity・context比較 | implementerはClaude Code session（本staging進行役）、reviewerはcodex exec read-only sandboxの別process・別context。exact HEAD `84d2fca3`（round 1）・`adf490c2`（round 2）・`c8c10f5c`（round 3）で実行 |
| reviewerが対象差分を変更していないこと | はい（read-only sandbox、review後の`git status` clean） |

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/02_要件/01_ワークフロー要件.md`（REQ-WF-006）、`02_要件/00_要件一覧.md`、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md`、`01_システム概要/02_用語・略語.md`（TERM-ASC-105）
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: 00候補 → 01確定 → 台帳active。staging上の候補IDはTERM-ASC-104だったが、#1334の採番と衝突したため台帳ではTERM-ASC-105で確定した（内容は同一）。pass
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: pass（診断の主語「GherkinシナリオID」は既存文言との連続性のため維持し、02 §4に記録）
- 要件・変更・SCN・テストの追跡: REQ-WF-006 → AC-WF-006 → SCN-UNIT-SCNID-001〜004 → `test/features/unit/scenario-id-grammar.feature` → `src/domain/scenario-id.ts`、`issue.ts`、`delivery.ts`。`trace:check`合格
- `no-spec-impact`の場合の限定的根拠: 該当しない
- UI・トークンの判断: UIなし、token非該当

## 11. 総合判定と再開地点

- 未解決Critical/High: なし
- Medium/Lowの記録: REV-02（Low、sourceコメントの採番参照）
- 判定: approved
- 新しい権限が必要な事項: なし
- 残存リスク: 利用projectの既存stagingに文法外IDがあれば次回`issue validate`で拒否される（診断がIDと文法を名指しする）
- 次に許可される操作: `workflow record --step=10 --post-terminal-intake`、PR #1353のhead更新
- 次回の再開地点: staging journal Step 10（post-terminal intake）
