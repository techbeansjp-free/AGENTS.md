# 115 課題1141 review成果物を版管理下へ置く手順の明記 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。**実利用者からの報告に基づく。** 既定構成では`review evidence`が必ず失敗していた。

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1141 |
| 比較基点 | `3968a1c79e06c521176ca777e9904a0131e5cacc` |
| H_impl | `3d16d649e03722c29833402fb373f004abf816e6` |
| 対象差分 | 配布文書3件、CLI usage、変更履歴の5 file |
| 対象外 | `review evidence`のgit tree要求、stagingの出力先、publication境界の新設、`roleContracts`の`allowedPaths` |
| 残り予算 | Step 10の上限3ラウンドのうち2を残す |
| ラウンド | Step 10 ラウンド1 |
| ラウンド数 | 1（Step 10のラウンド1）。**Step 7のreadiness checkは3ラウンド予算に数えない** |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260903_050229_review成果物を版管理下へ置く手順を配布物へ明記する |
| モード | full（Q-01が偽。配布される公開契約へ手順の要求を追加する） |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-005。**本文を変えず変更履歴だけを更新する** |
| 成果物行数 | 製品: 配布3文書とCLI usageで19 insertions・4 deletions。仕様: 1行。支援層: staging 00〜03が29588文字 |
| 縮小の先行評価 | 実施済み。**新規Scenarioを足さない。** 特定文言の存在をassertするtestは文言を変えるたび保守対象になり、検出できるのは自分が今書いた文の消失だけである。形式契約は既存`SCN-UNIT-PACKAGE-013`が検査する |
| authority | 変更しない。**検査codeへの変更0行** |
| 実施者・日時 | reviewer、2026-09-03（JST） |

### 0.1 routing入力契約

| role欄 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類 | standard（riskがlow） | claude | Opus 5、effort high | 未解決Critical/Highがあれば停止し、是正後に同sessionの次ラウンドで再review | implementerとreviewerはいずれも本sessionのAI agentであり、**Git commit authorも同一である。§9に実態どおり記録し、独立性が成立したとは主張しない** |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 3箇所が版管理下を要求すること | `src/cli.ts:4885-4892`、`scripts/check_file_audit.ts:26,748`、`.agent-skill-chain/schemas/delivery-state.schema.json:290-292` | `review evidence`は`git rev-parse <final>:<path>`で読む。`audit:check`は`AUDIT_DIRECTORY = "docs/reviews"`配下でなければ拒否する。schemaは`^(?:docs/reviews/|\.agent-skill-chain/reviews/).+`を要求する | 実測 |
| 配布物に記述が無いこと | `grep -rln "docs/reviews" .agent-skill-chain/skills/ templates/ docs/` | **0件** | 実測 |
| `roleContracts`が配布対象外であること | `package.json`の`files` | `.agent-skill-chain/project/`を含まない。**reviewer.allowedPathsとの食い違いは#1047が所有する** | 実測 |
| 検査codeの非変更 | `git diff --name-only` | `src/cli.ts`・`scripts/`・`schemas/`への変更0件。変更は配布文書3件とCLI usageと変更履歴 | 実測 |
| 指示とschemaの一致 | 追記した本文とschemaのパターン | `docs/reviews/`と`.agent-skill-chain/reviews/`の両方を示す。schemaが許す集合と一致する | 実測 |
| helpの更新 | `review evidence --help` | `版管理下のreview成果物。final commitのgit treeから読むため一時ステージング内のpathは受理しない` | 実行観測 |
| 差分の限定 | `git diff --name-only` | 5 fileのみ | 実測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: pass。`検査の要求 → 手順の記述`の一方向。artifact本文へ自身のcommit SHAを書いていない
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_impl`が直前commitであり、`H_impl..HEAD`の差分は本artifact 1 fileのみ
- reviewer stable IDがPR author/観測済み`H_impl` author stable IDと異なる: **異ならない。** §9に実態を記録した
- 既定branch追随: **実施済み。** **本artifactを作る前に`rebase --onto origin/main`で追随した。** 差分は追加のみで、他Issueの履歴行に触れていない

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | M | package | package | 手順の正本。`H_final`を述べる段落の直前へ置き、なぜ版管理下かを述べる | 規範文書は実装を参照しない。循環なし | AC-1141-01 | 追記の削除で復旧する | pass |
| `.agent-skill-chain/skills/step-10-review/SKILL.md` | M | package | package | 実行時に読む位置での再掲。正本と同じ内容を短く述べる | skillは正本へ一方向。循環なし | AC-1141-02。SCN-UNIT-PACKAGE-013 | 同上 | pass |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | M | package | package | 成果物を書く位置での再掲 | templateは正本へ一方向。循環なし | AC-1141-02。SCN-UNIT-PACKAGE-013 | 同上 | pass |
| `src/cli-usage.ts` | M | package | package | `--artifact`のdescriptionへ要求を書く。**flag構成を変えない** | usage定義のみ。循環なし | AC-1141-03 | 1行の差し替えで復旧する | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | 非変更範囲を実際に維持した契約へ限定して記録する | 履歴のみ。循環なし | 該当なし | 1行の削除で復旧する | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: pass（`git diff --name-status`が`M`5件）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: pass
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: 修正は発生していない

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-1141-ROLE-001 | `reviewer.allowedPaths`が`[.agent-skill-chain/role-log/, .agent-skill-chain/tmp/]`で`docs/reviews/`を含まない。**追記した指示と宣言が食い違う** | 本Issueのscopeへ及ばない | なし | **本Issueでは是正しない。** `package.json`の`files`に`.agent-skill-chain/project/`が無く`roleContracts`は配布対象外である。宣言と実態の食い違いは#1047が所有する | `package.json`の`files`の実測 | no-spec-impact | pass |

**報告の修正案3件のうち2件を実測で棄却した。** 案1（git tree存在をdigest照合へ切替）と案2（staging内なら分岐）は、staging digestがproviderからattestされないため、reviewerが確認したartifactとmerge時のartifactを外部証拠で結べなくなる。`H_final`の独立承認境界を壊す。**採ったのは案3である。**

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1141-01 | 該当なし | 規範文書への追記 | `docs/reviews/`と`.agent-skill-chain/reviews/`の両方を示す | pass | §1 |
| AC-1141-02 | SCN-UNIT-PACKAGE-013 | skillとtemplateへの追記 | `skills:check`が合格 | pass | §7 |
| AC-1141-03 | 該当なし | CLI helpへの追記 | helpが版管理下を要求する旨を返す | pass | §1 |
| AC-1141-04 | SCN-UNIT-PACKAGE-013 | 全変更 | 9種の高速ゲートと`npm test`・`conformance:check`が合格。**検査codeへの変更0行** | pass | §7 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | `development.json`の`capabilities.privacySecurity`が`applicable`であり、承認証拠の所在に関する手順である | 検査codeへの変更0行。`H_final`の独立承認境界を壊す案を実測で棄却した |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 外部review証跡を記録する経路そのものである | 指示が示すpathをschemaのパターンと突合した |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 人が触れるUIを持たない文書とCLIである | `projectKind`が`cli` |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UI componentもthemeも持たない | `capabilities.designTokens`が`not-applicable` |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 指示が示すpathがschemaのパターンと一致する |
| 価値 | 利用者・運用上の目的を満たすか | pass | 既定構成で`review evidence`と履歴監査が通る経路が読み取れる |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 記述の追加だけで、検査codeへ触れない |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 正本を規範文書に置き、他3箇所が同じ内容を短く述べる |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 既存語`H_final`をそのまま使い、新規用語を導入しない |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | schemaが許す2 pathの両方を示す。片方だけを示すと他方を選んだ利用者が詰まる |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 記述の追加であり部分適用が残らない |
| 境界値 | 空、最大、最小、重複、Unicode等 | 対象外 | 記述の追加であり値を解釈しない |
| 悪用 | 指示を口実に検査を緩める | pass | **検査codeへの変更0行を`git diff --name-only`で確認した。** 3箇所の要求はそのまま働く |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | **digest照合案を棄却した理由が安全性そのものである。** staging digestはproviderからattestされず、同一actorの自己申告になる |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | 既存の記述を消していない。既存のcommit済み成果物は影響を受けない |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | repository状態はrevertで戻る。配布物であるため、merge後に自動releaseが成立するとtagとGitHub Releaseはrevertで取り消せない（§8） |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | 配布3文書・CLI help・変更履歴。**`roleContracts`との食い違いは配布対象外であることを実測し#1047へ帰属させた** |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| M-01 | Medium | **指示は機械強制されない。** 利用者が読まなければ従わない | 実測。`skills:check`は文言の存在を検査しない | 遵守 | **本PRでは機構を足さない。** publication境界（staging draftを専用操作で版管理pathへ公開しdigestを結ぶ）はrole authorityの拡張を伴い範囲を超える。**#1141のコメントへ設計を記録した** | valid（out-of-scope。record-only） | **残存する。** owner決裁 |
| M-02 | Medium | `reviewer.allowedPaths`に`docs/reviews/`が無い | `development.json` | 宣言と実態 | **本Issueでは是正しない。** `roleContracts`は配布対象外であり#1047が所有する | valid（out-of-scope。record-only） | 残存する |

**Critical/High 0件。**

## 6. ラウンド固有の確認

### ラウンド1（Step 10、2026-09-03、candidate HEAD = `H_impl`）

- 全評価基準を確認した: はい。肯定5観点・敵対8観点をすべて評価した
- 指摘を確定した: はい。M-01とM-02はいずれも記録のみ
- 次ラウンド対象のCritical/High: **なし**
- blocking: 0件

### ラウンド2

- 本artifactのcommitでHEADが動くため、次ラウンドで再固定する

### ラウンド3

- 未実施。**外部reviewの指摘があれば本ラウンドで扱う**

## 7. テスト結果

**承認証拠は成功CI runである。** 手書きの件数表はそれ自体が自己申告であり、承認証拠にならない。

実行したcommand: `docs:format`、`test:format`、`lint`、`format:check`、`typecheck`、`source:check`、`trace:check`、`architecture:check`、`skills:check`、`build`、`package:check`、`npm test`、`conformance:check`、`audit:check`、`issue validate`、`review evidence --help`

全layerの合計: **1413 scenario、1397 成功、0 失敗、16 skip。** `conformance:check`はproject rule 20件・orphan 0件で合格。

失敗またはskipがある層: **skipした16 scenarioはsemantic graphのGraphQLite assetが未設定な環境に起因する。** 本差分はこの経路に触れない。

対応する成功CI run: PR作成後に取得し、merge前に現`H_final`へ束縛する。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | **入る** | 手順の正本が加わる |
| `.agent-skill-chain/skills/step-10-review/SKILL.md` | **入る** | 実行時に読む位置での再掲が加わる |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | **入る** | 成果物を書く位置での再掲が加わる |
| `src/cli-usage.ts` | **入る**（`dist/src/`として） | helpのdescriptionが変わる |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | なし |

判断: 配布物を更新した

根拠: `package.json`の`files`は`.agent-skill-chain/docs/`・`skills/`・`templates/`・`dist/src/`を含む。**配布digestが変わるため、mergeで自動releaseの計画が成立する見込みである。**

**releaseの起動と計画は分けて記録する。** `.github/workflows/release.yml`は`main`への`push`で起動する。その後`planAutoRelease`が配布digestの差分を見て計画を決める。**本変更は配布digestを変えるため、version bumpとtagが作られる見込みである。** merge後に実観測する。**npm公開は自動経路では発火しない。**

**ロールバックの対象範囲を分けて記録する。**

| 対象 | revertで戻るか | 実行者 | 手順 | 完了確認 |
|---|---|---|---|---|
| repository状態 | **戻る** | 実行役 | 打ち消しPRを通常mergeする | `git diff`が元の状態と一致する |
| 公開済みtagとGitHub Release | **戻らない** | repository owner | 削除せず次versionで前進する | `gh release list`に新tagが載る |
| npm registryの公開物 | **公開されない** | 該当しない | 本経路では公開しない | `npm view`に該当versionが無い |

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | **本artifact作成時点では無い。** exact-headは`H_final`。immutable GitHub reviewは0件 |
| reviewerがPR author・実装commit authorと異なる | **異ならない。** implementerもreviewerも本sessionの同一AI agentであり、Git commit authorも同一である |
| 観測したreview commentとapprovalの件数 | 本session内のreview 1ラウンド（Critical/High 0、Medium 2）。GitHub review 0件 |

**`02_品質基準.md`の「独立reviewが成立しない場合」に従う。** 同節は「既定は停止ではなく、無記録での通過の禁止である」と定める。

**PR作成後の外部reviewを待つ。** CodeRabbitがfree limitで出力できない場合は、その事実をPRへ記録したうえで判断する。

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/15_要件追跡/01_変更履歴.md`。**REQ-WF-005の本文は変えていない**
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: pass。新規用語を導入していない
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: pass
- 要件・変更・SCN・テストの追跡: REQ-WF-005 → AC-WF-005 → 既存`SCN-UNIT-PACKAGE-013`。**新規SCNを追加しないため追跡表を変更しない**
- UI・トークンの判断: UIを所有しないためdesign token・layout tokenは対象外。ADRを追加しない

## 11. 総合判定と再開地点

- 未解決Critical/High: **なし**
- Medium/Lowの記録: M-01（指示は機械強制されない）、M-02（role契約との食い違いは#1047）
- 判定: approved（Step 10 ラウンド1）
- 新しい権限が必要な事項: mergeは別authority
- 残存リスク:
  1. **指示は機械強制されない。** publication境界の新設はowner決裁
  2. **`reviewer.allowedPaths`との食い違いが残る（#1047）**
  3. **配布digestが変わるため自動releaseの計画が成立する見込みである。** 公開済みtagはrevertで取り消せない
- 次に許可される操作: push、PR作成、外部reviewの到着確認、必須check 2件の全緑確認、およびownerが承認したauthorityによる**通常merge commit方式**でのmerge。**squash mergeを使わない**
- 次回の再開地点: PR作成から
