# 90 課題1054 非スラッシュ終端の許可pathを完全一致にする 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。**#1054を2本へ分割したうちのPR-1であり、製品の照合欠陥の是正だけを含む。** project choiceの宣言変更（`development.json`）は同じ差分に含めず、PR-2が持つ。**`valid: true`は候補が既定branchを基準に検証を通過したことを示すだけであり、既定branchへの反映を示さない。**

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象Issue | #1054のPR-1（#1051のblocker） |
| 比較基点 | `ca08ecc2d8f08d98566bcf6b930877129c770360` |
| H_impl | `b3f6661848e978aadc63051687116e66018ea977` |
| reviewer | codex `gpt-5.6-sol`（effort high、implementerと別context）。Step 7で1ラウンド、Step 10で1ラウンド、いずれも`approved` |
| 実施日 | 2026-08-29（JST） |
| ラウンド数 | 2（Step 7が1、Step 10が1。同一範囲の上限は3で、それぞれ2を残している） |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260828_195937_implementerのallowedPathsにscriptsとgithubを追加し宣言を実態へ合わせる |
| モード | full（Q-03が偽。role契約の照合規則に触れるsecurity-boundary変更） |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md:67-73`のREQ-WF-007「6 roleの許可path・操作・禁止操作・必要証拠を検証し」。**個々のpath値は列挙していない**（`grep -rn 'allowedPaths' docs/specs/`は0件） |
| 成果物行数 | 製品: `src/domain/role.ts` +8/-4行、追跡表1行。支援層: feature +5行、step定義 +58行 |
| 縮小の先行評価 | 実施済み。既存SCN-UNIT-ROLE-004は終端条件を観測しないため代替にならない。新しい機構は足さず#1047へ帰属させた。**設定値を固定するtestは作らず、照合契約を固定するtestを1件だけ足した** |
| 分割の理由 | **`pr create`がproject choiceの許可拡大を`ASC-TRUST-001`で拒否したため**（issuecomment-5459508216）。本PRは拡大を含まないため製品CLIの正規経路で作成できる（実測） |

## 1. 入力証拠

| 観測 | 出所 | 内容 | 種別 |
|---|---|---|---|
| 是正前の欠陥 | `validateRoleOperation`へfile単位の要素を注入 | `<file>/child`と`<file>/a/b.ts`が**許可される** | 実測 |
| 両方向比較 | 是正前後の実装を同じ21入力へ並べた | **変化は3件だけ**（配下pathがALLOW→DENY）。**残る18件は不変** | 実測 |
| 失敗テストの先行観測 | `npm run test:unit -- --name "SCN-UNIT-ROLE-005"`（是正前） | `1 scenario (1 failed)`。`…/child`と`…/nested/deep.ts`が`allowed: true` | 実測 |
| 変異試験 | 非`/`終端の早期returnを取り除きdirectory照合へ戻す | `1 scenario (1 failed)`。**複写で復元**後は`1 scenario (1 passed)` | 実測 |
| 合成経路 | `grep -rn 'validateRoleOperation' src/ scripts/` | **製品の呼び出し元は存在しない。** `routing roles`は`validateRoleAssignment`だけを呼ぶ | 実測 |
| 孤立SCN検査 | `scripts/check_trace.ts:548-563` | 要件から到達できないSCNをerrorにする | 静的読解 |
| 配布面 | `package.json` | `main`も`exports`も未宣言。入口は`bin`だけ。`files`に`dist/src/`を含む | 一次資料 |

## 変更ファイル個別監査

| ファイル | 種別 | 所有 | 層 | 責務 | 依存方向 | 対応SCN | 復旧 | 判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/role.ts` | M | package owner | package | 非`/`終端要素を完全一致だけで照合する | `validateRoleOperation`から単方向 | SCN-UNIT-ROLE-005 | 分岐を戻す | pass |
| `test/features/unit/role-tier.feature` | M | package owner | package | 照合契約のScenarioを1件足す | 実装へ単方向 | SCN-UNIT-ROLE-005 | Scenarioを戻す | pass |
| `test/steps/role-tier.steps.ts` | M | package owner | package | 契約を引数で注入し候補pathごとに判定する | 実装へ単方向 | SCN-UNIT-ROLE-005 | step定義を戻す | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package owner | spec | REQ-WF-007 unit行へSCN-UNIT-ROLE-005と実装先`src/domain/role.ts`を追加 | 追跡のみ | — | 行を戻す | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: pass（`git diff --name-only ca08ecc2 b3f6661848e978aadc63051687116e66018ea977`が4件）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: pass。**repository固有のfile pathは本PRに含まれない**
- 計画の対象外（強制点の実装、他roleのcontract、他field、`weakened`分類規則、#1051の是正内容）に触れていない: pass

## 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/domain/role.ts` | 入る（`files`に`dist/src/`） | 宣言された入口は`bin`だけであり、CLIは`validateRoleOperation`を通らない。**CLIの振る舞いは変わらない** |
| `docs/specs/15_要件追跡/00_追跡表.md`、`test/features/unit/role-tier.feature`、`test/steps/role-tier.steps.ts` | 入らない | なし |

判断: 配布物を更新しない
根拠: 配布文書（`AGENTS.md`、`.agent-skill-chain/docs/`、`.agent-skill-chain/schemas/`）は`allowedPaths`の照合規則を記述しておらず、`project-choice.schema.json`も型と一意性しか定めていない。追随すべき記述が存在せず、CLIの振る舞いも変わらない

## 3. ラウンド別の指摘と是正

| Step | ラウンド | 判定 | finding | 是正 |
|---|---:|---|---|---|
| 7（設計・計画） | 1 | approved | DR1-M-01（01の版が成果物間で不一致）、DR1-M-02（symlink記述が02と03で矛盾）。**Critical/High 0件** | 02 §0の版を訂正しdigestはjournalを正本とした。03 §5.2のsymlink記述を訂正した。いずれもresolved |
| 10（実装） | 1 | approved | Low-01（追跡表の実装先列に`src/domain/role.ts`が無い）。**Critical/High/Medium 0件** | 実装先へ`src/domain/role.ts`を併記し、`trace:check`と`docs:format`で再確認した |

**レビューは分割前の5 file差分に対して実施した。** 本PRはその真部分集合であり、`development.json`の宣言変更を含まない。reviewerが`development.json`について与えた判定（AC-01〜AC-03の適合）は本PRの対象外であり、PR-2が持つ。

**reviewerが独立に確認した命題。** SCN-UNIT-ROLE-005の期待値は`allowedPaths`から導出されておらず、元のdirectory照合へ戻す変異で失敗するため、変異を隠す構造ではない。

**rollbackの制約（reviewerの指摘）。** 宣言（PR-2）だけを残して照合を旧実装へ戻すと権限が配下へ再拡張される。**rollbackは宣言と照合を一体で戻す。**

## 4. 実測値

| 対象 | 値 |
|---|---|
| `npm test` | **940 scenarios / 5,523 steps 全通過**（unit 587・integration 315・e2e 38） |
| `lint`・`format:check`・`typecheck`・`source:check`・`project:quality`・`docs:format`・`test:format`・`trace:check`・`architecture:check`・`package:check` | すべて exit 0 |
| AC-08の変異試験 | 1件。非`/`終端の早期returnを取り除く変異は**死亡** |
| AC-09の確認 | 両方向比較の18件が不変。`src`・`src/`・`test/…`・`docs/specs/…`の許可を含む |

## 5. 判定

- 未解決Critical/High: **なし**
- 判定: **approved**（Step 10 ラウンド1）
- 残存リスク:
  1. **宣言と実態を突き合わせる強制点は存在しない。** `validateRoleOperation`に製品の呼び出し元が無く、本変更は実行時の強制を増やさない。**この監査はrelease経路の保護を主張しない。** #1047が所有する
  2. `pathAllowed`はUnicode正規化を行わない。本変更では触れていない
  3. `dist/src/`は配布されるため、深いimportで`validateRoleOperation`を直接使い、かつ非`/`終端要素を宣言している利用側があれば判定が狭まる。CLIの振る舞いは変わらない
  4. **本PRだけでは#1051の権限は解けない。** 宣言はPR-2が持つ
- 次に許可される操作: PR作成とその後の人間レビュー。**merge・release・publish・cleanupはそれぞれ別authority**
