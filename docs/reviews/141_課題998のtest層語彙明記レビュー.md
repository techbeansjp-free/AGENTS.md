# 141 課題998のtest層語彙明記レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #998 |
| ラウンド | Step 10 ラウンド1〜2 |
| 比較基点 | `5137183f96a5f104841ea8328844202d39e66cfa` |
| H_impl | `376430307c07d0cb66ca83c1019bc5b7450ef7b6` |
| 比較基点の由来 | worktree作成時点の`origin/main`のtip |
| モード | full（Q-01がfalse） |
| 対象差分 | 6 file、+45 -14。commitは`3bb91361`と`37643030`（取り込みの前進commit） |
| 対象外 | 案(A)（`testLayers`を単調性の対象から外す）。**#1208 でowner決裁待ち**。案(C)（語彙をpackageが定義する）。全SCN×全layerの直積要求（#995）。`docs/reviews/`のrole authority不整合（#1047） |
| 残り予算 | **1**（同一範囲で最大3ラウンド、収束後にHEADが動いたときの取り直しを1回まで） |
| ラウンド数 | 2。ラウンド2は`pr create`後の外部指摘の取り込みである（#1194・#1201の経路） |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260904_102211_test層の語彙非定義と縮小経路をschemaへ明記する |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-006（**改訂**） |
| 成果物行数 | 製品 **+2行**（配布schema 2件のdescription）。仕様 **+2 -1行**。支援層 **+41 -13行**（既存stepの延長と共通読み取り関数）。**支援層/成果物 = 20.5倍。絶対値は41行**。ラウンド2の取り込みで支援層 +27 -12が増えた |
| 縮小の先行評価 | **新規SCNを足さない。** #982 では `SCN-UNIT-TRACE-010` を新設したが、それは「拒否する009」と「受理する010」で判定の向きが逆だったためである。本件は `testLayers` のschema shapeという009と**同一の対象・同一の向き**であり、分ける理由がない。分けると同じfixtureを2度組み立てる。**運用ポリシーの「手段の追加より既存手段の縮小を先に評価する」に従う** |
| 決裁 | 利用側報告 #998 への対応 |
| 実施者・日時 | reviewer（claude）、2026-09-04（JST） |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節） | advanced | claude | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerとreviewerが**同一session**である。0.2節に逸脱として開示する。**本Issueの起点は外部の利用側報告である** |

### 0.2 開示する逸脱

1. **implementerとreviewerが同一sessionである。** 緩和は、**要求の出所が外部の利用側projectであること**、起票時の主張4件をすべて実測で検証して否定したこと、変異試験を行ったことである。
2. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 主張1の検証 | `src/domain/project-choice-shrink.ts` | `SHRINKABLE_FIELD_PATHS`に`projectChoices.testLayers`が含まれる。**「二度と直せません」は成立しない** | 一次資料 |
| 主張2の検証 | `test/features/unit/project-choice-diff.feature` | `SCN-UNIT-CHOICE-010`が`testLayers`の縮小を検査している | 一次資料 |
| 主張3の検証 | `src/domain/enforcement.ts` | 拒否diagnosticが`projectChoiceShrinkProposals`の二段階手順を**名指しで案内する** | 一次資料 |
| 主張4の検証 | 同上 | `requiredAuthority`は`既定ブランチのproject policy owner`。**迂回路は存在する** | 一次資料 |
| 記述の不在 | `.agent-skill-chain/schemas/project-choice.schema.json` | `testLayers`に`description`が無かった | 実行記録 |
| テスト | `npm run conformance:check` | `1494 scenarios (1478 passed, 16 skipped)` | テスト出力 |
| commit前candidate | 5 file | working tree clean | Git index |
| Phase A artifact | `docs/reviews/141_課題998のtest層語彙明記レビュー.md` | `H_impl` = `3bb91361`。`H_impl..H_final`の差分pathは本file 1件 | Git観測 |
| commit後external | PR、CI run、review | Step 11で観測する | 外部のimmutable証拠 |

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/schemas/project-choice.schema.json` | M | project | project | `testLayers` へ `description` を1件足した。**JSON Schemaの注釈keywordであり検証に影響しない** | 適合 | REQ-WF-006 / AC-01〜AC-03 / SCN-UNIT-TRACE-009 | `minItems`・型・`required` を変えていない | pass |
| `.agent-skill-chain/schemas/project-policy.schema.json` | M | project | project | 同一の `description` を `$defs.projectChoices` 側へ足した | 適合 | 同上 | 同上 | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | project | spec | REQ-WF-006へtest層の語彙非定義と除去手順を追記した | 適合 | REQ-WF-006 | 記述だけで実行authorityを持たない | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | project | spec | 変更履歴を1行足した | 適合 | 同上 | 同上 | pass |
| `test/steps/unit.steps.ts` | M | package | package | 既存 `SCN-UNIT-TRACE-009` のThen stepへ記述のassertを2件足した。**新規SCNを足していない。** 取り込みで配布schema 2件の共通読み取り関数へまとめ、009と010の両方を2件loopへ変えた | 適合 | AC-01・AC-02・AC-04・AC-05 | fixtureは実fileの読み取りで外部へ到達しない | pass |
| `docs/reviews/141_課題998のtest層語彙明記レビュー.md` | A | project | project | 本artifact。**取り込みroundでは`比較基点..H_impl`へ最初のartifact commitが入るため、自身を個別監査行に持つ**（#1194の新経路の構造的帰結） | 適合 | REQ-WF-006 | 記述だけで実行authorityを持たない | pass |

## 2. 受け入れ条件の確認

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-TRACE-009 | 両schemaの `description` | `1 scenario (1 passed)` | pass | 「package側は語彙を定義しない」をassertする。変異M1でkill |
| AC-02 | SCN-UNIT-TRACE-009 | 同上 | 同上 | pass | 「projectChoiceShrinkProposals」をassertする。変異M1でkill |
| AC-03 | 原文読解 | 同上 | — | pass | 「`minItems: 1`を持つのは「テスト層が1つもないprojectはない」ためである」を書いた |
| AC-04 | SCN-UNIT-TRACE-009 | 同上 | `1 scenario (1 passed)` | pass | 既存scenarioのThen stepを延ばした |
| AC-05 | SCN-UNIT-TRACE-009、SCN-UNIT-CHOICE-002〜011 | 変更なし | `11 scenarios (11 passed)`（TRACE系）、conformance全合格 | pass | `minItems` が 1 のまま。単調性scenarioが全件合格 |

### 2.2 不変条件

| INV ID | 内容 | 判定 | 証拠 |
|---|---|---|---|
| INV-01 | `minItems: 1`・型・`required` を変えない | pass | `git diff` は両schemaそれぞれ **+1行**（descriptionのみ） |
| INV-02 | `classifyMonotonicArray` と `ASC-TRUST-001` を変えない | pass | `src/domain/project-choice-diff.ts` と `src/domain/enforcement.ts` は差分に現れない |
| INV-03 | 強制点も新規SCNも足さない | pass | 差分のtestは既存stepの延長14行だけである |

## 3. 肯定的評価

- **起票時の4主張をすべて実測で否定した。** 縮小経路・検査・診断の3つが #1044 以降に入っており、「二度と直せません」「迂回路がない」は成立しない。**報告を字面で受け取ると、既に解けている問題へ手段を足すことになる。**
- **自分の想定も1つ否定した。** 「拒否理由が経路を示していないのでは」と考えたが、`enforcement.ts` の診断は既に二段階手順を名指ししていた（DISC-802）。**推論せず実読した。**
- **新規SCNを足さなかった。** #982 では足したが、あちらは判定の向きが逆で、こちらは同一である。**同じ形の変更でも、分けるかどうかは対象と向きで決まる。**
- **単調性判定を1行も変えていない。** 差分は注釈keyword 2行と文書とtestだけである。
- **案(A)を勝手に採らなかった。** 検知の緩和は配布契約変更であり、**#1208 でowner決裁待ちに置いた。**

## 4. 敵対的評価

| 観点 | 攻撃 | 結果 |
|---|---|---|
| 記述の消失 | `description` を消す | 変異M1でkill |
| 判定の変更 | `minItems` を外す | `SCN-UNIT-TRACE-009` の既存assertがkillする |
| 判定の変更 | 単調性から `testLayers` を外す | `SCN-UNIT-CHOICE-002` が失敗する。**本Issueの範囲外であり #1208 の決裁対象である** |
| 片側だけの記述 | `project-policy.schema.json` 側を書かない | **両方へ足した。** 利用側がどちらを読むか決まっていない |
| 過剰な検査 | 語彙の妥当性を検査する門を足す | **採らない。** 案(C)であり、利用側の多様性を狭める |
| 費用の残存 | 分類上の誤りを直すたびに2つのPRを要する | **成立する。** 5節ADV-01に記録し #1208 へ委ねる |

## 5. 指摘

| ID | severity | 事実 | 観測 | 由来 | 対処 | 判定 | 残余 |
|---|---|---|---|---|---|---|---|
| F-01 | Major | **語彙非定義と縮小経路がschemaから読めなかった。** 利用側は「packageが語彙を定義しないのに縮小だけ禁じる」と読むしかない | 利用側報告 #998 | 外部の判断 | 両schemaへ `description` を足した | valid / resolved | ADV-01 |
| F-02 | — | 「一度混入した誤りを是正できない」「迂回路がない」 | 同上 | 外部の判断 | **valid でない。** `SHRINKABLE_FIELD_PATHS` に含まれ、`SCN-UNIT-CHOICE-010` が検査し、拒否diagnosticが手順を名指しする。**起票後に #1044 で解消された** | **invalid（解消済み）** | なし |
| DISC-801 | Low | **起票時の主張4件がすべて解消済みだった** | 実行記録 | 実測 | 着手範囲を「記述の欠落」だけに絞った | valid / resolved | なし |
| DISC-802 | Low | **拒否diagnosticは既に二段階手順を名指ししていた。** 自分の想定が誤りだった | 実行記録 | 実測 | 診断文を変更しない | valid / resolved | なし |
| ADV-01 | Medium | **分類上の誤りを直すたびに2つのPRを要する。** 二段階手順の構造的な費用である | 敵対評価 | 敵対評価 | **本Issueでは修正しない。** 検知の緩和は配布契約変更でowner決裁を要する。**#1208 に置いた** | valid / record-only | 是正の費用が2 PRのまま残る |

| F-03 | **Major** | **配布schemaは2件あるのに、`project-choice.schema.json`だけをassertしていた。** `project-policy.schema.json`側のdescriptionを消す変異が**生存する**。実測で確認した | PR #1209 の外部review | 外部review | 読み取りを`readDistributedChoiceSchemas()`へまとめ、009と010の両方で2件をloopする。変異M2・M3でkill | valid / resolved | なし |
| DISC-803 | Low | **是正の置換が1箇所だけ適用されず、黙って落ちていた。** 010側のThen stepは字下げが違い、patternが一致しなかった | 変異試験 | 変異試験 | **変異M3が生存したことで気付いた。** 正しいpatternで再適用した | valid / resolved | なし |

**未解決のCritical / Highは0件である。**

## 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `.agent-skill-chain/schemas/project-choice.schema.json` | 入る | 利用側が語彙非定義と除去手順をschemaから読める |
| `.agent-skill-chain/schemas/project-policy.schema.json` | 入る | 同上 |
| `docs/specs/02_要件/01_ワークフロー要件.md` | 入らない | project所有の仕様であり利用projectには不在 |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | 同上 |
| `test/steps/unit.steps.ts` | 入らない | `package.json`の`files`に含まれない |

判断: 配布物を更新した
根拠: 配布schema 2件を更新した。**検証の振る舞いは変わらない。** `description` はJSON Schemaの注釈keywordである。

## 6. ラウンド固有の確認

### ラウンド1

- 対象: 実装差分の5 file。
- 確認: 個別監査6行、AC-01〜05、INV-01〜03、肯定5観点、敵対6観点、変異1件。
- 結果: blocking 0件。record-only 1件（ADV-01）。resolved 3件（F-01・DISC-801・DISC-802）。**F-02は実測で否定した。**

### ラウンド2

- 対象: `pr create`後に届いた外部指摘1件（F-03）。**#1194・#1201 で入れた取り込み経路の適用である。**
- 確認: 指摘を変異で再現した（`project-policy.schema.json`のdescription 2件を消しても12 scenarioが全合格）。是正後に同じ変異を2件とも再実行してkillを確認した。**是正は前進commitで行い、amendを使っていない。**
- 結果: blocking 0件。resolved 2件（F-03・DISC-803）。

## 7. テスト結果

**表を書く前に1本ずつ実行した。**

| 層・検査 | コマンド | 件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---|---:|---:|---:|---|
| unit・integration・e2e | `npm run conformance:check`（内部で`npm test`を実行する） | 1494 | 1478 | 0 | 16 | pass |
| 静的検査 | `lint`・`format:check`・`typecheck`・`source:check`・`trace:check`・`architecture:check`・`project:quality`・`cli:check`・`workflow:check`・`package:check`・`docs:format`・`test:format` | 12 | 12 | 0 | 0 | pass |
| 監査 | `npm run audit:check` | 1 | 1 | 0 | 0 | pass |

### 7.1 変異試験

| ID | 変異 | 結果 |
|---|---|---|
| M1 | `testLayers` の `description` を消す（`project-choice.schema.json`） | kill |
| M2 | `testLayers` の `description` を消す（`project-policy.schema.json`） | kill（**当初生存**） |
| M3 | `forbiddenTestFileSuffixes` の `description` を消す（`project-policy.schema.json`） | kill（**当初生存**） |

**3件すべてkill。** M2とM3は外部指摘を受けて実測し、**当初生存していた**。復元後に `SCN-UNIT-TRACE-0` 系12件の再実行で緑を確認している。

## 8. 総合判定と再開地点

**approved。** 未解決のCritical / Highは0件である。再開地点は実装計画1節の順序表。
