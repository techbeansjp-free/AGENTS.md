# 83 課題965 配布準備工程と配布前品質検証の分離 適用レビュー

> 状態: `candidate-verified`（外部承認待ち）。REQ-SQ-020が定める新しい形へ`package.json`と`release.yml`を切り替え、release workflow契約validatorを双方の入口へ対応させた変更を、merge済みdefault branch headを比較基点として固定した内部監査証拠である。**`valid: true`は候補が既定branchを基準に検証を通過したことを示すだけであり、既定branchへの反映を示さない。**

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象Issue | #965（親 #1025 第1層） |
| 比較基点 | `cf1a62a7030132019a021aced0366b1918244971` |
| H_impl | `ccf33b5273c669448e87516db93e6f6a0e7d8697` |
| reviewer | claude（4経路×2版の実測とgate全実行）。外部諮問としてcodexとfableへ独立に判定させた |
| 実施日 | 2026-08-28 |
| ラウンド数 | 2 |
| Step chain | 迂回: 手書き運用で進めており、workflow recordを経由していない |
| 仕様の所有箇所 | `docs/specs/02_要件/04_仕様・品質管理要件.md` REQ-SQ-020（必須・合意、根拠 Issue #965・#978）。「準備工程は構築だけを行い、配布前品質検証は独立した入口が持つ」「gate集合は減らさない」「準備工程の形とrelease workflowが呼ぶ入口の一致を要求し、片方だけの変更を双方向で拒否する」 |
| 成果物行数 | **製品50行**（`package.json` +3/-1、`.github/workflows/release.yml` +2/-2、`src/domain/release.ts` +45/-12）。支援層はtest 42行（`test/steps/auto-release.steps.ts` +37/-5、`test/features/unit/auto-release.feature` +5）、仕様1行（追跡表へSCN 1件） |
| 縮小の先行評価 | **新しい機構を1つも作っていない。** 新形の文字列は`scripts/check_project_quality.ts`の`DISTRIBUTION_PREPARE_COMMAND`と`DISTRIBUTION_GATES`から導出し、書き写していない。反例testは#978で入った既存のSCN-UNIT-DISTSCRIPT-001〜009とSCN-INT-DISTGATE-001〜020をそのまま使い、1件も追加していない。**前セッションが取り下げたPR-3の支援層172行は再作成していない** |

### 支援層が製品を超えていないことの確認

支援層42行に対し製品50行。**比は0.84倍で、運用ポリシーの条件を満たす。** 支援層が薄い理由は、この変更が#978で用意済みの前方互換validatorを起動する「activation差分」だからである。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| **仕様が新形を要求している** | `docs/specs/02_要件/04_仕様・品質管理要件.md` REQ-SQ-020 | 必須・合意。実装欄に`package.json`と`.github/workflows/release.yml`が列挙されている | 仕様 |
| 前方互換validatorが既に存在 | `scripts/check_project_quality.ts:112-147` | `prepack === "npm run build"`のとき`prepare`の一致と`verify:distribution`のgate列完全一致を要求する | 既存コード |
| 対応検査が既に存在 | `scripts/check_conformance.ts:937-985` | 形と release.yml の入口の対応を両方向で拒否する | 既存コード |
| **workflow契約validatorが未対応だった** | `src/domain/release.ts:787`、`:842`、`:898` | `npm run prepack`を文字列で固定しており、切替と同時にworkflow契約が破れる | 実行観測（4 scenarioが失敗） |
| **利用者側の失敗（比較基点）** | npm git依存install | **exit 0で終わり`node_modules/.bin/`が作られない** | 実行観測 |
| 同上（pnpm） | pnpm git依存install（`allowBuilds`あり） | **exit 1** `ERR_PNPM_PREPARE_PACKAGE`（`npm run prepack`が失敗） | 実行観測 |

## 変更ファイル個別監査

| path | status | owner | target layer | 責務・配置 | 依存・循環 | 仕様・追跡 | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `package.json` | M | package owner | 配布契約 | `prepare`・`prepack`を構築だけにし、`verify:distribution`へgate列を移す | 変更なし | REQ-SQ-020、SCN-UNIT-DISTSCRIPT-001〜009 | 逆変換で戻る。**前方互換validatorが旧形も受理する** | pass |
| `.github/workflows/release.yml` | M | package owner | release | 検証段階とpublish直前の2箇所を新しい入口へ | 変更なし | REQ-SQ-020、SCN-INT-DISTGATE-001〜020 | 2行の逆変換で戻る | pass |
| `src/domain/release.ts` | M | package owner | domain（release契約） | workflow契約が双方の入口を受理する | 追加依存なし | REQ-GH-002、REQ-GH-003、REQ-SQ-020 | 逆変換で戻る | pass |
| `test/steps/auto-release.steps.ts` | M | package owner | test | 実workflowへの断定を双方の入口へ広げ、境界の反例stepを足す | 実装へ単方向 | SCN-INT-AUTORELEASE-001、SCN-INT-AUTORELEASE-003、SCN-UNIT-RELWF-P04 | 逆変換で戻る | pass |
| `test/features/unit/auto-release.feature` | M | package owner | test | 接尾辞つきscript名の反例を1件追加 | 実装へ単方向 | SCN-UNIT-RELWF-P04 | 4行の除去で戻る | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | 利用側 | 仕様 | SCN-UNIT-RELWF-P04をREQ-GH-003の行へ追記 | 参照のみ | REQ-GH-003 | 1件の除去で戻る | pass |

## 2. 受け入れ条件の確認

| AC（Issue #965 の OUTCOME） | 結果 | 証拠 |
|---|---|---|
| OUTCOME-01 git依存installで`bin`が実在し起動する | **充足（npm）。** pnpmはconsumerの`allowBuilds`を要する | 下記の経路別実測表 |
| OUTCOME-02 準備工程が`.git`の無い展開先でも完了する | 充足 | pnpmのcodeload tarball経路（`.git`なし）で比較基点は失敗、本PRは23.6秒で成功 |
| OUTCOME-03 gateの集合と順序が変わらない | 充足 | `verify:distribution`は`DISTRIBUTION_GATES`から導出。`project:quality`が順序完全一致を検査し`valid: true` |
| OUTCOME-04 `bin`が配布物に含まれない状態を検出できる | 充足（既存） | `package:check`。本PRで変更していない |
| OUTCOME-05 既存シナリオが全通過 | 充足 | `npm test`が**1010 scenario全通過** |

### 2.1 開発考慮事項の適用判定（必須）

| ID | 判定 | 確認 |
|---|---|---|
| DC-PRIVACY | applicable | **準備工程で実行するcodeを構築だけに縮めた。** consumerの環境で品質gate全体を走らせない |
| DC-OBSERVABILITY | applicable | npmの「exit 0で壊れる」状態が解消する。失敗は準備工程の非0終了として表面化する |
| DC-UX | applicable | 案内どおりの手順が動く。最小lifecycleを実測した |
| DC-TOKENS | not-applicable | UI要素を持たない |

## 3. 肯定的評価

- **仕様に既にある要件を起動しただけで、新しい契約を足していない。** REQ-SQ-020は必須・合意であり、実装欄に本PRが触れた2 fileが列挙されている
- **新形の文字列を書き写していない。** validatorの`DISTRIBUTION_GATES`から導出したため、gate列のずれが原理的に起きない
- **未対応のvalidatorを実測で見つけた。** `src/domain/release.ts`が`npm run prepack`を固定していることは、切替後に4 scenarioが落ちて初めて分かった
- **利用者経路を版をまたいで実測した。** 比較基点と本PRの両方を、実際のGitHub経路でinstallして観測している

## 4. 敵対的評価

| 反例 | 結果 |
|---|---|
| gateが減っている | **不成立。**`verify:distribution`はgate列を順序込みで完全一致で持ち、`project:quality`がそれを検査する |
| `npm pack`が検証なしになった | **成立する。**これは意図した変更である。**不可逆な公開の直前検証はrelease workflowの2箇所が持つ**（S83-M-01） |
| release workflowが旧入口を呼び続けている | **不成立。**`conformance:check`が形と入口の対応を両方向で拒否し、`valid: true`である |
| bump経路に重いgateが混入する | **不成立。**`src/domain/release.ts`は`verify:distribution`もbump jobから排除するよう更新した |
| pnpmのgit依存が既定で成立する | **不成立。**`allowBuilds`なしでは`ERR_PNPM_GIT_DEP_PREPARE_NOT_ALLOWED`のままである。**package側ではopt-inできない。**隔離storeで再確認した |
| 仕様外の経路を製品契約へ足している | **不成立。**REQ-SQ-020が先にあり、本PRはその実装欄どおりの変更である |
| tarball経路が壊れた | **不成立。**npm・pnpmの双方で`--help`と最小lifecycleがexit 0 |

## 5. 指摘

| ID | 深刻度 | 内容 | 状態 |
|---|---|---|---|
| S83-M-01 | Medium | **手元の`npm pack`が品質gateを実行しなくなる。** 従来は`prepack`がその役割を兼ねていた | 未是正。**意図した変更である。**REQ-SQ-020が「準備工程は構築だけを行う」と定めており、不可逆な公開の直前検証はrelease workflowの2箇所（検証段階とpublish直前）が持つ。`conformance:check`が入口の対応を強制する |
| S83-M-02 | Medium | pnpmのgit依存installはconsumerが`allowBuilds`を書かなければ成立しない | 未是正。**package側では回避できない**pnpmのsecurity policyである。本PRは「書いた場合に成立する」状態を作る。書かない場合は明示的なerrorで停止し、npmのような無言の破損にはならない |
| S83-L-01 | Low | `src/domain/release.ts`はworkflow本文だけを入力とするため、形と入口の対応そのものは判定できない | 未是正。**対応の判定は`checkDistributionGateReachability`が`package.json`と突き合わせて行う。**二重化しない |
| S83-L-02 | Low | git依存経路はREADMEにも`00_利用案内.md`にも記載がない | 未是正。公開入口は`npx agent-skill-chain`（#984）であり、本PRは仕様が要求する準備工程の形を満たすに留める |

### ラウンド予算

ラウンド2で収束した。未解決のCritical/Highは0件。上限3ラウンドに対して1ラウンドの余裕を残している。

## 6. ラウンド固有の確認

### ラウンド1

全評価基準（肯定: 正しさ・価値・実現可能性・整合性・保守性／敵対: 反例・失敗経路・境界値・悪用・安全性・データ損失・rollback・範囲漏れ）を確認した。Medium 2、Low 2。新規Critical/High 0件。判定 **candidate-verified（自動reviewを待つ）**。

**外部諮問2件の判定が割れ、仕様の原文で決着させた。** codexは案B（今直す）を選び、根拠としてREQ-SQ-020が必須・合意であることを挙げた。fableは案A（対象外へ転換してclose）を選び、根拠として`04_機能/01_ワークフローv0.3.md:48`の「公開入口は`npx`」を挙げた。**両者の引用を実見した結果、REQ-SQ-020は「準備工程は構築だけを行い、配布前品質検証は独立した入口が持つ」と規範的に書いており、実装欄に`package.json`と`release.yml`を列挙している。** fableの引用は公開入口を定めるが取得元をregistryへ限定していない。したがってcodexを採った。

**fableの反論のうち1点は実測で否定された。** 「案Bを実施してもpnpmは`prepack`の`.git`依存で成立しない」という主張は、`prepack`が構築だけになれば`.git`を要求しなくなるため成り立たない。実測でも`allowBuilds`ありのpnpmが23.6秒で成功している。

### ラウンド2（自動review）

CodeRabbitがactionable 3件を出した。**2件を是正し、1件は根拠を示して採らなかった。**

| 指摘 | 判定 | 対応 |
|---|---|---|
| `bump_version` jobの成功messageが拒否条件と一致していない（配布前品質検証を含むのにaudit:checkだけ述べている） | 妥当 | messageを「配布前品質検証とaudit:checkを含まないことを確認した」へ改めた |
| **release前品質gateの検出が`\b`終端のため`verify:distribution-extra`のような接尾辞つきscriptを同じgateと誤認する** | **妥当。**gate迂回の実経路である | `blockHasNpmRun`が既に持っていた`(?=\s|$)`の境界を`npmRunPattern`へ切り出し、yaml全体の検査からも同じ規則を使う。**境界規則の正本を1箇所にした** |
| 「実施日」が実際の監査日と一致していない可能性 | **採らない。** | 監査は日本標準時2026-08-28に実施しており記載は正しい。commitのauthor dateはUTC表記のため2026-08-27T16時台に見えるが、同一時刻である |

**境界の是正は変異試験で確認した。** `npmRunPattern`の終端を`\b`へ戻すと、追加したSCN-UNIT-RELWF-P04だけが失敗する（`4 scenarios (3 passed, 1 failed)`）。

是正後に`npm test`を再実行し**1011 scenario全通過**。実装commitはamendで畳み、`H_impl`を取り直している。

## 7. テスト結果

| 層・検査 | コマンド | 結果 |
|---|---|---|
| 配布前品質検証（全gate） | `npm run verify:distribution` | **exit 0**（本artifactのcommit後に実行） |
| 全層 | `npm test` | **1011 scenario全通過** |
| 契約 | `npm run project:quality`、`npm run conformance:check` | ともに`valid: true` |
| 静的 | `npm run typecheck`、`npm run lint`、`npm run format:check` | exit 0 |

**利用者経路の実測。** 比較基点`cf1a62a7`と本PR`698f1235`を、実際のGitHub経路（`github:techbeansjp-free/AGENTS.md#<ref>`）で隔離directoryへinstallした。Node.js v24.19.0、npm 11.17.0、pnpm 11.24.0。

| 経路 | 比較基点 `cf1a62a7` | 本PR `698f1235` |
|---|---|---|
| npm git依存 | **exit 0。`node_modules/.bin/`が作られない（無言の破損）** | **exit 0。binが作られ`--help`がexit 0** |
| pnpm git依存（`allowBuilds`なし） | exit 1 `ERR_PNPM_GIT_DEP_PREPARE_NOT_ALLOWED` | exit 1 同左（**package側では変えられない**） |
| pnpm git依存（`allowBuilds`あり） | **exit 1 `ERR_PNPM_PREPARE_PACKAGE`（`npm run prepack`が失敗）** | **exit 0。23.6秒でbinが作られる** |
| tarball（`npm pack`成果物） | 成立 | **成立（npm・pnpmとも）** |

**この実測はラウンド2の是正前のhead `698f1235`で行った。** 是正はrelease workflow契約validatorとtestだけを触っており、**installの挙動を決める`package.json`と`.github/workflows/release.yml`は`698f1235`と`ccf33b52`で同一である**（`git diff 698f1235 ccf33b52 -- package.json .github/workflows/release.yml`が空）。

**文書記載の最小lifecycle**（`.agent-skill-chain/00_利用案内.md:33-36`）を、本PRのgit依存installとtarball installの双方で実行した。

| 手順 | git依存（npm） | tarball（npm） | tarball（pnpm） |
|---|---|---|---|
| 導入preview `install --root=.` | 終了値0 | 終了値0 | 終了値0 |
| 導入適用 `install --root=. --apply` | 終了値0 | 終了値0 | 終了値0 |
| 診断 `doctor --root=.` | 終了値0 | 終了値0 | 終了値0 |

**`npm pack`の所要時間。** 旧形は品質gate全体を実行していたため約6分（pnpmの`allowBuilds`経路の実測で6分20秒）。新形は**15.4秒**で、`package/dist/`の件数は164で同一である。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `package.json` | manifestとして入る | **`scripts`が変わる。**consumerのgit依存installで`prepare`が構築を実行するようになる |
| `src/domain/release.ts` | 入る（`files`が`dist/src/`を列挙する） | 利用側のrelease workflow契約検証が、新しい入口も受理するようになる |
| `.github/workflows/release.yml`、`test/` | 入らない | `files`が列挙しない |

判断: 配布物を更新した

根拠: `package.json`の`scripts`と`dist/src/domain/release.ts`が変わる。**consumerが観測する変化は「git依存installで実行fileが作られる」ことと、利用側workflow契約検査が新しい入口を受理することである。**`npm run package:check`はexit 0で、`files`の列挙と`bin`の指し先は変えていない。

## 9. 独立reviewの成立

| 条件 | 充足 |
|---|---|
| reviewerが実装担当の判断を入力に持たない | 充足。仕様原文、既存validatorのcode、4経路×2版のinstall実測を入力にした |
| 各ラウンドの判定と根拠が原文引用を伴う | 充足。REQ-SQ-020と`DISTRIBUTION_PREPARE_COMMAND`を引用 |
| 有限ラウンドで終了する | 充足。1ラウンドで収束 |
| 外部証拠 | PR作成後のCI結果と自動reviewで補う |

**諮問が割れたときに仕様の原文へ戻ったことが、この変更で最も効いた判断である。** 前セッションはIssueコメントで「git依存は仕様外」と結論していたが、**耐久仕様の必須要件と矛盾していた。** Issueコメントは仕様の正本ではない。

## 10. 仕様整合性

`docs/specs/`は更新していない。

REQ-SQ-020は既に本変更を要求しており、追跡表もSCN-UNIT-DISTSCRIPT-001〜009（`scripts/check_project_quality.ts`）とSCN-INT-DISTGATE-001〜020（`scripts/check_conformance.ts`、`.github/workflows/release.yml`）で対応済みである。`src/domain/release.ts`の変更はREQ-GH-002・REQ-GH-003の既存scenario（SCN-INT-AUTORELEASE-001/003、SCN-INT-RELEASE-001、SCN-UNIT-RELWF-P02）が覆っており、いずれも本PRで通過する。**新しい要件を導入していない。**

## 11. 総合判定と再開地点

**判定: candidate-verified（外部承認待ち）**

- 未解決Critical: 0件
- 未解決High: 0件
- 未解決Medium: 2件（S83-M-01は意図した変更、S83-M-02はpnpmのconsumer policyで回避不能）
- 記録したLow: 2件

再開地点: ステップ11（PR作成）。**merge後にIssue #965 のcloseコメントへ経路別実測表を残す。**#984（publish）と#1024（consumer acceptanceの自動化）は本PRの範囲外である。
