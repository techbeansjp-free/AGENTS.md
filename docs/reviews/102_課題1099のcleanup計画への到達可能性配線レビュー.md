# 04 レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1099 |
| ラウンド | Step 10 ラウンド1〜3、およびmerge直前の最終review（ラウンド4） |
| 比較基点 | `01d081961aea799cfc296eb85e1372a0c2184cea` |
| H_impl | `be056ed8fb3a7b12fca4afb98eec1821add62406` |
| 比較基点の由来 | review開始時点の`origin/main`のtip。PR #1098（#1097の復旧可能性）のmerge commitである。前へ進めていない |
| Step 10のreview session ID | `945b28f9395d3ee1bd1c83873103c3a480ef8e8c1afaf42b8f491b5701231696` |
| モード | full |
| 対象差分 | `src/cli.ts`、`test/features/integration/finalize-ignored-artifacts.feature`、`test/steps/finalize-ignored-artifacts-cli.steps.ts`、`docs/specs/15_要件追跡/`の2 file。commitは`4989805e`と`1b494042` |
| 対象外 | `recoveryReachable`の判定式（#1097で確定）。squash merge時の残存リスク（#1097の残存リスク1）。`planWorktreeCleanup`の呼び出し形のリファクタ。`finalizeIgnoredPathAllowlist`のfallback分岐の被覆（R1-N06） |
| 残り予算 | **0**（同一範囲で最大3ラウンド。総2ラウンドで設計していたが、**ラウンド3で`audit:check`が検出した本artifactの書式不備1件を是正して使い切った**。6節を参照する） |
| ラウンド数 | 3。ラウンド1は実装差分`4989805e`、ラウンド2は是正`1b494042`、**ラウンド3は`audit:check`が検出した本artifactの書式不備1件の是正**が対象である |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260901_184046_reachableFromDefaultBranchがcli層で渡されず-complete経路だけ免除が働かない |
| 仕様の所有箇所 | `docs/specs/02_要件/02_プロジェクトライフサイクル要件.md`のREQ-LC-009。**#1097で「同fieldが真であると観測できたときだけ免除する」と確定済みであり、本Issueはその要件が`--complete`経路で成立していない実装欠陥である。** REQ-LC-009の本文は変えない |
| 成果物行数 | 製品 **+4 / −0行**（`src/cli.ts`）。仕様 **+2 / −0行**。支援層 **+296 / −4行**（test 2 file） |
| 縮小の先行評価 | 4案を評価し3案を不採用とした。`planWorktreeCleanup`の入力をobjectごと渡すリファクタは同種の漏れを構造的に防げるが4呼び出しすべての引数構成が変わり回帰面が広がるため別Issueへ分離。`assessWorktreeRemovalSafety`の入力で同fieldを必須にする案は呼び出し元をすべて壊すため不採用。既存SCNのfixtureを配線経由へ書き換える案は既存SCNが判定単体の契約を守っており別の量を測ることになるため不採用。**さらにラウンド1で、自分が足したSCN-E2E-WTCLEAN-007が空振りであることが判明したため約190行ごと撤回した。** 支援層は足す方向だけでなく削る方向にも動かした |
| 実施者・日時 | reviewer（codexとClaude fableの2体、およびmerge直前のcodex）、統合はcoordinator（claude）。**ラウンド1〜3は2026-09-01、merge直前reviewは2026-09-02。** いずれも実施済みである |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節と`review-session.json`） | advanced | codex、claude（fable） | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerは本session（claude）。reviewerは**codexの別invocation**と**Claude fableの別context**の2体。**両reviewerはfileを1つも変更していない。** fable側は変異試験をscratchpad複製で実施したと報告し、`git status`は当該review中に製品fileの変更を示していない |

**開示する逸脱が4件ある。**

1. **implementerとcoordinatorが同一sessionである。** reviewer 2体との分離は成立している。**書き込みモードのcodex起動が環境の権限設定で拒否されたため、implementerの委譲は断念した。** findingの採否判断をcoordinatorが行っており`roleContracts.coordinator.forbiddenOperations`の`self_approve`に接する。
2. **reviewerにClaude fableを使ったが、`memo/めも.md`は「fableは使用しない」と指示している。** 本Issueのreview実施後にこの指示を確認した。**fableの指摘（R1-B01・R1-B02を含む）は実測付きで検証しており内容は採用したが、指示との食い違いを隠さず記録する。** 以降のreviewはcodexのみとする。
3. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** `scripts/check_file_audit.ts`が同directoryを要求する一方、`validateRoleOperation`は実行時に強制されていない。解消を #1047 へ委譲する。
4. **ラウンド1のreview依頼が現物と乖離した（R1-B02）。** #1097と同じ手順ミスであり、`memo/めも.md`が引き継ぎ時点で「artifactをamendした後にgateを回し直さずpushした」と明記していた逸脱と同型である。前進commitへ畳んで再実行した。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | https://github.com/techbeansjp-free/AGENTS.md/issues/1099 | AC-1099-01〜03。Step 8で33486文字の本文を同期しdigestが一致 | 人間判断 |
| 差分 | `01d08196..be056ed8` | 6 file（review artifactを含む） | 既存コード |
| テスト | `npm test`（`quality`が内包） | 1389 scenarios、失敗0件 | テスト出力 |
| 仕様 | `docs/specs/15_要件追跡/`の2 file | updated | 既存文書 |
| commit前candidate | 対象差分5 path | `git status --short`が意図しないpath 0件 | Git index |
| Phase A artifact | `docs/reviews/102_課題1099のcleanup計画への到達可能性配線レビュー.md` | `H_impl..H_final`は本artifact 1 fileだけ | Git観測 |
| commit後external | PRは本artifact確定後に作成する | Step 11で観測する | 外部のimmutable証拠 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **成立する。** `npm run architecture:check`が違反0件。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: **`H_impl = be056ed8`は本artifact commitの親である。**
- reviewer stable IDがPR author/`H_impl` author stable IDと異なる: **未観測。** PR #1100はreview 0件・review comment 0件で、trusted provider由来のreviewer actor IDが存在しない。**別providerや別contextであることはstable IDの差を証明しない。** 0.1節の逸脱1・2と9節を参照する。
- 既定branch追随を行った場合: **行っていない。**

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/cli.ts` | M | package | package | `FinalizeState`から`WorktreeCleanupPlanInput`への値の転送4行。加工しない | pass。module依存を追加しない | REQ-LC-009 / AC-1099-01 / SCN-INT-FINALIGN-006〜009 | 転送を外せば復旧する | pass |
| `test/features/integration/finalize-ignored-artifacts.feature` | M | package | package | 配線を通す8 scenarioの追加 | pass | AC-1099-01〜03 | 隔離repositoryで動く | pass |
| `test/steps/finalize-ignored-artifacts-cli.steps.ts` | M | package | package | 同step定義とfixture。trusted policyを同梱する | pass | 同上 | 実workspaceへ書き込まない | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package | spec | 新規SCNの追跡登録 | pass。`trace:check`が`orphanScenarios: []` | 同上 | 実行authorityを持たない | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | 1行追記 | pass | 同上 | 同上 | pass |
| `docs/reviews/102_課題1099のcleanup計画への到達可能性配線レビュー.md` | A | package | evidence | 本artifactのラウンド3版。**merge直前reviewの是正で`H_impl`が動き監査対象へ入る** | pass。実行codeを持たない | 全AC | 内容を戻せば復旧する | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **`比較基点..H_impl`すなわち`01d08196..be056ed8`の6 pathと本表の6行が一致する。** merge直前reviewの是正で本artifactが`比較基点..H_impl`へ入るため、artifact自身の行を表へ加えた。`H_impl..H_final`は本artifactの1 fileだけである。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **成立する。**
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: **ラウンド1の是正は上記5 fileと、撤回した`merge-completion-cleanup`の2 fileに閉じている。**

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | 計画は「1 scenarioだけ」と宣言していたが、SCN-INT-FINALIGN-006を足した時点でGREENになった後、**4呼び出しを個別に落とす変異試験では1つしかkillできなかった** | 支援層が1 scenarioから8 scenarioへ拡大した。目的・scope・AC・security境界・不可逆操作はいずれも変わらない | なし（`assess-discovery`は`continue`） | 4呼び出しを個別に覆う4 SCNと、免除の非適用側を4経路に固定する4 SCNを足した。**空振りだったE2E 1件は撤回した** | 13 scenarioがGREEN。削除変異4件がすべて検出される | updated | pass |

**「1 scenario」で足りると判断した根拠が誤っていた。** 4呼び出しはdry-run/apply × `--complete`有無の別経路であり、1件では配線の1/4しか守れない。**本Issueが是正しようとしている欠陥（配線が検査されていない）をそのまま残す。**

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1099-01 | SCN-INT-FINALIGN-006〜009 | `src/cli.ts`の4行 | pass | pass | 4経路すべてでcleanup計画が`ready`／`cleanup-preview`が`succeeded` |
| AC-1099-02 | 同上 | 同上 | pass | pass | 4呼び出しの削除変異がすべて検出される |
| AC-1099-03 | SCN-INT-FINALIGN-010〜013 | 変更しない | pass | pass | 到達不能な入力では4経路すべてでupstream由来の理由を含んだまま拒否される |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | worktree削除という不可逆操作の可否判定への入力を追加する。個人情報と秘密情報は扱わない | INV-01〜INV-05、変異試験3型 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | not-applicable | 実行時ログを1行も増やさない。`reasons`の内容は#1097で確定済みで本Issueは経路差を消すだけである | 出力schemaとphase構成を変えない |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | UI sourceが無い。利用者接点はCLIの構造化出力である | 既存SCN-INT-FINALIGN-001〜005が回帰を確認する |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | token sourceを持たない | 変更対象はTypeScript 1 fileの4行である |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 実物worktreeで`cleanup-preview`が`succeeded`になる。削除変異4件が全件検出される |
| 価値 | 利用者・運用上の目的を満たすか | pass | `--complete`の有無で判定が変わらなくなった |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 既存fieldの転送4行。追加依存も追加authorityも要さない |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | REQ-LC-009の本文を変えず、追跡表と変更履歴だけを更新した。`trace:check`が`orphanScenarios: []` |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 観測と判定の分離を維持した。cli層は受け渡しだけを行う |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | 到達不能なfixtureをSCN-INT-FINALIGN-010〜013で4経路に固定した |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 転送は失敗しない。観測の失敗は#1097でfail-closed済み |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | `undefined`が生じるのはworktree不在時だけで、その場合は`already-absent`へ分岐する |
| 悪用 | 注入、経路脱出、権限外操作等 | not-applicable | boolean 1つの転送であり新しい外部境界を作らない |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | `--cleanup-authority`と承認済みpreview digestの要求を弱めていない |
| データ損失 | 上書き、削除、部分公開、履歴消失 | **finding** | **R1-N01。** 定数`true`変異が5742で生存する。ただし`buildFinalizeReport`が未変異の値で独立に安全判定し`applyFinalize`が拒否するため**削除は起きず理由も出力に残る。冗長な門がfail-closedで受け止める** |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | 転送を外せば元へ戻る |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | **finding** | **R1-N06。** trusted policy同梱で`finalizeIgnoredPathAllowlist`のfallback分岐の被覆がこのfeatureから失われた。実効allowlistは既定と同一でverdictは不変である |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| R1-B01 | High | `SCN-E2E-WTCLEAN-007`が空振り。4配線をすべて削除した変異体でも全assertionが合格する | fixtureのtarget HEADとstale tracking refと`origin/main`が三者一致し、免除が無くてもupstream由来の理由が発生しない。coordinatorの削除変異でも1件もkillされないことを実測済み | `test/features/e2e/`、`test/steps/merge-completion-cleanup.steps.ts` | 約190行ごと撤回し追跡表の登録も削除 | resolved | なし |
| R1-B02 | High | review依頼と現物が乖離。DISC-001が対処済みと記録する3点が対象commitに存在しなかった | `git diff HEAD --stat`が4 file dirtyを示した | staging全体 | 前進commit `1b494042`へ畳みgateと変異試験を再実行 | resolved | なし |
| R1-N01 | Medium | 定数`true`変異がverdictを反転させずにupstream由来の理由だけを消す | 当時のSCNは`state`しか検査せず、理由文字列を通すtestがcli経路に存在しなかった | `src/cli.ts`、`test/` | 非適用側4 SCNを足し理由文字列も検査。3045・3177・5645が検出されるようになった | resolved | **5742は生存する。** 上記4節のとおり冗長な門がfail-closedで受け止める |
| R1-N02 | Medium | apply結果の判定が製品の自己申告に依っている | `state`と`branchPreserved`はいずれも製品の出力 | `test/steps/` | `git worktree list --porcelain`と`git branch --list`で独立に観測 | resolved | なし |
| R1-N03 | Medium | step直上のcommentが二重に虚偽 | `executeCompletionFlow`の説明を別経路のstepへ付け、trusted policyが無いと書きながら同一commitで同梱していた | 同上 | 実態へ書き換えた | resolved | なし |
| R1-N04 | Medium | 支援層が上流の最小計画と未整合で縮小評価も残していなかった | 要件定義は「1 scenarioだけ」、実装計画はSCN-006だけを指定 | staging | DISC-001へ拡大の事実と判断と対処を記録。01の9節も更新 | resolved | なし |
| R1-N05 | Low | 01のAC-1099-01が指すSCNが出荷形と不一致 | 006は非`--complete`、`--complete`は007へ改番 | staging | AC表とGherkin例を出荷形へ合わせた | resolved | なし |
| R1-N06 | Low | trusted policy同梱でfallback分岐の被覆が失われた | 実効allowlistは既定と同一でverdictは不変 | `test/steps/` | 記録のみ | valid（対象外） | **fallback分岐の被覆をどこで担保するかは未決** |
| R2-N01 | Low | artifact自身の点検 | ラウンド数と残り予算を先に書き、空振りSCNの撤回と定数`true`変異の生存を隠していない | 本artifact | 記録のみ | valid（記録） | なし |
| R3-B01 | Low | 本文の`H_impl`が短縮SHAで`audit:check`が拒否した | `parseFileAudit`は40桁のみ受理する | 本artifact | 完全SHAへ直した | resolved | なし |
| R3-N01 | Low | artifactの最終点検 | 変異表3型・逸脱4件・残存リスクの開示を確認した | 本artifact | 記録のみ | valid（記録） | なし |
| R4-B01 | Medium | ラウンド3の指摘件数がsessionと不一致だった | sessionは11件でresolved 8件・valid 3件、artifactは10件・7件・3件と記載していた | 本artifact | 6節と5節を実態へ直した | resolved | なし |
| R4-B02 | Medium | 定数`true`変異5742の残存影響を実際より安全に書いていた | `SCN-INT-FINALIGN-013`は`src/cli.ts:5670`で先に終了するため5742を通らない。preview後に到達性が真から偽へ変わる場合、変異体は5742の拒否理由を消し`applyFinalize`は汎用TOCTOU errorを返す | 本artifact | 7.1節と11節を実態へ直した | resolved | **削除防止自体は維持される** |
| R4-N01 | Low | `?? true`変異についてstep commentと変異表が矛盾していた | 同fieldは`false`であり`false ?? true`は`false`のまま。表の「等価」が正しくcommentが誤り | `test/steps/` | commentを直した | resolved | なし |
| R4-N02 | Low | 残存リスクの件数に構造的な転送漏れ再発を数えていなかった | 0節と`02_設計.md`は「同種の漏れは構造的に残る」と記録している | 本artifact | 11節へ加えた | resolved | なし |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: **はい。** 肯定5観点と敵対8観点を対象差分`4989805e`へ適用した。reviewerはcodexとClaude fableの2体で、観点8件を明示的に指定した。**codexへ渡したreview契約file（`1099-review.md`）には`.claude/hooks/asc-contract-citation.sh`が要求する`## 規範の引用`節を設け、REQ-LC-009と運用ポリシーとINVを原文で引用した。** 本artifactではなく契約file側の話である。
- 指摘を確定した: **blocking 2件（R1-B01・R1-B02）、record-only 6件。**
- 次ラウンド対象のCritical/High: **R1-B01・R1-B02。**

### ラウンド2

- 未解決Critical/High: **なし。**
- 修正差分: `src/`は変更なし。`test/`の2 fileと撤回した2 file、`docs/specs/`の2 file、および本artifact。
- 修正で触れた隣接範囲: staging 2文書（01・03）。
- 既承認・未変更範囲を再走査していない: **成立する。** 既存SCNのassertionを1件も変更していない。

### ラウンド3

- 全指摘の最終分類: **5節の指摘表は15行で、resolved 12件・valid 3件である。** 内訳は2系統に分かれる。
  - **`review-session.json`へ記録した11件**（ラウンド1〜3）: resolved 8件（R1-B01・R1-B02・R1-N01〜N05・R3-B01）、valid 3件（R1-N06・R2-N01・R3-N01）。
  - **sessionへ記録していない4件**（R4-B01・R4-B02・R4-N01・R4-N02）: すべてresolved。**merge直前reviewの指摘であり、予算3ラウンドを使い切った後のためsessionへは追記していない。** 記録先は5節と`git log`である。
  - 未解決のCritical/Highはない。
- 任意の危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: **縮小した危険範囲はない。** 免除条件は#1097のまま変えていない。
- 同じ範囲の予算を自動更新していない: **成立する。** 総3ラウンドで打ち切る。
- AIによる最終裁定: **approved。** ラウンド3は`npm run audit:check`が`比較基点またはH_implの完全SHAがありません`で拒否した書式不備の是正である。`H_impl`を短縮SHAで書いていた。`parseFileAudit`は40桁のみを受理する。**製品・仕様・testの差分は1行も変えていない。**

## 7. テスト結果

| 層・検査 | コマンド | シナリオ・件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---:|---:|---:|---:|---|
| 形式 | `npm run docs:format` / `npm run test:format` | 2 | 2 | 0 | 0 | pass |
| 静的検査 | `npm run quality` | 1 | 1 | 0 | 0 | pass |
| unit・integration・e2e | `npm test` | 1389 | 1373 | 0 | 16 | pass |
| 型・既存一式・配布物 | `build` / `package:check` / `architecture:check` / `trace:check` / `conformance:check` | 5 | 5 | 0 | 0 | pass |

### 7.1 変異試験

**3型で行った。** 判定を広げる変更なので、広げすぎと狭めすぎの両方を確認した。

| 型 | 3045 | 3177 | 5645 | 5742 |
|---|---|---|---|---|
| 転送行を削除 | kill | kill | kill | kill |
| 定数`true`へ置換 | kill | kill | kill | **survive** |
| `?? true`を付加 | **等価** | 等価 | 等価 | 等価 |

**`?? true`型は等価変異である。** `initialState`が`undefined`になるのはworktree不在時だけで、その場合`planWorktreeCleanup`は`already-absent`へ分岐しupstream由来の理由を評価しない。隔離repositoryで変異前後の出力が完全に一致することを実測した。他3箇所は常に`FinalizeState`であり`inspectFinalizeState`が必ずbooleanを設定する。

**定数`true`型の5742だけが生存する。** `SCN-INT-FINALIGN-013`は`src/cli.ts:5670`で先に終了するため5742を通らない。**削除防止自体は維持される** — `buildFinalizeReport`が未変異の`state.reachableFromDefaultBranch`で独立に安全判定し、`applyFinalize`が拒否するためである。

**ただし「理由も出力に残る」は全条件では成立しない。** preview後に到達性が真から偽へ変わる場合、変異体は5742の拒否理由を消し、`applyFinalize`は`src/domain/finalize.ts:848-850`の汎用TOCTOU errorを返す。**利用者に見える理由がupstream由来から汎用TOCTOUへ変わる。** 削除は起きないが診断は劣化する。

復元はすべて変更前fileの複写で行い`git checkout`を使っていない。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/cli.ts` | 入る | **`worktree finalize`の4経路すべてで、既定branchから到達できるworktreeのcleanup計画が`ready`になる。** 受理する入力は変えない |
| `test/features/integration/finalize-ignored-artifacts.feature` | 入らない | なし |
| `test/steps/finalize-ignored-artifacts-cli.steps.ts` | 入らない | なし |
| `docs/specs/15_要件追跡/00_追跡表.md` | 入らない | なし |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | なし |
| `docs/reviews/102_課題1099のcleanup計画への到達可能性配線レビュー.md` | 入らない | なし |

判断: 配布物を更新した

根拠: `src/cli.ts`はcompileされて配布される。`npm run package:check`が合格する。**受理する入力を変えないため、従来通る呼び出しは通り続ける。**

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | Step 11のPR作成後に観測する |
| reviewerがPR author・実装commit authorと異なる | codexは別provider・別contextで異なる。fableは同一providerの別contextである |
| 観測したreview commentとapprovalの件数 | Step 11で観測する |

**外部reviewerのcheckがpassでもreview commentとapprovalの実体を観測する。** 両方0件だった場合だけ`RVX-REPORTED-SUCCESS-WITHOUT-REVIEW-001`の条件に当たる。**現時点では例外を適用していない。**

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/15_要件追跡/00_追跡表.md`（8 SCNの登録）、`docs/specs/15_要件追跡/01_変更履歴.md`（1行追記）。**REQ-LC-009の本文は変えていない。**
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: **用語の追加・変更・廃止はない。** TERM-ASC-086を参照するだけである。
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: **成立する。**
- 要件・変更・SCN・テストの追跡: REQ-LC-009 → AC-LC-009 → AC-1099-01〜03 → SCN-INT-FINALIGN-006〜013 → `src/cli.ts`。`npm run trace:check`が`orphanScenarios: []`・`orphanRequirements: []`。
- `no-spec-impact`の場合の限定的根拠: 該当しない。
- UI・トークンの判断: UI無し。tokenは`not-applicable`。

## 11. 総合判定と再開地点

- 未解決Critical/High: **なし。**
- Medium/Lowの記録: R1-N01（定数`true`変異の5742生存）、R1-N06（fallback分岐の被覆喪失）を残存として記録する。
- 判定: approved
- 新しい権限が必要な事項: **なし。**
- 残存リスク: 3件。(1) **定数`true`変異が5742で生存する。** 削除防止は維持されるが、**preview後に到達性が変わる条件では診断が汎用TOCTOU errorへ劣化する**（7.1節）。(2) **trusted policy同梱で`finalizeIgnoredPathAllowlist`のmanifest不在fallback分岐の被覆がこのfeatureから失われた。** 実効allowlistは既定と同一でverdictは不変である。(3) **同種の転送漏れは構造的に残る。** `planWorktreeCleanup`の入力をobject単位で渡す形へのリファクタを回帰面の広さから見送っており、新しい呼び出しを足せば同じ漏れが起こりうる。**回帰SCNは4呼び出しを固定するが5つ目は守らない。**
- 次に許可される操作: Step 11のPR作成。
- 次回の再開地点: `pr create`。
