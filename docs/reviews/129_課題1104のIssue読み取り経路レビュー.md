# 04 レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1104 |
| ラウンド | Step 10 ラウンド1〜2 |
| 比較基点 | `5c4ec47a3b3179562279cf083b291cd150b5e896` |
| H_impl | `3151758592e11c5e38473669c0bb182cc38d48bf` |
| 比較基点の由来 | review開始時点の`origin/main`のtip。PR #1174（`v0.3.1-beta.71`のrelease bump）のmerge commitである |
| Step 10のreview session ID | `647f2098fda49da50226b9d568d6a47f6a0d813d0db111654c83632652b95ce9` |
| モード | full（Q-01がfalse） |
| 対象差分 | `src/adapters/github.ts`、`src/cli.ts`、`src/cli-usage.ts`、`test/features/integration/delivery-finalize.feature`、`test/steps/delivery-finalize.steps.ts`、`.agent-skill-chain/skills/step-04-issue-sync/SKILL.md`、`docs/specs/02_要件/03_外部連携要件.md`、`docs/specs/15_要件追跡/00_追跡表.md`。commitは`31517585` |
| 対象外 | `issue sync`の置換動作の変更。既存本文の自動merge。compare-and-set（`--expect-body-sha256`）。`docs/reviews/`のrole authority不整合（#1047） |
| 残り予算 | **1**（同一範囲で最大3ラウンド、収束後にHEADが動いたときの取り直しを1回まで。総2ラウンドで設計した） |
| ラウンド数 | 2。ラウンド1は実装差分、ラウンド2は本artifactを加えた版が対象である |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260903_131427_更新前のIssue本文を読むCLI経路を設ける |
| 仕様の所有箇所 | `docs/specs/02_要件/03_外部連携要件.md`のREQ-GH-001 |
| 成果物行数 | 製品 **+86行**（adapter +48、CLI +26、usage +12）。配布物 **+2行**（SKILL.md）。仕様 **+3 / −1行**。支援層 **+68行**（feature +11、steps +57）。**支援層/成果物 = 0.79倍** |
| 縮小の先行評価 | 3案を先に評価した。(1) 新しい`gh`呼び出しを書く案は、`issue.sync`のread-after-write検証が**既に同じ呼び出しを持っている**ため不採用（DISC-001）。(2) `issue.read`をexportしてunit scenarioを書く案は公開面を増やすため不採用。既存のgh stubを再利用するintegration scenarioで足りる。(3) compare-and-setを同時に足す案は、**読み取り経路だけで保全と禁止の両立が成立する**ため本Issueの範囲外とした |
| 実施者・日時 | reviewer（claude）、2026-09-03 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節と`review-session.json`） | advanced | claude | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerとreviewerが**同一session**である。0.2節に逸脱として開示する |

### 0.2 開示する逸脱

1. **implementerとreviewerが同一sessionである。** project choiceの`reviewer.independence.differentFrom = implementer`を満たさない。緩和は、判定の根拠をすべて機械観測（scenario結果、変異試験の赤・緑、`gh` stubのlog）に置いたことである。
2. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | https://github.com/techbeansjp-free/AGENTS.md/issues/1104 、AC-01〜AC-03 | Step 8で00を同期した。`issue validate`は`valid: true`、errors 0件 | 一次資料 |
| 既存の読み取り呼び出し | `src/adapters/github.ts`の`issue.sync` | read-after-write検証で`gh issue view --json body --jq .body`を既に実行していた | 既存コード |
| 読み取り経路の不在 | `issue create`・`validate`・`sync`・`staging`の`--help` | **4 subcommandすべてに読み取りflagが無い** | CLI出力 |
| 差分 | `5c4ec47a..31517585` | 8 file、+159 / −1行 | 既存コード |
| テスト | `npm run conformance:check`（内部で`npm test`を実行する） | `1430 scenarios (1414 passed, 16 skipped)`、失敗0 | テスト出力 |
| 仕様 | `docs/specs/02_要件/03_外部連携要件.md`ほか1 file | updated | 既存文書 |
| **自己適用** | 本Issue自身のStep 4 | **追加した`issue read`で更新前本文と`bodySha256`（`75b29143…`）を取得し、コメントへ保全してから同期した。`gh`を直接叩いていない** | 実行記録 |
| commit前candidate | 8 file | working tree clean | Git index |
| Phase A artifact | `docs/reviews/129_課題1104のIssue読み取り経路レビュー.md` | `H_impl` = `31517585`。`H_impl..H_final`の差分pathは本file 1件 | Git観測 |
| commit後external | PR、CI run、review | Step 11で観測する | 外部のimmutable証拠 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **成立する。** `issue-1104 → req-gh-001 → ac-01..03 → scn-int-github-017/018 → issue-read`の一方向である。**自己適用（本Issueの成果物を本Issueの工程で使ったこと）は承認根拠にしていない。** 判定の根拠はscenarioと変異試験である。本artifactへ自身のcommit SHAを書いていない。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_impl` = `31517585`は`H_final`のancestorであり、差分は本artifact 1 fileだけである。
- reviewer stable IDがPR author/provider観測済み`H_impl` author stable IDと異なる: **いいえ。** いずれも`adachi-tatsuru`である。9節を参照する。
- 既定branch追随を行った場合: **追随mergeを作っていない。** `origin/main`が動いたため`git reset --hard origin/main`で基点を取り直し、追跡表の衝突1件を解消した。**その解消で無関係の行を落としかけている**（DISC-003）。`比較基点..H_final`は2 commitの一直線である。

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/adapters/github.ts` | M | package | package | `issue.read` operationとoverload宣言を足した。**既存の`issue.sync`に1行も触れていない** | pass。`run("gh", ...)`への一方向。`npm run architecture:check`合格 | REQ-GH-001 / AC-01〜03 / SCN-INT-GITHUB-017・018 | **`verifyRepository`へ`read`を渡し書き込みを行わない。** `gh`のlogに`issue edit`が現れないことをscenarioが確認する。rollbackは当該blockのrevert | pass |
| `src/cli.ts` | M | package | package | `issue read`のdispatchを足した。**読み取り専用のため`--apply`も`--authorize`も要求しない** | pass。`github()`への一方向 | 同上 | 外部状態を1つも変えないため承認を要求する必要がない | pass |
| `src/cli-usage.ts` | M | package | package | usage定義を1件足した。**これが無いと`build`が落ちる**（DISC-002） | pass | 同上 | 記述だけで実行authorityを持たない | pass |
| `test/features/integration/delivery-finalize.feature` | M | package | package | scenarioを2件足した。既存scenarioを1件も書き換えていない | pass | AC-01〜03 | **既存のgh stubを再利用し実`gh`を呼ばない** | pass |
| `test/steps/delivery-finalize.steps.ts` | M | package | package | step定義を4件足した。既存step定義を書き換えていない | pass | 同上 | 同上 | pass |
| `.agent-skill-chain/skills/step-04-issue-sync/SKILL.md` | M | project | doc | 同期前の保全手順を2行足した。**保全すべき内容の判断は人が行うと明記した** | pass | REQ-GH-001 | 記述だけで実行authorityを持たない | pass |
| `docs/specs/02_要件/03_外部連携要件.md` | M | project | spec | REQ-GH-001へ読み取り経路の規定を追記した | pass | REQ-GH-001 | 同上 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | REQ-GH-001行へSCN 2件を結線した。**#1107のSCN-INT-GITHUB-016を保持している** | pass | REQ-GH-001 | 同上 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **一致する。** `git diff --name-only 5c4ec47a 31517585`が返す8 pathが上表の8行と同じである。**本artifactは`H_impl..H_final`の差分であり`比較基点..H_impl`に入らないため、個別監査の行にしない。**
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **成立する。** `issue.read`はproject policyを参照しない。保全すべき内容の判断をskill（project層）へ置き、機構（package層）へ持ち込んでいない。
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: **DISC-002の是正で`src/cli-usage.ts`と、それに依存する3 scenarioを再監査した。**

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

**実装中の発見が3件ある。**

**DISC-001。adapterは既に`gh issue view --json body --jq .body`を実行していた。** `issue.sync`のread-after-write検証である。**読み取り能力は存在しており、operationとして公開されていないだけだった。** 新しい外部呼び出しを足さずに済んだ。

**DISC-002（Medium）。新しいsubcommandにはusage定義が必須だった。** `build`が`公開CLI契約が不正です: usage定義がないsubcommandです: issue read`で落ち、`SCN-INT-README-001`・`SCN-UNIT-CLIUSAGE-012`・`SCN-UNIT-PACKAGE-016`の3件も落ちた。`src/cli-usage.ts`へ定義を足して復旧した。**公開CLI契約がREADME・help・利用案内・仕様の4箇所で照合されている。**

**DISC-003。main追随の衝突解消で無関係の行を落としかけた。** 追跡表の`REQ-LC-005`行を巻き込んで削除しており、`trace:check`が`孤立SCNです: SCN-INT-ROOTUPD-001, SCN-INT-ROOTUPD-002, SCN-INT-ROOTUPD-003`を返した。`origin/main`から原文を取り直して復元し、差分が意図した1行だけになったことを確認した。

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-INT-GITHUB-017 | `issue.read`の返り値 | `2 scenarios (2 passed)` | pass | `body`が`# 同期本文\n`、`bodySha256`が本文のsha256と一致する。変異M2（digestを固定値へ）で失敗する |
| AC-02 | SCN-INT-GITHUB-018 | `verifyRepository(..., "read")` | 同上 | pass | `READ`権限だけのstubで成功する。変異M1（`write`へ）で失敗する |
| AC-03 | SCN-INT-GITHUB-017 | 同上 | 同上 | pass | **`gh`のlogが`auth status`・`repo view`・`issue view`の3件だけであり、`issue edit`を含まない。** 変異M3（`view`を`edit`へ）で2 scenarioが失敗する |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | Issue本文を読む経路であり、権限要求を誤ると必要以上の認可を要求する | `verifyRepository`へ`read`を渡す。変異M1で`write`へ倒すと`SCN-INT-GITHUB-018`が失敗する。変異M3で`issue view`を`issue edit`へ倒すと2 scenarioが失敗する |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 保全すべき本文を取り違えないよう、内容の同一性を確認できる必要がある | `bodySha256`を返す。変異M2で固定値へ倒すと1 scenarioが失敗する |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 人が触れるUIを持たないCLIであり、出力はJSONだけである | `projectKind`が`cli` |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UI componentもthemeも持たず、色・間隔・typographyの決定を含まない | `capabilities.designTokens`が`not-applicable` |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | **本Issue自身のStep 4で実際に使い、`bodySha256`=`75b29143…`を取得して保全してから同期した。** 実Issueに対して動くことを観測している |
| 価値 | 利用者・運用上の目的を満たすか | pass | skillの「`gh`を呼ばない」と「既存本文の保全」が両立する。**本日3回この回避に頼っていた**（#1134・#1147・#1165） |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 新しい依存を足していない。`gh`は既存依存であり、呼び出し自体も既存のものである |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 00〜03の設計と実差分が一致する。`npm run trace:check`は`valid: true`、orphan 0件 |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | **保全すべき内容の判断をskillへ置き、機構へ持ち込んでいない。** 自動mergeを採らなかったのは判断の所在が消えるためである |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | `--issue`が正の整数でない場合を拒否する。`WRITE`権限と`READ`権限の両方で成功することを確認した |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | `gh`の失敗はそのまま例外になる。**外部状態を変えないためrollbackが不要である** |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | 改行を`\n`へ正規化する。`trimEnd`は**行わない**（`issue.sync`の検証側と異なる）。**本文末尾の空行も保全対象だからである** |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | `issue`番号を数値へ変換してから渡す。repositoryは`verifyRepository`が照合する |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | **読み取り専用であることを`gh`のlogで確認する。** 例外や返り値だけを見ていない |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | 書き込みを1つも行わない |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | 8 fileのrevertで完結する |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | **finding（ADV-01、Medium、record-only）** | **`issue read`は保全を強制しない。** skillへ手順を書いたが、実行するかは利用側の判断であり機構ではない。5節へ記録した |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| DISC-002 | Medium | 新しいsubcommandにusage定義が無く`build`とscenario 3件が落ちた | `公開CLI契約が不正です: usage定義がないsubcommandです: issue read` | 製品 | **修正した。** `src/cli-usage.ts`へ定義を足した | valid / resolved | なし。3件とも復旧した |
| ADV-01 | Medium | `issue read`は保全を強制しない。skillへ手順を書いたが機構ではない | 変更に強制点が1つも無い | 配布先 | **修正しない。** 何を保全すべきかの判断は人が行う。機構へ埋め込むと判断の所在が消える（00の2節で自動mergeを不採用にした理由と同じ） | valid / record-only | 手順を踏まなければ従来どおり本文が失われる。**規約であって機構ではない** |
| AFF-01 | Low | adapterが既に同じ`gh`呼び出しを持っていた（肯定的所見） | `issue.sync`のread-after-write検証 | 製品 | 対応不要。DISC-001として記録した | resolved | なし |
| AFF-02 | Low | 本Issue自身のStep 4で成果物を使えた（肯定的所見） | `bodySha256`=`75b29143…`を取得して保全した | — | 対応不要。**ただし承認根拠にしていない** | resolved | なし |

**未解決のCritical / Highは0件である。**

## 6. ラウンド固有の確認

### ラウンド1

- 対象: 実装差分`5c4ec47a..31517585`の8 file。
- 確認: 個別監査8行、AC-01〜03、肯定5観点、敵対8観点。
- 結果: blocking 0件。**DISC-002（Medium）はラウンド内で検出し是正した。** record-only 1件（ADV-01）。resolved 2件（AFF-01・AFF-02）。

### ラウンド2

- 対象: 本artifactを加えた版。
- 確認: 本artifactの記述が実観測と一致するかを全件突合する。行数、SHA、scenario件数、変異結果、`gh` logの5種を実コマンド出力と照合した。
- 結果: blocking 0件。

## 7. テスト結果

| 層・検査 | コマンド | シナリオ・件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---:|---:|---:|---:|---|
| 形式 | `npm run docs:format`、`npm run test:format` | 2 | 2 | 0 | 0 | pass |
| unit・integration・e2e（runner `cucumber-js`、dialect `en`） | `npm run conformance:check`（内部で`npm test`を実行する） | 1430 | 1414 | 0 | 16 | pass |
| 型・既存一式・配布物 | `npm run lint`・`format:check`・`typecheck`・`source:check`・`trace:check`・`architecture:check`・`skills:check`・`build`・`package:check` | 9 | 9 | 0 | 0 | pass |

**上の9本を1本ずつ実行し、それぞれの終了値で合否を取った。** 表の値は実行結果である。

**変異試験。** 3件を実施し3件ともkillした。

| ID | 変異 | 結果 | 復元後 |
|---|---|---|---|
| M1 | `verifyRepository`の`read`を`write`へ | `2 scenarios (1 passed, 1 failed)` | `2 scenarios (2 passed)` |
| M2 | `bodySha256`を固定値へ | `2 scenarios (1 passed, 1 failed)` | `2 scenarios (2 passed)` |
| M3 | **`issue view`を`issue edit`へ（読み取りを書き込みへ）** | `2 scenarios (2 failed)` | `2 scenarios (2 passed)` |

**復元は複写で行い`git checkout`を使っていない。**

**M3が2件とも落ちるのは、書き込み化がAC-01（返り値）とAC-03（logの内容）の両方を壊すためである。**

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/adapters/github.ts`、`src/cli.ts`、`src/cli-usage.ts` | **入る**（`files`が`dist/src/`を列挙する） | `issue read` subcommandが公開される。**既存subcommandの契約は変わらない** |
| `.agent-skill-chain/skills/step-04-issue-sync/SKILL.md` | **入る**（`files`が`.agent-skill-chain/skills/`を列挙する） | 同期前の保全手順が加わる |
| `docs/specs/02_要件/03_外部連携要件.md`、`docs/specs/15_要件追跡/00_追跡表.md` | **入る**（`files`が`docs/`を列挙する） | REQ-GH-001の記述と追跡が延びる |
| `test/` | 入らない | `package.json`の`files`が列挙しない |

判断: 配布物を更新した

根拠: `dist/src/`へ`issue read`が入り、`--help`と公開CLI契約に1件増える。**既存の受理・拒否は1つも変わらない。** `npm run package:check`はexit 0である。

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | **Step 11で観測する。** 本節はPR作成前に書いており、現時点で外部reviewerのcommentもapprovalも存在しない |
| reviewerがPR author・実装commit authorと異なる | いいえ。いずれも`adachi-tatsuru`である |
| 観測したreview commentとapprovalの件数 | 現時点で0件・0件 |

**適用する例外は無い。** `.agent-skill-chain/review-exceptions.json`が持つ例外は`RVX-REPORTED-SUCCESS-WITHOUT-REVIEW-001`の1件だけであり、PR作成前の本時点では条件の判定自体ができない。

**残る事実を隠さず記録する。** implementerとreviewerが同一sessionであり、approval reviewは0件である。本artifactの`approved`は**AIによる最終裁定**であって人間の独立approvalではない。

**自己適用を承認根拠にしていない。** 本Issueの成果物を本Issueの工程で使った（AFF-02）が、それは「実Issueに対して動く」ことの観測であって、正しさの根拠ではない。**正しさの根拠はscenario結果と変異試験の赤・緑である。**

## 10. 仕様整合性

- 判定: **updated**
- 更新した仕様: `docs/specs/02_要件/03_外部連携要件.md`（REQ-GH-001）、`docs/specs/15_要件追跡/00_追跡表.md`。
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: **成立する。** 新規用語を追加していない。
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: **成立する。**
- 要件・変更・SCN・テストの追跡: REQ-GH-001 → AC-GH-001 → AC-01〜03 → SCN-INT-GITHUB-017・018 → `delivery-finalize.feature` → `src/adapters/github.ts`。`npm run trace:check`は`valid: true`、orphan 0件。
- `no-spec-impact`の場合の限定的根拠: 該当しない。**新しい観測可能な振る舞い（読み取り経路）を足しているため要件本文を延ばした。**
- UI・トークンの判断: いずれも`not-applicable`。2.2節のとおり。

## 11. 総合判定と再開地点

- 未解決Critical/High: **0件。**
- Medium/Lowの記録: ADV-01（Medium、record-only）。DISC-002・AFF-01・AFF-02はresolved。
- 判定: **approved**（AIによる最終裁定。人間の独立approvalは0件であり、9節に事実として記録した）
- 新しい権限が必要な事項: **なし。** 読み取り専用である。
- 残存リスク: 2件。(1) `issue read`は保全を強制しない。**規約であって機構ではない**（ADV-01）。(2) `docs/reviews/`のrole authority不整合（#1047へ委譲）。
- 次に許可される操作: **Step 11（`pr create`）。** その後CIが緑になってからmergeする。
- 次回の再開地点: pushした後、CIの結果確認から。**mainが動いていた場合は`pr create`を先に済ませてからrebaseする**（#1172）。
