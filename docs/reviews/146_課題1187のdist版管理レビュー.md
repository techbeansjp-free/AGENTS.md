# 146 課題1187のdist版管理レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1187 |
| ラウンド | Step 10 ラウンド1 |
| 比較基点 | `5512712a0b93da30eb9071d78c5c288fc0bd6a90` |
| H_impl | `8917aa4b21f891b683e2b828df9a310ee77dd601` |
| 比較基点の由来 | worktree作成時点の`origin/main`のtip |
| モード | full（Q-01とQ-05がfalse） |
| 対象差分 | **監査対象8 file、+79 -3。** ほかに版管理下へ置いた生成物 `dist/` 61 file（**個別監査の対象外。本PRで除外を導入した**）。commitは`8917aa4b` |
| 対象外 | `prepare`/`prepack` の削除（**保護fileが内容を強制する。Phase 2**）。`build` の改名（**不要と判定**）。CIの変更（**不要と判定**）。`tsconfig.build.json` のemit設定（保護file）。`docs/reviews/`のrole authority不整合（#1047） |
| 残り予算 | **2**（同一範囲で最大3ラウンド、収束後にHEADが動いたときの取り直しを1回まで） |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260904_223551_distを版管理下へ置き承認機構つき環境でもnpx-githubが成立するようにする |
| 仕様の所有箇所 | `docs/specs/02_要件/03_外部連携要件.md`のREQ-GH-005（**改訂**） |
| 成果物行数 | 製品 **+42 -1行**（`check_file_audit.ts` +21、`check_conformance.ts` +8、`.gitignore` +6 -1、仕様 +7）。支援層 **+37 -2行**（feature +5、steps +32 -2）。**支援層/成果物 = 0.9倍**。ほかに生成物61 file |
| 縮小の先行評価 | **codexが必須とした3点のうち2点を、fableの実物確認により棄却した。** (1)`build`→`dist:build`改名は**npmのlifecycle scriptではないため自動実行経路が存在せず、防げる事故が無い**。費用は保護file 2件の変更である。(2)CIへのdist同期検査の追加は、**既存stepが「build → install --apply → `git status --porcelain`が空」の順であり既に落ちる**。(3)`prepare`/`prepack`削除は保護fileのproposal経路が要るためPhase 2へ分離した。**「必須条件」を費用と実効性で1件ずつ検証した** |
| 決裁 | **owner決裁（2026-09-04）。**「prepare に依存しない配布形にする。**許可します。最もベストな方法をfable or codexと協議して決めて**」 |
| 実施者・日時 | reviewer（claude）、2026-09-04（JST） |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節） | advanced | claude | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerとreviewerが**同一session**である。0.2節に逸脱として開示する。**配布形の設計は外部2者（codex・fable）へ諮問し、片方がもう片方を訂正した** |

### 0.2 開示する逸脱

1. **implementerとreviewerが同一sessionである。** 緩和は、**配布形の設計を外部2者へ諮問し、fableがcodexの必須条件2件を実物確認で棄却したこと**、判定の根拠をすべて実行結果に置いたことである。
2. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| dist構成 | `find dist -type f` | `.js` 58、`.map` 116、`.d.ts` 58、`.cjs` 1、`.txt` 2。合計12MB | 実行記録 |
| 追跡対象 | `git status --porcelain -uall -- dist` | **61 file・11MB**（`dist/vendor/typescript.cjs` が 9.1MB） | 実行記録 |
| ignore判定 | `git check-ignore -v` | `dist/src/cli.d.ts` と `dist/src/cli.js.map` は除外、`dist/src/cli.js` と `dist/vendor/typescript.cjs` は追跡 | 実行記録 |
| 実行bit | `git ls-files -s dist/bin/agent-skill-chain.js` | `100755` | 実行記録 |
| 決定性 | commit後の `npm run build` → `git status --porcelain -uall -- dist` | **0行** | 実行記録 |
| 衝突1 | `conformance:check` | `モード判定質問の文面が許可外のfileにあります: dist/src/domain/mode.js`。**追跡した瞬間に落ちる** | 実行記録 |
| 衝突2 | `audit:check` | 生成物61 fileが個別監査表の対象になる | 実行記録 |
| CIの既存検査 | `.github/workflows/ci.yml` | 「build → install --apply → `git status --porcelain`が空」の順。**dist乖離はここで落ちる** | 一次資料 |
| `build` の性質 | npm仕様・fableの実物確認 | **lifecycle scriptではない。** git dependencyのinstallで自動実行されない | 外部の判断 |
| テスト | `npm run conformance:check` | `1497 scenarios (1481 passed, 16 skipped)` | テスト出力 |
| commit前candidate | 監査対象8 file + 生成物61 file | working tree clean | Git index |
| Phase A artifact | `docs/reviews/146_課題1187のdist版管理レビュー.md` | `H_impl` = `8917aa4b`。`H_impl..H_final`の差分pathは本file 1件 | Git観測 |
| commit後external | PR、CI run、review | Step 11で観測する | 外部のimmutable証拠 |

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.gitignore` | M | project | project | `dist/` の一括除外をやめ、実行に不要な `.d.ts` と `.map` だけを除外した。**保護fileの `tsconfig.build.json` に触らずに追跡対象を絞れる** | 適合 | REQ-GH-005 / AC-01・AC-02 | 戻せば `dist/` は再びuntrackedになる | pass |
| `scripts/check_file_audit.ts` | M | project | project | 期待path集合から `dist/` を除いた。**生成物は同じsourceの派生であり、1行ずつ書かせると本来の変更が埋没する** | 適合。新しいimportを増やしていない | REQ-GH-005 / AC-05 / SCN-UNIT-AUDITBUMP-008 | 除外は監査対象を狭めるが、**導出の正しさはCIの `git status --porcelain` が見る** | pass |
| `scripts/check_conformance.ts` | M | project | project | モード判定質問の複製走査から `dist/` を除いた。`dist/src/domain/mode.js` は `src/domain/mode.ts` のcompile結果である | 適合 | REQ-GH-005 / AC-06 | 独立した正本ではないため、複製検出の目的を損なわない | pass |
| `docs/specs/02_要件/03_外部連携要件.md` | M | project | spec | REQ-GH-005へ生成物の監査除外を追記した | 適合 | REQ-GH-005 | 記述だけで実行authorityを持たない | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | 新規SCN 1件を結線した | 適合 | 同上 | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | project | spec | 変更履歴を1行足した | 適合 | 同上 | 同上 | pass |
| `test/features/unit/audit-bump-exclusion.feature` | M | package | package | scenario 1件を追加した。既存7件を1件も変えていない | 適合 | AC-05 | fixtureは一時repositoryで外部へ到達しない | pass |
| `test/steps/audit-bump-exclusion.steps.ts` | M | package | package | 対応するstep定義を追加した | 適合 | 同上 | 同上 | pass |

**`dist/` 61 fileは個別監査の対象外である。** 本PRで導入した除外の最初の適用であり、REQ-GH-005 と `SCN-UNIT-AUDITBUMP-008` がその根拠を持つ。

## 2. 受け入れ条件の確認

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | 実測 | `.gitignore` | `git ls-files dist` 61件 | pass | `.js` 58、`.cjs` 1、`.txt` 2 |
| AC-02 | 実測 | 同上 | `git check-ignore` | pass | `.d.ts` と `.js.map` が除外される |
| AC-03 | 実測 | 同上 | `git ls-files -s` | pass | `100755` |
| AC-04 | 実測 | — | `git status --porcelain -uall -- dist` | pass | **commit後の再buildで0行** |
| AC-05 | SCN-UNIT-AUDITBUMP-008 | `check_file_audit.ts` | `1 scenario (1 passed)` | pass | 実装commitへ `dist/` を含めても監査が合格する。変異M1でkill |
| AC-06 | conformance:check | `check_conformance.ts` | `1497 scenarios` 全合格、静的検査13件 | pass | **衝突2件を実測で検出し是正した** |
| AC-07 | 実測 | — | `git diff --name-only` | pass | `package.json`・`ci.yml`・`tsconfig.build.json`・`check_project_quality.ts` は差分に現れない |

### 2.2 不変条件

| INV ID | 内容 | 判定 | 証拠 |
|---|---|---|---|
| INV-01 | 保護fileを1つも変更しない | pass | 差分8 fileにいずれも含まれない |
| INV-02 | `dist/` はsourceから決定的に導出される | pass | commit後の再buildでdrift 0行 |
| INV-03 | CIの `git status --porcelain` 検査を弱めない | pass | `ci.yml` を変更していない |

## 3. 肯定的評価

- **外部の「必須条件」を鵜呑みにしなかった。** codexは `build` の改名とCI変更を必須としたが、fableの実物確認で**どちらも不要**と判明した。**改名は保護file 2件の変更を強制するだけで、防げる事故が存在しない。**
- **保護fileを1つも触っていない。** `tsconfig.build.json` のemit設定を変えず、`.gitignore` で追跡対象を絞った。
- **既存gateとの衝突を実測で2件見つけた。** どちらも「追跡した瞬間に必ず落ちる」形であり、読解では見落としやすい。
- **決定性を確認した。** commit後の再buildでdriftが0行である。
- **段階を分けた。** Phase 1 は保護file変更ゼロで利用者の障害を根治し、Phase 2（install-time build廃止）へproposalを要する変更を寄せた。

## 4. 敵対的評価

| 観点 | 攻撃 | 結果 |
|---|---|---|
| 監査の空洞化 | `dist/` 除外を使って製品変更を隠す | **できない。** 生成物はsourceから導出され、CIの `git status --porcelain` が乖離を落とす |
| 除外の消失 | `check_file_audit.ts` の除外を消す | 変異M1でkill |
| 生成物の手編集 | `dist/` を直接書き換えてcommitする | **CIのbuild後clean検査が落とす。** ただし**そのCI stepを弱める変更は本PRの検査対象外である。** 5節ADV-01 |
| 型定義の要求 | 利用側が `.d.ts` を必要とする | **CLI専用でexportsを持たない。** 必要になれば追跡対象を広げる |
| 二重経路 | 許可環境では `prepare` が再buildする | **成立する。** 内容は同一だが二重である。**Phase 2 で解消する。** 5節ADV-02 |
| 履歴の肥大 | TypeScript更新のたび9.1MBが積まれる | **成立する。** scripts無し配布では追跡以外の選択肢が無い。5節ADV-03 |

## 5. 指摘

| ID | severity | 事実 | 観測 | 由来 | 対処 | 判定 | 残余 |
|---|---|---|---|---|---|---|---|
| F-01 | **High** | **公開入口が承認機構つき環境で成立しなかった。** `prepare` が実行されず `dist/` が生成されない。**利用側はpnpmで、既定で承認を要求する** | 利用側報告 #1187 | 外部の判断 | `dist/` を版管理下へ置いた | valid / resolved | ADV-02 |
| DISC-1301 | **Medium** | **`checkModeQuestionText` が `dist/src/domain/mode.js` を複製として拒否する。** 追跡した瞬間に必ず落ちる | 実行記録 | 実測 | 生成物を走査対象から外した | valid / resolved | なし |
| DISC-1302 | Medium | **`build` は npm の lifecycle script ではない。** codexが必須とした改名で防げる事故が存在しない | 外部の判断（fable） | 外部review | 改名しない。**保護file 2件の変更を回避した** | valid / resolved | なし |
| DISC-1303 | Low | **CIの既存stepでdist乖離が落ちる。** codexが必須としたCI変更が不要 | 一次資料 | 実測 | `ci.yml`（保護file）を触らない | valid / resolved | なし |
| DISC-1304 | Medium | **`prepare`/`prepack` の削除は保護fileに塞がれている。** codex案Aがそれ自体でproposal経路を要求していた | 一次資料 | 実測 | Phase 1 と Phase 2 に分けた | valid / resolved | ADV-02 |
| ADV-01 | Medium | **CIのclean検査を弱める変更を、本PRの検査は見ない。** `ci.yml` は保護fileだが、検査step自体の意味を変える余地はある | 敵対評価 | 敵対評価 | **本Issueでは修正しない。** 保護fileの変更にはproposal経路が要る。**#980（配布gateの実行保証）が同型の論点を持つ** | valid / record-only | 生成物の真正性がCI step 1つに依存する |
| ADV-02 | Medium | **許可環境では `prepare` が再buildする二重経路が残る。** 内容は同一だが、tagに入ったJSと実行されるJSが別物になりうる | 敵対評価 | 敵対評価 | **Phase 2 で `prepare`/`prepack` を削除する。** 保護fileのproposal経路が要る | valid / record-only | 二重経路 |
| ADV-03 | Low | **`dist/vendor/typescript.cjs` が 9.1MB あり、TypeScript更新のたび履歴へ積まれる** | 実測 | 敵対評価 | **受容する。** scripts無し配布では追跡以外の選択肢が無い。**Git LFS は Git dependency 取得でpointerだけになるため採らない** | valid / record-only | repositoryサイズの増加 |

**未解決のCritical / Highは0件である。** F-01は解消済み、ADV-01〜03は記録のみである。

## 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `dist/` 61 file | **入る** | **承認機構つき環境でも `npx github:` が即実行できるようになる。** 本PRの目的である |
| `.gitignore` | 入らない | `files` に含まれない |
| `scripts/check_file_audit.ts`・`check_conformance.ts` | 入らない | `files` に `scripts/` が無い |
| `docs/specs/` 3件 | 入らない | project所有の仕様であり利用projectには不在 |
| `test/` 2件 | 入らない | 同上 |

判断: 配布物を更新した
根拠: `dist/` を版管理下へ置いた。**tarballの内容は変わらない**（`files` は元から `dist/bin/` と `dist/src/` を含む）。**変わるのは、buildせずに取得したtreeがそのまま実行可能になることである。**

## 6. ラウンド固有の確認

### ラウンド1

- 対象: 監査対象8 file。生成物61 fileは除外の最初の適用である。
- 確認: 個別監査8行、AC-01〜07、INV-01〜03、肯定5観点、敵対6観点、変異1件。
- 結果: blocking 0件。record-only 3件（ADV-01〜03）。resolved 5件（F-01・DISC-1301〜1304）。

## 7. テスト結果

**表を書く前に1本ずつ実行した。**

| 層・検査 | コマンド | 件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---|---:|---:|---:|---|
| unit・integration・e2e | `npm run conformance:check`（内部で`npm test`を実行する） | 1497 | 1481 | 0 | 16 | pass |
| 静的検査 | `lint`・`format:check`・`typecheck`・`source:check`・`trace:check`・`architecture:check`・`project:quality`・`cli:check`・`workflow:check`・`package:check`・`skills:check`・`docs:format`・`test:format` | 13 | 13 | 0 | 0 | pass |
| 監査 | `npm run audit:check` | 1 | 1 | 0 | 0 | pass |

**初回の `conformance:check` は2度落ちた。** 1度目は `checkModeQuestionText` の複製検出、2度目は新規SCNの追跡表未結線である。いずれも是正して全合格を確認している。

### 7.1 変異試験

| ID | 変異 | 結果 |
|---|---|---|
| M1 | `check_file_audit.ts` の `dist/` 除外を消す | kill |

**1件kill。** 復元後に `SCN-UNIT-AUDITBUMP-00` 系8件の再実行で緑を確認している。

## 8. 総合判定と再開地点

**approved。** 未解決のCritical / Highは0件である。再開地点は実装計画1節の順序表。
