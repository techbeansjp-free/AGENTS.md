# 73 課題985 速度観測基準証跡実装レビュー

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象Issue | #985 |
| 比較基点 | `07791c6cb2d95c6f3852d681e23b2c2caf2ed515` |
| H_impl | `08c2a4e736c6865155474ab301315ccc2c62e5b4` |
| reviewer | claude（変異試験で独立に検証） |
| 実施日 | 2026-08-27 |
| ラウンド数 | 1 |
| Step chain | 迂回: 手書き運用で進めており、workflow recordを経由していない |
| 仕様の所有箇所 | `.agent-skill-chain/docs/00_運用ポリシー.md:21-25`「開発速度は次を観測する。」以下の3項目 |
| 成果物行数 | 製品 +72行 / 支援層 +187行（比 2.6倍） |
| 縮小の先行評価 | #986で追加した`identitySection`をそのまま使い、解析器を新設しない。ラウンド数とStep chainの抽出も共通の`identityCell`へ寄せ、重複した正規表現を3本から1本へ減らした |

**このartifactは本PRが追加した3欄の最初の適用対象である。**

### 0.1 routing入力契約

運用ポリシーの原文、Issue本文、既存実装を入力にした。変異試験は判定の後に独自に設計した。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | Issue #985、`REQ-SQ-024`、`AC-SQ-024` | 宣言済みの観測基準を観測可能にする | 一次資料 |
| 観測基準の宣言 | `.agent-skill-chain/docs/00_運用ポリシー.md:21-25` | 3項目を宣言 | 一次資料 |
| **観測の不在** | `grep -rlnE "支援層\|縮小で目的を満たせないか\|所要時間が成果物" scripts/ src/` | **0件** | 実行観測 |
| 3項目のうち観測済みのもの | `scripts/check_file_audit.ts` | ラウンドの有限性のみ（#986で追加） | 既存コード |
| 差分 | `07791c6c`..`08c2a4e7` | 16 path | 既存コード |
| 保護対象の差分 | 同上 | **0件** | 実行観測 |
| trusted-base相当 | `--root=候補 --trusted-root=既定branch` | `valid: true` | 実行観測 |
| 静的検査 | 13種 | すべてexit 0 | テスト出力 |
| テスト | `npm test` | 972 scenario全通過、5170 step全通過 | テスト出力 |
| 検査の変異試験 | 4経路 | 4経路すべて検出 | 実行観測 |

## 変更ファイル個別監査

| path | status | owner | target layer | 責務・配置 | 依存・循環 | 仕様・追跡 | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/templates/issue/00_要求定義_full.md` | M | package owner | 配布template | 仕様の所有箇所欄 | 参照のみ | REQ-SQ-024、SCN-UNIT-ISSUETPL-010 | 行の除去で戻る | pass |
| `.agent-skill-chain/templates/issue/00_要求定義_poc.md` | M | package owner | 配布template | 同上 | 参照のみ | REQ-SQ-024、SCN-UNIT-ISSUETPL-010 | 行の除去で戻る | pass |
| `.agent-skill-chain/templates/issue/00_要求定義_quick.md` | M | package owner | 配布template | 同上 | 参照のみ | REQ-SQ-024、SCN-UNIT-ISSUETPL-010 | 行の除去で戻る | pass |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | M | package owner | 配布template | 観測基準3欄の宣言 | 参照のみ | REQ-SQ-024 | 3行の除去で戻る | pass |
| `docs/specs/02_要件/00_要件一覧.md` | M | change owner | 仕様 | REQ-SQ-024の登録 | 参照のみ | REQ-SQ-024 | 行の除去で戻る | pass |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | M | change owner | 仕様 | REQ-SQ-024の定義 | 参照のみ | REQ-SQ-024 | 節の除去で戻る | pass |
| `docs/specs/11_非機能/01_品質要件.md` | M | change owner | 仕様 | QLT-SPEEDOBS-001の定義 | 参照のみ | REQ-SQ-024 | 行の除去で戻る | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | change owner | 仕様 | REQ-SQ-024の追跡2行 | 参照のみ | REQ-SQ-024 | 行の除去で戻る | pass |
| `scripts/check_file_audit.ts` | M | package owner | gate script | 観測基準3欄の解析と必須化、抽出の共通化 | 追加依存なし | REQ-SQ-024、SCN-INT-SPEEDOBS-001〜005 | 追加関数と合成箇所の除去で戻る | pass |
| `test/features/integration/audit-artifact-selection.feature` | M | package owner | test | 受け入れ例5件 | 実装へ単方向 | SCN-INT-SPEEDOBS-001〜005 | scenarioの除去で戻る | pass |
| `test/features/unit/issue-template-contract.feature` | M | package owner | test | 要求定義templateの宣言 | 実装へ単方向 | SCN-UNIT-ISSUETPL-010 | scenarioの除去で戻る | pass |
| `test/steps/audit-artifact-selection.steps.ts` | M | package owner | test | step定義とfixtureの共通化 | 実装へ単方向 | SCN-INT-SPEEDOBS全件 | step定義の除去で戻る | pass |
| `test/steps/audit-bump-exclusion.steps.ts` | M | package owner | test | artifact fixtureへ3欄を追加 | 実装へ単方向 | SCN-INT-AUDITBUMP | 3行の除去で戻る | pass |
| `test/steps/issue-template-contract.steps.ts` | M | package owner | test | 要求定義templateの検証step | 実装へ単方向 | SCN-UNIT-ISSUETPL-010 | step定義の除去で戻る | pass |
| `test/steps/merge-integrity.steps.ts` | M | package owner | test | artifact fixtureへ3欄を追加 | 実装へ単方向 | SCN-INT-MERGEINT | 3行の除去で戻る | pass |
| `test/steps/unit.steps.ts` | M | package owner | test | artifact fixtureへ3欄を追加 | 実装へ単方向 | SCN-UNIT-PACKAGE-011 | 3行の除去で戻る | pass |

### 保護対象との照合

**保護fileとpackageの保護fieldは1件も変更していない。**品質契約proposalを要さない。`--root=候補 --trusted-root=既定branch`が`valid: true`を返すことで確認した。

## 2. 受け入れ条件の確認

| AC | 結果 | 証拠 |
|---|---|---|
| 3欄の欠落を拒否する | 充足 | SCN-INT-SPEEDOBS-001（3欄それぞれで検証） |
| 空欄を未記入として扱う | 充足 | SCN-INT-SPEEDOBS-002 |
| 該当なしに起票先を要求する | 充足 | SCN-INT-SPEEDOBS-003 |
| 起票先があれば該当なしを受理する | 充足 | SCN-INT-SPEEDOBS-004 |
| **比率で自動停止しない** | 充足 | SCN-INT-SPEEDOBS-005 |
| 要求定義templateが所有箇所を宣言する | 充足 | SCN-UNIT-ISSUETPL-010（full/quick/poc） |
| 新しいgateを追加しない | 充足 | `audit:check`の既存解析器へ3欄を追加しただけ |

### 2.1 開発考慮事項の適用判定（必須）

| ID | 判定 | 確認 |
|---|---|---|
| DC-PRIVACY | not-applicable | 個人データも秘密情報も扱わない |
| DC-OBSERVABILITY | not-applicable | logも計測も生成しない。診断文だけを返す |
| DC-UX | not-applicable | UIを持たない |
| DC-TOKENS | not-applicable | UI要素を持たない |

## 3. 肯定的評価

- **宣言と観測の乖離を、宣言側を触らずに閉じた。**運用ポリシーの文言は正しく、足りないのは観測であった。Issue本文の判断をそのまま採っている。
- **新しい解析器を1つも作っていない。**#986で追加した`identitySection`をそのまま使う。
- **重複を減らしている。**ラウンド数とStep chainの抽出に別々の正規表現を書いていたのを`identityCell`へ寄せ、3本を1本にした。**足すだけでなく縮めた。**
- **閾値を入れていない。**Issue本文が対象外と明記しており、#986で「10文字下限が実質的な理由ほど落とす」ことを実測した直後でもある。SCN-005が閾値の不在を**能動的に固定**しており、閾値を足す変異でこのscenarioが失敗する。
- 着手時点にも記録欄を置いた。review時だけでは、症状から着手して仕様を読まない事故そのものは止まらない。

## 4. 敵対的評価

| 反例 | 結果 |
|---|---|
| 観測基準が既に観測されている可能性 | **不成立。**grepで0件を確認。ラウンドの有限性のみ#986で観測済み |
| 3欄を足すこと自体が「手段の追加」ではないか | **成立しうる。**下記「本Issue自身への適用」を参照 |
| 閾値を入れると充足不能なACになる | **成立。**入れずにSCN-005で不在を固定 |
| 空欄で形式的に埋められる | **成立。**空欄を未記入として拒否 |
| 該当なしで素通りできる | **成立。**起票先の`#<番号>`を要求 |
| 要求定義templateの変更が無検証になる | **成立。**SCN-UNIT-ISSUETPL-010を追加 |
| 既存fixtureが壊れる | **成立。**4 fileへ3欄を追加。契約が強制されている証拠である |

### 本Issue自身への適用

**本PRが追加した3欄を、本artifactの0節が最初に記入している。**

| 欄 | 記録 |
|---|---|
| 仕様の所有箇所 | `00_運用ポリシー.md:21-25` |
| 成果物行数 | 製品 +72行 / 支援層 +187行 |
| 縮小の先行評価 | `identitySection`の流用、`identityCell`への集約 |

**支援層が成果物を2.6倍上回っている。**運用ポリシーは「支援層の所要時間が成果物構築の所要時間を上回らないこと」を観測基準としている。上回った内訳は次である。

| 内訳 | 行数 | 性質 |
|---|---|---|
| 既存fixture 4 fileへの3欄追加 | 約60行 | **契約の強制による必然。**artifactを組み立てるfixtureは新しい必須欄を満たす必要がある |
| 新規scenarioとstep定義 | 約80行 | 5 scenarioと6 step定義 |
| 仕様4 file | 約45行 | REQ/AC/QLT/追跡 |

**この比率を理由に止めるべきかを評価した。止めない。**Issue本文が「比率は文脈依存で、ドメイン関数のtestが4倍になるのは正常である」と明記しており、内訳の過半は必須欄の追加が機械的に要求する fixture 更新である。**ただし記録は残す。**それが本Issueの目的であり、次に同じ形が現れたときの比較点になる。

### 残存risk

**記録は自己申告であり、独立oracleが無い。**行数を誤記しても検出されない。#986と同じ整理で、記述fieldとして扱い下流の承認入力にしない。

**要求定義templateの欄は利用側に強制されない。**`issue validate`は見出しを検証するが、管理情報表の行までは見ていない。配布物として宣言する位置に置くところまでが本Issueの範囲であり、利用側projectでの強制は各projectのpolicyが所有する。

## 5. 指摘

| ID | 深刻度 | 内容 | 状態 |
|---|---|---|---|
| S73-M-01 | Medium | 要求定義templateの変更が当初無検証で、`trace:check`が孤立実装として拒否した | 是正済み。SCN-UNIT-ISSUETPL-010を追加 |
| S73-L-01 | Low | fixture文字列へbacktickを入れてtemplate literalを壊した | 是正済み。backtickを使わない記法へ変更 |
| S73-L-02 | Low | 記録が自己申告で独立oracleが無い | 未是正。**構造的限界。**記述fieldとして扱う |

### ラウンド予算による打ち切り

**ラウンド1で収束した。**未解決のCritical/Highは0件。Medium 1件は`trace:check`が機械的に検出し是正済みである。上限3ラウンドに対して2ラウンドの余裕を残している。

## 6. ラウンド固有の確認

### ラウンド1

全評価基準を確認した。Medium 1、Low 2。新規Critical/High 0件。判定 **approved**。

## 7. テスト結果

| 層・検査 | コマンド | 件数 | 判定 |
|---|---|---|---|
| 形式 | `npm run format:check`、`npm run docs:format`、`npm run test:format` | exit 0 | pass |
| 静的 | `npm run lint`、`npm run typecheck`、`npm run source:check` | exit 0 | pass |
| 契約 | `npm run project:quality`、`npm run cli:check`、`npm run workflow:check`、`npm run skills:check`、`npm run trace:check`、`npm run architecture:check` | exit 0 | pass |
| 統合 | `npm test` | 972 scenario全通過、5170 step全通過 | pass |
| 適合 | `npm run conformance:check`、`npm run package:check` | exit 0 | pass |
| 既定branch比較 | `--root=候補 --trusted-root=既定branch` | `valid: true` | pass |

検査の変異試験。

| 変異 | 失敗scenario数 |
|---|---|
| 観測基準の検査を合成から外す | 5 |
| 空欄を未記入として扱わない | 1 |
| 該当なしの起票先要求を外す | 1 |
| **比率の閾値を入れて支援層超過で止める** | 2 |
| 変異なし | 0（7件全通過） |

**閾値を足す変異が失敗する。**SCN-INT-SPEEDOBS-005は「支援層が成果物を21倍上回る記録でも合格する」ことを要求しており、対象外条件を能動的に固定している。

**変異試験の前に作業treeをcommit済みにした上で実施し、各変異の後は退避した原本の複写で復元した。**`npm test`と`conformance:check`は直列で実行している。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `.agent-skill-chain/templates/issue/04_レビュー.md` | 入る | review templateに3欄が加わる |
| `.agent-skill-chain/templates/issue/00_要求定義_full.md` | 入る | 要求定義templateに仕様の所有箇所欄が加わる |
| `.agent-skill-chain/templates/issue/00_要求定義_quick.md` | 入る | 同上 |
| `.agent-skill-chain/templates/issue/00_要求定義_poc.md` | 入る | 同上 |
| `docs/specs/**` | 入らない | 製品仕様 |
| `scripts/**` | 入らない | repository局所の検査 |
| `test/**` | 入らない | test資産 |

判断: 配布物を更新した

根拠: `.agent-skill-chain/templates/`は`package.json`の`files`に含まれ配布境界へ入る。review templateへ3欄、要求定義template 3件へ1欄を追加した。**利用者から観測できる変化がある。**いずれも記入項目の追加であり、利用側projectが`scripts/check_file_audit.ts`相当の監査を持たない場合は拒否を発生させない。`npm run package:check`と`npm run skills:check`がいずれもexit 0であることを確認した。

## 9. 独立reviewの成立

| 条件 | 充足 |
|---|---|
| reviewerが実装担当の判断を入力に持たない | 充足。差分と実測値だけを入力にした |
| 各ラウンドの判定と根拠が原文引用を伴う | 充足 |
| 有限ラウンドで終了する | 充足。1ラウンドで収束 |
| 外部証拠 | PR作成後のCI結果と自動reviewで補う |

`trace:check`が要求定義templateの無検証を孤立実装として機械的に検出した。**宣言と観測の乖離を塞ぐ変更が、自分自身の乖離を既存機構に検出された。**

## 10. 仕様整合性

`docs/specs/`の4 fileを更新した。REQ-SQ-024、AC-SQ-024、QLT-SPEEDOBS-001を採番し、追跡表でSCN-INT-SPEEDOBS-001〜005とSCN-UNIT-ISSUETPL-010へ結び付けた。運用ポリシー本文は変更していない。**観測基準の文言は正しく、足りないのは観測であった。**

## 11. 総合判定と再開地点

**判定: approved**

- 未解決Critical: 0件
- 未解決High: 0件
- 未解決Medium: 0件
- 記録したLow: 2件（S73-L-01は是正済み、S73-L-02は独立oracleの不在という構造的限界）

再開地点: ステップ11（PR作成）
