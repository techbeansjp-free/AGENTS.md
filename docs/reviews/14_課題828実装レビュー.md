# 課題828 実装レビュー

> 状態: `ready-for-pr`。setup・update・deleteの隔離環境検証と、それを成立させるlifecycle安全性の強化を、merge済みdefault branch headを比較基点として固定した内部監査証拠である。独立reviewの承認は自己申告せず、CIとPR reviewの外部証拠で確定する。

## 判定

| 項目 | 値 |
|---|---|
| 状態 | ready-for-pr |
| 比較基点 | `43a2d7c9d944c724a237642a19553d7f1e3cfc6a` |
| H_impl | `bf602388735c96d85bdf4302227bc6ad7830f97d` |
| H_impl tree | `2542403f39799d489aa853c7fb1c688610a3dda0` |
| Critical | 0 |
| High | 0 |
| Medium | 0 |
| Low | 0 |
| role分離 | implementerはCodex `gpt-5.6-sol`（reasoning high、service tier default）、coordinatorとreviewerはClaude Opus 5。同一scopeでidentityが異なる |

実際の一時隔離ディレクトリで`install` / `update` / `delete`を順に実行し、利用側資産を侵害せずpackage管理資産だけを扱うことを検証する。検証で不足が判明した安全性だけをdomainへ追加した。

## 開発考慮事項

| ID | 考慮事項 | 判定 | 理由 | 要求・確認証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | 利用側資産の削除という不可逆操作を扱うため、path traversal、symlink、record改ざん、TOCTOUの反例を先に固定する | SCN-INT-LIFECYCLE-004〜006 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 部分失敗時の未処理対象と再実行手順を運用判断へ使う | `removed` / `pending` / `recovery`、SCN-INT-LIFECYCLE-007 |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | applicable | previewの結果だけで削除可否を判断させるため、保持理由と未処理対象を表示契約とする | `retained`、`pending`、SCN-E2E-LIFECYCLE-002 |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | 視覚componentとlayoutを所有せずtoken適用対象が存在しない | projectKindがcli、UI source変更なし |

## 変更ファイル個別監査

| path | status | owner | target layer | 責務・配置 | 依存・循環 | 仕様・追跡 | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/lifecycle.ts` | M | package maintainer | domain | managed asset recordのpath・hash・Unicode正規化・containment・重複を削除前に全件検証し、symlinkを除外し、部分失敗を報告する | `src/lib/*`だけへ依存。公開export名と引数契約は不変 | AC-828-01〜05 | 削除直前の再確認で不一致なら削除せず`retained`へ移す。失敗時はrecordを保持して再実行可能にする | pass |
| `test/features/integration/lifecycle-isolation.feature` | A | package maintainer | test | 隔離ディレクトリでの正常系、利用側資産保持、改ざん、traversal、TOCTOU、部分失敗、再実行、Unicode・読取専用の反例を固定する | featureからstep定義への参照のみ | SCN-INT-LIFECYCLE-001〜009 | 反例が消えると削除安全条件の検出を失うため実装と同時に戻す | pass |
| `test/features/e2e/lifecycle-isolation-cli.feature` | A | package maintainer | test | CLIのinstall・update・deleteと、deleteの既定preview非writeを検証する | featureからstep定義への参照のみ | SCN-E2E-LIFECYCLE-001〜002 | 同上 | pass |
| `test/features/integration/lifecycle-worktree.feature` | M | package maintainer | test | 既存7 scenarioのIDを`SCN-INT-LIFECYCLEBASE-*`へ改番し、新規`SCN-INT-LIFECYCLE-*`との衝突を避ける | featureからstep定義への参照のみ | SCN-INT-LIFECYCLEBASE-001〜007 | 改番のみで検証内容は不変。IDを戻せば旧表記へ復帰する | pass |
| `test/steps/lifecycle-isolation.steps.ts` | A | package maintainer | test | 上記featureのstep実装。`mktemp`配下の隔離ディレクトリだけを変更する | domain exportと一時fixtureだけへ依存 | SCN-INT/E2E-LIFECYCLE系 | 実workspace、実remote、他worktreeを変更しない | pass |
| `docs/specs/12_運用保守/00_運用設計.md` | M | repository maintainer | 仕様 | lifecycleの隔離検証方針、fail closed条件、部分失敗時の復旧手順を成立中の契約として記録する | 仕様から実装への参照のみ | AC-828-01〜05 | 節を戻せば旧記述へ復帰する | pass |
| `docs/specs/04_機能/00_ワークフローv0.3.md` | M | repository maintainer | 仕様 | 改番した既存SCN参照を実際のIDへ同期する | 仕様からtestへの参照のみ | SCN-INT-LIFECYCLEBASE-* | 参照を戻せば旧表記へ復帰する | pass |
| `docs/specs/02_要件/01_課題834要件.md` | M | repository maintainer | 仕様 | 同上。改番したSCN参照を同期する | 仕様からtestへの参照のみ | SCN-INT-LIFECYCLEBASE-* | 同上 | pass |
| `docs/specs/15_要件追跡/01_課題834追跡.md` | M | repository maintainer | 仕様 | 同上。追跡表のSCN参照を同期する | 追跡表からtestへの参照のみ | SCN-INT-LIFECYCLEBASE-* | 同上 | pass |
| `docs/specs/15_要件追跡/06_課題828追跡.md` | A | repository maintainer | 仕様 | AC、enforcement point、反例SCN、証拠、rollbackを対応付ける | 追跡表から実装とtestへの片方向参照 | AC-828-01〜05 | fileを削除すれば追跡が消えるため索引と同時に戻す | pass |
| `docs/specs/00_仕様書構成/00_仕様書索引.md` | M | repository maintainer | 仕様 | 追跡文書の索引へ課題828追跡を追加する | 索引から追跡文書への片方向link | AC-828-01 | 行を戻せば旧索引へ復帰する | pass |

Gitの`43a2d7c9d944c724a237642a19553d7f1e3cfc6a..bf602388735c96d85bdf4302227bc6ad7830f97d`に含まれる11 pathと表の11行は重複なし・欠落なしで一致する。

## 肯定・敵対レビュー

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ | pass | 隔離ディレクトリでinstall・update・deleteを順に実行し、期待したpackage資産だけが変化する |
| 価値・実現可能性 | pass | agent-skill-chain以外のskill・文書・設定・consumer所有資産がdelete後も保持される |
| 整合性 | pass | `00_運用ポリシー.md`の「更新・アンインストールは利用側所有資産と変更済み資産を保持する」と一致する |
| 保守性 | pass | 公開export名と引数契約を変えず、戻り値へのfield追加だけで部分失敗を表現した |
| 反例・失敗経路 | pass | 不正JSON、objectでない`files`、不正hash、絶対path、`..`、非NFC path、重複portable pathを削除開始前に拒否する |
| 境界値・悪用 | pass | symlinkはhash一致でも削除対象にせず、境界外移動を拒否する |
| 安全性・データ損失 | pass | 削除直前の再確認で変更済み資産を`retained`へ移し、部分失敗ではrecordを保持して再実行可能にする |
| rollback | pass | 未処理pathと再実行手順を`recovery`で返す |
| 範囲漏れ | pass | `src/cli.ts`、protected file、package scriptを変更していない |

### 敵対観点の残論点

既存7 scenarioのIDを`SCN-INT-LIFECYCLE-*`から`SCN-INT-LIFECYCLEBASE-*`へ改番した。conformance bindingは同IDを参照していないためbinding影響はないが、仕様3件の参照は同一変更内で同期した。ID改番はtestの検証内容を変えない。

## テスト結果

| 層・検査 | コマンド | 成功 | 失敗 | 判定 |
|---|---|---:|---:|---|
| project設定・静的品質 | `project:quality`、`lint`、`format:check`、`typecheck`、`source:check` | 5 | 0 | pass |
| 全BDD | `npm test` | 330 | 0 | pass |
| 配布・追跡・監査 | `build`、`docs:format`、`test:format`、`trace:check`、`architecture:check`、`conformance:check`、`package:check` | 7 | 0 | pass |

`audit:check`は本artifactを追加したH_finalで実行する。

## 停止点

- H_impl: `bf602388735c96d85bdf4302227bc6ad7830f97d`。
- mergeはCIとreviewの外部証拠が揃った時点で別操作として行う。
- release、branch削除、worktree cleanupは本Issueの終了処理では実施しない。

H_finalはこのreview artifactだけを追加するcommitとし、以後このtracked artifactを変更しない。
