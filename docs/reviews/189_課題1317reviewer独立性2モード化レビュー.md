# 課題1317 reviewer独立性2モード化レビュー

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 3（review session） |
| 対象SHA・文書ダイジェスト | H_impl は本表の`H_impl`行の値 |
| 比較基点 | `a21cc2095f64079f11e046107a7b1cbcbcdb2e58` |
| H_impl | `ef3eb5a0358af78e42553b22cd406be3423aab3f` |
| 対象差分 | 比較基点 `a21cc209`。監査対象は29 path |
| 対象外 | 6経路E2E（full / quick / poc / merge disabled / assisted / automatic）。**ownerの指示で後続タスク** |
| 残り予算 | round 3を実施。上限4に対し残り1 |
| ラウンド数 | 3 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260910_181607_reviewer独立性を2モード化しStep-10のPR前完了を明示する |
| 仕様の所有箇所 | `.agent-skill-chain/docs/00_運用ポリシー.md` のrole分離節。原文「implementerと最終reviewerは異なるagent identityかつ異なるcontextとし、独立性が不明ならPR、merge、finalizeをfail-closedで停止する」 |
| 成果物行数 | 製品 `src/` と配布build `dist/`、支援層は test と配布文書。`git diff --numstat a21cc209..ef3eb5a0` で再現する |
| 縮小の先行評価 | 既存の`independence`枠へmodeを載せ、新しい機構を足していない。round 3の是正も既存の`compareTrustedPolicy`・`observeMergeReviewEvidence`へ条件を1つ足すだけに留めた |
| 実施者・日時 | implementer: Claude（本session）。reviewer: codex（別provider process）およびCodeRabbit。2026-09-10 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類 | critical | codex | provider既定 | 未解決時は停止 | implementerはClaude、reviewerはcodexで別provider process。契約でread-onlyと非編集を明示し、`git status`は前後ともclean |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | Issue #1317、AC-01〜AC-07 | 7件すべてにSCNが対応する | テスト出力 |
| 差分 | `a21cc209..ef3eb5a0` | 監査対象29 path | 既存コード |
| テスト | `npm test` | 1754 scenarios / 1738 passed / 16 skipped / **0 failed** | テスト出力 |
| 仕様 | `docs/specs/15_要件追跡/00_追跡表.md` ほか配布文書 | updated | 既存文書 |
| commit前candidate | 本節の変更ファイル個別監査表 | 29 path | Git index |
| Phase A artifact | `docs/reviews/` 配下の本artifact 1 file | H_impl..H_final は本artifactだけ | Git観測 |
| review session | .agent-skill-chain/tmp/issues/20260910_181607_reviewer独立性を2モード化しStep-10のPR前完了を明示する | round 1〜3、anchorは`a21cc209`と`c74b77f4` | Git観測 |

**Phase Bの外部証拠を本artifactへ書かない。** PR number、Actions run ID、immutable review IDはPR作成後にしか存在せず、`review evidence`とdelivery stateがappend-onlyで保持する。

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **確認した。**
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: **確認した。**
- reviewerの独立性が`merge.reviewIndependence`の要求水準を満たす: reviewerはcodexおよびCodeRabbitで、implementerと別identity・別context。**本変更が定義する`context-isolated`を満たす。**
- （Phase B。ここでは判定しない）trusted providerが観測したPR/CI/reviewが`H_final`へ一致することは、PR作成後に`review evidence`が観測して記録する。
- 既定branch追随を行った場合: **していない。** `比較基点`は着手時の`origin/main` tipである。

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/00_利用案内.md` | M | package owner | package | 配布される利用案内。起動口の指針を所有する | pass | AC-07の周辺 | 記述のみ | pass |
| `.agent-skill-chain/docs/00_運用ポリシー.md` | M | package owner | package | 独立性の正本。機構と記録の境界も所有する | pass | AC-01〜AC-05 | 宣言を戻せば従来へ戻る | pass |
| `.agent-skill-chain/schemas/project-policy.schema.json` | M | package owner | package | policyの受理範囲 | pass | AC-01・AC-02 | 任意keyのため既存policyは不変 | pass |
| `.agent-skill-chain/skills/step-10-review/SKILL.md` | M | package owner | package | Step 10の契約 | pass | AC-01〜AC-04 | 記述のみ | pass |
| `.agent-skill-chain/skills/step-11-pr/SKILL.md` | M | package owner | package | Step 11の契約 | pass | AC-01 | 記述のみ | pass |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | M | package owner | package | review成果物の書式。Phase A/Bの境界を所有する | pass | 循環の解消 | 記述のみ | pass |
| `docs/reviews/189_課題1317reviewer独立性2モード化レビュー.md` | A | project | evidence | 本artifact。round 3の是正commitが後に来るため監査対象へ入る | pass | 全AC | 記述のみ | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | 追跡表 | pass | 新規SCN 9件を登録 | 記述のみ | pass |
| `src/cli.ts` | M | package owner | package | trusted policyからmodeを解決し、mergeの実経路へ渡す配線 | pass | AC-01・AC-02・AC-07 | 未配置だけ既定、読めるが不正なら停止 | pass |
| `src/domain/delivery.ts` | M | package owner | package | merge認可と拒否診断 | pass | AC-01・AC-02 | 宣言で強制点を復元できる | pass |
| `src/domain/enforcement.ts` | M | package owner | package | trusted policyの弱化比較 | pass | AC-01・AC-02 | 引き上げは通し弱化だけ拒否する | pass |
| `src/domain/lifecycle.ts` | M | package owner | package | `doctor`の報告 | pass | AC-06 | 門を足さない | pass |
| `src/domain/policy.ts` | M | package owner | package | policyの検証とmode解決 | pass | AC-01・AC-02 | 不正値を拒否する | pass |
| `src/domain/review.ts` | M | package owner | package | review evidenceの判定 | pass | AC-03・AC-04 | 未知値は最も強いmodeで検証する | pass |
| `src/domain/routing-independence.ts` | M | package owner | package | role独立性の判定 | pass | AC-05 | identity比較は維持する | pass |
| `src/types.ts` | M | package owner | package | 共有型 | pass | AC-01・AC-02 | 任意fieldである | pass |
| `test/features/e2e/workflow-step-enforcement-cli.feature` | M | project | project | mergeの実経路のscenario | pass | AC-01・AC-02 | なし | pass |
| `test/features/integration/delivery-finalize.feature` | M | project | project | merge認可のscenario | pass | AC-01・AC-02 | なし | pass |
| `test/features/integration/lifecycle-isolation.feature` | M | project | project | doctorのscenario | pass | AC-06 | なし | pass |
| `test/features/integration/risk-policy-migration.feature` | M | project | project | review evidence CLIのscenario | pass | AC-07 | なし | pass |
| `test/features/unit/review-policy-package.feature` | M | project | project | review evidenceのscenario | pass | AC-03・AC-04 | なし | pass |
| `test/features/unit/risk-policy.feature` | M | project | project | trusted比較のscenario | pass | AC-01・AC-02 | なし | pass |
| `test/features/unit/routing-independence.feature` | M | project | project | role独立性のscenario | pass | AC-05 | なし | pass |
| `test/steps/delivery-finalize.steps.ts` | M | project | project | 同上のstep | pass | AC-01・AC-02 | なし | pass |
| `test/steps/lifecycle-isolation.steps.ts` | M | project | project | 同上のstep | pass | AC-06 | なし | pass |
| `test/steps/risk-policy.steps.ts` | M | project | project | review evidence CLIとtrusted比較のfixture | pass | AC-01・AC-04・AC-07 | なし | pass |
| `test/steps/routing-independence.steps.ts` | M | project | project | 同上のstep | pass | AC-05 | なし | pass |
| `test/steps/unit.steps.ts` | M | project | project | 同上のstep | pass | AC-03・AC-04 | なし | pass |
| `test/steps/workflow-step-enforcement.steps.ts` | M | project | project | mergeの実経路のharnessとstep | pass | AC-01・AC-02 | なし | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **確認した。** `check_file_audit.ts`の`auditedExpected`が生成物を除くため照合対象は29である。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **確認した。**
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: round 3で是正した8件は該当fileと呼び出し側だけを再確認し、変異試験で強制主体を確かめた。

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | **既存の`self-review`検査が偽陽性だった。** project policyが読めずCLIが非0終了していただけで、独立性の強制点を一度も通っていなかった | 強制点の実在が未検証だった | なし | fixtureへ`project-policy.json`を置き`actor-independent`を宣言させた | SCN-INT-RISK-024の14 variantが本来の理由で通過 | updated | pass |
| DISC-002 | `buildReviewEvidence`の戻り値にモードが無く、下流が判定を再構成するとモードが落ちる | `actor-independent`で拒否したものが既定で承認され得る | なし | 戻り値へ`independenceMode`を含めた | SCN-INT-RISK-024 | updated | pass |
| DISC-003 | **`pr merge`の実経路は`authorizeMerge`の手前で別のselectorを通る。** そこがmodeを受け取らず、既定modeが実経路では到達不能だった | 報告された停止が解けていなかった | なし | modeを必須引数にしてtrusted policyから渡した | SCN-E2E-WFSTEP-051。両方向の変異を殺すことを実測した | updated | pass |
| DISC-004 | 展開先の解決に失敗する導入先で`doctor`が停止した。**私が入れた回帰である** | 他の診断まで返せなくなる | なし | 報告の計算が失敗しても他の診断を返し、判定不能であることを述べる | SCN-INT-LIFECYCLE-043 | updated | pass |
| DISC-005 | `merge`はeffective policy合成で丸ごと差し替わる。**`reviewIndependence`のfield削除だけで既定へ下がる** | `ASC-TRUST-001`を素通りする | なし | 弱化比較へ単調性を足し、引き上げは通す | SCN-UNIT-RISK-010、SCN-UNIT-RISK-029 | updated | pass |
| DISC-006 | manifest経路の`validateProjectPolicyManifest`が`merge.reviewIndependence`を未知keyとして拒否していた | manifest構成のprojectがmodeを宣言できない | なし | enum検証つきで受理する | `npm test`全スイート | updated | pass |
| DISC-007 | `doctor`の未管理資産報告が、recordを検証できない場合に展開済み全fileを「recordに無い」と報告し得た | 誤った件数を出す | なし | record読み取り成否を`installed`と別に持ち、判定不能を返す | SCN-INT-LIFECYCLE-043 | updated | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-INT-MERGE-017、SCN-E2E-WFSTEP-051、SCN-UNIT-RISK-029 | `authorizeMerge`のモード分岐と`pr merge`実経路の配線 | 合格 | pass | implementer・PR author・reviewerが同一actorで`pr merge`が成功する。modeを固定する変異は両方向とも落ちる |
| AC-02 | SCN-INT-MERGE-018、SCN-E2E-WFSTEP-051、SCN-UNIT-RISK-010 | 同上と`compareTrustedPolicy` | 合格 | pass | 同一観測で`actor-independent`宣言時は`pr.merge`を呼ばずに拒否する。弱化差分は`ASC-TRUST-001`が拒否する |
| AC-03 | SCN-UNIT-REVIEW-030 | `validateImmutableCandidateEvidence` | 合格 | pass | 同一actorで独立性error 0件 |
| AC-04 | SCN-UNIT-REVIEW-031 | 同上 | 合格 | pass | `actor-independent`で独立性error 2件 |
| AC-05 | SCN-UNIT-ROUTING-013 | `checkRoutingIndependence` | 合格 | pass | 同一provider・同一tierでBR-836-12が出ない |
| AC-06 | SCN-INT-LIFECYCLE-042、SCN-INT-LIFECYCLE-043 | `doctor`の`unmanagedAssets` | 合格 | pass | 件数と対処を報告し、観測できない場合は判定不能を返す |
| AC-07 | SCN-INT-RISK-023、SCN-INT-RISK-027 | `review evidence`のmode解決 | 合格 | pass | policy未配置は既定で動き、壊れたJSONと未知値は停止する |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | reviewの独立性は承認authorityの境界であり、緩めると自己承認が可能になるため脅威分析の対象である | `actor-independent`の強制点をSCN-E2E-WFSTEP-051とSCN-UNIT-REVIEW-031で固定し、`ASC-TRUST-001`の弱化拒否をSCN-UNIT-RISK-010で固定した |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 適用した独立性モードをreview evidenceの結果と拒否診断へ残し、`doctor`が管理対象外資産の観測可否を報告する | `buildReviewEvidence`の戻り値へ`independenceMode`を含め、SCN-INT-MERGE-019で診断がモードを述べることを、SCN-INT-LIFECYCLE-043で判定不能の報告を固定した |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | CLIはJSONだけを出力しWeb画面を持たない | `unmanagedAssets`欄は機械可読なJSON fieldでありUI componentを持たない |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UIを持たないためdesign tokenとlayout tokenの適用対象が存在しない | 変更したfileにstyle・token定義は含まれない |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 未宣言・`context-isolated`・`actor-independent`の3通りを、判定関数と`pr merge`実経路の両方で実測した |
| 価値 | 利用者・運用上の目的を満たすか | pass | 単独運用の恒常停止が実経路で解けることを確認した。報告元と同じ構成で実測した |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 新しい依存を足していない |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 正本・skill・template・schema・CLI・trusted比較・testを同時に変更した |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 既存の`independence`枠へ載せ、modeの解決を`resolveReviewIndependence`1箇所へ寄せた |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | modeを固定する変異を両方向とも作り、いずれもSCN-E2E-WFSTEP-051が落とすことを実測した |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | policy未配置だけを既定とし、壊れたJSONと未知値は停止する。SCN-INT-RISK-027の変異試験で確認した |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | 不正値を`policy.ts`のpolicy経路とmanifest経路の両方で拒否する |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | modeの解決元をcandidate filesystemから既定branchのtrusted policyへ移した。`--implementer-actor-id`の拒否は維持される |
| 安全性 | 認証、承認、秘密情報、Zero Trust | finding | **既定を緩めるためD1317-01として記録した。** 強制点は宣言で維持し、弱化は`ASC-TRUST-001`が拒否する |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | 判定の変更であり書き込みを増やさない |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | policyへ`actor-independent`を宣言すれば従来の挙動へ戻る |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | finding | **`context-isolated`の代替安全条件のうち機構が観測しているのは一部である。** D1317-11として記録し、正本の主張を実態へ合わせ、残る穴を #1319 へ分離した |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| D1317-01 | High | reviewer独立性がactor単位に固定され、別のGitHub利用者が居ないprojectでは変更のリスクに関係なくStep 10とmergeが完了不能だった | 報告元 techbeansjp/doepion-bpmplus-v2 #121 と実測 | Step 10とmerge | `merge.reviewIndependence`で2段階へ分離した | resolved / acceptance-violation | **既定を緩める。** 強制点は宣言で維持し、両モードでAPPROVEDとexact HEAD一致は必須である |
| D1317-02 | High | Step 10の成果物がPR番号・run ID・review IDを要求しながら「H_final後は更新しない」と定めており、PR作成前は埋められず作成後は直せない循環だった | template 35行 | Step 10 | Phase A（PR前）とPhase B（PR後）へ分離した | resolved / acceptance-violation | なし |
| D1317-03 | Medium | 同一providerかつ同一論理tierだけを理由に独立性違反としていた。別contextでも一律拒否される | `routing-independence.ts` | role設定 | 撤去した。identity比較は維持する | resolved / improvement | なし |
| D1317-04 | High | **既存の`self-review`検査が偽陽性だった。** policy読み込みエラーで非0終了していただけで強制点を通っていなかった | fail-safe投入後に露見 | 強制点の実在 | fixtureへ`project-policy.json`を置き本来の理由で通るようにした | resolved / fix-regression | なし |
| D1317-05 | Medium | policy不在で`review evidence`が停止し、展開先の解決失敗で`doctor`が停止した。**いずれも私が入れた回帰である** | SCN-INT-RISK-023、SCN-INT-LIFECYCLE-043 | 停止 | 既定へ倒すfail-safeとした。round 3で「未配置だけ既定、読めるが不正なら停止」へ絞り込んだ | resolved / fix-regression | なし |
| D1317-06 | Medium | 復旧でretainedになった資産が以後`update`の対象から外れる帰結が後から見えなかった | 実利用者repositoryのcloneで23件を実測 | 可視性 | `doctor`が件数と対処を報告する | resolved / improvement | **門は足していない。** `healthy`は変えない |
| D1317-07 | Low | review成果物が一時stagingに置かれたままでは`review evidence`も履歴監査も成立しない | round 2の観測 | Step 10 | `docs/reviews/`へ置き、artifact自身を最終ラウンドの対象にした | resolved / improvement | なし |
| D1317-08 | High | **`pr merge`の実経路は`authorizeMerge`の手前で`observeMergeReviewEvidence`内のselectorを通る。そこがmodeを受け取らず常にPR author・implementation authorを除外していた。** round 1〜2の追加testは純関数だけを直接呼ぶためこの前段を通らず、既定modeが実経路では到達不能なまま残っていた | 独立review（codex）の指摘。`src/cli.ts`の`currentIndependentApprovals`と呼出箇所 | mergeの実経路 | modeを必須引数にしてtrusted policyから渡した。配線の強制主体は`typecheck`である | resolved / acceptance-violation | なし。SCN-E2E-WFSTEP-051が実経路で両方向を固定し、mode固定の変異を両方向とも落とす |
| D1317-09 | High | `compareTrustedPolicy`が`merge.reviewIndependence`を弱化比較に含めていなかった。`merge`は丸ごと差し替わるため、値の書き換えだけでなく**fieldの削除も既定への弱化になる**が、いずれも`ASC-TRUST-001`を素通りしていた | 独立review（codex）の指摘。`src/domain/enforcement.ts` | trusted policyの自己緩和防止 | 単調性比較を足した。引き上げは通す | resolved / acceptance-violation | なし。SCN-UNIT-RISK-010が弱化を、SCN-UNIT-RISK-029が引き上げの受理を固定する |
| D1317-10 | High | `review evidence`のmode解決が、policy未配置だけでなく壊れたJSON・未知構造・境界外symlink・権限やIOの失敗まで一律で既定へ倒していた。解決元も既定branchのtrusted policyではなくcandidate filesystemだった | 独立review（codex）の指摘。`src/cli.ts` | 独立性の要求水準 | trusted policyを正本にし、**真の未配置だけを既定、読めるが不正なら停止**とした。`buildReviewEvidence`も未知値を正規化せずerrorとして記録する | resolved / acceptance-violation | なし。SCN-INT-RISK-027が変異試験で強制主体を確かめている |
| D1317-11 | High | **`context-isolated`が要求すると書いた条件のうち、機構が観測しているのは一部である。** 別session/contextであること、肯定・敵対の両観点を見たことはreview sessionのround入力に無く、finding 0件のroundだけで収束できる | 独立review（codex）の指摘。`src/domain/review-convergence.ts`の`ReviewRoundInput` | 正本の主張 | 正本へ「どこまでが機構で、どこからが記録か」を明記し、過大な主張を取り消した。**機構側の穴は #1319 へ分離した** | resolved / acceptance-violation | **記録に依存する条件が残る。** 実害と是正費用の比較は #1319 で扱う |
| D1317-12 | Medium | `doctor`の未管理資産報告が、`mappings`の1例外で全件を空にし「なし」と断定していた。recordを検証できない場合も`installed`だけを見て全展開fileを未管理として数え得た | 独立review（codex）とCodeRabbitの指摘。`src/domain/lifecycle.ts` | 可視性 | record読み取り成否を`installed`と別に持ち、観測できないときは判定不能と次の操作を返す | resolved / fix-regression | なし。SCN-INT-LIFECYCLE-043が固定する |
| D1317-13 | Medium | 独立review不足の拒否診断が、actor除外を行っていない`context-isolated`でも「除外した」と述べ、別actorを用意する対処へ誘導していた | CodeRabbitの指摘。`independentReviewDiagnostic` | 診断 | 適用したmodeを述べ、実際に適用した条件だけをchecksへ並べる | resolved / improvement | なし。SCN-INT-MERGE-019が固定する |
| D1317-14 | Medium | manifest経路の`validateProjectPolicyManifest`が`merge.reviewIndependence`を未知keyとして拒否し、manifest構成のprojectがmodeを宣言できなかった | CodeRabbitの指摘。`src/domain/policy.ts` | manifest経路 | enum検証つきで受理する | resolved / fix-regression | なし |
| D1317-15 | Low | role設定の検証結果が未設定でも`?? []`で空errorへ丸められ、走査0回のまま合格し得た | CodeRabbitの指摘。`test/steps/routing-independence.steps.ts` | test | 未実行を明示的に失敗させる | resolved / improvement | なし |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: **はい。** 肯定5観点と敵対8観点を`c74b77f4`へ適用した。reviewerはcodexで、契約でread-onlyと非編集を明示した。
- 指摘を確定した: **High 3件・Medium 3件。** §5のD1317-01〜06がそれである。
- 次ラウンド対象のCritical/High: 0件。

### ラウンド2

- 未解決Critical/High: 0件。
- 修正差分: round 1の是正を`c74b77f4`へ収め、review成果物を`docs/reviews/`へ置いた（D1317-07）。
- 修正で触れた隣接範囲: `review evidence`と`doctor`の呼び出し側だけを再確認した。
- 既承認・未変更範囲を再走査していない: **していない。**

### ラウンド3

- 全指摘の最終分類: **High 4件とMedium 4件を追加で確定し、すべてresolvedにした。** D1317-08〜D1317-11とD1317-12〜D1317-15である。**round 1〜2の追加testが判定関数だけを直接呼んでいたため、実経路の齟齬を捕まえられていなかった。**
- 任意の危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: `context-isolated`が機構として保証しない条件を正本から取り消し、機構側の是正は #1319 へ分離した。**主張を実態より広く残さない。**
- 同じ範囲の予算を自動更新していない: **していない。** 上限4に対し残り1である。
- AIによる最終裁定: approved。

## 7. テスト結果

**手書きの件数表はそれ自体が自己申告であり、それだけを承認証拠にしない。** Phase Aとしてlocal実行の観測を記録する。

- 実行したcommandの一覧: `npm test`、`npm run conformance:check`、`npm run package:check`、静的gate 12本（directories:check・skills:check・cli:check・workflow:check・project:quality・lint・format:check・typecheck・source:check・trace:check・architecture:check・package:check）
- 全layerの合計: **1754 scenarios / 1738 passed / 0 failed / 16 skipped。**
- 失敗またはskipがある層: skipは16件で、いずれも本変更の対象外の既存scenarioである。**失敗は0件のため層別展開は不要である。**

**成功CI runはPhase Bの外部証拠である。** run ID・対象HEAD・conclusionはPR作成後にしか観測できないため本artifactへ書かず、`review evidence`とdelivery stateが記録する。

runnerは`agent-skill-chain-project/cucumber-js`、`projectChoices.gherkinDialect`は`en`である。

**変異試験を3箇所で行った。** `pr merge`実経路のmodeを`actor-independent`へ固定する変異と`context-isolated`へ固定する変異はいずれもSCN-E2E-WFSTEP-051が落とす。`review evidence`のmode解決へcatch-allを戻す変異はSCN-INT-RISK-027が落とす。**最初の試行では`dist/`を再buildせず変異が届いておらず、生存と誤読した。** 再buildして確かめ直した。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/domain/delivery.ts` | 入る | merge認可が`merge.reviewIndependence`に従い、拒否診断が適用modeを述べる |
| `src/domain/enforcement.ts` | 入る | `reviewIndependence`の弱化を`ASC-TRUST-001`が拒否する |
| `src/domain/review.ts` | 入る | review evidenceの独立性判定がモードに従い、未知値を拒否する |
| `src/domain/routing-independence.ts` | 入る | 同一provider・同一tierだけでは違反にならない |
| `src/domain/lifecycle.ts` | 入る | `doctor`へ`unmanagedAssets`欄が増え、観測可否を返す |
| `src/domain/policy.ts` | 入る | `merge.reviewIndependence`をpolicy経路とmanifest経路の両方で受理し、不正値を拒否する |
| `src/types.ts` | 入る | 同上 |
| `src/cli.ts` | 入る | `pr merge`と`review evidence`がtrusted policyからモードを読む |
| `dist/src/` | 入る | 上記のbuild成果物 |
| `.agent-skill-chain/00_利用案内.md` | 入る | package manager script経由を推奨しない旨を追記した |
| `.agent-skill-chain/docs/00_運用ポリシー.md` | 入る | 独立性の2段階と、機構と記録の境界を明記した |
| `.agent-skill-chain/skills/step-10-review/SKILL.md` | 入る | Step 10のPR前完了と2モードを明記した |
| `.agent-skill-chain/skills/step-11-pr/SKILL.md` | 入る | 要求水準の参照へ変更した |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | 入る | Phase A/Bを分離し、外部証拠の要求をPhase Bへ移した |
| `.agent-skill-chain/schemas/project-policy.schema.json` | 入る | enumを追加した |
| `docs/reviews/189_課題1317reviewer独立性2モード化レビュー.md` | 入らない | なし |
| `docs/specs/15_要件追跡/00_追跡表.md` | 入らない | なし |
| `test/features/e2e/workflow-step-enforcement-cli.feature` | 入らない | なし |
| `test/features/integration/delivery-finalize.feature` | 入らない | なし |
| `test/features/integration/lifecycle-isolation.feature` | 入らない | なし |
| `test/features/integration/risk-policy-migration.feature` | 入らない | なし |
| `test/features/unit/review-policy-package.feature` | 入らない | なし |
| `test/features/unit/risk-policy.feature` | 入らない | なし |
| `test/features/unit/routing-independence.feature` | 入らない | なし |
| `test/steps/delivery-finalize.steps.ts` | 入らない | なし |
| `test/steps/lifecycle-isolation.steps.ts` | 入らない | なし |
| `test/steps/risk-policy.steps.ts` | 入らない | なし |
| `test/steps/routing-independence.steps.ts` | 入らない | なし |
| `test/steps/unit.steps.ts` | 入らない | なし |
| `test/steps/workflow-step-enforcement.steps.ts` | 入らない | なし |

判断: 配布物を更新した

根拠: 配布されるCLIの判定条件と配布文書を変更したため。**利用側への影響がある。** 未宣言のprojectは既定が`context-isolated`となり、単独運用でStep 10とmergeを完了できるようになる。`actor-independent`を宣言したprojectの挙動は変わらず、既定branchで宣言した要求水準を候補側から下げることもできない。

## 9. 独立reviewの成立

**この節はPhase Aで埋める。** PR作成後にしか存在しない値は書かず、`review evidence`が記録する。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated（本repositoryの`merge.reviewIndependence`は未宣言であり既定が適用される） |
| その要求を満たすこと | はい（reviewerはcodexおよびCodeRabbitで、implementerのClaudeと別identity・別contextである） |
| reviewerとimplementerのidentity・context比較 | implementerはClaude（本session）。reviewerはcodexの別provider processとCodeRabbitのPR review。**同一processでも同一contextでもない** |
| reviewerが対象差分を変更していないこと | はい（契約でread-onlyと非編集を明示し、`git status`は観測前後ともcleanだった。是正commitはすべてimplementer側が作成した） |

**承認元、承認者、承認日時、失効日時は正本を参照し、ここへ複製しない。**

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `.agent-skill-chain/docs/00_運用ポリシー.md`、`.agent-skill-chain/skills/step-10-review/SKILL.md`、`.agent-skill-chain/skills/step-11-pr/SKILL.md`、`.agent-skill-chain/templates/issue/04_レビュー.md`、`.agent-skill-chain/schemas/project-policy.schema.json`、`docs/specs/15_要件追跡/00_追跡表.md`
- ドメイン用語台帳の追跡: `context-isolated`と`actor-independent`は`merge.reviewIndependence`のenum値として`project-policy.schema.json`が正本を持つ。
- 要件・変更・SCN・テストの追跡: AC-01〜AC-07 → SCN-INT-MERGE-017、SCN-INT-MERGE-018、SCN-INT-MERGE-019、SCN-E2E-WFSTEP-051、SCN-UNIT-RISK-010、SCN-UNIT-RISK-029、SCN-UNIT-REVIEW-030、SCN-UNIT-REVIEW-031、SCN-UNIT-ROUTING-013、SCN-INT-LIFECYCLE-042、SCN-INT-LIFECYCLE-043、SCN-INT-RISK-023、SCN-INT-RISK-027 → 各featureとstep
- `no-spec-impact`の場合の限定的根拠: 該当しない。
- UI・トークンの判断: UIなし。DC-UXとDC-TOKENSがnot-applicableである。

## 11. 総合判定と再開地点

- 未解決Critical/High: **0件。** round 3で確定したHigh 4件（D1317-08〜D1317-11）とMedium/Low 4件（D1317-12〜D1317-15）はすべて本ラウンドで是正し、全スイート・conformance・静的gate 12本で確認した。
- Medium/Lowの記録: §5へ記録した。out-of-scopeとしたものは無い。
- **AIによる最終裁定: approved。** 既定を緩める変更だが、`actor-independent`の強制点は宣言で維持され、**その宣言を候補側から下げる差分は`ASC-TRUST-001`が拒否する。** どちらのモードでもAPPROVED verdictとexact HEAD一致は必須である。判定関数と`pr merge`実経路の両方で3通りを実測し、変異試験で強制主体を確かめた。fail-openではない。
- 新しい権限が必要な事項: **本変更は権限境界を緩める。** ownerが既定を`context-isolated`とする方針を明示的に選択した。
- 残存リスク: **2件ある。** ひとつは`context-isolated`の代替安全条件のうち別session/contextと両観点評価が機構として観測されていないことで、正本の主張を実態へ合わせたうえで #1319 へ分離した。もうひとつは6経路E2E（full / quick / poc / merge disabled / assisted / automatic）が未実施であることで、ownerの指示により後続タスクとした。
- 次に許可される操作: `workflow record --step=10`、CI確認、`pr merge --merge`。
- 次回の再開地点: 6経路E2Eの追加、続いて #1319。
