# 118 課題988 conformanceの合成経路を双方向で突き合わせる 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。**双方向にしたことで、自分の宣言漏れを実際に検出した。**

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #988 |
| 比較基点 | `89de9189aef51aa001922d9f91542e4c9eeb2a57` |
| H_impl | `20d94c41dd1bf4e27ae3f605cd7f14b55282f8dc` |
| 対象差分 | integration feature、integration steps、仕様3件の5 file |
| 対象外 | 個別検査の判定ロジック、検査自体が保護対象外である点、合成対象を増やすこと |
| 残り予算 | Step 10の上限3ラウンドのうち1を残す |
| ラウンド | Step 10 ラウンド2 |
| ラウンド数 | 2（Step 10のラウンド1〜2）。**quickにStep 7は無い** |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260903_061156_conformanceの公開入口へ合成されている個別検査を双方向で突き合わせる |
| モード | quick（Q-01〜Q-08がすべて真。配布契約もtrust境界もgate定義も変えない） |
| 仕様の所有箇所 | `docs/specs/02_要件/04_仕様・品質管理要件.md`のREQ-SQ-004 |
| 成果物行数 | test: 84 insertions。仕様: 3行。**`scripts/`と`src/`への変更0行** |
| 縮小の先行評価 | 実施済み。**Issueの対象内候補2を実測で棄却した。** 14 enforcement pointのうち5件がledgerの外で強制されており、合成を要求すると偽陽性になる。**候補1（違反treeでの実行）も採らない。** `replicate`がrepository全体の複写とproject policy一式を要求し、支援層の所要時間が成果物構築を上回る。**既存の`SCN-INT-MODEQ-011`と同型の機構を1件から18件へ一般化した** |
| authority | 変更しない。**test層の追加だけで、製品codeの実行経路に現れない** |
| 実施者・日時 | reviewer、2026-09-03（JST） |

### 0.1 routing入力契約

| role欄 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類、変異試験 | routine（riskがlow） | claude | Opus 5、effort high | 変異が1件でも生存したら停止する | implementerとreviewerはいずれも本sessionのAI agentであり、**Git commit authorも同一である。§9に実態どおり記録し、独立性が成立したとは主張しない** |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 候補2が成立しないこと | `PROJECT_RULE_ENFORCEMENT_POINTS`の全14 point | 合成されているのは8件。**5件（`checkConformance`、`checkFixedMarkdownNames`、`checkJapaneseDocuments`、`validateDistributionImpact`、`validateOwnershipBoundary`）はledgerの外で強制されている** | 実測 |
| 同型機構の存在 | `test/steps/mode-questions.steps.ts:412-441` | `checkModeQuestionText`について**すでに同じ目的の検査が存在する**。commentが「配線の存在で回帰を守る」と述べている | 実測 |
| `errors.push(`直後だけでは足りないこと | 是正前後の件数 | 直後だけを見る観測は**9件**しか拾えない。本体全体からspreadを拾うと**18件** | 実測 |
| 宣言漏れの実検出 | `SCN-INT-LEDGER-006` | 最初の宣言で`validateReviewExceptions`が漏れており、**逆向きの検査が実際に検出した** | 実行観測 |
| `scripts/`・`src/`の非変更 | `git diff --name-only origin/main HEAD` | `scripts/`0件、`src/`0件。変更は`test/`2件、`docs/specs/`3件 | 実測 |
| 変異の有効性 | 変異5件 | 合成行の削除4件と未登録の検査追加1件を**全件kill** | 実測 |
| 全gate | `npm test`、`conformance:check` | 1415 scenario、1399成功、0失敗、16 skip。project rule 20件、orphan 0件 | 実行観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: pass。**観測は source text だけを入力にし、宣言を読まない。** 読むと自明に一致して検出力が消える
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_impl`が直前commitであり、`H_impl..HEAD`の差分は本artifact 1 fileのみ
- reviewer stable IDがPR author/観測済み`H_impl` author stable IDと異なる: **異ならない。** §9に実態を記録した
- 既定branch追随: **実施済み。** 本artifactを作る前に`origin/main`へ追随し、他Issueの履歴行への削除が0件であることを`git diff origin/main`で確認した

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `test/steps/project-rule-ledger.steps.ts` | M | project | project | 宣言1件、観測関数1件、Given 1件、Then 2件。**既存fileへ足し、新しいfileを作らない** | steps → source textの一方向。宣言と観測は互いを参照しない | AC-01、AC-02。SCN-INT-LEDGER-005、006 | revertで復旧する | pass |
| `test/features/integration/project-rule-ledger.feature` | M | project | project | Scenario 2件。既存の`SCN-INT-LEDGER`家族へ連番で足す | featureはstepsへ一方向。循環なし | 同上 | 同上 | pass |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | M | package | spec | REQ-SQ-004の本文へ双方向一致の要求を足す | 仕様は実装を参照しない。循環なし | REQ-SQ-004 | 同上 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package | spec | SCN 2件を既存行へ足す | 追跡のみ。循環なし | 該当なし | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | 棄却した候補を含めて記録する | 履歴のみ。循環なし | 該当なし | 1行の削除で復旧する | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: pass（`git diff --name-only origin/main HEAD`が5件）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: pass
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: 修正は§2.0のとおり2件

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | **Issueの対象内候補2は成立しない。** 全14 enforcement pointのうち5件が`checkRepositoryRuleLedger`の外で強制されている | 候補2を採ると偽陽性5件 | なし | 候補2を採らず、既存の`SCN-INT-MODEQ-011`と同型の機構を18件へ一般化した | 14 pointの合成有無の実測 | no-spec-impact。候補の選択であり要件を変えない | pass |
| DISC-002 | 最初の宣言で`validateReviewExceptions`が漏れていた。**双方向のうち逆向きの検査がこれを検出した** | なし | なし | 宣言へ追加した | `SCN-INT-LEDGER-006`の失敗と是正 | no-spec-impact | pass |
| DISC-003 | `errors.push(`の直後だけを見る観測では、1回の`push`へ複数検査を並べる書き方の2件目以降を落とす | 検出力が半分 | なし | 本体全体からspread呼び出しを拾う形へ変えた | 是正前後の件数比較（9件→18件） | no-spec-impact | pass |

**「4件へ一律に与える」とは1件を4件へ複写することではない。** 複写すると5件目の検査を足したとき同じ穴が残る。双方向にしたのはそのためであり、実際に自分の宣言漏れを検出した。

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-INT-LEDGER-005 | 宣言→合成の向き | 合格 | pass | §7 |
| AC-02 | SCN-INT-LEDGER-006 | 合成→宣言の向き | 合格。**実際に登録漏れを検出した** | pass | §7 |
| AC-03 | 該当なし | `scripts/`と`src/`への変更0行 | `git diff --name-only`で0件 | pass | §1 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | 検査が消えたことを検出する手段であり、消失は保護境界の縮小に等しい | 変異5件を全件kill |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 「検査が消えたことを示す観測が存在しない」という欠落を埋める | 診断が欠落名・未登録名を列挙する |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 人が触れるUIを持たないtest層の追加である | `projectKind`が`cli` |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UI componentもthemeも持たず、色・間隔・typographyの決定を含まない | `capabilities.designTokens`が`not-applicable` |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 合成行の削除4件がすべて検出される |
| 価値 | 利用者・運用上の目的を満たすか | pass | refactoringで検査が1件消えたことに気付ける |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | source textの読み取りだけで、隔離repositoryもproject policyも要らない |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | REQ-SQ-004の本文が双方向の要求を述べ、SCN 2件が対応する |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | **合成対象を足したとき宣言も更新しないと落ちる。** 保守を強制する側に働く |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | 変異5件を全件kill |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 関数が見つからなければ空配列を返し、宣言18件がすべて欠落として報告される |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | `new Set`で重複を潰し、`sort`で順序を固定する |
| 悪用 | **宣言と合成行を同時に消せば通る** | **valid（M-01）** | testは信頼境界ではなく、candidateが同一PRで両方を消せる。**「継続的担保」ではなく「回帰検出」として記録した** |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | 読み取り専用。**観測が宣言を読まない非対称が検出力の根拠である** |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | 書き込みを行わない |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | revertで戻る。配布境界に入らない |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | 既存の`SCN-INT-MODEQ-011`は`checkModeQuestionText`について残る。重複するが、本Scenarioが落ちたときの切り分けに役立つため削らない |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| M-01 | Medium | **宣言と合成行を同時に消せば通る。** testは信頼境界ではない | 実装の読解 | 意図的な迂回 | **機構を足さない。** 目的はrefactoringでの意図しない脱落の検出であり、意図的な迂回はreviewが受け持つ。「継続的担保」と書かず「回帰検出」と書いた | valid（設計判断。record-only） | 残存する |
| L-01 | Low | `SCN-INT-MODEQ-011`と本Scenarioが`checkModeQuestionText`について重複する | 実測 | 保守 | **削らない。** 本Scenarioが落ちたとき、単一検査の配線が生きているかを`MODEQ-011`が切り分ける | valid（record-only） | 残存する |

**Critical/High 0件。**

## 6. ラウンド固有の確認

### ラウンド1（Step 10、2026-09-03、candidate HEAD = `H_impl`）

- 全評価基準を確認した: はい。肯定5観点・敵対8観点をすべて評価した
- 指摘を確定した: はい。M-01とL-01はいずれも記録のみ
- 次ラウンド対象のCritical/High: **なし**
- blocking: 0件

### ラウンド2

- 本artifactのcommitでHEADが動くため、次ラウンドで再固定する

### ラウンド3

- 未実施。**予算1を外部reviewの是正のために残す**

## 7. テスト結果

**承認証拠は成功CI runである。** 手書きの件数表はそれ自体が自己申告であり、承認証拠にならない。

実行したcommand: `docs:format`、`test:format`、`lint`、`format:check`、`typecheck`、`source:check`、`trace:check`、`architecture:check`、`skills:check`、`build`、`package:check`、`npm test`、`conformance:check`、`audit:check`、`issue validate`

全layerの合計: **1415 scenario、1399 成功、0 失敗、16 skip。** `conformance:check`はproject rule 20件・orphan 0件で合格。

失敗またはskipがある層: **skipした16 scenarioはsemantic graphのGraphQLite assetが未設定な環境に起因する。** 本差分はこの経路に触れない。

対応する成功CI run: PR作成後に取得し、merge前に現`H_final`へ束縛する。

**変異試験の全量。**

| 変異 | 結果 |
|---|---|
| `checkTrustedScriptPinning`の合成を外す | kill |
| `checkDistributionGateReachability`の合成を外す | kill |
| `checkModeQuestionText`の合成を外す | kill |
| `checkLifecycleIgnore`の合成を外す | kill |
| 宣言に無い検査を合成へ足す | kill |

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `test/steps/project-rule-ledger.steps.ts` | 入らない | `package.json`の`files`に`test/`を含まない |
| `test/features/integration/project-rule-ledger.feature` | 入らない | 同上 |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | 入らない | `files`は`.agent-skill-chain/docs/`を含むが`docs/specs/`を含まない |
| `docs/specs/15_要件追跡/00_追跡表.md` | 入らない | 同上 |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | 同上 |

判断: 配布物を更新しない

根拠: `package.json`の`files`は`dist/bin/`、`dist/src/`、`dist/vendor/`、`.agent-skill-chain/00_利用案内.md`、`.agent-skill-chain/skills/`、`.agent-skill-chain/templates/`、`.agent-skill-chain/schemas/`、`.agent-skill-chain/policy/`、`.agent-skill-chain/docs/`、`README.md`、`AGENTS.md`である。**変更5 fileのいずれも含まれない。**

**releaseの起動と計画は分けて記録する。** `.github/workflows/release.yml`は`main`への`push`で起動する。その後`planAutoRelease`が配布digestの差分を見て計画を決める。**本変更は配布digestを変えないため、version bumpとtagは作られない見込みである。** merge後に実観測する。

**ロールバックの対象範囲を分けて記録する。**

| 対象 | revertで戻るか | 実行者 | 手順 | 完了確認 |
|---|---|---|---|---|
| repository状態 | **戻る** | 実行役 | 打ち消しPRを通常mergeする | `git diff`が元の状態と一致する |
| 公開済みtagとGitHub Release | **作られない見込み** | 該当しない | 配布digestが変わらない | `gh release list`に新tagが載らない |
| npm registryの公開物 | **公開されない** | 該当しない | 本経路では公開しない | `npm view`に該当versionが無い |

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | **本artifact作成時点では無い。** exact-headは`H_final`。immutable GitHub reviewは0件 |
| reviewerがPR author・実装commit authorと異なる | **異ならない。** implementerもreviewerも本sessionの同一AI agentであり、Git commit authorも同一である |
| 観測したreview commentとapprovalの件数 | 本session内のreview 2ラウンド（Critical/High 0、Medium 1、Low 1）。GitHub review 0件 |

**`02_品質基準.md`の「独立reviewが成立しない場合」に従う。** 同節は「既定は停止ではなく、無記録での通過の禁止である」と定める。

**ただし本件では、追加した検査自身が独立した検出者として働いた。** 私が最初に書いた宣言は`validateReviewExceptions`を落としており、`SCN-INT-LEDGER-006`がそれを検出した。**同一actorの自己申告だけで通過していない証拠として記録する。**

**PR作成後の外部reviewを待つ。** 予算1ラウンドを残してある。

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/02_要件/04_仕様・品質管理要件.md`のREQ-SQ-004、`docs/specs/15_要件追跡/00_追跡表.md`、`docs/specs/15_要件追跡/01_変更履歴.md`
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: pass。新規用語を導入していない
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: pass。「合成」は既存の日本語であり、REQ-SQ-004の本文で対象を限定してから使っている
- 要件・変更・SCN・テストの追跡: REQ-SQ-004 → AC-SQ-004 → SCN-INT-LEDGER-005、SCN-INT-LEDGER-006
- UI・トークンの判断: UIを所有しないためdesign token・layout tokenは対象外。ADRを追加しない

## 11. 総合判定と再開地点

- 未解決Critical/High: **なし**
- Medium/Lowの記録: M-01（宣言と合成行を同時に消せば通る）、L-01（`MODEQ-011`との重複）
- 判定: approved（Step 10 ラウンド2）
- 新しい権限が必要な事項: mergeは別authority
- 残存リスク:
  1. **testは信頼境界ではない。** 意図的な迂回は防げない。回帰検出として記録した
  2. `SCN-INT-MODEQ-011`との重複を意図的に残した
  3. **合成対象を足す人が宣言も更新する必要がある。** 忘れると`SCN-INT-LEDGER-006`が落ちるため気付けるが、追加作業が1行増える
- 次に許可される操作: push、PR作成、外部reviewの到着確認、必須check 2件の全緑確認、およびownerが承認したauthorityによる**通常merge commit方式**でのmerge。**squash mergeを使わない**
- 次回の再開地点: PR作成から
