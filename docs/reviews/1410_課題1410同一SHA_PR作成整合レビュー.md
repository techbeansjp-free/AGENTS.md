# 04 レビュー

> 肯定と敵対の両観点を毎ラウンド確認する。指摘なしの承認は有効。Medium/Lowは記録するだけで、自動修正・追加ラウンド・ゲート停止を起こさない。用語と責務の境界は`成果物用語と責務境界`（`.agent-skill-chain/docs/01_開発ワークフロー.md`）を正本とする。
>
> 埋めた成果物は`docs/reviews/`または`.agent-skill-chain/reviews/`へ置き、実装commitの後にその1 fileだけをcommitする。

| 読者 | 読む節 |
|---|---|
| 発注・評価する人 | 要約 → §5 指摘 → §11 総合判定 |
| 実装・レビューする人 | 全節 |
| 運用する人 | 要約 → §8 配布物影響 → §10 仕様整合性 |

## 要約

**この5行だけで、何を見たreviewかが分かるように書く。** 各行は1〜2文。詳細は対応する節が持つ。

| 項目 | 内容 |
|---|---|
| 何が問題だったか | 同一SHAの`H_impl == H_final`を`pr create`が固定し、後段`pr merge`が必ず拒否していた。 |
| 何を解決しようとしたか | merge可能なfull/quickではPR作成前に診断付きで拒否する。PoCとmerge disabledはPR終端のまま維持する。 |
| 何を行ったか | review bindingの実効H_implを返して同一SHAを判定し、lock内とdispatch直前にも再検証した。実装とartifactを分けたGit fixtureと回帰を追加した。 |
| 何を確認したか | 修正前誤受理を再現し、修正後のdry-run/apply拒否、WFSTEP 59成功・2 skip、全件2,272成功・27 skip、配布gateを確認した。 |
| 判定 | approved。固定HEADの独立Opusレビューで製品コードのCritical/Highはなく、fixtureと履歴文言の指摘を是正した。 |

## 1. 入力証拠

PR番号、Actions run ID、immutable review IDはPR作成後にしか存在しないため**この文書へ書かない。** `review evidence`とdelivery stateがappend-onlyで保持する。reviewerの独立性は`merge.reviewIndependence`が決める。既定の`context-isolated`はimplementerと別session/context、exact HEAD固定、対象差分の非変更、肯定・敵対reviewとfinding記録を要求し、同一GitHub actorでも成立する。このときtracked artifactの`approved`と保存済みreview session・Step 10 bindingがformal approvalになる。`actor-independent`はPR authorおよび観測済み`H_impl` commit authorと別のstable actor IDによるprovider `APPROVED`を要求する。両modeともexact HEAD一致は必須とし、tracked文書へ自身のcommit SHAを書かず、**H_final後はartifactを更新しない。**

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | .agent-skill-chain/tmp/issues/20260923_102535_1410-pr-merge-same-head | staging digest f7a86e8021fc92776099f0fbddac85dccadd33a6a687d59c118d6754031a5b9f | 既存コード |
| 差分 | `241990d6562fb7fb04279d5a9ce0d674a1f31aaf`..`30a4f43477805fed12ae26a9933693477e914511` | 9 path | 既存コード |
| テスト | `SCN-E2E-WFSTEP-1410`、全WFSTEP、`npm test` | 対象1成功、WFSTEP 59成功・2 skip、全件2,272成功・27 skip | テスト出力 |
| 仕様 | ワークフロー、品質基準、CLI契約、追跡表、変更履歴 | 同一SHAの早期拒否と例外が一致 | 既存文書 |
| commit前candidate | .agent-skill-chain/docs/01_開発ワークフロー.md、.agent-skill-chain/docs/02_品質基準.md、dist/src/cli.js、docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md、docs/specs/15_要件追跡/00_追跡表.md、docs/specs/15_要件追跡/01_変更履歴.md、src/cli.ts、test/features/e2e/workflow-step-enforcement-cli.feature、test/steps/workflow-step-enforcement.steps.ts | H_impl 30a4f43477805fed12ae26a9933693477e914511 | Git index |
| Phase A artifact | 本fileをcommit後に観測 | 未作成 | Git観測 |
| review session | .agent-skill-chain/tmp/issues/20260923_102535_1410-pr-merge-same-head | Opus候補の採否確認後にround 1へ記録 | Git観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: Step 10は独立Opusの読取専用reviewを確認し、H_implをGit commitから固定した。artifactは自身のcommit SHAを記載しない。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: artifact commit後にGit差分を照合する。
- reviewerの独立性が要求水準を満たす: 別のClaude Code CLI Opus sessionで`Read/Glob/Grep`だけを許可し、差分変更なしをGit statusで確認する。
- 既定branch追随を行った場合、取り込みがartifact commitより前にあり、`比較基点`が取り込んだ既定branch tip、`H_impl`がartifact直前の最新commitを指し、個別監査表を`比較基点..H_impl`から再生成した: 追随なし。比較基点はmainの`241990d6`。

### 1.1 変更ファイル個別監査

基準SHAとの差分にある全ファイルを、生成物（`dist/`等）も含めて1ファイル1行で記録する。まとめ行、directory単位の一括承認、test成功だけの代替を認めない。`audit:check`の他の検査対象外となる生成物でも、各行へ生成元との対応確認方法と配布影響の確認方法を記録し、差分path集合と表のpath集合を一致させる。

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | M | package owner | docs | Step 10後のPR作成契約 | docs→CLI、循環なし | AC-1410-01 | 文書revert可 | pass |
| `.agent-skill-chain/docs/02_品質基準.md` | M | package owner | docs | PR作成安全gate | docs→CLI、循環なし | AC-1410-01 | 文書revert可 | pass |
| `dist/src/cli.js` | M | package owner | 生成物 | `src/cli.ts`の配布用出力 | source→dist、循環なし | AC-1410-01 | `npm run build`後にGit clean、package:check合格 | pass |
| `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | M | package owner | docs/specs | CLI公開契約 | spec→src、循環なし | AC-1410-01/03 | 文書revert可 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package owner | docs/specs | ACとSCNの対応 | spec→test、循環なし | AC-1410-01/03 | 文書revert可 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package owner | docs/specs | 同一SHAの変更履歴 | spec→src、循環なし | AC-1410-01 | 旧pr-bound再実行への影響も明記 | pass |
| `src/cli.ts` | M | package owner | CLI adapter | Step10 bindingの実効HEAD判定 | adapter→domain、循環なし | AC-1410-01/02/03 | provider前・lock内・dispatch直前に拒否、revert可 | pass |
| `test/features/e2e/workflow-step-enforcement-cli.feature` | M | package owner | test | 同一SHAの実行可能AC | test→CLI、循環なし | SCN-E2E-WFSTEP-1410 | 修正前失敗・修正後合格 | pass |
| `test/steps/workflow-step-enforcement.steps.ts` | M | package owner | test | 実Gitとfake GitHubのPR履歴fixture | test→CLI、循環なし | SCN-E2E-WFSTEP-1410/002/004/037/038 | 2 commit rebaseとartifact監査pathも再検証 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: `git diff --name-only 241990d6..806306c9`の9 pathと一致。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: 製品側は既存`src/cli.ts`のbinding gateのみ。test fixtureはtest配下に限定。
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: Opus指摘後にtest fixtureと変更履歴、rebase fake providerを対象再検証。

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

発見IDは実装計画（fullは03、quick/pocは集約00）の`DISC-*`と同じ字面を使い、本文書内で別IDへ言い換えない。

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-1410-01 | PR #117のartifactは現行H_implより古いSHAを宣言していた | 単純にmergeで同一SHAを許す案はformal approvalで失敗 | AC-1410-01を案Bへ再基準化 | merge可能なfull/quickの`pr create`で早期拒否 | PR #117 artifactと`resolveContextIsolatedFormalApproval`の照合 | updated | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1410-01 | SCN-E2E-WFSTEP-1410 | `src/cli.ts`の同一SHA gate | 修正前preview受理、修正後dry-run/apply拒否 | pass | CucumberとGit履歴 |
| AC-1410-02 | SCN-E2E-WFSTEP-1410 | provider前・lock内・dispatch直前のgate | provider side effectなし | pass | fake GitHubの呼出し記録 |
| AC-1410-03 | SCN-E2E-WFSTEP-002/004/037/038 | suffix付fixture・rebase fixture | 既存PR create系成功 | pass | WFSTEP suite 59成功、2 skip |

### 2.2 開発考慮事項の適用判定（必須）

00の判定・理由・証拠から差分が無ければ、表の代わりに`開発考慮事項の適用判定は00_要求定義.md §6.1と同じ`の1行を置ける（01〜03と同じ参照行）。差分がある行だけを表に残してよい。**`review validate`はこの§2.2の内容を検証しない**（01〜03の`issue validate`と異なり、review artifactのDC判定に対する機械検証は無い）。記述量を減らすための人・エージェント向けの案内であり、参照行を置いても4行の表を書いても合否は変わらない。

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | merge authority境界を保護 | INV-1410-01/02、SCN-E2E-WFSTEP-1410 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 修復可能な拒否理由を返す | CLI診断とCucumber出力 |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | Node CLIに画面・支援技術向けUIがない | package.jsonのbin |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | 視覚componentがない | projectKind=cli |

## 3. 肯定的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ（要件と観測結果の一致） | pass | 修正前の誤受理を再現し修正後に拒否を確認 |
| 価値（利用者・運用上の目的） | pass | merge不能PRの固定をPR作成前に防ぐ |
| 実現可能性（環境・依存・権限） | pass | 既存bindingから実効H_implを得て追加provider権限不要 |
| 整合性（設計・コード・テスト・仕様） | pass | workflow、quality、CLI契約、追跡表、E2Eを同期 |
| 保守性（責務・命名・変更容易性） | pass | `assertCreatableReviewHead`に判定を集中 |

## 4. 敵対的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 反例（要件を破る入力・状態） | pass | 同一SHA、別SHA、merge disabled、PoCを区別 |
| 失敗経路（外部失敗・部分失敗） | pass | provider前、lock内、dispatch直前に再検証 |
| 境界値（空、最大、最小、重複、Unicode） | pass | HEADはGit SHAで厳密照合、別SHA正常例を回帰 |
| 悪用（注入、経路脱出、権限外） | pass | 入力文字列ではなく検証済みbinding SHAを使用 |
| 安全性（認証、承認、秘密情報、Zero Trust） | pass | formal approval、trusted authority、CASを変更しない |
| データ損失（上書き、削除、部分公開、履歴消失） | pass | 拒否時はPR・delivery stateを生成しない |
| ロールバック（復旧参照、状態保持、再開可能性） | pass | artifact専用commitを作り再実行できる診断 |
| 範囲漏れ（呼び出し元、利用側、配布物、文書） | pass | src/dist、CLI契約、workflow、quality、fixtureを個別監査 |

## 5. 指摘

指摘なしの場合は「指摘なし」と明記する。

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| REV-1410-01 | Medium | suffix正常例fixtureにartifact専用commitがなかった | Opus読取専用reviewとWFSTEP fixture | test/steps | base→実装→artifactのGit履歴へ修正 | resolved | なし |
| REV-1410-02 | Medium | artifact監査pathが実装変更を含まず、監査証拠とずれる | Opus読取専用review | test/steps | 実装pathを含む監査表へ修正 | resolved | なし |
| REV-1410-03 | Low | 変更履歴の実例に誤解を招く文言があった | Opus読取専用review | 変更履歴 | 案Bの停止条件を明記 | resolved | なし |
| REV-1410-04 | Medium | REBASE tamper fixtureが意図した失敗経路に到達せず、曖昧な正規表現で通過した | Opusの固定HEAD review、SCN-E2E-WFSTEP-038 | test/steps | tamperをrebase中間commitの親へ移し、終端診断を完全一致で確認 | resolved | なし |
| REV-1410-05 | Medium | 既存SCN-E2E-WFSTEP-033の診断regexは`reconciliation_required`で誤通過しうる | 最終HEADのOpus読取専用review | test/steps | 本Issueの同一SHA契約外。後続テスト改善候補として記録 | valid / out-of-scope | 既存tamper SCNの反例証明が弱い |
| REV-1410-06 | Medium | squash終端親検証の専用反例SCNがない | 最終HEADのOpus読取専用review | test/steps | 本Issueの同一SHA契約外。後続テスト改善候補として記録 | valid / out-of-scope | squash parent checkの退行を検出しにくい |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: §3・§4、独立Opusと進行役の差分照合。
- 指摘を確定した: 先行4件は修正済み。最終HEADのround 1は既存testに関するMedium 2件をrecord-onlyとした。
- 次ラウンド対象のCritical/High: なし。

### ラウンド2

- 未解決Critical/High: なし。round 2不要。
- 修正差分と、触れた隣接範囲: 該当なし。
- 既承認・未変更範囲を再走査していない: 該当なし。

### ラウンド3

- 全指摘の最終分類: 先行4件resolved、最終HEADの2件はvalid / out-of-scopeのrecord-only。
- 危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: 該当なし。
- 同じ範囲の予算を自動更新していない: 1 roundで収束。

## 7. テスト結果

- 実行したcommandの一覧: `npm test`（全件、制限のないhost実行）、WFSTEP対象`npm test -- --name ...`、`npm run build`、lint、format:check、typecheck、source:check、docs:format、test:format、trace:check、architecture:check、conformance:check、package:check、project:quality。
- 全layerの合計: 全件2,299シナリオ、2,272成功・27 skip・0失敗。最新fixtureでWFSTEP 61シナリオ、59成功・2 skip。conformance 87成功・816 steps。
- runner・Gherkin方言: Cucumber.js、Gherkin ja/en。サンドボックス内の初回全件はIPC/loopback EPERMで14失敗し、制限のないhost再実行で0失敗。

## 8. 配布物影響

packageとして配布する場合だけ記入する。配布境界はpackage manifestの配布file指定を正本とし、compileされて配布される`source`も含める。

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| .agent-skill-chain/docs/01_開発ワークフロー.md | 入る | PR作成の同一SHA拒否を利用者へ明記 |
| .agent-skill-chain/docs/02_品質基準.md | 入る | merge可能なfull/quickの安全gateを明記 |
| dist/src/ | 入る | `src/cli.ts`からbuildしたCLI実行物 |
| docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md | 入らない | なし |
| docs/specs/15_要件追跡/00_追跡表.md | 入らない | なし |
| docs/specs/15_要件追跡/01_変更履歴.md | 入らない | なし |
| src/cli.ts | 入る | CLIのbinding gateを更新 |
| test/features/e2e/workflow-step-enforcement-cli.feature | 入らない | なし |
| test/steps/workflow-step-enforcement.steps.ts | 入らない | なし |

判断: 配布物を更新した

根拠: `src/cli.ts`と生成された`dist/src/cli.js`、配布されるworkflow/quality文書を更新し、`npm run build`および`package:check`で対応を確認した。

## 9. 独立reviewの成立

PR作成前に観測できるものだけを書く。immutable review IDやapproval件数は書かない。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated |
| その要求を満たすこと | はい |
| reviewerとimplementerのidentity・context比較 | implementerはCodexの本session、reviewerは別contextのClaude Code CLI Opus read-only session。対象は固定H_impl `30a4f43477805fed12ae26a9933693477e914511`。 |
| reviewerが対象差分を変更していないこと | はい（OpusにRead/Glob/Grepだけを許可し、変更path集合は空） |

補助ローカルQwen経路は`routing delegated-review-diff`が`関連fileを取り切れませんでした`として推論前にdegradedとなった。収集上限のためレビュー成立とは数えず、独立Opusと進行役が同じH_implを確認した。

外部への不可逆な配布で外部証拠を要求され、かつ無い場合だけ次を記入する。承認元・承認者・承認日時・失効日時は正本を参照し複製しない。

| 項目 | 内容 |
|---|---|
| 適用する例外の識別子 | 該当なし |
| 観測値 | 該当なし |

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`、追跡表、変更履歴、workflow/quality。
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: 既存TERM-ASC-008/009のH_impl/H_finalを参照し新語なし。
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: なし。
- 要件・変更・SCN・テストの追跡: AC-1410-01/02→SCN-E2E-WFSTEP-1410、AC-1410-03→既存WFSTEP正常例。
- `no-spec-impact`の場合の限定的根拠: 該当なし。
- UI・トークンの判断: Node CLIのため画面・視覚tokenなし。

## 11. 総合判定と再開地点

- 未解決Critical/High: なし
- Medium/Lowの記録: Opusの先行4件は修正。最終HEADの既存testに関するMedium 2件はrecord-onlyで記録した。
- 判定: approved
- 新しい権限が必要な事項: PR作成まで許可済み。mergeは未許可。
- 残存リスク: 外部PRの既存pr-bound状態は本修正で遡及して直らない。既存WFSTEP-033とsquash反例の検査強化は後続課題。
- 次に許可される操作: artifact専用commit、branch push、PR作成、CI確認。
- 次回の再開地点: PR作成後のCIと外部review。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 1 |
| 対象SHA・文書ダイジェスト | 30a4f43477805fed12ae26a9933693477e914511 |
| 比較基点 | `241990d6562fb7fb04279d5a9ce0d674a1f31aaf` |
| H_impl | `30a4f43477805fed12ae26a9933693477e914511` |
| 対象差分 | .agent-skill-chain/docs/01_開発ワークフロー.md、.agent-skill-chain/docs/02_品質基準.md、dist/src/cli.js、docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md、docs/specs/15_要件追跡/00_追跡表.md、docs/specs/15_要件追跡/01_変更履歴.md、src/cli.ts、test/features/e2e/workflow-step-enforcement-cli.feature、test/steps/workflow-step-enforcement.steps.ts |
| 対象外 | 比較基点に存在し変更されていない範囲 |
| 残り予算 | 3ラウンド |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260923_102535_1410-pr-merge-same-head |
| 仕様の所有箇所 | `.agent-skill-chain/docs/01_開発ワークフロー.md`、`02_品質基準.md`、CLI契約。 |
| 成果物行数 | 製品`src/cli.ts` 37追加・3削除、生成物12追加・3削除、支援層test/文書は`git diff --numstat`で個別監査。 |
| 縮小の先行評価 | 既存review bindingの実効H_implを流用し、`pr merge`の権限契約を変えずに`pr create`側の拒否だけ追加した。 |
| 実施者・日時 | Codex implementer、2026-09-23T02:35:00Z。Claude Code CLI Opus reviewer、2026-09-23T02:30:00Z以降。 |

`比較基点`と`H_impl`の値は40桁の小文字hexをbacktickで囲んだものだけにする。注記・branch名・短縮SHAを同じcellへ書かない。由来は別行へ書く。

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類 | high（merge権限境界） | Claude Code CLI Opus | Opus読取専用、別context | 未解決Critical/Highなら停止 | 独立CLI session、Read/Glob/Grepのみ、変更path空 |
