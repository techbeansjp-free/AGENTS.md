# 04 レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1107 |
| ラウンド | Step 10 ラウンド1〜2 |
| 比較基点 | `2bb0d82b89b7b1d585dd9d46d52389c2daff40b9` |
| H_impl | `c66c6ed7d2b4f50123deddb961e8404e7992b168` |
| 比較基点の由来 | review開始時点の`origin/main`のtip。PR #1171（#1165）のmerge commitである |
| Step 10のreview session ID | `971fe2b266b7bc2f436eac165e5f879abf42650f0bf2a39135af2b26093e8003` |
| モード | full（Q-01がfalse） |
| 対象差分 | `src/adapters/github.ts`、`test/features/unit/merge-method-policy.feature`、`test/features/integration/delivery-finalize.feature`、`test/steps/merge-method-policy.steps.ts`、`test/steps/delivery-finalize.steps.ts`、`.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/policy/00_利用案内.md`、`.agent-skill-chain/policy/sample.json`、`docs/specs/02_要件/03_外部連携要件.md`、`docs/specs/15_要件追跡/00_追跡表.md`、`docs/specs/15_要件追跡/01_変更履歴.md`。commitは`c66c6ed7` |
| 対象外 | `src/domain/delivery.ts`の`resolveMergeMethod`（**既に2つの独立した拒否理由を持つ**）。`audit:check`の判定logic。GitHubのrepository設定。`docs/reviews/`のrole authority不整合（#1047） |
| 残り予算 | **1**（同一範囲で最大3ラウンド、収束後にHEADが動いたときの取り直しを1回まで。総2ラウンドで設計した） |
| ラウンド数 | 2。ラウンド1は実装差分、ラウンド2は本artifactを加えた版が対象である |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260903_114920_squashを基本NGとしmerge方式の既定値と配布雛形を揃える |
| 仕様の所有箇所 | `docs/specs/02_要件/03_外部連携要件.md`のREQ-GH-004 |
| 成果物行数 | 製品 **+29 / −6行**（`mergeMethodFlag`の新設と呼び出し）。配布物 **+17 / −20行**（規範文書2件と雛形1件）。仕様 **+4 / −3行**。支援層 **+128行**（feature +11、steps +117）。**支援層/成果物 = 4.4倍** |
| 縮小の先行評価 | 3案を先に評価した。(1) `mergeMethodFlag`をexportしてunit scenarioを直接書く案は、本Issueが要求していない公開面を増やすため不採用。(2) source中の`"--squash"`字面を数える案は**一度採用したがDISC-004で撤去した**。設計を案Bへ戻した時点で誤った検査になる。(3) `merge.methods`のschemaから`squash`を除く案は、`audit:check`を既定branchで走らせないprojectまで縛るため不採用（`02_品質基準.md`の「永久に塞がる門を作らない」に接する） |
| 実施者・日時 | reviewer（claude）、2026-09-03 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節と`review-session.json`） | advanced | claude | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerとreviewerが**同一session**である。0.2節に逸脱として開示する |

### 0.2 開示する逸脱

1. **implementerとreviewerが同一sessionである。** project choiceの`reviewer.independence.differentFrom = implementer`を満たさない。緩和は、判定の根拠をすべて機械観測（scenario結果と変異試験の赤・緑）に置いたことである。
2. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 決裁 | Issue #1107 のownerコメント | 「squashは基本NGとします」 | 一次資料 |
| 要求・受け入れ条件 | https://github.com/techbeansjp-free/AGENTS.md/issues/1107 、AC-01〜AC-03 | Step 8で00を同期した。`issue validate`は`valid: true`、errors 0件 | 一次資料 |
| 起票時実測 | Issue #1107 | 2026-09-02のmainでsquash 4件がすべてrelease failure、merge commit 3件がすべてsuccess | 一次資料 |
| 差分 | `2bb0d82b..c66c6ed7` | 11 file、+178 / −29行 | 既存コード |
| テスト | `npm run conformance:check`（内部で`npm test`を実行する） | `1425 scenarios (1409 passed, 16 skipped)`、失敗0 | テスト出力 |
| 仕様 | `docs/specs/02_要件/03_外部連携要件.md`ほか2 file | updated | 既存文書 |
| commit前candidate | 11 file | working tree clean | Git index |
| Phase A artifact | `docs/reviews/127_課題1107のmerge方式既定値是正レビュー.md` | `H_impl` = `c66c6ed7`。`H_impl..H_final`の差分pathは本file 1件 | Git観測 |
| commit後external | PR、CI run、review | Step 11で観測する。**本節はPR作成前に書いており、外部証拠はまだ無い** | 外部のimmutable証拠 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **成立する。** `issue-1107 → req-gh-004 → ac-01..03 → scn-int-github-016/scn-unit-mergemethod-010 → merge-method-flag`の一方向である。本artifactへ自身のcommit SHAを書いていない。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_impl` = `c66c6ed7`は`H_final`のancestorであり、差分は本artifact 1 fileだけである。
- reviewer stable IDがPR author/provider観測済み`H_impl` author stable IDと異なる: **いいえ。** いずれも`adachi-tatsuru`である。9節を参照する。
- 既定branch追随を行った場合: **行っていない。** 基点`1db2a07a`は`origin/main`のtipであり、`比較基点..H_final`は2 commitの一直線である。

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/adapters/github.ts` | M | package | package | `mergeMethodFlag`は「方式1つ → flag 1つ」の純関数。**許可可否を判断しない。** `export`していない | pass。project policyもfilesystemも参照しない。`npm run architecture:check`合格 | REQ-GH-004 / AC-01 / SCN-INT-GITHUB-016 | **受理する方式を広げない。** `merge`・`rebase`・`squash`は従来どおり実行し、**未知値だけが例外になる。** 例外は`gh`実行の前に投げるため外部副作用が起きない。rollbackは当該2箇所のrevert | pass |
| `test/features/unit/merge-method-policy.feature` | M | package | package | scenarioを1件追加した（雛形の宣言）。**DISC-004で撤去したsource走査scenarioの跡が残っていない** | pass | AC-02 / SCN-UNIT-MERGEMETHOD-010 | fixtureは配布fileの読み取りだけである | pass |
| `test/features/integration/delivery-finalize.feature` | M | package | package | scenarioを1件追加した（未知方式で例外） | pass | AC-01 / SCN-INT-GITHUB-016 | 既存のgh stubを再利用し、実`gh`を呼ばない | pass |
| `test/steps/merge-method-policy.steps.ts` | M | package | package | step定義を3件追加し、`declaredMethods`で型を絞った | pass。既存step定義を書き換えていない | AC-02 | 同上 | pass |
| `test/steps/delivery-finalize.steps.ts` | M | package | package | step定義を2件追加した。既存の`stubPath`・`ghLog`を再利用する | pass | AC-01 | **ghのlogを読んで`pr merge`が呼ばれていないことを確認する。** 例外だけを見て終わらせない | pass |
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | M | project | doc | 1文を置換した。squashを許可する記述を削り、merge commitで行う理由を書いた | pass | REQ-GH-004 | 記述だけで実行authorityを持たない | pass |
| `.agent-skill-chain/policy/00_利用案内.md` | M | project | doc | squash専用レシピを削除し、**GitHub設定側の無効化**を要求する記述へ置き換えた | pass | REQ-GH-004 | 同上 | pass |
| `.agent-skill-chain/policy/sample.json` | M | project | config | `methods`と`branchMethods[].methods`を`["merge"]`だけにした | pass | AC-02 / SCN-UNIT-MERGEMETHOD-010 | **配布雛形であり本repositoryの実policyではない。** 本repositoryの`project-policy.json`を変更していない | pass |
| `docs/specs/02_要件/03_外部連携要件.md` | M | project | spec | REQ-GH-004へ既定値と雛形の規定を追記した | pass | REQ-GH-004 | 記述だけで実行authorityを持たない | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | REQ-GH-001行へSCN-INT-GITHUB-016、REQ-GH-004行へSCN-UNIT-MERGEMETHOD-010を結線し、**撤去したSCN-UNIT-MERGEMETHOD-009を外した** | pass | REQ-GH-001、REQ-GH-004 | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | project | spec | 1行追加した | pass | REQ-GH-004 / Issue #1107 | 同上 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **一致する。** `git diff --name-only 2bb0d82b c66c6ed7`が返す11 pathが上表の11行と同じである。**本artifactは`H_impl..H_final`の差分であり`比較基点..H_impl`に入らないため、個別監査の行にしない。**
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **成立する。** `mergeMethodFlag`はproject policyを参照しない。逆に**許可可否をpackage層のadapterへ持ち込まなかったことがDISC-003の是正の要点である。**
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: **DISC-003の是正で`src/adapters/github.ts`と、それに依存するtest 4 fileを再監査した。**

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

**実装中の発見が4件ある。**

**DISC-001。`src/domain/delivery.ts`の拒否logicは既に実装済みだった。** `methods`に含まれない方式（`:207-209`）と長命branchペアのsquash/rebase（`:210-216`）を**両方**拒否する。着手時の計画に含めていた「policy検証で落とす」は不要になり、対象外へ移した。**「機構が無い」を実測で否定した。**

**DISC-002。追加したstepが`@typescript-eslint/no-unsafe-assignment`で3件落ちた。** `declaredMethods`ヘルパーで型を絞って解消した。

**DISC-003（High）。adapterでsquashを無条件に例外にしたところ既存4件が落ちた。** `SCN-E2E-WFSTEP-015`・`035`・`037`と`SCN-INT-GITHUB-011`である。いずれもsquashを**fixtureとして**使う既存scenarioであり、落ちた理由は**policyで許可した方式をadapterが上書きしていた**ことである。00で宣言した案B（既定は拒否、明示宣言は残す）と実装が食い違っていた。案Bへ戻し、`squash`を明示のcaseとして受理して未知値だけを例外にした。4件が復旧した。

**DISC-004。source中の`"--squash"`字面0件を見る`SCN-UNIT-MERGEMETHOD-009`は、DISC-003の是正で誤った検査になった。** 撤去し、実挙動を見る`SCN-INT-GITHUB-016`へ差し替えた。

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-INT-GITHUB-016 | `mergeMethodFlag`の`throw` | `1 scenario (1 passed)` | pass | 未知方式`fast-forward`で例外になり、**ghのlogに`pr merge`が現れない。** 変異M1（未知値をsquashへ倒す旧実装へ復帰）で失敗する |
| AC-02 | SCN-UNIT-MERGEMETHOD-010 | `.agent-skill-chain/policy/sample.json` | `10 scenarios (10 passed)` | pass | `methods`と`branchMethods[].methods`の和集合が`["merge"]`である。変異M2・M3で失敗する |
| AC-03 | SCN-INT-MERGEMETHOD-003 | `src/domain/delivery.ts`（**変更していない**） | `6 scenarios (6 passed)` | pass | 長命branchペアのsquash拒否が変わっていない。**この経路へ1行も触れていないことが個別監査で確認できる** |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | merge方式は取り込み先の監査可能性を決める。**受理する方式を広げないことが安全条件である** | 変異M1で未知値をsquashへ倒すと`SCN-INT-GITHUB-016`が失敗する。受理する3方式は変更前後で同一である |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 拒否の診断が理由を述べないと、利用側は何を直せばよいか分からない | 例外文が`merge、rebase、squashのいずれかを指定してください`と次の操作を述べる。**規範文書側には`audit:check`の2区間導出が壊れる理由を書いた** |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 人が触れるUIを持たないCLIであり、出力はJSONだけである | `projectKind`が`cli` |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UI componentもthemeも持たず、色・間隔・typographyの決定を含まない | `capabilities.designTokens`が`not-applicable` |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 受理する3方式は変更前後で同一であり、**変わるのは未知値の扱いだけである**。`1425 scenarios`の失敗0がこれを裏付ける |
| 価値 | 利用者・運用上の目的を満たすか | pass | 利用者が報告した「このシステムを使うとsquashになってしまう」に対し、**配布物の中の矛盾を断つ**。規範文書がsquashを勧め、雛形が`["merge","squash"]`を配り、adapterの既定がsquashだった3点を揃えた |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 依存package、lockfile、外部の存在を1件も変えていない |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | **DISC-003で一度食い違い、00〜03とREQ-GH-004の本文を実装へ揃え直した。** `npm run trace:check`は`valid: true`、orphan 0件 |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 許可可否をadapterへ持ち込まず、`resolveMergeMethod`の責務を保った。rollbackは2箇所のrevertで完結する |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | `merge`・`rebase`・`squash`の3方式と未知値`fast-forward`の4入力を検査する。**squashが従来どおり通ることを既存4 scenarioが押さえている** |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 例外は`run("gh", ...)`の**前**に投げる。ghのlogに`pr merge`が現れないことを`SCN-INT-GITHUB-016`が確認する |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | 空文字と未知文字列はいずれも例外へ落ちる。`method`の型は3値へ絞られており、TS呼び出し元からは未知値が入らない |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | 方式は3値の完全一致でしか受理しない。例外文へ入力値を含めるが、shellへ渡さない |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | 許可可否をadapterで判断しない。**policyの決定をadapterが上書きしない**ことがDISC-003の是正で担保された |
| データ損失 | 上書き、削除、部分公開、履歴消失 | **finding（ADV-01、Medium、record-only）** | 本変更は配布雛形と規範文書を変えるが、**既にsquashで運用しているprojectの`project-policy.json`は変えない。** 更新しても既存構成は動き続ける。5節へ記録した |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | 11 fileのrevertで完結する |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | **finding（ADV-02、Medium、record-only）** | **製品側の強制だけでは足りない。** web UIと`gh pr merge`は`pr merge`を経由しないため、GitHubのrepository設定でsquash mergingを無効化しないとsquashは起き続ける。利用案内へ明記したが、**機構ではない。** 5節へ記録した |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| DISC-003 | **High** | adapterでsquashを無条件に例外にし、policyで許可した方式を上書きしていた。既存4件が失敗した | `SCN-E2E-WFSTEP-015`・`035`・`037`、`SCN-INT-GITHUB-011`の失敗 | 製品 | **修正した。** 案Bへ戻し、`squash`を明示のcaseとして受理して未知値だけを例外にした | valid / resolved | なし。4件が復旧し全体で失敗0 |
| ADV-01 | Medium | 既にsquashで運用しているprojectは、更新しても`project-policy.json`が変わらないため従来どおり動く | 変更対象に利用側のpolicy fileが含まれない | 配布先 | **修正しない。** 利用側の設定を製品が書き換えるべきではない。**移行は利用側の判断である** | valid / record-only | 更新しただけではsquash運用は止まらない。規範文書と雛形が新規projectを正しい既定へ導く |
| ADV-02 | Medium | web UIと`gh pr merge`は`pr merge`を経由しないため、製品側の強制が届かない | `src/adapters/github.ts`は`pr merge`経路にだけある | 配布先 | **修正しない。** GitHubのrepository設定は製品の管轄外である。利用案内へ明記した | valid / record-only | **規約であって機構ではない。** GitHub設定を強制する機構は本Issueの範囲外である |
| DISC-004 | Low | source走査scenarioがDISC-003の是正で誤った検査になった | 設計を案Bへ戻すと`--squash`の字面が正当に復活する | test | **撤去し実挙動のscenarioへ差し替えた** | valid / resolved | なし |

**未解決のCritical / Highは0件である。DISC-003はHighだったがラウンド1でresolvedにした。**

## 6. ラウンド固有の確認

### ラウンド1

- 対象: 実装差分`2bb0d82b..c66c6ed7`の11 file。
- 確認: 個別監査11行、AC-01〜03、肯定5観点、敵対8観点。
- 結果: blocking 0件。**DISC-003（High）はラウンド1の中で検出し是正した。** record-only 2件（ADV-01・ADV-02）。

### ラウンド2

- 対象: 本artifactを加えた版。
- 確認: 本artifactの記述が実観測と一致するかを全件突合する。行数、SHA、scenario件数、変異結果の4種を実コマンド出力と照合した。
- 結果: blocking 0件。

## 7. テスト結果

| 層・検査 | コマンド | シナリオ・件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---:|---:|---:|---:|---|
| 形式 | `npm run docs:format`、`npm run test:format` | 2 | 2 | 0 | 0 | pass |
| unit・integration・e2e（runner `cucumber-js`、dialect `en`） | `npm run conformance:check`（内部で`npm test`を実行する） | 1425 | 1409 | 0 | 16 | pass |
| 型・既存一式・配布物 | `npm run lint`・`format:check`・`typecheck`・`source:check`・`trace:check`・`architecture:check`・`skills:check`・`build`・`package:check` | 9 | 9 | 0 | 0 | pass |

**上の9本を1本ずつ実行し、それぞれの終了値で合否を取った。** 表の値は実行結果である。**#1147で「実行していない検査を合格と書いた」誤りを出したため、本Issueでは実行の有無を先に確かめている。**

**変異試験。** 3件を実施し3件ともkillした。

| ID | 変異 | 結果 | 復元後 |
|---|---|---|---|
| M1 | 未知値をsquashへ倒す（旧実装への復帰） | `10 scenarios (9 passed, 1 failed)` | `10 scenarios (10 passed)` |
| M2 | 配布雛形の`methods`へsquashを戻す | `10 scenarios (9 passed, 1 failed)` | `10 scenarios (10 passed)` |
| M3 | 配布雛形の`branchMethods`へsquashを戻す | `10 scenarios (9 passed, 1 failed)` | `10 scenarios (10 passed)` |

**復元は複写で行い`git checkout`を使っていない。**

**M2とM3を分けている理由。** `methods`だけを見る検査は`branchMethods`側のsquash復活を素通しする。**和集合で数えることでどちらの経路も塞ぐ。**

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/adapters/github.ts` | **入る**（`files`が`dist/src/`を列挙する） | 未知のmerge方式が既定値で実行されず例外になる。**3方式の実行は変わらない** |
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | **入る**（`files`が`.agent-skill-chain/docs/`を列挙する） | squashを許可する記述が消える |
| `.agent-skill-chain/policy/00_利用案内.md` | **入る**（`files`が`.agent-skill-chain/policy/`を列挙する） | squash専用レシピが消え、GitHub設定側の無効化要求が加わる |
| `.agent-skill-chain/policy/sample.json` | **入る**（同上） | 雛形のmerge方式が`merge`だけになる |
| `docs/specs/02_要件/03_外部連携要件.md` | **入る**（`files`が`docs/`を列挙する） | REQ-GH-004の記述が延びる |
| `docs/specs/15_要件追跡/00_追跡表.md` | **入る**（同上） | SCNの結線が変わる |
| `docs/specs/15_要件追跡/01_変更履歴.md` | **入る**（同上） | 1行増える |
| `test/` | 入らない | `package.json`の`files`が列挙しない |

判断: 配布物を更新した

根拠: `dist/src/adapters/github.js`が未知方式で例外を投げるようになる。**受理する3方式の実行は変わらない。** 配布する規範文書と雛形からsquashを許可する記述が消える。**既にsquashで運用しているprojectの`project-policy.json`は変更しないため、更新だけでは動作が変わらない**（ADV-01）。`npm run package:check`はexit 0である。

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | **Step 11で観測する。** 本節はPR作成前に書いており、現時点で外部reviewerのcommentもapprovalも存在しない |
| reviewerがPR author・実装commit authorと異なる | いいえ。いずれも`adachi-tatsuru`である |
| 観測したreview commentとapprovalの件数 | 現時点で0件・0件 |

**適用する例外は無い。** `.agent-skill-chain/review-exceptions.json`が持つ例外は`RVX-REPORTED-SUCCESS-WITHOUT-REVIEW-001`の1件だけであり、PR作成前の本時点では条件の判定自体ができない。

**残る事実を隠さず記録する。** implementerとreviewerが同一sessionであり、approval reviewは0件である。本artifactの`approved`は**AIによる最終裁定**であって人間の独立approvalではない。**本変更の方針はrepository ownerが「squashは基本NGとします」と明示決裁している。** mergeはそのauthorityに依存する。

**主観判断を承認根拠にしていないことが緩和である。** AC-01〜03の判定はscenario結果と変異試験の赤・緑に置いた。**とりわけDISC-003は、私の読解ではなく既存4 scenarioの赤が検出した。**

## 10. 仕様整合性

- 判定: **updated**
- 更新した仕様: `docs/specs/02_要件/03_外部連携要件.md`（REQ-GH-004）、`docs/specs/15_要件追跡/00_追跡表.md`、`docs/specs/15_要件追跡/01_変更履歴.md`。
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: **成立する。** 新規用語を追加していない。
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: **成立する。** `SCN-UNIT-MERGEMETHOD-009`は追加と同一PR内で撤去したため、廃止済み連番として扱う必要がない。
- 要件・変更・SCN・テストの追跡: REQ-GH-004 → AC-GH-004 → AC-01〜03 → SCN-INT-GITHUB-016、SCN-UNIT-MERGEMETHOD-010 → `delivery-finalize.feature`、`merge-method-policy.feature` → `src/adapters/github.ts`。`npm run trace:check`は`valid: true`、orphan 0件。
- `no-spec-impact`の場合の限定的根拠: 該当しない。**新しい観測可能な振る舞い（未知値の例外）と配布物の既定変更があるため要件本文を延ばした。**
- UI・トークンの判断: いずれも`not-applicable`。2.2節のとおり。

## 11. 総合判定と再開地点

- 未解決Critical/High: **0件。** DISC-003はHighだったがラウンド1でresolvedにした。
- Medium/Lowの記録: ADV-01・ADV-02（いずれもMedium、record-only）。DISC-004はresolved。
- 判定: **approved**（AIによる最終裁定。人間の独立approvalは0件であり、9節に事実として記録した。**方針はownerの明示決裁がある**）
- 新しい権限が必要な事項: **なし。**
- 残存リスク: 3件。(1) 既にsquash運用中のprojectは更新しても止まらない（ADV-01）。(2) web UIと`gh pr merge`へ製品側の強制が届かない（ADV-02）。(3) `docs/reviews/`のrole authority不整合（#1047へ委譲）。
- 次に許可される操作: **Step 11（`pr create`）。** その後CIが緑になってからmergeする。
- 次回の再開地点: pushした後、CIの結果確認から。**mainが動いていた場合は`pr create`を先に済ませてからrebaseする。**
