# 117 課題1004 監査境界の両親着地形を診断へ添える 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。**当初案を実装し、既存Scenarioの検出で棄却して再設計した。**

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1004 |
| 比較基点 | `2df6133b44db2d4b5edb87a4f8f75bcf61f9055b` |
| H_impl | `ea3c67fe2d20fa56f4aa88c1092d0710a1214a33` |
| 対象差分 | 検査script、unit feature、unit steps、仕様3件の6 file |
| 対象外 | 候補側の親の選択、比較基点の`merge-base`の相手、親3個以上のoctopus merge、`isReleaseBumpSide`のmerge認識、`src/` |
| 残り予算 | Step 10の上限3ラウンドのうち1を残す |
| ラウンド | Step 10 ラウンド2 |
| ラウンド数 | 2（Step 10のラウンド1〜2）。**Step 7のreadiness checkは3ラウンド予算に数えない** |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260903_055143_audit-checkの境界commitの候補branch側の親を着地形で判定する |
| モード | full（Q-01とQ-03が偽。公開仕様の本文を改訂し、監査の判定入力へ触れる） |
| 仕様の所有箇所 | `docs/specs/11_非機能/01_品質要件.md`のQLT-AUDSEL-001とQLT-AUDSEL-003 |
| 成果物行数 | 製品: `check_file_audit.ts`で56 insertions・7 deletions。test: 145 insertions。仕様: 4行。支援層: staging 00〜03が34000文字 |
| 縮小の先行評価 | 実施済み。**新しいCLI操作もflagも足さない。** 判定はcommit構造から導出でき、利用者からの追加入力を要求しない。**integration層へ同型を重ねない。** 既存のunit Scenarioは`initRepo()`で実Git repositoryを作っており、追随mergeも実commit構造として組める。**新しい判定不能状態を作らない。** 診断の追加だけで合否の境界を動かさない |
| authority | 変更しない。**`src/`への変更0行。CI・releaseのgate定義と`audit:check`の呼び出し位置も変えない** |
| 実施者・日時 | reviewer、2026-09-03（JST） |

### 0.1 routing入力契約

| role欄 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類、変異試験 | standard（riskがmedium） | claude | Opus 5、effort high | 実履歴の前後比較で`valid`が1件でも変わったら停止する | implementerとreviewerはいずれも本sessionのAI agentであり、**Git commit authorも同一である。§9に実態どおり記録し、独立性が成立したとは主張しない** |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 是正前の誤診断 | 実履歴`24347540`をHEADにして是正前scriptを実行 | 直前にmergeされた別PRの13 fileが「余分なpath」として並ぶ。真の理由（artifact未commit）が読み取れない | 実測 |
| 当初案が緩和を作ること | `SCN-INT-AUDITBUMP-004` | 着地形で候補側を選び直す案で`valid: false` → `valid: true`。祖先関係で選ぶ案でも同じ | 実測 |
| 判定の不変 | `main`直近30 merge + 非first-parent 25 merge = 55 commit | 是正前後で`valid`が**全件一致**。緩和**0件** | 実測 |
| 診断の改善 | 同55 commit | **20件**で注記が付いた。注記なしの不合格は4件 | 実測 |
| 選択の非変更 | `git diff` | `reviewHead`の導出（`.at(-1)`）と`base`の導出（`uniqueMergeBase(implementation, boundaryParents[0])`）を1行も変えていない | 実測 |
| `src/`の非変更 | `git diff --name-only origin/main HEAD` | `src/`0件。変更は`scripts/`1件、`test/`2件、`docs/specs/`3件 | 実測 |
| 変異の有効性 | 変異7件 | 6件kill、1件生存。生存はrelease bump親を持つ構成でしか区別できない診断の数値差 | 実測 |
| 全gate | `npm test`、`conformance:check` | 1415 scenario、1399成功、0失敗、16 skip。project rule 20件、orphan 0件 | 実行観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: pass。`commit構造 → 観測 → 注記`の一方向。artifact本文の申告値を導出へ使わない
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_impl`が直前commitであり、`H_impl..HEAD`の差分は本artifact 1 fileのみ
- reviewer stable IDがPR author/観測済み`H_impl` author stable IDと異なる: **異ならない。** §9に実態を記録した
- 既定branch追随: **実施済み。** 本artifactを作る前に`rebase --onto origin/main`で追随し、他Issueの履歴行への削除が0件であることを`git diff origin/main`で確認した

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `scripts/check_file_audit.ts` | M | project | project | 観測1関数と注記1関数を切り出し、4診断へ連結する | `candidateFinalPathCount` → `finalAuditPaths`の一方向。循環なし | AC-1004-01からAC-1004-04。QLT-AUDSEL-001、QLT-AUDSEL-003 | revertで復旧する | pass |
| `test/features/unit/audit-artifact-selection.feature` | M | project | project | Scenario 2件と既存Scenarioへ1 stepを足す | featureはstepsへ一方向。循環なし | SCN-UNIT-AUDITSEL-012からSCN-UNIT-AUDITSEL-014 | 同上 | pass |
| `test/steps/audit-artifact-selection.steps.ts` | M | project | project | fixture builder 1件、Given 2件、Then 3件 | stepsは検査scriptへ一方向。循環なし | 同上 | 同上 | pass |
| `docs/specs/11_非機能/01_品質要件.md` | M | package | spec | QLT-AUDSEL-001とQLT-AUDSEL-003の本文を実装の実態へ合わせる | 仕様は実装を参照しない。循環なし | 該当なし | 同上 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package | spec | SCN 2件を既存行へ足す | 追跡のみ。循環なし | 該当なし | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | 棄却した2案を含めて記録する | 履歴のみ。循環なし | 該当なし | 1行の削除で復旧する | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: pass（`git diff --name-only origin/main HEAD`が6件）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: pass
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: 修正は§2.0のとおり2件

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-1004-DESIGN-001 | **Issue本文の対象内「候補branch側の親を構造から区別する」を2通り実装し、いずれも従来の不合格を合格へ倒した** | 対象範囲とACの中心が変わる | あり | **選択を変えず診断だけを直すscopeへ縮小した。** 00・01・02・03を再確定した | `SCN-INT-AUDITBUMP-004`が当初案で`valid: true`、現行案で`valid: false` | QLT-AUDSEL-002の改訂を取り下げ、QLT-AUDSEL-001を「位置で選ぶ」明示側へ書き換えた | pass |
| DISC-1004-VACUOUS-001 | **最初に書いた「注記を付けない」Scenarioが空振りだった。** 合格するfixtureを使ったため`errors`が空で、常に注記を付ける変異が生存した | negative-testの検出力 | なし | 不合格になるfixtureへ差し替え、`valid`が`false`であることまでassertした。到達不能だった`selectedWellFormed`分岐を削除した | 変異「注記を常に付ける」が構成上作れなくなったこと | no-spec-impact | pass |

**なぜ当初案が緩和になるか。** `SCN-INT-AUDITBUMP-004`の構造は「実装 → 追随merge → review artifact commit → 追随merge」であり、HEADは最後の追随mergeである。第1親はreview artifact commitなので、その側の着地形は成立する。**そこを候補側と選ぶと、最後の追随mergeが取り込んだ既定branchの変更が監査範囲から外れたまま合格する。** 追随merge上のHEADはそもそも監査点として成立しておらず、選択を直しても正しくならない。

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1004-01 | SCN-UNIT-AUDITSEL-013 | `candidateSideNote`と`candidateFinalPathCount` | 合格。両親の件数を1件と2件で違えた | pass | §7 |
| AC-1004-02 | SCN-UNIT-AUDITSEL-012、SCN-UNIT-AUDITSEL-014 | 観測配列の長さ判定 | 合格。親1個と親3個の双方 | pass | §7 |
| AC-1004-03 | SCN-UNIT-AUDITSEL-001からSCN-UNIT-AUDITSEL-012、SCN-INT-AUDITBUMP-001からSCN-INT-AUDITBUMP-004 | 選択の非変更 | 合格。実履歴55 commitで`valid`が全件一致 | pass | §1 |
| AC-1004-04 | SCN-UNIT-AUDITSEL-013 | 注記の3行目と4行目 | 合格 | pass | §7 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | 監査の診断へ触れる。**当初案は監査範囲の導出そのものを変え、実測で緩和を起こした** | 実履歴55 commitで`valid`の不変を実証した。緩和0件 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 変更の目的が診断可能性そのものである | 注記3行。`SCN-UNIT-AUDITSEL-013`が文言と実件数をassertする |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 人が触れるUIを持たないCLIであり、出力はJSONだけである | `projectKind`が`cli` |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UI componentもthemeも持たず、色・間隔・typographyの決定を含まない | `capabilities.designTokens`が`not-applicable` |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | `24347540`の診断が真因へ到達できる形になった |
| 価値 | 利用者・運用上の目的を満たすか | pass | 実履歴20件で注記が付き、いずれも原因の読み替えを示す |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 追加の`git diff`は境界commitあたり最大2回の定数増 |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | **仕様を実装の実態へ合わせ直した。** 当初案の記述を残していない |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 観測は`finalAuditPaths`をそのまま呼び、判定基準を複写しない |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | **既存Scenarioが当初案の反例だった。** 現行案では55 commitで反例0件 |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | `git`失敗は既存の`allowFailure`経路のまま。注記は診断の付加であり合否を変えない |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | 親1個・2個・3個をScenarioで固定した |
| 悪用 | 診断の改善を口実に判定を緩める | pass | **これが本件の最大のリスクであり、実際に一度踏んだ。** 55 commitで`valid`の不変を実証した |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | 診断はcommit OIDとfile数だけを含む。artifact本文の申告値を導出へ使わない |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | 読み取り専用の判定である |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | revertで戻る。配布境界に入らないため公開済み資産を残さない |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | `audit:check`の唯一の呼び出し元はCIとlocalのnpm scriptであり、引数も出力形式も変えない |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| M-01 | Medium | **注記は選択を直さない。** 追随merge上でHEADを取った利用者は、注記を読んで自分でHEADを取り直す必要がある | 実装の読解 | 運用 | **機械的な是正を足さない。** 追随merge上のHEADは監査点として成立しておらず、自動で「正しい」HEADを選ぶ経路を作ると監査範囲が縮む | valid（設計判断。record-only） | 残存する |
| L-01 | Low | 変異1件が生存する。着地形の件数を`review head`でなく`parent`から数える変異 | 変異試験7件 | 診断の数値 | **反射的にfixtureを足さない。** release bump親を持つ構成でしか区別できず、影響は診断の数値差である。fixture構築費用が便益を上回る | valid（record-only） | 残存する |

**Critical/High 0件。**

## 6. ラウンド固有の確認

### ラウンド1（Step 10、2026-09-03、candidate HEAD = `H_impl`）

- 全評価基準を確認した: はい。肯定5観点・敵対8観点をすべて評価した
- 指摘を確定した: はい。M-01とL-01はいずれも記録のみ
- 次ラウンド対象のCritical/High: **なし**
- blocking: 0件

### ラウンド2

- 本artifactのcommitでHEADが動くため、次ラウンドで再固定する

### ラウンド3

- 未実施。**予算1を外部reviewの是正のために残す**

## 7. テスト結果

**承認証拠は成功CI runである。** 手書きの件数表はそれ自体が自己申告であり、承認証拠にならない。

実行したcommand: `docs:format`、`test:format`、`lint`、`format:check`、`typecheck`、`source:check`、`trace:check`、`architecture:check`、`skills:check`、`build`、`package:check`、`npm test`、`conformance:check`、`audit:check`、`issue validate`

全layerの合計: **1415 scenario、1399 成功、0 失敗、16 skip。** `conformance:check`はproject rule 20件・orphan 0件で合格。

失敗またはskipがある層: **skipした16 scenarioはsemantic graphのGraphQLite assetが未設定な環境に起因する。** 本差分はこの経路に触れない。

対応する成功CI run: PR作成後に取得し、merge前に現`H_final`へ束縛する。

**変異試験の全量。**

| 変異 | 結果 |
|---|---|
| 注記を常に空にする | kill |
| 親1個でも注記を付ける | kill |
| 親3個でも注記を付ける | **構成上作れなくした。** 判定を親の個数でなく観測配列の長さで行うため、`< 2`と`!== 2`が同値になる |
| 件数を常に0にする | kill |
| 第1親の観測を落とす | kill |
| 追随mergeの説明文を落とす | kill |
| 差分1件の非audit path経路から注記を外す | kill |
| 件数を`review head`でなく`parent`から数える | **生存。** L-01 |

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `scripts/check_file_audit.ts` | 入らない | `package.json`の`files`に`scripts/`を含まない |
| `test/features/unit/audit-artifact-selection.feature` | 入らない | 同上 |
| `test/steps/audit-artifact-selection.steps.ts` | 入らない | 同上 |
| `docs/specs/11_非機能/01_品質要件.md` | 入らない | `files`は`.agent-skill-chain/docs/`を含むが`docs/specs/`を含まない |
| `docs/specs/15_要件追跡/00_追跡表.md` | 入らない | 同上 |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | 同上 |

判断: 配布物を更新しない

根拠: `package.json`の`files`は`dist/bin/`、`dist/src/`、`dist/vendor/`、`.agent-skill-chain/00_利用案内.md`、`.agent-skill-chain/skills/`、`.agent-skill-chain/templates/`、`.agent-skill-chain/schemas/`、`.agent-skill-chain/policy/`、`.agent-skill-chain/docs/`、`README.md`、`AGENTS.md`である。**変更6 fileのいずれも含まれない。**

**releaseの起動と計画は分けて記録する。** `.github/workflows/release.yml`は`main`への`push`で起動する。その後`planAutoRelease`が配布digestの差分を見て計画を決める。**本変更は配布digestを変えないため、version bumpとtagは作られない見込みである。** merge後に実観測する。

**ロールバックの対象範囲を分けて記録する。**

| 対象 | revertで戻るか | 実行者 | 手順 | 完了確認 |
|---|---|---|---|---|
| repository状態 | **戻る** | 実行役 | 打ち消しPRを通常mergeする | `git diff`が元の状態と一致する |
| 公開済みtagとGitHub Release | **作られない見込み** | 該当しない | 配布digestが変わらない | `gh release list`に新tagが載らない |
| npm registryの公開物 | **公開されない** | 該当しない | 本経路では公開しない | `npm view`に該当versionが無い |

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | **本artifact作成時点では無い。** exact-headは`H_final`。immutable GitHub reviewは0件 |
| reviewerがPR author・実装commit authorと異なる | **異ならない。** implementerもreviewerも本sessionの同一AI agentであり、Git commit authorも同一である |
| 観測したreview commentとapprovalの件数 | 本session内のreview 2ラウンド（Critical/High 0、Medium 1、Low 1）。GitHub review 0件 |

**`02_品質基準.md`の「独立reviewが成立しない場合」に従う。** 同節は「既定は停止ではなく、無記録での通過の禁止である」と定める。

**ただし本件では、既存の自動Scenarioが独立した検出者として働いた。** `SCN-INT-AUDITBUMP-004`は本Issueと無関係に既存していたものであり、私の当初設計が作った緩和を私より先に検出した。これはreviewerの独立性の代替にはならないが、**同一actorの自己申告だけで通過していない証拠として記録する。**

**PR作成後の外部reviewを待つ。** 予算1ラウンドを残してある。

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/11_非機能/01_品質要件.md`のQLT-AUDSEL-001とQLT-AUDSEL-003、`docs/specs/15_要件追跡/00_追跡表.md`、`docs/specs/15_要件追跡/01_変更履歴.md`。**QLT-AUDSEL-002は変更していない**
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: pass。新規用語を導入していない
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: pass。「着地形」は仕様本文で「`H_impl..review head`がreview artifactちょうど1 file」と定義してから使っている
- 要件・変更・SCN・テストの追跡: REQ-GH-005 → AC-GH-005 → SCN-UNIT-AUDITSEL-013、SCN-UNIT-AUDITSEL-014
- UI・トークンの判断: UIを所有しないためdesign token・layout tokenは対象外。ADRを追加しない

## 11. 総合判定と再開地点

- 未解決Critical/High: **なし**
- Medium/Lowの記録: M-01（注記は選択を直さない）、L-01（変異1件生存）
- 判定: approved（Step 10 ラウンド2）
- 新しい権限が必要な事項: mergeは別authority
- 残存リスク:
  1. **注記は選択を直さない。** 利用者が自分でHEADを取り直す必要がある
  2. **変異1件が生存する。** release bump親を持つ構成での診断の数値差
  3. **当初案を一度出荷しかけた。** 既存Scenarioが検出したが、同型のScenarioが無い領域では同じ誤りが通る
- 次に許可される操作: push、PR作成、外部reviewの到着確認、必須check 2件の全緑確認、およびownerが承認したauthorityによる**通常merge commit方式**でのmerge。**squash mergeを使わない**
- 次回の再開地点: PR作成から
