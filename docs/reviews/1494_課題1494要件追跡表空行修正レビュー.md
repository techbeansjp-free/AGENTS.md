# 04 レビュー

> 肯定と敵対の両観点を毎ラウンド確認する。指摘なしの承認は有効。Medium/Lowは記録するだけで、自動修正・追加ラウンド・ゲート停止を起こさない。用語と責務の境界は`成果物用語と責務境界`（`.agent-skill-chain/docs/01_開発ワークフロー.md`）を正本とする。
>
> 埋めた成果物は`docs/reviews/`または`.agent-skill-chain/reviews/`へ置き、実装commitの後にその1 fileだけをcommitする。

| 読者 | 読む節 |
|---|---|
| 発注・評価する人 | 要約 → §5 指摘 → §11 総合判定 |
| 実装・レビューする人 | 全節 |
| 運用する人 | 要約 → §8 配布物影響 → §10 仕様整合性 |

## 要約

**この5行だけで、何を見たreviewかが分かるように書く。** 各行は1〜2文。詳細は対応する節が持つ。

| 項目 | 内容 |
|---|---|
| 何が問題だったか | `docs/specs/15_要件追跡/00_追跡表.md`の表本体に4箇所の空行が混入し、GFMの表継続条件を破っていた。GitHub・VS Code双方のMarkdownレンダラーで空行以降の全行がプレーンテキスト表示になっていた |
| 何を解決しようとしたか | 該当4箇所（元223・253・295・315行目）の空行を削除し、1つの連続した表へ復元する。見出し「## 共通既定値」前後の正当な空行は保持する |
| 何を行ったか | `docs/specs/15_要件追跡/00_追跡表.md`から空行4行だけを削除（追加・内容変更は無し） |
| 何を確認したか | `check_trace.ts`のvalid維持、`check_japanese_docs.ts`合格、lint/format:check/project:quality合格、独立reviewer（Codex、別process・別context）による削除行が空行のみであることとcontent-equivalence（SHA-256一致）の確認 |
| 判定 | approved |

## 1. 入力証拠

PR番号、Actions run ID、immutable review IDはPR作成後にしか存在しないため**この文書へ書かない。** `review evidence`とdelivery stateがappend-onlyで保持する。reviewerの独立性は`merge.reviewIndependence`が決める。既定の`context-isolated`はimplementerと別session/context、exact HEAD固定、対象差分の非変更、肯定・敵対reviewとfinding記録を要求し、同一GitHub actorでも成立する。このときtracked artifactの`approved`と保存済みreview session・Step 10 bindingがformal approvalになる。`actor-independent`はPR authorおよび観測済み`H_impl` commit authorと別のstable actor IDによるprovider `APPROVED`を要求する。両modeともexact HEAD一致は必須とし、tracked文書へ自身のcommit SHAを書かず、**H_final後はartifactを更新しない。**

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | .agent-skill-chain/tmp/issues/20260926_044552_要件追跡表の表中の空行を除去しGFM表示崩れを解消する | staging digest 90df091565040574c7eb27ef73a257be888233b7be3666cb48e4d53eb38e8e8f | 既存コード |
| 差分 | `f30decbd8b46f4c49b414cda4afaf2281a89cae7`..`02aa7614075a2a7f27c43078e39b0d5270057375` | 1 path | 既存コード |
| テスト | `npm run lint`、`npm run format:check`、`npm run project:quality`、`node --import tsx scripts/check_trace.ts`、`npm run docs:format` | 全件合格（本文書§7参照） | テスト出力 |
| 仕様 | `docs/specs/15_要件追跡/00_追跡表.md`は表示形式のみの記録文書であり、要件・AC・SCNの内容自体は所有しない | 変更なしを確認 | 既存文書 |
| commit前candidate | docs/specs/15_要件追跡/00_追跡表.md | H_impl 02aa7614075a2a7f27c43078e39b0d5270057375 | Git index |
| Phase A artifact | 本fileをcommit後に観測 | 未作成 | Git観測 |
| review session | .agent-skill-chain/tmp/issues/20260926_044552_要件追跡表の表中の空行を除去しGFM表示崩れを解消する | round 1実施済み | Git観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: はい。差分1 file・依存辺0本
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_final`確定後に`audit:check`で検証する
- reviewerの独立性が要求水準を満たす: はい。`merge.reviewIndependence`未宣言のため既定`context-isolated`。reviewer（Codex、`codex exec -s read-only`、別process・別provider・別context）はimplementer（Claude、本worktree session）と別context。対象差分を変更していないことを`git status`で確認済み
- 既定branch追随を行った場合、取り込みがartifact commitより前にあり、`比較基点`が取り込んだ既定branch tip、`H_impl`がartifact直前の最新commitを指し、個別監査表を`比較基点..H_impl`から再生成した: 該当なし（既定branch追随を行っていない。`worktree create`時のorigin/main SHAのままreview実施）

### 1.1 変更ファイル個別監査

基準SHAとの差分にある全ファイルを、生成物（`dist/`等）も含めて1ファイル1行で記録する。まとめ行、directory単位の一括承認、test成功だけの代替を認めない。`audit:check`の他の検査対象外となる生成物でも、各行へ生成元との対応確認方法と配布影響の確認方法を記録し、差分path集合と表のpath集合を一致させる。

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package（ASC本体） | docs/specs | 要件追跡表という記録文書の表示形式のみを修正。空行4行の削除だけで内容行は1文字も変更しない | spec → spec（同一file内の書式修正、循環無し） | 対象外。要件ID・AC ID・SCN IDの対応関係自体は変更していない（独立reviewerがcontent-equivalenceをSHA-256一致で確認） | システム仕様書の書式修正のみ。`git revert`で即時rollback可能 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: はい（1 path）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: はい（docs/specs内の書式修正のみ）
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: 該当なし（round 1でfinding無し）

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

発見IDは実装計画（fullは03、quick/pocは集約00）の`DISC-*`と同じ字面を使い、本文書内で別IDへ言い換えない。

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | 空行は当初1箇所（223行目）と見えたが、file全体を`awk 'NF==0{print NR}'`で走査した結果、同一表内に計4箇所（223・253・295・315行目）存在すると判明した。309・311行目の空行は「## 共通既定値」見出し前後の正当な区切りであり対象外と判定した | 対象範囲（4箇所すべてが表を破っていたか） | なし（対象範囲の確定のみ、契約変更なし） | 4箇所すべてを削除し正当な2箇所（309・311）は保持した | 削除後`awk`で残存空行が2,4,6,306,308のみ（表開始前とheading前後）であることを確認 | no-spec-impact（表示形式のみ） | pass |

### 2.1 受け入れ条件とシナリオ

本変更は正式なStep 1〜2（要件定義）を経由しない直接修正（owner承認済み、軽微な表示形式修正）であり、番号付きAC/SCNは新設していない。代わりに次の非形式的な受け入れ条件を用いた。

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01（表の連続性復元） | 非形式（独立reviewerによる直接確認） | `docs/specs/15_要件追跡/00_追跡表.md`から空行4行を削除 | pass | pass | 独立reviewer（Codex）が削除範囲内に空行以外が無いこと、表の行順・対応関係が保たれていることを確認 |
| AC-02（内容非変更） | 非形式（SHA-256比較） | 同上 | pass | pass | 独立reviewerが「空行を除外した変更前後の内容はSHA-256まで完全一致」と報告 |
| AC-03（既存整合性検査の維持） | 非形式（既存gate再実行） | `node --import tsx scripts/check_trace.ts` | pass | pass | `valid: true, errors: []`（変更前後で結果同一） |

### 2.2 開発考慮事項の適用判定（必須）

00の判定・理由・証拠から差分が無ければ、表の代わりに`開発考慮事項の適用判定は00_要求定義.md §6.1と同じ`の1行を置ける（01〜03と同じ参照行）。差分がある行だけを表に残してよい。**`review validate`はこの§2.2の内容を検証しない**（01〜03の`issue validate`と異なり、review artifactのDC判定に対する機械検証は無い）。記述量を減らすための人・エージェント向けの案内であり、参照行を置いても4行の表を書いても合否は変わらない。

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | not-applicable | 個人情報・秘密情報を含まない、既存の要件追跡表の書式修正のみ | 変更内容（空行削除）に秘密情報・個人情報が含まれないことを目視確認 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | not-applicable | ログ・監視・運用手順に触れない | 変更対象は静的なMarkdown記録文書のみ |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 画面契約・操作・a11y検証の対象を持たない。GitHub/エディタのMarkdownレンダリング崩れの是正であり、本プロジェクトが提供するUI仕様ではない | 対象外 |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | token・component・layoutに触れない | 対象外 |

## 3. 肯定的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ（要件と観測結果の一致） | pass | 表の連続性が復元され、GitHub/VS CodeのMarkdownレンダラーで正しく表として表示されることを目視確認 |
| 価値（利用者・運用上の目的） | pass | 要件追跡表を読む開発者・reviewerが、該当行以降を読める状態に戻る |
| 実現可能性（環境・依存・権限） | pass | 追加依存・環境変更なし。既存fileの空行削除のみ |
| 整合性（設計・コード・テスト・仕様） | pass | `check_trace.ts`の結果が変更前後で同一（valid: true, errors: []） |
| 保守性（責務・命名・変更容易性） | pass | 単一fileの書式修正のみで責務混入なし |

## 4. 敵対的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 反例（要件を破る入力・状態） | not-applicable | 入力を受け取る機能ではない（静的記録文書の書式修正） |
| 失敗経路（外部失敗・部分失敗） | not-applicable | 外部I/O・副作用を持たない |
| 境界値（空、最大、最小、重複、Unicode） | pass | 削除対象が「空行のみ」であることをbyte単位のdiffで確認（削除行に非空白文字が無いことを独立reviewerが確認） |
| 悪用（注入、経路脱出、権限外） | not-applicable | 対象外 |
| 安全性（認証、承認、秘密情報、Zero Trust） | not-applicable | 対象外 |
| データ損失（上書き、削除、部分公開、履歴消失） | pass | 削除は空行4行のみで内容行の削除・上書きが無いことをSHA-256比較で確認済み。`git revert`で即時rollback可能 |
| ロールバック（復旧参照、状態保持、再開可能性） | pass | 単一commitの単純な差分であり`git revert`で即時rollback可能 |
| 範囲漏れ（呼び出し元、利用側、配布物、文書） | pass | `docs/specs/15_要件追跡/00_追跡表.md`はビルド成果物・配布物に含まれない記録文書のみであり、他の呼び出し元・利用側は存在しない |

## 5. 指摘

指摘なし。

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: はい
- 指摘を確定した: はい（指摘なし）
- 次ラウンド対象のCritical/High: なし

### ラウンド2

対象外: round 1がfindings=0で収束したため、ラウンド2は実施しない / 検証証拠: review-session state（`review round --apply`実行結果、status=converged）

### ラウンド3

対象外: ラウンド2を実施していないため対象外 / 検証証拠: 同上

## 7. テスト結果

- 実行したcommandの一覧: `npm run lint`、`npm run format:check`、`npm run project:quality`、`node --import tsx scripts/check_trace.ts`、`npm run docs:format`
- 全layerの合計: 本変更はdocs/specsのみの書式修正のため、対象コード（src/test）への影響が無いことを`check_trace.ts`のvalid維持で確認した。フルtest suiteはCI（日本語文書・Gherkin・型・配布物の品質検証job）で実行される
- runner・Gherkin方言: 対象外（本変更はGherkin fileを持たない）

## 8. 配布物影響

packageとして配布する場合だけ記入する。配布境界はpackage manifestの配布file指定を正本とし、compileされて配布される`source`も含める。

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| docs/specs/15_要件追跡/00_追跡表.md | 入らない | なし |

判断: 配布物を更新しない

根拠: `docs/specs/`は開発時の記録文書であり、npm packageの配布file指定（`files`）に含まれない。runtimeの利用条件・CLI・APIの振る舞いに影響しない。

## 9. 独立reviewの成立

PR作成前に観測できるものだけを書く。immutable review IDやapproval件数は書かない。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated |
| その要求を満たすこと | はい |
| reviewerとimplementerのidentity・context比較 | implementer: Claude（Sonnet 5、本worktree session、product code編集）。reviewer: Codex（`codex exec -s read-only`、別process・別provider・別context起動、`git show`/`git diff`によるexact HEAD比較のみ） |
| reviewerが対象差分を変更していないこと | はい。reviewerは`-s read-only`（sandbox書込不可）で起動し、review後`git status`で対象差分（`docs/specs/15_要件追跡/00_追跡表.md`）に変更が無いことを確認した |

外部への不可逆な配布で外部証拠を要求され、かつ無い場合だけ次を記入する。承認元・承認者・承認日時・失効日時は正本を参照し複製しない。

| 項目 | 内容 |
|---|---|
| 適用する例外の識別子 | {正本fileのexceptionId} |
| 観測値 | {未実行と判定した根拠の実測値} |

## 10. 仕様整合性

- 判定: no-spec-impact
- 更新した仕様: なし
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: 該当なし（用語台帳への変更なし）
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: はい（内容の意味を一切変更していない）
- 要件・変更・SCN・テストの追跡: `check_trace.ts`のvalid（true）を変更前後で維持
- `no-spec-impact`の場合の限定的根拠: 削除した空行4行はいずれも表の内容（要件ID・AC ID・SCN ID等）を持たず、Markdownの表示形式にのみ影響する。要件・AC・SCNの対応関係、仕様の意味内容は一切変更していない
- UI・トークンの判断: 対象外（DC-UX・DC-TOKENS参照）

## 11. 総合判定と再開地点

- 未解決Critical/High: なし
- Medium/Lowの記録: なし（指摘なし）
- 判定: approved
- 新しい権限が必要な事項: なし
- 残存リスク: なし
- 次に許可される操作: review artifact commit（H_final）の作成、PR再push、merge
- 次回の再開地点: 該当なし（本変更で完結）

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 1 |
| 対象SHA・文書ダイジェスト | 02aa7614075a2a7f27c43078e39b0d5270057375 |
| 比較基点 | `f30decbd8b46f4c49b414cda4afaf2281a89cae7` |
| H_impl | `02aa7614075a2a7f27c43078e39b0d5270057375` |
| 対象差分 | docs/specs/15_要件追跡/00_追跡表.md |
| 対象外 | 比較基点に存在し変更されていない範囲 |
| 残り予算 | 5ラウンド（同一scope最大6回のうち1ラウンド消費、収束済み） |
| ラウンド数 | 1（`review round --apply`で記録、status=converged） |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260926_044552_要件追跡表の表中の空行を除去しGFM表示崩れを解消する |
| 仕様の所有箇所 | 該当なし: #1494（`docs/specs/15_要件追跡/00_追跡表.md`自身が対象であり、表示形式のみの修正のため上位仕様の所有箇所は無い） |
| 成果物行数 | 製品: `docs/specs/15_要件追跡/00_追跡表.md` 4行削除のみ（追加0）。支援層: 本review artifact1件。判断のある箇所だけを記述し閾値判定はしない |
| 縮小の先行評価 | 既存手段（`git revert`等）での代替は無い。空行削除という最小の変更そのものが対応であり、これ以上縮小できない |
| 実施者・日時 | implementer: Claude（Sonnet 5、本worktree session）2026-09-26。reviewer: Codex（`codex exec`、別context）2026-09-26。進行役: Claude（Sonnet 5、本session）が採否を確認・記録 |

`比較基点`と`H_impl`の値は40桁の小文字hexをbacktickで囲んだものだけにする。注記・branch名・短縮SHAを同じcellへ書かない。由来は別行へ書く。

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review（§3・§4）、finding分類（§5、今回は指摘なし） | routine（risk=trivial、scope=1 file・空行4行削除のみ。provider既定=medium reasoning effort、上限内） | codex（Codex Sol基本候補。ローカルLLM設定なしのため既定候補を使用） | provider既定modelをそのまま使用（`--model`で固定しない） | Codex利用不能ならOpus等別reviewerへ切替え理由をjournal evidenceへ記録する（今回は到達可能につき未発動） | implementer(Claude、本worktree session)とreviewer(Codex、別process起動、`-s read-only`でproduct code非変更)のidentity・provider・context分離を確認。review後`git status`で対象差分不変を確認 |
