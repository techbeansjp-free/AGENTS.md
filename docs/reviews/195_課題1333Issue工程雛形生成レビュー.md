# 04 レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

**本templateを埋めた成果物は版管理下へ置く。** 一時ステージングに置いたままでは`review evidence`も履歴監査も成立しない。`docs/reviews/`または`.agent-skill-chain/reviews/`配下へ複写し、実装commitの後にその1 fileだけをcommitする。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 2 |
| 対象SHA・文書ダイジェスト | b664a0069b69940e820c75ab1b75ecde50574415 |
| 比較基点 | `30c87cb334dc598ff58ed5c36d73904790679d82` |
| H_impl | `b664a0069b69940e820c75ab1b75ecde50574415` |
| 対象差分 | .agent-skill-chain/skills/step-04-issue-sync/SKILL.md、.agent-skill-chain/skills/step-08-design-sync/SKILL.md、.agent-skill-chain/skills/step-10-review/SKILL.md、dist/src/cli-usage.js、dist/src/cli.js、dist/src/domain/issue.js、dist/src/domain/review-artifact.js、docs/reviews/195_課題1333Issue工程雛形生成レビュー.md、docs/specs/01_システム概要/02_用語・略語.md、docs/specs/02_要件/00_要件一覧.md、docs/specs/02_要件/01_ワークフロー要件.md、docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md、docs/specs/15_要件追跡/00_追跡表.md、docs/specs/15_要件追跡/01_変更履歴.md、src/cli-usage.ts、src/cli.ts、src/domain/issue.ts、src/domain/review-artifact.ts、test/features/unit/issue-scaffolding.feature、test/steps/issue-scaffolding.steps.ts |
| 対象外 | 比較基点に存在し変更されていない範囲 |
| 残り予算 | 0（収束後の取り直しroundを使用済み） |
| ラウンド数 | 2 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260911_174814_policy事前充填-Issue同期本文-review-artifact雛形を生成する |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md` REQ-WF-015、`docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` |
| 成果物行数 | 実装・配布・test・仕様・前round artifactを含む20 path、比較基点後4 commit |
| 縮小の先行評価 | 既存template・staging validator・GitHub adapter・descriptor固定writerを流用し、新schemaと新provider経路は追加しなかった |
| 実施者・日時 | implementer兼ローカルreviewer: Codex、2026-09-11T18:50:00+09:00。外部reviewer: CodeRabbit（PR #1346、round 2へ6件記録） |

`比較基点`と`H_impl`の2行は`review reanchor`と`audit:check`が機械的に読む。値は40桁の小文字hexをbacktickで囲んだものだけにし、注記・branch名・短縮SHAを同じcellへ書かない（識別行が一意に解決できず`identity-unresolvable`で拒否される）。由来の説明は`比較基点の由来`のような別行へ書く。

### 0.1 routing入力契約

providerとmodel設定はproject choiceとrouting evidenceの観測値を用い、固有のmodel slugだけからreview authorityを推測しない。

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類 | critical（risk=artifact） | ローカルCodexとPR上のCodeRabbit | project choiceのcritical mapping | Critical/High未解決なら停止 | CodeRabbitはimplementerと別contextで差分を変更していない。指摘反映後のexact H_final再reviewをmerge条件とする |

## 1. 入力証拠

レビュー証拠は二段階で完成させる。**Phase Aはtracked review artifactであり、PR作成前に確定する。** 製品・仕様・testを固定した`H_impl`の後、tracked review artifactだけを加えた`H_final`を作る。`H_impl`は`H_final`のancestor、commit間差分はproject policyが選ぶevidence-only pathだけとし、artifact path、SHA-256、Git blob OIDを観測する。**Phase Aで観測するのは`H_impl` commit SHA/author ID、repository、review sessionのanchorとroundである。** **PR number、Actions run ID、immutable review IDはPR作成後にしか存在しないので、tracked review artifactへ書かない**。旧版はこれらをPhase Aへ要求しつつ「H_final後はartifactを更新しない」と定めていたため、**PR作成前は埋められず作成後は直せない循環になっていた。**

**Phase Bはtracked artifactの外にある。** PR作成後、trusted providerからPR number/current head/author ID、Actions run ID/event/head/conclusion/関連PR番号、immutable review ID/commit/user ID/submittedAt/stateを実観測し、**`review evidence`とdelivery stateへappend-onlyで記録する。** PR・成功CI・承認reviewは`H_final`へ一致させる。**完了はtrusted providerの外部attestationだけで記録し、head変更時は外部証拠を作り直す。** caller申告actor、任意JSON、別PRのrun、COMMENTED、未完了CIは承認証拠にしない。

**reviewerの独立性はproject policyの`merge.reviewIndependence`が決める**。`context-isolated`（未宣言時の既定）はimplementerと別session/contextであること、exact HEADを固定したこと、reviewerが対象差分を変更していないこと、肯定・敵対レビューとfindingの記録を要求する。**同一GitHub actorでも成立する。** `actor-independent`はPR authorおよびobserved implementation commit authorと別のstable actor IDを要求し、**高リスク変更・不可逆操作・release・外部公開でproject policyが宣言して引き上げる。** **どちらのモードでもAPPROVED verdictとexact HEAD一致は必須である。** tracked文書へ自身のcommit SHAを書かず、H_final後はartifactを更新しない。

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | .agent-skill-chain/tmp/issues/20260911_174814_policy事前充填-Issue同期本文-review-artifact雛形を生成する | staging digest 18cb5115d08948394494006b44b3875e5b9699cc2d52a07e3f05b49ea15a1e62 | 既存コード |
| 差分 | `30c87cb334dc598ff58ed5c36d73904790679d82`..`b664a0069b69940e820c75ab1b75ecde50574415` | 20 path | 既存コード |
| テスト | `npm test -- --format summary`ほか | round 1で1824 scenarios中1808 passed・16 skipped・失敗0。round 2対象9 scenarios・48 steps合格 | テスト出力 |
| 仕様 | REQ-WF-015、TERM-ASC-102・103、追跡表 | updated、trace check合格 | 既存文書 |
| commit前candidate | 比較基点からの対象差分20 path | H_impl b664a0069b69940e820c75ab1b75ecde50574415 | Git index |
| Phase A artifact | 本fileをcommit後に観測 | 未作成 | Git観測 |
| review session | .agent-skill-chain/tmp/issues/20260911_174814_policy事前充填-Issue同期本文-review-artifact雛形を生成する | session a95aa923c09735bdc29e133a17c2bcad21f3b7923dd5b324e9cc48e0e6fc3361、round digest 00e5e0866f5fd3c53295215ccf5596f62121171111d63a6cf2f8866147e359fe | Git観測 |

**Phase Bの外部証拠をこの表へ書かない。** PR number、Actions run ID、immutable review IDはPR作成後にしか存在しない。**それらは`review evidence`とdelivery stateがappend-onlyで保持する正本であり、tracked review artifactは参照先を持たない。**

重要判断を推論だけで承認しない。新しい権限が必要な場合は、対象操作と決定権者を明示する。

- dependency/authority/evidence graphにcycle、self-loop、unknown node、tracked artifact自己SHAがない: はい。candidate自己評価の限定は§9へ開示
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: artifact commit後に`audit:check`で確認する
- reviewerの独立性が要求水準を満たす: CodeRabbitが別contextでround 1 HEADをreviewした。指摘反映後のexact H_final再reviewをPhase Bのmerge条件にする
- Phase BのPR/CI/review一致: PR作成後に`review evidence`で観測する
- 既定branch追随: なし。比較基点は作業開始時main `30c87cb334dc598ff58ed5c36d73904790679d82`

## 変更ファイル個別監査

追加・変更・削除した全ファイルを省略せず1ファイル1行で記録する。まとめ行、directory単位の一括承認、test成功だけによる代替を認めない。

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/skills/step-04-issue-sync/SKILL.md` | M | package | skill | Step 4生成経路 | CLI参照のみ | REQ-WF-015 | 手動body-fileへrollback可 | pass |
| `.agent-skill-chain/skills/step-08-design-sync/SKILL.md` | M | package | skill | Step 8生成経路 | CLI参照のみ | REQ-WF-015 | 手動body-fileへrollback可 | pass |
| `.agent-skill-chain/skills/step-10-review/SKILL.md` | M | package | skill | Step 10雛形経路 | CLI参照のみ | REQ-WF-015 | optional command | pass |
| `docs/reviews/195_課題1333Issue工程雛形生成レビュー.md` | A | package | review evidence | post-terminal roundの前回artifact | H_implまでの履歴に存在 | REQ-WF-015 | 本roundのartifact-only commitで更新 | pass |
| `docs/specs/01_システム概要/02_用語・略語.md` | M | package | spec | 用語102・103 | 要件参照 | REQ-WF-015 | 文書差分revert可 | pass |
| `docs/specs/02_要件/00_要件一覧.md` | M | package | spec | 要件索引 | 詳細要件参照 | REQ-WF-015 | 文書差分revert可 | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | package | spec | 機能要件正本 | CLI・domainへ下向き | REQ-WF-015 | 文書差分revert可 | pass |
| `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | M | package | spec | CLI契約 | usageと一致 | REQ-WF-015 | body-file互換あり | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package | trace | AC・SCN追跡 | 実装参照 | REQ-WF-015 | trace check合格 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | trace | 変更判断 | 要件参照 | REQ-WF-015 | 履歴保持 | pass |
| `src/cli-usage.ts` | M | package | interface | usage正本 | cliから参照 | REQ-WF-015 | 既存flag不変 | pass |
| `src/cli.ts` | M | package | interface | command orchestration | domainへ下向き | REQ-WF-015 | apply前拒否・再実行可 | pass |
| `src/domain/issue.ts` | M | package | domain | staging・同期本文 | fsとvalidator利用 | REQ-WF-015 | 決定論的生成 | pass |
| `src/domain/review-artifact.ts` | A | package | domain | 純粋描画・包含判定 | node:pathのみ | REQ-WF-015 | 副作用なし | pass |
| `test/features/unit/issue-scaffolding.feature` | A | package | test | AC反例 | step定義へ依存 | SCN 6件 | SCN-UNIT-ISSUESYNC-001で決定性検証 | pass |
| `test/steps/issue-scaffolding.steps.ts` | A | package | test | BDD実装 | domainへ下向き | SCN 6件 | temp fixture | pass |

- 基準SHAとの差分は20 path。`audit:check`契約で生成`dist/` 4件を除いた個別監査対象16 pathが表と完全一致する
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: はい
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: round 2の10 pathを対象9 scenarios、typecheck、lint、docs、trace、buildで再検証

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

モード別実装計画（fullは03、quickとpocは集約00）の発見記録を全件転記せずIDで参照し、最終状態とEvidenceを確認する。目的、scope、受け入れ条件、security境界、不可逆操作を変えなかった発見は上流再起動を要求しない。契約を変えた発見は、影響する成果物だけが再確定されていることを確認する。

発見IDはモード別実装計画（fullは03、quickとpocは集約00）の`DISC-*`と同じ字面を使い、本文書内で別IDに言い換えない。

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | policy manifest不在fixtureで`issue create`が失敗した | 既存互換性 | なし | manifest存在時だけchoicesを読む | 全テストのSCN-E2E-POC-002を含め失敗0 | no-spec-impact | pass |
| DISC-002 | review artifactの字句上repository内pathがsymlink親で外部へ解決できた | security境界・AC-WF-015 | security境界の明確化 | real rootから期待する親とreal parentの一致を要求 | SCN-UNIT-REVIEWARTIFACT-003 | updated | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-WF-015 | SCN-UNIT-ISSUEPREFILL-001、SCN-UNIT-ISSUESYNC-001 | `src/domain/issue.ts` | 対象feature 9 scenarios合格 | pass | mode/checkpoint全4組 |
| AC-WF-015 | SCN-UNIT-REVIEWARTIFACT-001〜004 | `src/domain/review-artifact.ts`、`src/cli.ts` | 対象9 scenarios合格 | pass | A/M/D・未確定・symlink外部解決・別root staging拒否 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | pathとGitHub本文を扱う | descriptor固定writer、digest再照合、SCN-UNIT-REVIEWARTIFACT-003 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 生成元とdigestを運用判断へ返す | JSONへbody SHA・artifact・mode・checkpointを出力。秘密本文は出さない |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | GUI・Web画面・a11y対象を所有しないNode CLI | package.jsonのbinとCLI usage |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | 視覚component・layoutを所有しない | projectKind=cli |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 全mode/checkpointとA/M/DをBDDで固定 |
| 価値 | 利用者・運用上の目的を満たすか | pass | 反復転記を3 commandで機械化 |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 既存Node・Git・GitHub adapterだけを使用 |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | trace・CLI・workflow・package gate合格 |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 描画と対象集合をdomain関数へ分離 |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | mode/checkpoint不一致、digest drift、判定未確定を検査 |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | provider前再照合、temp cleanup、排他的作成 |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | empty差分拒否、safeSlug・escapeCell・既存validatorを再利用 |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | H-01修正でsymlink祖先を拒否 |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | apply authorizationと既存GitHub adapterを維持 |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | artifact既存fileを拒否、body-file経路を維持 |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | full Step 4はlocal-active保持、失敗時再実行可能 |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | dist・3 skill・CLI usage・仕様追跡を更新 |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| H-01 | High | artifact出力の字句包含後に親realpathのrepository包含を確認せず、内部symlinkから外部へ書けた | `src/cli.ts`の旧outParent判定 | review artifact出力 | real rootから期待する親とreal parentを一致照合し反例SCNを追加 | resolved | なし |
| CR-3988169406 | Medium | full Step 4の旧CLI契約記述が生成経路と矛盾 | CodeRabbit inline review | CLI仕様 | 生成経路とbody-file経路の契約を明確化 | resolved | なし |
| CR-3988169412 | High | 対象20 pathに対し個別監査15行 | CodeRabbit inline review | review artifact | 生成dist 4件とartifact自身は個別監査の除外対象で、§8と`audit:check`が担うことを確認 | false-positive | なし |
| CR-3988169417 | High | context-isolated reviewの成立証拠が未確定 | CodeRabbit inline review | merge認可 | CodeRabbitの別context reviewをround 2へ記録し、final exact HEAD再reviewをmerge条件化 | resolved | final HEADのPhase B観測が必要 |
| CR-3988169423 | Medium | `--init`のusage fallbackが必須と誤読可能 | CodeRabbit inline review | CLI usage | 未指定時は拒否すると明記 | resolved | なし |
| CR-3988169443 | High | stagingからrootを逆算すると任意rootを受理できる | CodeRabbit inline review | path信頼境界 | `--root`またはcwdへ固定し規定issues directory直下だけを受理。反例SCN追加 | resolved | なし |
| CR-3988169451 | Medium | exclusive write失敗の診断がround用 | CodeRabbit inline review | 障害診断 | review artifact固有の残存entry確認診断へ変更 | resolved | なし |

指摘なしの場合は「指摘なし」と明記する。

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: はい。修正後H_implで肯定5・敵対8観点を再確認
- 指摘を確定した: H-01をresolvedとして確定
- 次ラウンド対象のCritical/High: なし

### ラウンド2

- 未解決Critical/High: なし
- 修正差分: CodeRabbit 6件を前進commit `b664a0069b69940e820c75ab1b75ecde50574415`へ反映。うち5件resolved、生成dist監査1件false-positive
- 修正で触れた隣接範囲: CLI usage、root/staging path境界、review artifact描画、追跡表、配布dist
- 既承認・未変更範囲を再走査していない: はい。対象9 scenarios、typecheck、lint、docs、trace、buildだけを再検証

### ラウンド3

- 全指摘の最終分類: 6件resolved、1件false-positive
- 任意の危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: 新commandは明示呼出し・既存file拒否
- 同じ範囲の予算を自動更新していない: はい
- AIによる最終裁定: approved。PR後の外部reviewをmerge条件とする

## 7. テスト結果

- 実行したcommandの一覧: round 1で`npm test -- --format summary`、typecheck、lint、format、source、docs、Gherkin、trace、architecture、conformance、package、CLI、workflow、skill gate。round 2で`npm test -- --tags @issue-scaffolding`、typecheck、lint、docs、trace、build
- 全layerの合計: round 1は1824 scenarios中1808 passed・16 skipped・失敗0。round 2は対象9 scenarios・48 steps合格
- runner・Gherkin方言: cucumber-js・en

## 8. 配布物影響

projectがpackageとして配布される場合だけ記入する。配布境界はpackage manifestの配布file指定を単一正本とし、compileされて配布される`source`も配布境界に含める。

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `dist/src/cli.js`、`dist/src/cli-usage.js`、`dist/src/domain/issue.js`、`dist/src/domain/review-artifact.js` | 入る | 3生成経路をCLIから利用可能 |
| `.agent-skill-chain/skills/step-04-issue-sync/SKILL.md`、`.agent-skill-chain/skills/step-08-design-sync/SKILL.md`、`.agent-skill-chain/skills/step-10-review/SKILL.md` | 入る | 各Stepで生成commandを案内 |
| `src/cli-usage.ts`、`src/cli.ts`、`src/domain/issue.ts`、`src/domain/review-artifact.ts` | 入る | compile対象sourceとして配布境界に含まれる |

判断: 配布物を更新した
根拠: `npm run build`と`npm run package:check`が生成distと配布inventoryを検証した

**runtimeの挙動が変わるのに配布文書が追随していない状態を残さない。** 利用者は配布物だけを読むため、repository内部の仕様書へ書いても届かない。

## 9. 独立reviewの成立

**この節はPhase Aで埋める。** 記入するのはPR作成前に観測できるものだけであり、**immutable review IDやapprovalの件数などPR作成後にしか存在しない値は書かない。** それらはPhase Bで`review evidence`が観測して記録する。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated |
| その要求を満たすこと | はい。CodeRabbitはimplementerと別contextでPR差分をreviewした。指摘反映後のexact H_final承認はPhase Bで再観測する |
| reviewerとimplementerのidentity・context比較 | implementerはCodex、外部reviewerはGitHub AppのCodeRabbitで別context |
| reviewerが対象差分を変更していないこと | はい。CodeRabbitは指摘のみ行い、修正はimplementerが前進commitで実施した |

外部への不可逆な配布で独立reviewの外部証拠を要求され、かつそれが無い場合だけ記入する。

| 項目 | 内容 |
|---|---|
| 適用する例外の識別子 | 該当なし |
| 観測値 | PR #1346のCodeRabbit reviewをround 2へ取り込み済み。immutable review IDとfinal exact HEAD一致はPhase B正本へ記録する |

**承認元、承認者、承認日時、失効日時は正本を参照し、ここへ複製しない。**

例外が正本に無い、または失効している場合は進めない。記録があれば実装・PR・mergeは進み、mergeを契機に自動で走るtag作成とReleaseも進む。止めても不可逆な行為を防げないためである。package registryへの公開など外部への不可逆な配布だけが、記録の有無にかかわらず独立reviewの外部証拠を要求する。削除、force push、履歴書き換えは独立reviewの対象外であり、既存のauthority経路で守る。

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: REQ-WF-015、CLI契約、TERM-ASC-102・103、追跡表、変更履歴
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: はい
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: trace check合格
- 要件・変更・SCN・テストの追跡: REQ-WF-015→AC-WF-015→6 SCN→Feature・実装を追跡
- `no-spec-impact`の場合の限定的根拠: 該当なし
- UI・トークンの判断: Node CLIで画面・視覚tokenを所有せずnot-applicable

## 11. 総合判定と再開地点

- 未解決Critical/High: 0件
- Medium/Lowの記録: 3件resolved
- 判定: approved
- 新しい権限が必要な事項: PR作成・mergeは既存automatic delivery authorityを使用
- 残存リスク: 指摘反映後のexact H_finalに対するCodeRabbit再reviewとCIをPhase Bで観測してからmergeする
- 次に許可される操作: review artifactだけのcommit、Step 10 post-terminal記録、PR更新
- 次回の再開地点: Step 10 post-terminal intake後、Step 11 merge再認可
