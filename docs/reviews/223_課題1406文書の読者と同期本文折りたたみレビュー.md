# 04 レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

**本templateを埋めた成果物は版管理下へ置く。** 一時ステージングに置いたままでは`review evidence`も履歴監査も成立しない。`docs/reviews/`または`.agent-skill-chain/reviews/`配下へ複写し、実装commitの後にその1 fileだけをcommitする。

## 要約

| 項目 | 内容 |
|---|---|
| 何が問題だったか | full modeのIssue本文が00〜03の連結で715〜888行・最大47,215字になり、要求を読みに来た発注・評価する人が設計と実装計画を連結で受け取っていた。各documentに読者の定義が無く、何を削るかを判断する基準も無かった（Issue #1406、#940） |
| 何を解決しようとしたか | 生成する全documentを読者と読む順のある要点へ縮約する。round 2でownerが対象を「今後新しく生成されるすべてのdocument」へ広げ、04レビュー・11 PR本文・11事前確認・12利用案内を追加し、理由の全面禁止をやめて任意とした |
| 何を行ったか | `renderIssueSyncBody`でfull Step 8を00＋折りたたみ01〜03にし、外部review指摘を受けて`escapeFoldBoundary`でcode外の生の`<details>`・`</details>`を実体参照へ置き換えた。配布する10雛形すべてへ読者表と読む順を置き、7雛形で管理情報を末尾へ移し、04へ要約節を新設した。規範へ読者定義と書きぶりの既定を置き、概要・索引・要件・用語台帳・追跡を更新し、SCN-UNIT-ISSUETPL-013/014で節順・読者表・要約を構造で固定した（分量の閾値は置かない） |
| 何を確認したか | reviewer実測で`npm test` 2,077 scenarios（2,061 passed、16 skipped、失敗0。`f9eff7d3`で単独実行）、静的検査12種＋`conformance:check`、対象unit 76 scenarios、雛形総量1,432行（基点1,434行未満）。新assertionの実効性は3変異で確認し、`escapeFoldBoundary`へは3回で延べ50入力（最終形は18形）を当てて是正前後の観測が一致することを確かめた。実stagingの同期本文でcode外の`<details>`3件と`</details>`3件が釣り合い、契約を持つ正本4箇所が同じ向きにそろうことを全件grepで確認した |
| 判定 | approved（§11と一致。未解決Critical/Highなし。validで残るのはM-02・M-09・L-01・L-09の4件で、M-09は`review round --apply`の前にStep 9を再記録する運用事項） |

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 2 |
| 対象SHA・文書ダイジェスト | 50fddeb3bfb6333e4b17a2f25cd0a58ea5cf2a47 |
| 比較基点 | `d499a5ef6a188a010e10c9e3017bedf46956ee6d` |
| H_impl | `50fddeb3bfb6333e4b17a2f25cd0a58ea5cf2a47` |
| 対象差分 | 比較基点..H_implの29 path。`.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/templates/issue/`の10件、`dist/src/domain/issue.js`、`src/domain/issue.ts`、`docs/specs/`の10件、`test/`の6件 |
| 対象外 | 比較基点に存在し変更されていない範囲。round 1で承認済みで`b7bf8deb`以降に差分が無い7 path（`docs/specs/00_仕様書構成/00_仕様書索引.md`、`01_システム概要/00_概要.md`、`02_要件/00_要件一覧.md`、`02_要件/01_ワークフロー要件.md`、`04_機能/01_ワークフローv0.3.md`、`test/features/unit/document-reader.feature`、`test/steps/document-reader.steps.ts`）は再走査しない。round 2で変更した残り21 pathはすべて再監査した |
| 残り予算 | 1ラウンド（03 §8の3ラウンド計画のうち2を実施。有限レビュー契約の上限6 counted roundに対しては4ラウンド残） |
| ラウンド数 | 2 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260916_090913_実装中に生成する全documentを読者を定めた要点のみへ縮約する |
| 仕様の所有箇所 | `docs/specs/02_要件/04_仕様・品質管理要件.md` REQ-SQ-024「**分量を判断の量へ比例させる。**…**これは記録の省略を許すものではない。**…既存の受理集合を1件も変えない」「**比率に閾値を設けて自動停止させない。**」。`docs/specs/02_要件/01_ワークフロー要件.md` REQ-WF-015「`issue sync --generate-body`は保存済みartifact inventoryとdigest、mode、checkpoint、projectのGherkin方言による検証結果から同期本文を決定論的に生成する」。`docs/specs/01_システム概要/02_用語・略語.md` TERM-ASC-102「同期本文: 検証済みIssue stagingからmodeとcheckpointの規定順で選んだ成果物を連結し、耐久Issue本文の全面置換へ渡す決定論的text」 |
| 成果物行数 | `git diff --numstat d499a5ef 51a9ec48`のreviewer実測。製品（`src/`・`.agent-skill-chain/`・`dist/`）は+475/−270の745行（雛形10件526、規範18、`src/domain/issue.ts` 112、`dist/src/domain/issue.js` 89）。支援層は+770/−10の780行（`test/` 6 file 669、`docs/specs/` 10 file 111）に本artifact約340行を加えた約1,120行。配布雛形の総量は1,434→1,432行、full複写直後は752→729行（reviewerが`wc -l`と隔離repositoryの複写で実測）。閾値判定はしない |
| 縮小の先行評価 | 02 §12で7案を評価済み。01〜03をIssue本文から外す案は耐久化先を失い、Issueコメントへ退避する案はadapterという新機構が要り、雛形へ「短く書け」と追記する案は#1137で2日で失効した。`issueRequiredHeadings`の縮小と節番号の付け替えはINV-01に反する。round 2の追加分とH-02・M-05の是正も新しいgate・CI step・adapterを作らず、既存の雛形・規範・unit SCNの範囲で行っている（構造assertionのみで分量の閾値は置かない）。`templates/specs/`52件は最大43行・表中心で縮約余地が無いため対象外 |
| 実施者・日時 | reviewer（Claude、context B。implementerのcontext Aとは別session）、2026-09-16T05:21:32Z |

`比較基点`と`H_impl`の2行は`review reanchor`と`audit:check`が機械的に読む。値は40桁の小文字hexをbacktickで囲んだものだけにし、注記・branch名・短縮SHAを同じcellへ書かない（識別行が一意に解決できず`identity-unresolvable`で拒否される）。由来の説明は`比較基点の由来`のような別行へ書く。

### 0.1 routing入力契約

providerとmodel設定はproject choiceとrouting evidenceの観測値を用い、固有のmodel slugだけからreview authorityを推測しない。

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review（§3・§4）、finding分類（§5） | critical（`project/choices/development.json` `roleContracts`の全4軸がcritical） | claude（同choice `modelMapping.roles.reviewer.provider`の観測値） | project_default/high/standard（同choiceの観測値） | context Bが起動できない場合は停止（03 §0.1）。本roundは起動済み | `merge.reviewIndependence`は未宣言（`project-policy.json`の`merge`は空object）のため既定`context-isolated`。implementerは進行役session（context A）、reviewerは別subagent session（context B）。exact HEAD `51a9ec48`を固定。変異試験で触れた3 fileは複写で復元済みで、`git status --short`は本artifact1件だけが未追跡（対象差分29 pathは不変） |

## 1. 入力証拠

レビュー証拠は二段階で完成させる。**Phase Aはtracked review artifactであり、PR作成前に確定する。** 製品・仕様・testを固定した`H_impl`の後、tracked review artifactだけを加えた`H_final`を作る。`H_impl`は`H_final`のancestor、commit間差分はproject policyが選ぶevidence-only pathだけとし、artifact path、SHA-256、Git blob OIDを観測する。**Phase Aで観測するのは`H_impl` commit SHA/author ID、repository、review sessionのanchorとroundである。** **PR number、Actions run ID、immutable review IDはPR作成後にしか存在しないので、tracked review artifactへ書かない**。旧版はこれらをPhase Aへ要求しつつ「H_final後はartifactを更新しない」と定めていたため、**PR作成前は埋められず作成後は直せない循環になっていた。**

**Phase Bはtracked artifactの外にある。** PR作成後、trusted providerからPR number/current head/author ID、Actions run ID/event/head/conclusion/関連PR番号を実観測し、**`review evidence`とdelivery stateへappend-onlyで記録する。** `actor-independent`ではimmutable review ID/commit/user ID/submittedAt/stateもproviderから観測する。PR・成功CIは`H_final`へ一致させ、approvalは下のmode別権限に従う。head変更時は証拠を作り直す。caller申告actor、任意JSON、別PRのrun、COMMENTED、未完了CIは承認証拠にしない。

**reviewerの独立性はproject policyの`merge.reviewIndependence`が決める**。`context-isolated`（未宣言時の既定）はimplementerと別session/contextであること、exact HEADを固定したこと、reviewerが対象差分を変更していないこと、肯定・敵対レビューとfindingの記録を要求する。**同一GitHub actorでも成立し、tracked artifactの`approved`と保存済みreview session/Step 10 bindingがformal approvalになる。** `actor-independent`はPR authorおよびobserved implementation commit authorと別のstable actor IDのprovider `APPROVED`を要求し、**高リスク変更・不可逆操作・release・外部公開でproject policyが宣言して引き上げる。** 両modeともexact HEAD一致は必須である。tracked文書へ自身のcommit SHAを書かず、H_final後はartifactを更新しない。

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | .agent-skill-chain/tmp/issues/20260916_090913_実装中に生成する全documentを読者を定めた要点のみへ縮約する | `staging-record.json`の記録値は`17096aa617227b4fb1c95583df4851d472a10d305f0d5472ba3a455fe6814662`。**reviewerが現在の内容から再計算した値は`0f8e20c59b471d2d57d083694cd4832c8e00f738043a227d6285434eb627c0bc`で一致しない。** DISC-006の追記後にStep 9が再記録されていないためで、`review round --apply`の前に再記録が要る（M-09）。00〜03を再読し、AC-01〜09・INV-01〜05・FR-01〜07をanchorにした | 既存コード |
| 差分 | `d499a5ef6a188a010e10c9e3017bedf46956ee6d`..`50fddeb3bfb6333e4b17a2f25cd0a58ea5cf2a47` | 29 path。round 2の`fixedDiff`は`b7bf8deb`..`51a9ec48`の22 path。round 1のみで以降不変は7 path | 既存コード |
| テスト | §7に全件 | reviewer実測。`npm test`は`f9eff7d3`で単独実行し2,077 scenarios（2,061 passed、16 skipped、失敗0）。最終HEAD `51a9ec48`の差分は雛形5 fileのみで、reviewerは対象unit 61 scenarios/334 stepsと静的検査8種を実行し全て終了値0。`conformance:check` 87 scenariosと残る静的検査は`f9eff7d3`で実測済み | テスト出力 |
| 仕様 | `docs/specs/`の変更10 fileと`.agent-skill-chain/docs/01_開発ワークフロー.md`を原文で確認 | updated。round 2でREQ-SQ-033へ対象範囲・review要約・理由の任意化を追記し、追跡表のREQ-SQ-033行へSCN-UNIT-ISSUETPL-013/014と04・11・11・12を追加、変更履歴1行を更新 | 既存文書 |
| commit前candidate | `.agent-skill-chain/docs/01_開発ワークフロー.md`、`.agent-skill-chain/templates/issue/`10件、`dist/src/domain/issue.js`、`src/domain/issue.ts`、`docs/specs/`10件、`test/`6件 | H_impl 50fddeb3bfb6333e4b17a2f25cd0a58ea5cf2a47 | Git index |
| Phase A artifact | 本fileを`H_impl`の後に1 fileだけcommitして`H_final`にする | 記入時点では未commit。自身のcommit SHAは書かない | Git観測 |
| review session | .agent-skill-chain/tmp/issues/20260916_090913_実装中に生成する全documentを読者を定めた要点のみへ縮約する | round 1はconverged。round 2は本artifactを入力に進行役が記録する | Git観測 |

**Phase Bの外部証拠をこの表へ書かない。** PR number、Actions run ID、immutable review IDはPR作成後にしか存在しない。**それらは`review evidence`とdelivery stateがappend-onlyで保持する正本であり、tracked review artifactは参照先を持たない。**

重要判断を推論だけで承認しない。新しい権限が必要な場合は、対象操作と決定権者を明示する。

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: はい。雛形→複写→検証→同期本文→GitHub adapterの直列で、本artifactは自SHAを書かない。`architecture:check`合格
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_impl`はcurrent HEAD `51a9ec48`と一致し、未追跡fileは本artifact1件だけ（`git status --short`）
- reviewerの独立性が`merge.reviewIndependence`の要求水準を満たす（`context-isolated`なら別session/context、`actor-independent`ならPR author・`H_impl` author stable IDと別のstable ID）: はい。未宣言のため`context-isolated`。別contextで、対象差分を恒久的に変更していない（変異試験の3 fileはmd5一致で復元済み）
- **（Phase B。ここでは判定しない）** trusted providerが観測したPR/CI/reviewが`H_final`へ一致することは、PR作成後に`review evidence`が観測して記録する: Phase Bで判定する
- 既定branch追随を行った場合、取り込みがreview artifact commitより前にあり、`比較基点`が取り込んだ既定branch tip、`H_impl`がartifact直前の最新commitを指し、個別監査表を`比較基点..H_impl`から再生成した: 追随なし。`d499a5ef`は`origin/main`のtipのままで、`H_impl`はその子8 commit（`b7bf8deb`→`8265f707`→`0381efa3`→`f23e76cc`→`f2f52b23`→`2f2ce1c8`→`f9eff7d3`→`51a9ec48`）の先端。個別監査表は`比較基点..H_impl`から再生成した

### 1.1 変更ファイル個別監査

追加・変更・削除した全ファイルを省略せず1ファイル1行で記録する。まとめ行、directory単位の一括承認、test成功だけによる代替を認めない。版管理下の生成物（`dist/`）は`audit:check`が照合対象から外すため本表に載せず、生成元を監査して配布影響は§8で判定する。

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | M | package owner | package | 「文書の読者」節に読者3区分と、既定は結論・理由は判断の再現に要る場合という書きぶりの既定を置く | 規範→雛形→同期本文の正方向。分量の規律は`00_運用ポリシー.md`「成立条件の観測」へ委譲 | FR-05、AC-05、SCN-UNIT-SPECDOC-001、REQ-SQ-033 | 追記のみ。revertで戻る。強制点を追加していない | pass |
| `.agent-skill-chain/templates/issue/00_要求定義_full.md` | M | package owner | package | 管理情報を末尾へ移し、読者表へ読む順と§5を追加。187行 | `prefillFullArtifacts`の置換行と必須13見出し、`[成果物:<kind>]`段落を保持。隔離repositoryの`issue create`で事前充填と節順を実測 | FR-03/04、AC-03/08/09、SCN-UNIT-ISSUETPL-011/013/014 | revertで戻る。受理集合不変（INV-01） | pass |
| `.agent-skill-chain/templates/issue/00_要求定義_poc.md` | M | package owner | package | 同上。177行。読者表の§1・§2・§4・§5・§6・§11は実見出しと一致 | `issueRequiredHeadings("poc")`の11見出しを保持 | FR-03、AC-04/09 | revertで戻る | pass |
| `.agent-skill-chain/templates/issue/00_要求定義_quick.md` | M | package owner | package | 同上。141行。読者表の§1・§2・§5・§9・§10は実見出しと一致 | `skills:check`のquick用語検査合格 | FR-03、AC-04/09 | revertで戻る | pass |
| `.agent-skill-chain/templates/issue/01_要件定義.md` | M | package owner | package | 同上。§6.2の4 bulletを2 bulletへ統合し150行。欄名はすべて残る | `### 2.1 ドメイン用語台帳の確定差分`markerと参照行の字面`開発考慮事項の適用判定は00_要求定義.md §6.1と同じ`（TERM-ASC-099）を保持。reviewerが複写後の実fileでも1件ずつ確認 | FR-03/04、AC-03/08/09 | revertで戻る | pass |
| `.agent-skill-chain/templates/issue/02_設計.md` | M | package owner | package | 同上。§3の冪等性・再試行可能性、§5の認証・承認・policyを統合し196行。欄名はすべて残る。low-risk短縮の対象見出し3件は`issue.ts` `LOW_RISK_SHORT_FORM_TARGETS`と一致 | routing入力契約の5語（role欄・provider欄・model設定欄・fallback欄・独立性証拠欄）と参照行の字面を保持（reviewerが`grep`で各1件確認） | FR-03/04、AC-03/08/09、TERM-ASC-104 | revertで戻る | pass |
| `.agent-skill-chain/templates/issue/03_実装計画.md` | M | package owner | package | 管理情報を末尾へ移し§11を§4.1へ統合。§9のコミット単位・push先とロールバック・保持証拠を統合し190行。欄名はすべて残る | `### 5.1 project選択test layer`、選ばれた検証の表header行、`**選定結果の各件について…**`、`直積にしない`、parallel-progress marker、参照行の字面を保持 | FR-03/04、AC-03/08/09 | revertで戻る | pass |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | M | package owner | package | 要約節を新設し、識別情報を末尾へ移し説明段落を縮約。外部review 2巡目で配置先を`docs/reviews/`と`.agent-skill-chain/reviews/`の両方へ戻した（TERM-ASC-101・規範282行と同じ集合）。219行 | 監査表の雛形行は`（repository相対path）`で始まる従来書式のままで`renderReviewArtifactDraft`の置換regexと一致。`H_final後はartifactを更新しない`ほか既存assertionが名指しする字面を保持 | FR-07、AC-09、SCN-UNIT-ISSUETPL-014、REQ-SQ-033 | revertで戻る。reviewerが要約節削除の変異を当ててSCNが落ちることを確認 | pass |
| `.agent-skill-chain/templates/issue/11_プルリクエスト事前確認.md` | M | package owner | package | 読者表を追加し確認項目を統合。外部review 2巡目で`## 4.1 PR作成後のread-back`を新設し、PR head・成功CI head・Actions runの関連PR番号の2項目を移した。71行。L-09の指摘を受け冒頭を「§4.1を除く全項目」へ直した | 移した2項目は`06_外部インターフェース/01_コマンド・GitHub契約.md`がmergeの前提として`pr-bound`で要求するものであり、PR作成の前提は`create-prepared`である（reviewerが両正本の原文で確認）。残る§1〜§4の項目はすべてPR作成前に観測できる | FR-03、AC-09、SCN-UNIT-ISSUETPL-014 | revertで戻る | pass |
| `.agent-skill-chain/templates/issue/11_プルリクエスト本文.md` | M | package owner | package | 読者表を追加し、重複bulletとラウンド別4行を統合。60行 | `Closes #（正本Issue番号）`とIssue参照規約linkを保持（ASC-CANON-SINGLE-SOURCE-001が字面で拘束） | FR-03、AC-09 | revertで戻る | pass |
| `.agent-skill-chain/templates/issue/12_利用案内.md` | M | package owner | package | 読者表とpoc行を追加し、使い方へ書きぶりの案内を置く。41行 | `check_directory_guides`の対象。`skills:check`合格 | FR-03、AC-09、SCN-UNIT-ISSUETPL-014 | revertで戻る。reviewerが読者表削除の変異を当ててSCNが落ちることを確認 | pass |
| `docs/specs/00_仕様書構成/00_仕様書索引.md` | M | repository maintainer | spec | 読み手別の読み順3行（round 1、以降不変） | 読者定義は規範へ相対linkで委譲 | FR-06、AC-06、SCN-UNIT-SPECDOC-002 | 追記のみ | pass |
| `docs/specs/01_システム概要/00_概要.md` | M | repository maintainer | spec | 6見出しとStep 0〜11のmermaid図（round 1、以降不変）。16→60行 | Step名と`workflow.ts` `WORKFLOW_STEPS`が一致 | FR-06、AC-06、REQ-SQ-034 | revertで戻る | pass |
| `docs/specs/01_システム概要/02_用語・略語.md` | M | repository maintainer | spec | TERM-ASC-121追加、TERM-ASC-102変更。102の定義へ「code外の生の`<details>`と`</details>`を実体参照へ置き換えて構造を保つ。開始tagも置き換えるため意図した入れ子foldは字面表示になる」を追記 | 候補（00）→確定（01）→耐久台帳→変更履歴が一方向 | 01 §2.1、REQ-SQ-033 | 追記・1行変更 | pass |
| `docs/specs/02_要件/00_要件一覧.md` | M | repository maintainer | spec | REQ-SQ-033/034の2行（round 1、以降不変） | 04_仕様・品質管理要件の本文と追跡表へ対応 | REQ-SQ-033/034 | 追記のみ | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | repository maintainer | spec | REQ-WF-015の同期本文の形（round 1、以降不変） | 06_外部IFと同じ表現 | FR-01/02、REQ-WF-015 | 1行変更 | pass |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | M | repository maintainer | spec | REQ-SQ-033へ対象範囲・review要約・理由の任意化の3段落と実装path 4件を追加。escape規則を「属性と大文字小文字を問わず`<details>`・`</details>`を置き換える。入れ子foldは字面表示。行跨ぎと未閉backtick以降は既知の限界」へ確定 | REQ-SQ-024を参照し閾値・gateを追加しない。06・TERM-ASC-102・staging 01/02と同じ向きにそろい、矛盾も粒度差も無いことをreviewerが全件grepで確認 | AC-SQ-033、FR-01/03/07、INV-05 | 追記・1段落の訂正 | pass |
| `docs/specs/04_機能/01_ワークフローv0.3.md` | M | repository maintainer | spec | Issue #1406の実装項目1行（round 1、以降不変） | 既存行と同形式 | SCN 6件 | 追記のみ | pass |
| `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | M | repository maintainer | spec | `issue sync`生成経路の折りたたみ契約。H-03是正で「表示内容を保ち末尾空白だけを除く。code外の生の`<details>`・`</details>`は実体参照へ置き換える。code内は置き換えない」へ書き換え | 実装・REQ-SQ-033・TERM-ASC-102・staging 01/02と同じ向きで、開始tagの扱いも4正本すべてが明記する | FR-01、AC-01、INV-05 | 1文のrevertで戻る | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | repository maintainer | spec | REQ-SQ-033行へSCN-UNIT-ISSUETPL-013/014と04・11・11・12の実装pathを追加 | `trace:check`合格（orphan 0）。013/014が04・11・12を実際に観測するため、L-03の対応ずれは解消 | REQ-SQ-033、AC-SQ-033、AC-08/09 | 追記のみ | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | repository maintainer | spec | 同日行へ対象拡大・要約節・理由の任意化を追記。header区切りの直後を維持 | 既存行と同形式。HEAD SHA欄は自SHAを書かない | REQ-SQ-033/034 | 追記のみ | pass |
| `src/domain/issue.ts` | M | package owner | package | 純粋関数`renderIssueSyncBody`に加え、`escapeFoldBoundary`がcode外の生の`<details>`・`</details>`（大文字・空白・属性付きを含む）を実体参照へ置き換える。L-08是正で死んだternaryを除き単純形へ | fileを読まず副作用なし。fence・inline codeの判定は同fileの既存規則と同型。扱えない2形（行を跨ぐtag、未閉backtick以降）をコメントで明示。`typecheck`・`lint`合格 | FR-01、AC-01、SCN-UNIT-ISSUESYNC-002、INV-05 | 関数のrevertで戻る | pass |
| `test/features/unit/document-reader.feature` | A | package owner | evidence | SCN-UNIT-SPECDOC-001/002（round 1、以降不変） | 実repository読取 | AC-05/06 | 削除で戻る | pass |
| `test/features/unit/issue-scaffolding.feature` | M | package owner | evidence | SCN-UNIT-ISSUESYNC-002/003。002へ「構造は本文中の閉じtagで壊れない」と「境界の置き換えは形を変えた閉じtagも捕まえる」を追加 | 既存001は不変 | AC-01/02、INV-05 | 削除で戻る | pass |
| `test/features/unit/issue-template-contract.feature` | M | package owner | evidence | SCN-UNIT-ISSUETPL-011/012に加え、round 2でSCN-UNIT-ISSUETPL-013（節順）と014（全10雛形の読者表と04の要約）を追加 | 既存001〜010は不変。`@issue-template-contract` tagで61 scenarios | AC-03/04/08/09 | 削除で戻る | pass |
| `test/steps/document-reader.steps.ts` | A | package owner | evidence | 規範・概要・索引の実file読取（round 1、以降不変） | `WorkflowWorld`のみ依存 | SCN-UNIT-SPECDOC-001/002 | 削除で戻る | pass |
| `test/steps/issue-sync-body.steps.ts` | A | package owner | evidence | 折りたたみの分解検査と合成経路、構造均衡とcode内保持に加え、L-06是正で期待値を字面定数`EXPECTED_INNER`へ移し、置換対象6形・保持4形を字面で検査するstepを追加 | 実stagingを作って検査。期待値が実装から独立した | SCN-UNIT-ISSUESYNC-002/003、INV-05 | 削除で戻る | pass |
| `test/steps/issue-template-brevity.steps.ts` | A | package owner | evidence | 複写後の事前充填・読者表・行数に加え、round 2で節順（最初の節が管理情報でない／管理情報が末尾）と配布10雛形の読者表・04要約5項目のstepを追加。閾値は追加していない | `createIssueStaging`と`validateIssue`を実際に通し、014は`process.cwd()`の実fileを読む（fixture複写にしていない） | SCN-UNIT-ISSUETPL-011〜014、INV-01/04 | 削除で戻る。reviewerが3変異で実効性を確認 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: はい。`git -c core.quotepath=false diff --name-status d499a5ef 51a9ec48`の29 pathから生成物`dist/src/domain/issue.js`を除いた28行と一致（`parseFileAudit`で28 entryを機械照合）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: はい。雛形・規範は実測の出典としてIssue番号を引くだけで、project固有値を既定化していない
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: はい。H-02・M-03・M-04・L-03の是正が触れた4 file（`test/`2件、雛形2件）とstaging 01・02・03、追跡表だけを再監査した

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

モード別実装計画（fullは03、quickとpocは集約00）の発見記録を全件転記せずIDで参照し、最終状態とEvidenceを確認する。目的、scope、受け入れ条件、security境界、不可逆操作を変えなかった発見は上流再起動を要求しない。契約を変えた発見は、影響する成果物だけが再確定されていることを確認する。

発見IDはモード別実装計画（fullは03、quickとpocは集約00）の`DISC-*`と同じ字面を使い、本文書内で別IDに言い換えない。

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | 01 FR-03の観測可能な出力を「各節見出しの直下に読者を示す1行」と書いたが、02・03・実装・SCN・規範はすべて冒頭の読者表1つで成立しており、01だけが旧い記述で残っていた（出所: Step 10 round 1の指摘M-01） | FR-03の記述だけ。目的・scope・AC・security境界・不可逆操作は不変 | requirement（`disposition`は`rebaseline-affected-contracts`） | 01 FR-03を「冒頭の読者表」へ再確定し、03 §4.1へ記録。02は変更不要 | reviewer実測: staging原文確認、`issue validate --stage=design`が`valid: true`、staging digest再計算が記録と一致 | no-spec-impact（REQ-SQ-033は当初から「冒頭の読者表」） | pass |
| DISC-006 | 外部review 2巡目で、04が成果物の配置先を`docs/reviews/`だけに限定し正本（TERM-ASC-101と規範282行）の`.agent-skill-chain/reviews/`を落としていた。11事前確認はPR作成前の節に、PR作成後のread-backでしか成立しない2項目を置いていた | FR-03の雛形内容。目的・scope・AC・security境界・不可逆操作は不変 | なし（雛形内容の是正であり契約は変わらない） | 04の案内を両方の配置先へ戻し、11事前確認へ`## 4.1 PR作成後のread-back`を新設して2項目を移した。増えた6行は01・02・03の重複bulletの統合で戻した | reviewer実測: `06_外部インターフェース/01_コマンド・GitHub契約.md`と`01_開発ワークフロー.md`の原文で`create-prepared`（PR作成の前提）と`pr-bound`（mergeの前提）の境界を突合し、残る§1〜§4に同型の混在が無いことを確認。統合した6 bulletは欄名がすべて残ることを原文で確認。雛形総量1,432行、`@issue-template-contract` 61 scenarios合格、静的検査8種合格 | no-spec-impact（雛形内容であり契約の記述は変えていない） | finding（L-09） |
| DISC-005 | 開始tagも置き換える契約を書いたのは06_外部IF契約だけで、REQ-SQ-033・TERM-ASC-102・staging 01/02は閉じtagしか書いていなかった。利用者が意図した入れ子foldが字面表示になる仕様変化もどの正本にも無く、あわせて`escapeFoldBoundary`が到達しない分岐を持ち未閉backtickの限界がコメントに無かった | INV-05の記述。矛盾ではなく不足だが、H-03と同じ「契約を持つ正本の一部だけを直す」型 | なし（記述の粒度と死んだ分岐の除去であり、受け入れ条件・security境界・不可逆操作は不変） | REQ-SQ-033・TERM-ASC-102・staging 01 INV-05・02 §3へ開始tagと入れ子foldの字面表示、行跨ぎと未閉backtickの限界を追記。`replace`のternaryを外して単純形へ戻し、限界をコメントへ明記した | reviewer実測: 18形の再測定で是正前後の観測がすべて一致（置換7形・保持11形、mismatch 0）。契約を持つ正本4箇所を全件grepし、同じ向きで粒度差が無いことを確認。`npm test` 2,077 scenarios失敗0、静的検査12種＋`conformance:check`合格 | updated（REQ-SQ-033、TERM-ASC-102） | pass |
| DISC-004 | 同じ契約を持つ仕様正本3箇所のうち`06_外部インターフェース/01_コマンド・GitHub契約.md`だけが「escapeしない」の旧記述で残り実装と矛盾していた。あわせて`escapeFoldBoundary`が属性付き`</details foo>`と生の`<details>`開始tagを素通ししていた | INV-05。矛盾した記述を読んだ実装者がescapeを外すとDISC-003の欠陥が再発する | なし（契約の記述と検出範囲の是正であり、受け入れ条件・security境界・不可逆操作は不変） | 06の当該1文を実態へ書き換え、検出を`<\/?\s*details\b[^>]*>`へ広げて開始tagも置き換える。SCN-UNIT-ISSUESYNC-002へ形を変えた入力の字面assertionを追加し、期待値を実装から導出しない形へ直した | reviewer実測: 敵対入力18件で境界を再観測（閉じtagの大文字・空白・属性付き、開始tagの素・属性付き・大文字は置換。inline code・fence・`< /details>`・`<detailsfoo>`・`<detail>`・`<summary>`は保持）。実stagingの本文でcode外の`<details>`3件・`</details>`3件の均衡を再確認。契約を持つ正本を全件grepして取り残しを点検（M-08を検出） | updated（`06_外部インターフェース/01_コマンド・GitHub契約.md`）。粒度差はDISC-005で解消 | pass |
| DISC-003 | 成果物本文がcode fence外に持つ生の`</details>`が折りたたみを早期に閉じ、以降の本文と後続の成果物が区画の外へ出る（実測で`<details>`3件に対し`</details>`4件）。出所はPR外部reviewの指摘 | FR-01の観測可能な出力とINV-05。目的・scope・security境界・不可逆操作は不変 | acceptance（`disposition`は`rebaseline-affected-contracts`、影響成果物01・02・03） | `escapeFoldBoundary`を追加してcode外の閉じtagだけを`&lt;/details&gt;`へ置き換え、SCN-UNIT-ISSUESYNC-002へ構造均衡とcode内保持のassertionを追加。02 §12の「escapeしない」判断を訂正し、REQ-SQ-033・TERM-ASC-102・01 INV-05/FR-01/§10・02 §3/§6を「表示内容を保ち構造を壊さない」へ直した | reviewer実測: 敵対入力14件で境界を観測（大文字・空白・tab・複数個は置換、fence内・inline code内・`< /details>`は保持）。実stagingから生成した同期本文でcode外の`<details>`3件と`</details>`3件が釣り合うことを確認。`npm test` 2,077 scenarios失敗0 | updated（REQ-SQ-033、TERM-ASC-102）。`06_外部インターフェース/01_コマンド・GitHub契約.md`の未追随はDISC-004で解消 | pass |
| DISC-002 | ownerが対象を「今後新しく生成されるすべてのdocument」と確定し、04レビュー・11 PR本文・11事前確認・12利用案内が未対応だった。理由の全面禁止も誤りとされた | scopeとACが広がる。目的・security境界・不可逆操作は不変 | scope・acceptance（`disposition`は`rebaseline-affected-contracts`、影響成果物00〜03） | `templates/issue/`全10件を対象化し、管理情報を末尾へ移し、04へ要約節を新設、規範へ書きぶりの既定を追加、REQ-SQ-033へ3段落を追記。round 2の指摘を受けてAC-08/09とSCN-UNIT-ISSUETPL-013/014を追加し、01・02 §11・03 §3・§5.1・T02・追跡表を追随させた | reviewer実測: `npm test` 2,077 scenarios失敗0、静的検査12種＋`conformance:check`、隔離repositoryの`issue create`で複写729行・事前充填一致・最初の節が§1で管理情報が末尾、雛形総量1,432行、reviewer自身の変異3件すべてkill | updated（REQ-SQ-033、追跡表、変更履歴） | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-ISSUESYNC-002 | `src/domain/issue.ts` `renderIssueSyncBody`・`escapeFoldBoundary` | reviewer実測 passed。構造均衡・code内保持・置換対象6形と保持4形の字面assertionを含む | pass | cucumber 76 scenarios/424 steps（3 tag）と`npm test`全件。実stagingの本文でcode外の`<details>`3件と`</details>`3件が釣り合うことをreviewerが実測 |
| AC-02 | SCN-UNIT-ISSUESYNC-003 | 同上 | reviewer実測 passed | pass | 同上 |
| AC-03 | SCN-UNIT-ISSUETPL-011 | `templates/issue/`の00〜03 | reviewer実測 passed。隔離repositoryの複写は729行 | pass | 事前充填欄・冒頭の読者表・合計<752をSCNが観測 |
| AC-04 | SCN-UNIT-ISSUETPL-012 | 同上、`validateIssue`（不変） | reviewer実測 passed | pass | 複写の受理と必須見出し削除の名指し拒否 |
| AC-05 | SCN-UNIT-SPECDOC-001 | `.agent-skill-chain/docs/01_開発ワークフロー.md` | reviewer実測 passed | pass | 「文書の読者」節と3区分、強制点を持たない旨を原文で確認 |
| AC-06 | SCN-UNIT-SPECDOC-002 | 概要・索引 | reviewer実測 passed | pass | 6見出し・mermaid 1枚・読み順表 |
| AC-07 | 原文読解 | `.github/workflows/`、`scripts/` | `git diff --name-only d499a5ef 51a9ec48 -- .github scripts`は0件 | pass | reviewer実測。閾値停止・新gateなし |
| AC-08 | SCN-UNIT-ISSUETPL-013 | `templates/issue/`の00〜03（管理情報を末尾へ） | reviewer実測 passed。変異「00の管理情報を先頭へ戻す」でSCNが落ちることを確認 | pass | 最終HEADの隔離repository複写でも4 fileとも最初の節が§1、末尾の節が管理情報 |
| AC-09 | SCN-UNIT-ISSUETPL-014 | 配布10雛形と`04_レビュー.md`の要約 | reviewer実測 passed。変異「04の要約節削除」「12の読者表削除」でSCNが落ちることを確認 | pass | 10 fileすべてに冒頭16行内の読者表と3区分、04に要約5項目を実測 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | 同期本文はGitHubへの外部出力。外部review是正で本文の一部を実体参照へ置き換えるが、digest照合・writer lock・`--authorize`経路と情報の増減は無い | `escapeFoldBoundary`は文字列変換のみでpath・秘密情報を扱わない。digest規則は不変（reviewer実測） |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 縮約の効果は§0「成果物行数」と実測（複写752→729、雛形総量1,434→1,432）で観測し、閾値停止しない | REQ-SQ-024、`check_file_audit`の記録欄 |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | CLIであり画面契約とa11y検証の対象を持たない。文書の可読性はFR-03/05/06/07が直接所有する | `00_仕様書索引.md`が05_画面を対象外と宣言。UI差分0件 |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | tokenを所有しない | 同索引が17・18を対象外と宣言 |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | AC-01〜09をreviewer実測で確認。隔離repositoryの複写は729行、雛形総量は1,432行で基点1,434行を下回る。折りたたみは実stagingの本文で構造が釣り合う |
| 価値 | 利用者・運用上の目的を満たすか | pass | 目的が先頭に来て管理情報が末尾へ移り、04は要約5行で「何が問題で何を確認したか」が先に読める。配布10雛形すべてに読者と読む順がある |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 依存追加なし。管理情報の移動後も`prefillFullArtifacts`と`renderReviewArtifactDraft`が動くことを実測 |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | round 2の全指摘（H-02・H-03・M-03・M-04・M-05・M-07・M-08・L-02〜L-08）と外部review指摘3件が是正された。FR-03の2条項は実測729行・1,432行で成立し、escape契約を持つ正本4箇所は開始tagの扱いと既知の限界まで同じ向きでそろう。reviewerが全件grepで矛盾も粒度差も無いことを確認した |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 是正は構造assertion 2 scenarioと純関数1件に収まり、分量の閾値を足していない（INV-04・RQ-05）。`escapeFoldBoundary`はfence・inline codeの判定を同fileの既存規則と同型に保つ。L-05で指摘した表の直前の空行は全件戻り、同型の書き方は0件になった |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | 3変異（04の要約削除、00の管理情報を先頭へ復帰、12の読者表削除）はいずれも1 scenarioを落とした。`escapeFoldBoundary`へ3回で延べ50入力（最終形は18形）を当て、最終形の再測定は是正前の観測とmismatch 0。置換7形（閉じtagの素・大文字・空白・属性付き、開始tagの素・属性付き・大文字）と保持11形（inline code、```・~~~・字下げfence、`< /details>`、`<detailsfoo>`、`<detail>`、`<summary>`ほか）を確認した。扱えない2形は正本と実装コメントの両方へ既知の限界として明示された |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 雛形の複写・検証・同期の失敗経路は不変。`issue create`の`COPYFILE_EXCL`も不変 |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | 管理情報が最終節でも識別情報の読み取りは成立する。SCN-013は`headings.at(-1)`、SCN-014は冒頭16行を境界に観測する。`escapeFoldBoundary`は大文字`</DETAILS>`・`</ details >`・tab・1行2件を置換し、fence（```・~~~・字下げ）とinline code内を保持し、`< /details>`（tagにならない形）を正しく残すことをreviewerが実測した |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | 文書とtestの編集だけでpath・権限・外部実行を扱わない |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | 11事前確認の独立性項目は、既定`context-isolated`と矛盾していた旧文言から規範準拠へ直っており、緩和ではなく整合の回復である |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | 削除行を機械契約と全件突合し、`renderReviewArtifactDraft`の監査表行、`H_final後`、`formal approval`、`「JSON出力のみ」を非適用の根拠にせず`、Issue参照規約linkがすべて現存することを確認した。実体参照への置き換えは表示内容を保ち、`&lt;/details&gt;`はGitHub上で`</details>`と表示される |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | 全変更が文書とtestでrevertで戻る。同期済みIssueは再同期で戻る |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | 配布10雛形すべてが読者表を持ち、SCN-UNIT-ISSUETPL-014が10件を名指しで走査する。折りたたみを使わない経路（checkpoint 4・quick・poc）は分岐へ入らない。escape契約と入れ子foldの仕様変化は契約を持つ4正本すべてに書かれた |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| M-01 | Medium | 01 FR-03の観測可能な出力が設計・実装の「冒頭の読者表」と食い違っていた（round 1） | staging 01 §4 FR-03行、03 §4.1 DISC-001 | 01の要件記述 | DISC-001として再確定済み | resolved | なし |
| H-02 | High | round 2で追加した性質（04の要約節、管理情報の末尾配置、読む順、雛形総量）を観測するassertionが1件も無かった。AC-03が宣言する「最初の`## `見出しが管理情報でない」をSCN-UNIT-ISSUETPL-011が測っておらず、FR-07はAC・SCN・testのいずれも持たなかった | 是正前: `git diff --name-only b7bf8deb 8265f707`に`test/`が0件、`grep -rn "要約" test/`が0件 | 縮約を保つ中心機構が追加分に成立しない。製品の実行時挙動には影響しない | 是正済み: SCN-UNIT-ISSUETPL-013（複写した00〜03の最初の節が管理情報でなく管理情報が末尾）とSCN-UNIT-ISSUETPL-014（配布10雛形の読者3区分と04の要約5項目）を追加し、AC-08/09で受け入れ条件に結んだ。分量の閾値は置いていない（INV-04・RQ-05）。**reviewerが独立に3変異を当て、いずれもkillされることを確認した** | resolved | なし。次に同種の性質を足すときは同じ形でSCNを足す必要が残る |
| M-03 | Medium | FR-03と`12_利用案内.md`は「全10雛形が冒頭に読者表を持つ」と書くが、11事前確認と12自身が読者表を持たなかった | 是正前の`11_プルリクエスト事前確認.md` 1〜5行、`12_利用案内.md` 1〜13行 | 配布物の記述が配布物自身と一致しない | 是正済み: 2 fileへ読者表を追加し、10件すべてが持つ状態にした。SCN-UNIT-ISSUETPL-014が10件を名指しで走査する | resolved | なし |
| M-04 | Medium | 03がDISC-002へ部分的にしか追随しておらず、§3のRequirement ID、§5.1のSCN表、T02の対象範囲、mutation-test欄、02 §11が旧状態だった | 是正前のstaging 03 §3・§5.1・T02、02 §11 | 計画と実施内容の不一致 | 是正済み: §3を`FR-01〜07`、§5.1へSCN-013/014、T02の対象範囲を訂正、mutation-test欄を合計23件へ、01へAC-08/09、02 §11へFR-03/FR-07の追跡行を追加 | resolved | L-04として残る小さな追随漏れあり |
| L-03 | Low | 追跡表のREQ-SQ-033行が04・11・12の実装pathをSCN-UNIT-ISSUETPL-011/012へ束ねていたが、両SCNは00〜03の複写しか観測しない | 是正前の`docs/specs/15_要件追跡/00_追跡表.md` | 追跡表の対応が実態より広い | 是正済み: 同行へSCN-UNIT-ISSUETPL-013/014を追加。014が04・11・12を実際に走査するため対応が成立した | resolved | なし |
| M-05 | Medium | FR-03の観測「`templates/issue/`合計が1,434行より少ない」と00 OUTCOME-02が`0381efa3`時点で成立していなかった（実測1,436行）。原因はM-03是正で11事前確認と12へ読者表を足したこと | 是正前後の`wc -l`実測。基点1,434→`b7bf8deb`1,425→`8265f707`1,424→`0381efa3`1,436→`f23e76cc`1,425→`2f2ce1c8`**1,432** | staging 00・01の数値記述。git追跡下のREQ-SQ-033は「複写直後の00〜03合計を752行より少なく」としか定めておらず、仕様正本は一貫して真だった | 是正済み: 01・02・03・04の重複説明文を11行削り1,425行へ戻した。**欄・判断・記録・見出しは1つも削られていない**ことをreviewerが削除行の全件突合で確認（参照行の字面、routing 5語、`H_final後`・`formal approval`・`DISC-*`等の既存assertion対象はすべて現存）。複写直後は722行 | resolved | なし |
| M-02 | Medium | 02はimplementer=codexと宣言するが、実装は進行役session（claude、context A）が行った。project choiceの`fallback`（`implementer_unavailable`→coordinator）とは一致し、独立性`context-isolated`は満たす | 03 §0.1、02 §0.1、`.agent-skill-chain/project/choices/development.json` 19〜21行・158〜161行 | routing evidenceの欠落 | round 2でも同じ状態。記録済みの迂回として受理し、02 §0.1の文言とchoiceの不一致は別Issueの候補とする | valid | 迂回が常態化しrouting evidenceが蓄積されない |
| L-01 | Low | quick/pocの00雛形はround 1で各+6行だった | `wc -l`の実測 | 複写直後の行数 | round 2の縮約でquick 141行・poc 177行。full複写は729行で752行を下回り、雛形総量も1,432行で基点未満。対象外のまま記録する | valid | なし |
| L-02 | Low | 「内容を1文字も落とさない」が末尾空白を`trimEnd`する実装と一致していなかった（外部reviewも同じ点を指摘） | 是正前のREQ-SQ-033・TERM-ASC-102・01 INV-05 | 仕様文言の厳密性 | 是正済み: REQ-SQ-033・TERM-ASC-102・01 INV-05/FR-01/§10・02 §3/§6を「表示内容を保ち、折りたたみの構造を壊さない。各成果物の末尾空白は除く」へ訂正。reviewerが原文で確認 | resolved | なし |
| L-04 | Low | 03 §3の`Acceptance Criteria ID`欄が`AC-01〜07`のままでAC-08/09を含まず、`verification-input.json`も`FR-01〜06`・`AC-01〜07`のままだった | 是正前のstaging 03 §3と`verification-input.json` | 計画の記述と入力JSONの範囲 | 是正済み: §3を`FR-01〜07`・`AC-01〜09`へ、`verification-input.json`を`FR-01〜07`・`AC-01〜09`へ更新。reviewerが両方を原文で確認し、選定される6手段が変わらないことも確認した | resolved | なし |

| L-05 | Low | M-05是正で表の直前の空行が削られ、説明の段落行と表が空行なしで接する箇所が入った。比較基点では同じ書き方が1件（parallel-progress marker）だけだった | 全file走査で基点1件・`f23e76cc`9件・`2f2ce1c8`**0件** | 配布雛形の見え方。`.md`は`format:check`の対象外のため強制する機構は無い | 是正済み: 外部review是正で9箇所すべてに空行が戻り、`04_レビュー.md`の説明文1行の削除で総量は1,432行（基点1,434行未満）を維持した | resolved | なし |

| R-01 | High | **製品の欠陥（外部review由来）。** 成果物本文がcode fence外に持つ生の`</details>`が折りたたみを早期に閉じ、以降の本文と後続の成果物が区画の外へ出ていた。実測で`<details>`3件に対し`</details>`4件 | PR外部reviewの指摘とDISC-003。是正後はreviewerが実stagingの本文でcode外の`<details>`3件・`</details>`3件の均衡を実測 | full Step 8の同期本文。Issueを読む発注・評価する人に本文が壊れて届く | 是正済み: `escapeFoldBoundary`でcode外の閉じtagだけを実体参照へ置き換え、SCN-UNIT-ISSUESYNC-002へ構造均衡とcode内保持のassertionを追加（implementer申告の変異4件kill、reviewerが敵対入力14件で境界を実測） | resolved | 残るのはM-07の素通し経路 |
| R-02 | Medium | **不変条件の記述（外部review由来）。** 「1文字も落とさない」が`trimEnd`する実装と不一致だった | 是正前のREQ-SQ-033・TERM-ASC-102・01 INV-05（L-02と同一事象） | 仕様文言 | 是正済み: L-02と同じ訂正で解消 | duplicate | なし（L-02として記録） |
| R-03 | Low | **artifactの表2箇所で列数が不一致（外部review由来）。** §0の`仕様の所有箇所`行と§1の`テスト`行がcell内の生の`|`でheaderと列数が合っていなかった | 本artifactの旧行31（header 2・row 3）と旧行58（header 4・row 3） | 本artifactの表示。`parseFileAudit`は個別監査表だけを読むため機械検査に掛からない | 是正済み: reviewerが両行を書き換え（`<4|8>`を含む引用を該当部分のない引用へ、`テスト`行を4列へ）、全14表のheaderとrowのcell数一致を自作scriptで検査して0件にした | resolved | なし |
| H-03 | High | `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`が「折りたたみは内容を変えず、成果物本文に`</details>`の字面があってもescapeしない」と書いたままで、実装およびREQ-SQ-033・TERM-ASC-102の訂正と正面から矛盾していた | 是正前の該当段落。是正後は「表示内容を保ち末尾空白だけを除く。code外の生の`<details>`・`</details>`は実体参照へ置き換える。code内は置き換えない」（reviewerが原文で確認） | 外部interfaceの契約正本 | 是正済み: DISC-004として当該1文を実態へ書き換えた。reviewerが「escape・実体参照・折りたたみ・details」で`docs/specs/`・`.agent-skill-chain/docs/`・`.agent-skill-chain/templates/`を全件grepし、矛盾する記述が残っていないことを確認した | resolved | 記述の粒度差はM-08として残る |
| M-07 | Medium | `escapeFoldBoundary`が素通しする形が残っていた（属性付き`</details foo>`、大文字、本文中の生の`<details>`開始tag、改行を挟む形、未閉backtick以降） | 是正前のreviewer敵対入力14件の実測 | R-01と同じ失敗（後続成果物が区画の外へ出る、または全体が折りたたみ内に残る）を再現しうる | 是正済み: 検出を`<\/?\s*details\b[^>]*>`へ広げ開始tagも置き換える。**reviewerが是正後に18入力で再実測**し、閉じtag（素・大文字・空白・属性付き）と開始tag（素・属性付き・大文字）は置換、inline code・fence・`< /details>`・`<detailsfoo>`・`<detail>`・`<summary>`は保持を確認。行を跨ぐ形はコメントで既知の限界として明示された | resolved | 未閉backtick以降はL-07として残る |
| L-06 | Low | SCN-UNIT-ISSUESYNC-002の一致assertionの期待値が`escapeFoldBoundary`自身から導出されており、同関数の変更に対して独立していなかった | 是正前の`expectedInner` | 当該assertionだけを見ると変異が素通しになる | 是正済み: 期待値を字面定数`EXPECTED_INNER`へ移し、置換対象6形と保持4形を字面で検査するstepを追加した（reviewerが原文で確認） | resolved | なし |

| M-08 | Medium | 開始tagも実体参照へ置き換える契約を明記するのは06だけで、REQ-SQ-033・TERM-ASC-102・staging 01/02は閉じtagしか書いていなかった。入れ子foldが字面表示になる仕様変化もどの正本にも無かった | 是正前の全件grep（reviewer実測） | 記述の粒度差。閉じtagだけと読んだ実装者が開始tagの処理を落とすと入れ子で構造が壊れる | 是正済み: DISC-005として4正本へ「属性と大文字小文字を問わず`<details>`・`</details>`を置き換える。入れ子foldは字面表示。行跨ぎと未閉backtick以降は既知の限界」を追記。reviewerが全件grepで同じ向き・同じ粒度になったことを確認 | resolved | なし |
| L-07 | Low | 未閉のinline backtickより後ろの同一行はcode扱いになり置換されない。GFMでは未閉backtickはliteralとして描画されるため、その後ろの生tagがlive HTMLになりうる | reviewer実測（`` x`</details>y ``は変化しない）。是正前は既知の限界に挙げられていなかった | 稀な字面での構造破壊 | 是正済み（記録として）: 行跨ぎと並べて実装コメントとREQ-SQ-033へ既知の限界として明記した。挙動は変えていない | resolved | 稀な入力では依然として壊れうるが、限界として文書化された |
| L-08 | Low | `escapeFoldBoundary`内の`replace`のternaryが`structural`にmatchした時点で常に真で、else枝へ到達しなかった | 是正前の当該ternary（正規表現の包含関係で確認） | 死んだ分岐。読者に「置き換えない場合がある」と誤読させる | 是正済み: `value.replace(structural, (tag) => …)`の単純形へ戻した。reviewerが18形の再測定で挙動不変（mismatch 0）を確認 | resolved | なし |

| M-09 | Medium | stagingへDISC-005・DISC-006を追記した後にStep 9が再記録されておらず、`staging-record.json`の`digest`が現在の内容と一致しない。最終HEAD時点の記録値は`17096aa6…4662`、reviewerが現在の内容から再計算した値は`0f8e20c5…c0bc` | reviewerが`calculateStagingDigest`で再計算して比較（round 2の最後の2回とも不一致を観測） | `review round --apply`と`workflow record --step=10`はstaging digestを照合するため、この状態では不一致で止まる。git追跡下の差分と本reviewの対象には影響しない | 修正（PR作成前の運用手順）: artifactをcommitする前に`workflow record --step=9`でStep 9を再記録し、digestを現在の内容へ固定してから`review round --apply`へ進む。**Step 10記録より後では上流再確定が要るため、順序を守る** | valid | 順序を誤るとStep 10記録後にdigest再固定の経路が狭くなる |

| R-04 | Low | **外部review 2巡目。** `04_レビュー.md`が成果物の配置先を`docs/reviews/`だけに限定し、TERM-ASC-101と`01_開発ワークフロー.md`が受理する`.agent-skill-chain/reviews/`を落としていた | 是正前の04冒頭。正本はTERM-ASC-101のevidence-only allowlistと規範282行 | 配布雛形の案内。利用projectが`.agent-skill-chain/reviews/`へ置くと雛形の案内と食い違う | 是正済み: 両方の配置先を案内する形へ戻した（reviewerが正本と同じ集合であることを確認） | resolved | なし |
| R-05 | Medium | **外部review 2巡目。** 11事前確認が「全項目を満たしてから`pr create`を実行する」と定める節に、PR作成後のread-backでしか成立しない2項目（PR head・成功CI head・独立review commitの一致、Actions runの関連PR番号）を置いていた | 是正前の§3末尾。`06_外部インターフェース/01_コマンド・GitHub契約.md`はPR作成の前提を`create-prepared`、mergeの前提を`pr-bound`と定める | PR作成前に満たせない項目を前提に数えると、利用者は満たせないまま進むか不要に止まる | 是正済み: `## 4.1 PR作成後のread-back（`pr-bound`で確認する）`を新設して2項目を移した。**reviewerが残る§1〜§4を1項目ずつ検査し、同型の混在が無いことを確認した** | resolved | 冒頭の「全項目」の文言はL-09として残る |
| R-06 | Low | **外部review 2巡目（本artifact）。** §0の`対象外`が`src/domain/issue.ts`と`docs/specs/`の一部を「未変更」として挙げていたが、round 2で両方とも変更・再監査していた | 是正前の§0 `対象外`行と`git diff --name-only b7bf8deb 51a9ec48`（22 path） | 本artifactの記述。監査範囲の誤表示 | 是正済み: reviewerが`b7bf8deb`以降に差分の無い7 pathを実測して列挙し、残る21 pathは再監査した旨へ書き換えた | resolved | なし |
| R-07 | Low | **外部review 2巡目（本artifact）。** 静的検査の件数を13種と書いたが列挙は12個で、敵対入力の件数も箇所により32と50で食い違っていた | 是正前の§0・§1・§4・§7 | 本artifactの記述。実測値の再現性 | 是正済み: 静的検査を「12種＋`conformance:check`」、敵対入力を「3回で延べ50入力（最終形は18形）」へ統一した | resolved | なし |
| L-09 | Low | 11事前確認の冒頭は「全項目を満たしてから`pr create`を実行する」と書くが、新設した§4.1はPR作成後にしか成立しない。節内に「`pr create`の前には成立しない」と書かれているため実害は小さいが、冒頭の文言は§4.1を含むと読める | `.agent-skill-chain/templates/issue/11_プルリクエスト事前確認.md`の3行目と§4.1（reviewer実測） | 配布雛形の案内。利用者が§4.1をPR作成前の前提と誤読しうる | 対象外（記録のみ）: 冒頭を「§4.1を除く全項目を満たしてから`pr create`を実行する」へ1語足すのが安価。次に同fileへ触れるときに直す | 冒頭を「§4.1を除く全項目を満たしてから`pr create`を実行する。§4.1はPR作成後の`pr-bound`で確認する」へ直した | resolved |

指摘なしの場合は「指摘なし」と明記する。本roundは未解決Critical/High 0件。resolvedはM-01・H-02・H-03・M-03・M-04・M-05・M-07・M-08・L-02〜L-08・R-01・R-03・R-04〜R-07の21件、duplicateはR-02、validで残るのはM-02（routingの迂回）・M-09（staging digestの再記録待ち）・L-01（quick/pocの行数）・L-09（11事前確認の冒頭文言）の4件。

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: はい。§3の5観点と§4の8観点を全件確認し、根拠を各行へ書いた
- 指摘を確定した: はい。M-01・M-02・L-01・L-02の4件
- round 1内でのM-01是正を検証した: はい。implementerがgit追跡下のfileを変えずstagingの01 FR-03と03 §4.1（DISC-001）を再確定し、reviewerが原文確認・`issue validate --stage=design`・staging digest再計算・対象unitの再実行で確認した。`H_impl`は`b7bf8deb`のままでroundを消費しない
- 次ラウンド対象のCritical/High: なし（round 1は`approved`で収束）

### ラウンド2

- 未解決Critical/High: なし。round 2で確定したH-02とH-03、外部review由来のR-01はいずれも同round内で是正され、reviewerが独立に再検証した。M-03〜M-08・L-02〜L-08・R-02〜R-07も同roundで是正・再検証済み。残るM-09はstaging digestの再記録という運用手順、L-09は雛形の文言で、いずれもgit追跡下の差分の正しさに影響しない
- 修正差分: `b7bf8deb`..`51a9ec48`の22 file。内訳はownerのscope拡大（DISC-002）による実装14 fileと、H-02是正の`test/features/unit/issue-template-contract.feature`（+12行、SCN-013/014）・`test/steps/issue-template-brevity.steps.ts`（+96行）、M-03是正の`11_プルリクエスト事前確認.md`・`12_利用案内.md`、M-05是正の01・02・03・04雛形（説明文11行の削除）、M-04・L-03是正の`docs/specs/15_要件追跡/00_追跡表.md`とstaging、外部review是正の`src/domain/issue.ts`（`escapeFoldBoundary`）・`dist/src/domain/issue.js`・`test/`2 file・雛形4 file（空行の復元）・`docs/specs/`2 file、H-03/M-07/L-06是正の`docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`・`src`・`dist`・`test/`2 file、M-08/L-07/L-08是正の`docs/specs/`2 file・`src`・`dist`、外部review 2巡目（R-04・R-05）是正の雛形5 file
- 修正で触れた隣接範囲: `renderReviewArtifactDraft`の監査表置換regex、`prefillFullArtifacts`の置換行、`check_skill_templates`のmarker、既存testが字面で名指しする04・11本文の記述を現在の原文で再確認した。隔離repositoryで`issue create`を実行し、管理情報の移動が事前充填と節順を壊していないことを実測した。**新assertionの実効性はreviewer自身の変異3件で確認した**（04の要約節削除→`review templateは問題・解決・実施・確認・判定の要約表を持つ`が失敗、00の管理情報を先頭へ復帰→`00から03の最初の節見出しは管理情報ではない`が失敗、12の読者表削除→`全templateは冒頭に読者3区分の読者表を持つ`が失敗。各1 scenarioだけが落ち、複写で復元してmd5一致を確認）。M-05是正の削除11行は、参照行の字面・routing 5語・`H_final後`・`formal approval`・`「JSON出力のみ」を非適用の根拠にせず`・`DISC-*`・`### 5.1 project選択test layer`・parallel-progress markerと1件ずつ突き合わせ、機械契約が要求する字面を含まないことを確認した。外部review是正では`escapeFoldBoundary`へ敵対入力14件を、H-03/M-07是正後はさらに18件を与えて置換境界を実測し、`dist/src/domain/issue.js`が`tsc -p tsconfig.build.json`の出力とsourceMappingURL行以外一致することを確認し、折りたたみを使わない経路（checkpoint 4・quick・poc）が分岐へ入らないことをSCN-UNIT-ISSUESYNC-003と原文で確認した。最終roundでは契約を持つ正本を全件grepしてH-03と同型の取り残しを点検し、M-08の粒度差が解消したことを確認した。L-08の単純化では18形を再測定して挙動不変（mismatch 0）を確かめ、staging digestが再記録待ちであることをM-09として記録した
- 既承認・未変更範囲を再走査していない: はい。`docs/specs/`の概要・索引・要件一覧・機能一覧、`test/`の4件は`b7bf8deb`以降に差分が無く、個別監査表では「round 1、以降不変」と記録して再評価していない。最終是正で触れた雛形5件と、その隣接依存（`create-prepared`と`pr-bound`の境界を定める`01_開発ワークフロー.md`・`06_外部インターフェース/01_コマンド・GitHub契約.md`、TERM-ASC-101の配置先、統合した6 bulletの欄名）だけを再監査した。`b7bf8deb`以降に差分の無い7 pathは再走査していない

### ラウンド3

- 全指摘の最終分類: 未実施（round 2で収束。resolved 21件、duplicate 1件、valid 4件はいずれも停止理由にしない）
- 任意の危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: 未実施。危険範囲なし（全変更が文書とtestでrevert可能）
- 同じ範囲の予算を自動更新していない: はい。round 2までで2 counted round
- AIによる最終裁定: approved（round 2で確定）

## 7. テスト結果

- 実行したcommandの一覧: reviewer実測（worktree `20260916_092713-1406-document-reader-brevity`、最終HEAD `51a9ec48`）。(1) `npm test`は`f9eff7d3`で単独実行 → 2,077 scenarios（2,061 passed、16 skipped）、10,907 steps（10,857 passed、50 skipped）、終了値0。**最終HEADの差分は雛形5 fileのMarkdownだけで、implementerが単独実行中のためreviewerは並走させていない。** (2) `npm run conformance:check`（`f9eff7d3`） → 87 scenarios全合格。(3) `cucumber --tags "@issue-scaffolding or @document-reader or @issue-template-contract"`（`f9eff7d3`） → 76 scenarios/424 steps、最終HEADでは`--tags "@issue-template-contract"` → 61 scenarios/334 steps全合格。(4) 静的検査12種を1本ずつ順に実行し全て終了値0（`f9eff7d3`で全12種、最終HEADでは`typecheck`・`lint`・`docs:format`・`test:format`・`skills:check`・`workflow:check`・`cli:check`・`trace:check`の8種）: `typecheck`・`lint`・`format:check`・`docs:format`・`test:format`・`skills:check`・`workflow:check`・`cli:check`・`trace:check`・`architecture:check`・`source:check`・`package:check`。(5) 変異試験3件（reviewer独自）→ すべてkill、複写で復元しmd5一致。(6) `escapeFoldBoundary`へ敵対入力を3回で延べ50入力（最終形は18形） → 最終形は期待値と全件一致（mismatch 0）。置換は閉じtag（素・大文字・空白・tab・属性付き・1行2件）と開始tag（素・属性付き・大文字）、保持はinline code・fence（```・~~~・字下げ）・`< /details>`・`<detailsfoo>`・`<detail>`・`<summary>`。扱えない2形（行跨ぎ・未閉backtick以降）は正本とコメントに既知の限界として明示。(7) 実stagingから`renderIssueSyncBody`で本文を生成し、code外の`<details>`3件・`</details>`3件・`<summary>`3件の均衡を実測。(8) 隔離した一時repositoryで`issue create --mode=full` → 複写729行、事前充填一致、4 fileとも最初の節が§1で末尾の節が管理情報。(9) `wc -l .agent-skill-chain/templates/issue/*.md` → 1,432行（基点1,434行未満）。段落直後に空行なしで始まる表は0件。(10) `tsc -p tsconfig.build.json`を一時dirへemitし`dist/src/domain/issue.js`と突合 → sourceMappingURL行以外の差分0。(11) `git diff --name-only d499a5ef 51a9ec48 -- .github scripts` → 0件。(12) 本artifactの全14表についてheaderとrowのcell数一致を自作scriptで検査 → 不一致0件。(13) 契約を持つ正本を「escape・実体参照・折りたたみ・details」でgrepし4箇所を全件突合。(14) staging digestを再計算し`staging-record.json`と比較 → 不一致（M-09）。(15) 11事前確認の§1〜§4を1項目ずつ`create-prepared`/`pr-bound`の境界と突合
- 全layerの合計: 2,077 scenarios、2,061 passed、失敗0、16 skipped、10,907 steps（`f9eff7d3`で実測。最終HEADの差分はMarkdown 5 fileのみ）。**`npm test`は他のnpm scriptと同時に走らせず単独で実行した**（並行実行は`dist/`を奪い合い偽陽性を生む）
- runner・Gherkin方言: cucumber-js、方言`en`（keyword英語・本文日本語）。`test:format`合格で確認
- implementer申告との差: `npm test`（2,077/2,061/16）と静的検査の結果、`@issue-template-contract` 61 scenarios/334 steps、雛形総量1,432行は一致した。implementerが数える「静的検査15種」は`build`・`project:quality`・`conformance:check`を含む数え方で、本節の12種＋`conformance:check`とは範囲が違う。18形の再測定、3 tag合計76 scenarios、複写729行、実bodyの均衡、正本の全件grep、`create-prepared`/`pr-bound`境界の突合、staging digestの再計算（M-09）はreviewerの独自実測である

## 8. 配布物影響

projectがpackageとして配布される場合だけ記入する。配布境界はpackage manifestの配布file指定を単一正本とし、compileされて配布される`source`も配布境界に含める。

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | 入る | 利用projectの規範に「文書の読者」節と書きぶりの既定が加わる。強制点なし |
| `.agent-skill-chain/templates/issue/00_要求定義_full.md` | 入る | 複写される雛形の目的が先頭、管理情報が末尾になる |
| `.agent-skill-chain/templates/issue/00_要求定義_poc.md` | 入る | 同上 |
| `.agent-skill-chain/templates/issue/00_要求定義_quick.md` | 入る | 同上 |
| `.agent-skill-chain/templates/issue/01_要件定義.md` | 入る | 同上。説明文が1文へ縮む（表の直前の空行は保持） |
| `.agent-skill-chain/templates/issue/02_設計.md` | 入る | 同上。説明文が1文へ縮む |
| `.agent-skill-chain/templates/issue/03_実装計画.md` | 入る | 同上。§11が§4.1へ統合され、説明文が1文へ縮む |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | 入る | review成果物の先頭に要約5行が入り、識別情報が末尾へ移り、配置先の案内が`docs/reviews/`と`.agent-skill-chain/reviews/`の両方になる |
| `.agent-skill-chain/templates/issue/11_プルリクエスト事前確認.md` | 入る | 読者表が入り、独立性の判定基準が`merge.reviewIndependence`になり、PR作成後にしか成立しない2項目が`§4.1 PR作成後のread-back`へ分かれる |
| `.agent-skill-chain/templates/issue/11_プルリクエスト本文.md` | 入る | PR本文に読者表が入る |
| `.agent-skill-chain/templates/issue/12_利用案内.md` | 入る | 読者表とpoc行が入り、使い方に書きぶりの案内が入る |
| `dist/src/domain/issue.js` | 入る | `issue sync --generate-body --checkpoint=8`（full）の本文が00＋折りたたみ01〜03になり、code外の生の`<details>`・`</details>`が属性と大文字小文字を問わず実体参照として出力される（表示は元の字面のまま。意図した入れ子foldは字面表示になる）。reviewerが`tsc`の出力と一致を確認 |
| `docs/specs/00_仕様書構成/00_仕様書索引.md` | 入らない | なし |
| `docs/specs/01_システム概要/00_概要.md` | 入らない | なし |
| `docs/specs/01_システム概要/02_用語・略語.md` | 入らない | なし（TERM-ASC-102の定義を訂正し開始tagの扱いを明記） |
| `docs/specs/02_要件/00_要件一覧.md` | 入らない | なし |
| `docs/specs/02_要件/01_ワークフロー要件.md` | 入らない | なし |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | 入らない | なし（REQ-SQ-033のescape規則と既知の限界を確定） |
| `docs/specs/04_機能/01_ワークフローv0.3.md` | 入らない | なし |
| `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | 入らない | なし（折りたたみ契約を実態へ訂正） |
| `docs/specs/15_要件追跡/00_追跡表.md` | 入らない | なし |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | なし |
| `src/domain/issue.ts` | 入る | compileされて配布される`source`として配布境界に含まれる。`escapeFoldBoundary`が`<details>`・`</details>`の両方を扱う。配布される実体は`dist/src/domain/issue.js` |
| `test/features/unit/document-reader.feature` | 入らない | なし |
| `test/features/unit/issue-scaffolding.feature` | 入らない | なし |
| `test/features/unit/issue-template-contract.feature` | 入らない | なし |
| `test/steps/document-reader.steps.ts` | 入らない | なし |
| `test/steps/issue-sync-body.steps.ts` | 入らない | なし |
| `test/steps/issue-template-brevity.steps.ts` | 入らない | なし |

判断: 配布物を更新した

根拠: `package.json`の`files`（`dist/src/`・`.agent-skill-chain/docs/`・`.agent-skill-chain/templates/`）とcompileされて配布されるsourceに入る13 pathを更新済み。配布10雛形はすべて読者表を持ち総量は1,434→1,432行で、次回のpackage更新で利用projectへ届く。折りたたみの実体参照置き換えは`dist/src/domain/issue.js`として配布され、利用者の同期本文の構造が保たれる。既存stagingは受理集合不変（INV-01）で通ることを`issue validate`で確認した。

**runtimeの挙動が変わるのに配布文書が追随していない状態を残さない。** 利用者は配布物だけを読むため、repository内部の仕様書へ書いても届かない。

## 9. 独立reviewの成立

**この節はPhase Aで埋める。** 記入するのはPR作成前に観測できるものだけであり、**immutable review IDやapprovalの件数などPR作成後にしか存在しない値は書かない。** それらはPhase Bで`review evidence`が観測して記録する。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated（`.agent-skill-chain/project-policy.json`の`merge`は空objectで`reviewIndependence`未宣言のため既定） |
| その要求を満たすこと | はい（implementerと別session/context、exact HEAD `f9eff7d3`固定、対象差分の非変更、肯定・敵対reviewとfindingを§3〜§5へ記録） |
| reviewerとimplementerのidentity・context比較 | implementer: 進行役Claude session（context A、03 §0.1）。reviewer: 進行役が起動した別のClaude subagent session（context B）。GitHub actorは同一（`context-isolated`では許容）。provider・tierは独立性の判定に使わない |
| reviewerが対象差分を変更していないこと | はい（変異試験で一時的に触れた雛形3 fileは複写で復元しmd5一致を確認。`git status --short`は`?? docs/reviews/223_課題1406文書の読者と同期本文折りたたみレビュー.md`の1件だけ、`git diff`は空） |

外部への不可逆な配布で独立reviewの外部証拠を要求され、かつそれが無い場合だけ記入する。

| 項目 | 内容 |
|---|---|
| 適用する例外の識別子 | 該当なし（本PRはpackage registryへの公開を含まない） |
| 観測値 | 該当なし |

**承認元、承認者、承認日時、失効日時は正本を参照し、ここへ複製しない。**

例外が正本に無い、または失効している場合は進めない。記録があれば実装・PR・mergeは進み、mergeを契機に自動で走るtag作成とReleaseも進む。止めても不可逆な行為を防げないためである。package registryへの公開など外部への不可逆な配布だけが、記録の有無にかかわらず独立reviewの外部証拠を要求する。削除、force push、履歴書き換えは独立reviewの対象外であり、既存のauthority経路で守る。

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `02_要件/00_要件一覧.md`（REQ-SQ-033/034）、`02_要件/01_ワークフロー要件.md`、`02_要件/04_仕様・品質管理要件.md`（REQ-SQ-033へ対象範囲・review要約・理由の任意化を含む）、`04_機能/01_ワークフローv0.3.md`、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`01_システム概要/00_概要.md`・`02_用語・略語.md`、`00_仕様書構成/00_仕様書索引.md`、`15_要件追跡/00_追跡表.md`（SCN-UNIT-ISSUETPL-013/014と04・11・11・12を追加）、`01_変更履歴.md`。外部review是正では`02_要件/04_仕様・品質管理要件.md`（REQ-SQ-033）と`01_システム概要/02_用語・略語.md`（TERM-ASC-102）を、H-03是正では`06_外部インターフェース/01_コマンド・GitHub契約.md`を、M-08是正では再びREQ-SQ-033とTERM-ASC-102へ開始tagと既知の限界を追記した
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: はい。round 2で用語の追加・変更は無い（TERM-ASC-121・102はround 1のまま）
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: 読者3区分の字面は規範・雛形10件・概要・索引・台帳・testで一致する。同期本文のescape規則は契約を持つ4正本（06外部IF・REQ-SQ-033・TERM-ASC-102・staging 01/02）すべてが同じ向き・同じ粒度になり、開始tagの扱いと既知の限界まで一致した（reviewerが全件grepで確認）
- 要件・変更・SCN・テストの追跡: REQ-SQ-033→AC-SQ-033→SCN-UNIT-ISSUESYNC-002/003・ISSUETPL-011〜014・SPECDOC-001、REQ-SQ-034→AC-SQ-034→SPECDOC-002。`trace:check`はorphan 0で合格し、013/014が04・11・12を走査する。DISC-003で追加したescape規則はSCN-UNIT-ISSUESYNC-002の構造均衡assertionが観測する
- `no-spec-impact`の場合の限定的根拠: 該当なし（updated）
- UI・トークンの判断: UIなし、token不要。`00_仕様書索引.md`が05・17・18を対象外と宣言

## 11. 総合判定と再開地点

- 未解決Critical/High: なし。H-02・H-03・R-01はいずれもround 2内で是正され、reviewerが独立に再検証した（新assertionへの変異3件、`escapeFoldBoundary`への延べ50入力、契約正本4箇所の全件grep、実stagingの本文均衡、`npm test`と静的検査12種＋`conformance:check`、`create-prepared`/`pr-bound`境界の突合）
- Medium/Lowの記録: resolvedはM-01・H-02・H-03・M-03〜M-08・L-02〜L-08・R-01・R-03〜R-07の21件、duplicateはR-02。validで残るのはM-02（routingの迂回）、M-09（staging digestの再記録待ち）、L-01（quick/pocの行数）、L-09（11事前確認の冒頭文言）。Medium/Lowは停止理由にしない
- 判定: approved
- 新しい権限が必要な事項: なし
- 残存リスク: 行を跨ぐtagと未閉backtick以降は稀な字面で折りたたみを壊す（正本とコメントに既知の限界として明示済み）。利用者が意図した入れ子foldは字面表示になる。L-09の文言を誤読するとPR作成前に止まる。GitHubの`<details>`描画仕様変更（02 §12、owner判断）。M-02の迂回が常態化しrouting evidenceが蓄積されない
- 次に許可される操作: **先に`workflow record --step=9`でStep 9を再記録し、staging digestを現在の内容へ固定する（M-09）。** その後に本artifactを`H_impl`の後へ1 fileだけcommitして`H_final`にし、`review round --apply`でround 2を確定、`workflow record --step=10`、`pr create --head-sha=<H_final>`へ進む。順序を逆にするとdigest不一致で止まり、Step 10記録後は上流再確定が要る
- 次回の再開地点: journal最終entryとstaging。digestはM-09の再記録で更新されるため、再開時は`staging-record.json`の実値を読む。round 3を開く必要は無く、Step 9再記録→artifact commit→Step 10記録から再開できる
