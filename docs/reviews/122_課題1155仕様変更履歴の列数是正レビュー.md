# 122 課題1155 仕様変更履歴の列数是正 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。**新しい「量を判断へ比例させる」原則に従って書く。判断のある節を書き、判断の無い節は1行へまとめる。**

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1155 |
| 比較基点 | `3d048a5c16c98fd726596abcc62e06fee0132cb3` |
| H_impl | `2fa7ab14b63808c4f97cc9203ff0b62aca45f719` |
| 対象差分 | `docs/specs/15_要件追跡/01_変更履歴.md` とラウンド1の成果物の2 file |
| 対象外 | **列数検査を足すこと。** 実測で不一致が1件だけと判明したため。変更履歴の列定義そのもの。他の仕様fileの表 |
| 残り予算 | Step 10の上限3ラウンドを使い切った |
| ラウンド | Step 10 ラウンド3 |
| ラウンド数 | 3（Step 10のラウンド1〜3） |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260903_092450_仕様変更履歴の列数をそろえる |
| モード | quick（Q-01〜Q-08がすべて真） |
| 仕様の所有箇所 | 該当なし: #1155。`01_変更履歴.md` は記録fileであり要件を持たない |
| 成果物行数 | 製品: 1 fileで2 insertions・1 deletion。**支援層: staging 00が5,225文字（本日平均34,806文字の15%）** |
| 縮小の先行評価 | 実施済み。**列数検査を足さない。** 起票時に自ら定めた「1件のために検査を足すか、N件あるから足すかで判断が変わる」に照らし、実測1件のため足さない |
| authority | 変更しない。記録fileの表示上の是正である |
| 実施者・日時 | reviewer、2026-09-03（JST） |

### 0.1 routing入力契約

| role欄 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類 | routine（riskがlow） | claude | Opus 5、effort high | 他Issueの履歴行へ触れたら停止する | implementerとreviewerはいずれも本sessionのAI agentであり、**Git commit authorも同一である** |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 列ずれの現存 | `01_変更履歴.md:6`（是正前） | 8列。見出しは9列。`Issue・PR` 列が欠け `判断者` と `HEAD SHA` がずれていた | 実測 |
| **不一致は1件だけであること** | `docs/specs/` 全Markdown表 | inline codeと `\|` を除いて数え直し**1件**。**最初の「8件」は誤りだった** | 実測 |
| 検査が無いこと | `docs:format`、`trace:check` | いずれもMarkdown表の列数を検査しない | 実測 |
| 内容の非変更 | `git diff` | 2 insertions・1 deletion。削除1行は是正対象の行そのもの | 実測 |
| 他Issueの行の非変更 | `git diff origin/main` | 削除は当該1行のみ | 実測 |
| 全gate | `conformance:check` | 1419 scenario、1403成功、0失敗、16 skip | 実行観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: pass。記録fileであり他から参照されない
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: pass
- reviewer stable IDがPR author/実装commit author stable IDと異なる: **異ならない。** §9に記録した
- 既定branch追随: **実施済み。** `main` がrelease bumpで動いたため追随し、他Issueの履歴行への削除が0件であることを再確認した

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | 該当1行へ `Issue・PR` 列を挿入し、本変更自身の行を追加する | 記録のみ。循環なし | AC-01からAC-03。SCN-UNIT-SPECNORM-001 | revertで復旧 | pass |
| `docs/reviews/122_課題1155仕様変更履歴の列数是正レビュー.md` | A | package | evidence | ラウンド1の成果物。**ラウンド3の比較基点..H_implへ入るため個別監査の対象になる** | evidence層は製品を参照しない | 該当なし | commitを戻す | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: pass（2件）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: pass

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | **最初の実測が誤っていた。** エスケープ済みパイプ `\|` とinline code内のパイプを列区切りと誤認し8件と報告した | 「N件あるから検査を足す」判断へ傾きかけた | なし | inline code区間を除去し `\|` を除いてから数え直した。**正しくは1件である。** #1155へ訂正を記録した | 再計数の結果 | no-spec-impact | pass |

**反射的に検査を足す判断の直前で、自分の計数方法を疑って測り直したことで止まった。**

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-SPECNORM-001 | 該当行が9列 | 正しい計数で9列 | pass | §1 |
| AC-02 | SCN-UNIT-SPECNORM-001 | 表の内容を変えない | `git diff` で列区切り1つの挿入のみ | pass | §1 |
| AC-03 | SCN-UNIT-SPECNORM-001 | 他Issueの履歴行へ触れない | `git diff origin/main` の削除が当該1行のみ | pass | §1 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | not-applicable | 記録fileの表示上の列ずれであり、認可も秘密情報も扱わない | 変更は列区切り1つの挿入のみ |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 変更履歴は仕様変更の正本であり、誰が判断したかの読解を誤らせていた | `判断者` と `HEAD SHA` が正しい位置に来る |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 人が触れるUIを持たないMarkdown文書である | `projectKind` が `cli` |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UI componentもthemeも持たず、色・間隔・typographyの決定を含まない | `capabilities.designTokens` が `not-applicable` |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 該当行が9列になり値の位置が正しくなった |
| 価値 | 利用者・運用上の目的を満たすか | pass | 変更履歴の読解を誤らせない |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 列区切り1つの挿入で成立する |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 本変更自身も変更履歴へ記録した |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 表の内容を変えていない |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| **悪用** | **他Issueの履歴行を巻き込む** | pass | **過去に `sed` で同型の事故を起こしている。** `git diff origin/main` の削除が当該1行のみであることを追随後にも再確認した |
| 反例 | 要件を破る入力・状態がないか | pass | 正しい計数で全行9列 |
| 安全性・データ損失・ロールバック | 認可・秘密情報・上書き・復旧 | pass | 記録fileの1行編集。revertで戻る |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | `package.json` の `files` に `docs/specs/` を含まない |
| 失敗経路・境界値 | 外部失敗・空・重複 | 対象外 | 1行の編集であり値を解釈しない |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| L-01 | Low | **列数検査が無い状態は残る。** 同型の欠陥が将来また入りうる | 実測。`docs:format` は表の列数を見ない | 将来 | **足さない。** 実測1件であり、素朴なパイプ計数は偽陽性を7件出した。**1件の是正のために払う費用として釣り合わない** | valid（設計判断。record-only） | 残存する |

**Critical/High 0件。**

## 6. ラウンド固有の確認

### ラウンド1（Step 10、2026-09-03、candidate HEAD = `H_impl`）

- 全評価基準を確認した: はい。肯定5観点・敵対5観点をすべて評価した
- 指摘を確定した: はい。L-01は記録のみ
- 次ラウンド対象のCritical/High: **なし**
- blocking: 0件

### ラウンド2

- 本artifactのcommitでHEADが動くため再固定する

### ラウンド3（Step 10、2026-09-03、candidate HEAD = Step chain是正後のartifact commit）

- **1回目のstagingが `gh auth` の一時失敗で `reconciliation-required` へ入り、`dispatchClaimedAt` 消費済みのため正規経路で復旧できなかった（#1157 として起票）**
- **delivery stateの削除やstagingの複製は統制の迂回であるため採らず、Step 0からやり直した。** 本artifactの Step chain 欄を新しいstaging pathへ直した
- E-01（valid、是正済み）: Step chain 欄が旧stagingを指していた
- blocking: 0件

## 7. テスト結果

**承認証拠は成功CI runである。**

実行したcommand: `docs:format`、`test:format`、`lint`、`format:check`、`typecheck`、`source:check`、`trace:check`、`architecture:check`、`skills:check`、`conformance:check`、`audit:check`、`issue validate`

全layerの合計: **1419 scenario、1403 成功、0 失敗、16 skip。** `conformance:check` は内部で `npm test` を実行するため単独で回した。

失敗またはskipがある層: skipした16 scenarioはsemantic graphのGraphQLite assetが未設定な環境に起因する。本差分はこの経路に触れない。

対応する成功CI run: PR作成後に取得し、merge前に現`H_final`へ束縛する。

**変異試験を行わない。** 記録fileの1行是正であり判定logicを持たない。

## 8. 配布物影響

| 変更path | 配布境界に入るか |
|---|---|
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない |

判断: 配布物を更新しない

根拠: `package.json` の `files` は `docs/specs/` を含まない。

**releaseの起動と計画は分けて記録する。** `release.yml` は `main` への `push` で起動し、その後 `planAutoRelease` が配布digestの差分を見て計画を決める。**本変更は配布digestを変えないため、version bumpとtagは作られない見込みである。**

| 対象 | revertで戻るか | 実行者 | 手順 | 完了確認 |
|---|---|---|---|---|
| repository状態 | **戻る** | 実行役 | 打ち消しPRを通常mergeする | `git diff` が元の状態と一致する |
| 公開済みtagとGitHub Release | **作られない見込み** | 該当しない | 配布digestが変わらない | `gh release list` に新tagが載らない |
| npm registryの公開物 | **公開されない** | 該当しない | 本経路では公開しない | `npm view` に該当versionが無い |

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | **本artifact作成時点では無い。** exact-headは`H_final`。immutable GitHub reviewは0件 |
| reviewerがPR author・実装commit authorと異なる | **異ならない。** 同一sessionのAI agentであり、Git commit authorも同一である |
| 観測したreview commentとapprovalの件数 | 本session内のreview 2ラウンド（Critical/High 0、Low 1）。GitHub review 0件 |

**`02_品質基準.md` の「独立reviewが成立しない場合」に従う。** 同節は「既定は停止ではなく、無記録での通過の禁止である」と定める。

**本件の発端は外部reviewerである。** PR #1154 で CodeRabbit が同型の欠陥を指摘し、その調査で `main` 側の本件が判明した。**私が単独で見つけたものではない。**

**PR作成後の外部reviewを待つ。** 予算1ラウンドを残してある。

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `15_要件追跡/01_変更履歴.md`。**要件・SCN・追跡のいずれも変えない**
- ドメイン用語台帳: 新規用語を追加しない
- 要件・変更・SCN・テストの追跡: 既存 `SCN-UNIT-SPECNORM-001` が仕様構造を検査する。**新規SCNを追加しないため追跡表を変更しない**
- UI・トークンの判断: UIを所有しないため対象外。ADRを追加しない

## 11. 総合判定と再開地点

- 未解決Critical/High: **なし**
- Medium/Lowの記録: L-01（列数検査が無い状態は残る）
- 判定: approved（Step 10 ラウンド3）
- 新しい権限が必要な事項: mergeは別authority
- 残存リスク:
  1. **列数検査が無いため同型の欠陥が将来また入りうる。** 実測1件のため足さないと判断した
  2. **私の最初の計数が誤っていた。** 同型の誤りを避けるため、表の列数を数えるときはinline codeとエスケープを先に除くことを記録した
- 次に許可される操作: push、PR作成、外部reviewの到着確認、必須check 2件の全緑確認、およびownerが承認したauthorityによる**通常merge commit方式**でのmerge。**squash mergeを使わない**
- 次回の再開地点: PR作成から
