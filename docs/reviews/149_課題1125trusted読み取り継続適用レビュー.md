# 149 課題1125 trusted読み取り継続適用レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1125 |
| ラウンド | Step 10 ラウンド1 |
| 比較基点 | `2f0d2288dea778f54769acfc5eb19f6a66e96ed2` |
| H_impl | `ac81a17922f06a97610c07d751a9b70bb833ddf6` |
| 比較基点の由来 | worktree作成時点の`origin/main`のtip（PR #1222 のmerge commit） |
| モード | full（Q-03がfalse。trusted品質gateの制御flowを変える） |
| 対象差分 | 7 file、+62 -21。commitは`ac81a179` 1件 |
| 対象外 | `validateTrustedQualityMigration`の内容。`ci.yml`側の読み取り。proposal registryの内容。`TQP-TRUSTED-READ-CONTINUE-001`の扱い（**規則上削除できない**）。`docs/reviews/`のrole authority不整合（#1047） |
| 残り予算 | **2**（同一範囲で最大3ラウンド。以降は収束後のHEAD移動に対する取り直し1回のみ） |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260905_101701_trusted-quality読み取り失敗で依存しない検査まで飛ばさない |
| 仕様の所有箇所 | `docs/specs/02_要件/04_仕様・品質管理要件.md`のREQ-SQ-030（**1段落追記**） |
| 成果物行数 | 製品 **+13 -13行**（`check_project_quality.ts` +21 -13、`package.json` +1 -1。うち8行はdoc comment）。仕様 **+5 -2行**。支援層 **+72 -0行**（feature +5、steps +67）。**支援層/成果物 = 5.5倍** |
| 縮小の先行評価 | **新しい機構を足さない。** 同一file内の`ci.yml`の読み取りが既に`if/else`であり、その形へ揃えただけである。代替2案（`return`直前でmigration検査を呼ぶ／読み取り失敗を無視する）を棄却した。**支援層が5.5倍になったのは、境界の両側（飛ばす検査と飛ばさない検査）をそれぞれ名指しで判定する必要があるためである。** 件数や`valid===false`だけの判定では変異2件のどちらかが生存する |
| 決裁 | **既定branchへ事前登録済みの`TQP-TRUSTED-READ-CONTINUE-002`（品質契約 11→12）が承認境界を満たす。** 登録はPR #1222でmerge済み |
| 実施者・日時 | reviewer（claude）、2026-09-05（JST） |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節） | advanced | claude | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerとreviewerが**同一session**である。0.2節に逸脱として開示する |

### 0.2 開示する逸脱

1. **implementerとreviewerが同一sessionである。** 緩和は、判定の根拠をすべて実行結果に置いたこと（sha256の一致、`--trusted-root`付き実行、両方向の変異試験）と、**是正後の内容がPR #1222の証跡でbyte単位に事前固定されていること**である。**内容を自分で決める余地が無い。**
2. **本Issueの前段（PR #1222）も私の作業である。** ただし `afterSha256` は既定branchへmerge済みであり、**本PRから変更できない。**
3. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 欠陥の現存 | `scripts/check_project_quality.ts`（`2f0d2288`時点） | 読み取り失敗直後の`return`により`validateTrustedQualityMigration`が実行されない | 一次資料 |
| 同一file内の不一致 | 同file の`ci.yml`読み取り | すぐ上は既に`if/else`である | 一次資料 |
| 要件の要求 | REQ-SQ-030 | 「その値に依存する検査だけを飛ばして残りを続行する」 | 一次資料 |
| **本文の再現** | PR #1222 証跡の2節のdiffを逐語で当てる | `145c8030dd7b778334fe30a47d15b81af34afbe77ffcfae677672e435d94f01c`。**登録済み`afterSha256`と一致** | 実行記録 |
| 二段階承認 | `check_project_quality.ts --root=. --trusted-root=<main>` | `valid: true`。`checks`に「base事前登録済みversioned staged proposalによる品質契約更新」 | 実行記録 |
| 実際に出る診断 | `SCN-UNIT-QUALITY-019` | `候補の保護対象file .github/workflows/trusted-quality.yml が存在しません` と `candidateのtrusted品質契約変更はbaseで事前登録済みのversioned staged proposalと完全一致しません` が**同時に出る** | テスト出力 |
| テスト | `npm run conformance:check` | `1498 scenarios (1482 passed, 16 skipped)`。project rule 21件、orphan 0件 | テスト出力 |
| 追跡 | `npm run trace:check` | valid。orphan 0件 | テスト出力 |
| commit前candidate | 7 file | working tree clean | Git index |
| commit後external | PR、CI run、review | Step 11で観測する | 外部のimmutable証拠 |

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `scripts/check_project_quality.ts` | M | package | script | 早期`return`を`else`分岐へ置き換える。**内容は`afterSha256`でbyte単位に固定されている** | `protectedFileText`と`validateTrustedQualityMigration`への単方向。循環なし | REQ-SQ-030 / AC-01・AC-03・AC-04 / SCN-UNIT-QUALITY-019 | **受理集合を広げない。** 読み取り失敗は引き続き`valid: false`。rollbackは次versionのproposal経由 | pass |
| `package.json` | M | project | project | `agentSkillChain.qualityContractVersion` を11から12へ。**保護fieldであり同じproposalが承認境界を満たす** | 適合 | AC-02 | 減少は受理されない。rollbackは次versionへ進める | pass |
| `test/features/unit/source-quality.feature` | M | package | test | Scenario 1件の追加 | test層のみ | AC-04・AC-06 / SCN-UNIT-QUALITY-019 | 一時directoryのfixture copyだけを扱う | pass |
| `test/steps/unit.steps.ts` | M | package | test | Given 1件とThen 1件。**診断文字列と`checks`の両方を名指しで判定する** | 同上 | 同上 | 同上 | pass |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | M | project | spec | REQ-SQ-030へ1段落追記。**強制主体を名指しする** | 適合 | REQ-SQ-030 / AC-SQ-030 | 記述のみ | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | REQ-SQ-030行へ新規SCNを結線 | 適合 | 同上 | 記述のみ | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | project | spec | 変更履歴を1行足す | 適合 | 同上 | 記述のみ | pass |

## 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `scripts/check_project_quality.ts` | 入らない | `package.json`の`files`に`scripts/`が無い |
| `package.json` | 入る | 配布file一覧そのもの。**ただし`files`も`bin`も`version`も変えていない** |
| `test/` 2件 | 入らない | `files`に`test/`が無い |
| `docs/specs/` 3件 | 入らない | project所有の仕様であり利用projectには不在 |

判断: 配布物を更新しない
根拠: **配布file数は343のまま変わらない。** `package:check` が合格した。変更した`agentSkillChain.qualityContractVersion`は開発repository固有の品質契約versionであり、利用projectの動作に影響しない。

## 2. 差分の要約

| 変更 | 内容 |
|---|---|
| 制御flow | 早期`return`を`else`分岐へ置き換え、workflow本文に依存する検査だけを飛ばす |
| 品質契約 | version 11 → 12 |
| Scenario | `SCN-UNIT-QUALITY-019` を追加 |
| 仕様 | REQ-SQ-030へ「早期returnを禁じる」と強制主体を追記 |

## 3. 肯定review

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ | pass | `--trusted-root` 付き実行が `valid: true`。sha256が `afterSha256` と一致 |
| 価値 | pass | 読み取り失敗と二段階承認違反が同時に存在するとき、1回の実行で両方が出る |
| 実現可能性 | pass | 既存の`if/else`の形へ揃えるだけである。新しい機構を足していない |
| 整合性 | pass | REQ-SQ-030 / AC-SQ-030 / SCN-UNIT-QUALITY-019 へ結線し `trace:check` が valid |
| 保守性 | pass | **同一file内の扱いの不一致が消えた。** 読み取り失敗の分岐が2箇所とも同じ形になる |

## 4. 敵対review

| 反例・攻撃 | 検証 | 結果 |
|---|---|---|
| 早期`return`へ戻しても通るのではないか | 変異M1を適用して `SCN-UNIT-QUALITY-019` を実行 | **kill。** migration検査のerrorが消える |
| 依存する検査も`else`の外へ出したら通るのではないか | 変異M2 | **kill。** `checks`に「base workflowによるcandidate設定のread-only検証」が残る |
| 受理集合を広げていないか | 読み取り失敗時の`valid` | **`false`のまま。** errorを積む処理を1行も消していない |
| 読み取り成功時の判定が変わっていないか | 既存Scenario | `1498 scenarios` 全合格。**既存assertionを1件も変更していない** |
| `afterSha256` と食い違っていないか | `sha256sum` | **一致。** 証跡のdiffから逐語で再現した |
| 候補側の自己承認になっていないか | 照合はbase checkoutのvalidator | **候補のcodeを実行しない。** 本PRから `afterSha256` を変更できない |
| 診断へfile内容や絶対pathが漏れていないか | 既存の`protectedReadError`を通す | 変更していない |

## 5. finding分類

| ID | 分類 | 内容 | 対処 |
|---|---|---|---|
| DISC-001 | resolved | 期待した診断 `protected差分なしで品質契約versionを変更できません` ではなく `candidateのtrusted品質契約変更は…完全一致しません` が出た。`qualityContractVersion` は `PROTECTED_PACKAGE_FIELDS` に属するため差分0件にならない | assertionを実際に出る診断へ直した。**どちらも `validateTrustedQualityMigration` からしか出ない**ため、migration検査が走った証拠としては等価である |
| DISC-002 | resolved | 未commitのまま `conformance:check` を走らせると `SCN-INT-CONSUMER-*` が `保護fileがHEADから変わっています` で落ちる | 実装をcommitしてから再実行した。10/10合格。**test側が正しい** |
| ADV-01 | record-only | 支援層/成果物が **5.5倍** である | **境界の両側を名指しで判定するために必要である。** 件数や `valid===false` だけの判定では変異2件のどちらかが生存することを実測した。縮小するとINV-01の検出力を失う |
| ADV-02 | record-only | `TQP-TRUSTED-READ-CONTINUE-001` は `staged` のまま残り続ける | 規則どおりである。適用されるのは実際の変更内容と完全一致する側だけであり、両立しない |

**blocking 0件。未解決のCritical / High 0件。**

## 6. 検証結果

| 検査 | 結果 |
|---|---|
| `npm run conformance:check` | `1498 scenarios (1482 passed, 16 skipped)`、`7891 steps`。project rule 21件、orphan 0件 |
| `npm run project:quality` | `valid: true`、errors 0件 |
| `--trusted-root=<main>` 付き実行 | `valid: true`。migration検査が `checks` に現れる |
| `npm run lint` / `format:check` / `typecheck` / `source:check` | すべて合格 |
| `npm run directories:check` / `skills:check` / `cli:check` / `workflow:check` | すべて合格 |
| `npm run docs:format` / `test:format` / `package:check` / `architecture:check` | すべて合格 |
| `npm run trace:check` | valid。orphan 0件 |
| `npm run audit:check` | Step 11直前に実行する |
| `sha256sum scripts/check_project_quality.ts` | `145c8030…`。**登録済み `afterSha256` と一致** |

### 変異試験

| ID | 変異 | 結果 |
|---|---|---|
| M1 | `else` 分岐を早期 `return` へ戻す | **kill** |
| M2 | 依存する検査も `else` の外へ出す | **kill** |

**2件ともkill。** 変異は複写で戻し、`sha256sum` が `afterSha256` へ復帰することを確認した。

## 7. 仕様更新

- **REQ-SQ-030へ1段落追記した。** 早期returnが「その値に依存する検査だけを飛ばす」に反すること、**強制主体が `SCN-UNIT-QUALITY-019` であること**を明記した
- **新規SCN `SCN-UNIT-QUALITY-019` を追跡表のREQ-SQ-030行へ結線した。** `trace:check` の orphan は0件
- **既存の3段落と既存SCNのassertionを1件も変更していない**
- 用語台帳の差分なし

## 8. 判定

**承認。** blocking 0件、未解決のCritical / High 0件。resolved 2件（DISC-001・002）、record-only 2件（ADV-01・02）。
