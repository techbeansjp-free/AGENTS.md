# 04 レビュー

> 肯定と敵対の両観点を毎ラウンド確認する。指摘なしの承認は有効。Medium/Lowは記録するだけで、自動修正・追加ラウンド・ゲート停止を起こさない。用語と責務の境界は`成果物用語と責務境界`（`.agent-skill-chain/docs/01_開発ワークフロー.md`）を正本とする。
>
> 埋めた成果物は`docs/reviews/`または`.agent-skill-chain/reviews/`へ置き、実装commitの後にその1 fileだけをcommitする。

| 読者 | 読む節 |
|---|---|
| 発注・評価する人 | 要約 → §5 指摘 → §11 総合判定 |
| 実装・レビューする人 | 全節 |
| 運用する人 | 要約 → §8 配布物影響 → §10 仕様整合性 |

## 要約

**この5行だけで、何を見たreviewかが分かるように書く。** 各行は1〜2文。詳細は対応する節が持つ。

| 項目 | 内容 |
|---|---|
| 何が問題だったか | 固定prefixだけのallowlistではネストしたcacheや拡張子別生成物を宣言できず、finalizeが繰り返し拒否した。 |
| 何を解決しようとしたか | trusted policyから限定された再帰directory・拡張子patternを宣言できるようにする。ASC管理記録の自動削除は対象外。 |
| 何を行ったか | 両schema、runtime matcher、検証シナリオ、仕様、配布distを更新した。 |
| 何を確認したか | 2,189シナリオ中2,173成功・16 skip、conformance 87成功。独立reviewerがschema/runtime、Git列挙・削除、symlink、ASC管理領域を確認した。 |
| 判定 | approved。指摘0件。 |

## 1. 入力証拠

PR番号、Actions run ID、immutable review IDはPR作成後にしか存在しないため**この文書へ書かない。** `review evidence`とdelivery stateがappend-onlyで保持する。reviewerの独立性は`merge.reviewIndependence`が決める。既定の`context-isolated`はimplementerと別session/context、exact HEAD固定、対象差分の非変更、肯定・敵対reviewとfinding記録を要求し、同一GitHub actorでも成立する。このときtracked artifactの`approved`と保存済みreview session・Step 10 bindingがformal approvalになる。`actor-independent`はPR authorおよび観測済み`H_impl` commit authorと別のstable actor IDによるprovider `APPROVED`を要求する。両modeともexact HEAD一致は必須とし、tracked文書へ自身のcommit SHAを書かず、**H_final後はartifactを更新しない。**

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | .agent-skill-chain/tmp/issues/20260921_021037_finalize再帰allowlist | staging digest 536b325934c07ffe484497efbf33472d5a4a9c8845990212bb0807e4287a66ef | 既存コード |
| 差分 | `c6f7b5563a3c94bcad0b163e00e7c0e459e381f5`..`9d8581a3066593cdc0ae49556605852bdb111176` | 16 path | 既存コード |
| テスト | /tmp/asc-1447-verify.log、SCN-UNIT-FINALIGN-038・039、SCN-INT-FINALIGN-014 | 2,173 passed、16 skipped、0 failed | テスト出力 |
| 仕様 | docs/specs/02_要件、06_外部インターフェース、15_要件追跡 | REQ-LC-009、AC-LC-009を更新・追跡 | 既存文書 |
| commit前candidate | .agent-skill-chain/schemas/00_利用案内.md、.agent-skill-chain/schemas/project-policy-manifest.schema.json、.agent-skill-chain/schemas/project-policy.schema.json、dist/src/domain/policy.js、dist/src/domain/worktree-removal-safety.js、docs/specs/01_システム概要/02_用語・略語.md、docs/specs/02_要件/02_プロジェクトライフサイクル要件.md、docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md、docs/specs/15_要件追跡/00_追跡表.md、docs/specs/15_要件追跡/01_変更履歴.md、src/domain/policy.ts、src/domain/worktree-removal-safety.ts、test/features/integration/finalize-ignored-artifacts.feature、test/features/unit/finalize-ignored-artifacts.feature、test/steps/finalize-ignored-artifacts-cli.steps.ts、test/steps/finalize-ignored-artifacts.steps.ts | H_impl 9d8581a3066593cdc0ae49556605852bdb111176 | Git index |
| Phase A artifact | docs/reviews/01_課題1447_レビュー.md | H_impl直後に本fileだけをcommitする | Git観測 |
| review session | .agent-skill-chain/tmp/issues/20260921_021037_finalize再帰allowlist | round 1 converged、digest d70e9070e8fbe2985ff2eb9f62b538bb1479008be312acd5c4c97cef1587f7d7 | Git観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: 独立reviewerと実装者を別contextへ分離し、sessionはH_implへ固定。自己SHAは本fileへ記録しない。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: 本fileだけをH_implの次commitへ置き、audit:checkで確認する。
- reviewerの独立性が要求水準を満たす: /root/review_1447は/rootと別contextでread-onlyの16 pathを確認し、product fileを変更しなかった。
- 既定branch追随を行った場合、取り込みがartifact commitより前にあり、`比較基点`が取り込んだ既定branch tip、`H_impl`がartifact直前の最新commitを指し、個別監査表を`比較基点..H_impl`から再生成した: 追随なし。比較基点は作業開始時main tip。

### 1.1 変更ファイル個別監査

基準SHAとの差分にある全ファイルを、生成物（`dist/`等）も含めて1ファイル1行で記録する。まとめ行、directory単位の一括承認、test成功だけの代替を認めない。`audit:check`の他の検査対象外となる生成物でも、各行へ生成元との対応確認方法と配布影響の確認方法を記録し、差分path集合と表のpath集合を一致させる。

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/schemas/00_利用案内.md` | M | 確認済み（領域: docs） | docs | 文書 | 文書。循環なし | 新構文の利用案内、AC-LC-009 | 文書。revert | pass |
| `.agent-skill-chain/schemas/project-policy-manifest.schema.json` | M | 確認済み（領域: 設定） | 設定 | 設定。値の意味は確認済み | 設定。循環なし | SCN-UNIT-FINALIGN-039、AC-LC-009 | 設定。revert | pass |
| `.agent-skill-chain/schemas/project-policy.schema.json` | M | 確認済み（領域: 設定） | 設定 | 設定。値の意味は確認済み | 設定。循環なし | SCN-UNIT-FINALIGN-039、AC-LC-009 | 設定。revert | pass |
| `dist/src/domain/policy.js` | M | 確認済み（領域: 生成物） | 生成物 | 生成物。生成元との対応を再build後のclean差分で確認 | 生成元 → 生成物 | src/domainとのbuild一致、AC-LC-009 | §8の配布物影響表とpackage filesで確認。revert | pass |
| `dist/src/domain/worktree-removal-safety.js` | M | 確認済み（領域: 生成物） | 生成物 | 生成物。生成元との対応を再build後のclean差分で確認 | 生成元 → 生成物 | src/domainとのbuild一致、AC-LC-009 | §8の配布物影響表とpackage filesで確認。revert | pass |
| `docs/specs/01_システム概要/02_用語・略語.md` | M | 確認済み（領域: docs/specs） | docs/specs | システム仕様書 | spec → src（許可された向き） | REQ-LC-009、AC-LC-009、変更履歴・追跡表 | システム仕様書。revert | pass |
| `docs/specs/02_要件/02_プロジェクトライフサイクル要件.md` | M | 確認済み（領域: docs/specs） | docs/specs | システム仕様書 | spec → src（許可された向き） | REQ-LC-009、AC-LC-009、変更履歴・追跡表 | システム仕様書。revert | pass |
| `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | M | 確認済み（領域: docs/specs） | docs/specs | システム仕様書 | spec → src（許可された向き） | REQ-LC-009、AC-LC-009、変更履歴・追跡表 | システム仕様書。revert | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | 確認済み（領域: docs/specs） | docs/specs | システム仕様書 | spec → src（許可された向き） | REQ-LC-009、AC-LC-009、変更履歴・追跡表 | システム仕様書。revert | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | 確認済み（領域: docs/specs） | docs/specs | システム仕様書 | spec → src（許可された向き） | REQ-LC-009、AC-LC-009、変更履歴・追跡表 | システム仕様書。revert | pass |
| `src/domain/policy.ts` | M | package owner | package domain | 既存のpolicy検証または削除安全判定に限定 | schema・policy→domain→CLI。循環なし | REQ-LC-009、AC-LC-009、SCN-UNIT-FINALIGN-038・039 | trusted policy、ASC管理領域を確認。revert可能 | pass |
| `src/domain/worktree-removal-safety.ts` | M | package owner | package domain | 既存のpolicy検証または削除安全判定に限定 | schema・policy→domain→CLI。循環なし | REQ-LC-009、AC-LC-009、SCN-UNIT-FINALIGN-038・039 | trusted policy、ASC管理領域を確認。revert可能 | pass |
| `test/features/integration/finalize-ignored-artifacts.feature` | M | 確認済み（領域: test） | test | test。検証内容は確認済み | test → 対象（許可された向き） | AC-LC-009、SCN-UNIT-FINALIGN-038・039またはSCN-INT-FINALIGN-014 | test。revert | pass |
| `test/features/unit/finalize-ignored-artifacts.feature` | M | 確認済み（領域: test） | test | test。検証内容は確認済み | test → 対象（許可された向き） | AC-LC-009、SCN-UNIT-FINALIGN-038・039またはSCN-INT-FINALIGN-014 | test。revert | pass |
| `test/steps/finalize-ignored-artifacts-cli.steps.ts` | M | 確認済み（領域: test） | test | test。検証内容は確認済み | test → 対象（許可された向き） | AC-LC-009、SCN-UNIT-FINALIGN-038・039またはSCN-INT-FINALIGN-014 | test。revert | pass |
| `test/steps/finalize-ignored-artifacts.steps.ts` | M | 確認済み（領域: test） | test | test。検証内容は確認済み | test → 対象（許可された向き） | AC-LC-009、SCN-UNIT-FINALIGN-038・039またはSCN-INT-FINALIGN-014 | test。revert | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: git diff --name-onlyで16 path、表も16 path。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: matcherはpackage domain、選択値はtrusted project policy、仕様とtestは証拠に限定。
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: 指摘なし。

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

発見IDは実装計画（fullは03、quick/pocは集約00）の`DISC-*`と同じ字面を使い、本文書内で別IDへ言い換えない。

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-1447-01 | ASC staging記録の削除は専用commandが所有する | INV-01とASC管理境界 | なし | 再帰patternの照合から.agent-skill-chain/を除外 | SCN-UNIT-FINALIGN-038、独立review | updated | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-LC-009 | SCN-UNIT-FINALIGN-038・039、SCN-INT-FINALIGN-014 | schema、worktree-removal-safety.ts | 対象unit 39・integration 14成功 | pass | test/features、/tmp/asc-1447-verify.log |

### 2.2 開発考慮事項の適用判定（必須）

00の判定・理由・証拠から差分が無ければ、表の代わりに`開発考慮事項の適用判定は00_要求定義.md §6.1と同じ`の1行を置ける（01〜03と同じ参照行）。差分がある行だけを表に残してよい。**`review validate`はこの§2.2の内容を検証しない**（01〜03の`issue validate`と異なり、review artifactのDC判定に対する機械検証は無い）。記述量を減らすための人・エージェント向けの案内であり、参照行を置いても4行の表を書いても合否は変わらない。

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
開発考慮事項の適用判定は00_要求定義.md §6.1と同じ

## 3. 肯定的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ（要件と観測結果の一致） | pass | AC-LC-009の3 SCNが成功。schema/runtime受理集合の代表例に不一致なし。 |
| 価値（利用者・運用上の目的） | pass | subpackageのcacheと拡張子生成物をtrusted policyで宣言できる。 |
| 実現可能性（環境・依存・権限） | pass | 外部依存追加なし。Gitのignored file列挙と隔離worktree削除を確認。 |
| 整合性（設計・コード・テスト・仕様） | pass | 両schema、runtime、dist、REQ-LC-009、AC-LC-009、SCNを照合。 |
| 保守性（責務・命名・変更容易性） | pass | matcherとvalidatorを既存のdomain関数内へ限定した。 |

## 4. 敵対的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 反例（要件を破る入力・状態） | pass | 不正globと誤誘導pathをSCN-UNIT-FINALIGN-038・039で拒否。 |
| 失敗経路（外部失敗・部分失敗） | pass | 不明なGit状態と未追跡の非許可資産は従来どおり拒否。 |
| 境界値（空、最大、最小、重複、Unicode） | pass | 空値、制御文字、NFC、重複とschema/runtimeの一致を確認。 |
| 悪用（注入、経路脱出、権限外） | pass | traversal、meta、.git、symlinkを独立reviewerが確認。 |
| 安全性（認証、承認、秘密情報、Zero Trust） | pass | trusted policy経路を維持し、candidateの自己緩和を許さない。 |
| データ損失（上書き、削除、部分公開、履歴消失） | pass | ASC管理領域を再帰指定から除外し、symlink外部targetが保持されることを隔離fixtureで確認。 |
| ロールバック（復旧参照、状態保持、再開可能性） | pass | 変更commitをrevert可能。削除applyは実作業treeで行っていない。 |
| 範囲漏れ（呼び出し元、利用側、配布物、文書） | pass | survey/finalize共通判定、package配布のschema・dist・src、仕様を確認。 |

## 5. 指摘

指摘なしの場合は「指摘なし」と明記する。

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| 指摘なし | - | 独立review round 1でfinding 0件 | review session digest d70e9070e8fbe2985ff2eb9f62b538bb1479008be312acd5c4c97cef1587f7d7 | 16 path | - | resolved | なし |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: 肯定・敵対、仕様、16 path、Git実挙動を確認。
- 指摘を確定した: 0件。
- 次ラウンド対象のCritical/High: なし。

### ラウンド2

- 未解決Critical/High: なし。
- 修正差分と、触れた隣接範囲: なし。
- 既承認・未変更範囲を再走査していない: round 2未実施。

### ラウンド3

- 全指摘の最終分類: round 1で指摘なし。
- 危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: ASC管理領域を再帰patternの照合対象から除外。
- 同じ範囲の予算を自動更新していない: round 1で収束。

## 7. テスト結果

- 実行したcommandの一覧: npm run verify:distribution（audit:check前まで）、対象unit/integration、lint、typecheck、format、trace、docs format、source quality、package、build、独立reviewerのGit fixtureとgit diff --check。
- 全layerの合計: 2,189 scenarios（2,173 passed、16 skipped、0 failed）、conformance 87 passed。audit:checkはreview artifact commit後に再実行する。
- runner・Gherkin方言: Cucumber.js、feature/stepsは既存project choiceに準拠。

## 8. 配布物影響

packageとして配布する場合だけ記入する。配布境界はpackage manifestの配布file指定を正本とし、compileされて配布される`source`も含める。

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| .agent-skill-chain/schemas/00_利用案内.md | 入る | 配布対象としてschema・source・distを更新しpackで確認 |
| .agent-skill-chain/schemas/project-policy-manifest.schema.json | 入る | 配布対象としてschema・source・distを更新しpackで確認 |
| .agent-skill-chain/schemas/project-policy.schema.json | 入る | 配布対象としてschema・source・distを更新しpackで確認 |
| dist/src/ | 入る | 配布対象としてschema・source・distを更新しpackで確認 |
| docs/specs/01_システム概要/02_用語・略語.md | 入らない | なし |
| docs/specs/02_要件/02_プロジェクトライフサイクル要件.md | 入らない | なし |
| docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md | 入らない | なし |
| docs/specs/15_要件追跡/00_追跡表.md | 入らない | なし |
| docs/specs/15_要件追跡/01_変更履歴.md | 入らない | なし |
| src/domain/policy.ts | 入る | 配布対象としてschema・source・distを更新しpackで確認 |
| src/domain/worktree-removal-safety.ts | 入る | 配布対象としてschema・source・distを更新しpackで確認 |
| test/features/integration/finalize-ignored-artifacts.feature | 入らない | なし |
| test/features/unit/finalize-ignored-artifacts.feature | 入らない | なし |
| test/steps/finalize-ignored-artifacts-cli.steps.ts | 入らない | なし |
| test/steps/finalize-ignored-artifacts.steps.ts | 入らない | なし |

判断: 配布物を更新した
根拠: 両project-policy schema、src/domain、dist/src/domainを更新し、package filesで配布対象を確認した。

## 9. 独立reviewの成立

PR作成前に観測できるものだけを書く。immutable review IDやapproval件数は書かない。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated |
| その要求を満たすこと | はい |
| reviewerとimplementerのidentity・context比較 | implementer=/root、reviewer=/root/review_1447、別agent context。H_impl 9d8581a3066593cdc0ae49556605852bdb111176で固定。 |
| reviewerが対象差分を変更していないこと | はい。16対象pathに変更なし。coordinatorの未追跡review artifactのみ。 |

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: 用語、ライフサイクル要件、CLI契約、追跡表、変更履歴。
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: TERM-ASC-065の変更を用語集とIssue stagingで照合。
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: 変更語は既存allowlistの範囲拡張として明記。
- 要件・変更・SCN・テストの追跡: REQ-LC-009→AC-LC-009→SCN-UNIT-FINALIGN-038・039、SCN-INT-FINALIGN-014→test/features。
- `no-spec-impact`の場合の限定的根拠: 非該当。仕様を更新した。
- UI・トークンの判断: CLI domain変更で画面・layout・tokenに影響なし。

## 11. 総合判定と再開地点

- 未解決Critical/High: なし。
- Medium/Lowの記録: なし。
- 判定: approved
- 新しい権限が必要な事項: PR作成にはrepository write authorityを使用。merge・finalizeは別操作。
- 残存リスク: trusted policy ownerが広すぎる再帰指定を選ぶ可能性。構文を限定し、ASC管理領域を保護する。
- 次に許可される操作: review artifact単独commit、Step 10記録、PR作成。
- 次回の再開地点: PR作成後のread-backとdelivery state。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 1 |
| 対象SHA・文書ダイジェスト | 9d8581a3066593cdc0ae49556605852bdb111176 |
| 比較基点 | `c6f7b5563a3c94bcad0b163e00e7c0e459e381f5` |
| H_impl | `9d8581a3066593cdc0ae49556605852bdb111176` |
| 対象差分 | .agent-skill-chain/schemas/00_利用案内.md、.agent-skill-chain/schemas/project-policy-manifest.schema.json、.agent-skill-chain/schemas/project-policy.schema.json、dist/src/domain/policy.js、dist/src/domain/worktree-removal-safety.js、docs/specs/01_システム概要/02_用語・略語.md、docs/specs/02_要件/02_プロジェクトライフサイクル要件.md、docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md、docs/specs/15_要件追跡/00_追跡表.md、docs/specs/15_要件追跡/01_変更履歴.md、src/domain/policy.ts、src/domain/worktree-removal-safety.ts、test/features/integration/finalize-ignored-artifacts.feature、test/features/unit/finalize-ignored-artifacts.feature、test/steps/finalize-ignored-artifacts-cli.steps.ts、test/steps/finalize-ignored-artifacts.steps.ts |
| 対象外 | 比較基点に存在し変更されていない範囲 |
| 残り予算 | 3ラウンド |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260921_021037_finalize再帰allowlist |
| 仕様の所有箇所 | docs/specs/02_要件/02_プロジェクトライフサイクル要件.mdのREQ-LC-009、docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md |
| 成果物行数 | git show --stat: 16 files、201 insertions、10 deletions。 |
| 縮小の先行評価 | 固定prefixの追加では可変深さ・拡張子別生成物を表せない。任意globより限定構文へ縮小した。 |
| 実施者・日時 | reviewer=/root/review_1447、coordinator=/root、2026-09-20T17:30:00Z |

`比較基点`と`H_impl`の値は40桁の小文字hexをbacktickで囲んだものだけにする。注記・branch名・短縮SHAを同じcellへ書かない。由来は別行へ書く。

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding 0件 | advanced | Codex別context | 独立contextでH_impl固定 | blocker時は停止・修正round | /root/review_1447と/rootは別context、16 path不変 |
