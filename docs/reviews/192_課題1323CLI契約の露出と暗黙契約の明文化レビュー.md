# 04 レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1323（#1325の項目1・2と#1322の文書部分を同じPRで扱う） |
| ラウンド | Step 10 ラウンド1〜4 |
| 比較基点 | `5368d4d04b4ca43da9bfc037b4d3e63d5ba7a384` |
| H_impl | `97b16bef539740b6ec314dfa8e949678f3f76e24` |
| 比較基点の由来 | PR #1327のmerge commitである`origin/main`のtip。実装途中でこれをbranchへmergeして追随した（merge commitは`比較基点..H_impl`の一直線上にあり、artifact commitより前にある） |
| Step 10のreview session ID | `00fde00ecbf772f76ae27d2717a70e28975bbdfcda486be384d7209e50c6e8cd` |
| モード | full |
| 対象差分 | `src/adapters/review-session.ts`、`src/cli.ts`、`src/cli-usage.ts`、`src/domain/finalize.ts`、`dist/src/`の生成物4件、`test/features/unit/review-round-init.feature`、`test/steps/review-round-init.steps.ts`、配布template 04、規範文書01、step-09 skill、`docs/specs/`5件 |
| 対象外 | `--root`の追加（`pr create`/`pr merge`のstaging配置要求により複製が再発する）、`review artifact --init`と`issue create`の転記（#1325残件）、`worktree create --staging`（#1322残件）、拒否条件の変更 |
| 残り予算 | 0（同一範囲で最大3ラウンド。ラウンド1で独立reviewerのHigh 1件・Medium 2件・Low 1件と自己適用の発見1件を受け、ラウンド2で是正差分を再reviewして収束した。ラウンド3は本artifactのcommitによるHEAD移動に対する取り直しで製品差分を変えない。ラウンド4は`pr create`が要件ID`REQ-WF-009a`を文法違反として拒否したための是正（IDを`REQ-WF-014`へ改名）に対する取り直しで、収束後の別枠を使った。通算4ラウンドで上限） |
| ラウンド数 | 4（ラウンド3は本artifact 1 fileのcommitに対する取り直し、ラウンド4は要件ID改名の取り直し） |
| Step chain | 経由: /home/tatsuru/Projects/techbeansjp-free/AGENTS.md/.worktrees/20260911_092957-1323-cli-contract-exposure/.agent-skill-chain/tmp/issues/20260911_093112_CLI契約をhelpと診断と雛形へ露出し-templateと規範文書の暗黙契約を明文化する |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md` REQ-WF-009「`--help`は必須flag検証より先に評価してusageを返す」。着手時点で入力fileの契約・診断の再開手順・review artifactの識別行の書式は規定されておらず、本PRでREQ-WF-009の追記とREQ-WF-014の新設で規定した |
| 成果物行数 | 製品 +378 / −18行（`src/`4 file。`review-session.ts`の雛形生成が約130行、`cli.ts`の`--init`分岐が約80行、`cli-usage.ts`のinputContractとrequiredWhenが約150行、`finalize.ts`は1行）。test +703行（feature 15 scenario、steps）。配布template・規範文書・skill +11 / −1行。仕様 +19 / −3行。支援層（staging 00〜03と本artifact）は約950行 |
| 縮小の先行評価 | 3案を先に評価した。(1) `review round`・`workflow record`へ`--root`を足す案は`pr create`/`pr merge`がstagingのroot配下配置を要求するため複製が再発し不採用（codex/fable諮問）。(2) 雛形をstdoutへ出す案はnotesと混ざり`--file`へそのまま渡せず不採用。(3) 雛形がfindingsを埋める案は前round blockerの再評価が人の判断であり不採用。採用した`--init`は既存のpreview経路（`observeReviewDiff`・`deriveEffectiveHead`・`readStoredReviewSession`）を再利用し判定を複製しない |
| 実施者・日時 | reviewer（codex、別process）とcoordinator（Claude Code）、2026-09-11 |

### 0.1 routing入力契約

providerとmodel設定はproject choiceとrouting evidenceの観測値を用い、固有のmodel slugだけからreview authorityを推測しない。

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節と`review-session.json`） | standard | codex（`codex exec --sandbox read-only`） | provider既定（`--model`を固定しない） | Critical/High未解決なら停止 | implementer（Claude Code session）とreviewer（codex別process、read-only sandbox）は別context。reviewerの変更path集合は空 |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | https://github.com/techbeansjp-free/AGENTS.md/issues/1323 、AC-01〜AC-06、INV-01〜INV-05 | Step 4で00・01を、Step 8で00〜03を同期し`sync-verified` | 耐久トラッカー |
| 差分 | `5368d4d0..97b16bef` | 20 file（`dist/`4件と本artifactを含む）。製品差分は`src/`4 file、+378 / −18行 | 既存コード |
| テスト | `npm test`、`npm run conformance:check` | 7節 | テスト出力 |
| 仕様 | `docs/specs/`5 file | updated | 既存文書 |
| commit前candidate | 19 file（個別監査15行と`dist/`生成物4件） | working tree clean | Git index |
| Phase A artifact | `docs/reviews/192_課題1323CLI契約の露出と暗黙契約の明文化レビュー.md` | `H_impl` = `97b16bef`。`H_impl..H_final`の差分pathは本file 1件 | Git観測 |
| commit後external | PR、CI run、review | Step 11で観測する。本節はPR作成前に書いており外部証拠はまだ無い | 外部のimmutable証拠 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: 成立する。02 §2.3の`cli-review-round-init → review-draft → review-diff、review-session-store`は一方向で、`architecture:check`が循環なしを観測した。雛形は`candidateHeadSha`を`--head`の実commitから取り、artifact自身のSHAを書かない
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_impl` = `97b16bef`は`H_final`の直接の親であり、差分pathは本artifact 1件
- reviewerの独立性が`merge.reviewIndependence`の要求水準を満たす: 満たす。未宣言の既定`context-isolated`で、reviewerはcodexの別process
- 既定branch追随を行った場合: 行った。`origin/main`のtip `5368d4d0`（PR #1327のmerge）を実装commitの後、本artifact commitの前にmergeした。`比較基点`はその取り込んだtipで、`H_impl`はartifact直前の最新commit（要件ID改名commit）である。個別監査表は`比較基点..H_impl`から再生成した

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/adapters/review-session.ts` | M | package | package | `buildReviewRoundDraft`（雛形の組み立て）と`STAGING_DIGEST_RERECORD_HINT`（診断の案内文）。previewと同じ読み取り関数を合成し判定を複製しない | domain（review-convergence、evidence-reanchor）への既存依存のみ | REQ-WF-014、AC-01〜AC-03・AC-05、SCN-UNIT-REVINIT-001〜010、DIAGHINT-001 | 書き込み無し。差し戻しで復旧 | pass |
| `src/cli.ts` | M | package | package | `review round --init`のflag解釈・`--out`の包含と既存検査・`wx`書き込み。`pr merge`直前再検証の診断へ案内文 | cli → adapters | AC-03・AC-05、SCN-UNIT-REVINIT-004〜010、DIAGHINT-002 | `--out`はrealpathで包含判定し`wx`で作成 | pass |
| `src/cli-usage.ts` | M | package | package | `inputContract`と`ConditionalUsageFlag.requiredWhen`の型追加、`review round`・`pr create`のusage定義、`missingRequiredFlags`の条件付きflag対応 | なし | REQ-WF-009、AC-04、SCN-UNIT-CLIHELP-010、REVINIT-010 | 既存fieldは不変 | pass |
| `src/domain/finalize.ts` | M | package | package | cleanup-apply拒否のrecovery 1行追加。判定は変えない | なし | AC-05、SCN-UNIT-DIAGHINT-003 | なし | pass |
| `test/features/unit/review-round-init.feature` | A | package | package | 新規Feature 1 file 15 scenario | steps 1 fileへ | AC-01〜AC-06 | fixtureは一時repository | pass |
| `test/steps/review-round-init.steps.ts` | A | package | package | 上記featureの専用steps。実git repositoryとstagingを組み合わせ、CLIの`main`を同一processで呼ぶ | src/cli、src/adapters | 同上 | 同上 | pass |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | M | package | package | §0へ`H_impl`行と`比較基点`の値形式、2行の厳密書式の注記 | なし | FR-07、AC-06、SCN-UNIT-DOCROW-001 | `skills:check`合格 | pass |
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | M | package | package | Q-08の判定例表4行。質問文本体は不変 | なし | FR-08、AC-06、`workflow:check` | 合格 | pass |
| `.agent-skill-chain/skills/step-09-implement/SKILL.md` | M | package | package | stagingをworktree内へ置く手順の段落 | なし | FR-09、AC-06 | `skills:check`合格 | pass |
| `docs/specs/01_システム概要/02_用語・略語.md` | M | spec | spec | TERM-ASC-100を追加（main mergeでTERM-ASC-099の直後へ整列） | なし | TERM-ASC-100 | なし | pass |
| `docs/specs/02_要件/00_要件一覧.md` | M | spec | spec | REQ-WF-014の行 | なし | REQ-WF-014 | なし | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | spec | spec | REQ-WF-009へinputContract・診断・一括診断の追記、REQ-WF-014の新設 | なし | REQ-WF-009、REQ-WF-014 | なし | pass |
| `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | M | spec | spec | review round行と`--help`評価順序への追記 | なし | AC-04 | なし | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | spec | spec | REQ-WF-009/009aのunit行を1行追加 | なし | SCN 15件 | なし | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | spec | spec | header直後へ1行（main mergeで#1327の行と並ぶ） | なし | 同上 | なし | pass |
| `docs/reviews/192_課題1323CLI契約の露出と暗黙契約の明文化レビュー.md` | A | package | evidence | 本artifact。ラウンド4の是正commitがラウンド3のartifact commitより後にあるため`比較基点..H_impl`に含まれる | なし | 本artifact全節 | なし | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: 一致する（`git diff --name-only 5368d4d0 97b16bef`の20件のうち`dist/`の生成物4件を除く16件と上表16行。本artifactはラウンド3で先にcommitされているため`比較基点..H_impl`に含まれ、表にも1行置く。生成物は8節の配布物影響で`dist/`1件として扱う）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: していない
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: ラウンド2は`review-session.ts`・`cli.ts`・`cli-usage.ts`・test 2 file・`docs/specs/`3 fileだけを再監査した

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | `check_cli_usage.ts`は`flags.<name>`の直接参照だけを「実装が読んだflag」と認識する | 実装の書き方 | なし | 直接参照へ書き換え | `cli:check` valid | なし | pass |
| DISC-002 | ラウンド1の独立reviewでHigh 1件・Medium 2件・Low 1件（5節） | INV-01・INV-04の成立条件 | なし | 全件是正しSCN 4件を追加 | 変異M11〜M14 kill、7節 | REQ-WF-009・009a | pass |
| DISC-003 | 自Issueのround 1を`--init`で作ったところ、anchorのID列が「重複なしの昇順」でなければならない暗黙契約に当たった | INV-01 | なし | 雛形側で正規化しinputContractへ明記 | SCN-UNIT-REVINIT-001の入力を非昇順・重複ありへ | review round契約 | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-REVINIT-001、002 | `buildReviewRoundDraft`、`review round --init` | 2/2合格 | pass | 7節。本Issueのround 1・2の入力自体を`--init`で生成し、そのまま`review round --apply`が受理した（自己適用） |
| AC-02 | SCN-UNIT-REVINIT-003 | 同上（session有り） | 1/1合格 | pass | 7節。本Issueのround 2は`--init`の出力に再評価findingsを足しただけで受理された |
| AC-03 | SCN-UNIT-REVINIT-004〜010 | `cli.ts`の`--init`分岐、`review-session.ts`のerror化、`cli-usage.ts`の`requiredWhen` | 7/7合格 | pass | 7節 |
| AC-04 | SCN-UNIT-CLIHELP-010 | `inputContract` | 1/1合格 | pass | 7節 |
| AC-05 | SCN-UNIT-DIAGHINT-001〜003 | 3箇所の診断 | 3/3合格 | pass | 7節。判定不変は既存SCN（REVIEWCONV、FINALIZE系）の全件合格で確認 |
| AC-06 | SCN-UNIT-DOCROW-001 | template・規範文書・skill | 1/1合格 | pass | 7節 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | 副作用を`--out`の新規file 1件に限定し、包含判定を親directoryのrealpathで行い、stagingとsessionを書かない | INV-02・INV-04、SCN-UNIT-REVINIT-002・004・005・007 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 拒否診断に次の1手を含める。判定は変えない。ログの永続化は無い | SCN-UNIT-DIAGHINT-001〜003 |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 本CLIはGUIを持たずJSON出力のみである | project choiceのcapability選択 |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | 画面・themeを持たない | project choiceのcapability選択 |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 15 SCN合格。本Issueのround 1・2の入力を`--init`が生成し、`review round --apply`がそのまま受理した |
| 価値 | 利用者・運用上の目的を満たすか | pass | 利用側が8回の失敗で発見した契約が、`--help`の`inputContract`と`--init`の雛形で1回で得られる |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | runtime依存を足していない。保護fileに触れていない |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 02 §2.2の部品4点が実装と一致。REQ-WF-009/009aを成立状態へ更新した |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 雛形はpreview経路の関数を再利用し判定を複製しない。禁止語`skeleton`は`draft`へ改めた（REV-04） |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | finding（REV-02、resolved） | HEADを進めていないsessionへの`--init`がpreviewで拒否される雛形を書いた。errorへ変えSCN-UNIT-REVINIT-008・009で固定 |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | `--out`の親が実在しない、SHAが解決できない、sessionが読めない、はいずれもerrorで止まり何も書かない |
| 境界値 | 空、最大、最小、重複、Unicode等 | finding（DISC-003、resolved） | anchorのID列の重複・非昇順が拒否される暗黙契約。雛形側で正規化 |
| 悪用 | 注入、経路脱出、権限外操作等 | finding（REV-01、resolved） | stagingを指すdirectory symlink配下を`--out`にするとstaging内へ書けた。親directoryのrealpathで包含判定へ変えSCN-UNIT-REVINIT-007で固定 |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | 判定・認可条件を1つも変えない。書き込みは`wx`の新規file 1件 |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | 既存fileは上書きしない（SCN-UNIT-REVINIT-005）。stagingとsessionを書かない（002） |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | 雛形は再生成できる。差し戻しで復旧 |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | finding（REV-03、resolved） | conditional化した`--file`が不足flagの一括診断から落ちた。`requiredWhen`で一括診断へ戻しSCN-UNIT-REVINIT-010で固定 |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| REV-01 | High | `--out`の包含判定が字句上のprefixだけで、stagingを指すsymlink配下を指定するとstaging内へ書ける | reviewerが`/proc/self/fd`とdirectory symlinkで再現 | INV-02・INV-04 | 親directoryのrealpathで判定。SCN-UNIT-REVINIT-007 | resolved / invariant-violation | なし |
| REV-02 | Medium | HEAD不一致・空fixedDiffをnotesにするだけで、previewが拒否する雛形を書く | converged sessionでHEADを進めず`--init`すると`fixedDiff=[]`のround 2が出力される | INV-01 | errorで止める。SCN-UNIT-REVINIT-008・009 | resolved / invariant-violation | なし |
| REV-03 | Medium | `--file`をconditionalへ移したため`missingRequiredFlags`が`--file`を一括診断に含めない | `review round --staging=x`が`--staging`だけを返す | REQ-WF-009 | `requiredWhen`述語を足す。SCN-UNIT-REVINIT-010 | resolved / fix-regression | なし |
| REV-04 | Low | 用語台帳が禁止する`skeleton`を関数名・変数名・testで使用 | `buildReviewRoundSkeleton` | 用語 | `buildReviewRoundDraft`へ改名 | resolved / improvement | なし |
| DISC-02 | Medium | `--init`がanchorのID列を与えられた順のまま書き、「重複なしの昇順」の契約で拒否される | 自Issueのround 1生成で再現 | INV-01 | 正規化とinputContractへの明記 | resolved / acceptance-violation | なし |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: 確認した。対象はH_impl相当のmerge commit `107f0db2`の全差分（`review-session.json` round 1、digest `b5e6c00f`）
- 指摘を確定した: REV-01〜REV-04、DISC-02
- 次ラウンド対象のCritical/High: REV-01

### ラウンド2

- 未解決Critical/High: 0件。REV-01はresolved
- 修正差分: `src/adapters/review-session.ts`、`src/cli.ts`、`src/cli-usage.ts`、test 2 file、`docs/specs/`3 file（`review-session.json` round 2の`fixedDiff` 11件と一致。round 2の入力自体を`--init`で生成した）
- 修正で触れた隣接範囲: `dist/`の生成物
- 既承認・未変更範囲を再走査していない: `finalize.ts`と配布文書はラウンド1のまま

### ラウンド3

- 全指摘の最終分類: すべてresolved。新規指摘なし
- 任意の危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: 対象差分は本artifact 1 fileのみ
- 同じ範囲の予算を自動更新していない: していない
- AIによる最終裁定: approved

### ラウンド4（要件ID改名の取り直し）

- 対象: `pr create`がevidenceの要件ID`REQ-WF-009a`を`^[A-Z][A-Z0-9-]*-\d+$`違反として拒否した。`trace:check`は受理していたため、仕様書5 fileと本artifactでIDを`REQ-WF-014`へ改名した
- 修正差分: `docs/specs/`5 fileと本artifact。製品差分なし
- 検証: `trace:check`・`docs:format`合格
- 予算: 収束後の取り直し1ラウンドの別枠を使い、通算4ラウンドで上限

## 7. テスト結果

実行runnerは`cucumber-js`（`node --import tsx`経由）、`projectChoices.gherkinDialect`は`en`、test layerはunit・integration・e2eの3層である。

- 実行したcommand: `npm test`、`npm run conformance:check`、`npm run lint`、`npm run typecheck`、`npm run format:check`、`npm run test:format`、`npm run docs:format`、`npm run trace:check`、`npm run workflow:check`、`npm run skills:check`、`npm run cli:check`
- 全layer合計: 1793 scenario（合格1777、skip 16、失敗0）。失敗0件（ラウンド2是正後の実測）
- `conformance:check`: 87 scenario（合格87）
- 静的gate 9本: すべてexit 0
- 変異試験: 14件（M1〜M10はラウンド1前、M11〜M14はラウンド2是正の差し戻し）。生存0件
- 自己適用: 本Issueのround 1・2の入力を`review round --init`で生成し、`review round --apply`がそのまま受理した。round 1生成時にDISC-003（ID列の順序）を検出した

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/adapters/review-session.ts` | 入る（`package.json`の`files`が`dist/src/`を列挙する） | `review round --init`の雛形生成と、digest不一致診断の案内文 |
| `src/cli.ts` | 入る | `review round --init`の受理、`pr merge`直前再検証の診断の案内文 |
| `src/cli-usage.ts` | 入る | `--help`の`inputContract`、条件付きflagの一括診断 |
| `src/domain/finalize.ts` | 入る | cleanup-apply拒否のrecoveryに`--approved-digest`の案内 |
| `dist/` | 入る | 上記4 fileのcompile生成物 |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | 入る | §0の`H_impl`行と厳密書式の注記 |
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | 入る | Q-08の判定例表 |
| `.agent-skill-chain/skills/step-09-implement/SKILL.md` | 入る | stagingをworktree内へ置く手順 |
| `test/`、`docs/specs/` | 入らない | `package.json`の`files`が列挙しない |

判断: 配布物を更新した

根拠: `dist/src/`の4 fileに新subcommand分岐・雛形生成・診断文言・help fieldが入り、配布template・規範文書・skillの本文が変わる。既存flagの判定・出力field・拒否条件は変えていない。

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated（`merge.reviewIndependence`未宣言の既定） |
| その要求を満たすこと | はい |
| reviewerとimplementerのidentity・context比較 | implementerはClaude Code session（commit author `tatsuru`）、reviewerは`codex exec --sandbox read-only`の別process。同一GitHub actorだが別session/contextである |
| reviewerが対象差分を変更していないこと | はい（read-only sandboxで実行し、review前後で`git status`に差分なし） |

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `02_要件/00_要件一覧.md`（REQ-WF-014）、`02_要件/01_ワークフロー要件.md`（REQ-WF-009・009a）、`06_外部インターフェース/01_コマンド・GitHub契約.md`、`15_要件追跡/00_追跡表.md`、`15_要件追跡/01_変更履歴.md`、`01_システム概要/02_用語・略語.md`
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: 00 §4.2 TERM-001（候補）→ 01 §2.1 TERM-ASC-100（確定）→ 用語台帳 TERM-ASC-100（active）
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: ない。禁止語`skeleton`は除いた（REV-04）
- 要件・変更・SCN・テストの追跡: REQ-WF-009・009a → AC-WF-009・009a → SCN 15件 → `review-round-init.feature`（`trace:check`合格）
- `no-spec-impact`の場合の限定的根拠: 該当なし
- UI・トークンの判断: UI無し

## 11. 総合判定と再開地点

- 未解決Critical/High: 0件
- Medium/Lowの記録: REV-02・REV-03・DISC-02（Medium）、REV-04（Low）はいずれもresolved
- 判定: approved
- 新しい権限が必要な事項: なし
- 残存リスク: `requiredWhen`はround 1でだけ必要な`--base`・`--scope`・`--ac`をsession有無で判定できないため、それらの欠落はhandlerが個別に報告する（1回の診断にはならない）
- 次に許可される操作: `workflow record --step=10`、`pr create`
- 次回の再開地点: Step 11
