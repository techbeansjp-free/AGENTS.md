# 137 課題1194の外部review取り込み経路レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1194 |
| ラウンド | Step 10 ラウンド1 |
| 比較基点 | `0c485f66cf5a4d74c07c8f19b9fd4e75ec82a598` |
| H_impl | `cedc9e4d2a4d8d54852fd3ed0aabe3c3f11d7dec` |
| 比較基点の由来 | worktree作成時点の`origin/main`のtip |
| モード | full（Q-01・Q-02・Q-03がfalse） |
| 対象差分 | 9 file、+214 -8。commitは`cedc9e4d` |
| 対象外 | `validateReleaseWorkflow`が字面だけを見る問題（#1191 F-02）。CIの二重test実行（#1178）。delivery stateのhead束縛の前進（本Issueでは扱わない。**`pr merge`は`merge.mode=disabled`のため実行経路に無い**） |
| 残り予算 | **2**（同一範囲で最大3ラウンド、収束後にHEADが動いたときの取り直しを1回まで） |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260904_055139_pr-create後の外部review指摘を条件付きで同じPRへ取り込む |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-004・REQ-WF-005（**両方とも改訂**） |
| 成果物行数 | 製品 **+70 -4行**（`workflow.ts` +39 -3、`cli.ts` +22、`cli-usage.ts` +9 -1）。規範文書 **+13 -1行**。仕様 **+4 -3行**。支援層 **+127行**（feature +30、steps +97）。**支援層/成果物 = 1.8倍** |
| 縮小の先行評価 | **着手前に実験して変更対象を4箇所から2箇所へ減らした。** `audit:check`が4 commit構造（実装→artifact→是正→artifact更新）を受理するかを実worktreeで確かめ、**受理することを確認した**（DISC-401）。唯一のエラーはartifactの`H_impl`行が古いことだけだった。**`check_file_audit.ts`を1行も変更していない。** また`readWorkflowJournal`の消費者を全数確認し、**CIの何もjournalを読まない**ことを確かめた（DISC-402）。塞いでいたのはgateではなく記録の真正性だった |
| 決裁 | repository ownerの明示決裁。**工程縮小の許可を含む** |
| 実施者・日時 | reviewer（claude）、2026-09-04 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節） | advanced | claude | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerとreviewerが**同一session**である。0.2節に逸脱として開示する |

### 0.2 開示する逸脱

1. **implementerとreviewerが同一sessionである。** 緩和は、判定の根拠を実験と機械観測に置いたこと、**codexへ諮問して「削るべきは検証ではなく再演である」という切り分けを得たこと**、変異試験が私のtest設計の不足を1件検出したことである。
2. **本PRはowner決裁により工程を縮小している。** Step 0〜8をStep 9の直前へまとめて記録した。**縮小の対象は実施回数と所要時間であり、安全条件・authority分離・fail-closed不変条件を含まない。** 変異試験、静的検査10件、`conformance:check`、`audit:check`はすべて実行している。
3. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 実害 | PR #1177・#1189・#1192・#1195 | 外部review指摘によるPRの作り直しが**4回**。**1回あたり実測で概ね1時間** | 実行記録 |
| 矛盾 | `01_開発ワークフロー.md` | 禁止の理由が「journalが封じられるから」であり、同じ文書の3段落下に「HEADが変われば次roundで再reviewする」と書いてある | 一次資料 |
| 実験 | 実worktreeで4 commit構造を作成 | `audit:check`が**受理した**。errorはartifactの`H_impl`行だけ | 実行記録 |
| 実験 | `grep -rn "readWorkflowJournal"` | 消費者は`src/cli.ts`だけ。**CIの何も読まない** | 一次資料 |
| 諮問（codex） | Issue #1194 | 「削るべきは検証ではなく再演である」「同一PRで修正・exact-head再検証すれば得られる保証との差は示されていない」 | 外部の判断 |
| テスト | `npm run conformance:check` | `1472 scenarios (1456 passed, 16 skipped)` | テスト出力 |
| commit前candidate | 9 file | working tree clean | Git index |
| Phase A artifact | `docs/reviews/137_課題1194の外部review取り込み経路レビュー.md` | `H_impl` = `cedc9e4d`。`H_impl..H_final`の差分pathは本file 1件 | Git観測 |
| commit後external | PR、CI run、review | Step 11で観測する | 外部のimmutable証拠 |

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/workflow.ts` | M | package | package | `postTerminalIntake`のtype・field許可・parse・順序判定からの除外・Step 11より後という条件 | pass。新しいimportを増やしていない | REQ-WF-004 / AC-01〜06 / SCN-UNIT-WFJRNL-022〜027 | **既存journalは同fieldを持たないため判定は変更前と同一になる。** rollbackは本fileのrevert | pass |
| `src/cli.ts` | M | package | package | `--post-terminal-intake`の受理と前提条件の検証 | pass。cli → domainの一方向 | 同上 | Step 11未記録での指定と、Step 10以外への指定を拒否する | pass |
| `src/cli-usage.ts` | M | package | package | usage定義へoptional flagを1件足した | pass | 同上 | `cli:check`が実在を強制する | pass |
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | M | package | package | 禁止を条件付き許可へ置き換え、5条件を明記した | pass | REQ-WF-005 | **配布境界に入る。** 記述だけで実行authorityを持たない | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | project | spec | REQ-WF-004とREQ-WF-005を改訂した | pass | 同上 | 同上 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | 新規SCN 6件を結線した | pass | 同上 | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | project | spec | 変更履歴を1行足した | pass | 同上 | 同上 | pass |
| `test/features/unit/workflow-step-enforcement.feature` | M | package | package | scenario 6件を追加した。既存を1件も変えていない | pass | AC-01〜06 | fixtureは構造体であり外部へ到達しない | pass |
| `test/steps/workflow-step-enforcement.steps.ts` | M | package | package | 対応するcaseを追加した | pass | 同上 | 同上 | pass |

## 2. 受け入れ条件の確認

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-WFJRNL-022 | `validateStepJournal` | `8 scenarios (8 passed)` | pass | Step 11後のintake記録で`outOfOrder`が空になる。変異M1でkill |
| AC-02 | SCN-UNIT-WFJRNL-023 | 同上 | 同上 | pass | Step 11より前のintakeが診断つきで拒否される。変異M2でkill |
| AC-03 | SCN-UNIT-WFJRNL-024 | 同上 | 同上 | pass | intake指定のない再記録で`outOfOrder`が`[11]`になる。**この反例が除外そのものを消す変異を殺す** |
| AC-04 | SCN-UNIT-WFJRNL-025 | `parseStepJournal` | 同上 | pass | Step 9へのintake指定が拒否される。変異M3でkill |
| AC-05 | SCN-UNIT-WFJRNL-026 | 同上 | 同上 | pass | `"yes"`が拒否される。変異M4でkill |
| AC-06 | SCN-UNIT-WFJRNL-027 | 同上 | 同上 | pass | JSON往復でintakeが保持される。変異M5でkill |

### 2.2 不変条件

| INV ID | 内容 | 判定 | 証拠 |
|---|---|---|---|
| INV-01 | 守る性質を変えない | pass | `review round`の前提（前roundのcandidate HEADがancestor）を変更していない。**HEADが動けば次roundで再reviewするという既存の担保をそのまま使う** |
| INV-02 | 記録を消さない | pass | 除外するのは`lastByStep`の構築だけであり、entryはjournalに残る。`SCN-UNIT-WFJRNL-027`が往復を固定する |
| INV-03 | `audit:check`と`conformance:check`の合否条件を変えない | pass | **両fileを1行も変更していない。** 差分9 fileに含まれない |

## 3. 肯定的評価

- **着手前に実験して変更対象を半分にした。** `audit:check`の4 commit受理は**20秒の実験**で決着し、`check_file_audit.ts`を触らずに済んだ。**推論していたら4箇所を変更していた。**
- **journalの消費者を全数確認した。** CIの何も読まないという事実が、目的を「gateを緩める」ではなく「記録を正しく残せるようにする」へ絞り込んだ。
- **除外は順序判定だけに限った。** 記録は残る。
- **抜け道を両方向で塞いだ。** Step 11未記録での指定と、Step 10以外への指定を拒否する。
- **既存journalの判定を変えない。** `postTerminalIntake`を持たないentryの扱いは変更前と同一である。

## 4. 敵対的評価

| 観点 | 攻撃 | 結果 |
|---|---|---|
| 抜け道 | Step 11を経ずにintakeを指定する | 変異M2でkill。`SCN-UNIT-WFJRNL-023` |
| 抜け道 | Step 9等へintakeを指定して順序判定を外す | 変異M3でkill。`SCN-UNIT-WFJRNL-025` |
| 抜け道 | falsyな値で記録しつつfieldを存在させる | 変異M4でkill。`SCN-UNIT-WFJRNL-026` |
| 検査の無効化 | 順序判定からの除外を消す | 変異M1でkill |
| 検査の無効化 | field許可一覧から外す | 変異M5でkill（**当初生存。往復の反例を足した**） |
| 記録の消失 | intakeをjournalへ残さない | `SCN-UNIT-WFJRNL-027`が往復を固定する |
| **残る穴** | intakeを何回でも記録する | **予算はreview session側（通算4ラウンド）が持つ。** journal側は回数を数えない。5節ADV-01に記録する |

## 5. 指摘

| ID | severity | 事実 | 観測 | 由来 | 対処 | 判定 | 残余 |
|---|---|---|---|---|---|---|---|
| DISC-401 | Low | **`audit:check`は4 commit構造を既に受理する。** 推論では「変更が要る」と見ていた | 実worktreeでの実験 | 実測 | `check_file_audit.ts`を変更しない。**変更対象が4箇所から2箇所へ減った** | valid / resolved | なし |
| DISC-402 | Low | **journalをCIの何も読まない** | 消費者の全数確認 | 実測 | 目的を「記録の真正性」へ絞った | valid / resolved | なし |
| DISC-403 | Medium | **変異M5が当初生存した。** 直接構築したentryだけを検査しており、parseを通す往復が無かった | 変異試験 | 変異試験 | `SCN-UNIT-WFJRNL-027`を足した | valid / resolved | なし |
| DISC-404 | Low | **fixtureで`quick` modeのstep集合を取り違えた** | scenarioの失敗 | テスト | `requiredSteps("quick")`を実行して`[0,1,4,9,10,11]`へ直した | valid / resolved | なし |
| ADV-01 | Medium | **journal側はintakeの回数を数えない。** 予算はreview session側が持つ | 敵対評価 | 敵対評価 | **修正しない。** 予算を2箇所で持つと食い違う。が通算4ラウンドで拒否するため、intakeを何度記録してもroundが開けない | valid / record-only | journalに冗長なintake entryが残りうる |
| ADV-02 | Medium | **delivery stateの`create.headSha`は前進しない。** 取り込みでHEADが動いても更新されない | 設計 | 敵対評価 | **本Issueでは扱わない。** `pr merge`の実効HEAD再観測は`merge.mode=disabled`のため実行経路に無い。**扱わないことを明示して残す** | valid / record-only | `merge.mode`を変える場合は先に本件を解消する |

**未解決のCritical / Highは0件である。**

## 6. ラウンド固有の確認

### ラウンド1

- 対象: 実装差分の9 file。
- 確認: 個別監査9行、AC-01〜06、INV-01〜03、肯定5観点、敵対7観点、変異6件。
- 結果: blocking 0件。record-only 2件（ADV-01・02）。resolved 4件（DISC-401〜404）。

## 7. テスト結果

**表を書く前に1本ずつ実行した。**

| 層・検査 | コマンド | 件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---|---:|---:|---:|---|
| unit・integration・e2e | `npm run conformance:check`（内部で`npm test`を実行する） | 1472 | 1456 | 0 | 16 | pass |
| 静的検査 | `lint`・`format:check`・`typecheck`・`source:check`・`architecture:check`・`skills:check`・`package:check`・`docs:format`・`test:format`・`trace:check` | 10 | 10 | 0 | 0 | pass |
| 監査 | `npm run audit:check` | 1 | 1 | 0 | 0 | pass |

### 7.1 変異試験

| ID | 変異 | 結果 |
|---|---|---|
| M1 | 順序判定からの除外を消す | kill |
| M2 | Step 11より後という条件を消す | kill |
| M3 | Step 10限定を消す | kill |
| M4 | `true`限定を消す | kill |
| M5 | field許可一覧から外す | kill（**当初生存。往復の反例で塞いだ**） |

**5件すべてkill。**

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | **入る** | **本Issueの主目的である。** 利用者の工程契約が変わる |
| `src/domain/workflow.ts`・`src/cli.ts`・`src/cli-usage.ts` | **入る**（`dist/src/`） | `--post-terminal-intake`が利用者側でも使える |
| `docs/specs/` 3 file | **入る**（`docs/`） | REQ-WF-004・005の改訂が配布される |
| `test/` 2 file | 入らない | `files`が列挙しない |

判断: 配布物を更新した

根拠: 規範文書とCLIの両方が配布境界に入る。**利用者は同じ工程で同じ費用を払っていたため、本変更は利用者側の速度も改善する。** `npm run package:check`はexit 0である。

## 9. 独立reviewの成立

**reviewerはimplementerと同一である**（`adachi-tatsuru`）。緩和は次の3点である。

1. **codexへ諮問し、切り分けを得た。** 「欠陥検出と全工程を別PRでやり直すことは別である。後者が追加で欠陥を検出した証拠はない」。**この切り分けは私が持っていなかった。**
2. **判定の根拠を実験に置いた。** `audit:check`の受理も、journalの消費者も、推論ではなく実行で確かめた。
3. **変異試験がtest設計の不足を1件検出した**（DISC-403）。

## 10. 仕様整合性

- `REQ-WF-004`を改訂し、post-terminal intakeを順序判定から外すことを明記した。
- `REQ-WF-005`を改訂し、`pr create`後の取り込み条件を明記した。
- 新規SCN 6件（`SCN-UNIT-WFJRNL-022`〜`027`）を追跡表へ結線した。`trace:check`のorphanは0件である。
- **既存SCNを1件も削除していない。**

## 11. 総合判定と再開地点

**判定: 合格。** 未解決のCritical / Highは0件である。

残余はADV-01（journal側が回数を数えない）とADV-02（delivery stateのhead前進を扱わない）である。**ADV-02は`merge.mode`を`disabled`から変える場合に先に解消する必要がある。**

**本変更の効果は、次に外部reviewer指摘が届いたときに測れる。** 作り直しが発生しなければ成功である。
