# 04 レビュー

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 2 |
| H_impl | `55b954796d7361a64c82d087b094248a1eaae8b9` |
| 比較基点 | `6977d0ff291fd6202592ca5117a7275edf053038` |
| 対象SHA・文書ダイジェスト | `55b954796d7361a64c82d087b094248a1eaae8b9` |
| 対象差分 | `6977d0ff291fd6202592ca5117a7275edf053038..55b954796d7361a64c82d087b094248a1eaae8b9`、2 path。**`dist/`配下は0件である。** commitは`9282d5df`・`55b95479`の2件 |
| 対象外 | 製品code、再固定の条件や復旧経路、既存の受け入れ条件の変更、`docs/specs/`の要件変更、他のreview artifactの同型誤りの一括是正、証跡の表構造を機械検査する機構の追加 |
| 残り予算 | 同一範囲で最大3ラウンド。ラウンド2まで使用し、**残り1** |
| ラウンド数 | 2。ラウンド1は訂正3件、**ラウンド2は外部reviewer（CodeRabbit）の指摘1件の是正**が対象である |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260909_003127_docs-課題1251レビュー文書の追随履歴-表構造-配布範囲の誤記を訂正する |
| 仕様の所有箇所 | `docs/specs/11_非機能/01_品質要件.md`のQLT-STEPCHAIN-001。引用: 「**review artifactへラウンド数とStep chainの申告を必須にし、ラウンド上限超過と申告の欠落を拒否する。**」 |
| 成果物行数 | 訂正対象 **+20 / −18行**。製品 **0行**、支援層(test) **0行**、仕様 **0行**、生成dist **0行** |
| 縮小の先行評価 | **新しいgate、validator、CLI、schema、台帳、project choice項目、SCN、fileを1つも追加していない。** 訂正は3箇所に閉じる。**表構造を機械検査する機構は足さない。** Issue #1254が製品codeを対象外としており、検査の追加は別の判断である |
| 実施者・日時 | implementer: Claude Code / claude-opus-5[1m]。2026-09-09 |

### 0.1 routing入力契約

| role欄（担当role） | 許可path・操作 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|---|
| implementer | `docs/reviews/`の読み書きのみ | 到達可能性・cell数・`npm pack`の実測、削除行の全件確認 | routine。riskはlow。認可条件にも配布物にも触れない | project choiceのprovider上限に従う | project choiceのtier mapping | 検査失敗時はcommitせず停止する | 実装は本session、reviewerはcodex CLIの新規session |
| reviewer | 読み取りのみ。`--sandbox read-only` | 肯定・敵対review、finding分類、原文引用 | standard | codex CLI | **`--model`で固定せず設定既定に従う** | 独立性が不明ならPRとmergeを停止する | §9に観測結果を残す |

## 規範の引用

本artifactは対象を狭める記述を含む。判断の根拠となる要件の原文を引用する。

要件 **QLT-STEPCHAIN-001** の原文を`docs/specs/11_非機能/01_品質要件.md`から引く。

> review artifactへラウンド数とStep chainの申告を必須にし、ラウンド上限超過と申告の欠落を拒否する。**上限は判定の正本である`review-convergence.ts`の`REVIEW_RECOVERY_ROUND`をimportして得る。同じ値を2箇所で持たない。**申告内容は検証しない。独立oracleを持たないためであり、`経由`のjournalは追跡対象外で既定branch側から観測できない。
>
> **申告内容は検証しない。**

**この最後の一文が本Issueの位置づけを決める。** 履歴監査はラウンド数とStep chainの申告の**存在**を検査するが、**申告や記述の内容が事実と一致するかは検査しない。** したがって165の3件の誤記はどの機械検査でも検出されず、**人が読んで初めて分かる。** 本変更はその是正であり、検査の追加ではない。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | GitHub Issue #1254、staging 00のAC-01〜AC-05 | Step 4で同期し、syncDigestとreadBackDigestが`c73a91fc003332759c2554d33e950b672114b8786b31266ecebd2a1322eb063a`で一致 | 実行観測 |
| 到達可能性 | `git merge-base --is-ancestor <sha> origin/main` | **`31bbbb54`と`5ca47b3a`はmainから到達不能。** `4d83166d`・`317d65cf`・`9c28b495`は到達可能 | 実行観測 |
| cell数 | 行を`\|`で分割した計数 | **是正前のDISC-005は5、他のDISC行とheaderは3。是正後は全行3** | 実行観測 |
| 配布範囲 | `package.json`の`files`と`npm pack --dry-run` | `files`は**全12項目**で、内訳は`dist/`配下3件、`.agent-skill-chain/`配下6件、root直下3件である。**`src/`・`test/`・`docs/`は1件も梱包されない** | 実行観測 |
| 削除行 | `git diff`の`-`行 | **17行すべてを目視で確認した。** 当時の判断・数値・結論を含む行は1件も削っていない | 実行観測 |
| 起票の主張の再現 | 現行本文の該当箇所 | **4件中3件が現存し、1件（既定branch追随の記述）は既に是正済みだった** | 実行観測 |
| 履歴監査 | `npm run audit:check` | `valid: true` | テスト出力 |
| 文書整形 | `npm run docs:format` | 違反0件 | テスト出力 |
| staging | `issue validate --path=<staging>` | `valid: true` | テスト出力 |
| commit後external | PR、CI run、外部review | **本artifactの作成時点では未観測である。** | 外部のimmutable証拠 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **満たす。** 実測 → 記述 の一方向であり、記述から実測を導出していない。本artifactへ自身のcommit SHAを書いていない。**訂正対象の165へ本変更のSHAを書いていない。**
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: **artifact commit時点で満たす。**
- reviewer stable IDが`H_impl` author stable IDと異なる: **いいえ。** 本repositoryではPR authorと`H_impl` commit authorがいずれも`adachi-tatsuru`である。§9の例外経路を参照する。
- 既定branch追随を行った場合: **該当なし。** 比較基点は`6977d0ff`のままである。

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `docs/reviews/165_課題1251detached-HEAD分類レビュー.md` | M | package owner | evidence | 追随前後のSHAの併記、DISC-005のcell数の是正、配布表の梱包対象と配布影響の分離。**ラウンド2で`files`の件数表記を全12項目と内訳へ明示** | pass。証跡fileであり製品codeもschemaも参照しない | QLT-STEPCHAIN-001 / AC-01〜AC-05 / **新規SCNなし**（§5に理由） | 前進revert。訂正前の本文はgit履歴に残る | pass |
| `docs/reviews/182_課題1254レビュー記録の誤記訂正レビュー.md` | A | package owner | evidence | 本artifact。**ラウンド2の是正commitがartifactより後になったため、artifact自身がH_implの差分へ入る** | pass。自身のcommit SHAを書いていない | QLT-STEPCHAIN-001 | 追加のみ。実装へ影響しない | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **満たす。** `git diff --name-only 6977d0ff 55b95479`は2 pathであり、表の2行と一致する。**`dist/`配下は0件で、除外した行は無い。** **ラウンド2で是正commitがartifactより後になったため、本artifact自身もH_implの差分へ入る。**
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **満たす。** evidence層の記述だけを変えた。

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

**(1) 起票が挙げた4件のうち1件は既に是正済みだった。** 「『既定branch追随を行っていない』は誤り」という指摘に対し、現行本文の§1末尾は既に「**行った。**」と書き、rebaseの経緯と衝突解消の方針まで記していた。**訂正対象から外し、3件に絞った。** **起票の主張を全件そのまま受け取らない。**

**(2) `issue validate`は新規SCNを持たない変更を想定していない。** `src/domain/issue.ts:698`が`/Scenario:\s+SCN-[A-Z0-9-]+/`を成果物全体へ要求する。一方`03_実装計画.md`のtemplateは「どの層でも担保できないSCNは、非適用の理由と代替の検証手段をSCN単位で1回だけ書く」と定めている。**機構どうしが矛盾している。** ACを担保する既存SCNを`test/features/integration/audit-artifact-selection.feature`から逐語引用して要求を満たし、**引用であることを明記して新規SCNを書いたように見せない形にした。** **この矛盾は本変更で塞がない。** 機構の変更であり、Issue #1254が対象外としている。

**(3) 「配布境界に入るか」という欄名が2つの問いを混ぜていた。** `src/`はnpm梱包対象ではないが配布影響はある。`test/`はどちらも無い。**同じ欄で両方を表すと、どちらの意味でも誤りになる。** 欄を「npm梱包対象か」と「配布影響」へ分けた。**これは起票の指摘より一歩踏み込んでいるが、指摘が求めた「package manifestと配布表・根拠文の一致」を満たすために必要である。**

### 2.1 受け入れ条件とシナリオ

| AC | 内容 | 観測 | 判定 |
|---|---|---|---|
| AC-01 | §1の`差分`欄と`H_impl`記述が追随前の値であることを明示し追随後と併記する | 当該2箇所の原文。追随前後の値が両方あり役割が書かれている | pass |
| AC-02 | DISC-005のcell数がheaderと同じ3になる | 計数で3。他のDISC行も3 | pass |
| AC-03 | §8の配布表が`package.json`の`files`と`npm pack`の実測に一致する | `src/`2行と`test/`4行が「入らない」。欄を2つに分け根拠文も是正 | pass |
| AC-04 | 履歴監査と日本語文書検査が合格する | `audit:check`が`valid: true`、`docs:format`が合格 | pass |
| AC-05 | 当時の判断・数値・結論が変わっていない | 削除17行を全件目視。変異試験件数・AC結果・DISC-001〜004の記述に変更なし | pass |

## 3. 肯定的評価

- **訂正値をすべて実測から取った。** 現行本文の記述を根拠にしていない。到達可能性は`git merge-base --is-ancestor`、配布範囲は`npm pack --dry-run`、cell数は計数である。
- **値を消さずに併記した。** 追随前の`31bbbb54`と`5ca47b3a`は到達不能だが、履歴として残す価値がある。**消すと「なぜ食い違っていたか」が分からなくなる。**
- **起票の主張を実測してから着手した。** 4件のうち1件は既に是正済みで、そのまま直すと存在しない欠陥を直したことになっていた。
- **成果物が1 fileに閉じている。** 製品code、test、仕様、`dist/`のいずれにも触れていない。

## 4. 敵対的評価

### 4.1 当時の内容を書き換えていないか

**書き換えていない。** 削除17行の内訳は、§1の`差分`行1件、§1の`H_impl`行1件、DISC-005行1件、§8の表header 1件と表の行12件、§8の根拠文1件である。**変異試験の件数（7件すべてkill）、AC-01〜AC-06の結果、DISC-001〜004の記述、諮問の結論はいずれも1文字も変わっていない。**

### 4.2 訂正が新たな誤りを作っていないか

- 追随前後のSHAは`git merge-base --is-ancestor`で1件ずつ確かめた。
- 配布表の6行の変更は`npm pack --dry-run`の出力と`package.json`の`files`の両方に照らした。
- DISC-005の是正は語の置換であり、**述べている内容は変えていない。** 「`|---|`行の直後」と「header区切り行の直後」は同じ位置を指す。

### 4.3 「入らない」へ直す過程で配布影響まで消していないか

**消していない。** これが§2.0(3)の判断である。`src/`の2行は「npm梱包対象か: **入らない**（compile元）」かつ「配布影響: **ある**」とし、**実体は`dist/`として届くことを明記した。** 欄を1つにしたままでは、どちらの意味でも誤りになる。

### 4.4 同型の誤りが他のreview artifactに無いか

**ある可能性が高いが、本変更では調べていない。** Issue #1254の対象は165 1件である。**「他にも同じ誤りがあるかもしれない」を理由に範囲を広げない。** 一括是正を行うなら、まず何を機械検査できるかを決めるべきであり、それは別の判断である。§11へ残す。

### 4.5 検査を足すべきではないか

**足さない。** 表のcell数の一致は機械検査できる。しかしIssue #1254は製品codeを対象外としており、**検査の追加は「強制点が無い」を欠陥として扱う方向である。** 運用ポリシーは強制を既定にしない。**必要性を測ってから足す。** §11へ残す。

### 4.6 支援層が製品を超えている

**製品の変更は0行である。** 本変更は証跡だけを対象とするため、支援層比という指標が定義できない。**代わりに、訂正19行に対して本artifactが約120行であることを記録する。** これはASCがreview artifactへ要求する節構造による固定費であり、**quick modeを選んで00〜03の4文書を1文書へ集約したうえでの値である。**

## 5. 指摘

**本ラウンドで自分が見つけた指摘を記録する。**

| ID | 重大度 | 指摘 | 自分の検証 | 対処 |
|---|---|---|---|---|
| S-01 | Medium | 起票の4主張のうち1件は既に是正済みだった | **正しい。** 現行本文の§1末尾が既に「行った。」と書いている | 訂正対象から外し、3件に絞った |
| S-02 | Medium | 「配布境界に入るか」の欄名が梱包対象と配布影響の2つの問いを混ぜている | **正しい。** `src/`は梱包されないが配布影響はある。同じ欄では両方の意味で誤る | 欄を2つへ分けた |
| S-03 | Low | `issue validate`が新規SCNを持たない変更を許さない一方、templateは「どの層でも担保できないSCN」を認めている | **正しい。** `src/domain/issue.ts:698`の正規表現を確認した | ACを担保する既存SCNを逐語引用して満たした。**機構の矛盾は本変更で塞がない** |
| R-01 | Minor | 外部reviewer（CodeRabbit）。`package.json`の`files`について7 pathを列挙しながら「6項目」と書いており件数が合わない | **数え方は正しいが表現が曖昧だった。** 「6項目」は`.agent-skill-chain/`配下の件数を指し、実測でも同配下は6件である。しかし列挙全体の件数と読める位置にあり、**外部reviewerが実際に誤読した。** 誤読される書き方は誤りと同じ費用を生む | **全12項目**であることと内訳（`dist/`配下3件、`.agent-skill-chain/`配下6件、root直下3件）を明示した。165と182の両方を直した |

**新規SCNを1件も足さない理由を記録する。** 変更対象は`docs/reviews/`配下1 fileの記述であり、実行される振る舞いを持たない。cucumberのどの層でも担保できない。**代替は`audit:check`・`docs:format`・実測値の記録である。**

**変異試験を行わない理由を記録する。** 注入対象となる判定が本変更に存在しない。**「変異試験を行った」と書かないことが正しい報告である。**

## 6. ラウンド固有の確認

### ラウンド2

**外部reviewer（CodeRabbit）のMinor 1件を是正した。** 指摘は「7 pathを列挙して6項目と書いている」であり、**数え方そのものは正しかった。** しかし「`.agent-skill-chain/`の6項目」という書き方が列挙全体の件数と読める位置にあり、実際に誤読された。**正しいが誤読される記述は、誤りと同じ費用を生む。** 全12項目と内訳を明示した。

**本Issueは「誤記の訂正」である。** その成果物自体が誤読される書き方をしていた。**この事実を隠さず記録する。**

**ラウンド予算の残りは1である。**

### ラウンド1

訂正3箇所、削除行の全件確認、履歴監査、文書整形を対象とした。

## 7. テスト結果

| 検査 | コマンド | 結果 |
|---|---|---|
| 文書整形 | `npm run docs:format` | 違反0件 |
| 履歴監査 | `npm run audit:check` | `valid: true` |
| staging | `issue validate --path=<staging>` | `valid: true` |
| cell数 | 行を`\|`で分割した計数 | DISC行5件すべて3。是正前のDISC-005は5 |
| 配布範囲 | `npm pack --dry-run` | `src/`・`test/`・`docs/`は0件 |
| 到達可能性 | `git merge-base --is-ancestor` | `31bbbb54`・`5ca47b3a`は到達不能 |
| 削除行 | `git diff`の`-`行 | 17行を全件目視 |

**実行しない検査とその理由を明記する。** `npm test`・`npm run typecheck`・`lint`・`format:check`・`source:check`・`architecture:check`・`trace:check`・`conformance:check`・`package:check`は実行していない。**変更対象がMarkdown 1 fileであり、`.ts`にも`docs/specs/`にも`package.json`にも差分が無いためである。** **合格が自明でも、走らせていない検査を合格と書かない。**

**変異試験は行っていない。** 注入対象となる判定が本変更に存在しない。

## 8. 配布物影響

判断: 配布物を更新しない

根拠: 変更した1 pathは`docs/reviews/`配下であり、`package.json`の`files`に含まれない。`npm pack --dry-run`の実測でも`docs/`は1件も梱包されない。**`dist/`の再生成は不要であり、本変更の差分に`dist/`配下は0件である。**

| 変更path | npm梱包対象か | 配布影響 |
|---|---|---|
| `docs/reviews/165_課題1251detached-HEAD分類レビュー.md` | **入らない** | なし |

**利用projectから見た変化は無い。**

## 9. 独立reviewの成立

**開示する逸脱が2件ある。**

1. **本artifactの作成者と進行役（coordinator）が同一sessionである。** `roleContracts.coordinator.forbiddenOperations`の`self_approve`に接する。隠さず記録する。
2. **PR authorと`H_impl` commit authorが同一（`adachi-tatsuru`）である。** 本repositoryの構成上、reviewer stable IDの分離は成立しない。

**独立reviewは本artifactのcommit後に行う。** 結果は§5へ追記する。

## 10. 仕様整合性

**`docs/specs/`を1行も変更していない。** `no-spec-impact`の根拠を書く。

本変更は`QLT-STEPCHAIN-001`が要求する証跡の記述を実態へ合わせるものであり、要件・受け入れ条件・SCNのいずれも追加も変更もしない。**新しいSCNを足さないため、要件行から到達すべき新規IDが発生しない。** 同要件が「申告内容は検証しない」と明記しており、**記述の正しさは機械検査の対象外である。** その空白を人の読解で埋めるのが本変更である。

## 11. 総合判定と再開地点

**判定: pass（ラウンド2）。** 外部reviewerのMinor 1件を是正した。

再開地点は`workflow record --step=10`、`review round`、`pr create`である。

**残る観測を3件記録する。**

1. **同型の誤りが他のreview artifactにある可能性が高い。** 本変更では調べていない。一括是正を行うなら、先に何を機械検査できるかを決めるべきである。
2. **表のcell数の一致は機械検査できるが、本変更では足さない。** Issue #1254が製品codeを対象外としており、**「強制点が無い」を欠陥として扱わない。** 必要性を測ってから足す。
3. **`issue validate`とtemplateの矛盾は残る。** 新規SCNを持たない変更は、既存SCNの逐語引用でしか要求を満たせない。**これは書式を満たすための操作であり、実質の担保ではない。**
