# 124 課題1159 監査ラウンド上限の正本import化 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。**PR #1150 の実装漏れの是正である。私の誤りである。**

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1159 |
| 比較基点 | `dd063283a0d8e02394e13ae163db2435612bbb6c` |
| H_impl | `35c56655d90a251650fbc3a67b8ee8cc8d6acc06` |
| 対象差分 | 検査script、domain、integration feature、仕様3件の6 file |
| 対象外 | **次はいずれも同じ理由で対象外である。** 上限そのものの変更（4はowner決裁済み）、`review-convergence.ts` の判定logic、他の重複定数の調査 |
| 残り予算 | Step 10の上限3ラウンドのうち1を残す |
| ラウンド | Step 10 ラウンド2 |
| ラウンド数 | 2（Step 10のラウンド1〜2） |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260903_100802_audit-checkのラウンド上限を判定の正本からimportする |
| モード | quick（Q-01〜Q-08がすべて真） |
| 仕様の所有箇所 | `docs/specs/11_非機能/01_品質要件.md` の QLT-STEPCHAIN-001 |
| 成果物行数 | 製品: `check_file_audit.ts` で11 insertions・2 deletions、`review-convergence.ts` で2 deletions・2 insertions。**支援層: staging 00が6,051文字（本日平均34,806文字の17%）** |
| 縮小の先行評価 | 実施済み。**定数の同期検査を足さない。** importで構造的に断てば検査が不要になる |
| authority | 変更しない。**上限値4はowner決裁済みであり、本変更は取得経路だけを変える** |
| 実施者・日時 | reviewer、2026-09-03（JST） |

### 0.1 routing入力契約

| role欄 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類、変異試験 | routine（riskがlow） | claude | Opus 5、effort high | 上限値が変わったら停止する | implementerとreviewerはいずれも本sessionのAI agentであり、**Git commit authorも同一である** |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| **欠陥の実地再現** | Issue #1155 の round 4 | `review round` が round 4 を受理したが `audit:check` が `reviewラウンドが上限を超えています: 4（上限3）` で拒否した | 実行観測 |
| 上限の乖離 | 3箇所 | `02_品質基準.md` は4、`review-convergence.ts` は4、`check_file_audit.ts:626` は**3** | 実測 |
| doc commentとの矛盾 | `check_file_audit.ts:625` | 「上限は`02_品質基準.md`が所有する。ここは同じ値を強制するだけ」と明記しながら追随していなかった | 実測 |
| importが可能であること | `check_file_audit.ts` の既存import | `src/domain/issue.js`、`src/lib/process.js`、`src/lib/version.js` を既にimportしている。**新しい依存の種類ではない** | 実測 |
| 上限値の非変更 | `git diff` | `REVIEW_ROUND_BUDGET = 3`、`REVIEW_RECOVERY_ROUND = REVIEW_ROUND_BUDGET + 1` を変えていない。export修飾子を足しただけ | 実測 |
| 変異の有効性 | 変異3件 | **M1（上限を3へ直書きへ戻す）が本欠陥そのものの再現である。** 全件kill | 実測 |
| 全gate | `conformance:check` | project rule 20件、orphan 0件で合格 | 実行観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: pass。`check_file_audit.ts` → `review-convergence.ts` の一方向。逆参照は無い
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: pass
- reviewer stable IDがPR author/実装commit author stable IDと異なる: **異ならない。** §9に記録した
- 既定branch追随: 不要。本artifact作成時点で `origin/main` は base から動いていない

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `scripts/check_file_audit.ts` | M | project | project | 上限を正本からimportする | `review-convergence.ts` への一方向 | AC-01からAC-03 | revertで復旧 | pass |
| `src/domain/review-convergence.ts` | M | package | package | 定数2件へexport修飾子を足す。**値を変えない** | 参照されるだけ。循環なし | 同上 | 同上 | pass |
| `test/features/integration/audit-artifact-selection.feature` | M | project | project | 反例を4→5へ直し、4を受理するScenarioを足す | featureはstepsへ一方向 | SCN-INT-STEPCHAIN-001、010 | 同上 | pass |
| `docs/specs/11_非機能/01_品質要件.md` | M | package | spec | QLT-STEPCHAIN-001へimport元を明記する | 仕様は実装を参照しない | QLT-STEPCHAIN-001 | 同上 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package | spec | SCN 1件を既存行へ足す | 追跡のみ | 該当なし | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | 実装漏れであったことを含めて記録する | 履歴のみ | 該当なし | 1行の削除で復旧 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: pass（6件）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: pass

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | **新規Scenarioの採番が既存の `SCN-INT-STEPCHAIN-005` と衝突した。** featureに `005` が2つ並んだ | 追跡の一意性 | なし | `010` へ採番し直した。**featureの `Scenario:` 行を全件grepしてから採番すべきだった** | `grep SCN-INT-STEPCHAIN` の全件確認 | no-spec-impact | pass |

**同型の誤りを本session内で繰り返している。** #1155 では表の列数を数え違え、ここでは採番の重複を見落とした。**いずれも「書く前に全件を数える」で防げる。**

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-INT-STEPCHAIN-010 | 4ラウンドの受理 | 合格 | pass | §7 |
| AC-02 | SCN-INT-STEPCHAIN-001 | 5ラウンドの拒否 | 合格 | pass | §7 |
| AC-03 | SCN-INT-STEPCHAIN-001 | 上限を2箇所で持たない | `MAX_REVIEW_ROUNDS = REVIEW_RECOVERY_ROUND` | pass | §1 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | ラウンド上限は有限レビュー契約のfail-closed不変条件である | **上限値を変えない。** `git diff` で `= 3` と `+ 1` が不変であることを確認した |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 診断文言が上限値を含むため、importにより実値と表示が必ず一致する | 変異M2（上限を5へ）が診断文言のassertionで検出される |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 人が触れるUIを持たないCLIであり、出力はJSONだけである | `projectKind` が `cli` |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UI componentもthemeも持たず、色・間隔・typographyの決定を含まない | `capabilities.designTokens` が `not-applicable` |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 4を受理し5を拒否する |
| 価値 | 利用者・運用上の目的を満たすか | pass | **owner決裁で入れた取り直し1ラウンドが実際に使えるようになる。現状は決裁が実質無効だった** |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 既存のimport経路をそのまま使う |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 3箇所の上限が1つの正本から導かれる |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | **同じ値を2箇所で持つ構造を断った。** 次に上限が変わっても追随漏れが起きない |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| **悪用** | **importにより上限の変更が1箇所で済み、緩和が見えにくくなる** | pass | 上限は `02_品質基準.md` が所有し、その改訂はowner決裁を要する。**本変更は取得経路だけを変え、値を動かさない** |
| 反例 | 要件を破る入力・状態がないか | pass | 変異3件を全件kill。**M1が本欠陥そのものの再現である** |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | 4を受理し5を拒否する両側をScenarioで固定した |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | importが解決できなければ `typecheck` が落ちる |
| 安全性・データ損失・ロールバック | 認可・上書き・復旧 | pass | 読み取り専用の定数参照。revertで戻る |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | `MAX_REVIEW_ROUNDS` の参照は `check_file_audit.ts` 内の1箇所 |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| M-01 | Medium | **同じ値を2箇所で持つ構造が他にもあるかを数えていない** | 起票時の「実測すべきこと」 | 将来 | **本Issueでは数えない。** 1箇所の是正のために全域調査を行うと支援層が成果物を上回る。**必要になった時点で数える** | valid（設計判断。record-only） | 残存する |
| L-01 | Low | **採番の重複を1回起こした** | §2.0のDISC-001 | 追跡の一意性 | `010` へ直した。**書く前に全件grepする** | valid（record-only） | 残存しない |

**Critical/High 0件。**

## 6. ラウンド固有の確認

### ラウンド1（Step 10、2026-09-03、candidate HEAD = `H_impl`）

- 全評価基準を確認した: はい。肯定5観点・敵対6観点をすべて評価した
- 指摘を確定した: はい。M-01とL-01はいずれも記録のみ
- 次ラウンド対象のCritical/High: **なし**
- blocking: 0件

### ラウンド2

- 本artifactのcommitでHEADが動くため再固定する

### ラウンド3

- 未実施。**予算1を外部reviewの是正のために残す**

## 7. テスト結果

**承認証拠は成功CI runである。**

実行したcommand: `docs:format`、`test:format`、`lint`、`format:check`、`typecheck`、`source:check`、`trace:check`、`architecture:check`、`skills:check`、`conformance:check`、`audit:check`、`issue validate`

全layerの合計: `conformance:check` が **project rule 20件・orphan 0件で合格**。内部で `npm test` を実行するため単独で回した。

失敗またはskipがある層: skipした16 scenarioはsemantic graphのGraphQLite assetが未設定な環境に起因する。本差分はこの経路に触れない。

対応する成功CI run: PR作成後に取得し、merge前に現`H_final`へ束縛する。

**変異試験の全量。**

| 変異 | 結果 |
|---|---|
| **上限を3へ直書きへ戻す**（本欠陥の再現） | kill |
| 上限を5へ | kill |
| 比較を `>=` へ | kill |

## 8. 配布物影響

| 変更path | 配布境界に入るか |
|---|---|
| `src/domain/review-convergence.ts` | **入る**（`dist/src/` として） |
| `scripts/check_file_audit.ts` | 入らない |
| `test/` 1件 | 入らない |
| `docs/specs/` 3件 | 入らない |

判断: 配布物を更新した

根拠: `package.json` の `files` は `dist/src/` を含む。**6 fileのうち1 fileが配布境界に入る。ただし変更はexport修飾子の追加だけで、値も挙動も変えていない。**

**releaseの起動と計画は分けて記録する。** `release.yml` は `main` への `push` で起動し、その後 `planAutoRelease` が配布digestの差分を見て計画を決める。**本変更は配布digestを変えるため、version bumpとtagが作られる見込みである。** merge後に実観測する。

| 対象 | revertで戻るか | 実行者 | 手順 | 完了確認 |
|---|---|---|---|---|
| repository状態 | **戻る** | 実行役 | 打ち消しPRを通常mergeする | `git diff` が元の状態と一致する |
| 公開済みtagとGitHub Release | **戻らない** | repository owner | 削除せず次versionで前進する | `gh release list` に新tagが載る |
| npm registryの公開物 | **公開されない** | 該当しない | 本経路では公開しない | `npm view` に該当versionが無い |

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | **本artifact作成時点では無い。** exact-headは`H_final`。immutable GitHub reviewは0件 |
| reviewerがPR author・実装commit authorと異なる | **異ならない。** 同一sessionのAI agentであり、Git commit authorも同一である |
| 観測したreview commentとapprovalの件数 | 本session内のreview 2ラウンド（Critical/High 0、Medium 1、Low 1）。GitHub review 0件 |

**`02_品質基準.md` の「独立reviewが成立しない場合」に従う。** 同節は「既定は停止ではなく、無記録での通過の禁止である」と定める。

**本欠陥は製品自身の実行によって発見された。** Issue #1155 で取り直しラウンドを使おうとして `audit:check` が拒否し、そこで初めて追随漏れが判明した。**私がPR #1150 のreviewで見落とした欠陥を、製品を使うことが検出した。**

**PR作成後の外部reviewを待つ。** 予算1ラウンドを残してある。

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: QLT-STEPCHAIN-001、`15_要件追跡/00_追跡表.md`、`01_変更履歴.md`
- ドメイン用語台帳: 新規用語を追加しない
- 要件・変更・SCN・テストの追跡: REQ-SQ-023 → AC-SQ-023 → SCN-INT-STEPCHAIN-001、SCN-INT-STEPCHAIN-010
- UI・トークンの判断: UIを所有しないため対象外。ADRを追加しない

## 11. 総合判定と再開地点

- 未解決Critical/High: **なし**
- Medium/Lowの記録: M-01（他の重複定数を数えていない）、L-01（採番の重複を1回起こした）
- 判定: approved（Step 10 ラウンド2）
- 新しい権限が必要な事項: mergeは別authority
- 残存リスク:
  1. **同じ値を2箇所で持つ構造が他にもあるかを数えていない。** 必要になった時点で数える
  2. **PR #1150 のreviewで私がこの欠陥を見落とした。** 「所有元が変わったのに追随していない」形は、変更したfileだけを見ていると気付けない
- 次に許可される操作: push、PR作成、外部reviewの到着確認、必須check 2件の全緑確認、およびownerが承認したauthorityによる**通常merge commit方式**でのmerge。**squash mergeを使わない**
- 次回の再開地点: PR作成から
