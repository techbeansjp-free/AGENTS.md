# 04 レビュー

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 1 |
| H_impl | `fa024088c4627e7723df9da4a62ec91f934a0216` |
| 比較基点 | `7f2aedac0225eccefd82c5a6ed02b8eb0cd09c71` |
| 対象SHA・文書ダイジェスト | `fa024088c4627e7723df9da4a62ec91f934a0216` |
| 対象差分 | `7f2aedac0225eccefd82c5a6ed02b8eb0cd09c71..fa024088c4627e7723df9da4a62ec91f934a0216`、7 path。うち`dist/`配下1件は生成物として個別監査の対象外とし、配布影響は§8へ残す |
| 対象外 | finalize allowlistの変更、安全条件・authority・fail-closed不変条件の変更、`issue staging`の自動実行、scratch fileの自動削除、理由文のstaging directory単位への集約 |
| 残り予算 | 同一範囲で最大3ラウンドのうち1ラウンドを使用。**残り2。** |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260908_121321_finalize拒否理由へ次に採る行動を示す |
| 仕様の所有箇所 | `docs/specs/02_要件/02_プロジェクトライフサイクル要件.md`のREQ-LC-009。引用: 「**保持理由は、その領域を所有するcommandが存在する場合それを示す。**」 |
| 成果物行数 | 実装 +34、test +86、仕様 +5、生成dist +21 |
| 縮小の先行評価 | **新しいgate、validator、CLI、schema、台帳、project choice項目を1つも追加していない。** 新規fileも作らず、`owningCommandHint`はexportもしない。**判定logicへ1行も触っていない。** #1248で検討した4つの緩和案はすべて棄却済みであり、本変更は報告だけを直す |
| 実施者・日時 | implementer: Claude Code / claude-opus-5[1m]。reviewer: codex CLI / 設定既定model / reasoning effort high。2026-09-08 |

### 0.1 routing入力契約

| role欄（担当role） | 許可path・操作 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|---|
| implementer | `src/`、`test/`、`docs/specs/`、`dist/`の読み書き | SCN実行結果、判定不変の前後比較、変異試験 | routine。riskはlow、判定を変えない | project choiceのprovider上限に従う | project choiceのtier mapping | 検査失敗時はcommitせず停止する | reviewerはcodexの新規sessionで実装contextを共有しない |
| reviewer | 読み取りのみ。`--sandbox read-only` | 肯定・敵対review、finding分類、原文引用 | standard | codex CLI | **`--model`で固定せず設定既定に従う** | 独立性が不明ならPRとmergeを停止する | reviewer自身が「ファイル変更・commit・pushは行っていません」と報告した |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | GitHub Issue #1290、staging 00・01のAC-01からAC-03 | Step 8で`sync-verified`、syncDigestとreadBackDigestが`f925b62980e070d8978be2894e88c1902937eca754cf90758d2ca98f633d5b7f`で一致 | 実行観測 |
| 是正前の状態 | 是正前`dist/`の`assessWorktreeRemovalSafety` | blocking理由は`allowlist外の無視対象資産です: <path>`だけで、所有commandを示さない | 実行観測 |
| 是正後の実環境 | `worktree survey --root=<repository root>` | **blocking資産379件のうち192件へ案内が付き、187件には付かない。** 内訳は`tmp/`直下のscratch 186件と`runtime/`1件 | 実行観測 |
| 既存機構の存在 | `issue staging --root=<worktree>` | **17 worktreeすべてで`state: deletion-ready`。** 採るべき行動は存在していた | 実行観測 |
| 対象SCN | `--name`で絞り込んだcucumber実行 | **43 scenarios、215 steps成功**（新3件と既存40件） | テスト出力 |
| 変異試験 | 8件 | **7 kill、等価1件、生存0。うち2件はreviewer由来である** | テスト出力 |
| 仕様 | `docs/specs/02_要件/02_プロジェクトライフサイクル要件.md`、`15_要件追跡/` | updated | 既存文書 |
| commit前candidate | `git diff --name-only 7f2aedac fa024088` | 7 path。うち生成dist 1件 | Git index |
| Phase A artifact | `docs/reviews/178_課題1290保持理由への所有command案内レビュー.md` | H_implの後にこの1 fileだけをcommitしてH_finalとする | Git観測 |
| commit後external | PR、CI run、外部review | **本artifactの作成時点では未観測である。** | 外部のimmutable証拠 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **満たす。** 観測→判定→理由生成の一方向であり、**理由生成は判定へ影響を戻さない。**
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: **artifact commit時点で満たす。**
- reviewer stable IDが`H_impl` author stable IDと異なる: **満たす。**
- 既定branch追随を行った場合: **該当なし。** 比較基点は`7f2aedac`のままである。

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/worktree-removal-safety.ts` | M | package owner | domain | `owningCommandHint`と対応表を追加し、blocking理由の末尾へ案内を足す。**判定logicへ1行も触っていない** | pass。新しい依存を追加していない。**`STAGING_LIFECYCLE_AREAS`を流用していない** | REQ-LC-009 / AC-01〜AC-03 / SCN-UNIT-FINALIGN-035〜037 | `safe`と2つのartifact配列は不変。前進revertで復旧 | pass |
| `test/features/unit/finalize-ignored-artifacts.feature` | M | package owner | evidence | SCN-UNIT-FINALIGN-035〜037の追加 | pass | AC-01〜AC-03 | 追加のみ。既存scenarioを削除していない | pass |
| `test/steps/finalize-ignored-artifacts.steps.ts` | M | package owner | evidence | 案内の出現範囲、大小文字、正規化を要する入力、判定不変を検査する | pass。既存step定義を書き換えていない | AC-01〜AC-03 | 追加のみ | pass |
| `docs/specs/02_要件/02_プロジェクトライフサイクル要件.md` | M | package owner | spec | REQ-LC-009へ案内の条項、契約の区別、2つの非正規化条項、強制主体の分離を追記 | pass。名指ししたSCN 3件は同じ要件の追跡行に登録済み | REQ-LC-009 / AC-LC-009 | 既存段落を削除していない | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package owner | spec | 新3 SCNの追跡登録 | pass。`trace:check`のorphanが0件 | REQ-LC-009 | 既存行は不変 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package owner | spec | 変更理由と判断の記録1行 | pass。9列のheader区切り直後へ挿入 | REQ-LC-009 | 既存行は不変 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **満たす。** `git diff --name-only`の7 pathのうち、生成物である`dist/`配下1件を除いた6 pathと表の6行が一致する。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **満たす。**
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: **満たす。** ラウンド1の是正は`src/domain/worktree-removal-safety.ts`、`test/steps/`1 file、仕様1 fileに閉じている。

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 判断 | 対処 | 検証 |
|---|---|---|---|---|---|
| DISC-001 | 既存の`validArtifactPath`が、空segment、`.`、`..`、絶対path、`\`、制御文字、非NFCを案内より前で不正観測へ分離している | reviewerが挙げた4つの境界入力は案内へ届かない | **案内側で正規化しない。** 既存の入力契約に依存する | 依存関係をコメントと仕様へ明記し、SCN-UNIT-FINALIGN-036で合成経路から確認した | 4入力の実測 |
| DISC-002 | 実環境のblocking資産は379件で、192件へ案内が付き187件には付かない | 案内の切り分けが過剰でも過少でもない | 実測値をそのまま記録する | §1へ記録した | `worktree survey`の出力 |

いずれも目的、scope、AC、security境界、不可逆操作を変えない。`workflow assess-discovery`の判定は`continue`である。

### 2.1 受け入れ条件とシナリオ

| AC ID | 判定 | 根拠 |
|---|---|---|
| AC-01 該当領域へ案内が付く | pass | SCN-UNIT-FINALIGN-035。案内が1件だけで、対象pathに付き、既存文字列が先頭から変わらない |
| AC-02 非該当領域と接頭辞延長へ案内が付かない | pass | SCN-UNIT-FINALIGN-036。`tmp/issues`そのもの、`tmp/issues-other/x`、`tmp/1290/scratch.md`、**大文字変種**、および正規化を要する4入力 |
| AC-03 案内の有無が判定を変えない | pass | SCN-UNIT-FINALIGN-037。`safe`と2つのartifact配列を前後で突き合わせる |

## 3. 肯定的評価

- **判定logicへ1行も触っていない。** 変更は対応表の追加と理由文字列の連結だけである。
- **#1248で棄却された緩和案を実装していない。** 同じ問題に対し、安全条件を変えずに解いた。
- **案内の切り分けを実環境で実測した。** 192件へ付き187件へ付かない。過剰でも過少でもない。
- **既存の入力契約へ依存し、二重の正規化を導入していない。** `validArtifactPath`が案内より前で不正な形を分離する。

## 4. 敵対的評価

**reviewerが2件の生存変異を構成し、うち1件は私の見落としだった。**

| 反例 | 内容 | 実測 | 対処 |
|---|---|---|---|
| 1 | `artifact.toLowerCase().startsWith(entry.prefix)`へ変える | **43 scenarios全緑のまま生存した。** case-sensitive環境で別pathを所有領域と誤認する | `.AGENT-SKILL-CHAIN/TMP/ISSUES/x`の反例をSCN-036へ足した。是正後にkillする |
| 2 | 案内側で`//`と`./`の正規化を足す | 全緑のまま生存した | **等価変異と判定した。** これらの入力は`validArtifactPath`が案内より前で不正観測へ分離するため、案内側の正規化は観測不能である。判定根拠をコメントと仕様へ明記した |

**reviewerの境界指摘のうち、合成経路では成立しなかったもの。**

| 指摘 | 実測 | 判断 |
|---|---|---|
| `.agent-skill-chain/tmp/issues/`（末尾slash）へ案内が出る | **出ない。** 空segmentとして`validArtifactPath`が拒否する | reviewerは`owningCommandHint`を単独で見ており、合成経路の前段を見ていない |
| `tmp/issues/../runtime/x`へ誤案内が出る | **出ない。** `..`segmentとして拒否される | 同上 |
| `tmp/issues//x`、`./.agent-skill-chain/tmp/issues/x` | **どちらも出ない。** 拒否される | 同上 |

**それでも指摘は有益だった。** 前段の保証に依存していることが文書化されていなかったため、SCN-036へ4入力を足して合成経路から確認し、コメントと仕様へ依存関係を明記した。**前段が変わればこの実装は壊れる。**

**reviewerの指摘で訂正した主張。**

| 私の当初の主張 | 訂正 |
|---|---|
| 「報告だけを変え、契約を変えない」 | **不正確。** `reasons`の文字列は戻り値とCLI出力へ露出する観測可能な契約である。正しくは「**安全判定と資産分類を変えず、診断出力の契約だけを意図的に変える**」。仕様へ書き直した |
| 案内文「previewと承認済みhashで先に片付けられます」 | **説明であって実行できる形ではない。** 2段階の完全なcommandへ書き直した |
| 実測「371件、内訳184+187+1」 | **合計が合わない。** 一度だけ測り直し、379件、192件へ案内あり、186件が`tmp/`直下のscratch、1件が`runtime/`へ統一した |

**reviewerの指摘のうち採らなかったもの。**

| 指摘 | 判断 | 理由 |
|---|---|---|
| 案内をpath単位でなく別枠で一度だけ出す | 別Issueへ | 192回の反復は読みにくいが、集約はpath単位の情報を落とす。**判定を変えない範囲でも、情報を減らす変更は別の判断である** |
| 「同期済み」の定義や`<worktree>`の特定方法まで案内へ書く | 採らない | 理由文が肥大する。**最初に実行する完全なcommandを示せば、その出力が次を教える。** 2段階のcommandはその形にした |

## 5. 指摘

| ID | 重大度 | 内容 | 対処 |
|---|---|---|---|
| H-01 | High | 大小文字を畳み込む変異が全緑のまま生存する | 是正。反例を追加し、意図をコメントと仕様へ明記した |
| M-01 | Medium | 「契約を変えない」が不正確。診断契約は変えている | 是正。判定の契約と診断の契約を区別して書き直した |
| M-02 | Medium | 案内が実行可能なcommandになっていない | 是正。2段階の完全なcommandへ直した |
| M-03 | Medium | 実測の件数が合わない | 是正。一度だけ測り直して統一した |
| L-01 | Low | 前段の入力契約への依存が文書化されていない | 是正。SCN-036へ4入力を足し、コメントと仕様へ明記した |
| L-02 | Low | 案内が192回反復されて埋もれる | 別Issueへ分離する |

## 6. ラウンド固有の確認

### ラウンド1

- reviewerはcodex CLIの新規sessionで、`--sandbox read-only`、`--model`未指定である。
- **inline contextで全差分を渡した。** repository探索をさせず、実装・SCN・変異試験・採らなかった手段5件を本文へ埋め込んだ。
- 指摘6件のうちHigh 1件・Medium 3件・Low 1件を是正し、Low 1件を別Issueへ分離した。**reviewerが構成した2変異のうち1件は是正後にkillへ変わり、1件は等価変異と判定して根拠を残した。**
- **reviewerの境界指摘4件は合成経路では成立しなかった。** 実測で確認し、なぜ成立しないかを§4へ記録した。

## 7. テスト結果

| 検証 | コマンド | 結果 |
|---|---|---|
| project品質 | `npm run project:quality` | 合格 |
| 静的検査 | `npm run lint` | エラー0件 |
| formatter非破壊 | `npm run format:check` | 差分0件 |
| strict型検査 | `npm run typecheck` | 型error 0件 |
| source契約 | `npm run source:check` | 合格 |
| 文書形式 | `npm run docs:format` | 合格 |
| Gherkin形式 | `npm run test:format` | 合格 |
| 追跡整合 | `npm run trace:check` | orphanが要件・SCN・実装とも0件 |
| 依存方向 | `npm run architecture:check` | 違反0件 |
| 配布物 | `npm run package:check` | 合格 |
| 対象SCN | `npm test -- --name 'SCN-UNIT-FINALIGN\|SCN-UNIT-WTIGN'` | **43 scenarios、215 steps成功** |
| conformance | `npm run conformance:check` | 合格、87 scenarios、約14秒 |
| 監査 | `npm run audit:check` | 本artifact確定後に実行する |

**`npm test`を`conformance:check`と別に実行していない。** 本変更は`conformance:check`が名指しするSCNを増減させず、全Gherkinの合格はCIの`quality`段が担う。

### 変異試験

**8件、7 kill、等価1件、生存0。うち2件はreviewer由来である。**

| 群 | 変異 | 結果 |
|---|---|---|
| A 記入欄削除 | A1 案内の連結を消す | kill |
| A | A2 対応表の接頭辞を到達不能にする | kill |
| B 値の空洞化 | B1 接頭辞の末尾slashを外して延長を巻き込む | kill |
| B | B2 案内文字列を空にする | kill |
| C 走査回避 | C1 前方一致をincludes一致へ緩める | kill |
| C | **C2 案内が出たpathをallowed側へ入れる** | kill。**判定を変える方向の変異である** |
| C | **C3 案内判定をallowlist判定より前へ移し許可へ倒す** | kill。同上 |
| D reviewer由来 | D1 大小文字を畳み込む | kill。**当初は生存した** |
| D | D2 案内側で`//`と`./`の正規化を足す | **等価。** `validArtifactPath`が案内より前で分離するため観測不能である |

**C2とC3は判定を変える方向の変異であり、案内と判定の分離を直接検査している。**

**変異scriptの空振りを2件検出して直した。** 案内文字列の部分削除は「issue staging」を残すため反例にならず、前方一致の置換対象はprettier整形後の実体と食い違っていた。**空振りを生存として報告しない。**

**変異後は必ず`npm run build`と`git status`で`dist/`の汚染を掃いた。**

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/domain/worktree-removal-safety.ts`、`dist/src/domain/worktree-removal-safety.js` | 入る | blocking理由のうち`.agent-skill-chain/tmp/issues/`配下のものへ、所有commandの案内が末尾へ加わる |
| `docs/specs/`の3 file | 入らない | ASC自身の仕様と追跡であり配布物に含まれない |
| `test/`の2 file | 入らない | 配布境界外の検証資産である |

判断: 配布物を更新した

根拠: `src/domain/worktree-removal-safety.ts`は配布されるpackage所有資産であり、`worktree survey`と`worktree finalize`の`reasons`という外部観測可能な出力が変わる。**変えないのは判定の契約であり、変えるのは診断の契約である。** `safe`、`allowedIgnoredArtifacts`、`blockingIgnoredArtifacts`、欄構成、分類名、終了値の規則はいずれも不変であり、削除可能範囲は1 byteも広がらない。

## 9. 独立reviewの成立

- reviewerはcodex CLIの新規sessionであり、implementerのcontextを共有しない。
- reviewer自身が「ファイル変更・commit・pushは行っていません」と報告した。
- **reviewerは2つの生存変異を構成し、1件は是正後にkillへ変わり、1件は等価変異として根拠を残した。** さらに私の主張3件を訂正させた。
- **reviewerの境界指摘4件は合成経路では成立しなかったが、前段への依存を文書化する契機になった。**
- `--model`を指定せずconfig既定に従った。

## 10. 仕様整合性

- `docs/specs/02_要件/02_プロジェクトライフサイクル要件.md`のREQ-LC-009へ、案内の条項、判定契約と診断契約の区別、大小文字を畳み込まないこと、案内側で正規化しないこと、強制主体の分離、`role-log`・`metrics`・`runtime`に案内が無いことを追記した。
- `docs/specs/15_要件追跡/00_追跡表.md`へ新3 SCNを登録した。`trace:check`のorphanは0件である。
- `docs/specs/15_要件追跡/01_変更履歴.md`へ1行を追加した。9列のheader区切り直後である。
- **`docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`は変更していない。** 同文書は`worktree survey`の欄構成と分類名を規定しており、**理由文の個々の文言は規定していない**ためである。
- 用語台帳は変更していない。既存の`TERM-ASC-062`を参照する。

## 11. 総合判定と再開地点

**判定: 合格。** ラウンド1でHigh 1件・Medium 3件・Low 1件を是正し、Low 1件を別Issueへ分離した。reviewerが構成した生存変異1件がkillへ変わることを実測し、1件は等価変異として根拠を残した。変異8件のうち7件がkill、1件が等価、生存0である。

**この変更は#1248の反省から生まれた。** 同じ診断を読んで私は「機構の欠陥」と誤読し、安全条件の緩和を検討して両アドバイザーに棄却された。**診断が次の行動を示さないこと自体が、緩和提案を誘発する経路になっていた。** 本変更はその経路を、判定を1つも変えずに断つ。

再開地点は本artifactのcommit（H_final）、`review round`の記録、`pr create`である。分離した課題は理由文の集約（L-02）である。
