# 課題857 適用レビュー

> 状態: `ready-for-pr`。品質検査の走査境界変更の**適用段階**を、merge済みdefault branch headを比較基点として固定した内部監査証拠である。proposal登録段階はPR #859でmerge済みであり、default branchのregistryが`TQP-QUALITY-SCAN-BOUNDARY-001`を所有している。独立reviewの承認は自己申告せず、CIとPR reviewの外部証拠で確定する。

## 判定

| 項目 | 値 |
|---|---|
| 状態 | ready-for-pr |
| 比較基点 | `1fe30101b721e3a7ece9f5fb3c223e06b8c6d901` |
| H_impl | `058f6764fcf611a3541e4add36cbda9bcbb55adf` |
| H_impl tree | `46eaac0d5d6af8df8dc7b33a29f51aa34fb8b280` |
| Critical | 0 |
| High | 0（レビュー中に検出したfixture 1件は是正済み） |
| Medium | 0 |
| Low | 0 |
| role分離 | implementerはCodex `gpt-5.6-sol`（reasoning high、service tier default）、coordinatorとreviewerはClaude Opus 5。同一scopeでidentityが異なる |
| 前提 | PR #859がmain `1fe30101b721e3a7ece9f5fb3c223e06b8c6d901`へmerge済み。baseのregistryが`TQP-QUALITY-SCAN-BOUNDARY-001`を`staged`で保持する |

## 開発考慮事項

| ID | 考慮事項 | 判定 | 理由 | 要求・確認証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | 走査範囲は検出強度に直結する信頼境界であり、除外は最小限でなければならない | `issues`のroot直下限定、fixtureによる深い階層の検出確認 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | localで品質gateが実行できることが開発の観測性そのものである | `lint`と`source:check`のlocal合格 |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | Node CLIでGUIまたはWeb UIを変更しない | projectKindがcli、UI source変更なし |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | 視覚componentとlayoutを所有せずtoken適用対象が存在しない | UI sourceなし |

## 変更ファイル個別監査

| path | status | owner | target layer | 責務・配置 | 依存・循環 | 仕様・追跡 | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `scripts/check_source_quality.ts` | M | package maintainer | script | `walk`へ走査rootを渡し、`memo`と`.worktrees`をdirectory名で、`issues`をroot直下だけで除外する | scriptからdomainへの片方向依存 | AC-857-01〜03 | 除外を戻せば旧走査範囲へ復帰する。適用後SHA-256がbaseのproposalと完全一致する | pass |
| `eslint.config.mjs` | M | package maintainer | 品質設定 | `ignores`へ`memo/**`とroot相対の`issues/**`を追加する | 設定のみ | AC-857-01 | 同上 | pass |
| `package.json` | M | package maintainer | package metadata | `agentSkillChain.qualityContractVersion`を1から2へ1段階更新する | metadataのみ | AC-857-05 | versionを戻せば適用前へ復帰する | pass |
| `test/steps/unit.steps.ts` | M | package maintainer | test | `SCN-UNIT-QUALITY-008`のfixtureが品質契約versionをtrusted rootの実値から`n`→`n+1`で解決する | step定義からscriptへの参照のみ | SCN-UNIT-QUALITY-008〜009 | scenarioの検証内容は変えていない | pass |

Gitの`1fe30101b721e3a7ece9f5fb3c223e06b8c6d901..058f6764fcf611a3541e4add36cbda9bcbb55adf`に含まれる4 pathと表の4行は重複なし・欠落なしで一致する。

## proposalとの一致確認

reviewerが独立に、適用後のfile内容がbaseで事前登録済みのSHA-256と完全一致することを確認した。

| 対象 | 期待SHA-256 | 実測SHA-256 | 判定 |
|---|---|---|---|
| `eslint.config.mjs` | `6466cbe9cc2b060e19842a2ed0ec1746e64185456bc0561a11a27b4e3ab7b77b` | 同一 | pass |
| `scripts/check_source_quality.ts` | `726432a2de4b92c9101e05ca681b501c0d1431c8db581a6806525d5a2a266b3a` | 同一 | pass |

`npm run project:quality`が合格することで、`validateTrustedQualityMigration`が`sameMap`によりbaseのproposalと完全一致を確認したことになる。

## レビュー中に検出した問題と是正

| 問題 | 真因 | 是正 |
|---|---|---|
| `SCN-UNIT-QUALITY-008`が「baseで事前登録済みのversioned staged proposalと完全一致しません」で失敗 | fixtureが品質契約versionを`1`から`2`へとhard-codeしていた。実repositoryのversionが2になった時点で`trustedVersion`が2となり、`fromVersion: 1`の提案が1段階更新に該当しなくなる | gate側を変えず、fixtureがtrusted rootの実versionを読んで`n`→`n+1`で提案する形へ是正した。今後versionが上がっても壊れない |

## 肯定・敵対レビュー

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ | pass | localで`lint`、`source:check`、`SCN-UNIT-QUALITY-003`がすべて合格する |
| 価値・実現可能性 | pass | repository ownerが自身のcheckoutで品質gateを実行できる。`ASC-DOGFOOD-DOGFOODING-001`の前提が成立する |
| 整合性 | pass | 除外対象は`.gitignore`、AGENTS.mdの「現在の実行規約として扱わない」宣言、`ASC-DOGFOOD-DISTRIBUTION-BOUNDARY-001`と一致する |
| 保守性 | pass | `walk`へrootを渡す形にしたため、root限定の除外を宣言的に表現できる |
| 反例・失敗経路 | pass | 深い階層の`issues`は引き続き走査する。fixtureで`src/asc857/deep/issues/`内のJavaScriptを検出することを確認済み |
| 境界値・悪用 | pass | 除外はdirectory名の完全一致とroot相対pathだけで、任意pathの除外機構を導入していない |
| 安全性・データ損失 | pass | 検査は読み取り専用で外部writeを持たない |
| rollback | pass | 3 fileの差分revertとversionの戻しで復帰する |
| 範囲漏れ | pass | `.agent-skill-chain/project/`、CI workflow、package scriptを変更していない |

### 敵対観点の残論点

除外したdirectoryはGit管理外であり、CIには存在しない。したがってCIの検出強度は本変更の前後で変わらない。変わるのはlocal実行時の走査範囲だけである。**localとCIで走査対象が一致しないという性質自体は残る**が、これは`.gitignore`された作業用directoryが存在する以上避けられず、除外対象を明示宣言することで観測可能にした。

## テスト結果

| 層・検査 | コマンド | 成功 | 失敗 | 判定 |
|---|---|---:|---:|---|
| project品質契約 | `project:quality` | 1 | 0 | pass。baseのproposalとの完全一致を確認 |
| 静的品質 | `lint`、`format:check`、`typecheck`、`source:check` | 4 | 0 | pass。**localで初めて全件合格** |
| 全BDD | `npm test` | 432 | 0 | pass |
| 配布・追跡・監査 | `build`、`docs:format`、`test:format`、`trace:check`、`architecture:check`、`conformance:check`、`package:check` | 7 | 0 | pass |

`audit:check`は本artifactを追加したH_finalで実行する。

## 停止点

- H_impl: `058f6764fcf611a3541e4add36cbda9bcbb55adf`。
- 本PRのmergeでIssue #857を終了する。
- release、branch削除、worktree cleanupは本Issueの終了処理では実施しない。

H_finalはこのreview artifactだけを追加するcommitとし、以後このtracked artifactを変更しない。
