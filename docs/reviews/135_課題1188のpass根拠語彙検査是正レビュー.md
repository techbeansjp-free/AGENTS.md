# 135 課題1188のpass根拠語彙検査是正レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1188 |
| ラウンド | Step 10 ラウンド1 |
| 比較基点 | `0c485f66cf5a4d74c07c8f19b9fd4e75ec82a598` |
| H_impl | `5fe30e5cf42a1b6208e482f6e8b28acc19f2cd8e` |
| 比較基点の由来 | `origin/main`のtip。**本PRはPR #1192 の再構築である**（0.2節を参照） |
| モード | full（Q-01とQ-05がfalse） |
| 対象差分 | 6 file、+331 -19。commitは`5fe30e5c` |
| 対象外 | 既存artifactの遡及是正。登録語彙の増減。`docs/reviews/`のrole authority不整合（#1047） |
| 残り予算 | **2**（同一範囲で最大3ラウンド、収束後にHEADが動いたときの取り直しを1回まで） |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260904_051754_pass根拠語彙検査のcode-fence誤検出と行内語彙の取りこぼしを是正する |
| Step chain | 経由:  |
| 仕様の所有箇所 | `docs/specs/02_要件/04_仕様・品質管理要件.md`のREQ-SQ-031（**改訂**） |
| 成果物行数 | 製品 **+85 -17行**（`check_file_audit.ts`）。仕様 **+9 -2行**。支援層 **+170行**（feature +40、steps +130）。**支援層/成果物 = 2.0倍** |
| 縮小の先行評価 | 3案を先に評価した。(1) F-03へ**案A（引用内容が対象diff内のfileに実在することを要求する）は採らない**。実測した引用16件のうち`grep -n "純関数"`・`[skip ci]`・`permissions`のように**対象source本文に無い正当な引用**が複数あり、拒否すると**F-01と逆向きの偽陽性を同じPRで作る**。(2) 採ったのは**案B（引用が登録語彙そのものである場合を拒否する）**で、循環引用は「引用が証拠として機能していないことが字面だけで確定する」唯一の形である。(3) F-05は**既存`SCN-UNIT-CLAIMVOCAB-006`が既に検査している**ため新規scenarioを足さない |
| 決裁 | PR #1186 への外部reviewer指摘。**5件のうち4件をvalid、1件をvalidでないと判定した** |
| 実施者・日時 | reviewer（claude）、2026-09-04 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節） | advanced | claude | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerとreviewerが**同一session**である。0.2節に逸脱として開示する。**本Issueの起点そのものが外部reviewerの指摘である** |

### 0.2 開示する逸脱

1. **implementerとreviewerが同一sessionである。** 緩和は、**本Issueの4 findingがすべて外部reviewer（CodeRabbit）の検出であること**、判定の根拠を実コード読解と走査実測に置いたこと、両方向の変異試験を行ったことである。
2. **本PRは PR #1192 の再構築である。** #1192 はStep 11記録後に外部reviewerから Major 2件を受け、うち1件が受け入れ条件 AC-01 を満たさない欠陥だった（4個で開いたfenceを3個で閉じる偽陽性）。工程正本がStep 11後の取り込みを認めないため閉じて作り直した。**構造そのものは #1194 で起票済みである。**
3. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 外部review | PR #1186 | Actionable 5件。**4件valid、1件valid でない** | 外部の判断 |
| F-02の確認 | `scripts/check_file_audit.ts` | `HIGH_RISK_CLAIM_TOKENS.find`は宣言順で最初の1語しか返さない。並びは`純関数`・`pure function`・…・`冪等`・`idempotent` | 一次資料 |
| F-05の反証 | `test/steps/evidence-claim-vocabulary.steps.ts` | `SCN-UNIT-CLAIMVOCAB-006`のfixtureは`` `src/a.ts` ``が主張とは別cellにあるbacktick 2個である | 一次資料 |
| 走査実測 | `docs/reviews/` 全130 file | **`pass`行と登録語彙の組68件。SCN併記0件、引用あり16件、引用が登録語彙そのもの2件** | 実行記録 |
| テスト | `npm run conformance:check` | `1476 scenarios (1460 passed, 16 skipped)` | テスト出力 |
| commit前candidate | 6 file | working tree clean | Git index |
| Phase A artifact | `docs/reviews/135_課題1188のpass根拠語彙検査是正レビュー.md` | `H_impl` = `5fe30e5c`。`H_impl..H_final`の差分pathは本file 1件 | Git観測 |
| commit後external | PR、CI run、review | Step 11で観測する | 外部のimmutable証拠 |

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `scripts/check_file_audit.ts` | M | project | project | fence追跡、語彙×cellの直積、判定値正規化、循環引用の拒否の4点。**新しいimportを増やしていない** | pass。file内で閉じる。`architecture:check`合格 | REQ-SQ-031 / AC-01〜06 / SCN-UNIT-CLAIMVOCAB-009〜016 | 診断を`errors`へ積むだけで既存の`errors`と同じ扱いである。現行の全artifactへ誤検出0件を実測した | pass |
| `test/features/unit/evidence-claim-vocabulary.feature` | M | package | package | scenario 8件を追加した。既存8件を1件も変えていない | pass | AC-01〜06 | fixtureは文字列であり外部へ到達しない | pass |
| `test/steps/evidence-claim-vocabulary.steps.ts` | M | package | package | 対応するstep定義を追加した | pass | 同上 | 同上 | pass |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | M | project | spec | REQ-SQ-031へfence・直積・正規化・循環引用の4条項を追記した | pass | REQ-SQ-031 | 記述だけで実行authorityを持たない | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | 新規SCN 8件を結線した | pass | 同上 | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | project | spec | 変更履歴を1行足した | pass | 同上 | 同上 | pass |

## 2. 受け入れ条件の確認

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-CLAIMVOCAB-009 | `fenceMarker`と`openFence` | `16 scenarios (16 passed)` | pass | 説明用code block内の表が診断対象にならない。変異M1でkill |
| AC-02 | SCN-UNIT-CLAIMVOCAB-010 | 同上 | 同上 | pass | 閉じないfence以降を対象外にする。**「閉じていないので本文」とするとfenceを開くだけで残り全体を対象へ戻せる** |
| AC-03 | SCN-UNIT-CLAIMVOCAB-011 | 語彙×cellの直積 | 同上 | pass | `\| idempotent \| pass \| pure function。SCN-… \|`で1列目の裸の`idempotent`を検出する。変異M7でkill |
| AC-04 | SCN-UNIT-CLAIMVOCAB-012 | 同上 | 同上 | pass | 同じ語彙が証拠ありcellと裸cellの両方にあるとき裸cellを検出する。変異M8でkill |
| AC-05 | SCN-UNIT-CLAIMVOCAB-013 | `verdictValue` | 同上 | pass | `p ass`が`pass`にならない。変異M4でkill |
| AC-06 | SCN-UNIT-CLAIMVOCAB-014 | 循環引用の除去 | 同上 | pass | 登録語彙そのものだけを引用したcellが`SCN-UNIT-CLAIMVOCAB-014`で拒否される。変異M5でkill |

**AC以外に2件のscenarioを足した。** `SCN-UNIT-CLAIMVOCAB-015`（異なるfence記号では閉じない、変異M2でkill）と`SCN-UNIT-CLAIMVOCAB-016`（cell内のfence記号をfenceと読まない、変異M6でkill）である。**いずれも変異が生存したために足した。**

### 2.2 不変条件

| INV ID | 内容 | 判定 | 証拠 |
|---|---|---|---|
| INV-01 | 機械検査は意味的真偽を判定しない | pass | 引用が対象コードの原文かは判定しない。**案Aを採らなかった理由がこれである** |
| INV-02 | 適用単位はcurrent review artifact 1件 | pass | `audit:check`が`H_impl..current`から一意に導出する経路を変えていない |
| INV-03 | 既存の`SCN-UNIT-CLAIMVOCAB-001〜008`を1件も削除せず合格を保つ | pass | 8件すべて合格。**feature fileへの変更は追記のみで既存行を1行も変えていない** |

## 3. 肯定的評価

- **偽陽性と偽陰性を同じ変更で塞いだ。** fence追跡は受理を広げ、直積と循環引用拒否は受理を狭める。**反例の向きが逆なので両方向の変異試験を要求した。**
- **F-03へ実在検査を採らなかった。** 実測16件のうち複数が対象source本文に無い正当な引用であり、実在検査は偽陽性を作る。**「偽陽性を塞ぐPRで別の偽陽性を作らない」を優先した。**
- **F-05を根拠つきで否定した。** 既存fixtureの役割を読み、指摘が見落としていることを示した。**指摘を全件受け入れる姿勢は、既存の検査を無駄に増やす。**
- **変異が生存するたびにscenarioを足した。** M2とM6は当初生存し、それぞれ`SCN-015`・`SCN-016`で塞いだ。
- **M3を等価と確認して足さなかった。** 判定列のcellは`verdictValue`で厳密に`pass`へ正規化されたものだけが来るため登録語彙を含み得ない。**反射的にfixtureを足していない。**

## 4. 敵対的評価

| 観点 | 攻撃 | 結果 |
|---|---|---|
| 偽陽性の復活 | fence追跡を消す | 変異M1でkill（3 scenario失敗） |
| 偽陽性の復活 | 異なるfence記号で閉じる | 変異M2でkill |
| 偽陽性の復活 | fence記号の行頭限定を外す | 変異M6でkill |
| 偽陽性の復活 | 判定値の内部文字も削る | 変異M4でkill |
| 偽陰性の復活 | 語彙の直積を1件へ絞る | 変異M7でkill |
| 偽陰性の復活 | cellの直積を1件へ絞る | 変異M8でkill（4 scenario失敗） |
| 偽陰性の復活 | 循環引用も併記と認める | 変異M5でkill |
| 検査範囲の拡大 | 判定列も対象にする | **等価。** `pass`へ正規化されるcellは登録語彙を含み得ない |
| 逃げ道 | fenceを開いて残り全体を対象外にする | **成立する。** 5節ADV-01に記録する |

## 5. 指摘

| ID | severity | 事実 | 観測 | 由来 | 対処 | 判定 | 残余 |
|---|---|---|---|---|---|---|---|
| F-01 | Major | **code fence内の表を実表として解析していた。** 説明用code block内の`pass`行が診断対象になり、実データでない例で`audit:check`が失敗する | PR #1186 の外部review | 外部review | fence追跡を足した。**行頭3文字以上に限り、開いたfenceは同じ記号でだけ閉じる** | valid / resolved | ADV-01 |
| F-02 | Major | **行内の登録語彙を1件しか検査していなかった。** `.find`が宣言順で最初の1語しか返さず、証拠を持つcellの語で行全体が受理された | 同上 | 外部review | 語彙×cellの直積へ変えた | valid / resolved | なし |
| F-03 | Major | **原文引用の判定がbacktickの個数だけだった。** 登録語彙と無関係なinline codeで通る | 同上 | 外部review | **実在検査は採らず**、循環引用の拒否を採った。実測で16件中2件が該当 | valid / resolved | ADV-02 |
| F-04 | Minor | **判定値の正規化が内部文字まで削っていた。** `p ass`が`pass`になる | 同上 | 外部review | 前後の空白と外側装飾だけを剥がす形へ変えた | valid / resolved | なし |
| F-05 | — | 「証拠を別cellへ置いた`pass`行を拒否するscenarioが無い」 | 同上 | 外部review | **valid でない。** `SCN-UNIT-CLAIMVOCAB-006`が検査している。行全体でbacktickを数える実装へ戻すと同scenarioが失敗する | **invalid** | なし |
| DISC-201 | Low | **`pass`行と登録語彙の組68件のうちSCN併記は0件だった。** 実運用では併記手段として引用しか使われていない | `docs/reviews/` 130 fileの走査 | 実測 | **受理は残す。** 併記手段を1つに絞ると、SCNで示すのが自然な場合に逃げ道が無くなる | valid / record-only | SCN併記の経路が使われないまま残る |
| F-06 | **High** | **`fenceMarker`がdelimiterの先頭1文字しか返していなかった。** 4個のbacktickで開いたfenceを3個の行が閉じ、fence内の表が監査対象になる。AC-01を満たしていなかった | PR #1192 の外部review | 外部review | 文字と長さとinfo stringの3条件へ変えた。変異M2・M9・M10でkill | valid / resolved | なし |
| F-07 | Medium | **変更履歴の変異試験の実績が実測と食い違っていた** | 同上 | 外部review | 実測値へ直した | valid / resolved | なし |
| ADV-01 | Medium | **fenceを開けば残り全体を検査対象から外せる。** 閉じないfence以降を対象外にする設計の帰結である | 設計 | 敵対評価 | **修正しない。** 逆にすると「fenceを開くだけで残り全体を対象へ戻せる」形になり、**偽陽性を塞ぐ本Issueの目的と衝突する。** 強制点はStep 10のreviewerである | valid / record-only | 意図的にfenceを開いた artifact は検査されない |
| ADV-02 | Medium | **循環引用の拒否は「引用が証拠として機能していない」形のうち1つしか塞がない。** 無関係なinline codeは通る | 設計 | 敵対評価 | **修正しない。** 実在検査は正当な引用を巻き込む（3件を実測）。`INV-01`が意味的真偽の判定を禁じている | valid / record-only | 無関係な引用が併記として通る |

**未解決のCritical / Highは0件である。**

## 6. ラウンド固有の確認

### ラウンド1

- 対象: 実装差分の6 file。
- 確認: 個別監査6行、AC-01〜06、INV-01〜03、肯定5観点、敵対9観点、変異11件。
- 結果: blocking 0件。record-only 3件（DISC-201・ADV-01・ADV-02）。resolved 6件（F-01〜04、F-06、F-07）。**F-05は根拠つきでinvalidと判定した。** F-06・F-07はPR #1192 の外部reviewが検出した。

## 7. テスト結果

**表を書く前に1本ずつ実行した。**

| 層・検査 | コマンド | 件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---|---:|---:|---:|---|
| unit・integration・e2e | `npm run conformance:check`（内部で`npm test`を実行する） | 1476 | 1460 | 0 | 16 | pass |
| 静的検査 | `lint`・`format:check`・`typecheck`・`source:check`・`architecture:check`・`skills:check`・`package:check`・`docs:format`・`test:format`・`trace:check` | 10 | 10 | 0 | 0 | pass |
| 監査 | `npm run audit:check` | 1 | 1 | 0 | 0 | pass |

### 7.1 変異試験

| ID | 変異 | 結果 |
|---|---|---|
| M1 | fence追跡を消す | kill（3 scenario失敗） |
| M2 | 異なる記号でもfenceを閉じる | kill |
| M3 | 判定列も検査対象にする | **等価。** `pass`へ正規化されるcellは登録語彙を含み得ない |
| M4 | 判定値の内部文字も削る | kill |
| M5 | 循環引用も併記と認める | kill |
| M6 | fence記号の行頭限定を外す | kill |
| M7 | 語彙の直積を1件へ絞る | kill |
| M8 | cellの直積を1件へ絞る | kill（4 scenario失敗） |
| M9 | 閉鎖delimiterの長さ条件を外す | kill |
| M10 | 閉鎖delimiterのinfo string条件を外す | kill |

**10件kill、1件は等価と確認した。** M2とM6は自分の変異試験で当初生存し`SCN-015`・`SCN-016`で塞いだ。**M9・M10はPR #1192 の外部reviewが検出した欠陥に対応する。**

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `scripts/check_file_audit.ts` | **入らない**（`package.json`の`files`が`scripts/`を列挙しない） | 本repositoryのCIでのみ動く |
| `docs/specs/` 3 file | **入る**（`files`が`docs/`を列挙する） | REQ-SQ-031の改訂が配布される |
| `test/` 2 file | 入らない | `files`が列挙しない |

判断: 配布物を更新した

根拠: `docs/`が配布境界に入るためREQ-SQ-031の記述が配布される。**ただし検査そのものは配布されない。** この非対称は #1169 から継続しており、本Issueで解消しない。`npm run package:check`はexit 0である。

## 9. 独立reviewの成立

**reviewerはimplementerと同一である**（`adachi-tatsuru`）。緩和は次の3点である。

1. **本Issueの4 findingはすべて外部reviewer（CodeRabbit）の検出である。** 私は実コードで確認して判定しただけであり、**うち1件は根拠つきでinvalidと判定した。**
2. **変異試験が2件の不足を検出した。** M2とM6は当初生存し、それぞれ新しいscenarioで塞いだ。**コード読解では見つけていない。**
3. 本PRも外部reviewerのreviewを受ける。

## 10. 仕様整合性

- `REQ-SQ-031`を改訂した。fenceの扱い、語彙とcellの直積、判定値正規化の内部文字保持、循環引用の拒否の4条項を追記した。
- 新規SCN 10件（`SCN-UNIT-CLAIMVOCAB-009`〜`018`）を追跡表へ結線した。`trace:check`のorphanは0件である。
- **既存SCNを1件も削除していない。** feature fileへの変更は追記のみである。

## 11. 総合判定と再開地点

**判定: 合格。** 未解決のCritical / Highは0件である。

**本artifact自身が1件で捕まった。** 当初のAC-06行は「判定は純関数である。根拠は（登録語彙の引用）」という**循環引用そのもの**を証拠欄へ書いており、新しい検査が拒否した。**#1169 に続いて2度目の自己検出であり、導入初日に実害を1件防いでいる。** SCN IDの併記へ書き換えた。

残余リスクはADV-01（fenceを開けば残り全体を対象外にできる）とADV-02（無関係な引用が併記として通る）である。**いずれも「機械検査は形式だけを検査する」というREQ-SQ-031の設計上の限界であり、強制点はStep 10の独立reviewerである。**
