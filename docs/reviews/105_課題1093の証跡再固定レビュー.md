# 04 レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1093 |
| ラウンド | Step 10 ラウンド1〜2 |
| 比較基点 | `aacf83c6ff77e892dc7203acd9b0f9646881f497` |
| H_impl | `597897893140f27a7d401cf2b6301e3e02473fd6` |
| 比較基点の由来 | worktree作成時点の`origin/main`のtip |
| モード | full |
| 対象差分 | 10 path。`src/domain/evidence-reanchor.ts`（新規144行）、`src/adapters/evidence-reanchor.ts`（新規187行）、`src/cli.ts`、`src/cli-usage.ts`、仕様3件、test 3件 |
| 対象外 | (a) 既存の孤立記録3件の書き換え。(b) `recordStagingSync`のdigest無検査。(c) ruleset変更。(d) anchorをpatch-id/tree hashへ置き換えること |
| 残り予算 | **1**（同一範囲で最大3ラウンド。総2ラウンドで設計し、CI是正のために予算1を残す） |
| ラウンド数 | 2。ラウンド1は実装差分、ラウンド2は本artifactを加えた版が対象である |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260901_035920_製品が強制するrebaseの後にreview-sessionを再固定する経路が無く証跡が孤立commitを指す |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-005。**着手時点でこの規定はHEADが前進する場合だけを想定し、rebaseで系譜が置き換わる場合を規定していなかった。** |
| 成果物行数 | 製品 **+444 / −0行**（新規331行、既存file +113行）。仕様 **+5 / −1行**。支援層 **+722 / −0行** |
| 縮小の先行評価 | 設計12節で**代替案12件を評価し8件を根拠付きで不採用**にした。blob一致・tree hash・patch-id・`delivery-state.json`へのfield追加・`steps.jsonl`へのentry追加・hash chain・CIゲートへの検知配置・review層の先行実装である。**追加するfileは1件だけに抑えた。** |
| 実施者・日時 | reviewer（fable・codex）、coordinator（claude）、2026-09-01 |

### 0.1 routing入力契約

| role欄 | 必要証拠 | 必要model tier | provider欄 | 独立性証拠欄 |
|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節） | advanced | fableおよびcodex | implementerは**claude**、reviewerは**fableとcodex**。providerとcontextがともに異なる |

**開示する逸脱が2件ある。**

1. **implementerが計画の当初想定と異なる。** `codex exec --full-auto`がhost側のauto mode分類器に拒否され起動できないため、03の0.1節で**implementerをclaudeと先に宣言**したうえで進行役が実装した。**この帰結は既存の #1040 が所有する。**
2. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 本Issueでは権限拡大をscope外としてrisk受容し、解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | https://github.com/techbeansjp-free/AGENTS.md/issues/1093 、AC-1093-01〜21 | Step 8で00〜03を同期し`sync-verified`のcheckpoint 8へ遷移した | 一次資料 |
| 差分 | `aacf83c6..59789789` | **10 file、+1171 / −1行**。製品+444/−0 | 既存コード |
| テスト | `npm test` | `1368 scenarios (1352 passed, 16 skipped)`、失敗0 | テスト出力 |
| 仕様 | `docs/specs/`配下3 file | updated | 既存文書 |

- dependency/authority/evidence graphにcycle、self-loop、unknown nodeがない: **成立する。** `git_object → diff_observation → equivalence → reanchor_record → effective_head → {evidence_match, reachability}`の一方向であり、**等価性判定は再固定記録を引数に取らない**（型で保証）。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: 成立する。

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/evidence-reanchor.ts` | A | package | package | 等価性判定・chain検証・実効HEAD導出・到達性判定の純関数144行。**副作用を持たない** | pass。gitを呼ばない。`stableJson`と`isRecord`だけを読む | REQ-WF-005 / AC-1093-03・04・09・12・14・21 | **記録を等価性の根拠にしない。** rollbackは本fileの削除 | pass |
| `src/adapters/evidence-reanchor.ts` | A | package | package | chainの読み書きと層ごとのanchor導出187行 | pass。既存の`withStagingMutationLock`と`observeReviewDiff`を再利用し新機構を作らない | AC-1093-01・02・05・06・13・15・16・17・20 | **旧headと旧baseを引数で受け取らない。** 既存stateへ書き込まない | pass |
| `src/cli.ts` | M | package | package | 2 commandのdispatchと共通処理83行 | pass | AC-1093-05・16 | `applyMode`で承認を必須にする | pass |
| `src/cli-usage.ts` | M | package | spec | usage契約30行 | pass | AC-1093-05 | 記述のみ | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | project | spec | REQ-WF-005へ再固定の規定を1文 | pass | REQ-WF-005 | 記述のみ | pass |
| `docs/specs/01_システム概要/02_用語・略語.md` | M | project | spec | TERM-ASC-084・085の2行 | pass | 同上 | 記述のみ | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | 追跡2行 | pass | 全SCN | 記述のみ | pass |
| `test/features/unit/evidence-reanchor.feature` | A | package | package | unit scenario 17件 | pass | AC-1093-01〜17・20・21 | fixtureは`initRepo()`と一時directoryに閉じる | pass |
| `test/features/integration/evidence-reanchor.feature` | A | package | package | integration scenario 4件 | pass | AC-1093-10・11・18・19 | 同上 | pass |
| `test/steps/evidence-reanchor.steps.ts` | A | package | package | step定義とfixture 613行 | pass | 全SCN | **本repositoryの実stagingを対象にしない** | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **一致する。** 10 pathが上表の10行と同じである。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **成立する。**

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

**実装中の発見が3件ある。いずれもfixtureまたは検査の欠陥であり、契約は変えていない。**

1. **fixtureの`replay`が等価でなかった。** 新baseが被レビューpathへ触れており、新base→新headの差分が旧base→旧headと変わっていた。**等価性判定は正しく動いており、fixtureの側が誤っていた。** 新baseは別pathだけを変える形へ直した。
2. **CLI契約検査がdispatchの書き方を拒否した。** `command === "pr" || command === "review"`の合併条件では`collectDispatchBlocks`が解釈できず、`usage定義がないsubcommandです: pr`となった。さらにflagをhelperで読むと`usageが必須と宣言したflagを実装が読んでいません`となった。**dispatch blockごとに1 commandとし、flagをblock内で読む形へ直した。製品の検査が正しく効いている。**
3. **変異M6が同一head拒否に隠されていた**（7.1節）。

### 2.1 受け入れ条件とシナリオ

| AC | 内容 | SCN | 観測 |
|---|---|---|---|
| AC-1093-01 | 等価なrebaseで記録が1件積まれる | SCN-UNIT-REANCHOR-001 | 緑。修正前は赤（module不在） |
| AC-1093-02 | 既存の耐久stateが1 byteも変わらない | SCN-UNIT-REANCHOR-002 | 緑。byte比較で観測する |
| AC-1093-03 | 内容が変わったrebaseを拒否する | SCN-UNIT-REANCHOR-003 | 緑。両側のdigestが理由に含まれる |
| AC-1093-04 | 連鎖条件違反の記録を導出に使わない | SCN-UNIT-REANCHOR-004 | 緑 |
| AC-1093-05 | delivery state不在で`pr reanchor`を拒否 | SCN-UNIT-REANCHOR-005 | 緑 |
| AC-1093-06 | delivery state存在で`review reanchor`を拒否 | SCN-UNIT-REANCHOR-006 | 緑 |
| AC-1093-07 | 収束判定が実効HEADと一致する | SCN-UNIT-REANCHOR-007 | 緑 |
| AC-1093-08 | provider観測不能で`unverifiable` | SCN-UNIT-REANCHOR-008 | 緑 |
| AC-1093-09 | 再固定記録なしで判定不変 | SCN-UNIT-REANCHOR-009 | 緑 |
| AC-1093-10 | provider呼び出し0件 | SCN-INT-REANCHOR-001 | 緑 |
| AC-1093-11 | 実git fixtureで到達性を報告 | SCN-INT-REANCHOR-002 | 緑。**本repositoryの実stagingを対象にしない** |
| AC-1093-12 | file modeだけの差を等価としない | SCN-UNIT-REANCHOR-010 | 緑。**blob一致では通ってしまう反例である** |
| AC-1093-13 | 未収束sessionの`review reanchor`を拒否 | SCN-UNIT-REANCHOR-011 | 緑 |
| AC-1093-14 | 旧head==新headを拒否 | SCN-UNIT-REANCHOR-012 | 緑 |
| AC-1093-15 | SHA未解決を拒否 | SCN-UNIT-REANCHOR-013 | 緑 |
| AC-1093-16 | `step11-recorded`以外で両方拒否 | SCN-UNIT-REANCHOR-014 | 緑 |
| AC-1093-17 | 同一記録の再実行が追記しない | SCN-UNIT-REANCHOR-015 | 緑 |
| AC-1093-18 | provider観測が上限1 | SCN-INT-REANCHOR-003 | 緑 |
| AC-1093-19 | 再固定後にbinding検査を通過 | SCN-INT-REANCHOR-004 | 緑 |
| AC-1093-20 | 旧baseを引数で受け取らない | SCN-UNIT-REANCHOR-016 | 緑。source原文を検査する |
| AC-1093-21 | 祖先でなければ`rewritten` | SCN-UNIT-REANCHOR-017 | 緑 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 判定 | 観測 |
|---|---|---|
| DC-PRIVACY | applicable | 証跡の照合対象を変える。**旧baseを利用者から受け取らない設計**により、任意の旧baseで「旧diff＝新diff」を満たす対を作る経路を塞いだ。SCN-UNIT-REANCHOR-016が固定する |
| DC-OBSERVABILITY | applicable | 拒否理由に両側のdiff digestを出す。到達性を三値で返し、観測不能を正常と断定しない。SCN-UNIT-REANCHOR-003・008が観測する |
| DC-UX | not-applicable | project choiceの`humanCenteredUi`が`not-applicable`である |
| DC-TOKENS | not-applicable | project choiceの`designTokens`が`not-applicable`である |

## 3. 肯定的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ | pass | 既存の`observeReviewDiff`を再利用し、新しい比較器を書いていない |
| 価値 | pass | **実測8件中3件の孤立記録を将来分について解消する。** 製品が強制するrebaseから製品の手順で復旧できる |
| 実現可能性 | pass | 追加fileは2件。既存の不変条件を1つも触っていない |
| 整合性 | pass | 既存の`recordReviewRound`と同型のlocked write + read-back経路へ乗せた |
| 保守性 | pass | 等価性判定を1箇所に置き複製していない |

## 4. 敵対的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 反例 | pass | 変異10点がいずれも対象scenarioを殺す（7.1節）。うち2点はラウンド中の是正で殺せるようになった |
| 失敗 | pass | SHA未解決、連鎖違反、command境界違反、未収束、等価性不成立のすべてでfail-closedへ倒れる |
| 境界 | pass | file mode差、同一head、空chain、`step11-recorded`以外の状態を観測した |
| 悪用 | pass | **旧baseを引数で受け取らない。** 記録の申告を根拠にせず消費時にGit objectから再計算する |
| 安全性 | pass | 既存の`journal/steps.jsonl`と`journal/delivery-state.json`へ1 byteも書かない。SCN-UNIT-REANCHOR-002が固定する |
| 損失 | not-applicable | 追記と観測だけで、削除も上書きも行わない |
| 復旧 | pass | 再固定記録の読み取りを撤去すれば従来の判定へ戻る |
| 範囲 | pass | `recordStagingSync`、ruleset、既存の孤立記録に触れていない |

## 5. 指摘

**Step 3で6件、Step 7で8件、実装中に3件。未解決のCritical/Highは0件である。**

| ID | 深刻度 | 種別 | 状態 | 内容 |
|---|---|---|---|---|
| S3-B01 | High | acceptance-violation | resolved | 00が3箇所で「blocker付きsessionの拒否」を要求しているのに、01にそれを覆うFR・BR・ACが1件も無かった。`assertConvergedReviewSession`の`status`検査は**消費時**であり書き込み時の拒否を保証しない |
| S7-B01 | High | acceptance-violation | resolved | **照合点の見落とし。** `pr create`の3経路すべてが通る`assertCurrentReviewJournalBinding`の冒頭（`src/cli.ts:429`）が`assertConvergedReviewSession`より手前で投げる。計画は`:441`と`assertConvergedReviewSession`だけを名指ししており、**`:429`への言及がstaging文書全体で0件だった。** 計画どおり実装するとFR-1093-05が不成立のまま全testが緑になる |
| S7-N04 | High | security | resolved | **`--old-base`を利用者申告のまま受け取る設計だった。** 任意の旧baseを選べると「旧diff＝新diff」を満たす対を作れ、未レビューのbase内容を含むheadへ証跡を移送できる。旧headと旧baseを引数から削り耐久stateから導出する形へ改めた |
| S7-N02 | Medium | improvement | resolved | 変異M2（`changedPaths`比較を外す）が**等価変異**だった。`digest`はfull diff本文のsha256でありpath名もmode行も本文へ埋め込まれている |
| IMPL-01 | Medium | improvement | resolved | fixtureの`replay`が等価でなかった。**等価性判定は正しく動いておりfixtureの側が誤っていた** |
| IMPL-02 | Medium | improvement | resolved | CLI契約検査がdispatchの合併条件とhelperでのflag読みを拒否した。**製品の検査が正しく効いている** |
| IMPL-03 | Medium | vacuous-assertion | resolved | 変異M6が同一head拒否に隠されていた（7.1節） |

## 6. ラウンド固有の確認

### ラウンド1

- 対象は`aacf83c6..59789789`の実装差分である。
- **Step 3とStep 7の readiness check をfableの独立invocationで実施し、方針と設計はfableとcodexの両方へ諮問した。**
- **すべての指摘を受け入れる前に実測で検証した。** S7-B01は`src/cli.ts:425-445`の原文を読み、`:429`が`:433`より手前にあることを確認した。S7-N04は束縛先（`review-convergence.ts:28`、`delivery-state.ts:24`）の実在を確認した。
- **codexがfableの等価性設計を否定した。** blob一致は実行bit・symlink/submodule mode・削除・base側の同一path変更を区別できない。既存`observeReviewDiff`の完全diff digestを再利用する形を採った。

### ラウンド2

- 対象は本artifactを加えた版である。
- 0.1節の逸脱2件、7節の実測値が本文の主張と一致することを確認した。
- **新規findingは0件である。**

## 7. テスト結果

| 検証 | 結果 |
|---|---|
| `npm test` | `1368 scenarios (1352 passed, 16 skipped)`、失敗0 |
| CI同順の全chain | `project:quality`→`quality`→`docs:format`→`test:format`→`trace:check`→`architecture:check`→`build`→`package:check`→`conformance:check`の9 commandを**終了値を明示的に見るループで**完走した |

### 7.1 変異試験

**復元はすべて複写で行い`git checkout`を使わない。**

| 変異 | 内容 | 対象scenarioの結果 |
|---|---|---|
| M1 | `digest`比較を外す | **2件**が赤 |
| M3 | 連鎖条件を外す | 1件が赤 |
| M4 | 同一head拒否を外す | 1件が赤 |
| M5 | `refreshStoredStagingDigest`を呼ばない | 1件が赤 |
| M6 | 冪等判定を外す | 1件が赤 |
| M7 | delivery state検査を外す | 1件が赤 |
| M8 | `converged`検査を外す | 1件が赤 |
| M9 | 観測不能を`reachable`へ倒す | 1件が赤 |
| M12 | 旧baseを引数で受け取れるようにする | 1件が赤 |
| M15 | 到達性述語を反転する | 1件が赤 |

**M5とM6は最初に生存した。等価と決めつけず、等価でない最小入力を1件ずつ特定した。**

- **M5**: `staging-record.json`のartifact一覧が追記へ追随していることを測っていなかった。chainの長さと実効HEADだけを見ていたためである。record内容の検査を足して殺した。
- **M6**: **同一head拒否に隠されていた。** 冪等判定を外すと二回目は`oldHeadSha === input.newHeadSha`で例外になり、chainの長さは1件のままになる。**chainの長さだけを見るとどちらも1件で区別できない。** 「二回目が成功すること」を測る形へ改めて殺した。**assertionが主張と別の量を測っていた。**

### 7.2 差し戻し検証（rollback-validation）

再固定は追記と観測だけで不可逆操作を持たないため、rollback-validationは該当しない。代わりに**再固定記録の読み取りを撤去すると従来の判定へ戻ること**を、AC-1093-09（chainが空なら実効HEADは固定済み記録head）が固定する。

### 7.3 成果物行数と命題の確認

**製品 +444 / −0行、支援層 +722 / −0行で、支援層は製品の約1.63倍である。**

`.agent-skill-chain/docs/00_運用ポリシー.md`が所有する命題「正しさと開発速度の両立」に照らして確認する。開発速度の観測は3点である。

| 観測点 | 判定 |
|---|---|
| 支援層の所要時間が成果物構築の所要時間を上回らない | **21 SCN・securityRelevant=trueのfeatureとして均衡の範囲である。** step定義613行のうちfixture構築が約半分を占める。実git repositoryとdelivery stateとreview sessionを実APIで作る必要があるためである |
| 同一scopeのreviewラウンドが有限で予算内に収束する | 総2ラウンドで設計し予算1を残した |
| **手段の追加より既存手段の縮小を先に評価する** | 設計12節で**代替案12件を評価し8件を不採用**にした。追加fileは1件から2件に留め、比較器・lock機構・hash chainのいずれも新設していない |

## 8. 配布物影響

配布境界へ入る変更pathは次の4件である。いずれも`package.json`の`files`が`dist/src/`として配布対象とする。

- `src/domain/evidence-reanchor.ts`
- `src/adapters/evidence-reanchor.ts`
- `src/cli.ts`
- `src/cli-usage.ts`

判断: 配布物を更新した

根拠: 公開CLIへ`pr reanchor`と`review reanchor`の2 commandを新設する。**既存commandの終了値と出力keyの意味は変えない。** 再固定記録を持たない既存stateの判定は変更前と完全に同一である。

## 9. 独立reviewの成立

- **reviewerとimplementerのstable actor IDは同一である。** 本repositoryの全sessionが単一のアカウントで走る。**providerがclaude・fable・codexで異なることはcontextの独立性であって、identityの独立性ではない。**
- **外部reviewerによる独立approvalは0件である。**
- **したがって11節の`approved`は、人間による独立approvalを伴わないAIの最終裁定である。**
- **これは本PR固有の事情ではなく、本repositoryの恒常的な条件である。** 実行時にimplementer identityとreviewerの独立性を検証する機構が無いことは既存の #1040 が所有する。
- 進行役はS7-B01・S7-N04を受け入れる前に原文で実測し、旧commitがlocalとproviderの両方に残存することも独立に確認した。

## 10. 仕様整合性

| 変更 | 更新先 | 追跡 |
|---|---|---|
| rebaseで系譜が置き換わる場合の再固定 | `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-005 | REQ-WF-005 / AC-WF-005 |
| 用語IDの追加 | `docs/specs/01_システム概要/02_用語・略語.md` | TERM-ASC-084、TERM-ASC-085 |
| 追跡行の追加 | `docs/specs/15_要件追跡/00_追跡表.md` | SCN-UNIT-REANCHOR-001〜017、SCN-INT-REANCHOR-001〜004 |

- **`trace:check`はAC↔SCNの対応の正しさを見ない。** 追加行の各列をscenarioの実体と人が原文で突合した。21件のSCNがすべてFeature fileに実在することを確認した。
- **用語IDの衝突を確認した。** 台帳の現行最大は083で084・085は欠番であり、両IDを使うstagingは本Issueだけであることを他worktreeを含めて確認した。

## 11. 総合判定と再開地点

**approved。** 未解決のCritical/Highは0件、blockingは0件である。**この`approved`は、人間による独立approvalを伴わないAIの最終裁定である**（9節）。

- 再開地点はStep 11の`pr create`である。
- **`pr create`後にmainが動いた場合は、本PRで実装した`pr reanchor`を自分で使う。** 使えないならその事実が最も重いfindingである。
- CIが赤になった場合は残り予算1のラウンドで是正する。**赤を通す理屈を作らない。**
