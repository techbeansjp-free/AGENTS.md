# 87 課題982・998 project choice縮小の拒否診断 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。宣言済みproject choiceを縮小できない件について、**経路そのものを作る変更ではなく、拒否理由が経路の不在を述べていなかった点だけ**を是正した内部監査証拠である。**`valid: true`は候補が既定branchを基準に検証を通過したことを示すだけであり、既定branchへの反映を示さない。**

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象Issue | #982、#998（親 #1025 第1層） |
| 比較基点 | `a2cc8818af9770609f2ae25666db944b34399f29` |
| H_impl | `7b446c937e62e6d9f0fbb1bf448b7e546fad28a4` |
| reviewer | claude（機構の同定と反例1件）。外部諮問としてcodexとfableへ独立に判定させた |
| 実施日 | 2026-08-28 |
| ラウンド数 | 2 |
| Step chain | 迂回: 手書き運用で進めており、workflow recordを経由していない |
| 仕様の所有箇所 | `.agent-skill-chain/docs/01_開発ワークフロー.md`「HumanOverrideの承認元はcandidateの管理境界から分離し、candidate作成前から既定branch上で保護された設定へ置く」。本変更はこの信頼モデルを変えず、**現在の拒否が何を要求しているかを正しく述べるだけである** |
| 成果物行数 | **製品1行**（`src/domain/enforcement.ts`の診断文。注釈6行を含めて+8/-1）。支援層はtest 38行（反例1件）、仕様1行 |
| 縮小の先行評価 | **経路を作る変更（二段階proposalの一般化）は採らなかった。** 両アドバイザーが本命と判定したが、**両者ともowner決裁が要ると判定した**（検知の緩和条件を新設する配布契約変更）。本PRは実装者裁量の範囲に限る |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| **#982と#998は同一機構** | `src/domain/project-choice-diff.ts:270`、`:694-701` | `classifyMonotonicArray`が`testLayers`と`forbiddenTestFileSuffixes`の要素削除を`weakened`とする | 既存コード |
| 拒否点 | `src/domain/enforcement.ts:572` | `ASC-TRUST-001`「project choiceによる検証弱化を防止する」 | 既存コード |
| **拒否理由が経路を示さない** | 同`:582`（比較基点） | 「弱化を取り消すか、検証を強化する方向へ変更してください」だけ | 既存コード |
| **migrationの両経路も同じ理由で拒否する** | 概念migration: `src/domain/enforcement.ts:1202`（`planMigration`が`compareTrustedPolicy`を`compatibility`として抱く）、`:1256`（`applyMigration`が`!plan.compatibility.allowed`で拒否）。file migration: `src/domain/migration.ts:414`（`planFileMigration`がplanへ`compatibility`を載せる）、`:512-514`（適用時に`!compatibility.allowed`で拒否理由へ積む） | **authority診断が案内する「trusted側の先行更新」は、どちらのmigration経路でも実行できない** | 既存コード |
| 候補側の他の承認経路も適用外である | `validateOverride`（`src/domain/enforcement.ts:619`）は個別ruleの`enforceOperation`用でprojectChoices比較へ渡らない。workflow overrideはStep欠落用、role overrideはmodel上限用。`policy evaluate`のusage（`src/cli-usage.ts:512`）に承認flagは無い | **projectChoices縮小を承認できるflag・override・環境変数は無い** | 既存コード |
| 同型の閉塞がもう1件ある | `src/domain/project-choice-diff.ts:860` | `quality.forbiddenTypes`も同じ`classifyMonotonicArray`である | 既存コード |
| 本packageの前例 | `.github/trusted-quality-proposals.json`、`scripts/check_project_quality.ts` | 二段階proposalで同型問題を解いている。**ただし`package.json`の`files`に含まれず、利用側へ配布されていない** | 既存コード |

## 変更ファイル個別監査

| path | status | owner | target layer | 責務・配置 | 依存・循環 | 仕様・追跡 | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/enforcement.ts` | M | package owner | domain（信頼比較） | 弱化拒否の`next`へ、縮小がowner authorityの操作であることと候補側に適用経路が無いことを書く | 変更なし | REQ-SQ-013相当のtrusted比較要件、SCN-UNIT-SAT-015 | 逆変換で戻る。**判定は変えていない** | pass |
| `test/steps/project-policy-satisfiability.steps.ts` | M | package owner | test | 縮小差分の拒否理由を断定する | 実装へ単方向 | SCN-UNIT-SAT-015 | 追加分の除去で戻る | pass |
| `test/features/unit/project-policy-satisfiability.feature` | M | package owner | test | 反例1件 | 実装へ単方向 | 同上 | 4行の除去で戻る | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | 利用側 | 仕様 | SCN-UNIT-SAT-015を既存行へ追記 | 参照のみ | 同上 | 1件の除去で戻る | pass |

## 2. 受け入れ条件の確認

| 条件 | 結果 | 証拠 |
|---|---|---|
| 縮小を試みた利用者が、次に何を要するか分かる | 充足 | 拒否理由が「既定branchのproject policy ownerのauthority操作」「候補側から適用する経路は製品CLIにありません」を含む |
| **存在しない経路を案内しない** | 充足 | 「trusted側で先に更新せよ」だけを返さない。`policy migrate`も拒否することを踏まえた文面である |
| 判定そのものを変えない | 充足 | `classifyMonotonicArray`も`compareTrustedPolicy`の判定も無変更。`npm test`が1014 scenario全通過 |

### 2.1 開発考慮事項の適用判定（必須）

| ID | 判定 | 確認 |
|---|---|---|
| DC-PRIVACY | not-applicable | 扱う情報は変わらない |
| DC-OBSERVABILITY | applicable | **拒否理由が必要なauthorityと経路の不在を述べる。** 無言でも循環案内でもない |
| DC-UX | applicable | 利用側が実際に踏んで停止した経路である |
| DC-TOKENS | not-applicable | UI要素を持たない |

## 3. 肯定的評価

- **経路を作る誘惑を退けた。** 両アドバイザーが本命とした案（二段階proposalの一般化）は、**両者ともowner決裁が要ると判定した**。実装者裁量の範囲だけを実施した
- **誤った定式化を訂正した。** 当初は「保護された既定branchでは構造的に縮小できない」と書いたが、強制点は製品CLIであり、packageはCIへの必須check配線を要求も配布もしていない。正確には「**owner向けの経路が製品外にしかなく、文書化されておらず、AIワークフローから使えない**」である
- **循環案内を作らなかった。** `policy migrate`も同じ比較で拒否することを確認したうえで文面を決めた

## 4. 敵対的評価

| 反例 | 結果 |
|---|---|
| 文言を変えただけで何も解決していない | **成立する。経路は作っていない。** ただし作る変更はowner決裁を要する。本PRは「拒否理由が嘘をつかない」ことだけを保証する |
| 単調性検知を外すべきだ | **不成立。**`testLayers`は`trace validate`のlayer要求に直結し、`forbiddenTestFileSuffixes`は`check_trace.ts`の禁止判定に直結する。縮小は検証要求の実質的縮小になり得る |
| `capabilities`と同じ`not-applicable`形へ揃えるべきだ（#982の要望） | **本PRの範囲外。**`oneOf`追加なら非破壊だが、**#998は解けない**（要素の削除はnot-applicable化ではない）。fableの指摘どおり |
| 「候補側から適用する経路が無い」は言い過ぎだ | **不成立。**`validateOverride`はrule専用、workflow overrideはStep欠落専用、`policy evaluate`に承認flagは無く、`policy migrate`も同じ比較で拒否する。両アドバイザーが独立に確認した |
| 文言をtestで固定していない | **不成立。**SCN-UNIT-SAT-015が固定する。変異で旧文言へ戻すとこの反例だけが落ちる |

## 5. 指摘

| ID | 深刻度 | 内容 | 状態 |
|---|---|---|---|
| S87-H-01 | High | **縮小の承認経路そのものが製品に無い。** 利用側は誤って宣言した要素を、製品の手順では取り除けない | 未是正。**owner決裁が要る**（検知の緩和条件を新設する配布契約変更）。両アドバイザーが本命と判定した二段階proposalの一般化を、canonical Issueとして起票する |
| S87-M-01 | Medium | `authority` fieldの診断も「trusted側で先に更新してください」と案内するが、`policy migrate`が拒否するため製品内では実行できない | 未是正。同じcanonical Issueで扱う |
| S87-M-02 | Medium | `quality.forbiddenTypes`も同じ閉塞を持つ。#982・#998のどちらにも書かれていない | 未是正。canonical Issueの対象へ含める |
| S87-L-01 | Low | 診断文が2文と長い | 未是正。**短くすると経路の不在が落ちる** |

### ラウンド予算

ラウンド2で収束した。未解決のCritical/Highは**1件（S87-H-01）だが、本PRのscope外であり、owner決裁を要する設計判断として分離する。**上限3ラウンドに対して1ラウンドの余裕を残している。

## 6. ラウンド固有の確認

### ラウンド1

全評価基準を確認した。High 1（scope外）、Medium 2、Low 1。**本PRのscope内での新規Critical/Highは0件**。判定 **candidate-verified（自動reviewを待つ）**。

**外部諮問2件を入力にした。** codexは「canonical Issueへ統合し、exact proposalに従う縮小経路を用意する」、fableは「案1（二段階proposalの一般化）＋案5（診断文の是正）」を選び、**両者とも案1にowner決裁が要ると判定した。** 案3（単調性の放棄）と案4（authority分類への付け替え）は両者とも却下した。

**fableが私の定式化を1つ訂正した。**「保護された既定branchでは構造的に経路が無い」は極限では誤りで、強制点は製品CLIであり、packageはCIへの必須check配線を配布していない。ownerは通常のreview PRやbypassで更新できる。**閉じているのは候補側＝AIワークフローに対してであり、それは設計意図どおりである。**

### ラウンド2（自動review）

CodeRabbitがactionable 2件とnitpick 2件を出した。**3件を是正し、1件は事実誤認として採らなかった。**

| 指摘 | 判定 | 対応 |
|---|---|---|
| 追跡表のREQ-WF-008行から存在しない`SCN-UNIT-SAT-011`を削除せよ | **事実誤認。採らない。** 当該行は`SCN-UNIT-SAT-010、SCN-UNIT-SAT-014、SCN-UNIT-SAT-015`であり、011は列挙されていない。`grep`で全feature fileにも存在しないことを確認した | 変更なし |
| 同行の実装欄へ`src/domain/enforcement.ts`を足せ | 妥当。SCN-UNIT-SAT-015は`compareTrustedPolicy`を実行する | 追記した |
| 反例が診断文の片方の節しか固定していない | 妥当 | 「既定branchのproject policyを先に更新してください」も断定へ追加した |
| 根拠を実行経路で辿れる形にし、file migrationと概念migrationを分けよ | 妥当 | 入力証拠の表を`planMigration`・`applyMigration`と`planFileMigration`・その適用の4箇所へ行番号つきで書き換え、承認経路が無いことの根拠も`validateOverride`と`policy evaluate`のusage参照つきにした |
| 「実施日」が未来日である | **採らない。** 日本標準時2026-08-28に実施しており記載は正しい。commitのauthor dateはUTC表記のため前日に見える | 変更なし |

## 7. テスト結果

| 層・検査 | コマンド | 結果 |
|---|---|---|
| 全層 | `npm test` | **1014 scenario全通過** |
| 配布前品質検証（全gate集約） | `npm run verify:distribution` | exit 0 |
| 監査 | `npm run audit:check` | `valid: true` |

**変異試験。** 診断文を比較基点の「弱化を取り消すか、検証を強化する方向へ変更してください」へ戻すと、SCN-UNIT-SAT-015だけが失敗する（`1 scenario (1 failed)`）。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/domain/enforcement.ts` | **入る**（`files`が`dist/src/`を列挙する） | **拒否理由の文言が変わる。判定は変わらない** |
| `test/`、`docs/specs/` | 入らない | `files`が列挙しない |

判断: 配布物を更新した

根拠: `dist/src/domain/enforcement.js`の診断文が変わる。受理・拒否の判定は一切変えていない。`npm run package:check`はexit 0。

## 9. 独立reviewの成立

| 条件 | 充足 |
|---|---|
| reviewerが実装担当の判断を入力に持たない | 充足。実装の分岐、`planMigration`の拒否条件、両アドバイザーの独立判定を入力にした |
| 各ラウンドの判定と根拠が原文引用を伴う | 充足。診断文と`classifyMonotonicArray`の該当行を引用 |
| 有限ラウンドで終了する | 充足。1ラウンドで収束 |
| 外部証拠 | PR作成後のCI結果と自動reviewで補う |

**「経路を作る変更はownerの決裁範囲」と切り分けたことが、この変更で最も効いた判断である。** 両アドバイザーが本命とした案を、**推奨のまま実装しなかった。**

## 10. 仕様整合性

`docs/specs/15_要件追跡/00_追跡表.md`へSCN-UNIT-SAT-015を追記しただけである。要件本文は変更していない。**新しい要件を導入しない。**

## 11. 総合判定と再開地点

**判定: candidate-verified（外部承認待ち）**

- 未解決Critical: 0件
- 未解決High: 1件（S87-H-01。**本PRのscope外。owner決裁を要する設計判断としてcanonical Issueへ分離する**）
- 未解決Medium: 2件（S87-M-01、S87-M-02。同じcanonical Issueで扱う）
- 記録したLow: 1件

再開地点: ステップ11（PR作成）。**merge後にcanonical Issueを起票し、#982・#998から関連付ける。**
