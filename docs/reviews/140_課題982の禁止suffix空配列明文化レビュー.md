# 140 課題982の禁止suffix空配列明文化レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #982 |
| ラウンド | Step 10 ラウンド1 |
| 比較基点 | `7c9c91646d002d01836b8044eea0abcc278fbad0` |
| H_impl | `a0b70d11d75ef498d804d331fef6a3803483da90` |
| 比較基点の由来 | worktree作成時点の`origin/main`のtip |
| モード | full（Q-01がfalse） |
| 対象差分 | 8 file、+79 -3。commitは`a0b70d11` |
| 対象外 | 案(B)（`capabilities`形式への型変更）。**#1206 でowner決裁待ち**。他の単調性2 fieldの記述変更。`docs/reviews/`のrole authority不整合（#1047） |
| 残り予算 | **2**（同一範囲で最大3ラウンド、収束後にHEADが動いたときの取り直しを1回まで） |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260904_091908_禁止テストsuffixの空配列を方針不採用の正規表現として明文化する |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-006（**改訂**） |
| 成果物行数 | 製品 **+2行**（配布schema 2件のdescription）。配布規範 **+2行**（`00_運用ポリシー.md`）。仕様 **+3 -2行**。支援層 **+73行**（feature +5、steps +68）。**支援層/成果物 = 18.3倍** |
| 縮小の先行評価 | **新しい検査を足さない。** 空配列の扱いは`check_trace.ts`が既に実装しており、欠けていたのは記述だけである。**記述の欠落へ機構を足すのは手段の追加である。** 既存`SCN-UNIT-TRACE-009`への相乗りも評価し、採らなかった。009は「拒否する」、010は「受理する」で判定の向きが逆であり、1つにまとめると片方を消す変異が生存する。**支援層が18倍なのはschema記述の変更が2行だからで、絶対値は73行である** |
| 決裁 | 利用側報告 #982 への対応。**案(C)を実施すると #982 のコメントで予告済みであり、指示が無い場合の既定として明示していた** |
| 実施者・日時 | reviewer（claude）、2026-09-04（JST） |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節） | advanced | claude | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerとreviewerが**同一session**である。0.2節に逸脱として開示する。**本Issueの起点は外部の利用側報告である** |

### 0.2 開示する逸脱

1. **implementerとreviewerが同一sessionである。** 緩和は、**要求の出所が外部の利用側projectであること**、起票時の主張を実測で検証して1件を否定したこと、両方向の変異試験を行ったことである。
2. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 起票時主張の検証 | `src/domain/project-choice-shrink.ts` | `SHRINKABLE_FIELD_PATHS`に`projectChoices.forbiddenTestFileSuffixes`が含まれる。**「キーを削除できない」は解消済みだった** | 一次資料 |
| 空配列の実装上の扱い | `scripts/check_trace.ts:138` | `Array.isArray(forbiddenSuffixes) ? forbiddenSuffixes : []`。禁止0件として通る | 一次資料 |
| 記述の不在 | `.agent-skill-chain/schemas/project-choice.schema.json` | `description` が **0件**だった | 実行記録 |
| 縮小経路の記載範囲 | `.agent-skill-chain/` | `schemas/00_利用案内.md`にはあり、`docs/`の規範文書3件には無かった | 実行記録 |
| 非対称の妥当性 | 両schema | `testLayers`は`minItems: 1`、`forbiddenTestFileSuffixes`は持たない | 一次資料 |
| テスト | `npm run conformance:check` | `1494 scenarios (1478 passed, 16 skipped)` | テスト出力 |
| commit前candidate | 8 file | working tree clean | Git index |
| Phase A artifact | `docs/reviews/140_課題982の禁止suffix空配列明文化レビュー.md` | `H_impl` = `a0b70d11`。`H_impl..H_final`の差分pathは本file 1件 | Git観測 |
| commit後external | PR、CI run、review | Step 11で観測する | 外部のimmutable証拠 |

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/schemas/project-choice.schema.json` | M | project | project | `description` を1件足した。**JSON Schemaの注釈keywordであり検証に影響しない** | 適合 | REQ-WF-006 / AC-01・AC-02 / SCN-UNIT-TRACE-010 | `required`・型・`minItems` を変えていない。既存policyはvalidのまま | pass |
| `.agent-skill-chain/schemas/project-policy.schema.json` | M | project | project | 同一の `description` を `$defs.projectChoices` 側へ足した。**利用側がどちらを読むか決まっていない** | 適合 | 同上 | 同上 | pass |
| `.agent-skill-chain/docs/00_運用ポリシー.md` | M | project | project | 単調性を述べる段落の直後へ縮小経路を1段落足した | 適合 | REQ-WF-006 / AC-03 | 記述だけで実行authorityを持たない | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | project | spec | REQ-WF-006へ空配列の扱いを追記した | 適合 | REQ-WF-006 | 同上 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | 新規SCN 1件を結線し、検証欄を実測へ直した | 適合 | 同上 | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | project | spec | 変更履歴を1行足した | 適合 | 同上 | 同上 | pass |
| `test/features/unit/trace.feature` | M | package | package | scenario 1件を追加した。既存9件を1件も変えていない | 適合 | AC-04・AC-05 | fixtureは実fileの読み取りで外部へ到達しない | pass |
| `test/steps/unit.steps.ts` | M | package | package | 対応するstep定義とworld型を追加した | 適合 | 同上 | 同上 | pass |

## 2. 受け入れ条件の確認

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-TRACE-010 | 両schemaの `description` | `1 scenario (1 passed)` | pass | 「空配列は「この方針を持たない」の正規表現である」をassertする。変異M1でkill |
| AC-02 | 原文読解 | 同上 | — | pass | 「`testLayers`と違い`minItems`を持たないのは…」を同一descriptionに書いた |
| AC-03 | 原文読解 | `00_運用ポリシー.md` | — | pass | 単調性3 fieldと二段階手順と`schemas/00_利用案内.md`への参照を書いた |
| AC-04 | SCN-UNIT-TRACE-010 | 変更なし（既存実装） | `1 scenario (1 passed)` | pass | `validatePolicy` が `valid: true` を返す |
| AC-05 | SCN-UNIT-TRACE-010 | 変更なし | 同上 | pass | `minItems` が `undefined` であることをassertする。変異M2でkill |

### 2.2 不変条件

| INV ID | 内容 | 判定 | 証拠 |
|---|---|---|---|
| INV-01 | `required`・型・`minItems` を変えない | pass | `git diff` は両schemaそれぞれ **+1行**（descriptionのみ） |
| INV-02 | `testLayers` の `minItems: 1` を変えない | pass | 差分に現れない。`SCN-UNIT-TRACE-009` が合格を維持する |
| INV-03 | 強制点を足さない | pass | 追加したのはscenario 1件で、門ではない |

## 3. 肯定的評価

- **起票時の主張を実測で検証し、1件を否定した。** 「キーを削除できない」は #1044 の縮小提案で解消済みだった。**報告を全件受け入れると、既に解けている問題へ二重に手段を足す。**
- **受理集合を1 byteも動かしていない。** 差分は両schemaそれぞれ+1行の注釈keywordだけである。既存の利用側policyはすべてvalidのままである。
- **記述そのものをassertした。** 説明の不在が実害だったので、説明を消す変異を殺す形にした。
- **非対称を明文化した。** `testLayers` に `minItems: 1` があり本fieldに無いのは意図的で、報告者の質問への回答でもある。
- **案(B)を勝手に採らなかった。** 単調性3 fieldの型変更は判定・縮小提案・全利用側の移行に及ぶ。**#1206 でowner決裁待ちに置いた。**

## 4. 敵対的評価

| 観点 | 攻撃 | 結果 |
|---|---|---|
| 記述の消失 | `description` を消す | 変異M1でkill |
| 受理集合の縮小 | `minItems: 1` を足す | 変異M2でkill |
| 片側だけの記述 | `project-policy.schema.json` 側を書かない | **両方へ足した。** 利用側がどちらを読むか決まっていない |
| 判定の向きの混同 | `SCN-UNIT-TRACE-009` へ相乗りする | **採らない。** 拒否と受理を1 scenarioへ入れると片方を消す変異が生存する |
| 過剰な強制 | 空配列と非空の一貫性を検査する門を足す | **採らない。** 記述の欠落へ機構を足すのは手段の追加である |
| 理由の欠落 | 空配列は「なぜ持たないか」を宣言できない | **成立する。** 5節ADV-01に記録し #1206 へ委ねる |

## 5. 指摘

| ID | severity | 事実 | 観測 | 由来 | 対処 | 判定 | 残余 |
|---|---|---|---|---|---|---|---|
| F-01 | Major | **空配列の意味がschemaにも規範文書にも書かれていなかった。** 利用側から「設定ミスで空になった」と「意図的に方針を持たない」を区別できない | 利用側報告 #982 | 外部の判断 | 両schemaへ `description` を足し、規範文書へ縮小経路を書いた | valid / resolved | ADV-01 |
| F-02 | — | 「是正しようとしてもキーを削除できない」 | 同上 | 外部の判断 | **valid でない。** `SHRINKABLE_FIELD_PATHS` に含まれ、#1044 の二段階手順で `[]` へ到達できる。**起票後に解消された** | **invalid（解消済み）** | なし |
| F-03 | Minor | **縮小経路の記載が `schemas/00_利用案内.md` にしか無かった。** 規範文書だけを読む利用者に経路が見えない | 走査 | 実測 | `00_運用ポリシー.md` へ書いた | valid / resolved | なし |
| DISC-701 | Low | **`project-choice.schema.json` に `description` が1件も無かった** | 実行記録 | 実測 | 姉妹schemaの既存patternに揃えた | valid / resolved | なし |
| DISC-702 | Low | **JSONを再serializeしてfile全体を再整形し、差分が641行になった** | 実行記録 | 実測 | `git checkout` で戻し、文字列置換へ切り替えた。**差分は2 fileそれぞれ+1行になった** | valid / resolved | なし |
| ADV-01 | Medium | **空配列は「なぜこの方針を持たないか」を宣言できない。** `capabilities` の `{status, reason, evidence}` と違い理由を持てない | 敵対評価 | 敵対評価 | **本Issueでは修正しない。** 型変更は単調性判定・縮小提案・全利用側の移行に及ぶ。**#1206 でowner決裁待ちに置いた** | valid / record-only | 理由なしの空配列が残る |

**未解決のCritical / Highは0件である。**

## 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `.agent-skill-chain/schemas/project-choice.schema.json` | 入る | 利用側が空配列の意味と除去手順をschemaから読める |
| `.agent-skill-chain/schemas/project-policy.schema.json` | 入る | 同上 |
| `.agent-skill-chain/docs/00_運用ポリシー.md` | 入る | 規範文書だけを読む利用者にも縮小経路が見える |
| `docs/specs/02_要件/01_ワークフロー要件.md` | 入らない | project所有の仕様であり利用projectには不在 |
| `docs/specs/15_要件追跡/00_追跡表.md` | 入らない | 同上 |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | 同上 |
| `test/features/unit/trace.feature` | 入らない | `package.json`の`files`に含まれない |
| `test/steps/unit.steps.ts` | 入らない | 同上 |

判断: 配布物を更新した
根拠: 配布schema 2件と配布規範文書1件を更新した。**検証の振る舞いは変わらない。** `description` はJSON Schemaの注釈keywordである。

## 6. ラウンド固有の確認

### ラウンド1

- 対象: 実装差分の8 file。
- 確認: 個別監査8行、AC-01〜05、INV-01〜03、肯定5観点、敵対6観点、変異2件。
- 結果: blocking 0件。record-only 1件（ADV-01）。resolved 4件（F-01・F-03・DISC-701・DISC-702）。**F-02は実測で否定した。**

## 7. テスト結果

**表を書く前に1本ずつ実行した。**

| 層・検査 | コマンド | 件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---|---:|---:|---:|---|
| unit・integration・e2e | `npm run conformance:check`（内部で`npm test`を実行する） | 1494 | 1478 | 0 | 16 | pass |
| 静的検査 | `lint`・`format:check`・`typecheck`・`source:check`・`trace:check`・`architecture:check`・`project:quality`・`cli:check`・`workflow:check`・`package:check`・`docs:format`・`test:format` | 12 | 12 | 0 | 0 | pass |
| 監査 | `npm run audit:check` | 1 | 1 | 0 | 0 | pass |

### 7.1 変異試験

| ID | 変異 | 結果 |
|---|---|---|
| M1 | `description` を消す | kill |
| M2 | `minItems: 1` を足す | kill |

**2件ともkill。** 復元後に再実行して緑を確認している。

## 8. 総合判定と再開地点

**approved。** 未解決のCritical / Highは0件である。再開地点は実装計画1節の順序表。
