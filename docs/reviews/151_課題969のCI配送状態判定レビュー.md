# 151 課題969のCI配送状態判定レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #969 |
| ラウンド | Step 10 ラウンド1 |
| 比較基点 | `f365944eae0bbfb42c4dcb1b092081ef9ceb406d` |
| H_impl | `3444bdedc273761b9295b36a07066635213ffb36` |
| 比較基点の由来 | worktree作成時点の`origin/main`のtip |
| モード | full（Q-01がfalse。拒否診断の文言とadapter観測型が変わる） |
| 対象差分 | 11 file（うちdist 3件は生成物）、commitは`3444bded` 1件 |
| 対象外 | **#1226（CI証拠がworkflow・App・run IDへ拘束されていない）。** `neutral`と`skipped`の成功扱い。自動polling。人間への自動通知。新subcommand。猶予時間の正本宣言。`docs/reviews/`のrole authority不整合（#1047） |
| 残り予算 | **2**（同一範囲で最大3ラウンド。以降は収束後のHEAD移動に対する取り直し1回のみ） |
| ラウンド数 | 1 |
| Step chain | 経由: /home/tatsuru/Projects/techbeansjp-free/AGENTS.md/.worktrees/20260905_162705-969-ci-delivery-inspection/.agent-skill-chain/tmp/issues/20260905_163919_CI-runの観測を成否で絞らず拒否診断へ配送状態を含める |
| 仕様の所有箇所 | `docs/specs/02_要件/03_外部連携要件.md`のREQ-GH-001（**1段落追記**） |
| 成果物行数 | 製品 **+131 -8行**（`ci-delivery.ts` +125新規、`github.ts` +9 -2、`cli.ts` +21 -6）。仕様 **+3行**。支援層 **+195行**（feature +30、steps +165）。**支援層/成果物 = 1.5倍** |
| 縮小の先行評価 | **門を1つも足していない。** queryの絞り込みを外して純関数へ移しただけで、新しい観測経路も新しいsubcommandも足していない。代替4案のうち3案（診断文字列へ直書き、3値の永続化、猶予時間の正本宣言）を棄却した。**支援層が1.5倍なのは、stateだけでなく次の行動の文言まで判定するためである。** stateのみの検査では変異M5が生存する |
| 決裁 | **外部への諮問2者が一致した形。** 永続化・polling・新gateを足さない |
| 実施者・日時 | reviewer（claude）、2026-09-05（JST） |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節） | advanced | claude | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerとreviewerが**同一session**である。0.2節に逸脱として開示する。**設計は外部2者へ諮問した** |

### 0.2 開示する逸脱

1. **implementerとreviewerが同一sessionである。** 緩和は、設計を外部2者へ諮問したことと、判定の根拠をすべて実行結果に置いたことである。
2. **本Issueで私は2回訂正した。** (1) 根拠に挙げた `requiredChecks` は `[]` で空虚だった。(2)「意図したcheckが未生成なら必ず名前が観測集合に現れない」という一般化も誤りだった。**どちらも外部からの指摘で気付いた。** 実際に効いている拒否点は別にあり、そちらの方が強い。
3. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 4状態が潰れていた | `src/adapters/github.ts`（変更前） | query に `status=success` があり、run未生成・実行中・失敗がすべて該当0件になる | 一次資料 |
| 診断が1文だった | `src/cli.ts:1115`（変更前） | `current H_finalと対象PRへ直接関連するsuccessful pull_request CI runがありません` | 一次資料 |
| 実際に効いている拒否点 | `src/cli.ts:1091-1115` | `event === "pull_request"`・head SHA・PR番号・`conclusion === "success"` をすべて要求する。**`requiredChecks` に依存しない** | 一次資料 |
| `requiredChecks` が空 | `.agent-skill-chain/project-policy.json` | `policy.merge.requiredChecks` は `[]`。**私が最初に挙げた根拠は空虚だった** | 実行記録 |
| 判定の正しさ | `SCN-UNIT-CIDEL-001〜007` | 7件合格 | テスト出力 |
| 検出力 | 変異6件 | **すべてkill** | テスト出力 |
| 受理集合の不変 | 拒否条件の差分 | **1行も変えていない。** 成功判定は呼び出し側に残る | Git観測 |
| テスト | `npm run conformance:check` | `1509 scenarios (1493 passed, 16 skipped)`。project rule 21件、orphan 0件 | テスト出力 |
| 追跡 | `npm run trace:check` | valid。orphan 0件 | テスト出力 |
| commit前candidate | 11 file | working tree clean | Git index |
| commit後external | PR、CI run、review | Step 11で観測する | 外部のimmutable証拠 |

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/ci-delivery.ts` | A | package | domain | 配送状態の判定。**純関数であり永続状態もpollingも持たない**（`export function inspectCiDelivery(input: CiDeliveryInput): CiDeliveryInspection` が引数以外を読まず戻り値以外を書かない。SCN-UNIT-CIDEL-001〜007 が値だけで判定する） | domainはadapterを参照しない。循環なし | REQ-GH-001 / AC-01〜AC-06 / SCN-UNIT-CIDEL-001〜007 | **門ではない。** 判定結果でmergeを通すことはない。revertで戻る | pass |
| `src/adapters/github.ts` | M | package | adapter | queryから `status=success` を外し `status` を返す | 適合 | REQ-GH-001 | **取得範囲が広がるだけで呼び出し回数は変わらない** | pass |
| `src/cli.ts` | M | package | cli | 拒否診断へ判定結果を含める。**拒否条件を1行も変えない** | 適合 | REQ-GH-001 / AC-05 | 受理集合を広げない | pass |
| `test/features/unit/ci-delivery.feature` | A | package | test | Scenario 7件 | test層のみ | AC-01〜AC-06 | 副作用なし | pass |
| `test/steps/ci-delivery.steps.ts` | A | package | test | **stateだけでなく次の行動の文言まで判定する** | 同上 | 同上 | 同上 | pass |
| `docs/specs/02_要件/03_外部連携要件.md` | M | project | spec | REQ-GH-001へ1段落追記。**既存本文を1文字も変えない** | 適合 | REQ-GH-001 / AC-GH-001 | 記述のみ | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | 新規SCN 7件の結線 | 適合 | 同上 | 記述のみ | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | project | spec | 変更履歴1行 | 適合 | 同上 | 記述のみ | pass |

**`dist/` 3件は個別監査の対象外である。** `isGeneratedDistributionPath` が生成物を除外する（#1187）。

## 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/domain/ci-delivery.ts`・`src/adapters/github.ts`・`src/cli.ts` | **入る** | 実装の正本 |
| `dist/src/` | **入る** | compile結果 |
| `test/` 2件・`docs/specs/` 3件 | 入らない | `files` に含まれない |

判断: 配布物を更新した
根拠: **配布file数は344になる**（`dist/src/domain/ci-delivery.js` の追加）。`package:check` が合格した。**変わるのは拒否時の診断内容であり、拒否そのものの条件ではない。**

## 2. 差分の要約

| 変更 | 内容 |
|---|---|
| run観測 | `status=success` を外し、`status` を返す |
| 判定 | 純関数 `inspectCiDelivery` を新設 |
| 診断 | 配送状態・該当run件数・head SHA・イベント時刻・観測時刻・経過・猶予時間・**次の行動** |
| 拒否条件 | **変更なし** |

## 3. 肯定review

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ | pass | 7件合格、変異6件すべてkill |
| 価値 | pass | 遅延22分の実測が `pending` に分類され、人間を呼ばない |
| 実現可能性 | pass | 保護fileを変更せず、新しい観測経路も足さない |
| 整合性 | pass | REQ-GH-001 / AC-GH-001 / 新規SCN 7件へ結線し `trace:check` が valid |
| 保守性 | pass | **判定が純関数なので単体testと変異試験にかけられる**（SCN-UNIT-CIDEL-001〜007 が `inspectCiDelivery` を直接呼び、変異6件をkillした） |

## 4. 敵対review

| 反例・攻撃 | 検証 | 結果 |
|---|---|---|
| 猶予ちょうどをどちらへ倒すか | 変異M1（`<=` を `<`） | **kill。** 境界は `pending` 側に含める |
| 別eventのrunを該当にしないか | 変異M2 | **kill** |
| 複数PRに紐づくrunを該当にしないか | 変異M3 | **kill** |
| 別head SHAのrunを該当にしないか | 変異M4 | **kill** |
| 次の行動を取り違えないか | 変異M5（`pending` の文言を `undelivered` と同じに） | **kill（3件）。** stateだけの検査なら生存していた |
| 時刻の逆転を受理しないか | 変異M6 | **kill** |
| 受理集合が広がっていないか | 拒否条件の差分 | **1行も変えていない。** 成功判定は呼び出し側に残る |
| 判定が門になっていないか | 判定の位置 | **拒否が確定した後にだけ呼ぶ。** 判定結果でmergeを通す経路が無い |

## 5. finding分類

| ID | 分類 | 内容 | 対処 |
|---|---|---|---|
| DISC-001 | resolved | step fileの `import` 規約を誤った | 既存step fileに合わせた |
| DISC-002 | resolved | 引数を取る `Then` は `function (this: World, arg)` にしないと `this` が `any` になる | 型注釈を足した |
| ADV-01 | record-only | **私が本Issueで2回訂正した。** `requiredChecks` が空であること、「未生成なら必ず名前が現れない」が一般には成り立たないこと | **どちらも外部からの指摘で気付いた。** 実際に効いている拒否点を特定し直した |
| ADV-02 | record-only | **判定は出力へ載るだけで進行役の行動を強制しない。** 規約であって機構ではない | owner決裁「強制は基本使わない」に照らすと、ここへ門を置く根拠が無い。**安全側は既にfail-closedである** |
| ADV-03 | record-only | CI証拠がworkflow・App・run IDへ拘束されていない | **本Issueとは別の欠陥。** #1226 へ分離した |

**blocking 0件。未解決のCritical / High 0件。**

## 6. 検証結果

| 検査 | 結果 |
|---|---|
| `npm run conformance:check` | `1509 scenarios (1493 passed, 16 skipped)`。project rule 21件、orphan 0件 |
| `npm run lint` / `format:check` / `typecheck` / `source:check` | すべて合格 |
| `npm run project:quality` | `valid: true` |
| `npm run directories:check` / `skills:check` / `cli:check` / `workflow:check` | すべて合格 |
| `npm run docs:format` / `test:format` / `package:check` / `architecture:check` | すべて合格 |
| `npm run trace:check` | valid。orphan 0件 |
| `npm run audit:check` | Step 11直前に実行する |

### 変異試験

| ID | 変異 | 結果 |
|---|---|---|
| M1 | 境界を `<=` から `<` へ | **kill** |
| M2 | `event === "pull_request"` の絞り込みを消す | **kill** |
| M3 | PR番号の一意性検査を消す | **kill** |
| M4 | head SHAの照合を消す | **kill** |
| M5 | `pending` の次の行動を `undelivered` と同じにする | **kill（3件）** |
| M6 | 観測時刻の前後関係の検査を消す | **kill** |

**6件ともkill。** 変異は複写で戻し、復元を確認した。

## 7. 仕様更新

- **REQ-GH-001へ1段落追記した。** 成否で絞らないこと、3値の定義、次の行動、イベント時刻の出所、**強制主体が新規SCN 7件であること**を明記した
- **新規SCN 7件を追跡表へ結線した。** `trace:check` の orphan は0件
- **既存本文を1文字も変えていない。既存SCNも1件も削除していない**

## 8. 判定

**承認。** blocking 0件、未解決のCritical / High 0件。resolved 2件（DISC-001・002）、record-only 3件（ADV-01〜03）。
