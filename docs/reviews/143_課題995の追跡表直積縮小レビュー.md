# 143 課題995の追跡表直積縮小レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #995 |
| ラウンド | Step 10 ラウンド1 |
| 比較基点 | `29ff66c6eae42a572ec5b9a73fff387e546bd0ea` |
| H_impl | `7976c3208ed90836282895abada14f7ea4e465c1` |
| 比較基点の由来 | worktree作成時点の`origin/main`のtip |
| モード | full（Q-01がfalse） |
| 対象差分 | 6 file、+44 -8。commitは`7976c320` |
| 対象外 | `04_レビュー.md`（**既に縮小済み**）。本repository自身の追跡表（**元から直積でない**）。`check_trace.ts`の判定。`docs/reviews/`のrole authority不整合（#1047） |
| 残り予算 | **2**（同一範囲で最大3ラウンド、収束後にHEADが動いたときの取り直しを1回まで） |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260904_125729_追跡表templateのtest層直積要求を担保する層だけへ縮小する |
| 仕様の所有箇所 | `docs/specs/02_要件/04_仕様・品質管理要件.md`のREQ-SQ-008（**改訂**） |
| 成果物行数 | 配布template **+10 -4行**。仕様 **+2 -1行**。支援層 **+32 -3行**（既存scenarioの延長のみ）。**支援層/成果物 = 3.2倍。絶対値は32行** |
| 縮小の先行評価 | **本Issueそのものが縮小である。追加する手段は無い。** 新規SCNを足す案を評価し、採らなかった。`SCN-UNIT-PACKAGE-008` が既に同じtemplateの同じ性質を見ており、分けると同じfileを2度読む。**強制点も足していない** |
| 決裁 | 利用側報告 #995 への対応。**運用ポリシーが明示的に推奨する「手段の縮小」の方向である** |
| 実施者・日時 | reviewer（claude）、2026-09-04（JST） |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節） | advanced | claude | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerとreviewerが**同一session**である。0.2節に逸脱として開示する。**本Issueの起点は外部の利用側報告である** |

### 0.2 開示する逸脱

1. **implementerとreviewerが同一sessionである。** 緩和は、**要求の出所が外部の利用側projectであること**、本repository自身の運用を実測して根拠にしたこと、両方向の変異試験を行ったことである。
2. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** 解消を #1047 へ委譲する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 直積要求の実在 | `templates/specs/15_要件追跡/00_追跡表.md` | 「選択した全層を、同じ要件・受け入れ条件・SCN IDに対して1層1行で記録する」 | 一次資料 |
| 同上 | `templates/issue/03_実装計画.md` | 「全選択層を同じSCN IDへ1層1行で対応付ける」 | 一次資料 |
| 既に縮小済みの先例 | `templates/issue/04_レビュー.md` | 「**失敗またはskipがある層だけ**を…展開する。全層を毎回書き出さない」 | 一次資料 |
| 本repositoryの運用 | `docs/specs/15_要件追跡/00_追跡表.md` 冒頭 | 「1行は1つのFeature完全pathとtest layerを表す」 | 一次資料 |
| 同上の実測 | 同file 184行 | 層は単一。`unit` 85、`integration` 82、`e2e` 17。**直積ではない** | 実行記録 |
| 判定の非強制 | `scripts/check_trace.ts` | 本repositoryの非直積な表が合格している。**直積を強制していない** | 実行記録 |
| 利用側の実害 | 報告本文 | 21 SCNのうち**14件が両層とも非適用** | 外部の判断 |
| テスト | `npm run conformance:check` | `1495 scenarios (1479 passed, 16 skipped)` | テスト出力 |
| commit前candidate | 6 file | working tree clean | Git index |
| Phase A artifact | `docs/reviews/143_課題995の追跡表直積縮小レビュー.md` | `H_impl` = `7976c320`。`H_impl..H_final`の差分pathは本file 1件 | Git観測 |
| commit後external | PR、CI run、review | Step 11で観測する | 外部のimmutable証拠 |

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/templates/specs/15_要件追跡/00_追跡表.md` | M | project | project | 表cellと本文の要求を担保する層だけへ縮小した。**本repository自身の冒頭文と同じ形に揃えた** | 適合 | REQ-SQ-008 / AC-01・AC-03 / SCN-UNIT-PACKAGE-008 | 記述だけで実行authorityを持たない。**既存の追跡表は縮小後の要求も満たすため利用側の移行作業が発生しない** | pass |
| `.agent-skill-chain/templates/issue/03_実装計画.md` | M | project | project | 同じ向きへ揃えた。層分割への罰になる点も書いた | 適合 | REQ-SQ-008 / AC-02・AC-03 / 同上 | 同上 | pass |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | M | project | spec | REQ-SQ-008へ縮小後の要求を追記した | 適合 | REQ-SQ-008 | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | project | spec | 変更履歴を1行足した | 適合 | 同上 | 同上 | pass |
| `test/features/unit/review-policy-package.feature` | M | package | package | 既存scenarioの説明文を新しい性質へ直した。**scenario数を増やしていない** | 適合 | AC-05 | fixtureは実fileの読み取りで外部へ到達しない | pass |
| `test/steps/unit.steps.ts` | M | package | package | 実装計画templateの読み取りを足し、新旧両方のassertを置いた | 適合 | AC-01〜AC-05 | 同上 | pass |

## 2. 受け入れ条件の確認

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-PACKAGE-008 | 追跡表template | `1 scenario (1 passed)` | pass | `担保する層だけ`・`直積にしない` をassertする。変異M1でkill |
| AC-02 | SCN-UNIT-PACKAGE-008 | 実装計画template | 同上 | pass | `担保する層だけを1層1行で対応付ける`・`直積にしない` をassertする。変異M2でkill |
| AC-03 | SCN-UNIT-PACKAGE-008 | 両template | 同上 | pass | `代替の検証手段`・`SCN単位で1回だけ書く` をassertする |
| AC-04 | SCN-UNIT-PACKAGE-008 | 両template | 同上 | pass | 旧文言の**否定assert**を2件置いた。**併記する変更も落ちる** |
| AC-05 | SCN-UNIT-PACKAGE-008 | — | 同上 | pass | 既存scenarioを延ばした。新規SCNは足していない |

### 2.2 不変条件

| INV ID | 内容 | 判定 | 証拠 |
|---|---|---|---|
| INV-01 | 担保漏れの検出という目的を失わない | pass | 担保がある組み合わせは引き続き記録し、担保が無いSCNには代替手段の記録を要求する |
| INV-02 | `check_trace.ts` の判定を変えない | pass | 差分に現れない。`trace:check` のorphanは0件 |
| INV-03 | `04_レビュー.md` と本repository自身の追跡表を変えない | pass | いずれも差分に現れない |

## 3. 肯定的評価

- **自分の実践を根拠にした。** 本repositoryの追跡表184行はすべて単一層で、冒頭文も「1行は1つのFeature完全pathとtest layer」である。**templateだけが自分の実践と違うことを要求していた。**
- **報告の3 fileのうち1件は対応不要だった。** `04_レビュー.md` は既に縮小済みで、**同じ向きの先例が同じtemplate群の中にあった。**
- **否定assertを置いた。** 新旧を併記する変更は肯定assertだけでは通る。
- **新規SCNを足さなかった。** 既存 `SCN-UNIT-PACKAGE-008` が同じ対象・同じ向きである。
- **利用側の移行作業が発生しない。** 既存の追跡表は縮小後の要求も満たす。**縮小は受理集合を広げる方向であり、後方互換である。**

## 4. 敵対的評価

| 観点 | 攻撃 | 結果 |
|---|---|---|
| 要求の復活 | 追跡表templateを直積へ戻す | 変異M1でkill |
| 要求の復活 | 実装計画templateを直積へ戻す | 変異M2でkill |
| 併記 | 新旧の両方を書く | **否定assertでkill** |
| 検出の喪失 | 担保がある層も書かなくてよいと読む | **代替手段の記録要求が残る。** ただし「担保できるのに書かない」は機械で検出できない。5節ADV-01 |
| 片側だけの是正 | 追跡表だけ直して実装計画を残す | **両方をassertした** |
| 過剰な強制 | 担保層の妥当性を検査する門を足す | **採らない。** 何が層かはpackageが定義しない（#998・#1208） |

## 5. 指摘

| ID | severity | 事実 | 観測 | 由来 | 対処 | 判定 | 残余 |
|---|---|---|---|---|---|---|---|
| F-01 | Major | **配布templateが直積を要求し、追跡表の大半が非適用行で埋まっていた。** 利用側で21 SCN中14件が両層とも非適用 | 利用側報告 #995 | 外部の判断 | 担保する層だけの記録へ縮小した | valid / resolved | ADV-01 |
| F-02 | Medium | **template群の中で要求が揃っていなかった。** `04_レビュー.md` だけ縮小済みだった | 走査 | 実測 | 2 fileを同じ向きへ揃えた | valid / resolved | なし |
| DISC-1002 | Medium | **本repository自身の追跡表は元から直積でない。** templateだけが自分の実践と違うことを要求していた | 実測 | 実測 | 縮小後の文言を本repositoryの冒頭文と同じ形にした | valid / resolved | なし |
| DISC-1003 | Low | **既存assertが旧文言を固定していた。** 縮小すると既存scenarioが落ちる | scenario実行 | 機構 | assertを新しい性質へ差し替え、否定assertも足した。**機構が「片方だけ直す」を止めた** | valid / resolved | なし |
| ADV-01 | Medium | **「担保できるのに書かない」は機械で検出できない。** 担保層の判断が利用側に委ねられる | 敵対評価 | 敵対評価 | **強制点を足さない。** 何が層かをpackageは定義しない（#998・#1208）。Step 10のreviewerが見る | valid / record-only | 過少申告の検出は人手に残る |

**未解決のCritical / Highは0件である。**

## 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `.agent-skill-chain/templates/specs/15_要件追跡/00_追跡表.md` | 入る | 利用側の追跡表から非適用行が消え、担保漏れが見つけやすくなる |
| `.agent-skill-chain/templates/issue/03_実装計画.md` | 入る | 同上。層を細かく分ける設計判断が採りやすくなる |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | 入らない | project所有の仕様であり利用projectには不在 |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | 同上 |
| `test/features/unit/review-policy-package.feature` | 入らない | `package.json`の`files`に含まれない |
| `test/steps/unit.steps.ts` | 入らない | 同上 |

判断: 配布物を更新した
根拠: 配布template 2件を更新した。**受理集合を広げる方向であり後方互換である。** 既存の追跡表は縮小後の要求も満たす。

## 6. ラウンド固有の確認

### ラウンド1

- 対象: 実装差分の6 file。
- 確認: 個別監査6行、AC-01〜05、INV-01〜03、肯定5観点、敵対6観点、変異2件。
- 結果: blocking 0件。record-only 1件（ADV-01）。resolved 4件（F-01・F-02・DISC-1002・DISC-1003）。

## 7. テスト結果

**表を書く前に1本ずつ実行した。**

| 層・検査 | コマンド | 件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---|---:|---:|---:|---|
| unit・integration・e2e | `npm run conformance:check`（内部で`npm test`を実行する） | 1495 | 1479 | 0 | 16 | pass |
| 静的検査 | `lint`・`format:check`・`typecheck`・`source:check`・`trace:check`・`architecture:check`・`project:quality`・`cli:check`・`workflow:check`・`package:check`・`skills:check`・`docs:format`・`test:format` | 13 | 13 | 0 | 0 | pass |
| 監査 | `npm run audit:check` | 1 | 1 | 0 | 0 | pass |

### 7.1 変異試験

| ID | 変異 | 結果 |
|---|---|---|
| M1 | 追跡表templateを直積要求へ戻す | kill |
| M2 | 実装計画templateを直積要求へ戻す | kill |

**2件ともkill。** 復元後に再実行して緑を確認している。

## 8. 総合判定と再開地点

**approved。** 未解決のCritical / Highは0件である。再開地点は実装計画1節の順序表。
