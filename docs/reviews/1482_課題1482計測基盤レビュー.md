# 04 レビュー

> 肯定と敵対の両観点を毎ラウンド確認する。指摘なしの承認は有効。Medium/Lowは記録するだけで、自動修正・追加ラウンド・ゲート停止を起こさない。用語と責務の境界は`成果物用語と責務境界`（`.agent-skill-chain/docs/01_開発ワークフロー.md`）を正本とする。
>
> 埋めた成果物は`docs/reviews/`または`.agent-skill-chain/reviews/`へ置き、実装commitの後にその1 fileだけをcommitする。

| 読者 | 読む節 |
|---|---|
| 発注・評価する人 | 要約 → §5 指摘 → §11 総合判定 |
| 実装・レビューする人 | 全節 |
| 運用する人 | 要約 → §8 配布物影響 → §10 仕様整合性 |

## 要約

| 項目 | 内容 |
|---|---|
| 何が問題だったか | Step/role/model待ち/review/deterministic toolの所要時間内訳を既存記録から再構成する手段が無く、「正しいが遅い」問題を定量化できなかった（Issue #1482）。 |
| 何を解決しようとしたか | 既存journal（`journal/steps.jsonl`・`review-session*.json`）の再利用に加え、role/model/deterministic tool境界を記録する`workflow mark`と、7指標（step_ms・role_ms・model_ms・deterministic_ms・review_rounds・support_ms・artifact_build_ms）を算出する`workflow metrics`を追加した。 |
| 何を行ったか | `src/domain/metrics.ts`・`src/adapters/metrics-journal.ts`の新規追加、`src/cli.ts`/`src/cli-usage.ts`への2 subcommand追加、`docs/specs/`4fileの更新、unit/integration Gherkin test 23 scenario追加（unit 13、integration 10）。3ラウンドの独立review（別Agent、別session/context）で発見したH1〜H3・M1〜M3・L1〜L3・新規指摘を全て是正した。 |
| 何を確認したか | `npm test`（project全体のcucumber suite）、`npm run quality`相当の静的検査一式、`npm run conformance:check`、手動mutation test 6件、実CLIでの手動smoke test、3ラウンドの独立review。§3・§4・§7に詳細を記す。 |
| 判定 | approved（§11参照） |

## 1. 入力証拠

PR番号、Actions run ID、immutable review IDはPR作成後にしか存在しないため**この文書へ書かない。** `review evidence`とdelivery stateがappend-onlyで保持する。reviewerの独立性は`merge.reviewIndependence`が決める。既定の`context-isolated`はimplementerと別session/context、exact HEAD固定、対象差分の非変更、肯定・敵対reviewとfinding記録を要求し、同一GitHub actorでも成立する。このときtracked artifactの`approved`と保存済みreview session・Step 10 bindingがformal approvalになる。`actor-independent`はPR authorおよび観測済み`H_impl` commit authorと別のstable actor IDによるprovider `APPROVED`を要求する。両modeともexact HEAD一致は必須とし、tracked文書へ自身のcommit SHAを書かず、**H_final後はartifactを更新しない。**

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | `.agent-skill-chain/tmp/issues/20260925_170531_v0.4.0-計測基盤/00_要求定義.md`〜`03_実装計画.md` | AC-1482-01〜07、INV-01〜04、FR-1482-01〜05、BR-1482-01〜03 | 既存コード |
| 差分 | `bb16faba15027304a11071631ddb6d21cf4c7d7a`..`15069d1d3f7f065e20e7fbfd6868a204c0fd0798` | 18 path（`dist/`4件、`docs/specs/`6件、`src/`4件、`test/`4件） | 既存コード |
| テスト | `npm test`（project全体）、`node --import tsx .../cucumber.js --config cucumber.mjs --name "SCN-MT-1482-\|SCN-INT-SPECNORM-001"` | §7参照 | テスト出力 |
| 仕様 | `docs/specs/01_システム概要/02_用語・略語.md`（TERM-ASC-131〜133）、`docs/specs/02_要件/00_要件一覧.md`・`01_ワークフロー要件.md`（REQ-WF-026・027）、`docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`、`docs/specs/14_開発・品質/00_ディレクトリ構成.md`、`docs/specs/15_要件追跡/00_追跡表.md` | updated | 既存文書 |
| commit前candidate | dist/src/adapters/metrics-journal.js、dist/src/cli-usage.js、dist/src/cli.js、dist/src/domain/metrics.js、docs/specs/01_システム概要/02_用語・略語.md、docs/specs/02_要件/00_要件一覧.md、docs/specs/02_要件/01_ワークフロー要件.md、docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md、docs/specs/14_開発・品質/00_ディレクトリ構成.md、docs/specs/15_要件追跡/00_追跡表.md、src/adapters/metrics-journal.ts、src/cli-usage.ts、src/cli.ts、src/domain/metrics.ts、test/features/integration/metrics-cli.feature、test/features/unit/metrics.feature、test/steps/metrics-cli.steps.ts、test/steps/metrics.steps.ts | H_impl 15069d1d3f7f065e20e7fbfd6868a204c0fd0798 | Git index |
| Phase A artifact | `docs/reviews/1482_計測基盤レビュー.md` | 本fileを実装commitの後に単独commitする予定（H_final） | Git観測 |
| review session | `.agent-skill-chain/tmp/issues/20260925_170531_v0.4.0-計測基盤` | round 1、`review round --init --head=15069d1d...` | Git観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: 該当するgraphは無い（本変更はauthority/evidence graphの新規edgeを持たない。02 §2.3のdomain→adapter→CLIの一方向依存のみ）
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_impl=15069d1d...`。本artifactをcommitして`H_final`とする予定であり、差分は本file1件のみになる
- reviewerの独立性が要求水準を満たす: `context-isolated`。3ラウンドとも別Agent呼び出し（別session/context、model=opus、implementer=Claude Sonnet 5との別identity）。§9参照
- 既定branch追随を行った場合、取り込みがartifact commitより前にあり、`比較基点`が取り込んだ既定branch tip、`H_impl`がartifact直前の最新commitを指し、個別監査表を`比較基点..H_impl`から再生成した: 既定branch追随は発生していない（`origin/main`は着手時から`bb16faba...`のまま）

### 1.1 変更ファイル個別監査

基準SHAとの差分にある全ファイルを、生成物（`dist/`等）も含めて1ファイル1行で記録する。まとめ行、directory単位の一括承認、test成功だけの代替を認めない。`audit:check`の他の検査対象外となる生成物でも、各行へ生成元との対応確認方法と配布影響の確認方法を記録し、差分path集合と表のpath集合を一致させる。

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `dist/src/adapters/metrics-journal.js` | A | package owner | 生成物 | `src/adapters/metrics-journal.ts`のbuild出力。`npm run build`のclean差分で対応確認済み | 生成元 → 生成物（pass） | AC-1482-03〜07 | 配布影響は§8の配布物影響表で確認（`dist/`は配布対象、`package:check`で新規開発専用資産の混入なしを確認済み）。revertは`src/`のrevert後に`npm run build`で再生成すれば足りる | pass |
| `dist/src/cli-usage.js` | M | package owner | 生成物 | `src/cli-usage.ts`のbuild出力 | 生成元 → 生成物（pass） | AC-1482-06 | 配布影響は§8参照（上行と同一根拠） | pass |
| `dist/src/cli.js` | M | package owner | 生成物 | `src/cli.ts`のbuild出力 | 生成元 → 生成物（pass） | AC-1482-01〜07 | 配布影響は§8参照（上行と同一根拠） | pass |
| `dist/src/domain/metrics.js` | A | package owner | 生成物 | `src/domain/metrics.ts`のbuild出力 | 生成元 → 生成物（pass） | AC-1482-01〜05 | 配布影響は§8参照（上行と同一根拠） | pass |
| `docs/specs/01_システム概要/02_用語・略語.md` | M | package owner | docs/specs | TERM-ASC-131〜133を新規登録。既存用語IDは変更していない | spec → src（pass、既存用語の再定義なし） | 01 §2.1 | システム仕様書。revertは該当3行の削除のみ | pass |
| `docs/specs/02_要件/00_要件一覧.md` | M | package owner | docs/specs | REQ-WF-026・027を追加（SCN-INT-SPECNORM-001の二重列挙検査で必須と判明。round2で追加） | spec → src（pass） | REQ-WF-026・027 | システム仕様書。revertは該当2行の削除のみ | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | package owner | docs/specs | REQ-WF-026・027本文を追加 | spec → src（pass） | REQ-WF-026・027 | システム仕様書。revertは該当節の削除のみ | pass |
| `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | M | package owner | docs/specs | `workflow mark`・`workflow metrics`の契約行を追加 | spec → src（pass） | AC-1482-03〜07 | システム仕様書。revertは該当2行の削除のみ | pass |
| `docs/specs/14_開発・品質/00_ディレクトリ構成.md` | M | package owner | docs/specs | `.agent-skill-chain/metrics/<staging名>/events.jsonl`と新規src fileのdirectory契約を追加 | spec → src（pass） | 02 §4.3（H1是正） | システム仕様書。revertは該当行の削除のみ | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package owner | docs/specs | REQ-WF-026・027の追跡行を追加 | spec → src（pass） | SCN-MT-1482-*全件 | システム仕様書。revertは該当3行の削除のみ | pass |
| `src/adapters/metrics-journal.ts` | A | package owner | src/adapters | staging boundary検証・filesystem I/O（`assertWorkflowStaging`等既存関数を再利用）だけを担う。domain判定を再実装していない | domain → adapter → cli（pass、`src/domain/metrics.ts`をimportする側であり逆方向は無い。SCN-MT-1482-011で静的確認） | FR-1482-03〜05、AC-1482-03・04・06 | 新規fileのみ。revertはfile削除で既存機構に影響しない（H1是正によりstaging外へ配置したため、既存staging digestにも影響しない） | pass |
| `src/cli-usage.ts` | M | package owner | src | `workflow mark`・`workflow metrics`のusage定義を追加。既存subcommandのusageは変更していない | pass | AC-1482-03〜06 | 追記のみ。revertは追加した2 objectの削除のみ | pass |
| `src/cli.ts` | M | package owner | src | `workflow mark`・`workflow metrics`のdispatch blockを追加。boolean flag set（既存`workflowArguments`）へ`out`を追加（既存の他subcommandは`flags.out`を読まないため無害）。`presentFlag`ヘルパーを再利用 | pass | AC-1482-03〜06 | 追記が中心。revertは追加blockと`out`エントリの削除のみ | pass |
| `src/domain/metrics.ts` | A | package owner | src/domain | 計測イベントのparse/検証、step_ms・review_rounds・role_ms等の算出を担う純関数群。filesystem/processを参照しない（SCN-MT-1482-011で確認） | 依存無し（`workflow.ts`・`review-convergence.ts`・`role.ts`の型/関数を読むだけ） | FR-1482-01〜05、INV-01〜04 | 新規fileのみ。revertはfile削除で既存機構に影響しない | pass |
| `test/features/integration/metrics-cli.feature` | A | package owner | test | CLI統合test（AC-1482-06・07、H1/H2回帰確認を含む10 scenario） | test → 対象（pass） | SCN-MT-1482-006・007・010・016・017・019〜023 | test追加のみ。revertはfile削除でtest coverageが減るだけ | pass |
| `test/features/unit/metrics.feature` | A | package owner | test | domain関数のunit test（13 scenario） | test → 対象（pass） | SCN-MT-1482-001〜005・008・009・011〜015・018 | test追加のみ | pass |
| `test/steps/metrics-cli.steps.ts` | A | package owner | test | 上記integration test用step定義。`main()`を直接呼び出しCLI dispatchを実exerciseする | test → 対象（pass） | 同上 | test追加のみ | pass |
| `test/steps/metrics.steps.ts` | A | package owner | test | 上記unit test用step定義 | test → 対象（pass） | 同上 | test追加のみ | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: はい（`review artifact --init`が生成した18 pathとこの表のpath集合が一致）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: はい。新規fileはすべて`src/domain/`・`src/adapters/`（package owner領域）に置き、project policy・project choiceへは触れていない
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: 3ラウンドの独立reviewで発見した指摘は、該当fileと直接の依存先（`src/domain/metrics.ts`↔`src/adapters/metrics-journal.ts`↔`src/cli.ts`、および対応するtest）だけを都度再監査した

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

発見IDは実装計画（fullは03、quick/pocは集約00）の`DISC-*`と同じ字面を使い、本文書内で別IDへ言い換えない。

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-1482-01 | Q-01根拠は「SKILL.mdへの追記が必要になる可能性がある」と記していたが、02の設計判断（自動計装は不採用）により、実装はSKILL.mdを変更せず完結した | なし（モード判定は他の根拠で既にfalse） | なし | scope外として確定、対処不要 | 03 §4.1参照 | no-spec-impact | pass |
| DISC-1482-02 | round1独立review（H1）が、`journal/metrics-events.jsonl`をstaging配下に置く当初設計はstaging digest検査を壊すことを発見 | AC-1482-03・06の実装箇所変更。AC・FR・INVの実質は不変 | design-responsibility | staging外`.agent-skill-chain/metrics/<staging名>/events.jsonl`へ移設。01・02・docs/specsを再確定 | SCN-MT-1482-017（digest不変を直接検証）、round2・round3の実コード確認 | updated（TERM-ASC-131、REQ-WF-026・027） | pass |
| DISC-1482-03 | 01のAC-1482-06「非ゼロの実測値」はIssue本文・memoに無い、著者が追加した過剰な限定と判明（`workflow assess-discovery`で`changesAcceptanceCriteria=true`と確定） | AC-1482-06のpass基準が変わる（0を含む実測値もpassとする） | requirement | 01 §9のAC-1482-06観測方法欄を修正 | 03のDISC-1482-03記録、`workflow assess-discovery`実行結果 | no-spec-impact（AC文言はdocs/specsへ転記していない） | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1482-01 | SCN-MT-1482-001 | `computeStepDurationsMs`（metrics.tsの算出関数） | pass | pass | §7 |
| AC-1482-02 | SCN-MT-1482-002 | `computeReviewRounds`（metrics.tsの算出関数） | pass | pass | §7 |
| AC-1482-03 | SCN-MT-1482-003、019 | `validateNextMetricsEvent`・`workflow mark`（metrics.ts、metrics-journal.ts、cli.ts） | pass | pass | §7 |
| AC-1482-04 | SCN-MT-1482-004 | 同上（INV-04経路） | pass | pass | §7 |
| AC-1482-05 | SCN-MT-1482-005、012〜015、018 | `computeSupportArtifactSplit`・`computeMetricsWindowMs`（metrics.ts） | pass | pass | §7 |
| AC-1482-06 | SCN-MT-1482-006、016、017、020〜023 | `workflow metrics`（metrics-journal.ts、cli.ts）＋本Issue自身のstagingでの実測（03 §10 T05） | pass | pass | §7、03 §10 |
| AC-1482-07 | SCN-MT-1482-007 | repository grep（コード変更なし） | pass | pass | 03 §10 T05のM-02記録 |

### 2.2 開発考慮事項の適用判定（必須）

00の判定・理由・証拠から差分が無ければ、表の代わりに`開発考慮事項の適用判定は00_要求定義.md §6.1と同じ`の1行を置ける（01〜03と同じ参照行）。差分がある行だけを表に残してよい。**`review validate`はこの§2.2の内容を検証しない**（01〜03の`issue validate`と異なり、review artifactのDC判定に対する機械検証は無い）。記述量を減らすための人・エージェント向けの案内であり、参照行を置いても4行の表を書いても合否は変わらない。

開発考慮事項の適用判定は00_要求定義.md §6.1と同じ

## 3. 肯定的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ（要件と観測結果の一致） | pass | AC-1482-01〜07を全てtestで確認済み（§2.1）。手動CLI smoke testで数値も実観測と一致 |
| 価値（利用者・運用上の目的） | pass | 本Issue自身のstagingで実測しM-03を満たした（03 §10）。「正しいが遅い」の定量化という目的を実際に達成した |
| 実現可能性（環境・依存・権限） | pass | 新規外部依存package無し（Node標準機能のみ）。既存CLI・domain・adapter層の既存関数を最大限再利用（`assertWorkflowStaging`、`countedRounds`、`withStagingMutationLock`等） |
| 整合性（設計・コード・テスト・仕様） | pass | 02設計とsrc実装、01要件とtest、docs/specsと実コードの整合を3ラウンドの独立reviewで確認・是正済み（round1でH1〜H3等、round2・round3でdocs drift等） |
| 保守性（責務・命名・変更容易性） | pass | `src/domain/metrics.ts`（純関数、filesystem非依存）と`src/adapters/metrics-journal.ts`（I/O）を分離。既存`workflow record`と同じCLIパターンを踏襲 |

## 4. 敵対的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 反例（要件を破る入力・状態） | pass | Step番号重複（SCN-016）、role区間が開いたまま（SCN-015）、計測event logの不正行（SCN-023）、end labelの不一致（SCN-018）を検証し、いずれもfail-closedまたは正しい縮約で処理される |
| 失敗経路（外部失敗・部分失敗） | pass | event log欠落（SCN-021）、review session欠落・解析失敗、staging digest不一致時の既存拒否経路を維持 |
| 境界値（空、最大、最小、重複、Unicode） | pass | journal 0件・1件（SCN-008）、label長さ上限200文字、C0/C1制御文字・zero-width・bidi override・BOM拒否（round1 L1是正） |
| 悪用（注入、経路脱出、権限外） | pass | 制御文字を含むlabel拒否（SCN-010）、`--review-session`のsymlink・trust boundary検証を追加（round2で発見・是正） |
| 安全性（認証、承認、秘密情報、Zero Trust） | pass | 秘密情報を記録しない設計（label値は列挙またはrole名に限定）。既存trusted boundary検証（`assertWorkflowStaging`）を再利用 |
| データ損失（上書き、削除、部分公開、履歴消失） | pass | H2是正によりStep番号重複時のデータ消失を解消。H1是正により既存staging digest検査への非破壊を`calculateStagingDigest`の前後比較で直接検証（SCN-017） |
| ロールバック（復旧参照、状態保持、再開可能性） | pass | 新規file（`src/domain/metrics.ts`等）の削除だけで既存機構に影響しない設計（03 §9） |
| 範囲漏れ（呼び出し元、利用側、配布物、文書） | pass | `npm run cli:check`・`architecture:check`・`package:check`・`trace:check`・`conformance:check`で呼び出し元・配布境界・追跡表の整合を機械検証済み |

## 5. 指摘

本Issueの実装は3ラウンドの独立review（各回とも別Agent呼び出し・別session/context、model=opus）を経て、Step 10記録前に全指摘を是正した。この表は独立reviewが実際に発見しround間で解消した指摘の記録である（round内での自己発見ではなく、独立reviewerが発見したもの）。round1・round2は本Issueのstaging journalへ実時間で記録した対応履歴を持つ（03 §4.1 DISC-1482-01〜03参照）。全件`resolved`であり、現時点で未解決のCritical/Highは無い。

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| REV-01 | High | `journal/metrics-events.jsonl`をstaging配下に置くと`inventory()`がdigest対象にし、`workflow mark`のたびにstaging digest一致検査（`workflow record --step=10`・`review artifact`・`pr create`）が壊れる | round1独立review発見。本Issue自身のstagingで再現 | `src/adapters/metrics-journal.ts` | `.agent-skill-chain/metrics/<staging名>/events.jsonl`（staging外）へ移設 | resolved | なし（SCN-MT-1482-017で回帰確認済み） |
| REV-02 | High | `step_ms`をStep番号keyのRecordへ縮約し、同じStep番号の再記録・reconfirmでentryが上書き消失する | round1独立review発見 | `src/adapters/metrics-journal.ts` | `StepDuration[]`（配列）へ変更 | resolved | なし（SCN-MT-1482-016で回帰確認済み） |
| REV-03 | High | AC-1482-06/M-03（本Issue自身での実測）が実施されていなかった | round1独立review発見 | `03_実装計画.md` | 本Issue自身のstagingへ`workflow metrics`を実行し記録（03 §10） | resolved | なし |
| REV-04 | Medium | role区間が開いたままのとき`artifact_build_ms`が常に0・`support_ms`が常に100%になり誤解を招く。`computeMetricsWindowMs`が2つの個別spanのmaxを取り、eventがstep範囲外だと過小評価する | round1独立review発見 | `src/domain/metrics.ts` | open時はnull化。windowは合わせた集合のmin/maxへ変更 | resolved | なし（SCN-MT-1482-014・015で回帰確認済み） |
| REV-05 | Medium | 不正な行をwarningとして無視し部分計算するfail-openだった（02 §6のfail-closed要求と不一致） | round1独立review発見 | `src/adapters/metrics-journal.ts` | 不正行1件でも該当fieldをfail-closedでunavailableに変更 | resolved | なし（SCN-MT-1482-023で回帰確認済み、round3指摘で実データを持つfixtureへ強化） |
| REV-06 | Medium | `workflow mark`後の既存command成功、Step番号重複、CLI経由のINV-03/04等を確認する統合testが無かった | round1独立review発見 | `test/features/integration/metrics-cli.feature` | SCN-MT-1482-016・017・019〜023を追加 | resolved | なし |
| REV-07 | Low | labelの長さ上限が無く、C1制御文字・zero-width・bidi override・BOMを拒否していなかった。`kind=role`のlabelがROLES列挙値と無関係な値を受理していた | round1独立review発見 | `src/domain/metrics.ts` | 上限200文字、拒否文字種を拡大、ROLES enum検証を追加 | resolved | なし（SCN-MT-1482-012で回帰確認済み） |
| REV-08 | Low | kind別合計（totals）がreportに含まれていなかった。`--review-session`にtrusted boundary検証が無かった。`--out`が`presentFlag`ヘルパーを使っていなかった | round1独立review発見 | `src/adapters/metrics-journal.ts`、`src/cli.ts` | `role_ms`等を合計として追加、`assertRegularFilePath`追加、`presentFlag`使用に統一 | resolved | なし |
| REV-09 | Low | SCN-MT-1482-007のgrep patternが自file内のpattern文字列に自己一致していた | round1独立review発見 | `test/steps/metrics-cli.steps.ts` | 自file除外pathspecを追加 | resolved | なし |
| REV-10 | Medium | round2で追加した`assertRegularFilePath`が相対pathの`--review-session`を常に拒否する回帰（絶対path化前のrealpath比較） | round2独立review発見 | `src/adapters/metrics-journal.ts` | `path.resolve`で先に絶対化 | resolved | なし（SCN-MT-1482-020で回帰確認済み） |
| REV-11 | Low | `end`のlabelが開区間のlabelと不一致でも受理し、誤帰属を招く | round2独立review発見 | `src/domain/metrics.ts` | label不一致を拒否する検証を追加 | resolved | なし（SCN-MT-1482-018で回帰確認済み） |
| REV-12 | Medium | H1回帰test（旧SCN-017）が`workflow record`の自己修復により判別力を持たなかった | round2独立review発見 | `test/steps/metrics-cli.steps.ts` | `calculateStagingDigest`の前後直接比較へ変更 | resolved | なし |
| REV-13 | Medium | M3で指摘した統合testの一部（relative path review-session、malformed行、CLI INV-03、--out）が未追加だった | round2独立review発見 | `test/features/integration/metrics-cli.feature` | SCN-019〜023を追加 | resolved | なし |
| REV-14 | Low | `02_設計.md`・`docs/specs`にstaging配下配置（旧設計）の記述が残っていた | round2独立review発見 | `01_要件定義.md`、`02_設計.md`、docs/specs | 新配置の記述へ更新 | resolved | なし |
| REV-15 | Low | 03のM-03実測disclosureに、file mtimeと事象を誤って結び付けた不正確な記述があった | round2独立review発見 | `03_実装計画.md` | round3で発見された不正確性も含め訂正（下記REV-16参照） | resolved | なし |
| REV-16 | Low | `02_設計.md`のMetricsReport行が`step_ms: Record<string, number>`のまま残っていた。event logがstaging lifecycleに従うという誤った記述が残っていた。03のT01/T02/T04が旧設計を記述したままだった。03のmtime disclosureが「全4件」「複写時刻」と誤って記述していた（実際は3件が複写、4件目は新配置への実時間記録） | round3独立review発見（最終verification pass） | `02_設計.md`、`03_実装計画.md` | 該当箇所を訂正 | resolved | なし |
| REV-17 | Low | AC-1482-06の「非ゼロ」を外す変更が「契約自体は変えない」と誤って記述していた。DISC記録がAC変更に言及していなかった | round3独立review発見 | `03_実装計画.md` | `workflow assess-discovery`を正式実行しDISC-1482-03として独立記録、記述を訂正 | resolved | なし |
| REV-18 | Low | SCN-MT-1482-020のrelative path fileがmain()例外時にrepository rootへ残る可能性、SCN-023のfixtureが開区間しか持たずsuppressされる実データが無い、SCN-021の名称不整合 | round3独立review発見 | `test/steps/metrics-cli.steps.ts`、features | try/finally化、fixture強化、名称修正 | resolved | なし |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: はい。formal review sessionのround 1は本file記録時点で`H_impl=15069d1d...`（3回の独立review・是正を経た最終状態）を対象に実施し、findings=0で記録する
- 指摘を確定した: formal round 1自体に新規指摘は無い。§5の指摘表はformal round前に3回の独立review（Agent呼び出し、round1〜3と呼称）で発見・是正した履歴を記録したものであり、いずれも本file記録時点で`resolved`
- 次ラウンド対象のCritical/High: 無し

### ラウンド2

対象外: formal round 1がfindings=0で収束したため、ラウンド2は実施しない / 検証証拠: review-session state（`review round --apply`実行結果、status=converged）

### ラウンド3

対象外: ラウンド2を実施していないため対象外 / 検証証拠: 同上

## 7. テスト結果

- 実行したcommandの一覧: `npm test`（project全体）、`npm run lint`、`npm run format:check`、`npx tsc -p tsconfig.json --noEmit`、`npm run source:check`、`npm run project:quality`、`npm run docs:format`、`npm run test:format`、`npm run trace:check`、`npm run architecture:check`、`npm run conformance:check`、`npm run audit:check`相当（下記）、`npm run package:check`、`npm run cli:check`、`npm run workflow:check`、`npm run skills:check`、`npm run directories:check`
- 全layerの合計（`npm test`、2026-09-25、H_impl 15069d1d時点。docs/reviews/本fileの日本語文書検査fix後に再実行し確認）: 2347 scenarios（2330 passed、17 skipped、0 failed）、21724 steps（21669 passed、55 skipped、0 failed）。失敗0件
- 失敗またはskipがある層だけを`projectChoices.testLayers`の順に1層1行で展開: 失敗0件のため対象なし。skip 17件は本Issueの変更と無関係な既存の環境依存scenario（`@requires-linux-poc`等の既存tag条件によるもの）
- runnerと`projectChoices.gherkinDialect`: `cucumber-js`（`en` dialect）、project policy `.agent-skill-chain/project/choices/development.json`の`testRunner`/`gherkinDialect`と一致
- 本Issue固有の再実行（`--name`フィルタで高速確認、2026-09-25）: `node --import tsx ./node_modules/@cucumber/cucumber/bin/cucumber.js --config cucumber.mjs --name "SCN-MT-1482-|SCN-INT-SPECNORM-001"` で24 scenarios・234 steps全合格

## 8. 配布物影響

packageとして配布する場合だけ記入する。配布境界はpackage manifestの配布file指定を正本とし、compileされて配布される`source`も含める。

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| dist/src/ | 入る | reviewerが確認 |
| docs/specs/01_システム概要/02_用語・略語.md | 入らない | なし |
| docs/specs/02_要件/00_要件一覧.md | 入らない | なし |
| docs/specs/02_要件/01_ワークフロー要件.md | 入らない | なし |
| docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md | 入らない | なし |
| docs/specs/14_開発・品質/00_ディレクトリ構成.md | 入らない | なし |
| docs/specs/15_要件追跡/00_追跡表.md | 入らない | なし |
| src/adapters/metrics-journal.ts | 入る | reviewerが確認 |
| src/cli-usage.ts | 入る | reviewerが確認 |
| src/cli.ts | 入る | reviewerが確認 |
| src/domain/metrics.ts | 入る | reviewerが確認 |
| test/features/integration/metrics-cli.feature | 入らない | なし |
| test/features/unit/metrics.feature | 入らない | なし |
| test/steps/metrics-cli.steps.ts | 入らない | なし |
| test/steps/metrics.steps.ts | 入らない | なし |

判断: 配布物を更新した

根拠: `src/domain/metrics.ts`・`src/adapters/metrics-journal.ts`（新規）と`src/cli.ts`・`src/cli-usage.ts`（`workflow mark`・`workflow metrics`追加）が配布対象`dist/src/`へcompileされる。利用者は新しい2 CLI subcommandを使えるようになる（既存subcommandの振る舞いは変更していない）。`docs/specs/`はpackage manifestの配布file指定に含まれない（`npm run package:check`で確認済み）

## 9. 独立reviewの成立

PR作成前に観測できるものだけを書く。immutable review IDやapproval件数は書かない。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated |
| その要求を満たすこと | はい |
| reviewerとimplementerのidentity・context比較 | implementer: 本session（Claude Sonnet 5、coordinator/implementer役）。reviewer（3ラウンドとも）: `Agent`ツールで起動した別session/context（model=opus、general-purposeサブエージェント）。project choiceの`modelMapping.roles.reviewer`（claude、project_default、`independence.differentFrom=implementer`）と`roles.implementer`（codex）はどちらもClaude系列であり同一providerだが、00_運用ポリシー.mdは「同一provider・同一論理tierだけを理由に独立性違反としない」と明記しており、別session/context・exact HEAD固定・対象差分の非変更・肯定敵対review記録という`context-isolated`の要求水準を満たす |
| reviewerが対象差分を変更していないこと | はい（3回のreviewerはいずれも「read-only、worktreeを変更しない」ことを明示指示され、各reviewerの報告でも「変更を加えていない」と明記している。進行役（本session）が指摘を受けてsrc/test/docs差分を実装した） |

対象外: 外部への不可逆な配布・外部証拠の要求は無い（PR作成までが本Issueの停止点であり、merge・release・公開は別sessionの担当） / 検証証拠: 00 §2.4「今回の停止点」、03 §9

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/01_システム概要/02_用語・略語.md`（TERM-ASC-131〜133）、`docs/specs/02_要件/00_要件一覧.md`・`01_ワークフロー要件.md`（REQ-WF-026・027）、`docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`（`workflow mark`・`workflow metrics`）、`docs/specs/14_開発・品質/00_ディレクトリ構成.md`（新規file・event log配置）、`docs/specs/15_要件追跡/00_追跡表.md`（SCN追跡）
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: はい。00の候補（TERM-ASC-131〜133）→01の確定差分→docs/specsの現在有効な定義の順で一方向に確定した
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: はい。既存TERM-ASC-001〜130、1432、1437の意味は変更していない。新規3件のみ追加
- 要件・変更・SCN・テストの追跡: `docs/specs/15_要件追跡/00_追跡表.md`にREQ-WF-026・027の行を追加し、SCN-MT-1482-001〜023を全件追跡した（`SCN-INT-SPECNORM-001`が機械検証、pass）
- `no-spec-impact`の場合の限定的根拠: 該当しない（仕様更新あり）
- UI・トークンの判断: 該当しない（DC-UX・DC-TOKENS ともnot-applicable、CLIのみ）

## 11. 総合判定と再開地点

- 未解決Critical/High: なし
- Medium/Lowの記録: §5参照。全18件（REV-01〜18）を記録し、全件resolved
- 判定: approved
- 新しい権限が必要な事項: 無し
- 残存リスク: 支援層/成果物比率の実測（M-03）はrole=implementerのmarkを進行役が発行した場合にのみ意味を持つ。SKILL.mdへの組み込みはDISC-1482-01により本Issueのscope外とした（v0.4.1以降または別Issueの判断）
- 次に許可される操作: 本artifactを`docs/reviews/`へcommitし`H_final`とした後、`workflow record --step=10`、`pr create`（Step 11）
- 次回の再開地点: PR作成後。マージは別sessionが担当する（00 §2.4「今回の停止点」）

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 1 |
| 対象SHA・文書ダイジェスト | 15069d1d3f7f065e20e7fbfd6868a204c0fd0798 |
| 比較基点 | `bb16faba15027304a11071631ddb6d21cf4c7d7a` |
| H_impl | `15069d1d3f7f065e20e7fbfd6868a204c0fd0798` |
| 対象差分 | dist/src/adapters/metrics-journal.js、dist/src/cli-usage.js、dist/src/cli.js、dist/src/domain/metrics.js、docs/specs/01_システム概要/02_用語・略語.md、docs/specs/02_要件/00_要件一覧.md、docs/specs/02_要件/01_ワークフロー要件.md、docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md、docs/specs/14_開発・品質/00_ディレクトリ構成.md、docs/specs/15_要件追跡/00_追跡表.md、src/adapters/metrics-journal.ts、src/cli-usage.ts、src/cli.ts、src/domain/metrics.ts、test/features/integration/metrics-cli.feature、test/features/unit/metrics.feature、test/steps/metrics-cli.steps.ts、test/steps/metrics.steps.ts |
| 対象外 | 比較基点に存在し変更されていない範囲 |
| 残り予算 | 5ラウンド（同一scope最大6counted round中、formal round 1で収束のため1消費） |
| ラウンド数 | 1（formal round。是正のための独立review自体は3回実施したが、`review round --apply`によるcounted roundとしてはこのround 1のみ） |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260925_170531_v0.4.0-計測基盤 |
| 仕様の所有箇所 | `.agent-skill-chain/docs/01_開発ワークフロー.md`（Step表、モード判定質問）、`docs/specs/14_開発・品質/00_ディレクトリ構成.md`（`.agent-skill-chain/metrics/`予約領域の既存定義） |
| 成果物行数 | 製品（`src/`）: 約460行追加（metrics.ts 約320行、metrics-journal.ts 約270行、cli.ts/cli-usage.ts追記 約100行、3 commit差引の純増分概算）。支援層（staging 00〜03、本review artifact）: 00〜03合計約900行、review artifact約250行。支援層が成果物を大きく上回らない範囲に収まっている |
| 縮小の先行評価 | 00 §「3. 既存手段の縮小で足りないか」で評価済み。step_ms（既存`journal/steps.jsonl`のrecordedAt差分）とreview_rounds（既存`countedRounds`関数）は新規instrumentation無しで既存機構をそのまま再利用した。新規instrumentationはrole_ms/model_ms/deterministic_msの3指標に限定した |
| 実施者・日時 | coordinator/implementer（本session、Claude Sonnet 5）、2026-09-25 |

`比較基点`と`H_impl`の値は40桁の小文字hexをbacktickで囲んだものだけにする。注記・branch名・短縮SHAを同じcellへ書かない。由来は別行へ書く。

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review（§3・§4）、finding分類（§5、REV-01〜18を重大度別に分類済み） | standard（riskは02§3でlowと判定した変更） | claude（project choice `modelMapping.roles.reviewer`の上限、Opus以下） | model=opus、reasoningEffort=high（project choice準拠） | 3回とも正常完了。未解決時の停止・再開条件は発生しなかった | §9参照。3回とも別session/context、対象差分non-change（read-only指示、各reviewer報告で確認） |
