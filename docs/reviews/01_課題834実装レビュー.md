# Issue #834 実装レビュー

## 対象と証拠

- 比較基点: `320213fdcf2bb67c4eb50d24fbadc74f0618fbc1`（`main`）
- 対象branch: `feature/834-risk-proportional-policy`
- 対象: Issue #834のrisk比例型policy enforcement、I1〜I12不変条件、所有権分離、project固有directory、移行、自己緩和防止、診断、証跡、配布境界
- 対象外: PR merge、Issue close、branch・worktree削除、release、tag、package公開
- 実施主体: 実装者とは別の独立reviewer・verifier
- 仕様影響: `updated`

この記録は実装者の自己承認ではない。実装者のセルフチェックと独立reviewの裁定を分離し、後者だけを最終承認根拠とする。

## ラウンド1

全rubricで肯定・敵対reviewを行い、High 8件、Medium 5件を確定した。主な論点は、実operationへの接続、authorityの全条件比較、retry改竄・TOCTOU、実manifest移行、schemaとruntimeの一致、secret-safe serializer、npm pack abuse、汎用契約とrepository固有規約の分離である。ラウンド1は未承認とし、各findingに失敗SCNを割り当てた。

## ラウンド2

ラウンド1修正差分と隣接境界を確認し、manifestのkind/path authority、trusted enforcementの実operation接続、安全契約fingerprint、plan再導出・CAS、artifact拘束・crash recovery、serializer網羅性、pack content scan、shipped asset分離、conformance実在証拠、failed current evidenceとcache矛盾を未解決Highとした。ラウンド2も未承認とし、追加の失敗SCNで修正を拘束した。

## ラウンド3と収束

ラウンド2修正後に、state内approvalのauthority再利用、fragmented policyの不正なmonolith書き込み、trusted ref失敗時のfallback、recovery失敗時のjournal上書き、legacy fingerprintにevidence dedupe、汎用traceの固定3層を追加findingとした。対応する失敗SCNの赤を先に観測し、call-site独立authority、fragment inventory、fail-closed、journalとreportの分離、完全なstructured evidence拘束、project-selected layerで修正した。

独立reviewer・verifierはラウンド3で全findingを`resolved`と分類した。未解決Critical/Highは0件である。

### 肯定的観点

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ | pass | Issue #834の要求、I1〜I12、runtime、SCN、仕様追跡が対応する |
| 価値 | pass | safety境界を維持しつつ、修正経路とoffline進行を提供する |
| 実現可能性 | pass | 既存Node CLIの単責務domainとadapter境界に統合した |
| 整合性 | pass | 汎用contract、project choice、repository rule、実装証拠の所有者が分離された |
| 保守性 | pass | 巨大gateではなく、policy、enforcement、migration、conformanceを合成する |

### 敵対的観点

| 観点 | 判定 | 根拠 |
|---|---|---|
| 反例・境界値 | pass | unknown、重複、期限切れ、scope/actor/SHA不一致、stale/failed evidenceを拒否する |
| 失敗経路 | pass | partial migration、rollback中断、trusted ref解決失敗、network停止を分離する |
| 悪用・安全性 | pass | 自己緩和、path/Git内部、secret、artifact汚染、TOCTOUをfail-closedにする |
| データ損失 | pass | preview、書込み直前hash、revision CAS、read-after-write、durable journalを要求する |
| rollback | pass | before/after hashからrecoverし、同じplanを再実行できる |
| scope | pass | #834のpolicy基盤、所有権、配布、dogfood、仕様追跡に限定される |

## 不変条件・所有権・project directory

- I1〜I12は`.agent-skill-chain/policy/conformance.json`とschemaを機械正本とし、実在source/export、counterexample SCN、成功証拠、rollbackへ拘束した。
- 汎用package contractは`.agent-skill-chain/docs/`、`templates/`、`policy/default.json`に置き、具体的なproject choice・repository ruleを含めない。
- 本repositoryの選択と規約は`.agent-skill-chain/project/choices/`、`.agent-skill-chain/project/rules/`、`.agent-skill-chain/project/conformance/`へ分離し、`.agent-skill-chain/project-policy.json`はinventoryとauthority coreだけを持つ。
- localの誤配置はassist、PRのowner/evidenceはrequire、実artifactの汚染はdenyとし、機能拡張を不必要に停止させない。

## テスト・配布・dogfood結果

| 検査 | 結果 |
|---|---|
| risk targeted | 66シナリオ、347ステップ、全pass |
| 全回帰 | 177シナリオ、1032ステップ、全pass |
| conformance | I1〜I12、実在source/export、成功SCN証拠がpass |
| 型検査・build | pass |
| 日本語文書・Gherkin形式 | pass |
| package・prepack | pass |
| 実npm pack | 106ファイル、project policy・role log・開発計測・test fixture・秘密情報の混入0件 |
| dogfood | 本repositoryの通常拡張、安全拒否、rollback/retryがpass |
| `git diff --check` | pass |

## 最終裁定とrollback

- 判定: `approved`
- 未解決Critical/High: 0件
- 肯定・rubric: `pass`
- 敵対rubric: `pass`
- 残risk: なし
- 実release、tag、npm publish: 実施なし。本作業でも実施しない
- rollback: 本branchの差分をrevertし、consumer-ownedの`.agent-skill-chain/project-policy.json`、`.agent-skill-chain/project/`、`.agent-skill-chain/tmp/issues/`を保持する。移行途中はjournalとbefore/after hashからrecoverし、承認済みplan hashとexpected revisionで再実行する

PR作成後はCI結果を確認する。merge、Issue close、branch/worktree削除、release、tag、npm publishは行わない。
