# Issue #834 Phase A 実装レビュー

> 状態: `pending-external-attestation`。このtracked Phase A artifactはH_final後に更新しない。完了はtrusted GitHub providerが観測するPR head、成功CI、immutable reviewの外部attestationだけで記録する。この文書へH_final、CI run ID、review IDを自己参照として追記しない。

## 対象識別

| 項目 | 観測値 |
|---|---|
| 課題 / PR | Issue #834 / PR #835 |
| 比較基点 | `320213fdcf2bb67c4eb50d24fbadc74f0618fbc1` |
| H_impl | `930aaecf783d1e62a67d4a854f53d25a3e69a9d3` |
| 実装tree | `8d112835fb5e5ba6735618c6d67e46513ec4cedc` |
| branch | `feature/834-risk-proportional-policy` |
| reviewer | `/root/issue834_reviewer` |
| reviewedAt | `2026-08-23T23:06:17+09:00` |
| verifier | `/root/issue834_analyst` |
| verifierAt | `2026-08-23T23:06:17+09:00` |

対象はIssue #834のrisk比例enforcement、I1〜I12不変条件、循環拒否、所有権分離、project policy set、migration、diagnostic、review evidence、配布境界、および対応する仕様・testである。PR merge、Issue close、branch/worktree削除、release、tag、package公開は対象外とする。

## 独立review・QA結果

reviewerとverifierは実装者と分離し、H_implのproduct/spec/test差分を肯定・敵対の両観点で確認した。

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ | pass | Issue #834、I1〜I12、runtime、SCN、仕様追跡が対応する |
| 価値 | pass | safety/authority境界を維持し、安全な次行動とoffline進行を提供する |
| 実現可能性 | pass | policy、migration、review、conformanceを単責務のdomain/adapterへ分離した |
| 整合性 | pass | package contract、project choice、repository rule、implementation evidenceのownerを分離した |
| 保守性 | pass | UNIX/DDD/BDD-TDDの主経路を置換せず、薄い合成可能なenforcement pointとした |
| 反例・境界値 | pass | unknown、canonical tuple重複、scope/actor/SHA不一致、stale/failed evidence、cycle、Git-validな境界branchを検証する |
| 失敗・悪用 | pass | candidate自己緩和、自己申告trusted SHA、非default base、dirty worktree差替え、provider観測TOCTOU、path/Git内部、自己review、別PR CI run、秘密混入をfail closedにする |
| データ損失・rollback | pass | preview、CAS、before/after hash、durable journal、recover/retryを検証した |
| scope | pass | #834の不変条件・外部証拠・配布境界と直接必要な仕様/testへ限定した |

### CodeRabbit指摘分類

| 分類 | 判定・理由 |
|---|---|
| valid / resolved | caller自己申告SHA、default/base/head非拘束、dirty filesystem candidate、provider観測TOCTOU、external unavailableの状態、Actions tokenで不安定なpermission label、current-head CI/review、診断言語、override ruleId、durable before content、Gherkin dialect、canonical tuple重複、コメント・文字列内のnon-executable export、checkout credential、review actor fixture、metrics初期化、全project test layer追跡、false block改善時のnon-override deny維持、failed final evidenceの操作許可防止の各指摘を実装・仕様・反例testへ反映した。`Rk`はoperation合成点まで再調査し、初回棄却を訂正した |
| rejected | `RM`、`Hw`。固定SCN識別子をproject固有化する指摘はgeneric trace/conformance契約と矛盾し、現H_implに再現可能な欠陥を示さないため採用しない |
| still-valid | なし。H_implの未対応Critical / High / Medium / Lowは0件 |

Critical、High、Medium、Lowの未解決findingはいずれも0件である。実装者の自己確認は独立review/QAの証拠へ数えていない。

## ADR判定

ADRは`not-applicable`とした。#834で追加したarchitecture decisionは新しい独立ADRを必要とする横断的な選択ではなく、Issue #834の受け入れ条件を成立させる不変条件、authority/evidence境界、project ownership、migration/recovery、GitHub観測契約である。これらは`docs/specs/`の要件、機能、外部インターフェース、移行、追跡仕様へ直接記録し、SCNとruntimeへ拘束した。

## 検証証拠

| 検査 | 結果 |
|---|---|
| 信頼済みprovider・default/base/head・commit object・TOCTOU・pending反例 | pass |
| canonical conformance tuple・Git branch境界・独立actor反例 | pass |
| 全回帰 | 230 scenarios / 1314 steps、pass |
| repository conformance | I1〜I12、実在source/export、counterexample成功証拠、pass |
| 構築 / 型検査 / 文書 / Gherkin | pass |
| prepack | pass |
| actual package | 106 files、project policy・role log・開発計測・test fixture・秘密情報の混入なし |
| 差分検査 | pass |
| stale external run | Actions run `32643486234`はActions tokenのpermission label誤判定でfailed。原因をH_implで修正し、成功証拠に再利用しない |
| stale successful run | Actions run `32643877661`はhead `f98d391c4faa9dc2bad9d707e5bbb8c4879093f9`でsuccess。H_impl更新によりstaleとし、current headの成功証拠に再利用しない |
| stale successful run / review | Actions run `32644216949`とCodeRabbit checkはhead `f92ed42468dd09dfab9ab67632bb1734fd035a1d`でsuccess。Rk修正によりstaleとし、current headの成功証拠に再利用しない |

## Phase A裁定とrollback

- H_implのproduct/spec/test review: `pass`
- 独立QA: `pass`
- 未解決Critical / High / Medium / Low: `0 / 0 / 0 / 0`
- 外部attestation: `pending`
- 最終状態: `pending-external-attestation`
- release、tag、npm publish: 実施しない
- rollback: H_implをrevertし、consumer-ownedの`.agent-skill-chain/project-policy.json`、`.agent-skill-chain/project/`、`.agent-skill-chain/tmp/issues/`を保持する。migration途中はdurable journalのbefore contentからrecoverし、before/after hashでintegrityを検証し、call-site承認済みplan hashとexpected revisionで再実行する

Phase A以降、このtracked artifactは変更しない。H_finalはPhase A artifactだけを加えるcommitとして外部で識別し、PR・CI・reviewの完了判定はtrusted GitHub external attestationにのみ保持する。
