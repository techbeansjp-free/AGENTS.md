# 04 レビュー

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 1 |
| 対象SHA・文書ダイジェスト | `8cdd1281e68709e1eb1161794f057fe2da4d9bb6` |
| 比較基点 | `b95ea9f38721e2d47accfc768fe770748088cac6` |
| H_impl | `8cdd1281e68709e1eb1161794f057fe2da4d9bb6` |
| 対象差分 | `b95ea9f38721e2d47accfc768fe770748088cac6..8cdd1281e68709e1eb1161794f057fe2da4d9bb6`。7 file、158挿入、3削除。**本artifactは`H_impl`より後のcommitで加わるためこの範囲に含まれない** |
| 対象外 | 取り込み条件の緩和・強化、`REVIEW_ROUND_BUDGET`と`REVIEW_RECOVERY_ROUND`の値、規範文書2件の記述、判定logic（`review-convergence.ts`）、#1074（予算が帳簿合わせで消える構造） |
| 残り予算 | 3ラウンドのうち1使用。**残り2** |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260906_165025_pr-create後の外部指摘の取り込み可否でstep-10-review-skillが規範文書2件と矛盾する |
| 仕様の所有箇所 | `.agent-skill-chain/docs/01_開発ワークフロー.md`。引用: 「**`pr create`より後に届いた外部reviewerの指摘は、条件を満たす場合に同じPRへ取り込む。**」 |
| 成果物行数 | skill 1段落の置換。`check_skill_templates.ts` +32行。test +100行。仕様 +6行 |
| 縮小の先行評価 | 代替案5件を`02_設計.md`§12で根拠つきで不採用にした。**条件5件をskillへ複写しない判断が最大の縮小である。** 諮問は行わず、正本2件の原文一致と#1083の同型前例を根拠に決めた |
| 実施者・日時 | reviewer / 2026-09-06 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類、差分全文、変異試験 | routine（riskはlow、scopeは配布skillの記述） | project choiceのprovider上限に従う | project choiceのtier mappingに従う | 未解決Critical/Highがあれば停止し次roundで再評価する | reviewerはimplementerと別contextで起動する。reviewerは対象差分pathを1件も変更していない |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | Issue #1240、staging `01_要件定義.md`§6 | AC-01〜AC-06、INV-01〜INV-04 | 一次資料 |
| 矛盾の実証 | `01_開発ワークフロー.md:119`、`02_品質基準.md:85`、`step-10-review/SKILL.md:22` | 2対1で正反対 | 一次資料の原文 |
| 製品の実装 | Issue #980 / PR #1237 | `review round`（round 4）と`--post-terminal-intake`が通りmergeまで到達 | 外部のimmutable証拠 |
| 差分 | `b95ea9f38721e2d47accfc768fe770748088cac6..8cdd1281e68709e1eb1161794f057fe2da4d9bb6` | 7 file、158挿入、3削除 | 既存コード |
| テスト | `npm run conformance:check`（全suite内包） | 1553 scenarios（1537 passed、16 skipped）、失敗0 | テスト出力 |
| 変異試験 | 6変異を1件ずつ適用 | **6件すべてkill。** 生存0件 | テスト出力 |
| commit前candidate | 本artifactを除く7 file | `8cdd1281e68709e1eb1161794f057fe2da4d9bb6` | Git観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **確認した。** 依存は検査→skill本文（データ）→`node:fs`の単方向で、追加importは0件。本artifactへ自身のcommit SHAを書いていない
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけであり、trusted providerが観測したPR/CI/reviewが`H_final`へ一致している: `H_impl`は`8cdd1281e68709e1eb1161794f057fe2da4d9bb6`。本artifactの1 fileだけを加えて`H_final`にする
- reviewer stable IDがPR author/provider観測済み`H_impl` author stable IDと異なる: reviewerはimplementerと別contextで起動する。差分pathを1件も変更していない
- 既定branch追随を行った場合: **行っていない。** baseは`b95ea9f38721e2d47accfc768fe770748088cac6`のままである

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/skills/step-10-review/SKILL.md` | M | 本repository | package | 取り込みの方向を規範文書へ揃え、条件の正本を名指しする。条件は複写しない | pass。参照は`../../docs/01_開発ワークフロー.md`の1本 | REQ-WF-005 / AC-01〜AC-03 / SCN-UNIT-PACKAGE-021〜023 | 文書のみ。revertで完全に戻る | pass |
| `scripts/check_skill_templates.ts` | M | 本repository | project | 必須表現の存在と禁止表現の不在を検査する | pass。追加importは0件 | REQ-WF-005 / AC-04、AC-05 | 読み取り専用 | pass |
| `test/features/unit/review-policy-package.feature` | M | 本repository | evidence | SCN-UNIT-PACKAGE-021〜023のscenario定義 | pass | AC-01〜AC-06 | 一時複製に対する検査のみ | pass |
| `test/steps/unit.steps.ts` | M | 本repository | evidence | 上記scenarioのstep定義。marker一覧を対象から導出せず独立に列挙する | pass | 同上 | 同上 | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | 本repository | spec | REQ-WF-005へ、配布skillが規範文書と反対の方向を述べてはならない規定を追記 | pass | REQ-WF-005、AC-WF-005 | 追加なし | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | 本repository | spec | REQ-WF-005行へSCN-UNIT-PACKAGE-021〜023を追加 | pass | 同上 | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | 本repository | spec | 本変更の履歴行 | pass | 同上 | 同上 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **確認した。** `比較基点..H_impl`の`git diff --name-only`が返す7件と表の7行が一致する。`dist/`配下の差分は0件で、`scripts/`と`test/`はbuild対象に含まれない
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **確認した。** skillは汎用の記述、検査は`scripts/`、契約は`docs/specs/`へ置き、`src/`へ触れていない
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: **findingによる修正は0件である。** 変異6件は初回からすべてkillされた

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

**実装中の新規発見は0件である。** 起票時点で矛盾の全体像が原文3行で確定しており、#980での実測が製品側の実装を裏付けていた。**発見が無いこと自体を記録する。**

なお本Issueそのものが、#1074の設計諮問中に外部アドバイザーの指摘で見つかった発見である。**私は今日この矛盾を実際に踏んでいる。** #980でskillが禁止する手順（`pr create`後の取り込み）を実行しており、**そのときskillの当該段落を読んでいなかった。**

### 2.1 受け入れ条件とシナリオ

| AC | 内容 | SCN | 結果 |
|---|---|---|---|
| AC-01 | skillに禁止表現が無い | SCN-UNIT-PACKAGE-022 | pass |
| AC-02 | 取り込みを許す方向を述べ、正本を名指しする | SCN-UNIT-PACKAGE-021 | pass |
| AC-03 | `budget-exhausted`から開かない旨の記述がある | SCN-UNIT-PACKAGE-021 | pass |
| AC-04 | 必須表現の欠落を「」つきで名指しする | SCN-UNIT-PACKAGE-021 | pass |
| AC-05 | 禁止表現の混入を「」つきで名指しする | SCN-UNIT-PACKAGE-022 | pass |
| AC-06 | 既存のskill marker検査が合格し続ける | SCN-UNIT-PACKAGE-020、023 | pass |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | not-applicable | 認証・認可・信頼境界の判定を追加・変更・削除しない。取り込み条件は`01_開発ワークフロー.md`が持ち続け、skillは参照するだけで緩和も強化もしない。秘密情報を扱わない | 差分7 fileのいずれも`src/`に属さず、`review-convergence.ts`と予算の値へ触れない。追加importは0件 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 矛盾が再発したときに気付けないと、implementerが再び正本から行動を決められなくなる | 変異P4・P5（errorが名指ししない）がSCN-021・022に殺される。出力は`skills:check`のstdoutのerror行のみで、log保持・rotation・監視・常駐processを持たない。復旧はskillの記述を戻すことである |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | CIが非対話で実行する検査とimplementerが読むMarkdownであり、人が操作する画面・入力要素・focus順序・支援技術の対象になる成果物を持たない | 差分7 fileのいずれも表示層に属さない |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | 生む出力は検査のerror行とMarkdown本文だけであり、色・寸法・typography・間隔を決めるtokenを適用する描画対象が存在しない | DC-UXと同じ根拠 |

## 3. 肯定的評価

- **上流を直した。** 矛盾はskill 1箇所に閉じており、規範文書2件は一致していた。下流の個別是正ではなく、乖離している1点を正本へ揃えた。
- **条件を複写しなかった。** skillは`01_開発ワークフロー.md`を名指しするだけである。複写すれば二重管理になり、まさに今回と同じ乖離が再発する。
- **skillが持っていた正しい部分を落としていない。** 「取り直し1ラウンドは`budget-exhausted`から開かない」は実在する制約であり、markerで固定した。**古い記述を全否定していない。**
- **必須と禁止の両方を検査した。** 片方だけでは空虚になることを設計で示し、変異P1・P2がそれぞれ別のscenarioに殺されることで実証した。
- **変異P6が本変更の意味を直接測っている。** skillを元の禁止方向へ戻すと4 scenarioすべてが落ちる。

## 4. 敵対的評価

- **字面検査であり意味を見ない。** 必須語4件を並べただけの無意味な段落は通る。**「skillが正しいことを言っている」ことは担保しない。** 担保するのは方向の反転という粗い回帰だけである。
- **本検査は候補側にあり敵対耐性を持たない。** `check_skill_templates.ts`は`PROTECTED_FILES`に属さず、同一PRで定数ごと削除できる。担保は偶発的劣化の回帰検出である。
- **同型の乖離が他のskillに無いことを確認していない。** 本Issueは`step-10-review`の1段落だけを扱った。**12 skillの記述が規範文書と整合しているかを全件突合していない。** 同じ型の乖離が他所にある可能性は残る。
- **矛盾が生まれた原因を断っていない。** なぜskillだけが古いまま残ったのか、#1140の変更時にskillへ追随しなかった理由を追っていない。**再発防止は本件の字面検査だけであり、他の記述には効かない。**
- **私自身がこの矛盾を踏んでいた。** #980でskillが禁止する手順を実行し、そのときskillの当該段落を読んでいない。**検査を足しても「skillを読まない」ことは防げない。**

## 5. 指摘

| ID | 分類 | 内容 | 対処 |
|---|---|---|---|
| F-01 | Low | 同型の乖離が他の11 skillに無いことを確認していない | **本Issueでは扱わない。** 全件突合は別の作業であり、対象範囲を超える。敵対的評価へ記録した |

未解決のCritical/Highは0件である。

## 6. ラウンド固有の確認

### ラウンド1

固定initial HEADに対する全scope reviewである。`previousBlocking`、`fixedDiff`、`adjacentScope`はいずれも空である。

## 7. テスト結果

実行したcommandの一覧: `npm run lint`、`npm run format:check`、`npm run typecheck`、`npm run trace:check`、`npm run skills:check`、`npm run docs:format`、`npm run build`、`npm run conformance:check`、変異試験script

全layerの合計: **1553 scenarios（1537 passed、16 skipped）、失敗0**

失敗またはskipがある層: skipは16 scenarioで、いずれも本変更以前から存在する環境依存scenarioである。失敗は0件のため展開する層は無い。

**gateはすべて直列で実行した。** `npm test`と`conformance:check`の並行実行は`dist/bin`を壊す。

runnerは`@cucumber/cucumber`、`projectChoices.gherkinDialect`は英語keyword・日本語説明である。

対応する成功CI runの参照: **push前のため未観測。** PR作成後に必須checkの結果を観測する。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `.agent-skill-chain/skills/step-10-review/SKILL.md` | 入る | **implementerが読む指示が変わる。** `pr create`後の指摘を同じPRへ取り込めるようになる |
| `scripts/check_skill_templates.ts` | 入る | `skills:check`の受理集合が狭まる |
| `test/`配下2件 | 入る | なし |
| `docs/specs/`配下3件 | 入らない | なし |

判断: 配布物を更新した

根拠: skillと`scripts/`は`npm pack`の対象であり利用者が読み実行する。**skillを自分でfork・改変しているprojectがあれば、禁止表現を残したまま`skills:check`が落ちる。** 診断が該当表現を名指しするため是正できる。

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | push前のため未取得。PR作成後に外部reviewerの指摘を観測する |
| reviewerがPR author・実装commit authorと異なる | はい |
| 観測したreview commentとapprovalの件数 | 内部の敵対review finding 1件（Low）。対象範囲外として記録。外部reviewとapprovalはPR作成後に観測する |

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `02_要件/01_ワークフロー要件.md`、`15_要件追跡/`
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: **台帳への追加・変更・廃止は0件である。** 既存語（取り込み、収束、取り直しラウンド、`budget-exhausted`）の意味を変えていない
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: **確認した。** `conformance:check`が合格している
- 要件・変更・SCN・テストの追跡: REQ-WF-005 → AC-WF-005 → SCN-UNIT-PACKAGE-021〜023。`trace:check`でorphan 0件
- `no-spec-impact`の場合の限定的根拠: 該当しない
- UI・トークンの判断: UI無し

## 11. 総合判定と再開地点

- 未解決Critical/High: **0件**
- Medium/Lowの記録: F-01を対象範囲外として記録した
- 判定: approved
- 新しい権限が必要な事項: **なし。** `PROTECTED_FILES`所属fileを1件も変更していない
- 残存リスク: **字面検査であり意味を見ない。** 必須語を並べただけの段落は通る。**同型の乖離が他の11 skillに無いことを確認していない。** **矛盾が生まれた原因（#1140の変更時にskillへ追随しなかったこと）を断っていない。** 再発防止は本件の字面検査だけであり他の記述には効かない。**検査を足しても「skillを読まない」ことは防げない。** 本検査は候補側にあり敵対耐性を持たない
