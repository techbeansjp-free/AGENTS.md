# 04 レビュー

> 肯定と敵対の両観点を毎ラウンド確認する。指摘なしの承認は有効。Medium/Lowは記録するだけで、自動修正・追加ラウンド・ゲート停止を起こさない。用語と責務の境界は`成果物用語と責務境界`（`.agent-skill-chain/docs/01_開発ワークフロー.md`）を正本とする。
>
> 埋めた成果物は`docs/reviews/`または`.agent-skill-chain/reviews/`へ置き、実装commitの後にその1 fileだけをcommitする。

| 読者 | 読む節 |
|---|---|
| 発注・評価する人 | 要約 → §5 指摘 → §11 総合判定 |
| 実装・レビューする人 | 全節 |
| 運用する人 | 要約 → §8 配布物影響 → §10 仕様整合性 |

## 要約

**この5行だけで、何を見たreviewかが分かるように書く。** 各行は1〜2文。詳細は対応する節が持つ。

| 項目 | 内容 |
|---|---|
| 何が問題だったか | macOSの一時directoryにsymlink祖先があり、正常なfixtureが拒否された。全件実行では別のfixture、Linux専用PoC、APFSのUnicode正規化、実行sandboxのloopback/IPC制限も失敗した。 |
| 何を解決しようとしたか | macOS上で正常な全件テストを完走し、真のsymlink祖先拒否とPoCのfail-closedを維持する。 |
| 何を行ったか | fixtureの実体pathを使用し、Linux専用PoCをplatform tagで分け、非Linuxでのpreflight拒否を追加した。APFSで作れないNFD物理名の検査はGit観測を合成して維持した。 |
| 何を確認したか | 修正前の失敗を再現し、最終HEADの全件2,250シナリオ中2,223成功・27スキップ・0失敗。12品質gateを通した。 |
| 判定 | approved |

## 1. 入力証拠

PR番号、Actions run ID、immutable review IDはPR作成後にしか存在しないため**この文書へ書かない。** `review evidence`とdelivery stateがappend-onlyで保持する。reviewerの独立性は`merge.reviewIndependence`が決める。既定の`context-isolated`はimplementerと別session/context、exact HEAD固定、対象差分の非変更、肯定・敵対reviewとfinding記録を要求し、同一GitHub actorでも成立する。このときtracked artifactの`approved`と保存済みreview session・Step 10 bindingがformal approvalになる。`actor-independent`はPR authorおよび観測済み`H_impl` commit authorと別のstable actor IDによるprovider `APPROVED`を要求する。両modeともexact HEAD一致は必須とし、tracked文書へ自身のcommit SHAを書かず、**H_final後はartifactを更新しない。**

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | .agent-skill-chain/tmp/issues/20260921_083322_1430-macos-temp-fixture | staging digest de7f81b51ab425062267d2c848e38f189ed880ddfe23c5f045444530f78db8cd | 既存コード |
| 差分 | `4f6e655326c0703de2024a367bf13cc3e4ff882d`..`9e0d8eb1d168c70c92596aa79beae71b9175564b` | 8 path | 既存コード |
| テスト | `NPM_CONFIG_CACHE=/private/tmp/asc1430-npm-cache npm test` | 2,250シナリオ、2,223成功・27スキップ・0失敗。18,557ステップ、18,456成功・101スキップ。 | テスト出力 `/private/tmp/asc1430-himpl-full.log` |
| 仕様 | `docs/specs/15_要件追跡/00_追跡表.md` | 非Linuxのfail-closedシナリオをREQ-WF-003へ追跡 | 既存文書 |
| ローカルLLM | loopback `qwen3.8:27b` | 最終候補は0件。入力digest `fdb50010a288ad51ae83a19d864c36ace449bf2e4345a8a88ee47b12e3a14b43`、出力digest `c24829b553404e7a4b8743e2134a1770ab08e53f07a959eb76f8e1b03f308bf1`。正式delegated経路はmodel allowlist外でdegraded。 | 実行記録 `/private/tmp/asc1430-llm-final3-response.json` |
| 独立review | Codex Sol、Claude Opus | Solは有効指摘なし。Opusの実行可能性指摘は対象testで反証、残りのLowは§5記録。両者とも対象差分を編集せず。 | 別contextの読取専用review |
| commit前candidate | docs/specs/15_要件追跡/00_追跡表.md、test/features/e2e/poc-mode-cli.feature、test/features/e2e/workflow-step-enforcement-cli.feature、test/features/integration/poc-mode.feature、test/steps/cli-usage.steps.ts、test/steps/poc-mode.steps.ts、test/steps/worktree-ignored-artifacts.steps.ts、test/support/world.ts | H_impl 9e0d8eb1d168c70c92596aa79beae71b9175564b | Git index |
| Phase A artifact | 本fileをcommit後に観測 | 未作成 | Git観測 |
| review session | .agent-skill-chain/tmp/issues/20260921_083322_1430-macos-temp-fixture | converged、round `bf9ad76b963729c1f64d685b6ff9d98a3e5bd223909c9f1934522c6483d4f87b` | 保存済みsession |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: 変更はテストと追跡表に限り、実行authorityの変更なし。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: artifact commit後にGitで検証する。
- reviewerの独立性が要求水準を満たす: 別contextのCodex Solによる読取専用reviewで変更なしを確認。Opusも確認中。
- 既定branch追随を行った場合、取り込みがartifact commitより前にあり、`比較基点`が取り込んだ既定branch tip、`H_impl`がartifact直前の最新commitを指し、個別監査表を`比較基点..H_impl`から再生成した: 既定branch追随なし。

### 1.1 変更ファイル個別監査

基準SHAとの差分にある全ファイルを、生成物（`dist/`等）も含めて1ファイル1行で記録する。まとめ行、directory単位の一括承認、test成功だけの代替を認めない。`audit:check`の他の検査対象外となる生成物でも、各行へ生成元との対応確認方法と配布影響の確認方法を記録し、差分path集合と表のpath集合を一致させる。

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `docs/specs/15_要件追跡/00_追跡表.md` | M | docs/specs | docs/specs | SCN追跡 | spec → test、循環なし | REQ-WF-003・SCN-INT-POC-012 | 文書のみ。revert可 | pass |
| `test/features/e2e/poc-mode-cli.feature` | M | test | test | Linux専用PoC tag | test → PoC CLI、循環なし | AC-1430-03 | 非Linuxでskip、Linuxで実行。revert可 | pass |
| `test/features/e2e/workflow-step-enforcement-cli.feature` | M | test | test | Linux専用PoC tag | test → workflow CLI、循環なし | AC-1430-03 | 非Linuxでskip、Linuxで実行。revert可 | pass |
| `test/features/integration/poc-mode.feature` | M | test | test | Linux専用と非LinuxのPoC境界 | test → PoC adapter、循環なし | AC-1430-03・SCN-INT-POC-012 | 非Linuxでも実preflight拒否を検査。revert可 | pass |
| `test/steps/cli-usage.steps.ts` | M | test | test | CLI fixture実体path化 | test → CLI、循環なし | AC-1430-01 | symlink判定は製品側に残る。revert可 | pass |
| `test/steps/poc-mode.steps.ts` | M | test | test | 非LinuxのPoC拒否assertion | test → PoC adapter、循環なし | AC-1430-03・SCN-INT-POC-012 | 観測artifactとjournalの不作成を確認。revert可 | pass |
| `test/steps/worktree-ignored-artifacts.steps.ts` | M | test | test | APFS対応のUnicode反例 | test → hygiene、循環なし | AC-1430-02 | Cf実fileと合成NFD観測を維持。revert可 | pass |
| `test/support/world.ts` | M | test | test | 共通fixtureとplatform hook | test → fs/Cucumber、循環なし | AC-1430-01/03 | `realpath`でfixtureのみ正規化、製品検査は不変。revert可 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: Git差分8 pathと表8行が一致。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: 実装コード変更なし。
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: ローカルLLMのcoverage指摘を受け、PoC feature・step・追跡表を再確認。

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

発見IDは実装計画（fullは03、quick/pocは集約00）の`DISC-*`と同じ字面を使い、本文書内で別IDへ言い換えない。

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| 該当なし | 実装中に契約変更を要する発見なし | AC-1430-01〜03 | なし | 全件失敗を原因別に是正 | 全件testと個別回帰test | updated | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1430-01 | SCN-INT-HYGIENE-001 | 共通fixtureとCLI fixtureの`realpath` | 修正前失敗、修正後成功 | pass | 対象test、全件test |
| AC-1430-02 | SCN-INT-WORKTREE-005 | 製品側のsymlink拒否を変更せず維持 | 成功 | pass | 対象test、全件test |
| AC-1430-03 | SCN-INT-POC-012、Linux専用PoC、全件 | platform別PoCとAPFS反例を是正 | 2,223成功・27スキップ・0失敗 | pass | `/private/tmp/asc1430-himpl-full.log` |

### 2.2 開発考慮事項の適用判定（必須）

00の判定・理由・証拠から差分が無ければ、表の代わりに`開発考慮事項の適用判定は00_要求定義.md §6.1と同じ`の1行を置ける（01〜03と同じ参照行）。差分がある行だけを表に残してよい。**`review validate`はこの§2.2の内容を検証しない**（01〜03の`issue validate`と異なり、review artifactのDC判定に対する機械検証は無い）。記述量を減らすための人・エージェント向けの案内であり、参照行を置いても4行の表を書いても合否は変わらない。

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
開発考慮事項の適用判定は00_要求定義.md §6.1と同じ。

## 3. 肯定的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ（要件と観測結果の一致） | pass | macOS全件失敗0件、symlink反例が成功。 |
| 価値（利用者・運用上の目的） | pass | macOS開発機でtest suiteを実行可能にした。 |
| 実現可能性（環境・依存・権限） | pass | Linux専用PoCはLinuxで継続実行、非Linuxは実preflight拒否を検査。 |
| 整合性（設計・コード・テスト・仕様） | pass | REQ-WF-003にSCN-INT-POC-012を追加。 |
| 保守性（責務・命名・変更容易性） | pass | platform判定を共通Cucumber hookに集約。 |

## 4. 敵対的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 反例（要件を破る入力・状態） | pass | 真のsymlink祖先をSCN-INT-WORKTREE-005で拒否。 |
| 失敗経路（外部失敗・部分失敗） | pass | 非Linux PoCのpreflight拒否とartifact/journal不作成をSCN-INT-POC-012で検証。 |
| 境界値（空、最大、最小、重複、Unicode） | pass | APFSでNFD物理名を作れないため、Cf物理名とGit観測の合成NFDで反例を維持。 |
| 悪用（注入、経路脱出、権限外） | pass | 本変更は製品側のpath検査を緩和せず、真のsymlink反例を残す。 |
| 安全性（認証、承認、秘密情報、Zero Trust） | pass | PoCの`/usr/bin/bwrap`・`/usr/bin/prlimit`固定条件は変更なし。 |
| データ損失（上書き、削除、部分公開、履歴消失） | pass | test fixtureとtagのみ変更し、製品の永続データへ触れない。 |
| ロールバック（復旧参照、状態保持、再開可能性） | pass | Gitの1実装commitをrevert可能。 |
| 範囲漏れ（呼び出し元、利用側、配布物、文書） | pass | 全件実行と8 path監査で確認。 |

## 5. 指摘

指摘なしの場合は「指摘なし」と明記する。

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| LLM-01 | Medium | Linux専用PoCをskipすると非Linuxでのfail-closed確認が消える | ローカルLLMの候補とfeature照合 | PoC integration | SCN-INT-POC-012を追加 | resolved | なし |
| OPUS-01 | Medium | SCN-INT-POC-004/010はmacOSでも実行できるとの指摘 | 両tagを一時的に外した対象実行で両件とも`/usr/bin/prlimit ENOENT`を再現 | PoC integration | 間接呼出を確認してtagを維持 | false-positive | なし |
| OPUS-02 | Low | 非Linux専用SCN-INT-POC-012は現行Linux CIで実行されない | `.github/workflows`のLinux runner設定 | CI coverage | macOS実機の全件実行と対象test結果を記録 | valid | macOS CI runner導入までは手元実行で確認 |
| OPUS-03 | Low | APFS上のNFD名は合成Git観測であり`ls-files`解析のend-to-end検査ではない | NFDを物理file名として保持できないmacOSの観測 | Unicode境界 | 既存unitと合成観測を保持 | valid | Git解析の将来変更は別のLinux環境で確認が必要 |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: 8 pathと肯定・敵対の各観点を確認。
- 指摘を確定した: ローカルLLMの候補1件を再現可能なcoverage不足として採用し、同じH_impl前に修正。最終diffの再レビューで有効指摘0件。Opusの2件実行可能という主張は対象testで反証した。
- 次ラウンド対象のCritical/High: なし。

### ラウンド2

- 未解決Critical/High: なし。追加round不要。
- 修正差分と、触れた隣接範囲: 該当なし。
- 既承認・未変更範囲を再走査していない: 該当なし。

### ラウンド3

- 全指摘の最終分類: LLM-01 resolved、OPUS-01 false-positive、OPUS-02/03 valid Lowで残存リスク記録。
- 危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: PoC本体は変更なし。
- 同じ範囲の予算を自動更新していない: 1 round。

## 7. テスト結果

- 実行したcommandの一覧: `NPM_CONFIG_CACHE=/private/tmp/asc1430-npm-cache npm test`、`npm run lint`、`npm run format:check`、`npm run typecheck`、`npm run source:check`、`npm run docs:format`、`npm run test:format`、`npm run trace:check`、`npm run architecture:check`、`npm run conformance:check`、`npm run audit:check`、`npm run package:check`、`npm run project:quality`。
- 全layerの合計: 2,250シナリオ中2,223成功・27スキップ・0失敗。18,557ステップ中18,456成功・101スキップ。12品質gate成功。
- runner・Gherkin方言: cucumber-js、英語Gherkin。

## 8. 配布物影響

packageとして配布する場合だけ記入する。配布境界はpackage manifestの配布file指定を正本とし、compileされて配布される`source`も含める。

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| docs/specs/15_要件追跡/00_追跡表.md | 入らない | なし |
| test/features/e2e/poc-mode-cli.feature | 入らない | なし |
| test/features/e2e/workflow-step-enforcement-cli.feature | 入らない | なし |
| test/features/integration/poc-mode.feature | 入らない | なし |
| test/steps/cli-usage.steps.ts | 入らない | なし |
| test/steps/poc-mode.steps.ts | 入らない | なし |
| test/steps/worktree-ignored-artifacts.steps.ts | 入らない | なし |
| test/support/world.ts | 入らない | なし |

判断: 配布物を更新しない

根拠: 差分はtestと追跡表のみで、package manifestが配布対象とするruntime sourceに変更はない。

## 9. 独立reviewの成立

PR作成前に観測できるものだけを書く。immutable review IDやapproval件数は書かない。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated |
| その要求を満たすこと | はい |
| reviewerとimplementerのidentity・context比較 | implementerはこのworktreeの実装session、reviewerは`/root/review_1430_sol`の独立session。 |
| reviewerが対象差分を変更していないこと | はい |

外部への不可逆な配布で外部証拠を要求され、かつ無い場合だけ次を記入する。承認元・承認者・承認日時・失効日時は正本を参照し複製しない。

| 項目 | 内容 |
|---|---|
| 適用する例外の識別子 | {正本fileのexceptionId} |
| 観測値 | {未実行と判定した根拠の実測値} |

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/15_要件追跡/00_追跡表.md`。
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: 新規ドメイン用語なし。
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: 既存PoC用語のみ。
- 要件・変更・SCN・テストの追跡: REQ-WF-003 → SCN-INT-POC-012 → 非Linux preflight test。
- `no-spec-impact`の場合の限定的根拠: 該当なし。
- UI・トークンの判断: test fixtureと追跡表のみで画面・token変更なし。

## 11. 総合判定と再開地点

- 未解決Critical/High: なし
- Medium/Lowの記録: LLM-01 resolved、OPUS-01 false-positive、OPUS-02/03 valid Low。
- 判定: approved
- 新しい権限が必要な事項: PR作成・pushは通常のGitHub権限を使用。
- 残存リスク: Linux専用PoCはこのmacOS環境で実行せず、Linux CIが検証する。正式local reviewer経路は`qwen3.8:27b`がallowlist外でdegradedとなったため、直接loopbackの読取専用レビューで補完した。
- 次に許可される操作: review sessionとStep 10を記録し、artifact-only commit、PR作成。
- 次回の再開地点: CIとreview evidence確認。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 1 |
| 対象SHA・文書ダイジェスト | 9e0d8eb1d168c70c92596aa79beae71b9175564b |
| 比較基点 | `4f6e655326c0703de2024a367bf13cc3e4ff882d` |
| H_impl | `9e0d8eb1d168c70c92596aa79beae71b9175564b` |
| 対象差分 | docs/specs/15_要件追跡/00_追跡表.md、test/features/e2e/poc-mode-cli.feature、test/features/e2e/workflow-step-enforcement-cli.feature、test/features/integration/poc-mode.feature、test/steps/cli-usage.steps.ts、test/steps/poc-mode.steps.ts、test/steps/worktree-ignored-artifacts.steps.ts、test/support/world.ts |
| 対象外 | 比較基点に存在し変更されていない範囲 |
| 残り予算 | 3ラウンド |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260921_083322_1430-macos-temp-fixture |
| 仕様の所有箇所 | Issue #1430、REQ-WF-003、`.agent-skill-chain/docs/02_品質基準.md`。 |
| 成果物行数 | `git diff --stat 4f6e6553..9e0d8eb1`で8 pathのtest・仕様追跡の差分。 |
| 縮小の先行評価 | 共通fixtureの`realpath`だけでは残り29失敗が解消しないため、独立したfixture・platform・APFS反例へ範囲を限定して追加是正した。 |
| 実施者・日時 | coordinator、2026-09-21T09:05:00+09:00 |

`比較基点`と`H_impl`の値は40桁の小文字hexをbacktickで囲んだものだけにする。注記・branch名・短縮SHAを同じcellへ書かない。由来は別行へ書く。

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、LLM-01 resolved、OPUS-01反証 | medium | ollama（raw loopback）・Codex Sol・Opus | `qwen3.8:27b`、Sol、Opus | 未解決Critical/Highなら停止 | Solは別context・編集0 path。Opusは読取専用・編集0 path。 |
