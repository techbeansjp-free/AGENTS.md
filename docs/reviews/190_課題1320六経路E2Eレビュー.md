# 課題1320 六経路E2Eレビュー

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 2（review session） |
| 対象SHA・文書ダイジェスト | H_impl は本表の`H_impl`行の値 |
| 比較基点 | `5e0e6520df9b2390e30c77ccabc72e6141d5493a` |
| H_impl | `96177182003fba8128a62dc9419714a00f3f784f` |
| 対象差分 | 比較基点 `5e0e6520`。監査対象は4 path |
| 対象外 | `quick`のStep 0〜10のCLI化、`automatic`の追加、`src/`の変更、#1319、#1036 |
| 残り予算 | round 2を実施。上限4に対し残り2 |
| ラウンド数 | 2 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260910_212612_6経路の状態遷移を実CLI経路で検査する |
| 仕様の所有箇所 | `.agent-skill-chain/docs/01_開発ワークフロー.md` の状態遷移節。原文「PoCと`merge.mode=disabled`は`outcome=pull-request`のStep 11終端とする。`assisted`のauthority未成立は終端ではなく`pr-bound`の再開可能な待機であり、Step 11を記録しない」 |
| 成果物行数 | 製品 `src/` は0行。支援層は test 2 file と spec 2 file。`git diff --numstat 5e0e6520..96177182` で再現する |
| 縮小の先行評価 | 既存harnessへ引数を足すだけに留め、新しいharnessも新しい判定関数も作っていない。gh stubの追加は`issue edit`と`issue view`の2分岐で、`src/adapters/github.ts`が要求する呼び出しの実測に一致する |
| 実施者・日時 | implementer: Claude（本session）。2026-09-11 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類、変異試験の実測 | standard | CodeRabbit（PR上の独立review） | provider既定 | 未解決Critical/Highがあれば停止 | reviewerはPR上のCodeRabbitで、implementerのClaudeと別identity・別context |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | Issue #1320、AC-01〜AC-07 | 7件すべてにSCNまたは全体実行が対応する | テスト出力 |
| 差分 | `5e0e6520..96177182` | 監査対象4 path | 既存コード |
| テスト | `npm test` | 1759 scenarios / 1743 passed / 16 skipped / **0 failed** | テスト出力 |
| 仕様 | `docs/specs/15_要件追跡/00_追跡表.md`、`docs/specs/01_システム概要/02_用語・略語.md` | updated | 既存文書 |
| commit前candidate | 本節の変更ファイル個別監査表 | 4 path | Git index |
| Phase A artifact | `docs/reviews/` 配下の本artifact 1 file | H_impl..H_final は本artifactだけ | Git観測 |
| review session | .agent-skill-chain/tmp/issues/20260910_212612_6経路の状態遷移を実CLI経路で検査する | round 1〜2、anchorは`5e0e6520`と`6dde8fea` | Git観測 |

**Phase Bの外部証拠を本artifactへ書かない。** PR number、Actions run ID、immutable review IDはPR作成後にしか存在せず、`review evidence`とdelivery stateがappend-onlyで保持する。

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **確認した。** testの期待値は実装からimportして導出しておらず、すべてscenario側のリテラルである。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: **確認した。**
- reviewerの独立性が`merge.reviewIndependence`の要求水準を満たす: 本repositoryは未宣言であり既定の`context-isolated`が適用される。reviewerはPR上のCodeRabbitで、implementerと別identity・別contextである。
- （Phase B。ここでは判定しない）trusted providerが観測したPR/CI/reviewが`H_final`へ一致することは、PR作成後に`review evidence`が観測して記録する。
- 既定branch追随を行った場合: **していない。** `比較基点`は着手時の`origin/main` tipである。

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `docs/specs/01_システム概要/02_用語・略語.md` | M | project | spec | 耐久用語台帳。TERM-ASC-098を所有する | pass | TERM-ASC-098 | 記述のみ | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | 追跡表 | pass | 新規SCN 5件を登録 | 記述のみ | pass |
| `test/features/e2e/workflow-step-enforcement-cli.feature` | M | project | project | E2E scenarioの宣言 | pass | AC-01〜AC-05 | scenarioの削除で戻る | pass |
| `test/steps/workflow-step-enforcement.steps.ts` | M | project | project | E2E harnessとstep実装 | pass | AC-01〜AC-06 | 引数を戻せば元に戻る | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **確認した。** `dist/`と`src/`に差分が無いため、生成物の除外規則は本変更では作用しない。**round 2の是正commitはartifactだけを変えるため、`audit:check`がartifact専用commitの連なりを遡ってH_implを`96177182`と導出する。** artifact自身は監査表へ入らない。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **確認した。** 変更は`test/`と`docs/specs/`だけである。
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: 変異試験で検出した3件はいずれも`test/steps/workflow-step-enforcement.steps.ts`の該当scenarioだけを直した。

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-000 | `src/domain/delivery.ts`の`assisted`固有分岐は**到達不能である。** 直前の`Math.max(1, requiredReviews)`が常に1以上のため、`size < 1`は手前の検査で必ず拒否される | `assisted`と`automatic`は`authorizeMerge`の時点で区別されない | なし | SCN-E2E-WFSTEP-055の覆う範囲を限定し、**存在しない門を覆ったと書かない。** 実装の是正は #1036 へ観測事実として報告した | 該当行を直接読んで確認した | なし（#1036が所有する） | pass |
| DISC-003 | PoC E2Eを`poc-mode.steps.ts`ではなく`workflow-step-enforcement.steps.ts`へ置いた。gh stubがそちらにしか無い | SCN IDが計画の`SCN-E2E-POC-003`から`SCN-E2E-WFSTEP-056`へ変わった | なし | featureとsteps fileを揃えた。既存のPOC scenarioは変更していない | SCN-E2E-WFSTEP-056が合格 | 追跡表 | pass |
| DISC-004 | PoC baselineは`createIssueStaging`時のHEADで固定される（`src/domain/issue.ts:430`） | fixtureをcommitしてからstagingを作るとbaselineとHEADが一致し`pr create`が止まる | なし | stagingをfixture commitより前に作る順序へ直した | SCN-E2E-WFSTEP-056が合格 | なし | pass |
| DISC-005 | **merge実経路のexact-HEAD拘束がE2Eで未検査だった。** 拘束を外す変異が既存のSCN-E2E-WFSTEP-031・047でも生存した | 承認そのものがある状態でheadだけ外す入力をどのscenarioも作っていなかった | なし | `reviewCommitSha`制御を足し、SCN-E2E-WFSTEP-055へ旧HEAD承認の方向を追加した | 追加後は同じ変異がKILLEDになる | なし | pass |
| DISC-006 | **SCN-E2E-WFSTEP-054の初版が緩すぎた。** `merge_pending`だけを見ており、`assisted`分岐を削除しても合格した | `pr create`は`mergeReadyVerified`を偽で渡すため`automatic`も同じ`merge_pending`になる | なし | `continuation: wait-authority`とauthority固有の`next`文面まで検査へ足した | 3変異すべてKILLED | なし | pass |
| DISC-007 | SCN-E2E-WFSTEP-053の削除変異は等価変異だった。同じ門が`handlePullRequestMerge`の早い側とlocked section内の2箇所にある | 片側ずつ消しても観測可能な挙動が変わらない。多層防御であって穴ではない | なし | 両方を消す変異でKILLEDを確認した | 実測記録を§7へ残す | なし | pass |
| DISC-008 | 期待値を2回とも推測で書いて外した。`step11.outcome`は`merge`ではなく`merged`、独立approval不足の拒否は`authorizeMerge`ではなく前段selectorの文言だった | 推測した期待値は実経路が到達しない文言を固定しうる | なし | いずれも実測へ合わせ、後者は「実経路は`authorizeMerge`へ到達しない」という事実としてコメントへ残した | SCN-E2E-WFSTEP-055が合格 | なし | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-E2E-WFSTEP-052 | full のStep 0〜11を別processのCLIで起動 | 合格 | pass | journalが`[0..11]`かつ全entryのmodeがfull |
| AC-02 | SCN-E2E-WFSTEP-053 | disabled 終端への`pr merge` | 合格 | pass | 終了値1、`pull_request_complete`、`pr merge`未送出、Step 11が1件のまま |
| AC-03 | SCN-E2E-WFSTEP-054 | assisted のauthority未成立 | 合格 | pass | `merge_pending`、`continuation: wait-authority`、Step 11不在、`pr-bound` |
| AC-04 | SCN-E2E-WFSTEP-055 | assisted の成立・不成立・旧HEAD | 合格 | pass | `outcome: merged`到達、承認無しと旧HEADは`pr merge`未送出で拒否 |
| AC-05 | SCN-E2E-WFSTEP-056 | poc のPR停止終端と`pr merge`拒否 | 合格 | pass | `outcome: pull-request`、PoC拒否、`pr merge`未送出 |
| AC-06 | 上記5件 | 変異試験 | 合格 | pass | §7に15変異の実測を記録した |
| AC-07 | 既存全件 | `npm test` | 合格 | pass | 1759 scenarios / 0 failed。**既存比 +5 で追加分と一致する** |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | merge認可とauthority判定を観測対象に含むため、検査の緩さがそのまま承認境界の未検証範囲になる | SCN-E2E-WFSTEP-055が承認あり・なし・旧HEADの3方向を固定し、DISC-005で未検査だったexact-HEAD拘束を塞いだ |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 終端状態と診断文が利用者の次の操作を決めるため、観測できること自体が要件である | SCN-E2E-WFSTEP-053とSCN-E2E-WFSTEP-054が終了値・`state`・`continuation`・`next`を名指しで固定する |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 本製品はCLIでJSONだけを出力し、Web画面もUI componentも持たない | 変更対象は`test/`と`docs/specs/`であり、style・token定義を含まない |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | design tokenとlayout tokenを持つUI層が製品に存在しない | 変更したfileにtheme・component・state・breakpointの定義を含まない |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 6経路すべてについて、対応するscenarioが実CLIの別processを起動して終端を観測する |
| 価値 | 利用者・運用上の目的を満たすか | pass | 回帰検出の対象外だった4経路が対象に入った。**うち1つ（exact-HEAD拘束）は既存の穴を実際に塞いだ** |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 新しい依存を足していない。gh stubは実networkへ到達しない |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 02の設計対象と実装が一致する。SCN IDの変更はDISC-003として記録した |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | harnessは引数追加に留め、共通の末尾を`finalizePreparedPullRequest`へ切り出して重複を作らなかった |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | 15変異を作り、等価変異と判明した2件を除く13件がすべてKILLEDである |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 拒否経路（disabled終端、承認なし、旧HEAD、PoC）はすべて`pr merge`未送出まで観測する |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | assistedの独立approvalを0件と1件の両方で観測する。Unicodeは対象外で、既存の判定を変更していない |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | fixtureは`world.temp()`配下に閉じ、実`origin`へpushしない。`--authorize=approved`の意味を弱める迂回を作っていない |
| 安全性 | 認証、承認、秘密情報、Zero Trust | finding | **D1320-01として記録した。** 追加したのは観測だけで承認境界を緩めていないが、`assisted`固有分岐が到達不能である事実は残る |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | testの追加であり書き込み契約を持たない |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | scenarioの削除と引数の既定値復帰で元に戻る |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | 配布境界に`test/`と`docs/specs/`は含まれない。`src/`と`dist/`に差分が無いことをGitで確認した |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| D1320-01 | High | **merge実経路のexact-HEAD拘束がどのE2Eでも検査されていなかった。** 拘束を外す変異が既存のSCN-E2E-WFSTEP-031・047でも生存した | 変異試験の実測 | merge認可 | `reviewCommitSha`制御を足し、旧HEADへのAPPROVEDでmergeが成立しないことをSCN-E2E-WFSTEP-055で固定した | resolved / improvement | なし。同じ変異が追加後はKILLEDになる |
| D1320-02 | High | **SCN-E2E-WFSTEP-054の初版が空虚だった。** `merge_pending`だけを見ており、`assisted`分岐を削除しても合格した | 変異試験の実測 | 検査の有効性 | `continuation`と`next`文面まで検査へ足した | resolved / fix-regression | なし |
| D1320-03 | Medium | `assisted`固有分岐が到達不能であり、`assisted`と`automatic`が`authorizeMerge`で区別されない | `src/domain/delivery.ts`の該当行 | merge認可 | **本変更では実装を直さない。** 観測事実を #1036 へ報告し、SCN-E2E-WFSTEP-055の主張を限定した | valid / out-of-scope | **残る。** #1036が所有する |
| D1320-04 | Medium | 期待値を推測で2件書いて外した | `outcome`は`merged`、拒否は前段selectorの文言 | 検査の正しさ | 実測へ合わせ、後者は実経路の事実としてコメントへ残した | resolved / fix-regression | なし |
| D1320-05 | Low | 計画のSCN IDと実装のSCN IDが食い違った | `SCN-E2E-POC-003` → `SCN-E2E-WFSTEP-056` | 追跡 | DISC-003として記録し、追跡表と03の進捗表を実装側へ合わせた | resolved / improvement | なし |
| D1320-07 | Low | **§7.1の表が15件を列挙しているのに本文3箇所が13件と書いていた。** artifactは承認証拠であり、件数の不一致は実測記録としての追跡を壊す | 独立review（CodeRabbit）の指摘 | 証跡の正しさ | 表の実件数15へ本文を合わせ、うち2件が等価変異であることを明示した | resolved / fix-regression | なし |
| D1320-06 | Low | `SCN-E2E-WFSTEP-053`の削除変異が生存した | 変異試験の実測 | 検査の有効性 | **等価変異と判明した。** 同じ門が2箇所にある多層防御で、両方を消すとKILLEDになる。fixtureは足していない | false-positive / improvement | なし |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: **はい。** 肯定5観点と敵対8観点を`96177182`へ適用した。
- 指摘を確定した: **High 2件・Medium 2件・Low 2件。** うち4件は本ラウンドで解決し、D1320-03は #1036 へ分離、D1320-06は偽陽性である。round 2でD1320-07を追加した。
- 次ラウンド対象のCritical/High: **0件。**

### ラウンド2

- 未解決Critical/High: **0件。**
- 修正差分: 本artifactの§7.1の変異件数を表の実件数へ合わせ、監査表へartifact自身を追加した。**製品codeもtestも変更していない。**
- 修正で触れた隣接範囲: 無い。artifact内の記述だけである。
- 既承認・未変更範囲を再走査していない: **していない。**

## 7. テスト結果

**手書きの件数表はそれ自体が自己申告であり、それだけを承認証拠にしない。** Phase Aとしてlocal実行の観測を記録する。

- 実行したcommandの一覧: `npm test`、`npm run conformance:check`、`npm run package:check`、静的gate 14本（directories:check・skills:check・cli:check・workflow:check・project:quality・lint・format:check・typecheck・source:check・trace:check・architecture:check・docs:format・test:format・package:check）
- 全layerの合計: **1759 scenarios / 1743 passed / 0 failed / 16 skipped。** 既存比 +5 で、追加したscenario数と一致する
- 失敗またはskipがある層: skipは16件で、いずれも本変更の対象外の既存scenarioである。**失敗は0件のため層別展開は不要である。**

**成功CI runはPhase Bの外部証拠である。** run ID・対象HEAD・conclusionはPR作成後にしか観測できないため本artifactへ書かず、`review evidence`とdelivery stateが記録する。

runnerは`agent-skill-chain-project/cucumber-js`、`projectChoices.gherkinDialect`は`en`である。

### 7.1 変異試験の実測

**経路ごとに削除・狭窄・空洞化の3種を作った。** 変異のたびに`npm run build`を行った。**#1317で`dist/`を再buildせず変異が届かず生存と誤読した実測があるためである。**

| 変異 | 対象 | 結果 |
|---|---|---|
| full列をquick列へ狭める | `MODE_STEP_SEQUENCES.full` | KILLED |
| `requiredSteps`を常にquick列へ倒す | `requiredSteps` | KILLED |
| `step11-recorded`分岐を早い側だけ削除 | `handlePullRequestMerge` | **等価変異（SURVIVED）** |
| 同じ分岐を権威側だけ削除 | locked section | **等価変異（SURVIVED）** |
| 同じ分岐を両方削除 | 両方 | KILLED |
| 終了値を1から0へ変える | `pull_request_complete`の返却 | KILLED |
| `pullRequestTerminal`を常に偽にする | 同上 | KILLED |
| `assisted`分岐を削除 | `decideDeliveryContinuation` | KILLED（初版ではSURVIVED。D1320-02） |
| `wait-authority`を`invoke-pr-merge`へ変える | 同上 | KILLED |
| `merge_pending`条件から`wait-authority`を外す | `pr create` | KILLED |
| `assisted`を`stop-at-pr`へ倒す | `decideDeliveryContinuation` | KILLED |
| `APPROVED`判定を外す | `currentIndependentApprovals` | KILLED |
| exact-HEAD一致を`!== undefined`へ広げる | 同上 | KILLED（追加前はSURVIVED。D1320-01） |
| PoC終端判定を削除 | `decideDeliveryContinuation` | KILLED |
| PoCの`pr merge`拒否を削除 | `assertWorkflowMergeAllowed` | KILLED |

**生存2件はいずれも等価変異であることを、両方を同時に消す変異で確かめた。** 反射的にfixtureを足していない。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `docs/specs/01_システム概要/02_用語・略語.md` | 入らない | なし |
| `docs/specs/15_要件追跡/00_追跡表.md` | 入らない | なし |
| `test/features/e2e/workflow-step-enforcement-cli.feature` | 入らない | なし |
| `test/steps/workflow-step-enforcement.steps.ts` | 入らない | なし |

判断: 配布物を更新しない

根拠: 配布境界は`package.json`の`files`が単一正本であり、`dist/bin/`・`dist/src/`・`dist/vendor/`・`.agent-skill-chain/`配下の指定path・`README.md`・`AGENTS.md`・`CLAUDE.md`である。**本変更の4 pathはいずれも含まれない。** `src/`と`dist/`に差分が無いことをGitで確認しており、runtimeの利用条件は変わらない。

## 9. 独立reviewの成立

**この節はPhase Aで埋める。** PR作成後にしか存在しない値は書かず、`review evidence`が記録する。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | context-isolated（本repositoryの`merge.reviewIndependence`は未宣言であり既定が適用される） |
| その要求を満たすこと | はい（reviewerはPR上のCodeRabbitで、implementerのClaudeと別identity・別contextである） |
| reviewerとimplementerのidentity・context比較 | implementerはClaude（本session）。reviewerはPR上で動くCodeRabbitである。**同一processでも同一contextでもない** |
| reviewerが対象差分を変更していないこと | はい（reviewerはPR上のコメントだけを出力し、commitを作らない。是正commitはすべてimplementer側が作成する） |

**承認元、承認者、承認日時、失効日時は正本を参照し、ここへ複製しない。**

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/15_要件追跡/00_追跡表.md`、`docs/specs/01_システム概要/02_用語・略語.md`
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: TERM-ASC-098は00の候補、01の確定差分、耐久台帳の順に一方向へ追跡できる。
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: **確認した。** 既存語の再定義は行っていない。
- 要件・変更・SCN・テストの追跡: AC-01〜AC-07 → SCN-E2E-WFSTEP-052〜056 → `test/features/e2e/workflow-step-enforcement-cli.feature`と同steps
- `no-spec-impact`の場合の限定的根拠: 該当しない。
- UI・トークンの判断: UIなし。DC-UXとDC-TOKENSがnot-applicableである。

## 11. 総合判定と再開地点

- 未解決Critical/High: **0件。** D1320-01とD1320-02は本ラウンドで解決した。
- Medium/Lowの記録: §5へ記録した。D1320-03だけを #1036 へ分離し、他は解決または偽陽性である。
- 判定: approved
- **AIによる最終裁定: approved。** 6経路すべてが実CLI経路で観測される状態になり、追加した検査が空虚でないことを15変異で確かめた。**`src/`を1行も変更していないため、利用者の挙動は変わらない。**
- 新しい権限が必要な事項: なし。
- 残存リスク: **`assisted`固有分岐が到達不能である事実（D1320-03）が残る。** 実装の是正は #1036 が所有する。本変更はその事実を隠さず、SCN-E2E-WFSTEP-055が覆う範囲をコメントで限定している。
- 次に許可される操作: `workflow record --step=10`、`pr create`、CI確認、`pr merge --merge`。
- 次回の再開地点: #1319、#1036。
