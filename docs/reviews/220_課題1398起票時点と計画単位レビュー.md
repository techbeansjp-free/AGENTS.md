# 04 レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

**本templateを埋めた成果物は版管理下へ置く。** 一時ステージングに置いたままでは`review evidence`も履歴監査も成立しない。`docs/reviews/`または`.agent-skill-chain/reviews/`配下へ複写し、実装commitの後にその1 fileだけをcommitする。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 2 |
| 対象SHA・文書ダイジェスト | 2bb7bd4b2277550a4b87f0dfb9caad445f8278fd |
| 比較基点 | `dc479419982dbba973bc022fadff2adc718abe00` |
| H_impl | `2bb7bd4b2277550a4b87f0dfb9caad445f8278fd` |
| 対象差分 | .agent-skill-chain/00_利用案内.md、.agent-skill-chain/docs/01_開発ワークフロー.md、.agent-skill-chain/skills/step-00-stage/SKILL.md、.agent-skill-chain/skills/step-04-issue-sync/SKILL.md、.agent-skill-chain/templates/00_利用案内.md、.agent-skill-chain/templates/planning/00_利用案内.md、.agent-skill-chain/templates/planning/01_計画単位.md、docs/specs/01_システム概要/02_用語・略語.md、docs/specs/02_要件/00_要件一覧.md、docs/specs/02_要件/01_ワークフロー要件.md、docs/specs/15_要件追跡/00_追跡表.md、docs/specs/15_要件追跡/01_変更履歴.md、scripts/check_directory_guides.ts、test/features/integration/issue-admission-timing-distribution.feature、test/features/unit/issue-admission-timing.feature、test/steps/issue-admission-timing-distribution.steps.ts、test/steps/issue-admission-timing.steps.ts |
| 対象外 | 比較基点に存在し変更されていない範囲 |
| 残り予算 | counted round 4、収束後のHEAD移動に対する取り直し2 |
| ラウンド数 | 2 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260915_044239_起票時点の規律と計画単位を規範へ加える |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-023。着手時は同fileにREQ-WF-002がIssue計画のtracker同期を、REQ-WF-022が分割判断を所有するが、**どちらもGitHub Issueをいつ作ってよいかを所有していなかった**。本変更がその空白を同fileへ追加した |
| 成果物行数 | 成果物105追加0削除（`.agent-skill-chain/`配下）、支援層358追加0削除（`docs/specs/`・`test/`・`scripts/`）。比3.41倍。閾値判定はせず記録する |
| 縮小の先行評価 | 最小形は規範1節とStep skillへの案内だけ（約20行）で規律は成立する。templateは利用者が毎回ゼロから書く手間を消すためのもので規律の成立条件ではないと03へ明記し、行数超過時の削除対象に指定した。validator・CLI・schema・provider adapterは00 §2.2で対象外と宣言し実装していない |
| 実施者・日時 | reviewer: 独立context（Claude、toolsあり読み取り専用）2026-09-15。coordinatorがGit・gate・変異の一次証拠を照合 |

`比較基点`と`H_impl`の2行は`review reanchor`と`audit:check`が機械的に読む。値は40桁の小文字hexをbacktickで囲んだものだけにし、注記・branch名・短縮SHAを同じcellへ書かない（識別行が一意に解決できず`identity-unresolvable`で拒否される）。由来の説明は`比較基点の由来`のような別行へ書く。

### 0.1 routing入力契約

providerとmodel設定はproject choiceとrouting evidenceの観測値を用い、固有のmodel slugだけからreview authorityを推測しない。

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定5観点、敵対8観点、finding 14件、自作変異22件 | advanced（risk low・外部契約変更true・配布interface） | claude（project choiceの上限Opus） | project_default、effort high | 未解決Critical/Highがあれば停止しfallbackしない | implementerのcontextと別sessionで起動。reviewerが変更したpathは0件で、`git status`が開始時と同一であることを本人が報告 |

## 1. 入力証拠

レビュー証拠は二段階で完成させる。**Phase Aはtracked review artifactであり、PR作成前に確定する。** 製品・仕様・testを固定した`H_impl`の後、tracked review artifactだけを加えた`H_final`を作る。`H_impl`は`H_final`のancestor、commit間差分はproject policyが選ぶevidence-only pathだけとし、artifact path、SHA-256、Git blob OIDを観測する。**Phase Aで観測するのは`H_impl` commit SHA/author ID、repository、review sessionのanchorとroundである。** **PR number、Actions run ID、immutable review IDはPR作成後にしか存在しないので、tracked review artifactへ書かない**。旧版はこれらをPhase Aへ要求しつつ「H_final後はartifactを更新しない」と定めていたため、**PR作成前は埋められず作成後は直せない循環になっていた。**

**Phase Bはtracked artifactの外にある。** PR作成後、trusted providerからPR number/current head/author ID、Actions run ID/event/head/conclusion/関連PR番号を実観測し、**`review evidence`とdelivery stateへappend-onlyで記録する。** `actor-independent`ではimmutable review ID/commit/user ID/submittedAt/stateもproviderから観測する。PR・成功CIは`H_final`へ一致させ、approvalは下のmode別権限に従う。head変更時は証拠を作り直す。caller申告actor、任意JSON、別PRのrun、COMMENTED、未完了CIは承認証拠にしない。

**reviewerの独立性はproject policyの`merge.reviewIndependence`が決める**。`context-isolated`（未宣言時の既定）はimplementerと別session/contextであること、exact HEADを固定したこと、reviewerが対象差分を変更していないこと、肯定・敵対レビューとfindingの記録を要求する。**同一GitHub actorでも成立し、tracked artifactの`approved`と保存済みreview session/Step 10 bindingがformal approvalになる。** `actor-independent`はPR authorおよびobserved implementation commit authorと別のstable actor IDのprovider `APPROVED`を要求し、**高リスク変更・不可逆操作・release・外部公開でproject policyが宣言して引き上げる。** 両modeともexact HEAD一致は必須である。tracked文書へ自身のcommit SHAを書かず、H_final後はartifactを更新しない。

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | .agent-skill-chain/tmp/issues/20260915_044239_起票時点の規律と計画単位を規範へ加える | staging digest f4c3822c3520e550fd75c7492c6ba435a3067a992509ba059b671db2f32975d0 | 既存コード |
| 差分 | `dc479419982dbba973bc022fadff2adc718abe00`..`2bb7bd4b2277550a4b87f0dfb9caad445f8278fd` | 17 path | 既存コード |
| テスト | `npm run test:unit`・`npm run test:integration`・変異13件 | unit 1223合格、integration 689（673合格16skip）、変異生存0 | テスト出力 |
| 仕様 | `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-023ほか5 file | updated。`trace:check`合格 | Git差分と既存文書 |
| commit前candidate | .agent-skill-chain/00_利用案内.md、.agent-skill-chain/docs/01_開発ワークフロー.md、.agent-skill-chain/skills/step-00-stage/SKILL.md、.agent-skill-chain/skills/step-04-issue-sync/SKILL.md、.agent-skill-chain/templates/00_利用案内.md、.agent-skill-chain/templates/planning/00_利用案内.md、.agent-skill-chain/templates/planning/01_計画単位.md、docs/specs/01_システム概要/02_用語・略語.md、docs/specs/02_要件/00_要件一覧.md、docs/specs/02_要件/01_ワークフロー要件.md、docs/specs/15_要件追跡/00_追跡表.md、docs/specs/15_要件追跡/01_変更履歴.md、scripts/check_directory_guides.ts、test/features/integration/issue-admission-timing-distribution.feature、test/features/unit/issue-admission-timing.feature、test/steps/issue-admission-timing-distribution.steps.ts、test/steps/issue-admission-timing.steps.ts | H_impl 2bb7bd4b2277550a4b87f0dfb9caad445f8278fd | Git index |
| Phase A artifact | 本file | H_impl `2bb7bd4b`の子として本fileだけをcommitしH_finalとする | Git観測 |
| review session | 同staging | session `8fc4d0118f3f68da133538f9a6314f949ff7d84783312c0b1400a48d788ce5c6`、round 2で`converged` | ASC保存session |

**Phase Bの外部証拠をこの表へ書かない。** PR number、Actions run ID、immutable review IDはPR作成後にしか存在しない。**それらは`review evidence`とdelivery stateがappend-onlyで保持する正本であり、tracked review artifactは参照先を持たない。**

重要判断を推論だけで承認しない。新しい権限が必要な場合は、対象操作と決定権者を明示する。

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: はい。本artifactは自身のcommit SHAを持たず、H_implを名指しするだけである。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: はい。本fileだけを`2bb7bd4b`の子としてcommitする。
- reviewerの独立性が`merge.reviewIndependence`の要求水準を満たす: はい。policyに宣言が無く既定の`context-isolated`を適用し、implementerと別session・別contextで実施した。詳細は§9。
- **（Phase B。ここでは判定しない）** trusted providerが観測したPR/CI/reviewが`H_final`へ一致することは、PR作成後に`review evidence`が観測して記録する: 本artifactへは書かない。
- 既定branch追随を行った場合の条件: 該当なし。本branchは`dc479419`から分岐して以降、既定branchの追随を行っていない。

### 1.1 変更ファイル個別監査

追加・変更・削除した全ファイルを省略せず1ファイル1行で記録する。まとめ行、directory単位の一括承認、test成功だけによる代替を認めない。

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/00_利用案内.md` | M | package | package | 中央索引へ新directory 1行 | 索引→guideの一方向 | REQ-WF-023 | 1行revert | pass |
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | M | package | package | **規律の唯一の所有者**。耐久トラッカー段落の直後へ配置し導出元と隣接させた | 規範は下流を参照しない | AC-WF-023 / SCN-UNIT-ADMIT-001〜004 | 節削除でrevert | pass |
| `.agent-skill-chain/skills/step-00-stage/SKILL.md` | M | package | package | 入口link。本文複製なしを明記 | skill→規範 | SCN-UNIT-ADMIT-005 | 2行revert | pass |
| `.agent-skill-chain/skills/step-04-issue-sync/SKILL.md` | M | package | package | 規律が拘束する操作を持つStepへの入口（round 1 F-13） | skill→規範 | AC-WF-023 | 2行revert | pass |
| `.agent-skill-chain/templates/00_利用案内.md` | M | package | package | templates配下の実導線へplanning行（round 1 F-05） | 索引→guide | REQ-WF-023 | 1行revert | pass |
| `.agent-skill-chain/templates/planning/00_利用案内.md` | A | package | package | directory入口。peerと同じ5見出し（round 1 F-10） | guide→規範・template | SCN-INT-ADMIT-007 | file削除 | pass |
| `.agent-skill-chain/templates/planning/01_計画単位.md` | A | package | package | 計画単位の雛形。進捗・担当・優先度の列を持たない | template→規範（文言参照） | SCN-UNIT-ADMIT-006 | file削除 | pass |
| `docs/specs/01_システム概要/02_用語・略語.md` | M | package | spec | TERM-ASC-117・118。既存IDと衝突なし | 用語→要件 | TERM-ASC-117/118 | 2行revert | pass |
| `docs/specs/02_要件/00_要件一覧.md` | M | package | spec | REQ-WF-023行。REQ-WF-022直後 | 一覧→要件本文 | REQ-WF-023 | 1行revert | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | package | spec | REQ-WF-023本文とAC-WF-023 | 要件→実装path | AC-WF-023 | 追加分revert | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package | spec | unit行とintegration行。`trace:check`合格 | 要件→SCN→feature→実装 | 全SCN | 2行revert | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | header区切りの直後へ挿入し末尾の移行表へ混入していない | 記録のみ | 全ID | 1行revert | pass |
| `scripts/check_directory_guides.ts` | M | package | package | `ENTRY_DOCUMENTS`へ1 entry。無いと新directoryが入口未定義で落ちる | script→filesystem | SCN-INT-ADMIT-007 | entry削除 | pass |
| `test/features/integration/issue-admission-timing-distribution.feature` | A | package | evidence | 配布経路1 scenario | feature→steps | SCN-INT-ADMIT-007 | file削除 | pass |
| `test/features/unit/issue-admission-timing.feature` | A | package | evidence | unit 6 scenario | feature→steps | SCN-UNIT-ADMIT-001〜006 | file削除 | pass |
| `test/steps/issue-admission-timing-distribution.steps.ts` | A | package | evidence | `checkDirectoryGuides`で入口充足を判定（round 1 F-02） | steps→scripts・src | SCN-INT-ADMIT-007 | 隔離dirのみ書く | pass |
| `test/steps/issue-admission-timing.steps.ts` | A | package | evidence | 節を切り出して判定。fragment・表header・限定句まで観測（round 1 F-04/F-07/F-14） | steps→filesystem | SCN-UNIT-ADMIT-001〜006 | 読み取りのみ | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: はい、17件。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: はい。templateとguideは具体的なtracker名・期間・採番済みIDを持たず、guideが明示的に禁止する。`src/`・`schemas/`・`policy/`・CLIへの変更は0行。
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: はい。round 2の修正は9 pathに限られ、上流の00は目的・scopeが不変のため変更していない。

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

モード別実装計画（fullは03、quickとpocは集約00）の発見記録を全件転記せずIDで参照し、最終状態とEvidenceを確認する。目的、scope、受け入れ条件、security境界、不可逆操作を変えなかった発見は上流再起動を要求しない。契約を変えた発見は、影響する成果物だけが再確定されていることを確認する。

発見IDはモード別実装計画（fullは03、quickとpocは集約00）の`DISC-*`と同じ字面を使い、本文書内で別IDに言い換えない。

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | `workflow verification-set`へ設計どおりの入力を渡した選定結果に`integration-test`が含まれた。02設計の初版は「test layerは`unit`だけ」と書いていた | AC集合が1件拡大。目的・scope・security境界・不可逆操作は不変 | あり（受け入れ条件） | SCN-INT-ADMIT-007を追加し01 §9・§10・§6.2と02 §10・§11を再確定。00は変更しない | `workflow verification-set`の出力に`integration-test`が含まれること、および隔離rootでの導入結果 | updated | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-WF-023 | SCN-UNIT-ADMIT-001 | 規範節の起票条件・導出・限定句 | 合格 | pass | `npm run test:unit` 1223 scenarios全合格 |
| AC-WF-023 | SCN-UNIT-ADMIT-002 | 規範節の対象外4種の列挙 | 合格 | pass | 同上。変異R-M3がkillされることを確認 |
| AC-WF-023 | SCN-UNIT-ADMIT-003 | 規範節の既定1週間と変更可能性 | 合格 | pass | 同上。変異M-05・M-06・R-M14がkill |
| AC-WF-023 | SCN-UNIT-ADMIT-004 | 規範節の分割単位に関する記述 | 合格 | pass | 同上 |
| AC-WF-023 | SCN-UNIT-ADMIT-005 | Step 0 skillのlinkとfragment | 合格 | pass | 同上。変異R-M8・R-M9がkill |
| AC-WF-023 | SCN-UNIT-ADMIT-006 | 計画単位templateの5欄と表header | 合格 | pass | 同上。変異R-M21がkill |
| AC-WF-023 | SCN-INT-ADMIT-007 | 導入先への配置と入口充足 | 合格 | pass | `npm run test:integration` 689 scenarios（673合格16skip） |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | not-applicable | 認証・認可・信頼境界・秘密情報のいずれも扱わず、追加するのは文書とMarkdown雛形だけである | 変更17 pathに`src/lib/security.ts`と認可経路を含まない。`src/`への変更0行 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | not-applicable | ログ・保持・rotation・監視・復旧のいずれも追加・変更しない。journalの記録項目も変えない | `workflow record`の入出力に差分なし。案内の追加は観測機構ではない |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 画面契約とa11y検証の対象を持たない。成果物はMarkdown文書で、CLIの出力形式も変更していない | 変更17 pathにUI sourceが無く、CLI出力に差分0 |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | component、theme、breakpoint、layout tokenを変更しない | project choicesにUI・token項目が無く、変更pathはMarkdownとTypeScriptのみ |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 規範1節・Step 0とStep 4のlink・template 2 fileが実在し、linkとfragmentが解決する。round 1で指摘されたcommitの多義を「着手を決めた時点」へ閉じた |
| 価値 | 利用者・運用上の目的を満たすか | pass | `issue create`がGitHub Issueを作らず、Step 4が既存Issueを前提に開始する構造を実測で確認。その間隙に判断の拠り所が置かれた |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 強制点を持たないため既存projectは無視しても動作が変わらない。既定1週間と90パーセンタイルという変更基準が実行可能 |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | round 1で「前掲」が後掲であること、要件本文の限定不足、用語のRQ/BR ID不整合を指摘され、いずれも是正済み。`trace:check`合格 |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 規律の本文を規範文書1箇所が所有し、skillとtemplateは参照だけを持つ。複製して規範を空にする変異M-07がkillされることで強制を確認 |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | round 1でreviewerが22変異を構成し18が生存。うち安価に殺せる5件を是正し再実行で全kill。意味反転型7件は字面一致judgementでは原理的に殺せないため、blockingにせず限界として記録する |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | round 1 F-02で`doctor().healthy`が入口不足を観測しないことを実証され、`checkDirectoryGuides`へ替えて診断文字列を名指しした |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | `extractSection`の節境界が次見出しまで取るため既存段落を含む。round 1 F-04でこれにより`/期限/`が無関係な文で充足されると指摘され、列挙を具体化して解消 |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | packageへproject固有値を持ち込めないことをguideが明示的に禁止。integration testの書き込み先は`fs.mkdtemp`の隔離directoryに限る |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | `src/`・`schemas/`・`policy/`・CLIへの変更0行。追加した記述は拒否・停止・承認の判定経路を持たない |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | 削除0行。既存段落・既存表への上書きなし。17 fileすべて追加のみ |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | 追加のみのため単独revertで戻る。`ENTRY_DOCUMENTS`とdirectoryは同一commit内で対であり片方だけ残る状態を作らない |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | round 1でtemplates導線の欠落（F-05）とStep 4へのlink欠落（F-13）を指摘され是正。F-06（配布必須listへの登録）は故障注入証跡を破壊するため採らず、follow-upへ分離した |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| F-01 | High | 「着手をcommitした時点」のcommitが多義で、Git読みではgitignore下のstagingを持つASC自身のStep 4が本節へ違反する | 規範本文と`.gitignore:18`とstep-04 SKILL契約の突合 | 規範文書1節、TERM-ASC-117、REQ-WF-023本文 | 「着手を決めた時点」へ置換し、Git commitの有無を指さないことと、Step 4が本節に反しないことを明記 | resolved | なし。限定句を落とす変異NEW-1がkillされる |
| F-02 | High | SCN-INT-ADMIT-007のassertion名が測る量と食い違う。`doctor().healthy`は入口充足を観測しない | 隔離rootで`doctor.healthy=true`のまま`checkDirectoryGuides.valid=false`を実証 | integration層のassertion | `checkDirectoryGuides`へ替え、診断文字列を名指しし、`entries`で走査も確認 | resolved | なし |
| F-03 | Medium | 「前掲の基準」が実際は後掲 | 追加節より前に分割の語が0件 | 規範文書1行 | 後掲へ訂正し所有節名を明記 | resolved | なし |
| F-04 | Medium | 節境界の過大取りで対象外列挙のassertionが無関係な既存文で充足される | 変異R-M3が生存 | unit assertion | 列挙を法令・契約上の期限と他チームからの依存要求まで具体化 | resolved | `extractSection`は既存段落を含んだままだが、具体化により誤充足は解消 |
| F-05 | Medium | templates配下の実導線にplanning行が無い | 両索引の表を突合 | 導線1行 | `templates/00_利用案内.md`へ追加 | resolved | なし |
| F-06 | Medium | 配布必須資産listにplanningの2 fileが未登録 | peerのguideは登録済み | 配布検査の一貫性 | **採らない。** 追加すると故障注入証跡SCN-INT-CONSUMER-005が破壊されることを実測。#1221が同じ取引を評価し支援層が重すぎると記録済み | out-of-scope | 配布自体は`files`が`templates/`をdirectory丸ごと含むため保証されている。required集合と接続部の分離はfollow-up |
| F-07 | Medium | linkのfragmentを捨てるため誤ったanchorが生存する | 変異R-M8とR-M9が生存 | unit assertion | fragmentを節見出しと突合する判定を追加 | resolved | なし |
| F-08 | Medium | 支援層が成果物の3.41倍 | `git diff --numstat`の層別集計 | 保守費用 | 行数削減ではなく検出力回復を選択。生存変異5件を全kill | valid | 比率は残る。観測値として記録し閾値判定はしない |
| F-09 | Low | 変更履歴のHEAD SHA欄が未置換の指示文 | 当該行 | 記録1行 | 作業treeとbase SHAの形へ置換 | resolved | なし |
| F-10 | Low | 新guideに正本見出しが無い | peerのguideは5見出し | guide 1件 | 正本見出しを追加 | resolved | なし |
| F-11 | Low | 用語が引くRQ-1398-01等が解決しない | grepで0件 | 用語2行 | RQ-01・BR-01・RQ-02・BR-03へ訂正 | resolved | なし |
| F-12 | Low | 「既存の検査を変更しない」がENTRY_DOCUMENTS編集と食い違う | 差分と要件本文の突合 | 要件本文1文 | 本要件が追加したdirectory以外という限定を追加 | resolved | なし |
| F-13 | Low | 規律が拘束する操作を持つStep 4にlinkが無い | step-04 SKILL契約 | 入口2行 | Step 4へlinkを追加 | resolved | なし |
| F-14 | Low | 5見出しの存在だけを見るため表行削除が生存する | 変異R-M21が生存 | unit assertion | 完了条件・タスク・持ち越しの3節で表header行まで判定 | resolved | なし |

指摘なしの場合は「指摘なし」と明記する。

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: はい。独立reviewerが肯定5観点・敵対8観点・変更ファイル個別監査15 path・自作変異22件を実施した。
- 指摘を確定した: 14件。High 2、Medium 6、Low 6。判定は`changes-requested`。
- 次ラウンド対象のCritical/High: F-01とF-02の2件。Criticalは0件。

### ラウンド2

- 未解決Critical/High: なし。F-01とF-02はいずれも`resolved`。
- 修正差分: 前進commit `2bb7bd4b`で9 path。amendは使っていない。
- 修正で触れた隣接範囲: F-01の語変更に追随してunit assertionを更新し、限定句を落とす変異NEW-1を追加してkillを確認した。round 1で生存していたR-M3・R-M8・R-M9・R-M14・R-M21を再実行し、6変異すべてkill・生存0。
- 既承認・未変更範囲を再走査していない: はい。上流の00は目的・scopeが不変のため変更していない。

### ラウンド3

- 全指摘の最終分類: round 2で確定済み。resolved 12、valid 1（F-08、観測値として記録）、out-of-scope 1（F-06、follow-upへ分離）。
- 任意の危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: 追加の危険範囲なし。本変更は強制点を持たず、17 fileすべて追加のみで削除0行。
- 同じ範囲の予算を自動更新していない: はい。`review round`は2で`converged`となり、counted roundは4残っている。
- AIによる最終裁定: 未解決Critical/Highなしで承認。F-06は故障注入証跡の破壊を実測したうえで#1221の先例に従い採らないと判断した。

## 7. テスト結果

- 実行したcommandの一覧: `npm run verify:distribution`（project:quality→quality→build→docs:format→test:format→trace:check→architecture:check→conformance:check→audit:check→package:check）、および`npm run test:unit`・`npm run test:integration`・`npx cucumber-js --name ADMIT`。
- 全layerの合計: unit 1223 scenarios全合格。integration 689 scenarios（673合格・16 skip・失敗0）。**`audit:check`だけは本artifactのcommit前に実行したため不合格であり、これはreview artifactが未commitである状態に起因する。** artifact commit後に再実行して合格を確認する。
- skipがある層: integrationの16 scenario・50 stepは既存の条件付きskipであり、本変更の失敗ではない。
- runner・Gherkin方言: `cucumber-js`、`en`。説明は日本語。
- conformance: project rule 21件、orphan 0件、I1〜I12、固定model slug 0件で合格。
- 変異試験: 実装者の7変異（削除4・値の空洞化2・走査回避1）とround 1 reviewerの生存5変異、およびF-01の限定句を落とすNEW-1の合計13変異を実行し、**生存0**。

## 8. 配布物影響

projectがpackageとして配布される場合だけ記入する。配布境界はpackage manifestの配布file指定を単一正本とし、compileされて配布される`source`も配布境界に含める。

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `.agent-skill-chain/00_利用案内.md` | 入る | 中央索引に`templates/planning/`の行が増える |
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | 入る | **起票時点と計画単位の節が増える。利用者が読む規律そのもの** |
| `.agent-skill-chain/skills/step-00-stage/SKILL.md` | 入る | Step 0開始前に規律を読む案内が増える |
| `.agent-skill-chain/skills/step-04-issue-sync/SKILL.md` | 入る | 規律への入口linkが増える |
| `.agent-skill-chain/templates/00_利用案内.md` | 入る | templates導線に`planning/`が増える |
| `.agent-skill-chain/templates/planning/00_利用案内.md` | 入る | 新directoryの入口文書が届く |
| `.agent-skill-chain/templates/planning/01_計画単位.md` | 入る | **計画単位の雛形が届く。利用者が複製して使う** |
| `docs/specs/`配下5 file | 入らない | repository内部の仕様と追跡。`package.json`の`files`に含まれない |
| `scripts/check_directory_guides.ts` | 入らない | `files`に`scripts/`が無いため配布されない |
| `test/`配下4 file | 入らない | 同上 |

判断: 配布物を更新した

{上のどちらか一方の行だけを残し、他方は削除する。`判断:`の行は1件だけにする}

根拠: 更新先は`.agent-skill-chain/docs/01_開発ワークフロー.md`の新節、`skills/step-00-stage/SKILL.md`と`skills/step-04-issue-sync/SKILL.md`のlink、`templates/planning/`の2 file、および索引2 file。**runtimeの挙動は変えていない**が、利用者が読む規律を追加したため配布文書の更新にあたる。

**runtimeの挙動が変わるのに配布文書が追随していない状態を残さない。** 利用者は配布物だけを読むため、repository内部の仕様書へ書いても届かない。

## 9. 独立reviewの成立

**この節はPhase Aで埋める。** 記入するのはPR作成前に観測できるものだけであり、**immutable review IDやapprovalの件数などPR作成後にしか存在しない値は書かない。** それらはPhase Bで`review evidence`が観測して記録する。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | `context-isolated`。project policyに`merge.reviewIndependence`の宣言が無いため既定を適用した |
| その要求を満たすこと | はい。implementerと別sessionかつ別contextで起動し、exact HEAD `2bb7bd4b` に固定した |
| reviewerとimplementerのidentity・context比較 | implementerはcoordinator sessionが実行。reviewerは別contextで起動し、渡した入力は比較基点・H_impl・差分fileと正本pathだけである。同一provider（claude）だが`context-isolated`は同一actorでも成立する |
| reviewerが対象差分を変更していないこと | はい。reviewerは変異を一時directoryとメモリ上の複製で実施し、`git status`が開始時と同一であることを自ら報告した。round 1後の修正はcoordinatorが前進commit `2bb7bd4b` として別に行った |

外部への不可逆な配布で独立reviewの外部証拠を要求され、かつそれが無い場合だけ記入する。

| 項目 | 内容 |
|---|---|
| 適用する例外の識別子 | {正本fileのexceptionId} |
| 観測値 | {未実行と判定した根拠の実測値} |

**承認元、承認者、承認日時、失効日時は正本を参照し、ここへ複製しない。**

例外が正本に無い、または失効している場合は進めない。記録があれば実装・PR・mergeは進み、mergeを契機に自動で走るtag作成とReleaseも進む。止めても不可逆な行為を防げないためである。package registryへの公開など外部への不可逆な配布だけが、記録の有無にかかわらず独立reviewの外部証拠を要求する。削除、force push、履歴書き換えは独立reviewの対象外であり、既存のauthority経路で守る。

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/02_要件/00_要件一覧.md`（REQ-WF-023行）、`02_要件/01_ワークフロー要件.md`（REQ-WF-023本文とAC-WF-023）、`01_システム概要/02_用語・略語.md`（TERM-ASC-117・118）、`15_要件追跡/00_追跡表.md`（unit行とintegration行）、`15_要件追跡/01_変更履歴.md`（1行）。
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: はい。00の候補（TERM-ASC-117・118）→01の確定差分→耐久台帳の順で、逆流していない。
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: はい。既存のTERM-ASC-001〜116を再定義せず、廃止も行っていない。round 1 F-11で指摘された根拠IDの不整合は是正済み。
- 要件・変更・SCN・テストの追跡: REQ-WF-023 → AC-WF-023 → SCN-UNIT-ADMIT-001〜006・SCN-INT-ADMIT-007 → feature 2 file → 実装。`npm run trace:check`合格。
- `no-spec-impact`の場合の限定的根拠: 該当しない。配布contractを変更するため`updated`とした。
- UI・トークンの判断: DC-UXとDC-TOKENSはともに`not-applicable`。変更pathにUI sourceが無い。

## 11. 総合判定と再開地点

- 未解決Critical/High: なし。round 1のHigh 2件（F-01・F-02）はいずれもround 2で`resolved`。
- Medium/Lowの記録: Medium 6のうち5件resolved、F-06はout-of-scopeとしてfollow-upへ分離。Low 6はすべてresolved。F-08（支援層比3.41倍）はvalidのまま観測値として記録し、閾値判定はしない。
- 判定: approved
- 新しい権限が必要な事項: なし。merge、Issue close、branch削除、release、公開は本reviewの承認に含めない。
- 残存リスク: (1) 字面一致judgementでは意味反転型の変異を原理的に殺せない。round 1 reviewerの指摘どおりで、限界として記録する。(2) F-06の配布必須listへの登録は`check_package_contents.ts`の`required`集合と接続部が同一fileに同居するため採れない。配布自体は`files`の`templates/`で保証されている。分離はfollow-up Issueとする。
- 次に許可される操作: 本artifactを`H_impl`の子として単独commitし、`audit:check`の合格を確認したうえで`pr create`へ進む。
- 次回の再開地点: review session `8fc4d0118f3f68da133538f9a6314f949ff7d84783312c0b1400a48d788ce5c6`、round 2、`converged`。H_impl `2bb7bd4b2277550a4b87f0dfb9caad445f8278fd`。
