# 04 レビュー

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 2 |
| 対象SHA・文書ダイジェスト | `a46dc0d90db6dc8dbcc35e2c26beeb2e2a43906b` |
| 比較基点 | `cef40bf3b714e7f9075d86cf7eb393131aefc05d` |
| H_impl | `a46dc0d90db6dc8dbcc35e2c26beeb2e2a43906b` |
| 対象差分 | `cef40bf3b714e7f9075d86cf7eb393131aefc05d..a46dc0d90db6dc8dbcc35e2c26beeb2e2a43906b`。10 file、1066挿入、0削除 |
| 対象外 | 全ローカルgateの二重実行、gitignore対象の汎用除外機構、個別gateの除外範囲の変更、CIでの強制、Windows drive pathへの対応 |
| 残り予算 | 同一範囲で最大3ラウンドのうち2ラウンドを使用。残り1ラウンド |
| ラウンド数 | 2 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260906_031312_ローカルgateの走査境界がignored生成物で変わることを差分として観測できない |
| 仕様の所有箇所 | `docs/specs/02_要件/04_仕様・品質管理要件.md`のREQ-SQ-032（本変更で新設）。引用: 「**gateの判定差分を測らない。** 除外述語はgate全体ではなくgate内の一部の検査へ適用される」 |
| 成果物行数 | 製品の変更行数 289行（domain 231、script 変更後の実体、要件・追跡 58）。支援層 341行（test 341）。stagingの00〜03は838行 |
| 縮小の先行評価 | 全gate二重実行harnessを`conformance:check`単体4分22秒の実測から不採用にした。round 1の指摘を受け、**観測の単位をgateから除外述語へ縮め、証明できない主張（gateの判定差分）を落とした。** 代替4案の不採用理由は`02_設計.md`§12に記録 |
| 実施者・日時 | reviewer（codex、implementerと別identity・別context） / 2026-09-06 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類、差分全文 | standard（riskはlow、scopeは観測層と報告script） | project choiceのprovider上限に従う。観測値はcodex CLI | project choiceのtier mappingに従う。固有model slugをreview authorityの根拠にしない | 未解決Critical/Highがあれば停止し次roundで再評価する | reviewerはcodex、implementerはClaude。identityとcontextが異なる。reviewerは対象差分pathを1件も変更していない |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | Issue #960、staging `01_要件定義.md`§9 | AC-01〜AC-05、INV-01〜INV-05 | 一次資料 |
| 差分 | `cef40bf3b714e7f9075d86cf7eb393131aefc05d..a46dc0d90db6dc8dbcc35e2c26beeb2e2a43906b` | 10 file、1066挿入 | 既存コード |
| テスト | `npm run conformance:check`（全suite内包） | 1521 scenarios（1505 passed、16 skipped）、失敗0 | テスト出力 |
| 仕様 | `docs/specs/02_要件/04_仕様・品質管理要件.md`、`docs/specs/15_要件追跡/` | updated | 既存文書 |
| commit前candidate | 本artifactを除く10 file | `a46dc0d90db6dc8dbcc35e2c26beeb2e2a43906b` | Git index |
| Phase A artifact | `docs/reviews/154_課題960除外述語被覆観測レビュー.md` | 本commitで追加する1 fileのみ | Git観測 |
| commit後external | trusted providerのPR / CI run | PR作成後に観測する | 外部のimmutable証拠 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **確認した。** 依存は`scripts/report_scan_boundary.ts` → `src/domain/scan-boundary.ts` → 各gateの既存述語の一方向のみ。観測層自身を述語一覧へ含めていない。本artifactへ自身のcommit SHAを書いていない
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけであり、trusted providerが観測したPR/CI/reviewが`H_final`へ一致している: `H_impl`は`a46dc0d90db6dc8dbcc35e2c26beeb2e2a43906b`。本artifactの1 fileだけを加えて`H_final`にする
- reviewer stable IDがPR author/provider観測済み`H_impl` author stable IDと異なる: reviewerはcodex、implementerとPR authorはClaude/tatsuru。異なる
- 既定branch追随を行った場合: **行っていない。** baseは`cef40bf3`のままである

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/scan-boundary.ts` | A | 本repository | package | 除外述語の被覆観測と比較。除外規則を1つも定義せず、`readonly sources: readonly ExclusionPredicateSource[]` として述語を引数で受け取る（SCN-UNIT-SCANBND-001） | pass。各gate述語への一方向のみ。self-loopなし | REQ-SQ-032 / AC-01〜AC-04 / SCN-UNIT-SCANBND-001〜004 | 読み取りのみ。判定不能pathを除外側へ倒さない | pass |
| `scripts/report_scan_boundary.ts` | A | 本repository | project | 実repositoryに対する非権威的な報告。期待述語一覧を供給元と独立に持つ | pass。domain moduleへの一方向 | REQ-SQ-032 / AC-05 / SCN-UNIT-SCANBND-005 | 読み取りのみ。不完全なら非0終了 | pass |
| `package.json` | M | 本repository | project | npm script keyを1件追加。**`EXPECTED_SCRIPTS`の既存keyを1件も変更していない** | pass | REQ-SQ-032 | `project:quality`が合格 | pass |
| `test/features/unit/scan-boundary.feature` | A | 本repository | evidence | `SCN-UNIT-SCANBND-001`〜`005`のscenario定義 | pass | AC-01〜AC-05 | 一時directoryへfixtureを作り実workspaceを変更しない | pass |
| `test/steps/scan-boundary.steps.ts` | A | 本repository | evidence | 同scenarioのstep定義。代表生成物を対象codeから導出せず独立に列挙した | pass | 同上 | 同上 | pass |
| `docs/specs/02_要件/00_要件一覧.md` | M | 本repository | spec | REQ-SQ-032の一覧行 | pass | REQ-SQ-032 | 追加のみ | pass |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | M | 本repository | spec | REQ-SQ-032の本文。強制するSCNを名指しする | pass | REQ-SQ-032、AC-SQ-032 | 追加のみ。既存要件を1件も削っていない | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | 本repository | spec | REQ-SQ-032専用行 | pass | 同上 | 既存行へ相乗りしていない | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | 本repository | spec | 本変更の履歴行 | pass | 同上 | 追加のみ | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **確認した。** 生成物である`dist/src/domain/scan-boundary.js`は`isGeneratedDistributionPath`により個別監査の対象外である。`git diff --name-status cef40bf3..a46dc0d90db6dc8dbcc35e2c26beeb2e2a43906b`が返す10件のうち生成物1件を除いた9件と表の9行が一致する
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **確認した。** domain moduleは述語を引数で受け取り、gate固有の登録は`scripts/`側に置いた
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: **確認した。** round 1のF-01〜F-05は`src/domain/scan-boundary.ts`・`scripts/report_scan_boundary.ts`・`test/`と、それに伴う要件・追跡の再確定に閉じている

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | 独立reviewerが7件を検出。**F-01（High）: 当初の`semanticDelta`は実gateの判定到達pathを測っていなかった。** 除外述語はgate全体ではなくgate内の一部へ適用される | F-01は設計の中心を無効にする。AC-02とAC-03の数値を実gateの証拠に使えない | AC変更あり（AC-01〜AC-04を書き直しAC-05を追加）。目的・scope・security境界・不可逆操作はいずれも不変 | 観測の単位をgateから除外述語へ縮め、測るものを「どの述語にも掛からない生成物」へ変更。F-02〜F-05も是正 | commit `a46dc0d90db6dc8dbcc35e2c26beeb2e2a43906b`。変異12件を全件kill | updated | pass |

**F-01の是正は機能の追加ではなく主張の縮小である。** 運用ポリシーの「手段が開発速度を損なうとき、縮小するのは手段の側である」に従い、証明できない測度を残さなかった。

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-SCANBND-001 | `observeScanBoundary`の返却構造 | 5 scenarios passed | pass | 述語ごとにowner・appliesTo・excluded・reasonCode・件数が返り、被覆漏れが`uncovered`へ現れることを検査している |
| AC-02 | SCN-UNIT-SCANBND-002 | `compareScanBoundary` | 同上 | pass | 覆う生成物で`scopeDelta > 0`かつ`uncoveredDelta = 0` |
| AC-03 | SCN-UNIT-SCANBND-003 | 同上 | 同上 | pass | 覆わない生成物で寄与pathの集合が一致する |
| AC-04 | SCN-UNIT-SCANBND-004 | 不完全判定と理由code | 同上 | pass | 述語欠落・未公開・二重登録・期待一覧外・判定不能pathを別のcodeと述語IDで区別する |
| AC-05 | SCN-UNIT-SCANBND-005 | 報告scriptの終了値 | 同上 | pass | 一時repositoryへ対し別processで実行し、process終了値が非0であることを観測する |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | not-applicable | 実装は純関数2件と報告scriptで、認証・認可・信頼境界の判定に関与しない。個人情報も秘密情報も入出力に現れない | 走査対象を与えられたroot配下へ限定し、drive修飾pathと`..`を判定不能として拒否する。`SCN-UNIT-SCANBND-001`が両方を検査する。`src/lib/security.ts`への依存を追加していない |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 本変更そのものが観測可能性を提供する。出力項目と失敗時の終了値を要件で確定する必要がある | 出力は述語ごとのowner・appliesTo・excluded（path/reasonCode/reason）・件数と`uncovered`。不完全は5種の理由codeで区別し非0終了する。`SCN-UNIT-SCANBND-004`と`005`が担保する。保持・rotation・監視は該当しない。観測結果を永続化せず実行のたびに算出するためである |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | UIを実装しない。成果物は純関数moduleとCLI報告scriptで、画面・操作・支援技術のいずれも関与しない | 差分10 fileのいずれも表示層に属さない。本repositoryにUI層は存在しない |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UIが無いためtokenの適用対象が存在しない。色・寸法・typographyのいずれも差分に現れない | DC-UXと同じ根拠。token定義fileは本repositoryに存在しない |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | AC-01〜AC-05が`SCN-UNIT-SCANBND-001`〜`005`で観測でき、変異12件を全件killした |
| 価値 | 利用者・運用上の目的を満たすか | pass | 着手時点の実障害（`source:check`が`.claude/`で落ちる）と#953の両方を、同じ問い「どの述語にも掛からないか」で説明できる |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 依存packageを1件も追加せず、既存のGitとNodeだけで動く |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | REQ-SQ-032の本文と実装の判断が一致し、`trace:check`と`conformance:check`が合格した |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 観測層は述語を引数で受け取り、gate固有の登録は`scripts/report_scan_boundary.ts`の`EXCLUSION_PREDICATE_SOURCES`へ置いた。除外規則を1つも持たない |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | finding | **round 1のF-01。** 述語の適用範囲をgate全体だと仮定していた。主張を縮めて解消した |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | finding | **round 1のF-05。** Git観測後に消えたentryを黙って無視していた。`scan-failed`として報告するよう是正した |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | 重複登録・期待一覧外・空segment・`..`・drive修飾を検査している。1領域あたり2000件の上限に達した場合も`scan-failed`とする |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | 走査対象を与えられたroot配下へ限定し、symlinkを辿らない。判定不能pathを除外側へ倒さない |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | 読み取りのみで外部へ接続しない。**本層は信頼境界ではないことを要件と変更履歴へ明記した** |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | 差分は1066挿入・0削除。既存の記述を1行も落としていない |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | 追加fileの削除と`package.json`の1行削除で戻る。永続状態を持たない |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | finding | **round 1のF-07。** reviewerへ渡した差分から`dist/`が欠落していた。round 2では`git diff`全体を渡した |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| F-01 | High | `semanticDelta`が実gateの判定到達pathを測っていない。述語はgate全体ではなくgate内の一部へ適用される | reviewerが`.claude/hooks/generated.sh`を`directories:check`へ与え、実gateは見ないのに差分が1になることを実測 | 設計の中心 | 観測の単位をgateから除外述語へ縮め、測るものを「どの述語にも掛からない生成物」へ変更した | resolved | **gateの判定差分は依然として測れない。** 測るには各gateの内部を観測する必要があり、`check_source_quality.ts`は保護対象である |
| F-02 | High | 対象一覧を供給元から導出しており削除を検出できない。SCN-004が報告scriptも終了値も実行していない | reviewerの静的読解 | 回帰検出の有効性 | 期待述語一覧を供給元と独立に持ち、`SCN-UNIT-SCANBND-005`で報告scriptを別processで実行して終了値を観測する | resolved | なし |
| F-03 | High | AC-01が要求する除外理由codeが実在せず、報告がincluded pathもreasonも捨てている | 同上 | AC-01の充足 | `reasonCode`を追加し、報告へ述語ごとのowner・appliesTo・excluded・`uncovered`を含めた | resolved | なし |
| F-04 | Medium | 不完全な観測どうしでも`comparable=true`になり誤読できる | reviewerが`../outside`を含む観測で実測 | 比較結果の解釈 | `compareScanBoundary`が`complete`を確認し、不完全なら比較を拒否する | resolved | なし |
| F-05 | Medium | Git観測後に消えたentryを黙って無視し、`-z`なしの行処理でfilenameが変形し得る | reviewerの静的読解 | fail-closed条件 | `ls-files -z`と`status -z`へ変更し、消えたentryとsymlinkを`scan-failed`として報告する | resolved | なし |
| F-06 | Low | `C:/outside.ts`がsegment検査を通過する | reviewerの実測 | 入力検証 | drive修飾を持つpathを別に拒否した。**対応OSはPOSIXを前提とすることを要件へ明記した** | resolved | Windowsでの動作は本要件の対象外である |
| F-07 | Low | reviewerへ渡した差分全文から`dist/`が欠落していた | reviewerの指摘 | レビュー入力の完全性 | round 2では`git diff`の全体を渡した。**implementerの手順の誤りである** | resolved | なし |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: **確認した。** 固定initial HEAD `e3d0f305`に対する全scope reviewとして実施した
- 指摘を確定した: F-01〜F-03（High）、F-04・F-05（Medium）、F-06・F-07（Low）の7件
- 次ラウンド対象のCritical/High: F-01・F-02。**F-01は設計の中心を無効にするため、機能の追加ではなく主張の縮小で応じた**

### ラウンド2

- 未解決Critical/High: **0件。** F-01〜F-03はcommit `a46dc0d90db6dc8dbcc35e2c26beeb2e2a43906b`で解消した
- 修正差分: `e3d0f305..a46dc0d90db6dc8dbcc35e2c26beeb2e2a43906b`。`src/domain/scan-boundary.ts`、`scripts/report_scan_boundary.ts`、`test/`2件、要件・追跡4件、`dist/`
- 修正で触れた隣接範囲: 要件のAC・INV（AC-01〜AC-04の書き直しとAC-05・INV-05の追加）、REQ-SQ-032の本文、追跡表の`SCN-UNIT-SCANBND-005`。**stagingの01と03も再確定した**
- 既承認・未変更範囲を再走査していない: **確認した。** `00_要求定義.md`と`02_設計.md`の§1.2以外はラウンド1で承認済みで、再走査していない

### ラウンド3

- 全指摘の最終分類: **実施していない。** ラウンド2で収束した
- 任意の危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: 該当なし
- 同じ範囲の予算を自動更新していない: **確認した。** 残り1ラウンドを消費していない
- AIによる最終裁定: approved

## 7. テスト結果

実行したcommandの一覧: `npm run lint`、`npm run format:check`、`npm run typecheck`、`npm run source:check`、`npm run docs:format`、`npm run test:format`、`npm run skills:check`、`npm run cli:check`、`npm run package:check`、`npm run architecture:check`、`npm run project:quality`、`npm run trace:check`、`npm run conformance:check`、`npm run build`

全layerの合計: **1521 scenarios（1505 passed、16 skipped）、失敗0件。**

失敗またはskipがある層: skipは16 scenarioで、いずれも本変更以前から存在する環境依存scenarioである。失敗は0件のため展開する層は無い。

対応する成功CI runの参照: **PR作成後に観測する。** 本artifact作成時点では未生成である。

runnerは`@cucumber/cucumber`、`projectChoices.gherkinDialect`は英語keyword・日本語説明である。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/domain/scan-boundary.ts` | 入る | 公開exportが2関数と型定義ぶん増える。既存exportは1件も減らない |
| `dist/src/domain/scan-boundary.js` | 入る | 上記のbuild生成物 |
| `scripts/report_scan_boundary.ts` | 入る | `npm run scan-boundary:report`が使えるようになる |
| `package.json` | 入る | npm script keyが1件増える |
| `test/features/unit/scan-boundary.feature` | 入らない | なし |
| `test/steps/scan-boundary.steps.ts` | 入らない | なし |
| `docs/specs/02_要件/00_要件一覧.md` | 入らない | なし |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | 入らない | なし |
| `docs/specs/15_要件追跡/00_追跡表.md` | 入らない | なし |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | なし |

判断: 配布物を更新した

根拠: `src/domain/scan-boundary.ts`のexportと`package.json`のscript keyが利用者から見える。**既存の契約は1件も変えていない。** 追加のみで後方互換である。

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | あり |
| reviewerがPR author・実装commit authorと異なる | はい |
| 観測したreview commentとapprovalの件数 | reviewerはcodex CLIで実施。round 1でfinding 7件（High 3、Medium 2、Low 2）を提出。PR上のcommentとapprovalはPR作成後に観測する |

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/02_要件/00_要件一覧.md`、`docs/specs/02_要件/04_仕様・品質管理要件.md`（REQ-SQ-032を新設）、`docs/specs/15_要件追跡/00_追跡表.md`、`docs/specs/15_要件追跡/01_変更履歴.md`
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: **台帳への追加・変更・廃止は0件である。** 走査境界・走査差分・被覆差分の意味はREQ-SQ-032の本文で確定した
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: **確認した。** `conformance:check`が合格している
- 要件・変更・SCN・テストの追跡: REQ-SQ-032 → AC-SQ-032 → SCN-UNIT-SCANBND-001〜005 → `test/features/unit/scan-boundary.feature`。`trace:check`でorphan 0件
- `no-spec-impact`の場合の限定的根拠: 該当しない
- UI・トークンの判断: UI無し。DC-UX・DC-TOKENSともnot-applicable

## 11. 総合判定と再開地点

- 未解決Critical/High: **0件**
- Medium/Lowの記録: F-04〜F-07をいずれもresolvedとして記録した
- 判定: approved
- 新しい権限が必要な事項: **なし。** `PROTECTED_FILES`所属fileを1件も変更していない
- 残存リスク: **gateの判定差分は測れない。** 測るには各gateの内部（walker、拡張子filter、`npm pack`の出力）を観測する必要があり、`check_source_quality.ts`は保護対象である。報告scriptは`EXPECTED_SCRIPTS`に固定されないため trusted な回帰gateにならず、回帰の担保は`npm test`側の反例testが持つ
- 次に許可される操作: `workflow record --step=10`、その後`pr create`
- 次回の再開地点: 本staging配下の`journal/steps.jsonl`の最終行
