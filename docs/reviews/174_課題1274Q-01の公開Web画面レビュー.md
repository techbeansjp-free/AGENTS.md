# 04 レビュー

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 1 |
| H_impl | `3353bee049853ca8b7b3eb2708fcec4c1f2c7e24` |
| 比較基点 | `03cbfdfd3d0dfd7aebef5345145afb3d23231d33` |
| 対象SHA・文書ダイジェスト | `3353bee049853ca8b7b3eb2708fcec4c1f2c7e24` |
| 対象差分 | `03cbfdfd3d0dfd7aebef5345145afb3d23231d33..3353bee049853ca8b7b3eb2708fcec4c1f2c7e24`、9 path。うち`dist/`配下1件は生成物として個別監査の対象外とし、配布影響は§8へ残す |
| 対象外 | 「約束したか」を唯一の判別子にする案、pathだけの一律allowlist・denylist、fail-closed条件の緩和、利用側の公開境界そのものの決定、`classifyMode`と`detectQuickDisqualifiers`の判定logicの変更、新しい台帳・project choice項目・専用validator |
| 残り予算 | 同一範囲で最大3ラウンドのうち1ラウンドを使用。**残り2。** 収束後のHEAD移動に対する取り直し1ラウンドは未使用 |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260908_033043_Q-01の対象へ公開Web画面を位置づけ表示調整の判定基準を確定する |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-011。引用: 「**Q-01の対象は公開Web画面を含む。**」 |
| 成果物行数 | 実装 +2、配布文書 +12、test +64、仕様 +9、生成dist +2 |
| 縮小の先行評価 | **私が当初推した「約束したかを唯一の判別子にする」案を採らない。** 未宣言の提供物を取り逃し、内部の約束まで外部契約になり、生成・同期経路の提供先が漏れ、保証を削除して契約外とする変更を対象外にできてしまう。**新しいgate、validator、CLI、schema、台帳、project choice項目を1つも追加していない。** 既存の質問文・判定例・根拠欄で完結する |
| 実施者・日時 | implementer: Claude Code / claude-opus-5[1m]。reviewer: codex CLI / 設定既定model / reasoning effort high。2026-09-08 |

### 0.1 routing入力契約

| role欄（担当role） | 許可path・操作 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|---|
| reviewer | 読み取りのみ。`--sandbox read-only` | 肯定・敵対review、finding分類、原文引用 | advanced。riskはmedium、外部契約変更あり | codex CLI | **`--model`で固定せず設定既定に従う** | 独立性が不明ならPRとmergeを停止する | implementerはClaude Code、reviewerはcodexの新規session。reviewer自身が「file編集・commit・pushは行っていません」と報告した |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | GitHub Issue #1274、staging 01のAC-1274-01から03 | Step 8で`sync-verified`、syncDigestとreadBackDigestが`d82415e88c3a95dc782429cb22fa23ea95e2c611b0c559aab324c7243971f29e`で一致 | 実行観測 |
| 是正前の状態 | 現行の正本 | 判定例は3行。「公開Web」への言及なし、確認対象4項目への言及なし | 実行観測 |
| 是正後 | 同上 | 判定例6行。確認対象4項目と条件付きの表記修正文が実在する | 実行観測 |
| 差分 | `03cbfdfd..3353bee0` | 9 path | 既存コード |
| テスト | 対象SCNを`--name`で絞り込んだcucumber実行 | MODEQ 15 scenarios。関連する既存SCNを含む38 scenarios、190 stepsが成功 | テスト出力 |
| 変異試験 | 31件 | 31 kill。**うち10件はreviewer由来である** | テスト出力 |
| Q-01の文字数 | `MODE_QUESTIONS` | 276文字から389文字。`|`と改行を含まない | 実行観測 |
| 仕様 | `docs/specs/02_要件/01_ワークフロー要件.md`、`15_要件追跡/` | updated | 既存文書 |
| commit前candidate | `git diff --name-status 03cbfdfd 3353bee0` | 9 pathすべてM。うち生成dist 1件 | Git index |
| Phase A artifact | `docs/reviews/174_課題1274Q-01の公開Web画面レビュー.md` | H_implの後にこの1 fileだけをcommitしてH_finalとする | Git観測 |
| commit後external | PR、CI run、外部review | **本artifactの作成時点では未観測である。** | 外部のimmutable証拠 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **満たす。** nodeもedgeも追加していない。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: **artifact commit時点で満たす。**
- reviewer stable IDが`H_impl` author stable IDと異なる: **満たす。** **PR author stable IDとの比較は本artifactの作成時点では未確認であり、PR作成後に観測する。**
- 既定branch追随を行った場合: **該当なし。** 比較基点は`03cbfdfd`のままである。

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/mode.ts` | M | package owner | domain | `MODE_QUESTIONS`のQ-01へ1文を足す。**他の7問と判定logicに触れない** | pass。domainからfile I/Oとadapterへの依存を追加していない | REQ-WF-011 / AC-1274-01 / SCN-UNIT-MODEQ-023 | 既存の主文、ID、失格分類、極性を維持。前進revertで復旧 | pass |
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | M | package owner | package | 質問文の正本、判定例6行、確認対象4項目、条件付きの表記修正文 | pass。`MODE_QUESTIONS`と同じcommitで変える。片側だけの差分は`checkModeQuestionText`が拒否する | REQ-WF-011 / AC-1274-01・02・03 / SCN-UNIT-MODEQ-023・024・025 | **#1268で入れた既存3行と3つの注意文を削除していない。** 追加と3行目の具体化のみ | pass |
| `test/features/unit/mode-questions.feature` | M | package owner | evidence | SCN-UNIT-MODEQ-025の追加と024の説明更新 | pass | AC-1274-02・03 | 追加と説明更新のみ。既存scenarioを削除していない | pass |
| `test/steps/mode-questions.steps.ts` | M | package owner | evidence | 023の期待値へ2判断を追加、024を過不足ない一致へ、025を追加、`tableBody`の終端判定を是正 | pass。既存step定義を書き換えていない | AC-1274-01・02・03 | 追加と是正のみ | pass |
| `test/support/markdown.ts` | M | package owner | evidence | 行内HTMLコメントの扱いを是正し、コメント外の可視内容を残す | pass。**#1268と#1273が使う共通helperであり、両者のSCNが合格し続けることを確認した** | AC-1274-02 | 既存の退避検出を維持したまま可視部分を保持する | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | package owner | spec | REQ-WF-011へ公開Web画面の扱いと保証範囲を追記 | pass。名指ししたSCN 3件は同じ要件の追跡行に登録済み | REQ-WF-011 / AC-WF-011 | 既存段落を削除していない | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package owner | spec | SCN-UNIT-MODEQ-025の追跡登録 | pass。`trace:check`のorphanが要件・SCN・実装とも0件 | REQ-WF-011 / AC-WF-011 | 既存行は不変 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package owner | spec | 変更理由と判断の記録1行 | pass。9列のheader区切り直後へ挿入 | REQ-WF-011 | 既存行は不変 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **満たす。** `git diff --name-status`の9 pathのうち、生成物である`dist/`配下1件を除いた8 pathと表の8行が一致する。`dist/`の配布影響は§8へ残す。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **満たす。**
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: **満たす。** M-01は`test/steps/`1 file、M-02は`test/support/`1 fileに閉じ、L-01は`test/`の2 fileである。

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | **要件レビューで、私のFR-07が外部契約変更をtrueへ通す独立の例外になっていた。** 「意味・操作識別・支援技術への伝達を変えない表記修正はtrue」の3条件では、外部へ提供する固定文字列の変更、生成・同期先への影響、表記自体への外部向け保証の変更を除けない | AC-1274-03の担保範囲。**弱い例外をそのまま正本へ固定するところだった** | AC | 上流を再起動せず01の該当節だけを再確定した。FR-07へ条件を加え、INV-1274-02へ「trueの判定例は外部契約の追加・変更・削除または影響未確認を上書きしない」を追記した | 変異H1（条件節だけの削除）をkill | REQ-WF-011へ条件付きの表記修正文を反映 | pass |
| DISC-002 | **設計レビューで、3 cellの誤った7行目が`tableBody`に表の終端として捨てられ、6行固定の完全一致を素通りすることが判明した。** 自分でも再現した | AC-1274-02の担保範囲 | なし | 同じ表の中の列数違反を終端にせず本文へ入れ、呼び出し側の比較で落とす | 変異D2をkill | 不要 | pass |
| DISC-003 | **実装レビューで、上の是正が不十分と判明した。** 末尾のパイプを省略した行（GFMは外側のパイプを省略できる）も終端として捨てられ、行内HTMLコメントを添えた可視の表行は`visibleMarkdownLines`が行ごと削除していた | 同上。**「7行目を拒否する」保証に2つの回避が残っていた** | なし | `cells`は行頭のパイプだけを要求し、末尾の有無で捨てない。`visibleMarkdownLines`はコメント区間だけを取り除き、残りが空白でなければ可視行として残す | 7行目の4形態をすべてkill。**#1268と#1273のSCNも合格し続けることを確認した** | 不要 | pass |
| DISC-004 | SCNの説明とコメントが旧契約（3判断・3行）のままだった | 記録の正確さ | なし | 5判断・6組へ更新した | `test:format`合格 | 不要 | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1274-01 | SCN-UNIT-MODEQ-023 | Q-01へ足した1文の2判断 | 合格 | pass | 2判断を個別に落とす変異をkillする。**正本と`MODE_QUESTIONS`を同時に変える形で作り、照合器が落ちたことを検出力と取り違えていない** |
| AC-1274-02 | SCN-UNIT-MODEQ-024 | 正本の判定例6行 | 合格 | pass | 判定値の反転6件、行の削除6件、4行目の確認条件の削除、退避2件、**7行目の4形態**をkillする |
| AC-1274-03 | SCN-UNIT-MODEQ-025 | 確認対象4項目と条件付きの表記修正文 | 合格 | pass | 4項目の個別削除、**条件節だけの削除**、既存注意文の削除、退避2件をkillする |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | not-applicable | 質問文と判定例の変更であり、信頼境界、認可、秘密情報の扱いを変更しない | reviewerがsourceの変更はQ-01定数の1行のみと確認した |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 判定の根拠を後から検証できないと基準が緩みの口実になる | 確認対象4項目とSCN-UNIT-MODEQ-025 |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | applicable | **判定対象がa11y上の意味と利用可能性を含む。** 実行者の認知負荷そのものも対象である | 判定例5行目がa11yを名指しする。真・偽・不明の3種と4類型を欠かさない |
| DC-TOKENS | Design System・Design/Layout Token | applicable | **判定対象がdesign tokenの外部提供を含む。** ASC自身はtokenを追加しない | 判定例6行目。**製品名ではなく外部consumerへ提供する事実が判別対象である** |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 独立reviewerがAC-1274-01から03とINV-1274-01から07を原文引用で判定し、Medium 2件の是正後はすべてpassである |
| 価値 | 利用者・運用上の目的を満たすか | pass | 契約対象でない表示調整の条件が明示され、解釈の相違が常に`full`へ集約される状態を解消する |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 新しいgate、validator、CLI、schema、台帳、project choice項目を追加していない |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 02の§11に列挙した対応と実差分の8 pathが一致する |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 追加は質問文へ1文、判定例へ3行、確認対象4項目、条件付きの表記修正文1つである。**既存SCNを再利用し新規は1件だけとした** |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | 変異31件中31 kill。**うち10件はreviewer由来である。** 私が最初に作った21件だけでは、列数違反・末尾パイプ省略・行内コメントの3経路に届かなかった |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 不明・未回答・根拠なしを`full`へ倒す既定と、確認できない場合のfalseを変えていない |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | 判定例の真・偽・不明の3種、7行目の4形態、退避2形態を固定集合で比較する。**最大・重複・Unicodeは本件のscenarioに含まれない。** 既存のSCN-UNIT-SEC-001から006が担保する |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | 判定例と確認対象をHTMLコメント・code blockへ退避する経路を塞いだ。**行内コメントで可視行を隠す経路も塞いだ** |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | gateを追加せず既存gateも弱めていない。**trueの判定例が外部契約の追加・変更・削除または影響未確認を上書きしない旨を要件と正本へ明記した** |
| データ損失 | 上書き、削除、部分公開、履歴消失 | not-applicable | 変更箇所に永続化・削除処理を追加していない |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | 前進revertで復旧する。既に記入済みの成果物へ遡及的な要求をしない |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | `dist`のbuild結果を同一commitへ含めた。**共通helperを共有する#1268と#1273のSCNも合格し続けることを確認した** |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| M-01 | Medium | **末尾のパイプを省略した7行目が表の終端として捨てられる。** GFMは外側のパイプを省略でき、これは同じ表の行である。正しい6行の直後へ足すと「本文6行」の完全一致を素通りする | reviewerが実際のCucumberで合格を再現し、私も`cells`が`null`を返すことを再現した | AC-1274-02 | `cells`は行頭のパイプだけを要求し、末尾の有無で捨てない | resolved | なし |
| M-02 | Medium | **行内HTMLコメントを添えた可視の表行が検査から消える。** `visibleMarkdownLines`が`<!--`を含む行を丸ごと捨てていた | reviewerが実際のCucumberで合格を再現し、私も可視部分が空になることを再現した | AC-1274-02。**#1268と#1273が共有するhelperの既存欠陥である** | コメント区間だけを取り除き、残りが空白でなければ可視行として残す。閉じないコメントが始まる行では手前の可視部分だけを残す | resolved | なし |
| L-01 | Low | SCNの説明とコメントが旧契約（3判断・3行）のままだった | reviewerが原文を引用 | 記録の正確さ | 5判断・6組へ更新した | resolved | なし |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: はい。AC-1274-01から03、INV-1274-01から07を原文引用で判定した。
- 指摘を確定した: M-01、M-02、L-01。**Critical/Highは0件。** 3件ともこのラウンド内で是正した。
- 次ラウンド対象のCritical/High: なし。
- verdictはAPPROVE_WITH_FINDINGS。

## 7. テスト結果

実行したcommandの一覧。

- `npm run lint`
- `npm run format:check`
- `npm run typecheck`
- `npm run docs:format`
- `npm run test:format`
- `npm run trace:check`
- `npm run verify:distribution`
- `node --import tsx ./node_modules/@cucumber/cucumber/bin/cucumber.js --config cucumber.mjs --name 'SCN-UNIT-MODEQ|SCN-UNIT-SCNSCOPE|SCN-UNIT-PACKAGE-02'`

runnerは`@cucumber/cucumber`、`projectChoices.gherkinDialect`は`en`、`projectChoices.testLayers`は`unit`、`integration`、`e2e`である。

対象と関連シナリオの実行は38 scenarios、38 passed、0 failed、0 skipped。190 steps、190 passed。**共通helperを共有する#1268と#1273のSCNを含む。**

変異試験の母集団は31件である。内訳は、Q-01の2判断の個別削除2件、判定値の反転6件、行の削除6件、7行目の追加4形態、4行目の確認条件の削除1件、判定例の退避2件、確認対象4項目の個別削除4件、条件節だけの削除1件、既存注意文の削除1件、確認対象と表記修正文の退避2件、**および行内コメント付き7行目1件**である。**31件すべてをkillした。**

**全件killを成果として報告しない。** 母集団のうち10件はreviewer由来であり、私が最初に作った21件だけでは、列数違反・末尾パイプ省略・行内コメントの3経路に届かなかった。**「7行目を拒否する」という保証は、3ラウンドの指摘を経て初めて実装に追いついた。**

失敗またはskipがある層: **対象シナリオの実行では0件である。** 全suiteの結果は`verify:distribution`の実行ログを正本とする。

対応する成功CI runの参照: **本artifactの作成時点では存在しない。** branchを未pushであるため、run IDとURLと対象HEADは提出時に別途記録する。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/domain/mode.ts`、`dist/src/domain/mode.js` | 入る | `MODE_QUESTIONS`のQ-01の質問文が276文字から389文字になる |
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | 入る | Q-01の行が変わり、判定例が3行から6行へ、確認対象4項目と条件付きの表記修正文が増える |
| `docs/specs/`の3 file | 入らない | ASC自身の仕様と追跡であり配布物に含まれない |
| `test/`の3 file | 入らない | 配布境界外の検証資産である |

判断: 配布物を更新した

根拠: `src`と`.agent-skill-chain/docs/`はいずれも配布されるpackage所有資産であり、利用者が読む実行契約が変わる。**質問の件数、ID、失格分類、極性、回答形式、`00_モード判定.json`のschemaは不変である。** 既に記入済みの成果物へ遡及的な要求をしない。

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | 本artifactの作成時点ではなし。提出時にPRとCI runで観測する |
| reviewerがPR author・実装commit authorと異なる | `H_impl` authorとは異なる。reviewerはcodex CLIの独立sessionである。**PR authorとの比較はPR作成後に観測する** |
| 観測したreview commentとapprovalの件数 | 要件レビュー1ラウンド（REQUEST_CHANGES、High 1・Medium 3・Low 1）、設計レビュー1ラウンド（APPROVE_WITH_FINDINGS、Medium 3・Low 3）、実装レビュー1ラウンド（APPROVE_WITH_FINDINGS、Medium 2・Low 1）。**すべて是正済みでCritical/Highは実装ラウンドで0件** |

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-011、`docs/specs/15_要件追跡/00_追跡表.md`、`01_変更履歴.md`
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: **満たす。** 用語を追加していない。確定したのは既存語の適用範囲である。
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: **満たす。**
- 要件・変更・SCN・テストの追跡: REQ-WF-011、AC-WF-011、SCN-UNIT-MODEQ-023、SCN-UNIT-MODEQ-024、SCN-UNIT-MODEQ-025。`trace:check`のorphanが要件・SCN・実装とも0件。
- `no-spec-impact`の場合の限定的根拠: 該当なし。
- UI・トークンの判断: DC-UXとDC-TOKENSはともにapplicable。**ASC自身に画面UIは無いが、判定対象が利用側の公開Web画面とdesign tokenだからである。** ASCはtokenと視覚componentを追加しない。

## 11. 総合判定と再開地点

- 未解決Critical/High: なし。
- Medium/Lowの記録: M-01、M-02、L-01はresolved。
- 判定: approved
- 新しい権限が必要な事項: なし。push、PR作成、mergeはユーザーが2026-09-07に対話上で明示承認している。**出典は当該対話の指示であり、GitHub上の識別子や監査logの参照を持たない。この文書を操作許可の正本にしない。** merge承認は`pr.merge`だけを許可し、branch削除、Issue終了、release、公開、cleanupを連結しない。mergeは`--merge`で行い`--squash`を使わない。
- 残存リスク: **判定例が緩みとして働くriskを機械では検出しない。** 条件文と確認対象で抑え、検出は独立reviewが担う。**`classifyMode`は根拠の非空しか確認しないため、誤った推論を根拠欄へ書いてもanswer=trueなら`quick`になる。** これは本件が導入した緩和ではなく、REQ-WF-011自身が限定として明記している。
- **主張の範囲。** 本件が担保するのは、Q-01の質問文が2判断を述べること、正本の判定表に確定した6例と判定値の対応が過不足なく維持されること、確認対象4項目と条件付きの表記修正文が実在することである。**「利用者全員が同じ事実から同じ回答を作る」ことは担保しない。** 個別案件の意味判断は証拠に基づく独立reviewに残る。**判定例の意味的な妥当性の最終判断はpackage ownerが持つ。**
- 次に許可される操作: 本artifactをH_implの後に単独commitしてH_finalとし、`audit:check`と`verify:distribution`をH_finalで実行する。その後にbranchをpushしPRを作成する。
- 次回の再開地点: H_finalでの配布gate実行。
