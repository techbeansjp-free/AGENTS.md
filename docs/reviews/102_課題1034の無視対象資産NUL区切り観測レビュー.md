# 04 レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1034 |
| ラウンド | Step 10 ラウンド1〜2 |
| 比較基点 | `fc745f4f1132454e157e7603dc4e21d64226c9b6` |
| H_impl | `bde3ccce9ec91896c562db13e59336a1ff78d0ea` |
| 比較基点の由来 | review開始時点の`origin/main`のtip。**review session作成から本artifact commitまでの間に`origin/main`は動いていない**（`git fetch`後に同一SHAであることを確認した） |
| Step 10のreview session ID | `6552dfefe7a2637fccc321104a53977e0ea4011e1afbc6bcab21f257ab83e97a` |
| モード | full |
| 対象差分 | `src/domain/worktree.ts`、`src/cli.ts`、`test/features/unit/worktree-ignored-artifacts.feature`、`test/features/integration/worktree-ignored-artifacts.feature`、`test/steps/worktree-ignored-artifacts.steps.ts`、`docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`、`docs/specs/15_要件追跡/00_追跡表.md`。commitは`c8c26cb3`・`23be80e3`・`ef70844a`・`bde3ccce` |
| 対象外 | `validArtifactPath`のNFC検査と空segment拒否の変更。`-c core.quotepath=false`の付与。`untracked`の観測経路。`stash list`の分割式。`src/domain/hygiene.ts`と`src/adapters/repository-graph.ts`。#993の`maxBuffer`。`createIsolatedWorktree`の共通fixture抽出（5節のR1-F06） |
| 残り予算 | **1**（同一範囲で最大3ラウンド。総2ラウンドで設計し、CI是正のために予算1を残した） |
| ラウンド数 | 2。ラウンド1は実装差分、ラウンド2は本artifactを加えた版が対象である |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260831_181557_worktree-finalizeのls-filesが-zと-no-quotepathを欠き非ASCII名で必ずブロックされる |
| 仕様の所有箇所 | `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`の`worktree survey`行と`worktree finalize --dry-run`行。**着手時点でこの2行は無視対象資産の観測形式を規定していなかった。** その欠落を埋めることが本Issueの要求の一部である |
| 成果物行数 | 製品 **+4 / −4行**（`src/domain/worktree.ts` +2/−2、`src/cli.ts` +2/−2）。仕様 **+4 / −2行**。支援層 **+287行**（unit feature +32、integration feature +7、steps +248） |
| 縮小の先行評価 | 5案を先に評価した。(1)`-c core.quotepath=false`を足す案は、実測で`-z`付きの出力が`quotepath`の値によらずbyte単位で同一であるため冗長として不採用。(2)`--no-quotepath`を足す案は、`git ls-files`の有効なflagではないことを実測して不採用。(3)`validArtifactPath`のバックスラッシュ拒否を緩める案は、判定基準そのものを緩めるためINV-01に反し不採用。(4)末尾スラッシュのdirectory entryを正規化する案は、allowlistのprefix照合がdirectoryをどう扱うかという別の決定を伴うため不採用。(5)`createIsolatedWorktree`を`test/support/`へ抽出する案は、計画T01が既存step定義を書き換えないと定めた意図的選択でありscope外。**step定義の新設は01の9節が事前に固定した7件（Given 1・When 3・Then 3）で、実装もちょうど7件である。** |
| 実施者・日時 | reviewer（fable）、coordinator（claude）、2026-08-31 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節と`review-session.json`） | advanced | fable | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerは**claude**であり、reviewerは**fable**である。providerとcontextがともに異なる |

**開示する逸脱が2件ある。**

1. **implementerが計画の当初想定と異なる。** `codex exec --full-auto`がhost側のauto mode分類器に拒否され起動できないため、02の0.1節と03の0.1節で**implementerをclaudeと先に宣言**したうえで進行役が実装した。**reviewerをfableに固定してclaudeをreviewerに含めていない。** reviewerがimplementerと別providerである性質は保たれている。**この帰結（実行時にimplementer identityとreviewerの独立性を検証する機構が無い）は既存の #1040 が所有する。**
2. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** `scripts/check_file_audit.ts:26`が`AUDIT_DIRECTORY = "docs/reviews"`を要求する一方、`validateRoleOperation`は実行時に強制されていない。**本Issueでは権限拡大をscope外としてrisk受容し、解消を #1047 へ委譲する。**

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | https://github.com/techbeansjp-free/AGENTS.md/issues/1034 、AC-1034-01〜07 | Step 8で00〜03を同期。`syncDigest`と`readBackDigest`が`40a1b3f3a987a7db2a3c399cb1bef51d391f2783488e58d628430a19903ee400`で一致 | 一次資料 |
| 差分 | `fc745f4f..bde3ccce` | 7 file、+293 / −4行。**製品差分は`ls-files`の引数1つと分割式1つを2箇所へ、計+4/−4行** | 既存コード |
| テスト | `npm test` | `1325 scenarios (1309 passed, 16 skipped)`、失敗0 | テスト出力 |
| 仕様 | `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`ほか1 file | updated | 既存文書 |
| commit前candidate | 7 file（1.1節の表） | working tree clean | Git index |
| Phase A artifact | `docs/reviews/102_課題1034の無視対象資産NUL区切り観測レビュー.md` | `H_impl` = `bde3ccce`。`H_impl..H_final`の差分pathは本file 1件 | Git観測 |
| commit後external | PR、CI run、review | Step 11で観測する。**本節はPR作成前に書いており、外部証拠はまだ無い** | 外部のimmutable証拠 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **成立する。** 02の2.3節の3 nodeは`worktree-survey-cli → inspect-finalize-state → assess-worktree-removal-safety`の一方向であり、観測結果を自身の正しさの根拠にしていない。本artifactへ自身のcommit SHAを書いていない。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_impl` = `bde3ccce`は`H_final`のancestorであり、差分は本artifact 1 fileだけである。PR・CI・reviewの観測はStep 11で行う。
- reviewer stable IDがPR author/provider観測済み`H_impl` author stable IDと異なる: **いいえ。** 本repositoryではPR authorと`H_impl` commit authorがいずれも`adachi-tatsuru`である。9節の例外経路を参照する。
- 既定branch追随を行った場合: **行っていない。** 基点`fc745f4f`は`origin/main`のtipであり、追随mergeを作っていない。`比較基点..H_impl`は4 commitの一直線である。

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/worktree.ts` | M | package | package | `inspectFinalizeState`の観測責務のまま。`ls-files`へ`-z`を足し分割を`\0`へ変えた2行だけである | pass。新しいmodule依存を作らない。nodeもedgeも増やさない。`npm run architecture:check`合格 | REQ-SQ-001 / AC-1034-01〜06 / SCN-UNIT-WTIGN-001〜006 | **`validArtifactPath`を1行も変えていない。** `untracked`と`stash list`の分割式にも触れていない。rollbackは当該2行のrevert | pass |
| `src/cli.ts` | M | package | package | `collectWorktreeSurvey`内の同じ2行だけである | pass。`src/domain/worktree.ts`と同型の変更で一貫する | REQ-SQ-001 / AC-1034-07 / SCN-INT-WTIGN-001 | 同上。呼び出し元は`worktree survey`と`doctor`の2つで、いずれも観測形式の是正が意図どおり効く | pass |
| `test/features/unit/worktree-ignored-artifacts.feature` | A | package | package | unit scenarioを6件持つ新規Featureである。既存Featureを1件も書き換えていない | pass | AC-1034-01〜06 / SCN-UNIT-WTIGN-001〜006 | fixtureは`initRepo()`と`temp()`の一時directory内に閉じる。**実`.agent-skill-chain/project`を複写しないため`REPOSITORY_READ_EXCEPTIONS`の宣言が要らない** | pass |
| `test/features/integration/worktree-ignored-artifacts.feature` | A | package | package | integration scenarioを1件持つ新規Featureである | pass | AC-1034-07 / SCN-INT-WTIGN-001 | 同上。`bin/agent-skill-chain.ts`をsubprocessで起動する | pass |
| `test/steps/worktree-ignored-artifacts.steps.ts` | A | package | package | step定義を7件（Given 1・When 3・Then 3）追加した。01の9節が事前に固定した上限と内訳に一致する | pass。既存step定義を1件も書き換えていない | AC-1034-01〜07 / SCN-UNIT-WTIGN-001〜006、SCN-INT-WTIGN-001 | 同上。一時fileを作らない | pass |
| `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | M | project | spec | `worktree survey`行と`worktree finalize --dry-run`行へ1文ずつ足した。他のcommand行に触れていない | pass | REQ-SQ-001 / AC-SQ-001 | 規則の記述だけで実行authorityを持たない | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | project | spec | REQ-SQ-001の既存行を書き換えず、下へ2行追加した | pass | REQ-SQ-001 / AC-SQ-001 / SCN-UNIT-WTIGN-001〜006、SCN-INT-WTIGN-001 | 追跡の追加だけで実行authorityを持たない | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **一致する。** `git diff --name-only fc745f4f bde3ccce`が返す7 pathが上表の7行と同じである。**本artifactは`H_impl..H_final`にあり、この範囲には入らない。**
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **成立する。** `-z`はgitの出力形式の指定であり、project ruleにしていない。
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: **ラウンド1のfindingのうち5件を修正した。** 修正範囲は`test/steps/worktree-ignored-artifacts.steps.ts`の1 fileだけであり、**製品差分は1行も変わっていない。**

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

**実装中の発見は0件である。** 03の4.1節に記録がない。03の11.1節はT04の実測記録であり、AC・scope・security境界・不可逆操作の契約はいずれも変えていない。

### 2.1 受け入れ条件とシナリオ

| AC ID | 内容 | SCN | 観測 |
|---|---|---|---|
| AC-1034-01 | 非ASCII名で種別不明の理由が出ない | SCN-UNIT-WTIGN-001 | 緑。修正前は赤。**ラウンド1で観測列と生の非ASCII名がallowlist診断へ現れることの検査を追加した** |
| AC-1034-02 | 改行を含む名前が1件として数えられる | SCN-UNIT-WTIGN-002 | 緑。修正前は赤。`deepEqual`で件数と生名の両方を測る |
| AC-1034-03 | `core.quotepath`の設定で観測結果が変わらない | SCN-UNIT-WTIGN-003 | 緑。修正前は赤。fixture repositoryの`git config`で両設定を実際に走らせる |
| AC-1034-04 | allowlistを満たす非ASCII名がblockingにならない | SCN-UNIT-WTIGN-004 | 緑。修正前は赤。**ラウンド1で観測列の検査を追加した** |
| AC-1034-05 | 実fixtureで作れる危険なpathは従来どおり拒否される | SCN-UNIT-WTIGN-005 | 緑。修正前から緑（正常系の回帰）。**ラウンド1でバックスラッシュ名をfixtureへ追加し、拒否件数をfixture件数との完全一致へ強めた** |
| AC-1034-06 | ASCII名だけなら観測列が期待列と完全一致する | SCN-UNIT-WTIGN-006 | 緑。修正前から緑（正常系の回帰） |
| AC-1034-07 | survey CLIが非ASCII名で種別不明を返さない | SCN-INT-WTIGN-001 | 緑。修正前は赤。**ラウンド1で終了値0と対象worktree entryの実在の検査を追加した** |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 判定 | 観測 |
|---|---|---|
| DC-PRIVACY | applicable | **`validArtifactPath`の判定規則を1行も変えていない。** `git diff --name-only`に`src/domain/worktree-removal-safety.ts`が含まれないことを確認した。危険なpath 3種（制御文字・NFC不一致・バックスラッシュ）の拒否維持をSCN-UNIT-WTIGN-005が反例で固定する |
| DC-OBSERVABILITY | applicable | 症状は診断が「種別が不明です」と出て利用者が対処できないことである。**是正後は生のfile名がallowlist診断へ現れ、対処可能な形になる。** SCN-UNIT-WTIGN-001がこれを正の形で固定する |
| DC-UX | not-applicable | project choiceの`humanCenteredUi`が`not-applicable`である |
| DC-TOKENS | not-applicable | project choiceの`designTokens`が`not-applicable`である |

## 3. 肯定的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ | pass | 判定規則を1つも変えていない。変えたのは`ls-files`の引数1つと分割式1つを2箇所である |
| 価値 | pass | 本repositoryで「path種別が不明です」が**62件から1件**へ減った。残る1件は事前に対象外と宣言したものと一致する |
| 実現可能性 | pass | 製品差分は+4/−4行である |
| 整合性 | pass | `src/domain/hygiene.ts`と`src/adapters/repository-graph.ts`の既存`-z`利用と形が揃った |
| 保守性 | pass | 2箇所で異なっていた分割式（`"\n"`と`/\r?\n/u`）が`"\0"`へ統一された |

## 4. 敵対的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 反例 | pass | 変異5件がいずれも対象scenarioを殺す（7.1節）。うち1件はラウンド1の是正で新設した |
| 失敗 | pass | `git`が失敗した場合の既存の振る舞いを変えていない。種別不明を許可へ倒さない |
| 境界 | pass | 非ASCII名・改行名・NFD名・制御文字名・バックスラッシュ名・ASCII名の6種を実fixtureで作って観測した |
| 悪用 | pass | 危険なpath 3種の拒否維持を反例で固定した。**生名の診断出力への注入も不成立である。** `allowlist外…: ${artifact}`へ到達できるのは`validArtifactPath`合格名のみで制御文字を含まず、不合格名は`describeUnknownArtifact`がJSON文字列化する |
| 安全性 | pass | `validArtifactPath`を1行も変えていない。allowlist照合の順序も変えていない |
| 損失 | not-applicable | 観測層は書き込みを行わない |
| 復旧 | pass | rollback-validationを実測した（7.2節）。差分をrevertすると変更前の振る舞いへ戻る |
| 範囲 | pass | `untracked`・`stash list`・`hygiene.ts`・`repository-graph.ts`に触れていない |

## 5. 指摘

**ラウンド1で6件、ラウンド2で0件。未解決のCritical/Highは0件である。** blockingは0件で`converged`した。

| ID | 深刻度 | 種別 | 状態 | 内容 |
|---|---|---|---|---|
| R1-F01 | High | acceptance-violation | resolved | AC-1034-05が列挙する危険なpath 3種のうち**バックスラッシュ名がfixtureに無く**、`validArtifactPath`のバックスラッシュ拒否分岐がrepository全体で未検証だった。**実装計画T01自身がこの種別をfixtureへ含めると宣言しており、無記録の逸脱だった** |
| R1-F02 | Medium | improvement | resolved | SCN-INT-WTIGN-001が`result.error`しか見ず、走査失敗でentryごと消えても緑のまま通った。終了値0と対象worktree entryの実在の検査を追加した |
| R1-F03 | Medium | improvement | resolved | 危険なpathの拒否を「1件以上」で測っており、複数件のうち1件だけ拒否されても合格した。fixture件数との完全一致へ強めた |
| R1-F04 | Low | improvement | resolved | `buildFinalizeReport(state as never) as unknown as FinalizeReport`の二重castが型検査を潰していた。castなしで型が通ることをtscで実測し`ReturnType`へ置き換えた |
| R1-F05 | Low | improvement | resolved | SCN-UNIT-WTIGN-001と004が観測列を一切見ず、観測が空になる退行で空振り合格した。観測列のassertを追加した |
| R1-F06 | Low | out-of-scope | valid | `createIsolatedWorktree`の約45行が`worktree-survey.steps.ts:55`の`createSurveyRepository`とほぼ同型の複製である。`test/support/`への共通fixture抽出は縮小候補だが、計画T01が既存step定義を書き換えないと定めた意図的選択でありscope外とする |

**R1-F06は本Issueで是正しない。** **この1件を根拠に新しいIssueを起こしていない。**

## 6. ラウンド固有の確認

### ラウンド1

- 対象は`fc745f4f..ef70844a`の実装差分である。
- **敵対reviewをfableの独立invocationで実施した。** implementer（claude）とは別providerである。
- **すべての指摘を受け入れる前に実測で検証した。** R1-F01のバックスラッシュ名を実際に作って`ls-files -z`へ生のバックスラッシュで載ることを`od -c`で確認し、R1-F04の`as never`が不要であることをproject同一optionのtscで確認してから是正へ進んだ。
- 是正は前進commit `bde3ccce`で積んだ。**製品差分は1行も変えていない。** 変えたのはtestだけである。

### ラウンド2

- 対象は本artifactを加えた版である。
- 0.1節の逸脱2件、5節のR1-F06の未是正、7節の実測値が本文の主張と一致することを確認した。
- **新規findingは0件である。** 未解決blockingは0件で`converged`した。
- **予算1を残している。** CIが赤になった場合の是正をこのラウンドで載せる。

## 7. テスト結果

| 検証 | 結果 |
|---|---|
| `npm test` | `1325 scenarios (1309 passed, 16 skipped)`、失敗0 |
| CI同順の全chain | `project:quality`→`quality`→`docs:format`→`test:format`→`trace:check`→`architecture:check`→`build`→`package:check`→`conformance:check`の9 commandを**終了値を明示的に見るループで**完走した |

### 7.1 変異試験

**復元はすべて複写で行い`git checkout`を使わない。** ラウンド1の是正後に全件を再実行した。

| 変異 | 内容 | 対象scenarioの結果 |
|---|---|---|
| M-a | `src/domain/worktree.ts`から`-z`を外す | 7件中**6件**が赤 |
| M-b | `src/domain/worktree.ts`の分割を`"\n"`へ戻す | **5件**が赤 |
| M-c | `src/cli.ts`から`-z`を外す | 1件が赤（SCN-INT-WTIGN-001） |
| M-d | `src/cli.ts`の分割を`/\r?\n/u`へ戻す | 1件が赤（SCN-INT-WTIGN-001） |
| M-e | 観測を空配列にする（空振り経路） | **5件**が赤 |

**`-z`の除去と分割式の差し戻しを別々に当てた。** `cli.ts`側の2件はintegration scenarioが単独で殺す。

**ラウンド1の是正で検出力が上がった。** M-aは5件から6件、M-bは4件から5件を殺すようになった。**M-eはラウンド1の是正で新設した変異であり、是正前は0件しか殺さなかった。**

### 7.2 回帰の再測とrollback-validation

本repositoryの`worktree survey --root=.`で「path種別が不明です」の件数を測った。

| 状態 | 件数 |
|---|---:|
| 修正後 | **1** |
| 差分をrevert | **62** |
| 復元 | **1** |

**revert後は62件であり、着手時に測った55件ではない。** 着手後に専用worktreeが2本増えた（#1033と#1034）ためである。**同一のrepository状態で連続して測った1 ⇄ 62 ⇄ 1が有効な比較である。** 55と62を同じ量として扱わない。

**残る1件は宣言どおりである。** `node_modules/.cache/issue-1049-ac04-fixture/`の末尾スラッシュdirectory entryで、01の12節が対象外(a)として先に切ったものと完全に一致する。

### 7.3 修正前の赤の観測

T01完了時点で**5件が赤、2件が緑**であった。緑の2件は正常系の回帰であり、計画で事前に緑と宣言していた。

| scenario | T01時点 |
|---|---|
| SCN-UNIT-WTIGN-001・002・003・004 | 赤 |
| SCN-INT-WTIGN-001 | 赤 |
| SCN-UNIT-WTIGN-005・006 | 緑（宣言どおり） |

SCN-UNIT-WTIGN-003の赤は`quotepath=true`でクォート形、`false`で生名となり2値が食い違うためであった。

### 7.4 INV-01の確認

`git diff --name-only fc745f4f bde3ccce`に`src/domain/worktree-removal-safety.ts`が**含まれない。** `validArtifactPath`を1行も変えていない。

## 8. 配布物影響

配布境界へ入る変更pathは`src/domain/worktree.ts`と`src/cli.ts`の2件である。いずれも`package.json`の`files`が配布対象とする。`docs/specs/`と`test/`は配布対象外である。

判断: 配布物を更新した

根拠: 利用側が受け取る`worktree survey`と`worktree finalize`の無視対象資産の観測形式が変わり、非ASCII名に対する判定結果が変わるため、配布物のdigestが変化する。

## 9. 独立reviewの成立

- **`.agent-skill-chain/review-exceptions.json`の`exceptions`は`RVX-REPORTED-SUCCESS-WITHOUT-REVIEW-001`の1件だけである。** 全文を読んで確認した。本Issueへ適用できる例外区分は存在しない。
- 外部reviewerによるapprovalは**0件**である。`approved`はAIによる最終裁定である。
- ラウンド1の敵対reviewは**implementer（claude）とは別providerのfable**が実施した。
- 進行役は指摘を受け入れる前に、R1-F01のバックスラッシュ名の生成可否とR1-F04の型castの要否を独立に実測した。

## 10. 仕様整合性

| 変更 | 更新先 | 追跡 |
|---|---|---|
| 無視対象資産をNUL区切りで観測し実行環境のgit設定に依存しないこと | `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | REQ-SQ-001 / AC-SQ-001 |
| 追跡行の追加 | `docs/specs/15_要件追跡/00_追跡表.md` | REQ-SQ-001 / AC-SQ-001 / SCN-UNIT-WTIGN-001〜006、SCN-INT-WTIGN-001 |

- **`trace:check`はAC↔SCNの対応の正しさを見ない。** 追加行の各列をscenarioの実体と人が原文で突合した。追加した2行のFeature列の7件のSCNがすべて実在し、実装列の`src/domain/worktree.ts`と`src/cli.ts`がいずれも実在することを確認した。
- **用語台帳へ追加していない。** 01の2.1節のとおり新語は無い。
- **`doctor`も同じ`collectWorktreeSurvey`を通るため観測形式が変わる。** `doctor`の仕様は「CLIが注入した走査結果」を報告すると定義しており、走査の正本は`worktree survey`行である。今回その行へNUL区切りを明記したため仕様矛盾は生じない。

## 11. 総合判定と再開地点

**approved。** 未解決のCritical/Highは0件、blockingは0件、`converged`である。

- 再開地点はStep 11の`pr create`である。
- **`pr create`は作成直後のread-backで`reconciliation-required`へ落ちることが既知である（#1077・#1079・#1081・#1085の4/4で再現）。** このとき副作用を再送しない。`pr create`を再実行するとread-only照合で既存PRを1件だけ束ね、`step11-recorded`まで進む。一致が0件または2件以上なら停止して人へ返す。
- CIが赤になった場合は残り予算1のラウンドで是正する。**赤を通す理屈を作らない。**
