# 111 課題1046 確定済み用語2件の耐久台帳反映 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。**Issue #965がStep 9で反映すべきだった用語2件の欠落を埋める。**

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1046 |
| ラウンド | Step 10 ラウンド2 |
| 比較基点 | `ca066bb986aaa850b27ec9420ad38e52e2e67676` |
| H_impl | `269aba71e13f954162a24e1d757de45b59b56d54` |
| 対象差分 | 耐久用語台帳と仕様変更履歴の2 file、3行。**実装は2 commitである。ラウンド2の是正を前進commitで積んだ** |
| 対象外 | 用語台帳の他項目の見直し、#1024の要件、**反映漏れを検出する新機構**（§5のM-01に評価結果を記す） |
| 残り予算 | Step 10の上限3ラウンドのうち1を残す |
| ラウンド数 | 2（Step 10のラウンド1と2） |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260902_171420_Issue-965が確定した用語2件を耐久用語台帳へ反映する |
| モード | quick（Q-01からQ-08がすべて真。外部契約もgateも変えない文書追記である） |
| 仕様の所有箇所 | `docs/specs/01_システム概要/02_用語・略語.md`そのもの |
| 成果物行数 | 製品: 0行。仕様: 3行。支援層: quick集約00が1文書 |
| 縮小の先行評価 | 実施済み。**新規Scenarioを足さない。** 台帳の特定行の存在をassertするtestは用語が増えるたび保守対象になり、検出できるのは自分が今書いた行の消失だけである。形式契約は既存の`SCN-UNIT-PACKAGE-013`が検査する |
| authority | 変更しない。`docs/specs/`は保護対象ではない |
| 実施者・日時 | reviewer、2026-09-02（JST） |

### 0.1 routing入力契約

| role欄 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類 | standard（riskがlow） | claude | Opus 5、effort high | 未解決Critical/Highがあれば停止し、是正後に同sessionの次ラウンドで再review | implementerとreviewerはいずれも本sessionのAI agentであり、**Git commit authorも同一である。§9に実態どおり記録し、独立性が成立したとは主張しない** |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 欠落の実在 | 変更前の`docs/specs/01_システム概要/02_用語・略語.md` | `TERM-ASC-`を機械抽出して51件、最大087、**072と073が欠番**であることを確認 | 実測 |
| 定義の正本 | `#965`のstaging `01_要件定義.md:37-38` | 2語の定義・成立例・反例・禁止表現・出典・ownerが`add`として確定済み。状態欄は`candidate（Step 9の仕様反映でactiveへ昇格する）` | 静的読解 |
| 意味の一致 | 上記原文と追加した2行 | 定義・成立例・反例・禁止表現・出典・ownerを1語ずつ突合し**完全一致**。状態欄だけを`active、v0.3.1、なし`へ写像した | 実測 |
| 標準語の非衝突 | 変更前の台帳全文 | 「配布準備工程」「配布前品質検証」がDistributionコンテキストに存在しない | 実測 |
| 既存行の非変更 | `git diff origin/main HEAD` | **3 insertions、0 deletions。** 既存51件の用語行と他Issueの履歴行に変更なし | 実測 |
| 形式契約の充足 | `npm run trace:check`、`SCN-UNIT-PACKAGE-013` | 合格。10列・空欄なし・種別`system`・状態`active`・ID重複なし・同一コンテキスト標準語重複なし | 実行観測 |
| 連番欠落検査案の棄却 | 変更前の台帳 | **欠番が36箇所ある。** 連続性は不変条件ではない | 実測 |
| staging突合案の棄却 | `git check-ignore -v` | `.gitignore:13`が`.agent-skill-chain/tmp/`を除外する。**CIのcheckoutにstagingは存在しない** | 実行観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: pass。`#965 staging → 本作業 → 耐久用語台帳`の一方向。artifact本文へ自身のcommit SHAを書いていない
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_impl`はラウンド2の是正commitであり、`H_impl..HEAD`の差分は本artifact 1 fileのみ
- reviewer stable IDがPR author/観測済み`H_impl` author stable IDと異なる: **異ならない。** §9に実態を記録した
- 既定branch追随: 本artifact作成時点で`比較基点`は`origin/main`のtipと一致する

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `docs/specs/01_システム概要/02_用語・略語.md` | M | package | spec | 確定済み用語2件の耐久化だけを行う。意味を新たに決めない | 宣言データであり呼び出しを持たない。循環なし | AC-1046-01、02、03。SCN-UNIT-PACKAGE-013 | 追加2行の削除で復旧する | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | 反映した事実と、検出機構を追加しない判断の根拠を記録する | 履歴への追記のみ。循環なし | AC-1046-04 | 1行の削除で復旧する | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: pass（`git diff --name-status`が`M`2件）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: pass
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: 修正は発生していない

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-1046-CONTIGUITY-001 | 検出案として用語IDの連番欠落検査を検討したが、**既存台帳に36箇所の欠番がある** | 検出案の棄却 | なし | 案を採らず棄却根拠を記録した。**導入すれば既存台帳が即座に不合格になる** | `TERM-ASC-`の機械抽出結果 | no-spec-impact | pass |
| DISC-1046-STAGING-001 | stagingは`.gitignore:13`で除外されており、**CIのcheckoutに存在しない** | 検出機構の実現可能性 | なし | staging由来の突合はCIで成立しないと結論し記録した | `git check-ignore -v`の出力 | no-spec-impact | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1046-01 | 該当なし | 台帳の追加2行 | 両IDが`active、v0.3.1、なし`で存在する | pass | §1 |
| AC-1046-02 | SCN-UNIT-PACKAGE-013 | 同上 | `trace:check`と`npm test`が合格 | pass | §7 |
| AC-1046-03 | 該当なし | `git diff` | 追加3行のみ。既存51件の用語行に変更なし | pass | §1 |
| AC-1046-04 | 該当なし | 00の§11と変更履歴 | 候補5件の可否と根拠を記述した | pass | §5のM-01 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | not-applicable | 認証情報も個人情報も扱わず、trust境界の判定入力を変えない | 変更file集合が仕様文書2件であること |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | not-applicable | ログ出力、相関情報、保持、rotation、監視、障害対応のいずれも追加・変更しない | 同上 |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 人が触れるUIを持たない仕様文書の変更である | `projectKind`が`cli` |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UI componentもthemeも持たない | `capabilities.designTokens`が`not-applicable` |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 追加した2行の定義・成立例・反例・禁止表現・出典が#965の原文と1語ずつ一致する |
| 価値 | 利用者・運用上の目的を満たすか | pass | 用語IDの衝突を台帳だけで検出できる。他Issueのstagingを実見しなくてよい |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | `docs/specs/`は保護対象ではなくproposalを要しない |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 追加2行が`validateDomainGlossary`の全条件を満たす |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 追加のみで既存行に触れない。新規検査を持ち込まない |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | 状態欄を`candidate`のまま写すと`validateDomainGlossary`が拒否する。`active`へ写像したうえで合格を実測した |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 文書追記であり部分適用が残らない。形式違反はfail-closedで拒否される |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | 10列すべてが非空である。IDも標準語も重複しない。**欠番が残ること自体は不変条件違反ではない** |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | 宣言データの追加であり実行経路を持たない |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | 定義を#965のstagingから引き、本作業で新たに意味を決めない |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | 追加のみ。既存51件の非変更を`git diff`で実測した |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | revertで完全に戻る |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | `docs/specs/`は配布境界に入らない（§8） |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| M-01 | Medium | **反映漏れを検出する機構が無い状態は残る。** Issue #1046の期待する挙動2を満たしていない | 候補5件の評価結果 | 同型の再発 | **本PRでは実装しない。** 既存の`validateDomainGlossary`・`check_trace.ts`・`check_conformance.ts`はいずれも台帳に無い用語を知る入力を持たない。連番欠落検査は既存台帳に36箇所の欠番があり成立しない。staging突合は`.gitignore`がstagingを除外するためCIから読めない。**成立させるにはstagingの耐久化という設計変更が要る。別Issueへ分離する** | valid（out-of-scope。評価結果を記録） | **残存する。** 同型の反映漏れは引き続きreviewでしか検出できない |
| L-01 | Low | 台帳の欠番が36箇所残る | 実測 | なし | **是正しない。** 欠番の由来を1件ずつ追う費用に見合う利益がない。連続性を不変条件にしない判断をM-01へ記録済み | valid（記録のみ） | 残存する |

**Critical/High 0件。**

## 6. ラウンド固有の確認

### ラウンド1（Step 10、2026-09-02、candidate HEAD = `H_impl`）

- 全評価基準を確認した: はい。肯定5観点・敵対8観点をすべて評価した
- 指摘を確定した: はい。M-01とL-01は記録のみ。**R2-H01を1件確定し、ラウンド2で是正した**
- 次ラウンド対象のCritical/High: **なし**
- blocking: 0件

### ラウンド2（Step 10、2026-09-02、本ラウンド、candidate HEAD = `H_impl`）

**ラウンド1で1件のblockingを確定した。** 既定branch追随のrebaseで衝突を解消する際、`base`列の書き換えに使ったsedが**#1111の変更履歴行のbase SHAまで書き換えていた。** 他Issueの確定済み記録を改変する変更であり、差分が追加のみという不変条件にも反する。

| ID | 内容 | 対応 |
|---|---|---|
| R2-H01 | 追随時のsedが#1111の履歴行の`base`列を`fbe1cef7`から`f1d65061`へ書き換えていた | #1111の行を原文へ戻し、**前進commitで是正した。** `git diff origin/main HEAD`が3 insertions・0 deletionsであることを実測した |

**是正をamendで畳めない。** review sessionは前ラウンドのcandidate HEADが現HEADのancestorであることを要求する。amendは前ラウンドのcommitを到達不能にするため、是正は前進commitにした。その結果`H_impl`はラウンド2のcommitへ繰り上がる。

- 未解決Critical/High: なし
- 修正差分: `docs/specs/15_要件追跡/01_変更履歴.md`の1行
- 修正で触れた隣接範囲: なし
- blocking: 0件

### ラウンド3

- 未実施。同一範囲の予算3のうち1を残している。**外部reviewの指摘があれば本ラウンドで扱う**

## 7. テスト結果

| 層・検査 | コマンド | シナリオ・件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---|---|---|---|---|
| 文書・受け入れ形式 | `npm run docs:format` / `test:format` | 2 | 2 | 0 | 0 | pass |
| 追跡・構造 | `npm run trace:check` / `architecture:check` | 2 | 2 | 0 | 0 | pass。孤立0件 |
| conformance | `npm run conformance:check` | project rule 20件 | 20 | 0 | 0 | pass。orphan 0件 |
| 全test層（unit・integration・e2e、runnerはcucumber-js） | `npm test` | 1392 scenarios | 1376 | 0 | 16 | pass |
| レビュー成果物の監査 | `npm run audit:check` | 1 | 1 | 0 | 0 | pass |

**skipした16 scenarioはsemantic graphのGraphQLite assetが未設定な環境に起因する。** 本差分はこの経路に触れない。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `docs/specs/01_システム概要/02_用語・略語.md` | 入らない | なし |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | なし |

判断: 配布物を更新しない

根拠: `package.json`の`files`は`dist/`、`.agent-skill-chain/`配下の6項目、`README.md`、`AGENTS.md`だけを列挙する。**変更した2 fileはこの集合に入らない。**

**releaseの起動と計画は分けて記録する。** `.github/workflows/release.yml`は`main`への`push`で起動するため、**workflow自体は起動する。** その後`planAutoRelease`が現行tagの存在と配布digest一致で計画を`skipped`とする。**「発火しない」ではなく「起動するが計画がskipされる見込み」が正確である。**

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | **本artifact作成時点では無い。** exact-headは`H_final`。immutable GitHub reviewは0件 |
| reviewerがPR author・実装commit authorと異なる | **異ならない。** implementerもreviewerも本sessionの同一AI agentであり、Git commit authorも同一である |
| 観測したreview commentとapprovalの件数 | 本session内のreview 2ラウンド（High 1は是正済み、Medium 1・Low 1は記録のみ）。GitHub review 0件 |

**`02_品質基準.md`の「独立reviewが成立しない場合」に従う。** 同節は「既定は停止ではなく、無記録での通過の禁止である」と定める。

**PR作成後の外部reviewを待つ。** ただし直前の#1123ではCodeRabbitがfree limitに達し詳細reviewを出力できなかった。**同じ状態が続く場合は、その事実をPRへ記録したうえでmergeする。**

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/01_システム概要/02_用語・略語.md`、`15_要件追跡/01_変更履歴.md`
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: pass。**#965で`candidate`だった2語を`active`へ昇格させた。これが本作業そのものである**
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: pass。標準語の非衝突を全文照合で確認した
- 要件・変更・SCN・テストの追跡: AC-1046-02 → 既存`SCN-UNIT-PACKAGE-013`。**新規SCNを追加しないため追跡表を変更しない**
- UI・トークンの判断: UIを所有しないためdesign token・layout tokenは対象外。ADRを追加しない

## 11. 総合判定と再開地点

- 未解決Critical/High: **なし**
- Medium/Lowの記録: M-01（検出機構の不在）、L-01（欠番の残存）。**High R2-H01は是正済み**
- 判定: approved（Step 10 ラウンド2）
- 新しい権限が必要な事項: mergeは別authority
- 残存リスク:
  1. **同型の反映漏れはreviewでしか検出できない。** 機構化にはstagingの耐久化が要る（別Issueへ分離する）
  2. 台帳の欠番36箇所は残る。連続性を不変条件にしない判断を記録した
- 次に許可される操作: push、PR作成、外部reviewの到着確認、必須check 2件の全緑確認、およびownerが承認したauthorityによる**通常merge commit方式**でのmerge。**squash mergeを使わない**
- 次回の再開地点: PR作成から
