# 課題826 実装レビュー

> 状態: `ready-for-pr`。手動dispatchのrelease自動化を、merge済みdefault branch headを比較基点として固定した内部監査証拠である。独立reviewの承認は自己申告せず、CIとPR reviewの外部証拠で確定する。

## 判定

| 項目 | 値 |
|---|---|
| 状態 | ready-for-pr |
| 比較基点 | `7e5a7de9bc271db165fb7a65bedba4663910dc06` |
| H_impl | `da3a42d6a9e860bebfc384a39b52d3b21e31b6f0` |
| H_impl tree | `7eeca83d3d5b043ab056c8d7dd049634d36b9caf` |
| Critical | 0 |
| High | 0 |
| Medium | 0 |
| Low | 0 |
| role分離 | implementerはCodex `gpt-5.6-sol`（reasoning high、service tier default）、coordinatorとreviewerはClaude Opus 5。同一scopeでidentityが異なる |

release候補の検証、version決定、Git tag、GitHub Release、npm公開を独立stageとして実装する。既定はdry-runで外部更新を行わず、各操作は実施後に外部状態を読み直して成功を確定する。

## 開発考慮事項

| ID | 考慮事項 | 判定 | 理由 | 要求・確認証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | npm token等の秘密値と公開権限を扱うため、最小権限と非出力を設計時に固定する | job単位permission、`NODE_AUTH_TOKEN`のenv渡し、秘密値出力stepの不在をSCN-INT-RELEASE-001で検証 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 途中失敗時にどこまで完了したかを運用判断へ使う | `$GITHUB_STEP_SUMMARY`への日本語記録、`summarizeReleaseOutcome`、SCN-UNIT-RELEASE-007 |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | applicable | dispatch入力と要約だけで判断させるため、日本語のstep名と入力説明を表示契約とする | 全step名の日本語化、入力descriptionの日本語化、SCN-INT-RELEASE-001 |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | 視覚componentとlayoutを所有せずtoken適用対象が存在しない | projectKindがcli、UI source変更なし |

## 変更ファイル個別監査

| path | status | owner | target layer | 責務・配置 | 依存・循環 | 仕様・追跡 | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.github/workflows/release.yml` | A | repository maintainer | CI | 手動dispatchのrelease workflow。validate、tag、github_release、npm_publishを独立jobへ分ける | jobは`needs`で一方向に連なり循環しない | AC-826-01〜06 | 既定はdry-run、job単位で最小権限、失敗時は成功報告を拒否。fileを削除すればrelease自動化前へ戻る | pass |
| `src/domain/release.ts` | A | package maintainer | domain | release計画と結果要約とworkflow契約検証の純関数 | `src/lib/version.js`だけへ依存し外部処理を持たない | AC-826-02〜05、SCN-UNIT-RELEASE-001〜008 | 不正入力をrejectedへ倒す。fileを削除すれば計画機能が消えるだけでrelease以外へ影響しない | pass |
| `scripts/plan_release.ts` | A | package maintainer | script | 環境変数からrelease計画を組み立てJSONを出力する薄いentrypoint | `src/domain/release.js`だけへ依存 | AC-826-02 | package scriptへ登録せずworkflowからだけ呼ぶ。削除で計画出力が消えるだけ | pass |
| `test/features/unit/release-plan.feature` | A | package maintainer | test | dry-run、単調version、tag重複、ref、gate、npm入力、部分失敗、不正入力の反例を固定する | featureからstep定義への参照のみ | SCN-UNIT-RELEASE-001〜008 | 反例が消えるとrelease安全条件の検出を失うため実装と同時に戻す | pass |
| `test/features/integration/release-workflow.feature` | A | package maintainer | test | 実workflow fileがtrigger・権限・dry-run既定・gate・秘密非出力を満たすことを検証する | featureからstep定義への参照のみ | SCN-INT-RELEASE-001〜002 | 同上 | pass |
| `test/steps/release.steps.ts` | A | package maintainer | test | 上記featureのstep実装。実workspaceを変更しない | domain exportと実workflow fileの読み取りだけ | SCN-UNIT-RELEASE-001〜008、SCN-INT-RELEASE-001〜002 | 読み取り専用で外部writeを行わない | pass |
| `docs/specs/12_運用保守/00_運用設計.md` | M | repository maintainer | 仕様 | release手順、権限境界、dry-run、失敗時の復旧を成立中の契約として記録する | 仕様から実装への参照のみ | AC-826-01〜06 | 節を戻せば旧記述へ復帰する | pass |
| `docs/specs/13_移行・廃止/00_移行方針.md` | M | repository maintainer | 仕様 | versionとtagの決定規則をrelease方針として記録する | 仕様から実装への参照のみ | AC-826-02 | 同上 | pass |
| `docs/specs/01_システム概要/02_用語・略語.md` | M | repository maintainer | 仕様 | release関連の用語をユビキタス言語へ追加する | 用語台帳から仕様・実装への参照のみ | AC-826-01 | 行を戻せば旧台帳へ復帰する | pass |
| `docs/specs/15_要件追跡/04_課題826追跡.md` | A | repository maintainer | 仕様 | AC、enforcement point、反例SCN、証拠、rollbackを対応付ける | 追跡表から実装とtestへの片方向参照 | AC-826-01〜06 | fileを削除すれば追跡が消えるため索引と同時に戻す | pass |
| `docs/specs/00_仕様書構成/00_仕様書索引.md` | M | repository maintainer | 仕様 | 追跡文書の索引へ課題826追跡を追加する | 索引から追跡文書への片方向link | AC-826-01 | 行を戻せば旧索引へ復帰する | pass |

Gitの`7e5a7de9bc271db165fb7a65bedba4663910dc06..da3a42d6a9e860bebfc384a39b52d3b21e31b6f0`に含まれる11 pathと表の11行は重複なし・欠落なしで一致する。

## 肯定・敵対レビュー

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ | pass | dry-run、単調version、tag重複、既定branch以外のref、gate欠落・失敗、不正SHA、不正version形式をすべて反例で拒否する |
| 価値・実現可能性 | pass | 手動dispatchだけで起動し、通常のmain pushではreleaseされない |
| 整合性 | pass | project choiceの`release`が定める「明示authorityによる手動操作」と一致する |
| 保守性 | pass | 判断は純関数へ、外部操作はworkflowへ分離した |
| 反例・失敗経路 | pass | 各stageは操作の成功だけでなく外部状態の読み直しが一致した場合だけsucceededにする |
| 境界値・悪用 | pass | 秘密値を出力するstepを契約検査で拒否し、npm公開はenvironment承認とid-token、provenanceを必須にする |
| 安全性・データ損失 | pass | tagとReleaseは既存確認後にだけ作成し、公開済みnpm versionは削除ではなくdeprecateを復旧手段とする |
| rollback | pass | 各stageの復旧手順を要約へ日本語で出力する |
| 範囲漏れ | pass | package.jsonのscript、protected file、CI workflowを変更していない |

### 敵対観点の残論点

`validateReleaseWorkflow`は依存追加を避けるため文字列一致でYAMLを検査する。構文的に等価だが表記の異なるYAML（引用符違い、flow style）を見落とす可能性がある。実workflowは本検査に合わせた表記で固定しており、workflowの表記を変える変更は同検査で落ちる。YAMLパーサー導入は依存追加を伴うためout-of-scopeとして記録する。

## テスト結果

| 層・検査 | コマンド | 成功 | 失敗 | 判定 |
|---|---|---:|---:|---|
| project設定・静的品質 | `project:quality`、`lint`、`format:check`、`typecheck`、`source:check` | 5 | 0 | pass |
| 全BDD | `npm test` | 319 | 0 | pass |
| 配布・追跡・監査 | `build`、`docs:format`、`test:format`、`trace:check`、`architecture:check`、`conformance:check`、`package:check` | 7 | 0 | pass |

`audit:check`は本artifactを追加したH_finalで実行する。

## 停止点

- H_impl: `da3a42d6a9e860bebfc384a39b52d3b21e31b6f0`。
- 本PRではrelease、tag、npm公開を実行しない。workflowは追加するだけである。
- mergeはCIとreviewの外部証拠が揃った時点で別操作として行う。

H_finalはこのreview artifactだけを追加するcommitとし、以後このtracked artifactを変更しない。
