# 84 課題996 mode assessment入力形式の提示 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。`--assessment`が受け取る入力形式が配布物のどこにも定義されておらず、唯一Q-01〜Q-08を扱う配布schemaは別の形（mode決定の記録）であったため、利用者が仕様どおりに書くと停止していた欠陥を、merge済みdefault branch headを比較基点として固定した内部監査証拠である。**`valid: true`は候補が既定branchを基準に検証を通過したことを示すだけであり、既定branchへの反映を示さない。**

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象Issue | #996（親 #1025 第1層） |
| 比較基点 | `f9e020cb11747aeb971ffa6d25e13991134c17b6` |
| H_impl | `a3d2ac54da6d1a2632369b45bff7a1ba9ac7c37e` |
| reviewer | claude（利用側fixtureでの再現と変異試験） |
| 実施日 | 2026-08-28 |
| ラウンド数 | 2 |
| Step chain | 迂回: 手書き運用で進めており、workflow recordを経由していない |
| 仕様の所有箇所 | `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`「usageと必須入力の提示」。「`src/cli-usage.ts`がsubcommandごとの要約、必須flag、条件付きflag、任意flagと既定値、位置引数、実行例を保持する**単一正本**である」「**無言で未指定として扱わない**」 |
| 成果物行数 | **製品22行**（`src/cli-usage.ts` +5/-1、`src/adapters/json-input.ts` +18/-3）。支援層はtest 46行（反例1件）、仕様1行（追跡表へSCN 1件） |
| 縮小の先行評価 | **新しいschemaも新しい配布文書も作っていない。** 形式の提示は既存の発見経路であるusage（`--help`）へ載せ、配布文書は既存の自動生成区画がそのまま案内する。「配布schemaのmode決定記録も受理する」案は評価したが、`mode`を尊重するのか無視するのかが曖昧になりfail-closedを崩すため採らなかった |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| **Issue #996の前提はmainで解消済み** | `.agent-skill-chain/docs/01_開発ワークフロー.md:21-28`、`src/domain/mode.ts` | Q-01〜Q-08の質問文が両方に存在し、`conformance:check`が完全一致を強制する（REQ-WF-011） | 既存コード |
| **quickは到達できる** | 利用側fixtureで製品CLIを実行 | 全件`true`＋根拠の回答mapを渡すと`"mode": "quick"`、終了値0 | 実行観測 |
| **形式が配布物のどこにも無い** | `.agent-skill-chain/`全体の横断検索 | `assessment`の語が配布物の文書・skill・schemaに1件も無い（`tmp/issues/`の過去成果物を除く） | 実行観測 |
| **記録どおりに渡すと停止する** | 同fixture | `workflow-mode-decision.schema.json`に従ったJSONで`ASC-CLI-VALIDATION-001`。理由は「mode assessmentが不正です」だけで期待形式を示さない | 実行観測 |
| 発見経路 | 配布文書の自動生成区画 | 「必須flagを1件ずつ試す必要はない。`--help`を付けるとusageをJSONで返す」と案内する。**そこに形式が無いことが欠陥である** | 既存コード |

**この欠陥はreviewではなく製品実行で見つけた。** #996の主張を確かめるためにCLIを動かし、最初の1回で踏んだ。

## 変更ファイル個別監査

| path | status | owner | target layer | 責務・配置 | 依存・循環 | 仕様・追跡 | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/cli-usage.ts` | M | package owner | CLI usage正本 | `--assessment`の説明へ回答mapの形を書く | 変更なし | REQ-WF-009、SCN-UNIT-CLIUSAGE-013 | 逆変換で戻る | pass |
| `src/adapters/json-input.ts` | M | package owner | 入力adapter | 3つの拒否理由へ同じ形を載せる | 追加依存なし | 同上 | 逆変換で戻る | pass |
| `test/steps/cli-usage.steps.ts` | M | package owner | test | 記録を渡した場合の反例 | 実装へ単方向 | SCN-UNIT-CLIUSAGE-013 | 追加分の除去で戻る | pass |
| `test/features/unit/cli-usage.feature` | M | package owner | test | 反例1件 | 実装へ単方向 | 同上 | 4行の除去で戻る | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | 利用側 | 仕様 | SCN-UNIT-CLIUSAGE-013を既存行へ追記 | 参照のみ | 同上 | 1件の除去で戻る | pass |

## 2. 受け入れ条件の確認

| 条件 | 結果 | 証拠 |
|---|---|---|
| 利用者が形式を知る経路が存在する | 充足 | `issue create --help`の`--assessment`説明が回答mapの形を示す |
| 誤った形を渡したとき期待形式が返る | 充足 | 拒否理由に`Q-01〜Q-08をキーに{"answer":…,"evidence":…}を持つobjectを渡してください`が入る |
| 正しい形でquickへ到達できる | 充足 | 利用側fixtureで`"mode": "quick"`、終了値0 |
| 既存シナリオが全通過 | 充足 | `npm test`が**1012 scenario全通過** |

### 2.1 開発考慮事項の適用判定（必須）

| ID | 判定 | 確認 |
|---|---|---|
| DC-PRIVACY | not-applicable | 入力形式の提示だけで、扱う情報は変わらない |
| DC-OBSERVABILITY | applicable | **拒否理由が期待形式を名指しする。** 無言でも曖昧でもない |
| DC-UX | applicable | 利用者が製品を使い始める最初の分岐点である |
| DC-TOKENS | not-applicable | UI要素を持たない |

## 3. 肯定的評価

- **Issueの主張を先に実行で確かめた。** 「質問文が無い」という前提はmainで解消済みであり、放置すると存在しない欠陥を直すところだった
- **同じ症状の別原因を、自分が踏んで特定した。** 利用者の再現手順をなぞる過程で最初の1回で停止した
- **既存の発見経路へ載せた。** 新しいschemaも文書も作らず、`--help`という既に案内済みの経路の内容を正した
- **受理範囲を広げなかった。** 記録も受理する案はfail-closedを崩すため採らなかった

## 4. 敵対的評価

| 反例 | 結果 |
|---|---|
| #996の主張どおり質問文が無い | **不成立。**main時点で規範文書と`src/domain/mode.ts`の両方に存在し、機械的に一致が強制されている |
| quickが構造的に到達不能 | **不成立。**利用側fixtureで`"mode": "quick"`を観測した |
| 文言を変えただけで検出できない | **不成立。**変異で期待形式をmessageから外すとSCN-UNIT-CLIUSAGE-013が落ちる |
| usageの説明は誰も読まない | **不成立。**配布文書の自動生成区画が`--help`を正規の発見経路として案内している |
| 記録も受理すべきである | **採らない。**`mode`を尊重するのか無視するのかが曖昧になる。fail-closedの原則に反する |
| 形式の提示は`--help`だけで足りるか | **限界がある。**skillの記述（Step 0）は形式を持たない。**skill本文への追記は配布物の変更であり、別Issueで評価する**（S84-M-01） |

## 5. 指摘

| ID | 深刻度 | 内容 | 状態 |
|---|---|---|---|
| S84-M-01 | Medium | Step 0のskill本文は「Q-01〜Q-08の回答と根拠」と述べるだけで、`issue create`のcommandも入力形式も持たない。**利用者のagentはusageを引かなければ形式に到達できない** | 未是正。skill本文の拡張は配布物の契約変更であり、本PRのscopeを超える。#1024（consumer acceptance）の対象として評価する |
| S84-L-01 | Low | 配布schema `workflow-mode-decision.schema.json` は記録であって入力ではない、という区別が配布物の文書に無い | 未是正。usageの説明文へ「mode決定記録そのものは受理しない」と明記して当面の混同を防ぐ |

### ラウンド予算

ラウンド2で収束した。未解決のCritical/Highは0件。上限3ラウンドに対して1ラウンドの余裕を残している。

## 6. ラウンド固有の確認

### ラウンド1

全評価基準を確認した。Medium 1、Low 1。新規Critical/High 0件。判定 **candidate-verified（自動reviewを待つ）**。

### ラウンド2（自動review）

CodeRabbitがactionable 2件を出した。**2件とも是正した。どちらも私の誤りである。**

| 指摘 | 判定 | 対応 |
|---|---|---|
| review artifactの要件IDがREQ-WF-001だが、追跡表の該当行はREQ-WF-009である | **妥当。記載ミス。** | REQ-WF-009へ直した |
| **usageと拒否理由が「Q-01〜Q-08」「{answer, evidence}を持つ」と述べるが、実装はキーを検証せず両fieldも任意である。検証していないことを検証済みのように書いている** | **妥当。** `readModeAssessment`はキーを問わず、`answer`・`evidence`はいずれも省略可能で、欠けたIDは`classifyMode`が不明として扱い`full`へ倒す | 文言を「質問IDをキーに…を渡してください。回答か根拠が欠けたIDは不明として扱いfullへ倒します」へ改めた。**受理範囲の記述と実装を一致させ、classifyModeの帰結まで書いた** |

是正後に対象scenarioを再実行し`13 scenarios (13 passed)`。実装commitはamendで畳み、`H_impl`を取り直している。

## 7. テスト結果

| 層・検査 | コマンド | 結果 |
|---|---|---|
| 全層 | `npm test` | **1012 scenario全通過** |
| 契約 | `project:quality`、`trace:check`、`architecture:check`、`package:check`、`conformance:check`、`docs:format`、`test:format` | すべてexit 0 |
| 静的 | `typecheck`、`lint`、`format:check` | exit 0 |

**利用側fixtureでの再現（比較基点 `f9e020cb`）。** 隔離directoryへ`install --apply`した利用側repositoryで製品CLIを実行した。

| 入力 | 比較基点の観測 | 本PRの観測 |
|---|---|---|
| 配布schema `workflow-mode-decision.schema.json` に従ったJSON | **`ASC-CLI-VALIDATION-001`。理由は「mode assessmentが不正です」だけ** | **同じく拒否。理由が`Q-01〜Q-08をキーに{"answer":…,"evidence":…}を持つobjectを渡してください`を含む** |
| Q-01〜Q-08の回答map | `"mode": "quick"`、終了値0 | 同左（変わらない） |
| `issue create --help` | `--assessment=<path>`「事前評価のJSON file」 | **回答mapの形と、記録を受理しないことを示す** |

**変異試験。** 拒否理由から期待形式を外すと、SCN-UNIT-CLIUSAGE-013だけが失敗する（`13 scenarios (12 passed, 1 failed)`）。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/cli-usage.ts`、`src/adapters/json-input.ts` | **入る**（`files`が`dist/src/`を列挙する） | **consumerが観測するusageと拒否理由の文言が変わる。** 受理する入力の集合は変えていない |
| `test/`、`docs/` | 入らない | `files`が列挙しない |

判断: 配布物を更新した

根拠: `dist/src/cli-usage.js`と`dist/src/adapters/json-input.js`の内容が変わる。**受理・拒否の判定そのものは変えておらず、変わるのは提示する文言だけである。**`npm run package:check`はexit 0。

## 9. 独立reviewの成立

| 条件 | 充足 |
|---|---|
| reviewerが実装担当の判断を入力に持たない | 充足。利用側fixtureでの実行観測と変異試験を入力にした |
| 各ラウンドの判定と根拠が原文引用を伴う | 充足。CLI・GitHub契約とIssue #996本文を引用 |
| 有限ラウンドで終了する | 充足。1ラウンドで収束 |
| 外部証拠 | PR作成後のCI結果と自動reviewで補う |

**Issueの主張を実行で確かめたことが、この変更で最も効いた判断である。** #996は「quickが構造的に到達不能」と述べていたが、その原因は既に解消していた。**同じ症状を出す別の原因が残っていた。**

## 10. 仕様整合性

`docs/specs/15_要件追跡/00_追跡表.md`へSCN-UNIT-CLIUSAGE-013を追記した。要件本文は変更していない。**本変更はCLI・GitHub契約が既に定める「単一正本が必須入力を提示する」を満たすための修正であり、新しい要件を導入しない。**

## 11. 総合判定と再開地点

**判定: candidate-verified（外部承認待ち）**

- 未解決Critical: 0件
- 未解決High: 0件
- 未解決Medium: 1件（S84-M-01はskill本文の契約変更であり#1024で評価）
- 記録したLow: 1件

再開地点: ステップ11（PR作成）。**merge後にIssue #996 のcloseコメントへ、前提が解消済みであったことと残存原因の実測を残す。**
