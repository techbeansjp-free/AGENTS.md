# 121 課題1135 支援層の分量を判断の量へ比例させる原則 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。**本artifact自身が新しい原則に従って書かれている。判断のある節を書き、判断の無い節を1行へまとめた。**

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1135 |
| 比較基点 | `239eb366d7aec4c64090fcc0b27983c048e10c69` |
| H_impl | `28683bb8c0b994f5499b9719b366da245e6c50fd` |
| 対象差分 | 運用ポリシー、issue template 4件、仕様2件の7 file。**12 insertions、0 deletions** |
| 対象外 | **次はいずれも同じ理由で対象外である。** 検査codeの変更（受理集合を変えない方針）、DC判定とrouting契約の記録箇所の集約（検査側の変更を伴うため分離）、templateの節削除（実測で削減対象として妥当でないと判明）、rebase連鎖とCI回数（#1135の優先度2・3が所有） |
| 残り予算 | Step 10の上限3ラウンドのうち1を残す |
| ラウンド | Step 10 ラウンド2 |
| ラウンド数 | 2（Step 10のラウンド1〜2） |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260903_080942_支援層の分量を判断の量へ比例させる原則を明記する |
| モード | full（Q-01が偽。配布される公開契約の本文を変える） |
| 仕様の所有箇所 | `docs/specs/02_要件/04_仕様・品質管理要件.md` の REQ-SQ-024「開発速度の観測基準を記録させる」 |
| 成果物行数 | 製品: 7 fileで12 insertions・0 deletions。**支援層: staging 00〜03が10,894文字（本日平均34,806文字の31%）** |
| 縮小の先行評価 | **実施済み。隔離実験で機械要求の最小を実測してから削減対象を選んだ。** templateを削る案と検査を緩める案を実測で棄却した |
| authority | 変更しない。**検査codeへの変更0行。`issue validate` の受理集合は不変** |
| 実施者・日時 | reviewer、2026-09-03（JST） |

### 0.1 routing入力契約

| role欄 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類 | routine（riskがlow） | claude | Opus 5、effort high | 受理集合が1件でも変わったら停止する | implementerとreviewerはいずれも本sessionのAI agentであり、**Git commit authorも同一である。§9に実態どおり記録する** |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 命題違反の現存 | 本日merge した5 PR | staging 00〜03が1件平均34,806文字。#1145 は**12行の変更に約35,000文字** | 実測 |
| 機械要求の最小 | 隔離実験用staging | `01`〜`03` を最小化して `issue validate` が `valid: true`。**合計9,002文字。差の74%は要求されていない** | 実行観測 |
| 要求される4点 | `src/domain/issue.ts:604,675,681`、`src/domain/staging.ts:483` | fileの存在、DC 4行表、placeholder不在、`Scenario: SCN-` の行が1つ以上。**節構造は検査されない** | 実測 |
| stagingが読まれないこと | `scripts/check_file_audit.ts:723` | commentが「staging（`.agent-skill-chain/tmp/`）は`.gitignore`の対象で、追跡fileが0件である」と明記 | 実測 |
| 起票時の記述の誤り | `00_要求定義_full.md` の `##` 節 | **14節・12強制・任意2。** 起票時の「29節のうち17節が要求されていない」は誤り | 実測 |
| templateが肥大していないこと | 4 templateの文字数 | 3,398〜5,464文字。**私が書く34,806文字の1/6以下** | 実測 |
| 肥大の直接原因 | `02_設計.md` template原文 | 「該当しない節は削除せず、対象外理由を1文で記録する」が節ごとの反復を生む | 実測 |
| 検査codeの非変更 | `git diff --name-only` | `src/`・`scripts/` 0件。変更は配布文書5件と仕様2件 | 実測 |
| **原則の有効性** | 本Issue自身のstaging | **10,894文字。本日平均の31%。69%削減** | 実行観測 |
| 全gate | `npm test`、`conformance:check` | 1415 scenario、0失敗。project rule 20件、orphan 0件 | 実行観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: pass。原則を運用ポリシー1箇所へ置き、template 4件が同じ内容を短く述べる一方向
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: pass
- reviewer stable IDがPR author/実装commit author stable IDと異なる: **異ならない。** §9に記録した
- 既定branch追随: **不要。** 本artifact作成時点で `origin/main` は base から動いていない

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/docs/00_運用ポリシー.md` | M | package | package | 原則の正本。成立条件の直後へ置く | 規範文書は実装を参照しない | AC-1135-01、AC-1135-02 | revertで復旧 | pass |
| `.agent-skill-chain/templates/issue/00_要求定義_full.md` | M | package | package | 12節が全て強制であるため「存在は要求するが分量は要求しない」と述べる | templateは正本へ一方向 | AC-1135-01 | 同上 | pass |
| `.agent-skill-chain/templates/issue/01_要件定義.md` | M | package | package | 機械要求の4点を明示し1行集約を許す | 同上 | AC-1135-01 | 同上 | pass |
| `.agent-skill-chain/templates/issue/02_設計.md` | M | package | package | **肥大の直接原因の直後へ置く。** 「1文で記録する」と「1行へまとめる」を隣接させる | 同上 | AC-1135-01 | 同上 | pass |
| `.agent-skill-chain/templates/issue/03_実装計画.md` | M | package | package | 同上 | 同上 | AC-1135-01 | 同上 | pass |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | M | package | spec | REQ-SQ-024の本文へ原則を記録する | 仕様は実装を参照しない | REQ-SQ-024 | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | 実測と起票時の誤りの訂正を記録する | 履歴のみ | 該当なし | 1行の削除で復旧 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: pass（7件）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: pass

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | **起票時の「29節のうち17節が機械検査に要求されていない」は誤り。** 実測は14節・12強制・任意2 | 削減対象の選定 | なし | templateを削る案を棄却し、書き方の側を直す案へ変えた | `00_要求定義_full.md` の `##` 節を数えた | no-spec-impact | pass |
| DISC-002 | 肥大の直接原因は `02_設計.md` の「該当しない節は削除せず、対象外理由を1文で記録する」である | 是正箇所 | なし | 無記録を防ぐ性質を保ったまま「同じ理由なら1行へまとめる」を隣接させた | template原文の読解 | no-spec-impact | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1135-01 | SCN-UNIT-PACKAGE-013 | 5 fileが原則を述べる | `skills:check` errors 0件 | pass | §7 |
| AC-1135-02 | SCN-UNIT-PACKAGE-013 | 「記録の省略を許すものではない」の明記 | 運用ポリシーとREQ-SQ-024の双方に存在 | pass | §1 |
| AC-1135-03 | SCN-UNIT-PACKAGE-013 | 受理集合の不変 | `git diff --name-only` に `src/`・`scripts/` 0件。`npm test` 0失敗 | pass | §1、§7 |
| AC-1135-04 | 該当なし | 本Issue自身のstaging | **10,894文字。本日平均の31%** | pass | §1 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | 記録の省略を許すと「無記録での通過の禁止」を破る | 「記録の省略を許すものではない」を原則と同じ段落へ置いた |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 対象外である事実を残すか消すかの判断そのものである | 「対象外である事実は必ず残し、同じ理由を複数回書き直す作業だけを止める」を運用ポリシーとREQ-SQ-024の双方へ書いた |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 人が触れるUIを持たないMarkdown文書である | `projectKind`が`cli` |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UI componentもthemeも持たず、色・間隔・typographyの決定を含まない | `capabilities.designTokens`が`not-applicable` |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 5 fileすべてが原則と「省略を許さない」旨を述べる |
| 価値 | 利用者・運用上の目的を満たすか | pass | **本Issue自身で69%削減を実証した。** 判断のある箇所は1つも削っていない |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 12行の追加で成立する |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | REQ-SQ-024の本文が原則を記録する |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 正本を運用ポリシー1箇所へ置いた |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | 既存の記入済み成果物は受理集合が不変のため通り続ける |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 記述の追加であり部分適用が残らない |
| 境界値 | 空、最大、最小、重複、Unicode等 | 対象外 | 記述の追加であり値を解釈しない |
| **悪用** | **「短く書いてよい」を「記録しなくてよい」と読んで対象外の事実を消す** | **pass** | **これが本変更の最大のリスクである。** 「判断のある箇所は具体的に書き」を先に置き、「記録の省略を許すものではない」を同じ段落へ置いた。**別の場所に置くと読み飛ばされる** |
| 悪用 | 原則を口実に検査を緩める | pass | **検査codeへの変更0行を `git diff --name-only` で確認した** |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | 記述の追加であり認可を変えない |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | **0 deletions。** 既存の記述を1行も消していない |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | revertで戻る。配布digestが変わるためtagはrevertで取り消せない（§8） |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | 運用ポリシー1件とtemplate 4件。**`04_レビュー.md` へ触れていない**（`ROUTING_INPUT_CONTRACT_MARKERS` 5件を `skills:check` が強制するため） |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| M-01 | Medium | **原則は機械強制されない。** 利用者が読まなければ従わない | 実測。`skills:check` は文言の存在を検査しない | 遵守 | **機構を足さない。** 分量の閾値による自動停止は REQ-SQ-024 が明示的に禁じている（「実質的な記録ほど落ちる代理指標になりやすい」） | valid（設計判断。record-only） | 残存する |
| M-02 | Medium | **DC判定とrouting契約が依然5文書・3文書に重複する（計4,314文字）** | 実測 | 支援層の量 | **本Issueでは是正しない。** 検査が4文書すべてにDC表を要求しており、集約は検査側の変更を伴う。#1135のコメントへ案A・Bとして記録した | valid（out-of-scope。record-only） | 残存する |

**Critical/High 0件。**

## 6. ラウンド固有の確認

### ラウンド1（Step 10、2026-09-03、candidate HEAD = `H_impl`）

- 全評価基準を確認した: はい。肯定5観点・敵対9観点をすべて評価した
- 指摘を確定した: はい。M-01とM-02はいずれも記録のみ
- 次ラウンド対象のCritical/High: **なし**
- blocking: 0件

### ラウンド2

- 本artifactのcommitでHEADが動くため再固定する

### ラウンド3

- 未実施。**予算1を外部reviewの是正のために残す**

## 7. テスト結果

**承認証拠は成功CI runである。**

実行したcommand: `docs:format`、`test:format`、`lint`、`format:check`、`typecheck`、`source:check`、`trace:check`、`architecture:check`、`skills:check`、`build`、`package:check`、`npm test`、`conformance:check`、`audit:check`、`issue validate`

全layerの合計: **1415 scenario、1399 成功、0 失敗、16 skip。** `conformance:check` はproject rule 20件・orphan 0件で合格。

失敗またはskipがある層: skipした16 scenarioはsemantic graphのGraphQLite assetが未設定な環境に起因する。本差分はこの経路に触れない。

対応する成功CI run: PR作成後に取得し、merge前に現`H_final`へ束縛する。

**変異試験を行わない。** 記述の追加であり判定logicを持たない。変異させる対象が無い。

## 8. 配布物影響

| 変更path | 配布境界に入るか |
|---|---|
| `.agent-skill-chain/docs/00_運用ポリシー.md` | **入る** |
| `.agent-skill-chain/templates/issue/00_要求定義_full.md` | **入る** |
| `.agent-skill-chain/templates/issue/01_要件定義.md` | **入る** |
| `.agent-skill-chain/templates/issue/02_設計.md` | **入る** |
| `.agent-skill-chain/templates/issue/03_実装計画.md` | **入る** |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | 入らない |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない |

判断: 配布物を更新した

根拠: `package.json` の `files` は `.agent-skill-chain/docs/` と `.agent-skill-chain/templates/` を含む。**7 fileのうち5 fileが配布境界に入る。**

**releaseの起動と計画は分けて記録する。** `release.yml` は `main` への `push` で起動し、その後 `planAutoRelease` が配布digestの差分を見て計画を決める。**本変更は配布digestを変えるため、version bumpとtagが作られる見込みである。** merge後に実観測する。**npm公開は自動経路では発火しない。**

| 対象 | revertで戻るか | 実行者 | 手順 | 完了確認 |
|---|---|---|---|---|
| repository状態 | **戻る** | 実行役 | 打ち消しPRを通常mergeする | `git diff` が元の状態と一致する |
| 公開済みtagとGitHub Release | **戻らない** | repository owner | 削除せず次versionで前進する | `gh release list` に新tagが載る |
| npm registryの公開物 | **公開されない** | 該当しない | 本経路では公開しない | `npm view` に該当versionが無い |

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | **本artifact作成時点では無い。** exact-headは`H_final`。immutable GitHub reviewは0件 |
| reviewerがPR author・実装commit authorと異なる | **異ならない。** 同一sessionのAI agentであり、Git commit authorも同一である |
| 観測したreview commentとapprovalの件数 | 本session内のreview 2ラウンド（Critical/High 0、Medium 2）。GitHub review 0件 |

**`02_品質基準.md` の「独立reviewが成立しない場合」に従う。** 同節は「既定は停止ではなく、無記録での通過の禁止である」と定める。

**本変更はrepository ownerの明示承認を得ている。** 2026-09-03、支援層の縮小に着手してよいかの問いに対し「許可します」を得た。**owner承認なしで配布物の記述規範を変えていない。**

**PR作成後の外部reviewを待つ。** 予算1ラウンドを残してある。**`pr create` 後の指摘は同じPRへ取り込まず、判定と分離先をreviewスレッドへ返信する。**

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: REQ-SQ-024の本文、`15_要件追跡/01_変更履歴.md`
- ドメイン用語台帳: **新規用語を追加しない**
- 要件・変更・SCN・テストの追跡: REQ-SQ-024 → AC-SQ-024 → 既存 `SCN-UNIT-PACKAGE-013`。**新規SCNを追加しないため追跡表を変更しない**
- UI・トークンの判断: UIを所有しないため対象外。ADRを追加しない

## 11. 総合判定と再開地点

- 未解決Critical/High: **なし**
- Medium/Lowの記録: M-01（原則は機械強制されない）、M-02（DC判定とrouting契約の重複4,314文字が残る）
- 判定: approved（Step 10 ラウンド2）
- 新しい権限が必要な事項: mergeは別authority
- 残存リスク:
  1. **規約は機構ではない。** 分量の閾値による自動停止は REQ-SQ-024 が明示的に禁じている
  2. **DC判定とrouting契約の重複が残る。** 検査側の変更を伴うため分離した
  3. **配布digestが変わるため自動releaseの計画が成立する見込みである。** 公開済みtagはrevertで取り消せない
- 次に許可される操作: push、PR作成、外部reviewの到着確認、必須check 2件の全緑確認、およびownerが承認したauthorityによる**通常merge commit方式**でのmerge。**squash mergeを使わない**
- 次回の再開地点: PR作成から
