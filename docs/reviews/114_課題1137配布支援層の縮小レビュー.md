# 114 課題1137 配布templateとStep skillの支援層縮小 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。**`00_運用ポリシー.md`の成立条件違反の是正である。** 支援層の所要時間が成果物構築を上回っていた。

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1137 |
| ラウンド | Step 10 ラウンド2（外部review反映） |
| 比較基点 | `b6acf118b9636d98e7fc876ab2b4ee31f184f2ae` |
| H_impl | `d94d7bfda9bea6960f8c51f34335142664f116a3` |
| 対象差分 | 配布template 2件、Step skill 1件、変更履歴の4 file |
| 対象外 | 機械強制されている節、00〜03の集約profile、mode判定質問、Step列、二段階承認、audit 2 commit構造 |
| 残り予算 | Step 10の上限3ラウンドのうち1を残す |
| ラウンド数 | 2（Step 10のラウンド1と2）。**Step 7のreadiness checkは3ラウンド予算に数えない** |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260903_033642_配布templateとStep-skillの機械強制されていない支援層を縮小する |
| モード | full（Q-01が偽。配布される公開契約を変更する） |
| 仕様の所有箇所 | `docs/specs/02_要件/04_仕様・品質管理要件.md`のREQ-SQ-008。**本文を変えず変更履歴だけを更新する** |
| 成果物行数 | 製品: 配布文書3件で10 insertions・16 deletions。仕様: 1行。支援層: staging 00〜03が29113文字 |
| 縮小の先行評価 | 実施済み。**新規Scenarioを足さない。** 節の有無をassertするtestはtemplateを変えるたび保守対象になり、検出できるのは自分が今消した節の復活だけである。形式契約は既存`SCN-UNIT-PACKAGE-013`が検査する |
| authority | 変更しない。**機械検査の受理集合を変えない** |
| 実施者・日時 | reviewer、2026-09-03（JST） |

### 0.1 routing入力契約

| role欄 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類 | standard（riskがlow） | claude | Opus 5、effort high | 未解決Critical/Highがあれば停止し、是正後に同sessionの次ラウンドで再review | implementerとreviewerはいずれも本sessionのAI agentであり、**Git commit authorも同一である。§9に実態どおり記録し、独立性が成立したとは主張しない** |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 成立条件違反 | `.agent-skill-chain/docs/00_運用ポリシー.md` | 「支援層の所要時間が成果物構築の所要時間を上回らないこと」に対し、実測が支援層40〜60分・成果物20〜40分 | 静的読解と実測 |
| 縮小の許容範囲 | 同文書 | 「縮小の対象は手段の実施回数、範囲、所要時間であり、安全条件、authority分離、fail-closed不変条件に属する手段を含まない」 | 静的読解 |
| template節数 | `00_要求定義_full.md`と`src/domain/issue.ts` | template 29節に対し`issueRequiredHeadings("full")`は12節 | 実測 |
| §10・§11が必須であること | `issueRequiredHeadings`の返り値 | `10. P-01〜P-07の適用計画`と`11. 図表と識別子の判断`が必須12見出しに含まれる。**助言では削れるとされていた** | 実測 |
| §0.1が必須であること | `scripts/check_skill_templates.ts:112-127` | `ROUTING_INPUT_CONTRACT_MARKERS`が`role欄`・`provider欄`・`model設定欄`・`fallback欄`・`独立性証拠欄`の5件を`04_レビュー.md`へ強制する | 実測 |
| 実際に落ちたこと | `npm run skills:check` | §0.1を縮めた状態で`templates/issue/04_レビュー.md: routing入力契約の独立性証拠欄がありません`。**撤回した** | 実行観測 |
| §5.2が非必須であること | 必須12見出しの一覧 | `### 5.2`は`## 5. 要求の概要`の下位であり必須に含まれない | 実測 |
| §7が非強制であること | `check_file_audit.ts`と`check_skill_templates.ts` | `7. テスト結果`をparseする箇所が0件 | 実測 |
| 受理集合の不変 | 9種の高速ゲート | lint・format:check・typecheck・source:check・test:format・trace:check・architecture:check・skills:check・package:checkがすべて合格 | 実行観測 |
| 差分の限定 | `git diff --name-only` | 4 fileのみ | 実測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: pass。`機械検査 → 配布template`の一方向。artifact本文へ自身のcommit SHAを書いていない
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_impl`が直前commitであり、`H_impl..HEAD`の差分は本artifact 1 fileのみ
- reviewer stable IDがPR author/観測済み`H_impl` author stable IDと異なる: **異ならない。** §9に実態を記録した
- 既定branch追随: **実施済み。** **本artifactを作る前に`rebase --onto origin/main`で追随した。** 先にartifactを作るとbaseが動くたびSHA行を書き直す手戻りが出る

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/templates/issue/00_要求定義_full.md` | M | package | package | §5.2を削除し委譲先を§5.1へ1文で示す。必須12見出しは不変 | templateは検査を規定しない。循環なし | AC-1137-01。SCN-UNIT-PACKAGE-013 | revertで戻る。**既存の記入済み成果物は§5.2を持っていても通る** | pass |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | M | package | package | §7の手書き件数表を成功CI run参照と合計と失敗層の展開へ置き換える。§0.1と機械parse対象の節は不変 | 同上 | AC-1137-02 | 同上 | pass |
| `.agent-skill-chain/skills/step-10-review/SKILL.md` | M | package | package | template全文読取をDC判定が`applicable`の領域と差分が触れた領域へ条件化する。`ROUTING_INPUT_CONTRACT_MARKERS`は不変 | skillはtemplateへ一方向。循環なし | AC-1137-03 | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | 非変更範囲を実際に維持した契約へ限定して記録する | 履歴のみ。循環なし | 該当なし | 1行の削除で復旧する | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: pass（`git diff --name-status`が`M`4件）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: pass
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: §0.1の撤回で`04_レビュー.md`を再監査した

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-1137-ENFORCED-001 | 諮問で挙がった削減候補6件のうち**3件が機械強制**だった | scopeが縮む。削減量が当初見込みより小さい | scope変更あり | 3件を対象外へ移し、00と02のscope欄を実測へ合わせた。**`04`の§0.1は実際に縮めてから`skills:check`の拒否で気付き撤回した** | `skills:check`の失敗と回復 | no-spec-impact | pass |

**この発見が本Issueの中心である。** 「助言で削れるとされた」を根拠に削っていたら、`issue validate`と`skills:check`を壊していた。**縮小の前に強制点を機械的に確かめる手順が要る。**

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1137-01 | SCN-UNIT-PACKAGE-013 | §5.2の削除 | `issue validate`がvalid=true、`skills:check`が合格 | pass | §7 |
| AC-1137-02 | 該当なし | §7の置換 | 成功CI runの参照を要求する本文になった | pass | §1 |
| AC-1137-03 | 該当なし | Step 10読取の条件化 | DC判定と差分へ条件化された | pass | §1 |
| AC-1137-04 | SCN-UNIT-PACKAGE-013 | 全変更 | 9種の高速ゲートと`npm test`・`conformance:check`が合格 | pass | §7 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | **applicable** | `development.json`の`capabilities.privacySecurity`が`applicable`であり、REQ-SQ-009が「本CLIでは前二者を適用し」と定める。**本変更はStep 10のtemplate読取をDC判定へ条件化するため、判定を誤ると必要な基準を読まない経路ができる** | 変更file 4件が`PROTECTED_FILES`をどれも含まないこと、`issue validate`・`skills:check`・`audit:check`の受理集合が不変であること、条件化後もDC-PRIVACYが`applicable`である本projectでは脅威・対策・監査templateの全文読取が従来どおり要求されること |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | `04`の§7は観測の記録面そのものである | 手書き件数表を成功CI runの参照へ移し、自己申告を承認証拠から外した |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 人が触れるUIを持たない文書とCLIである | `projectKind`が`cli` |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UI componentもthemeも持たない | `capabilities.designTokens`が`not-applicable` |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 機械強制されている節を1件も削らず、非強制の節だけを縮めた |
| 価値 | 利用者・運用上の目的を満たすか | pass | 全利用者の1 Issueあたりの記述量と読取量が減る |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 変更file 4件がいずれも保護対象外である |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 02の3コンポーネントがそのまま3 fileの編集になった |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 委譲先を明示し、同じ内容を2箇所に置かない |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | 必須12見出しと5 markerの残存を実測した |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 文書の編集であり部分適用が残らない |
| 境界値 | 削除後の必須見出し数 | pass | ちょうど12で`issue validate`が合格する |
| 悪用 | 条件化を口実に必要な基準を読まない | pass | 条件をDC判定と差分の実態へ結んだ。**`not-applicable`判定自体の妥当性は§2.2でreviewされる。** 判定を偽ることは条件化の前後で同じく可能であり、本変更が新たな抜け道を作っていない |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | 検査codeへ触れていない。受理集合が変わらない |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | **既存の記入済み成果物は削除した節を持っていても通る。** 過去の成果物を無効にしない |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | repository状態はrevertで戻る。配布物であるため、merge後に自動releaseが成立するとtagとGitHub Releaseはrevertで取り消せない（§8） |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | 配布3文書と変更履歴。**縮小量が小さいことは残存リスクとして記録した** |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| M-01 | Medium | **削減量が小さい。** 支援層の主因は00〜03の分割そのものであり、本変更は節3件の縮小に留まる | 本artifactの成果物行数欄。staging 00〜03が29113文字 | 費用 | **本Issueでは解かない。** 集約profileは`issue.ts`・`agile-verification.ts`・`staging.ts`・`workflow.ts`の4箇所が分割を前提とし、判定基準にowner決裁が要る | valid（out-of-scope。record-only） | **残存する。** 別Issueで扱う |
| M-02 | Medium | 助言6件のうち3件が誤りだった | `skills:check`の失敗 | 手順 | **本PRで対処済み。** 削除前に強制点を実測する手順を採り、1件は実際に撤回した。**この手順自体は機械強制されていない** | valid（記録のみ） | 残存する |
| L-01 | Low | `04`の§7の置換は書式の指示であり、機械強制されない | 実測 | 遵守 | **本PRでは機構を足さない。** 承認証拠が成功CI runであることは`02_品質基準.md`が既に定めており、二重化しない | valid（記録のみ） | 残存する |
| M-03 | Medium | **条件化の安全性はDC判定が正しいことに依存する。** 判定を誤ると必要な基準を読まない | R2-H01。本artifact自身が誤判定した | Step 10の読取範囲 | **本PRでは機構を足さない。** 判定の妥当性は§2.2でreviewされるが強制は機械ではない。機構化するなら`development.json`の`capabilities`とartifactのDC欄を突合する検査が要り、本Issueの範囲を超える | valid（out-of-scope。record-only） | **残存する。** 別Issueで扱う |

**Critical/High 0件。**

## 6. ラウンド固有の確認

### ラウンド1（Step 10、2026-09-03、candidate HEAD = `H_impl`）

- 全評価基準を確認した: はい。肯定5観点・敵対8観点をすべて評価した
- 指摘を確定した: はい。M-01、M-02、M-03、L-01はいずれも記録のみ
- 次ラウンド対象のCritical/High: **なし**
- blocking: 0件

### ラウンド2（Step 10、2026-09-03、本ラウンド、外部review反映）

**CodeRabbitがMajor 4件・Minor 1件を投稿し、すべて有効だった。**

| ID | 重大度 | 内容 | 対応 |
|---|---|---|---|
| R2-H01 | Major | **本artifactのDC-PRIVACYを`not-applicable`と判定していた。** `development.json`の`capabilities.privacySecurity`は`applicable`であり、REQ-SQ-009も「本CLIでは前二者を適用し」と定める | `applicable`へ訂正し、理由と証拠を具体化した。**本変更がStep 10のtemplate読取をDC判定へ条件化する以上、判定の誤りが必要な基準を読まない経路を作る** |
| R2-H02 | Major | `04_レビュー.md`の§7が矛盾していた。「選択された各層を1層1行で記録する」と「失敗またはskipがある層だけを展開する」を同時に要求していた | 縮小形式へ統一し、4項目の記録要求として書き直した |
| R2-H03 | Major | 本artifactの§7が成功CI runの参照を「PR作成後に観測する」としたまま§11で`approved`としていた。**本PRが導入した「承認証拠は成功CI run」という規則を、本artifact自身が満たしていなかった** | run ID・event・conclusion・対象HEADを記録した。**HEADが動くためmerge前に再取得する条件も明記した** |
| R2-H04 | Major | 変更履歴がREQ-SQ-008だけを記録していた。DC判定の妥当性はREQ-SQ-009が所有する | REQ-SQ-009とAC-SQ-009を追記した |
| R2-L01 | Minor | 「templateを全文読む」の表現が不自然 | 「templateの全文を読む」へ統一した |

**R2-H01は本PRが作った危険を本PR自身が踏んだ実例である。** 敵対的評価の「悪用」欄で「条件化を口実に必要な基準を読まない」をriskとして挙げながら、同じartifactの§2.2で誤った`not-applicable`を書いていた。**条件化の安全性は、DC判定が正しいことに依存する。** 判定自体は§2.2でreviewされるが、その強制は機械ではなくreviewである。§5のM-03へ残存リスクとして記録した。

- 未解決Critical/High: なし
- 修正差分: 配布文書2件、変更履歴1件、本artifact
- 修正で触れた隣接範囲: なし
- blocking: 0件

### 手順上の逸脱の記録

**是正対象がH_impl側の3 fileを含むため、amendでH_implへ畳んだ。** 前進commitにすると`H_impl..current`がreview artifact以外を含み`audit:check`が落ちる。その結果、Step 10 journalのreview session bindingと`pr create`が固定したdelivery stateは旧HEADを指したまま陳腐化している。

`review reanchor`は内容等価性を要求するが、本ラウンドは内容を変える是正であり等価にならない。**この経路の欠落は#1074と#1138が扱う既知の構造欠陥であり、本PRのscopeでは是正しない。**

### ラウンド3

- 未実施。同一範囲の予算3のうち1を残している

## 7. テスト結果

**承認証拠は成功CI runである。** 手書きの件数表はそれ自体が自己申告であり、承認証拠にならない。**本artifactは今回導入した書式を自ら適用する。**

実行したcommand: `docs:format`、`test:format`、`lint`、`format:check`、`typecheck`、`source:check`、`trace:check`、`architecture:check`、`skills:check`、`build`、`package:check`、`npm test`、`conformance:check`、`audit:check`、`issue validate`

全layerの合計: **1413 scenario、1397 成功、0 失敗、16 skip。** `conformance:check`はproject rule 20件・orphan 0件で合格。

失敗またはskipがある層: **skipした16 scenarioはsemantic graphのGraphQLite assetが未設定な環境に起因する。** 本差分はこの経路に触れない。

対応する成功CI run: **run ID `33670432370`**（`event=pull_request`、`conclusion=success`、対象HEAD `238520d8dd003964e699732adb3f8fa6a79f3e16`）。https://github.com/techbeansjp-free/AGENTS.md/actions/runs/33670432370

**本ラウンドの是正でHEADが動くため、新しいHEADに対する成功CI runをmerge前に再取得する。** 上のrun IDはラウンド1のHEADに対するものであり、承認証拠は現`H_final`へ束縛し直す。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `.agent-skill-chain/templates/issue/00_要求定義_full.md` | **入る** | §5.2が消え、委譲先が明示される |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | **入る** | §7が成功CI runの参照を要求する |
| `.agent-skill-chain/skills/step-10-review/SKILL.md` | **入る** | template全文読取が条件化される |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | なし |

判断: 配布物を更新した

根拠: `package.json`の`files`は`.agent-skill-chain/templates/`と`skills/`を含む。**配布digestが変わるため、mergeで自動releaseの計画が成立する見込みである。**

**releaseの起動と計画は分けて記録する。** `.github/workflows/release.yml`は`main`への`push`で起動する。その後`planAutoRelease`が配布digestの差分を見て計画を決める。**本変更は配布digestを変えるため、version bumpとtagが作られる見込みである。** merge後に実観測する。**npm公開は自動経路では発火しない。**

**ロールバックの対象範囲を分けて記録する。**

| 対象 | revertで戻るか | 実行者 | 手順 | 完了確認 |
|---|---|---|---|---|
| repository状態 | **戻る** | 実行役 | 打ち消しPRを通常mergeする | `git diff`が元の状態と一致する |
| 公開済みtagとGitHub Release | **戻らない** | repository owner | 削除せず次versionで前進する | `gh release list`に新tagが載る |
| npm registryの公開物 | **公開されない** | 該当しない | 本経路では公開しない | `npm view`に該当versionが無い |

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | **CodeRabbitのreviewを1件観測した。** 投稿headはラウンド1の`H_final` `238520d8`。Major 4件・Minor 1件はすべて有効で是正した |
| reviewerがPR author・実装commit authorと異なる | **異ならない。** implementerもreviewerも本sessionの同一AI agentであり、Git commit authorも同一である |
| 観測したreview commentとapprovalの件数 | 本session内のreview 2ラウンド（Critical/High 0、Medium 3・Low 1）。**GitHub reviewはCodeRabbitが1件でMajor 4・Minor 1。すべて有効で是正済み** |

**`02_品質基準.md`の「独立reviewが成立しない場合」に従う。** 同節は「既定は停止ではなく、無記録での通過の禁止である」と定める。

**PR作成後の外部reviewを待つ。** CodeRabbitがfree limitで出力できない場合は、その事実をPRへ記録したうえで判断する。

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/15_要件追跡/01_変更履歴.md`。**REQ-SQ-008の本文は変えていない**
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: pass。新規用語を導入していない
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: pass
- 要件・変更・SCN・テストの追跡: REQ-SQ-008 → AC-SQ-008 → 既存`SCN-UNIT-PACKAGE-013`。**新規SCNを追加しないため追跡表を変更しない**
- UI・トークンの判断: UIを所有しないためdesign token・layout tokenは対象外。ADRを追加しない

## 11. 総合判定と再開地点

- 未解決Critical/High: **なし**
- Medium/Lowの記録: M-01（削減量が小さい）、M-02（助言の誤り3件）、M-03（DC判定への依存）、L-01（書式指示は非強制）。**Major R2-H01からR2-H04とMinor R2-L01はすべて是正済み**
- 判定: approved（Step 10 ラウンド2）
- 新しい権限が必要な事項: mergeは別authority
- 残存リスク:
  1. **削減量が小さい。** 支援層の主因は00〜03の分割であり、集約profileを別Issueで扱う
  2. **縮小前に強制点を実測する手順は機械強制されていない。** 同型の誤りは再発しうる
  3. **条件化の安全性はDC判定が正しいことに依存する。** 本artifact自身が誤判定した（R2-H01）。判定の妥当性はreviewが強制する
  4. **配布digestが変わるため自動releaseの計画が成立する見込みである。** 公開済みtagはrevertで取り消せない
- 次に許可される操作: push、PR作成、外部reviewの到着確認、必須check 2件の全緑確認、およびownerが承認したauthorityによる**通常merge commit方式**でのmerge。**squash mergeを使わない**
- 次回の再開地点: PR作成から
