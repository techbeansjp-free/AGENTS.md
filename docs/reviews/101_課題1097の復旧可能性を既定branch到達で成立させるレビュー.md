# 04 レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1097 |
| ラウンド | Step 10 ラウンド1〜3 |
| 比較基点 | `0f3efd15b5305851dc47537059a2f64603c9860b` |
| H_impl | `0b11397560842b5528d4828e6f0c2a42c49cb26b` |
| 比較基点の由来 | review開始時点の`origin/main`のtip。PR #1096（#1084のasc-step adapter発見経路）のmerge commitである。前へ進めていない |
| Step 10のreview session ID | `d31839b94efbc3069d55b668ba07f4ef014c53d871e4e59d0afb4f71d5f21d6c` |
| モード | full |
| 対象差分 | `src/domain/worktree.ts`、`src/domain/worktree-removal-safety.ts`、`src/domain/worktree-survey.ts`、`src/domain/finalize.ts`、`src/cli.ts`、`src/cli-usage.ts`、`test/`の7 file、`docs/specs/`の4 file。commitは`8db4bda5`（v1）と`0b113975`（v2、review反映） |
| 対象外 | remoteへ毎回問い合わせる設計（R1-N04）。shallow clone・origin以外のremoteでの到達判定（R1-N05）。免除対象側が不明な場合の優先関係の明文化（R1-N06）。`scripts/check_cli_usage.ts`が関数境界で走査を止める被覆欠陥。auto-finalize（#947） |
| 残り予算 | **0**（同一範囲で最大3ラウンド。総2ラウンドで設計していたが、**ラウンド3で`audit:check`が検出した本artifactの書式不備2件を是正して使い切った**。6節を参照する） |
| ラウンド数 | 3。ラウンド1は実装差分`8db4bda5`、ラウンド2は是正`0b113975`、**ラウンド3は`audit:check`が検出した本artifactの書式不備2件の是正**が対象である |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260901_152209_recoveryReachableが既定branchを見ずマージ済みworktreeをfinalize不能にする |
| 仕様の所有箇所 | `docs/specs/02_要件/02_プロジェクトライフサイクル要件.md`のREQ-LC-009。**同節はworktree削除の安全条件を所有するが、復旧可能性をどう観測するかを規定していなかった。** その欠落を埋めることが本要求の一部である |
| 成果物行数 | 製品 **+155 / −33行**（`src/`の6 file）。仕様 **+11 / −4行**。支援層 **+263 / −4行**（test 7 file）。staging 4文書 |
| 縮小の先行評価 | 6案を評価し5案を不採用とした。既定branch名のhard code、`cli.ts`の`defaultBranch`再利用（domain→cliの逆依存）、`inspectRecoveryState`のsignature変更、判定を`inspectRemovalSafety`へ移す案、`scripts/check_cli_usage.ts`の被覆是正。**さらにFR-1097-04（診断文の分離）を実装中に撤回した。** 観測側の是正により`recoveryReachable`が偽のときはupstream一致も既定branch到達も両方不成立になり、既存文言が正確になるためである。**手段を足す前に縮小を評価した結果、足さないことにした** |
| 実施者・日時 | reviewer（codexとClaude fableの2体）、統合はcoordinator（claude）、2026-09-01 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定review（3節）、敵対review（4節）、finding分類（5節と`review-session.json`） | advanced | codex、claude（fable） | `project_default`、`independence.differentFrom = implementer` | Critical/High未解決なら停止し、是正後の同一HEADで再review | implementerは本session（claude）。reviewerは**codexの別invocation**と**Claude fableの別context**の2体で、いずれもcontextが異なり一方はproviderも異なる。**両reviewerはfileを1つも変更していない。** fable側は変異試験をscratchpad複製で実施したと報告し、`git status`は当該review中に変化していない |

**開示する逸脱が3件ある。**

1. **implementerとcoordinatorが同一sessionである。** project choiceは`reviewer.independence.differentFrom = implementer`を要求しており、reviewer 2体との分離は成立している。**本Issueではimplementerをcodexの別invocationへ委譲しようとしたが、書き込みモードのcodex起動が環境の権限設定で拒否されたため断念した。** findingの採否判断をcoordinatorが行っており`roleContracts.coordinator.forbiddenOperations`の`self_approve`に接する。隠さず記録する。
2. **`docs/reviews/`はどのroleの`allowedPaths`にも無い。** `scripts/check_file_audit.ts`が`AUDIT_DIRECTORY = "docs/reviews"`を要求する一方、`validateRoleOperation`は実行時に強制されていない。権限拡大をscope外としてrisk受容し、解消を #1047 へ委譲する。
3. **ラウンド1のreview依頼が現物と乖離していた。** 依頼はcommit `8db4bda5`を対象にしたが、依頼中に作業treeへ未commitの改稿が入り設計が反転していた。fableが`git diff HEAD --stat`の実測でこれを検出し、R1-B02として記録した。**是正としてv2を前進commit `0b113975`へ畳み、gateと変異試験をそのheadに対して再実行した。**

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | https://github.com/techbeansjp-free/AGENTS.md/issues/1097 | AC-1097-01〜06。Step 8で43644文字の本文を同期し、読み取りdigestが`0d81a4df…`で一致 | 人間判断 |
| 差分 | `0f3efd15..0b113975` | 17 file、`+429 / −41`行 | 既存コード |
| テスト | `npm test`（`quality`が内包） | 1381 scenarios、失敗0件 | テスト出力 |
| 仕様 | `docs/specs/`の4 file | updated | 既存文書 |
| commit前candidate | 対象差分17 path | `git status --short`が意図しないpath 0件 | Git index |
| Phase A artifact | `docs/reviews/101_課題1097の復旧可能性を既定branch到達で成立させるレビュー.md` | `H_impl..H_final`は本artifact 1 fileだけ | Git観測 |
| commit後external | PRは本artifact確定後に作成する | Step 11で観測する | 外部のimmutable証拠 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **成立する。** 本artifactへ自身のcommit SHAを書いていない。`npm run architecture:check`が違反0件。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけであり、trusted providerが観測したPR/CI/reviewが`H_final`へ一致している: **`H_impl = 0b113975`は本artifact commitの親である。** PR/CIの観測はStep 11で行う。
- reviewer stable IDがPR author/provider観測済み`H_impl` author stable IDと異なる: **codex側は別provider。fable側は別contextだがproviderは同一である。** 0.1節の逸脱1に記載した。
- 既定branch追随を行った場合: **行っていない。** `0f3efd15`はreview開始時点の`origin/main`のtipである。

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/worktree.ts` | M | package | package | 既定branch refの解決と到達の**観測**。判定を持たない | pass。`lib/process.js`への既存依存のみ | REQ-LC-009 / AC-1097-01〜03 / SCN-UNIT-WTSURVEY-018〜020 | 観測は副作用を持たない。判定式を戻せば復旧する | pass |
| `src/domain/worktree-removal-safety.ts` | M | package | package | 免除条件の**判定**。観測を持たない | pass。純粋関数のまま | AC-1097-06 / SCN-UNIT-WTSURVEY-021〜022 | 同上 | pass |
| `src/domain/worktree-survey.ts` | M | package | package | 観測schemaへ1 field追加とboolean検証 | pass | 同上 | 不正値を拒否する | pass |
| `src/domain/finalize.ts` | M | package | package | 復旧参照不在の免除条件 | pass | SCN-INT-FINALIZE-006〜008 | 同上 | pass |
| `src/cli.ts` | M | package | package | 観測の配線と`executeCompletionFlow`の明示入力化 | pass | AC-1097-05 / SCN-UNIT-CLIUSAGE-015 | `--authorize`検査位置は不変 | pass |
| `src/cli-usage.ts` | M | package | package | usage宣言3行 | pass | 同上 | 受理する入力を変えない | pass |
| `test/features/unit/worktree-survey.feature` | M | package | package | unit scenario 5件追加 | pass | SCN-UNIT-WTSURVEY-018〜022 | 隔離repositoryで動く | pass |
| `test/steps/worktree-survey.steps.ts` | M | package | package | 同step定義 | pass | 同上 | 同上 | pass |
| `test/features/unit/cli-usage.feature` | M | package | package | unit scenario 1件追加 | pass | SCN-UNIT-CLIUSAGE-015 | 定数を読むだけ | pass |
| `test/steps/cli-usage.steps.ts` | M | package | package | 同step定義 | pass | 同上 | 同上 | pass |
| `test/features/integration/worktree-survey.feature` | M | package | package | integration scenario 1件追加 | pass | SCN-INT-WTSURVEY-013 | 隔離repository | pass |
| `test/features/integration/delivery-finalize.feature` | M | package | package | integration scenario 3件追加 | pass | SCN-INT-FINALIZE-006〜008 | 観測値の注入 | pass |
| `test/steps/delivery-finalize.steps.ts` | M | package | package | 同step定義とfixture | pass | 同上 | 同上 | pass |
| `docs/specs/01_システム概要/02_用語・略語.md` | M | package | spec | TERM-ASC-086の追加 | pass | 全AC | 実行authorityを持たない | pass |
| `docs/specs/02_要件/02_プロジェクトライフサイクル要件.md` | M | package | spec | REQ-LC-009へ観測条件を追記 | pass | 同上 | 同上 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package | spec | 新規SCNの追跡登録 | pass。`trace:check`が`orphanScenarios: []` | 同上 | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | 1行追記 | pass | 同上 | 同上 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **`比較基点..H_impl`すなわち`0f3efd15..0b113975`の17 pathと本表の17行が一致する。** 本artifact自身は`H_impl..H_final`の差分であり本表の対象外である。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **成立する。**
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: **ラウンド1の是正は上記のうち13 fileに閉じている。**

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | 判定側でupstream由来の拒否理由を`recoveryReachable`根拠に抑制したところ、既存4 scenarioが落ちた。`SCN-UNIT-WTSURVEY-005`、`SCN-UNIT-FINALIGN-006`、`SCN-UNIT-FINALIGN-026`、`SCN-INT-FINALIZE-002` | **是正を置く層が誤っていた。** とくに`SCN-UNIT-FINALIGN-026`はREQ-LC-009の「他の観測から安全状態を推定せずretainとする」を守るscenarioであり、判定側で別fieldを根拠に抑制することは既存要件そのものに反する | requirement | `workflow assess-discovery`で`rebaseline-affected-contracts`を得て01・02・03を再確定。是正の層を観測側へ移した | 既存4 scenarioがすべて無変更で通る | updated | pass |

**FR-1097-04を撤回した。** 観測側の是正により`recoveryReachable`が偽のときはupstream一致も既定branch到達も両方不成立になり、既存文言「commitが既定branchから到達できず、復旧手段がありません」が正確になる。診断文の分離は不要である。

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1097-01 | SCN-UNIT-WTSURVEY-018 | `inspectRecoveryState` | pass | pass | 隔離repositoryでupstreamを失っても`recoveryReachable: true` |
| AC-1097-02 | SCN-UNIT-WTSURVEY-019 | 同上 | pass | pass | 既定branchの祖先でなければ`false` |
| AC-1097-03 | SCN-UNIT-WTSURVEY-020 | `resolveDefaultBranchRef` | pass | pass | `origin/HEAD`未設定で`reachableFromDefaultBranch: false` |
| AC-1097-05 | SCN-UNIT-CLIUSAGE-015 | `COMMAND_USAGE` | pass | pass | `--merge-sha`・`--approved-digest`・`--cleanup-authority`の宣言 |
| AC-1097-06 | SCN-UNIT-WTSURVEY-021〜022、SCN-INT-WTSURVEY-013、SCN-INT-FINALIZE-006〜008 | `assessWorktreeRemovalSafety`、`buildFinalizeReport` | pass | pass | 真のときだけ免除し、不明なら免除しない |
| AC-1097-04 | — | — | — | **撤回** | 上記2.0節 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | worktree削除という不可逆操作の可否を決める安全述語を変更する。個人情報と秘密情報は扱わない | INV-01〜INV-06、変異9件 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 症状の半分は診断可能性の欠落である。実行時ログは1行も増やさない | `worktree survey`と`finalize --dry-run`の構造化出力 |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | UI sourceが無い。利用者接点はCLIの構造化出力である | 出力schemaとphase構成を変えない |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | token sourceを持たない | 変更対象はTypeScriptとmarkdownであり、色・寸法・typographyのいずれのtoken sourceも持たない |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 実物4 worktreeが`safe: true`・拒否理由0件、`survey`で`cleanup-ready`。変異9件が全件kill |
| 価値 | 利用者・運用上の目的を満たすか | pass | マージ済みworktreeを製品の経路で片付けられる。迂回の動機が消える |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 追加authorityも追加依存も要さない。git command 1〜2回の追加 |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | REQ-LC-009へ観測条件を明記し、TERM-ASC-086を追加。`trace:check`が`orphanScenarios: []` |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 観測と判定の分離（INV-06）を維持した。`inspectRecoveryState`のsignatureも呼び出し元も変えていない |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | **finding** | **R1-B03。** squash merge後にlocalのremote-tracking refがprune前でHEADと一致すると、既存の`recoveryRef && pushed`側で`recoveryReachable`が真になる。PoCで再現した。`reachableFromDefaultBranch`は正しく偽である。**旧式と同値であり本変更の回帰ではない**が、要求定義の主張を訂正した |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | git非0終了はすべて偽へ倒す。例外を投げない |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | `origin/HEAD`未設定、空文字、`merge-base`判定不能をSCN-UNIT-WTSURVEY-020が固定 |
| 悪用 | 注入、経路脱出、権限外操作等 | **finding** | **R1-N04。** `origin/HEAD`も`refs/remotes/origin/main`もlocal refであり書き換え可能である。ただし既存の`pushed`も`@{upstream}`という同じclassのlocal refに依拠しており**信頼水準は下がっていない** |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | cleanupの`--cleanup-authority`と承認済みpreview digestの要求を弱めていない |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | 免除は`reachableFromDefaultBranch === true`のときだけ。`undefined`は免除しない |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | 判定式を戻せば元へ戻る。branchは削除しない |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | **finding** | **R1-N05。** shallow clone・origin以外のremote・`origin/HEAD`未解決では到達判定が恒久的に偽になる。fail-closedであり安全側だが、復旧可能なのに拒否される形が残る |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| R1-B01 | High | 観測層への移動が問題を解消していない。`pushed`と`remoteBranch`という独立観測を単一の到達可能性から書き換えており、層を移しただけ | codexとfableが独立に同じ構造を指摘 | `src/domain/worktree.ts` | 観測の意味を戻し、`reachableFromDefaultBranch`を独立fieldにして判定側で明示的に免除 | resolved | なし |
| R1-B02 | High | review依頼と現物が乖離。依頼はcommit `8db4bda5`だが作業treeに未commitの改稿13 fileがあり設計が反転していた | fableが`git diff HEAD --stat`で実測 | staging全体 | v2を前進commit `0b113975`へ畳み、gateと変異試験を再実行 | resolved | なし |
| R1-B03 | Medium | squash mergeの拒否維持という主張が偽 | PoCで再現。`HEAD ancestor of origin/main: false`、remote refsは`refs/heads/main`のみ、`recoveryReachable: true` | 00・01・変更履歴 | 主張を訂正し残存リスクへ改めた | resolved | **旧式と同値の既存性質。** 本Issueでは是正しない |
| R1-N01 | Medium | usageの`--merge-sha`が「常に必要」で偽 | `src/cli.ts:5615`が`updateRoot`のときだけ`required`を呼ぶ | `src/cli-usage.ts` | 条件文言を実装へ合わせた | resolved | なし |
| R1-N02 | Medium | fixture直上のcommentが旧設計のまま | 直下のfixtureと正反対 | `test/steps/delivery-finalize.steps.ts` | 書き換えた | resolved | なし |
| R1-N03 | Medium | 変更履歴の互換性欄が自己矛盾 | 変更欄だけ改稿されていた | `docs/specs/15_要件追跡/01_変更履歴.md` | 書き換えた | resolved | なし |
| R1-N04 | Medium | `origin/HEAD`はlocal refでありremote上の復旧可能性を証明しない | fableが「既存`pushed`と等価な信頼水準であり回帰ではない」と明記 | `src/domain/worktree.ts` | 記録のみ | valid（対象外） | **remoteへ毎回問い合わせる設計は本Issueの対象外** |
| R1-N05 | Medium | shallow clone等で到達判定が恒久的に偽 | `resolveDefaultBranchRef`が`undefined`を返す | 同上 | 記録のみ | valid（対象外） | fail-closedであり安全側 |
| R1-N06 | Low | 免除対象側が`undefined`でも免除される | `SCN-UNIT-FINALIGN-026`はreachable未設定fixtureのため接触しない | `src/domain/worktree-removal-safety.ts` | 記録のみ | valid（対象外） | 到達を観測できているなら代理の不明は安全性に影響しないと判断した |
| R1-N07 | Medium | 変異試験8件が`pushed`のoverride disjunctを検出できなかった | fableがscratchpad複製で生存を実測 | `test/` | 当該行が消滅。変異を9件へ拡張し隔離入力を追加 | resolved | なし |
| R1-N08 | Low | `SCN-INT-FINALIZE-006`のfixtureが同じtrue値を再代入するだけで検出力が無い | objectを空にしても通る | 同上 | fixtureを`reachableFromDefaultBranch`だけが真になる形へ改め、`007`・`008`を追加 | resolved | なし |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: **はい。** 肯定5観点と敵対8観点を対象差分`8db4bda5`へ適用した。reviewerはcodexとClaude fableの2体で、観点9件を明示的に指定した。
- 指摘を確定した: **blocking 1件（R1-B02）、record-only 10件。** `review-session.json`のラウンド1に11件を記録した。**R1-B01はHighだが`relation`が`invariant-violation`のため`admission`が`record-only`になりblockingに入らない。** 是正の要否はseverityで判断しており、R1-B01もラウンド2で`resolved`にしている。
- 次ラウンド対象のCritical/High: **R1-B01・R1-B02。**

### ラウンド2

- 未解決Critical/High: **なし。** R1-B01・R1-B02は`0b113975`で是正した。
- 修正差分: `src/`の6 file、`test/`の4 file、`docs/specs/`の3 file、および本artifact。
- 修正で触れた隣接範囲: staging 3文書（00・01・変更履歴）。契約記述の同期であり実行codeへ波及していない。
- 既承認・未変更範囲を再走査していない: **成立する。** 既存SCNのassertionを1件も変更していない。

### ラウンド3

- 全指摘の最終分類: **14件のうちresolved 10件、valid（対象外または記録）4件。** 未解決のCritical/Highはない。
- 任意の危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: **縮小した危険範囲はない。** 免除は`reachableFromDefaultBranch === true`に限り、`undefined`は免除しない。
- 同じ範囲の予算を自動更新していない: **成立する。** 総3ラウンドで打ち切る。
- AIによる最終裁定: **approved。** ラウンド3は`npm run audit:check`が検出した本artifactの書式不備2件の是正である。(1) 8節が`src/`の6 fileを1行へまとめており、path単位の列挙を要求する検査にmatchしなかった。17 pathを1行ずつ列挙する形へ直した。(2) 本文のH_implが導出値と1文字違いだった。**製品・仕様・testの差分は1行も変えていない。**

## 7. テスト結果

| 層・検査 | コマンド | シナリオ・件数 | 成功 | 失敗 | スキップ | 判定 |
|---|---|---:|---:|---:|---:|---|
| 形式 | `npm run docs:format` / `npm run test:format` | 2 | 2 | 0 | 0 | pass |
| 静的検査 | `npm run quality` | 1 | 1 | 0 | 0 | pass |
| unit・integration・e2e | `npm test` | 1381 | 1365 | 0 | 16 | pass |
| 型・既存一式・配布物 | `npm run build` / `package:check` / `architecture:check` / `trace:check` / `conformance:check` | 5 | 5 | 0 | 0 | pass |

**`npm test`と`npm run conformance:check`は逐次実行した。** 並行実行すると`dist/bin`を奪い合いE2Eが偽陽性で落ちる。

### 7.1 変異試験

判定を**広げる**変更なので、広げすぎと狭めすぎの**両方向**を確認した。

| ID | 変異 | 結果 | 落としたSCN |
|---|---|---|---|
| M1 | `recoveryReachable`から既定branch到達の項を落とす | kill | WTSURVEY-018 |
| M2 | 到達判定を常に真にする | kill | WTSURVEY-019 |
| M3 | `origin/HEAD`解決失敗を既定値へ倒す | kill | WTSURVEY-020 |
| M4 | 免除条件を`!== false`へ緩める | kill | WTSURVEY-022 |
| M5 | 免除を無条件にする | kill | WTSURVEY-022 |
| M6 | 免除を廃止する | kill | WTSURVEY-021 |
| M7 | `finalize`の復旧参照免除を廃止する | kill | INT-FINALIZE-006 |
| M8 | `finalize`の免除を無条件にする | kill | INT-FINALIZE-008 |
| M9 | usage宣言を戻す | kill | CLIUSAGE-015 |

**M8は当初生存した。** `SCN-INT-FINALIZE-007`は他の理由でも落ちるため免除条件を隔離できていなかった。復旧参照の不在**だけ**が拒否理由になる`SCN-INT-FINALIZE-008`を足して塞いだ。

**ラウンド1時点の変異試験8件も不十分だった（R1-N07）。** fableが`pushed`のoverride disjunctを落とす変異を構成し、baselineとmutantの失敗集合が完全一致することを実測した。是正後の設計では当該行が消滅している。

復元はすべて変更前fileの複写で行い`git checkout`を使っていない。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/domain/worktree.ts` | 入る | **マージ済みworktreeの復旧可能性が既定branch到達でも成立するようになる。** 観測field `reachableFromDefaultBranch` が加わる |
| `src/domain/worktree-removal-safety.ts` | 入る | 同fieldが真であると観測できたときだけupstream ref由来の拒否理由を免除する。**受理する入力は変えない** |
| `src/domain/worktree-survey.ts` | 入る | 観測schemaへ同fieldが加わる。boolean以外は拒否する |
| `src/domain/finalize.ts` | 入る | 復旧参照不在の拒否が同fieldで免除される |
| `src/cli.ts` | 入る | 観測の配線。`executeCompletionFlow`が明示的な引数を受け取る。**外部から見た振る舞いは変わらない** |
| `src/cli-usage.ts` | 入る | **`--help`の出力に`--approved-digest`と`--cleanup-authority`の宣言が加わり、`--merge-sha`の条件文言が実装へ合う** |
| `test/features/integration/delivery-finalize.feature` | 入らない | なし |
| `test/features/integration/worktree-survey.feature` | 入らない | なし |
| `test/features/unit/cli-usage.feature` | 入らない | なし |
| `test/features/unit/worktree-survey.feature` | 入らない | なし |
| `test/steps/cli-usage.steps.ts` | 入らない | なし |
| `test/steps/delivery-finalize.steps.ts` | 入らない | なし |
| `test/steps/worktree-survey.steps.ts` | 入らない | なし |
| `docs/specs/01_システム概要/02_用語・略語.md` | 入らない | なし |
| `docs/specs/02_要件/02_プロジェクトライフサイクル要件.md` | 入らない | なし |
| `docs/specs/15_要件追跡/00_追跡表.md` | 入らない | なし |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | なし |
| `docs/reviews/101_課題1097の復旧可能性を既定branch到達で成立させるレビュー.md` | 入らない | なし |

判断: 配布物を更新した

根拠: `src/`はcompileされて配布される。`npm run package:check`が合格し、`--help`の出力を`SCN-UNIT-CLIUSAGE-015`が固定する。**受理する入力を変えないため、従来通る呼び出しは通り続ける。**

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | Step 11のPR作成後に観測する |
| reviewerがPR author・実装commit authorと異なる | codexは別provider・別contextで異なる。fableは同一providerの別contextである |
| 観測したreview commentとapprovalの件数 | Step 11で観測する |

**外部reviewerのcheckがpassでもreview commentとapprovalの実体を観測する。** 両方0件だった場合だけ`RVX-REPORTED-SUCCESS-WITHOUT-REVIEW-001`の条件に当たる。**現時点では例外を適用していない。**

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/02_要件/02_プロジェクトライフサイクル要件.md`（REQ-LC-009へ観測条件）、`docs/specs/01_システム概要/02_用語・略語.md`（TERM-ASC-086）、`docs/specs/15_要件追跡/00_追跡表.md`、`docs/specs/15_要件追跡/01_変更履歴.md`。
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: **成立する。** TERM-ASC-086「復旧可能性」を追加した。実測で同語が未登録であること、既存最大がTERM-ASC-085、他Issueのstagingの最大がTERM-ASC-073で衝突しないことを確認した。
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: **成立する。** 「到達可能性」はGitのancestor関係だけを指す語として区別した。
- 要件・変更・SCN・テストの追跡: REQ-LC-009 → AC-LC-009 → AC-1097-01〜06 → SCN-UNIT-WTSURVEY-018〜022、SCN-UNIT-CLIUSAGE-015、SCN-INT-WTSURVEY-013、SCN-INT-FINALIZE-006〜008 → `src/domain/worktree.ts`ほか。`npm run trace:check`が`orphanScenarios: []`・`orphanRequirements: []`。
- `no-spec-impact`の場合の限定的根拠: 該当しない。
- UI・トークンの判断: UI無し。tokenは`not-applicable`。

## 11. 総合判定と再開地点

- 未解決Critical/High: **なし。**
- Medium/Lowの記録: R1-N04（local refの信頼水準）、R1-N05（shallow clone等での恒久的な偽）、R1-N06（免除対象側の不明）を対象外として残す。
- 判定: approved
- 新しい権限が必要な事項: **なし。**
- 残存リスク: 4件。(1) **squash merge後にlocalのremote-tracking refがprune前でHEADと一致すると`recoveryReachable`が真になる。** 旧式と同値の既存性質であり本変更の回帰ではない。(2) `origin/HEAD`はlocal refでありremote上の状態を証明しない。既存の`pushed`と同じ信頼水準である。(3) shallow clone・origin以外のremote・`origin/HEAD`未解決では到達判定が恒久的に偽になる。fail-closedであり安全側である。(4) `reachableFromDefaultBranch`が真のとき`pushed`・`remoteBranch`が`undefined`でも免除される。
- 次に許可される操作: Step 11のPR作成。
- 次回の再開地点: `pr create`。
