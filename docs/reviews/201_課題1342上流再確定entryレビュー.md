# 04 レビュー

> すべてのラウンドで肯定・敵対の両観点を確認する。`成果物用語と責務境界`は`.agent-skill-chain/docs/01_開発ワークフロー.md`を正本とし、要求・要件・設計・計画・システム仕様書の責務越境と追跡切れをfindingにする。指摘を無理に作らず、指摘なしの承認を有効とする。Medium/Lowだけを理由に自動修正・追加レビュー・ゲート停止を起こさない。

**本templateを埋めた成果物は版管理下へ置く。** 一時ステージングに置いたままでは`review evidence`も履歴監査も成立しない。`docs/reviews/`または`.agent-skill-chain/reviews/`配下へ複写し、実装commitの後にその1 fileだけをcommitする。

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 3 |
| 対象SHA・文書ダイジェスト | 6e0786447a04ed918d8150fedb049dac93cb20f8 |
| 比較基点 | `83a4a895350a42a4c7b93e3220eb8060187572c0` |
| H_impl | `6e0786447a04ed918d8150fedb049dac93cb20f8` |
| 対象差分 | .agent-skill-chain/docs/01_開発ワークフロー.md、.agent-skill-chain/schemas/workflow-step-journal.schema.json、dist/src/cli-usage.js、dist/src/cli.js、dist/src/domain/workflow.js、docs/specs/01_システム概要/02_用語・略語.md、docs/specs/02_要件/00_要件一覧.md、docs/specs/02_要件/01_ワークフロー要件.md、docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md、docs/specs/07_データ/01_管理データ.md、docs/specs/15_要件追跡/00_追跡表.md、docs/specs/15_要件追跡/01_変更履歴.md、src/cli-usage.ts、src/cli.ts、src/domain/workflow.ts、test/features/unit/workflow-reconfirmation.feature、test/steps/workflow-reconfirmation.steps.ts |
| 対象外 | 比較基点に存在し変更されていない範囲。既存`TERM-ASC-001`〜`107`の採番、review round予算の値 |
| 残り予算 | 0ラウンド（同一scope最大3を使い切り。収束後のHEAD移動に対する取り直し1ラウンドは別枠で未使用） |
| ラウンド数 | 3。round 1は初回全scope review、round 2はPR #1362のmergeで既定branchが動いたことへの取り直し、round 3は外部reviewer（CodeRabbit）指摘の是正 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260912_111845_上流再確定をjournalへ順序違反にせず記録できるようにする |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-004「**journal追記経路もStep順序判定と同じ条件で通す。**Step 11記録後の追記とterminal delivery state後のStep 0〜10は、post-terminal intakeのStep 10だけを受理し、それ以外は拒否する」 |
| 成果物行数 | 製品（`src/`・`test/`・配布schema）544行追加・2行削除。支援層（`docs/`・`.agent-skill-chain/docs/`）9行追加・6行削除 |
| 縮小の先行評価 | 既存の`postTerminalIntake`（Step 10専用、順序判定から外す）と同型の機構を任意field 1件で実現し、新しい判定器も新規subcommandも作っていない。順序判定そのものの緩和（`validateStepJournal`の単調性条件を外す）は、flagなしの後付けを一律に受理する抜け道になるため採らなかった。追記先は既存の`workflow record`とCLI flag 1つに収めた |
| 実施者・日時 | implementer: coordinator（`tatsuru`）。reviewer: 別contextの独立reviewer（codex、read-only sandbox）。2026-09-12T12:05:00+09:00 |

`比較基点`と`H_impl`の2行は`review reanchor`と`audit:check`が機械的に読む。値は40桁の小文字hexをbacktickで囲んだものだけにし、注記・branch名・短縮SHAを同じcellへ書かない（識別行が一意に解決できず`identity-unresolvable`で拒否される）。由来の説明は`比較基点の由来`のような別行へ書く。

### 0.1 routing入力契約

providerとmodel設定はproject choiceとrouting evidenceの観測値を用い、固有のmodel slugだけからreview authorityを推測しない。

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対の両表とfinding分類を記録。先行PR #1354でREV-01〜06、本branchで指摘なし | risk=path、mode=full、scope=ISSUE-1342から`advanced`以上 | codex（`PROVIDER_AUTONOMOUS_CEILINGS.codex`のreasoningEffort上限`high`） | `codex:provider_recommended_default:high:default` = `critical`（trusted policyの観測値） | 未解決Critical/Highが残る場合は停止し、予算を超える指摘はfollow-up Issueへ分離する | implementerはこのsessionのcoordinator、reviewerは`codex exec --sandbox read-only`の別process・別context。reviewerはworktreeへ書き込めず、対象差分のpath集合は`比較基点..H_impl`の17 pathで前後不変 |

## 1. 入力証拠

レビュー証拠は二段階で完成させる。**Phase Aはtracked review artifactであり、PR作成前に確定する。** 製品・仕様・testを固定した`H_impl`の後、tracked review artifactだけを加えた`H_final`を作る。`H_impl`は`H_final`のancestor、commit間差分はproject policyが選ぶevidence-only pathだけとし、artifact path、SHA-256、Git blob OIDを観測する。**Phase Aで観測するのは`H_impl` commit SHA/author ID、repository、review sessionのanchorとroundである。** **PR number、Actions run ID、immutable review IDはPR作成後にしか存在しないので、tracked review artifactへ書かない**。旧版はこれらをPhase Aへ要求しつつ「H_final後はartifactを更新しない」と定めていたため、**PR作成前は埋められず作成後は直せない循環になっていた。**

**Phase Bはtracked artifactの外にある。** PR作成後、trusted providerからPR number/current head/author ID、Actions run ID/event/head/conclusion/関連PR番号、immutable review ID/commit/user ID/submittedAt/stateを実観測し、**`review evidence`とdelivery stateへappend-onlyで記録する。** PR・成功CI・承認reviewは`H_final`へ一致させる。**完了はtrusted providerの外部attestationだけで記録し、head変更時は外部証拠を作り直す。** caller申告actor、任意JSON、別PRのrun、COMMENTED、未完了CIは承認証拠にしない。

**reviewerの独立性はproject policyの`merge.reviewIndependence`が決める**。`context-isolated`（未宣言時の既定）はimplementerと別session/contextであること、exact HEADを固定したこと、reviewerが対象差分を変更していないこと、肯定・敵対レビューとfindingの記録を要求する。**同一GitHub actorでも成立する。** `actor-independent`はPR authorおよびobserved implementation commit authorと別のstable actor IDを要求し、**高リスク変更・不可逆操作・release・外部公開でproject policyが宣言して引き上げる。** **どちらのモードでもAPPROVED verdictとexact HEAD一致は必須である。** tracked文書へ自身のcommit SHAを書かず、H_final後はartifactを更新しない。

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | .agent-skill-chain/tmp/issues/20260912_111845_上流再確定をjournalへ順序違反にせず記録できるようにする | staging digest 3c0364e29d3fa3d46cb6b900a01a6f45bbc52085990400965337d580da423122 | 既存コード |
| 差分 | `83a4a895350a42a4c7b93e3220eb8060187572c0`..`6e0786447a04ed918d8150fedb049dac93cb20f8` | 17 path | 既存コード |
| テスト | `npm test`、`npm run conformance:check`（`H_impl`=`6e078644`で実行） | 1,917 scenarios（1,901 passed / 16 skipped / 0 failed）、conformance 87 scenarios全pass | テスト出力 |
| 仕様 | `docs/specs/`の7 fileと`.agent-skill-chain/docs/01_開発ワークフロー.md` | updated | 既存文書 |
| commit前candidate | .agent-skill-chain/docs/01_開発ワークフロー.md、.agent-skill-chain/schemas/workflow-step-journal.schema.json、dist/src/cli-usage.js、dist/src/cli.js、dist/src/domain/workflow.js、docs/specs/01_システム概要/02_用語・略語.md、docs/specs/02_要件/00_要件一覧.md、docs/specs/02_要件/01_ワークフロー要件.md、docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md、docs/specs/07_データ/01_管理データ.md、docs/specs/15_要件追跡/00_追跡表.md、docs/specs/15_要件追跡/01_変更履歴.md、src/cli-usage.ts、src/cli.ts、src/domain/workflow.ts、test/features/unit/workflow-reconfirmation.feature、test/steps/workflow-reconfirmation.steps.ts | H_impl 6e0786447a04ed918d8150fedb049dac93cb20f8 | Git index |
| Phase A artifact | `docs/reviews/201_課題1342上流再確定entryレビュー.md`（evidence-only path） | `H_impl`=`6e078644`の上へこの1 fileだけを加えて`H_final`にする | Git観測 |
| review session | .agent-skill-chain/tmp/issues/20260912_111845_上流再確定をjournalへ順序違反にせず記録できるようにする | session ID `e631f10adc17f0985b1e330736a15b95ba4c3ea375d8cd2f1fa8bbc396b74b42`、round 1、status `converged`、latest round digest `0b11831a1c7142a92d28686066cd4e13b4cfe147498c9c06299246242f0bb2e0` | Git観測 |

**Phase Bの外部証拠をこの表へ書かない。** PR number、Actions run ID、immutable review IDはPR作成後にしか存在しない。**それらは`review evidence`とdelivery stateがappend-onlyで保持する正本であり、tracked review artifactは参照先を持たない。**

重要判断を推論だけで承認しない。新しい権限が必要な場合は、対象操作と決定権者を明示する。

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **はい。** 本fileは自身のcommit SHAを書かず、`H_impl`と`比較基点`だけを固定する
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: **はい。** `H_final`は本file 1件だけを追加するcommitであり、`H_impl`=`6e078644`をただ1つの親に持つ
- reviewerの独立性が`merge.reviewIndependence`の要求水準を満たす（`context-isolated`なら別session/context、`actor-independent`ならPR author・`H_impl` author stable IDと別のstable ID）: **はい。** project policyに`merge.reviewIndependence`の宣言がないため既定の`context-isolated`を適用する
- **（Phase B。ここでは判定しない）** trusted providerが観測したPR/CI/reviewが`H_final`へ一致することは、PR作成後に`review evidence`が観測して記録する: Phase Bで記録する
- 既定branch追随を行った場合、取り込みがreview artifact commitより前にあり、`比較基点`が取り込んだ既定branch tip、`H_impl`がartifact直前の最新commitを指し、個別監査表を`比較基点..H_impl`から再生成した: **該当しない。** 本branchは既定branch追随のmerge commitを1件も持たない（`git log --merges --oneline origin/main..HEAD`が0件）。`比較基点`は分岐元の既定branch tip`83a4a895`である

### 1.1 変更ファイル個別監査

追加・変更・削除した全ファイルを省略せず1ファイル1行で記録する。まとめ行、directory単位の一括承認、test成功だけによる代替を認めない。

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | M | package owner | package規範文書 | Step記録節へ`workflow record --reconfirm`の1文を追加 | なし（文書） | REQ-WF-004。SCN-UNIT-RECONFIRM-001〜007 | 配布規範文書の追記のみ。revertで旧文へ戻る | pass |
| `.agent-skill-chain/schemas/workflow-step-journal.schema.json` | M | package owner | package配布schema | journal entryの公開契約。`reconfirmation`と`postTerminalIntake`の値域とStep条件を定義 | schemaからruntimeへの参照はない（一方向） | AC-02、AC-06。ajvで9件の受理・拒否を実測 | 任意fieldの追加であり既存journalは不変。revert可能 | pass |
| `docs/specs/01_システム概要/02_用語・略語.md` | M | package owner | spec / 用語台帳 | TERM-ASC-109「上流再確定entry」を1行追加 | 用語台帳 → 要件の参照方向 | TERM-ASC-109。REQ-WF-004から参照 | 追記のみ。mainのTERM-ASC-106・107と重複しないことをreviewが確認 | pass |
| `docs/specs/02_要件/00_要件一覧.md` | M | package owner | spec / 要件索引 | REQ-WF-004行の根拠へIssue #1342を追加 | 索引 → 要件本文 | REQ-WF-004 | 追記のみ | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | package owner | spec / 要件正本 | REQ-WF-004本文へ上流再確定entryの受理条件・位置条件・先行entryの定義を追加。**この節が条件の正本** | 要件 → 実装・SCN | REQ-WF-004、AC-WF-004 | 追記のみ。条件を下流へ複製していない | pass |
| `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | M | package owner | spec / CLI契約 | `workflow record`行へ`--reconfirm`を追加 | CLI契約 → 実装 | REQ-WF-004 | 追記のみ | pass |
| `docs/specs/07_データ/01_管理データ.md` | M | package owner | spec / データ契約 | journal行に任意field `postTerminalIntake`・`reconfirmation`が現れることと値域だけを書く。**受理条件はREQ-WF-004を正本として指し複製しない** | データ仕様 → 要件（参照のみ） | REQ-WF-004 | 追記のみ。先行PRでいったん条件を複製してしまい正本参照へ直した（REV-04） | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package owner | spec / 追跡表 | REQ-WF-004行へSCN-UNIT-RECONFIRM-001〜007を登録 | 追跡表 → feature file | SCN-UNIT-RECONFIRM-001〜007 | 追記のみ | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package owner | spec / 変更履歴 | 本変更の1行を追加 | 履歴 → 要件・SCN | REQ-WF-004、SCN-UNIT-RECONFIRM-001〜007、TERM-ASC-109 | 追記のみ | pass |
| `src/cli-usage.ts` | M | package owner | CLI usage正本 | `workflow record`のoptionalFlagへ`--reconfirm`を1件追加 | usage → cli.ts（`check_cli_usage.ts`が双方向照合） | REQ-WF-009の一致検査。SCN-UNIT-RECONFIRM-004 | flag追加のみ。既定はflagなし | pass |
| `src/cli.ts` | M | package owner | CLI handler | `--reconfirm`を読み、Step 1〜9に限ってentryへ`reconfirmation: true`を載せる | cli → domain の一方向 | AC-03、AC-04。SCN-UNIT-RECONFIRM-003・004 | Step 10・11と先行entryなしを拒否。journalは追記前検証で不変 | pass |
| `src/domain/workflow.ts` | M | package owner | domain正本 | `reconfirmation` fieldの型・parse検証・順序判定除外・先行entry検査（`humanOverride`を含む3種を除外）・Step 11後の位置検査 | domain は adapters/cli へ依存しない | AC-01〜AC-07。SCN-UNIT-RECONFIRM-001〜007 | 検証は追記前に行い、拒否時にjournalを書き換えない | pass |
| `test/features/unit/workflow-reconfirmation.feature` | A | package owner | test / Gherkin | 本Issueの7 scenarioを1 fileへ集約 | feature → steps | SCN-UNIT-RECONFIRM-001〜007 | test追加のみ | pass |
| `docs/reviews/201_課題1342上流再確定entryレビュー.md` | A | package owner | evidence | 本review artifact。**追随mergeより前にcommitしたため`比較基点..H_impl`へ入る。** 内容は`H_final`で本file自身を更新して確定する | evidence → 実装（参照のみ） | 本Issue全体 | 追加のみ。revertでartifactが消えるだけ | pass |
| `test/steps/workflow-reconfirmation.steps.ts` | A | package owner | test / step定義 | 上記featureのstep実装。CLI経路とdomain関数の双方を検査 | steps → src の一方向 | 同上 | test追加のみ | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **はい。** `83a4a895..6e078644`の差分は18 pathで、うち生成物である`dist/src/cli-usage.js`・`dist/src/cli.js`・`dist/src/domain/workflow.js`の3件は個別監査表から外す（`scripts/check_file_audit.ts`の`isGeneratedDistributionPath`が同じ除外を行う）。**除外するのは個別監査表だけであり、配布物影響の表には生の差分をそのまま載せている。** 残る15 pathが表と一致する。**本review artifact自身が15 pathに含まれる。** round 1のartifact commitが追随mergeより前にあるためで、`H_impl..H_final`は本file 1件の更新になる
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **はい。** 追加したのはjournal entryの任意fieldとその検証だけで、authority判定・信頼境界・外部副作用に触れていない
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: **はい。** 先行PRのREV-01は配布schemaとその受理集合、REV-02・05は`src/domain/workflow.ts`と新規scenario、REV-03・04は`docs/specs/07_データ/01_管理データ.md`、REV-06は追跡表だけを対象にした

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

モード別実装計画（fullは03、quickとpocは集約00）の発見記録を全件転記せずIDで参照し、最終状態とEvidenceを確認する。目的、scope、受け入れ条件、security境界、不可逆操作を変えなかった発見は上流再起動を要求しない。契約を変えた発見は、影響する成果物だけが再確定されていることを確認する。

発見IDはモード別実装計画（fullは03、quickとpocは集約00）の`DISC-*`と同じ字面を使い、本文書内で別IDに言い換えない。

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-1342-01 | 変異試験で`parseStepJournal`の値検査とStep範囲検査が生存した。CLI経由のscenarioでは不正値を書けず、parse層の防御が未検査だった | AC-05を追加 | requirement | 直接`parseStepJournal`を呼ぶSCN-UNIT-RECONFIRM-005を追加 | 変異D・Eがそれぞれ1 failed | updated | pass |
| DISC-1342-03 | 配布schemaが`additionalProperties: false`でありながら`reconfirmation`を定義せず、runtimeの正規出力を拒否する。同型の欠落は既存の`postTerminalIntake`にもあった（REV-01） | 公開schemaが`07_データ`の記述と食い違う | なし | 2 fieldを`const: true`で定義しStep制約を`allOf`でruntimeへ一致させた | ajvで9件の受理・拒否を実測し全件一致 | no-spec-impact | pass |
| DISC-1342-04 | `validateStepJournal`が`reconfirmation`の位置を検査せず、Step 0〜11の後へ手編集で再確定entryを足すと`valid: true`になる（REV-02） | AC-06を追加。書込み側と読取り側で受理集合が食い違う | requirement | `postTerminalIntake`の位置検査と対称な条件を追加 | 変異L・M・N・PをSCN-UNIT-RECONFIRM-006がkill。Oは既存intake scenario 3件がkill | updated | pass |
| DISC-1342-05 | `07_データ`のjournal記述が任意fieldとしてHumanOverride行だけを説明（REV-03）。その是正がREQ-WF-004の位置条件だけを複製し契約を分岐させた（REV-04） | 目的・scope・ACは不変 | なし | **下流で条件を書き直さない。** 任意fieldの存在と値域だけを書き、条件はREQ-WF-004を正本として指す | 追加reviewが0 findingで承認 | updated | pass |
| DISC-1342-07 | 先行PR #1354のbranch履歴が`audit:check`のmerge不変条件に違反した。2回の追随がいずれも**用語IDの採番し直しをmerge commitの中で行い**、`TERM-ASC-106`と`TERM-ASC-107`を失った | 目的・scope・AC・security境界は不変。実装内容は1文字も変えない | なし | PR #1354をcloseし、**追随mergeを一切持たない実装commitとして現在のmain上へ置き直した** | `git log --merges --oneline origin/main..HEAD`が0件 | no-spec-impact | pass |
| DISC-1342-08 | 先行entry判定が`humanOverride`を除外しておらず、**一度も実施していないStepを「再確定」できた**（REV-05）。あわせて追跡表の証拠欄が6 scenarioを列挙しながら「5 scenarios合格」と記録（REV-06） | AC-07を追加 | requirement | `!candidate.humanOverride`を追加しSCN-UNIT-RECONFIRM-007を追加。追跡表を7 scenariosへ直す | 変異Q・Rをkill。**変異Rは当初のscenarioが単一Stepしか見ておらず生存した。** Step 3とStep 7の2通りを検査する形へ直してkillした | updated | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-01 | SCN-UNIT-RECONFIRM-001 | `src/cli.ts`の`--reconfirm`処理、`src/domain/workflow.ts`の順序判定除外 | pass | pass | Step 0〜8記録済みstagingで`--step=3 --reconfirm`が受理され、`workflow verify`が`valid: true`・completedSteps 0..8を返す |
| AC-02 | SCN-UNIT-RECONFIRM-002 | `src/domain/workflow.ts`の`reconfirmation`永続化、配布schema | pass | pass | 追記entryが`reconfirmation: true`を持ち、journal行で通常entryと区別できる |
| AC-03 | SCN-UNIT-RECONFIRM-003 | `src/domain/workflow.ts`の単調性判定（未変更） | pass | pass | `--reconfirm`無しの`--step=3`は`outOfOrder`で拒否され、journalは変わらない |
| AC-04 | SCN-UNIT-RECONFIRM-004 | `src/cli.ts`のStep範囲検査、`src/domain/workflow.ts`の先行entry検査 | pass | pass | 先行entryの無いStep 5と`--step=10 --reconfirm`がそれぞれ理由を名指しして拒否され、journalは変わらない |
| AC-05 | SCN-UNIT-RECONFIRM-005 | `src/domain/workflow.ts`の`parseStepJournal` | pass | pass | `true`以外の値とStep 10の行を理由を名指しして拒否する |
| AC-06 | SCN-UNIT-RECONFIRM-006 | `src/domain/workflow.ts`の`validateStepJournal`の位置検査 | pass | pass | Step 11より後の再確定entryを名指しの診断で拒否し、Step 11より前の同じentryは受理する |
| AC-07 | SCN-UNIT-RECONFIRM-007 | `src/domain/workflow.ts`の先行entry検査 | pass | pass | `humanOverride`だけが先行するStep 3とStep 7の再確定entryを、それぞれのStep番号を名指しして拒否する。通常entryを先に置いた場合は受理する |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | not-applicable | 変更はstep journalの任意field 1件とその構造・順序検証に限る。認証・認可・信頼境界の判定、秘密情報の取得・保存・伝送・表示・伏字化・破棄のいずれにも触れない。journalへ載るのはStep番号・skill ID・mode・時刻・成果物path・evidence文字列で、個人情報も秘密も含まない | `git diff origin/main 6e078644 -- src/`の全hunkを確認し、認証・認可・秘密処理の追加は0件 |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 拒否時の診断文言を新設しており、運用者が次に採る行動を決められるかが対象になる | 拒否診断は「Step Nの上流再確定entryに先行する通常entryがありません」「上流再確定entryはStep 11より後に置けません。Step 11記録後に置けるのはpost-terminal intakeのStep 10だけです」「reconfirmationはtrueだけを受理します」「reconfirmationはStep 1〜9にだけ指定できます」の4件で、いずれも条件と許される形を名指しする。SCN-UNIT-RECONFIRM-004・005・006・007が文言を固定し、変異Pがその空洞化をkillする。秘密・個人情報は載らない |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 画面契約を持たない。追加したのは`workflow record`の値を取らないflag 1件とJSON出力で、対話的な入力手順、視覚的表現、支援技術への伝達を持たない。「JSON出力のみ」を根拠にせず、この変更が画面・a11y検証の対象を1つも持たないことを根拠にする | `src/cli-usage.ts`の差分はoptionalFlag 1件の追加のみ。UI層・token・画面仕様への差分は0 file |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | design/layout tokenを持つ成果物へ触れていない | `docs/specs/17_デザイン/`・`18_レイアウト/`への差分は0 file |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 独立reviewerがAC-01〜07とINV-01〜03を実装位置（`src/cli.ts:5329`、`src/domain/workflow.ts:578`・`694`・`708`・`739`）へ対応づけて確認した。範囲内のmerge commitは0件 |
| 価値 | 利用者・運用上の目的を満たすか | pass | 通常entryの単調性を緩和せず、明示的な`reconfirmation: true`だけを順序判定から外すため、上流再確定を記録可能にしつつ後付けの一般解禁を避けている |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 新規依存も新規権限も要らない。CLI、parser、validator、配布schema、usage、仕様、`dist/`が一貫して更新され、静的検査8種が終了値0 |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | mainが持ち込んだ`workflow advance`（#1335）は再確定後もStep 9を次Stepとして導出し、`review progress`（#1336）はstaging digestから除外された別journalで経路が分離している。独立reviewerが実測した |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 既存の`postTerminalIntake`と同型の構造で、通常entry抽出・先行entry検査・terminal位置検査が分離されている。到達不能条件を置かない理由をコードコメントで明示している |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | 先行通常entryなし、`reconfirmation !== true`、Step 0・10、Step 11より後、`humanOverride`だけが先行、の各反例をいずれも拒否する。先行PRで見つかった2件（Step 11後の手編集journal、humanOverride先行）は是正済みで、本roundでは新しい反例が出なかった |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 構造・順序検査はjournal transaction開始前に行い、拒否時に既存journalを置換する経路がない。CLIの拒否後もjournalはbyte単位で不変であることをSCN-UNIT-RECONFIRM-003・004が固定する |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | Step 0・10・11を拒否しStep 1〜9だけを受理する。位置はStep 11の直前を受理し直後を専用診断で拒否する。`reconfirmation`の値域は`true`のみ |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | flagだけで未記録Stepを完成扱いにできない。先行entryは「同じStepの、再確定でもpost-terminal intakeでも`humanOverride`でもない通常entry」に限るため、再確定entryの連鎖でも人間承認の記録でも先行条件を自作できない |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | 認証・認可・信頼境界・秘密処理に触れない。書込みadapterのStep 11後・terminal delivery state後の封印を維持し、読取り側にも同じ条件を置いた |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | append-only journalへの任意field追加であり、削除・上書き・移行処理を含まない。記録は残し順序判定だけを外す原則を維持している |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | `reconfirmation`を持たない既存journalの受理集合は不変で、当該commitのrevertで従来形式へ戻る。旧版CLIが新fieldを未知fieldとして拒否する互換性制約を変更履歴へ明記した |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | 先行PRで配布schemaの欠落（REV-01）とデータ仕様の欠落（REV-03）が捕まり是正済み。本roundでは配布境界・文書の漏れが出なかった |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| REV-01 | Medium | 配布schemaが`additionalProperties: false`でありながら`reconfirmation`を定義せず、runtimeの正規出力を拒否する | `workflow record --step=3 --reconfirm`が生成する正規entryをschemaへ渡すと未知propertyとして拒否される | 配布schemaを検証に使うconsumer全体 | 修正。2 fieldを`const: true`で定義しStep制約をruntimeへ一致させた | resolved | なし。ajvで9件を実測 |
| REV-02 | Medium | `validateStepJournal`がStep 11より後の再確定entryを位置検査せず、手編集journalが`valid: true`になる | Step 0〜11の通常entryへStep 3の`reconfirmation: true`を末尾追加すると`valid: true`・completedSteps 0..11 | 保存済みjournalを読む全経路 | 修正。`postTerminalIntake`と対称な位置検査を追加 | resolved | なし。SCN-006と変異4件で固定 |
| REV-03 | Low | `07_データ`のjournal記述が任意fieldとしてHumanOverride行だけを説明している | 当該節の本文 | 仕様の読者 | 修正。任意fieldの存在と値域を追記 | resolved | なし |
| REV-04 | High | REV-03の是正がREQ-WF-004の位置条件だけを複製し、先行entry条件を複製しなかったため同一契約が不均等に分岐した | 先行通常entryなしでStep 11より前に再確定entryを置くと追記文の条件は満たすが実装は拒否する | `07_データ`と`02_要件`の2文書 | 修正。**下流で条件を書き直すのをやめ正本を指す形へ直した** | resolved | なし |
| REV-05 | Medium | 先行entry判定が`humanOverride`を除外せず、**一度も実施していないStepを再確定できた** | Step 0〜4、Step 5のhumanOverride、Step 5の再確定entryで`valid: true` | `validateStepJournal`を読む全経路 | 修正。`!candidate.humanOverride`を追加しSCN-007を追加 | resolved | なし。変異Q・Rでkill |
| REV-06 | Low | 追跡表の証拠欄が6 scenarioを列挙しながら「5 scenarios合格」と記録 | `15_要件追跡/00_追跡表.md` | 追跡表の読者 | 修正。7 scenariosへ直した | resolved | なし |

指摘なしの場合は「指摘なし」と明記する。

**本branchのround 1の指摘はなし。** 上表6件はすべて先行PR #1354で出た指摘であり、実装内容へ反映済みのまま本branchへ持ち越している。未解決のCritical/Highはない。

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: **はい。** 固定initial HEAD `6e078644`の全scopeを肯定・敵対の両観点でreviewした
- 指摘を確定した: 指摘なし（blocking 0件）で収束した。先行PR #1354で出たREV-01〜06は本branchへ持ち越す前に是正済みで、再発していないことをreviewerが個別に確認した
- 次ラウンド対象のCritical/High: なし

### ラウンド2

- 未解決Critical/High: なし。round 1から持ち越した未解決はない
- 修正差分: PR #1362（Issue #1337）のmergeで既定branchが`83a4a895`へ前進したことへの取り直し。追随merge `3e360b74`と、用語IDを`TERM-ASC-109`へ移す通常commit `6e078644`
- **追随mergeの中で採番し直しをしていない。** `src/domain/merge-integrity.ts`はmerge commitがどちらの親の内容も失わないことを不変条件とする。mergeでは`TERM-ASC-108`の2行（#1337と本Issue）を両方残し、自分側の移動はmergeの後の通常commitで行った。`audit:check`のmerge検査は`valid`である
- 修正で触れた隣接範囲: 用語台帳1行、変更履歴1行、`src/domain/workflow.ts`のTSDoc 1行。実装の振る舞いは変えていない
- 既承認・未変更範囲を再走査していない: **はい。** round 1で確認済みかつ未変更の範囲は再走査せず、追随差分と採番し直しに限って再reviewした

### ラウンド3

- 外部reviewer（CodeRabbit）が`pr create`より後に3件を指摘し、`01_開発ワークフロー.md`の取り込み条件を満たしたため同sessionのround 3で処理した。**前進commitで是正し、reviewスレッドへ判定を返信して解決した。**
  - **valid（是正）**: 値を付けた`--reconfirm`が有効として通る。parserは`--name`だけを`__present__`にするため`!== undefined`では`--reconfirm=false`を受理していた。**利用者が無効化したつもりの入力が、意図と逆の記録としてjournalへ残る。** `presentFlag()`で`__present__`だけを受理し、同じ形をしていた既存の`--post-terminal-intake`も同時に直した。SCN-UNIT-RECONFIRM-008で固定
  - **valid（是正）**: 本artifactのPhase A artifact欄のpathが実tracked pathと不一致だった。生成時の既定名のまま書いていた
  - **not-applicable**: 先行entryへmode一致を要求する案。**採ると`workflow promote-full`によるquick→full昇格経路を壊す。** 昇格後は同じjournal内に`quick`と`full`のentryが並ぶことが正規であり、mode整合は`modeTransitionConflicts`（`src/domain/workflow.ts:630-642`）が既に単調昇格だけを許す形で担っている
- 全指摘の最終分類: 上記3件のうち2件が`resolved`、1件が`not-applicable`。先行PRのREV-01〜06もすべて`resolved`
- 任意の危険範囲を除外・既定無効・ロールバック可能へ縮小した結果: 追加した`reconfirmation`はflag未指定時に付かない任意fieldであり、既定は従来どおり。Step 1〜9・先行通常entry必須・Step 11より前という3条件で範囲を絞り、当該commitのrevertで従来形式へ戻る
- 同じ範囲の予算を自動更新していない: **はい。** 本branchはround 1で収束し、sessionの作り直しも上限の引き上げも行っていない。先行PR #1354のsessionは引き継がず、履歴の作り直しに伴い新しいstagingで新しいsessionを開いた（DISC-1342-07）
- AIによる最終裁定: approved。未解決Critical/Highなし

## 7. テスト結果

- 実行したcommandの一覧: `npm run lint`、`npm run format:check`、`npm run typecheck`、`npm run docs:format`、`npm run test:format`、`npm run trace:check`、`npm run architecture:check`、`npm run workflow:check`、`npm run conformance:check`、`npm test`。いずれも`H_impl`=`6e078644`で実行し終了値0
- 全layerの合計: `npm test` 1,918 scenarios（1,901 passed / 16 skipped / 1 failed）、10分38秒。**失敗はSCN-INT-GITHUB-031の時間窓flake**（600ms待機の計測が並行負荷で415ms）で、単独再実行 `--name 'SCN-INT-GITHUB-031'` は1 scenario合格。`conformance:check` 87 scenarios（87 passed）。いずれも`H_impl`=`6e078644`で実測
- 対象scenarioの単独実行: `SCN-UNIT-RECONFIRM-001`〜`008` 8 scenarios（8 passed）。post-terminal intakeとの併走確認を含む`SCN-UNIT-RECONFIRM|intake`は16 scenarios（16 passed）
- 変異試験7件: L（Step 11後の位置検査ごと削除）・M（位置条件の反転）・N（`terminalIndex > 0 && index > terminalIndex + 1`へ狭める）・P（診断文言の空洞化）をSCN-UNIT-RECONFIRM-006が、Q（先行entry判定から`humanOverride`除外を削除）・R（除外をStep 3だけに狭める）をSCN-UNIT-RECONFIRM-007がkill。O（`reconfirmation`への限定を外し全entryへ当てる）はSCN-UNIT-RECONFIRM単独では生存し、**既存のpost-terminal intake scenario 3件がkillした**。等価変異ではなく、既存scenarioが同じ境界を検査している
- **変異試験の設計上の学び**: 変異Rは当初のSCN-UNIT-RECONFIRM-007が単一Stepしか検査していなかったため生存した。除外条件を特定のStep番号へ狭める変異は、1 Stepだけの検査を通過する。Step 3とStep 7の2通りを検査する形へ直してkillした
- runner・Gherkin方言: cucumber-js（`cucumber.mjs`）、`gherkinDialect: en`。project choicesの選択値

## 8. 配布物影響

projectがpackageとして配布される場合だけ記入する。配布境界はpackage manifestの配布file指定を単一正本とし、compileされて配布される`source`も配布境界に含める。

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | 入る | Step記録節へ`workflow record --step=<N> --reconfirm`の1文が加わる。利用者は上流再確定の記録手段を配布文書から知る |
| `.agent-skill-chain/schemas/workflow-step-journal.schema.json` | 入る | journal entryの公開契約へ任意field `reconfirmation`と`postTerminalIntake`が加わる。**このschemaで自分のjournalを検証しているconsumerは、これまで正規の`postTerminalIntake` entryを未知fieldとして拒否していた。本変更で受理する** |
| `dist/src/cli-usage.js` | 入る | `workflow record --help`のoptionalFlagへ`--reconfirm`が1件現れる |
| `dist/src/cli.js` | 入る | `workflow record`が`--reconfirm`を受理する。flag未指定時の振る舞いは不変 |
| `dist/src/domain/workflow.js` | 入る | journalのparseと順序検証が`reconfirmation`を受理・検査する。fieldを持たない既存journalの判定は不変 |
| `docs/specs/01_システム概要/02_用語・略語.md` | 入らない | repository内部の仕様書。`package.json`の`files`に含まれない |
| `docs/specs/02_要件/00_要件一覧.md` | 入らない | 同上 |
| `docs/specs/02_要件/01_ワークフロー要件.md` | 入らない | 同上 |
| `docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md` | 入らない | 同上 |
| `docs/specs/07_データ/01_管理データ.md` | 入らない | 同上 |
| `docs/specs/15_要件追跡/00_追跡表.md` | 入らない | 同上 |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | 同上 |
| `src/cli-usage.ts` | 入る | `dist/`へcompileされて配布される`source`であり配布境界に含める。外部観測可能な変化は`dist/src/cli-usage.js`と同じ |
| `src/cli.ts` | 入る | 同上。外部観測可能な変化は`dist/src/cli.js`と同じ |
| `src/domain/workflow.ts` | 入る | 同上。外部観測可能な変化は`dist/src/domain/workflow.js`と同じ |
| `test/features/unit/workflow-reconfirmation.feature` | 入らない | `package.json`の`files`に`test/`は含まれない |
| `test/steps/workflow-reconfirmation.steps.ts` | 入らない | 同上 |

判断: 配布物を更新した

根拠: `.agent-skill-chain/docs/01_開発ワークフロー.md`のStep記録節へ`--reconfirm`の記述を追加し、`.agent-skill-chain/schemas/workflow-step-journal.schema.json`へ`reconfirmation`と`postTerminalIntake`の値域・Step条件を追加した。`dist/`は`npm run build`で再生成済みである。

**runtimeの挙動が変わるのに配布文書が追随していない状態を残さない。** 利用者は配布物だけを読むため、repository内部の仕様書へ書いても届かない。

## 9. 独立reviewの成立

**この節はPhase Aで埋める。** 記入するのはPR作成前に観測できるものだけであり、**immutable review IDやapprovalの件数などPR作成後にしか存在しない値は書かない。** それらはPhase Bで`review evidence`が観測して記録する。

| 項目 | 内容 |
|---|---|
| 適用した独立性モード | `context-isolated`。project policyに`merge.reviewIndependence`の宣言がないため既定を適用する（`grep -rn "reviewIndependence" .agent-skill-chain/project/choices/*.json .agent-skill-chain/project-policy.json`が0件） |
| その要求を満たすこと | はい |
| reviewerとimplementerのidentity・context比較 | implementerはこのsessionのcoordinator（commit author `tatsuru <info@ruaprom.jp>`）。reviewerは`codex exec --skip-git-repo-check --sandbox read-only -C <worktree>`で起動した別processで、会話contextを共有しない。exact HEAD `6e0786447a04ed918d8150fedb049dac93cb20f8`を依頼文で固定した。同一GitHub actorだが`context-isolated`はこれを許す |
| reviewerが対象差分を変更していないこと | はい。reviewerはread-only sandboxで起動しておりworktreeへ書き込めない。review前後で`比較基点..H_impl`の差分path集合は同じ17 pathであり、`H_impl`も`6e078644`から動いていない |

外部への不可逆な配布で独立reviewの外部証拠を要求され、かつそれが無い場合だけ記入する。

該当しない。外部への不可逆な配布（package registryへの公開）を本PRでは行わず、独立reviewは`context-isolated`の要求水準で成立している。例外を適用しない。

**承認元、承認者、承認日時、失効日時は正本を参照し、ここへ複製しない。**

例外が正本に無い、または失効している場合は進めない。記録があれば実装・PR・mergeは進み、mergeを契機に自動で走るtag作成とReleaseも進む。止めても不可逆な行為を防げないためである。package registryへの公開など外部への不可逆な配布だけが、記録の有無にかかわらず独立reviewの外部証拠を要求する。削除、force push、履歴書き換えは独立reviewの対象外であり、既存のauthority経路で守る。

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/01_システム概要/02_用語・略語.md`（TERM-ASC-109追加）、`docs/specs/02_要件/00_要件一覧.md`、`docs/specs/02_要件/01_ワークフロー要件.md`（REQ-WF-004本文）、`docs/specs/06_外部インターフェース/01_コマンド・GitHub契約.md`、`docs/specs/07_データ/01_管理データ.md`、`docs/specs/15_要件追跡/00_追跡表.md`、`docs/specs/15_要件追跡/01_変更履歴.md`、`.agent-skill-chain/docs/01_開発ワークフロー.md`
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: **はい。** TERM-ASC-109「上流再確定entry」を`active`として台帳へ追加し、REQ-WF-004から参照する
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: **はい。** 台帳上はTERM-ASC-106（workflow advance）、107（parallel progress evidence）、108（上流再確定entry）が各1定義で意味も分離していることをreviewerが確認した。**先行PR #1354では採番が106→107→108と2度動いており、その原因と再発防止はIssue #1360へ分離した**
- 要件・変更・SCN・テストの追跡: REQ-WF-004 → AC-WF-004 → SCN-UNIT-RECONFIRM-001〜007 → `test/features/unit/workflow-reconfirmation.feature`・`test/steps/workflow-reconfirmation.steps.ts` → `src/domain/workflow.ts`・`src/cli.ts`。`trace:check`が終了値0
- `no-spec-impact`の場合の限定的根拠: 該当しない（`updated`）
- UI・トークンの判断: DC-UXとDC-TOKENSはいずれも`not-applicable`。画面契約とtoken成果物を1件も持たない

## 11. 総合判定と再開地点

- 未解決Critical/High: なし
- Medium/Lowの記録: REV-01・02・05（Medium）、REV-03・06（Low）、REV-04（High）はいずれも`resolved`。record-onlyのまま残した指摘はない
- 判定: approved
- 新しい権限が必要な事項: なし。**先行PR #1354ではstaging digestの再固定にrepository ownerの決裁を要したが、本branchは新しいstagingで工程を通しており当該迂回を使っていない**
- 残存リスク: (1) 用語IDの採番は今回`TERM-ASC-109`で衝突していないが、採番規則そのものが並行作業で衝突する構造は残る（Issue #1360）。(2) 先行PR #1354のbranchはremoteに残っており、そのmerge commitは`audit:check`のmerge不変条件に違反したままである。PRはcloseしており既定branchへは到達しない
- 次に許可される操作: 本fileを`H_impl`の上へ1 commitで載せて`H_final`にし、`review validate --artifact`と`audit:check`を通したうえで`workflow record --step=10`を記録する。その後`pr create`でPRを作成し、CI緑を確認して`gh pr merge --merge`する
- 次回の再開地点: PRのCI結果の確認から。既定branchが動いた場合、残り予算は3ラウンド（＋取り直し1）である
