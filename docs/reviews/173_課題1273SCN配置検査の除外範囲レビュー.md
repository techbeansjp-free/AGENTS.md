# 04 レビュー

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 1 |
| H_impl | `64627c77e1ffb9b8b8b6ffb43d21aab639eec7a0` |
| 比較基点 | `1f0c546f299be80721d87a88f8a52e1928abe536` |
| 対象SHA・文書ダイジェスト | `64627c77e1ffb9b8b8b6ffb43d21aab639eec7a0` |
| 対象差分 | `1f0c546f299be80721d87a88f8a52e1928abe536..64627c77e1ffb9b8b8b6ffb43d21aab639eec7a0`、12 path。うち`dist/`配下2件は生成物として個別監査の対象外とし、配布影響は§8へ残す |
| 対象外 | `walkRepositoryFiles`の走査範囲、判定にgit追跡状態を用いる案、要件本文検査の意味的偽陽性、`isIssueStagingPath`のexport削除、一時領域内の誤配置featureを別経路で検出し直す機構 |
| 残り予算 | 同一範囲で最大3ラウンドのうち1ラウンドを使用。**残り2。** 収束後のHEAD移動に対する取り直し1ラウンドは未使用 |
| ラウンド数 | 1 |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260907_204534_SCN配置検査の除外を一時ライフサイクル領域の正本へ揃える |
| 仕様の所有箇所 | `docs/specs/02_要件/04_仕様・品質管理要件.md`のREQ-SQ-017。引用: 「**判定にgit追跡状態を用いない。**」 |
| 成果物行数 | 実装 +59、検査script +21、test +234、仕様 +32、生成dist +78 |
| 縮小の先行評価 | **起票時に私が推した案2（`git ls-files`で追跡fileへ限る）を採らない。** `REQ-SQ-017`が「判定にgit追跡状態を用いない」と明文で定めており、既存`SCN-UNIT-SCNSCOPE-007`が固定している。**`isStagingLifecyclePath`自身を強化する案も採らない。** 追跡混入拒否が退行する。**新しい検査器、gate、CLI、schemaを1つも追加していない。** 走査範囲も変えていない |
| 実施者・日時 | implementer: Claude Code / claude-opus-5[1m]。reviewer: codex CLI / 設定既定model / reasoning effort high。2026-09-07 |

### 0.1 routing入力契約

| role欄（担当role） | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類、原文引用 | advanced。riskはmedium、外部契約変更あり | codex CLI。Codexの推論レベル上限highの範囲内 | **`--model`で固定せず設定既定に従う** | 独立性が不明ならPRとmergeを停止する | implementerはClaude Code、reviewerはcodexの新規session。`--sandbox read-only`で起動し、reviewer自身が「file編集・commit・pushは行っていません」と報告した |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 要求・受け入れ条件 | GitHub Issue #1273、staging 01のAC-1273-01からAC-1273-06 | Step 8で`sync-verified`、syncDigestとreadBackDigestが`cbe68e079343efe63d0ca7a9659ec4c53196ff916ad7e423586f25a89dc81a7b`で一致 | 実行観測 |
| 欠陥の実測 | クリーンなmain（`1f0c546f`、作業tree clean）で`npm run trace:check` | 18件のerror。全件が`.agent-skill-chain/tmp/handoffs/`配下の2 fileを指す。当該fileは`git ls-files`が0件、`.gitignore:18`に掛かる | 実行観測 |
| 是正前の再現（T00） | 一時領域4件へSCN定義を置いた隔離fixture | 配置違反4件 | テスト出力 |
| 是正後 | 同一fixture | 配置違反0件 | テスト出力 |
| 陳腐化の実測 | `.gitignore`と`git check-ignore` | `.gitignore:21-23`が`role-log`・`metrics`・`runtime`を無視する。REQ-SQ-017が除外しない理由として書いていた前提は成立しない | 実行観測 |
| 差分 | `1f0c546f..64627c77` | 12 path | 既存コード |
| テスト | 対象SCNを`--name`で絞り込んだcucumber実行 | 19 scenarios、95 steps、すべて成功 | テスト出力 |
| 変異試験 | 28件 | 27 kill。生存1件はこのplatformでの等価変異 | テスト出力 |
| oracle退化の比較実験 | 固定期待集合と述語導出の期待集合 | 報告10件が固定oracleと一致し、退化oracleは近似path 4件を取りこぼす | テスト出力 |
| 仕様 | `docs/specs/02_要件/04_仕様・品質管理要件.md`、`00_要件一覧.md`、`15_要件追跡/` | updated | 既存文書 |
| commit前candidate | `git diff --name-status 1f0c546f 64627c77` | 12 pathすべてM。うち生成dist 2件 | Git index |
| Phase A artifact | `docs/reviews/173_課題1273SCN配置検査の除外範囲レビュー.md` | H_implの後にこの1 fileだけをcommitしてH_finalとする | Git観測 |
| commit後external | PR、CI run、外部review | **本artifactの作成時点では未観測である。** | 外部のimmutable証拠 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: **満たす。** nodeもedgeも追加していない。
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: **artifact commit時点で満たす。**
- reviewer stable IDがPR author/`H_impl` author stable IDと異なる: **`H_impl` authorとは異なる。** PR authorとの比較は本artifactの作成時点では未確認であり、PR作成後に観測する。
- 既定branch追随を行った場合: **該当なし。** 比較基点は`1f0c546f`のままである。

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/staging.ts` | M | package owner | domain | segment検証helperの抽出と、SCN配置検査専用の厳格述語の追加 | pass。helperは分類も正規化規則も持たず、述語から呼ばれるだけである | REQ-SQ-017 / AC-1273-01・03・06 / SCN-UNIT-SCNSCOPE-010、012、014、015 | **`isStagingLifecyclePath`と`isIssueStagingPath`の判定結果を変えていない。** 前進revertで復旧 | pass |
| `scripts/check_trace.ts` | M | package owner | project | SCN配置検査の除外先を厳格述語へ替える | pass。走査範囲と共有walkerの列挙結果を変えない | AC-1273-01・02 / SCN-UNIT-SCNSCOPE-010、011、015 | 除外領域外の拒否を維持。前進revertで復旧 | pass |
| `scripts/report_scan_boundary.ts` | M | package owner | project | 述語登録を実態へ合わせる | pass。`EXPECTED_PREDICATE_IDS`と供給元を独立に列挙する既存構造を保つ | AC-1273-05 | 追加のみ。既存登録を削除していない | pass |
| `src/domain/scan-boundary.ts` | M | package owner | domain | 適用範囲の説明を実装へ合わせる | pass。説明だけで判定を持たない | AC-1273-05 | 記述のみ | pass |
| `test/features/unit/scn-scope.feature` | M | package owner | evidence | SCN-010から015の6件を追加し、006を反転 | pass | AC-1273-01〜04・06 | 既存scenarioを削除していない | pass |
| `test/steps/scn-scope.steps.ts` | M | package owner | evidence | 固定期待集合、生入力の判定列、要件本文の表示本文検査 | pass。既存step定義を書き換えていない | 同上 | 追加と、005のfixture移設のみ | pass |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | M | package owner | spec | REQ-SQ-017の除外範囲・理由・受け入れる検出損失、REQ-SQ-032の述語名 | pass。名指ししたSCN 6件は同じ要件の追跡行に登録済み | REQ-SQ-017 / AC-SQ-017 | 既存段落を削除していない | pass |
| `docs/specs/02_要件/00_要件一覧.md` | M | package owner | spec | REQ-SQ-017の名称 | pass | REQ-SQ-017 | 1行のみ | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package owner | spec | 追加SCNの追跡行1件 | pass。`trace:check`のorphanが要件・SCN・実装とも0件 | REQ-SQ-017 / AC-SQ-017 | 既存行は不変 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package owner | spec | 変更理由と判断の記録1行 | pass。9列のheader区切り直後へ挿入 | REQ-SQ-017 | 既存行は不変 | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: **満たす。** `git diff --name-status`の12 pathのうち、生成物である`dist/`配下2件を除いた10 pathと表の10行が一致する。`dist/`の配布影響は§8へ残す。
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: **満たす。**
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: **満たす。** H-01は`src/domain/staging.ts`と`test/`の2 file、M-01は仕様1 file、M-02は`test/steps/`1 fileに閉じている。

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-001 | `isStagingLifecyclePath`は`isIssueStagingPath`が持つ区切り正規化とsegment検証を持たない。単純な差し替えは4件の判定を退行させる | 実装の形が変わる。1行の差し替えでは足りない | AC | 上流を再起動せず00と01の該当節だけを再確定した。FR-01aとAC-1273-03を追加した | 配布moduleへ生のpathを直接入力して4件の差を実測した | REQ-SQ-017へ入力契約を追記 | pass |
| DISC-002 | **`isStagingLifecyclePath`を強化すると追跡混入拒否が退行する。** `\`はPOSIXで通常文字であり`.agent-skill-chain/tmp/..\draft.md`は`tmp`配下に実在しうる合法なfile名である | 既存述語を触れない。設計の形が変わる | AC | 既存述語の契約を変えず、SCN配置検査専用の厳格述語を別に追加した。AC-1273-06とSCN-UNIT-SCNSCOPE-014を足した | 合法POSIX名12件で旧述語12件・強化版0件を実測 | REQ-SQ-017へ「安全側の向きは呼び出し元ごとに逆」と明記 | pass |
| DISC-003 | **私の実装が、走査由来のpathでも`\`を区切りへ倒しており、領域外のfileを除外していた。** `.agent-skill-chain/role-log\evil.feature`の親directoryは`.agent-skill-chain`である | INV-1273-01の違反。領域外4件の配置診断が4件から0件へ落ちていた | なし | 正規化規則を述語ごとに分け、共有するのはsegment検証だけにした。厳格述語は`slash`でOS本来の区切りだけを正規化する | 是正前0件・是正後4件を実測。SCN-UNIT-SCNSCOPE-015で固定 | REQ-SQ-017へPOSIXの`\`の扱いを明記 | pass |
| DISC-004 | 標識文だけを検査すると、段落の後続本文を空にする変異が生存する | AC-1273-04の担保範囲 | なし | 各段落から判断の実体を1文ずつ名指しした。**assertionをACを超えて広げていない。** ACは元から「新しい除外範囲と理由が入る」を要求している | 本文空洞化を含む7変異を構成して全件kill | 不要 | pass |

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1273-01 | SCN-UNIT-SCNSCOPE-010、006 | 除外先を厳格述語へ替える | 合格 | pass | 除外先を戻す変異と、領域一覧から4領域を1件ずつ落とす変異をkillする |
| AC-1273-02 | SCN-UNIT-SCNSCOPE-011、005、002、003、015 | 固定期待集合との完全比較 | 合格 | pass | 境界区切りをprefix前方一致へ緩める変異、所定locationの`.feature`条件を落とす変異、走査由来pathで`\`を区切りへ倒す変異をkillする |
| AC-1273-03 | SCN-UNIT-SCNSCOPE-012 | 生のpath入力への判定列 | 合格 | pass | 空・`.`・`..`の拒否を個別に落とす3変異と、segment検証を外す変異をkillする |
| AC-1273-04 | SCN-UNIT-SCNSCOPE-013 | REQ-SQ-017の表示本文 | 合格 | pass | 理由の削除、HTMLコメント退避、code block退避、**段落本文の空洞化4件**、旧規定の復活をkillする |
| AC-1273-05 | なし。独立reviewの完了条件 | 述語登録とREQ-SQ-032 | 該当なし | pass | reviewerが`id`・`owner`・`appliesTo`・`reasonCode`・`reason`・`excludes`と`EXPECTED_PREDICATE_IDS`を実装と突合し、7入力での除外件数まで実行して確認した |
| AC-1273-06 | SCN-UNIT-SCNSCOPE-014 | `isStagingLifecyclePath`の契約維持 | 合格 | pass | 同述語を厳格化する変異をkillする。reviewerが9,928文字列と非文字列6入力で基点版との差分0件を確認した |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | not-applicable | 除外判定の変更であり、信頼境界、認可、秘密情報の扱いを変更しない | 新しいgateとauthorityを追加していない。reviewerが差分を確認した |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | **偽陽性そのものが運用可能性の欠陥である。** 手元とCIで判定が割れると原因の切り分けができない | SCN-UNIT-SCNSCOPE-010と011の報告path集合の完全一致。述語登録の更新 |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | CLIでありGUIを追加しない。診断文言も変えない | projectKindはcliである。差分に視覚要素はない |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | 視覚componentとtokenを追加しない | project choicesのcapabilities.designTokensに従う |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 独立reviewerがAC-1273-01から06とINV-1273-01から06を原文引用で判定し、H-01の是正後はすべてpassである |
| 価値 | 利用者・運用上の目的を満たすか | pass | クリーンなrepositoryで`trace:check`が緑になる。一時領域の運用を守ったまま基準線が取れる |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 新しいgate、validator、CLI、schema、依存packageを追加していない |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | 02の§11に列挙した対応と実差分の10 pathが一致する |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 分類は`STAGING_LIFECYCLE_AREAS`のまま。**正規化規則は共有せず、segment検証だけを共有する。** 共有範囲の判断根拠をコメントへ残した |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | 変異28件中27 kill。**うち10件はreviewer由来である。** 生存1件はこのplatformでの等価変異 |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 判定不能なpathを除外しない。観測不能な述語は`complete:false`として扱う既存構造を変えない |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | 領域そのもの、4領域の近似path、所定location、空文字、`\`をfile名に含むpathを固定集合で比較する。**最大・重複・Unicodeは本件のscenarioに含まれない。** 既存SCN-UNIT-SEC-001から006が担保する |
| 悪用 | 注入、経路脱出、権限外操作等 | pass | `..`を含むpathを除外しない。**除外領域内から領域外を指すsymlinkで領域外の定義を隠せない**（既存SCN-UNIT-SCNSCOPE-008、009） |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | gateを追加せず既存gateも弱めていない。**`isStagingLifecyclePath`の契約を変えず追跡混入拒否を維持した。** 判定にgit追跡状態を用いない |
| データ損失 | 上書き、削除、部分公開、履歴消失 | not-applicable | 変更箇所に永続化・削除処理を追加していない |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | 前進revertで復旧する。保存形式を変更しない |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | `dist`のbuild結果を同一commitへ含めた。REQ-SQ-017、REQ-SQ-032、要件一覧、追跡表、変更履歴、述語登録、`scan-boundary.ts`の説明へ反映した |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| H-01 | High | **厳格述語が走査由来のpathでも`\`を区切りへ倒しており、領域外のfileを除外していた。** `.agent-skill-chain/role-log\evil.feature`の親directoryは`.agent-skill-chain`であり、領域外4件の配置診断が4件から0件へ落ちていた | reviewerが反例を構成し、私も再現した | AC-1273-02、INV-1273-01、INV-1273-03 | 正規化規則を述語ごとに分け、共有するのはsegment検証だけにした。厳格述語は`slash`でOS本来の区切りだけを正規化する。SCN-UNIT-SCNSCOPE-015で固定した | resolved | なし |
| M-01 | Medium | REQ-SQ-017に「走査範囲からIssue一時ステージング`.agent-skill-chain/tmp/issues/`だけを除く」が現在形で残り、新しい除外範囲と矛盾していた | reviewerが両方の表示を示した | AC-1273-04 | 当該段落を一時ライフサイクル領域の記述へ書き換え、旧規定の文字列をSCN-013の禁止集合へ加えた | resolved | なし |
| M-02 | Medium | 標識文だけを検査しており、**各段落の後続本文を空にする変異が生存した。** 理由の具体的説明、受容理由、代替検出しない限定が消えても19 scenarioが通過した | reviewerが本文削除を実測した | AC-1273-04 | 各段落から判断の実体を1文ずつ名指しした。本文空洞化4件を含む7変異でkillを確認した | resolved | なし |
| L-01 | Low | 変異M06（厳格述語から`slash`を落とす）が生存する。**Linuxでは`path.sep`が`/`であり`slash`は恒等関数のため、このplatformでは等価変異である** | 変異試験の生存1件 | AC-1273-03 | 記録のみ。**区別できる入力がPOSIX上に存在しない。** 区切りを注入するseamは製品側に無く、testのためだけに作らない | valid | Windowsでの区切り正規化はこのrepositoryのCIとローカルでは検証されない |

## 6. ラウンド固有の確認

### ラウンド1

- 全評価基準を確認した: はい。AC-1273-01から06、INV-1273-01から06を原文引用で判定した。
- 指摘を確定した: H-01、M-01、M-02、L-01。**Criticalは0件。**
- 次ラウンド対象のCritical/High: なし。H-01、M-01、M-02はこのラウンド内で是正した。
- verdictはAPPROVE_WITH_FINDINGS。**reviewerの当初verdictはREQUEST_CHANGESであり、H-01の是正をもって解消した。**

## 7. テスト結果

実行したcommandの一覧。

- `npm run lint`
- `npm run format:check`
- `npm run typecheck`
- `npm run source:check`
- `npm run docs:format`
- `npm run test:format`
- `npm run trace:check`
- `npm run scan-boundary:report`
- `npm run verify:distribution`
- `node --import tsx ./node_modules/@cucumber/cucumber/bin/cucumber.js --config cucumber.mjs --name 'SCN-(UNIT|INT)-SCNSCOPE'`

runnerは`@cucumber/cucumber`、`projectChoices.gherkinDialect`は`en`、`projectChoices.testLayers`は`unit`、`integration`、`e2e`である。

対象シナリオの実行は19 scenarios、19 passed、0 failed、0 skipped。95 steps、95 passed。

`verify:distribution`の全suite実行は1630 scenarios、1614 passed、0 failed、16 skipped。**skipは既存の環境依存scenarioであり本件が追加したものではない。**

変異試験の母集団は28件である。内訳は、除外先を戻す1件、領域一覧から4領域を1件ずつ落とす4件、segment検証を個別に落とす3件、厳格述語とIssue述語からsegment検証を外す2件、領域そのものの一致を落とす1件、境界区切りをprefix前方一致へ緩める1件、所定locationの`.feature`条件を落とす1件、`isStagingLifecyclePath`を厳格化する1件、厳格述語からOS区切り正規化を落とす1件、REQ-SQ-017への6件、**および独立reviewerの指摘から構成した7件**（段落本文の空洞化4件、旧規定の復活1件、厳格述語で`slash`を`replaceAll`へ戻す1件、Issue述語からWindows正規化を落とす1件）である。**27件をkillした。生存1件はL-01の等価変異である。**

**全件killを成果として報告しない。** 母集団のうち10件はreviewer由来であり、私が最初に作った21件だけでは、走査由来pathでの`\`の扱いと段落本文の空洞化に届かなかった。

oracle退化の比較実験。固定期待集合では報告10件が完全一致し、**述語から導出した退化oracleでは`tmp-old`・`role-log-old`・`metrics-old`・`runtime-old`の4件を取りこぼす。** 期待集合を述語自身から導出すると、判定を`startsWith(area)`へ緩めた実装変異を検出できない。

失敗またはskipがある層: **対象シナリオの実行では0件である。** 全suiteの結果は`verify:distribution`の実行ログを正本とする。

対応する成功CI runの参照: **本artifactの作成時点では存在しない。** branchを未pushであるため、run IDとURLと対象HEADは提出時に別途記録する。

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/domain/staging.ts`、`dist/src/domain/staging.js` | 入る | `isStagingLifecycleScanPath`が新しくexportされる。既存2 exportの判定結果は不変 |
| `src/domain/scan-boundary.ts`、`dist/src/domain/scan-boundary.js` | 入る | 適用範囲の説明が変わる。判定は持たない |
| `scripts/`の2 file | 入らない | project層の検査であり配布物に含まれない |
| `docs/specs/`の4 file | 入らない | ASC自身の仕様と追跡であり配布物に含まれない |
| `test/`の2 file | 入らない | 配布境界外の検証資産である |

判断: 配布物を更新した

根拠: `src`は配布されるpackage所有資産であり、export一覧が1件増える。**追加のみで後方互換であり、既存exportの判定結果は変わらない。** reviewerが基点の実exportを抽出して現行distに欠落0件であることを確認した。

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | 本artifactの作成時点ではなし。提出時にPRとCI runで観測する |
| reviewerがPR author・実装commit authorと異なる | `H_impl` authorとは異なる。reviewerはcodex CLIの独立sessionである。**PR authorとの比較はPR作成後に観測する** |
| 観測したreview commentとapprovalの件数 | 要件レビュー1ラウンド（REQUEST_CHANGES、High 3・Medium 3）、設計レビュー1ラウンド（REQUEST_CHANGES、High 1・Medium 2・Low 2）、実装レビュー1ラウンド（REQUEST_CHANGES、High 1・Medium 2）。**すべて是正済みでCriticalは0件** |

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/02_要件/04_仕様・品質管理要件.md`のREQ-SQ-017とREQ-SQ-032、`00_要件一覧.md`、`docs/specs/15_要件追跡/00_追跡表.md`、`01_変更履歴.md`
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: **満たす。** 用語を追加していない。
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: **満たす。** M-01で矛盾する旧規定を解消した。
- 要件・変更・SCN・テストの追跡: REQ-SQ-017、AC-SQ-017、SCN-UNIT-SCNSCOPE-010から015。`trace:check`のorphanが要件・SCN・実装とも0件。
- `no-spec-impact`の場合の限定的根拠: 該当なし。
- UI・トークンの判断: DC-UXとDC-TOKENSはともにnot-applicable。

## 11. 総合判定と再開地点

- 未解決Critical/High: なし。
- Medium/Lowの記録: H-01、M-01、M-02はresolved。L-01はvalidとして記録のみとする。追加loopとgate停止を生じさせない。
- 判定: approved
- 新しい権限が必要な事項: なし。push、PR作成、mergeはユーザーが2026-09-07に対話上で明示承認している。**出典は当該対話の指示であり、GitHub上の識別子や監査logの参照を持たない。この文書を操作許可の正本にしない。** merge承認は`pr.merge`だけを許可し、branch削除、Issue終了、release、公開、cleanupを連結しない。mergeは`--merge`で行い`--squash`を使わない。
- 残存リスク: **一時ライフサイクル領域内へ誤って置かれた実featureを配置検査で検出しなくなる。** これは受け入れる契約の縮小であり、`checkLifecycleIgnore`は領域内の追跡済みfileを拒否するが**未追跡の誤配置featureまで代替検出する保証ではない。** L-01により、Windowsでの区切り正規化はこのrepositoryのCIとローカルでは検証されない。
- **主張の範囲。** 本件が担保するのは、一時ライフサイクル領域4件の配下でSCN配置違反を報告しないこと、除外領域外・所定location・4領域の近似path・区切り文字をfile名に含む領域外pathの扱いが固定期待集合と一致すること、生のpath入力への入力契約、`isStagingLifecyclePath`の契約維持、REQ-SQ-017の表示本文の記述である。**要件本文検査側の意味的偽陽性は対象外であり、別Issueへ分ける。**
- 次に許可される操作: 本artifactをH_implの後に単独commitしてH_finalとし、`audit:check`と`verify:distribution`をH_finalで実行する。その後にbranchをpushしPRを作成する。
- 次回の再開地点: H_finalでの配布gate実行。
