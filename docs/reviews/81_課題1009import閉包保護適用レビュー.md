# 81 課題1009 trusted validatorのimport閉包保護 適用レビュー

> 状態: `candidate-verified`（外部承認待ち）。登録済みproposal `TQP-PROTECTED-CLOSURE-001` を適用し、`src/lib/entrypoint.ts`と`src/lib/security.ts`を`PROTECTED_FILES`へ加える変更を、merge済みdefault branch headを比較基点として固定した内部監査証拠である。**`valid: true`は候補が既定branchを基準に検証を通過したことを示すだけであり、既定branchへの反映を示さない。**独立reviewの承認は自己申告せず、CIとPR reviewの外部証拠で確定する。

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象Issue | #1009 |
| 比較基点 | `388fdd5eec32e370382b0bf8ed00b3da764b3a40` |
| H_impl | `e02f67654cb46d286b27bff65063c96011493b72` |
| reviewer | claude（旧validatorと新validatorのA/B実測と変異試験で独立に確認） |
| 実施日 | 2026-08-27 |
| ラウンド数 | 1 |
| Step chain | 迂回: 手書き運用で進めており、workflow recordを経由していない |
| 仕様の所有箇所 | `docs/specs/02_要件/04_仕様・品質管理要件.md` REQ-SQ-026（本PRで新設）。`.agent-skill-chain/docs/00_運用ポリシー.md`のauthority分離とfail-closed不変条件。**本変更はauthority分離を強める方向であり、同文書が縮小対象外とする手段を弱めない** |
| 成果物行数 | **総変更量で測る。製品64行**（`scripts/check_project_quality.ts` +62、`package.json` +1 / -1）。支援層はtest 123行、仕様17行 |
| 縮小の先行評価 | 新しい機構を作っていない。`PROTECTED_FILES`は既存の列挙で追加は2行、proposal適用の経路は#978・#1002の前例をそのまま使う。**bootstrap拘束は既存の状態へ関数1本を足しただけで、候補codeを実行しない。**`import閉包のhash manifestをpinする案`（案B）も評価したが、新機構でありowner承認範囲外のため採らなかった。案Bは中期の本命としてIssueに残す |

### 支援層が製品を超えていることの評価

test 123行が製品64行を上回る。**運用ポリシーの「支援層が成果物を大きく超えたら設計を疑う」に照らして評価した。**

- 内訳の大半は候補tree複写の足場26行で、`test/steps/lockfile-protection.steps.ts`と重複している。**共通化は別Issue向きと判断した**
- 3 scenarioはfail-closedなauthority拘束の3分岐（改竄・正常・読み取り失敗）を固定しており、削ると分岐が無検証になる
- **運用ポリシーは、縮小対象からfail-closed不変条件に属する手段を除いている**
- 比は1.9倍であり、過去の破綻例（8行の成果物に172行の支援層＝21倍）とは桁が異なる

S81-L-03として記録する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| **保護機構が非保護moduleへ依存** | `scripts/check_project_quality.ts:218`、`:391-392`、`:415` | `stableJson`（非保護`src/lib/security.ts`）が保護snapshotのhash計算、proposal契約の照合、版targetの算出に使われる | 既存コード |
| 非保護実行面の規模 | `policy.ts` 1749行、`types.ts` 229行、`security.ts` 177行、`entrypoint.ts` 29行 | 計2184行が`PROTECTED_FILES`の外 | 実行観測 |
| **保護の効果（A/B）** | 同一の改竄候補（`security.ts`へ1行追記、版据え置き） | **旧validator（保護10件）は`valid: true`、新validator（12件）は`valid: false`** | 実行観測 |
| **bootstrapの成立** | 保護追加と同じPRで対象を改竄した候補 | **是正前は`valid: true`で通る。**backdoorを封印できた | 実行観測 |
| **bootstrap拘束の効果** | 次の版上げを模した候補 | `新たに保護対象へ加えるfileを同じPRで変更できません: src/types.ts`を名指しで拒否 | 実行観測 |
| 誤検出しないこと | 改竄なしの保護追加 | 拘束は何も報告しない | 実行観測 |
| 読み取り不能時 | `PROTECTED_FILES`を読めない形へ改変した候補 | `候補のPROTECTED_FILESを読み取れません`で拒否 | 実行観測 |
| after hash | `check_project_quality.ts` | `3f910b02…`。登録済みproposalと**byte単位で一致** | 実行観測 |
| 版targetのhash | `qualityContractVersion: 8` | `2c624232…`。登録済みproposalと一致 | 実行観測 |
| **適用の判定** | `--root=候補 --trusted-root=merge済みmain` | `valid: true` | 実行観測 |
| bootstrap規約の遵守 | `git diff --name-only origin/main` | **保護対象2 fileは無変更** | 実行観測 |
| 静的・契約検査 | 10種 | すべてexit 0 | テスト出力 |
| テスト | `npm test` | 1006 scenario全通過 | テスト出力 |

## 変更ファイル個別監査

| path | status | owner | target layer | 責務・配置 | 依存・循環 | 仕様・追跡 | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `scripts/check_project_quality.ts` | M | package owner | 品質契約validator | 保護対象へ2件追加し、bootstrap拘束を足す | 追加依存なし | REQ-SQ-026、QLT-PROTBOOT-001 | 逆変換で戻るが**proposalの二段階を要する** | pass |
| `package.json` | M | package owner | 品質契約 | 契約versionを7→8へ | 変更なし | Issue #1009 | 版の減少は受理されない。**rollbackは前進で行う** | pass |
| `test/features/unit/protection-bootstrap.feature` | A | package owner | test | 反例3件 | 実装へ単方向 | SCN-UNIT-PROTBOOT-001〜003 | fileの削除で戻る | pass |
| `test/steps/protection-bootstrap.steps.ts` | A | package owner | test | fixtureとstep定義 | 実装へ単方向 | 同上 | fileの削除で戻る | pass |
| `test/steps/unit.steps.ts` | M | package owner | test | 保護fixtureの複写リストへ2件追加 | 実装へ単方向 | 既存SCN-UNIT-QUALITY | 2行の除去で戻る | pass |
| `docs/specs/02_要件/00_要件一覧.md` | M | 利用側 | 仕様 | REQ-SQ-026の登録 | 参照のみ | REQ-SQ-026 | 1行の除去で戻る | pass |
| `docs/specs/02_要件/04_仕様・品質管理要件.md` | M | 利用側 | 仕様 | REQ-SQ-026の本文 | 参照のみ | 同上 | 節の除去で戻る | pass |
| `docs/specs/11_非機能/01_品質要件.md` | M | 利用側 | 仕様 | QLT-PROTBOOT-001の新設 | 参照のみ | 同上 | 1行の除去で戻る | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | 利用側 | 仕様 | REQ-SQ-026行の追加 | 参照のみ | 同上 | 1行の除去で戻る | pass |

## 2. 受け入れ条件の確認

| AC（Issue #1009） | 結果 | 証拠 |
|---|---|---|
| `src/lib/entrypoint.ts`と`src/lib/security.ts`を保護対象へ加える | 充足 | after hashが登録済みproposalとbyte単位で一致 |
| 品質契約versionを7→8へ進める | 充足 | `--trusted-root`が`valid: true` |
| 保護が効くことを実測で示す | 充足 | 旧validatorは`valid: true`、新validatorは`valid: false` |
| bootstrapを機構で拘束する | 充足 | `validateProtectionBootstrap`。変異試験3方向で確認 |
| `check_conformance.ts`・`types.ts`・`policy.ts`は入れない | 充足 | 変更頻度を実測して選別。owner決裁の範囲どおり |
| 既定branchへのproposal登録（PR-1） | 充足済み | PR #1011でmerge済み |

### 2.1 開発考慮事項の適用判定（必須）

| ID | 判定 | 確認 |
|---|---|---|
| DC-PRIVACY | not-applicable | hashとpath名だけを扱う |
| DC-OBSERVABILITY | applicable | **拒否理由をpath名指しで返す。**無言で通さない |
| DC-UX | not-applicable | UIを持たない |
| DC-TOKENS | not-applicable | UI要素を持たない |

## 3. 肯定的評価

- **保護機構自身の依存を閉じた。** `security.ts`の`stableJson`は保護snapshotのhash計算そのものに使われる。**これを細工されると保護機構が破れる。**
- **効果をA/Bで実測した。** 保護対象を増やす変更は「増やした」だけで安心しやすい。旧validatorが同じ改竄を`valid: true`で通すことを観測してから登録した。
- **bootstrapを規約から機構へ移した。** 登録PRのレビューでは「適用PRは列挙追加だけを行う」と書いていたが、**それは運用規約であって機構ではなかった。**自動reviewの指摘を実測で確認し、拘束を足した。
- **費用を実測して選別した。** 2026-07以降の変更頻度が1 commitの2件だけを入れ、21 commitの`check_conformance.ts`は外した。
- **候補のcodeを実行しない。** 候補側の一覧はsourceから静的に読み取る。trusted validatorがcandidateを実行しないという既存の境界を守っている。

## 4. 敵対的評価

| 反例 | 結果 |
|---|---|
| 保護対象を増やしただけで実効が無い | **不成立。**A/B実測で旧validatorが同じ改竄を通すことを確認した |
| **保護追加と同じPRで対象を改竄できる** | **是正した。**`validateProtectionBootstrap`が拒否する。**ただし本版の適用には窓が残る**（S81-M-01） |
| 拘束が誤検出して正常な保護追加を止める | **不成立。**改竄なしの保護追加では何も報告しない。変異試験で確認 |
| 一覧を読めない形へ書き換えて拘束を素通りする | **不成立。**読み取り不能は拒否する（fail-closed） |
| 保護対象2 fileを本PRで改竄している | **不成立。**`git diff --name-only`で無変更を確認した |
| これで閉包が閉じる | **不成立。**`policy.ts` 1749行と`types.ts` 229行は開いたままである。**案Aは第一歩であり終着ではない**（S81-M-02） |
| 版を戻せる | **不成立。**版の減少は受理されない。rollbackは前進でのみ行える |
| **この拘束自体をcandidateが消せる** | **成立する。**`check_project_quality.ts`は保護fileなのでproposalが要るが、**testは保護対象外**であり同一PRで消せる。#1002のS78-M-01と同根で、本PRで悪化しない |

## 5. 指摘

| ID | 深刻度 | 内容 | 状態 |
|---|---|---|---|
| S81-M-01 | Medium | **本版（7→8）の適用そのものには窓が残る。**拘束はbase側で走るため効くのは次の版上げからである | 未是正。**構造的で回避不能。**保護対象を初めて加える回には必ず窓が開く。適用PRの差分を最小に保ち、対象2 fileが無変更であることを実測で示した |
| S81-M-02 | Medium | `src/domain/policy.ts`（1749行）と`src/types.ts`（229行）が保護境界の外に残る | 未是正。**構造的。**列挙への逐次追加はもぐら叩きであり、案B（import閉包のhash manifest）をIssue #1009 に中期の本命として残す |
| S81-L-01 | Low | rollbackは版の前進でしか行えない | 未是正。proposalの`rollback`欄へ手順を記載済み |
| S81-L-02 | Low | `protectedSnapshot`が保護対象の欠損で例外終了する | 未是正。**保護fileのため8→9でしか直せない。**別Issueへ分離する |
| S81-L-03 | Low | 支援層123行が製品64行を上回る（1.9倍） | 未是正。**足場26行が既存testと重複している。**共通化は別Issue向き |

### ラウンド予算

ラウンド1で収束した。未解決のCritical/Highは0件。上限3ラウンドに対して2ラウンドの余裕を残している。

## 6. ラウンド固有の確認

### ラウンド1

全評価基準（肯定: 正しさ・価値・実現可能性・整合性・保守性／敵対: 反例・失敗経路・境界値・悪用・安全性・データ損失・rollback・範囲漏れ）を確認した。Medium 2、Low 3。新規Critical/High 0件。判定 **candidate-verified（自動reviewを待つ）**。

**本PRは登録PR（#1011）のラウンド2で自動reviewが出したHighへの回答を含む。** 「bootstrap回避が規約であって機構でない」という指摘を実測で確認し、owner決裁を経て拘束を本proposalへ含めた。

## 7. テスト結果

| 層・検査 | コマンド | 件数 | 判定 |
|---|---|---|---|
| 形式 | `npm run format:check`、`npm run docs:format`、`npm run test:format` | exit 0 | pass |
| 静的 | `npm run lint`、`npm run typecheck`、`npm run source:check` | exit 0 | pass |
| 契約 | `npm run project:quality`、`npm run trace:check`、`npm run architecture:check`、`npm run package:check` | exit 0 | pass |
| 統合 | `npm test` | 1006 scenario全通過 | pass |
| 既定branch比較 | `--root=候補 --trusted-root=merge済みmain` | `valid: true` | pass |

保護の効果。同一の改竄候補（`src/lib/security.ts`へ1行追記、版は据え置き）を2つのvalidatorで判定した。

| validator | `PROTECTED_FILES` | 判定 |
|---|---|---|
| 比較基点のもの | 10件 | **`valid: true`（素通り）** |
| 本PRの適用後 | 12件 | **`valid: false`（拒否）** |

bootstrap拘束の変異試験。

| 入力 | 期待 | 実測 |
|---|---|---|
| 保護追加＋同一PRで改竄 | 拒否 | **`新たに保護対象へ加えるfileを同じPRで変更できません: src/types.ts`** |
| 保護追加のみ（改竄なし） | 誤検出しない | **検出なし** |
| `PROTECTED_FILES`を読めない形へ改変 | fail-closed | **`候補のPROTECTED_FILESを読み取れません`** |

hashの突合。

| 対象 | 登録済みproposalのafter | 本PRの実測 | 一致 |
|---|---|---|---|
| `scripts/check_project_quality.ts` | `3f910b022f50052651b3684b7b95e99a4d8acbdb2fc696fabe8cfb4898d1c292` | 同一 | 一致 |
| `agentSkillChain.qualityContractVersion` | `2c624232cdd221771294dfbb310aca000a0df6ac8b66b696d90ef06fdefb64a3` | 同一 | 一致 |

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `scripts/check_project_quality.ts` | 入らない | `files`は`dist/bin/`、`dist/src/`、`.agent-skill-chain/`の一部、`README.md`、`AGENTS.md`を列挙する。`scripts/`は含まない |
| `package.json` | manifestとして入る | `agentSkillChain.qualityContractVersion`が7→8。**consumerの実行経路は変わらない** |
| `test/`、`docs/specs/` | 入らない | `files`が列挙しない |

判断: 配布物を更新しない

根拠: `src/`を触っておらず`dist/`の内容は変わらない。consumerが観測できるCLI、公開API、schema、templateのいずれも変化しない。`package.json`の`agentSkillChain.qualityContractVersion`はmanifestとして配布されるが、これは本repositoryの`.github/trusted-quality-proposals.json`との照合に使う値であり、consumerの実行経路から参照されない。`npm run package:check`がexit 0であることを確認した。

## 9. 独立reviewの成立

| 条件 | 充足 |
|---|---|
| reviewerが実装担当の判断を入力に持たない | 充足。旧・新validatorのA/B実測、変異試験3方向、hash突合だけを入力にした |
| 各ラウンドの判定と根拠が原文引用を伴う | 充足。`PROTECTED_FILES`と`stableJson`の使用箇所を引用 |
| 有限ラウンドで終了する | 充足。1ラウンドで収束 |
| 外部証拠 | PR作成後のCI結果と自動reviewで補う |

**bootstrapが実際に成立することを実測してから拘束を足したことが、この変更で最も効いた判断である。** 登録PRでは「適用PRは列挙追加だけを行う」という自分の運用規約を根拠に「不成立」と書いていた。**規約は機構ではない。**

## 10. 仕様整合性

`docs/specs/`を4箇所更新した。

| file | 更新内容 |
|---|---|
| `02_要件/00_要件一覧.md` | REQ-SQ-026を登録 |
| `02_要件/04_仕様・品質管理要件.md` | REQ-SQ-026の本文。拘束の内容、候補codeを実行しないこと、**初回に窓が残ることを明記** |
| `11_非機能/01_品質要件.md` | QLT-PROTBOOT-001を新設 |
| `15_要件追跡/00_追跡表.md` | REQ-SQ-026行を追加 |

保護対象の集合そのものは要件で定めない。**集合の拡大はauthority boundaryの変更であり、proposalの二段階手順とowner決裁を要する**ことを本文へ書いた。

## 11. 総合判定と再開地点

**判定: candidate-verified（外部承認待ち）**

- 未解決Critical: 0件
- 未解決High: 0件
- 未解決Medium: 2件（S81-M-01は回避不能、S81-M-02は案BとしてIssueに残す）
- 記録したLow: 3件

再開地点: ステップ11（PR作成）。**merge後に`protectedSnapshot`の例外終了を別Issueへ起票する。**
