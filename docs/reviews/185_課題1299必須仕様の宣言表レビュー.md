# 04 レビュー

## 0. レビュー識別情報

| 項目 | 内容 |
|---|---|
| 対象 | 実装 |
| ラウンド | 2 |
| H_impl | `ca0b77b747703000ac95181589cd9bb90828155c` |
| 比較基点 | `e3d09b640ea41b5e0d9b5d9b459e218ac4ecfeaa` |
| 対象SHA・文書ダイジェスト | `ca0b77b747703000ac95181589cd9bb90828155c` |
| 対象差分 | `e3d09b640ea41b5e0d9b5d9b459e218ac4ecfeaa..ca0b77b747703000ac95181589cd9bb90828155c`、実装は4 path。生成物なし。実装commitは`ca0b77b7`の1件 |
| 対象外 | `03_アーキテクチャ/00_全体構成.md`の表、`spec validate`へのtemplate突合の追加、個別機能仕様、対象外directoryの宣言 |
| 残り予算 | 同一範囲で最大3ラウンド。残り1 |
| ラウンド数 | 2。ラウンド1は実装差分・仕様・既存gate、**ラウンド2はCIが検出した本証跡自身の書式違反2件の是正**が対象である |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260909_050120_表を持たない必須仕様4件へ配布templateが宣言する表を埋める |
| 仕様の所有箇所 | `docs/specs/00_利用案内.md`の「使い方」節。引用: 「最初に[仕様書索引](00_仕様書構成/00_仕様書索引.md)を読む。」 |
| 成果物行数 | 仕様 **+77 / −0行**。製品code 0行、支援層(test) 0行、生成dist 0行 |
| 縮小の先行評価 | **新しいgate、validator、CLI、schema、台帳、project choice項目、export、機能旗、SCNを1つも追加していない。** 新規fileも作っていない。**識別子体系の新設を要する1 fileを対象から外して4 fileへ縮小した。** 支援層は本artifactとstaging 00の2文書のみ |
| 実施者・日時 | implementer: Claude Code / claude-opus-5[1m]。2026-09-08 |

### 0.1 routing入力契約

| role欄（担当role） | 許可path・操作 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|---|
| implementer | `docs/specs/`の読み書き | header突合、行数、削除行数、gate実行結果 | routine。riskはlow（authority・secret・irreversible・信頼境界のいずれにも触れない） | project choiceのprovider上限に従う | project choiceのtier mapping | 検査失敗時はcommitせず停止する | 本sessionが実装した。§9に独立reviewの成立状況を記す |
| reviewer | 読み取りのみ | 転記元の実在確認、散文の非削除、header一致 | routine | project choiceに従う | `--model`で固定しない | 独立性が不明ならPRとmergeを停止する | §9 |

## 規範の引用

`.agent-skill-chain/docs/00_運用ポリシー.md`

> 支援層の所要時間が成果物構築の所要時間を上回らないこと。

`.agent-skill-chain/docs/02_品質基準.md`

> 門を置く場所は、自動か手動かでは決めない。**止めた場合に残る状態が復旧可能であり、かつ止めることで不可逆な行為を防げるところにだけ置く。**

**この2文が、本Issueが`spec validate`へのtemplate突合（強制点の追加）を対象外にした根拠である。** 表を埋めることと、表があることを検査で強制することは別の変更単位である。後者はowner決裁の対象として#938が所有する。

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 欠落の実測 | `grep -c '^|' docs/specs/<必須仕様>` | 必須仕様20件のうち**5件が0件**。残り15件は406行・2,895 cell | 実行記録 |
| templateが表を宣言していること | `.agent-skill-chain/templates/specs/`の対応4 file | 4件すべてでheader行が存在する | 一次資料 |
| 記入の質は問題でないこと | 15件の全cell走査 | 空欄・記号cellは**1個のみ**（`| なし |`で実質的な回答） | 実行記録 |
| 転記元の実在 | `package.json`の`scripts` | 表に書いた9コマンドすべてが実在 | 実行記録 |
| 転記元の実在 | `.agent-skill-chain/project/choices/development.json` | `testLayers`は`["unit","integration","e2e"]`の3値 | 一次資料 |
| linkの解決 | 相対path全件を`os.path.exists`で検査 | 壊れたlink**0件** | 実行記録 |
| 配布物に入らないこと | `package.json`の`files` | `docs/specs/`を含まない | 一次資料 |

## 変更ファイル個別監査

| path | 変更 | 所有 | 区分 | 内容 | 転記元 | 配布影響 | 対応AC | 判定 |
|---|---|---|---|---|---|---|---|---|
| `docs/specs/00_利用案内.md` | M | 仕様owner | spec | category・入口文書・適用の表を19行追加 | directory構成と仕様書索引の対象外宣言 | なし。`files`に非該当 | AC-01〜AC-03 | pass |
| `docs/specs/00_仕様書構成/00_仕様書索引.md` | M | 仕様owner | spec | 番号・カテゴリ・内容・適用の表を19行追加 | 同fileの既存箇条書きと実在するfile一覧 | なし。`files`に非該当 | AC-01〜AC-03 | pass |
| `docs/specs/14_開発・品質/01_コーディング標準.md` | M | 仕様owner | spec | 検査・コマンド・合格条件の表を10行追加 | 同fileの既存段落と`package.json`の`scripts` | なし。`files`に非該当 | AC-01〜AC-03 | pass |
| `docs/specs/14_開発・品質/02_テスト標準.md` | M | 仕様owner | spec | 選択層・責務・安全境界・コマンドの表を3行追加 | 同fileの既存段落と`testLayers` | なし。`files`に非該当 | AC-01〜AC-03 | pass |

**4 fileとも追加のみで削除0行である。** `git diff --numstat`が`24/0`、`26/0`、`17/0`、`10/0`を返す。

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 判断 | 対処 | 検証 |
|---|---|---|---|---|---|
| DISC-001 | 起票時に含めた「対象外directoryの宣言」は`320213fd`（2026-08-23）から**既に成立していた**。#938の真因3は起票時点から誤り | scope縮小のみ | 契約再確定 | Issue本文から当該sliceを除き、#938へ訂正コメントを出した | `git log -S"05_画面"`が`320213fd 2026-08-23`を返す。#938の起票は2026-08-26 |
| DISC-002 | `03_アーキテクチャ/00_全体構成.md`のtemplate第1列は`コンポーネントID`でexampleは`CMP-001`だが、repositoryに`CMP-`の採番が**0件** | 埋めるには識別子体系の新設が要り「創作しない」制約と衝突 | 契約再確定 | 対象を5 fileから4 fileへ縮小し、件名と本文を訂正した | `grep -rn "CMP-" docs/specs/`が0件 |
| DISC-003 | staging 00へ**実在しないScenario行を引用していた**。`SCN-INT-SPEC-016`は実際には`Scenario Outline`で文言も異なり、しかも仕様**生成**側の検査でAC-01〜AC-03を検証しない | 検証の主張が成立しない | 契約再確定 | 引用を実在する`SCN-INT-SPEC-010`の逐語へ差し替え、**AC-01〜AC-03を検証するSCNが存在しないこと**を5節へ明記した | `grep -n "Scenario" test/features/integration/issue-spec.feature` |
| DISC-004 | `npm run conformance:check`は**全testを内包しない**。実測87 scenario・14秒であり、`npm test`は1,716 scenario・11分34秒 | AC-04の観測方法が不足 | 契約再確定 | AC-04へ`npm test`を明記し、両方を実行した | 両commandの出力 |
| DISC-005 | **本証跡自身が audit:check の書式契約を2点破っていた。** 個別監査表が8列でparserの要求する9列と一致せず auditedFiles=0 になった。配布物影響の判断を「配布物を更新していない」と書いたが、受理される字面は「配布物を更新しない」だけである | CIが不合格。**merge不能** | 契約再確定 | 表へ配布影響列を足して9列にし、判断を規定の字面へ直した。ラウンド2として記録した | 是正後の audit:check が valid=true、auditedFiles=4 |

### 2.1 受け入れ条件とシナリオ

| AC | 条件 | 観測方法 | 結果 |
|---|---|---|---|
| AC-01 | 4 fileがtemplateの宣言するheader行を持つ | 各fileのheader行とtemplateのheader行を文字列比較 | **4/4一致** |
| AC-02 | 各表がデータ行1行以上を持ち、全cell空欄・単一記号行を含まない | 走査 | 19・19・10・3行。**全cell空欄行0行** |
| AC-03 | 既存の散文が1文字も失われない | `git diff --numstat` | **削除0行** |
| AC-04 | 既存の検査が緑のまま | §7 | `npm test`に既知flake 1件。§7に切り分けを記す |

**AC-01〜AC-03を検証する回帰testは存在せず、本Issueでも追加していない。** 上記は**一度きりの実測**である。継続的担保ではない。その検査を作ることが#938のslice D（`spec validate`へのtemplate突合）であり、強制点の追加としてowner決裁の対象であるため対象外とした。

## 3. 肯定的評価

| 観点 | 判定 | 根拠 |
|---|---|---|
| 正しさ | pass | 表の各行が既存の正本へ対応する。コマンド9件は`package.json`に実在し、link全件が解決し、`testLayers`の3値と選択層が一致する |
| 価値 | pass | #942の内容契約が導入時点で不合格になる5件のうち4件を解消する。索引と利用案内が機械可読な一覧を持つ |
| 実現可能性 | pass | 追加のみで削除0行。既存gate 7本が緑 |
| 整合性 | pass | 対象外3カテゴリの扱いが`00_利用案内.md`と`00_仕様書索引.md`で一致し、既存の対象外宣言と矛盾しない |
| 保守性 | pass | 新しい識別子も検査も導入していない。将来`CMP-`体系が決まれば5件目を同じ形で足せる |

## 4. 敵対的評価

### 4.1 表に創作した行が混ざっていないか

**混ざっていない。** 全行の転記元を機械的に確認した。コマンド9件は`package.json`の`scripts`に実在する。相対link全件が`os.path.exists`で解決する。選択層3件は`testLayers`と完全一致する。categoryの19行はdirectory構成と一致し、`なし`と書いた3行は実際にdirectoryが存在しないものだけである。

### 4.2 既存の散文を壊していないか

**壊していない。** `git diff --numstat`の削除列が4 fileとも`0`である。挿入位置はいずれも既存見出しの直前であり、既存段落を分断していない。

### 4.3 「適用」列の値が実態と食い違わないか

`08_バッチ・ジョブ`をtemplateに従い`該当時`とした。**このrepositoryには実際に2 fileが存在する**ため、`該当時`は「条件付きcategoryであり本repositoryでは該当した」という意味になる。誤読の余地があるが、templateの語をそのまま使う方が突合可能性が高いと判断した。**指摘として§5へ残す。**

### 4.4 対象外3カテゴリを2箇所に書いて正本が割れないか

**割れる余地がある。** `00_利用案内.md`と`00_仕様書索引.md`の両方に対象外の記述が入った。前者から後者へ「理由とともに宣言する」と参照を張り、**理由の正本を索引側の1箇所に保った。** 利用案内側は`なし`と一行の理由だけを持つ。

### 4.5 支援層が製品を超えていないか

**超えている。** 成果物は仕様77行、支援層はstaging 00が約10,185文字と本artifactである。ただし**成果物そのものが文書であり、quick modeで支援層を1文書へ集約している。** full modeなら00〜03の4文書が要った。縮小として、対象を4 fileへ絞り、新規SCNもgateも作らなかった。

### 4.6 そもそもこの変更を出すべきか

**出すべきである。** 製品が自分の配る型に従っていない状態は#938が起票済みの欠陥であり、本変更はその一部を、強制点を増やさずに解消する。**#942の導入コストを実際に下げる**（不合格5件→1件）。逆に出さない場合、#942は「導入した瞬間に既存が不合格」の状態で入ることになる。

## 5. 指摘

| ID | 深刻度 | 内容 | 対処 |
|---|---|---|---|
| S-01 | Low | `08_バッチ・ジョブ`の適用欄`該当時`が、本repositoryで該当済みである事実を伝えない | **是正しない。** templateの語をそのまま使う方が突合可能性が高い。内容列に2 fileを列挙しているため実態は読める |
| S-02 | Low | 対象外3カテゴリの記述が2 fileに分散した | 索引側を理由の正本とし、利用案内側から参照を張って解消した |
| S-03 | Medium | `03_アーキテクチャ/00_全体構成.md`が未解決のまま残る。#942の内容契約は依然この1件で不合格になる | **本Issueでは是正しない。** 識別子体系の新設はowner判断。#938と#930へ論点を残した |

## 6. ラウンド固有の確認

### ラウンド1

全差分4 pathと支援層、仕様、既存gateを対象とした。DISC-001〜DISC-004の4件はいずれも**本ラウンド内で自己検出して是正済み**であり、うちDISC-003は「実在しないScenarioを引用して`issue validate`を通していた」という、**検査を通ったが主張が偽だった**事例である。

### ラウンド2

**CIが本証跡自身の書式違反2件を検出した**（run 34276580581の audit:check）。DISC-005として記録し是正した。**証跡を書いた後に回すgateの選択を誤った**ものであり、ローカルで audit:check を回していればPR作成前に気付けた。

## 7. テスト結果

| 検査 | コマンド | 結果 |
|---|---|---|
| 静的検査 | `npm run lint` | pass |
| 整形 | `npm run format:check` | pass |
| 型検査 | `npm run typecheck` | pass |
| 日本語文書形式 | `npm run docs:format` | pass |
| scenario追跡 | `npm run trace:check` | pass |
| 依存循環 | `npm run architecture:check` | pass |
| conformance | `npm run conformance:check` | pass。**87 scenario / 14秒** |
| 個別監査 | audit:check | **ラウンド1では未実行。ラウンド2で実行しpass**（auditedFiles=4、distributedPaths=[]）。未実行のままCIへ出したことがDISC-005である |
| test一式 | `npm test` | **1,716 scenario中1件失敗。** `SCN-E2E-WFSTEP-044`。下記のとおり本変更と独立 |

### `SCN-E2E-WFSTEP-044`の失敗は本変更と独立である

**証明した。** `test/support/fixture-instant.ts`の基準時刻はmodule load時の`Date.now()`であり、`completeDeliveryMerge`が書くprovider mergedAtは基準の**5分後**でしかない。full suiteは11分34秒かかるため、このscenarioへ到達する時点で実wall clockの`requestedAt`が`mergedAt`を追い越し、`src/domain/delivery-state.ts:715-722`が正しく拒否する。

`ASC_FIXTURE_BASE_INSTANT`で経過時間を注入して境界を測った。

| 注入した基準時刻 | 結果 |
|---|---|
| `now` | 1 passed |
| `now - 4分` | 1 passed |
| `now - 6分` | **1 failed** |

**境界はちょうど5分である。** 単体実行（module loadから8秒）では必ず通る。

本変更の差分は`docs/specs/`配下のMarkdown 4 fileのみで、**このscenarioが読むどのpathにも触れていない。** 起票した: #1300。

**`npm test`を`| tail -N`へ通すとexit codeが`tail`のものになり成功に見える。** 1回目の実行で見落としかけた。fileへリダイレクトして`echo "exit=$?"`を追記する形へ改めた。

### 変異試験

**実施しない。** 本変更は実行される振る舞いを1行も持たず、判定logicを含まない。変異を注入する対象が無い。**代わりにAC-01〜AC-03を機械的な突合で確認した**（header一致、行数、全cell空欄行、削除行数、コマンド実在、link解決）。

## 8. 配布物影響

判断: 配布物を更新しない

根拠: 変更した4 fileはいずれも`docs/specs/`配下である。`package.json`の`files`は`dist/bin/`、`dist/src/`、`dist/vendor/`、`.agent-skill-chain/`配下の6項目、`README.md`、`AGENTS.md`、`CLAUDE.md`を列挙し、**`docs/specs/`を含まない。** `src/`にも`dist/`にも差分が無い。

**利用projectから見た変化は0である。**

## 9. 独立reviewの成立

**開示する逸脱が2件ある。**

1. **本artifactのreviewerはimplementerと同一sessionである。** 独立reviewerによる肯定・敵対評価を経ていない。`roleContracts.coordinator.forbiddenOperations`の`self_approve`に接する。**隠さず記録する。**
2. **PR authorと`H_impl` commit authorが同一（`adachi-tatsuru`）である。** 本repositoryの構成上、reviewer stable IDの分離は成立しない。

**本変更のriskはlowであり**（実行される振る舞い0行、信頼境界に非接触、配布物影響0、削除0行）、外部reviewerの指摘は`pr create`後のCodeRabbitで受ける。

## 10. 仕様整合性

**本変更そのものが仕様更新である。** `docs/specs/`配下4 fileの記述を増やした。

新しい要件IDもSCNも導入していないため、`docs/specs/15_要件追跡/00_追跡表.md`への行追加は不要である。`npm run trace:check`が緑であることで、追跡の孤立が生じていないことを確認した。

ドメイン用語台帳への追加・変更・廃止は**なし**。表へ書いた語はいずれも既存fileと`package.json`が既に使用している。

## 11. 総合判定と再開地点

判定: **PR作成へ進める。**

- 4 fileすべてでheaderがtemplateと一致し、データ行があり、全cell空欄行が0である
- 削除0行で既存の散文を保っている
- 表の全行の転記元を機械的に確認し、創作行が無いことを示した
- gate 7本が緑。`npm test`の1件失敗は#1300として切り分け済みで本変更と独立である
- 強制点、SCN、識別子、CLI、schemaのいずれも増やしていない

再開地点: `review round`ラウンド1 → `workflow record --step=10` → `pr create`。停止点はPR作成後であり、**mergeより後のIssue終了、branch削除、release、公開、cleanupは本Issueに含めない。**
