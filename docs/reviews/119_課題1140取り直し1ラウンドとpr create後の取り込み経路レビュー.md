# 119 課題1140 取り直し1ラウンドとpr create後の取り込み経路 実装レビュー

> 状態: `candidate-verified`（外部承認待ち）。**owner決裁2件を2026-09-03に取得したうえで実装した。**

## 0. レビュー識別情報

| 項目 | 値 |
|---|---|
| 対象 | 実装 |
| 対象Issue | #1140 |
| 比較基点 | `124446557c10c27995a6056472aba8b244b1e2b2` |
| H_impl | `0912a29305a79b5485048a9f72300e0ff966f524` |
| 対象差分 | domain 1件、unit feature、unit steps、配布規範文書2件、Step skill、template、仕様3件の10 file |
| 対象外 | 案A・B・C、`pr reanchor`とdelivery stateの再拘束経路、Step 11の封を開けること、有限性そのものの撤廃 |
| 残り予算 | Step 10の上限3ラウンドのうち1を残す。**取り直し1ラウンドは本変更が入る前のため使えない** |
| ラウンド | Step 10 ラウンド2 |
| ラウンド数 | 2（Step 10のラウンド1〜2）。**Step 3・7のreadiness checkは3ラウンド予算に数えない** |
| Step chain | 経由: .agent-skill-chain/tmp/issues/20260903_070732_収束後のHEAD移動へ取り直し1ラウンドを別枠で許し-pr-create後の指摘はfollow-up-Issueへ回す |
| モード | full（Q-01、Q-02、Q-03が偽。公開契約と保存形式を変え、fail-closed不変条件の実装定数を緩める） |
| 仕様の所有箇所 | `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-005 |
| 成果物行数 | 製品: `review-convergence.ts`で42 insertions・11 deletions。test: 111 insertions。配布文書: 4件で6行。仕様: 4行 |
| 縮小の先行評価 | 実施済み。**案A（新session連鎖）を諮問2件で棄却した。** store形式変更・supersedes graph・anchor継続性検査・連鎖再導出が恒常的な支援層として残り、本事象の救済にその全部が不要である。**新しいCLI操作もflagも足さない。** 変更は既存guardの上限値と比較演算子だけである。**Step 11の封を開けない。** 機構ではなく規約で扱う |
| authority | **緩める方向の変更であり、owner決裁を要した。** 決裁1「増やす: 収束後にHEADが動いたときの取り直しを1回だけ許す」、決裁2「別Issueで対応することを基本とする。明記してかまわない」。いずれも2026-09-03に明示承認 |
| 実施者・日時 | reviewer、2026-09-03（JST） |

### 0.1 routing入力契約

| role欄 | 必要証拠 | 必要model tier | provider欄 | model設定欄 | fallback欄 | 独立性証拠欄・非変更証拠 |
|---|---|---|---|---|---|---|
| reviewer | 肯定・敵対review、finding分類、変異試験 | standard（riskがhigh） | claude | Opus 5、effort high | 既存Scenarioが1件でも落ちたら停止する | implementerとreviewerはいずれも本sessionのAI agentであり、**Git commit authorも同一である。§9に実態どおり記録し、独立性が成立したとは主張しない** |

## 1. 入力証拠

| 証拠 | 参照先 | 観測結果 | 根拠種別 |
|---|---|---|---|
| 閉じているのはguard 1つであること | `src/domain/review-convergence.ts:447-452` | 収束済みsessionへの追加roundは**予算内なら既に許可されている**。閉じているのは`round > 3`だけ | 実測 |
| `pr create`後の封が別物であること | `src/adapters/workflow-journal.ts:418,430`、`src/adapters/evidence-reanchor.ts:78`、`src/domain/delivery-state.ts:610` | Step 11記録後はjournal追記不可。terminal delivery state後はStep 0〜10追記不可。`review reanchor`はdelivery state固定後に使用不可 | 実測 |
| 実地再現 | PR #1145 | reviewを2ラウンドで収束させ予算1を残して`pr create`。CodeRabbitの指摘が届き、`review round`は通ったが`workflow record --step=10`が`Step 11記録後はworkflow journalへ追記できません`で拒否 | 実行観測 |
| 諮問の一致 | codexとfableへ独立に諮問 | **両者が同じ限定2件と同じ最小手段を指摘した。** 案A・B・Cをいずれも棄却 | 実行観測 |
| owner決裁 | 2026-09-03のownerメッセージ | 決裁1「増やす: 収束後に HEAD が動いたときの取り直しを1回だけ許す。許可します」。決裁2「別Issueで対応することを基本とする。明記してかまわない」 | 一次資料 |
| 到達しない分岐の存在 | 変異試験 | round 4を収束後だけに開く条件を別分岐で実装したが、**外す変異が生存した**。`REVIEW_ROUND_BUDGET`到達後の非収束状態は`budget-exhausted`しか取り得ない | 実測 |
| 変異の有効性 | 変異5件 | 上限を5へ、statusを`===`へ、上限を3へ、parse上限を3へ、`budget-exhausted`判定の削除を**全件kill** | 実測 |
| 全gate | `npm test`、`conformance:check` | 1415 scenario、1399成功、0失敗、16 skip。project rule 20件、orphan 0件 | 実行観測 |

- dependency/authority/evidence graphにcycle、self-loop、unknown node、candidate自己評価、tracked artifact自己SHAがない: pass。定数 → guard → status導出 → parse の一方向。`advanceReviewSession`と`parseReviewSessionState`は互いを呼ばない。**保存側の`status`をauthorityにせず再導出する**
- `H_impl`が`H_final`のancestorで、その差分がreview artifactだけである: `H_impl`が直前commitであり、`H_impl..HEAD`の差分は本artifact 1 fileのみ
- reviewer stable IDがPR author/観測済み`H_impl` author stable IDと異なる: **異ならない。** §9に実態を記録した
- 既定branch追随: **実施済み。** 本artifactを作る前に`origin/main`へ追随し、他Issueの履歴行への削除が0件であることを`git diff origin/main`で確認した

## 変更ファイル個別監査

| path | 変更種別 | owner | target layer | 単一責務・配置根拠 | 依存方向・循環 | 仕様・AC・SCN | 安全・rollback | 個別判定 |
|---|---|---|---|---|---|---|---|---|
| `src/domain/review-convergence.ts` | M | package | package | 上限を定数2件へ集約し、guard・status導出・parseが参照する | 定数→3箇所の一方向。循環なし | AC-1140-01からAC-1140-04。REQ-WF-005 | revertで復旧する | pass |
| `test/features/unit/review-convergence.feature` | M | project | project | Scenario 2件。既存5件を1件も変えない | featureはstepsへ一方向。循環なし | SCN-UNIT-REVIEWCONV-006、007 | 同上 | pass |
| `test/steps/review-convergence.steps.ts` | M | project | project | When 2件、Then 3件。既存`004`のThenは拒否理由の変化に合わせて正規表現だけ直す | stepsはdomainへ一方向。循環なし | 同上 | 同上 | pass |
| `.agent-skill-chain/docs/02_品質基準.md` | M | package | package | 有限レビュー契約の正本。取り直し1ラウンドと`pr create`後の取り込み経路を述べる | 規範文書は実装を参照しない。循環なし | AC-1140-05 | 同上 | pass |
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | M | package | package | レビュー配置の節で同じ内容を短く述べる | 同上 | 同上 | 同上 | pass |
| `.agent-skill-chain/skills/step-10-review/SKILL.md` | M | package | package | 実行時に読む位置での再掲 | skillは正本へ一方向。循環なし | 同上 | 同上 | pass |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | M | package | package | 残り予算欄へ別枠であることを注記する | templateは正本へ一方向。循環なし | 同上 | 同上 | pass |
| `docs/specs/02_要件/01_ワークフロー要件.md` | M | package | spec | REQ-WF-005の本文を実装へ合わせる | 仕様は実装を参照しない。循環なし | REQ-WF-005 | 同上 | pass |
| `docs/specs/15_要件追跡/00_追跡表.md` | M | package | spec | SCN 2件を既存行へ足す | 追跡のみ。循環なし | 該当なし | 同上 | pass |
| `docs/specs/15_要件追跡/01_変更履歴.md` | M | package | spec | 棄却した案と到達しない分岐の削除を含めて記録する | 履歴のみ。循環なし | 該当なし | 1行の削除で復旧する | pass |

- 基準SHAとの差分path集合と表のpath集合が完全一致する: pass（`git diff --name-only origin/main H_impl`が10件）
- package層へproject固有値、project層へ汎用機構、spec/evidence層へ実行authorityを混入していない: pass
- 個別findingを修正した場合、そのファイルと隣接依存だけを再監査した: 修正は§2.0のとおり1件

## 2. 受け入れ条件の確認

### 2.0 実装中に発見した事実と前向きな対処

| 発見ID | 事実 | 影響 | 契約変更 | 対処 | Verification Evidence | 仕様反映 | 判定 |
|---|---|---|---|---|---|---|---|
| DISC-1140-DEAD-001 | **round 4を収束後だけに開く条件を別分岐として実装したが、到達しなかった。** `REVIEW_ROUND_BUDGET`到達後の非収束状態は`budget-exhausted`しか取り得ない | その分岐を外す変異が生存した | なし | 分岐を削除し、含意を担う`budget-exhausted` guardのdoc commentへ根拠を書いた | 変異「収束条件を外す」が構成上作れなくなったこと。変異「statusを`===`へ戻す」がkillされること | no-spec-impact。要件の内容は変わらない | pass |

**同型の誤りを本session内で2回した。** #1004 の `selectedWellFormed` も到達しない分岐だった。いずれも変異試験が検出した。

### 2.1 受け入れ条件とシナリオ

| AC ID | SCN ID | 実装 | テスト結果 | 判定 | 証拠 |
|---|---|---|---|---|---|
| AC-1140-01 | SCN-UNIT-REVIEWCONV-006 | `REVIEW_RECOVERY_ROUND`とguard | 合格 | pass | §7 |
| AC-1140-02 | SCN-UNIT-REVIEWCONV-006 | 同上 | 合格。round 5を拒否する | pass | §7 |
| AC-1140-03 | SCN-UNIT-REVIEWCONV-004 | `budget-exhausted` guard | 合格。**既存Scenarioがそのまま反例になる** | pass | §7 |
| AC-1140-04 | SCN-UNIT-REVIEWCONV-007 | status導出の`>=` | 合格 | pass | §7 |
| AC-1140-05 | 該当なし | 配布規範文書2件、Step skill、template | `docs:format`と`skills:check`が合格 | pass | §7 |

### 2.2 開発考慮事項の適用判定（必須）

| ID | 考慮事項 | 判定 | 理由 | 実装・検証証拠 |
|---|---|---|---|---|
| DC-PRIVACY | Privacy/Security by Design | applicable | **fail-closed不変条件の実装定数を緩める方向の変更である** | 変異5件を全件kill。`budget-exhausted`からの更新を1経路も開かない |
| DC-OBSERVABILITY | Secure Logging・Observability・運用可能性 | applicable | 「是正が証跡に残らない」欠落を埋めるための変更である | round 4が直前round digestへ連鎖することを`SCN-UNIT-REVIEWCONV-006`が固定する |
| DC-UX | Human-Centered UI/UX・アクセシビリティ | not-applicable | 人が触れるUIを持たないCLIと文書であり、出力はJSONとMarkdownだけである | `projectKind`が`cli` |
| DC-TOKENS | Design System・Design/Layout Token | not-applicable | UI componentもthemeも持たず、色・間隔・typographyの決定を含まない | `capabilities.designTokens`が`not-applicable` |

## 3. 肯定的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 正しさ | 要件と観測結果が一致するか | pass | 収束後のround 4が受理され、round 5が拒否される |
| 価値 | 利用者・運用上の目的を満たすか | pass | 収束後に届いた指摘の是正を証跡へ残せる。**ただし`pr create`より前に限る** |
| 実現可能性 | 実行環境・依存・権限で成立するか | pass | 定数2件と比較演算子1件の変更で成立する |
| 整合性 | 設計、コード、テスト、仕様が一致するか | pass | REQ-WF-005の本文が取り直しの条件と`pr create`後の経路を述べる |
| 保守性 | 責務、命名、変更容易性が妥当か | pass | 上限を定数へ集約し、3箇所の片方だけを変えられないようにした |

## 4. 敵対的評価

| 観点 | 確認内容 | 判定 | 根拠 |
|---|---|---|---|
| 反例 | 要件を破る入力・状態がないか | pass | 変異5件を全件kill |
| 失敗経路 | 外部失敗・部分失敗を安全に扱うか | pass | 既存のwrite-then-read-back経路と staging mutation lock を変更しない |
| 境界値 | 空、最大、最小、重複、Unicode等 | pass | round 4と5の双方をScenarioで固定した |
| 悪用 | **任意の1 pushで新品の予算をもらう** | pass | **`budget-exhausted`からは開かない。** `SCN-UNIT-REVIEWCONV-004`が固定し、変異「`budget-exhausted`判定の削除」がkillされる |
| 悪用 | 取り直しラウンドの終端を`active`のまま残して上限を超える | pass | status導出を`>=`にした。変異「statusを`===`へ戻す」がkillされる |
| 悪用 | 保存済み`status`を`converged`へ書き換えて取り直しを開く | pass | `parseReviewSessionState`が全roundを先頭から再導出して照合する。**保存側をauthorityにしない** |
| 安全性 | 認証、承認、秘密情報、Zero Trust | pass | **緩める方向の変更をowner決裁なしで実施しなかった。** 決裁前は諮問結果の記録に留めた |
| データ損失 | 上書き、削除、部分公開、履歴消失 | pass | 既存の保存済みsessionは全て3件以下であり読み取り互換である |
| ロールバック | 復旧参照、状態保持、再開可能性 | pass | revertで戻る。配布digestが変わるためtagはrevertで取り消せない（§8） |
| 範囲漏れ | 呼び出し元、利用側、配布物、文書 | pass | 配布規範文書2件、Step skill、templateの4箇所へ同じ内容を置いた |

## 5. 指摘

| ID | 重大度 | 内容 | 証拠 | 影響範囲 | 対応 | 状態・分類 | 残存リスク |
|---|---|---|---|---|---|---|---|
| M-01 | Medium | **`pr create`後の取り込み禁止は機構で強制されない。** 規約であり、利用者が読まなければ従わない | 実装の読解 | 遵守 | **機構化しない。** 強制するにはStep 11の封を開ける必要があり、独立approverが確認した内容とmergeされる内容がずれる。reviewが受け持つ | valid（設計判断。record-only） | 残存する |
| M-02 | Medium | **取り直し1ラウンドが使えるのは`pr create`より前だけである。** PR #1145 の状況は決裁1では救済されない | 実測 | 適用範囲 | 決裁2の規約が救済する。両決裁が独立に必要であることを00へ記録した | valid（設計判断。record-only） | 残存する |
| L-01 | Low | 既存`SCN-UNIT-REVIEWCONV-004`のThenの正規表現を`3 round`から`budget終了済み`へ変えた | 実測 | 既存test | **拒否理由が変わったためであり、拒否そのものは変わらない。** 同じ入力が同じく拒否されることを確認した | valid（record-only） | 残存しない |

**Critical/High 0件。**

## 6. ラウンド固有の確認

### ラウンド1（Step 10、2026-09-03、candidate HEAD = `H_impl`）

- 全評価基準を確認した: はい。肯定5観点・敵対10観点をすべて評価した
- 指摘を確定した: はい。M-01、M-02、L-01はいずれも記録のみ
- 次ラウンド対象のCritical/High: **なし**
- blocking: 0件

### ラウンド2

- 本artifactのcommitでHEADが動くため、次ラウンドで再固定する

### ラウンド3

- 未実施。**予算1を外部reviewの是正のために残す**

## 7. テスト結果

**承認証拠は成功CI runである。** 手書きの件数表はそれ自体が自己申告であり、承認証拠にならない。

実行したcommand: `docs:format`、`test:format`、`lint`、`format:check`、`typecheck`、`source:check`、`trace:check`、`architecture:check`、`skills:check`、`build`、`package:check`、`npm test`、`conformance:check`、`audit:check`、`issue validate`

全layerの合計: **1415 scenario、1399 成功、0 失敗、16 skip。** `conformance:check`はproject rule 20件・orphan 0件で合格。

失敗またはskipがある層: **skipした16 scenarioはsemantic graphのGraphQLite assetが未設定な環境に起因する。** 本差分はこの経路に触れない。

対応する成功CI run: PR作成後に取得し、merge前に現`H_final`へ束縛する。

**変異試験の全量。**

| 変異 | 結果 |
|---|---|
| 上限を5へ引き上げる | kill |
| statusの`>=`を`===`へ戻す | kill |
| 上限を3へ戻す | kill |
| parse上限を3へ戻す | kill |
| `budget-exhausted`判定を削除する | kill |
| round 4を収束後だけに開く別分岐を外す | **構成上作れなくした。** 到達しない分岐だったため削除した |

## 8. 配布物影響

| 変更path | 配布境界に入るか | 影響 |
|---|---|---|
| `src/domain/review-convergence.ts` | **入る**（`dist/src/`として） | ラウンド上限が3から4へ変わる |
| `.agent-skill-chain/docs/02_品質基準.md` | **入る** | 有限レビュー契約の本文が変わる |
| `.agent-skill-chain/docs/01_開発ワークフロー.md` | **入る** | レビュー配置の節が変わる |
| `.agent-skill-chain/skills/step-10-review/SKILL.md` | **入る** | 実行時の指示が加わる |
| `.agent-skill-chain/templates/issue/04_レビュー.md` | **入る** | 残り予算欄の注記が加わる |
| `test/features/unit/review-convergence.feature` | 入らない | `files`に`test/`を含まない |
| `test/steps/review-convergence.steps.ts` | 入らない | 同上 |
| `docs/specs/02_要件/01_ワークフロー要件.md` | 入らない | `files`に`docs/specs/`を含まない |
| `docs/specs/15_要件追跡/00_追跡表.md` | 入らない | 同上 |
| `docs/specs/15_要件追跡/01_変更履歴.md` | 入らない | 同上 |

判断: 配布物を更新した

根拠: `package.json`の`files`は`dist/src/`、`.agent-skill-chain/docs/`、`.agent-skill-chain/skills/`、`.agent-skill-chain/templates/`を含む。**変更10 fileのうち5 fileが配布境界に入る。**

**releaseの起動と計画は分けて記録する。** `.github/workflows/release.yml`は`main`への`push`で起動する。その後`planAutoRelease`が配布digestの差分を見て計画を決める。**本変更は配布digestを変えるため、version bumpとtagが作られる見込みである。** merge後に実観測する。**npm公開は自動経路では発火しない。**

**ロールバックの対象範囲を分けて記録する。**

| 対象 | revertで戻るか | 実行者 | 手順 | 完了確認 |
|---|---|---|---|---|
| repository状態 | **戻る** | 実行役 | 打ち消しPRを通常mergeする | `git diff`が元の状態と一致する |
| 公開済みtagとGitHub Release | **戻らない** | repository owner | 削除せず次versionで前進する | `gh release list`に新tagが載る |
| npm registryの公開物 | **公開されない** | 該当しない | 本経路では公開しない | `npm view`に該当versionが無い |
| **4件のroundを持つ`review-session.json`** | **戻らない** | 利用者 | 旧版で読むと`1〜3件が必要です`で拒否される。新しいstagingで再走する | `review round`が通る |

## 9. 独立reviewの成立

| 項目 | 内容 |
|---|---|
| 独立reviewの外部証拠 | **本artifact作成時点では無い。** exact-headは`H_final`。immutable GitHub reviewは0件 |
| reviewerがPR author・実装commit authorと異なる | **異ならない。** implementerもreviewerも本sessionの同一AI agentであり、Git commit authorも同一である |
| 観測したreview commentとapprovalの件数 | 本session内のreview 2ラウンド（Critical/High 0、Medium 2、Low 1）。GitHub review 0件 |

**`02_品質基準.md`の「独立reviewが成立しない場合」に従う。** 同節は「既定は停止ではなく、無記録での通過の禁止である」と定める。

**ただし本件では、外部の独立した判断が3つ働いた。**

1. **codexとfableが独立に諮問へ答え、私が最初に提示した案Aを両者とも棄却した。** 私の当初案は採用されていない
2. **repository ownerが両決裁を明示承認した。** 緩める方向の変更をowner決裁なしで実施していない
3. **変異試験が到達しない分岐を検出した。** 私が書いた条件が無意味であることを、私より先に機械が示した

これらはreviewerの独立性の代替にはならないが、**同一actorの自己申告だけで通過していない証拠として記録する。**

**PR作成後の外部reviewを待つ。** 予算1ラウンドを残してある。**本変更が入った後も、`pr create`後の指摘は同じPRへ取り込まない。**

## 10. 仕様整合性

- 判定: updated
- 更新した仕様: `docs/specs/02_要件/01_ワークフロー要件.md`のREQ-WF-005、`docs/specs/15_要件追跡/00_追跡表.md`、`docs/specs/15_要件追跡/01_変更履歴.md`
- ドメイン用語台帳の候補・確定・現在有効な定義が一方向に追跡できる: pass。新規用語を導入していない
- 未定義語、同一コンテキスト内の重複定義、根拠なしの意味変更、表記揺れ、置換先なしの廃止がない: pass。`有限レビュー契約`という既存語をそのまま使う
- 要件・変更・SCN・テストの追跡: REQ-WF-005 → AC-WF-005 → SCN-UNIT-REVIEWCONV-006、SCN-UNIT-REVIEWCONV-007
- UI・トークンの判断: UIを所有しないためdesign token・layout tokenは対象外。ADRを追加しない

## 11. 総合判定と再開地点

- 未解決Critical/High: **なし**
- Medium/Lowの記録: M-01（`pr create`後の禁止は機構で強制されない）、M-02（取り直しは`pr create`より前だけ）、L-01（既存Thenの正規表現変更）
- 判定: approved（Step 10 ラウンド2）
- 新しい権限が必要な事項: mergeは別authority
- 残存リスク:
  1. **規約は機構ではない。** `pr create`後の取り込み禁止はreviewが受け持つ
  2. **4件のroundを持つsessionは旧版で読めない。** 旧版へ戻す運用は無いが、記録しておく
  3. **同型の到達しない分岐を本session内で2回書いた。** 変異試験がいずれも検出したが、変異試験を省略した領域では通る
- 次に許可される操作: push、PR作成、外部reviewの到着確認、必須check 2件の全緑確認、およびownerが承認したauthorityによる**通常merge commit方式**でのmerge。**squash mergeを使わない**
- 次回の再開地点: PR作成から
