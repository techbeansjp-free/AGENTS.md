# 汎用版 Storybook ルール更新レビュー

**レビュー実施日時**: 2026 年 1 月 31 日 12:41:57

**レビュー対象**: 汎用版 `AGENTS_STORYBOOK_RULES.md` への「ダークモード適用方法」「サイドバー表示順」「プレビューテーマ」セクションの追加

**レビュー基準**: [`AGENTS_REVIEW_RULE.md`](../.agents/レビュールール.md)

---

## レビュー概要

### レビュー目的

汎用版 Storybook 規約（`AI/AGENTS.md/AGENTS_STORYBOOK_RULES.md`）に、Tailwind + addon-themes を用いたダークモード適用手順、サイドバー連番表示のルール、プレビュー iframe の light/dark 切り替えの説明を追加した。本レビューでは、追加内容の整合性と既存規約との一貫性を確認する。

### レビュー対象ファイル

- [`AGENTS_STORYBOOK_RULES.md`](../.agents/Storybookルール.md)（汎用版・更新）
  - 「サイドバー表示順（連番で表示する）」の追加
  - 「プレビューでのテーマ（light/dark）」の追加
  - 「ダークモード適用方法」セクションの追加
  - 最終更新日の更新（2026 年 1 月 31 日）

---

## 変更内容の概要

### 追加された内容

1. **サイドバー表示順（連番で表示する）**
   - `main.ts` の `stories` 配列で読み込み順を明示するルール
   - 新規ストーリー追加時は該当階層ブロックの末尾にパスを追加するルール
   - 具体例（Radio.stories.tsx 追加時）の記載

2. **プレビューでのテーマ（light/dark）**
   - addon-themes の「Themes」でプレビュー側の light/dark を切り替える旨
   - 管理 UI の dark とプレビュー iframe の dark の違いの説明
   - 「ダークモード適用方法」セクションへのアンカーリンク

3. **ダークモード適用方法**（新規セクション）
   - 概要・目的・注意（管理 UI とプレビューのテーマの違い）
   - 必要なパッケージ（`@storybook/addon-themes`）
   - main.ts / preview.tsx の設定（withThemeByClassName、プレビュー背景、ThemeSync、parameters）
   - global.css・Tailwind の設定（`darkMode: 'class'`、トークン参照）
   - ツールバーで「Preview background」のみ非表示にする方法（manager-head.html）
   - addon-essentials と Storybook 10 に関する注意

4. **最終更新日**
   - 「2026 年 1 月 31 日（ダークモード適用方法・サイドバー表示順・プレビューテーマのセクションを追加）」に更新

### 変更の目的

- Tailwind CSS の `darkMode: 'class'` とデザイントークンを用いるプロジェクトで、Storybook プレビューにダークモードを正しく適用する手順を汎用版に明文化する。
- サイドバーの連番表示とプレビューテーマの扱いを、他プロジェクトでも参照できる形で揃える。

---

## レビュー結果サマリー

### ✅ 良好な点

1. **汎用性の維持**: プロジェクト固有の記述（PascalCase、AGENTS_CODING_RULES 参照等）は含まれておらず、他プロジェクトでも流用しやすい記述になっている。
2. **セクション配置**: 「Storybook 論理構造（固定）」の直後で、変更禁止事項の次に「サイドバー表示順」「プレビューテーマ」を置き、その後に「ダークモード適用方法」を配置しており、論理構造と実装手順の流れが分かりやすい。
3. **内部リンク**: 「プレビューでのテーマ」から「[ダークモード適用方法](#ダークモード適用方法)」へのアンカーリンクが正しく設定されている。
4. **既存セクションとの整合**: 既存の「各階層の役割」「デザイントークンの絶対ルール」等の記述と矛盾しておらず、追加のみで整合性が保たれている。
5. **optional の明示**: ThemeSync、manager-head.html、parameters.backgrounds.disable などは「（任意）」と明示され、必須と任意が区別されている。

### ⚠️ 改善が必要な点

**なし** — 今回の追加内容について、指摘事項はなし。

---

## 詳細レビュー結果

### 1. サイドバー表示順

- `main.ts` の `stories` で読み込み順を明示する旨が書かれており、実装者が従いやすい。
- 新規ストーリー追加時の「該当階層ブロックの末尾に追加」というルールが明確。
- 例として Radio.stories.tsx のパスが記載されており、パス形式の理解に役立つ。

### 2. プレビューでのテーマ（light/dark）

- addon-themes の「Themes」でプレビュー側を切り替える旨が簡潔に書かれている。
- 管理 UI の dark とプレビュー内の dark が別であることが注意として明記されている。
- 「ダークモード適用方法」へのリンクで詳細に誘導できている。

### 3. ダークモード適用方法

- **概要**: 目的と「管理 UI とプレビューは別」という注意がはっきりしている。
- **必要なパッケージ**: `@storybook/addon-themes` とバージョン方針が記載されている。
- **main.ts / preview.tsx**: withThemeByClassName、プレビュー背景、ThemeSync、parameters の 4 点が整理されている。
- **global.css / Tailwind**: トークン参照と `darkMode: 'class'` の記載があり、Tailwind 利用プロジェクトで再現しやすい。
- **manager-head.html**: Preview background ボタンのみ非表示にする方法がコード例付きで書かれている。
- **注意**: Storybook 10 と addon-essentials の扱いが一文で補足されている。

### 4. 既存規約との整合性

- クイックリファレンスや論理構造・階層の役割とは矛盾しておらず、追加のみで整合している。
- デザイントークンは「01_デザイン基盤が唯一の正」という既存ルールと、global.css でのトークン参照の説明が一致している。
- 参考資料・最終宣言など既存セクションは変更されておらず、体裁も既存レビューと同様。

---

## 整合性確認

### ドキュメント間の整合性

- 汎用版のみの更新のため、同一ファイル内のセクション間のリンク・参照が正しいことを確認した。
- 「プレビューでのテーマ」から「ダークモード適用方法」へのアンカーリンクは、Markdown の見出しと一致している。

### 既存規約との整合性

- Storybook 論理構造（固定）、各階層の役割、デザイントークンの絶対ルールとの齟齬なし。
- LLM エージェント向け実行ルールや AI 自己チェックリストは今回変更しておらず、既存のまま有効。

---

## 総合評価

### ✅ 総合評価: **合格**

汎用版 `AGENTS_STORYBOOK_RULES.md` への「ダークモード適用方法」「サイドバー表示順」「プレビューでのテーマ」の追加は、他プロジェクトでも流用可能な汎用的な手順として適切に記載されている。既存の Storybook 規約と整合しており、参照のみで実装に反映しやすい内容である。

### 主な成果

1. Tailwind + addon-themes を用いたダークモード適用手順が汎用版に明文化された。
2. サイドバー連番表示とプレビューテーマの扱いが、一つの規約で参照できるようになった。
3. manager-head.html による「Preview background」のみ非表示にする方法が、任意オプションとして記載された。
4. プロジェクト固有の記述を避け、汎用版としての位置づけが保たれている。

---

## 承認

本レビュー結果を踏まえ、以下のファイルを更新済みとして扱います。

- ✅ [`AGENTS_STORYBOOK_RULES.md`](../.agents/Storybookルール.md)（汎用版・更新内容を承認）

---

**レビュー完了日**: 2026 年 1 月 31 日 12:41:57
