# サブ有無共通成果物追加 レビュー

**レビュー実施日時**: 2026 年 3 月 4 日 14:45:17  
**レビュー対象**: 監査不備調査に伴う規約改善（サブあり・なし共通で同じ提出物・完了条件を満たす運用の明文化）  
**レビュー担当**: [`.review/README.md`](./README.md) に従い実施

---

## レビュー概要

「メインのみで実行する場合」もサブ運用時と同じ提出物表・同じ完了条件を満たすよう、規約を追加・更新した内容の整合性・妥当性をレビューする。

- 提出物の共通定義（サブあり・なし同一）
- トレーサビリティの証跡: 書記委譲（workflow.db）または memo 実行ログ（メインのみ時）
- 監査者用チェックリストの「各工程でログ」「サブ使用」の扱い

---

## 1. 対象ファイルの確認

| 種別 | パス | 変更内容 |
|------|------|----------|
| boot | `.agents/boot/CORE.md` | §1 フェーズ MUST に「サブを使用しない場合も同じ提出物表・同じ完了条件」、§3 に書記サブを呼ばない場合の memo 実行ログ暫定記録 |
| scribe | `.agents/scribe/書記役とログ委譲.md` | §4 書記が使えない場合の暫定: メインが memo に CONTRACT 同様の項目で 1 件ずつ追記 |
| rules | `.agents/rules/サブエージェント抜かし防止.md` | §2.2 成果物の共通定義（サブあり・なし共通）、§4.6 トレーサビリティ（書記委譲済み or memo 実行ログ） |
| rules | `.agents/rules/監査者用チェックリスト.md` | 各工程でログ: (1) workflow.db または (2) memo 実行ログ。サブ使用確認はサブ運用時必須・メインのみ時はスキップ。本レビューで「各工程で必ずログを残す」証跡を workflow.db のみ→ memo 併記に修正 |
| rules | `.agents/rules/実行ルール.md` | サブを使用しない場合も提出物表・同じ完了条件を満たす旨を追記 |
| rules | `.agents/rules/ワークフローとフェーズ定義.md` | フェーズ定義冒頭に必須成果物の共通定義は提出物表を参照しサブあり・なし共通で適用 |

いずれも存在し、意図した配置・記載である。

---

## 2. 規約間の整合性

### 2.1 CORE との整合

- ✅ CORE §1 の「サブを使用しない場合も同じ提出物表・同じ完了条件」はサブエージェント抜かし防止の提出物表・4.6 チェックリストと一致する。
- ✅ CORE §3 の「書記サブを呼ばない場合は memo 内実行ログに暫定記録」は書記役とログ委譲の「書記が使えない場合の暫定」および監査者用チェックリストの証跡（memo 実行ログ）と一致する。

### 2.2 監査者用チェックリストとの対応

- ✅ 「各工程でログが残っているか」は (1) workflow.db の execution_logs、(2) メインのみの場合は memo の実行ログのいずれかで足りると明記されている。
- ✅ 「各フェーズでサブエージェントが使用されているか」をサブ運用時必須・メインのみ時はスキップし memo 実行ログでトレーサビリティを確認する旨に修正済み。
- ✅ 「各工程で必ずログを残す」の証跡を workflow.db に加えメインのみの場合は memo 実行ログで足りる旨に修正済み（本レビューで対応）。

### 2.3 参照パス

- ✅ 監査者用チェックリストからの `../scribe/CONTRACT.md`・`../scribe/書記役とログ委譲.md` は `.agents/rules/` 起点で正しい。
- ✅ サブエージェント抜かし防止からの書記役とログ委譲・CONTRACT 参照は `../scribe/` で正しい。

---

## 3. レビュー結果

- **整合性**: 問題なし。サブあり・なし共通の成果物・完了条件・トレーサビリティ証跡が CORE・書記役とログ委譲・サブエージェント抜かし防止・監査者用チェックリスト・実行ルール・ワークフローとフェーズ定義の間で一貫している。
- **修正実施**: 監査者用チェックリストの「各工程で必ずログを残す」証跡を workflow.db のみから「workflow.db / メインのみの場合は memo 実行ログ」に変更。「各フェーズでサブエージェントが使用されているか」をサブ運用時必須・メインのみ時スキップに変更。

以上により、本規約更新はレビュー完了とする。
