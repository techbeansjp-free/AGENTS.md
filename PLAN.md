# PLAN: ローカル実行用 `.agent-skill-chain/` 配下シェルスクリプトがCLI未検出時に自動導入フォールバックを持たない

- Issue: `ISSUE-677`
- 対応する DESIGN: `DESIGN.md`

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | 共有実装の新規追加（`.agent-skill-chain/scripts/cli-resolve.sh`） | 公開関数 `asc_resolve_cli` と内部関数 `_asc_try_resolve`・`_asc_auto_install` を実装する。自身の位置（`${BASH_SOURCE[0]}`）から2階層上をリポジトリルートとして算出する。`_asc_try_resolve` は3経路を固定順（`bin/agents-md.js` → `node_modules/.bin/agent-skill-chain` → PATH上の `agent-skill-chain`）で判定し、第3経路は「実体の存在・実行可能・非空」のファイル単位検査と「`--help` が exit code 0 で終了する」起動可能性検証の双方を採用条件とする（自動導入の有無にかかわらず常時適用し、不合格なら当該経路を採用せず未解決として扱う）。未解決時は `_asc_auto_install` がオプトアウト判定（値が厳密に `0` のときのみ中止）→ 対話判定（標準入力と標準エラー出力の双方が端末なら確認）→ `npm` 可用性判定 → `npm install -g agent-skill-chain@latest` 実行（出力は標準エラー出力へ迂回）→ 再解決（`npm prefix -g` の bin ディレクトリを PATH へ加えたうえで `_asc_try_resolve` を再実行する。起動可能性検証は同関数に内包されるため独立の検証段は設けない）、の順に進む。各停止点で原因を含む日本語メッセージを標準エラー出力へ出し非ゼロを返す。標準出力へは何も書かない。ヘッダコメントに自動導入の既定挙動とオプトアウト手段を記載する | `AC-2, AC-3, AC-7, AC-9, AC-10, AC-11` | なし |
| 2 | ラッパー定型前文への一括置換（`scripts/` 直下40本・`ci/` 配下12本の計52本） | 上記52本について、既存の `REPO_ROOT` 算出と3経路解決ブロックを、対象54本（本変更単位の52本と変更単位3の2本）で文字単位に同一の定型前文（`${BASH_SOURCE[0]}` からの共有実装パス算出 → 読み取り可能性検査 → `source` の成否検査 → `asc_resolve_cli` の定義有無検査 → `asc_resolve_cli` 呼び出し）へ置き換える。自己位置の算出に `$0` を用いてはならない（`source` される変更単位3の2本と同一テキストを成立させるため）。末尾の `exec "${ASC_CLI[@]}" <サブコマンド> "$@"` は既存のサブコマンド・引数透過のまま維持する。各ファイル冒頭の説明コメントは変更しない | `AC-1, AC-4, AC-6, AC-8` | `#1` |
| 3 | アダプタ2本の組み込み（`.agent-skill-chain/adapters/claude.sh`・`同 human.sh`） | 変更単位2と同一の定型前文をファイル冒頭（`set -euo pipefail` と既存のディレクトリ変数算出の直後）へ追加し、既存の `_asc_cli` 関数の本体を「`asc_resolve_cli` を呼び、成功なら `"${ASC_CLI[@]}" "$@"` を実行、失敗ならその戻り値を `return` する」形へ置き換える。`exit` は使わない（両ファイルは他スクリプトから `source` されるため）。両ファイルが既存で使う `REPO_ROOT`・`SCRIPTS_DIR`・`ADAPTER_DIR` は他用途で参照されているため削除しない | `AC-1, AC-4, AC-6, AC-8` | `#1` |
| 4 | CIワークフローの共有実装への統一（配布元テンプレートと `.github/` の同一変更） | `agent-skill-chain-ci.yml` の「Ensure agent-skill-chain CLI」ステップから3経路のインライン判定を削除し、共有実装を `source` して `asc_resolve_cli` を呼ぶ形へ置き換える。採用された経路を示す情報出力は維持する。ステップのコメントを、共有実装へ統一した旨と統一の理由（重複による横展開漏れの再発防止）へ書き換える。配布元テンプレートと本リポジトリの `.github/` を同一内容にし、テンプレート同期検査が通ることを確認する。root-cleanup 用ワークフローにはCLI解決の記述が無いため変更しない | `AC-5` | `#1` |
| 5 | 重複検出テストの追加 | `.agent-skill-chain/` 配下の `*.sh` 全件を走査し、(a) 3つの literal（`bin/agents-md.js`・`node_modules/.bin/agent-skill-chain`・`command -v agent-skill-chain`）が連続12行の窓に同居するファイルの集合が `{共有実装}` の部分集合であること（0件・1件のいずれも合格、共有実装以外を含めば不合格）、(b) literal `node_modules/.bin/agent-skill-chain` を含むファイルがちょうど1件で、それが共有実装であること、(c) 共有実装が3つの literal をすべて含むこと（行間距離は問わない）、を表明する。(a) を「ちょうど1件」としないのは、共有実装の記述スタイルと窓幅12を結合させないため | `AC-1` | `#2, #3` |
| 6 | 全数実行テストハーネスの追加 | シナリオごとにフィクスチャを1回だけ用意し、対象54本を「`.agent-skill-chain/` 配下の `*.sh` のうちラッパー定型前文の固定 literal（共有実装パスを算出する行）を含むファイル」として動的に列挙し、1本ずつ実行する共通ヘルパを追加する。併せて、列挙件数が54であること、内訳が `scripts/` 直下40・`ci/` 配下12・`adapters/` 配下2であること、補集合（前文を含まない `*.sh`）が既知の非対象集合（共有実装・`hooks/` 配下・`adapters/codex.sh`）と完全に一致することを表明する（置換漏れが列挙から静かに脱落するのを防ぐ）。スタブCLIは argv を記録して exit code 0 を返し、`--help` にも exit code 0 を返す。実行ごとに初期化するのはスタブの記録ファイルと環境変数（`PATH`・`HOME`・`AGENT_SKILL_CHAIN_AUTO_INSTALL`）のみとし、標準入力は端末に接続しない | `AC-4, AC-6, AC-8` | `#2, #3` |
| 7 | 後方互換テストの追加（AC-4） | 変更前の版（変更着手前のリビジョン）の複製と変更後の複製の双方に、第1経路の位置へ argv 記録スタブを配置し、54本を同一引数で実行して、委譲先サブコマンド名・引数列・正常系 exit code が一致することを比較する | `AC-4` | `#6` |
| 8 | consumer 展開テストの追加（AC-6） | 配布・更新手順で展開される `.agent-skill-chain/` 一式を一時ディレクトリへ複製し、3経路いずれも不在かつ `npm` をスタブへ差し替えた環境で54本を実行し、全件が「共有実装を解決できない」で終わらず自動導入分岐へ到達することを検証する | `AC-6` | `#6` |
| 9 | 共有実装解決不能テストの追加（AC-8） | 展開物の複製に対し、共有実装の削除・切り詰め（部分展開相当）・読み取り権限剥奪・不正な内容への差し替え（構文エラー）の4状態それぞれで54本を実行し、全件が探索パスを含む日本語エラーを標準エラー出力へ出して非ゼロ終了し、スタブCLIが一度も呼ばれないことを検証する | `AC-8` | `#6` |
| 10 | 経路順序テストの追加（AC-9） | 3経路それぞれへ識別可能な別々のスタブを配置した環境と、そこから上位経路を順に取り除いた2つの派生環境で、対象ラッパーを実行して実際に起動されたスタブを記録し、判定順序が `bin/agents-md.js` → `node_modules/.bin/agent-skill-chain` → PATH の順であることを検証する。第3経路のスタブは `--help` に exit code 0 を返す実行可能・非空の実体とする（第3経路の採用条件に起動可能性検証が常時含まれるため） | `AC-9` | `#1, #2` |
| 11 | 自動導入・オプトアウトテストの追加（AC-2・AC-3・AC-10） | 3経路いずれも不在の非対話環境で、(a) `npm` スタブが導入成功を模擬したとき自動導入が確認を求めずに試行され、再解決後に処理が続行すること、(b) `npm` スタブが失敗したとき原因を含む日本語エラーで非ゼロ終了すること、(c) `AGENT_SKILL_CHAIN_AUTO_INSTALL=0` のとき `npm` スタブが一度も呼ばれずオプトアウトを示す日本語メッセージで非ゼロ終了すること、(d) 同一環境で当該環境変数を設定しない場合は自動導入が試行されること、を検証する。対話環境での確認プロンプト拒否経路（AC-3 の (b)）は、擬似端末を割り当てて拒否応答を与える形で自動検証を試み、環境上不可能な場合は手動手順として `VALIDATION.md` へ回す | `AC-2, AC-3, AC-10` | `#1, #2` |
| 12 | 導入後再解決テストの追加（AC-7） | `npm` スタブが導入先を PATH 外のディレクトリへ配置する環境で、(a) 再解決に成功して処理が続行すること（導入先の実体は `--help` に exit code 0 を返す）、(b) 導入先を意図的に配置しない場合に「導入は成功したがCLI実体を解決できない」旨の日本語エラーで非ゼロ終了すること、(c) 導入先の実体が `--help` で非ゼロを返す場合も同じ経路で日本語エラー＋非ゼロ終了となること（起動可能性検証が再解決に内包されること）を検証する。実環境のグローバル導入先レイアウトでの1件の確認は手動手順として `VALIDATION.md` へ回す | `AC-7` | `#1, #2` |
| 13 | 並行実行テストの追加（AC-11） | 遅延を持ち導入先を段階的に配置する `npm` スタブを用意し、3経路いずれも不在の環境で複数のラッパーを同時起動して全プロセスの終了を待ち、各プロセスが「正常系 exit code での委譲成功」か「日本語エラー＋非ゼロ終了」のいずれかへ収束し、部分配置状態のCLI実体が委譲に使われないことを検証する。段階的配置には、(a) 実体が未配置、(b) 実体はあるがサイズ0または実行権限なし、(c) 実体は完全だが `--help` が非ゼロで終了する（依存未配置相当）、の3段階を必ず含める。(c) は、自ら自動導入を行わないプロセスが第3経路で拾った場合にも起動可能性検証で排除されることを確認するため、自動導入を行わないプロセス（`AGENT_SKILL_CHAIN_AUTO_INSTALL=0` を与えたプロセス）を同時起動する条件でも検証する。実 `npm` による並行グローバル導入の1件の確認は手動手順として `VALIDATION.md` へ回す | `AC-11` | `#1, #2` |
| 14 | 既存テストの追随修正 | CIワークフローの「Ensure agent-skill-chain CLI」ステップの bash 実体をフィクスチャ上で実行している既存の単体テストを、変更単位4の新しいステップ本文（共有実装の `source`）に合わせて修正する。フィクスチャへ共有実装を配置する前処理を追加し、3経路のいずれかが既にある場合に `npm install` を呼ばないという既存の検証意図は維持する | `AC-5` | `#4` |
| 15 | 利用者向け記述の追加（README） | 導入手順の節へ、CLI未導入時にラッパーが `npm install -g agent-skill-chain@latest` を自動試行すること、対話環境では確認を挟むこと、`AGENT_SKILL_CHAIN_AUTO_INSTALL=0` で無効化できること（値が厳密に `0` のときのみ無効化され、それ以外の値・未設定は既定の試行になること）を追記する | `AC-2, AC-10` | `#1` |

<!-- 変更単位を追加する場合は # を連番で追加する -->

## 実装順序の見直しについて

実装中に作業順序（上記の変更単位の並び）のみを見直す場合は、本ファイルのみを更新すればよい。設計要素・責務・境界そのものを変更する場合は、DESIGN.md の更新（および設計ゲートの再通過）が必要になる点に注意する。

変更単位1を最初に置くのは、以降のすべての変更単位が共有実装の公開契約（`asc_resolve_cli` と `ASC_CLI`）に依存するため。変更単位2と3は互いに独立で並行に進められるが、いずれも変更単位1の完了後でなければ動作確認ができない。変更単位5・6は対象54本すべての置換が終わった後でなければ、走査結果・全数実行が中間状態を拾って誤った結果になる。変更単位7〜9は共通ハーネス（変更単位6）の上に構築するため、その後に置く。変更単位10〜13は共有実装の分岐そのものを対象とし、ハーネスに依存しないため変更単位6の完了を待たずに着手できる。変更単位14は変更単位4のステップ本文が確定してからでなければ修正内容を決められない。

自動化できない検証（対話環境での確認プロンプト拒否、実環境でのグローバル導入先レイアウト、実 `npm` による並行導入）は、SPEC が `hybrid` としている AC-3・AC-7・AC-11 の手動部分であり、独立検証セグメントで `VALIDATION.md` へ手順・実行者・証跡とともに記録する。
