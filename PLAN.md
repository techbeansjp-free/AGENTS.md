# PLAN: security: reconcile workflowがpushトリガーのYAML自己参照によりGate Check Runを偽造可能

- Issue: `ISSUE-342`
- 対応する DESIGN: `DESIGN.md`

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | `reconcile`ワークフローの`pull_request_target`化 | `.github/workflows/agent-skill-chain-reconcile.yml`の`on:`を`push`（`branches-ignore`）から`pull_request_target: types: [synchronize]`へ変更する。`jobs.reconcile`へ`if: github.event.pull_request.base.ref == github.event.repository.default_branch`を追加する（`gate.yml`と同型）。checkoutステップを`actions/checkout@v7`＋`ref: ${{ github.event.pull_request.base.sha }}`・`fetch-depth: 0`・`persist-credentials: false`へ変更し、続けて`git fetch --no-tags origin "pull/${{ github.event.pull_request.number }}/head:refs/agent-skill-chain/targets/${{ github.event.pull_request.head.sha }}"`でPR headをread-onlyなgit objectとして取得するステップを追加する（`gate.yml`と同一パターン）。`Derive issue_id`ステップの`env.BRANCH`を`${{ github.event.pull_request.head.ref }}`へ変更し、dependabot分岐の判定を`gh api repos/{owner}/{repo}/pulls?head=...`によるAPI照会から新規`env.PR_AUTHOR: ${{ github.event.pull_request.user.login }}`の直接参照へ簡素化する（API呼び出しステップを削除、3分岐の意図・`ISSUE-<n>`抽出・exit 1挙動は維持）。あわせて、dependabot分岐の直後に`chore/root-cleanup-*` prefix一致で`skip_checks=true`とする新規分岐を追加する（design-gate指摘`root-cleanup-branch-exclusion-dropped`対応：旧`on: push`版の`branches-ignore: [main, 'chore/root-cleanup-*']`はpushされたブランチ自身を対象に除外していたが、`pull_request_target`の`branches-ignore`はbaseブランチ側にのみ適用されるため字句移植では機能せず、`agent-skill-chain-root-cleanup.yml`が作成する短命PRへのsynchronizeで`ISSUE-<n>`抽出失敗によるジョブ失敗を招く。DESIGN.md「障害・ロールバック考慮」失敗モード7参照）。`Reconcile gates against pushed SHA`ステップの対象SHA引数を`${{ github.event.pull_request.head.sha }}`、新設のpr_number引数を`${{ github.event.pull_request.number }}`に変更する。`permissions`ブロックは既存3スコープ（`contents: read`, `checks: write`, `pull-requests: read`）を維持したうえで`actions: read`を新規追加する（design-gate指摘`resolve-approved-baseline-missing-actions-read-permission`対応：変更単位#3の`resolveApprovedBaseline`が呼び出す`GET actions/runs?check_suite_id=...`が`actions`スコープの読み取り権限を独立に要求するため。DESIGN.md「コンポーネント構成」参照）。job名`reconcile`・step名・step id`ctx`は維持する。同一変更を配布テンプレート側の同名ファイル（`.agent-skill-chain/templates/github/.github/workflows/agent-skill-chain-reconcile.yml`）にも適用する。前ラウンドで検討された新規`agent-skill-chain-reconcile-trigger.yml`は作成しない（作成済みでもないため削除作業も不要） | AC-1, AC-2, AC-4, AC-6 | なし |
| 2 | `gate-reconcile.sh` / `reconcile()`へのpr_number引数追加＋GitHubモード3分岐化 | `.agent-skill-chain/scripts/gate-reconcile.sh`および`src/commands/gate.ts`の`reconcile()`のシグネチャを`<issue_id> <target_sha>`から`<issue_id> <target_sha> [pr_number]`へ拡張する（第3引数は任意、GitHub呼び出し経路では変更単位#1のワークフローから常に渡される）。`for (const gateId of SEGMENTS)`ループ・`artifactDigestAtSha`呼び出し・`changed=true`側の無効化分岐・`changed=false`側の再発行分岐自体は既存のまま維持するが、GitHubモードでの`approved_artifacts`取得を`reviewFilePath`からの読み込みから`resolveApprovedBaseline`呼び出しへ置き換えるにあたり、**「無変更のまま単純に読み込み元だけ差し替える」実装をしてはならない**（design-gate指摘`no-baseline-treated-as-vacuously-unchanged-reissues-success`：`resolveApprovedBaseline`が「候補なし」を返すケースを空配列と区別せず`.some()`へ渡すと恒偽になり、未レビューSHAへもsuccessを再発行する）。#3で定義する`resolveApprovedBaseline`の`{found:true,approvedArtifacts}`/`{found:false}`戻り値を先に分岐し、`found:false`の場合は`report`書込み・`publishCheckRun`呼び出しをいずれも行わず当該ゲートの照合を丸ごとスキップして次ゲートへ進む（`downstreamInvalidated`は変更しない）。`found:true`の場合のみ従来の`changed`判定・無効化/再発行分岐へ進む。**`found:true`かつ`changed:false`（success再発行）の分岐に限り、`publishCheckRun`を呼び出す直前に`report.gate.approved_artifacts`を、`reviewFilePath`から読み込んだローカル`report`の値ではなく`resolveApprovedBaseline`の戻り値（`approvedArtifacts`、trusted baseline自体）で上書きしてから`canonicalJson(report)`を構築する（design-gate指摘`reconcile-republish-embeds-untrusted-local-approved-artifacts-enabling-two-push-baseline-poisoning`対応：この上書きを行わない場合、push1で`reviews/<gate>.yaml`の`approved_artifacts`のみを未来のdigestへ書き換えsuccess Check Runのoutput.textへ汚染値を埋め込ませ、push2で実成果物をそのdigestへ一致させて`changed=false`を騙し取る2段階攻撃が成立する。`changed:true`（`action_required`発行）側はこの上書きを行わない：`resolveApprovedBaseline`の候補条件は`conclusion=success`のみのため`action_required`のCheck Runが後続の候補に採用されることはなく、ローカル`report`由来の値がembedされていても信頼汚染は生じない。DESIGN.md「障害・ロールバック考慮」失敗モード8参照）** | 該当なし（#3の前提整備） | なし |
| 3 | `resolveApprovedBaseline`実装 | `src/commands/gate.ts`へ`resolveApprovedBaseline`を新規実装する。戻り値は`{ found: true; approvedArtifacts: { path: string; digest: string }[] }`（`approvedArtifacts`が空配列である場合を含む）と`{ found: false }`を判別可能な構造体（discriminated union）とし、`approvedArtifacts: []`と`found: false`を型として衝突させない（design-gate指摘`no-baseline-treated-as-vacuously-unchanged-reissues-success`対応。#2で定義する`reconcile()`側の3分岐がこの型に基づき`found`を先に分岐する前提）。GitHubモード（`config.coordination.backend === 'github'`）でのみ呼び出される。(a) `pr_number`が渡されていれば`GET pulls/{pr_number}/commits`を直接呼び出し、空であれば従来通り`GET commits/{target_sha}/pulls`でPRを特定してから同エンドポイントを呼ぶ。対応するPRが0件の場合は`{ found: false }`を返す（I8）。(b) target_shaより前のコミットを新しい順に`GET commits/{sha}/check-runs?check_name=<config.checks[gateId]>`で照会し、`conclusion=success`な候補を新しい順に走査する。(c) 各候補について`GET actions/runs?check_suite_id=<候補の`check_suite.id`>&head_sha=<候補コミットのSHA>`でsource identity（発行元workflowファイルパス、応答`workflow_runs[].path`）を検証し、`.github/workflows/agent-skill-chain-gate.yml`・`.github/workflows/agent-skill-chain-reconcile.yml`のいずれとも一致しない候補は棄却し次候補を試す。同ステップで応答`workflow_runs[].event`が`pull_request_target`と一致することも併せて検証し、path一致でもevent不一致（例: `push`）の候補は棄却し次候補を試す（mainを未取込の既存issueブランチに残存する旧`on: push`版ワークフローが発行した偽装Check Runを排除するため、DESIGN.md「障害・ロールバック考慮」移行ギャップ節参照）。(d) 検証を通過した候補の`output.text`を`canonicalJson(GateReport)`としてパースし`approved_artifacts`を復元し`{ found: true, approvedArtifacts }`を返す。JSON構文エラー（65,535文字上限による切り詰めを含む）時も棄却し次候補を試す。候補が尽きれば`{ found: false }`を返す。(a)〜(d)いずれかのAPI呼び出し自体の失敗（0件応答ではない）は`CliError`でreconcileコマンド全体を失敗させる。`test/helpers/gh-stub.ts`へ`GET /repos/{owner}/{repo}/pulls/{number}/commits`・`GET /repos/{owner}/{repo}/commits/{sha}/pulls`（pr_number省略時フォールバック用）・`GET /repos/{owner}/{repo}/actions/runs`のスタブ応答を追加する（design-gate指摘`check-suite-id-filter-unverified-may-return-multiple-workflow-runs`対応：実APIは`check_suite_id`クエリパラメータで単一workflow runへ絞り込める（本リポジトリの実check_suite_idに対する`gh api`実行で実測確認済み、`DESIGN.md`参照）ため、スタブも`check_suite_id`未指定時は複数件、指定時は該当1件のみを返すよう実装し、実際のAPI挙動を忠実に再現する）。`test/integration/reconcile.test.ts`のgithubバックエンドテスト群へ次のケースを追加する: (i) pr_number指定時にSPEC.md本体を書き換えつつ同一push内で`issues/1/.agent-skill-chain/reviews/spec.yaml`の`approved_artifacts`digestも書き換えて偽装してもCheck Run履歴由来の基準と照合されinvalidatedされるケース、(ii) 対応するsuccess Check Runが一度も存在しない（`resolveApprovedBaseline`が`{found:false}`を返す）場合、`reconcile()`が当該ゲートについて`publishCheckRun`を一切呼び出さず（`success`・`action_required`いずれのCheck Runも発行されない）、`reviews/<gate>.yaml`への書込みも行わないことをアサートするケース（design-gate指摘`no-baseline-treated-as-vacuously-unchanged-reissues-success`対応：本ケースが「照合がスキップされる」ことの検証対象を、`success`が誤って再発行されないことまで含めて明示的にアサートする）、(iii) `pulls/{number}/commits`等のCheck Run照会自体がエラー応答した場合にreconcileコマンドが失敗するケース、(iv) 最新のCheck Runの発行元`actions/runs.path`がtrusted workflowファイルパスと一致しない場合（偽装Check Run）はその候補を棄却し、trusted pathから発行された次点の候補（存在すればそれ、無ければ`{found:false}`となり(ii)と同様publishCheckRunを一切呼び出さない）を採用するケース、(v) pr_number省略時に`commits/{sha}/pulls`フォールバック経路が機能するケース、(vi) 発行元`actions/runs.path`はtrusted workflowファイルパスと一致するが`actions/runs.event`が`push`（trusted pathと同名のままmainを未取込のissueブランチに残存する旧`on: push`版ワークフローが発行したケースを模擬）である候補は棄却し、event/pathとも一致する次点の候補（存在すればそれ、無ければ`{found:false}`となり(ii)と同様publishCheckRunを一切呼び出さない）を採用するケース、(vii) 上流ゲート（例: spec-gate）が`changed=true`で`downstreamInvalidated=true`となった直後に、下流ゲート（例: design-gate）が`resolveApprovedBaseline`から`{found:false}`を返す場合でも下流ゲートのpublishCheckRunが呼ばれず（スキップが優先される）、かつその次の下流ゲート（例: implementation-gate）へ`downstreamInvalidated=true`がそのまま引き継がれることをアサートするケース（design-gate指摘`no-baseline-treated-as-vacuously-unchanged-reissues-success`対応：スキップが`downstreamInvalidated`の伝播を打ち消さないことの検証）、(viii) 二段階push攻撃の再現ケース（design-gate指摘`reconcile-republish-embeds-untrusted-local-approved-artifacts-enabling-two-push-baseline-poisoning`対応）：1回目の`reconcile()`呼び出しで実成果物content不変（digest D1）のまま`issues/<id>/.agent-skill-chain/reviews/<gate>.yaml`の`approved_artifacts`のみをテスト用の未来digest D2へ書き換えた状態を与え、`changed=false`でsuccess Check Runが再発行されること、かつ発行された`output.text`（`gh-stub`が記録するcheck-runs API呼び出しの`output.text`引数）をパースした`approved_artifacts`がD2ではなくD1（`resolveApprovedBaseline`が返したtrusted baseline）と一致することをアサートする。続けて、直前で発行されたsuccess Check Runをgh-stubのCheck Run発行履歴へ反映した状態で2回目の`reconcile()`呼び出しを行い、実成果物を実際にdigest D2となる内容へ書き換えたうえで`changed=true`（invalidated）となること、`publishCheckRun`が`success`ではなく`action_required`で呼ばれることをアサートする | AC-1, AC-5 | #2 |
| 4 | 配布テンプレート同期の確認 | `.agent-skill-chain/ci/verify-template-sync.sh`（`agent-skill-chain verify template-sync`）をローカルで実行し、`.github/workflows/agent-skill-chain-reconcile.yml`のリポジトリ実体と配布テンプレートが完全一致することを確認する。差異があれば#1の変更をテンプレート側へ反映する。新規ファイルを作成しないため、同期確認対象は本Issue着手前と同じ単一ファイルのままである | AC-3 | #1 |
| 5 | 既存ユニットテストの追随・拡張 | `test/unit/dependabot-ci-skip.test.ts`を次の点で更新する：(a) `agent-skill-chain-reconcile.yml`の`on.pull_request_target.types`が`['synchronize']`であることを検査する項目を追加、(b) `jobs.reconcile.if`が`github.event.pull_request.base.ref == github.event.repository.default_branch`を含むことを検査する項目を追加、(c) `permissions`が`contents: read`, `checks: write`, `pull-requests: read`, `actions: read`（変更単位#1で新規追加）の4スコープを含むことを検査、(d) `Derive issue_id`ステップの`env.BRANCH`が`github.event.pull_request.head.ref`を参照し、`env.PR_AUTHOR`が`github.event.pull_request.user.login`を参照すること・`gh api ... pulls?head=`によるAPI照会runステップが存在しないことを検査する項目を追加、(e) `Reconcile gates against pushed SHA`ステップが`github.event.pull_request.head.sha`・`github.event.pull_request.number`を参照していることを検査する項目を追加（AC-6）、(f) `chore/root-cleanup-*` prefix分岐が`skip_checks=true`を設定することを検査する項目を追加（design-gate指摘`root-cleanup-branch-exclusion-dropped`対応）。`test/unit/dependabot-ci-skip-exec.test.ts`は`ctx`ステップの`run`本文抽出＋env直接注入という実行方式のため、`PR_AUTHOR`という新規env名への追随のみ必要で、実行方式自体の変更は不要であることを確認する | AC-1, AC-3, AC-4, AC-6 | #1, #2 |
| 6 | ADR-0016のfinalize対応準備 | 設計ゲート承認後、進行役が`.agent-skill-chain/scripts/adr-finalize.sh`を起動できる状態にしておく（本Issueの設計セグメントでは`docs/adr/ADR-0016-reconcile-workflow-run-trust-boundary.md`を`status: proposed`のまま作成済みとし、`accepted`への遷移はADR finalizationワーカーの責務とする） | 該当なし（設計ゲート完了条件） | #1〜#5 |
| 7 | 既存openなissueブランチへのmain取込み確認（移行ギャップ是正の運用手順） | #1が本Issueのマージによりmainへ反映された後、進行役はマージ時点で開いている全issueブランチ（`git worktree list --porcelain`・`gh pr list --state open`で洗い出し可能）について、`git merge origin/main`（またはrebase）によるmain取込みが完了しているか確認する。#8の自動検知（`::warning::`アノテーション。design-gate指摘`unmigrated-detection-step-lacks-write-permission`対応でPRコメント投稿は行わない、DESIGN.md「コンポーネント構成」参照）を一次シグナルとして未取込ブランチを識別し、未取込のブランチが残る場合、進行役はそのブランチのworktree/PRへ取込みを促す、または当該ワーカーの次回起動時に取込みを実施させる。取込みが完了するまでの間、当該ブランチには旧`on: push`版`agent-skill-chain-reconcile.yml`が残存し続けるため（#3のevent検証により承認済み基準の信頼汚染は防がれるが、旧版ワークフロー自体の実行は排除されない）、進行役はそのブランチのreconcile結果を完全に信頼できるものとしては扱わない（DESIGN.md「障害・ロールバック考慮」移行ギャップ節参照）。確認手順・完了判定基準は`VALIDATION.md`で確定する | AC-1 | #1, #3, #8 |
| 8 | 未移行reconcile検知の自動化（design-gate指摘`migration-step-has-no-automated-enforcement-or-detection`対応。当初`.github/workflows/agent-skill-chain-gate.yml`側へ追加しようとしたが、design-gate指摘`gate-yml-change-violates-spec-scope-exclusion`対応で対象を`.github/workflows/agent-skill-chain-reconcile.yml`側へ変更） | `.github/workflows/agent-skill-chain-gate.yml`は変更しない（SPEC.mdスコープ外節）。代わりに変更単位#1で`pull_request_target`化する`.github/workflows/agent-skill-chain-reconcile.yml`の同一ジョブへ新規ステップ`Detect unmigrated reconcile workflow`を追加する。#1の「PR headの取得」ステップ（`git fetch`によるread-only object取得）の直後に配置し、`git show refs/agent-skill-chain/targets/${{ github.event.pull_request.head.sha }}:.github/workflows/agent-skill-chain-reconcile.yml`をread-only参照する（新規checkout・ビルド不要、実行しない）。内容に`on: push`が含まれる場合（本Issue以前の自己参照構成が当該PR headに残存していることを意味する）、`::warning::`アノテーションを出力する（design-gate指摘`unmigrated-detection-step-lacks-write-permission`対応：PRコメント投稿は行わない。理由は変更単位#1で維持する既存`permissions`に`issues: write`/`pull-requests: write`が含まれておらず、既に`checks: write`を保有するこの信頼済みワークフローへ書込み権限をさらに追加することは本Issueの最小権限方針に反するため。DESIGN.md「コンポーネント構成」参照）。このステップの成否は同ジョブの他ステップ（`Reconcile gates against pushed SHA`によるCheck Run発行）へは一切影響させない。既存のrequired status check（`agent-skill-chain/{spec,design,implementation,validation}-gate`・`verify`）の成否には一切影響させず、`.agent-skill-chain/templates/github/provisioning/rulesets/main.json`の変更は伴わない。同一変更を配布テンプレート側の同名ファイル（`.agent-skill-chain/templates/github/.github/workflows/agent-skill-chain-reconcile.yml`）にも適用する。`test/unit/dependabot-ci-skip.test.ts`（または新規テストファイル）へ、当該ステップの`run`スクリプトが`on: push`を含むreconcile.yml内容に対して警告を出し、`pull_request_target`のみを含む内容に対しては警告を出さないことを検査するケースを追加する | AC-1 | #1 |

## 実装順序の見直しについて

実装中に作業順序（上記の変更単位の並び）のみを見直す場合は、本ファイルのみを更新すればよい。設計要素・責務・境界そのものを変更する場合は、DESIGN.mdの更新（および設計ゲートの再通過）が必要になる点に注意する。
