# PLAN: 反証ルーブリックに合格到達条件・blocking 基準・過去ラウンド・ラウンド上限を与え、ゲートを収束させる

- Issue: `ISSUE-729`
- 対応する DESIGN: `DESIGN.md`

## 目的・対象範囲

本計画は、DESIGN.md が確定させた設計要素（D1〜D8）を、どの順序で・どの変更単位に分割して実装し、何をもって完了と判定するかを定める。設計要素そのものの定義・責務境界・依存方向は本計画の対象外であり、実装順序と検証手順のみを扱う。実装途中で順序だけを見直す場合は本ファイルのみを更新する。

**変更単位の順序は「承認率を上げる側を先に、停止性を課す側を後に」置く。** ラウンド上限だけが先に効くと、承認率が変わらないまま打ち切りだけが働き、溢れた判断がすべて人間へ流れる。下表の `#3` は D1（合格到達条件・blocking 基準）を含み、`#6`・`#7` が限定と打ち切りを載せる。この前後関係は入れ替えない。

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | ラウンド導出の純関数 | レビュー一覧・Issue 識別子・ゲート識別子・当該反復の反復識別子・trusted actor 集合を引数として受け取り、ラウンド番号と過去ラウンドの判定記録（ラウンド番号・target_sha・slot ごとの判定・finding の severity/origin/code/根拠要約）を導出する関数を新設する。当該反復の反復識別子を持つ証跡、trusted actor 以外が投稿した証跡、反復識別子を取り出せない証跡は計数から除外する。target_sha では絞り込まない。導出できない場合はその旨と理由を返す。根拠要約は 1 finding あたり 600 文字、節全体 24000 文字を上限とし、超過時は古いラウンドから省略して省略した旨を残す。外部入出力を持たず、設定の読み込みにも依存しない | `AC-3`, `AC-9` | なし |
| 2 | 耐久記録の取得と閾値検査述語 | `#1` の純関数へ渡すレビュー一覧を PR のレビュー全件取得（頁送り）から得る薄い層と、閾値の下限・順序関係を検査する述語を同一モジュールへ置く。PR 番号が無い場合・trusted actor の登録が無い場合・取得に失敗した場合は導出不能として返す | `AC-3`, `AC-8`, `AC-9` | `#1` |
| 3 | 反証ルーブリックの置換 | 判定プロンプトの反証ルーブリックを、合格到達条件・探索指示（1 件以上の探索要求を除去）・blocking 基準の 3 条件・不合格条件・不緩和条項・反証観点への適用範囲限定・実証性の用語定義の構成へ置き換える。立証ルーブリックは変更しない | `AC-1`, `AC-2`, `AC-7` | なし |
| 4 | 過去ラウンド節と蒸し返し禁止指示 | 判定プロンプトへ過去ラウンドの節を追加する。過去ラウンドが 1 つ以上ある場合は判定記録と蒸し返し禁止の指示（未修正 blocking の同一 code 再提出は対象外である旨を含む）を、初回の場合は過去ラウンドが無い旨とラウンド番号 0 を、導出不能の場合は取得できなかった旨・過去ラウンドが存在しないことを意味しない旨・ラウンド番号を導出できていない旨を展開する。導出結果は引数として外から与えられる形にする | `AC-3`, `AC-4` | `#1`, `#3` |
| 5 | 設定項目の追加 | レビュー設定へ限定閾値・打ち切り閾値の節を追加する。スキーマへ整数・下限（限定閾値 1 以上、打ち切り閾値 2 以上）を表現し、既定値（限定閾値 2・打ち切り閾値 4）を定義する。設定の読み込みがスキーマ検証の直後に `#2` の述語で順序関係（限定閾値が打ち切り閾値より真に小さい）を検査し、違反を日本語の理由付き例外とする。設定リファレンス文書へ両項目を追記する | `AC-8` | `#2` |
| 6 | 高ラウンドの追加要件 | ラウンド番号が限定閾値以上のとき、判定プロンプトへ「3 条件をすべて満たし、かつ実証性を満たす反例だけを blocking とする」旨と、3 条件を取り除かない旨、実証性を満たさない反例は warning 以下として記録する旨を追加する。限定閾値未満のときは当該記述を出さない。導出不能のときも出さない | `AC-5`, `AC-7` | `#4`, `#5` |
| 7 | 判定プロンプト生成の引数拡張とアダプタからの受け渡し | 判定プロンプト生成コマンドへ PR 番号・反復識別子を任意引数として追加し、アダプタが証跡投稿に使うのと同じ値を渡す。証跡投稿は prompt digest 再計算の際に自らの PR 番号・反復識別子から導出したラウンド情報を用いる | `AC-5`, `AC-6`, `AC-9` | `#6` |
| 8 | 証跡検証への打ち切りの組み込み | 証跡検証で最新反復の反復識別子を先に確定させ、それを除外して導出したラウンド情報から期待 prompt digest を生成する。打ち切り（判定に使うラウンド番号が打ち切り閾値以上、かつ未解消の blocking が残る）が成立する場合は判定不能を機械的に設定し、最終判定を人間判断とする。打ち切りの評価は既存の差し戻し判定より先に置く。理由欄へラウンド番号・打ち切り閾値・未解消 blocking 件数を載せる。判定記録のスキーマ・証跡の schema 版・verdict 結線経路は変更しない | `AC-6`, `AC-9` | `#7` |
| 9 | 自動テストの追加・既存テストの更新 | 下記「必須テストケース」の全項目に対応する単体テスト・結合テストを追加する。判定プロンプトの golden 比較・prompt digest 往復・判定プロトコルの既存テストのうち、`#3`・`#4`・`#6`・`#7` により出力が変わるものを更新する | `AC-1`〜`AC-9` | `#1`〜`#8` |

## 検証手順と観測単位

すべての完了条件を、ゲートレビューの実運用ではなく、当該処理を単体で直接実行した結果を観測単位として判定する。用いる直接実行は次の3種類である。

1. ラウンド導出の純関数を単体テストから直接呼び出す（GitHub API もファイルシステムも介さない）。レビュー一覧は組み立てた値として与える。
2. 判定プロンプト生成を一時リポジトリに対して直接実行し、出力文字列を検査する。ラウンド情報は引数として与え、ラウンド番号のみを変えた 2 本の出力を比較する。
3. 証跡検証を、組み立てたレビュー一覧・PR 情報を与えて直接実行し、判定記録の最終判定と理由を検査する。

## 必須テストケース

次のケースは網羅の要であり、いずれも省略しない。

| ケース | 状態 | 期待 | 対応 AC-ID |
|---|---|---|---|
| 合格到達条件の明示 | 任意の Issue・ゲート・target_sha で判定プロンプトを生成 | 反証ルーブリックに、blocking 基準を満たす反例が 1 件も無い場合に pass とする旨が現れる。反例を必ず 1 件以上見つけることを求める記述が現れない | `AC-1` |
| 3 条件の全ラウンド併記 | ラウンド番号が限定閾値未満の場合と限定閾値以上の場合の 2 本を生成 | いずれにも 3 条件をすべて満たす反例だけを blocking とし、いずれかを満たさない反例は warning 以下として記録する旨が現れる。3 条件のいずれかを不要とする記述がどちらにも現れない | `AC-2` |
| **過去ラウンドの展開（記録あり）** | 過去ラウンドの判定記録が存在し取得できる | finding code・severity・立証と反証の判定に加え、各 finding の根拠要約（対象とした記述と述べられた欠陥）が現れる | `AC-3` |
| **過去ラウンドの展開（初回）** | 過去ラウンドが存在しない | 過去ラウンドが無い旨が現れる | `AC-3` |
| **過去ラウンドの展開（取得不能）** | 過去ラウンドが存在しうるが耐久記録から取得できない（ローカルモード／PR 番号なし／trusted actor 未登録／取得失敗の4経路をそれぞれ実行する） | 4経路とも、過去ラウンドが無い旨は現れず、取得できなかった旨が現れる。初回の場合と同一の文言にならない | `AC-3` |
| 蒸し返し禁止の指示 | 過去ラウンドの判定記録が存在する | 是正済み論点を新たな根拠なく再び blocking として提出しない旨と、未修正のまま残っている blocking の同一 code 再提出は禁止の対象外である旨の双方が現れる | `AC-4` |
| **限定の真部分集合性** | ラウンド番号のみを変えた 2 本（限定閾値未満・限定閾値以上）を生成して比較 | 限定閾値以上の側にのみ「3 条件をすべて満たし、かつ実証性を満たすものに限定する」旨が現れ、3 条件は取り除かれず併記される。限定閾値未満の側には、実証性を満たさない反例を blocking から外す旨の記述が現れない。不緩和条項（実証性を満たす反例は blocking のまま）はどちらにも現れてよい | `AC-5`, `AC-7` |
| **打ち切りの機械設定** | 判定に使うラウンド番号が打ち切り閾値に達し、未解消の blocking が残り、レビュア自身の判定不能表明は偽 | 最終判定が人間判断になる。承認は記録されない。理由にラウンド番号・打ち切り閾値・未解消 blocking 件数が含まれる。レビュアの自己申告のみを入力とする実装では本ケースが失敗する | `AC-6` |
| **打ち切りと差し戻しの評価順序** | 上と同じ状態で blocking が存在する | 差し戻しではなく人間判断になる。既存導出の順序をそのまま用いた実装では差し戻しになり本ケースが失敗する | `AC-6` |
| 打ち切り未到達 | ラウンド番号が打ち切り閾値未満で未解消の blocking が残る | 差し戻しのまま。人間判断へ倒れない | `AC-6` |
| 重大性区分の非除外 | 限定閾値以上・打ち切り閾値以上を含む任意のラウンドで生成 | データ喪失・セキュリティ低下・既存機能の回帰・目的の未達という区分そのものを blocking から除外する記述が現れない。これらに該当し 3 条件と実証性を満たす反例が blocking のままである旨が現れる | `AC-7` |
| **閾値の順序関係（大なり）** | 限定閾値が打ち切り閾値より大きい設定を読み込む | 設定エラーとして拒否され、当該設定でゲートレビューが実行されない | `AC-8` |
| **閾値の順序関係（等号）** | 限定閾値と打ち切り閾値が等しい設定を読み込む | 同上。等号を受理する実装では本ケースが失敗する | `AC-8` |
| 閾値の下限 | 限定閾値 0 の設定、打ち切り閾値 1 の設定、整数でない設定 | いずれもスキーマ検証で拒否される | `AC-8` |
| 既定値 | 設定の既定値を読み込む | 限定閾値が打ち切り閾値より真に小さい | `AC-8` |
| **ラウンド計数の単位（Strict）** | Strict の反復が N 回実行され、各反復が 2 件の証跡を残す。同一反復の 2 件は同一の反復識別子、異なる反復は相異なる反復識別子を持つ。各反復の target_sha は毎回異なる | ラウンド番号が N になる。証跡件数 2N にはならない | `AC-9` |
| **ラウンド計数の単位（Standard）** | 同じ N 回の反復を Standard（1 件ずつ）で実行 | ラウンド番号が Strict の場合と同じ N になる | `AC-9` |
| **一時領域の消失と target_sha 変化** | 直前ラウンドの一時的な作業領域が存在せず、各反復の target_sha が異なる | ラウンド番号が 0 へ戻らない | `AC-9` |
| **当該反復の除外（時点整合）** | 同一反復の slot 1 の証跡が耐久記録に既に存在する状態で、slot 2 のプロンプト生成時点・証跡投稿時点・証跡検証時点のラウンド番号を求める | 3 時点とも slot 1 のプロンプト生成時点と同じ値になる。除外しない実装では slot 間で prompt digest が食い違い本ケースが失敗する | `AC-9`, `AC-6` |
| **信頼できない投稿主体の除外** | trusted recorder actor 以外が投稿した、証跡と同形の本文を含むレビューが混在する | 当該レビューはラウンド計数に含まれない。含める実装では、混在によりラウンド番号が押し上がり本ケースが失敗する | `AC-9` |
| **導出不能時の帰結** | 耐久記録からラウンド番号を導出できず、未解消の blocking が残る | 限定が適用されず、打ち切りを理由とする承認も記録されず、最終判定は人間判断ではなく差し戻しになる | `AC-9` |
| 分量上限 | 過去ラウンドの根拠要約が 1 finding あたり 600 文字を超える場合、および節全体が 24000 文字を超える場合 | 前者は切り詰められ切り詰めた旨が現れる。後者は古いラウンドから省略され、省略したラウンド番号と省略した旨が現れる。黙って落ちない | `AC-3` |
| 既存判定の非回帰 | 打ち切りが成立せず、レビュアの判定不能表明も偽で、両観点が pass かつ blocking 無し | 最終判定が承認になる。本変更前と同一 | `AC-6` |
| prompt digest 往復 | 判定プロンプト生成・証跡投稿・証跡検証を同一のラウンド情報で通す | 期待 digest が一致し証跡が受理される。ラウンド情報が一方の時点だけ異なる場合は受理されず人間判断へ倒れる | `AC-6`, `AC-9` |

## 適用するテスト種別の判断

| 区分 | 判断 | 根拠 |
|---|---|---|
| lint / format・型検査・単体テスト・変更範囲の結合テスト・静的セキュリティ検査・依存関係と秘密情報の走査 | 実施 | 常時必須の区分であり無条件に適用する |
| 契約テスト | 実施 | 判定プロンプト生成コマンドの引数を拡張し、証跡検証の期待 digest 生成順序を変えるため、既存の呼び出し形式が無変更で動作すること、および証跡の schema 版が変わらないことを固定する |
| 権限境界テスト | 実施 | ラウンド計数が trusted recorder actor の照合に依存するため、照合の有無で結果が変わることを確認する |
| E2E・アクセシビリティ・ユーザビリティ | 不要 | 利用者向けの画面・操作フローを含まない |
| 負荷・性能テスト | 不要 | 性能上の要求も想定される負荷の変化も無い。追加される外部呼び出しは 1 ゲート反復あたりの PR レビュー一覧取得のみ |
| データ移行・ロールバック整合テスト | 不要 | 永続データの構造変更を伴わない。判定記録のスキーマも証跡の schema 版も変更しない |
| 運用・障害復旧テスト | 不要 | デプロイ手順・監視設定を変更しない |
| 外部連携テスト | 不要 | 新規の外部連携先を追加せず、既存の PR レビュー取得経路を再利用する |

## 完了判定

- 上記の必須テストケースがすべて自動テストとして存在し成功する。
- ラウンド導出を GitHub API もファイルシステムも介さずに単体テストから直接呼び出せる。
- 変更後のリポジトリに対する既存の全自動テストが成功する（判定プロンプトの golden 比較・prompt digest 往復・判定プロトコルの既存テストの更新を含む）。
- 設定リファレンス文書に新設項目が記載され、設定文書同期の検査が成功する。
- 配布テンプレートと展開結果の同期検査、参照・用語の検査、文書長の検査が成功する。
