# PLAN: bugfix: resumeしたsegment workerがPR/Issueのレビューフィードバックを一切参照せず静的completion checklistだけで完了と自己判定する

- Issue: `ISSUE-446`
- 対応する DESIGN: `DESIGN.md`

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | roles.yaml静的ルール追加 | `.agent-skill-chain/config/roles.yaml` の `role_contracts.{spec_worker,design_worker,implementation_worker,validation_worker}.rules` へ「作業再開時は対象Issue/PRの最新レビュー・コメント（`review_status`セクションがあれば含む）を確認し、ファイル存在やcommit済みであることのみを根拠に完了と判定しない」旨の1行を追加する。既存の `rules` 配列の他要素・`inputs`/`outputs`/`completion`/`forbidden` は変更しない。 | `AC-1` | なし |
| 2 | test/helpers/gh-stub.ts 拡張 | 既存 `pr view <branch>` 分岐（`branch名`キー）が `number,state,headRefName,reviews,comments`（`latestReviews`ではなく`reviews`、2026-08-05再改定、ADR-0027）の統合`--json`フィールド指定に応答できるようにする。`state`は`OPEN`/`CLOSED`/`MERGED`のいずれも指定でき、`state`の値によって`reviews`/`comments`の返却内容を変えない（2026-08-05再改定、ADR-0028、下記#3参照）。`GhStub` インターフェースへ、reviewer毎の複数提出（時系列で`APPROVED`/`CHANGES_REQUESTED`/`COMMENTED`を任意順に混在させられる）を注入するヘルパー（例: `seedPrReviews(branch, reviews)`、各要素に`author`/`state`/`body`/`submittedAt`を持たせる）を追加する。加えて、`pr view <branch>` を非ゼロ終了させるヘルパー（例: `seedPrViewFailure(branch, { stderr })`）を追加し、stderrに`no pull requests found`を含むケース（PR未作成）と含まないケース（認証切れ等の一般障害）の両方を呼び出し側テストが指定できるようにする。新規に `gh api repos/{owner}/{repo}/pulls/<pr番号>/comments`（インラインレビューコメント取得、ADR-0027、`state`不問でPR解決成功時に呼ばれる、ADR-0028）へ応答する分岐を追加し、インラインレビューコメントを注入するヘルパー（例: `seedPrReviewThreadComments(prNumber, comments)`）と、当該API呼び出しを非ゼロ終了させるヘルパーを追加する。既存の `issue view --json comments` は変更不要（既に対応済み）。 | `AC-2, AC-3, AC-4, AC-5`（自動テストの前提条件） | なし |
| 3 | review-status.ts 新規作成 | `src/lib/review-status.ts` を新規作成する。`detectGithubReviewStatus(root, issueNumber)` と `detectLocalBlockingFindings(root, issueNumber, segment)`、およびそれぞれのYAML整形を担う `formatReviewStatusBlock(data)` を実装する。DESIGN.md「未対応の判定基準」節のとおり、レビューは `gh pr view --json ...,reviews`（`latestReviews`ではなく全レビュー提出履歴を返す`reviews`、2026-08-05再改定、ADR-0027）を対象に、reviewerごとに`state`が`APPROVED`または`CHANGES_REQUESTED`である提出のみを`submittedAt`昇順で最新の1件求め（`COMMENTED`提出は比較対象から除外）、最新提出が`CHANGES_REQUESTED`であるreviewerのみを未対応とする（`gh pr view --json reviewDecision`はbranch protection未設定リポジトリで常に空文字列を返すことを実機確認済みのため不採用、DESIGN.md「未対応の判定基準」参照）。コメントは時刻カットオフを行わず、`git` 呼び出しは一切行わない（`since`相当の値は持たない）。本文が `<!-- agent-skill-chain:` で始まらないものはすべて「未対応」とみなす（定型marker除外のみ、AC-4、ADR-0026）。marker prefixのハードコード重複を避けるため、`src/lib/review-evidence.ts` の `REVIEW_EVIDENCE_MARKER` を再利用し、`src/commands/report.ts` の非export定数 `MARKER`（値は同一prefixファミリー）はexportして共有するか、`review-status.ts` 側で独立に判定関数（prefix比較）を持つ場合もこれら2箇所の実測値と一致させる。投稿者（author）単位の除外は行わない——worker報告・進行役の修正依頼コメントが同一actorから投稿されるため。GitHubモードは「Issue側」（`gh issue view --json comments`、常に実行）と「PR側」（`resolveCurrentBranch` 成功時のみ、まずbranch名がAGENTS.mdのブランチ命名規則（`<type>/<issue-id>-<slug>`）に照らして対象issueNumberと一致するか（正規表現 `^[^/]+/${issueNumber}-`）を検証し、一致する場合のみ `gh pr view <branch> --json number,state,headRefName,reviews,comments` を直接1回呼ぶ。`findOpenPrByHead()`は経由しない。branch名が対象issueNumberと不一致の場合はbranch解決失敗と同じ明示的な失敗として扱う、2026-08-05再改定、design-gate blocking finding `PR_RESOLUTION_NOT_BOUND_TO_TARGET_ISSUE`是正。上記が成功した場合（PRの`state`は`OPEN`/`CLOSED`/`MERGED`いずれでも処理を変えない、2026-08-05再改定、design-gate blocking finding `CLOSED_PR_FEEDBACK_SILENTLY_DISCARDED`是正、ADR-0028——直前の実装は`state !== 'OPEN'`を「PR未作成」と同一視し取得済みの`reviews`/`comments`を破棄していたが、この分岐を撤廃する）、続けて`gh api repos/{owner}/{repo}/pulls/<pr番号>/comments`（REST API、既存の`paged()`/`pagedCheckRuns()`と同様の`per_page=100&page=N`反復）を呼びインラインレビューコメントを取得し、PR会話コメントと同じ基準で未対応コメント（`source: 'review_thread_comment'`）へ統合する。この呼び出しが失敗した場合はPR側検出全体を失敗にはせず、`gh pr view`で既に取得済みの`reviews`/`comments`は`prResult=成功`のまま保持し、当該失敗のみを独立した部分障害（`side: 'pr_review_thread_comments'`）として扱う（2026-08-05再改定、design-gate round10 blocking finding `INLINE_COMMENT_FETCH_FAILURE_DISCARDS_FETCHED_PR_REVIEWS`是正、ADR-0028。implementation-gate blocking finding `REVIEW-THREAD-COMMENTS-NOT-FETCHED`是正、ADR-0027）を独立に実行し、reviewerごとの未対応判定で「未対応」と確定した後（`CHANGES_REQUESTED`提出の`submittedAt`より新しい）に当該reviewerが`COMMENTED`を1件以上追加提出していれば、その全件の本文を`submittedAt`昇順の配列として同一エントリの`comment_bodies`（文字列配列、任意項目、件数上限なし、`UnresolvedGithubReview`への追加フィールド）として付加する（2026-08-05再々改定、design-gate round11 blocking finding `INTERMEDIATE_COMMENTED_REVIEW_BODIES_DROPPED_FOR_UNRESOLVED_REVIEWER`是正、ADR-0028。「未対応」の判定基準自体は変更しない。該当する`COMMENTED`提出が無ければ`comment_bodies`は含めない）。それぞれを「成功（0件以上のデータを取得。PR側の『PR未作成』も成功扱い）」「失敗（`gh`非ゼロ終了・JSON解釈失敗・branch解決失敗）」に正規化してから合成する（DESIGN.md「Issue側／PR側の分離」合成規則、design-gate指摘・2026-08-05再改定）。PR側の「PR未作成」判定は、`gh pr view`が非ゼロ終了かつstderrが`no pull requests found`に一致する場合のみとし、それ以外の非ゼロ終了・JSON解釈失敗は失敗として扱う（DESIGN.md「検出失敗（GitHubモード）」、design-gate blocking finding `PR_RESOLUTION_FAILURE_SILENTLY_TREATED_AS_NO_PR`是正）。**両方失敗**（Issue側・PR側の`gh pr view`自体がいずれも失敗）の場合のみ `{ detection: 'failed', reason }`（両側の理由を含む）を返す。この場合`gh pr view`自体が失敗しておりインラインコメント取得は試行されないため`side: 'pr_review_thread_comments'`は出現しない。**一方が成功・他方が失敗**の場合は、成功側で実際に検出した `unresolved_reviews`/`unresolved_comments`（0件でもよい）を保持したまま `{ detection: 'succeeded', ..., partial_failures: [{ side: 'issue' \| 'pr', reason }] }` を返す——失敗側の障害を理由に成功側の検出結果を破棄しない。**両方成功**（Issue側成功・PR側の`gh pr view`成功）の場合は、合計0件かつインラインコメント取得の失敗も無ければ `undefined`、いずれかに該当すれば `mode: 'github', detection: 'succeeded'`（`pr_number` はPR解決成功時のみ含める、`pr_number?: number`）を返す。**インラインレビューコメント取得の失敗**（`gh pr view`成功後のみ発生し得る、2026-08-05再改定、design-gate round10 blocking finding `INLINE_COMMENT_FETCH_FAILURE_DISCARDS_FETCHED_PR_REVIEWS`是正、ADR-0028）は、上記いずれの分岐とも独立に`partial_failures`へ`{ side: 'pr_review_thread_comments', reason }`を追加する。`partial_failures`の型は`side: 'issue' | 'pr' | 'pr_review_thread_comments'`の配列（0〜3要素、`issue`と`pr`は両方失敗時は`detection:'failed'`側へ倒れるため実質同時共存しない）となる。`segment.ts`側のブロック省略判定は、`detectGithubReviewStatus()`/`detectLocalBlockingFindings()`の返り値が`undefined`か否かのみで行い、`partial_failures`/`local_read_failures`/`source_segment`等の内容自体は`segment.ts`側で判定しない（DESIGN.md「AC-4との関係」「責務集中の確認」参照、design-gate指摘`OMISSION_LOGIC_DUPLICATED_IN_SEGMENT_TS`是正——`unresolved_reviews`/`unresolved_comments`/`unresolved_blocking_findings`が全て空であっても`partial_failures`/`local_read_failures`が非空の場合は`undefined`にしないという判定は`review-status.ts`側の返り値構築ロジックに一元化する）。ローカルモードは `spec`/`design`/`implementation`/`validation` 全segmentのgate reportを `tryReadYamlFile()` で走査し、`origin` が起動対象segmentに対応する値（`spec`→`specification`、他は同名）と一致する `blocking` findingのみを収集する。収集した各finding要素には、由来したgate reportのsegment名を`source_segment`（`spec|design|implementation|validation`のいずれか）として付加する（DESIGN.md「ローカルモードのgate report走査」参照、design-gate指摘`LOCAL_FINDING_PROVENANCE_LOST`是正）。ファイル不存在（`tryReadYamlFile()` が `undefined`）はそのsegment0件として継続し、YAML解釈失敗（例外）はsegment単位で`try/catch`する（他segmentの走査は継続）。**全segmentが失敗**した場合のみ `{ mode: 'local', detection: 'failed', reason }` を返す。**1segment以上が成功**（ファイル不存在による0件を含む）した場合は、収集済みのblocking finding（各`source_segment`付き）を保持したまま `{ mode: 'local', detection: 'succeeded', gate: segment, unresolved_blocking_findings: [...] }` を返し、1つでも読み込みに失敗したsegmentがあれば `local_read_failures: [{ segment, reason }]`（非空）を付加する（`undefined`にはしない、GitHub側の`partial_failures`と対称、design-gate blocking finding `LOCAL_MODE_PARTIAL_FAILURE_DISCARDS_DETECTED_FINDINGS`是正、ADR-0026）。全segment成功かつfindingが0件の場合のみ`undefined`（AC-4、セクション省略）を返す。`formatReviewStatusBlock(data)` は文字列連結を行わず、既存の `src/lib/yaml-io.ts` の `toYamlString()`（`segment.ts`/`lease.ts`で既に使用されている既存export）へ構造化データをそのまま渡し、戻り値をインデントして連結する——コメント本文（第三者が自由に書ける文字列）を手書きの文字列補間で埋め込まない（DESIGN.md「埋め込むコメント本文のシリアライズ安全性」、design-gate指摘・2026-08-05）。 | `AC-2, AC-3, AC-4, AC-5, AC-6` | `#2`（テスト実行に必要） |
| 4 | segment.ts 組み込み | `src/commands/segment.ts` の `start()` に、既存の `issueBlock` 構築と同列で `review-status.ts` の結果を呼び出し、非空（または検出失敗）の場合のみ `parts` へ `review_status:` ブロックを追加する処理を組み込む。`config.coordination.backend` による分岐は既存の `issueBlock` 分岐と対称に実装する。ローカルモード（`detectLocalBlockingFindings()`）へは従来どおり`repoRoot()`（`src/lib/paths.ts`、既存・変更なし）の返り値を渡す。GitHubモード（`detectGithubReviewStatus()`）へは別に`worktreeRoot()`（`src/lib/paths.ts`、既存・変更なし、`git rev-parse --show-toplevel`で現在の作業ツリー自身のrootを返す）の返り値を渡す（2026-08-05再改定、design-gate round11 blocking finding `PR_SIDE_DETECTION_STRUCTURALLY_FAILS_VIA_REPOROOT`是正、ADR-0028）——`worktreeRoot()`呼び出しを`try/catch`し、例外時は`repoRoot()`の返り値へフォールバックする（`segment start`をクラッシュさせない、AC-5）。`review-status.ts`側（`detectGithubReviewStatus`）のシグネチャ・内部実装は変更しない。 | `AC-1〜AC-7` の結線 | `#1, #3` |
| 5 | 単体テスト | `test/unit/review-status.test.ts`（新規）で `detectGithubReviewStatus`／`detectLocalBlockingFindings` の判定ロジック（**PRの`state`が`CLOSED`または`MERGED`でも、取得済みの`CHANGES_REQUESTED`レビュー・未対応コメントが`unresolved_reviews`/`unresolved_comments`から破棄されず引き続き検出される（`CLOSED_PR_FEEDBACK_SILENTLY_DISCARDED`是正の回帰防止、ADR-0028）**、**「PR未作成」（成功・0件）として扱われるのは`gh pr view`が`no pull requests found`で失敗した場合のみであり、`state`の値そのものではこの判定を行わない**、**reviewerが`CHANGES_REQUESTED`提出後、確定した未対応判定より新しい`COMMENTED`提出が1件あれば、その本文が同一エントリの`comment_bodies`（配列）へ1件追加される（`INTERMEDIATE_COMMENTED_REVIEW_BODIES_DROPPED_FOR_UNRESOLVED_REVIEWER`是正の回帰防止、ADR-0028）**、**同一reviewerが未対応判定より新しい`COMMENTED`を複数回（例: 3件）追加提出した場合、`comment_bodies`に3件すべてが`submittedAt`昇順で含まれる（最新1件のみに切り詰められないこと、`INTERMEDIATE_COMMENTED_REVIEW_BODIES_DROPPED_FOR_UNRESOLVED_REVIEWER`是正の回帰防止、ADR-0028）**、**`CHANGES_REQUESTED`提出が無い（未対応と確定していない）reviewerの`COMMENTED`提出は`comment_bodies`を含め一切`unresolved_reviews`へ現れない（未対応判定基準自体は変更されないことの回帰防止、SPEC.md AC-2のGiven節が`CHANGES_REQUESTED`限定であることに基づく設計判断の確認）**、**reviewerが`CHANGES_REQUESTED`提出後に`COMMENTED`のみを追加提出した場合も引き続き未対応として検出される（`LATEST-REVIEWS-MASKS-CHANGES-REQUESTED`是正の回帰防止、ADR-0027）**、**reviewerが`CHANGES_REQUESTED`提出後に`APPROVED`を提出した場合は未対応から除外される**、**同一reviewerの複数回の`CHANGES_REQUESTED`→`COMMENTED`→`CHANGES_REQUESTED`のような混在履歴でも時系列で正しく最新の`APPROVED`/`CHANGES_REQUESTED`のみを評価する**、**PR側検出が`gh pr view`成功後にインラインレビューコメント取得（`gh api pulls/comments`）まで到達し、取得したコメントが`source: 'review_thread_comment'`で`unresolved_comments`へ統合される（`REVIEW-THREAD-COMMENTS-NOT-FETCHED`是正の回帰防止、ADR-0027）**、**インラインレビューコメント取得が非ゼロ終了・JSON解釈失敗しても、`gh pr view`で既に取得済みの`unresolved_reviews`/`unresolved_comments`（`CHANGES_REQUESTED`レビューを含む）は破棄されず`detection: 'succeeded'`のまま保持され、`partial_failures`に`side: 'pr_review_thread_comments'`のみが追加される（PR側全体は失敗として扱われない、`INLINE_COMMENT_FETCH_FAILURE_DISCARDS_FETCHED_PR_REVIEWS`是正の回帰防止、ADR-0028）**、CHANGES_REQUESTED抽出、`<!-- agent-skill-chain:`定型marker本文の除外、時刻カットオフ無しでの全非markerコメント検出、Issue側・PR側の両方が失敗した場合の`detection: 'failed'`、**Issue側成功（コメントN件、0件を含む）かつPR側失敗（branch解決失敗／`gh pr view`失敗）の場合は`detection: 'succeeded'`でIssue側の検出結果を保持しつつ`partial_failures`に`side: 'pr'`を含める**、**逆にPR側成功（レビュー・コメント有り）かつIssue側失敗の場合も同様にPR側の検出結果を保持しつつ`partial_failures`に`side: 'issue'`を含める**、**`gh pr view`が非ゼロ終了しstderrが`no pull requests found`に一致する場合はPR未作成（非失敗・0件）としてIssue側が非空なら`succeeded`かつ`partial_failures`なし**、**`gh pr view`が非ゼロ終了しstderrが上記文言に一致しない場合（認証切れ等）はPR側を失敗として扱い、Issue側成功時は`partial_failures`に`side: 'pr'`を含める**、**branch名が対象issueNumberのブランチ命名規則（`<type>/<issue-id>-<slug>`）と不一致の場合は`gh pr view`を呼ばずPR側を失敗として扱い、Issue側成功時は`partial_failures`に`side: 'pr'`を含める（他Issueのbranch・`main`等での誤検出防止、`PR_RESOLUTION_NOT_BOUND_TO_TARGET_ISSUE`是正）**、両方成功し合計0件の場合のundefined、origin一致によるcross-segment gate report検出、ローカルgate report不存在時は0件継続、**ローカルモードで1segmentのYAML破損があっても他segmentで検出済みのblocking findingは保持したまま`local_read_failures`（非空）を付加した`succeeded`を返す**、**ローカルモードでfindingが0件でも1segmentが読み込み失敗していれば`undefined`にはせず`local_read_failures`付きの`succeeded`を返す**、**ローカルモードで`spec`/`design`/`implementation`/`validation`全segmentのYAMLが破損している場合のみ`detection: 'failed'`を返す**、**ローカルモードで複数segmentのgate reportにorigin一致のblocking findingがある場合、各finding要素の`source_segment`が由来したgate reportのsegment名と一致する**、PR未作成かつIssueコメントも無い場合のundefined）を直接検証する。`formatReviewStatusBlock`がコメント本文中の`rules:`や`---`等YAML構造を模した文字列を含む入力に対しても構造を破壊しない（`toYamlString`経由でエスケープされる）ことを検証するケースを追加する。`test/unit/roles.test.ts` に `role_contracts.*.rules` へのAC-1文言を検査するケースを追加する。 | `AC-1, AC-2, AC-3, AC-4, AC-5, AC-6` | `#1, #3` |
| 6 | 結合テスト | `test/integration/github-backend.test.ts` へ、`gh-stub` 経由でCHANGES_REQUESTEDレビューあり／未対応コメントあり／全てAPPROVEDかつコメント無し／`gh` 呼び出し失敗（Issue側・PR側**両方**）／**対象branchのPRが`CLOSED`または`MERGED`の状態で`CHANGES_REQUESTED`レビュー・未対応コメントが実在する場合、`segment start`のプロンプトに引き続きそれらが同梱される（`CLOSED_PR_FEEDBACK_SILENTLY_DISCARDED`是正の回帰防止、ADR-0028）**／**Draft PR作成前（PR未解決、stderrが`no pull requests found`）でもIssueコメントのみで検出される**／**Issue側は成功しPR側のみ`gh`呼び出しが失敗（stderrが`no pull requests found`と不一致）する場合、Issue側の未対応コメントがプロンプトへ含まれたまま`partial_failures`相当の失敗通知も同梱される（成功側の検出結果が消えないこと）**／**現在のbranchが対象issueNumberとは異なるIssue（または`main`）の命名規則を持つ場合、`gh pr view`が呼ばれずPR側が失敗として扱われ、Issue側が成功していればその検出結果は保持されたまま失敗通知も同梱される（`PR_RESOLUTION_NOT_BOUND_TO_TARGET_ISSUE`是正の回帰防止）**／**reviewer-aが`CHANGES_REQUESTED`提出後に`COMMENTED`のみを追加提出した状態で`segment start`を呼ぶと、プロンプトの`unresolved_reviews`に当該reviewerのレビューが引き続き含まれる（`LATEST-REVIEWS-MASKS-CHANGES-REQUESTED`是正の回帰防止、ADR-0027）**／**PRの差分行にインラインレビューコメントが投稿されている状態で`segment start`を呼ぶと、プロンプトの`unresolved_comments`に`source: 'review_thread_comment'`のエントリが含まれる（`REVIEW-THREAD-COMMENTS-NOT-FETCHED`是正の回帰防止、ADR-0027）**／**`CHANGES_REQUESTED`レビューが実在する状態で`gh pr view`は成功するがインラインレビューコメント取得（`gh api pulls/comments`）のみ非ゼロ終了する場合、プロンプトの`unresolved_reviews`に当該`CHANGES_REQUESTED`レビューが引き続き含まれたまま、`partial_failures`に`side: 'pr_review_thread_comments'`が同梱される（PR側全体が失敗として扱われないこと、`INLINE_COMMENT_FETCH_FAILURE_DISCARDS_FETCHED_PR_REVIEWS`是正の回帰防止、ADR-0028）**、**メイン作業ツリーとは別に作成した一時git worktree（`git worktree add`）を対象branchでcheckoutし、そのworktree配下にcwdを設定した状態（`worker-launch.sh`経由の通常起動経路を模す）で`segment start`を呼び出すと、PR側検出がそのworktree自身のbranchを正しく解決し、対象PRの`CHANGES_REQUESTED`レビュー・未対応コメントが`unresolved_reviews`/`unresolved_comments`へ含まれる（`worktreeRoot()`が正しく使われ`repoRoot()`のメイン作業ツリー正規化の影響を受けないこと、`PR_SIDE_DETECTION_STRUCTURALLY_FAILS_VIA_REPOROOT`是正の回帰防止、ADR-0028）**、の12パターンで `segment start` を呼び出し、出力プロンプトを検証するケースを追加する。ローカルモード（`test/integration/issue-lifecycle.test.ts` 等の既存ローカルbackendスイート）へ、`reviews/<segment>.yaml` に blocking finding がある場合／無い場合、他segmentのgate reportにorigin一致のblocking findingが記録されている場合（cross-segment差し戻し、収集された各finding要素の`source_segment`が実際の由来segment名と一致することを検証する）、対象segmentのgate report自体が存在しない場合（0件継続、failedにならないこと）、**他segmentで検出済みのblocking findingがある状態で別の1segmentのYAMLが破損している場合（`succeeded`＋`local_read_failures`が同梱され、検出済みfindingが消えないこと）**、および**`spec`/`design`/`implementation`/`validation`全segmentのYAMLが破損している場合（`detection: 'failed'`）**の `segment start` 出力を検証するケースを追加する。 | `AC-2, AC-3, AC-4, AC-5, AC-6` | `#2, #4` |
| 7 | 実地再現確認 | 本Issueの独立検証セグメントで、Issue #441の再現手順と同型の状況（design-gateでblocking finding→PRへ修正依頼コメント→`worker-launch.sh` でdesign_worker再起動）を用意し、実際のworker完了報告・作業ログに当該フィードバックへの具体的言及があることを確認する。 | `AC-7` | `#4, #6` |

## 実装順序の見直しについて

実装中に作業順序（上記の変更単位の並び）のみを見直す場合は、本ファイルのみを更新すればよい。設計要素・責務・境界そのものを変更する場合は、DESIGN.md の更新（および設計ゲートの再通過）が必要になる点に注意する。
