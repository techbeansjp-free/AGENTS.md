# PLAN: quick免除が解除されたIssueでも成果物が1つも無いセグメントが必須成果物検査の対象にならない欠陥の是正

- Issue: `ISSUE-741`
- 対応する DESIGN: `DESIGN.md`

## 目的・対象範囲

本計画は、必須成果物検査を適用するセグメントの集合（以下 R）を開始済みセグメント集合（以下 S）の上流閉包へ拡張する変更を、どの順序で・どの単位に分割して実装し、何をもって完了と判定するかを定める。設計要素そのものの定義は本計画の対象外であり、実装順序のみを扱う。実装途中で順序だけを見直す場合は本ファイルのみを更新する。

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | セグメント連鎖順序の導出 | セグメント定義の `next` を先頭要素から辿って固定順を導出する関数を、セグメント定義読み込みの責務へ追加する。先頭要素は他のどの要素の `next` にも現れない要素とし、鎖が一本に定まらない場合は例外とする | `AC-15` | なし |
| 2 | 対象集合導出の純関数 | S・連鎖順序・閉包追加可否の3つをすべて引数として受け取り R を導出し、各要素へ閉包追加標識を付す関数を新設する。外部入出力を持たず、連鎖順序を自ら読み込まないためセグメント定義読み込みへも依存しない（`#1` への依存は実装順序上の先行関係のみで、`#1` の導出結果は呼び出し側が引数として渡す）。S が空なら空、閉包追加可否が偽なら S と同一、真なら S の最下流要素より上流かつ閉包追加除外集合（実装・独立検証）に属さない要素を加える | `AC-3`, `AC-6`, `AC-8`, `AC-9`, `AC-15` | `#1` |
| 3 | quick 判定への解決可否の追加 | 判定結果へ「シグナル入力自体を取得できたか」を追加する。未解決条件は両モードで同一構成の2条件とする。第1条件は取得操作が成功しないこと（GitHub モードはラベル取得コマンドの非0終了、ローカルモードは Issue 状態ファイルの読み取り操作の失敗。ファイル不在を含む）、第2条件は取得できたが内容を解釈できないこと（GitHub モードは応答の JSON 解釈失敗、ローカルモードは状態ファイル内容の構造解釈失敗）である。ローカルモードは現行実装が不在を「quick 未要求」へ畳み込み、読み取りと解釈の失敗を例外として送出するため、いずれも捕捉して未解決へ割り当てる。未解決時に提示する固定書式のメッセージを追加する。既存の要求有無・免除可否・解除理由の判定は変更しない。例外送出を捕捉へ変えることで既存の呼び出し側（必須成果物検査コマンド・実装セグメントの作業契約生成）の挙動が変わりうるため、両者の挙動を「必須テストケース」で固定する | `AC-4`, `AC-10`, `AC-12`, `AC-13` | なし |
| 4 | 必須成果物検査コマンドの対象集合適用 | S を引数で受け取る呼び出し形式を追加する。`#2` と `#3` を組み合わせて R を確定し、対象集合の要約（各要素と閉包追加標識）を標準エラー出力へ書き、R の各セグメントについて既存の成果物存在判定を適用して欠落を集約する。欠落メッセージはセグメント名と成果物名を含み、閉包追加分については当該セグメントが未開始であること自体が失敗理由であると読み取れる文言を加える。単一セグメントを引数で受ける従来形式は閉包追加を行わない従来どおりの挙動として残す | `AC-4`, `AC-5`, `AC-6`, `AC-7`, `AC-8`, `AC-9`, `AC-10`, `AC-11`, `AC-12`, `AC-13` | `#2`, `#3` |
| 5 | 開始済みセグメント集合の解決失敗を空集合へ読み替えない | CI ワークフローの当該実行段で終了コードの握り潰しを解消し、導出失敗が空の S として下流へ渡らないようにする。あわせて、S 導出段より前に base 取得が保証されていること（取得深度が履歴を切り詰めないこと・base 取得段が先行すること・握り潰しが無いこと）を、本リポジトリの自動テストとして実行するワークフロー定義の静的検査として追加する。検査対象は本リポジトリ内の展開結果と配布テンプレートの両方のワークフロー定義であり、この静的検査は配布物へ含めず配布先の CI では実行しない（DESIGN の D5 で確定）。S 導出スクリプトの失敗時メッセージは変更しない。要件6 が現行動作の維持を求めており、現行実装は base 解決失敗と差分算出失敗のそれぞれで互いに区別できる理由を標準エラー出力へ提示済みだからである | `AC-2` | なし |
| 6 | CI ワークフローの権限・トークン付与と呼び出し変更 | 権限宣言へ Issue の読み取りを追加する。必須成果物検査を実行する段の環境変数へトークンとリポジトリ識別子を与え、セグメントごとの繰り返し呼び出しを `#4` の一括呼び出しへ置き換える。配布テンプレートと展開結果を同一に保つ | `AC-14`, `AC-15` | `#4`, `#5` |
| 7 | 自動テストの追加・更新 | 下記「検証手順」の全項目に対応する単体テスト・結合テストを追加する。既存の必須成果物検査の結合テスト、CI ワークフローの静的検査テスト、検査対象判定の直接実行テストのうち、変更単位により影響を受けるものを更新する | `AC-1`〜`AC-16` | `#1`〜`#6` |

## 検証手順と観測単位

**すべての完了条件を、CI ジョブ全体の実行結果ではなく、当該検査を単体で直接実行した結果を観測単位として判定する。** 非 Draft PR では root 直下成果物の検査が必須成果物検査より前の段で必ず失敗しジョブが停止するため、ジョブ全体を観測単位に置くと、実装が正しくても完了条件が観測不能になり不成立と読めてしまう。この限定は完了条件の第1項だけでなく全項目に等しく適用する。

具体的には次の3種類の直接実行を用いる。

1. 対象集合導出の純関数を単体テストから直接呼び出す（CI ワークフローも Git も介さない）。
2. 必須成果物検査コマンドを一時リポジトリに対して直接実行し、終了コードと標準エラー出力を確認する。
3. CI ワークフロー定義から実行段の記述本文を取り出し、同等のシェルとして単体で実行して終了コードと出力を確認する。あわせてワークフロー定義の内容自体を静的に解析する。いずれも本リポジトリの自動テストとして、本リポジトリ内のワークフロー定義に対して実行する。

## 必須テストケース

次のケースは網羅の要であり、いずれも省略しない。

| ケース | 状態 | 期待 | 対応 AC-ID |
|---|---|---|---|
| 閉包追加あり・充足 | quick 未要求、S に上流の欠けたセグメントがあり、閉包追加分と開始済み分の必須成果物がすべて存在（閉包追加分はブランチ差分に現れないワークツリー上のファイルとして成果物存在述語を満たす） | 成功 | `AC-8` |
| **閉包追加あり・充足（履歴実績による充足を含む）** | quick 未要求、S に上流の欠けたセグメントがあり、R の必須成果物のうち1つ以上が「当ブランチで追加後に削除した履歴を持つファイル」——ワークツリー上には存在せず、base から HEAD までの履歴に追加が現れることによって成果物存在述語を満たすファイル——であり、残りも含めて R の必須成果物がすべて成果物存在述語を満たす | 成功。ワークツリー上に当該ファイルが無いことを理由に失敗させない | `AC-8` |
| 閉包追加あり・閉包追加分が欠落 | quick 未要求、設計・実装計画セグメントが未開始で当該成果物も存在しない | 失敗。未開始であること自体が失敗理由と読み取れる文言を含む | `AC-9` |
| 閉包追加あり・開始済み分のみ欠落 | quick 未要求、閉包追加分は存在するが開始済みセグメントの成果物が欠落 | 失敗 | `AC-9` |
| **閉包追加あり・閉包追加分と開始済み分が同時に欠落** | quick 未要求、設計・実装計画セグメントが未開始で当該成果物も存在せず、同時に開始済みセグメントの成果物も欠落 | 失敗。両方の欠落をセグメント名と成果物名の対で列挙し、閉包追加分については未開始であること自体が失敗理由と読み取れる文言を含む | `AC-9` |
| **免除解除かつ閉包追加あり** | quick 要求だがガードレール抵触で解除、かつ閉包追加対象が存在し必須成果物が欠落 | 失敗。解除理由と、欠落したセグメント名・成果物名の両方を提示 | `AC-11` |
| **免除解除かつ閉包追加なし** | quick 要求だが解除、閉包追加対象は無く開始済み分のみ欠落 | 失敗 | `AC-11` |
| **免除解除かつ ISSUE-692 の再現条件** | quick 要求だが変更差分がスキーマ定義を含むため解除。実装セグメント・独立検証セグメントが開始済みである一方で設計・実装計画セグメントが開始されておらず `DESIGN.md`・`PLAN.md` が成果物存在述語を満たさない | 失敗。解除理由と、閉包追加された設計・実装計画セグメントの欠落成果物名を提示し、当該セグメントが未開始であること自体が失敗理由と読み取れる文言を含む | `AC-11` |
| 免除解除かつ充足 | quick 要求だが解除、閉包追加の有無を問わず R の必須成果物がすべて存在 | 成功。解除理由は提示する | `AC-10` |
| 免除有効・充足 | quick 要求かつ risk が normal かつガードレール非抵触。開始済みセグメントの非免除成果物がすべて存在 | 成功。閉包追加なし。免除対象の成果物は要求しない | `AC-4` |
| **免除有効・欠落** | quick 要求かつ risk が normal かつガードレール非抵触。開始済みセグメントの非免除成果物のうち1つ以上が欠落 | 失敗。欠落したセグメント名と成果物名を提示。免除の有効性を理由に検査自体を省略しない | `AC-5` |
| **シグナル未解決・充足** | GitHub モードの2経路（ラベル取得コマンドの非0終了／終了コード0だが JSON として解釈できない応答）とローカルモードの2経路（Issue 状態ファイルの読み取り操作の失敗／状態ファイル内容を quick シグナルを保持する構造として解釈できない）の計4経路それぞれについて、開始済み分は充足 | 成功。4経路とも未解決として扱い、免除も閉包追加も適用しない旨を提示。ローカルモードの2経路が例外として送出され検査が続行しない実装は本ケースで失敗する | `AC-12` |
| **シグナル未解決・欠落** | 同じ4経路それぞれについて開始済み分が欠落 | 失敗。免除も閉包追加も適用しない旨に加え、欠落をセグメント名と成果物名の対で提示する。閉包追加対象があっても当該分の欠落では失敗しない | `AC-13` |
| **ローカルモードの状態ファイル不在** | ローカルモードで Issue 状態ファイルが存在せず、S が空でなく閉包追加対象が有り、閉包追加対象の必須成果物は欠落し開始済み分は充足 | 成功。不在を未解決（第1条件）として扱い、免除も閉包追加も適用しない旨を提示する。不在を「解決成功かつ quick 未要求」と分類した実装では閉包追加が働き本ケースが失敗するため、分類の違いがテスト結果として現れる | `AC-12`, `AC-13` |
| **quick 判定の既存呼び出し側の挙動固定** | ローカルモードで Issue 状態ファイルが存在するが読み取り操作に失敗する場合と、内容を quick シグナルを保持する構造として解釈できない場合のそれぞれについて、(a) 必須成果物検査コマンドを直接実行する（R の必須成果物がすべて存在する場合と、1つ以上欠落する場合の両方）、(b) 実装セグメントの作業契約生成コマンドを直接実行する | (a) 例外で中断せず未解決として扱い、免除を適用せずセグメント定義の全 `outputs` を要求して判定へ到達する。すべて存在すれば終了コード0、欠落があれば終了コード非0で欠落をセグメント名と成果物名の対で提示する。汎用の予期しないエラーとして終了する実装は本ケースで失敗する。(b) 本変更の適用前後で終了コードと標準エラー出力が変わらない（同じ状態ファイルを quick 判定より前に同じ読み取り関数で読むため、読み取り・解釈の失敗はそこで中断する）。quick 判定側の変更が当該コマンドへ波及した実装は本ケースで失敗する | `AC-12`, `AC-13` |
| 主経路 | quick 未要求。要求・要件から順に成果物を追加した状態、および要求・要件と設計・実装計画と独立検証が開始済みで実装セグメントだけが未開始の文書のみの Issue。いずれも未開始の上流要素は実装セグメントのみで、同セグメントは閉包追加除外集合に属するため閉包追加は起きない | 成功 | `AC-6` |
| 成果物削除 | base 側にのみ存在した成果物を当ブランチで削除 | 失敗 | `AC-7`, `AC-9` |
| S 空 | 差分がどのセグメントにも対応しない | 成功。検査を1回も実行しない | `AC-3` |
| S 解決失敗 | base を解決できない場合と差分を算出できない場合を、別々のケースとして実行する（原因を base 解決失敗に限定しない） | いずれも失敗。理由を標準エラー出力へ提示し、両者が互いに区別できる文面である（現行のメッセージを維持する） | `AC-2` |
| S 解決失敗の前提保証 | 本リポジトリの自動テストとして、本リポジトリ内の展開結果と配布テンプレート両方のワークフロー定義を静的に解析する（配布物には含めず配布先 CI では実行しない） | リポジトリ取得の深度が履歴を切り詰めず、base 取得段が S 導出段より前にあり、S 導出段が導出失敗を空の S へ読み替えない。3点のいずれかを欠いた定義では検査が失敗する | `AC-2` |
| 検査対象判定 | Dependabot ブランチかつ作成者が Dependabot、root-cleanup ブランチかつ作成者が admin。ワークフロー定義から当該実行段の本文を取り出し、段の `env` だけを入力として単体実行する | 検査省略。段の本文が他の段の出力へ依存する形へ変わった場合は、この取り出し実行自体が成立せず失敗として現れる | `AC-1` |
| 検査対象判定の不能 | Issue 番号を解決できず許可条件にも一致しない、root-cleanup 形式だが非 admin、権限確認自体が失敗。同じく段の本文を取り出して単体実行する | 失敗。理由を提示 | `AC-16` |
| ワークフロー定義 | 展開結果と配布テンプレートの両方を静的に解析する | 必須成果物検査の段に Issue 読み取り権限とトークンがあり、両ファイルが一致する。「R の導出規則を含まない」ことは、必須成果物検査を呼び出す実行段の本文に閉包追加対象の判定・上流判定に相当する記述（セグメント名の列挙・連鎖順序に基づく位置比較・集合演算）が現れないことを確認して判定する | `AC-14`, `AC-15` |

## 適用するテスト種別の判断

| 区分 | 判断 | 根拠 |
|---|---|---|
| lint / format・型検査・単体テスト・変更範囲の結合テスト・静的セキュリティ検査・依存関係と秘密情報の走査 | 実施 | 常時必須の区分であり無条件に適用する |
| 権限境界テスト | 実施 | CI ワークフローへ Issue 読み取り権限を追加するため、付与の有無と対象範囲を静的検査で確認する |
| 契約テスト | 実施 | 必須成果物検査コマンドの呼び出し形式を追加するため、従来形式の挙動が不変であることを確認する。あわせて quick 判定は複数のコマンドが共有するコンポーネントであり、返り値への項目追加だけでなく例外送出から捕捉への制御の変更を伴うため、quick 判定を利用する既存の全呼び出し側（必須成果物検査コマンド・実装セグメントの作業契約生成）の挙動を「必須テストケース」の該当ケースで固定する |
| E2E・アクセシビリティ・ユーザビリティ | 不要 | 利用者向けの画面・操作フローを含まない |
| 負荷・性能テスト | 不要 | 性能上の要求も想定される負荷の変化も無い。追加されるのは1回の Issue ラベル取得のみ |
| データ移行・ロールバック整合テスト | 不要 | 永続データの構造変更を伴わない |
| 運用・障害復旧テスト | 不要 | デプロイ手順・監視設定を変更しない |
| 外部連携テスト | 不要 | 新規の外部連携先を追加せず、既存の Issue ラベル取得経路を再利用する |

## 完了判定

- 上記の必須テストケースがすべて自動テストとして存在し成功する。
- 対象集合導出を CI ワークフローも Git も介さずに単体テストから直接呼び出せる。
- S 導出段の前提（取得深度が履歴を切り詰めない・base 取得段の先行・導出失敗を空集合へ読み替えない）が、本リポジトリの自動テストとして実行される静的検査として存在し成功する。
- 展開結果と配布テンプレートのワークフロー定義が一致する。
- 変更後のリポジトリに対する既存の全自動テストが成功する。
