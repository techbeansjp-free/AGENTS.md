<!--
このファイルはAGENTS.mdが定める4セグメント・4ゲートの規約に基づく雛形であり、Issue毎に複製して使う（セグメント: design、成果物: PLAN.md。DESIGN.md とは別ファイル）。
設計（何を・なぜ・どの構造にするか）と実装計画（どの順序で・どの変更単位で実装するか）は責務が異なる。
実装途中で作業順序だけを見直す場合、DESIGN.md 自体を変更する必要はない。
<...> のプレースホルダを実際の内容に置き換えて記入すること。
-->

# PLAN: Issue成果物テンプレート・ADRテンプレート自体がAGENTS.md禁止のセクション番号参照を含み全Issueへ伝播する

- Issue: `ISSUE-592`
- 対応する DESIGN: `DESIGN.md`

## 実装順序・変更単位

DESIGN.md のコンポーネント構成（対応表）で定義した書き換え内容を、以下の順序で実装する。各変更単位は独立したテキスト置換であり、相互依存は無い。1つの変更単位（commit）にまとめて実施してよい。

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | `templates/issue/SPEC.md` の由来行書き換え | 冒頭コメント2行目を、DESIGN.mdの対応表に記載した書き換え後の文言へ置換する | `AC-1, AC-2` | なし |
| 2 | `templates/issue/DESIGN.md` の由来行書き換え | 冒頭コメント2行目を、DESIGN.mdの対応表に記載した書き換え後の文言へ置換する | `AC-1, AC-2` | なし |
| 3 | `templates/issue/PLAN.md` の由来行書き換え | 冒頭コメント2行目を、DESIGN.mdの対応表に記載した書き換え後の文言へ置換する | `AC-1, AC-2` | なし |
| 4 | `templates/issue/VALIDATION.md` の由来行書き換え | 冒頭コメント1行目を、DESIGN.mdの対応表に記載した書き換え後の文言へ置換する | `AC-1, AC-2` | なし |
| 5 | `templates/adr/ADR.md` の由来行書き換え | 冒頭コメント2行目を、DESIGN.mdの対応表に記載した書き換え後の文言へ置換する | `AC-1, AC-2` | なし |
| 6 | 検証 | `.agent-skill-chain/scripts/lint-references.sh` を対象5ファイルへ明示パス指定で実行し違反ゼロ・終了コード0を確認する。続けて引数無し（既定のリポジトリ全体走査）で実行し、対象5ファイル起因の違反が無いこと、かつ他の生きたファイルへの新規違反（回帰）が無いことを確認する。`.agent-skill-chain/ci/verify-doc-length.sh` を対象5ファイルへ実行し行数上限超過が無いことを確認する | `AC-1, AC-2, AC-3` | `#1, #2, #3, #4, #5` |

<!-- 変更単位を追加する場合は # を連番で追加する -->

## 実装順序の見直しについて

実装中に作業順序（上記の変更単位の並び）のみを見直す場合は、本ファイルのみを更新すればよい。設計要素・責務・境界そのものを変更する場合は、DESIGN.md の更新（および設計ゲートの再通過）が必要になる点に注意する。#1〜#5は対象ファイルが互いに独立しているため任意の順序で実施でき、並行実施も可能である。#6（検証）は#1〜#5の完了後に実施する。
