# PLAN: npm 配布物から runtime 履歴・自己拡張固有文書・インストール状態を除外する

- Issue: `ISSUE-244`
- 対応する DESIGN: `DESIGN.md`

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | 配布許可リスト | `package.json` で `.agent-skill-chain/` 全体ではなく導入対象8 namespaceを指定する | `AC-1`, `AC-2` | なし |
| 2 | package-files 検証 | 配布禁止パスと全必須 namespace の代表資産を `npm pack --dry-run --json` の結果で検査する | `AC-1`, `AC-2`, `AC-3` | `#1` |
| 3 | 回帰検証 | package-files テスト、型検査、全テスト、pack の実測を実行する | `AC-1`, `AC-2`, `AC-3` | `#1`, `#2` |

## 実装順序の見直しについて

配布物の変化を先に最小化し、同じ実測コマンドを使う統合テストで契約を固定する。新しい導入対象 namespace を将来追加する場合は、許可リストと必須資産テストを同時に更新する。
