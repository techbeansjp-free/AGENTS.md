# PLAN: env トークン非設定の資格情報ストア限定環境でゲートレビュアが認証できない

- Issue: `ISSUE-758`
- 対応する DESIGN: `DESIGN.md`

## 目的・対象範囲

DESIGN.md が定めた設計要素を、どの順序で・どの単位に分割して実装するかを定める。対象は
`.agent-skill-chain/adapters/claude.sh`、`.agent-skill-chain/standards/TEST_POLICY.md`、および claude アダプタの
ゲートレビュア起動経路を駆動する結合テストである。設計要素・責務・境界そのものの変更は本書では扱わない。

## 前提

- 変更対象のアダプタは1ファイルのシェルスクリプトであり、隔離サブプロセス構成関数・隔離プローブ関数・
  診断関数・ゲートレビュア起動関数がいずれも同一ファイル内にある。分割配置は本 Issue では行わない。
- 既存の結合テストは、実際のシェルでアダプタを駆動し、実行系（レビュア本体・隔離プローブ）を上書きで
  差し替える形をとる。本 Issue の追加テストも同じ形をとる。
- 上書きにより差し替えてよい実行系は、資格情報ストアへ問い合わせるコマンド・隔離環境の認証確認・
  レビュア実行系の3つに限られる。分類の検出、取得可否の判定、認証ファイルの配置、設定ディレクトリの
  決定と子プロセス環境の構成、復帰時の隔離領域削除は実経路のまま検証する。

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | 取得コマンド解決（`D-STORE-RESOLVE`） | 取得コマンド文字列と時間上限を解決する関数を追加する。上書き変数が呼び出し元環境にあればそれを用い、無ければ macOS かつ既定の問い合わせコマンドが実行可能な場合に限り既定値を与え、いずれでもなければ取得手段なしと解決する。時間上限の指定値が正整数でない場合も取得手段なしとして扱う。上書き変数は基底環境集合へ加えない | `AC-4` `AC-7` | なし |
| 2 | 取得と配置（`D-STORE-STAGE`） | 配置先の認証ファイルを空で先行作成して 0600 にし、取得ステップを独自プロセスグループで起動して標準出力を当該ファイルのみへリダイレクト、標準エラーを破棄する。監視プロセスで時間上限を強制し、上限到達時はプロセスグループへ終了要求後に強制終了する。終了確認後、値を出力しない構造検査を行い、取得結果状態を記録する。作業用ファイルは隔離設定ディレクトリの外へ置く。`staged` 以外の状態では配置した認証ファイルを削除する。呼び出し側へは失敗を返さない | `AC-2` `AC-5` `AC-6` `AC-8` `AC-13` | `#1` |
| 3 | 分類判定の結線（`D-CLASS-SELECT`） | 隔離サブプロセス構成関数の認証素材準備部で、レビュー対象アダプタが claude であり、呼び出し元の環境変数トークンがいずれも空で、かつ分類Bの複製が行われなかった場合に限り `#2` を起動する。基底環境集合と設定ディレクトリの決定は変更しない | `AC-1` `AC-11` | `#2` |
| 4 | 診断の拡張（`D-DIAGNOSTIC`） | 認証不成立時の診断へ、分類Cの取得結果状態に基づく記述と、設定ディレクトリが常に隔離領域内を指す旨を追加する。分類C限定構成を「利用できない」と断定する既存の記述を、取得結果状態に応じた記述へ置き換える。実値・認証ファイルの内容・取得ステップの標準エラーは出力しない | `AC-3` `AC-12` | `#3` |
| 5 | 実機確認規約と模倣前提（`D-ASSUMPTION-REGISTRY`） | `.agent-skill-chain/standards/TEST_POLICY.md` へ、模倣前提の識別子・前提・前提が外れた場合に観測される挙動の対応表と、分類C経路を変更する Issue に対する macOS 実機確認の実施時期・実行者・記録先の規約を追加する | `AC-10` | なし |
| 6 | 分類C成立の回帰テスト | 取得コマンド・隔離プローブ・レビュア実行系を差し替えた分類C構成で、隔離設定ディレクトリへ資格情報が配置され verdict が返り `final` が確定値になることを検証する。差し替えの代替は資格情報の実値をコマンド文字列・引数・環境変数へ置かず、期待値をファイルから読み取る | `AC-2` | `#3` |
| 7 | 分類A・分類Bの回帰テストと非問い合わせ検証 | 分類A構成・分類B構成それぞれで、取得ステップの起動記録が1件も無いこと、verdict が返り `final` が確定値になることを検証する。取得コマンドの代替は起動事実を記録して失敗する | `AC-9` `AC-11` | `#3` |
| 8 | 設定ディレクトリと環境変数集合の検証 | 3分類すべてで `CLAUDE_CONFIG_DIR` が隔離領域配下を指し呼び出し元設定ディレクトリとも呼び出し元ホーム配下とも一致しないことを検証する。あわせて、レビュア実行系の代替が自プロセスの環境変数名と値を隔離領域内のファイルへ記録し、必須集合11個の存在・追加許容集合3個以外の変数名の不在・`HOME` の値・全変数値への実値非出現を検証する | `AC-1` `AC-4` | `#3` |
| 9 | 隔離設定ディレクトリの内容限定と呼び出し元副作用の検証 | 呼び出し元設定ディレクトリに設定ファイル・hooks 定義・MCP サーバ定義・権限設定が存在する分類C構成で、隔離設定ディレクトリに認証要素のみが存在することを検証する。あわせて呼び出し元ホームと設定ディレクトリの内容一覧・更新時刻が起動前後で一致することを検証する | `AC-5` `AC-7` | `#3` |
| 10 | 権限と残存の検証 | 分類C構成で、実行中の隔離領域ディレクトリが 0700・配置した認証ファイルが 0600 であること、正常終了後に隔離領域が削除され複製が残らないことを検証する。あわせて認証確認不成立の構成、レビュア時間上限超過の構成、監視プロセス起動失敗の構成、取得の時間上限超過の構成それぞれで隔離領域が削除され `final` が `human_required` になることを検証する | `AC-6` `AC-12` `AC-13` | `#3` |
| 11 | 実値非露出の検証 | 取得コマンドの代替が自身の標準出力・標準エラーの接続先・自プロセスの起動列・自プロセスの環境変数を、隔離設定ディレクトリ外の記録先へ書き出すようにしたうえで、取得成功・取得失敗・取得の時間上限超過・認証確認不成立の4構成で、子プロセスの環境変数とプロセス起動列、呼び出し元が記録する標準出力・標準エラー、取得ステップと配置ステップの起動列と環境変数、アダプタの診断とログ、取得ステップの標準出力・標準エラーそれぞれの接続先と隔離設定ディレクトリの認証ファイル以外へ流れた内容を収集し、いずれにも資格情報の実値が現れないことを検証する | `AC-8` | `#4` |
| 12 | 診断内容の検証 | 3分類のいずれからも認証情報を用意できない構成、および取得が時間上限内に完了しない構成で、呼び出し元の標準エラーへ分類ごとの検出結果と設定ディレクトリの扱いを含む診断が届き、`final` が `human_required` になることを検証する | `AC-3` | `#4` |

`#1`〜`#4` は実装、`#5` は規約、`#6`〜`#12` は検証である。`#5` は他の変更単位に依存しないため任意の
時点で実施できる。`#6`〜`#12` は対応する実装単位の完了後に実施する。

## テスト適用性の判断

`.agent-skill-chain/standards/TEST_POLICY.md` が定める7カテゴリすべてについて適用要否を判断した結果は次のとおり。

| カテゴリ | 適用 | 判断根拠 |
|---|---|---|
| ユーザー操作・画面フロー | 非適用 | 画面・対話フローを持たない |
| API・サービス境界 | 適用 | アダプタと外部資格情報ストアの境界が追加されるため、取得ステップの成功・失敗・無応答の各契約を `#6` `#10` `#11` `#12` で検証する |
| 認証・認可・秘密情報 | 適用 | 資格情報の取得・配置・隔離を扱うため、権限境界を `#8` `#9` `#10`、秘密情報の非露出を `#11` で検証する |
| 性能ホットパス・SLO変更 | 非適用 | 実行頻度はゲートレビュア起動時に限られ、性能目標を変更しない |
| DB migration | 非適用 | 永続データ構造を持たない |
| デプロイ・監視・運用変更 | 非適用 | デプロイ・監視の構成を変更しない |
| 外部連携 | 適用 | 外部資格情報ストアとの連携を追加するため、障害系（取得失敗・無応答・形式不適合）を `#10` `#11` `#12` で検証する |

常時必須のカテゴリ（lint / format、型検査、単体テスト、変更範囲の結合テスト、静的アプリケーション
セキュリティテスト、依存関係・秘密情報スキャン）は無条件に実行する。

## 完了条件

- `#1`〜`#5` の実装・規約追加が完了している。
- `#6`〜`#12` の検証が追加され、リポジトリの自動テストが成功する。
- SPEC.md の `AC-1`〜`AC-9`・`AC-11`〜`AC-13` が上表のいずれかの変更単位で検証されている。
- `AC-10` は独立検証セグメントで macOS 実機の人間の実行者が実施し、`VALIDATION.md` へ実行者・実行日時・
  模倣前提の識別子ごとの結果を記録する。

## 実装順序の見直しについて

実装中に変更単位の並びのみを見直す場合は本書のみを更新する。設計要素・責務・境界そのものを変更する
場合は DESIGN.md の更新と設計ゲートの再通過が必要になる。

## 対象外

- codex アダプタ、セグメント作業ワーカー起動経路、ゲート判定ロジック、Coordination Backend 連携の変更。
- 設定スキーマ・状態スキーマ・テンプレートの変更。本 Issue は実行時の環境変数による上書きのみを新設し、
  設定ファイルの項目を追加しない。
- macOS Keychain 以外の外部資格情報ストアへの対応。
