# PLAN: gh の --slurp 依存を除去し、古い gh の環境でゲートの取得経路が黙って劣化しないようにする

- Issue: `ISSUE-774`
- 対応する DESIGN: `DESIGN.md`

## 目的・対象範囲

`DESIGN.md` が定めた設計要素（`gh-json`、3 つの gh 呼び出し側、ラウンド履歴診断、gh スタブ）を、どの順序で・どの単位に分割して実装し、各単位をどう検証するかを確定する。設計要素・責務・境界そのものは本ファイルでは変更しない。

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | `gh-json` モジュール新設 | gh 標準出力を出力形 (i)(ii)(iii) から要素列へ変換する純関数群と専用の解釈失敗例外を追加する。単一文書として解釈できる入力はその結果を用い、できない場合のみ文字列リテラルとエスケープを認識する走査で文書境界を求める。JSON 文書が 1 つも無い入力は解釈失敗とする。既定値へのフォールバックを持たない | `AC-2`, `AC-3`, `AC-4` | なし |
| 2 | gh スタブの拡張 | 一覧応答の出力形（単一文書・連結文書・ページ配列）を切り替える状態と、`--slurp` を含む起動を `unknown flag: --slurp` を標準エラー出力へ出して非 0 終了で拒否するモードを追加する。既存スタブが `--slurp` の有無で応答形を変えている箇所も同じ機構へ寄せる | `AC-5`, `AC-8`, `AC-9` | なし |
| 3 | 配列応答経路の置換 | 承認済み baseline 解決・証跡検証・ラウンド履歴取得・Issue events 取得の配列応答から `--slurp` を除去し、各所の個別解釈を `gh-json` の配列応答用抽出へ置換する。解釈失敗の写像は経路ごとに保つ（停止 / 理由付きの取得不能 / 検証不成立） | `AC-1`, `AC-2`, `AC-4` | `#1`, `#2` |
| 4 | オブジェクト応答経路の置換 | Check Run 一覧・Actions run 一覧の取得から `--slurp` を除去し、個別解釈を `gh-json` のオブジェクト応答用抽出へ置換する。出力形 (iii) のオブジェクト応答も同一経路で受理する | `AC-1`, `AC-3`, `AC-4` | `#1`, `#2` |
| 5 | ラウンド履歴診断の追加 | `gate reviewer-prompt` が取得不能を検出した場合に、その旨と理由を 1 行の診断として標準エラー出力へ書き出す。標準出力の判定プロンプト本文と終了コードには一切触れない | `AC-6`, `AC-7` | `#3` |
| 6 | 残存確認と回帰 | `--slurp` の残存が無いことを実測し、既存テスト一式と golden fixture 検査を通す | `AC-1`, `AC-7`, `AC-8`, `AC-9` | `#1`〜`#5` |

## 変更単位ごとの検証

- `#1`: 出力形 (i)(ii)(iii) の配列応答・オブジェクト応答それぞれから同一の要素集合が得られること。空文字列・空白のみ・閉じていない断片・JSON でない文字列が解釈失敗になること。`[]`・`{"check_runs":[]}`・`[[],[]]` が要素 0 件として成立すること。文字列リテラル内に `{`・`}`・`[`・`]` を含む連結文書（レビュー本文を模したもの）が正しく分割されること。
- `#2`: スタブが `--slurp` を含む起動を非 0 終了で拒否し、`--paginate` のみの起動には正常応答を返すこと。スタブが一時ディレクトリの実行可能ファイルとテスト専用の状態ファイルのみを使い、実行環境の gh 実体・バージョン・設定ファイル・恒久的な PATH 設定を書き換えないこと（`AC-9`）。
- `#3`: `--slurp` を拒否する gh の下でラウンド履歴が構築され、ラウンド番号が導出されること。取得できなかった旨と過去ラウンドが無い旨が判定プロンプトへ展開されないこと（`AC-5`）。3 ページに分かれる配列応答が欠落・重複なく取得され、出力形 (ii)(iii) を与えても要素集合が同一であること（`AC-2`）。終了コード 0 で JSON として解釈できない出力を返した場合に、停止する経路は停止し、ラウンド履歴経路は理由付きの取得不能となること（`AC-4`）。
- `#4`: 複数ページのオブジェクト応答が出力形 (ii) で与えられたとき、全ページの要素が連結されること。先頭ページのみの採用や解釈失敗による空集合にならないこと（`AC-3`）。同じ経路へ出力形 (iii) を与えた場合も要素集合が同一であること（`AC-8`）。
- `#5`: 取得失敗時に診断行が標準エラー出力へ現れ、終了コードが 0 のままであり、標準出力の判定プロンプト本文に当該診断行が含まれないこと（`AC-6`）。
- `#6`: 変更後のリポジトリで `grep -rc -- '--slurp' src/` を実行し、一致件数が 0 であることを実測して報告する（着手前の実測値は `src/commands/gate.ts` 10、`src/lib/gate-round.ts` 1、`src/lib/review-light.ts` 1 の計 12）。あわせて `git diff` で `--paginate` を与えていた取得経路が引き続き `--paginate` を与えていることを確認する（`AC-1`）。判定プロンプトの固定出力（golden fixture）に差分が無いことを確認する（`AC-7`）。既存の単体・統合テスト一式が成功すること。

## 実装上の制約

- 判定プロンプト本文の文字列を変更しない。証跡に記録済みの prompt digest と一致しなくなり、既存 PR の証跡検証が不成立へ倒れるためである。
- 解釈失敗時に空集合・既定値へフォールバックする分岐を新設しない。既存の失敗写像（停止 / 理由付きの取得不能 / 検証不成立）はいずれも維持する。
- 実行環境にインストールされた gh を差し替えない。検証はテスト専用のスタブと既存の注入口だけで行う。

## 実装順序の見直しについて

実装中に上記の変更単位の並びのみを見直す場合は、本ファイルのみを更新すればよい。設計要素・責務・境界そのものを変更する場合は `DESIGN.md` の更新と設計ゲートの再通過が必要になる。

## 対象外

- 実装そのもの（本セグメントの成果物は計画までとする）。
- `test/fixtures/gate-reviewer-prompt-golden.txt` を含む固定出力の実在確認と証跡化。これは独立検証セグメントで行う。
- `.agent-skill-chain/scripts/` および adapters 配下の gh 呼び出しの変更。

## 未決事項

- 無し。
