<!--
正本: AGENTS.md §4セグメント・4ゲート
このファイルは Issue 毎に複製して使う雛形である（セグメント: design、成果物: PLAN.md。DESIGN.md とは別ファイル）。
設計（何を・なぜ・どの構造にするか）と実装計画（どの順序で・どの変更単位で実装するか）は責務が異なる。
実装途中で作業順序だけを見直す場合、DESIGN.md 自体を変更する必要はない。
-->

# PLAN: 診断: Issue #208マージ後にrelease/root-cleanupワークフローが起動しない原因調査

- Issue: `ISSUE-211`
- 対応する DESIGN: `DESIGN.md`

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | `doctor.sh` へのコメント追加 | `.agent-skill-chain/scripts/doctor.sh` のヘッダコメント（環境診断の説明行の直後）に、`本スクリプトは読み取り専用の診断であり、リポジトリの状態を変更しない。` の1行を追加する。それ以外の行（`set -euo pipefail` 以降の実行ロジック）は一切変更しない | `AC-1` | なし |
| 2 | 構文確認 | `bash -n .agent-skill-chain/scripts/doctor.sh` を実行し、コメント追加後もシェル構文が正しいことを確認する。あわせて `doctor.sh` の実行結果（`doctor -h` 相当の出力）が変更前後で同一であることを目視確認する | `AC-1` | `#1` |
| 3 | 差分スコープ確認 | `git diff` を確認し、変更ファイルが `.agent-skill-chain/scripts/doctor.sh` の1件のみであること、`.github/workflows/` 配下・`src/`・`SPEC.md` に変更が及んでいないことを確認する | `AC-1` | `#1, #2` |
| 4 | commit・push | 変更単位1〜3の完了後、実装セグメントとして commit・push する。これにより既存の Draft PR（Issue #211）のheadブランチへ差分が反映される | `AC-1` | `#1, #2, #3` |

## 本 PLAN の対象外（後続セグメントの責務）

- PR を `gh pr merge --admin --squash` でマージする作業（AC-2 実地観測の前提条件）。
- マージ後のワークフロー起動有無の観測（AC-2）・原因切り分けの結論記録（AC-3）。これらは検証セグメントが VALIDATION.md に検証手順・実行者・証跡を記載したうえで実施する。

## 実装順序の見直しについて

実装中に作業順序（上記の変更単位の並び）のみを見直す場合は、本ファイルのみを更新すればよい。設計要素・責務・境界そのものを変更する場合は、DESIGN.md の更新（および設計ゲートの再通過）が必要になる点に注意する。
