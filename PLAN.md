# PLAN: size:quick の Issue でゲートが構造上通過不能な問題の是正

- Issue: `ISSUE-733`
- 対応する DESIGN: `DESIGN.md`

## 目的・対象範囲

本計画は、ゲート判定機構へ quick 免除の成否を第一級の入力として導入し、判定プロンプトを判定対象区間と判定軸区間へ分離したうえで、判定軸区間の値と起動経路の中断有無を決定する変更を、どの順序で・どの単位に分割して実装し、何をもって完了と判定するかを定める。設計要素そのものの定義（責務・境界・判断の根拠）は本計画の対象外であり、実装順序と検証手順のみを扱う。実装途中で順序だけを見直す場合は本ファイルのみを更新する。

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | quick 免除の解決 | size シグナル・risk シグナル・ガードレール判定を三値で解決し quick 免除の成否を導くモジュールを新設する。取得元は GitHub モードが Issue ラベル、ローカルモードが Issue 状態ファイル、ガードレール判定は base SHA から target SHA までの変更差分とする。値が 0 件なら既定値、値域外・同一区分の複数指定・取得失敗は解決不能とする。base SHA 未指定と差分取得失敗は判定不能とする。ガードレール対象パスの定義は既存の共有定義をそのまま用いる | `AC-1`, `AC-2`, `AC-3` | なし |
| 2 | 必須成果物検査の免除判定の呼び出し先変更 | 既存の必須成果物検査が用いる免除判定を、`#1` が持つ取得元アクセス（ラベル取得・Issue 状態ファイル読み取り）を呼び出す形へ寄せ、自身の二値へ畳み込む。観測挙動は一切変更しない | なし（回帰防止） | `#1` |
| 3 | 必須成果物の読み取りと AC-ID 抽出の三値化 | 対象ゲートの必須成果物集合を定義し（spec は `SPEC.md`、design は `DESIGN.md` と `PLAN.md`、implementation は空、validation は `VALIDATION.md`）、成果物ごとの読み取り結果を「取得できた」「不在を確認できた」「読み取り不能」の三値で返すモジュールを新設する。不在は target SHA のツリー列挙が成功しかつ該当エントリ 0 件であることをもって肯定的に確認する。列挙失敗・blob 以外・内容取得失敗は読み取り不能とする。集合全体の値と、`SPEC.md` の読み取り結果から導く AC-ID 抽出結果の三値も同モジュールで導く | `AC-19`, `AC-20`, `AC-21` | なし |
| 4 | 判定規則の純関数化 | 判定軸区間の値、必須成果物ごとの提示種別、起動経路の中断有無を決める関数を、外部入出力を持たないモジュールとして新設する。`DESIGN.md` の判定軸決定表と提示種別表をそのまま実装する。提示種別は必須成果物ごとに決めるため、不在と読み取り不能が混在する入力でも成果物ごとに一意に定まる | `AC-4`, `AC-5`, `AC-6`, `AC-7`, `AC-8`, `AC-9`, `AC-10`, `AC-11`, `AC-12`, `AC-13`, `AC-19`, `AC-20`, `AC-21`, `AC-22`, `AC-23` | なし |
| 5 | 代替判定基準の取得と抽出 | trusted base 側の起動経路から Issue 本文（GitHub モード）または Issue 状態ファイルの要求内容（ローカルモード）を取得するモジュールを新設する。取得した本文から成果物全文の一方向転記区間を除去する。除去範囲は開始マーカーの最初の出現から終了マーカーの最後の出現までとし、開始のみ・終了のみの片側欠落もそれぞれ本文末尾まで・本文先頭からとして扱う。除去後に残ったマーカーと同一の行も捨てる。除去後の本文を先頭から 1 度走査して囲み区間（fenced code block・HTML コメント。終了記号が現れない場合は本文末尾まで、先に開始した種別を優先）を特定し、その内部の行を見出し行として扱わずに ATX 見出し行で節へ分割する。正規化（前後空白・強調記号・末尾コロンの除去、英字の小文字化、全角英数字と全角空白の半角化）した見出しテキストが要求記述節ラベルへ完全一致する節だけを採用する。採用した節の内容を空行区切りのブロックへ分割して、空白のみ・HTML コメントのみ・水平線のみ・記入省略表現のみのブロックを捨てる。記入省略表現への一致判定は、前後の空白除去 → 行頭の Markdown リスト記号（`-`・`*`・`+` のいずれか 1 文字、または 1 個以上の半角数字に `.` もしくは `)` が続く並び。いずれも直後に 1 個以上の空白が続く場合のみ）の除去 → 英字の小文字化 → 末尾の句読点除去の順に正規化した 1 行に対して行い、リスト記号の除去は行頭に残る限り繰り返す。残ったブロックを全採用節にわたり原文順で連結し、残ったブロックが 1 件以上あるときだけ展開可能とする。残ったブロックが 0 件・要求記述節が 1 件も無い場合・取得失敗、および規則を一意に適用できないその他の入力はすべて展開不能とする。文字数・分量その他の量的な判定も、記述の意味を推定する判定も置かない。要求記述節ラベルと記入省略表現はいずれも実装のコード上の固定値とし、設定ファイル・環境変数・成果物のいずれからも与えない。記入省略表現の列挙は現行の要素から増やさない。除去・節の選別・抽出は入出力を持たない純関数として分離する。ワーカー供給経路（ブランチ上のファイル・PR 本文・Issue コメント・本文の写し）は一切読まない | `AC-5`, `AC-6`, `AC-17` | なし |
| 6 | 判定プロンプトの区間分離 | 判定プロンプト生成へ、判定対象区間と判定軸区間の開始・終了を表す一意な標識行 4 本を導入する。判定プロンプトへ展開する可変内容のすべて（成果物本文・変更差分・Issue 本文に加え、過去ラウンドの判定記録として再掲する finding の識別子・根拠要約とラウンド計数時の診断文言）について、同一の標識文字列を無害な表記へ置換する。無害化対象を由来ごとの列挙で管理せず、trusted base 側が組み立てる固定文言以外のすべてを対象とする。既存の「適用対象の AC-ID」節と conformance ルーブリック節を判定軸区間へ統合し、判定軸の値に応じて内容を切り替える。差分・判定対象成果物・上流の承認済み成果物を判定対象区間で囲む。反証ルーブリック・最終判定の扱い・出力 JSON 契約・ハルシネーション防止の指示は区間の外へ残す | `AC-4`, `AC-5`, `AC-6`, `AC-7`, `AC-8`, `AC-18`, `AC-19` | `#1`, `#3`, `#4`, `#5` |
| 7 | 判定対象区間の成果物提示 | 必須成果物を成果物ごとの読み取り結果と免除の成否から 4 種類の提示（内容展開／免除による正当な不在／本来存在すべき欠落／内容を取得できなかった旨）へ振り分ける。同一ゲートの必須成果物のうち存在し内容を取得できたものはその内容を展開し、一部が不在または読み取り不能であることを理由に省略しない。必須成果物以外の判定対象成果物は target SHA に存在するものだけを提示する。上流の承認済み成果物は取得できた場合のみ展開し、不在なら当該提示を出力せず、読み取り不能なら内容を取得できなかった旨を明示する | `AC-9`, `AC-10`, `AC-11`, `AC-20`, `AC-22`, `AC-23` | `#3`, `#4`, `#6` |
| 8 | 中断判定の配線（単一解決） | 判定プロンプト生成コマンドを、入力解決を 1 回だけ行い、その解決結果へ中断判定を適用し、中断時は判定プロンプトを出力せず理由を標準エラーへ書いて非 0 で終了する形へ変更する。中断しない場合は同じ解決結果を判定プロンプトの組み立てへ渡す。組み立て関数自体には中断規則を持たせない。中断判定用の独立したサブコマンドは新設せず、レビュア起動シェルは無変更とする（既存のフェイルセーフが非 0 終了を人間判断へ倒す） | `AC-12`, `AC-13`, `AC-21` | `#1`, `#3`, `#4`, `#5` |
| 9 | 不在許容の拡張 | 承認対象成果物の収集で不在を許容する条件を、「必須成果物を持たないゲートである」から「必須成果物を持たないゲートである、または quick 免除が成立している」へ拡張する。証跡投稿の段と検証済みゲート状態の組み立ての 2 箇所に適用する | `AC-12`, `AC-14` | `#1` |
| 10 | ゲート状態の再照合の不在標識対応 | 承認済み成果物の再照合で、記録済み digest が不在標識である成果物については不在を許容し、実在するようになっていれば digest 不一致として無効化する。免除の成否は再解決しない | なし（付随変更・回帰防止） | `#3` |
| 11 | 判定値の集約と要求体数の充足検査 | 1 回のゲートレビュー起動単位（attempt）の最終判定を導く純関数を新設する。入力は `DESIGN.md` の判断 D14 が定める 3 つとする——(i) 要求体数（`確定`（1 以上の整数）または `未解決`）、(ii) 起動済み slot の集合（起動に成功した slot のみ。空集合を含む）、(iii) 起動済み slot の集合を定義域とし、各 slot へ `確定`（判定値の組）または `未確定` を与える全域写像。実際の起動体数は (ii) の要素数として求め、(iii) のうち `確定` である件数からは求めない。要求体数と起動済み slot の集合はいずれも引数として受け取り、判定値の件数から推定しない。導出順序は判断 D14 の表をそのまま実装する。要求体数が `未解決` である場合、起動済み slot の集合が空である場合、その要素数が要求体数に満たない場合、および 1 件以上の判定結果が `未確定` である場合は、いずれも返された側の判定値の内容によらず `human_required` を導出し、その結果をゲート状態へ書き出す（非 0 終了だけで終わらせない）。証跡の検証経路とゲート状態への結線経路の双方をこの関数の呼び出しへ寄せる。結線経路は attempt 単位で判定値をまとめてから集約し、判定値を受け取れなかった起動済み slot へ `未確定` を与えて入力から落とさない | `AC-14`, `AC-15`, `AC-16`, `AC-24` | `#1` |
| 12 | 自動テストの追加・更新 | 下記「必須テストケース」の全項目に対応する単体テスト・結合テストを追加する。判定プロンプトの内容を突き合わせる既存テストと固定出力の比較用データを新しい区間構成へ更新する。既存の判定プロンプト決定性テスト・証跡テスト・アダプタテストのうち影響を受けるものを更新する | `AC-1`〜`AC-24` | `#1`〜`#11` |

## 検証手順と観測単位

観測単位は、レビュアの実行結果ではなく**当該関数・当該コマンドを直接呼び出した結果**とする。レビュアの起動には外部の実行系と認証が要り、その可用性に完了条件を依存させると、実装が正しくても完了条件が観測不能になるためである。具体的には次の 4 種類の直接実行を用いる。

1. 判定規則の純関数を単体テストから直接呼び出す（Git も取得元も介さない）。判定軸の決定表・提示種別表・中断規則の全行をこの経路で網羅する。
2. 代替判定基準の転記区間除去・囲み区間の特定・要求記述節の選別・ブロック抽出・展開可否の判定を、文字列を入力とする純関数として単体テストから直接呼び出す。
3. 判定プロンプト生成コマンドを一時リポジトリに対して直接実行し、標準出力を区間境界で切り出して各区間の内容を確認する。取得元は取得コマンドの差し替えで制御する。中断する入力では、終了コードが非 0 であること・標準出力に判定プロンプトが出ないこと・標準エラーに中断理由が出ることを同じ実行で確認する。
4. 判定プロンプトの組み立て関数を、解決済みの値を直接与えて単体テストから呼び出す。起動経路では到達しない提示の組み合わせ（免除不成立かつ必須成果物が不在、必須成果物が読み取り不能）はこの経路で観測する。

`AC-14`・`AC-15`・`AC-16`・`AC-24`（最終判定の導出）は判定プロンプトを介さず、判断 D14 の 3 入力から最終判定を導出する経路を直接実行して観測する。観測は 2 段で行う。(a) 集約の純関数へ、要求体数・起動済み slot の集合・slot ごとの判定結果を直接与えて導出結果を確認する。起動体数が要求体数に満たない場合・判定結果が `未確定` である slot を含む場合・要求体数が `未解決` である場合も、この経路で全組み合わせを網羅する。(b) 判定値を記録する経路（証跡の検証経路とゲート状態への結線経路）を直接実行し、導出結果がゲート状態へ書き出されることを確認する。レビュアが 1 体も起動されない入力（`AC-24` の (a)）は、起動経路を中断させたうえでゲート状態の最終判定を読み出して観測する。

## 必須テストケース

次のケースは網羅の要であり、いずれも省略しない。両モード（GitHub モードのラベル由来、ローカルモードの Issue 状態ファイル由来）を入力とする必要があるケースは、両方を実施する。

### 免除の成否

| ケース | 状態 | 期待 | 対応 AC-ID |
|---|---|---|---|
| 免除成立 | size が `quick`、risk が `normal`、ガードレール非該当 | 成立。両モードで同じ結果 | `AC-1` |
| 免除不成立（全 7 通り） | 3 シグナルをすべて解決でき、上記の組み合わせに一致しない 7 通り。size 未付与により `standard` へ解決される場合と risk 未付与により `unclassified` へ解決される場合を含む | 不成立。未付与は解決不能として扱わない | `AC-2` |
| 解決不能（各事由） | ラベル取得の失敗、応答の解釈失敗、値域外の値、同一区分のラベル 2 件以上、Issue 状態ファイルの読み取り失敗、base SHA を解決できない、差分取得の失敗 | すべて不成立。解決できない値を既定値とみなさず、判定を中断もしない | `AC-3` |

### 判定軸区間

| ケース | 状態 | 期待 | 対応 AC-ID |
|---|---|---|---|
| 免除成立・成果物充足 | 免除成立、AC-ID 1 件以上、必須成果物に不在なし（すべて取得できた場合と 1 件以上が読み取り不能な場合の両方、代替判定基準の展開可否の両方） | `AC-ID 全件網羅`。代替判定基準へ切り替えず判定不能指示も含まない。4 ゲートすべて | `AC-4` |
| **免除成立・成果物不在・AC-ID あり** | design ゲートで `SPEC.md` に AC-ID があり `DESIGN.md`・`PLAN.md` が不在、validation ゲートで `VALIDATION.md` が不在。代替判定基準を展開できる | `代替判定基準`。網羅を立証すべき必須成果物が正当に不在である旨と実体が出力され、`AC-ID 全件網羅` の指示は出力されない | `AC-5` |
| 免除成立・AC-ID 0 件 | 免除成立、AC-ID 抽出に成功して 0 件（`SPEC.md` 不在を含む）。代替判定基準を展開できる。4 ゲートすべて。必須成果物が読み取り不能な入力を含む | `代替判定基準`。AC-ID が正当に存在しない旨と実体が出力される。ゲートの別によらず同一の実体 | `AC-5` |
| 免除成立・展開不能 | 上記 2 ケースと同じ入力で、(i) Issue 本文を取得できない、(ii) 本文に要求記述節ラベルへ一致する見出しが 1 件も無い（見出しの無い本文と、`担当者: 未定`・`詳細は後日追記` のような管理上の記述だけの本文をいずれも含める）、(iii) 要求記述節はあるがその内容が記入省略表現のみである、のそれぞれ | 生成は例外で中断せず成功し、判定不能指示が出力される。代替判定基準も `AC-ID 全件網羅` も出力されない。要求記述節の外に空でないブロックが本文に存在することだけを理由に展開可能とならない | `AC-6` |
| **囲み区間内の偽見出し** | 要求記述節ラベルと同一の ATX 見出しを、(i) fenced code block の内部、(ii) HTML コメントの内部、(iii) 終了記号が現れない未閉鎖の fenced code block・HTML コメントの内部、へそれぞれ置いた本文。囲み区間の外に要求記述節が無い場合と、囲み区間の外にも要求記述節がある場合の両方 | 囲み区間の内部の見出しが要求記述節として採用されない。囲み区間の外に要求記述節が無い場合は展開不能となり判定不能指示が出力される。囲み区間の外にある要求記述節の内容だけが実体として書き出される | `AC-6`, `AC-5` |
| **採用ブロックの有無** | 要求記述節の内容が、(i) 記入省略表現のみである本文（記入省略表現が Markdown の箇条書き項目として書かれた本文を必ず含める。`## 受入基準` の内容が `- TBD` の 1 行だけである本文、`* 未定`、`1. なし`、および入れ子の `- - TBD` を含める）、(ii) 空白・HTML コメント・水平線のみである本文、(iii) 記入省略表現と実質的な記述が混在する本文、(iv) 極端に短い 1 行の受入基準のみである本文（`## 受入基準` の内容が `終了コードを 0 にする。` である本文を必ず含める）、(v) 短い節と長い節が混在する本文 | (i)(ii) は採用ブロックが 0 件となり展開不能となって判定不能指示が出力される。とくに (i) の箇条書き形の記入省略表現は、リスト記号の有無にかかわらず記入省略表現として捨てられる。(iii)(iv)(v) は採用ブロックが 1 件以上となり展開可能となり、採用ブロックが原文順で書き出される。**分量を理由に展開不能とならない**——とくに (iv) は展開可能となり、短い受入基準だけを持つ Issue が判定不能指示へ倒れない。(v) では短い節の内容も落とさずに書き出す | `AC-6`, `AC-5` |
| **要求記述節の選別** | 要求記述節ラベルへ一致する節と、一致しない節（`初期自走範囲`・`リスク分類`・担当者等）が混在する本文。配布 Issue テンプレートが生成する本文を必ず含める。見出しラベルの表記揺れ（強調記号付き・末尾コロン付き・全角英数字）を含める | 判定軸区間へ書き出される実体が要求記述節の内容のみからなり、一致しない節と前文が現れない。正規化後に一致する表記揺れは採用される | `AC-5`, `AC-6` |
| **免除不成立・AC-ID あり（主経路）** | 免除不成立の全事由、必須成果物の充足状態と代替判定基準の展開可否を問わない。4 ゲートすべて | `AC-ID 全件網羅`。代替判定基準は展開されない。本変更の適用前後で判定軸区間の内容が変化しない | `AC-7` |
| 免除不成立・AC-ID 0 件 | 免除不成立、AC-ID 抽出に成功して 0 件。展開できる場合を含む | 判定不能指示。代替判定基準は展開されない | `AC-8` |
| 抽出不能 | `SPEC.md` が読み取り不能。免除の成否・必須成果物の充足・展開可否のすべての組み合わせを含み、免除成立かつ展開可能な入力を必ず含める | 判定不能指示。0 件と同一視せず代替判定基準へ切り替えない | `AC-19` |

### 判定対象区間

| ケース | 状態 | 期待 | 対応 AC-ID |
|---|---|---|---|
| 成果物充足 | 必須成果物がすべて存在し内容を取得できた。免除の成否を問わない。4 ゲートすべて | 内容が展開され、不在を示す記述を含まない。必須成果物を持たないゲートでは不在を示す記述を含まないことのみを要求 | `AC-9` |
| 免除成立・不在 | 免除成立、必須成果物 1 件以上が不在、読み取り不能なし。spec・design・validation | 各成果物の名称と、quick 免除により正当に不在である旨。無言の空欄にしない | `AC-10` |
| 免除不成立・不在 | 免除不成立、必須成果物 1 件以上が不在、読み取り不能なし。spec・design・validation | 各成果物の名称と、本来存在すべきであるのに欠落している旨。正当な不在として提示しない | `AC-11` |
| **一部だけが不在（部分充足）** | design ゲートで `DESIGN.md` が存在し内容を取得でき、`PLAN.md` が不在。読み取り不能なし。免除成立と不成立の両方 | 取得できた成果物の内容が展開され、同時に不在の成果物について免除の成否に応じた不在の旨が明示される。一部が不在であることを理由に取得できた内容を省略しない | `AC-10`, `AC-11` |
| 読み取り不能 | 必須成果物 1 件以上が読み取り不能、不在を確認できた成果物なし。免除の成否を問わない。spec・design・validation | 各成果物の名称と、内容を取得できなかった旨。正当な不在としても欠落としても提示せず、免除の成否で提示内容が変わらない | `AC-20` |
| **不在と読み取り不能の混在（免除成立）** | design ゲートで `DESIGN.md` が不在、`PLAN.md` が読み取り不能。免除成立 | 読み取り不能な成果物は内容を取得できなかった旨（不在としても欠落としても提示しない）、不在の成果物は quick 免除により正当に不在である旨。両者が成果物ごとに区別して提示され、いずれも無言の空欄にならない | `AC-22` |
| **不在と読み取り不能の混在（免除不成立）** | design ゲートで `DESIGN.md` が不在、`PLAN.md` が読み取り不能。免除不成立 | 読み取り不能な成果物は内容を取得できなかった旨、不在の成果物は本来存在すべきであるのに欠落している旨。正当な不在として提示しない | `AC-23` |
| 非必須成果物の削除 | implementation ゲートで変更差分に削除されたパスが含まれる | 当該パスは提示対象から外れ、判定対象区間に不在を示す記述が現れない | `AC-9` |
| 上流成果物の不在 | design ゲートで `SPEC.md` が不在（免除成立） | 上流の承認済み成果物の提示自体が出力されず、不在を示す記述が現れない | `AC-9` |

### 区間の非重複

| ケース | 状態 | 期待 | 対応 AC-ID |
|---|---|---|---|
| 標識の分離 | 成果物内容のうち AC-ID 以外の箇所と Issue 本文に、それぞれ相異なる標識文字列を与える。免除の成否・成果物の充足・AC-ID 抽出結果・展開可否のすべての組み合わせ。4 ゲートすべて | 両区間の開始位置と終了位置が一意に識別でき、占める位置が互いに素。成果物側の標識が判定軸区間に現れず、代替判定基準を展開する入力では Issue 本文側の標識が判定対象区間に現れない | `AC-18` |
| **区間境界の偽装** | 区間境界の標識行と同一の文字列を、(i) 成果物本文、(ii) 変更差分、(iii) Issue 本文、(iv) 過去ラウンドの判定記録として再掲される finding の根拠要約・識別子、へそれぞれ書き込む。(iv) は前ラウンドのレビュアが当該文字列を含む finding を返した状態を入力として与える | いずれの由来の可変内容についても埋め込まれた側は無害な表記へ置換され、判定プロンプト中に真正の標識以外の同一文字列が現れず、両区間の開始位置・終了位置が一意に識別できる | `AC-18` |

### 起動経路の中断

| ケース | 状態 | 期待 | 対応 AC-ID |
|---|---|---|---|
| 起動する | 読み取り不能なしかつ必須成果物がすべて存在、または読み取り不能なしかつ免除成立。4 ゲートすべて | 中断せずレビュアが起動され、判定値を伴うゲート状態が生成される | `AC-12` |
| 免除不成立・不在で中断 | 免除不成立、必須成果物 1 件以上が不在、読み取り不能なし。spec・design・validation | 読み取り時点で中断しレビュアを起動せず、`approved` を導出しない | `AC-13` |
| 読み取り不能で中断 | 必須成果物 1 件以上が読み取り不能。免除成立の入力を必ず含める。不在との混在を含める。spec・design・validation | 検知時点で中断しレビュアを起動せず、`approved` を導出しない。免除成立でも中断し、不在として扱って継続しない | `AC-21` |
| 中断した attempt の最終判定 | 上記 2 ケースが定める中断のすべて | 当該 attempt のゲート状態の最終判定が `human_required` として書き出される。未導出のまま残らない | `AC-24` |

### 判定軸の取得経路

| ケース | 状態 | 期待 | 対応 AC-ID |
|---|---|---|---|
| **ワーカー供給値の非採用** | 判定軸が代替判定基準となる入力で、ブランチ上の成果物ファイル・PR 本文・Issue コメント・本文の写しに、受入基準を緩める記述を置いた状態と置かない状態の両方。4 ゲートすべて | 判定軸区間の実体は trusted base 側が取得した Issue 本文のみに由来し、両状態で実体が一致する | `AC-17` |
| **成果物全文の転記区間の除去** | Issue 本文に、ゲート通過時の一方向転記によって成果物全文が書き込まれている状態。開始・終了マーカーが揃った場合、開始のみの場合、終了のみの場合 | 転記された成果物全文が判定軸区間に現れない。転記区間を除いた残りに要求記述節が 1 件も無ければ展開不能として扱う | `AC-17`, `AC-6` |
| **転記区間内の成果物見出し** | 転記される成果物本文が `要求`・`目的・背景` 等、要求記述節ラベルと一致する見出しを含む状態 | 当該見出しと配下の内容が要求記述節として採用されない。節の選別が転記区間除去の後に適用されることを、除去を無効化した比較実行で確認する | `AC-17` |
| **転記マーカーの偽装** | 転記される成果物本文の中に、転記の開始マーカー・終了マーカーと同一の文字列が含まれる入力。埋め込みが 1 箇所の場合と複数箇所の場合、開始側を埋め込む場合と終了側を埋め込む場合、および転記側の無害化を適用した本文と適用していない本文の両方 | 埋め込まれたマーカー以降のワーカー著述内容が判定軸区間に現れない。除去範囲は最後の終了マーカーまでとなり、除去後に残ったマーカーと同一の行も捨てられる。ワーカー著述内容を埋め込んだ場合と埋め込まない場合とで判定軸区間へ書き出される実体が一致する | `AC-17` |

### 最終判定の導出

| ケース | 状態 | 期待 | 対応 AC-ID |
|---|---|---|---|
| 実体化済み・両 pass | 判定軸が `AC-ID 全件網羅` または `代替判定基準`。要求体数以上のレビュアが起動され全員が判定値を返し、全員が両観点 pass で blocking なし。要求体数 1 体（standard・light）と 2 体（strict）の両方 | `approved`。代替判定基準の場合も到達する | `AC-14` |
| 実体化済み・否定判定 | 同上で全員分の判定値がそろっており、(i) 否定判定あり・pending なし、(ii) 否定判定ありかつ他のレビュアまたは他の観点が pending、(iii) 否定判定なしでいずれかが pending。strict では 2 体の判定値が食い違う組み合わせを含める | (i) と (ii) は `rejected`、(iii) は `human_required`。要求体数の判定がそろった attempt の内部での導出規則は本変更の前後で変化しない | `AC-15` |
| 判定不能指示 | 判定軸が判定不能指示で、全レビュアの conformance が pending。反証側が否定判定の場合と否定判定なしの場合 | 前者は `rejected`、後者は `human_required`。いずれも `approved` を導出しない | `AC-16` |
| **レビュアが 1 体も起動されない** | 起動経路が中断した attempt（免除不成立下の必須成果物欠落、免除の成否を問わない読み取り不能、不在と読み取り不能の混在のすべて）。spec・design・validation | 最終判定が `human_required` として導出され、ゲート状態へ書き出される。未導出のまま残らない | `AC-24` |
| **判定値の部分欠落** | 要求体数以上のレビュアが起動されたうえで、1 体以上がタイムアウト・異常終了・スキーマ不適合により判定値を確定できない。strict で 2 体中 1 体のみが返した場合と、起動された全部が返さなかった場合の両方。返された側が両観点 pass の場合・否定判定を含む場合・pending を含む場合のすべて。4 ゲートすべて | いずれも `human_required`。返された側に否定判定があっても `rejected` を導出しない。`approved` も導出しない | `AC-24` |
| **起動体数の不足** | 起動されたレビュアの体数が要求体数に満たず、起動された全員が判定値を返した attempt（strict が 2 体を要求するのに 1 体しか起動されず、その 1 体が両観点 pass を返した場合を必ず含める）。返された側が否定判定を含む場合・pending を含む場合も実施。4 ゲートすべて | いずれも `human_required`。起動体数が少ないことによって `approved` の条件が緩まない | `AC-24` |
| **要求体数の解決不能** | 要求体数の入力が `未解決`（attempt のレビュープロファイルを解決できない）である状態で判定値を集約する。起動済み slot と判定結果がそろっている場合も実施する | `human_required`。プロファイル未解決を既定値へ落として要求体数を 1 体とみなさない | `AC-24` |
| **起動体数と判定確定件数の区別** | 要求体数 1、起動済み slot 2 件でうち 1 件が両観点 pass・もう 1 件が `未確定` である attempt と、要求体数 1、起動済み slot 1 件でその 1 件が両観点 pass である attempt | 前者は `human_required`、後者は `approved`。両者が同一の入力へ畳み込まれず、集約関数が起動体数と判定結果が確定した件数を区別する | `AC-14`, `AC-24` |
| **起動体数が要求体数を上回る** | 要求体数 1、起動済み slot 2 件で、(i) 2 件とも `確定` かつ両観点 pass で blocking なし、(ii) 1 件が両観点 pass・もう 1 件が否定判定、の 2 通り。起動済み slot の集合と slot ごとの判定結果を、要求体数を上限とせずに表現できることを入力の構成段階で確認する | (i) は `approved`、(ii) は `rejected`。超過分の slot を集約の対象から落とさない。要求体数を超える起動済み slot 数が入力として表現できずに例外・切り捨てが起きない | `AC-14`, `AC-15`, `AC-24` |
| **要求体数の充足（母数の独立性）** | 同一の起動済み slot の集合と同一の判定結果に対して、要求体数だけを 1 体と 2 体で変えて集約する | 要求体数 1 体では `approved`、2 体では `human_required` となり、母数が実際の判定値の件数ではなくプロファイル由来の要求体数から決まる | `AC-24` |

### 回帰

| ケース | 状態 | 期待 | 対応 AC-ID |
|---|---|---|---|
| 必須成果物検査の免除判定 | 変更単位 `#2` の適用前後 | 免除の成否・解除理由の提示・検査対象の成果物集合がすべて不変 | なし（回帰防止） |
| ゲート状態の再照合 | 承認時に不在だった成果物がその後も不在の場合、実在するようになった場合、承認時に実在した成果物が変化した場合 | 順に「変化なし」「変化あり」「変化あり」。無効化は当該ゲートと全下流ゲートへ及ぶ | なし（回帰防止） |
| 判定プロンプトの決定性 | 同一入力に対する複数回・複数クローンでの生成 | 出力と digest が完全一致する（既存テストの維持。免除不成立の入力で実施する） | なし（回帰防止） |
| 中断判定と判定プロンプトの単一解決 | 判定プロンプト生成コマンドの 1 回の実行に対し、各取得元へのアクセス回数を数える | quick 免除の解決・必須成果物の読み取り・代替判定基準の取得がそれぞれ 1 回以下であり、中断判定と判定プロンプトの組み立てが同一の解決結果を受け取る | `AC-21` |
| 独立判定値の上書き防止 | ローカルモードの strict で 2 体分の判定値を結線する。先に否定判定、後に両観点 pass を結線する順序と、その逆の順序 | いずれの順序でも `rejected`。後から結線された判定値が先行する否定判定を上書きしない | `AC-15` |
| 全 AC の自動テスト | 変更適用後のリポジトリ | `AC-1` から `AC-24` のそれぞれに対応する自動テストが実行されすべて成功する | なし（完了条件の確認） |

## 適用するテスト種別の判断

| 区分 | 判断 | 根拠 |
|---|---|---|
| lint / format・型検査・単体テスト・変更範囲の結合テスト・静的セキュリティ検査・依存関係と秘密情報の走査 | 実施 | 常時必須の区分であり無条件に適用する |
| 認証・認可・秘密情報に関するセキュリティテスト、権限境界テスト | 実施 | 判定軸の実体の取得経路を trusted base 側へ限定する変更であり、自己承認経路の遮断がこの Issue の安全性の中核である。ワーカー供給経路に置いた記述が判定軸へ現れないこと、区間境界を偽装できないこと、および起動体数が要求体数に満たない attempt から `approved` が導出されないことを専用のテストで確認する |
| 契約テスト | 実施 | 判定プロンプトの区間境界の識別可能性と、判定プロンプト生成コマンドの引数・終了コード契約を、呼び出し側（レビュア起動シェル、無変更）の期待と突き合わせる。変更単位 #8 のとおり独立したサブコマンドは新設しないため、新設サブコマンドを対象とする契約テストは置かない |
| E2E・アクセシビリティ・ユーザビリティ | 不要 | 利用者操作画面・操作フローを含まない |
| 負荷・性能テスト | 不要 | 性能上の要求も想定される負荷の変化も無い。追加される外部アクセスは quick 免除の解決で最大 1 回、代替判定基準の取得で最大 1 回であり、後者は免除が成立し判定軸の切替が必要な入力に限られる |
| データ移行・ロールバック整合テスト | 不要 | 永続データの構造を変更しない。Coordination Backend の状態モデル・スキーマ・設定項目・ゲート状態の記録形式はいずれも無変更である |
| 運用・障害復旧テスト | 不要 | デプロイ手順・監視設定を変更しない |
| 外部連携テスト | 不要 | 新規の外部連携先を追加せず、既存の Issue 情報取得経路を再利用する |

## 完了判定

- 上記の必須テストケースがすべて自動テストとして存在し成功する。
- 判定軸の決定・提示種別の決定・中断有無の決定を、Git も取得元も介さずに単体テストから直接呼び出せる。
- quick 免除が成立し、`SPEC.md` から AC-ID を 1 件以上抽出でき、対象ゲートの必須成果物が免除により不在である入力で、`AC-ID 全件網羅` の指示が出力されず、代替判定基準が判定軸区間へ実体として展開される。
- 免除不成立かつ AC-ID を 1 件以上抽出できる入力に対する判定軸区間の内容が、本変更の適用前後で変化しない。
- 判定軸区間へ書き出される代替判定基準の実体が、要求記述節ラベルへ一致する見出しを持つ節の内容のみからなる。管理上の記述だけが書かれた本文と、要求記述節の見出しを持たない本文では展開不能となり判定軸が `inconclusive 指示` へ倒れる。
- fenced code block・HTML コメントの内部に置かれた要求記述節ラベルと同一の見出しが要求記述節として採用されない。未閉鎖の囲み区間についても同様である。
- 要求記述節の内容が記入省略表現の固定列挙へ完全一致するブロックのみからなる本文が展開不能となる。記入省略表現が Markdown の箇条書き項目として書かれた本文（`## 受入基準` の内容が `- TBD` の 1 行だけである本文を必ず含める）も同様に展開不能となる。逆に、記入省略表現へ一致しないブロックが 1 件以上あれば、その分量にかかわらず展開可能となる。とくに `## 受入基準` の内容が `終了コードを 0 にする。` だけである本文が展開可能となり、分量を理由に展開不能へ倒れない。
- ワーカー供給経路に受入基準を緩める記述を置いた状態と置かない状態とで、判定軸区間へ書き出される実体が一致する。
- 転記される成果物本文の中へ転記マーカーと同一の文字列を埋め込んだ状態と埋め込まない状態とで、判定軸区間へ書き出される実体が一致する。
- 過去ラウンドの判定記録として再掲される finding の根拠要約・識別子へ区間境界の標識行と同一の文字列を含めても、判定プロンプト中に真正の標識以外の同一文字列が現れず、両区間の境界が一意に識別できる。
- 判定プロンプト生成コマンドの 1 回の実行において、中断判定と判定プロンプトの組み立てが同一の解決結果を受け取り、各取得元へのアクセスが 1 回以下である。
- 集約の純関数が、要求体数・起動済み slot の集合・slot ごとの判定結果の 3 入力を受け取り、実際の起動体数を起動済み slot の集合の要素数から求める。要求体数 1 で 2 体が起動され 1 体のみが判定値を返した attempt と、要求体数 1 で 1 体が起動され判定値を返した attempt が、同一の入力へ畳み込まれない。
- 起動されたレビュアの体数が要求体数に満たない attempt から `approved` が導出されない。返された判定値がすべて両観点 pass である場合も導出されない。
- 起動されたレビュアのうち 1 体以上の判定値を確定できない attempt から `approved` も `rejected` も導出されず、`human_required` が導出される。
- レビュアが 1 体も起動されない attempt と、判定がそろわない attempt のいずれでも、最終判定がゲート状態へ書き出され、未導出のまま残らない。
- 最終判定の導出が、証跡の検証経路とゲート状態への結線経路の双方で同一の集約規則を用いており、規則を持つ実装が 1 箇所である。
- 変更後のリポジトリに対する既存の全自動テストが成功し、`verify-doc-length`・`adr-lint`・`lint-references`・`lint-vocab` を含む継続的統合の検査がすべて成功する。
