# PLAN: bugfix: gate workflowが未レビュー状態をジョブ失敗として扱い全PRのCIを恒常的に赤くしている

- Issue: `ISSUE-349`
- 対応する DESIGN: `DESIGN.md`

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | `verify.ts: gate-reportのpending/otherErrors分離とexit code 2導入` | `src/commands/verify.ts`の`gateReport()`（D1）を、リテラル`gate.final === 'pending'`であることの検出のみを`pendingErrors`へ、スキーマ検証・target_sha検証・approved_artifacts検証の違反を`otherErrors`へ分離するよう改修する。`gate.final === 'pending'`の場合にのみ`gate.conformance`/`gate.falsification`/`gate.final`各フィールドの案内メッセージを`pendingErrors`へ積む（白紙スキャフォールドは3フィールドとも同時にpending）。`gate.final`が確定値（approved/rejected/human_required）の場合は、`conformance`/`falsification`が個別に`pending`のままでもこの2フィールドを一切チェックしない（otherErrorsにもpendingErrorsにも計上しない——design-gate指摘pending-fast-path-swallows-rejectedへの是正。`conformance`/`falsification`個別のpendingを判定基準に含めると、standard profileで`falsification='fail'`＋blocking findingを提出しつつ`conformance`が`pending`のまま、という`final='rejected'`の正当なgate-reportまで誤ってpending扱いにしてしまうため）。`human_required`は`pendingErrors`に計上せず、非pending違反が無ければexit 0で通過させて通常の`gate publish`経路に乗せる（design-gate指摘human-required-collapsed-into-pending-check-runへの是正。`publish()`はリテラル`final=pending`のみを拒否し、`human_required`はexit 0で詳細情報付きaction_required Check Runを発行できることが`test/integration/gate-judgment.test.ts`の既存テストで検証済みのため、`publish()`側の変更は不要）。`otherErrors.length > 0`なら`otherErrors`（+参考情報としてpendingErrorsも併記可）を出力しexit 1。`otherErrors.length === 0 && pendingErrors.length > 0`なら`pendingErrors`をstderrへ出力しexit 2。両方0件ならexit 0。`GATE_REPORT_USAGE`の説明文を「0=スキーマ適合かつfinal確定（approved/rejected/human_required）、2=final=pending（白紙スキャフォールド、未レビューであり違反ではない）」へ更新する。`test/integration/verify.test.ts`の「pendingのまま検証する」ケース（フィクスチャは`conformance`/`falsification`/`final`が3フィールドとも`pending`の白紙スキャフォールドであり`gate.final === 'pending'`に該当する）は`status, 2`を期待する（メッセージのstderr出力先・文言は変更しないため、当該assert.match群はそのまま維持できる）。加えて`gate verify-evidence`が実運用上生成する`final: human_required`のケース（`conformance`/`falsification`とも`pending`）のテストは、非pending違反が無ければ`status, 0`（exit 0で`gate publish`経路へ進む）を、digest不一致等の非pending違反が併存すれば`status, 1`を期待するよう整備する。 | `AC-1, AC-2, AC-3` | なし |
| 1.5 | `gate.ts: reconcileのunchanged分岐がpublish()と同じfinal→conclusion導出を使うよう是正` | `src/commands/gate.ts`に`checkRunConclusionForFinal()`（`final`が`approved`なら`success`、`rejected`なら`failure`、それ以外（`human_required`・`pending`）なら`action_required`）を新設し、`publish()`の既存の同等インライン三項演算子をこの関数呼び出しへ置き換える。`reconcile()`のunchanged分岐（`approved_artifacts`のdigestが新SHAでも不変な場合）が無条件に発行していたconclusion`'success'`を、`checkRunConclusionForFinal(report.gate.final)`から導出する値へ置き換える（blocking finding: human-required-publish-clobbers-reconciled-gate）。これにより、直前に永続化された`reviews/<gate>.yaml`の`final`が`human_required`または`rejected`のまま対象ファイルが変化していないケースで、無条件`success`への誤昇格を防ぐ。`test/integration/reconcile.test.ts`（github backend）へ、承認済みではなく`human_required`のまま永続化されたgate-reportに対しunchanged commitでreconcileした場合にCheck Runのconclusionが`action_required`のまま維持される（`success`へ昇格しない）ことを検証する新規テストを追加する。さらに、`reconcile()`自身のchanged分岐が書き込むリテラル`pending`（invalidate時に`conformance`/`falsification`/`final`を`'pending'`へ書き換え、`approved_artifacts`のdigestは更新しない）がその後のunchanged分岐へ流入するケース——一度invalidateされたgateに対し、対象成果物を承認時のdigestと一致する内容へ復元したcommitで再度reconcileした場合に、conclusionが`success`へ自動復帰せず`action_required`のままであること（invalidate後の`success`復帰は再レビュー経由のみ、DESIGN.md・ADR-0019で意図した挙動変化として確定）——を検証するテストケースも追加する。 | `AC-1`（blocking finding是正、SPEC.mdの新規ACではないためAC-1の実効性維持として扱う） | `#1` |
| 2 | `agent-skill-chain-gate.yml: リテラルpending時action_required Check Run発行 + Publish Check Runステップのpendingスキップ` | `verify-and-publish`ジョブの`Verify gate report schema`ステップ（D2）へ`id: schema`を付与し、`GH_TOKEN`/`GATE_ID`(`matrix.gate`)/`HEAD_SHA`(`needs.detect-segments.outputs.target_sha`)環境変数を追加する。`set +e`で`verify-gate-report.sh`のexit codeを捕捉し、`CODE -eq 2`（リテラル`final=pending`。実運用では到達しない理論上のフォールバック）の場合のみ`Verify local-review evidence`ステップと同型の`node -e`＋`gh api -X POST repos/{owner}/{repo}/check-runs`で`name: agent-skill-chain/${GATE_ID}-gate`, `conclusion: action_required`のCheck Runを発行したうえで`echo "pending=true" >> "$GITHUB_OUTPUT"`してから`exit 0`する。`CODE -eq 0`（approved/rejected/human_required。`final=human_required`の未確定gate-reportもここに含まれ、後続の`gate publish`が詳細情報付きaction_requiredを発行する）の場合は`echo "pending=false" >> "$GITHUB_OUTPUT"`してからそのまま終了（既存挙動維持）。`CODE -eq 1`はそのまま`exit "$CODE"`（既存挙動維持、pending出力は書かずジョブが停止する）。続く`Publish Check Run`ステップ（`gate-publish.sh`経由で`src/commands/gate.ts`の`publish()`を呼ぶ）の現状無条件実行（if条件なし）を`if: steps.schema.outputs.pending != 'true'`へ変更する。`publish()`関数（リテラル`report.gate.final === 'pending'`のみを拒否するガード含む）自体は変更しない。 | `AC-1, AC-2, AC-3` | `#1` |
| 3 | `agent-skill-chain-gate.yml: detect-segmentsへdependabotスキップ分岐追加` | `detect-segments`ジョブの`Resolve immutable context`ステップ（id: context）へ`ACTOR: ${{ github.event.pull_request.user.login }}`環境変数を追加し、Issue ID抽出失敗時の既存`exit 1`分岐の前に`elif [[ "$ACTOR" == "dependabot[bot]" && "$BRANCH" == dependabot/* ]]; then echo "issue_id=" >> "$GITHUB_OUTPUT"; echo "skip_checks=true" >> "$GITHUB_OUTPUT"`を挿入する（`agent-skill-chain-ci.yml`の既存分岐と同型）。成功分岐（Issue ID抽出成功時）にも`echo "skip_checks=false" >> "$GITHUB_OUTPUT"`を追加する。`Detect started segments`ステップ（id: segments）へ`if: steps.context.outputs.skip_checks != 'true'`を追加する。ジョブ出力定義の`matrix: ${{ steps.segments.outputs.matrix }}`を`matrix: ${{ steps.segments.outputs.matrix || '[]' }}`へ変更し、ステップ未実行時でも`verify-and-publish`ジョブの`if`条件（`needs.detect-segments.outputs.matrix != '[]'`）が有効な空配列JSONとして評価されるようにする。 | `AC-4, AC-5` | なし |
| 4 | `配布テンプレートへの同期` | `.agent-skill-chain/templates/github/.github/workflows/agent-skill-chain-gate.yml`へ変更単位2・3と同一の差分を反映し、`.agent-skill-chain/scripts/verify-template-sync.sh`（CI: `agent-skill-chain-ci.yml`の`verify-template-sync`相当ステップ）をgreenに保つ。 | `AC-6` | `#2, #3` |
| 5 | `実機確認` | 変更単位1〜4がmainへマージされた後、実装時点でオープンだったIssue駆動PR（pendingゲートを持つもの、SPEC.md記載の#345等）へ対し`verify-and-publish`を再実行または新規pushで再評価し、SUCCESS化とCheck Run（action_required）発行を目視確認する。結果は`VALIDATION.md`（manual検証記録）へ記載する。 | `AC-7` | `#1, #2, #3, #4` |

## 実装順序の見直しについて

実装中に作業順序（上記の変更単位の並び）のみを見直す場合は、本ファイルのみを更新すればよい。設計要素・責務・境界そのものを変更する場合は、DESIGN.md の更新（および設計ゲートの再通過）が必要になる点に注意する。
