# PLAN: 配布CIテンプレート agent-skill-chain-root-cleanup.yml の npm ビルド前提を条件付きにする

- Issue: `ISSUE-686`
- 対応する DESIGN: `DESIGN.md`

## 実装順序・変更単位

DESIGN.md の設計要素 E1〜E6 を、次の順序・単位で実装する。変更対象は root-cleanup ワークフローの配布正本と展開先の 2 ファイル、および新規テストのみであり、CLI 本体・共有 CLI 解決実装・除去処理・root clean 検証・認証情報の運用は変更しない。

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | 判定ステップの追加と npm 手順の条件化（配布正本） | 配布正本ワークフローへ npm 前提判定ステップ（E1）を追加し、既存の `npm ci`・`npm run build` ステップへ判定結果を実行条件として付与する（E2）。`build` 判定は install 前提と `scripts.build` 妥当性の論理積とし、`scripts.build` の妥当性は「空白を除いて空でない文字列」に限定する。判定は条件式内でのみ評価し、ステップは常に終了コード 0 で終わる形にする | `AC-1, AC-3, AC-6, AC-7` | なし |
| 2 | CLI 保証ステップの追加（配布正本） | 配布正本ワークフローへ CLI 保証ステップ（E3）を、実行条件なしで、`npm run build` ステップの後・除去処理ステップの前に追加する（E4）。共有 CLI 解決実装を読み込んで公開関数を呼び、成功時は解決経路を 1 行出力し、失敗時はその非ゼロ終了コードでステップを終了する | `AC-2, AC-8, AC-9` | `#1` |
| 3 | 先頭コメントの更新（配布正本） | 追加した 2 ステップの意図（npm 前提を持たない consumer で無条件失敗しないこと、CLI 未検出が沈黙して後続へ伝播しないこと、CLI 保証を npm 手順の後に置く理由）を、ワークフロー先頭コメントへ自己完結した形で記載する。既存の実行契機・再帰トリガ防止・認証情報に関する記述は変更しない | `AC-4` | `#1, #2` |
| 4 | 展開先への複製 | 変更後の配布正本の内容を本リポジトリの `.github/workflows/` 側の同名ワークフローへそのまま複製し、2 ファイルを同一内容にする（E5） | `AC-5, AC-4` | `#1, #2, #3` |
| 5 | 判定ステップの振る舞い検証テスト | 配布正本のワークフロー定義から判定ステップの実行本文を機械的に取り出し、取り出し結果が取り出し元の記述と一致することを確認したうえで、一時ディレクトリの模擬構成上で実行する（E6）。構成は、何も持たない／lockfile のみ／全前提あり／`scripts.build` 不在および非正常値（空文字列・空白のみ・`null`・文字列以外の型）／lockfile 不在＋有効な build script／JSON として解析不能な `package.json` を網羅し、各構成での 2 出力と終了コード 0 を確認する | `AC-1, AC-3, AC-6, AC-7` | `#1` |
| 6 | CLI 保証ステップの振る舞い検証テスト | 同様に CLI 保証ステップの実行本文を取り出し・一致確認したうえで、3 経路のそれぞれを単独で用意した模擬構成（成功・解決経路に対応した出力・追加導入が起動しないこと・実行後に解決可能であること）と、3 経路が無く自動導入も成立しない模擬構成（非ゼロ終了・失敗理由の出力）で実行する。npm は `PATH` 先頭へ置く記録用の代替コマンドで模し、実ネットワークへはアクセスしない | `AC-8, AC-9` | `#2` |
| 7 | 構造検査テスト | 配布正本の定義上で、CLI 保証ステップが除去処理・root clean 検証の各ステップより前にあり、実行条件を持たず、共有 CLI 解決実装を参照していること、および `npm ci`・`npm run build` の各ステップが判定ステップの対応する出力を実行条件に持つことを確認する。併せて配布正本と展開先の内容一致を確認する | `AC-2, AC-4, AC-5` | `#2, #4` |
| 8 | 全体検査の実行 | 本リポジトリのテストスイート全体を実行して成功を確認し、テンプレート同期検査・用語検査・参照検査・ADR 検査を含む機械検査を通す。実行結果は独立検証セグメントの証跡として残す | `AC-4, AC-5` | `#5, #6, #7` |

## 実装順序の見直しについて

実装中に作業順序（上記の変更単位の並び）のみを見直す場合は、本ファイルのみを更新すればよい。設計要素・責務・境界そのもの（判定入力の定義、`build` 条件への install 条件の合成、CLI 保証ステップの配置とその根拠、共有 CLI 解決実装への一元化）を変更する場合は、DESIGN.md の更新および設計ゲートの再通過が必要になる。
