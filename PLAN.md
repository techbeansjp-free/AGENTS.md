# PLAN: 判定プロンプトへ gate_id 別の固定入力集合を展開し、成果物の事実主張を検証可能にする

- Issue: `ISSUE-751`
- 対応する DESIGN: `DESIGN.md`

## 目的・対象範囲

DESIGN.md が定めた設計要素を、どの順序で・どの単位に分割して実装し、どの検証で受入条件へ結線するかを定める。実装対象は判定プロンプトの入力導出モジュール、生成関数、設定層、およびそれらのテストである。

## 前提

- 変更点は判定プロンプトの生成に閉じており、レビュアの権限・隔離条件・証跡スキーマは変更しない。
- 判定プロンプトの生成経路は `buildReviewerPrompt` 1 つであり、`gate reviewer-prompt` の標準出力とレビュー証跡照合の期待値は同一の実体から得られる。この性質は本 Issue でも保つ。
- 既存の golden fixture（`test/fixtures/gate-reviewer-prompt-golden.txt`）はプロンプト全文と一致比較される。区間が増える本 Issue では再生成が必要になる。

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | `設定層の上限受理` | `review.prompt_max_input_bytes` を optional として config スキーマ・型・既定値解決へ追加する。未設定時は既定値 600000 を適用する。値の妥当性検査は既存の round_limit と同じ経路で行う | `AC-5` | なし |
| 2 | `バイト列読み出し` | blob をバイト列のまま読み出す実行ラッパーを追加する。バイト長計数と非テキスト判定の基礎になる | `AC-4` | なし |
| 3 | `固定表と上流解決` | gate_id から展開対象列・抽出元列を返す固定表と、上流成果物パスの解決（当該 Issue の ADR は base から target の差分から導出）を実装する。上流が無い・不在・導出不能の各表現を返す | `AC-1`, `AC-8` | `#2` |
| 4 | `根拠ファイルの導出` | target SHA のツリーのパス列を走査順とし、抽出元本文への部分文字列一致で根拠ファイルを 1 段導出する。展開順序リスト上で既出のパスを除く | `AC-2`, `AC-3` | `#3` |
| 5 | `上限検査と予算配分` | 必須入力の実測合計を求め、上限超過なら日本語メッセージで生成を失敗させる。上限以下なら予算を求め、根拠ファイルを走査順に分類する（展開・非テキスト省略・予算超過省略） | `AC-4`, `AC-5` | `#1`, `#4` |
| 6 | `区間の組み立て` | 上流成果物区間を gate_id 別の解決結果で構築し、憲法文書・両一覧・根拠ファイルの各区間を追加する。ハルシネーション防止区間を両一覧と整合する内容へ書き換える | `AC-1`, `AC-6`, `AC-7` | `#5` |
| 7 | `既存区間と経路の保全` | 既存区間が削除・改変されていないことを確認し、golden fixture を再生成する。生成経路が 1 つのままであることを確認する | `AC-9` | `#6` |

## 検証計画

### 常時必須（テストポリシーの分類に従う）

lint / format、型検査、単体テスト、変更範囲の結合テスト、SAST、依存関係・secret スキャンを実行する。本 Issue はユーザー操作・API 境界・認証・性能ホットパス・DB migration・デプロイ・外部連携のいずれにも該当しないため、変更種別ごとの追加検証は適用しない（判断は省略せず、非該当として記録する）。

### 受入条件ごとの検証

| AC-ID | 検証方法 | 検証内容 |
|---|---|---|
| `AC-1` | `automated` | 固定表の各列に対応する入力が実在する fixture を 4 つの gate_id で生成し、当該行の全入力が見出し付きの全文で現れること、および和集合の外にあるファイルが現れないことを確認する |
| `AC-2` | `automated` | 実在パスと非実在パスを混在させ、`AGENTS.md`・根拠ファイル・差分だけが名指しするパスを別に置いた fixture で、展開が抽出元由来かつ実在のものに限られること、同一ファイルが 2 回展開されないことを確認する |
| `AC-3` | `automated` | 同一引数で 2 回生成し、その間に作業ツリーへ未コミット変更を加えても生成物がバイト列一致することを確認する（既存の決定性テストの範囲を新区間へ広げる） |
| `AC-4` | `automated` | 予算を超える根拠ファイルを持つ fixture で、省略一覧にパス・バイト長・digest・理由が現れ、いずれの根拠ファイルも部分展開されないことを確認する |
| `AC-5` | `automated` | 同 fixture で、展開対象列 3 列の全入力が全文で展開され省略一覧に現れないことを確認する。加えて必須入力だけで上限を超える fixture で生成が非 0 終了し、切り捨てが起きないことを確認する |
| `AC-6` | `automated` | 展開ファイルと省略ファイルが同時に存在する fixture で、両一覧と適用値が現れ、抽出された根拠ファイルが各々ちょうど 1 回どちらか一方に現れることを確認する |
| `AC-7` | `automated` | ハルシネーション防止区間が両一覧を判別手段として参照し、展開済みを検証不能として扱う記述を含まないことを確認する |
| `AC-8` | `automated` | 上流成果物が欠ける fixture と base SHA を与えない呼び出しで、終了コード 0 かつ不在・導出不能が本文に明示されることを確認する |
| `AC-9` | `automated` | 既存区間が全て残ることを確認し、`gate reviewer-prompt` の標準出力と証跡照合が用いる期待値が一致することを確認する |

### 非テキスト経路の検証（受入条件の Given に無いが要件 6 が規定する経路）

NUL バイトを含む blob を根拠ファイルとして名指しする fixture を用意し、次を確認する。この経路は受入条件の Given には現れないが、要件 6 が省略理由の 2 系統として規定するため検証計画へ含める。

- 当該ファイルが本文へ展開されず、省略一覧に理由「非テキスト」で現れる。
- 当該ファイルが予算を消費せず、後続の根拠ファイルの展開結果が非テキストファイルの有無に影響されない。
- 省略一覧の digest とバイト長が blob の実体と一致する。

### 回帰の確認

- 既存の判定プロンプト関連テスト（決定性、golden 比較、証跡照合、アダプタ起動）が通ること。
- golden fixture の再生成が、区間の追加と上流成果物区間の変更のみに由来し、既存区間の文言変更を含まないこと。

## 実装順序の見直しについて

実装中に作業順序のみを見直す場合は本ファイルのみを更新する。設計要素・責務・境界そのものを変更する場合は DESIGN.md の更新と設計ゲートの再通過が必要になる。

## 未決事項

- 上限の既定値の妥当性は運用観測後に見直す余地がある。見直しは設定値の変更で完結し、本 PLAN の変更単位には影響しない。

## 対象外

- 判定プロンプトの入力スナップショットからの再生成と照合機構。
- 過去に記録済みのレビュー証跡の再検証、consumer リポジトリの導入版更新手順。
- レビュアへのツール呼び出し許可、成果物内引用の機械的検証。
