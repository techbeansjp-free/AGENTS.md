# PLAN: worktree削除の「未pushのcommit」判定がsquash merge済みブランチを誤ってブロックする

- Issue: `ISSUE-692`
- 対応する DESIGN: `DESIGN.md`

## 目的・前提

worktree 削除コマンド（`agent-skill-chain cleanup <issue_id>` と、その薄いラッパーである `.agent-skill-chain/scripts/cleanup.sh`）の削除前検査のうち「未pushのcommitが無い」条件について、「保全判定の主根拠を到達可能性に置き、内容一致は記録済み push 位置がある場合の一方向の補助とし、確定できない場合は必ず削除を拒否する」という設計を、どの順序で・どの変更単位に分割して実装し、各単位を何で検証するかを定める。

前提として、本 Issue の実装は変更単位1〜10 について完了済みである。本書はそれらについては着手前の見積りではなく、実際に採った実施順序・検証手段・引き受けたリスクを記述する。設計見直しで追加確定した変更単位11〜13 は未実施であり、本書がその実施計画を定める。実装順序は最終的な依存関係として整理したものであり、実際の作業では同じクラスの欠陥（保全されていない commit を保全済みと誤判定する経路）が複数の変種で見つかるたびに判定の土台自体を見直したため、単位2〜4は複数回にわたって作り直されている。その経緯自体は Git 履歴と Issue の記録に残る。

## 用語

- 統合先ブランチ（base）: Issue ブランチの変更が最終的に取り込まれる既定ブランチ。
- ブランチ固有 commit: 統合先ブランチとの分岐点から Issue ブランチ先端までの commit 列。判定の単位はこの1件ずつである。
- 保全済み: ブランチ固有 commit の変更内容が、実 remote 上のいずれかの ref から到達可能であるか、統合先ブランチへ取り込まれている状態。
- ローカル限定 commit: 保全済みでない commit。worktree を削除すると復元できない。
- 記録済み push 位置: 実 remote 上のいずれかの ref から到達可能であることが確認済み、または確認済みであったと Git 上の証跡で裏付けられる commit SHA（live remote head、完了済み PR の head SHA、検証 ref で裏付けた Integration Record の `head_sha`）。
- 判定不能: 保全済みか否かを確定できない状態。安全側として削除を拒否する。

## 入力・出力

- 入力: 下記に要約する `SPEC.md` の要件1〜10 と AC-1〜AC-10、および `DESIGN.md` が確定した判定の3値（未push無し／確定的な未保全／判定不能）・保全根拠の採否条件・安全側の定義。
- 出力: 判定ロジックとその利用側の変更、Integration Record スキーマへの任意フィールド追加、各 AC に対応する自動テスト。検証の実施結果は `VALIDATION.md` が保持する。

### 実装が満たすべき要件（要約）

1. 「未pushのcommitがある」拒否は、ローカル限定 commit が存在する場合にのみ発生させる。SHA 一致・祖先関係の不成立自体を拒否根拠にしない。
2. squash merge で内容が統合先へ取り込まれている場合は保全済みとする。統合先が分岐後に前進し、ブランチ先端の tree と一致する commit が統合先に存在しない場合も同様とする。
3. merge commit 方式・rebase merge 方式でマージされている場合も保全済みとする。
4. 判定結果は upstream 追跡 ref の状態（未設定・gone・統合先を指す・remote の実体と乖離）によって変わらない。
5. ローカル限定 commit が1つでもあれば削除を拒否し、拒否理由と当該 commit を特定できる情報（SHA または件数）を日本語で出力する。PR がマージ済みであることだけを根拠に検査を省略しない。
6. 保全済みかを確定できない場合は削除を拒否し、何が確定できなかったかを日本語で示す。不確定を保全済みとして扱わない。
7. 判定処理はブランチ・統合先・remote 上の ref・作業ツリーを変更しない（読み取りと情報取得のみ）。
8. 他3条件（writer lease・未commitの変更・PR または Integration Record 完了）の意味と挙動、および削除が worktree 削除操作と prune を経由する点は変更しない。
9. 未push判定を共有する他用途（期限切れ writer lease の回収可否判定、lease 再取得時の残作業判定）で、ローカル限定 commit が残る worktree を安全でないものとして扱う性質を維持する。
10. 判定は Git の事実だけで成立させる。Coordination Backend 由来の情報は補助に留め、GitHub モードでもローカルモードでも squash merge 済み worktree で誤検知を起こさない。

### 受入条件（要約）

| AC-ID | 満たすべき挙動 |
|---|---|
| `AC-1` | 全 commit が push 済みで squash 統合され、統合先が前進し、remote ブランチ ref と remote-tracking ref が削除済みの構成で、削除が成功する |
| `AC-2` | squash merge 後に作られ、remote のどの ref からも到達できず統合先にも無い commit が残る場合、非0終了で拒否し SHA または件数を日本語で出力する |
| `AC-3` | 一度も push されず統合先にも無く、マージ済み PR も無い commit が残る場合、非0終了で拒否し日本語理由を出力する |
| `AC-4` | ブランチ先端が統合先の祖先になる形（merge commit 方式・fast-forward）で統合済みの場合、削除が成功する |
| `AC-5` | rebase merge 方式で別 SHA として統合され、ブランチ先端が統合先の祖先でない場合でも、削除が成功する |
| `AC-6` | upstream 追跡 ref が統合先ブランチを指す構成でも、squash 統合済みなら削除が成功する |
| `AC-7` | remote の当該ブランチ ref へ push 済みで未マージ、PR または Integration Record が closed の構成で、削除が成功する |
| `AC-8` | 統合先を特定できない等で保全済みかを確定できない場合、非0終了で拒否し事由を日本語で出力する |
| `AC-9` | 有効な writer lease あり・未commitの変更あり・PR または Integration Record 未完了の各構成で、既存の日本語拒否理由のまま非0終了する |
| `AC-10` | 回収可否判定と残作業判定の2用途で、ローカル限定 commit が残る worktree は「未保全側」として扱われ作業消失が起きない。保全済み worktree の扱いも定義された挙動に一致する |

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | 判定APIの3値化 | 未push判定の戻り値を「未push無し／確定的な未保全（commit SHA 列を伴う）／判定不能（事由を伴う）」の3値へ変更する。理由を必要としない既存利用側のために boolean へ畳み込む互換ラッパーを残す | `AC-2`, `AC-3`, `AC-8` | なし |
| 2 | 保全根拠の収集 | 実 remote への問い合わせで得た head SHA を、ローカルの remote-tracking ref の有無・鮮度を前提にせず根拠として採用する。remote-tracking ref は実 remote の値と一致する場合のみ到達元へ採用する。upstream 追跡設定そのものは根拠にしない | `AC-1`, `AC-4`, `AC-6`, `AC-7` | `#1` |
| 3 | commit 単位の到達可能性判定 | 判定単位をブランチ全体から、分岐点からブランチ先端までの commit 1件ずつへ変更する。各 commit が記録済み push 位置または到達元 ref から到達可能かを検査し、到達不能なものを列挙する | `AC-1`, `AC-2`, `AC-3`, `AC-4`, `AC-5` | `#1`, `#2` |
| 4 | 内容一致による補助判定 | 記録済み push 位置より後に作られ到達可能性で保全を立証できない commit のために、当該 commit が触れた path の内容と統合先の内容の一致を一方向の補助根拠として用いる。適用は記録済み push 位置が1つ以上ある場合に限る。commit 由来のファイル名が Git の pathspec magic として解釈される経路を残さない | `AC-2`, `AC-3` | `#3` |
| 5 | 統合完了位置の記録 | Integration Record スキーマへ `head_sha`（任意）を追加し、local backend の統合完了遷移コマンドを新設する。遷移時にブランチ先端の到達可能性を確認してから `head_sha` と検証 ref へ記録し、確認できない場合は記録せず日本語で理由を返す | `AC-1`, `AC-5` | `#3` |
| 6 | 既知の保全位置の供給 | 削除コマンドが、完了済み PR の head SHA（GitHub モード）または Integration Record の `head_sha` と検証 ref（local backend）を判定へ供給する。供給された位置は無検証で信用せず、到達可能性または検証 ref による裏付けを経て採用する | `AC-1`, `AC-5`, `AC-7` | `#4`, `#5` |
| 7 | 検査順序と拒否メッセージ | 削除コマンドの未push検査を統合完了検査より前へ移す。確定的な未保全では件数と短縮 SHA を、判定不能では事由を、それぞれ日本語で出力する。他3条件の判定基準と削除経路は変更しない | `AC-2`, `AC-3`, `AC-8`, `AC-9` | `#6` |
| 8 | 判定を共有する他用途の確認 | 期限切れ lease の回収可否判定と lease 再取得時の残作業判定が、既知の保全位置を渡さない呼び出しのまま安全側（未保全側）へ倒れることを確認し、その扱いを定義された挙動として固定する | `AC-10` | `#1` |
| 9 | 受入・回帰テスト | 全 AC に対する自動テストと、過去に検出した偽陰性の反例に対する反証テストを追加する。削除の可否はコマンドの終了コードと worktree の残存／削除を一体で検証する | `AC-1`〜`AC-10` | `#7`, `#8` |
| 10 | 設計・実装計画成果物 | quick 免除が解除された状態に合わせ、実際に採った設計判断を記述する `DESIGN.md`・本 `PLAN.md`・保全根拠の採否規則を記録する proposed ADR を作成する | なし（成果物要求への対応） | `#9` |
| 11 | reflog 由来 push 位置の不採用 | remote-tracking ref の reflog にある `update by push` エントリを記録済み push 位置として採用する経路を削除する。live remote head の祖先であるエントリは根拠を追加せず、祖先でないエントリは force-push で捨てられた到達不能位置であるため、採用は要件1が禁じる誤った保全成立を生む | `AC-1`, `AC-2` | `#3` |
| 12 | `head_sha` 欠落の非拒否化と復旧手順の是正 | `head_sha` を持たない完了済み Integration Record を独立した削除拒否理由として扱う経路を削除し、根拠を1つ供給しないだけの状態に戻す。拒否が必要な場合の案内は、ブランチの再 push、または `head_sha` の記録と検証 ref の設定という実際に完了できる手順へ改める | `AC-1`, `AC-4`, `AC-9` | `#6`, `#7` |
| 13 | 全 live remote head の object 取得 | live remote head の object がローカルに無い場合、Issue ブランチの head に限らず取得を試みる。取得できた head は到達元集合へ加え、取得できない head が残る場合は未保全と確定させず事由付きの判定不能とする | `AC-1`, `AC-8` | `#2` |

## 各変更単位の検証手段

| # | 検証手段 | 判定基準 |
|---|---|---|
| 1 | 判定関数の単体テスト | 3値それぞれが期待どおり返り、互換ラッパーが同じ入力で整合する boolean を返す |
| 2 | 実 remote を持つ一時リポジトリを組み立てる結合テスト | remote-tracking ref を削除・改変した構成でも判定結果が変わらない。upstream 追跡先を統合先へ向けた構成で誤検知しない |
| 3 | 到達可能性の受入テストと反証テスト | squash merge・merge commit・rebase merge の3方式で、統合前のブランチ先端を記録済み push 位置として与えた場合に削除が成功する。同一 path を複数 commit で更新して squash 統合した構成でも、全 commit が記録済み push 位置の祖先であるため削除が成功する。ローカル限定 commit が残る構成では削除が拒否される |
| 4 | 反証テスト（内容一致の適用範囲） | 記録済み push 位置より後の commit について、pathspec magic として解釈されるファイル名を追加した構成、空白のみを変更した構成、統合済み path を変更して直後に取り消した構成のいずれでも削除が拒否される。記録済み push 位置が皆無の構成では、内容が一致していても削除が拒否される |
| 5 | 統合完了遷移の結合テスト | 到達可能な先端のみ `head_sha` と検証 ref へ記録される。到達不能・判定不能では記録せず状態を変更しない |
| 6 | 未 push の位置を記録した Record を用いる反証テスト | 未 push の位置が記録されていても、その自己参照だけを根拠に削除が許可されない |
| 7 | 削除コマンドの終了コードと出力の検証 | 未保全 commit がある場合は統合完了状況にかかわらず SHA を含む理由が出る。他3条件の既存の拒否理由が回帰していない |
| 8 | 回収・再取得の2用途に対する結合テスト | ローカル限定 commit が残る構成では作業消失が起きない。squash merge 済みで保全済みの構成での扱いが決定的であり、かつ削除・書き換えを伴わない |
| 9 | 全件テストと継続的検査 | 追加した受入・反証テストを含めて全件が成功し、PR の継続的検査が成功する |
| 10 | 成果物の存在検査と ADR 検査 | quick 免除が解除された状態で設計セグメントの必須成果物の欠落が解消され、追加した ADR がライフサイクル検査を通る |
| 11 | 反証テスト（force-push 後の reflog） | remote ブランチを force-push で巻き戻し、旧 push 位置が live remote head から到達不能かつ統合先にも未取込みで reflog にのみ残る構成で、削除が拒否される |
| 12 | 受入テスト（`head_sha` 欠落 Record） | `head_sha` を持たない完了済み Record を持ちつつ、ブランチが remote へ push 済みであるか統合先の祖先になっている構成で削除が成功する。Git だけでは保全を立証できない構成では拒否され、拒否理由に再 push または検証 ref 設定の手順が含まれる。`head_sha` 欠落そのものを拒否理由として期待している既存の統合テスト（`test/integration/issue-lifecycle.test.ts`）は、この新しい期待へ置き換える |
| 13 | 結合テスト（他ブランチ head の object 取得） | Issue ブランチ以外の live remote head の object がローカルに無い構成で、取得に成功すれば当該 head を到達元として判定が成立し、取得に失敗すれば `unpreserved_commits` ではなく事由付きの判定不能となる |

## リスクと対処

| リスク | 影響 | 対処 |
|---|---|---|
| 保全されていない commit を保全済みと誤判定する（偽陰性） | 復元不能な作業消失。本 Issue で最も避けるべき失敗 | 主根拠を到達可能性に限定し、内容一致は記録済み push 位置が確認できる場合のみの一方向の補助とする。確定できない場合は必ず拒否する。検出済みの反例をすべて反証テストとして固定し、以後の変更で復活しないことを全件テストで確認する |
| 偽陰性の修正が振れすぎ、保全済みの worktree の削除を拒否する（偽陽性） | 本 Issue の目的（削除の誤検知の解消）が達成されない | 各修正のたびに、squash merge・merge commit・rebase merge の3方式で削除が成功することを維持する既存テストを合わせて実行する |
| 実 remote への問い合わせが失敗する環境がある | 判定不能となり削除できない | 作業消失を伴わない側の失敗であることを設計上の受容事項とし、事由を日本語で出力して利用者が状況を把握できるようにする |
| Integration Record スキーマの変更が quick 免除の解除条件に該当する | 通常フローの成果物が追加で必要になる | 変更を任意フィールドの追加のみに留めて後方互換を保ち、解除された成果物要求には変更単位10で対応する |
| 判定関数を共有する他2用途へ変更が波及する | 回収・再取得の挙動が意図せず変わる | 互換ラッパーを維持して呼び出し側の変更を不要にし、両用途の扱いを結合テストで固定する |
| commit 由来の文字列を Git のコマンド引数として扱う | 引数が特殊構文として解釈され、比較対象が空になって未保全 commit を見逃す | 文字列を文字どおりの path として扱う指定を付け、該当するファイル名を用いた反証テストで固定する |
| 変更単位11〜13 が未実施のまま検証セグメントへ進む | 設計と実装が乖離し、AC-1・AC-8 の充足が成果物上で立証できない | 3単位をいずれも小さな独立変更として切り出し、それぞれに専用の反証・受入テストを対応付けて完了条件へ含める |

## 制約

- 判定はブランチ・ref・作業ツリーを書き換えない。統合完了記録が作成する検証 ref と、判定に必要な object の取得を除き、Git の状態を変更しない。
- 削除コマンドの出力契約（成功時は削除した worktree パスを標準出力へ、失敗時は非0終了コードと日本語理由を標準エラー出力へ）を維持する。
- 削除の拒否理由は削除前検査の4条件に対応するものだけとし、判定根拠の不足を独立した拒否条件として追加しない。
- 判定を無効化・強制迂回するオプションを追加しない。
- 各変更単位は、追加した反証テストを含む全件テストが成功する状態で確定させる。
- 本 Issue の4成果物（`SPEC.md`・`DESIGN.md`・`PLAN.md`・`VALIDATION.md`）はリポジトリルート直下に置いたまま移動しない。配置はセグメントマニフェスト（`.agent-skill-chain/config/segments.yaml` の `outputs`）が裸ファイル名として定義しており、移動はセグメント自体の変更にあたる破壊的変更になる。マージ準備完了状態の PR でルート直下の4成果物を検出する継続的検査は、この配置を前提に「マージ前に必ず失敗する」設計として意図的に置かれたものであり、マージ操作に連鎖する後片付けが同一操作内で成果物を除去する。したがって当該検査の失敗は本計画で解消すべき欠陥ではない（由来: ADR-0007・ADR-0046）。

## 完了条件

- 変更単位1〜13 がすべて実装され、対応する検証手段が成功していること。
- AC-1〜AC-10 のすべてが自動テストで検証され、結果が `VALIDATION.md` へ記録されていること。
- 全件テストが成功し、PR の継続的検査が成功していること。

## 実装順序の見直しについて

実装中に作業順序（上記の変更単位の並び）のみを見直す場合は、本ファイルのみを更新すればよい。設計要素・責務・境界そのものを変更する場合は、`DESIGN.md` の更新（および設計ゲートの再通過）が必要になる点に注意する。

本 Issue では、判定の土台（何を保全の根拠とするか）を複数回にわたって変更した。これは作業順序の見直しではなく設計要素の変更であるため、そのつど `DESIGN.md` 側の判断として確定させるべきものであった。実装が先行し設計成果物が後追いになった経緯を踏まえ、最終的な判断内容は `DESIGN.md` に、そこへ至る実施順序と検証手段は本書に記述して整合させている。変更単位11〜13 は逆に設計が先行しており、`DESIGN.md` の判断を実装へ反映する順序で進める。

## 対象外

- 他3条件（writer lease・未commitの変更・PR または Integration Record 完了）の判定基準そのものの変更。
- worktree 削除後のローカルブランチ削除や remote ブランチ削除といった削除範囲の拡張。
- マージ方式の変更、PR マージコマンドの挙動変更、マージ後の統合先ブランチ同期処理の変更。
- 検証 ref による保全根拠を remote push 済みの水準まで強化すること（別 Issue #740 として分離）。根拠自体は本 Issue 内で成立しており、AC の充足に不足は無い。
