# PLAN: agent-skill-chainをClaude Codeスキルとして提供する実現可能性調査

- Issue: `ISSUE-428`
- 対応する DESIGN: `DESIGN.md`

## 実装順序・変更単位

DESIGN.md で定義した設計要素（ADR内セクション）を、実際に本Issueのcommit（`2d6ab8e1`）内で行った順序どおりに記述する。本Issueは調査のみでコード変更を伴わないため、「変更単位」はADRの節を書き上げた順序を指す。

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | Context・用語の確定 | 現行構成（`init`/`setup github`/`enforce on`/CLI）と問題提起を記述し、「スキル」「常時規範」「強制」の3用語を自己完結的に定義する | 調査対象の境界確定 | なし |
| 2 | 調査1（一次情報収集） | Claude Code公式文書5件・Agent Skills標準・OpenAI Codex文書・Anthropicプラグイン集・第三者OSS(Superpowers)の計9件を実際に参照し、進行的開示・CLAUDE.mdの常時ロード・強制との違い・hookのスコープ・ツール制御・スクリプト同梱・配布経路・分量制約・ベンダー中立性・既存事例の10項目(a)〜(j)を事実として整理する | 調査1、完了条件（一次情報根拠） | `#1` |
| 3 | 調査2（現行構成要素の対応表） | AGENTS.md本体・PreToolUse hook・GitHub ruleset・CLI本体・scriptsの5要素それぞれについて、調査1の事実を根拠にスキルへの収まり方を判定する | 調査2 | `#2` |
| 4 | 調査3（トレードオフの3層分解） | 強制層・規範層・手順層に分解し、層ごとにオンデマンド化の可否と代償を整理する | 調査3 | `#2, #3` |
| 5 | 調査4（利用者類型の分類） | 常時開発型・間欠開発型・非開発型の3類型を定義し、それぞれに適合するモデルを対応付ける | 調査4 | `#3` |
| 6 | 調査5（代替案比較）・Decision確定 | (a)完全スキル化・(b)並行提供・(c)ハイブリッド化・(d)見送りの4案を比較し、(b)+(c)の合成を推奨として確定する。未決着事項4件を「引き続き調査・議論が必要な事項」として明示的に区分する | 完了条件（推奨案の明示） | `#3, #4, #5` |
| 7 | Consequences記述 | 推奨採用時の利点・欠点・後続必要作業（`.claude/`書き込みの扱い、成果物自己完結性との整合等）を列挙する | Decision節の裏付け | `#6` |
| 8 | design segment検出によるDESIGN.md/PLAN.md追加 | `docs/adr/*`を含む差分により`detect-changed-segments.sh`が`design`セグメントを検出し`verify-artifacts.sh`がDESIGN.md/PLAN.mdを要求するため、実施済みの調査内容を事後的にDESIGN.md/PLAN.mdへ記述して追加する | I1追跡可能性 | `#1〜#7` |

<!-- 変更単位を追加する場合は # を連番で追加する -->

## 実装順序の見直しについて

実装中に作業順序（上記の変更単位の並び）のみを見直す場合は、本ファイルのみを更新すればよい。設計要素・責務・境界そのものを変更する場合は、DESIGN.md の更新（および設計ゲートの再通過）が必要になる点に注意する。

なお本Issueでは、design-gate通過後にverify-artifacts CI（PR差分に`docs/adr/*`が含まれることで`design`セグメントが検出され、`DESIGN.md`・`PLAN.md`の存在が必須成果物として要求される検査）の失敗を受け、本ファイルおよび`DESIGN.md`をADR完成（`#1`〜`#7`）後に追加で作成した（Issue #354・Issue #425のPLAN.mdと同種の事後transcription）。また本Issueの成果物ADRは、既存の`docs/adr/`に`ADR-0022`が本Issue起票時点で他Issue（#425）と番号衝突していたため、`ADR-0023`へ採番し直したうえでpushする。
