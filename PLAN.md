# PLAN: pr merge が base branch の最新性を保証せず、--admin 常用運用が strict_required_status_checks_policy を事実上バイパスする

- Issue: `ISSUE-493`
- 対応する DESIGN: `DESIGN.md`

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | config スキーマ・型定義への `merge.auto_update_branch` 追加 | `.agent-skill-chain/schemas/config.schema.yaml` の `merge` オブジェクトへ任意項目 `auto_update_branch: boolean` を追加（`required` は `autonomous` のみのまま維持し後方互換を保つ）。`src/lib/config.ts` の `AgentSkillChainConfig['merge']` 型・`.agent-skill-chain/config/agent-skill-chain.yaml` の日本語コメントを更新する | AC-1（既定挙動の根拠） | なし |
| 2 | migration定義の明記（config schema変更に伴う手順⑤） | DESIGN.mdの`config`コンポーネント節に明記したmigration定義（本フィールドは既存YAMLに存在しなくても`false`相当の既定値として扱われる後方互換な任意フィールドであり、既存設定ファイルへの自動書き換え・変換処理を伴うmigrationスクリプトは不要であること、`schema_version`〔`agent-skill-chain/config/v1`〕は加算のみのスキーマ変更のため据え置くこと）どおりに実装する。migrationスクリプトの新規作成は行わない（不要である旨がmigration定義そのものであるため）。AGENTS.md「設定」節が定める設定項目追加手順⑤に対応する | AC-1（既定挙動の根拠） | #1 |
| 3 | `src/lib/pr-freshness.ts` 新設（対象PR解決・最新性チェック） | `resolveMergeTarget()`（`args` から対象識別子を抽出し、見つからない場合は `gh pr view --json number` によるcwdベースの暗黙解決へフォールバック。戻り値は `{ target: string \| undefined; repoOverride: string \| undefined }` とし、`repoOverride`〔`-R/--repo` の値〕は対象識別子が直接見つかった場合・フォールバックに進んだ場合のいずれでも同一走査内で検出・返却する）・`checkFreshness(root, target, repo?, options?)`（`repo` 指定時は `gh pr view <target> --repo <repo> --json ...mergeStateStatus,baseRefOid`、未指定時は従来どおり `--repo` 無しで呼ぶ、`options.allowUnknownBackoff`〔既定true〕による`UNKNOWN`ポーリングの有無切り替え、`gh pr view` 失敗検知、取得できた`baseRefOid`を`FreshnessResult.baseSha`として返却）を実装する | AC-1, AC-3, AC-4, AC-5 | #1 |
| 4 | `src/lib/pr-freshness.ts` へ最新化ロジック追加 | `attemptUpdateBranch(root, prNumber, repo?)`（`repo` 指定時は `gh api -X PUT repos/<repo>/pulls/{n}/update-branch`、未指定時は従来どおり `gh api -X PUT repos/:owner/:repo/pulls/{n}/update-branch`〔`gh` CLI標準プレースホルダによるcwd暗黙解決〕を呼ぶ。ポーリングは受け取った同じ `repo` を引き継いで `checkFreshness(root, target, repo, { allowUnknownBackoff: false })` を `UPDATE_BRANCH_POLL_INTERVAL_MS`（3秒）間隔・`UPDATE_BRANCH_POLL_MAX_ATTEMPTS`（10回、合計最大30秒）上限でポーリングする完了確認）を実装する。戻り値 `UpdateResult`（`{ status: 'updated' | 'failed' | 'not_applicable'; baseSha?: string }`）は、ポーリングが`fresh`へ到達した時点の`checkFreshness()`呼び出し結果（`FreshnessResult`）が保持する`baseSha`をそのまま`UpdateResult.baseSha`へ引き継ぐ（`status: 'failed'`・`status: 'not_applicable'`の場合、および`FreshnessResult`に`baseSha`が無い場合は付与しない）。ポーリング内の各`checkFreshness()`呼び出しはバックオフを行わない単発問い合わせに限定し（`allowUnknownBackoff: false`）、合計待機時間が30秒を超過しないようにする。`BEHIND`/`check_failed`（1回の問い合わせで`UNKNOWN`のままだった場合を含む）はいずれも「未反映」として同様にポーリング継続対象とする。ポーリング中に`checkFreshness()`が`not_applicable`（対象PRがポーリング中に`OPEN`でなくなった）を返した場合は「未反映」とみなさず、残りのポーリング回数を消費せず直ちに`status: 'not_applicable'`を返す | AC-2 | #3 |
| 5 | `src/lib/pr-freshness.ts` へ失敗分類器追加 | `MergeFailureClassifier.classifyMergeFailure(stderr)` を実装する。既知の「明らかに無関係」パターン（権限不足・既にマージ済み・既にクローズ済み等を示す `gh` の標準エラー文言）のみを許可 list 化し、それ以外は `ambiguous` を返す | AC-6, AC-7 | なし |
| 6 | `src/commands/pr.ts` の `merge()` 統合 | `merge.autonomous` 確認の後・`gh(['pr','merge',...args])` 呼び出しの前に `resolveMergeTarget()` を呼び、戻り値 `{ target, repoOverride }` から `repoOverride` をローカル変数として保持したうえで、以降に呼ぶ `checkFreshness()`・`attemptUpdateBranch()` の**全て**の呼び出しへ対象識別子の解決経路（直接抽出／フォールバック）によらず一貫して渡す（`repoOverride` が `undefined` の場合はいずれの呼び出しも従来どおり `--repo` を付与しない）。`behind`/`check_failed`/最新化失敗時（`resolveMergeTarget()` が `target: undefined` を返す場合を含む）は日本語エラーメッセージで中断する。`attemptUpdateBranch()` が `status: 'not_applicable'` を返した場合は、他の最新化失敗（`status: 'failed'`）と同様に中断する（非0終了・`gh pr merge` は実行しない）が、メッセージは通常の最新化失敗メッセージと区別できる「対象PRが処理中にクローズ・マージされたため最新化を中断しました」という趣旨の日本語メッセージにする。`fresh` 確定直後、初回`checkFreshness()`が直接`fresh`を返した経路では`FreshnessResult.baseSha`を、`attemptUpdateBranch()`のポーリング経由で`fresh`に到達した経路では`UpdateResult.baseSha`を、それぞれ`preMergeBaseSha`としてローカル変数へ保持するのみとし、それ以外の追加I/Oを挟まず `gh(['pr','merge',...args])` を呼ぶ（窓の最小化）。`gh pr merge` 失敗時は `MergeFailureClassifier.classifyMergeFailure()` の結果に応じてメッセージを補完する。`gh pr merge` 成功時は `syncMainWorktree()` の前後で `preMergeBaseSha` があればベストエフォートで `gh pr view <target> --json baseRefOid` を1回呼び、値が異なれば標準エラー出力へ日本語警告を出す（取得不能時は無警告で正常終了、終了コードに影響しない）。`args` に `--auto` が含まれる場合でも本チェックは`gh pr merge`呼び出し時点までのみを対象とし、呼び出し後の非同期マージ実行タイミングの再チェックは行わない（DESIGN.mdの「`--auto`指定時の非同期マージとの関係」を参照。実装上の追加分岐は不要）。`MERGE_USAGE` の説明文を更新する | AC-1〜AC-7 | #3, #4, #5 |
| 7 | `test/helpers/gh-stub.ts` 拡張 | `gh pr view` の `mergeStateStatus`・`baseRefName`・`baseRefOid` フィールド返却、`state` を明示制御するテスト用フラグ、`gh api -X PUT .../update-branch` の成功/失敗を制御するスタブ状態を追加する。呼び出し回数に応じて `mergeStateStatus` を切り替えられるようにし（例: 1〜2回目は `BEHIND`、3回目以降は `CLEAN`）、`attemptUpdateBranch()` のポーリングが複数回の再問い合わせで `fresh` に到達するケースと、`UPDATE_BRANCH_POLL_MAX_ATTEMPTS` に到達しても解決しないケースの両方をテストで再現できるようにする。加えて、呼び出し回数に応じて `state` を `OPEN` から `MERGED`/`CLOSED` へ切り替えられるスタブ状態（例: 1回目は `OPEN`+`BEHIND`、2回目以降は `MERGED`）を追加し、`attemptUpdateBranch()` のポーリング中に対象PRが `OPEN` でなくなり `checkFreshness()` が `not_applicable` を返すケースを、ポーリング上限到達まで待たずに即座に打ち切られることを含めてテストで再現できるようにする。加えて、対象識別子を伴わない `gh pr view --json number` 呼び出し（`resolveMergeTarget()` のcwdベース暗黙解決フォールバック）を、成功（cwdの現在ブランチに紐づくPR番号を返す）と失敗（該当PRなしで非0終了）の両方に切り替えられるスタブ状態、および `gh pr merge` 成功後の `gh pr view --json baseRefOid` 呼び出しで返す `baseRefOid` をマージ前の値と異ならせる／同一に保つ／呼び出し自体を失敗させる、の3通りに切り替えられるスタブ状態も追加する。加えて、対象識別子を伴わない `gh pr view --json number` 呼び出し（フォールバック解決）に `--repo <値>` が付与されているかどうかをスタブ側で記録・検証できるようにし、`-R/--repo` 付き `args` でこのフォールバックが呼ばれた場合に限り `--repo` 引数が転送されていること、`-R/--repo` を含まない `args` の場合は `--repo` が付与されないことの両方をテストで区別できるようにする。加えて、対象識別子を伴う `gh pr view <target> --json ...`（`checkFreshness()` 本体の呼び出し）・`gh api -X PUT .../pulls/{n}/update-branch`（`attemptUpdateBranch()` の呼び出し）の両方についても、それぞれに渡された `--repo`／URL中のrepo部分をスタブ側で記録・検証できるようにし、対象識別子と `-R/--repo` が `args` に同時に含まれフォールバックを経由しない場合でも、`checkFreshness()`・`attemptUpdateBranch()` へ `-R` の値が渡されていることを区別してテストできるようにする | AC-1〜AC-7（テスト実行の前提） | なし |
| 8 | `test/integration/pr-merge.test.ts` へ受入テスト追加 | AC-1（behind時は既定で中断）・AC-2（update-branch API自体の失敗で中断、およびポーリング上限到達まで反映されず中断する場合の双方。複数回のポーリングを経て `fresh` になり成功する場合も含む）・AC-3（`--admin`付きでも迂回不可、`-R/--repo`等の値取り型オプションを含む引数でも対象PRを正しく解決できる）・AC-4（チェック失敗で中断。`args`に対象識別子が無く、かつ`gh pr view`によるcwdベースの暗黙解決も失敗する場合を含む）・AC-5（fresh時は既存挙動を維持。加えて`args`に対象識別子を含まない場合、`gh pr view`によるcwdベースの暗黙解決が機能し、fresh〔最新〕なPRであれば従来通りマージが成立することを検証し回帰していないことを確認する）・AC-6（明らかに無関係な失敗は既存出力を維持）・AC-7（TOCTOU疑いは日本語メッセージ付きで非0終了）の各ケースを追加する。加えてAC-IDに紐付かない設計上の確認事項として、(a) `fresh` 確定から `gh pr merge` 呼び出しまでの間にスタブへの追加呼び出しが発生しないことをモック呼び出し順序で確認するケース、(b) `gh pr merge` 成功後に事後の `baseRefOid` が変化しているスタブ設定で標準エラー出力へ日本語警告が出ることを確認するケース、(c) 事後の `gh pr view` 呼び出しが失敗するスタブ設定で警告が出ず終了コード0のまま正常終了することを確認するケース、(d) `auto_update_branch` 有効かつ`attemptUpdateBranch()`のポーリング経由で`fresh`に到達した経路（`UpdateResult.baseSha`経由）でも、(b)と同様に`preMergeBaseSha`が取得され事後検知（警告出力）が機能することを確認するケース、(e) `-R/--repo <owner>/<repo>` を含み対象識別子を含まない `args` で `merge()` を呼んだ場合、`resolveMergeTarget()` のcwdベース暗黙解決フォールバック（`gh pr view --json number`）へ `-R` の値が `--repo` として転送されることをスタブへの呼び出し引数で確認したうえで、`-R` が指すrepoのPRに対して `checkFreshness()` による最新性チェックが行われることを検証するケース、(f) `-R/--repo` を含まない `args` の場合は当該フォールバック呼び出しに `--repo` が付与されないこと（従来どおりcwdの既定repoで解決すること）を確認し回帰していないことを検証するケース、(g) `attemptUpdateBranch()` のポーリング中に `checkFreshness()` が `not_applicable`（対象PRが処理中に `OPEN` でなくなった）を返すスタブ設定で、ポーリング上限まで待たずに直ちに中断し、通常の最新化失敗メッセージとは区別できる「対象PRが処理中にクローズ・マージされたため最新化を中断しました」という趣旨の日本語メッセージで非0終了することを確認するケース、(h) `gh pr merge 123 -R owner/other-repo --admin` のように対象識別子（`123`）と `-R/--repo`（`owner/other-repo`）の両方が同一 `args` に含まれる（`resolveMergeTarget()` が対象識別子を `args` から直接抽出でき、cwdベース暗黙解決フォールバックを経由しない）場合に、`resolveMergeTarget()` が返す `repoOverride` が `checkFreshness()`（`gh pr view 123 --repo owner/other-repo --json ...`）・`auto_update_branch` 有効時は `attemptUpdateBranch()`（`gh api -X PUT repos/owner/other-repo/pulls/123/update-branch` および内部ポーリングの `checkFreshness()`）の両方へ一貫して渡され、`-R` が指す実際のマージ対象repoに対して最新性チェック・最新化が行われることをスタブへの呼び出し引数で確認するケースを追加する | AC-1〜AC-7、および設計上の確認事項(a)〜(h) | #6, #7 |

## 実装順序の見直しについて

実装中に作業順序（上記の変更単位の並び）のみを見直す場合は、本ファイルのみを更新すればよい。設計要素・責務・境界そのものを変更する場合は、DESIGN.md の更新（および設計ゲートの再通過）が必要になる点に注意する。
