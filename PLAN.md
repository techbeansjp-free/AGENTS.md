# PLAN: bugfix: resumeしたsegment workerがPR/Issueのレビューフィードバックを一切参照せず静的completion checklistだけで完了と自己判定する

- Issue: `ISSUE-446`
- 対応する DESIGN: `DESIGN.md`

## 実装順序・変更単位

| # | 変更単位 | 内容 | 対応 AC-ID | 依存する変更単位 |
|---|---|---|---|---|
| 1 | roles.yaml静的ルール追加 | `.agent-skill-chain/config/roles.yaml` の `role_contracts.{spec_worker,design_worker,implementation_worker,validation_worker}.rules` へ「作業再開時は対象Issue/PRの最新レビュー・コメント（`review_status`セクションがあれば含む）を確認し、ファイル存在やcommit済みであることのみを根拠に完了と判定しない」旨の1行を追加する。既存の `rules` 配列の他要素・`inputs`/`outputs`/`completion`/`forbidden` は変更しない。 | `AC-1` | なし |
| 2 | test/helpers/gh-stub.ts 拡張 | 既存 `pr view <branch>` 分岐（`branch名`キー）が `number,state,headRefName,latestReviews,comments` の統合`--json`フィールド指定に応答できるようにする。`GhStub` インターフェースへレビュー・コメントを注入するヘルパー（例: `seedPrReviews(branch, reviews)`）を追加する。加えて、`pr view <branch>` を非ゼロ終了させるヘルパー（例: `seedPrViewFailure(branch, { stderr })`）を追加し、stderrに`no pull requests found`を含むケース（PR未作成）と含まないケース（認証切れ等の一般障害）の両方を呼び出し側テストが指定できるようにする。既存の `issue view --json comments` は変更不要（既に対応済み）。 | `AC-2, AC-3, AC-4, AC-5`（自動テストの前提条件） | なし |
| 3 | review-status.ts 新規作成 | `src/lib/review-status.ts` を新規作成する。`detectGithubReviewStatus(root, issueNumber)` と `detectLocalBlockingFindings(root, issueNumber, segment)`、およびそれぞれのYAML整形を担う `formatReviewStatusBlock(data)` を実装する。DESIGN.md「未対応の判定基準」節のとおり、レビューは `latestReviews` の `state === 'CHANGES_REQUESTED'` のみを対象とする。コメントは時刻カットオフを行わず、`git` 呼び出しは一切行わない（`since`相当の値は持たない）。本文が `<!-- agent-skill-chain:` で始まらないものはすべて「未対応」とみなす（定型marker除外のみ、AC-4、ADR-0026）。marker prefixのハードコード重複を避けるため、`src/lib/review-evidence.ts` の `REVIEW_EVIDENCE_MARKER` を再利用し、`src/commands/report.ts` の非export定数 `MARKER`（値は同一prefixファミリー）はexportして共有するか、`review-status.ts` 側で独立に判定関数（prefix比較）を持つ場合もこれら2箇所の実測値と一致させる。投稿者（author）単位の除外は行わない——worker報告・進行役の修正依頼コメントが同一actorから投稿されるため。GitHubモードは「Issue側」（`gh issue view --json comments`、常に実行）と「PR側」（`resolveCurrentBranch` 成功時のみ `gh pr view <branch> --json number,state,headRefName,latestReviews,comments` を直接1回呼ぶ。`findOpenPrByHead()`は経由しない、2026-08-05再改定）を独立に実行し、それぞれを「成功（0件以上のデータを取得。PR側の『PR未作成』も成功扱い）」「失敗（`gh`非ゼロ終了・JSON解釈失敗・branch解決失敗）」に正規化してから合成する（DESIGN.md「Issue側／PR側の分離」合成規則、design-gate指摘・2026-08-05再改定）。PR側の「PR未作成」判定は、`gh pr view`が非ゼロ終了かつstderrが`no pull requests found`に一致する場合のみとし、それ以外の非ゼロ終了・JSON解釈失敗は失敗として扱う（DESIGN.md「検出失敗（GitHubモード）」、design-gate blocking finding `PR_RESOLUTION_FAILURE_SILENTLY_TREATED_AS_NO_PR`是正）。**両方失敗**の場合のみ `{ detection: 'failed', reason }`（両側の理由を含む）を返す。**一方が成功・他方が失敗**の場合は、成功側で実際に検出した `unresolved_reviews`/`unresolved_comments`（0件でもよい）を保持したまま `{ detection: 'succeeded', ..., partial_failures: [{ side: 'issue' \| 'pr', reason }] }`（`partial_failures`は非空・1要素）を返す——失敗側の障害を理由に成功側の検出結果を破棄しない。**両方成功**の場合は合計0件なら `undefined`、1件以上なら `mode: 'github', detection: 'succeeded'`（`partial_failures`キー自体を持たない、`pr_number` はPR解決成功時のみ含める、`pr_number?: number`）を返す。`segment.ts` 側のブロック省略判定は、`unresolved_reviews`/`unresolved_comments`が両方空でも`partial_failures`が非空なら省略しない（DESIGN.md「AC-4との関係」）。ローカルモードは `spec`/`design`/`implementation`/`validation` 全segmentのgate reportを `tryReadYamlFile()` で走査し、`origin` が起動対象segmentに対応する値（`spec`→`specification`、他は同名）と一致する `blocking` findingのみを収集する。ファイル不存在（`tryReadYamlFile()` が `undefined`）はそのsegment0件として継続し、YAML解釈失敗（例外）はsegment単位で`try/catch`する（他segmentの走査は継続）。**全segmentが失敗**した場合のみ `{ mode: 'local', detection: 'failed', reason }` を返す。**1segment以上が成功**（ファイル不存在による0件を含む）した場合は、収集済みのblocking findingを保持したまま `{ mode: 'local', detection: 'succeeded', gate: segment, unresolved_blocking_findings: [...] }` を返し、1つでも読み込みに失敗したsegmentがあれば `local_read_failures: [{ segment, reason }]`（非空）を付加する（`undefined`にはしない、GitHub側の`partial_failures`と対称、design-gate blocking finding `LOCAL_MODE_PARTIAL_FAILURE_DISCARDS_DETECTED_FINDINGS`是正、ADR-0026）。全segment成功かつfindingが0件の場合のみ`undefined`（AC-4、セクション省略）を返す。`formatReviewStatusBlock(data)` は文字列連結を行わず、既存の `src/lib/yaml-io.ts` の `toYamlString()`（`segment.ts`/`lease.ts`で既に使用されている既存export）へ構造化データをそのまま渡し、戻り値をインデントして連結する——コメント本文（第三者が自由に書ける文字列）を手書きの文字列補間で埋め込まない（DESIGN.md「埋め込むコメント本文のシリアライズ安全性」、design-gate指摘・2026-08-05）。 | `AC-2, AC-3, AC-4, AC-5, AC-6` | `#2`（テスト実行に必要） |
| 4 | segment.ts 組み込み | `src/commands/segment.ts` の `start()` に、既存の `issueBlock` 構築と同列で `review-status.ts` の結果を呼び出し、非空（または検出失敗）の場合のみ `parts` へ `review_status:` ブロックを追加する処理を組み込む。`config.coordination.backend` による分岐は既存の `issueBlock` 分岐と対称に実装する。 | `AC-1〜AC-6` の結線 | `#1, #3` |
| 5 | 単体テスト | `test/unit/review-status.test.ts`（新規）で `detectGithubReviewStatus`／`detectLocalBlockingFindings` の判定ロジック（CHANGES_REQUESTED抽出、`<!-- agent-skill-chain:`定型marker本文の除外、時刻カットオフ無しでの全非markerコメント検出、Issue側・PR側の両方が失敗した場合の`detection: 'failed'`、**Issue側成功（コメントN件、0件を含む）かつPR側失敗（branch解決失敗／`gh pr view`失敗）の場合は`detection: 'succeeded'`でIssue側の検出結果を保持しつつ`partial_failures`に`side: 'pr'`を含める**、**逆にPR側成功（レビュー・コメント有り）かつIssue側失敗の場合も同様にPR側の検出結果を保持しつつ`partial_failures`に`side: 'issue'`を含める**、**`gh pr view`が非ゼロ終了しstderrが`no pull requests found`に一致する場合はPR未作成（非失敗・0件）としてIssue側が非空なら`succeeded`かつ`partial_failures`なし**、**`gh pr view`が非ゼロ終了しstderrが上記文言に一致しない場合（認証切れ等）はPR側を失敗として扱い、Issue側成功時は`partial_failures`に`side: 'pr'`を含める**、両方成功し合計0件の場合のundefined、origin一致によるcross-segment gate report検出、ローカルgate report不存在時は0件継続、**ローカルモードで1segmentのYAML破損があっても他segmentで検出済みのblocking findingは保持したまま`local_read_failures`（非空）を付加した`succeeded`を返す**、**ローカルモードでfindingが0件でも1segmentが読み込み失敗していれば`undefined`にはせず`local_read_failures`付きの`succeeded`を返す**、**ローカルモードで`spec`/`design`/`implementation`/`validation`全segmentのYAMLが破損している場合のみ`detection: 'failed'`を返す**、PR未作成かつIssueコメントも無い場合のundefined）を直接検証する。`formatReviewStatusBlock`がコメント本文中の`rules:`や`---`等YAML構造を模した文字列を含む入力に対しても構造を破壊しない（`toYamlString`経由でエスケープされる）ことを検証するケースを追加する。`test/unit/roles.test.ts` に `role_contracts.*.rules` へのAC-1文言を検査するケースを追加する。 | `AC-1, AC-2, AC-3, AC-4, AC-5, AC-6` | `#1, #3` |
| 6 | 結合テスト | `test/integration/github-backend.test.ts` へ、`gh-stub` 経由でCHANGES_REQUESTEDレビューあり／未対応コメントあり／全てAPPROVEDかつコメント無し／`gh` 呼び出し失敗（Issue側・PR側**両方**）／**Draft PR作成前（PR未解決、stderrが`no pull requests found`）でもIssueコメントのみで検出される**／**Issue側は成功しPR側のみ`gh`呼び出しが失敗（stderrが`no pull requests found`と不一致）する場合、Issue側の未対応コメントがプロンプトへ含まれたまま`partial_failures`相当の失敗通知も同梱される（成功側の検出結果が消えないこと）**、の6パターンで `segment start` を呼び出し、出力プロンプトを検証するケースを追加する。ローカルモード（`test/integration/issue-lifecycle.test.ts` 等の既存ローカルbackendスイート）へ、`reviews/<segment>.yaml` に blocking finding がある場合／無い場合、他segmentのgate reportにorigin一致のblocking findingが記録されている場合（cross-segment差し戻し）、対象segmentのgate report自体が存在しない場合（0件継続、failedにならないこと）、**他segmentで検出済みのblocking findingがある状態で別の1segmentのYAMLが破損している場合（`succeeded`＋`local_read_failures`が同梱され、検出済みfindingが消えないこと）**、および**`spec`/`design`/`implementation`/`validation`全segmentのYAMLが破損している場合（`detection: 'failed'`）**の `segment start` 出力を検証するケースを追加する。 | `AC-2, AC-3, AC-4, AC-5, AC-6` | `#2, #4` |
| 7 | 実地再現確認 | 本Issueの独立検証セグメントで、Issue #441の再現手順と同型の状況（design-gateでblocking finding→PRへ修正依頼コメント→`worker-launch.sh` でdesign_worker再起動）を用意し、実際のworker完了報告・作業ログに当該フィードバックへの具体的言及があることを確認する。 | `AC-7` | `#4, #6` |

## 実装順序の見直しについて

実装中に作業順序（上記の変更単位の並び）のみを見直す場合は、本ファイルのみを更新すればよい。設計要素・責務・境界そのものを変更する場合は、DESIGN.md の更新（および設計ゲートの再通過）が必要になる点に注意する。
