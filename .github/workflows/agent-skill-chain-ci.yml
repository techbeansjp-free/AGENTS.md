# 正本: AGENTS.md §不変条件I1・I4・I6・I7 / §参照・コメントの陳腐化防止
#
# PRへのpushごとに、ブランチ名・worktreeパス・テンプレート同期・用語・参照規約・ADR・成果物・
# SPEC.mdのBDD記載充足・DESIGN.mdの図示要否判断記載・AC対応の各検査
# （.agent-skill-chain/ci/verify-*.sh・.agent-skill-chain/scripts/lint-*.sh・
# .agent-skill-chain/scripts/adr-lint.sh）を実行する。各スクリプトは agent-skill-chain CLI
# （src/agents-md.ts、ビルド後 bin/agents-md.js）への薄いラッパーであるため、検査本体の前に
# ビルドを行う。agent-skill-chain CLI自体の開発リポジトリ用テストスイート（npm test）は
# consumer環境では意味を持たないため、本ワークフローには含めない（Issue #290）。
# npm ci / npm run build は、それらを要する構成（package.json + lockfile、build script）を
# consumerが実際に持つ場合だけ実行する。agent-skill-chain自身の開発リポジトリはconsumerの
# 一種として両方を満たすため従来どおり実行され、package.json/lockfile/build scriptを持たない
# consumerでは無条件失敗しない（Issue #536）。npm ci/buildをスキップしても、各検査スクリプトが
# 解決に使う3経路（bin/agents-md.js・node_modules/.bin/agent-skill-chain・PATH上の
# agent-skill-chain）のいずれかを「Ensure agent-skill-chain CLI」ステップが保証するため、
# CLI未検出という別のエラーへ失敗ポイントが移動することはない（Issue #536）。
name: agent-skill-chain / ci

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  issues: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v7
        with:
          node-version: '20'

      - name: Detect npm build prerequisites
        id: npm-prereq
        run: |
          if [[ -f package.json && ( -f package-lock.json || -f npm-shrinkwrap.json ) ]]; then
            echo "ci=true" >> "$GITHUB_OUTPUT"
          else
            echo "ci=false" >> "$GITHUB_OUTPUT"
          fi
          if [[ -f package.json ]] && node -e "process.exit((require('./package.json').scripts || {}).build ? 0 : 1)" 2>/dev/null; then
            echo "build=true" >> "$GITHUB_OUTPUT"
          else
            echo "build=false" >> "$GITHUB_OUTPUT"
          fi

      - name: npm ci
        if: steps.npm-prereq.outputs.ci == 'true'
        run: npm ci

      - name: npm run build
        if: steps.npm-prereq.outputs.build == 'true'
        run: npm run build

      - name: Ensure agent-skill-chain CLI
        # Issue #677: ローカル用ラッパーと同じ共有実装を使い、CLI解決・自動導入の変更が
        # CIワークフローだけへ横展開されない状態の再発を防ぐ。
        run: |
          source .agent-skill-chain/scripts/cli-resolve.sh
          asc_resolve_cli
          if [[ ${#ASC_CLI[@]} -eq 2 && ${ASC_CLI[0]} == node ]]; then
            echo "agent-skill-chain CLI: bin/agents-md.js を使用します（追加インストール不要）"
          elif [[ ${ASC_CLI[0]} == "$PWD/node_modules/.bin/agent-skill-chain" ]]; then
            echo "agent-skill-chain CLI: node_modules/.bin/agent-skill-chain を使用します（追加インストール不要）"
          else
            echo "agent-skill-chain CLI: PATH上の agent-skill-chain を使用します"
          fi

      - name: Fetch base branch for diff-based checks
        # verify-artifacts の code 判定（git diff base...HEAD）が base を解決できるようにする。
        # actions/checkout@v7.0.1 はPRのマージrefのみをフェッチし、baseブランチ自体はローカルに
        # 存在しないため（PR #172 run 29717941752 で実落ち）、ここで明示的にフェッチする
        # （下記 verify-adr ステップと同一パターン。remote.origin.fetch の既定refspecにより
        # `origin/$BASE_REF` としてローカルへ反映される）。
        if: github.base_ref != ''
        env:
          BASE_REF: ${{ github.base_ref }}
        run: git fetch origin "$BASE_REF"

      - name: Derive issue_id
        id: ctx
        env:
          BRANCH: ${{ github.head_ref }}
          # Issue #219: 追加pushで変化するgithub.actorではなく、PRの起源（PR作成者）で判定する
          ACTOR: ${{ github.event.pull_request.user.login }}
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
        run: |
          ISSUE_ID="ISSUE-$(echo "$BRANCH" | sed -E 's#^[^/]+/([0-9]+)-.*#\1#')"
          if [[ "$ISSUE_ID" =~ ^ISSUE-[0-9]+$ ]]; then
            echo "issue_id=$ISSUE_ID" >> "$GITHUB_OUTPUT"
            echo "skip_checks=false" >> "$GITHUB_OUTPUT"
          elif [[ "$ACTOR" == "dependabot[bot]" && "$BRANCH" == dependabot/* ]]; then
            echo "issue_id=" >> "$GITHUB_OUTPUT"
            echo "skip_checks=true" >> "$GITHUB_OUTPUT"
          elif [[ "$BRANCH" =~ ^chore/root-cleanup-[0-9]{8}T[0-9]{6}Z$ ]]; then
            if ! ACTOR_PERMISSION="$(gh api "repos/$REPOSITORY/collaborators/$ACTOR/permission" --jq '.permission')"; then
              echo "root-cleanup PR作成者 '$ACTOR' のリポジトリ権限を確認できません" >&2
              exit 1
            fi
            if [[ "$ACTOR_PERMISSION" != "admin" ]]; then
              echo "root-cleanup PR作成者 '$ACTOR' はadmin権限を持たないため許可しません" >&2
              exit 1
            fi
            echo "issue_id=" >> "$GITHUB_OUTPUT"
            echo "skip_checks=true" >> "$GITHUB_OUTPUT"
          else
            echo "ブランチ名 '$BRANCH' から issue_id を抽出できません（.agent-skill-chain/config/agent-skill-chain.yaml branch.pattern に非適合、かつ Dependabot/root-cleanup許可リストにも非該当）" >&2
            exit 1
          fi

      - name: verify-branch-name
        if: steps.ctx.outputs.skip_checks != 'true'
        run: ./.agent-skill-chain/ci/verify-branch-name.sh

      - name: verify-worktree-path
        if: steps.ctx.outputs.skip_checks != 'true'
        run: ./.agent-skill-chain/ci/verify-worktree-path.sh

      - name: verify-root-clean (merge-ready)
        # ISSUE-590: マージ準備完了状態（Draftではない）のPRについて、repoRoot直下に対象4ファイル
        # （SPEC.md/DESIGN.md/PLAN.md/VALIDATION.md）が残存していないかを既存の
        # .agent-skill-chain/ci/verify-root-clean.sh（verify root-clean CLI、Issue #208で導入済み・
        # 無変更のまま再利用）で検査する。Issueブランチへ削除commitを追加できない制約上、
        # validation-gateまで正常に完了したPRでもこの検査は常に失敗する設計であり（ADR-0046）、
        # マージには `--admin` によるbypassを要求する。Draft中はroot直下に対象ファイルが存在する
        # ことが正常な中間状態であるため、draft == false のときのみ実行する。
        if: steps.ctx.outputs.skip_checks != 'true' && github.event.pull_request.draft == false
        run: ./.agent-skill-chain/ci/verify-root-clean.sh

      - name: verify-template-sync
        run: ./.agent-skill-chain/ci/verify-template-sync.sh

      - name: Detect started segments
        id: segments
        if: steps.ctx.outputs.skip_checks != 'true'
        env:
          BASE_REF: ${{ github.base_ref }}
        run: |
          detected="$(./.agent-skill-chain/scripts/detect-changed-segments.sh "$BASE_REF")"
          values="$(paste -sd, - <<< "$detected")"
          echo "values=$values" >> "$GITHUB_OUTPUT"

      - name: verify-artifacts (対象集合を一括検査)
        if: steps.ctx.outputs.skip_checks != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: ./.agent-skill-chain/ci/verify-artifacts.sh "${{ steps.ctx.outputs.issue_id }}" --started-segments "${{ steps.segments.outputs.values }}"

      - name: verify-spec-bdd
        if: steps.ctx.outputs.skip_checks != 'true' && contains(steps.segments.outputs.values, 'spec')
        run: |
          if [[ -f SPEC.md ]]; then
            ./.agent-skill-chain/ci/verify-spec-bdd.sh SPEC.md
          fi

      - name: verify-design-diagram
        if: steps.ctx.outputs.skip_checks != 'true' && contains(steps.segments.outputs.values, 'design')
        run: |
          if [[ -f DESIGN.md ]]; then
            ./.agent-skill-chain/ci/verify-design-diagram.sh DESIGN.md
          fi

      - name: verify-ac-coverage
        if: steps.ctx.outputs.skip_checks != 'true' && contains(steps.segments.outputs.values, 'validation')
        run: ./.agent-skill-chain/ci/verify-ac-coverage.sh "${{ steps.ctx.outputs.issue_id }}"

      - name: verify-adr (PRで変更されたADRごと)
        if: steps.ctx.outputs.skip_checks != 'true'
        env:
          BASE_REF: ${{ github.base_ref }}
        run: |
          git fetch origin "$BASE_REF"
          CHANGED_ADRS="$(git diff --name-only "origin/$BASE_REF"...HEAD -- 'docs/adr/*.md')"
          for adr_path in $CHANGED_ADRS; do
            ./.agent-skill-chain/ci/verify-adr.sh "$adr_path"
          done

      - name: lint-vocab
        if: steps.ctx.outputs.skip_checks != 'true'
        run: ./.agent-skill-chain/scripts/lint-vocab.sh

      - name: lint-references
        if: steps.ctx.outputs.skip_checks != 'true'
        run: ./.agent-skill-chain/scripts/lint-references.sh

      - name: Fetch base branch for secret scan
        if: github.base_ref != '' && steps.ctx.outputs.skip_checks != 'true'
        env:
          BASE_REF: ${{ github.base_ref }}
        run: git fetch origin "$BASE_REF"

      - name: lint-secrets (PRで追加された行のみ)
        if: github.base_ref != '' && steps.ctx.outputs.skip_checks != 'true'
        env:
          BASE_REF: ${{ github.base_ref }}
        run: ./.agent-skill-chain/scripts/lint-secrets.sh --diff "origin/$BASE_REF"

      - name: adr-lint
        if: steps.ctx.outputs.skip_checks != 'true'
        run: ./.agent-skill-chain/scripts/adr-lint.sh check
